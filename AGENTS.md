# 工业机械臂重构项目 · 智能体实施规则

> **作用范围**：本文件约束工业机械臂重构工作包（WP）和任务卡的产品实现、测试、验证与提交。
> 文档查询、代码问答、只读审查和方案讨论不进入本流程；但任何向仓库写入产品代码的会话，
> 只要改动属于本工业机械臂重构范围，均须遵守“编码规范”“验证与证据”和“硬性禁止”；
> 与本项目无关的 RobWork 维护任务不因本文件自动转化为 WP 实施任务。
>
> **流程定位**：本文件只规定实施过程，不定义产品语义。产品语义和实施边界来自
> `docs/industrial-robot-design/`：`DOCUMENT-BASELINE.md`（`IRD-D0-20260829`）、
> `requirements.md` v0.8、`architecture/`（`IRD-D10-20260829`，当前状态以注册表为准）、
> `module-design/`、`development-task-breakdown.md` v1.3、`work-packages/` 和 `agent-tasks/`。
>
> **原子执行单位**：一次运行只实施一张任务卡；一张任务卡对应一个隔离 worktree、
> 一个实现分支、一个实现提交和一组可独立复跑的证据。不得在同一次运行中开始第二张任务卡。

## 1. 角色、状态与权威顺序

### 1.1 角色分离

- **实施者**：只实现当前任务卡、运行验证、创建实现提交并输出完成报告；不得给自己的任务签署
  `Done`，不得解锁下一张任务卡。
- **独立验证者/治理协调者**：在独立执行上下文中复跑验证、检查范围与证据；通过后在
  `agent-tasks/task-status.md` 登记实现提交 SHA、证据、签署者和日期，并按前置关系解锁下一任务。
- **最终评审者**：独立于实施者和验证者，负责合入判断；不得用“测试通过”替代范围、接口和证据评审。

实施提交不能包含自身尚未产生的 SHA，因此状态账本更新必须由独立治理上下文在后续治理提交中完成。

### 1.2 状态账本

`docs/industrial-robot-design/agent-tasks/task-status.md` 是任务状态唯一权威：

- `Planned`：前置或入口条件未满足，不得实施；
- `Ready`：入口条件已满足，可由实施者领取；
- `Blocked`：需要外部决定或上游修复；
- `Done`：独立验证通过，且账本记录实现 SHA 和证据路径。

实施者不得自行把 `Planned` 改为 `Ready`、把当前任务改为 `Done`，或修改其他任务状态。

### 1.3 语义权威顺序

发生冲突时严格按以下顺序裁决：

```text
Accepted 需求
  → Accepted 架构契约 / ADR / Schema
  → 模块详细方案
  → 开发任务拆解总纲
  → 工作包计划
  → 任务卡
  → 实现、测试与证据
```

下游只能细化上游，不得改写上游。冲突不是由实施者“选择一个合理版本”解决；必须停止并报告
冲突文件、条款、消费者和应负责修改的权威所有者。

## 2. 目标任务选择

按以下顺序确定且只确定一张任务卡：

1. 用户指定 `WP-XX-TYY`：仅当账本状态为 `Ready` 时执行；否则报告状态和未满足条件后停止。
2. 用户只指定 `WP-XX`：在该 WP 内按工作包声明顺序选择第一张 `Ready` 卡；本轮不继续执行后续卡。
3. 用户未指定目标：按 `development-task-breakdown.md` 的阶段、关键路径和任务顺序，选择账本中
   第一张 `Ready` 卡。
4. 没有 `Ready` 卡：输出阻塞报告，不自动选择 `Planned` 卡，不自行改变账本。

阶段顺序为：A（WP-00～12）→ B（WP-13～15、WP-20 静态子集）→
C（WP-16～19、WP-12 完整报告）→ D（WP-21、WP-23 规模化）→
E（WP-22、WP-24、WP-25）。跨阶段实施必须先满足对应阶段门禁。

## 3. 第 0 步：开工门禁

开始任何修改前逐项核实 `docs/industrial-robot-design/DOCUMENT-BASELINE.md` §4，并至少记录以下结果：

1. 目标任务在状态账本中唯一存在且状态为 `Ready`；
2. 所有前置 Task/WP 均为 `Done`，账本含实现 SHA，卡内要求的工件真实存在；
3. 前置验证命令在当前代码状态仍可复跑通过；
4. 需求版本、阶段、发布切片与当前基线一致；
5. 任务引用的架构契约、ADR 和 Schema 状态满足入口要求；
6. 允许修改/禁止修改、公共接口所有者和依赖方向明确且不重叠；
7. 输入、输出、单位、状态、错误码、失败行为和所有权可唯一推导；
8. 卡内必执行命令语法完整、目标已登记，执行目录与路径明确；
9. 文档门禁通过，且不存在仍需实施者决定的跨模块语义。

命令引用的资产尚未存在时，仅以下情况不构成缺陷：当前任务明确拥有该资产，且卡内给出创建前可执行的
RED/原生命令。若资产由前置任务交付，则前置任务未 `Done`、资产缺失或不可执行都必须阻断。

### 3.1 Proposed 契约与豁免

- 产品实现引用的公共契约原则上必须为 `Accepted`。
- WP-00 等治理/自举任务可以在状态账本明确记录治理例外后执行，但不得把 `Proposed` 当作
  `Accepted`，也不得据此实现依赖该契约的产品语义。
- 其他豁免必须由用户明确指定**任务 ID、契约 ID、允许范围和有效期**；只记录为本任务豁免，
  不得批量提升契约状态。

任一门禁不满足，输出阻塞报告并停止。阻塞报告必须包含：目标任务、失败门禁、原始证据、受影响范围、
权威所有者和解除条件。不得用实现代码反向冻结设计。

## 4. 第 1 步：读取与实施备忘

按以下顺序阅读，阅读顺序不改变第 1.3 节的权威顺序：

1. `docs/industrial-robot-design/DOCUMENT-BASELINE.md`、`README.md` 和
   `agent-tasks/task-status.md`；
2. 当前 `docs/industrial-robot-design/agent-tasks/WP-XX-TYY-*.md`；
3. 任务卡引用的 `docs/industrial-robot-design/requirements.md` 条款和验收场景；
4. `docs/industrial-robot-design/architecture/contract-registry.md`、相关 ADR、契约正文、
   符号/诊断注册表和 Schema 示例；
5. 对应 `docs/industrial-robot-design/module-design/<module>.md`；
6. `docs/industrial-robot-design/development-task-breakdown.md` 和
   `work-packages/WP-XX-*.md`；
7. 任务涉及的现有代码、测试、CMake 目标和前置证据。

读完后在会话中给出“实施备忘”，不写入文档库。备忘至少包含：

- Task ID、阶段、发布切片和账本状态；
- 实现分支/worktree、基线及前置实现 SHA；
- 允许修改文件、禁止目录、唯一公共接口所有者；
- 输入输出、单位、错误码、关键状态和契约引用；
- CMake target、测试目标、逐字执行命令及其前置；
- 黄金数据版本、容差 profile、seed/threadCount（适用时）；
- 停止条件和已批准的任务级豁免。

备忘无法唯一填写时，不得编码。

## 5. 第 2 步：单任务实施

### 5.1 隔离与范围

- 默认使用从正确前置状态创建的隔离 worktree 和 `codex/` 前缀实现分支；用户明确指定现有工作区时除外。
- 开工前检查工作树；用户已有修改不得覆盖、清理、暂存或混入当前任务提交。
- 只修改任务卡允许的文件。公共接口只能由登记的所有者任务修改。
- 一次运行只产生当前任务卡的一个明确产出，不顺手实现下一任务或相邻功能。
- 需求、契约、模块方案、Schema、黄金数据和生成型 CSV 默认只读；只有当前治理任务明确拥有具体文件时
  才能修改。CSV 只能运行权威生成器产生，禁止手工编辑。
- `task-status.md` 不属于实施提交允许范围，由独立治理上下文更新。

### 5.2 RED → 最小实现 → GREEN

代码任务执行固定循环：

1. 先写任务卡规定的失败测试；
2. 运行精确测试并确认因缺少当前行为而失败，而不是环境、编译或夹具错误；
3. 编写使该测试通过的最小实现；
4. 重跑目标测试、契约测试和卡内其他必执行验证；
5. 仅在全部通过后整理证据和提交。

文档、治理、构建脚本等非产品代码任务使用任务卡规定的失败夹具或前置断言，不虚构不可能失败的
“RED 测试”。无法证明 RED 与当前产出的因果关系时，停止并报告任务卡缺口。

### 5.3 公共约束

- 公共语义只引用权威文档；不得自行发明枚举、默认值、容差、诊断码、字段或跨模块协议。
- core 不依赖 Qt Widgets/QApplication；业务模块不自建 `CollisionPolicy`、线程池、调度器、缓存、
  持久化写入口或插件间私有头依赖。
- 数值实现严格采用需求 §15.3 和相关契约规定的几何、单位、容差与退化规则，不放宽阈值。
- 黄金数据只读消费，测试运行不得回写 `testdata/`；证据写入
  `out/test-evidence/wp-xx/<run-id>/`。
- 修改验证脚本只允许其所有者任务执行，并必须保留失败夹具证明门禁不是被放宽。

## 6. 编码规范

### 6.1 文件头和接口注释

每个新建或实质修改的公共 `.hpp` 必须有块注释，逐项说明：

- `@brief` 和职责边界；
- 允许依赖、禁止依赖和公共所有者；
- 冻结语义的契约 ID、需求/方案节号和关联 Task ID；
- 涉及的物理量及 SI 单位；
- 所有权、生命周期和线程/并发约束；
- 错误返回、诊断和失败后的状态保持行为。

公共类和函数必须说明用途、每个参数及单位、返回值、错误路径、所有权和线程约束。错误返回类型使用
契约已经冻结的类型；不得为了统一形式自行引入 `expected<T, Error>` 或新的 `Diagnostic` 包装。

私有头文件和 `.cpp` 使用简短文件头，至少写职责、Task ID 和契约引用。复杂算法注释解释公式、前提和
权威来源；不逐行复述代码，不把具体容差或公式全文复制成第二真值。

### 6.2 命名与实现

- 公共术语和类型名必须与 `architecture/symbol-registry.md` 一致；禁止名不得以别名、包装或局部类型复活。
- 注释使用规范中文，代码标识符使用英文；用户可见文本经 WP-09 诊断/本地化目录管理。
- 不添加任务卡未要求的兼容层、抽象层、配置项或依赖。

## 7. 第 3 步：验证与证据

### 7.1 命令执行规则

- 从任务卡指定目录执行命令；未指定时从仓库根目录执行。
- 标为“必执行”或未标“回退”的命令必须逐字执行。
- 标为“回退”的命令只在主入口因入口脚本/环境基础设施不可用时使用；它不能替代失败的产品测试。
  使用回退时必须记录主命令、失败原因、回退命令和结果。
- 零命中静态扫描必须区分 `rg` 的“零命中”退出码与命令执行错误，不能把任意非零都视为成功。
- 不修改、跳过、缩小正则或放宽阈值来获得通过结果。

### 7.2 Windows Qt 规则

- Qt Widgets/GUI 测试在 Visual Studio x64 developer environment 下运行；
- 设置 `$env:QT_QPA_PLATFORM='windows'`，每次只用绝对路径启动一个测试可执行文件；
- 禁止使用 `QT_QPA_PLATFORM='offscreen'`，禁止合并或并行启动 Widget/Meta 测试；
- 平台插件初始化失败时，停止进程，检查继承的 `QT_*`/`QML_*` 变量后再启动；
- 仅使用 QCoreApplication 的模型测试不需要 GUI 平台插件。

### 7.3 必需验证

按任务风险执行并记录：

- Windows C++ 构建和测试默认在 Visual Studio x64 developer environment、Debug 配置运行；
  任务卡明确要求其他配置时按卡执行并记录。

1. 卡内目标测试、契约测试、失败场景和边界测试；
2. 构建目标及 `ctest`/包装脚本结果；
3. `check-boundaries.ps1`（脚本由当前/前置任务交付后）；
4. `git diff --check`、允许文件清单和禁止依赖扫描；
5. 若任务明确修改治理文档，运行双 PowerShell 文档门禁；
6. 若任务明确修改 Schema 或示例，运行 Schema 正反例验证。

每条证据至少记录：Task ID、完整命令、执行目录、实现提交 SHA（提交后补入完成报告）、配置、退出码、
测试数量与结果。涉及黄金/数值/性能数据时，另记录 datasetVersion、manifest SHA-256、seed、
threadCount、容差 profile、硬件和统计口径。

## 8. 第 4 步：实现提交、报告与停止

### 8.1 实现提交

- 所有必执行验证通过后才可提交；失败时不得创建“待修复”实现提交。
- 一张任务卡只创建一个实现提交，不混入其他任务、状态账本或用户已有修改。
- 标题：`WP-XX-TYY: 中文概括`。
- 正文空一行后使用中文分条，每条以 `- ` 开头并以“新增/修改/测试/证据/修复”等动词开头。
- 禁止 push、merge、rebase、amend 已共享提交或改写历史，除非用户对该具体操作明确授权。

### 8.2 完成报告

提交后输出并停止，报告必须包含：

1. Task ID、实现提交 SHA 和实际修改文件；
2. 每条验证命令、退出码和结果摘要；
3. 证据路径及复现所需版本、seed、线程、容差/硬件信息；
4. 未完成项、豁免、回退命令和发现的文档缺口；
5. 待独立验证和最终评审事项；
6. 自查：范围、公共头注释、接口注释、提交格式、用户修改保护均符合规则。

报告状态只能写“实现完成，待独立验证”，不得宣称任务 `Done`，不得继续下一张任务卡。

### 8.3 独立验证与账本更新

独立验证/治理上下文必须：

1. 在实现 SHA 上复跑卡内必执行命令和边界扫描；
2. 检查文件范围、公共接口所有权、证据完整性和提交格式；
3. 通过后在独立治理提交中把当前任务登记为 `Done`，填写实现 SHA、证据、签署者和日期；
4. 仅当下一任务全部前置均为 `Done` 且入口九项门禁通过时，才将其改为 `Ready`；
5. 失败时登记 `Blocked` 或保持 `Ready` 并说明返工条件，不伪造完成记录。

## 9. 硬性禁止

违反任一条立即停止并报告：

- 一轮实施两张或更多任务卡，或在当前提交中夹带相邻任务；
- 实施 `Planned`/`Blocked` 任务，自行解锁任务，或给自己的任务签署 `Done`；
- 修改任务卡允许范围之外的文件、其他 WP 公共接口或用户已有修改；
- 通过修改需求、契约、Schema、黄金数据、生成型 CSV 或验证规则让测试通过；
- 手工编辑追踪 CSV，绕过权威生成器；
- 跳过 RED、伪造失败夹具、缩小测试、把测试失败称为环境成功或美化结果；
- 在文档冲突、接口未冻结、命令损坏或前置工件缺失时自行猜测继续；
- 提交缺少规定文件头/接口注释的公共代码；
- 将实施者、独立验证者和最终评审者合并为同一执行上下文；
- 未经明确授权 push、合并发布分支、删除用户数据或改写 Git 历史。
