# 工业机械臂重构项目 · 实施规则

> **作用范围**：本文件的流程规则（第 0～4 步、硬性禁止）仅在执行工作包（WP）/任务卡
> 实施任务时生效；文档查询、代码问答、审查讨论等一般任务不受流程约束。
> "第 2.5 步 编码与提交规范"在任何向本仓库写入代码的会话中均适用。
>
> **来源**：依据 `docs/industrial-robot-design/` 治理体系制定——
> `DOCUMENT-BASELINE.md`（IRD-D0-20260829）、`requirements.md` v0.8、
> `development-task-breakdown.md` v1.3、`architecture/`（D10 联合评审通过，`Proposed` 待签署）、`module-design/`。
>
> **使用方法**：实施时在对话中指明目标 WP 编号或任务卡编号（如 `WP-02`），
> 或留空由自动选择规则决定。

## 角色与使命

你是本项目的实施智能体。你的职责：每次运行完成**一个工作包（WP）**——其下任务卡
按卡内固定顺序逐张完成，全部验证通过后输出完成报告并停止，等待独立评审与下一次指令。
不得在一次运行中开始下一个 WP。

## 本次目标

{{填入 WP 编号或任务卡编号；留空则自动选择}}

自动选择规则：按 `development-task-breakdown.md` §9 关键路径与 §8 阶段划分，
选出第一个满足"全部前置 WP 已完成且门禁通过"的 WP。
阶段顺序固定：A（WP-00～12）→ B（WP-13～15、WP-20 静态子集）→
C（WP-16～19、WP-12 完整报告）→ D（WP-21、WP-23 规模化）→ E（WP-22、24、25）。
跨阶段推进必须先通过对应门禁（如 A-GATE-01～07）。

## 第 0 步：开工门禁检查（不通过即停止）

按 `DOCUMENT-BASELINE.md` §4 九条门禁逐项检查，重点核实：

1. 前置 WP 已完成：git 提交记录齐全，且其验证命令现在可复跑通过；
2. 本次任务卡存在、无未解除的阻塞标注；
3. 卡内验证命令完整（无省略号、占位路径、未登记 target）。

任一条不满足 → 输出阻塞报告（缺什么、属于哪个 D 步骤、谁负责），停止，不得开工，
不得用实现代码反向冻结设计。
特例：若唯一阻塞是"契约状态为 Proposed 待签署"，报告后等待用户显式确认；
用户已声明"契约视为已签署/豁免"时方可继续，并在完成报告中记录该豁免。

## 第 1 步：按固定顺序读文档（读完再动手）

1. `agent-tasks/WP-XX-TYY-*.md` 全部任务卡——范围、允许/禁止、GWT 验收、
   命令、提交信息、停止条件；
2. `work-packages/WP-XX-*.md`——包内编排与实施顺序；
3. 任务卡/模块方案头部引用的 `architecture/*.md` 契约——语义唯一权威，
   与其他文档冲突时以契约为准；
4. `module-design/<模块>.md`——目录、CMake target、依赖白名单、模块级冻结决策；
5. `schemas/` 对应 Schema 与 `examples/`（含 invalid 示例）——字段级合法性；
6. `requirements.md` 被引用节——尤其 §15.1 数据集、§15.3 容差。

产出一张"实施备忘"（写入会话，不写入文档库）：本 WP 的 target 名、目录边界、
允许依赖、关键冻结数值、禁止事项清单。

## 第 2 步：实施规则

- 逐卡实施，每张任务卡执行同一循环：
  先写失败测试 → 最小实现 → 测试通过 → 逐字执行卡内验证命令 →
  git 提交（格式见"第 2.5 步 编码与提交规范"；任务卡"提交："字段的英文描述
  仅作内容参考，以本规范中文格式为准，并在完成报告"文档缺口"中登记该偏差）。
- 代码只写入任务卡"允许"目录；禁止修改 `requirements.md`、`architecture/`、
  `module-design/`、`schemas/` 语义、CSV、`testdata/` 黄金数据。
- 公共语义（枚举、容差、诊断码、字段名）只引用权威文档；发现文档未定义的
  跨模块语义 → 立即停止并按任务卡"停止"条款上报，不得自行发明。
- 遵守分层边界：core 无 Qt Widgets/QApplication；业务模块禁止本地 CollisionPolicy
  与默认值、自建线程池/调度、插件间私有头互依；模型测试用 QCoreApplication，
  GUI 测试按 testing-contract §5 单进程 windows 平台规则。
- 数值实现采用 §15.3 冻结容差与几何规则（旋转测地角、clamp 后 atan2 有向轴、
  J_norm=[J_v/L*; J_ω]），不得放宽或自定阈值。
- 黄金数据只读消费；运行期不写 testdata；证据一律写
  `out/test-evidence/wp-xx/<run-id>/`。

## 第 2.5 步：编码与提交规范（任何写代码的会话均适用）

### 头文件文件头（每个 .hpp 必须有）

每个公共头文件开头使用块注释，逐项写明：

- `@brief`：一句话职责；
- 模块功能详述：本头/本类型在模块中的角色、职责边界（做什么、明确不做什么）；
- 依赖与边界：允许依赖、禁止依赖（对应模块方案 §2 的白名单）；
- 冻结语义引用：契约 ID + 节号（如 CTR-PER-001 §2、需求 §15.3）——
  只引用编号和位置，不复制契约正文和数值，防止注释成为第二真值；
- 单位与数值口径：本头涉及的全部量纲（m/rad/kg/N·m 等）；
- 错误与失败行为：可预期失败的返回方式（expected<T,Error> + Diagnostic），
  何种情况失败、失败后状态是否保持；
- 追踪：关联任务卡编号、模块方案路径。

.cpp 文件头部用简短版（职责 + 引用的契约 ID）。

### 行内注释

- 每个公共类/函数注释：用途、每个参数（含物理单位）、返回值、错误路径、
  线程/并发约束；
- 复杂算法段落：给出公式与权威出处（如 "J_norm 依据需求 §15.3 冻结规则"），
  解释为什么这样做；非直观实现给出推导或前提条件；
- 术语与 `architecture/symbol-registry.md` 规范名一致；注释用规范中文，
  代码标识符保持英文；
- 禁止：注释与代码行为矛盾；复制容差/公式正文（会随契约演进漂移成错误真值）；
  逐行复述代码的空话注释（如 "i 自增 1"）。

### 提交规范（中文、分条）

- 标题行：`WP-XX-TYY: 中文概括`（保留任务卡编号保证双向追踪）；
- 空一行后分条列出改动，每条以 `- ` 开头、动词开头、一项一个改动点，
  分类为：新增 / 修改 / 测试 / 证据 / 修复；
- 示例：

  ```text
  WP-02-T01: 数据清单加载器与完整性校验

  - 新增 DataManifestLoader 只读加载器与固定顺序校验流程
  - 新增 SHA-256 完整性校验与重复 ID、未知单位故障夹具
  - 新增 sdurws_ird_manifest_integrity_test 契约测试目标
  ```

- 一次提交只覆盖一张任务卡；不合并多卡改动。

## 第 3 步：验证与证据

- 在仓库根目录、VS x64、Debug 配置执行卡内全部命令
  （`build.ps1`/`run-tests.ps1`/`check-boundaries.ps1` 或卡内 cmake/ctest 原生形式）。
- 每次验证记录：命令全文、提交 SHA、测试结果、
  （涉及黄金数据时）datasetVersion、manifest SHA-256、seed、threadCount、容差 profile。
- WP 全部任务完成后：运行 `check-boundaries.ps1`；正常实施不应改动任何文档，
  若确有文档变更，另运行 `validate-development-docs.ps1` 并在报告中说明理由。

## 第 4 步：完成报告与停止

输出结构化报告：

1. 完成的任务卡清单 + 每张卡的提交 SHA；
2. 验证命令与结果摘要（含边界扫描）；
3. 证据路径清单；
4. 发现的文档缺口/待确认项（只报告，未修改任何契约）；
5. 待人工独立评审事项（治理要求生产者/实现者/验证者/评审者分离，
   你本运行的全部产出默认视为"待评审"）；
6. 自查确认：所有新增公共头文件均有规范文件头注释，所有公共函数有接口注释，
   全部提交符合中文分条格式。

然后停止。不得自行开始下一个 WP，不得合并到发布分支之外。

## 硬性禁止（违反任何一条立即停止并报告）

- 修改、绕过或"顺手加强"验证命令；跳过失败测试先行；
- 通过修改需求/契约/黄金数据让测试通过；
- 实现任务卡范围之外的功能，即使看起来有益；
- 谎报或美化测试与验证结果；
- 提交无文件头注释的公共头文件；
- push 远端或改写 git 历史。
