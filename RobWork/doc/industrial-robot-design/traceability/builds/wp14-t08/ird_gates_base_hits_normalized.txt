      2 表外依赖边 ui->ui（sdurws_ird_ui_plugin → sdurws_ird_ui）不在 ARCH §3.5 白名单
      2 表外依赖边 ui->ui（sdurws_ird_ui_app → sdurws_ird_ui）不在 ARCH §3.5 白名单
      2 表外依赖边 ui->project（sdurws_ird_ui_plugin → sdurws_ird_project）不在 ARCH §3.5 白名单
      2 表外依赖边 ui->project（sdurws_ird_ui_app → sdurws_ird_project）不在 ARCH §3.5 白名单
      2 表外依赖边 modeling->ui（sdurws_ird_modeling_plugin → sdurws_ird_ui）不在 ARCH §3.5 白名单
      2 表外依赖边 ->ui（sdurws_ird_ui_gui_test → sdurws_ird_ui）不在 ARCH §3.5 白名单
      2 表外依赖边 ->testkit（sdurws_ird_ui_gui_test → sdurws_ird_testkit）不在 ARCH §3.5 白名单
      2 表外依赖边 ->testkit（sdurws_ird_testdata_lint → sdurws_ird_testkit）不在 ARCH §3.5 白名单
      2 目标 sdurws_ird_modeling 链接未知目标族 pugixml::pugixml（不在 ird/框架基线/gtest 词表；如属第三方新增依赖，先登记 DTB 再扩词表）
      2 目标 sdurws_ird_io_test 链接未知目标族 libzip::zip（不在 ird/框架基线/gtest 词表；如属第三方新增依赖，先登记 DTB 再扩词表）
      2 目标 sdurws_ird_io_contract_test 链接未知目标族 libzip::zip（不在 ird/框架基线/gtest 词表；如属第三方新增依赖，先登记 DTB 再扩词表）
      2 目标 sdurws_ird_io 链接未知目标族 libzip::zip（不在 ird/框架基线/gtest 词表；如属第三方新增依赖，先登记 DTB 再扩词表）
      2 目标 sdurws_ird_io 链接未知目标族 expat::expat（不在 ird/框架基线/gtest 词表；如属第三方新增依赖，先登记 DTB 再扩词表）
      2 测试目标 sdurws_ird_ui_test 直链他单元产品目标 sdurws_ird_testkit（允许形态仅同单元被测目标；跨单元需求经 testkit 替身或 DTB §4.5 登记）
      2 测试目标 sdurws_ird_ui_contract_test 直链他单元产品目标 sdurws_ird_testkit（允许形态仅同单元被测目标；跨单元需求经 testkit 替身或 DTB §4.5 登记）
      2 测试目标 sdurws_ird_ui_contract_test 直链他单元产品目标 sdurws_ird_project（允许形态仅同单元被测目标；跨单元需求经 testkit 替身或 DTB §4.5 登记）
      2 测试目标 sdurws_ird_ui_contract_test 直链他单元产品目标 sdurws_ird_execution（允许形态仅同单元被测目标；跨单元需求经 testkit 替身或 DTB §4.5 登记）
      2 测试目标 sdurws_ird_ui_contract_test 直链他单元产品目标 sdurws_ird_diagnostics（允许形态仅同单元被测目标；跨单元需求经 testkit 替身或 DTB §4.5 登记）
      2 测试目标 sdurws_ird_runtime_test 直链他单元产品目标 sdurws_ird_testkit（允许形态仅同单元被测目标；跨单元需求经 testkit 替身或 DTB §4.5 登记）
      2 测试目标 sdurws_ird_requirements_test 直链他单元产品目标 sdurws_ird_testkit（允许形态仅同单元被测目标；跨单元需求经 testkit 替身或 DTB §4.5 登记）
      2 测试目标 sdurws_ird_requirements_contract_test 直链他单元产品目标 sdurws_ird_testkit（允许形态仅同单元被测目标；跨单元需求经 testkit 替身或 DTB §4.5 登记）
      2 测试目标 sdurws_ird_reporting_test 直链他单元产品目标 sdurws_ird_testkit（允许形态仅同单元被测目标；跨单元需求经 testkit 替身或 DTB §4.5 登记）
      2 测试目标 sdurws_ird_reporting_contract_test 直链他单元产品目标 sdurws_ird_testkit（允许形态仅同单元被测目标；跨单元需求经 testkit 替身或 DTB §4.5 登记）
      2 测试目标 sdurws_ird_project_test 直链他单元产品目标 sdurws_ird_testkit（允许形态仅同单元被测目标；跨单元需求经 testkit 替身或 DTB §4.5 登记）
      2 测试目标 sdurws_ird_project_contract_test 直链他单元产品目标 sdurws_ird_testkit（允许形态仅同单元被测目标；跨单元需求经 testkit 替身或 DTB §4.5 登记）
      2 测试目标 sdurws_ird_policy_test 直链他单元产品目标 sdurws_ird_testkit（允许形态仅同单元被测目标；跨单元需求经 testkit 替身或 DTB §4.5 登记）
      2 测试目标 sdurws_ird_policy_contract_test 直链他单元产品目标 sdurws_ird_testkit（允许形态仅同单元被测目标；跨单元需求经 testkit 替身或 DTB §4.5 登记）
      2 测试目标 sdurws_ird_modeling_test 直链他单元产品目标 sdurws_ird_testkit（允许形态仅同单元被测目标；跨单元需求经 testkit 替身或 DTB §4.5 登记）
      2 测试目标 sdurws_ird_modeling_contract_test 直链他单元产品目标 sdurws_ird_testkit（允许形态仅同单元被测目标；跨单元需求经 testkit 替身或 DTB §4.5 登记）
      2 测试目标 sdurws_ird_io_test 直链他单元产品目标 sdurws_ird_testkit（允许形态仅同单元被测目标；跨单元需求经 testkit 替身或 DTB §4.5 登记）
      2 测试目标 sdurws_ird_io_contract_test 直链他单元产品目标 sdurws_ird_testkit（允许形态仅同单元被测目标；跨单元需求经 testkit 替身或 DTB §4.5 登记）
      2 测试目标 sdurws_ird_execution_test 直链他单元产品目标 sdurws_ird_testkit（允许形态仅同单元被测目标；跨单元需求经 testkit 替身或 DTB §4.5 登记）
      2 测试目标 sdurws_ird_execution_contract_test 直链他单元产品目标 sdurws_ird_testkit（允许形态仅同单元被测目标；跨单元需求经 testkit 替身或 DTB §4.5 登记）
      2 测试目标 sdurws_ird_evidence_test 直链他单元产品目标 sdurws_ird_testkit（允许形态仅同单元被测目标；跨单元需求经 testkit 替身或 DTB §4.5 登记）
      2 测试目标 sdurws_ird_evidence_contract_test 直链他单元产品目标 sdurws_ird_testkit（允许形态仅同单元被测目标；跨单元需求经 testkit 替身或 DTB §4.5 登记）
      2 测试目标 sdurws_ird_diagnostics_contract_test 直链他单元产品目标 sdurws_ird_testkit（允许形态仅同单元被测目标；跨单元需求经 testkit 替身或 DTB §4.5 登记）
      2 产品目标 sdurws_ird_ui_gui_test 链接 testkit（testkit.md §2.4 T-1：产品目标不得链接/包含 testkit）
      2 产品目标 sdurws_ird_testdata_lint 链接 testkit（testkit.md §2.4 T-1：产品目标不得链接/包含 testkit）
      2 业务域目标互链：sdurws_ird_modeling_plugin → sdurws_ird_modeling（ARC-02；R-1 无例外）
      2 ui 产品面疑似包含 Qt 类头（Q+大写约定）：<IRD_ROOT>/ui/src/WorkbenchShell_p.hpp
      2 ui 产品面疑似包含 Qt 类头（Q+大写约定）：<IRD_ROOT>/ui/src/WorkbenchShell.cpp
      2 ui 产品面疑似包含 Qt 类头（Q+大写约定）：<IRD_ROOT>/ui/src/WorkbenchContent_p.hpp
      2 ui 产品面疑似包含 Qt 类头（Q+大写约定）：<IRD_ROOT>/ui/src/WorkbenchContent.cpp
      2 ui 产品面疑似包含 Qt 类头（Q+大写约定）：<IRD_ROOT>/ui/src/View3DPlaceholder.cpp
      2 ui 产品面疑似包含 Qt 类头（Q+大写约定）：<IRD_ROOT>/ui/src/ShellSupport.cpp
      2 ui 产品面疑似包含 Qt 类头（Q+大写约定）：<IRD_ROOT>/ui/src/PolicySummaryCard.cpp
      2 ui 产品面疑似包含 Qt 类头（Q+大写约定）：<IRD_ROOT>/ui/src/ParamTablePanel.cpp
      2 ui 产品面疑似包含 Qt 类头（Q+大写约定）：<IRD_ROOT>/ui/src/DiagnosticPresentation.cpp
      2 ui 产品面疑似包含 Qt 类头（Q+大写约定）：<IRD_ROOT>/ui/src/CommandPalette_p.hpp
      2 ui 产品面疑似包含 Qt 类头（Q+大写约定）：<IRD_ROOT>/ui/src/CommandPalette.cpp
      2 ui 产品面疑似包含 Qt 类头（Q+大写约定）：<IRD_ROOT>/ui/src/CommandInteractionBridge.cpp
      2 ui 产品面疑似包含 Qt 类头（Q+大写约定）：<IRD_ROOT>/ui/src/AboutDialog.cpp
      2 ui 产品面疑似包含 Qt 类头（Q+大写约定）：<IRD_ROOT>/ui/include/sdurws/ird/ui/IGlobalShortcutRegistry.hpp
      2 ui 产品面疑似包含 Qt 类头（Q+大写约定）：<IRD_ROOT>/ui/include/sdurws/ird/ui/ICommandRegistry.hpp
      2 ui 产品面包含 Qt 头：<IRD_ROOT>/ui/src/GlobalShortcutRegistry.cpp（NFR-MNT-01：L2 计算内核零 Qt）
      2 ui 产品面包含 Qt 头：<IRD_ROOT>/ui/src/CommandRegistry.cpp（NFR-MNT-01：L2 计算内核零 Qt）
      2 project 产品面发现 RobWork 字面量 1 处（疑前缀拼接/剥离）：<IRD_ROOT>/project/src/ProjectStoreImpl.cpp（例外须 DTB §4.5 登记）
      2 L2 目标 sdurws_ird_ui 链接 Qt 库 Qt6::Widgets（NFR-MNT-01：计算核心零 Qt；例外仅 ui 界面目标形态）
      2 L2 目标 sdurws_ird_ui 链接 Qt 库 Qt6::Gui（NFR-MNT-01：计算核心零 Qt；例外仅 ui 界面目标形态）
      2 L2 目标 sdurws_ird_ui 链接 Qt 库 Qt6::Core（NFR-MNT-01：计算核心零 Qt；例外仅 ui 界面目标形态）
      1 IRD-GATE-T2
      1 IRD-GATE-T1
      1 IRD-GATE-SUB
      1 IRD-GATE-R5
      1 IRD-GATE-R4
      1 IRD-GATE-R3
      1 IRD-GATE-R1
