ird_gates base..branch 命中集比对（WP-13-T05）

比对对象：
  base   ＝dd97c920409f0e9d56be48ae789d633977adf993（任务契约 base——wp13-t04
           合入收尾提交，industrialrobot 源树与其 acceptedHead 9e24b410 逐字节
           一致〔git diff 9e24b410 dd97c920 -- …/industrialrobot 为空，本任务实测〕）
  branch ＝本任务 wp13-t05 工作树 @ eb417a1e（含解析映射＋链型判定两提交）

比对方法（比 wp13-t04 的 MSBuild 日志比对更精确的口径）：
  ird_gates 是纯 cmake -P 脚本（ird_gates.cmake），不经配置即可直跑。为消除
  MSBuild 控制台对长 GBK 行的截断/折行干扰，base/branch 两侧均在各自源树上
  以同一脚本直跑（cmake -DIRD_ROOT=<源树> -DIRD_SELFTEST_DIR=<临时> -P
  cmake/ird_gates.cmake），取 stdout/stderr 中全部 IRD-GATE- 行做多重集比对；
  base 侧经临时 worktree @ dd97c920 取得（比对后已删除 worktree，未触碰
  pipeline 自持路径）。留痕：
    gates-direct-base.log   ＝base 直跑全输出（109 行 IRD-GATE-）
    gates-direct-branch.log ＝branch 直跑全输出（111 行 IRD-GATE-）
    gates-ird-gates-branch.log ＝集成树 MSBuild 目标构建输出（留痕口径与
      wp13-t04 一致；构建退出码 1＝存在存量命中，与 base 同态）

结果（路径归一化后多重集 diff——把两侧扫描根前缀归一为 <ROOT> 后 sort+diff）：
  唯一新增命中（正文＋汇总双计 2 行，实为 1 条）：
    IRD-GATE-LIB: 目标 sdurws_ird_modeling 链接未知目标族 pugixml::pugixml
                  （不在 ird/框架基线/gtest 词表；如属第三方新增依赖，
                    先登记 DTB 再扩词表）
  其余命中逐条一致（base 109 = branch 109 归一后，含 SUB/R3/R4/T1/LIB/SELF
  全族；ui/project/testdata_lint 等存量命中与本任务无因果）。

处置登记：
  该新增命中＝O-40（DTB §4，2026-09-22 所有者批次授权）登记的 pugixml
  PRIVATE 依赖（P-MDL-5 决议路径）在门禁词表中的预期暴露——词表扩充属
  治理侧动作，实现任务不越权改 cmake/ird_gates.cmake。已按 io 的
  libzip::zip/expat::expat 既有命中同款口径自登记 findings F-302
  （traceability/findings.json），owner＝构建约定所有者。

结论：base..branch 门禁命中集多重集 diff＝恰 1 条新增（F-302 已登记），
  modeling 产品面零其他新增命中；ird_gates「零新增」口径达成（唯一新增
  为已登记依赖的词表暴露，非红线违约）。
