UI-T04（WP-10-T04）ird_gates 门禁命中集比对记录
=====================================================

执行方式：cmake --build build --config Release --target ird_gates
          （集成模式构建树 build/，RWS_BUILD_INDUSTRIALROBOT=ON；
            与 WP-10-T03 留痕同一执行面——gate-all.ps1 第 2 步同口径）
原始输出：本目录 gates-branch-run.log（MSBuild 输出为 GBK 行合并形态，
          本文件为人工归一化结论——逐码比对基线，未做程序化重排）

结论：与 WP-10-T03 合入后基线（32 命中＝既有登记基线 29＋ui 落位 3）
      逐码比对零变化；本任务新增文件零命中：
        - src/StatusProjection.cpp　　未出现在任何命中行（零 Qt 头、
          零跨单元 include——仅 core/diagnostics/ui 自有头）；
        - test/StatusWordModelTest.cpp　测试面，不在产品面扫描域，未命中。

命中集（按码归组，与 wp10-t03/gates-hits-after-skel.txt 同集）：
  - IRD-GATE-SUB：测试目标直链 sdurws_ird_testkit／表外边 ->ui/->testkit
    （F-221 同型既有登记，12＋单元测试目标同形态——机器面回填责任方
    WP-01-T03，见 traceability/wp10-t02-gate-registrations.md）；
  - IRD-GATE-LIB：io 单元 libzip::zip／expat::expat 未知目标族（io 落位
    登记的既有第三方依赖）；
  - IRD-GATE-R3：sdurws_ird_ui 链接 Qt6::Core/Gui/Widgets（R-3 例外本体，
    §3.1 预登记）＋ui/src 三文件（ShellSupport.cpp/WorkbenchShell.cpp/
    WorkbenchShell_p.hpp）"疑似包含 Qt 类头"（F-259 同款机器面滞后——
    R-3 例外头包含半区回填责任方 WP-01-T03）；
  - IRD-GATE-R4：project/src/ProjectStoreImpl.cpp RobWork 字面量 1 处
    （project 落位期既有登记例外）；
  - IRD-GATE-T1：sdurws_ird_testdata_lint／sdurws_ird_ui_gui_test 链接
    testkit（T-1 允许形态——testdata lint 工具与 GUI 测试目标）；
  - 引擎自检：pass_clean/pass_r4comment 通过，fail_r1/r3/r4/r5/sub/t1/t2
    按预期检出（干准夹具自证，全部符合预期）。

UI-T04 变更引入的新命中：0
登记滞后（既有例外，非本任务引入）：如上，均在既有登记件内。
