# EX-T09 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 ID | EX-T09（契约测试套件落地 FakeWorker/契约与故障注入；契约 tasks/foundation/EX-T09.json，status=ready） |
| 送验对象（冻结） | `293ef150589497da9635700830d828496c9b736a`（分支 wp08-t09 尖端；**验收全程按该 SHA**） |
| diff 范围 | `2ca48dace52a9f67aa570ef219aa828b6936f8eb..293ef150589497da9635700830d828496c9b736a`（base＝state.base＝origin/redesign-main 尖端；线性两提交：353b3362 实现＋293ef150 文档同步与留痕） |
| 分支漂移检测 | `git fetch origin` 后 `git rev-parse origin/wp08-t09` → ＝`293ef150…`＝冻结 SHA——**零漂移**（ACC §3） |
| 复现现场 | `git worktree add --detach acc_worktrees/ext09-A 293ef150…`（detached @ SHA）；独立 worktree 全新配置全新 build/，无实施构建树残留 |
| 记录回传 | 本记录落 `acc/EX-T09/1` 分支（`git worktree add -b acc/EX-T09/1 acc_worktrees/ext09-B origin/redesign-main`）并推送；attempt=1 |
| **verdict** | **fail（阻断级 B-1 一项；4.1~4.9/4.11 实质全部通过——构建、测试、门禁、变异检出力均经验收者亲手复现证实，返工仅需补证不需重做）** |
| 验收日期 | 2026-09-19 |
| 验收者环境 | Windows x64（10.0.26200）；VS 2022 Community（MSBuild 17.12.6）；CMake；pwsh 7（治理脚本）；**独立 worktree 复现**，与实施会话零共享上下文（ACC §2 四条独立性要求逐条满足：全新会话／未消费实施叙述、仅消费产物与文档／对抗式立场逐项找反证／构建测试全部亲手重跑） |

---

## 环境前置（ACC §3 复现口径）

- 集成模式：worktree A 内全新 `cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=<repo>/vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`，配置 exit 0；缓存 `grep RWS_BUILD_INDUSTRIALROBOT build/CMakeCache.txt` → `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON` 实证；`CMAKE_CXX_FLAGS="-DNOMINMAX -DWIN32_LEAN_AND_MEAN -DWIN32"` 对齐主树基线口径（F-008/F-098 登记的基线宏）。
- 未跟踪辅助件（F-007/F-099 已登记）：自主工作树复制 `RobWork/RobWorkStudio/bin/{RobWorkStudio.ini.shared.in,RobWorkStudio.ini.template.static}` 两件至 worktree A 后配置通过——验收者首配即踩中 F-007，与登记完全一致。
- 冒烟模式：另配全新目录 `acc_worktrees/ext09-A-smoke`，源＝worktree A 的 industrialrobot/，**带 vcpkg toolchain 绝对路径**（CORE-T01 验收 G-2 教训），配置 exit 0（Configuring done/Generating done）。

## 4.1 改动范围 —— 通过

- diff 文件清单（`git diff --name-status 2ca48dac..293ef150`）：24 个文件＝execution 单元 11 个（CMakeLists.txt 改；contract_test/ 新增 ArchiveFaultContractTest.cpp、ContractTestMain.cpp、RecoveryChild.hpp/cpp、RecoveryProcessContractTest.cpp，修改 LinkageContractTest.cpp、WorkerProcessContractTest.cpp；test/ 新增 ContractSuiteFacilities.hpp、ContractSuiteFacilitiesTest.cpp、TestMainReport.cpp）＋units/execution.md 1 个（§15.4 增 v0.11 行＋卡头版本行 v0.5→v0.11 追平＋构建落位行补句）＋traceability/ 12 个（builds/wp08-t09/ 留痕 11＋findings.json F-221 登记）。
- 逐个对照契约 `allowedFiles`（`execution/**`、`units/execution.md`、`traceability/**`）：机械过滤非三前缀文件结果为**空集，24/24 全部命中**，零越界。
- `forbiddenFiles`（REQUIREMENTS.md、ARCHITECTURE.md）：diff 中**零触碰**。
- SA-02：diff 无任何 `industrialrobot/` 之外框架源码；`patches/` 目录零改动（仅既有 0001-rwslibs-entry-option.patch 与 PATCHES.md）——零未登记框架改动。

## 4.2 可复现构建 —— 通过（验收者亲手重跑）

- 集成模式全新配置后全量 Release 构建：`final-exit=0`，`error C[0-9]+|error LNK[0-9]+|error MSB[0-9]+|fatal error` 计数 **0**（日志 acc_worktrees/ext09-A-build-full.log，91 个目标产物）。
- 冒烟模式：配置 exit 0＋两测试目标构建 exit 0、错误计数 0（acc_worktrees/ext09-A-smoke-config.log、ext09-A-smoke-build.log）。
- 4.10 指出的缺口是**留痕入库缺失**（见下），不是构建不可复现——验收者独立复现的构建结论与实施留痕声称的实质结论一致。

## 4.3 契约 verify 命令 —— 通过（2/2）

- verify[1]：`cmake --build build --config Release --target sdurws_ird_execution_test` → worktree A 集成树亲手执行 **exit 0**（sdurws_ird_execution_test.exe 产出）。
- verify[2]：`cmake --build build --config Release --target sdurws_ird_execution_contract_test` → 亲手执行 **exit 0**（sdurws_ird_execution_contract_test.exe 产出）。
- 冒烟树两测试目标同样构建成功并运行（见 4.4）。

## 4.4 acceptance 条目逐项核对 —— 通过（5/5 有证据，验收者亲手实跑）

| 契约 acceptance | 验收证据（验收者亲手） |
| --- | --- |
| 1. §11 矩阵全组逐项执行通过并留痕（gtest XML＋ird-test-report.json 并存）；落位双测试目标（test/*＝单元内纯逻辑用替身；contract_test/*＝跨单元 evidence 校验器/project 归档端口 fake/真实 worker 进程场景） | 集成＋冒烟两模式验收者各实跑一遍：**149/149＋38/38 全绿**（exit 0×4；`[PASSED] 149 tests`×2、`[PASSED] 38 tests`×2）。矩阵 12 组覆盖逐一核对实跑 XML 用例名：EX-SUB（EX_SUB_1 多例＋EX_SUB_2）、EX-SM（EX_SM_1~7 含 EX_SM_3 2 s/10 s 两例）、EX-WKR（EX_WKR_1/2/3 契约真进程＋EX_WKR_4 三例＋EX_WKR_5）、EX-CHN（乱序重排/重复丢弃/缺口断裂，单元＋契约双侧）、EX-CKP（1/2/3＋契约接线两例）、EX-CCH（1/2＋契约判定相等两例）、EX-REG（1~8 全组，UnknownRunRejectedWithNoArchiveWrite_EX_REG_1 实跑在列）、EX-ORT（EX_ORT_1 两例）、EX-ARC（1/2/3/4 本任务新增＋EX_ARC_5 既有）、EX-RCV（EX_RCV_1 本任务新增）、EX-RES（EX_RES_1 本任务新增＋EX-RES-2 既有 ResourceGovernanceSchedulerTest 套件）、EX-BLD（ExecutionBuild 套件＋Linkage 三例）。目标分工符合 §11 原文：真进程用例（worker/恢复/锁注入）全部落 contract_test，纯逻辑落 test。ird-test-report.json 与 gtest XML 并存：在库 4 份 report（两目标×两模式）＋4 份 XML，XML 顶层计数 tests=149/149/38/38 failures=0 实测 |
| 2. 替身边界声明在案（ScriptedEvaluator＋FakeWorkerMain＋FaultInterceptor 接缝＋ManualClock＋TempDir/DeterministicEnv——R-7/A-9）；faultPointId 命名登记（execution/process-launcher/create、execution/channel/send-frame、execution/channel/recv-frame、execution/archive/*） | `test/ContractSuiteFacilities.hpp` 文件头逐项声明（五类替身落位形态＋"不构成任何业务算法正确性证明"原文＋T-1 构建边界）；faultPointId 登记表 `registeredFaultPointIds()` 七项＝契约点名的四组（process-launcher/create、channel/send-frame、channel/recv-frame、archive/{begin,write-batch,finalize,abandon} 通配展开）；具名自证用例 `FaultPointIdRegistryMatchesSection11Handover` 实跑通过（4.5 变异 A 证明有红能力）；FaultInterceptor 真实消费见 FaultArchivePort（ArchiveFaultContractTest L306-363——FaultPlan/FaultLog 走登记原语） |
| 3. TestProcessRunner 按 TK-T11 触发接入真进程场景（EX-RCV-1/EX-WKR-2）；前置未就绪的用例如实标注阻塞 | TK-T11 已于 2026-09-11 合入（state.history），前置就绪、无阻塞用例。EX-RCV-1＝`RecoveryProcessContractTest.MainProcessKillReconstructsInterruptedOnRestart_EX_RCV_1` 实跑通过：TestProcessRunner 启动子进程（同一 exe 双形态，ContractTestMain 先于 gtest 拦截 `--ird-ex-rcv-child`）→建库→归档预留（无 manifest）→标记文件 EventWatch→`runner.kill()` 真进程终止（Killed 形态断言）→重启开库 writable=true（D-02 接管）→listRuns 空（D-13 不伪装）→预留批次字节数断言→T14 重建 Interrupted＋EX-TASK-INTERRUPTED（工厂行为非测试叙述）。EX-WKR-2 真进程面＝WorkerCrashFailsOnlyItsOwnTask（EX-T06 期登记）实跑通过 |
| 4. P-EX-8 处置：测试/装配侧 IExecutionDiagnosticsSink 空实现占位（project §3.2 同模式）；断言只针对 diagnostics.md §4.6 已收编 18 项 EX-* 码值，不私定新码值 | `NullExecutionDiagnosticsSink` 空实现占位（设施头 L209-217，注释载明裁决权归 diagnostics、project §3.2 先例）＋自证用例实跑通过；`registeredExStableCodes()` 18 项镜像与 diagnostics.md §4.6 权威清单**逐字一致**（验收者机械比对 diff 为空）；`isRegisteredExStableCode` 自检闸内嵌于 CollectingSink::hasReportCode（清单外码值出现在断言里即测试自身失败）＋镜像数量断言 `EXPECT_EQ(codes.size(), 18u)`；4.5 变异 C 证明自检闸有红能力 |
| 5. AT-10/11/13/34 场景载体就位；时钟敏感用例一律 ManualClock 虚拟推进、不 sleep 断言 | IRD_TEST_INFO 登记面经实跑 ird-test-report.json 实证：AT-10×6、AT-11×1、AT-13×2（契约套件新用例）、AT-34×2（单元 EX-SM-3 面＋契约 ThresholdExceeded——在库两 report 合计，与实跑一致）；ManualClock 用例（EX-ARC-4 兜底阈值、EX-SM-3）虚拟推进断言零 sleep；新用例退避时序以记录器注入（EX-ARC-1 delays=[1000,4000]ms 断言值，非真实时延） |

- **ird_gates 独立复核**：验收者亲手 `cmake --build build --config Release --target ird_gates` → exit 1（既有登记例外红，门禁设计行为）＋命中 **19 项**＝base（wp08-t08 期 17 项原始命中）＋**新增恰 2**（IRD-GATE-SUB：sdurws_ird_execution_test / sdurws_ird_execution_contract_test →testkit），逐目标与在库 gates-t09-hits-sorted.txt（18 token 归一化清单）比对一致，移除 0——实施侧 F-221 声称（"新增恰 2、移除 0、新增项为契约 acceptance 明文的 testkit 设施消费边"）**独立证实成立**。
- validate-task：验收者亲手复跑 `pwsh -File scripts/industrialrobot/validate-task.ps1 -TaskFile <worktree A 契约>` → **PASS (1 tasks)**（exit 0）。

## 4.5 测试真实失败能力（验证验证者） —— 通过（3 个变异注入，5 用例全红）

| 变异 | 注入内容（均在送验 diff 新增文件内） | 结果 |
| --- | --- | --- |
| A | ContractSuiteFacilities.hpp `registeredFaultPointIds()` 摘除 `kFaultArchiveWriteBatch` 登记项 | `FaultPointIdRegistryMatchesSection11Handover` **红**——faultPointId 登记自证面有检出力 |
| B | ArchiveFaultContractTest.cpp `FaultArchivePort::writeBatch` 注入判断改为恒透传（`if (false && …)`——故障注入失效） | `DiskFullBoundedRetryThenAbandonKeepsCompletedAndSkipsCache_EX_ARC_1`、`WriteRejectedStoreErrorPassthroughBoundedPath_EX_ARC_2` **双红**——故障注入→有界重试→abandon→诊断码→缓存跳过的行为断言面对注入真实性敏感，非恒真 |
| C | ContractSuiteFacilities.hpp `registeredExStableCodes()` 摘除 `EX-TASK-INTERRUPTED` | `RegisteredCodeMirrorHasExactlyTheIncorporatedEighteen_PEX8` **红**（数量断言）＋`MainProcessKillReconstructsInterruptedOnRestart_EX_RCV_1` **红**（码值自检闸把守的用例连带失败）——P-EX-8"不私定码值"从纪律变成可执行检查且检查本身有效 |
- 变异还原：两文件以备份精确还原，`git status --short` 零残留（git diff 为空）＋重建重跑 **149/149＋38/38 全绿恢复**——复现现场无污染。

## 4.6 架构红线（AGENTS §5 八条） —— 通过

1. **L2 零 Qt**：新增/修改 11 文件 `#include <Q` 扫描零命中；两测试目标链接面无 Qt。
2. **业务域互链（R-1）**：CMake diff 仅两测试目标链接面变更（+sdurws_ird_testkit＋GTest::gtest）；产品库 sdurws_ird_execution 与 worker 目标零变更；ird_gates 重跑除 2 条新增 SUB（测试目标→testkit，T-1 允许形态）外零新增。
3. **跨单元私有头（R-2）**：新增文件 include 面机械清点＝core/evidence/project/execution（本单元）公共头＋sdurws/ird/testkit/ 公共测试设施头（include/ 公共根）；runtime/policy/业务域零命中；契约目标私有包含根仅纳入本单元 test/（设施头同单元共享，不入 include/ 公共根）。
4. **SA-02 框架零修改**：见 4.1。
5. **R-4 名称前缀**：新增 8 文件 `RobWork` 字面量内容扫描零命中（diff 统计的命中均为 diff 头路径行）。
6. **权威唯一（PA-1）**：本任务为测试套件＋文档登记，不实现业务语义；缓存跳过断言消费 CacheCoordinator 既有双门槛（EX-T08 落位）、兼容判定消费 evidence judgeCacheHit（契约判定相等用例钉住）、恢复重建消费 TaskStateMachine::forRecoveryInterrupted 工厂（产品行为）——判定与存储语义全部走真实实现，替身只在"别人的能力"接缝（设施头替身边界声明原文）。
7. **不可变历史（PA-2）**：不触修订/写路径——不涉。
8. **不继承 old/**：全部用例按 units/execution.md §11 矩阵行＋testkit §6.4/§6.5 语义从零编写（真进程锁注入/子进程停靠协议/故障拦截接线均为新实现），无 old/ 引用。

## 4.7 注释规范（AGENTS §2） —— 通过

- **新增文件全查**（8/8，公共 API 头零新增——本任务无 include/ 公共头）：ContractSuiteFacilities.hpp（文件头设计依据＋替身边界声明五项＋P-EX-8 口径＋线程约束＋T-1 构建边界；类/函数 Doxygen 中文逐个——ManualClock 用法纪律、NullSink 为何是空实现而非适配器、ScriptedOutputDecoder 边界）、ContractSuiteFacilitiesTest.cpp（文件头＋逐用例设计依据＋断言处中文说明）、ArchiveFaultContractTest.cpp（文件头设计依据＋与既有用例零重复声明＋替身边界声明＋逐语义段注释：注入原理/计划覆盖预算/错误码对齐矩阵行/对照闭环目的）、RecoveryProcessContractTest.cpp（文件头＋场景边界如实声明＋逐步骤中文注释）、RecoveryChild.hpp/cpp（文件头协议语义＋错误语义"无假阳性通道"＋五步逐段注释）、ContractTestMain.cpp/TestMainReport.cpp（为什么替换 gtest_main＋子进程拦截时序＋监听器所有权）、WorkerProcessContractTest.cpp 新增段（EX-ARC-3 注入原理注释：无 FILE_SHARE_DELETE 句柄→RemoveDirectory 必然失败；EX-RES-1 两道守卫逐条）。
- **修改文件抽查**：CMakeLists.txt（每个增列块中文注释块说明链接面变更原因与 T-1 口径）、LinkageContractTest.cpp（白名单与 NoTestkitEdge 收窄重述的 Doxygen 级说明）。
- §2.5 高危信息表：物理单位（退避 1000/4000 ms、超时窗、字节计数）、坐标系/角度制式（不涉——编排与存储语义）、所有权（FaultArchivePort 构造注释"real 非所有权——null 删除器仅为满足形参面，寿命由夹具保证"；gtest 监听器"静态存储期——listeners 不接管所有权"）、线程约束（"全部设施为测试目标单线程使用——非线程安全""单线程入口"逐头声明）、错误语义（子进程失败路径显性退出码 3~6"失败＝环境错误——父进程超时路径暴露，无假阳性通道"；dev 通道与稳定码分流断言）、确定性来源（seed=20260909 固定值、FIFO 游标、登记表顺序固定）——**必填项零缺失**。

## 4.8 文档同步 —— 通过（附历史 open 条目消账核对）

- 实现与任务卡偏差：units/execution.md §15.4 v0.11 行（diff 实证）逐件登记落位（§11 缺口六行补齐、testkit 设施接入、P-EX-8 处置、AT 载体、LinkageContractTest 增量两处、ird_gates 登记两处）；**契约语义零变更**；卡头版本行 v0.5→v0.11 一次性追平（F-175 家族同型微瑕在 execution 卡自身消账——v0.11 行明文登记）。validate-task PASS 为验收者亲手复跑。
- 任务状态：契约 status=ready（翻转 done 属收尾环节，验收期现状正确）；pipeline/state.json 未被本任务触碰（diff 零命中——治理状态归所有者）。
- **findings.json 未关闭条目相关性核对（ACC §4.8 v1.2）**：head SHA 处共 221 条（F-221 为本任务实施侧自查登记），与本任务相关者逐条核对：
  - **F-221**（ird_gates 新增 2 命中，open，owner=WP-01-T03）：验收者独立重跑证实其声称成立（见 4.4）；处置归 WP-01-T03 例外行登记，非本任务可消账——维持 open 正确。
  - **F-031**（EX-T09.json acceptance 5 的 AT-34 迁注出处括注失准，open，owner="wp08-t09 领取者（领取前补正）＋契约编译治理所有者"）：实体面已兑现（AT-34 载体就位：EX-SM-3 既有用例＋EX-ARC-4 ThresholdExceeded 新用例 IRD_TEST_INFO 携带 AT-34，ManualClock 纪律在案）；但括注修订义务**未履行也无法由本任务合法履行**——tasks/foundation/EX-T09.json 不在契约 allowedFiles 内（改动即 4.1 越界），括注修订须由契约治理侧在收尾或治理窗口完成。**不构成本任务阻断，但该条不得继续以"领取者领取前补正"为唯一路径搁置——转请所有者裁决载体**（见问题清单 G-2）。
  - **F-212/F-213/F-216**（EventBus FIFO 断言面/进度节流敏感度/资源闸停派发敏感度，均 open，owner 注明"随 EX-T09 或后续冻结 diff 增量同步窗口顺手消账"）：本 diff 对三案涉案文件（EventBusDeliveryContractTest.cpp、SchedulerEventBusTest.cpp、ResourceGovernanceTest.cpp/TaskScheduler 实现）**零触碰**，且契约 acceptance 未列该义务——未顺手消账，如实注明不静默积压；三案仍待其 owner 后续处置。
  - **F-220**（gates 清单格式跨任务漂移，open）：本任务侧已部分兑现其建议（t09 清单统一规范化 token 形态＋hit-set-diff 附基线回补说明，跨任务可比性恢复）；口径统一归 gate-all.ps1 维护方的全量处置仍 open——维持 open 正确，注明部分兑现。
  - 其余 open 条目（F-004~F-211 等）经检索无一点名 EX-T09/wp08-t09/WP-08-T10 的未竟义务。

## 4.9 commit 质量 —— **不通过（阻断级 B-1 的组成部分；另有建议级 G-1）**

- 353b3362「[WP-08-T09] 落地 execution 契约测试套件（§11 矩阵补齐＋testkit 设施接入）」：**"落地"动词开头合规**；正文四段（What/Why/验证/影响面）齐备；验证段结论（双模式构建零错误、149/149＋38/38、ird_gates 新增恰 2 移除 0、validate PASS）经验收者复跑**全部证实为真**——粉饰检查通过（该提交未涉留痕清单声称）。
- 293ef150「[WP-08-T09] 单元卡 v0.11 落位登记＋门禁/测试证据留痕」：标题以名词"单元卡"开头非动词开头——**F-012 家族同型再现**（先例 F-214/F-215/F-218，均 suggestion 不阻断）→ **G-1/F-222 转登**。
- **B-1（阻断）**：该提交正文 What 段声称"traceability/builds/wp08-t09/：双模式构建日志、两目标×两模式 gtest XML＋运行日志、ird-test-report.json×4、ird_gates 门禁日志＋命中集合归一化清单＋diff（新增恰 2、移除 0）、validate-task.log"，验证段声称"本提交固化其原始文件"——**声称清单与在库不符**：`git ls-tree 293ef150` 该目录实际仅 11 件（gates txt×3＋XML×4＋report×4），缺 9 件 .log（build-integrated-full.log、build-smoke-config.log、build-smoke-full.log、gates-ird-gates-target.log、两目标×两模式 run.log×4、validate-task.log）。实施工作树磁盘实测 20 件齐全——9 件 .log 被 `.gitignore:87` 的 `*.log` 全局规则吞且未 `git add -f`（`git check-ignore -v` 实证三例）。与 EX-T01 attempt1（九件验证日志同因 fail）、IO-T02 attempt1、CORE-T02 attempt1 完全同族。被吞日志的**实质内容经验收者独立复跑证实为真**（4.2/4.4/4.4-ird_gates），故返工仅需补证不需重做——但 ACC §4.10"构建日志落位"判据未满足＋4.9"验证结论与留痕一致"声称面失实，按 §5 判据构成阻断。

## 4.10 留痕完整 —— **不通过（阻断级 B-1 本体）**

- 在库 11/20 件：XML×4（149/149、38/38 两模式，failures=0 实测）＋ird-test-report.json×4（AT 登记分布与实跑一致）＋gates 清单/diff/基线 3 件（新增恰 2、移除 0，与重跑一致）——这 11 件与报告声称的数字全部相符。
- **缺失 9 件 .log**（构建日志×3、门禁原始日志×1、运行日志×4、validate-task.log×1）——`git check-ignore -v` 实证被 `.gitignore:87 *.log` 吞且未 add -f；AGENTS §4.2"测试必须真实执行并留痕"的留痕半区在库缺位。
- `phase-one-validation.log 或 resultNote 已更新`条文：按先例口径（F-201 登记）以 traceability/builds/wp08-t09/ 全套留痕＋ird-test-report.json 为等效载体——等效载体因 B-1 缺日志半区而不完整。
- **返工输入**：实施者在 wp08-t09 分支对磁盘既有 9 件 .log 执行 `git add -f` 补交（内容与验收者复跑结论一致的先期验证），随后重新发起全量验收；无需重跑构建/测试（验收者已独立证实其实质结论）。

## 4.11 偷懒/缩水实现扫描 —— 通过

1. **标记扫描**：diff 新增行 `TODO|FIXME|stub|placeholder|未实现|not implemented` 正则命中**恰 1 处**＝用例名 `NullExecutionDiagnosticsSinkIsEmptyPlaceholder_PEX8`——系契约 acceptance 4 明文的"空实现占位"（project §3.2 同模式）的具名自证断言（断言其占位行为可注入可弃），属契约显式标注的豁免形态，非缩水标记。
2. **空实现检测**：`NullExecutionDiagnosticsSink::report/reportDev` 空体＝契约 acceptance 4 显式声明的占位（有自证用例钉住其占位语义，4.5 变异 C 证明镜像自检有红能力）；ScriptedOutputDecoder/ManualClock/FaultArchivePort/RecoveryChild 均为实质实现（FIFO 脚本耗尽 fail-closed、单调推进、计划命中＋日志、五步停靠协议）——无未声明的空体/恒等返回。
3. **acceptance 逐条证据表**：见 4.4——五条均有具名测试（文件行号级）＋验收者实跑命令输出，零条无证据。
4. **测试强度**：断言校验行为与边界（写调用恰 3 次、退避逐毫秒、FaultLog 命中序、缓存跳过＋正常归档对照闭环、检查点字节逐位比对、目录残留在盘、两道登记互斥 EXPECT_THROW、恰一个 Running worker、kill 形态＝Killed）；3 变异 5 用例全红证明非恒真（与 4.5 联动）。

---

## 发现的问题清单

| # | 级别 | 内容 | 处置 |
| --- | --- | --- | --- |
| B-1 | **阻断** | 4.10/4.9：9 件验证 .log 留痕被 `.gitignore:87 *.log` 吞且未 `git add -f`，提交 293ef150 正文声称的留痕清单（双模式构建日志/运行日志/ird_gates 门禁日志/validate-task.log）与在库（11 件）不符——EX-T01 attempt1／IO-T02 attempt1／CORE-T02 attempt1 同族问题第四次发生。**实质结论经验收者独立复跑证实为真**（构建零错误、149/149＋38/38 双模式、ird_gates 新增恰 2、validate PASS），返工仅需补证 | 实施者在 wp08-t09 分支对磁盘既有 9 件 .log `git add -f` 补交（勿改动内容）＋重新发起**全量**验收；建议实施会话将 `traceability/**/*.log` 的 `git add -f` 固化进提交前自查清单（该家族已四次重演，见 G-3） |
| G-1 | 建议 | 293ef150 提交标题以名词"单元卡"开头非动词开头——ACC §4.9/AGENTS §6.3 字面要求未满足；F-012 家族同型再现（先例 F-214＝EX-T06、F-215＝EX-T07、F-218＝EX-T08 文档提交同型）。主实现提交 353b3362 合规 | 转登 **F-222**；对已推送历史不追溯改写（AGENTS §6.3）；与 F-012 家族合并处置 |
| G-2 | 建议 | F-031（open）的括注补正义务载体冲突：其要求"wp08-t09 领取者领取前补正 EX-T09.json acceptance 5 括注"，但该契约文件不在任务 allowedFiles 内——本任务实施者不改是合规的，义务事实上无人可合法履行；实体面（AT-34 载体＋ManualClock 纪律）已兑现 | 转请所有者裁决：由契约编译治理侧在 EX-T09 收尾（PIPE §4.7⑤ 契约翻转窗口）顺手修订该括注，或登记为批次口径修订（F-031 resolution 后半句既有路径）；本验收不翻转 F-031 状态 |
| G-3 | 建议 | `.gitignore *.log` 与留痕要求结构性冲突已四次导致同族 fail（CORE-T02/IO-T02/EX-T01/EX-T09），逐次人工 add -f 属重复成本 | 建议登记治理 finding：在 scripts/industrialrobot 提供留痕强制加入辅助（如 gate-all.ps1 产物清单 `git add -f` 收尾步骤），或对 `traceability/**` 目录加 `.gitignore` 负向豁免 `!*.log`，消除该家族重演土壤 |

## 裁决

- **verdict＝fail**：4.1~4.8/4.11 通过（含双模式构建、两目标四轮实跑、ird_gates 独立复核、3 变异 5 红的检出力验证），但 4.10 命中阻断级 B-1（留痕声称与在库不符——9 件 .log 未入库），按 ACC §5 fail 判据（"任一阻断级问题"）判 fail。
- 本记录即返工输入：修复路径单一且明确（`git add -f` 补交 9 件磁盘既有 .log→重新全量验收）；**无需重做任何实现或测试**。
- G-1/G-3 建议级已按 ACC §5 转登 findings.json（F-222＝G-1；G-3 新增登记 F-223），不独立阻塞；G-2 为对所有者的裁决请求（F-031 载体），非新 finding。
- 验收者未合入、未修改任何实施产物（worktree A 三处变异已精确还原并复跑全绿、`git status` 零残留）、未向任务分支 wp08-t09 推送任何提交。
- 本记录经 ACC §3 记录回传流程落 `acc/EX-T09/1` 分支并推送 origin 后，本次验收完成。
