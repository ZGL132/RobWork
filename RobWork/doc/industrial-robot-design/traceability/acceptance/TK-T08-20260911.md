# TK-T08 验收记录（对抗式验收）

| 字段 | 值 |
| --- | --- |
| 任务 ID | TK-T08（≙WP-02-T08，夹具与确定性环境 Fixture） |
| 契约 | tasks/foundation/TK-T08.json（status: ready，随收尾翻转 done） |
| 验收对象 | 分支 `wp02-t08`，head＝`5cfd4898fc313416700ee832c4f6cd45cacb70bc`（与 origin 一致——漂移检测 PASS），范围自 redesign-main 分叉点 397011a 起（2 提交：3c6659e 实现＋5cfd489 文档同步与证据） |
| 尝试序号 | attempt=1（evidence 分支 `acc/TK-T08/1`） |
| **verdict** | **pass**（4.1~4.11 全过；无阻断；ird_gates 仅 F-019 既有命中——非本任务变更面，见 4.6） |
| 验收日期 | 2026-09-11 |
| 独立性声明 | PIPE v1.7 §0.4 单会话降级；补偿＝worktree A（detached @ head 5cfd489）亲手复现（71/71）＋threadCount 下界放宽探针实证＋证据链前置自查（F-010 教训内化，日志 -f 入库） |

---

## 逐项证据（4.1~4.11）

| # | 检查 | 结论与证据 |
| --- | --- | --- |
| 4.1 | 改动范围 | diff＝Fixture.hpp/.cpp/gtest/GoldenFixture.hpp/FixtureTest.cpp/CMakeLists＋units/testkit.md＋traceability——allowedFiles 内；forbiddenFiles 零触碰 |
| 4.2 | 可复现构建 | worktree A 全新配置 exit 0（vcpkg 工具链）＋构建 exit 0＋测试 71/71 PASSED（验收者亲手，head 5cfd489）；集成全量 exit 0、错误行 0 |
| 4.3 | verify 命令 | `cmake --build build --config Release --target sdurws_ird_testkit_test` exit 0 |
| 4.4 | acceptance 逐条 | ①TK-FIX 生命周期六步：DemoFixture.SixStepLifecycle（①env 数据根→②数据集装载〔env 重定向 fixture 根〕→③容差档案 kin-fk@1.0.0→④确定性环境 seed=20260909/记录登记数据资产→⑤TempDir 就绪）＋PureFixture 纯算法形态（②③空态跳过）＋TempDir 三态/ReproRecord 往返与拒绝矩阵/DeterministicEnv 唯一来源 ✓；②keepOnFailure 现场保留：KeepsSceneOnFailureSignal（信号＋默认开关→保留→自清理）＋Disabled/NoSignal 两反例＋删除失败只告警不抛（Windows 句柄占用语义）✓ |
| 4.5 | 失败能力 | 实施段：keepOnFailure 决策禁用探针（Edit 精确编辑）→KeepsSceneOnFailureSignal 如期变红→恢复复绿（grep probe=0；一轮闭环无事故）；验收段：threadCount 下界 1→0 放宽（sed 行内无转义字符）→FromJsonRejectsInvalid_UT_FIX 如期变红→`git checkout` 还原→71/71 复绿（worktree A 亲手） |
| 4.6 | 红线 | 零 Qt；testkit 唯一单元依赖 core（库经既有链接，测试用 core::ContentDigester 公共头——R-2 合规）；库本体零 gtest（GoldenFixture 置 gtest/ 子目录——AssertMacros.hpp 分层先例，D-06 边界保持）；SA-02 零框架改动。ird_gates 仅 F-019 既有 2 处命中（lint 工具边，TK-T03 引入），本任务变更面零新增命中（ird-gates.log 留痕） |
| 4.7 | 注释 | 公共头全查（TempDir 生命周期/隔离三要素/失败保留语义；ReproRecord 默认值口径与 JSON 契约；DeterministicEnv"唯一来源"约束；GoldenFixture 六步逐步注释＋范围边界声明）；Fixture.cpp 实现要点与错误分支逐段中文说明；测试每用例追溯 TK-FIX/acceptance 条目 |
| 4.8 | 文档同步 | testkit.md v0.8 落位登记（接口零偏差＋noteTestFailure 增量接缝＋gtest 分层＋TK-T10 接缝预留/IRD_GIT_COMMIT 宏四条）；findings 核对：F-019 无新增（沿用），F-004/F-006/F-008/F-010/F-015~F-018 无涉（F-010 前置自查；F-018③"静默截断"教训在 fromJson 整数校验中内化） |
| 4.9 | commit 质量 | 两提交四段式＋Acc-Covered（1,2 / 3）；实施过程取舍如实入报告 notes |
| 4.10 | 留痕完整 | 13 份证据文件齐（日志×7＋XML×2＋报告＋validate-task/validate-docs/ird-gates）；ird-test-report/1 schema 合法 |
| 4.11 | 偷懒扫描 | 零命中；TempDir/ReproRecord/GoldenFixture 为实质实现（真实目录操作/JSON 序列化/六步编排，非 stub）；测试含正反例＋并行＋平台语义（Windows 句柄占用） |

## 问题清单

阻断级：无。建议级：无新增（⑥⑦/§7.2 分类归 TK-T10 接缝，已在 v0.8 登记）。
