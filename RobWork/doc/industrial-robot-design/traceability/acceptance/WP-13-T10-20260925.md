# WP-13-T10 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 ID | WP-13-T10（实现工具/场景/命名位姿对象编辑与命令；modeling 单元；需求 MDL-13/15/17） |
| 送验对象（冻结） | `0cd85cef9e6114acb4b5a1dfceef24c4aab74334`（分支 wp13-t10 尖端；漂移检查：`git ls-remote origin refs/heads/wp13-t10`＝该 SHA，无漂移——本记录全程按该 SHA 复现，不按分支名） |
| diff 范围 | `a2d1ae6cafb87c9ddc6e9b53770d3fdc3e922732..0cd85cef9e6114acb4b5a1dfceef24c4aab74334`（2 提交：91b99d48 实现＋0cd85cef 文档同步/留痕——与 WP-13-T09 先例同型双提交） |
| 验收分支／记录 | `acc/WP-13-T10/1`，本文件 |
| verdict | **pass**（4.1~4.11 全部通过；无阻断级问题；建议级 3 项转登 findings.json：F-342/F-343/F-344——均零行为影响，不阻塞合入） |
| 验收日期 | 2026-09-25 |

## 0. 独立性与执行环境声明（ACC §2/§3）

- 验收者为全新上下文会话，未消费实施者的任何过程叙述；只读代码、任务契约（`tasks/foundation/WP-13-T10.json`）、设计文档（`units/modeling.md` v0.12 @ 送验 SHA）与在库留痕；构建与测试全部亲手重跑（复现优先于采信）。
- **复现现场**：`git worktree add --detach <临时目录A> 0cd85cef…334`（detached @ SHA）；base 侧对照现场：detached worktree @ `a2d1ae6c…732`（gates 引擎直跑比对用）；记录回传现场：`git worktree add -b acc/WP-13-T10/1 <临时目录B> origin/redesign-main`。
- **集成模式**（A 内全新配置，主树缓存同口径）：`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DRWS_USE_STATIC_LINK_PLUGINS=ON -DCMAKE_TOOLCHAIN_FILE=<repo>/vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64` → CONFIGURE 生成完成零错误；缓存确认 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`。环境链缺口按 F-007/F-062/F-098/F-099 在案口径处置：`RobWorkStudio/bin/` 两份 ini 模板为 gitignore 的本机环境件，自主树复制进 worktree 后配置通过（属环境供给，非送验对象改动——A 内源树经 `git status` 确认零改动，突变测试还原后亦然）。
- **独立冒烟模式**（另一临时目录，`-S …/industrialrobot`＋vcpkg toolchain＋Qt CMAKE_PREFIX_PATH，AGENTS §4.1 修正口径）：配置"Generating done"零错误，全量构建 SMOKE_BUILD_EXIT=0、`error C|error LNK|error MSB` 计数 0。
- **宿主 GUI 冒烟**：本任务契约 verify 与 acceptance 均为计算库面（modeling 无界面目标，界面归 WP-13-T15），无 GUI 冒烟项——未申请桌面窗口授权，无未执行项。
- 验收者环境：Windows 10.0.26200 x64；Visual Studio 17 2022（MSVC 19.42）；Qt 6.11.1（msvc2022_64）；vcpkg 经典模式（主树 `vcpkg/installed/x64-windows`，gtest/pugixml 已装）。

## 1. 逐项证据（ACC §4，4.1~4.11）

### 4.1 改动范围 —— 通过

- `git diff --name-status a2d1ae6c..0cd85cef` 共 **26 文件**，逐一比对契约 `allowedFiles`：modeling 单元 14 文件（CMakeLists.txt、include 的 Parts/CommandHandlers/DiagCodes/RobotDesign 四头、src 的 Parts/CommandHandlers/DiagCodes/Readiness/RobotDesign 五件、test 的 PartsTest/CommandHandlersTest/DiagCodesTest 三改＋PartObjectCommandTest 一增）⊆ `modeling/**`；`traceability/builds/wp13-t10/` 11 件留痕 ⊆ `traceability/**`；`units/modeling.md` 在列。
- `forbiddenFiles`（REQUIREMENTS.md/ARCHITECTURE.md）零触碰；diff 无框架源码文件、`patches/` 零新文件（SA-02 通过）。

### 4.2 可复现构建 —— 通过（双模式零错误）

- 集成（A 内全新配置后逐目标构建，顺序执行）：`sdurws_ird_modeling` → **MODELING_EXIT=0**（产出 `build/RobWorkStudio/libs/Release/sdurws_ird_modeling.lib`）；`sdurws_ird_modeling_test` → **TEST_EXIT=0**；`sdurws_ird_modeling_contract_test` → **CONTRACT_EXIT=0**（三份日志 `error C|error LNK|error MSB` 计数均 0）。
- 冒烟（独立临时目录全新配置）：全量构建 **SMOKE_BUILD_EXIT=0**、错误计数 0；亲跑两 exe：`sdurws_ird_modeling_test` **130/130 PASSED**、`sdurws_ird_modeling_contract_test` **5/5 PASSED**——与在库 `smoke-modeling-test.xml`（130/0）、`smoke-modeling-contract-test.xml`（MdlBuildGraph 5）逐件一致。

### 4.3 契约 verify 命令 —— 6 条逐条亲手执行

| # | 命令 | 结果 |
| --- | --- | --- |
| 1 | `cmake --build build --config Release --target sdurws_ird_modeling` | exit 0，零错误 |
| 2 | `cmake --build build --config Release --target sdurws_ird_modeling_test` | exit 0，零错误；亲跑 exe **175/175 PASSED**（21 suites，含 `MdlPartObjectCommands` 14 条） |
| 3 | `ctest --test-dir build/RobWorkStudio/src/rwslibs/industrialrobot/modeling -C Release -L "^ird$"` | **2/2 PASSED**（modeling_test＋contract_test）；补亲跑契约 exe：**9/9 PASSED**——与在库 `integration-modeling-test.xml`（175/0）、`integration-modeling-contract-test.xml`（9/0）、`ird-test-report.json`（合并 184/184）逐件一致 |
| 4 | `cmake --build build --config Release --target ird_gates` | exit 1＝**存量命中（门禁按设计命中即 FATAL——基线侧同样 exit 1）**；按 F-336 建议采用**引擎直跑口径**取证：`cmake -DIRD_ROOT=<…industrialrobot> -DIRD_SELFTEST_DIR=<…> -P cmake/ird_gates.cmake`，head 侧（worktree @0cd85cef）与 base 侧（detached worktree @a2d1ae6c）命中码直方图**逐码一致**（IRD-GATE-SUB 57/R3 41/LIB 10/T1 5/R4 3/T2 1/R5 1/R1 1——码出现 119 次＝命中条目 56 条的展开计数，两口径均 head=base）→**归一化多重集 diff 为空＝零新增命中**，与在库 `ird-gates-base/branch-hits.txt`、`ird-gates-comparison.txt` 逐件一致（56 处全部为存量例外——ui Qt 面、testkit 边、io libzip/expat、modeling pugixml〔O-40〕等，处置归 DTB §4.5 治理通道，WP-13-T09 attempt 2 同口径先例） |
| 5 | `pwsh -File RobWork/scripts/industrialrobot/validate-task.ps1 -TaskFile …/WP-13-T10.json` | **PASS** (1 tasks)，exit 0 |
| 6 | `pwsh -File RobWork/scripts/industrialrobot/validate-docs.ps1` | **PASS** (20 units, 12 trace entries, 189 task files)，exit 0 |

### 4.4 acceptance 条目 —— 五条均有对应证据（全部为本次亲跑复现，具名用例）

| 契约 acceptance | 证据（亲手复现，集成 175＋9 内具名用例） |
| --- | --- |
| 1 值模型与编辑流（T_flange_tool m/rad、tcpList≥1、物性①②③同连杆、ObjectId 引用不复制、defaultTcp 引用校验） | `MdlParts.ToolTcpListIntegrity_IMdl13_WP13T10_ACC1`（空表/空键/重复键逐项违例＋**解码门强制**：空 TCP 表工具字节 encode 可过、decode 拒绝——双通道单判定）；`MdlPartObjectCommands.ToolAddPlansWithRootRefAndDualCompile`（T_flange_tool 装配、requiresDualCompile=true、根引用表增量、GeometryRef 资源引用无网格内嵌——MDL-13 引用面）；`…DefaultTcpReferenceValidationGatesToolApply` 三面（未设置/toolOid∉toolRefs/tcpKey 不在 tcpList→`MDL-READINESS-DEFAULT-TCP-INCOMPLETE` 阻断＋subject=被引工具；完整→Planned）；物性①②③与连杆同源：`InertiaMath.hpp` 单元私有单实现＋`…ToolPhysicsLayeredAssertionFace`（解码门拒 m≤0 载荷＋AssertionSuite 直证 `MDL-ASSERT-MASS-NONPOSITIVE` subject/比较三要素） |
| 2 引用保护（V-04/V-05；「删除」＝引用移除；跨域提示＋⑤事件） | `…V04RemoveDefaultTcpReferencedToolRejected`（移除被 defaultTcp 引用工具→RejectedHardAssert＋`MDL-REF-PROTECTED` subject=oid＋比较型 actual=1/expected=0＋拒绝零写入＋编译端口零调用）；`…V05RemoveUnreferencedToolIsReferenceRemoval`（恰好一次**根写入**、被移除对象零写入、inverse=根前一版本字节、摘要含"移除引用 1 项"＋"可能存在外部引用"）；`…SceneBatchMixedAddAndRemovePlansOnce`（批量混载一次根写入＋摘要跨域提示）；⑤事件面＝摘要提示面（观测点——事件发布归 project TxEngine 修订提交已验契约，单元卡 §14.6 v0.12 ⑤ 按 DTB §5.4 登记口径）；悬空检测归 requirements（注释与摘要文案双面钉住） |
| 3 SceneObject 约束（MDL-15） | `…SceneWorldPoseNotPreMultipliedByBasePlacement`（基座 Inverted 预设下写入字节 worldPose 逐元素等于输入——M-11 固连）；`…SceneRoleVocabularyMatchesPolicyTokens`（五值词表与 policy 侧 token 逐值同串＋词表外串不回解）；`…CollisionProfileHintRoundtripReportOnly`（往返保真＋④端口 resolveCalls=0——仅报告用途，策略权威归 policy） |
| 4 命名位姿（MDL-17、D-MDL-3） | `…NamedPosesMergePreservesReservedKeysAndSkipsCompile`（requiresDualCompile=false、无 `MDL-ASSERT-*` 产出、保留键 home/zero 原样保留且值逐位保留——V-27 建模侧、用户全集替换旧用户条目）；`…NamedPosesReservedKeyAndJointOrderRejected`（保留键越界/长度≠关节表→RejectedInvalidInput＋合法首建双写入）；`…PoseSetContentAbsentFromRootBytes`（同根不同位姿内容＝根字节逐位相同——不进 Description 的建模侧字节面）；`MdlParts.NamedPoseMergeReservedKeysAndJointOrder_WP13T10_ACC4`（字典序规范化/身份继承/四类违例轨道） |
| 5 三命令处理器 prepare 差异面（T08 基类复用；O-35 无点 token） | `…PayloadV2RemovalsRoundtripAndDamage`（v2 往返逐字段相等＋截断/尾随字节拒收；v1 魔数拒收由 `MdlCommandHandlers.PayloadCodecRoundtripAndDamage` 钉住）；apply-tool-definition 写入/移除二选一（混载/未知对象/token 错配→`…RemoveUnreferencedOrUnknownObjectRejectedInvalid` 等拒收面）；apply-scene-objects 批量增/改/移除混载（`…SceneBatchMixed…`）；apply-named-poses 合并流（上条）；命令 token 亲查 `CommandHandlers.hpp:127/131/135`＝无点形态（O-35）；T08 基类复用＝prepare 公共段（保护门③.6→断言分域→计划最终化）＋契约测试 `MdlCommandPipelineContract` 9/9 不变通过；AT-28 建模侧断言＝三用例 `IRD_TEST_INFO` 带 AT-28 观测编号；golden roundtrip 面契约原文显式随 WP-13-T13/T16——契约声明式延后，非缩水 |

无"未执行标通过"项；`ird_gates` 已按引擎直跑口径真实执行（见 4.3 第 4 条）。

### 4.5 测试真实失败能力 —— 通过（两处变异均致红，注入后已还原）

在 detached worktree A 内注入（各自重建→亲跑→`git checkout --` 还原；还原后 `git status --porcelain`＝零改动、重跑复绿 **175/175**）：

- **M1**：`CommandHandlers.cpp:488` `assertReferenceProtection` 门禁用（首分支条件改 `if (true)` 直接返回）→ **1 FAILED**：`MdlPartObjectCommands.V04RemoveDefaultTcpReferencedToolRejected`（恰为引用保护面的具名用例——诊断断言 actual/expected 与 code 落空）；
- **M2**：`Parts.cpp:245` `mergeNamedPoseEntries` 保留键保留分支禁用（`if (false)`）→ **2 FAILED**：`MdlPartObjectCommands.NamedPosesMergePreservesReservedKeysAndSkipsCompile`＋`MdlParts.NamedPoseMergeReservedKeysAndJointOrder_WP13T10_ACC4`（V-27 保留键面在命令层与值模型层双双致红）。

### 4.6 架构红线 —— 通过（AGENTS §5 八条）

1. **L2 零 Qt**：modeling include/src/test grep `Qt|Q_OBJECT` 零命中；门禁命中集 head=base 零新增（Qt 面命中全部为 ui 单元存量）。
2. **R-1 业务域互链**：include/src 无 `ird/<requirements|kinematics|trajectory|dynamics|selection|optimization>` include（亲查 sort -u 全量 include 清单）；消费面＝core/policy/project/testkit 公共头（六类端口与登记边）。
3. **R-2 跨单元私有头**：全部跨单元 include 落至各单元 `include/sdurws/ird/<unit>/`（core 4＋policy 4＋project 2＋testkit 1 逐一存在性亲查）；单元内私有头仅 `InertiaMath.hpp`（同单元 src 内——I-MDL-5 单一实现防漂移）与测试域 `CommandFixtures.hpp`。
4. **SA-02 框架零修改**：diff 无框架文件、patches/ 零新文件（4.1）。
5. **R-4 名称前缀**：diff 新增行无 "RobWork" 名称前缀拼接/剥离逻辑（grep 亲查零命中）。
6. **PA-1 权威唯一**：两稳定码归 DiagCodes 登记簿（§9.5 表尾追加＋`IDiagnosticRegistry` 装配注册，产码经描述符不私定）；就绪 L7 判定收敛至 AssertionSuite::assertDefaultTcp 单一实现（呈现级 addNote 退役——NFR-MNT-04）；写路径仍归 project（处理器只产 CommandPlan）。
7. **PA-2 不可变历史**：移除＝仅根写入、被移除对象零写入、inverse=根前一版本字节（V-05 具名用例钉住）；拒绝零写入（V-04 钉住）。
8. **knownPitfalls O-35**：三命令 token 无点形态不变（CommandHandlers.hpp:127/131/135 亲查；契约 note 明示"三命令 token 即最终形态"）。

### 4.7 注释规范 —— 通过（公共头全查＋实现文件 4 件抽查）

- **公共头全查**：`Parts.hpp`（文件头设计依据/背景说明齐；四 struct 类注含业务概念/所有权与线程约束；逐字段单位 m/rad/kg/kg·m²、坐标系 {F}/世界系、确定性声明；mergeNamedPoseEntries 三段语义与参数方向/返回/错误轨道完整）；`CommandHandlers.hpp`（T10 增量文件头注＋assertDefaultTcp/assertReferenceProtection/PayloadRemovalSlot/DecodeOutcome.removedRefs 契约注释完整——检查面/触发条件/参数方向/线程约束齐）；`DiagCodes.hpp`（两新码常量注：分类/级别/paramSchema/增登依据/登记版本齐）；`RobotDesign.hpp`（IMdl13 枚举行注）。
- **实现文件抽查**：`Parts.cpp`（值相等口径/两码不预建纪律/合并流三段逐步注释＋"为什么"）、`CommandHandlers.cpp`（保护门先于断言的理由、候选播种修复的缺陷面说明、摘要四段确定性序、Restore 恒空 removals 语义）、`DiagCodes.cpp`（逐字段登记口径）、`Readiness.cpp`（coded 化收敛与单一判定面依据）——§2.5 高危信息（单位/坐标系/rad/所有权/线程/错误语义/确定性来源）逐项在注。
- **测试**：新测试 TU 文件头按 ACC1~5 契约逐条自证＋每用例 `IRD_TEST_INFO` 需求/AT 追溯。
- 注释滞后一处（零行为影响）→ 建议级 **F-342**（DiagCodes.hpp 工厂 @brief 计数"当前 20 项"未随 T10 两码增登同步为 22——DiagCodes.cpp 文件头注与 DiagCodesTest 22 码封闭断言本已正确），不构成本项不通过（AGENTS §2"注释随代码更新"的轻微违反，非"缺必填项"；F-316/F-339 同族再现，转登跟踪）。

### 4.8 文档同步与 findings 消账核对 —— 通过（3 项建议级转登）

- **实现与任务卡零偏差＋实现细化已按 DTB §5.4 登记**：modeling.md v0.12（§3.3 T10 行编辑流落位、§4.10 I-MDL-13 编号化补行、§9.5 两码实现期增登〔表尾追加纪律〕、§14.6 v0.12 九点变更记录——载荷 v2/removals、AssertionSuite 两断言、候选播种修复〔isNew 路径基线未播种＝潜伏缺陷随 T10 断言显形，修复已登记〕、⑤事件观测面口径、Debug 冒烟 SEH 注记）；validate-task/validate-docs 双 PASS。契约状态翻转（ready→done）与队列移出归收尾段（PIPE §4.7⑤），非实施侧义务——不在本记录扣分面。
- **findings.json 未关闭条目逐条核对**（在册至 F-341；F-318~F-320/F-331~F-334 编号空洞，无此条目）：
  - **F-337/F-339/F-340/F-341：已 fixed**（T09 返工 12284f72 修复——主线登记册状态亲查确认，与本任务送验面无残留关联；本次复现 175/175＋9/9 全绿为修复有效性的旁证）；
  - **F-338：维持 open 属预期**（T09 契约 verify 缺 contract_test 构建行；本次确认 **T10 契约同款缺口在案**→家族新实例转登 **F-344**；本次验收按 interim 口径亲手补建目标后取得 ctest 2/2 全量结果）；
  - F-310/F-314（T06/T07 契约 verify 缺行同族）、F-311/F-312（散文计数失准 T06/T07 实例）：open，所有权在契约编译/实施侧后续窗口——与本次送验面无可消账项；其中 **F-311/F-312 家族在本次送验面再现**（提交正文与 §14.6 v0.12 ⑦ 均写"MdlPartObjectCommands 12 条"，testsuite 实为 **14** 条；175=159＋14＋2 自洽，权威留痕 XML/报告数字全对、仅散文低估）→转登 **F-343**；
  - F-316（DiagCodes.hpp 工厂 @brief 计数家族）：open——**本次再现**（@brief 仍 20 项/枚举止于 T09，工厂实 22 项）→转登 **F-342**；
  - F-321~F-325/F-327~F-330（UI 宿主冒烟取证管线/勘误/UX 语义族）、F-335（消账回写治理）、F-336（gates MSBuild 捕获）：与 modeling 命令域无关联或为治理侧所有权；本次按 F-336 建议采用引擎直跑口径完成比对；F-323（全新树 RobWorkSim 目标编译失败）与本验收无关（本验收逐目标构建 industrialrobot 目标，不触达 RobWorkSim）；
  - 无 open 条目声称由本任务修复而实际未修复——无静默积压。

### 4.9 commit 质量 —— 通过

- 两提交 `91b99d48`（实现）/`0cd85cef`（文档同步＋留痕）——均 `[WP-13-T10]` 前缀＋动词开头；正文四段（What/Why/验证/影响面）齐备；"实现＋文档同步"双提交与 WP-13-T09 先例同型。
- 验证段结论与本次复现逐项一致：集成 175/175＋9/9（ird-test-report.json 合并 184/184）、冒烟 130/130＋5/5、ctest 2/2、validate 双 PASS、门禁 base..branch 归一化多重集比对零新增——**无粉饰**。
- 一处散文计数失准（"12 条"实为 14）→ 建议级 **F-343** 转登（权威留痕如实，非粉饰）。

### 4.10 留痕完整 —— 通过

- `traceability/builds/wp13-t10/` 11 件：集成 gtest XML 双件（175/0＋9/0）＋测试日志双件（尾部 175 PASSED/9 PASSED——与本次亲跑逐字一致）＋`ird-test-report.json`（summary 184/184，decisive=true——F-340 修复后的全量合并口径延续）＋冒烟 XML 双件（130/0＋5）＋`smoke-ird-test-report.json`（130 条全量——与 T09 验收先例同形态：契约 5 条以独立 XML 承载）＋gates 三件（base/branch 直方图逐码一致＋comparison 口径说明）。
- `phase-one-validation.log` 为 WP-03 时代聚合件、`resultNote` 归流水线状态——T08/T09 均未触碰，非本任务义务；任务级留痕以 builds/wp13-t10/ 落位，口径与先例一致。

### 4.11 偷懒/缩水实现扫描 —— 通过

- ① diff 新增行 `TODO|FIXME|XXX|HACK|stub|placeholder|未实现|not implemented` 扫描**零命中**；契约 acceptance 第 5 条原文显式声明"golden roundtrip 面随 WP-13-T13/T16"——契约声明式延后豁免，逐条列据成立。
- ② 空实现检测：新公共接口（`isReservedPoseKey`/`mergeNamedPoseEntries`/`checkInvariants(ToolDefinition)` 增核/`assertDefaultTcp`/`assertReferenceProtection`/`PayloadRemovalSlot` 编解码）函数体均为真实实现（亲读源码；两处突变致红旁证非恒真）。
- ③ 逐条证据表：见 4.4——五条 acceptance 每条均有具名测试/命令输出/文件行号级证据。
- ④ 测试强度：断言校验行为与边界（拒绝面 outcome+code+subject+comparison 四层、写入面逐字节解码回读、零写入/端口零调用负断言、往返逐字段相等、损伤面截断/尾随），非恒真非"只编译通过"；与 4.5 联动的两处突变均致红。

## 2. 发现的问题清单（分级）

- **阻断级：无。**
- **建议级（3 项，已转登 `traceability/findings.json`，编号顺延自 F-341）**：
  - **F-342**：DiagCodes.hpp `modelingCodeDescriptors` @brief 计数失实（"当前 20 项"＋枚举止于 T09，工厂实 22 项——DiagCodes.cpp 头注/DiagCodesTest 已正确，纯 .hpp 注释滞后）——F-316/F-339 家族再现；所有者：实施者侧（后续 modeling 任务顺手消账）。
  - **F-343**：散文计数失准——实现提交 91b99d48 正文与 modeling.md §14.6 v0.12 ⑦ 均写"MdlPartObjectCommands 12 条"，实际 14 条（XML tests="14"；175=159＋14＋2 自洽；权威留痕数字全对）——F-041/F-080/F-236/F-249/F-256/F-311/F-312 家族再现；所有者：实施者侧（后续文档同步窗口勘误；已推送提交不可改写，留档即可）。
  - **F-344**：WP-13-T10 契约 verify 数组缺 `sdurws_ird_modeling_contract_test` 构建行——F-300/F-304/F-310/F-314/F-338 家族再现（F-338 为 T09 同款、维持 open 属预期）；本次按 interim 口径补建后全量执行（ctest 2/2）；所有者：契约编译/治理侧。

## 3. 裁决

**verdict＝pass**：4.1~4.11 全部通过，无阻断级问题；建议级 3 项（F-342/F-343/F-344）随本记录转登登记册，不阻塞合入。送验对象＝SHA `0cd85cef9e6114acb4b5a1dfceef24c4aab74334`（非分支引用）；合入决策归所有者（ACC §6）。
