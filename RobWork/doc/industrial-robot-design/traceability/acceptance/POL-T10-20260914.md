# POL-T10 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | POL-T10 实现诊断构造（建议码表＋IPolicyDiagnostics）（unit=policy，masterWp=WP-07-T10） |
| 送验对象（冻结） | `de9119cce6891f4aba3cd35f59617e6564262e61`（分支 wp07-t10；`origin/wp07-t10` 经 rev-parse 核对＝该 SHA——无分支漂移，ACC §3） |
| diff 范围 | `5478bc51373e3a3048e62b13d509fd11afc92592..de9119cce6891f4aba3cd35f59617e6564262e61`（state.base＝origin/redesign-main 尖端＝base，与验收请求下发范围一致；4 个实现/留痕提交：58f53c56→4cff9a02→e5b0e5fe→de9119cc） |
| 验收协议 | acceptance-protocol.md v1.5（ACC），§4 全部 11 项逐项出证据 |
| 裁决 | **pass**（4.1~4.11 全部通过；无阻断级问题；建议级 2 条已随本记录分支转登 `traceability/findings.json` F-088/F-089，不阻塞合入） |
| 验收环境 | detached worktree `D:\10_Source_Repos\21_robot\acc-worktrees\pol-t10-a` @ de9119cc（送验 SHA，`git worktree add --detach`）。集成模式全新配置：`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=<仓库根>/vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`（F-062 登记的 worktree 复现前置——toolchain/Qt 前缀＋两件 RobWorkStudio.ini 本地资源从主树复制至 worktree `RobWork/RobWorkStudio/bin/`），配置 exit 0（36.7 s）、缓存 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`（grep 实证）；冒烟另配 `D:\tmp-acc-pol-t10\build-smoke`（带 vcpkg toolchain，AGENTS §4.1）。全部构建/测试/门禁/探针由验收者亲手执行，未采信实施者运行结果（ACC §2）；验收者日志目录 `D:\tmp-acc-pol-t10\`（configure-integration.log／verify-target-build.log／integration-fullbuild.log／smoke-configure.log／smoke-build.log／acc-integration-run.log／acc-smoke-*.log／acc-gates-run.log／acc-validate-task.log／probe1-*／probe2-*／restore-build.log／acc-integration-rerun.log） |
| 记录分支 | `acc/POL-T10/1`（attempt=1，验收请求下发序号） |

---

## 4.1 改动范围 — PASS

`git diff --name-status 5478bc5..de9119cc` 共 14 个变更文件，逐一对照契约 `allowedFiles`（policy/**、industrialrobot/CMakeLists.txt、units/policy.md、traceability/**）：

| 文件 | 状态 | 归属 |
| --- | --- | --- |
| `industrialrobot/policy/CMakeLists.txt` | M | `policy/**` ✓（产品库增列 src/Diagnostics.cpp；测试目标增列 test/DiagnosticsTest.cpp＋链接 sdurws_ird_testkit） |
| `policy/include/sdurws/ird/policy/Diagnostics.hpp` | A | `policy/**` ✓ |
| `policy/src/Diagnostics.cpp` | A | `policy/**` ✓ |
| `policy/test/DiagnosticsTest.cpp` | A | `policy/**` ✓ |
| `policy/src/CollisionQuery.cpp` | M | `policy/**` ✓（CLL 四值字面量迁码表） |
| `policy/src/Compatibility.cpp` | M | `policy/**` ✓（VERSION-INCOMPATIBLE 迁码表） |
| `policy/src/Errors.cpp` | M | `policy/**` ✓（registryCode 改经 diagCodeOf 委托码表） |
| `policy/src/JointLimits.cpp` | M | `policy/**` ✓（JNT 两值＋CLL 两值迁码表） |
| `policy/src/PolicyParsing.cpp` | M | `policy/**` ✓（INFO-DEFAULT-APPLIED 迁码表） |
| `policy/src/PolicyPort.cpp` | M | `policy/**` ✓（OBJECT-MISSING 迁码表） |
| `traceability/builds/wp07-t10/ird-test-report.json` | A | `traceability/**` ✓ |
| `traceability/builds/wp07-t10/sdurws_ird_policy_test-integration.xml` | A | `traceability/**` ✓ |
| `traceability/builds/wp07-t10/sdurws_ird_policy_test-smoke.xml` | A | `traceability/**` ✓ |
| `doc/industrial-robot-design/units/policy.md` | M | allowedFiles 显式列出 ✓（§9.6 码表落位注记＋§15.4 v0.11 行） |

- `forbiddenFiles`（REQUIREMENTS.md / ARCHITECTURE.md）：diff 中零触碰 ✓。
- 框架源码（RobWork/、RobWorkSim/、RobWorkStudio 其余）：diff 范围零文件；`industrialrobot/patches/` diff 为空（无新登记，SA-02 无涉）✓。
- 契约允许而未触碰的文件（industrialrobot 顶层 CMakeLists.txt）：未触碰，不构成越界。

## 4.2 可复现构建 — PASS

- **集成模式（worktree A 全新树）**：配置 exit 0（36.7 s），`grep RWS_BUILD_INDUSTRIALROBOT build/CMakeCache.txt` → `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON` 实证。日志 `D:\tmp-acc-pol-t10\configure-integration.log`。
  - 契约 verify 目标 `cmake --build build --config Release --target sdurws_ird_policy_test` **从零编译 exit 0**、日志零 `: error` 行，产物 `build/RobWorkStudio/bin/Release/sdurws_ird_policy_test.exe` 落位（日志 `verify-target-build.log`）。
  - 全树 `cmake --build build --config Release` exit 1＝**73 处 error 全部位于 RobWorkSim 侧插件**（RWSimulatorPlugin.vcxproj 71＋bt_plugin.rwplugin.vcxproj 2；出错头文件聚集 VelocityScrew6D.hpp×63／Math.hpp×8／Parallelizer.h×2——与 findings **F-008** 登记的框架基线 min/max 宏污染簇逐项同分布；`grep "error" | grep -ci "ird\|industrialrobot"`＝**0**，ird 面零错误）。按 F-036/F-054 家族既定处置口径以任务目标面从零构建补强（日志 `integration-fullbuild.log`）。
- **冒烟模式**：`cmake -S .../industrialrobot -B D:\tmp-acc-pol-t10\build-smoke -DCMAKE_TOOLCHAIN_FILE=<vcpkg>` 配置 exit 0＋`cmake --build ... --config Release` exit 0（日志内 "error" 命中 5 处均为 Errors.cpp/ErrorsTest.cpp 文件名，零真实编译错误；日志 `smoke-configure.log`／`smoke-build.log`）。
- **测试（验收者亲手运行）**：
  - 集成 `sdurws_ird_policy_test`：**200/200 PASSED**（`[==========] 200 tests from 38 test suites ran`；探针还原后复跑仍 200/200）——与送验主张 200/200（181 既有＋19 新增）一致。
  - 冒烟全量六目标：core 61＋evidence 205＋evidence_contract 14＋**policy 131**＋runtime 176＋testkit 97＝**684/684 零失败、全部 exit 0**——policy 冒烟 131/131 与送验主张一致（112 既有＋19 新增）。
- **门禁 `ird_gates`**（验收者亲手跑 `cmake --build build --config Release --target ird_gates`）：exit 1＝**6 处命中**——SUB×4（evidence_test／evidence_contract_test／**policy_test**／runtime_test → testkit）＋SUB×1（testdata_lint→testkit 表外依赖边）＋T1×1（testdata_lint 产品目标链接 testkit），即 **base 既有 5（F-019/F-051/F-072 逐码一致）＋本任务新增 1（policy_test 直链 testkit）**；IRD-GATE-R4 零触发；引擎自测 fail_r1/t1/r5/sub/r3/r4/t2 按预期检出、pass_clean/pass_r4comment 干净通过（日志 `acc-gates-run.log`）——与送验"6 命中＝base 5＋新增 1、如实登记不私改门禁"主张一致。

## 4.3 契约 verify 命令 — PASS

契约 `verify` 数组仅 1 条：`cmake --build build --config Release --target sdurws_ird_policy_test`——验收者在 worktree A 全新树亲手执行，exit 0（见 4.2）。无其他条目漏执行。

## 4.4 acceptance 条目逐项核对 — PASS（1/1 有证据）

| # | 契约条目 | 证据（具名测试/输出） |
| --- | --- | --- |
| 1 | checkDiagnosticRecord 族（testkit）码表与不变量用例通过 | **码表半场（PolicyDiagCodeTable，5 用例）**：`RegisteredCodesCoversWholeTable_SEC9_6`（29 值全表逐串逐位、kPolicyDiagCodeCount 钉住）、`RegisteredCodesDeterministicAcrossInstances_NFR_COR_02`（跨实例逐字节相同）、`NoDuplicateCodes_SEC9_6`（零重复）、`AllCodesMatchCoreSyntax_CORE_C3`（全表 ^[A-Z0-9]+(-[A-Z0-9]+)*$ 且 ≤64）、`RegistryCodeIntersectionConsistent_NFR_MNT_03`（异常轨 19 值 registryCode 逐串为码表成员——两表面防漂移）。**构造不变量半场（testkit 真实消费）**：`StableRecordPassesContractCheck_ERR_01`（checkDiagnosticRecord 通过＋字段逐一承载＋不发明 comparison/localName/runtimeName）、`SubjectOptionalIntegrityCheckedByConsumer_ERR_01`（allowTransient=false 判违规/true 判通过——校验器真实语义分岔）、`RejectsUnknownCodeFailFast_SEC9_6`（表外码/小写变体/近邻变体全部 PolicyError＋异常消息携带违规原文）、`RejectsBlankRequiredStrings_CORE_C3`（三必填串逐一）、`ComparativeRecordPassesContractAndComparativeChecks_UX_03`（checkDiagnosticRecord＋checkComparativeFields 双校验、6π/4π rad 逐位）、`RejectsComparisonWithInvalidUnit_UX_03`（无效句柄拦截）、`NotApplicableSidePassesCheckWithoutForgedValue_P_POL_2`（取值恒空）、`InvalidSidePreservesRawText_NFR_COR_03`（"nan" 原文）、`DeterministicConstruction_NFR_COR_02`（逐字段相等）。**辅助＋多态**：MakeComparativeHelpers 4 用例＋PolicyDiagnosticsInterface.PolymorphicUseThroughBaseReference_SEC9_6——集成 200/200、冒烟 131/131 内全部执行通过。**testkit 真实链接实证**：DiagnosticsTest.cpp include `<sdurws/ird/testkit/Check.hpp>`＋`<sdurws/ird/testkit/ContractCheck.hpp>`（checkDiagnosticRecord 声明位于 testkit ContractCheck.hpp:47），测试目标 target_link_libraries 增列 sdurws_ird_testkit，测试 exe 二进制内检出 "checkDiagnosticRecord 失败"/"allowTransient" 字符串；SubjectOptional 用例的稳定/瞬时口径分岔只有真实校验器才能产生 |

无任何条目虚标通过；ird_gates 结论如实登记为"6 命中＝base 5＋新增 1"（非零命中粉饰为通过），并以单元卡 §15.4 v0.11 ⑦ 登记"数据面与引擎矛盾、转所有者/WP-01 裁决"。

## 4.5 测试真实失败能力 — PASS（双探针，F-075 纪律：mutation 落 .cpp、git checkout 还原后重建复绿）

对实现文件 `src/Diagnostics.cpp` 注入两处 mutation（改后重建目标→跑用例→`git checkout` 还原→重建复跑）：

1. 探针 1：make() 第一步码表核对 `if (!isRegisteredPolicyDiagCode(code))` 改 `if (false && ...)` → **1 用例 FAILED**：`PolicyDiagnosticsMake.RejectsUnknownCodeFailFast_SEC9_6`（表外码 fail-fast 拦截面被真实验证；同套件其余 8 用例仍绿——探针精确落点）。
2. 探针 2：makeComparativeValue 有限分派 `if (std::isfinite(value))` 取反 → **4 用例 FAILED**：`ComparativeRecordPassesContractAndComparativeChecks_UX_03`／`InvalidSidePreservesRawText_NFR_COR_03`／`FiniteValueProvidesWithProvenance_DYN_06`／`NonFiniteValueInvalidatesWithRawText_NFR_COR_03`——恰为消费 Provided/Invalid 分派语义的四个用例，正反两侧语义均被真实验证。

还原后 `git status --porcelain` 零残留（探针仅存在于临时 worktree 未提交工作区，送验冻结对象 de9119cc 在 git 对象库中全程未被修改），重建复跑 **200/200 复绿**。断言非恒真、非"只编译通过"。

## 4.6 架构红线（AGENTS §5 八条） — PASS

1. **L2 零 Qt**：三个新文件 grep `#include <Q…/QtCore/QtWidgets/QtGui` 零命中 ✓。
2. **R-1 业务域互链**：新文件 sdurws/ird include 面仅 `ird/core/*`、`ird/policy/*`（本单元）＋测试 TU 的 `ird/testkit/*`（测试设施，§11 原文指定；仅测试目标链接，产品库零 testkit 依赖）＋std ✓。
3. **R-2 跨单元私有头**：include 均为 `include/sdurws/ird/<unit>/` 公共路径 ✓。
4. **SA-02 框架零修改**：4.1 已证（patches/ 无新文件）✓。
5. **R-4 名称前缀拼接/剥离**：三个新文件双引号对携带 "RobWork" 子串的字面量 grep 零命中；ird_gates R4 实测零新增触发 ✓。
6. **权威唯一（PA-1）**：本单元只产出**建议码**——policyDiagCode switch 表注释与文件头明文"码值权威＝diagnostics StableCodeRegistry（PA-1 不越权注册）"；`make()` 不注册任何码、不实现 sink、不裁决 P-PR-6/P-EX-8 的 sink 统一（knownPitfalls 处置与契约 note 一致）；Errors.cpp 的 registryCode 经 diagCodeOf 映射委托同一码表（全枚举无 default，NFR-MNT-03 单一字面量点），未新增/改写任何映射 ✓。
7. **不可变历史（PA-2）**：本任务无修订写路径涉足，无删除/改写通道 ✓。
8. **不恢复 old/**：新文件无 old/ 引用（grep 唯一命中为注释内 "threshold/ 家族"的字节子串 "hold/"，误报）✓。

## 4.7 注释规范（AGENTS §2） — PASS

- **公共头全查**（Diagnostics.hpp，431 行）：文件头（设计依据 §9.6/§3.1/§12＋"为什么要有这一层"背景＋与 Errors.hpp 两码面关系＋P-PR-6/P-EX-8 陷阱处置登记＋线程安全/确定性声明）、三实体逐一定义处中文 Doxygen 齐备（枚举 29 值各带建议码原文与来源行）；§2.5 高危信息逐项在位——单位/坐标系（本面无数值几何，不适用处如实不标；rad 作样例单位显式标注）、所有权（返回串静态存储期"调用方无需释放"、表只读引用无所有权转移）、线程约束（码表编译期冻结只读、实现无状态可并发共享、make() const 纯函数可重入）、错误语义（表外码/空串/无效单位＝调用方契约违约 fail-fast→PolicyError；CoreError 防御性转译不吞错——异常分类逐处标注）、确定性来源（编译期固定 switch、表序＝枚举序、一经交付只许表尾追加、无时钟无随机源）✓。
- **实现文件**（Diagnostics.cpp，195 行，全读）：文件头（含"字面量定义在头"的原因说明）；make() 四步核对逐步注释（每步业务含义＋失败走向＋radUnit()/转译总纲先例出处）；魔法数解释（%.17g round-trip 与 "C" locale、0xFFFF 无效句柄、29 项定长小表线性扫描理由）✓。
- **测试文件**（DiagnosticsTest.cpp，455 行，全读）：文件头含用例追溯命名约定＋替身边界声明（零替身）＋testkit 链接落位说明（POL-T11 前置）；每个 TEST 注明验证的条款（§9.6/ERR-01/UX-03/C-3/P-POL-2/NFR-COR-02/03/DYN-06）；断言处行内中文说明（EXPECT_* 后 << 消息）✓。
- 五个迁移 TU（CollisionQuery/Compatibility/Errors/JointLimits/PolicyParsing/PolicyPort）的注释同步改动随 diff 逐处核查——迁移状态说明与代码事实一致（AGENTS §2.1⑤ 注释随代码更新）✓。本任务新增 3 文件全部通读＋6 个修改文件 diff 全查，超出"实现文件抽查 ≥3 个"覆盖面。

## 4.8 文档同步＋findings 核对 — PASS

- 实现与任务卡**零语义偏差**；实现增量（码表全集 29 值构成与 POLICY-THRESHOLD-REQUIRED-MISSING 补登、五 TU 字面量迁移兑现、make() 四步核对、testkit 链接落位、门禁新增命中的根因分析与转裁决）已按 DTB §5.4 以 **units/policy.md §9.6 注记＋§15.4 v0.11（2026-09-14）** 增量修订登记（diff 实证 +8 行）——"代码先行、文档失实"不存在。
- 卡行对照：§12 POL-T10 行（产物 `Diagnostics.hpp/.cpp`＝建议码表＋IPolicyDiagnostics、依赖 POL-T02、验证 checkDiagnosticRecord 族（testkit）、完成条件"码表与构造不变量用例通过"）与交付一致；§9.6 冻结签名（registeredCodes/make 含缺省 comparison 参数）逐字落地；契约 knownPitfalls（P-PR-6/P-EX-8）处置与 §10.6 交接一致。
- `validate-task.ps1 -TaskFile POL-T10.json` 验收者亲手复跑（worktree A）：**PASS (1 tasks)** exit 0；`phase-one-validation.log:36` 含 `POL-T10.json: validate-task: PASS (1 tasks)` 既有条目 ✓。
- **findings.json 未关闭条目相关性核对**（v1.2 要求，基于 5478bc5 版本共 87 项）：
  - **F-075/F-068（探针纪律家族）**：本验收双探针均落 .cpp、git checkout 还原（新 mtime）＋重建复绿＋零残留核验，按其处置执行；
  - F-076/F-079/F-082/F-084/F-086（提交标题名词短语家族）：本任务 4 提交标题均 `[WP-07-T10]`＋动词开头（实现/收敛/补充/同步），家族未再踩；
  - F-010/F-077/F-080/F-081/F-083/F-085/F-087（留痕 .log 被 .gitignore 吞掉/正文清单与入库出入家族）：**本任务复踩该家族**——de9119cc 正文列举 8 件 .log 留痕实际零件入库（见问题清单 G-1→F-088）；
  - F-025/F-041/F-073（计数失实家族）：**本任务复踩该家族**——"6 套件 19 用例（码表 6＋构造 8）"实测为 4 套件（码表 5＋构造 9）（见 G-2→F-089）；
  - F-019/F-051/F-072（ird_gates 白名单/引擎 testkit 矛盾家族）：本验收实测 6 命中与三者登记逐一吻合，新增 SUB（policy_test→testkit）为同一根源第 3 处数据点——实施者已如实登记并转所有者/WP-01 裁决，维持原归口不重复立案；
  - F-004/F-007/F-008/F-009/F-062（构建环境/框架基线家族）：本验收环境前置即按 F-062 口径执行、全树构建实测止于 F-008（73 error 分布与其登记逐项一致）——均维持原归口；
  - F-078（policy.md 转义损毁）：本任务 diff 仅涉 §9.6/§15.4，未触碰损毁节，维持 DOC 治理归口。
  - **无历史发现被静默积压；本任务可顺手消账项：无。**

## 4.9 commit 质量 — PASS

范围 4 提交逐份核查：

- 标题均 `[WP-07-T10]` 前缀＋动词开头：`实现 policy 诊断构造（建议码表＋IPolicyDiagnostics）`／`收敛全单元建议码字面量至 Diagnostics 码表`／`补充诊断构造用例（testkit checkDiagnosticRecord 族）`／`同步 policy 单元卡并登记任务验证留痕`——未踩 F-012 家族。
- 正文四段（改动内容／改动原因／验证／影响面与兼容性）齐备，`Acc-Covered` 逐提交如实（中间步标"无/实现中间步"，留痕提交标"1 证据面补全"）。
- 验证结论与留痕核对：集成 200/200（181＋19）、冒烟 policy 131/131（112＋19）、ird_gates 6 命中＝base 5＋新增 1（含根因分析与转裁决处置）、validate-task PASS、契约 verify 目标通过——**全部经验收者亲手复现准确，无粉饰**；双探针实证断言有效。
- 两处散文计数/清单失实（不属粉饰：总数与结论均正确、方向为归并口径错位与 .log 未入库）见 G-1/G-2，建议级转登。

## 4.10 留痕完整 — PASS（附建议级 G-1）

- 在库证据本体：`traceability/builds/wp07-t10/` 双模式 gtest XML（integration `tests="200" failures="0"`／smoke `tests="131" failures="0"`，顶格实测，integration 38 套件 200 testcase 清点与验收者复跑一致）＋ird-test-report.json（目标 notes／testSuites／regression／gates 全节；码表 5 用例逐条枚举 trace，其余套件以计数引用）。
- resultNote 口径：ird-test-report.json 即本任务执行级结果登记（ACC 4.10"phase-one-validation.log 或 resultNote 已更新"满足）；validate-task 面由库内 `phase-one-validation.log:36` 既有 PASS 条目覆盖（验收者复跑 PASS 一致）。
- **G-1（建议级）**：de9119cc 正文声称 traceability/builds/wp07-t10/ 下登记**全套 11 件留痕**（verify-target-build.log／smoke-configure.log／smoke-build.log／integration-test-run.log／smoke-test-run.log／validate-task.log／gate-all.log／gates-current-wp07t10.log 等 8 件 .log＋2 XML＋1 JSON），`git ls-tree de9119cc` 实证**仅 3 件入库**（2 XML＋JSON），8 件 .log 全部被 `.gitignore:87` `*.log` 规则吞掉（`git check-ignore -v` 实证）；ird-test-report.json 的 `validateTask`（"PASS（validate-task.log）"）与 gates 字段亦引用未入库 .log——F-010 根因**第六次踩中**（F-081 第二次、F-083 第三次、F-085 第四次、F-087 第五次之后），"提交正文/报告字段引用在库文件而实际未入库"形态。判级沿 F-081/F-083/F-085/F-087 先例：证据本体（双模式 XML＋report）完整且全部数值主张经验收者亲手复现准确，建议级不阻断→转登 **F-088**。
- 说明：gate-all.ps1 未由验收者复跑——F-004/F-009 登记其非主树无法全量运行，其 ird_gates 门禁面已由验收者在 worktree A 亲手执行 `ird_gates` 目标覆盖（4.2），其集成/冒烟测试发现段结论与验收者复跑一致。

## 4.11 偷懒/缩水实现扫描 — PASS

1. **标记扫描**：diff 新增行 `TODO|FIXME|stub|placeholder|未实现|not implemented`（忽略大小写）grep 零命中（exit 1 实证）；无契约标注的"触发式延后"豁免需求。
2. **空实现检测**：公共接口均有完整实现——`policyDiagCode`（29 值全枚举 switch 无 default）、`policyDiagCodes()`（全表数组）、`isRegisteredPolicyDiagCode`（线性查表）、`registeredCodes()`（全表拷贝）、`make()`（四步核对＋core 工厂委托＋防御转译，无空函数体/仅返回默认值的桩）、`makeComparativeValue/makeNotApplicableValue/makeComparative`（语义完整）；`IPolicyDiagnostics` 为 §9.6 冻结签名的纯虚接口（设计本体非缩水），`PolicyDiagnostics` 为其唯一产品实现。
3. **acceptance 逐条证据表**：见 4.4——唯一条目有具名用例组＋验收者亲手实测＋testkit 真实链接的二进制/行为双实证，不缺。
4. **测试强度**：断言校验行为与边界（29 值全表逐串逐位、码句法逐字符、19 值交集逐串、表外码三形态含大小写/近邻变体、三必填串逐一、无效单位句柄、NotApplicable 取值恒空、"nan" 原文逐字、跨实例确定性、基类引用多态分派）；4.5 双探针实证非恒真。

---

## 问题清单

### 阻断级

无。

### 建议级（已强制转登 traceability/findings.json）

- **G-1→F-088**：F-010 根因第六次踩中——de9119cc 正文声称 `traceability/builds/wp07-t10/` 下"全套验证留痕"含 8 件 .log（gate-all/gates-current-wp07t10/verify-target-build/integration-test-run/smoke-configure/smoke-build/smoke-test-run/validate-task），`git ls-tree` 实证零件入库（`.gitignore:87` `*.log` 吞掉），ird-test-report.json 字段悬空引用 validate-task.log 等（F-077/F-080/F-081/F-083/F-085/F-087 同族第六例；证据本体完整、数值主张全部复现准确，建议级不阻断）。六次复踩，随记录分支登记 F-088，再次建议所有者对根因（gitignore 豁免缺失）提级处置。
- **G-2→F-089**：F-025/F-041/F-073 计数失实家族同型再现——提交 e5b0e5fe 正文、units/policy.md §15.4 v0.11 行、ird-test-report.json notes 三处称 DiagnosticsTest.cpp「6 套件 19 用例（码表 6＋构造不变量 8＋辅助 4＋多态 1）」，gtest 实测（提交内 XML 与验收者复跑一致）为 **4 套件 19 用例**（PolicyDiagCodeTable 5＋PolicyDiagnosticsMake 9＋MakeComparativeHelpers 4＋PolicyDiagnosticsInterface 1）——套件数与套内分配失实；总数 19 正确、逐用例均真实存在且通过（提交 XML 19 例与报告枚举/验收复跑逐一对应），方向为套件归并口径错位、非虚报非粉饰，建议级不阻断。

### 验收者环境说明

- 集成/冒烟构建、200/131/684 测试、ird_gates、validate-task、双探针 mutation 全部由验收者在 detached worktree @ de9119cc 亲手执行；实施者运行结果仅作主张比对基准，未作证据采信（ACC §2.4）。
- F-062 环境前置（vcpkg toolchain＋Qt 前缀＋两件 RobWorkStudio.ini 本地资源复制）为 worktree 集成配置的必要输入，全程未省略；全树构建止于 F-008 框架基线缺陷（73 error 全在 RobWorkSim 插件，分布与登记逐项一致，ird 面零错误），按既定口径以任务目标面从零构建补强。
- validate-state.ps1 未执行——本验收不涉流水线状态写操作（state.json 由编排者辖域管理，非送验产物）。
- 探针 mutation 仅存在于临时 worktree 的未提交工作区，已 `git checkout` 还原并经 `git status --porcelain` 零残留＋200/200 复绿双重核验；送验冻结对象（git 对象库中的 de9119cc）全程未被修改，任务分支未接收任何验收侧提交。
