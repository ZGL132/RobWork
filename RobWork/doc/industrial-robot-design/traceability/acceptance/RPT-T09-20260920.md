# RPT-T09 验收记录（acceptance record）

| 字段 | 值 |
| --- | --- |
| 任务 ID | RPT-T09（实现导出与归档协调——Export/Archive＋IReportArtifactSink 契约与 Fake；unit=reporting；masterWp=WP-H；interUnit=true；卡内行≙DTB WP-12-T03『幂等部分』/T04；实现任务——全量清单验收） |
| 送验对象（冻结） | 35d2ece4442af8ede6a7f788d8b4112d83cac3cc（分支 wp12-t09；diff 范围＝490ede49322408a4c92f094cc5532ad2d228a25a..35d2ece4442af8ede6a7f788d8b4112d83cac3cc＝state.base..state.headSha；range 内含 8a6004cc（实现）／e0836c5b（单元卡 v0.12 同步）／ec021752（验证留痕）／35d2ece4（留痕日志 -f 补交）四个提交） |
| 验收尝试 | attempt 1（分支漂移检测：`git rev-parse` 实测 `origin/wp12-t09` == 本地 `wp12-t09` == 35d2ece4442af8ede6a7f788d8b4112d83cac3cc 全 40 位逐字一致，零漂移；`acc/RPT-T09/1` 分支名此前不存在；state.attempts.accept==1 与请求一致） |
| 验收环境 | 复现 worktree A＝`git worktree add --detach D:/10_Source_Repos/21_robot/rpt-t09-acc-a` @ 35d2ece4（集成 build 树为本验收全新配置）；冒烟 build 树＝独立目录 `rpt-t09-acc-a/build-smoke`（本验收新建，源＝worktree A 的 industrialrobot，与集成树分离）；记录回传 worktree B＝`git worktree add -b acc/RPT-T09/1 D:/10_Source_Repos/21_robot/rpt-t09-acc-b` origin/redesign-main@490ede49；MSVC 2022 x64＋vcpkg 经典模式（toolchain 绝对路径指向主仓库 vcpkg——worktree 不含 vcpkg，只读第三方）＋Qt 6.11.1（D:/software/Qt）。集成配置＝`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=<主仓>vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`，缓存确认 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`（grep 实证）；初配失败一次（缺 `RobWorkStudio.ini.template.static`／`RobWorkStudio.ini.shared.in` 两件未追踪辅助件——F-200/F-007 既有登记环境链本验收再次亲历），自主树复制后配置成功（exit 0、CMake Error 计数 0）——非送验对象缺陷。验收全程只消费产物、契约、设计文档与在库留痕，未读取实施者过程叙述；结论全部出自验收者亲手重跑 |
| verdict | **pass**（4.1～4.11 全部通过；无阻断级问题；建议级 4 条已转登 findings.json F-242/F-243/F-244/F-245） |
| 验收日期 | 2026-09-20 |

## 4.1 改动范围 —— 通过

- `git diff --stat 490ede49..35d2ece4` 实证 **27 个变更文件**（8 件 reporting 代码/头/测试/CMake＋18 件 traceability/builds/wp12-t09/ 留痕＋1 件 units/reporting.md；6490 insertions/5 deletions，无删除、无重命名）。
- **allowedFiles 逐文件比对（越界零命中）**：`git diff --name-only 490ede49..35d2ece4 | grep -v -E '^RobWork/RobWorkStudio/src/rwslibs/industrialrobot/reporting/|^RobWork/doc/industrial-robot-design/units/reporting\.md$|^RobWork/doc/industrial-robot-design/traceability/'` **零输出**——8 件命中 `reporting/**`、18 件命中 `traceability/**`、1 件命中 `units/reporting.md`，27/27 全部在册。
- forbiddenFiles（REQUIREMENTS.md／ARCHITECTURE.md）**零触碰**（diff 无该两文件）。
- 无未登记框架源码改动（SA-02）：diff 零框架路径；`patches/` 目录变化 **0 件**。
- 涉及文件与契约产物列（『Export.hpp/.cpp、Archive.hpp/.cpp（＋IReportArtifactSink 契约与 Fake）』）一致：Archive.hpp/.cpp＋Export.hpp/.cpp＋test/FakeArtifactSink.hpp 全部为 `reporting/**` 内新增；CMakeLists.txt 仅增列源文件（M）。

## 4.2 可复现构建 —— 通过（集成全新树重配＋独立冒烟树，双模式零错误）

- **集成模式**（worktree A 全新 `build/`，配置 exit 0 后缓存 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON` grep 实证）：契约 verify 两目标 `cmake --build build --config Release --target sdurws_ird_reporting_test`／`…_contract_test` **均 exit 0，`error C[0-9]/error LNK/fatal error` 计数 0**（含 RobWork→RobWorkStudio→industrialrobot 依赖链的全新编译；产物 `build/RobWorkStudio/bin/Release/` 两测试 exe 就位）。
- **独立冒烟模式**（独立目录 build-smoke，`-DCMAKE_TOOLCHAIN_FILE=<主仓>vcpkg`＋Qt prefix——AGENTS §4.1 必带口径）：配置 **exit 0**＋全量构建 **exit 0、错误计数 0**；`reporting/` 下两测试 exe 就位并亲跑全绿（见 4.3）。
- **全树集成构建附加复现**：`cmake --build build --config Release`（全树口径，本验收追加执行）→ **exit 1，73 条 error 全部位于 RobWorkSim 框架插件**（RWSimulatorPlugin——框架头 `RobWork/RobWork/src/rw/math/Math.hpp` min/max 宏污染簇 C2059/C2334/C2143 等；bt_plugin.rwplugin——vcpkg Eigen Parallelizer.h 同族宏污染），与 F-008/F-235 既有登记的基线缺陷签名**精确一致**；涉事框架文件 Math.hpp 在 base/head/HEAD 三处 blob 同值（`git ls-tree` 实证 7357cd31…）——本任务 diff 零框架触碰（4.1），**industrialrobot 全部目标零错误**（error 扫描 industrialrobot 命中 0；两 verify 测试 exe 产物就位并全绿）。基线缺陷使全新树全树口径当前不可达——与 F-235 归因一致（环境性、非送验对象引入）。
- 说明：契约 verify 字面即两目标级口径（零错误已复现）；送验 integration-build.log 的「全树零错误」产自**主树**（日志内产物路径 `D:\10_Source_Repos\21_robot\RobWork\build\…`——AGENTS §4.1 规定的唯一交付口径树、增量缓存，其中 RWSimulatorPlugin.dll 构建成功在案）——该口径在主树成立、在全新 worktree 树被基线缺陷阻断（F-008/F-235），树scope 未在送验报告中标注——**G-4 转登 F-245**（与 F-036/F-038 强度表述家族同型，建议级）。

## 4.3 契约 verify 命令 —— 通过（两条逐条亲手执行，测试真实运行）

worktree A 按契约字面逐条独立执行，**两条全部 exit 0**：

1. `cmake --build build --config Release --target sdurws_ird_reporting_test` → exit 0（acc-int-build1.log）
2. `cmake --build build --config Release --target sdurws_ird_reporting_contract_test` → exit 0（acc-int-build2.log）

**测试真实运行（非只构建，双模式四跑全绿）**：

- 集成模式亲跑两 exe → `[==========] 150 tests from 29 test suites ran` ＋`[PASSED] 150 tests`；`51 tests from 20 test suites`＋`[PASSED] 51 tests`——零失败；
- 冒烟模式亲跑两 exe → 同为 **150＋51 全绿**。

**与送验留痕逐字一致**：在库四份 gtest XML 顶格统计（`tests="150"`/`tests="51"` × integration/smoke，failures="0"）与验收者亲跑一致；ird-test-report.json 声称的 150/150＋51/51 双模式全绿经亲跑证实。

**附加治理脚本（worktree A 上下文亲手复跑）**：`validate-task.ps1 -TaskFile tasks/foundation/RPT-T09.json` → **PASS (1 tasks)**；`validate-docs.ps1` → **PASS (20 units, 12 trace entries, 150 task files)**；`verify-task.ps1 -TaskFile …RPT-T09.json -DryRun` → **DRYRUN PASS**（dryrun 回显的两条命令与契约 verify 字面逐字一致——交叉印证）；`validate-state.ps1` → **PASS (phase=idle, queue=17, history=125, tasks indexed=150)**。均与在库 `validate-task.log`/`validate-docs.log` 一致。

**ird_gates 独立复跑（送验双树＋验收者全新第三树三方比对）**：验收者以 `cmake -DIRD_ROOT=<worktree A>/…/industrialrobot -DIRD_ENABLE_GIT=OFF -P cmake/ird_gates.cmake` 直跑 head 树——**原始 IRD-GATE- 行 61（与送验两日志同构：独立命中 27×流式＋摘要两遍＋自测提及 7）；规范摘要段 27 项命中与送验 base/branch 两日志路径归一化后逐行等同（唯一差异＝R4 既有命中的 worktree 绝对路径前缀——即送验『路径归一化 diff 为空』口径，GATES-HIT-SET-IDENTICAL 独立成立）**；27 项命中全部为既有登记归属族（testkit 直链豁免族/io libzip·expat 词表族/ui Qt 例外族/testdata_lint/project R4 字面量），**reporting 目标相关命中 0**（grep 实证）——RPT-T09 diff 零新增门禁命中。gate-all.ps1 冒烟步缺 Qt 前缀为既有环境项（gates-all-note.txt 已如实登记）。

## 4.4 acceptance 条目 —— 通过（五条逐项证据）

| # | 契约条目 | 证据（验收者亲读源码＋亲手复跑） |
| --- | --- | --- |
| 1 | RP-IDEM-1/2 幂等导出（判定键＝manifest 内容摘要 D-02） | **项目内**：实现＝幂等预检先于 begin、**零汇集座调用**（Archive.cpp:269-285——同 reportId 已 Finalized 且 `existing->manifestDigest == digest` → IdempotentHit 返回既有记录原文含原 publishedAtUtc）；unit `CoordinatorTest.IdempotentHitZeroRewrite`（第二次发布 `sink_.calls.size()` 零增——零重写观测面）＋`ExportServiceTest.ProjectArchiveIdempotentRepeat`（`sink_.published.size()` 零新增存储）亲跑通过。**外部**：目标已存在读字节比对摘要一致＝幂等成功无操作、不调 makeAtomicTarget（Export.cpp:405-420）；unit `ExternalIdempotentNoOp`（mtime 逐文件不变＋磁盘哈希不变＋`atomicTargetCreated` 计数零增）亲跑通过。**判定键**：`computeManifestDigest`（Archive.hpp:286-319 契约注释＋Archive.cpp:152-213 实现——编码域 contentIdentity＋按 relPath 字节序稳定排序的 (relPath,format,sha256,sizeBytes,rendererVersion,templateVersion)；排除域 ReportId/身份链/生成与发布时刻/格式常量，不以目标路径）＋`ManifestDigestTest` 七条（DeterministicSameInput/ReportIdentityExcludedFromKey/GenerationAndFinalizeTimeExcluded/ContentIdentityChangesDigest/VersionDimensionsChangeDigest/ArtifactOrderInsensitive/UsageOnEmptyAndDuplicate）亲跑通过 |
| 2 | RP-CONF-1 冲突拒绝 | **①项目内同 reportId 不同内容＝ArchiveConflict＋差异维度定位**：unit `ConflictIdentityDimension`（诊断 cause 含「身份维度」＋零汇集座调用——不覆盖）＋`ConflictVersionDimension`（「版本/工件集维度」）亲跑通过；实现＝Archive.cpp:291-309（identityDimension 按 contentIdentity 异同二分定位）；finalize 兜底复核路径＝Archive.cpp:478-483。**②外部 NeverOverwrite 默认拒绝＋OverwriteAtomic 原子替换**：unit `ExternalConflictNeverOverwriteKeepsExisting`（ExportFailed＋诊断 cause 含 io 族码 IO-PACK-TARGET-EXISTS＋recommendedAction 指 OverwriteAtomic＋既有文件字节原样保留）＋`ExternalOverwriteAtomicReplaces`（替换为新渲染字节——与参考目录幂等导出逐字节一致）＋`ExternalOverwriteFailurePreservesPrevious`（failCommit 注入→先前输出完整保留＋`.tmp` 零残留目录扫描）亲跑通过；失败保留选择与路径可重试（§16 口径）由各失败用例的「保留路径」断言与 RP-DISK-1 重试用例共同承载 |
| 3 | RP-DISK-1/RP-RO-1 | **磁盘满**：unit `DiskFullAbandonsThenRetrySucceeds`（diskFullAtWrite=1 注入→DiskFull＋`diag.comparison.expected` Provided 且值＝失败工件字节数＋`actual` NotProvided——比较型诊断两态断言；`calls.back()=="abandon"/"failed"`＋`activeSessions()==0`＋`listPublished` 空——清理与不入清单；清除注入重试 → Published）＋服务级 `ProjectArchiveDiskFullRetrySucceeds`（同请求原样重放成功——选择与路径保留）亲跑通过；实现＝Archive.cpp:388-415（abandon(Failed)＋ComparativeFields）。**只读项目两路径**：unit `ReadOnlyStoreRejectedWithTwoPathDiag`＋服务级 `ReadOnlyProjectTwoPaths`（ReadOnlyStore＋诊断 recommendedAction 含「两条路径」；同只读项目改 ExternalPath → 导出成功 2 文件——PM-07 readOnlyAllowed 口径）亲跑通过；实现＝Archive.cpp:331-345（begin 异常轨 ReadOnlyStore→两路径诊断） |
| 4 | RP-CANC-1 取消与未完成工件 | **取消检查点**：unit `CancelBeforeBegin`（入轨取消→Canceled 全空形态＋零汇集座调用）＋`CancelBetweenWritesAbandons`（逐工件写前检查点→abandon(Canceled)＋零 finalize＋listPublished 空——「文件存在≠已发布」）＋服务级 `CancelProjectArchiveAtEntry`/`CancelProjectArchiveBetweenWrites`/`CancelExternalExport`（外部取消＝abort 在途目标清理临时＋五成员全空）亲跑通过；UX-03 正常取消非错误（无诊断无 error）逐用例断言。**abandon 责任终结**：unit `CleanupFailureLeavesPublishedUntouched`（cleanupFailure 注入→残留计数可观测＋已 Finalized 报告零影响）＋契约侧 `SinkContractTest.AbandonTerminatesWithoutTouchingPublished` 亲跑通过；PM-08 未完成不入清单由全部失败/取消用例的 listPublished 空断言常驻承载 |
| 5 | IReportArtifactSink 契约与导出编排落地 | **四方语义逐字**：Archive.hpp:354-395 四方法签名与 §9.6 原文一致（begin/writeArtifact/finalize/abandon——错误轨约定：begin 环境错误异常轨、write/finalize 经 ArtifactStatus 六值、abandon 不抛）；契约侧 `SinkContractTest` 六条（BeginStagedFinalizePublishesOnce——D-13 原子发布入清单/FinalizeIdempotentHitKeepsPublishedBytes——D-14 幂等零重写/FinalizeConflictKeepsPublishedIntact/WriteOnlyIncreaseSemantics——只增幂等/AbandonTerminates…/ReadOnlyContextThrowsReadOnlyStore——§9.9 透传投影）＋`ArchiveContractTypes` 五条（枚举声明序钉住/值句柄 trivially_copyable/命名常量钉住/摘要类型＝core::ContentIdentity static_assert——P-RPT-9 基线）＋`PublicHeadersZeroForbiddenIncludes`（两公共头 include 行扫描零 io/runtime/execution/业务域/ui/Qt——P-RPT-1 常驻自证）亲跑通过。**编排顺序**：unit `PublishesWithOrchestrationOrder`（`calls` 逐条断言 begin→writeArtifact×2→finalize 恰 4 次）＋任一非 Ok→abandon（DiskFull/Conflict/取消三轨各用例）亲跑通过；**listPublished 仅 Finalized**：`PublishesWithOrchestrationOrder` 尾断言＋失败/取消用例空清单断言。**FakeArtifactSink 随任务交付**：test/FakeArtifactSink.hpp 文件头替身边界声明（「仅验证 reporting 侧契约语义，不构成 project 持久化正确性证明」——T11 收口归位在案）＋三项故障注入（seedPublished 冲突/diskFullAtWrite/cleanupFailure）实读在位。**陷阱处置**：P-RPT-4——本任务 diff 零 project/ 文件触碰（4.1 实证）、sink 落位 Archive.hpp 单侧冻结＋文件头交叉核对义务注明；P-RPT-1——公共头零 io 类型（契约扫描用例）＋写路径全经注入 makeAtomicTarget（Export.cpp:456-457 实读）；P-RPT-9——core 类型消费以 v0.1 Draft 为基线（static_assert 钉住＋Archive.hpp 文件头注明） |

## 4.5 测试真实失败能力 —— 通过（两处实现变异即红、还原复绿）

在 worktree A（本验收沙箱，任务分支零触碰）注入两处应失败变异，增量重建后亲跑：

1. **变异 1（Archive.cpp）**：取消路径 `m_sink->abandon(session, ReportEndReason::Canceled)` → `ReportEndReason::Failed`（sed 命中两处同型检查点）→ unit `CoordinatorTest.CancelBetweenWritesAbandons` 与 `ExportServiceTest.CancelProjectArchiveBetweenWrites` **变红**（abandon detail 期望 "canceled" 实得 "failed"）——RP-CANC-1 取消语义与 UX-03 形态红能力成立。
2. **变异 2（Export.cpp）**：外部幂等判定 `if (readable && digestOf(existingBytes) == wantDigest)` → `if (false)`（同字节目标不再走无操作路径）→ unit `ExportServiceTest.ExternalIdempotentNoOp` **变红**——RP-IDEM-1② 外部幂等无操作红能力成立。

合计 147 PASSED / **3 FAILED**（第三红为变异 1 经导出服务路径的连带暴露——同一缺陷双观测面）。`git checkout` 还原＋重建 exit 0 后 **150＋51 全绿复现**，`git status --porcelain` 空（沙箱干净）。测试非恒真、非「只编译通过」。

## 4.6 架构红线 —— 通过（AGENTS §5 八条逐条）

1. **L2 计算内核零 Qt**：四件产品新文件＋两测试文件 include 面亲读＝std＋gtest（测试目标）＋`sdurws/ird/{core,diagnostics,reporting}/…`——**零 Qt include**；CMake 配置期守卫（`^Qt[0-9]?::` 匹配即 FATAL——CMakeLists.txt:360-363）随配置通过；契约 `PublicHeadersZeroForbiddenIncludes` 随 51 全绿复跑；ird_gates R3 检查本任务零新增命中（4.3）。
2. **业务域互链禁止（R-1）**：`target_link_libraries(sdurws_ird_reporting …)` 保持 core/evidence/diagnostics/project 四条登记边零改动（CMakeLists.txt:188-192 实读）；配置期守卫（表外 `sdurws_ird_*` 边即 FATAL——:347-359）＋ird_gates SUB 检查 reporting 零命中双证。
3. **跨单元私有头禁止（R-2）**：两公共头均位于 `include/sdurws/ird/reporting/`；其 include 面仅他单元公共头（core/Digest、core/Identity、core/DiagData、diagnostics/Redaction——公共路径实证）；`../test/FakeArtifactSink.hpp` 为**单元内**测试替身相对包含（不出 include/、不跨单元——T07 RenderText.hpp 同先例）。
4. **框架零源码修改（SA-02）**：diff 零框架路径、patches/ 零新文件（4.1）。
5. **不做 RobWork 名称前缀拼接/剥离（R-4）**：新代码零名称解析面；ird_gates R4 命中仅 project 单元既有登记 1 处（双树命中集等同——4.3），本任务零新增。
6. **权威唯一（PA-1）**：写路径唯一归汇集座——「reporting 只经 IReportArtifactSink **请求**写入、不存在第二条项目写路径」（Archive.hpp 文件头背景说明＋实读协调器零直写磁盘）；幂等/冲突判定事实由 project 侧实现承载（sink 见磁盘事实）、reporting 侧仅编排；单位/诊断文案归 core/diagnostics（DiagnosticRecord::make＋diagcodes 引用——不自造码段；RPT-DISK-FULL/RPT-READONLY-STORE 两码引用自 §7.4 表并如实登记「待收编」状态）；命令入口零触碰。
7. **不可变历史（PA-2）**：已 Finalized 内容零覆盖（幂等/冲突两轨均零重写——4.4 条 1/2 实证）；报告值发布后不回写（stamped 拷贝盖章——Archive.cpp:437-439）；manifest 发布后作为持久化事实不再变更（PA-2 精神注释在案）。
8. **不继承/不恢复 old/**：diff 无 old/ 引用。

## 4.7 注释规范 —— 通过（公共头全查＋实现/测试文件抽查）

- **公共头 Archive.hpp（全查）**：文件头（L1-63）设计依据逐条锚定 §7.3/§7.4/§9.6/§9.9＋需求 RPT-01/PM-07/PM-08/PM-03＋契约 acceptance 1~5；背景说明（为何写路径只有汇集座）＋落位偏差四项（DTB §5.4 口径）＋P-RPT-4/P-RPT-9 处置＋线程约束段——每个 struct/class/enum/函数 Doxygen 中文齐备（ArtifactSessionRef 保留值纪律、ReportEndReason 三值语义、ArtifactStatus 六值判定语义、ReportArtifactManifest 字段逐行对应 §7.3 表、computeManifestDigest 编码域/排除域/复杂度/异常、IReportArtifactSink 四方法逐条前置后置、IReportPublishedIndex 为何需要独立只读缝、协调器 publish 六步编排明细）；§2.5 高危信息：单位（sizeBytes「单位：字节」）、所有权（借用/m_sink 可写引用——「project 实现/测试 fake」）、线程（会话内单线程/跨会话互斥归 project/索引并发只读）、错误语义（异常轨 vs 状态轨 vs 不抛三分约定）、确定性（排序键 relPath 字节序 NFR-COR-02、大端编码）——必填项零缺失（角度制式/坐标系：本单元纯字节/文本面，无几何量——N/A 合理）。
- **公共头 Export.hpp（全查）**：文件头（L1-54）同构齐备（设计依据＋编排职责分界＋落位偏差五项含 withEvidenceBundle 显式拒绝的「非桩」声明＋工件相对命名决策＋线程约束）；IReportResolver/ExportDestination/ReportExportRequest/ReportExportResult/ExportedFile/两服务类逐成员 Doxygen；结果形态约定（成功/失败/取消三态成员在场规则）显式成文。
- **实现文件抽查（3 件）**：Archive.cpp 文件头（L1-26 实现要点＋诊断码登记状态「待收编」如实注明）＋全函数段落注释（编码逐字节含义、排序不变式、每步「第 N 步」编号与失败走向）；Export.cpp 编排每步「第 N 步」＋每分支业务场景注释（幂等/NeverOverwrite 保守拒绝/OverwriteAtomic 不可读目标同轨语义/失败 abort 清理）＋外部幂等读探测的 P-RPT-1 边界论证（文件头偏差⑤）；CMakeLists.txt 增列段继承全文件中文注释块风格（RPT-T09 增列说明 L117-129——设计依据与陷阱处置逐条）。
- **测试文件（全查关键面）**：ArchiveExportTest.cpp 用例注释逐条标注验证的需求编号（RP-IDEM-1/RP-CONF-1①②/RP-DISK-1/RP-CANC-1/RP-RO-1——AGENTS §2.7 追溯字段）；ArchiveCrossUnitContractTest.cpp 提取函数注释（扫描口径）＋夹具说明；FakeArtifactSink.hpp 文件头替身边界声明（§2.3 契约文档级）＋注入成员逐项注释＋线程约束（非线程安全替身）。

## 4.8 文档同步 —— 通过（偏差已按 DTB §5.4 登记；findings 未关闭条目已核对）

- **单元卡 v0.10→v0.12**（e0836c5b）：文档头版本行（含 v0.11 停留 v0.10 的补登说明）＋构建落位行追加 RPT-T09 落地条款＋§14.4 v0.12 变更记录行；**六项落位偏差逐项与实现对照核实**（验收者亲读两公共头偏差注释与单元卡登记一一对应）：①IReportPublishedIndex 只读清单缝（Archive.hpp:29-36 偏差①——为何 sink 四方法面需要读取通道）；②IReportResolver 解析缝（Export.hpp 偏差①）；③取消结果形态＝全空（Export.hpp 偏差③/Archive 偏差② 取消令牌会话注入）；④withEvidenceBundle＝Usage 显式拒绝（Export.hpp 偏差④——RPT-T10 边界）；⑤外部幂等读探测经只读文件读（Export.hpp 偏差⑤——P-RPT-1 写出纪律不涉读探测的论证）；⑥DiskFull 比较诊断可用侧 NotProvided（Archive.cpp:391-394 注释与登记一致）；另 RPT-DISK-FULL/RPT-READONLY-STORE 两码「待收编」状态登记在案——**零未登记偏差**。
- **任务状态/追踪矩阵**：validate-docs PASS（亲跑同值）；契约 status 翻转归收尾段（PIPE §4.7，与既有任务同口径；F-201 已登记该条文与实践脱节）。
- **findings.json 未关闭条目相关性核对（v1.2）**：现册 **open 209 条**（total 241），逐族核对与本任务的关系——
  - **F-012/F-232~F-241 家族**（辅助提交标题名词短语开头）：**本任务再现**（见 4.9/G-1）——已按强制转登要求登记 **F-242**（编号自 F-241 顺延）；
  - **F-041/F-073/F-235/F-236/F-238 计数失准族**：本任务散文计数（留痕 7＋11＝18 件——两提交 `git show --name-status` 计数逐件实证；新增用例 30＋13＝43——`grep -c ^TEST` 实证；单元 150＝120＋30、契约 51＝38＋13——XML 顶格＋亲跑双证）经验收者逐项实测**全部准确——散文计数家族零复踩**；唯一计数精度瑕疵＝ird-test-report.json「各 61 处 IRD-GATE- 命中」措辞（独立命中实为 27、61 为含流式重复与自测提及的原始行计数，日志自身「命中 27 项」准确在案）——**G-2 转登 F-243**；
  - **F-010/F-077/F-081/F-083/F-085/F-087/F-088/F-090＋F-223 幻影清单族**（.gitignore `*.log` 第 87 行吞留痕）：**根因再次踩中**——ec021752 按目录 add 时 11 件日志被吞（该提交实入 7 件实证），35d2ece4 以 `git add -f` 补交并在提交正文如实说明（不 amend 已推送历史合规）；本验收 18/18 件全部在库（零证据缺失），属**自纠型再现**——**G-3 转登 F-244**；
  - **F-065/F-207/F-231 留痕编码族**：本任务在库日志 file 实测全 UTF-8、双重编码标记字节扫描零命中——零复踩；
  - **F-230**（[RPT-Txx] vs [WP-12-Txx] 前缀双轨口径）：本任务沿用 `[RPT-T09]` 前缀（契约 branch=wp12-t09、卡行≙WP-12-T03 幂等部分/T04），属已登记双轨口径的沿例，无新增发现；
  - **F-007/F-062/F-098/F-099/F-200 环境链**：验收复现期间再次亲历 ini 双辅助件缺失（初配失败一次）——F-200 既有登记的再证实，非本任务交付面问题，零新增登记；
  - **F-235/F-008/F-036/F-038 全树口径族**：本验收全新树全树构建复现 F-235 73 错（RWSimulatorPlugin＋bt_plugin——框架 Math.hpp blob base/head 同值，环境性）；送验「全树零错误」产自主树口径且未标注树 scope——**G-4 转登 F-245**；
  - **F-201**（ACC §4.10 resultNote 条文与流水线实践脱节）：本任务同样以收尾段翻转登记——既有登记继续有效；
  - 其余 open 条目（core/testkit/evidence/project/io/execution/policy/runtime/diagnostics/ui 各单元家族）与本任务交付面（reporting 导出/归档层）无交叉；**无可随本任务顺手消账项**。

## 4.9 commit 质量 —— 通过（四段式齐备、验证结论经独立复跑证实；标题风格 1 条建议级）

- **8a6004cc** `[RPT-T09] 实现导出与归档协调（Export/Archive＋IReportArtifactSink 契约与 Fake）`——动词开头合规；四段齐备（What 按文件/函数分点、Why 逐条对应 acceptance 1~5＋knownPitfalls 三项处置、How 构建与测试与门禁真实结论、影响面含六项偏差指引）；`Acc-Covered: 1,2,3,4,5`。
- **e0836c5b** `[RPT-T09] reporting 单元卡 v0.12：RPT-T09 实现落位登记（文档同步）`——四段齐备；标题前缀后以名词短语开头（F-012 家族，见 G-1/F-242）。
- **ec021752** `[RPT-T09] 验证留痕：双模式构建/测试执行/门禁/治理自查证据（wp12-t09）`——四段齐备；标题同族瑕疵（同 G-1）。
- **35d2ece4** `[RPT-T09] 验证留痕补交：门禁/构建/测试运行/治理自查日志 -f 入库（wp12-t09）`——四段齐备（What/Why 含 .gitignore 根因与先例引用/How 的 ls-files 核对/影响面）；标题同为名词短语开头族（同 G-1）。
- **验证结论与留痕一致性（粉饰排查）**：提交正文声称的分次入库件数（ec021752＝7 件、35d2ece4＝11 件——`git show --name-status` 逐件计数相符）、「18 件与 wp12-t08 留痕清单同形齐备」（实测 18/18）、双模式测试计数（150＋51——XML 顶格＋亲跑双证）、门禁命中集归一化 diff 为空（验收者三日志比对独立成立——4.3）、validate 双 PASS（亲跑同值）——**逐项相符，零粉饰、零夸大**；ec021752 的 11 件被吞事实由 35d2ece4 正文如实自曝（反向诚实——非掩饰）。

## 4.10 留痕完整 —— 通过

- gtest XML×4＋ird-test-report.json（schema `ird-test-report/1`，targets/tests/gates 三段、acceptance 逐条对应说明）＋构建日志（integration-build.log/smoke-configure.log/smoke-build.log——冒烟日志路径显式 Release 口径，F-055 关于配置声明的关切不涉）＋测试运行日志×4＋门禁双树日志＋gates-hit-set-diff.txt＋gates-all-note.txt＋validate 双日志——**18 件全部落位在库**（4.1/4.9 实证）。
- 在库四份 XML 顶格 tests/failures 属性与验收者双模式亲跑结论一致（150/51 × 2，零失败）；ird-test-report.json 的 notes 段（新增 30＋13 用例逐组列举）与 `grep -c ^TEST` 实测一致。
- gates-all-note.txt 如实登记 gate-all.ps1 冒烟步环境失败（脚本内部未传 Qt 前缀）与 DoD 门禁承担口径（ird_gates 双树直跑＋双树原始日志在库）——无掩饰。
- resultNote/status 更新随收尾段（PIPE §4.7）——F-201 已登记该条文与实践的脱节，此处如实注明。

## 4.11 偷懒/缩水实现扫描 —— 通过

- **①TODO/FIXME/stub/placeholder/未实现/not implemented**：diff 范围 reporting 全部代码/测试文件 diff 全文扫描（grep 六词族）**零命中**（无豁免条款需列出）。
- **②空实现检测**：`ReportArchiveCoordinator::publish`（Archive.cpp:226-512——六步编排全量逻辑：前置校验五路/幂等预检/取消检查点三处/逐工件对位写入/盖章 finalize/六态转译）与 `ReportExportService::exportReport`（Export.cpp——请求校验/渲染＋二次比对/一致性门/双目标分派/外部四轨）均为完整实现；`withEvidenceBundle==true → Usage` 为**契约明文允许并经 §14.4 v0.12 偏差④登记的显式边界**（「能力到位前显式拒绝而非静默忽略——此处为显式边界非桩」——Export.hpp 文件头原文），非未声明空实现；FakeArtifactSink/FileBasedIoFactory 为契约明文允许的注入替身且边界声明在案。
- **③逐条证据表**：见 4.4——acceptance 五条每条均有具名测试＋亲跑结论＋实现行号，任一条无证据即 fail 的反例不存在。
- **④测试强度**：4.5 两处实现变异实测 3 用例变红＋还原复绿；断言校验行为与边界（sink 调用记录逐方法断言/编排顺序恰 4 次/abandon 原因 detail 字符串/mtime 与磁盘哈希双观测/比较型诊断字段态 Provided·NotProvided/零临时残留目录扫描/既有文件逐字节保留/listPublished 空清单/重试成功闭环）——非恒真、非「只编译通过」。

## 发现的问题清单

| 级别 | 编号 | 内容 | 处置 |
| --- | --- | --- | --- |
| 建议 | G-1 | 三辅助提交 e0836c5b／ec021752／35d2ece4 标题前缀后以名词短语开头（"reporting 单元卡 v0.12：…"/"验证留痕：…"/"验证留痕补交：…"），不合 ACC §4.9/AGENTS §6.3"动词开头"字面——F-012/F-232/F-233/F-234/F-237/F-239/F-240/F-241 家族再现（家族新增第 14/15/16 例，RPT 系连续八任务复踩）。四正文四段齐备、验证结论经验收者独立复跑全部证实（无粉饰），纯风格瑕疵不构成阻断 | **已转登 findings.json F-242**（与家族合并处置；不追溯改写已推送历史；不阻塞合入） |
| 建议 | G-2 | ird-test-report.json gates 段「base/branch 各 61 处 IRD-GATE- 命中」措辞计数口径失准：61 为原始日志行数（含流式＋摘要两遍重复 27×2＋自测提及 7），**独立命中实为 27 项**（日志自身「命中 27 项」准确在案）；与 F-041/F-073/F-235/F-236/F-238 散文计数家族同型（方向为多报命中数——对交付面不利方向的夸大，但命中集实质结论「零新增」经三日志比对独立成立） | **已转登 findings.json F-243**（纯措辞精度问题；不阻塞合入） |
| 建议 | G-3 | F-010/F-223 根因（.gitignore 第 87 行全局 `*.log` 与留痕入库要求结构性冲突）再次踩中：ec021752 按目录 add 时 11 件日志被忽略规则吞掉，35d2ece4 以 `git add -f` 补交——**自纠型再现**（验收前已闭环、18/18 件在库零缺失、提交正文如实说明），未构成验收 fail | **已转登 findings.json F-244**（并入 F-223 系统性处置；不阻塞合入） |
| 建议 | G-4 | 送验 ird-test-report.json gates.build 段「集成模式 cmake --build build --config Release 全树零错误（integration-build.log）」未标注构建树 scope：该日志产自**主树**（日志内产物路径 `D:\10_Source_Repos\21_robot\RobWork\build\…` 实证——AGENTS §4.1 唯一交付口径树、增量缓存，其中 RWSimulatorPlugin.dll 构建成功），而**全新 worktree 树的全树构建被 F-008/F-235 基线缺陷阻断**（本验收复现 73 错——框架 Math.hpp blob base/head 同值 7357cd31…，零任务触碰、industrialrobot 目标零错误）。主树口径结论为真，但「全树零错误」的可达范围以树而定，与 F-036/F-038「强度表述」家族同型（建议级） | **已转登 findings.json F-245**（建议在报告口径标注构建树 scope；不阻塞合入） |
| 环境注记 | —— | 复现期间初配失败（ini 双辅助件缺失——bin/ 被 .gitignore 吞）为 F-200/F-007 家族既有登记环境链的再证实；非本任务交付面问题，零新增登记 | 已在 4.8 逐族核对留痕 |

## 验收者环境说明

- 复现与记录均在临时 worktree（rpt-t09-acc-a／rpt-t09-acc-b，冒烟树 rpt-t09-acc-a/build-smoke）完成；验收者对任务分支 wp12-t09 **零提交、零推送、零文件修改**（4.5 变异在沙箱 worktree A 内注入并已还原，`git status --porcelain` 复核干净）。
- 构建树：集成 `rpt-t09-acc-a/build`（全新配置，`RWS_BUILD_INDUSTRIALROBOT:BOOL=ON` 缓存确认）；冒烟 `rpt-t09-acc-a/build-smoke`（独立目录＋vcpkg toolchain＋Qt prefix）。测试执行：两模式各 2 目标亲跑（本轮共 4 次全绿运行＋1 次变异红运行＋1 次复绿运行）；ird_gates head 树直跑＋与送验双树日志归一化三方比对（基线树未另行建 worktree——送验 base 日志与 branch 日志经本次三方言比对互相钉住）。
- 记录落位：本文件随 F-242/F-243/F-244 转登一并提交于 `acc/RPT-T09/1` 分支并推送 `origin`（ACC §3 记录回传）。
