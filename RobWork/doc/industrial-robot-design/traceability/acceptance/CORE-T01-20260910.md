# CORE-T01 验收记录（对抗式验收）

| 字段 | 值 |
| --- | --- |
| 任务 ID | CORE-T01（≙WP-03-T01，masterWp WP-B） |
| 契约 | tasks/foundation/CORE-T01.json（status: ready，验收 pass 后由所有者翻转） |
| 验收对象 | 分支 `wp03-t01`，commit 范围 `7142e52..685c5f6`（实施提交 685c5f6，与 origin/wp03-t01 一致） |
| 配套留痕提交 | redesign-main `da407b6`（builds/wp03-t01/ 留痕＋DTB §4.4 gtest 版本登记＋phase-one-validation.log 追加） |
| **verdict** | **pass**（4.1～4.10 全部通过，无阻断级问题；4 条建议级问题留档，不阻塞合入） |
| 验收日期 | 2026-09-10 |
| 验收者环境 | Windows 10.0.26200 x64、Git Bash、VS2022（MSBuild 17.12.6+db5f6012）、Windows PowerShell 5.1、vcpkg 经典模式（gtest:x64-windows@1.18.0 已安装）；全程独立会话，仅读代码/契约/文档/留痕 |

---

## 逐项证据（acceptance-protocol §4，4.1～4.10）

### 4.1 改动范围 —— PASS

`git diff --stat 7142e52..685c5f6` 共 5 个文件，逐个比对契约 `allowedFiles`：

| 变更文件 | allowedFiles 归属 |
| --- | --- |
| `industrialrobot/CMakeLists.txt` | 显式列出 ✅ |
| `industrialrobot/core/CMakeLists.txt`（新） | `core/**` ✅ |
| `industrialrobot/core/include/sdurws/ird/core/README.md` | `core/**` ✅ |
| `industrialrobot/core/src/Core.cpp`（新） | `core/**` ✅ |
| `industrialrobot/core/test/BuildRedLineTest.cpp`（新） | `core/**` ✅ |

- `forbiddenFiles`（REQUIREMENTS.md、ARCHITECTURE.md）零触碰（diff 清单核对）。
- SA-02：diff 无任何框架源码（RobWork/RWS/RobWorkSim）文件；`industrialrobot/patches/` 无新文件（即无未登记 patch）。

### 4.2 可复现构建（验收者亲手重跑） —— PASS

**集成模式（重新配置）**：
```
$ cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON
  → Configuring done / Generating done（5.9s/11.5s）
$ grep RWS_BUILD_INDUSTRIALROBOT build/CMakeCache.txt
  RWS_BUILD_INDUSTRIALROBOT:BOOL=ON          ← 缓存确认
$ cmake --build build --config Release        ← 全量构建
  FULLBUILD_EXIT=0；"error C|LNK|MSB" 计数 0（日志 /tmp/acc-integration-fullbuild.log）
```
C++17（sdurws_ird_core STATIC）与 C++11 基线（sdurw_core/common/math dll）同树混链零错误——P-ENV-1 消账输入独立复现成立。

**独立冒烟模式（全新临时目录，先 rm -rf）**：
```
$ cmake -S RobWork/RobWorkStudio/src/rwslibs/industrialrobot -B build-acc-smoke \
    -G "Visual Studio 17 2022" -A x64 -DCMAKE_TOOLCHAIN_FILE=vcpkg/scripts/buildsystems/vcpkg.cmake
  → Configuring done (8.8s) / standalone smoke mode
$ cmake --build build-acc-smoke --config Release → SMOKE_BUILD_EXIT=0
  （sdurws_ird_core.lib＋sdurws_ird_core_test.exe＋gtest dll 拷贝全部成功）
```
⚠ 注意：裸冒烟命令（无 toolchain 参数，AGENTS.md §4.1 字面写法）自本任务起**配置失败**（`find_package(GTest CONFIG REQUIRED)` 找不到 GTest）——实施留痕 smoke-build.log 如实记录了带 toolchain 的实际命令，无粉饰；文档失同步登记为建议级问题 G-2。

### 4.3 契约 verify 命令 —— PASS

```
$ cmake --build build --config Release --target sdurws_ird_core_test
  sdurws_ird_core.lib → sdurws_ird_core_test.exe；EXIT=0
```
另经 `verify-task.ps1` 复核：`verify-task: PASS CORE-T01`（以 `-Command &` 方式调用；`-File` 方式存在脚本缺陷，见建议级问题 G-1，不影响 verify 命令本体结论）。

### 4.4 契约 acceptance 逐项 —— PASS

| acceptance 条目 | 证据 |
| --- | --- |
| ① 两模式构建零错误；C++17 与基线共存验证留痕（P-ENV-1 消账） | 4.2 双模式亲手复现零错误；phase-one-validation.log 追加段（da407b6）登记"P-ENV-1 消账：sdurws_ird_core（C++17 STATIC）与 sdurw_math/common（C++11 dll）同树混链构建成功" |
| ② gtest 按 DTB §5.5 定稿接入（P-ENV-2 已关闭，CR-07） | core/CMakeLists.txt:23 `find_package(GTest CONFIG REQUIRED)`、:86-88 `GTest::gtest_main`、:113-126 `ird_add_gtest`（add_test＋`<target>_report` XML 目标，对应 §5.5"沿用 ADD_RW_GTEST 宏语义、宏定义放各单元 CMake"）；vcpkg installed 实证 `gtest_1.18.0_x64-windows.list`；DTB §4.4 定稿行回填"2026-09-10 随 WP-03-T01 安装 gtest:x64-windows@1.18.0"；P-ENV-2 关闭登记于 units/core.md §10.2（v0.2，CR-07） |
| ③ 产品目标零 testkit 链接、零跨单元私有头 | 生成工程实证：sdurws_ird_core.vcxproj AdditionalDependencies 为空（STATIC 无链接）、Qt 引用计数 0；sdurws_ird_core_test.vcxproj 链接面＝sdurws_ird_core＋gtest/gtest_main＋sdurw_*（sdurw_math 传递）——无 `sdurws_ird_testkit`、无其他 ird 单元；`core/include/` 树下仅 README.md、零头文件；CMake 配置期守卫（core/CMakeLists.txt:137-150，R-1/R-2/R-3/T-1 断言 FATAL_ERROR）＋UT-BUILD 运行期扫描双层防线 |

ird_gates：留痕如实标注"未执行——门禁随 WP-01-T01 落地，尚未建成"（ird-test-report.json notExecuted 段、commit 正文、phase-one-validation.log 三处一致）✅。

### 4.5 测试真实失败能力（验证验证者） —— PASS

验收者构造 3 个探针（超协议要求的 ≥2），注入 core 源码树后直接运行测试可执行文件（扫描为运行期行为，无需重编）：

| 探针 | 预期红用例 | 实际结果 |
| --- | --- | --- |
| `core/src/ProbeQt.cpp`（`#include <QWidget>`） | NoQtInclude_NFR_MNT_01_R3 | **FAILED** ✅ |
| `core/src/ProbeCross.cpp`（`#include <sdurws/ird/testkit/TestPaths.hpp>`） | NoCrossUnitInclude_R1_R2_T1 | **FAILED** ✅ |
| `core/include/StrayHeader.hpp`（include 根越界头） | PublicHeaderPathLayout_R2 | **FAILED** ✅ |

合计 3 FAILED / exit 1；SourceTreeReachable 锚定用例保持绿（空集恒真防线亦间接验证）。探针删除后恢复 4/4 绿（exit 0），`git status` 确认工作树无残留。非恒真测试实证成立。

### 4.6 架构红线（AGENTS.md §5 八条） —— PASS

1. **L2 零 Qt**：vcxproj Qt 引用计数 0；探针实证 NoQtInclude 用例可红；CMake 守卫拦截 `Qt*::` 链接。构建日志中的 "Automatic MOC for sdurws_ird_core" 来自 RWS 顶层全局 `CMAKE_AUTOMOC` 传播，MOC 对无 Q_OBJECT 源空转，工程文件零 Qt 依赖（已核实）。
2. **R-1 业务域互链**：core 链接接口仅 sdurw_math（框架数学库，core.md §3.2 唯一允许依赖）；无任何 ird 单元目标边。
3. **R-2 跨单元私有头**：include/ 下零头文件；探针实证 PublicHeaderPathLayout 用例可红；CMake 注释明确"私有实现头只能放 src/"。
4. **SA-02 框架零修改**：diff 范围仅 industrialrobot/；patches/ 无新登记。
5. **R-4 名称前缀拼接**：本任务无名称解析逻辑（构建落位任务，不涉及）。
6. **PA-1 权威归属**：无越权实现他人语义；gtest 版本登记归 DTB 治理侧（已做）。
7. **PA-2 不可变历史**：无修订数据操作（不涉及）。
8. **old/ 继承**：代码零引用 old/。

### 4.7 注释规范（AGENTS.md §2） —— PASS

公共头全查：core 当前无公共头（include/ 仅 README.md，其中已说明接口随 T02～T08 落地）——无适用对象。实现面文件全查（本任务新增/大改共 3 个，满足"抽查 ≥3"且为全量）：

- `core/src/Core.cpp`：文件头（@file/@brief/设计依据 CORE-T01·NFR-MNT-01·ARC-02/背景说明"STATIC 库至少需要一个翻译单元"）齐备；空起步理由与后续拆分计划说明清楚。
- `core/test/BuildRedLineTest.cpp`：文件头＋设计依据（§8 UT-BUILD、NFR-MNT-01/02、R-1/R-2/R-3、契约 acceptance）＋背景（与 ird_gates/WP-01-T01 的分工、防恒真设计）；辅助函数（coreSourceDir/collectCppFiles/readFileOrFail）Doxygen 中文 @brief/@param/@return 齐备；4 个 TEST 均有 @brief＋需求/红线追踪；§2.5 适用项：错误语义（"读失败显性暴露而不是静默跳过"："目录级错误不抛异常，交由用例断言暴露"）、确定性来源（"已排序，保证失败信息确定可复现——NFR-COR-02"）、生命周期（static const 成员初始化说明）均有标注；单位/坐标系/角度制式不适用（无物理量代码）。
- `core/CMakeLists.txt`：头块（设计依据 §3.3/§3.2/DTB §5.1/§5.5/需求）＋各关键步骤中文注释（gtest 接入理由与 ODR 风险、/utf-8 的 GBK 误读成因（C2001）、R-2 include 根约定、冒烟/集成两模式差异、TARGET_RUNTIME_DLLS 机制、守卫的 NOTFOUND 语义不误报说明）。顶层 CMakeLists 修订段与 README 修订同规范。

### 4.8 文档同步 —— PASS（附观察）

- 与 units/core.md 零偏差：§3.3（目标名 sdurws_ird_core/别名 RWS::ird::core/cxx_std_17/链 sdurw_math/测试目标 sdurws_ird_core_test 不预建于骨架）与 §9 CORE-T01 行（产物范围、验收标准）逐项吻合；gtest 机制按 §5.5（P-ENV-2 关闭后改指）执行。
- 治理登记：DTB §4.4 gtest 首装版本回填（da407b6）；phase-one-validation.log 追加实施段记录。
- 契约 status 保持 `ready`：da407b6 正文登记"验收 pass 后由所有者改 done"——与协议 §6（合入决策归所有者）一致，非失同步。
- 留痕提交拆分至主线（da407b6）而非与代码同提交：因契约 allowedFiles 不含文档路径（同提交即越界），实施者以 commit 正文登记拆分理由，治理留痕按 AGENTS.md §6.2 主线小步提交——布局自洽。

### 4.9 commit 质量 —— PASS

685c5f6：标题 `[WP-03-T01] core 构建落位：INTERFACE 占位升级 STATIC 并接入 gtest 测试目标`（任务前缀✅，动宾摘要符合仓库惯例）；正文四段齐备——What（5 文件逐条）、Why（契约/单元卡 §9/需求 NFR-MNT-01·ARC-02/§5.5 机制与首装版本）、验证（两模式结论、verify 命令、失败能力自测、**ird_gates 如实标注未执行**）、影响面（默认 OFF 不影响基线、其余 19 单元不变、测试目录新增 dll）。验证结论与留痕一致，无粉饰（冒烟命令的 toolchain 参数在 smoke-build.log 中如实呈现）。

### 4.10 留痕完整 —— PASS

redesign-main `da407b6` 落位 `traceability/builds/wp03-t01/`：smoke-build.log（44 行，含实际命令全录）、sdurws_ird_core_test-smoke.xml（4/4）、integration-build.log（92 行，含 verify 命令与 ctest 注册验证）、integration-fullbuild.log（208 行）、sdurws_ird_core_test-integration.xml（4/4）、ird-test-report.json（schema ird-test-report/1，两模式 4/4、失败能力自测记录、notExecuted 如实登记、证据路径完整）；phase-one-validation.log 追加实施段（12 行）。留痕内容与验收者独立复现结果一致。

---

## 问题清单

### 阻断级

无。

### 建议级（不阻塞合入，留档处置）

| # | 问题 | 建议处置 |
| --- | --- | --- |
| G-1 | `scripts/industrialrobot/verify-task.ps1` 以 `powershell -File` 方式调用时失败：param 默认值 `$PSScriptRoot` 在该调用路径下解析为空 → `RepoRoot` 归约到 `D:\` → cmake 报 `D:/build is not a directory`；以 `-Command &` 方式调用则 PASS（两种方式均已在验收环境实证；probe 对照排除目录层级与 BOM 因素，根因未完全定位）。另有第 13 行行尾字面 `\n` 输出瑕疵。该脚本为既有治理资产（72117a4 修过 BOM 缺陷），不在 CORE-T01 改动范围 | 登记 DTB §4 治理项，由脚本所有者修复（至少注明须以 `-Command &` 或 pwsh 7 调用） |
| G-2 | AGENTS.md §4.1 冒烟命令未同步：自本任务起 `core/CMakeLists.txt` 含 `find_package(GTest CONFIG REQUIRED)`，无 toolchain 参数的裸冒烟命令配置失败（验收者复现：`Configuring incomplete, errors occurred`） | AGENTS.md §4.1 冒烟命令补 `-DCMAKE_TOOLCHAIN_FILE=<repo>/vcpkg/scripts/buildsystems/vcpkg.cmake`（或在 core/CMakeLists 对冒烟模式给出指引性报错信息） |
| G-3 | units/core.md §10.2 P-ENV-1 行尚未翻转为"已关闭"：消账输入已留痕（phase-one-validation.log）且经本次验收独立复现，但卡内状态行滞后——与 P-ENV-2 同批计划"两卡下次增量修订"（DTB §4.4 定稿行已登记该计划） | 按计划随 core.md/testkit.md 下次增量修订正式关闭，勿积压 |
| G-4 | RWS 顶层全局 `CMAKE_AUTOMOC` 传播使 industrialrobot 各目标出现无谓的 "Automatic MOC" 步骤（本次实证零 Qt 依赖、纯空转） | 后续单元任务评估顶层/目标级 `AUTOMOC OFF` 的构建收益（NFR-MNT-01 语义下倾向显式关闭） |

---

## 环境与复现说明

- 验收期间并行会话在 redesign-main 落了 `2639dda [DTB] v0.8`（自动化流水线登记）并持有工作树未跟踪文件——本验收通过临时 worktree 落本记录，未触碰其工作树状态。
- 验收者构建产物：集成模式复用仓库根 `build/`（重新配置后缓存 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`）；冒烟目录 `build-acc-smoke/`（保留供复查，可删）；全量构建日志 `/tmp/acc-integration-fullbuild.log`。
- 本记录落位 redesign-main（纯文档治理提交，先例 da407b6）；**wp03-t01 的代码合入决策归所有者**（协议 §6），本 pass 结论为其输入。
