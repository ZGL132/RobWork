# PRJ-T12 验收记录（attempt 1，2026-09-16）

| 字段 | 值 |
| --- | --- |
| 任务 | PRJ-T12 草稿服务（DraftServiceImpl）——tasks/foundation/PRJ-T12.json |
| 送验对象（冻结） | ff39c51deacfa439667c476a6998ea8a8f69387a（分支 wp04-t12；验收前复核 origin/wp04-t12 == 该 SHA，无分支漂移） |
| diff 范围 | 62db7b64fe6382558b40d9750b6cb37dbf973428（state.base＝origin/redesign-main）..ff39c51deacfa439667c476a6998ea8a8f69387a，共 2 提交（5006e293 实现＋测试＋单元卡 v0.14，10 文件 2638 insertions → ff39c51d 验证留痕 16 文件） |
| **verdict** | **pass**（4.1～4.11 全部通过、无阻断级问题；2 条建议级问题转登 findings.json F-161/F-162） |
| 验收环境 | 验收者全新上下文（未消费实施叙述）；复现现场＝detached worktree @SHA（D:\\10_Source_Repos\\21_robot\\.acc-prj-t12\\wt-a）；集成模式全新从零配置＋构建，独立冒烟另配临时目录（vcpkg toolchain 绝对路径）；MSVC 2022 x64 / Release；验收全程未向任务分支推送任何提交 |

## 4.1 改动范围 —— 通过

- `git diff --name-status 62db7b64..ff39c51d`：26 文件，全部落在契约 `allowedFiles` 内——`industrialrobot/project/**`（src/DraftServiceImpl.{hpp,cpp} 新增、include/.../DraftService.hpp 新增、ProjectStore.hpp、src/DiagRecords.hpp、src/ProjectStoreImpl.{hpp,cpp}、test/DraftServiceTest.cpp 新增、CMakeLists.txt）、`units/project.md`（3 行）、`traceability/builds/wp04-t12/**`（16 件）。
- `forbiddenFiles`（REQUIREMENTS.md/ARCHITECTURE.md）零触碰（diff name-only 反证在档）。
- SA-02：diff 范围无框架源码文件（`RobWork/RobWork`、`RobWork/RobWorkSim`、RWS 非 ird 部分三类反证查询均零输出）；`patches/` 无新文件（0001-rwslibs-entry-option.patch 与 PATCHES.md 均为 base 既有）——无未登记框架改动。

## 4.2 可复现构建 —— 通过（全新树从零，任务目标面）

- 集成模式：worktree A 全新从零配置（`-G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON`＋vcpkg toolchain＋`CMAKE_PREFIX_PATH=<Qt6 路径>`——主树缓存口径；**缓存确认 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`**）。`sdurws_ird_project_test`（契约 verify 目标）与 `sdurws_ird_project_contract_test` 两目标构建 **exit 0、error 计数 0**（验收者日志 .acc-prj-t12/verify-target-build.log、contract-build.log、final-test-run.log）。
  - 环境备注（同族既有登记，非实施缺陷）：首次配置失败两次——①Qt 未找到（缺 `CMAKE_PREFIX_PATH`，F-098 已登记）；②`RobWorkStudio.ini.template.static` 缺失（`[Bb]in/` gitignore 吞掉的本机辅助件，F-099 已登记）。自主树复制 ini 辅助件＋补 Qt prefix 后第三次配置零错误（configure.log，`Configuring done`＋`Generating done`）。
- 独立冒烟模式：独立临时目录 `.acc-prj-t12/smoke/build`（`-DCMAKE_TOOLCHAIN_FILE=<repo>/vcpkg/scripts/buildsystems/vcpkg.cmake`——AGENTS §4.1 口径）配置＋**全量构建 exit 0、错误 0**（smoke-configure.log、smoke-build.log）。
- 结论：两模式零错误，与送验报告 builds[].exitCode=0 一致；本验收为全新树从零构建。

## 4.3 契约 verify 命令 —— 通过

- 契约 verify 仅一条：`cmake --build build --config Release --target sdurws_ird_project_test`——在 worktree A 集成树（全新从零）亲手执行 **exit 0、error 计数 0**，测试 exe 正常产出并运行（首次构建即本命令；探针变异前后共重建 4 次均成功）。

## 4.4 acceptance 条目逐项证据 —— 通过（全部亲手执行）

| # | 契约 acceptance | 证据（验收者亲手复现） |
| --- | --- | --- |
| 1 | PRJ-TX-6 草稿用例：定时落盘（保存与应用分离——保存不产生修订，PM-04）→破坏（截断 JSON）→重开：损坏诊断＋.bak 恢复、.new 残留清理、旧草稿保留（D-12 单文件＋.bak 轮换）；放弃草稿语义（DiscardResult） | 集成 233/233、冒烟 233/233 含 `DraftServiceTest` 14 用例全绿。`SaveThenLoadRoundTripProducesNoRevision`（DraftServiceTest.cpp:351-385——save 后 revisions/ 计数不变＋磁盘 HEAD 不变＋drafts/ 内唯一名义文件＋无 .new/.bak 残留＋round-trip 全字段相等）；`CorruptCurrentRecoversFromBakAfterReopen`（:393-442——两代保存后裸写截断 current→close/open 重开→tryLoad 返回 .bak 的 v1＋diags 通道与 sink 通道双报 `PRJ-RECOVERY-ORPHAN-DRAFT`＋.bak 文件保留）；`StaleNewResidueDiscardedCurrentKeptOnReopen`（:449-492——裸写 .new 崩溃现场→重开加载→.new 被丢弃、current/.bak 原样保留＋devContains("discarded") 开发诊断可见）；`DiscardRemovesCurrentAndBakIdempotent`（:498-532——removedCurrent/removedBackup 双明细位＋放弃后 tryLoad 空＋幂等再放弃 ok=true 两位 false）。 |
| 2 | StaleRevisionRejected 冲突路径：应用＝恰好一个新修订、草稿基线过期拒绝并保留草稿（PM-04/AT-29——§8.3）；多模块草稿汇总投影用例过（§8.5 DraftProjection） | `ApplyCommittedExactlyOneRevisionFromDraft`（:544-576——submit(expectedRevision=base) 成功后 revisions/ 计数恰 +1＋磁盘 HEAD 前进至 newRevision）；`StaleBaselineRejectedAndDraftRetained`（:584-643——基线 r0 过期（并发提交推进 tip 至 r1）→Rejected(StaleRevision)＋零修订落盘＋结果诊断含 `PRJ-STALE-REVISION-REJECTED`＋草稿数据完整可读回＋origin=apply-retained 重存读回）；`SummarizeProjectsMultiModuleWithStaleAndSortOrder`（:651-708——alpha/beta/gamma 三条目按 moduleId 字典序、过期/新鲜 stale 两态逐条判定、损坏 gamma present=false＋开发诊断、与 list() 可用面一致两可用条目）。 |
| 3 | 生命周期边界：草稿与已应用修订的数据结构区别（§8.1）；只读打开与项目切换下的草稿行为（§8.6——只读不落盘）；60 s 默认落盘周期（PM-04-S1 归 WP-04-T20，本任务不提前实现——R2 边界） | `DraftOverwriteVsRevisionAppendOnly`（:722-776——同 (branch,module) 两次保存名义草稿文件恒 1（覆盖写 D-12）vs 两次提交修订目录 +2（只增 PA-2）＋草稿可携带非法中间态负载 `{{{not-json`）；`ReadOnlyOpenRejectsSaveAndDiscardAllowsRead`（:784-835——ReadOnly 重开后 tryLoad/list/summarize 全量可用而 save/discard 双双 LockHeldByOther＋`PRJ-LOCK-HELD`＋草稿磁盘字节逐字节不变）；`ClosedContextRejectsDraftWritesAndReads`（:842-883——requestClose 同步排空后写轨 ContextClosed 返回值＋`PRJ-WRITE-AUTHORITY-LOST`、读轨 tryLoad/list 抛 ContextClosed＋拒绝路径零磁盘副作用）；`AutosaveAndManualShareSingleSavePath`（:892-922——autosave/manual 同一入口同行为、origin 如实透传、均零修订——60 s 周期与定时器按 R2 归 WP-04-T20，本服务无定时器，边界即"落盘只发生在显式调用时"，与契约 R2 边界声明一致、未提前实现）。 |
| 4 | P-PR-1 处置：SourcedValue/ValueProvenance 来源标记透传（不解释——§3.2）以 core.md v0.1 为基线，冻结 diff 后增量同步、不私改 core | `PayloadWithSourcedValuePassthroughUnexplained`（:938-986——含 sourcedValue{raw,unit}＋value-provenance{source,scope,sourceVersion,recordedAt} 形态负载＋externalRefs 经 save→磁盘→tryLoad 全字段 round-trip 逐字节一致＋**不经服务层**直接 parseDraftDocument(磁盘字节) 断言 payload/externalRefs 一致——"project 未改写未解释"的磁盘面双证）。基线面：diff 范围 core/ 单元零触碰（4.1 反证）；BuildRedLineTest＋LinkageContractTest 在 233/15 集内全绿（零 Qt 扫描＋唯一单元边自动覆盖新增文件）。 |
| — | （ird_gates 执行状态） | 已建成（WP-01-T01 落地），本任务**实跑**：worktree A 构建 `ird_gates` 目标 exit 1（SA-10 设计行为——命中即非零），命中 **9 项＝7×IRD-GATE-SUB＋1×IRD-GATE-T1＋1×IRD-GATE-R4**，与留痕 ird-gates.log 及 F-145/wp04-t08～t11 基线逐项一致＝**零新增命中**（R4 唯一命中为 ProjectStoreImpl.cpp T08 既有工具版本串字面量；本任务新增文件零命中）。送验报告 gates.ird_gates 与验收者实测一致。 |

测试执行：集成 `sdurws_ird_project_test` **233/233 PASSED**、`sdurws_ird_project_contract_test` **15/15 PASSED**；冒烟两目标同数字 **233/233＋15/15**——与送验报告 claims 逐字一致（acc-prj-t12/acc-project-test-integration.xml、acc-contract-test-integration.xml、acc-project-test-smoke.xml、acc-contract-test-smoke.xml 四份 XML 为验收者亲跑产物；还原后复跑 final-test-run.log 233/233）。`validate-task` PASS（1 tasks）、`validate-docs` PASS（20 units, 12 trace entries, 150 task files）——均为验收者实跑。

## 4.5 测试真实失败能力 —— 通过（双探针即红、还原复绿）

- 探针 1（落盘协议）：DraftServiceImpl.cpp save 第 2 步 `.bak 轮换`条件 `if (currentExists)` 短路为 `if (false && currentExists)` → 重建后 `CorruptCurrentRecoversFromBakAfterReopen` **FAILED**（.bak 恢复断言依赖真实轮换，非恒真）。
- 探针 2（门卫面）：checkWriteGuard 第②道只读判定 `if (m_host.m_lock == nullptr)` 短路为 `if (false && ...)` → 重建后 `ReadOnlyOpenRejectsSaveAndDiscardAllowsRead` **FAILED**（只读拒绝断言依赖真实门卫，非恒真）。
- 全部还原后重建＋全量复跑 **233/233 PASSED**；`git status`/`git diff` 对 industrialrobot/ 零差异（复现现场与送验 SHA 逐字节一致，实施产物无残留改动）。

## 4.6 架构红线（AGENTS §5 八条）—— 通过

1. L2 零 Qt：DraftService.hpp/DraftServiceImpl.{hpp,cpp}/DraftServiceTest.cpp 及全部改动文件零 Qt include（`#include <Q|Qt` 扫描零命中；BuildRedLineTest 门内通过）。
2. R-1 业务域互链：project 非业务域六单元；无 modeling/requirements/kinematics/trajectory/dynamics/selection/optimization 依赖；跨单元仅 core 公共头消费（DiagData.hpp/Identity.hpp 实证位于 core/include/sdurws/ird/core/ 公共头路径）。
3. R-2 私有头：include 面仅 `sdurws/ird/core/*`＋`sdurws/ird/project/*` 公共头＋本单元 src/ 私有头（DraftServiceImpl.hpp 为 src/ 私有实现头不出 include/；win32/AtomicFile.hpp 为 T08 既有单元内部件）。
4. SA-02：diff 无框架文件、patches/ 零新增（见 4.1）。
5. R-4：本任务改动文件零 `RobWork` 字面量（grep 反证；ird_gates 唯一 R4 命中为 ProjectStoreImpl.cpp T08 既有 kCreatedWithToolVersion，本任务 diff 未触碰该常量）。
6. 权威唯一（PA-1）：草稿落盘语义归 project DraftService（§5.4 所有者）；修订写路径仍唯一归命令端口（本服务不产生修订——PM-04）；诊断码经 DiagRecords.hpp 唯一装配点（makeDraftCorrupt——diagnostics.md §8.2 映射行 draft-corrupt→PRJ-RECOVERY-ORPHAN-DRAFT，CR-08 不私造；落盘 I/O 无收编码走 reportDev）；定时器归 ui（本服务无定时器——R2 边界遵守）。
7. PA-2：save 不产生任何修订（revisions//HEAD 双观测断言在 4.4-1）；应用走命令端口恰一新修订；拒绝路径零修订落盘。
8. 无 old/ 恢复与历史实现继承（diff 零 old/ 引用；实现按单元卡 §5.4/§8.x 从零编写）。

## 4.7 注释规范 —— 通过

- 公共头全查：`DraftService.hpp`（399 行）——文件头（设计依据三锚点＋保存/应用分离背景＋落盘协议序列＋增量落位说明三项＋错误二分＋诊断产出＋线程模型＋生命周期所有权＋P-PR-1 处置）、三个结果类型与投影类型逐字段中文 Doxygen、五方法逐一 @brief/@param 方向/@return/@throws/复杂度/线程安全。
- 实现文件抽查（≥3 达标）：`DraftServiceImpl.hpp`（208 行——文件头实现形态/门卫复用口径/增量落位口径/错误语义/线程模型＋每方法契约）；`DraftServiceImpl.cpp`（840 行——落盘协议逐步注释〔第 0～3 步数学/业务含义＋失败路径走向〕、failWith 清理语义、门卫三道逐道、tryLoad 恢复序、summarize 一致性窗口、锁序约定）；`DraftServiceTest.cpp`（1052 行——文件头测试域语义约定＋14 用例逐一标注 acceptance 映射与断言依据）；另 diff 触及的 `DiagRecords.hpp`（makeDraftCorrupt 码值出处＋同码两形态说明）、`ProjectStore.hpp`（drafts() 访问器语义）、`ProjectStoreImpl.{hpp,cpp}`（装配序/析构序/锁序）均在位。
- §2.5 高危信息：物理单位（本任务无物理量计算——负载为不透明 canonical 字节）、坐标系/rad-deg（无角度量）、所有权（m_host 非 owning、m_drafts unique_ptr 持有、接口引用与上下文同生命周期）、线程约束（§9.8 任意线程写入口内部串行＋读轨并发安全＋锁序 lifecycle→writer）、错误语义（调用方错误 invalid_argument fail-fast vs 环境/状态错误返回值轨 vs 数据侧 try 轨三分类逐处标注）、确定性来源（NFR-COR-02 moduleId 字典序排序＋codec::dump canonical 字节＋目录枚举序 OS 相关的处理）——全部在位。

## 4.8 文档同步与 findings 相关性 —— 通过（2 条建议级转登）

- 实现与任务卡零语义偏差；实现口径①～④（DiscardResult 双锚点落位、discard 清理 .new、投影 present 位＝磁盘事实投影、save 不校验未知分支）按 DTB §5.4 于 DraftService.hpp/DraftServiceImpl.hpp 文件头＋units/project.md §15.4 v0.14 行双向登记；契约语义零变更声明与 diff 一致（§4.4.5/§5.4/§8.x 原文全部照语义实现）。`validate-task` PASS。
- 契约状态 ready→（合入段由所有者翻转 done，非验收者动作）。
- **findings.json 未关闭条目相关性核对**（139 条 open，最大编号 F-160）：
  - 无草稿服务/PRJ-T12 直接相关 open 条目（draft/草稿/autosave/自动保存全词扫描仅命中 F-15x 系 PRJ-T09 接口计数散文瑕疵——与 T12 无关）。
  - F-145（门禁基线 9 命中）：实测命中集合与登记逐项一致，零新增——复核成立。
  - F-098/F-099（集成复现环境口径）：本次验收再次踩中（Qt prefix＋ini 模板）——已有登记，无需新条目。
  - **卡头日期停留家族再现**（F-155/F-159 之后）：project.md 卡头『日期』行停留 2026-09-09，§15.4 v0.14 行为 2026-09-16——第九跳（**G-1 → F-161**）。
  - **提交标题家族再现**（F-012 系/F-158 之后）：ff39c51d『[governance] PRJ-T12 验证留痕：…』名词短语车道第四例；5006e293 动词开头合规（**G-2 → F-162**）。
- 可顺手消账项：无。正面观察：T11 的 G-1 幻影清单（F-157）缓解路径（git add -f 入库 .log）在本任务被实践——ff39c51d 的 16 件留痕含 11 份 .log **全部真实入库**（git ls-tree 实证 16/16；`check-ignore --no-index` 实证 `*.log` 规则仍命中、入库系强制跟踪生效）——F-157 缓解建议被采纳验证，条目本身属 T11 遗留由其责任方处置。

## 4.9 commit 质量 —— 通过（含 G-2 建议级）

- 两提交正文均为四段式（What/Why/How/影响面）＋`Acc-Covered:` 行；实现+测试+文档（5006e293）→留痕（ff39c51d）序列可独立回溯。
- 验证结论与留痕一致性：5006e293 声称"两模式 233/233＋15/15、门禁 9 命中零新增、validate-task PASS"、ff39c51d 声称的两模式结论与门禁结论经本验收逐项复现**全部为真**；实现期缺陷留证两项（AtomicFile 方法名误记/测试关闭后构造文档）如实登记无粉饰。
- 标题合规：5006e293『[WP-04-T12] 实现草稿服务…』动词开头合规；**ff39c51d 为 F-012 家族同型名词短语开头（G-2 → F-162）**。

## 4.10 留痕完整 —— 通过

- gtest XML 4 份（project/contract × integration/smoke）＋ird-test-report.json（acceptance 逐条对账＋builds/gates/implementationNotes 段）＋**11 份构建/测试/门禁/validate 日志全部在库**（git ls-tree 实证 16/16 文件——见 4.8 正面观察，本任务无幻影清单问题）。
- phase-one-validation.log：仓库自 CORE-T01 后无任务更新（grep 'PRJ-T' 零命中），ird-test-report.json＋builds/<branch>/ 为既成留痕载体（PRJ-T01～T11 全部先例一致）——按"或"口径满足（T11 验收同口径）。

## 4.11 偷懒/缩水实现扫描 —— 通过

- ①diff 范围 `TODO|FIXME|XXX|HACK|stub|placeholder|未实现|not implemented|暂不实现|待实现` 扫描：实现文件**零命中**；唯一命中为 test/DraftServiceTest.cpp 的 `StubHandlers` 引用（T09 既有测试处理器桩基建——合法测试基建，非缩水标记，无"触发式延后"豁免需求）。
- ②空实现：公共接口五方法（save/tryLoad/summarize/discard/list）全部有完整实现体（840 行），无仅返回默认值的空壳；门卫/票据/锁面为真实执行逻辑（4.5 双探针实证行为真实存在）。
- ③acceptance 逐条证据表：见 4.4——四条全部有具名测试＋行号级断言映射＋验收者亲跑 XML，无"已实现"声明依赖。
- ④测试强度：断言校验行为与边界（磁盘地面事实 revisions 计数/HEAD 读取/文件存在性逐字节、round-trip 全字段、幂等明细位、stale 两态、排序序、门卫拒绝码、关闭窗口、白名单九种恶意 token＋三种合法边界、跨项目拒绝、开发诊断通道可见性），非恒真；与 4.5 双探针联动实证会红。

## 问题清单（全部建议级，无阻断）

| # | 级别 | 发现 | 转登 |
| --- | --- | --- | --- |
| G-1 | 建议 | 卡头日期停留家族再现（F-155/F-159 之后第九跳）：project.md 卡头『日期』行停留 2026-09-09，§15.4 v0.14 行为 2026-09-16——版本半边落地、日期半边未同步 | F-161 |
| G-2 | 建议 | 提交标题家族再现（F-012 系/F-158 之后）：ff39c51d『[governance] PRJ-T12 验证留痕：…』名词短语车道再现（"PRJ-Txx 验证留痕"逐字模板第四例）；实现提交 5006e293 动词开头合规 | F-162 |

## 裁决

**pass** —— 4.1～4.11 全部通过，无阻断级问题；建议级 G-1/G-2 已转登 findings.json（F-161/F-162，随本 evidence 分支提交）。验收者不执行合入；送验对象冻结于 ff39c51deacfa439667c476a6998ea8a8f69387a。

## 验收者环境说明

- 复现现场：`git worktree add --detach D:\10_Source_Repos\21_robot\.acc-prj-t12\wt-a ff39c51deacfa439667c476a6998ea8a8f69387a`（验收后 `git status`/`git diff` 零差异还原）。记录回传：`git worktree add -b acc/PRJ-T12/1 <wt-b> origin/redesign-main`。
- 集成配置命令（主树缓存口径）：`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=<repo>/vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`，并自主树复制 `RobWork/RobWorkStudio/bin/RobWorkStudio.ini.template.static`（F-098/F-099 环境缺口，非本任务缺陷）。
- 冒烟配置：`cmake -S RobWork/RobWorkStudio/src/rwslibs/industrialrobot -B <tmp> -DCMAKE_TOOLCHAIN_FILE=<repo>/vcpkg/scripts/buildsystems/vcpkg.cmake`。
- 验收者亲跑产物（临时目录，含四份 gtest XML、构建/测试/门禁日志）：`D:\10_Source_Repos\21_robot\.acc-prj-t12\`。
