# EV-T04 实施验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | EV-T04 切片与身份（Slice）——契约 tasks/foundation/EV-T04.json |
| 送验对象（冻结） | `d58a64b0ff67447d07b97890118b1a32f946583c`（origin/wp05-t04 尖端与该 SHA 逐字一致——分支漂移检测通过） |
| diff 范围 | `62283b97291300900c187b767985093c09afe79d..d58a64b0ff67447d07b97890118b1a32f946583c`（3 提交：3750ed9b 实现／ee4fb9f3 留痕／d58a64b0 单元卡） |
| 验收日期 | 2026-09-13 |
| **verdict** | **PASS**（4.1～4.11 全部通过，无阻断级问题；建议级 1 条转登 F-061） |
| 验收环境 | 独立验收 worktree（`git worktree add --detach @d58a64b0`）全新构建树；MSVC 2022 x64＋vcpkg toolchain＋Qt 6.11.1；记录经 acc/EV-T04/1 分支回传 |

## 独立性声明

- 本验收为全新上下文（PIPE §0.4 派发模式），未消费实施者任何过程叙述；只读任务契约、设计文档、单元卡与留痕产物。
- 构建与测试由验收者**亲手复现**（独立 worktree 全新配置构建＋双模式测试运行），未采信实施者运行结果。
- 对抗式立场：4.5 双探针注入点（NaN 拒绝移除／CR-05 必填校验禁用）与实施者自证探针（CR-02 Policy 吸入投影）**不同位**，独立验证了测试真实失败能力。
- 送验对象全程冻结：验收期间无任何提交写入 wp05-t04；复现现场的临时改动（探针）在验收内 `git checkout` 逐字还原并复绿。

## 4.1～4.11 逐项证据

| # | 结论 | 证据（命令/文件/输出） |
| --- | --- | --- |
| 4.1 改动范围 | **通过** | `git diff --name-only 62283b97..d58a64b0` 共 23 文件：①evidence 单元 7 件（Slice.hpp/.cpp＋SliceTest.cpp 新增，Errors.hpp/.cpp/ErrorsTest.cpp/CMakeLists.txt 修改）⊂ allowedFiles `RobWork/RobWorkStudio/src/rwslibs/industrialrobot/evidence/**`；②traceability/builds/wp05-t04/ 15 件 ⊂ allowedFiles `traceability/**`；③units/evidence.md 1 件 ∈ allowedFiles。forbiddenFiles（REQUIREMENTS.md/ARCHITECTURE.md）零触碰（diff 空输出）；`git diff --name-only … -- "*patches*"` 零命中——无未登记框架源码改动（SA-02 ✓） |
| 4.2 可复现构建 | **通过** | 集成：验收 worktree 全新构建树 `cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON`（配置 exit 0，`grep CMakeCache` 确认 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`），目标面 `cmake --build build --config Release --target sdurws_ird_evidence_test -j` 零 error（日志 grep error 计数 0）。冒烟：全新独立树 `cmake -S <worktree>/RobWork/RobWorkStudio/src/rwslibs/industrialrobot -B <tmp> -DCMAKE_TOOLCHAIN_FILE=<vcpkg 绝对路径>`＋`cmake --build --config Release` exit 0。5 个 ird 测试目标（core/policy/runtime/testkit/evidence）在验收 worktree 从零构建全部零错误。全树构建未执行——F-008 基线缺陷判例口径（以任务目标面为准）。注：全新树配置按 F-007/F-009 口径需补 vcpkg toolchain＋Qt `CMAKE_PREFIX_PATH`＋被 `.gitignore [Bb]in/` 吞掉的框架辅助文件（RobWorkStudio.ini.template.static/.ini.shared.in，自主仓本地复制）——环境缺件非送验对象问题 |
| 4.3 契约 verify | **通过** | 契约 verify 唯一条目 `cmake --build build --config Release --target sdurws_ird_evidence_test` 验收者逐字执行：exit=0，零 error（增量重跑日志 /tmp/acc-ev-t04-verify-cmd.log） |
| 4.4 acceptance 三条 | **通过** | 逐条证据见下节"acceptance 逐条证据表"。ird_gates 真实执行（主仓只读复跑，非"未执行"标注）：恰好 3 处命中均为既有（IRD-GATE-T1＋IRD-GATE-SUB testdata_lint＝F-019 两项；IRD-GATE-SUB runtime_test→testkit＝F-051 一项），evidence 面零新增——与送验留痕 ird-gates.log 及 ird-test-report.json irdGates 字段逐项吻合 |
| 4.5 测试真实失败能力 | **通过** | 探针 A（独立注入点：删除 `SliceCodec::canonicalF64` 的 NaN/±Inf 编码入口拒绝→重建→运行）：`EvidenceSliceFloat.CanonicalF64NaNInfRejected` 如期变红（3 处 "预期抛出 EvidenceError，但未抛"，exit 1）。探针 B（`SliceBuilder::build` 第 6 步 CR-05 必填校验条件置 false→重建→运行）：`EvidenceSliceCr05.MandatoryEnvironmentEntries_CR_05` 如期变红（exit 1）。两探针注入后均 `git checkout` 逐字还原＋重建复绿 56/56（worktree 状态回零：`git status --porcelain` 空）。测试非恒真、实现-断言联动真实 |
| 4.6 架构红线 | **通过** | ①L2 零 Qt：Slice.hpp/.cpp/SliceTest.cpp grep Qt 头零命中（仅 std＋core/evidence 公共头）；②R-1 业务域互链：evidence 全目录 grep `ird/(modeling\|requirements\|kinematics\|trajectory\|dynamics\|selection\|optimization)` 零命中；③R-2 跨单元私有头：Slice.hpp 仅 include `<sdurws/ird/core/Digest.hpp>`（公共头路径）＋本单元公共头；④SA-02：见 4.1；⑤R-4 名称前缀拼接：grep 零命中；⑥PA-1：Slice.cpp 身份/摘要单点经 core::ContentDigester、错误码仅产建议码面（Errors.hpp 注释显式"码值权威归 StableCodeRegistry，本函数不承担注册职责——PA-1"）、evidence 无 CanonicalModel 重编码入口（Slice.cpp 逐行走读确认）；⑦PA-2：不涉历史写路径；⑧无 old/ 继承痕迹 |
| 4.7 注释规范 | **通过** | 公共头全查：Slice.hpp 418 行——文件头（设计依据逐条列 §4.2.2/§4.2.3/§5.1/§5.2＋CR-02/CR-05＋需求编号＋契约 acceptance 映射＋"为什么切片/双层身份是缓存正确性根基"背景＋线程安全契约）＋每个类/函数 Doxygen 中文（@param 方向/取值范围/@throws 触发条件/复杂度）。实现抽查 ≥3：Slice.cpp（实现头 97 行：字节布局表＋校验顺序固定说明＋D-1～D-5 口径登记＋逐步校验注释"第 1 步～第 7 步"）；SliceTest.cpp（每个 TEST 注明需求/AT 追溯＋替身边界声明＋共享样例对齐锚点说明）；Errors.hpp/.cpp/ErrorsTest.cpp 修改段（9→10 值口径注释同步，含 F-056 补登追溯）。本任务为身份/编码面（无物理量/坐标系语义），单位制标注不适用；确定性来源（大端/规范化排序/显式空白表无 locale）均有注释——§2.5 高危信息表逐项落实 |
| 4.8 文档同步＋findings 核对 | **通过** | 单元卡 v0.5（d58a64b0）：§15.4 追加行与实现逐项对照一致（InputSlice 字段面/冻结协议校验顺序/SliceCodec 双形态/D-1～D-5/CR-05 落地/测试 56 计数），卡头版本链推进合规（v0.4 行原文保留，grep 计数核对过）。实现偏差全部属"实现细节精确化"且已按 DTB §5.4 登记。findings.json 46 条 open 逐条核对：相关条目 F-019/F-051（门禁既有命中——本任务 report 按 findings 权威编号逐项标注，F-060 整改方向落实）、F-056（本任务新增第 3 处补登建议码 EVI-SLICE-INCOMPLETE，实施注释显式"F-056/EV-T04 转 owners 裁决"）、F-020/F-058（evidence.md 既有损毁——本任务修订面未触碰未恶化，归所有者）、F-046（Snapshot.cpp 风格——异文件；新文件 Slice.cpp 逐行通读无同型问题）、F-007/F-009/F-010/F-055（环境/留痕纪律——实施已正确应对：git add -f 留痕、报告声明构建树性质与配置）；F-012 家族 16 例（提交标题）/F-025/F-041（计数）/F-028/F-035（门禁措辞）/F-036/F-038（构建口径）——本任务三提交全部动词开头、全部计数实测吻合、门禁真实执行、口径表述准确，**零复现**；其余（TK-T03 系列/IO-T07/EX-T09/UI 系列等）与本任务无涉 |
| 4.9 commit 质量 | **通过** | 三提交标题均 `[WP-05-T04]`＋动词开头（实现/登记/登记）；正文四段（What/Why/验证/影响面）逐提交齐备；验证段与验收者实测逐项吻合：56/56 双模式 ✓、冒烟全量 394/394（core 61＋evidence 56＋policy 4＋runtime 176＋testkit 97）✓、集成回归 core 63＋policy 4＋runtime 260（＋1 例 worker 占位按设计跳过）＋testkit 97 ✓、ird_gates 3 既有命中 ✓、validate-task PASS ✓——无粉饰 |
| 4.10 留痕完整 | **通过** | traceability/builds/wp05-t04/ 15 件全部入库（*.log 经 `git add -f` 对抗 .gitignore——F-010 口径正确）：双模式 gtest XML 各一份（integration：tests=56 failures=0；smoke：tests=56——实测 tag 内容核对）；ird-test-report.json（ird-test-report/1，双模式计数＋单元回归面＋irdGates 逐项编号归属＋构建树性质显式区分）；integration-cache-check.txt（BOOL=ON）、构建/测试/verify/门禁/validate 日志齐备；探针三件（red-build/red-run/restore-build）。验收者实测数据与留痕零出入 |
| 4.11 偷懒/缩水扫描 | **通过** | ①diff **新增行** grep `TODO\|FIXME\|stub\|placeholder\|未实现\|not implemented` 零命中（唯一全文命中为 base 既有上下文行——EV-T03 §15.4 历史登记"本任务未实现该函数"，`git show 62283b97:…` 反证在档）；②空实现检测：Slice.cpp 869 行逐行走读，无空函数体/仅返回默认值的公共接口；③acceptance 逐条证据表见下节，三条全有具名测试＋命令输出；④测试强度：断言校验行为与边界（错误码面＋detail 就地定位＋身份字节等值＋位模式逐字节已知向量 0x3FF0000000000000＋-0.0/1e-15 差异面），4.5 双探针证明非恒真 |

## acceptance 逐条证据表（契约 acceptance 数组）

| # | acceptance 条目 | 证据 | 结论 |
| --- | --- | --- | --- |
| 1 | canonical 往返＋浮点位模式＋NaN 拒绝用例通过；EV-ID-1/2 通过 | `EvidenceSliceIdentity.CodecRoundTrip_EV_ID_1`（parse(encodeFull(x))==x 全字段＋双身份重算＋二次编码恒同）、`SameContentDeterministicId_EV_ID_1`（逆序/正序添加同身份）、`NoFloatApproximateEquivalence_EV_ID_2`（1e-15 差→sliceId 必不等）、`CanonicalF64BitPatternRoundTrip`（1.0=0x3FF0… 大端已知向量＋10 值含 -0.0/5e-324 位级往返）、`CanonicalF64NaNInfRejected`（NaN/±Inf 入口＋接收端双端拒绝）——全部在验收者亲手复现的双模式 56/56 中通过 | ✅ |
| 2 | CR-02：SliceCodec 排除字段与纪律按 foundation-api-diff.md CR-02 登记（摘要只经 core ContentDigester） | ①登记面：foundation-api-diff.md §CR-02 排除字段表已预登记 SliceCodec 行（magic IRDSLCE1/身份排除项），"实现期以各自 codec 单测钉住"义务由下列单测履行；②摘要单点：`DigestViaCoreContentDigesterOnly_CR_02` 手工重算 sliceId/inputBaselineId＝core ContentDigester 对对应形态编码的 SHA-256（Slice.cpp computeSliceDigest 为全文件唯一摘要通道）；③排除/参与面钉住：`Cr02ExcludedAndIncludedFieldsPinned_CR_02_CR_05`（排除面：改 Configuration/Policy/非基准 Environment→inputBaselineId 不变；参与面：改 Object/SampleSet/NameMap/CR-05 基线串→双身份均变） | ✅ |
| 3 | CR-05：Environment 条目 runtime.model-identity/runtime.robwork-baseline 必填校验与编码；该二条目进入 inputBaselineId；与 RT-T09 共享同一样例断言身份一致（evidence 不重算 CanonicalModel） | ①必填与编码：`MandatoryEnvironmentEntries_CR_05`（缺任一保留条目→build 拒绝且 detail 定位 token；重复保留条目拒绝；开关 false 不强制——"谁必填由组装方按评估器声明置位"）＋`ValueShapeGate_CR_05`（cid-\<64hex\> 形态/无空白恒久闸门）；②进入 inputBaselineId：`isBaselineEnvironmentToken` 封闭白名单＋`encodeBaselineProjection`（改基线串→双身份均变——`Cr02Excluded…` 参与面 4）；③共享样例：runtime 侧 RT-T09 用例 `SnapshotTest.Cr05ValueSupplyEnvironmentEntries_CR_05` 在验收者亲手复现的 runtime 260/260 中通过（值供给同一性断言）；evidence 侧 `SharedSampleValuePassThrough_RT_T09`（供给值→条目→编码→parse 全链逐字节零重编码＋CR-05 联合断言 2/3 身份行为）；④evidence 不重算 CanonicalModel：Slice.cpp 无任何模型重编码入口（逐行走读）；"共享同一样例"的实现口径（形态对齐＋值传递透明＋两侧联锁，因 modelIdentity 含构建采集值跨构建不稳定、两侧无共享常量机制）已在 units/evidence.md v0.5⑤＋SliceTest.cpp 文件头按 DTB §5.4 登记——登记衍生问题转登 **F-061**（runtime 侧注释与该口径失配），不构成阻断 | ✅ |

## 问题清单

### 阻断级

无。

### 建议级（已转登 traceability/findings.json F-061）

1. **F-061｜RT-T09 联合断言注释与实现失配**：runtime/test/SnapshotTest.cpp `Cr05ValueSupplyEnvironmentEntries_CR_05` 注释声称 evidence 侧与其"逐字节一致"断言共享样例值，而 evidence 侧实际实现为"输出形态对齐＋值传递全链零重编码"的等价断言面（该口径本身已在单元卡 v0.5⑤ 按 DTB §5.4 登记，实体主张全部有测试证据，EV-T04 判 pass 不受影响）。风险＝后读工程师按 runtime 侧注释误判测试覆盖。处置建议与责任方见 findings.json F-061。

## 验收者环境补充说明（复现口径，供返工/复核引用）

- 复现现场：`git worktree add --detach <tmpA> d58a64b0ff67447d07b97890118b1a32f946583c`；验收内探针改动已全部逐字还原（worktree clean）。
- 全新集成树配置需 F-007 登记的三件补齐：vcpkg toolchain 绝对路径＋`CMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`＋框架辅助文件 `RobWorkStudio/bin/RobWorkStudio.ini.template.static` 与 `RobWorkStudio.ini.shared.in`（被上游 `.gitignore [Bb]in/` 规则吞掉、git 树中不存在，自主仓本地复制）。
- runtime 测试可执行在 MSYS bash 环境报 exit 127（MSYS 加载器假象），cmd 下真实运行 260/260（含 1 例 `ContractSuite.ChildProcessWorker_RT_ID_1` 按设计 SKIPPED）。
- 主仓只读复跑（HEAD=d58a64b0＝送验对象）：`gate-all.ps1` 14/15（唯一 FAIL＝ird_gates 退出码 1，命中恰为 F-019×2＋F-051×1 既有）；`validate-task.ps1` PASS；`validate-docs.ps1` PASS（20 units, 12 trace entries, 150 task files）。
- 本记录经 acc/EV-T04/1 分支回传（worktree B 基于 origin/redesign-main），commit SHA 见分支尖端。
