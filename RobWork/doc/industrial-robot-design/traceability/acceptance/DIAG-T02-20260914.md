# DIAG-T02 验收记录（对抗式验收）

| 字段 | 值 |
| --- | --- |
| 任务 ID | DIAG-T02（masterWp WP-D/WP-09-T02，构建落位：diagnostics 占位转真实库） |
| 契约 | tasks/foundation/DIAG-T02.json（status: ready，随收尾翻转 done） |
| 验收对象 | 送验对象冻结为 `b5c9504eab8482c60b6653b27cf93d450c5dce75`（分支 `wp09-t02` 尖端）；base..head＝`aa34f3a8..b5c9504e`（base＝`aa34f3a87f66914f81ff07a07976bea97ab9e35f`＝送验时 origin/redesign-main 尖端，单提交）；漂移检测 PASS（`git rev-parse origin/wp09-t02`＝送验 SHA） |
| 尝试序号 | attempt=1（evidence 分支 `acc/DIAG-T02/1`） |
| **verdict** | **pass**（无阻断级；2 条建议级转登 findings.json F-100/F-101） |
| 验收日期 | 2026-09-14 |
| 独立性声明 | 全新上下文验收会话；只消费契约、设计文档、送验提交产物与留痕，未消费实施者任何过程叙述；构建与测试全部由验收者在独立 worktree 亲手重跑，不引用实施运行结果 |

---

## 验收者环境说明

- 复现现场：`git worktree add --detach D:/10_Source_Repos/21_robot/_acc_diagt02_a b5c9504e…`（detached @ 送验 SHA）；记录回传另用 `git worktree add -b acc/DIAG-T02/1 …_acc_diagt02_b origin/redesign-main`。
- 集成模式全新树（worktree A 内 `build/`）配置口径＝主树缓存实证口径（非 AGENTS §4.1 字面命令——该缺口即 F-007/F-062/F-098/F-099 登记项，本次全部再次亲历）：
  `-G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=<仓库根>vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64 -DCMAKE_CXX_FLAGS="-DNOMINMAX -DWIN32_LEAN_AND_MEAN -DWIN32`；配置成功后缓存确认 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`。
- 环境前置三件（全部由验收者自行补齐，不属送验产物）：①vcpkg toolchain（Boost 解析必需）；②Qt6 CMAKE_PREFIX_PATH；③`RobWorkStudio.ini.template.static`＋`RobWorkStudio.ini.shared.in` 两件 git 未追踪本地辅助件自主树复制（F-099 实证同款；缺件首配即 `CMake Error: …ini.template.static does not exist`）。
- 冒烟模式：独立目录 `_acc_diagt02_smoke`，源＝worktree A 的 `industrialrobot/`，toolchain 绝对路径指向主树 vcpkg（AGENTS §4.1 冒烟口径）。
- 验证脚本：`pwsh -File RobWork/scripts/industrialrobot/validate-task.ps1 -TaskFile <契约>`（worktree A 上下文，与调用方式无关口径复验）。

---

## 逐项证据

### 4.1 改动范围 —— PASS

- diff＝`aa34f3a8..b5c9504e` 共 1 提交、20 文件（+1320/−9）：`industrialrobot/CMakeLists.txt`（修改）、`diagnostics/**`（CMakeLists.txt＋src/Diagnostics.cpp＋test/ 两文件，均新增）、`units/diagnostics.md`（v0.2→v0.3）、`traceability/builds/wp09-t02/`（13 件留痕）。
- 全部落在契约 allowedFiles（`industrialrobot/diagnostics/**`＋`industrialrobot/CMakeLists.txt`＋`units/diagnostics.md`＋`traceability/**`）内：排除 allowedFiles 模式后 `git diff --name-only | grep -v …` 零输出（exit=1）。
- forbiddenFiles（REQUIREMENTS.md/ARCHITECTURE.md）零触碰；`patches/` 无新文件、框架源码（`RobWork/RobWork/src`、非 industrialrobot 的 `RobWorkStudio/src`）零触碰（同法 grep 零输出）——SA-02 合规。

### 4.2 可复现构建 —— PASS

- **集成模式**（全新树、重新配置、`RWS_BUILD_INDUSTRIALROBOT:BOOL=ON` 确认）：配置 exit 0；全量 `cmake --build build --config Release` **exit 0、`: error`/`: fatal error` 零命中**（按上述主树完整口径——首两轮以缺省 CXX_FLAGS 构建时全量 exit 1，73 错全部集中于 RWSimulatorPlugin(71)＋rwplugin(2) 的 min/max 宏污染簇，即 F-008/F-098 已登记框架基线缺陷，与本任务无关；补齐主树口径 CXX_FLAGS 后从零全量构建零错误，同步反证该 73 错为环境口径问题）。
- `sdurws_ird_diagnostics.lib` 静态库产物在集成构建树生成（`build/RobWorkStudio/libs/Release/sdurws_ird_diagnostics.lib`）——INTERFACE→STATIC 落位实证。
- **独立冒烟模式**：配置 exit 0、构建 exit 0、error 计数 0（日志 `_acc_diagt02_a/acc-smoke-configure.log`/`acc-smoke-build.log`）。
- 送验留痕 `integration-build.log` 产自主树存量增量树（路径 `D:\...\RobWork\build\`，RWSimulatorPlugin 仅 2 次提及＝跳过）——commit 正文声称"全量构建 exit 0"在该口径下如实、无"从零"修饰，不构成粉饰；本验收以全新树从零口径补强（见上）。

### 4.3 契约 verify 命令 —— PASS

逐条亲手执行（worktree A 集成树）：
- `cmake --build build --config Release --target sdurws_ird_diagnostics_test` → **exit 0**（exe 产出＋gtest DLL 拷贝完成）；
- 追加同形态验证 `--target sdurws_ird_diagnostics_contract_test` → **exit 0**。

### 4.4 acceptance 三条逐项 —— PASS

| # | 契约条目 | 证据 |
| --- | --- | --- |
| 1 | 双模式构建零错误：INTERFACE 占位升级 STATIC（C++17、PUBLIC 链 RWS::ird::core、零 Qt 含 Core——D-01） | 两模式构建 exit 0（4.2）；`sdurws_ird_diagnostics.lib` 产出；CMakeLists.txt:64-66 C++17 双设（`target_compile_features(PUBLIC cxx_std_17)`＋`CXX_STANDARD 17/REQUIRED/EXTENSIONS OFF`）；:82 `target_link_libraries(… PUBLIC sdurws_ird_core)`；零 Qt＝运行期 NoQtInclude 用例两模式全绿＋配置期守卫（Qt 正则 FATAL_ERROR）＋验收者 grep `#include <Q` 产品面零命中三重证据。别名 `RWS::ird::diagnostics` 于集成模式创建（CMakeLists.txt:89-91 条件守卫） |
| 2 | 红线扫描零命中：仅 diagnostics→core 一条单元边、零 Eigen/rw::math 直接包含、无私有头出 include | BuildRedLineTest 5 用例两模式全绿（验收者亲手运行）；include 面人工核查：`grep -rn "#include" include src test` 全集＝gtest＋std＋`sdurws/ird/core/` 四公共头，零 Eigen/rw/其他单元；公共头保留位 `include/sdurws/ird/diagnostics/` 下仅 README.md（PublicHeaderPathLayout 规则自动生效）。集成 `ird_gates` 目标亲手重跑：命中序列与送验留痕 `gates-current-wp09t02.log` 逐字一致（7 正式命中＋7 引擎自测），全部为 F-072/F-019 既有辖区（evidence/policy/runtime 测试直链 testkit×5＋testdata_lint 表外边＋T1），**diagnostics 零涉及、零新增命中** |
| 3 | `_test`/`_contract_test` 按 dtb §5.5 注册；P-DIAG-8 处置（契约测试以桩＋契约夹具验证，不私改 execution 侧链接） | diagnostics/CMakeLists.txt:22 `find_package(GTest CONFIG REQUIRED)`（REQUIRED 失败即停）；`ird_add_gtest` 宏＝add_test＋XML 报告目标（EXCLUDE_FROM_ALL）；contract 目标链接面＝仅 `sdurws_ird_diagnostics`＋`GTest::gtest_main`（零 execution/project 链接改动）；LinkageContractTest.cpp 文件头 P-DIAG-8 处置说明＋governance-log.md:121 P-DIAG-8 行 open 登记一致（"契约测试以桩验证；EX-T01 后补双库集成冒烟"） |

### 4.5 测试真实失败能力 —— PASS（验收者独立构造 4 处，均真实变红）

在冒烟树（已构建基线）注入后重编重跑：

| 探针 | 注入 | 结果 |
| --- | --- | --- |
| A | `src/Diagnostics.cpp` 尾部追加注释式 `// #include <QObject>` | `NoQtInclude_DT_BUILD_NFR_MNT_01` **FAILED** |
| B | 同文件追加注释式 `// #include <rw/math/Vector3D.hpp>` | `NoEigenOrRwMathDirectInclude_DT_BUILD` **FAILED** |
| C | `diagnostics/CMakeLists.txt` 追加 `set(_acc_probe sdurws_ird_execution)`（配置不受影响，文本扫描命中） | `UnitEdgeOnlyCore_DT_BUILD_R1_R2` **FAILED**（contract 侧）；同轮 SourceTreeReachable/NoCrossUnitInclude/PublicHeaderPathLayout/PublicCoreExposure/ConsumerStubCoreFinding 保持绿——失败定位精确非全红 |
| D | 还原 A/B/C 后将 `LinkageContractTest.cpp:112` 断言改为 `EXPECT_EQ(record.code, std::string{"ACC-T45-WRONG-CODE"})` | `PublicCoreExposure_DT_BUILD` **FAILED**（112 行断言报错）——断言非恒真 |

全部还原后重编：5+3 全绿、`git status` 零残留。

### 4.6 架构红线（AGENTS §5 八条）—— PASS

1. **L2/L3 零 Qt（含 Core，D-01）**：源码 include 面零 Q 头（4.4 表 2）；CMake 零 Qt 链接＋配置守卫禁止；L3 平台服务按单元卡 D-01 执行比上限更严口径 ✓
2. **业务域互链禁止（R-1）**：链接面仅 core 一条单元边（LINK/INTERFACE 两面读取守卫＋运行期扫描＋ird_gates 零涉及 diagnostics）✓
3. **跨单元私有头禁止（R-2）**：include 的 core 四头（DiagData/Identity/Provenance/Units.hpp）均在 `core/include/sdurws/ird/core/` 公共根；diagnostics 自身公共 include 根零私有头 ✓
4. **框架零源码修改（SA-02）**：4.1 已证 patches/ 无新文件、框架源码零触碰 ✓
5. **不做名称前缀拼接/剥离（R-4）**：新增代码无任何 RobWork 名称前缀处理逻辑（测试内字符串处理仅为构建图扫描逻辑，非 runtime 名称解析语义）✓
6. **权威唯一（PA-1）**：纯构建落位任务，无语义实现；未预建码表/注册表（StableCodeRegistry 随 DIAG-T03，注释明示 NFR-MNT-04 不预建）✓
7. **不可变历史（PA-2）**：无修订写路径触碰 ✓
8. **不继承 old/**：全目录 grep `old/` 零命中 ✓

### 4.7 注释规范（AGENTS §2）—— PASS

新增文件全部通读（本任务无新增公共代码头；include/ 仅 README 保留位非代码）：
- `src/Diagnostics.cpp`：文件头 Doxygen 中文齐备（@file/@brief/设计依据含契约与单元卡行引用/背景说明/后续任务落位安排/线程约束段）；空起步原因与"为什么"充分；
- `test/BuildRedLineTest.cpp`：文件头＋辅助函数中文说明＋每用例 `/** */` 说明；用例名带 DT_BUILD/NFR_MNT_01/R1_R2 追溯字段；失败信息中文＋确定性来源（"相对路径、已排序——失败信息确定性，NFR-COR-02 精神"）；"读失败显性失败（不留假阳性通道）"的边界说明；
- `test/LinkageContractTest.cpp`：文件头＋P-DIAG-8 处置专段；物理单位显式（"实际 6.4 rad，期望 2π rad"）；异常语义（"core/diag/c1:……调用方错误 fail-fast"）；core 契约 C-1/C-2 不变量注释；
- `diagnostics/CMakeLists.txt`（216 行）：分节中文注释块（gtest 接入理由/ODR 警示、/utf-8 的 MSVC GBK 误读背景、统一真名 vs 别名的两模式取舍、POST_BUILD DLL 拷贝的运行期依据、ird_add_gtest 宏、文件末配置期守卫的 NOTFOUND 行为说明）；
- 顶层 `industrialrobot/CMakeLists.txt` 增段：挂接次序约束（core 之后、消费方之前）与状态行同步注释。

### 4.8 文档同步 —— PASS

- `units/diagnostics.md` v0.3：卡头"构建落位"字段与 §1.2 构建骨架行实测刷新＋§14.4 变更记录条目——与实现逐项对上（STATIC/C++17/PUBLIC 链 core/零 Qt/别名/两测试目标/配置期守卫/src/ 空起步）；
- §11 DIAG-T02 任务行（1165 行）产物定义逐项核对零偏差：`diagnostics/CMakeLists.txt`（新）✓、`src/` 空起步 ✓、STATIC＋C++17＋链 core＋零 Qt ✓、`_test`/`_contract_test` 注册 ✓；
- §3.3:201 文字为 `target_link_libraries(… PUBLIC RWS::ird::core)`（别名）而实现用统一真名 `sdurws_ird_core`——**evidence（evidence/CMakeLists.txt:76）与 policy（policy/CMakeLists.txt:135）先例同款**（别名仅集成模式存在，真名两模式可解析；实现注释已披露理由），属先例既定取舍而非本任务新偏差；
- findings.json 未关闭条目（80 条 open）相关性核对：**F-007/F-009/F-062/F-098/F-099**（全新树集成配置环境链）——本验收亲历并以上述环境说明实证，非本任务消账对象（AGENTS/框架侧文档缺口）；**F-008**（RobWorkSim 插件宏污染）——本次缺省 CXX_FLAGS 轮全量构建 73 错全部再现，补主树口径后归零（同步反证）；**F-065**（留痕日志中文双重编码乱码）——送验 `gates-current-wp09t02.log` 延续存在（"娴嬭瘯鐩爣"型），系统性问题非本任务引入；**F-019/F-051/F-060/F-072/F-097**（ird_gates 红基线辖区）——本次 7 命中与其登记完全一致、零新增，处置权 WP-01；**F-010 家族**（.gitignore *.log 吞留痕）——本任务以 `git add -f` 正确规避，13 件留痕全部实际入库（diff --stat 实证），未复踩。

### 4.9 commit 质量 —— PASS（附 1 条建议级）

- 四段齐备（What/Why/验证/影响面）＋Acc-Covered: 1,2,3；
- 验证结论与留痕逐项比对一致：ird_gates"7 命中＝基线逐码一致、零新增"（本验收亲手复现证实）、"测试真实失败能力自证"（本验收独立重演证实）、validate-task PASS（复跑证实）——无粉饰；
- 标题 `[WP-09-T02] diagnostics 单元构建落位：INTERFACE 占位升级 STATIC 真实库` 前缀后以单元名词"diagnostics"开头，非动词开头——F-012 家族再现，建议级转登 **F-100**。

### 4.10 留痕完整 —— PASS

- 送验提交内 `traceability/builds/wp09-t02/` 13 件全部在库：构建/测试/门禁/validate 日志 8 份＋gtest XML 4 份＋ird-test-report.json；
- 4 份 XML 实测：integration 5+3、smoke 5+3，`failures="0"` 全部一致（2026-09-14 时间戳）；
- `ird-test-report.json`（schemaVersion ird-test-report/1）与验收者实测逐项吻合：8 用例两模式全绿、gates 7 命中基线逐码、validate-task PASS、realFailureEvidence 手法与重演一致；
- validate-task.ps1 复跑：`validate-task: PASS (1 tasks)` exit 0。

### 4.11 偷懒/缩水实现扫描 —— PASS

- ①关键词扫描：`TODO|FIXME|placeholder|未实现|not implemented` 于送验 diff 新增行与 diagnostics 全目录**零命中**；`stub` 唯一命中＝测试名 `ConsumerStubCoreFindingContracts_P_DIAG_8`（P-DIAG-8"桩处置"的契约显式命名，非未实现标记）；
- ②空实现：`Diagnostics.cpp` 仅注释无代码——为契约/单元卡 §11 DIAG-T02 行产物定义"src/ 空起步"的显式安排（锚点翻译单元＝STATIC 库最小条件；CORE-T01/POL-T01 同先例），豁免依据成立，非缩水；公共接口函数零个（落位期无接口，无预建——NFR-MNT-04）；
- ③逐条证据表：见 4.4——三条 acceptance 全部有具名测试/命令输出/文件行号支撑；
- ④测试强度：断言校验行为与边界（C-1 反例 EXPECT_THROW、C-2 凭据耦合、PUBLIC 传染行为级自证、扫描类用例有"扫描失效"自保断言 ASSERT_FALSE(refs.empty())）；与 4.5 联动的抽改验证证实非恒真。

---

## 问题清单

### 阻断级

无。

### 建议级（转登 findings.json）

| # | 问题 | 转登 |
| --- | --- | --- |
| G-1 | 送验提交 b5c9504e 标题前缀后以单元名词"diagnostics"开头，非动词开头——F-012 家族再现（该家族已 20+ 例，建议在 PIPE/AGENTS 层做模板强制而非逐例登记） | **F-100** |
| G-2 | 全新集成树配置缓存污染陷阱（F-007/F-098/F-099 家族补充形态）：首次无 toolchain 的失败配置会在 build 缓存残留 `Boost_DIR-NOTFOUND` 等失败值，此后**追加 toolchain/前缀参数重配仍然失败**（CONFIG 查找失败后缓存 NOTFOUND 不重找），必须删除整个 build 树重配——本次实证两轮；建议 AGENTS §4.1 环境口径记载时附注"首配失败后须删缓存重配" | **F-101** |

## 验收结论

DIAG-T02 送验对象 b5c9504e（wp09-t02）通过全部 11 项检查（4.1~4.11），无阻断级问题，建议级 2 条已转登 findings.json（F-100/F-101）。diagnostics 单元构建落位（INTERFACE→STATIC、两测试目标注册、红线三重防线、P-DIAG-8 桩处置）实现与契约及单元卡 v0.3 登记零偏差，双模式构建与测试全部由验收者亲手复现。verdict＝**pass**，移交所有者决策合入。
