# WP-13-T14 验收记录（attempt 1，2026-09-25）

| 字段 | 值 |
| --- | --- |
| 任务 | WP-13-T14 实现 Model Diff 差异增量表数据实体（IModelDiffService） |
| 送验对象（冻结 SHA） | `858629f9bad5d30a3ee077a0ca4a60fc4f4cea56`（验收全程按 SHA，不按分支名） |
| diff 范围 | `694449b8cb786e65bc7cfd199c3c93c07652ad66..858629f9bad5d30a3ee077a0ca4a60fc4f4cea56`（state.base..headSha） |
| verdict | **pass**（4.1～4.11 全部通过；无阻断级问题；建议级 1 条已转登 findings.json F-351） |
| 复现现场 | detached worktree `D:/10_Source_Repos/21_robot/_acc_t14_a` @ 858629f9；集成构建树 `D:/10_Source_Repos/21_robot/_acc_t14_build`；冒烟构建树 `D:/10_Source_Repos/21_robot/_acc_t14_smoke`（均临时，验收后删除） |
| 验收者环境说明 | 见 §3（含一节变异测试时间窗与冒烟树共用源树的过程事实及其处置——不影响送验对象缺陷判定） |

## 0. 分支漂移核查（ACC §3 送验对象冻结）

验收期间仅核查产物，未向 `wp13-t14` 分支推送任何提交（验收者无写任务分支动作）。送验对象即 `858629f9`（编排者下发的 40 位冻结 SHA，worktree A 检出核对一致：`HEAD is now at 858629f9 [WP-13-T14] 登记任务验证留痕…`）。

## 1. 逐项证据（ACC §4，4.1～4.11）

### 4.1 改动范围 —— PASS

`git diff --name-status 694449b8..858629f9` 共 28 个变更文件，逐个对照契约 allowedFiles：

| 组 | 文件 | allowedFiles 匹配 |
| --- | --- | --- |
| 产品代码 4 件 | `modeling/CMakeLists.txt`（M）、`modeling/include/sdurws/ird/modeling/ModelDiff.hpp`（A）、`modeling/src/ModelDiff.cpp`（A）、`modeling/test/ModelDiffTest.cpp`（A） | `RobWork/RobWorkStudio/src/rwslibs/industrialrobot/modeling/**` ✓ |
| 留痕 23 件 | `doc/industrial-robot-design/traceability/builds/wp13-t14/*`（23 个新文件：双模式构建日志、4 份 gtest XML、双 ird-test-report.json、ird-gates 四件、validate 双 log） | `traceability/**` ✓ |
| 文档 1 件 | `doc/industrial-robot-design/units/modeling.md`（M：卡头版本行 v0.15→v0.16＋§14.6 表尾追加 v0.16 行） | allowedFiles 显式列出 ✓ |

- forbiddenFiles（`REQUIREMENTS.md`、`ARCHITECTURE.md`）：diff 零触碰 ✓
- 框架源码（SA-02）：diff 内零框架文件；`patches/` 无新文件（无登记需求）✓

### 4.2 可复现构建 —— PASS（验收者从零亲手复现）

**集成模式**（全新配置树 `_acc_t14_build`，缓存确认 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`）：

- 配置：`cmake -S <worktreeA>/RobWork -B _acc_t14_build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=<repo>/vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`——带 toolchain/Qt 前缀为 F-007 已登记的全新树配置口径（AGENTS §4.1 字面命令在全新树会因 Boost/Qt 缺失失败，验收者首跑即命中，属在册环境事实非本任务缺陷）。
- worktree 需手工补 gitignore 辅助文件 `RobWorkStudio.ini.template.static`（`[Bb]in/` 忽略规则致 worktree 缺该 configure_file 输入；F-009 家族既有问题），从主检出复制，复现现场准备非产物修改。
- `cmake --build _acc_t14_build --config Release --target sdurws_ird_modeling sdurws_ird_modeling_test sdurws_ird_modeling_contract_test` → exit 0；日志 grep `error C|error LNK|fatal error` 零命中（日志中 7 处 "Error" 字样均为文件名 XMLErrorHandler.cpp/Errors.cpp/ErrorsTest.cpp）✓

**独立冒烟模式**（另配临时目录 `_acc_t14_smoke`，带 vcpkg toolchain＋Qt 前缀）：全量 `cmake --build` exit 0、`error C|error LNK|fatal error` 零命中 ✓

### 4.3 契约 verify 命令逐条 —— PASS（6/6 条独立执行；未执行项为零）

| # | 契约 verify 条目 | 验收者执行（口径：主树 build/ 字面路径→验收树 _acc_t14_build 等价执行） | 结果 |
| --- | --- | --- | --- |
| 1 | `cmake --build build --config Release --target sdurws_ird_modeling` | 实跑（三目标合一次调用，见 4.2） | exit 0 ✓ |
| 2 | `cmake --build build --config Release --target sdurws_ird_modeling_test` | 实跑（同上） | exit 0 ✓ |
| 3 | `ctest --test-dir build/RobWorkStudio/src/rwslibs/industrialrobot/modeling -C Release -L "^ird$"` | 实跑于 `_acc_t14_build` 同叶子路径 | **2/2 通过**（modeling_test＋contract_test；首跑 exit 0xc0000135＝STATUS_DLL_NOT_FOUND——全新树 exe 运行需 Qt/RobWork DLL 在 PATH，属验收环境事实；补 PATH 后 2/2，非产物缺陷）✓ |
| 4 | `cmake --build build --config Release --target ird_gates` | 实跑 | **exit 1＝56 处命中**，与 DTB §4.5 存量例外登记数（56 处）逐条同口径（编排者口径：存量命中既有状态，不判本任务新增；零新增证据见下节引擎直跑比对）✓（存量例外口径） |
| 5 | `validate-task.ps1 -TaskFile …/WP-13-T14.json` | 在 worktree A 实跑 | `validate-task: PASS (1 tasks)` ✓ |
| 6 | `validate-docs.ps1` | 在 worktree A 实跑 | `validate-docs: PASS (20 units, 12 trace entries, 189 task files)` ✓ |

### ird_gates 引擎直跑 base..head 归一化比对（ACC 4.4 附加证据＋编排者口径 2）—— 零新增

- head 侧：`cmake -DIRD_ROOT=<worktreeA>/…/industrialrobot -P cmake/ird_gates.cmake` → 119 次发射（exit 1 属存量既有状态）
- base 侧：临时 detached worktree @ `694449b8` 同脚本直跑 → 119 次发射；用毕即删（worktree remove）
- 归一化（以各自 IRD_ROOT 锚归一绝对路径）后逐行多重集比对：**unique-diff-lines=0**（119 vs 119 逐行完全一致）
- 两侧直方图一致：57 SUB / 41 R3 / 10 LIB / 5 T1 / 3 R4 / 1 R1 / 1 R5 / 1 T2——与送验附件 `ird-gates-comparison.txt` 登记直方图完全吻合
- 结论：**本任务零新增门禁命中**；集成树 ird_gates 目标 exit 1 的 56 处均为存量例外（DTB §4.5），非本任务新增

### 4.4 acceptance 条目逐条证据表 —— PASS（4/4 均有具名证据；未执行项为零）

| # | 契约 acceptance | 证据（验收者亲手复现） |
| --- | --- | --- |
| 1 | ModelDiff.hpp/.cpp 落位；§9.4.9 签名 diff(baseline, candidate)→ModelDiffReport（两 ModelingWorkingSet 输入）；三组分组＝结构/参数（DH、轴线、限位）/物性；条目含 ObjectId 定位 | ModelDiff.hpp:259-289（IModelDiffService 签名逐字＝units/modeling.md:1156 卡面签名——`diff(const ModelingWorkingSet&, const ModelingWorkingSet&) const` 直返报告无错误轨）；hpp:109-146（三值分组＋三态枚举）；hpp:176-203（ModelDiffEntry.objectId 定位锚注释）；UT `MdlModelDiff.PlacementEntityAndEmptyDiff_WP13T14_ACC1_ACC3`（经接口引用调用＋static_assert 基类关系）✓ |
| 2 | 差异输出 UT：增/删/改三态逐字段、权威字段变更与来源标记变更均入表；派生只读字段（DH 态 axis/origin、Explicit 态 dhDerived）不作为独立差异项（D-MDL-5）；同输入→同报告（NFR-COR-02 稳定排序——对象 id→字段序） | UT `PerFieldThreeStatesAndProvenance_WP13T14_ACC2`（九处差异计数＋内容双对账；仅来源变化条目 valueChanged=false/provenanceChanged=true 断言）；`DerivedReadOnlyFieldsNotReported_WP13T14_ACC2`（三分支：双侧 DH/双侧 Explicit/混合权威——恰 1 条 authority 条目、parameters 空）；`DeterminismAndStableFieldOrder_WP13T14_ACC2`（r1==r2＋j2Fields 四条序列机械钉住 axis→zeroOffset→bounds→workingRange＝§4.3-A 表行序）✓ |
| 3 | 不覆盖基线 UT：diff 纯函数、输入只读（post 不修改输入）；交换方向语义明确（报告方向字段）；空差集→空报告非 null | UT `PureFunctionInputsReadOnly_WP13T14_ACC3`（双证据：operator== deep 相等＋RobotDesignCodec canonical 字节 diff 前后逐位相等）；`DirectionMirrorOnSwap_WP13T14_ACC3`（方向字段互换＋Added↔Removed＋文本互换＋两面标记不变，forward 全条目镜像核对 checked≥3）；`PlacementEntityAndEmptyDiff`（三组 empty 断言＋报告方向字段对号）✓ |
| 4 | M-5 分工红线：不做呈现——接口无 UI 类型依赖、L2 零 Qt（ird_gates 复核）；AT-12 数据侧断言 | 三个新增文件 Qt 扫描（include.*Qt/Q_OBJECT/QString/QWidget）零命中；ird_gates 引擎直跑归一化零新增（R-3 门禁无 modeling 面新增）；UT `At12DataSidePerItemObservability_WP13T14_ACC4`（恰 3 处差异↔恰 3 条条目、定位四元组完整、报告纯值聚合 copy==report）✓ |

### 4.5 测试真实失败能力（变异测试，验收者真做）—— PASS

三处实质变异（改 worktree A 的 `ModelDiff.cpp`→增量重编 `sdurws_ird_modeling_test`→运行具名用例确认红→`git restore` 恢复；全程输出留存于验收者日志）：

| 变异 | 注入点（ModelDiff.cpp） | 期望变红的具名测试 | 实测 |
| --- | --- | --- | --- |
| A：D-MDL-5 豁免门失效 | diff 主编排 `authorityBothExplicit = true`（原为两侧 Explicit 合取） | `DerivedReadOnlyFieldsNotReported_WP13T14_ACC2` | **FAILED（1 test）** ✓ |
| B：稳定排序字段序键摘除 | `sortGroup` 谓词 `orders[a] != orders[b]` 段恒假化 | `DeterminismAndStableFieldOrder_WP13T14_ACC2` | **FAILED（1 test）**（j2Fields[3] 期望 "workingRange" 实得 "zeroOffset"——退化为主键 subjectPath 字典序）✓ |
| C：来源面判定恒假 | `compareSourced` 中 `f.provenanceChanged = false`（原为来源记录比较） | `PerFieldThreeStatesAndProvenance_WP13T14_ACC2` | **FAILED（1 test）** ✓ |

三次 `git restore` 后 `grep -c MUTATION` = 0（零残留）；恢复后重建并全量重跑 `sdurws_ird_modeling_test` → **223/223 全绿**。测试非恒真，4.5 与 4.11④ 联动通过。

### 4.6 架构红线（AGENTS §5 八条）—— PASS

1. **L2 零 Qt**：新增三文件 Qt 头扫描零命中（4.4 表#4）；ird_gates R-3 门禁 modeling 面零新增 ✓
2. **业务域互链禁止（R-1）**：新增 include 边仅 core 公共头（Identity.hpp/Provenance.hpp）＋本单元头（ModelDiff/Template/RobotDesign/Codec/Errors 等经 Template.hpp 传递）＋测试用 testkit AssertMacros（允许形态）；零 requirements/kinematics/trajectory/dynamics/selection/optimization 依赖 ✓
3. **跨单元私有头禁止（R-2）**：跨单元 include 仅 `include/sdurws/ird/core/` 公共头 ✓
4. **框架零源码修改（SA-02）**：diff 零框架文件、patches/ 无新文件 ✓
5. **不做名称前缀拼接/剥离（R-4）**：`"RobWork"` 字面拼接扫描零命中 ✓
6. **权威唯一（PA-1）**：diff 为 const 纯只读函数（两 const 引用入参、直返值），不触碰修订/写路径/名称解析语义 ✓
7. **不可变历史（PA-2）**：纯函数无历史接触面 ✓
8. **不继承 old/**：全新实现（ModelDiff.hpp/.cpp 均新增文件）✓

### 4.7 注释规范（AGENTS §2）—— PASS

- **公共头 ModelDiff.hpp（全查）**：文件头 @file/@brief＋设计依据（§9.4.9/§3.3/§3.4/§4.3/§4.8/D-MDL-5/MDL-08/AT-12/NFR-COR-02/ARC-04）＋背景说明＋比较面范围三项＋派生字段规则＋确定性＋线程安全＋错误语义，中文完整；每个枚举/结构体/类（ModelDiffGroup/ModelDiffChangeKind/ModelDiffEntry/ModelDiffReport/IModelDiffService/ModelDiffService）与全部公共函数（含 token 函数、operator==）中文 Doxygen；§2.5 高危信息齐备：单位（rad/m/kg/kg·m²/kg·m³，hpp:107/171、cpp:159/153/167/224 等）、坐标系（T_parent_joint/连杆系/质心基准，cpp:139-141/665-668）、所有权与线程约束（hpp:66-71 纯函数并发只读、hpp:221 纯值类型）、错误语义（hpp:67-71 无错误轨口径＋调用方数据破损归类）、确定性来源（hpp:60-64 classic locale＋17 位有效数字＋排序键四段）✓
- **实现文件 ModelDiff.cpp（抽查≥3 达标之主查件）**：文件头三段实现结构说明；每个函数（renderValue 重载族/compareSourced/emitSourced/DiffBuilder/sortGroup/diffRootFields/diffJoints/diffLinks/diffReferencesAndResources/diff）均有"为什么"级中文注释；关键算法（首现匹配、链序位、D-MDL-5 比较门、间接序 stable_sort）逐步讲解；魔法数字（FieldOrder 序数间隔、17 位有效数字）解释来源 ✓
- **测试文件 ModelDiffTest.cpp（抽查之次件）**：文件头自证基线声明（期望独立推导、固定规范文本 ObjectId）；fixture/辅助函数中文 Doxygen；用例注释逐断言块说明验证哪条 acceptance/需求；`IRD_TEST_INFO` 需求/AT 追溯字段 8/8 用例齐备 ✓
- 注：CppCheck 级繁体字/排版瑕疵未发现（对照 F-169 同型问题本任务零命中）

### 4.8 文档同步＋findings.json 相关性 —— PASS（含 1 条建议级转登）

- **单元卡同步**：units/modeling.md v0.16——卡头版本行与 §14.6 变更记录行同时落地（F-168 型"日期半边落地"问题零复发：卡头日期行 2026-09-25 与版本行一致）；§3.3 表 T14 行（`ModelDiff.hpp`｜`IModelDiffService`、差异增量表数据实体（MDL-08）｜T14）与 §9.4.9 行签名与实现逐字一致；§14.6 v0.16 九段登记（落位/分组映射/D-MDL-5 差异面/比较面范围/集合链序语义/确定性/测试/CMake/留痕）与实现逐项吻合
- **字段序一致性**：§4.3-A 表行序（localName→type→axis→origin→zeroOffset→bounds→workingRange→dhDerived）与实现 FieldOrder 常量（90→100→110→120→130→140→150→160）逐项一致；§4.3-B 行序（mass→centerOfMass→inertia→material→visual/collision）与 kLinkMass(190)…kLinkCollision(240) 一致；selfCollisionHints 不入表＝§4.3-B 行原文"不入根对象编码权威语义" ✓
- **未登记偏差**：零——分组映射扩大（origin/zeroOffset/workingRange 并入 Parameters 组）、比较面范围（部件对象内容不入表）均已按 DTB §5.4 在 §14.6 v0.16 ②④ 显式登记为语义决策 ✓
- **findings.json 未关闭条目相关性核对**（open 281 条全量过滤）：
  - **F-350 家族（F-300/F-304/F-310/F-314/F-338/F-344/F-345/F-350）与本任务直接相关且复发**：WP-13-T14 契约 verify 数组只列 `sdurws_ird_modeling` 与 `sdurws_ird_modeling_test` 两构建行，不含 `sdurws_ird_modeling_contract_test`——按清单字面执行到第 3 条 ctest（`-L "^ird$"` 含 contract_test）时该 exe 无人构建。验收者本次已在集成构建命令中直接带上该目标（4.3 表 #2/#3 执行未受阻），属建议级契约口径缺陷，**转登 F-351**（见 §2）。
  - F-292（测试目标→testkit 的 SUB 存量命中含 modeling_test/contract_test）与 F-302（pugixml LIB 存量命中）：与本任务门禁输出相关，均为在册存量例外（本次引擎直跑归一化比对零新增佐证），无消账动作需求，维持 open 属预期。
  - F-342/F-343/F-349（散文计数失准家族）：本任务**零复发**——实测 223/14/177/8（四份 XML 顶层 `tests="…" failures="0"`）、237/237（ird-test-report.json summary）、185/185（smoke-ird-test-report.json）与提交正文及 §14.6 v0.16 散文逐项吻合。
  - F-007/F-008/F-009（全新树配置参数/环境）：验收者实际命中 F-007（toolchain/Qt 前缀必需）与 F-009（worktree 缺 gitignore 的 ini 模板），均在册，非本任务新增。

### 4.9 commit 质量 —— PASS

`694449b8..858629f9` 两提交：

1. `f96393f9 [WP-13-T14] 实现 Model Diff 差异增量表数据实体（IModelDiffService）＋UT＋文档同步`——动词开头 ✓；正文四段齐备（What：hpp/cpp/test/CMake/单元卡逐件；Why：MDL-08/AT-12/NFR-COR-02/ARC-04＋核心语义决策；How：双模式构建/223+14/177+8/ctest/validate/ird_gates 逐项；影响面：仅新增与增列、零行为变化）；页脚 `Acc-Covered: 1,2,3,4` ✓
2. `858629f9 [WP-13-T14] 登记任务验证留痕（最终源码全新捕获，traceability/builds/wp13-t14/）`——动词开头 ✓；留痕件 23 件与正文声称一致（git ls-tree 实证 23 个新文件全部在库，F-166 型"幻影留痕"零命中）✓

验证段结论与验收者独立复现逐项吻合（构建零错误、223/223＋14/14、ctest 2/2、冒烟 177＋8、validate 双 PASS、ird_gates 零新增）——**无粉饰**。

### 4.10 留痕完整 —— PASS

- `traceability/builds/wp13-t14/` 23 件全部入库且计数与验收者实测一致：integration-modeling-test.xml（tests=223 failures=0）、integration-modeling-contract-test.xml（14/0）、smoke-modeling-test.xml（177/0）、smoke-modeling-contract-test.xml（8/0）、ird-test-report.json（summary total=237 passed=237 failed=0）、smoke-ird-test-report.json（185/185）、integration-build-modeling.log、smoke-build-modeling.log、validate-task.log（PASS）、validate-docs.log（PASS）、ird-gates 四件（比对结论与验收者直跑复现一致）✓
- gtest XML 原样留存（未手工转抄痕迹——XML 时间戳/时间为引擎产出）✓
- `phase-one-validation.log`：属 WP-03-T01 时期专用聚合口径（phase-one 任务集），不适用本任务；本任务留痕以 builds/wp13-t14/ 全集承载（与 T08～T13 先例同口径）；state.json 无 resultNote 字段——如实登记，不虚构"已更新" ✓

### 4.11 偷懒/缩水实现扫描 —— PASS

1. **标记扫描**：diff 范围内 `TODO|FIXME|stub|placeholder|未实现|not implemented` 新增行零命中（grep exit 1）✓
2. **空实现检测**：公共接口 `IModelDiffService::diff` 唯一实现 ModelDiffService::diff 具备完整主编排（方向字段＋比较门＋四段比较＋finalize 排序移交，ModelDiff.cpp:834-856）；token/比较/渲染函数全部实质实现，无"仅返回默认值"的公共函数 ✓
3. **逐条证据表**：acceptance 4 条全部有具名证据（见 4.4 表），零"已实现"声明依赖 ✓
4. **测试强度**：断言校验行为与边界（计数＋内容双对账、镜像全量核对、字节级只读证据、排序序列机械钉住）；三处变异（三态判定/稳定排序键/派生豁免条件——恰为编排者提示的三个篡改方向）全部真实变红（4.5），非恒真、非"只编译通过" ✓

## 2. 问题清单与转登

### 阻断级

无。

### 建议级（转登 findings.json）

- **F-351**：`tasks/foundation/WP-13-T14.json` verify 数组缺 `sdurws_ird_modeling_contract_test` 构建行（第 3 条 `ctest -L "^ird$"` 含该目标）——F-300/F-304/F-310/F-314/F-338/F-344/F-345/F-350 家族（契约 verify 缺目标构建行）在 T14 契约复发；本次验收已在构建命令中显式带上该目标故执行未受阻，属契约编译面口径缺口（建议级，非实现缺陷）。已按既有条目格式追加登记（编号顺延自 F-350）。

### 不构成问题但如实登记的过程事实（验收者环境说明，见 §3）

- 变异测试时间窗与冒烟树共用源码的污染事件及处置。

## 3. 验收者环境说明

1. **复现现场**：worktree A（detached @ 858629f9）＋全新集成树 `_acc_t14_build`＋全新冒烟树 `_acc_t14_smoke`；两树配置均带 vcpkg toolchain＋Qt6 前缀（F-007 在册口径）。
2. **gitignore 辅助文件**：worktree 缺 `RobWorkStudio.ini.template.static`（`[Bb]in/` 忽略规则），从主检出复制后 configure_file 通过（F-009 家族在册现象）。
3. **ctest DLL PATH**：全新集成树 ctest 首跑 exit 0xc0000135（Qt/RobWork 运行时 DLL 不在 PATH），补 PATH 后 2/2——环境事实非产物缺陷。
4. **ird_gates 口径**：集成树 ird_gates 目标 exit 1＝56 处存量命中（DTB §4.5 例外未清零，与编排者口径一致）；零新增以引擎直跑归一化比对独立证明（head 119＝base 119，逐行 diff 空）。
5. **变异测试时间窗事件（如实登记）**：4.5 变异测试与 4.2 冒烟全量构建共用 worktree A 源树；冒烟树在变异 B/C 窗口内编译了被注入的 ModelDiff.cpp，导致冒烟树 modeling_test 首跑出现 2 个失败（恰为变异 B/C 特征用例 PerFieldThreeStatesAndProvenance/DeterminismAndStableFieldOrder）。源码恢复（git status 干净＋MUTATION 零残留）后重建冒烟目标重跑 → **177/177＋8/8 全绿**，与送验留痕一致。该事件为验收者自身操作时序问题，**非送验对象缺陷**；教训：变异测试应与并行构建的源树隔离（后继验收建议对冒烟树使用独立 worktree 或在变异前完成冒烟构建）。
6. **代理/推送**：验收记录分支推送使用显式 refspec 并以 `git ls-remote` 核实（编排者口径 6）。

## 4. 裁决

- **verdict：pass**——4.1～4.11 全部通过，无阻断级问题；建议级 1 条（F-351）已转登，不阻塞合入。
- 送验对象：`858629f9bad5d30a3ee077a0ca4a60fc4f4cea56`（验收的是该 SHA，非分支引用）。
- 本验收者未修改任何实施产物、未向任务分支推送提交；验收记录与 findings.json F-351 登记落于 `acc/WP-13-T14/1` 分支（本 commit）。
