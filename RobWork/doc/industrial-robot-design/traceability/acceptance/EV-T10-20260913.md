# EV-T10 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | EV-T10 实现 evidence 评估器接口与注册表（③端口所有者） |
| 送验对象 | f67b597bbc675611dab5744463f86ba7af0752b5（40 位冻结 SHA，分支 wp05-t10） |
| diff 范围 | 11931ee7e3115a83d071b3c5612163925b2d50fe..f67b597bbc675611dab5744463f86ba7af0752b5（3 份提交：f4a7b11d 实现 / 6eae28a8 留痕 / f67b597b 文档同步） |
| 验收日期 | 2026-09-13 |
| verdict | **pass**（4.1～4.11 全部通过，无阻断级问题；建议级 1 条已转登 findings.json F-071） |
| 记录分支 | acc/EV-T10/1（本文件所在提交，推送 origin 后 commit SHA 见该分支尖端） |

## 独立性声明（ACC §2）

- 本验收为全新上下文会话，只消费任务契约（tasks/foundation/EV-T10.json）、送验产物（f67b597 冻结对象）与仓库文档；未读取实施者的任何过程叙述或辩护文本。
- 对抗式立场执行：每项检查均以"为不通过找证据"方式复现；构建与测试全部由验收者亲手重跑（不采信实施者运行结果）。
- 派发模式：PIPE §0.4 子代理派发，无需降级声明。
- 分支漂移检测：`git ls-remote origin refs/heads/wp05-t10` ＝ f67b597bbc675611dab5744463f86ba7af0752b5，与送验 SHA 一致，无漂移。

## 验收环境（ACC §3）

- 复现现场：`git worktree add --detach` 临时目录 A @ f67b597b（detached @ SHA，非分支名）。
- 集成模式：worktree A 内全新树重新配置 `cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON`，并按 F-062 教训补齐环境前置（-DCMAKE_TOOLCHAIN_FILE=vcpkg 绝对路径、-DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64、从主树复制两件 git 未跟踪的 RobWorkStudio.ini 模板）；`grep RWS_BUILD_INDUSTRIALROBOT build/CMakeCache.txt` ＝ `:BOOL=ON` 确认后构建。
- 冒烟模式：另配独立临时目录（vcpkg toolchain 绝对路径，AGENTS §4.1 口径）。
- 门禁类检查（ird_gates）：按编排口径回主仓库只读运行（worktree 内缺 vcpkg 会失败——F-009 同族）；主仓工作树恰检出 wp05-t10（＝送验 SHA），源码树与送验对象逐字节同源。

## 逐项证据（ACC §4，4.1～4.11）

### 4.1 改动范围 — PASS

- base..head 变更文件共 37 个（`git diff --name-only 11931ee7..f67b597b`），逐一对照契约 allowedFiles：
  - `RobWork/RobWorkStudio/src/rwslibs/industrialrobot/evidence/**`：8 个（CMakeLists.txt、Errors.hpp/.cpp、Evaluator.hpp/.cpp、README.md、ErrorsTest.cpp、EvaluatorTest.cpp）——allowedFiles[0] 内；
  - `RobWork/doc/industrial-robot-design/traceability/builds/wp05-t10/**`：28 份留痕——allowedFiles[3]（traceability/**）内；
  - `RobWork/doc/industrial-robot-design/units/evidence.md`：1 个——allowedFiles[2] 内。
- forbiddenFiles（REQUIREMENTS.md、ARCHITECTURE.md）零触碰（diff 清单不含）。
- SA-02：diff 无 RobWork/RWS 框架源码文件、patches/ 无新文件（零框架改动）。

### 4.2 可复现构建 — PASS（验收者全新树从零复现）

- 集成模式（worktree A 全新树，缓存确认 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`）：`cmake --build build --config Release --target sdurws_ird_evidence_test sdurws_ird_evidence_contract_test -j` → exit 0；`error C*/LNK/fatal` 计数＝0；evidence 面（Evaluator.cpp/Errors.cpp/两 test 目标）零告警；告警仅两类且均与本任务无涉：框架既有（Quaternion.h/LinearAlgebra.cpp/Units.cpp 等，F-008 同域存量）＋core 单元既有 Units.cpp 14 处 C4099（见问题清单 G-1/F-071——EV-T10 未触碰 core）。配置日志确认 CMake Error＝0。
- 冒烟模式（全新独立树全量构建）：exit 0；error 计数＝0；告警仅 MSB8029×15（验收临时目录环境告警，与产物无涉）。
- 两条结论均从零复现（全新树全量编译链路），强于送验留痕的增量树口径。

### 4.3 契约 verify 命令 — PASS

- 契约 verify＝`cmake --build build --config Release --target sdurws_ird_evidence_test`，逐字执行 exit 0（集成树增量复核，目标产物重新产出）。

### 4.4 acceptance 条目逐项核对 — PASS（3/3 均有具名证据）

1. **EV-REG-1/2 注册边界/并发/manifest 稳定用例通过**：EvidenceRegistrationBoundary 组 5 例＋EvidenceRegistrationManifest 组 3 例（`EvaluatorTest.cpp` L331～L695，TEST 名单经 grep 清点）——验收者亲手两模式运行 191/191 通过（集成 `[PASSED] 191 tests`、冒烟同）；并发用例为 8 线程×200 迭代真实压力（create 增量计数断言 1600 非空转）。
2. **不建调度器（D-12：与 execution 分界）**：`EvidenceRegistryNoScheduler.NoSchedulerFacilitiesInEvaluatorSources`（L1043）对 Evaluator.hpp/.cpp 源码扫描调度设施词面（Scheduler/TaskQueue/RunRegistry/std::queue/priority_queue）＋执行期能力字段词面（supportsPause/checkpointGranularity/forceTerminateCost——P-EX-7 双保险）零命中；文件头与契约头登记 D-12 边界（IEvaluationContext 仅接口、evidence 零实现）。
3. **descriptor 依赖/能力声明（§9.2）与 EvidenceProfileRegistry（§9.5）行为用例通过**：`DescriptorHasExactlyDesignFields_P_EX_7`（L904，结构化绑定钉死恰七字段＋成员类型 static_assert——字段面漂移即编译失败）＋`DeclarationClosureRejectionAndFactKeys_EV_REG_1`（L483，DeclarationInvalid 码面＋快照事实键∪半边）＋EvidenceProfileRegistryBehavior 组 4 例（L745～L894：身份覆盖计算/重复拒绝与多版本共存/语法拒绝逐 itemId/条件闭包三半场）。
- 附：ird_gates 非契约 acceptance 条目但属 DoD 自查——实际执行（主仓只读，exit 1），3 处命中（IRD-GATE-SUB runtime_test→testkit＝F-051；IRD-GATE-SUB testdata_lint→testkit＝F-019；IRD-GATE-T1 testdata_lint 链接 testkit＝F-019）与 wp05-t09 基线日志命中区块逐项一致（`git show 11931ee7:.../wp05-t09/ird-gates.log` 比对），零新增。ird_gates 结果与送验 ird-test-report.json irdGates 字段记载一致（且该字段已按 findings.json 权威编号逐条标注——F-060 处置建议落地）。

### 4.5 测试真实失败能力（验证验证者）— PASS（双探针）

- 探针 1：`src/Evaluator.cpp` registerEvaluator 重复键检查注入短路 `if (false && existing != m_evaluators.end())` → 重编译 exit 0 → `--gtest_filter=EvidenceRegistrationBoundary.DuplicateKeyRegistrationRejected_EV_REG_1` **exit 1 变红**（"同键重复注册必须被拒绝"断言检出短路）→ 逐字恢复＋touch 强制重编译（F-068 mtime 陷阱规避）→ 复绿 exit 0；`git diff` 零差异、PROBE 标记 grep 零残留。
- 探针 2：同文件 registerProfile 的 ProfileDuplicate 检查注入 `&& false &&` → `--gtest_filter=EvidenceProfileRegistryBehavior.DuplicateRejectedAndVersionsCoexist` **exit 1 变红** → 恢复＋touch → 复绿；git status 零差异（porcelain 空输出）、PROBE 零残留、全量重跑 191/191。
- 两探针分别钉住 acceptance 1（EV-REG-1 注册边界）与 acceptance 3（§9.5 重复拒绝）两条防线的真实失败能力。

### 4.6 架构红线（AGENTS §5 八条）— PASS

1. L2 零 Qt：Evaluator.hpp/.cpp、EvaluatorTest.cpp include 面无任何 Qt 头（grep 核验）；evidence CMakeLists 零 Qt 链接且配置期守卫断言在库。
2. R-1 业务域互链：`target_link_libraries(sdurws_ird_evidence PUBLIC sdurws_ird_core)` 唯一单元边（CMake 配置期红线守卫断言保持）——注册制协作经 IEvaluatorFactory 接口，无业务域链接。
3. R-2 跨单元私有头：仅 include `sdurws/ird/core/*` 与同单元 `sdurws/ird/evidence/*` 公共头路径。
4. SA-02 零框架修改：diff 面无框架文件、patches/ 无新登记（4.1 已核）。
5. R-4 名称前缀：ird_gates 实测 R-4 零命中（3 处命中均为 SUB/T1 既有）。
6. PA-1 权威唯一：诊断码值权威归 diagnostics StableCodeRegistry（Errors.hpp/registryCode 注释显式声明建议码面——PA-1 不自注册）；未越权实现 project/runtime 语义。
7. PA-2 不可变历史：不适用（本任务无修订写路径；错误码枚举只表尾追加并以 ErrorsTest 序号断言钉死——同纪律）。
8. old/ 历史实现：无恢复/继承痕迹（实现按 units/evidence.md §9 从零编写）。

### 4.7 注释规范（AGENTS §2）— PASS

- 公共头全查：`Evaluator.hpp`（804 行）文件头含设计依据（§9/§3.1/§10.2、需求 ID、契约 acceptance 映射）、背景说明（③端口定位）、D-12 调度分离边界、P-EX-7 处置登记、线程模型（§9.4 shared_mutex 三段纪律）、确定性（NFR-COR-02）；逐类逐函数 Doxygen 中文注释齐备（ThreadSafety 词表语义/EvaluationKey D-5 收敛理由/descriptor 七字段逐个含义与校验出处/两注册表行为冻结逐行/检查序固定说明）。
- 实现文件抽查（≥3 达标）：`Evaluator.cpp`（文件头实现口径 R-1～R-4＋manifest 字节布局逐段讲解＋每语义段"第几步/业务含义"注释——注册边界先于内容校验的为什么、锁序单向无死锁面、锁外工厂调用的理由）、`Evaluator.cpp` 错误轨（fail-fast/查询非抛的分轨语义）、`Errors.hpp/.cpp` diff（表尾追加纪律、建议码补登出处、F-056 登记链）、`EvaluatorTest.cpp`（文件头 acceptance↔用例映射＋替身边界声明 EV-REG-3 前置＋逐用例 §11 行对照）、`evidence/CMakeLists.txt`（EV-T10 交付面注释块）。
- §2.5 高危信息表：物理单位（progress percent 0～100 无量纲、contractVersion 无量纲、AttemptId u64——均有显式注释）、坐标系/角度制式（本任务无位姿/角度量——不适用面无违例）、所有权（factory unique_ptr 独占/实例归调用方/find 返回指针不接管——均注明）、线程约束（注册期单线程约定/运行期只读安全/mutable 锁——§2.5 表逐项落位）、错误语义（调用方契约违约 fail-fast vs 环境面查询轨 nullptr——注释区分）、确定性来源（检查序固定/字典序排序/大端编码/SHA-256 经 core::ContentDigester 唯一——NFR-COR-02 注释落位）。

### 4.8 文档同步＋findings 消账核对 — PASS

- 实现与任务卡零偏差：契约 acceptance 三条与交付物（Evaluator.hpp/.cpp、EvaluatorTest 18 用例、CMake 注册、README 落位段）一一对应；单元卡 §12 EV-T10 行（`units/evidence.md` L969）交付文件/验证方式/完成条件与实际一致。实现细节偏差（EvaluationKey D-5 收敛为 std::string 别名、create 未知键 nullptr 查询轨 R-1、检查序 R-2、profile 声明面复用 R-3、Profile 注册检查序 R-4）已按 DTB §5.4 在 units/evidence.md §15.4 v1.1 行逐条登记（+卡头版本行同步推进——F-057 教训未复发）。错误码 10→13 的表尾追加已在 ErrorsTest.cpp 序号断言（ProfileInvalid=12）与单元卡 v1.1 ④ 双面登记。
- 治理校验：validate-task PASS（1 tasks，worktree A 实测）、validate-docs PASS（20 units/12 trace/150 task files）、phase-one-validation.log L24 EV-T10 行在案。
- **findings.json 未关闭条目相关性核对（56 条 open，逐条结论）**：
  - **本任务基线相关（保持 open，不随本任务消账——修复面不在 allowedFiles）**：F-019（testdata_lint 2 处门禁命中——本任务实测命中之一部分，零新增）、F-051（runtime_test→testkit 门禁命中——同上）、F-008（框架全新树构建缺陷——验收复现按判例以任务目标面构建规避）、F-004/F-007/F-009/F-010/F-062（构建/环境/留痕流程面——与本任务产物无涉；F-010 之 .gitignore 吞 *.log 规则在本任务以 `git add -f` 规避〔CORE-T02 先例延续，28 份 .log 留痕确认入库〕）、F-065（留痕日志中文双重编码——本任务 ird-gates.log 同症，ird-test-report 已自我声明并以结构化字段规避）。
  - **本任务合规印证（不消账，佐证家族纪律已执行）**：F-012 家族（标题动词开头——本任务三份提交均 [WP-05-T10] 前缀＋动词开头，零复现）；F-028/F-035（门禁"未建成"过时措辞——本任务实际执行门禁并登记真实结论）；F-036/F-038/F-054/F-055（构建树性质/配置标注——ird-test-report 明确区分"集成＝增量树/冒烟＝全新树、均 Release"）；F-059/F-069（同家族）；F-060（门禁命中权威归属标注——ird-test-report irdGates 字段按 F-051×1＋F-019×2 逐条标注，处置建议已落地）；F-068（探针 mtime 陷阱——实施者留痕 probe-residue-check.txt 与验收者双探针均执行 touch＋零差异＋零残留三重核验）。
  - **本任务消账/推进的条目**：F-056 resolution ③（"闭包拒绝码是否独立成码由 EV-T10 EvaluatorRegistry 消费时复核"）——本任务已复核并兑现：依赖声明闭包问题独立走 DeclarationInvalid（EV-T02 预登记契约），其余注册期问题走表尾新增 EvaluatorDescriptorInvalid，码面分工在 Evaluator.hpp requireValidEvaluatorRegistration 注释与单元卡 v1.1 ② 登记；同时 F-056 家族再扩 3 值（EVI-EVALUATOR-DESCRIPTOR-INVALID/EVI-PROFILE-DUPLICATE/EVI-PROFILE-INVALID——P-PR-6 同模式补登建议，Errors.hpp 注释显式登记），仍归 diagnostics 所有者注册时同批裁决（F-056/F-066 保持 open，范围扩至含本 3 值）。
  - **不相关（runtime/testkit/policy/io/execution/project/治理脚本辖域）**：F-005/F-006/F-013/F-015/F-016/F-017/F-018/F-020/F-021/F-022/F-023/F-024/F-025/F-026/F-027/F-029/F-030/F-031/F-032/F-033/F-034/F-037/F-038/F-039/F-040/F-041/F-042/F-043/F-044/F-045/F-046/F-047/F-048/F-049/F-050/F-052/F-053/F-057/F-058/F-059/F-061/F-063/F-064/F-067/F-070——逐条过筛：本任务 diff 未触碰各自辖域文件（F-058 之 evidence.md"AI 执行就绪补充"节损毁：本任务对 evidence.md 为纯增量修订〔卡头＋§15.4 追加〕，未触及该节，保持 open 归 DOC 治理批次）。

### 4.9 commit 质量 — PASS

- 三份送验提交标题均 `[WP-05-T10]` 前缀＋动词开头（"实现…"/"登记…"/"同步…"），无 F-012 家族/F-069 形态偏差。
- 正文四段（What/Why/验证/影响面）齐备（`git log --format=%B` 逐份核对）。
- 验证结论与留痕一致性：集成 191/191＋5/5、冒烟 534/534（core 61＋evidence 191＋contract 5＋policy 4＋runtime 176＋testkit 97——分项加和核对一致）、ird_gates 3 处既有命中零新增、探针自证——均与验收者亲手复现及 gtest XML（tests="191" failures="0" 两模式；contract tests="5"）一致；构建树性质与配置显式标注（增量树/全新树、Release）。无粉饰；未执行项为零（ird-test-report notExecuted 空表）。

### 4.10 留痕完整 — PASS

- gtest XML 6 份（evidence/contract × smoke/integration）＋ird-test-report.json＋20 份构建/门禁/校验/探针日志全部入库（`git diff --name-only` 确认；.gitignore 全局 *.log 规则下以 `git add -f` 强制加入——F-010 规避先例延续，提交正文已声明）。
- ird-test-report.json 为 ird-test-report/1 格式：18 新增用例逐条 trace（acceptance/EV-REG 追溯）、notExecuted 空表、构建树性质与配置显式区分、门禁命中权威归属标注。
- phase-one-validation.log 含 EV-T10.json validate-task PASS 行（L24）。

### 4.11 偷懒/缩水实现扫描 — PASS

- ①TODO|FIXME|stub|placeholder|未实现|not implemented 扫描：evidence/ 目录零命中（grep exit 1；探针临时标记仅存在于验收期、恢复后零残留）。
- ②空实现检测：Evaluator.hpp 公共接口均有对应实现（Evaluator.cpp 498 行）或为契约纯虚接口（IEvaluationContext evidence 零实现＝D-12 设计语义并有注释与测试钉住，非缩水）；Errors.cpp 新增 3 码 token/registryCode 全表覆盖。
- ③acceptance 逐条证据表：ird-test-report.json testSuites 6 组 18 用例逐条具名 trace＋验收者 4.4 表独立复核——无"仅声明已实现"条目。
- ④测试强度：断言校验行为与边界（错误码精确断言/what() 前缀与 detail 字段断言/不覆盖语义断言/指针稳定性断言/并发计数断言/结构化绑定编译期钉死/源码扫描词面断言），非恒真；与 4.5 双探针联动证明会红。

## 问题清单

### 阻断级

无。

### 建议级（已转登 findings.json）

- **G-1（→F-071）**：`industrialrobot/core/src/Units.cpp` 存在 14 处 MSVC C4099 告警（`Quantity<QuantityKind::…>` 模板类型"以前使用 struct 现在使用 class"——struct/class 声明不一致）。验收者全新树从零构建时暴露；主仓增量树该文件无重编译活动而掩盖（F-036/F-052 同族"增量树掩盖"效应，方向为既有告警非错误）。**非本任务引入**（EV-T10 diff 未触碰 core 单元；送验留痕增量树日志零告警的表述在其口径下真实），不阻塞本验收。处置建议：core 实现所有者随下次触及 Units.cpp/Units.hpp 的任务统一 struct/class 声明消除告警（全新树/CI 全量构建的告警清洁面前置）。

## 裁决

- **verdict：pass**——4.1～4.11 全部通过，无阻断级问题；建议级 1 条（G-1→F-071）已按 ACC §5 转登 traceability/findings.json，不阻塞合入。
- 本验收不执行合入：合入决策归所有者（ACC §6）。
