# WP-14-T04 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | WP-14-T04（实现 CSV/JSON 需求导入与副本导出——字段字典/单位预览/逐行错误） |
| 送验对象（冻结） | `e63fba9a5e075370a7f03101d70dfd73e958d0e5`（origin/wp14-t04 尖端，ls-remote 核实一致——无分支漂移） |
| diff 范围 | `7ac106cd69a71e7b08d3d4ef92c2e885bdd41166..e63fba9a5e075370a7f03101d70dfd73e958d0e5`（base..head，共 2 提交：110d8457 实现＋e63fba9a 留痕） |
| 契约 | `RobWork/doc/industrial-robot-design/tasks/foundation/WP-14-T04.json` |
| 验收日期 | 2026-09-26 |
| 验收环境 | 独立会话（全新上下文，只消费产物与文档，未消费实施叙述；ACC §2 四条独立性要求逐条自查通过）；全部构建/测试/门禁/变异在临时 worktree 与独立构建树中由验收者亲手执行 |
| **verdict** | **pass（无阻断级问题；建议级 2 条，已转登 traceability/findings.json F-366~F-367）** |

## 0. 验收者环境与复现现场说明

- 复现现场：`git worktree add --detach D:/acc_tmp/acc-wp14-t04-a e63fba9a5…`（detached @ SHA，非分支名）；base 侧临时 worktree `D:/acc_tmp/acc-wp14-t04-base` @ `7ac106cd…`（门禁比对用）。
- worktree 缺 gitignored 辅助件（F-200/F-007 家族在案现象，本实录再现）：`RobWork/RobWorkStudio/bin/RobWorkStudio.ini.template.static` 与 `RobWorkStudio.ini.shared.in` 不随 git 检出（`.gitignore:79 [Bb]in/`）——验收者从主检出复制后 configure 通过。属环境前置，非送验对象缺陷。
- 集成/冒烟两模式全新配置参数（F-007 家族在案口径）：`-DCMAKE_TOOLCHAIN_FILE=<主仓库>/vcpkg/scripts/buildsystems/vcpkg.cmake`＋`-DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`（与主构建树缓存一致；worktree 内无 vcpkg 目录——gitignored）。
- 构建前 tasklist 核查：无 RobWorkStudio.exe 残留进程锁 DLL（编排提示第 1 条——本次未发生，无需 taskkill）。
- 验收记录按 ACC §3 落于 worktree B 分支 `acc/WP-14-T04/1`（基于 origin/redesign-main @ `7ac106cd…`），commit 后以显式 refspec 推送。

## 1. 逐项证据（ACC §4.1~4.11）

### 4.1 改动范围 —— 通过

- `git diff --name-status 7ac106cd..e63fba9a` 共 26 文件，逐一套 allowedFiles 模式比对：
  - 7 × `RobWork/RobWorkStudio/src/rwslibs/industrialrobot/requirements/**`（CMakeLists 修改＋Import.hpp/.cpp 新增＋DiagCodes.hpp/.cpp 修改＋DiagCodesTest.cpp 修改＋ImportTest.cpp 新增）
  - 18 × `RobWork/doc/industrial-robot-design/traceability/builds/wp14-t04/**`（留痕证据）
  - 1 × `RobWork/doc/industrial-robot-design/units/requirements.md`（§12 io 交接行＋§14.6 v0.4 登记）
- **越界文件＝0**；forbiddenFiles（REQUIREMENTS.md／ARCHITECTURE.md）零触碰。
- SA-02：diff 无 `patches/` 文件（`git diff --name-only -- …/patches` 输出空）、无框架源码改动。

### 4.2 可复现构建 —— 通过（双模式，验收者亲手重配重跑）

- 集成模式（全新构建树 `D:/acc_tmp/acc-wp14-t04-bld`，源＝worktreeA）：
  - `cmake -S <A>/RobWork -B <bld> -G "Visual Studio 17 2022" -A x64 -DCMAKE_TOOLCHAIN_FILE=…vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64 -DRWS_BUILD_INDUSTRIALROBOT=ON` → **Configuring done (34.4s) / Generating done**；`grep RWS_BUILD_INDUSTRIALROBOT CMakeCache.txt` → `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON` 确认。
  - `cmake --build <bld> --config Release --target sdurws_ird_requirements sdurws_ird_requirements_test sdurws_ird_requirements_contract_test` → **exit 0**，`error C/LNK/fatal` 计数 **0**；两测试 exe 与 STATIC 库齐备。
- 独立冒烟模式（全新构建树 `D:/acc_tmp/acc-wp14-t04-smoke`，源＝worktreeA 的 industrialrobot 子目录，带 vcpkg toolchain＋Qt prefix，Release）：
  - configure → exit 0（日志第 9 行 `industrialrobot: standalone smoke mode` 横幅确认独立模式）；全量 `cmake --build . --config Release` → **exit 0，error 计数 0**；`requirements/Release/` 下 lib＋两测试 exe 齐备。
  - 冒烟两测试直跑：**75/75 与 5/5 全绿**。

### 4.3 契约 verify 命令 —— 通过（3/3 亲手执行）

| # | 命令（按本验收构建树等价换算） | 结果 |
| --- | --- | --- |
| 1 | `cmake --build build --config Release --target sdurws_ird_requirements` | **exit 0**（零错误；三目标同批构建后再单跑本行字面形态复核实证——增量无操作即成功） |
| 2 | `ctest --test-dir <bld>/RobWorkStudio/src/rwslibs/industrialrobot/requirements -C Release -L "^ird$"` | **2/2 passed**（`100% tests passed, 0 tests failed out of 2`，label `ird`） |
| 3 | `pwsh -File RobWork/scripts/industrialrobot/validate-docs.ps1`（worktreeA 内执行） | **PASS（20 units, 12 trace entries, 189 task files）** |

- verify 数组缺两测试目标的显式构建行（仅库名＋ctest＋validate-docs——F-358 家族在案现象的 T04 新例）：验收者侧在全新构建树补建 `sdurws_ird_requirements_test`／`…_contract_test` 两目标并直跑 **75/75＋5/5 全绿**（集成与冒烟两模式各跑一轮）——属验收者侧正常动作，按建议级转登 **F-367**。

### 4.4 acceptance 条目逐项证据 —— 通过（7/7）

| # | acceptance 条目 | 证据（验收者实测；具名测试均在 75 例全绿集内） |
| --- | --- | --- |
| 1 | V-08 CSV 行级部分成功（AT-02） | `CsvPartialSuccess_CorrectRowsRetained_RowErrorsLocated_V08_AT02`（4 行含 2 错行→`status=Partial`＋`entries.size()==2`＝正确行数；两条行级错误均 `code==REQ-IMPORT-ROW-ERROR`＋context 三要素 `row=2; column=level; raw=Info`／`row=3; column=x; raw=abc,def`＋cause/action 非空）；`CsvDuplicateIdAndName_ReqImportDuplicateId_V08`（id/name 重复各一条 DUPLICATE-ID，携 `first-row=1`，重复行丢弃首行保留）；`CsvMissingRequiredColumn_StructuralRejection_V08`（缺 x/y/z→`Rejected`＋entries 恒空＋UNIT-ILLEGAL"必备列缺失"结构级诊断——该文件不可导入）；`CsvMissingOptionalColumn_DefaultValueAndReport_V08`（defaultedFields 报告＋字典缺省值黄金断言：Must/true/Generic/1e-3 m/π/180 rad）；`CsvUnmappedColumn_ReportedAsIgnored_V08`（多余列入 ignoredColumns——不静默丢弃） |
| 2 | 字段字典冻结表＋自动识别＋手动改映射（卡 §7.3） | `FieldDictionary_FrozenTableExposed_Acc2`（20 字段 canonical 名逐字对卡面原文序、必备集恰 {id,name,x,y,z}、种类 Length/Angle/Number/Text 逐列断言——WP-14-T01 冻结表零改动承接）；`AutoDetect_AliasesAndManualOverride_Acc2`（中文"名称/坐标X/位置容差/备注"与大写别名识别、词表外与空白污染不猜测、表级自动识别、手动 assign 覆盖、同字段双列首列胜出＋后列入未识别清单）；`fieldDictionary()` 经接口暴露（Import.hpp:497）供向导单位预览 |
| 3 | V-09 单位预览与 SI 归一（NFR-COR-03） | `UnitPreview_MmDegConversionCorrect_V09`（mm/deg 声明列预览换算正确、预览值＝落库值同一入口）；`CsvUnitNormalization_SiValuesAsserted_V09_NFR_COR03`（mm/deg 列落库 SI 值断言——**变异 M2 真变红证明断言绑定真实换算**）；`CsvUnitDeclarationIllegal_ReqImportUnitIllegal_V09`（非法声明→REQ-IMPORT-UNIT-ILLEGAL 逐列诊断）；`CsvFrameDangling_WarningAndRetained_V09`（裸 ObjectId→REQ-IMPORT-FRAME-UNKNOWN 警告＋条目按 ModelFrame 保留待解析）；换算经 `core::tryConvert` 唯一入口（Import.cpp:104） |
| 4 | V-10 JSON roundtrip 副本语义（AT-24） | `JsonRoundtrip_ExportCopy_FieldByFieldEqual_V10_AT24`（导入→exportCopy→重导入，条目值语义全等 `EXPECT_EQ(lhs,rhs)`——溯源按设计各指其源归零后比对）；`ExportCopy_AtomicFailure_OldFileIntact_ProjectUntouched_V10`（注入 prepare 失败 fake→ok=false＋旧文件**字节级**完好＋工作集值快照相等——零修订/项目不变）；`ExportCopy_OverwriteAtomic_DraftMarker_V10`（draft:true 标记入文档＋旧内容被顶替＋重导入 sourceMarkedDraft 透传）；JSON 通道经 io profile 注册制（`ird-requirements-import/1`，版本判定先于结构校验）＋IAtomicFileWriter OverwriteAtomic |
| 5 | CSV 按数据解析不执行公式（DTB 约束列原文） | `CsvFormulaCellsAsData_NoExecution_Req05_NFR_SEC03`（`=cmd|' /C calc'!A0` DDE 串与 `=1+1&REPT(...)` 文本列字面保留非求值；数值列 `=1+1`→行级错误原文回显 `raw==1+1`——任何路径不执行） |
| 6 | 导入仅产草稿；纯函数确定性；失败无半成品 | `MapCsv_Deterministic_SameInputSameOutput_Acc6`＋`MapJson_Deterministic_SameInputSameOutput_Acc6`（两次调用 outcomesEquivalent＋diags 相等）；`ImportRejected_NoDraftNoRevisionNoHalfProduct_Acc6_V20`（Rejected→entries 恒空、无行级半成品；签名面无文件/项目句柄——结构上不可能产生落盘/修订）；`PreconditionViolations_FailFast`（无表头/映射越界/rows-dataRows 失配/空字节/空路径→`std::invalid_argument`——调用方错误轨）；`CsvSourceDigest_ContentAddressed_Acc4_I_REQ8`（同内容同摘要、内容变摘要变、摘要＝RawTable 规范投影 SHA-256 自算对表——I-REQ-8 溯源口径） |
| 7 | 双模式构建＋ctest＋留痕＋ird_gates | 本记录 4.2/4.3/4.10 与门禁段——验收者独立复现：集成 0 错误、冒烟 0 错误、ctest 2/2、gtest 75/75＋5/5（双模式）、门禁 127=127 零新增 |

**P-REQ-8 等价承载依据核查**：卡面 §9.5 `io::ExportTarget` 在 io 磁盘基线无该类型（io.md 仅 §4 表 P-7 路径角色概念与 IoFwd `PathRole` 枚举值，非公共承载类型）——实现以 requirements 侧 `ExportTarget{filePath,draft}` 等价承载，写出全经 io（CSV＝`io::makeCsvWriter`＋文件目标内部"暂存＋原子替换"，Csv.hpp:366~401/575~577 实文核对；JSON＝`canonicalizeJson`＋`IAtomicFileWriter` OverwriteAtomic）。io.md §9.0（行 1000）"签名均为实现建议……实现期允许等价调整，语义不变"为依据原文；偏差已按 DTB §5.4 登记于 units/requirements.md §14.6 v0.4（本 diff 实测在案）。**语义零收窄**："失败时旧文件完好"由 prepare/commit 失败分支＋abort 清理承载并有测试钉住。

**诊断码 1→5 随附同步合法性核查**：§9.6 表 T04 行四码（ROW-ERROR/DUPLICATE-ID/UNIT-ILLEGAL/FRAME-UNKNOWN）消费者＝Import.cpp 产码面（本任务落位）——"不预建无消费者条目"纪律满足；DiagCodes.hpp 工厂清单表尾追加（T02 既有 1 码不动）＋DiagCodesTest `kStagedSection96Codes` 封闭断言 1→5 随附同步（规模恰等＋逐码存在性＋码值同串断言）——T03 期 ErrorsTest 8→9 先例同源，合法登记。

### 4.5 测试真实失败能力 —— 通过（3 处实质变异，全部真变红；变异后已恢复）

在 worktreeA 源树上变异 Import.cpp，冒烟构建树增量重编后跑具名用例（时序口径：全部构建/全量测试完成后才做变异；每次变异→构建→跑→**git restore 还原**；三处变异全部还原后重编全量 75/75＋5/5 复绿——`git diff` 计 0 行确认源树还原）：

| # | 变异点（Import.cpp） | 实质语义 | 结果（实测输出） |
| --- | --- | --- | --- |
| M1 | `collectEntries` 去重查找键追加 `+"\x01MUT"`（查找永不命中——去重判定失效） | CSV 去重判定 | `ReqImport.CsvDuplicateIdAndName_ReqImportDuplicateId_V08` **FAILED**（构建 exit 0、`1 test…1 FAILED`） |
| M2 | `tryConvertSi` 返回 `value` 直通（跳过 `core::tryConvert`——SI 归一系数被篡改） | 单位归一 | `UnitPreview_MmDegConversionCorrect_V09`＋`CsvUnitNormalization_SiValuesAsserted_V09_NFR_COR03` **2 FAILED** |
| M3 | `exportCopy` JSON 草稿标记条件 `if (target.draft)`→`if (!target.draft)`（标记时序/条件翻转） | 原子写出路径的 draft 标记语义 | `ExportCopy_OverwriteAtomic_DraftMarker_V10` **FAILED**（同组 3 测试中恰 1 红——失败定位精确） |

### 4.6 架构红线 —— 通过（AGENTS §5 八条逐条）

1. **L2 零 Qt**：`Import.hpp/Import.cpp/DiagCodes.*` 及本 diff 全部产品文件 Qt 扫描零命中；冒烟 configure 日志第 21 行确认 ui 插件不注册。
2. **业务域互链禁止（R-1）**：requirements 产品面 include 仅 core/io/diagnostics（§3.2 登记边）＋本单元公共头；`grep modeling|kinematics|trajectory|dynamics|selection|optimization` 零命中。
3. **跨单元私有头禁止（R-2）**：全部跨单元 include 均为 `include/sdurws/ird/<unit>/` 公共头（DiagData/Digest/Provenance/Units/AtomicFile/Csv/Json/diagnostics 两头）。
4. **框架零源码修改（SA-02）**：4.1 已证。
5. **名称前缀拼接/剥离禁止（R-4）**：扫描零命中；名称解析未在本单元实现（导入侧仅引用**文本语法**→引用种类的结构化承载，目标存在性显式留待 T05 就绪层/运行时解析——Import.cpp:428~434 注释与代码一致，权威归属未越界）。
6. **权威唯一（PA-1）**：导入只产内存草稿条目（mapCsv/mapJson 纯函数、无 DraftService/命令端口触达——签名面无句柄）；单位换算经 core 唯一入口、CSV 转义经 io 唯一转义点（`io::CsvCell::fromText`）、诊断码登记归 DiagCodes 单点。
7. **不可变历史（PA-2）**：exportCopy 零修订（无项目句柄）；导入不写任何持久态。
8. **不继承 old/ 实现**：Import.hpp/.cpp 全新编写（文件头自证＋无 old/ 路径引用）。

**ird_gates 独立复算（引擎直跑）**：base（`7ac106cd` worktree）与 head（`e63fba9a` worktree）各 `cmake -DIRD_ROOT=<industrialrobot 根> -P ird_gates.cmake`（exit 1＝存量命中既有状态，DTB §4.5 存量例外未清零）；命中行按盘符前缀归一化后 `sort`＋`diff` → **逐行完全一致，零新增命中**；双方各 **127 行**，分码直方图完全一致且与送验附件 `ird-gates-base-head-compare.md` 逐格吻合：LIB 10、R1 3、R3 41、R4 3、R5 1、SUB 63、T1 5、T2 1＝127（F-359 登记的 requirements 两测试目标 SUB×2 存量在两侧同在，无新增）。

### 4.7 注释规范 —— 通过（AGENTS §2；公共头全查＋实现文件抽查 3 个）

- **Import.hpp（公共头，全查）**：文件头注含设计依据（卡 §7.3/7.4/7.5/9.5/9.6/10.2＋REQ-05/12/NFR-SEC-03/AT-02/AT-24＋契约 acceptance 1~6）、职责边界"为什么"、4 项实现决策登记、线程安全/确定性总注；每个类/结构体（FieldSpec/FieldDictionary/FieldMapping/AutoDetectResult/ImportUnitOptions/UnitPreviewEntry/ImportOutcome/ExportTarget/ExportOutcome/IRequirementImporter/RequirementImporter）均有业务概念＋生命周期＋线程安全注；每个函数（含 autoDetectMapping/previewUnitConversion 自由函数）有 @brief/@param 方向与取值/@return/@throws；错误语义（调用方违约 fail-fast vs 源数据值面）与确定性来源（from_chars 不经 locale、core 唯一换算）显式标注——§2.5 高危信息表全项在案（单位 m/rad、行号 1 起、所有权"调用方持有"、测试注入点所有权）。
- **Import.cpp（抽查 1）**：文件头注（设计依据＋线程安全＋确定性来源）＋逐步语义段注释（单位校验链三步、行装配处理序与"错误行其余字段继续收集"的业务理由、去重基准"错误行不占用去重键"的 AT-02 语义论证、行号/ObjectId 全零的口径溯源）——"解释为什么"达标。
- **DiagCodes.cpp（抽查 2）**：清单 1→5 表尾追加段带共同登记口径注（分类族依据/文案键约定/paramSchema 四键语义）＋逐码字段级注释。
- **DiagCodesTest.cpp／ImportTest.cpp（抽查 3）**：用例名带 V-xx/AT/需求追溯＋`IRD_TEST_INFO` 登记字段＋用例头注说明验证的 acceptance 条目。
- 反面扫描：新增 diff 无复述式/占位/过期注释；无 TODO/FIXME（4.11 ①同源）。

### 4.8 文档同步 —— 通过

- 单元卡增量修订：units/requirements.md §14.6 **v0.4**（2026-09-26）落位登记 10 项实现决策（ExportTarget 等价承载/CSV 摘要口径/行号口径/ObjectId 待分配/导出 id 合成占位/引用文本语法/JSON 未知键拒绝/词表空单元格＝缺省/坐标表仅 Fixed 字面/CSV draft 值面拒绝）＋§12 io 交接行"已落位——WP-14-T04"标记——本 diff 实测在案，实现与登记逐项对得上（验收者逐项与 Import.hpp/.cpp 实文核对：决策 1＝Import.hpp:28~37＋ExportTarget 类注；决策 2＝digestRawTable 单点＋mapJson 字节摘要；决策 3＝rowNo＝行序 1 起；决策 4＝objectId 全零；决策 5＝collectEntries 去重基准；决策 6＝idTextOf 合成 "draft-<序>"；决策 7＝ref_frame/tcp 文本语法双向变换；决策 8＝JSON 记录键词表外→行级错误＋顶层未知键 io profile Reject；决策 9＝词表可选列空单元格走缺省；决策 10＝CSV 通道仅 Fixed 字面导出）。声明"无语义偏差"经核对成立。
- 契约状态：state.json 在 head 处仍为 queue 态——按 PIPE §4.7 状态翻转为合入收尾（治理侧）动作，T02/T03 先例一致，非缺陷。
- **findings.json 未关闭条目相关性核对（v1.2 义务）**：open 共 296 条，逐类筛查与本任务相关者：
  - **F-358**（WP-14-T02 契约 verify 缺测试目标构建行/ctest 行家族）——本任务契约 verify 缺两测试目标构建行（ctest 行已在），家族新例，转登 **F-367**；
  - **F-359**（ird_gates 基线 +2 SUB 存量）——本任务门禁比对 127=127 零新增，该存量维持既有状态，无恶化，无需动作；
  - F-361/F-363/F-364/F-365（T02/T03 遗留建议：标题前缀/魔数钉住/等价承载文档化残余）——F-364 标题家族本任务再现（见 4.9），转登 **F-366**；F-363（canonical 魔数）与 F-365（sdurw_math 链文档化）属 T03 交付面，本任务 diff 未触及，继续 open 合理；F-365 所涉"CMakeLists 注释文档化"在本任务 CMakeLists 增列注释中延续保持（T04 增列注释自证 io 边消费面），部分覆盖状态不变；
  - F-189/F-305/F-307（io Csv.hpp 注释重复、URDF Import.cpp 两项——modeling 单元同名文件）与本任务无涉，不消账。

### 4.9 commit 质量 —— 通过（建议级 1 条转登）

- 两提交均**动词开头**（"实现需求 CSV/JSON 导入与副本导出…"/"登记 WP-14-T04 验证留痕…"）；正文四段（What/Why/验证/影响面）齐备。
- 验证结论与留痕一致性（防粉饰核对，逐项与验收者独立实测比对）：**75/75、5/5、ctest 2/2、门禁 127=127 零新增、双模式零错误——全部一致**；ird-test-report.json summary 计数与 gtest XML `tests="75" failures="0"`／`tests="5" failures="0"` 与实测三方一致，无手工转抄痕迹。
- 建议级：两提交标题均无 `[WP-14-T04]` 方括号前缀（ACC §4.9／AGENTS §6.3 标题格式"[任务编号] 动词开头"）——F-012/F-100/F-361/F-364 标题家族再现，转登 **F-366**（不追溯改写已推送提交）。

### 4.10 留痕完整 —— 通过

- `traceability/builds/wp14-t04/` 18 件随 `e63fba9a` 提交：集成构建日志、冒烟 configure/build 日志、两测试运行日志、**gtest XML 原样留存**（XML 头 `tests="75" failures="0"`／`tests="5" failures="0"`）、ctest 日志、ird-test-report.json（75 例）与 ird-test-report-contract.json（5 例）——与验收者独立直跑输出三方一致；ird_gates base/head 直跑日志＋norm 命中集（各 127 行）＋直方图＋比对附件。
- phase-one-validation.log 未追加本任务条目——T02/T03 起留痕落位即 builds/wp14-txx/ 证据目录（acceptance-protocol §4.10 的"log 或 resultNote"口径由证据目录＋state.json history 收尾承载），与 T03 验收记录同口径处理，非缺陷。

### 4.11 偷懒/缩水实现扫描 —— 通过

- ①TODO/FIXME/stub/placeholder/未实现/not implemented：`git diff base..head -- requirements/` 增行扫描**零命中**（无契约标注的触发式延后项需豁免）。
- ②空实现检测：公共接口无非平凡空函数体（`fieldDictionary()` 仅返回只读成员引用——接口契约本意；其余 TU 实现共 1443 行实质逻辑，验收者逐段读过）。
- ③逐条证据表：见 4.4 的 7/7 表——每条均有具名测试＋断言明细，任一条不缺。
- ④测试强度：断言校验行为与边界（三要素逐字段、字节级文件比对、SI 值黄金断言、摘要自算对表、值语义全等 roundtrip）；**非恒真**——4.5 三处变异真变红实证。

## 2. 问题清单（分级）

### 阻断级

无。

### 建议级（均已按 ACC §5 转登 traceability/findings.json）

| 编号 | 一句话 | 去向 |
| --- | --- | --- |
| F-366 | F-364 标题家族再现：T04 两送验提交（110d8457/e63fba9a）标题无 `[WP-14-T04]` 前缀（动词开头与四段正文齐备、验证结论经独立复算一致，纯风格瑕疵） | findings.json，open，owner＝后续提交规避 |
| F-367 | F-358 家族新例：WP-14-T04 契约 verify 数组缺两测试目标（`sdurws_ird_requirements_test`/`_contract_test`）显式构建行——验收者侧补建执行（75/75＋5/5），建议契约模板补构建行 | findings.json，open，owner＝契约治理面 |

## 3. 裁决

**verdict＝pass**：4.1~4.11 全部通过，无阻断级问题；建议级 2 条已转登 findings.json（F-366/F-367），不阻塞合入。送验对象＝`e63fba9a5e075370a7f03101d70dfd73e958d0e5`（按 SHA 冻结口径）；合入决策按 ACC §6 归所有者，验收者不执行合入。
