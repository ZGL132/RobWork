# IO-T05 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | IO-T05（≙WP-11-T06）资源导入服务与外部源检测（ResourceIo：reader/snapshotter/probe） |
| 契约 | tasks/foundation/IO-T05.json（status=ready，dependsOn=IO-T02，acceptance 五条） |
| 送验对象 | 7f4c1143b0037ddd9eee245b210823e83e1845ce（40 位冻结；提交范围 cb0047d73f4bcbe323c3c2f25137e74604fc902b..7f4c1143，三笔提交 b9e894cf/3019f4e6/7f4c1143） |
| 分支漂移检测 | origin/wp11-t05 实测＝7f4c1143b0037ddd9eee245b210823e83e1845ce（git rev-parse 核实）——零漂移，验收全程按该 SHA（ACC §3 送验对象冻结） |
| 验收环境 | 全新 detached worktree（`git worktree add --detach` @ 7f4c1143＝`D:/10_Source_Repos/21_robot/acc-tmp/io-t05-wt-a`）从零复现：集成 build 树（worktree A 内 `build/`，重新配置后缓存实测 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`）与独立冒烟 build 树（`acc-tmp/io-t05-acc-smoke`，带 vcpkg toolchain）均为本验收新建（非主树缓存复用）；记录回传 worktree（`acc-tmp/io-t05-wt-b`，分支 acc/IO-T05/1 基于 origin/redesign-main＝cb0047d7）。MSVC 2022 x64＋vcpkg 经典模式。**环境前置三件**（worktree 干净检出的既有环境缺口，非实施缺陷——F-007/F-062/F-099 已登记同型，IO-T04 验收记录同款处置）：①集成配置需 `-DCMAKE_TOOLCHAIN_FILE=<主仓>/vcpkg/scripts/buildsystems/vcpkg.cmake`（缺省首配实测失败于 Could NOT find Boost——主树缓存实证 Boost 解析自 vcpkg）；②需 `-DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`；③`RobWork/RobWorkStudio/bin/` 下 `RobWorkStudio.ini.template.static`/`RobWorkStudio.ini.shared.in` 被 `.gitignore [Bb]in/` 规则吞、干净检出缺失，自主仓补拷（未跟踪环境文件，不入任何提交）。补齐后配置零错误。独立性（ACC §2 四条逐条满足）：全新上下文只读代码/契约/单元卡/在库留痕，未读取实施者过程叙述；对抗式立场逐项找反证；全部构建、测试、门禁对账、治理脚本与两处实现失效注入均为本验收者亲手执行（ACC §2.4 复现优先于采信——实施留痕数值仅作交叉比对，不作采信依据） |
| verdict | **pass**（4.1~4.11 全部通过、零阻断级问题；建议级 3 项新发现转登 findings.json——F-194/F-195/F-196，见问题清单；既有 open 条目相关性核对见 4.8） |
| 验收日期 | 2026-09-17 |

## 4.1 改动范围 —— 通过

- `git diff --name-status cb0047d7..7f4c1143` 实证 **20 个变更文件**（`--stat`：6043 insertions/23 deletions），逐个比对契约 `allowedFiles`：
  - `RobWork/RobWorkStudio/src/rwslibs/industrialrobot/io/**`×8（CMakeLists.txt、IoError.hpp、IoDiagnostics.cpp 修改；ResourceIo.hpp、ResourceIo.cpp、ResourceIoTest.cpp、ResourceSolidifyTest.cpp 新增）——在 `io/**` 内；
  - `RobWork/doc/industrial-robot-design/traceability/builds/wp11-t05/**`×11（双模式 configure/build/test-run 日志、双 gtest XML、ird-test-report.json、gates/validate-task/verify-task 日志）——在 `traceability/**` 内；
  - `RobWork/doc/industrial-robot-design/units/io.md`——allowedFiles 显式登记。
- `forbiddenFiles`（REQUIREMENTS.md/ARCHITECTURE.md）零触碰（不在 diff 文件清单）。
- SA-02：`git diff <base>..<head> -- RobWork/patches/` 空、`industrialrobot/patches/` 无新文件——零未登记框架改动；diff 范围内无任何框架源码（RobWork/RobWorkStudio 本体）文件。

## 4.2 可复现构建 —— 通过（双模式零错误，验收者从零亲手复现）

- **集成模式（重新配置）**：worktree A 内全新 `build/`（首配因环境前置失败后清空重配），命令 `cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DCMAKE_TOOLCHAIN_FILE=<主仓>/vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64 -DRWS_BUILD_INDUSTRIALROBOT=ON`——exit 0（30.5s），`grep RWS_BUILD_INDUSTRIALROBOT build/CMakeCache.txt` 输出 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`（ACC §3 要求的确认项），`grep -c "CMake Error"`＝0。构建 `cmake --build build --config Release --target sdurws_ird_io_test`——exit 0；`grep -c -E 'error C[0-9]+|fatal error|LINK : fatal'`＝**0**。产物 `build/RobWorkStudio/bin/Release/sdurws_ird_io_test.exe`（1,595,904 字节）＋`libs/Release/sdurws_ird_io.lib` 就位，vcpkg 运行时 DLL（zip/bz2/z/libexpat）随 POST_BUILD 拷贝就位。
- **独立冒烟模式**：`cmake -S RobWork/RobWorkStudio/src/rwslibs/industrialrobot -B <acc-tmp>/io-t05-acc-smoke -DCMAKE_TOOLCHAIN_FILE=<主仓>/vcpkg/scripts/buildsystems/vcpkg.cmake`——exit 0（`CMake Error` 计数 0）；`cmake --build … --config Release --target sdurws_ird_io_test`——exit 0，error 计数 0。
- **测试执行（双模式）**：集成 `sdurws_ird_io_test.exe` `[==========] 102 tests from 9 test suites ran.` `[PASSED] 102 tests.`；冒烟同 102/102。与本任务 io.md §15.5 v0.8 及 ird-test-report.json 声明数值一致（102＝既有 75＋IoResTest 17＋IoSolidifyTest 10）。

## 4.3 契约 verify 命令 —— 通过

- 契约 `verify` 数组仅一条：`cmake --build build --config Release --target sdurws_ird_io_test`——验收者在集成构建中**亲手执行该命令**（exit 0 零 error，见 4.2），并另经 `verify-task.ps1 -TaskFile <契约>` 在验收环境二次驱动同一命令（脚本自检仓库根解析至 worktree A，实测链接输出 `sdurws_ird_io.vcxproj -> …sdurws_ird_io.lib`、`sdurws_ird_io_test.vcxproj -> …sdurws_ird_io_test.exe`，结尾 `verify-task: PASS IO-T05`）。
- 配套治理脚本口径：`validate-task.ps1 -TaskFile <契约>`——`validate-task: PASS (1 tasks)`；`validate-docs.ps1`——`validate-docs: PASS (20 units, 12 trace entries, 150 task files)`。三者均为验收者在验收环境亲手执行。

## 4.4 acceptance 条目逐项证据 —— 通过（五条全绿，验收者亲手复跑）

| # | 契约条目 | 证据（具名测试＋断言面；两模式均过） |
| --- | --- | --- |
| 1 | **V15/V16 全绿** | V15：`IoResTest.CyclicIncludeReportsCycleWithPathList`（A→B→A 环报 `IO-FORMAT-XML-CYCLE`，params `cycle` 断言含 a.xacro、b.xacro 与 `->` 序列——环路径清单）；`IoResTest.SelfIncludeReportsCycle`（A→A 自环）；`IoResTest.IncludeChainBeyondDepthTriggersIncludeBudget`（n01→…→n17 十七层链，`IO-SEC-BUDGET-INCLUDE` 比较型三要素 actual=17/limit=16/unit=levels）；对照用例 `DiamondIncludeIsNotCycle`（菱形共享 4 节点 4 边完整产出——环与 DAG 正确分界）。V16：`IoResTest.BinaryStlDeclaredFacesLyingRejectedBeforeBodyRead`（头声明 1,000,000 面 vs 实体仅 84 字节头，收紧 MeshFaceCount=100 后 `IO-SEC-BUDGET-MESH` 读前即拒，detail 断言"读前"阶段短语）；`IoResTest.AsciiStlRealOversizeTriggersMeshBudgetMidRead`（真实 12 面 ASCII STL 流式计数超限 10，detail 断言"读中"阶段短语，actual=12）——触发时机读前/读中均可断言。MDL-19 文件层支撑（展开引擎/语义归 modeling 阶段 B）：实现仅做文件层护栏，无任何展开语义（ResourceIo.cpp dependencyTree 实读）。 |
| 2 | **V17 全绿** | `IoSolidifyTest.SourceChangedDuringCopyDetectedAndStagingCleaned`：256 KiB 源（4×64 KiB 块），经进度检查点定点注入（首块复制完成后改写源——FaultInterceptor 替身，注入点确定不依赖时序）→断言失败码 `IO-RES-CHANGED`、`source-changed=true`、recorded=复制前 digest 摘要对、复制前后双快照 digest 不等、注入确已发生、`fs::exists(staging)==false`（中转清理＋无部分副本——§8.4 步骤 5 前后双快照语义）。 |
| 3 | **V24/V25/V26/V27 全绿** | V24：`IoSolidifyTest.ReadonlyTargetWritesMapToReadonlyCode`（预置只读属性中转目标，写打开被 OS 以 ACCESS_DENIED 拒绝后按写方向＋READONLY 属性判别分码 `IO-RES-READONLY`，断言 ≠ACCESS-DENIED、direction=write）。V25：`IoResTest.AclDeniedReadMapsToAccessDeniedNotNotFound`（ACL 注入 Everyone DENY FILE_READ_DATA ACE——属性读保留使规范化存在性检查通过、失败精确发生在数据面打开；断言 `IO-RES-ACCESS-DENIED` ≠NOT-FOUND、direction=read——§4.2.5 四分类不降级）。V26：`IoResTest.DependencyTreeMissingLeafReportsMissingList`（依赖树缺叶→`IO-RES-MISSING`、missing-count=2、清单逐项、detail 断言"资源事实"——§2.5 无工程不可行结论）＋`IoSolidifyTest.ProbeReportsMissingForDeletedExternalFile`（外部引用指向已删文件→`ExternalRefState::Missing`＋unreachable 细分注记）。V27：`IoResTest.SnapshotDigestStableAcross100Reads`（同文件 100 次快照 digest 恒定且等于手工 ContentDigester 复算）；`IoResTest.RenameDoesNotChangeDigestPathIsNotIdentity`（改名后 digest 不变、finalPath 确已变化——路径不作身份）。NFR-REL-04 检测实现责任方落位。 |
| 4 | **三段边界用例通过** | 段①一次性读取：用例源均位于用户区临时根走 P-1（NFR-SEC-01 例外），且 V16/V27 等用例同时断言预算管辖生效（收紧限额即拒）——NFR-SEC-01 例外仍受预算管辖。段②外部引用记录：`ExternalRefRecord`＝{absPath＋recordedDigest}（ResourceIo.hpp §8.5 形态）；`IoSolidifyTest.ProbeReportsOkAndIgnoresPurePathSpelling` 断言大小写拼写不影响 digest 判据——不以裸路径充当引用。段③固化前契约：`IoSolidifyTest.SolidifyUnchangedSourceKeepsStagingCopyForProject`（solidifyToStaging 产出中转目录、副本逐字节完整、copiedDigest 复算一致、sourceChangedDuringCopy=false、**objects 目录负断言**——发布与 objects 入库归 project）；`IoSolidifyTest.NonAuthorizedStagingDirRejected`（非 `.staging` 形态中转→`IO-SEC-PATH-ESCAPE` 且零写入）。直接创建 CanonicalModel/objects 写入禁止：实现实读——solidifyToStaging 唯一写点为 stagingDir（ResourceIo.cpp），无任何 objects 路径；测试负断言钉住。 |
| 5 | **P-IO-2/P-IO-1 处置** | P-IO-2（§8.6 五条）：`IoSolidifyTest.AdapterRoutingAndLifecycleRulings`——Solidified 缓存命中同指针（§8.6.4 允许的不可变缓存）、accessVersion 标记、Recorded 改源后重读即 Changed（绝不缓存——S10 复查前提）、先取缓冲跨多次调用仍完整可读（实际至析构——强于最低保证）、双源未命中＝Missing；`IoSolidifyTest.AdapterConcurrentRecordedReadsAreSafe`——4 线程×8 次并发读取全 Ok 且视图内容逐字节校验（§8.6.3）；runtime 侧文档表述同步：io 侧以 io 自有 ResourceBytesView/ResourceReadStatus 承载（io.md §15.5 v0.8＋io.md §9.7 登记在案），L5 转译薄壳归装配层（P-IO-1 裁决——本单元零 runtime 依赖）。P-IO-1（注入面）：`makeRuntimeResourceAdapter(IProjectBytesSource*, IExternalRefSource*, …)` 注入式协作（测试替身 FakeProjectBytes/FakeExternalRefs 即 project L5 注入形态）；实读核验 ResourceIo.hpp include 面仅 core/io 两族——**零 project/runtime 编译依赖**；CMake 链接面仅 sdurws_ird_core＋sdurws_ird_diagnostics（配置期守卫＋ird_gates R1 双证）；不直读项目存储（对象库字节仅经 tryObjectBytes 注入到达——§2.3 非目标 6）。 |
| 附 | ird_gates | `gates-ird-gates-target.log` 实测：唯一命中去重恰 **14 处**＝wp11-t04 基线全等（SUB×9＋T1×1＋R4×1＝11 处 DTB §4.5 登记例外＋F-191 的 LIB×3——libzip::zip/expat::expat 词表待扩，归门禁所有者），**零新增命中**。 |

## 4.5 测试真实失败能力 —— 通过（两处实现失效注入均转红，还原后复绿）

1. **注入一（V24 只读判别失效）**：ResourceIo.cpp `classifyWin32Error` 写方向 READONLY 属性判别改为恒假（`if (false && …)`）→增量重编→`--gtest_filter=IoSolidifyTest.ReadonlyTargetWritesMapToReadonlyCode` 实测 `[ FAILED ] 1 test`——测试确实钉住该分码语义，非恒真。
2. **注入二（V15 环检测失效）**：dependencyTree 显式栈 DFS 的 onStack 回边判定改为恒假→增量重编→`--gtest_filter=IoResTest.CyclicIncludeReportsCycleWithPathList:IoResTest.SelfIncludeReportsCycle` 实测 `[ FAILED ] 2 tests`——环检测用例真实有效。
3. **还原复绿**：两处注入全部还原后 `git status`（io/ 目录）零输出＝源码与送验 SHA 逐字节一致；全量重跑 `[PASSED] 102 tests`。两次注入仅存在于临时 worktree 构建产物且已还原，任务分支与送验对象零触碰、零推送。

## 4.6 架构红线（AGENTS §5 八条逐条） —— 通过

1. **L2 计算内核零 Qt**：io 单元源码（含本次 ResourceIo.* 与测试）`grep -E '#include <Qt|#include <Q|QString|QWidget'` 零命中（唯一命中为既有 IoFwd.hpp 的"零 Qt 约束"注释文本，非代码）；io 无 Qt 链接（CMake 链接面 core＋diagnostics＋libzip＋expat）。
2. **R-1 业务域互链禁止**：CMake 配置期守卫断言 sdurws_ird_io 仅可链 core＋diagnostics（实测全量构建通过＝守卫未触发）；测试文件 include 面仅 core/io/diagnostics 公共头；ird_gates R1 零新增命中。
3. **R-2 跨单元私有头禁止**：ResourceIo.hpp 位于 `include/sdurws/ird/io/`（io 唯一对外暴露位）；引用他单元仅 `core/Digest.hpp`、`core/Identity.hpp` 公共头；实现文件含 expat/windows 头（L1/OS 面——PRIVATE 链接，expat 类型不出公共面，实读核验）。
4. **SA-02 框架零修改**：见 4.1（patches/ 零新登记）。
5. **R-4 名称前缀拼接/剥离禁止**：`IExternalRefSource.tryRecord` 对 resourceId canonical 文本透传不解释（ResourceIo.cpp 实读注记＋代码一致）；全文件无 "RobWork" 字符串拼接逻辑（grep 零命中）。
6. **PA-1 权威唯一**：io 仅执行文件层职责——固化止步中转（发布/objects 入库归 project，测试负断言钉住）；路径解析归 SafePath（io 自有职责）；不实现 project 编排/runtime 语义；外部引用 id 由 project 注入侧解释。
7. **PA-2 不可变历史**：本任务零修订写路径触碰（io 无 revision 语义）。
8. **不恢复 old/**：diff 范围零 `old/` 引用。

## 4.7 注释规范（AGENTS §2） —— 通过

- **公共头全查**：`ResourceIo.hpp`（907 行，新增）文件头注（设计依据逐条列 io.md §/需求 ID/契约 acceptance）＋每个类型/函数 Doxygen 中文注齐备；§2.5 高危信息全项在位——物理单位（sizeBytes/mtimeUtc 注明字节/NT FILETIME 100ns）、坐标系/路径语义（finalPath 实体路径、P-1/P-5/P-6 角色）、所有权（注入源借用不接管、句柄唯一所有权、纯值类型调用方所有）、线程约束（无状态并发安全/solidify 会话级互斥/adapter 缓冲发布后只读）、错误语义（四分类/资源事实与业务结论正交/调用方违约防御性分码）、确定性来源（digest 纯字节变换、依赖树字典序稳定、诊断 params 键序）。`IoError.hpp` 增量（FormatXmlSyntax 表尾追加）带完整追注（码面缺口论证＋登记指引）。
- **实现文件（实际全读，超出抽查 ≥3 要求）**：`ResourceIo.cpp`（2332 行）逐语义段中文注释、复杂算法逐步讲解（§4.4 防护流程②→⑦步骤标注、DFS 显式栈＋出口标记的环/DAG 分界推理、§8.4 步骤 1~6 逐步、§8.6 五条裁决逐条落点、CREATE_ALWAYS 预删除刻意不做的 MSVC remove 行为论证）；两份测试文件（ResourceIoTest.cpp 662 行/ResourceSolidifyTest.cpp 606 行）文件头＋每用例中文注明验证的需求/AT 编号（AGENTS §2.7 测试条款）。`IoDiagnosticsTest.cpp` 双清单机械比对同步处带登记注。
- 未发现 AGENTS §2.6 反面示例（无复述性废注、无中英混杂机翻腔、无过期注释、无空泛 TODO）。

## 4.8 文档同步＋findings.json 相关性 —— 通过

- **零偏差＋等价调整登记**：实现与任务卡 acceptance 五条一一对应（见 4.4）；三处等价调整（snapshot/dependencyTree/solidifyToStaging 增补 `BudgetScopeId budgetScope={}` 尾参、probe 返回 `IoResult<ExternalRefState>`、dependencyTree importRoot＝入口文档语义澄清）均按 io.md §9.0 条款于 §15.5 v0.8 正式登记——实现形态与登记逐项一致（实读比对）。`IO-FORMAT-XML-SYNTAX` 表尾追加四件套同步：IoError.hpp 枚举（第 59 值）＋IoDiagnostics.cpp token/描述符（paramSchema ["path","row","column"]）＋IoDiagnosticsTest 双清单机械比对（59 值上界/57 描述符计数）＋io.md §9.12 XML 族行。io.md 版本行 v0.8＋§1.2 骨架行＋§15.5 变更记录＋文档尾进度五处同步；`validate-docs PASS`。
- **findings.json 未关闭条目相关性核对**（168 open，与本题相关者逐条）：F-007/F-009/F-062/F-099（worktree 环境前置三件）——本次验收实测同型命中并按先例处置（见验收环境栏），保持 open；F-010/F-153（.gitignore 吞留痕日志）——本任务 11 件日志全部随 7f4c1143 入库，`git check-ignore` 零命中、`git log` 逐文件在案，**未复踩**；F-191（ird_gates LIB 词表 3 处 libzip/expat 待扩）——本次门禁 14 处命中中含此 3 处，与基线全等零新增，保持 open（责任方门禁所有者）；F-192（IO-FORMAT-JSON-SYNTAX 等向 diagnostics 内置码表收编确认未发生）——本任务新增 IO-FORMAT-XML-SYNTAX 属同一治理动作的**新增在途项**，转登 F-196；F-193（[docs] 前缀用于单元卡修订）——本任务第 3 笔提交同型，转登 F-195。
- phase-one-validation.log 未更新：按 IO-T02~T04 既有口径，任务留痕以 builds/wp11-t05/＋ird-test-report.json 承载（ACC §4.10"或 resultNote 已更新"满足），非缺口。

## 4.9 commit 质量 —— 通过（含 1 项建议级风格问题）

- 三笔提交逐份实读：b9e894cf（第 1/3 步）、3019f4e6（第 2/3 步）、7f4c1143（第 3/3 步）——正文四段（改动内容/改动原因/验证/影响面）齐备，需求 ID 与任务卡章节引注完整；验证结论诚实：前两笔如实声明"ird_gates/validate-task 未执行（随第 3 步收尾统一执行并留痕）"、双模式完整留痕随第 3 步登记——与第 3 笔入库留痕交叉核对一致，**无粉饰**。
- 标题：`[WP-11-T06] 实现 …`×2 动词开头合规（任务编号按 ≙ 映射 WP-11-T06，契约 note 登记在案）；第 3 笔 `7f4c1143 [docs] IO-T05 文档同步与验证留痕：io.md v0.8 增量修订＋双模式证据`——前缀 `[docs]` 用于 io.md 单元卡修订（应 `[units]` 或 `[WP-11-T06]`），F-193 家族同型再现，建议级转登 **F-195**；纯风格瑕疵不阻塞。

## 4.10 留痕完整 —— 通过

- `traceability/builds/wp11-t05/` 11 件全部随送验提交入库（F-010 家族按 wp11-t04 先例 `add -f` 强制规避——check-ignore 实测零命中）：integration/smoke 的 configure/build/test-run 日志、双 gtest XML（各 102 `<testcase>`、10 个 `failures="0"`）、ird-test-report.json（ird-test-report/1：targets 双目标 build/run＋gates 14 处分解＋selfCheck＋testSuites，IoResTest 17＋IoSolidifyTest 10 逐用例 trace＝acceptance/需求映射）、gates/validate-task/verify-task 日志。
- 验收者独立复跑产物（集成/冒烟双份 gtest XML＋配置/构建/测试日志）落 acceptance 临时目录（acc-tmp/io-t05-*），数值与在库留痕一致（102/102、错误 0、门禁 14 处）。

## 4.11 偷懒/缩水实现扫描 —— 通过

- **① 词表扫描**：`grep -rn -E 'TODO|FIXME|stub|placeholder|未实现|not implemented'` 于 diff 范围 7 个代码文件（ResourceIo.hpp/.cpp、IoError.hpp、IoDiagnostics.cpp、ResourceIoTest.cpp、ResourceSolidifyTest.cpp、CMakeLists.txt）**零命中**（无"触发式延后"豁免需要列出）。
- **② 空实现检测**：三接口全部方法均有完整执行体（实读核验：IResourceReader 四方法含完整防护链；IResourceSnapshotter probe/solidifyToStaging 全量；IRuntimeResourceAdapter 路由＋缓存＋稳定区全量）——无返回默认值的空壳；头文件先行/实现随提交 2 落位的三步提交策略已在提交信息与单元卡登记，送验 SHA 为完整实现。
- **③ 逐条证据表**：见 4.4 表——acceptance 五条逐条具名测试＋断言面，任一条均有可复现证据，无"已实现"口头声明依赖。
- **④ 测试强度**：断言校验行为与边界（码值/比较型三要素/阶段短语/清理负断言/同指针/并发视图逐字节校验），非恒真——4.5 两处失效注入实测转红为证。

## 发现的问题清单

| 级别 | 编号 | 内容 | 处置 |
| --- | --- | --- | --- |
| 阻断 | 无 | —— | —— |
| 建议 | F-194（新登 findings.json） | ResourceIo.cpp 使用 `std::mutex`/`std::lock_guard`（第 2095/2123/2269/2308 行）但 include 列表无 `<mutex>`——现依赖 MSVC STL 传递包含侥幸编译，属自包含性/可移植性缺陷（换标准库实现或调整传递头即可能断编） | 转登 findings.json（open，责任方：io 单元后续任务/IO-T06+ 顺手一行补 include）；不阻塞合入 |
| 建议 | F-195（新登 findings.json） | 第 3 笔提交 7f4c1143 标题前缀 `[docs]` 与内容主体不符（主体为 units/io.md 单元卡修订＋留痕，应 `[units]`/`[WP-11-Tkk]`）——F-012/F-027/F-179/F-185/F-193 提交标题风格家族再现；纯风格瑕疵，正文四段与验证结论无失实 | 转登 findings.json（open，随标题风格家族统一治理）；不阻塞合入 |
| 建议 | F-196（新登 findings.json） | IO-FORMAT-XML-SYNTAX（IoErrorCode 第 59 值，本任务表尾追加）向 diagnostics 内置码表（§4.6 87 码清单）的收编确认未发生——F-192（IO-FORMAT-JSON-SYNTAX 同型在途）的新增同类项 | 转登 findings.json（open，责任方：diagnostics 所有者收编确认，与 F-192 同批处置）；不阻塞合入 |
| 建议（既有，不新登） | F-007/F-009/F-062/F-099 | worktree 环境前置三件——本次验收同型命中、按先例处置 | 保持 open |
| 建议（既有，消账状态核对） | F-191 | 3 处 LIB 词表命中仍在（与基线全等，零新增） | 保持 open（门禁所有者） |

## 裁决

**verdict = pass**。4.1~4.11 全部通过、零阻断级问题；建议级 3 项（F-194/F-195/F-196）已按 ACC §5 转登 findings.json，不阻塞合入。合入决策按 ACC §6 归所有者。

## 验收者环境说明

- 复现现场：`git worktree add --detach D:/10_Source_Repos/21_robot/acc-tmp/io-t05-wt-a 7f4c1143b0037ddd9eee245b210823e83e1845ce`；集成树 `wt-a/build`（配置：VS17 2022 -A x64＋vcpkg toolchain＋Qt prefix＋`RWS_BUILD_INDUSTRIALROBOT=ON`，缓存实测 BOOL=ON）与冒烟树 `acc-tmp/io-t05-acc-smoke`（vcpkg toolchain）均为本次从零新建；记录回传现场 `acc-tmp/io-t05-wt-b`（分支 `acc/IO-T05/1`）基于 origin/redesign-main＝cb0047d73f4bcbe323c3c2f25137e74604fc902b。
- 全部证据（双模式配置/构建/测试 102/102、契约 verify 与治理脚本三件、门禁 14 处对账、两处失效注入与还原复绿、红线/词表扫描、留痕与提交实读）为验收者在本现场亲手执行；两处注入仅存在于临时 worktree 构建产物且已还原复绿（`git status` 零残留），任务分支与送验对象零触碰、零推送。验收完成后临时现场随流程删除，本记录与三元组为唯一持久证据。
