# WP-13-T02 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 ID | WP-13-T02（modeling 构建落位：占位库转真实目标＋门禁边登记） |
| 任务契约 | tasks/foundation/WP-13-T02.json（status=ready） |
| 送验对象（冻结） | `7fdfe8cfffba61e7bba188532afaa2f3479a4f28`（分支 wp13-t02 尖端） |
| diff 范围 | `010a263ca40988c0a66770759615af827fefa7ec..7fdfe8cfffba61e7bba188532afaa2f3479a4f28`（39 文件：＋35 A / ＋4 M，+5134/−10） |
| verdict | **pass**（4.1～4.11 全项通过；无阻断级问题；建议级 1 条已转登 findings F-293） |
| 验收日期 | 2026-09-22 |
| 验收者 | 独立验收会话（全新上下文；未消费实施者任何过程叙述，仅消费契约/设计文档/送验提交产物） |
| 记录分支 | `acc/WP-13-T02/1`（记录提交 SHA 见本文件所在分支尖端；收尾按 SHA 合并） |

---

## 1. 送验对象冻结与漂移检查（ACC §3）

- 验收全程按 40 位 SHA `7fdfe8cfffba61e7bba188532afaa2f3479a4f28` 进行；复现现场为 detached worktree（`git worktree add --detach`，不按分支名）。
- 漂移检查：验收执行中实测 `git ls-remote origin refs/heads/wp13-t02` → `7fdfe8cfffba61e7bba188532afaa2f3479a4f28`，与送验 SHA 一致，**无分支漂移**。

## 2. 逐项证据（ACC §4.1～4.11）

### 4.1 改动范围 —— **通过**

- `git diff --name-status 010a263c..7fdfe8cf` 共 39 文件，逐一比对契约 `allowedFiles`：
  - `RobWork/RobWorkStudio/src/rwslibs/industrialrobot/modeling/**`（15 文件：CMakeLists.txt、contract_test/2、include/3、src/3、test/5）✓
  - `RobWork/RobWorkStudio/src/rwslibs/industrialrobot/CMakeLists.txt`（M）✓
  - `RobWork/RobWorkStudio/src/rwslibs/industrialrobot/cmake/ird_gates_whitelist.cmake`（M）✓
  - `RobWork/doc/industrial-robot-design/units/modeling.md`（M，v0.2→v0.3）✓
  - `RobWork/doc/industrial-robot-design/traceability/**`（22 文件：builds/wp13-t02/ 留痕 20＋findings.json F-292 登记）✓
- `forbiddenFiles`（REQUIREMENTS.md、ARCHITECTURE.md）：diff 中零触碰 ✓
- 框架源码（`RobWork/` 其余、RobWorkSim 等）：diff 零触碰；`industrialrobot/patches/` 零 diff（SA-02 无需登记）✓

### 4.2 可复现构建（双模式，验收者亲手）—— **通过**

- **集成模式**：worktree A（detached @ 7fdfe8cf）全新构建树重新配置。缓存确认 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`（build/CMakeCache.txt）。环境前置按在册发现 F-007/F-254 口径补齐：vcpkg toolchain＋Qt6 `CMAKE_PREFIX_PATH`＋未追踪辅助文件 `RobWorkStudio.ini.template.static`（该文件被 .gitignore 吞掉、仅存于主工作树——F-007 标题明文在案；从主工作树复制，未改动任何被追踪文件）。配置 exit 0（`-- Configuring done (27.2s)`）。
  - `cmake --build build --config Release --target sdurws_ird_modeling` → **exit 0，错误计数 0**（日志 acc-v1-modeling.log）
  - `--target sdurws_ird_modeling_test` → **exit 0，错误计数 0**（acc-v2-test.log）
  - `--target sdurws_ird_modeling_contract_test` → **exit 0，错误计数 0**（acc-v3-contract.log）
- **独立冒烟模式**：独立目录 `/d/tmp_acc_wp13t02_smoke`（脱离 RobWorkStudio 顶层），`cmake -S .../industrialrobot -DCMAKE_TOOLCHAIN_FILE=vcpkg/.../vcpkg.cmake -DCMAKE_PREFIX_PATH=<Qt>`（AGENTS §4.1 toolchain 必带＋F-254 在册 Qt 前缀口径）→ 配置 exit 0；`cmake --build ... --config Release` 全量构建 **exit 0，错误计数 0**（含 modeling 产品库与两测试目标从零编译）。
- 首次冒烟/集成配置失败（缺 Qt 前缀、缺 ini 模板）均为验收环境前置缺失，属在册发现 F-007/F-254 范畴，与送验产物无关；补齐参数后双模式零错误。

### 4.3 契约 verify 命令逐条执行 —— **通过（7/7 逐条执行；第 5 条如实记录退出码 1 的在案原因）**

| # | 命令 | 结果 |
| --- | --- | --- |
| 1 | `cmake --build build --config Release --target sdurws_ird_modeling` | exit 0 |
| 2 | `cmake --build build --config Release --target sdurws_ird_modeling_test` | exit 0 |
| 3 | `cmake --build build --config Release --target sdurws_ird_modeling_contract_test` | exit 0 |
| 4 | `ctest --test-dir build/RobWorkStudio/src/rwslibs/industrialrobot/modeling -C Release -L "^ird$"` | **exit 0，2/2 passed**（sdurws_ird_modeling_test、sdurws_ird_modeling_contract_test） |
| 5 | `cmake --build build --config Release --target ird_gates` | exit 1——**ird_gates 门禁红为既有基线**（见下） |
| 6 | `pwsh -File RobWork/scripts/industrialrobot/validate-task.ps1 -TaskFile .../WP-13-T02.json` | **PASS (1 tasks)**，exit 0 |
| 7 | `pwsh -File RobWork/scripts/industrialrobot/validate-docs.ps1` | **PASS (20 units, 12 trace entries, 185 task files)**，exit 0 |

第 5 条详证（验收者亲跑 ird_gates 并解析命中输出）：全仓命中族分布 IRD-GATE-SUB 49 / IRD-GATE-R 42 / IRD-GATE-LIB 8 / IRD-GATE-T 6；其中 **modeling 相关命中恰 2 条**＝`sdurws_ird_modeling_test→sdurws_ird_testkit`、`sdurws_ird_modeling_contract_test→sdurws_ird_testkit`（IRD-GATE-SUB），与送验留痕 `gates-hit-set-diff.txt`（base 44 项→branch 46 项、新增恰 2、移除 0）及 findings **F-292**（实施者按 EV-T11/IO-T07 先例自登记，F-051/F-072 同根因：门禁引擎 SUB 形态一未对白名单 T-1 段"测试目标→sdurws_ird_testkit"豁免，处置权归 WP-01 构建所有者）**逐字一致**；`sdurws_ird_modeling` 产品目标零命中；六边登记后 IRD-GATE-GRAPH 零命中。该红为 F-019/F-051/F-072 链条的既有基线，非本任务引入的违规。

### 4.4 契约 acceptance 四条逐项核对 —— **通过（4/4，均有亲手复现证据）**

| # | acceptance 要点 | 证据 |
| --- | --- | --- |
| 1 | 目标落位与形态 | modeling/CMakeLists.txt L45 `add_library(sdurws_ird_modeling STATIC ...)`；目标名不变；别名 L98-100 `RWS::ird::modeling`（仅集成模式）；L53/L54-57 `cxx_std_17`＋`CXX_STANDARD 17/REQUIRED/EXTENSIONS OFF`；零 Qt（链接面无 Qt＋源面运行期扫描用例 ProductFaceHasZeroQtIncludes 绿＋配置期守卫）；两测试目标经自持 `ird_add_gtest` 宏（L174-186：add_test＋`<target>_report` XML＋EXCLUDE_FROM_ALL——与 core/CMakeLists.txt 同名宏逐行同款）；`set_tests_properties(... LABELS "ird")`（L230-231）；双模式构建零错误（4.2） |
| 2 | 配置期红线守卫随文件自持 | CMakeLists.txt L249-274 foreach 三模式：白名单外 `^sdurws_ird_`→FATAL_ERROR、`Qt[0-9]?::/Qt5::/Qt6::`→FATAL_ERROR、`^GTest::/^gtest$`→FATAL_ERROR；LINK_LIBRARIES＋INTERFACE_LINK_LIBRARIES 两面读取防漏；ird_gates 目标运行结果＝modeling 产品面零命中（4.3 第 5 条详证） |
| 3 | 门禁白名单六边登记 | ird_gates_whitelist.cmake `IRD_ALLOWED_UNIT_EDGES` 增 modeling→core/diagnostics/project/runtime/policy/io 六行（diff 实证，各带出处注）；`IRD_EXTRA_EDGE_REFS_EDGES/NOTES` 成对 1→7（各带 modeling.md §3.2 出处注）；亲跑 ird_gates：modeling 源集/产品目标零命中、IRD-GATE-GRAPH 零命中（EXTRA_EDGE_REFS 一致性核对通过） |
| 4 | 公共头三件落位 | `include/sdurws/ird/modeling/` 下 ObjectTypes.hpp（五对象 token——robot-design 经 `using runtime::kRobotDesignObjectType` 引入同一实体，Sources.hpp:108 权威常量 `"robot-design"`；测试以**地址相等**断言实体同一性 RobotDesignTokenIsRuntimeConstantEntity；SceneObjectRole 词表五值＋token 转发＋try 轨）、Errors.hpp（ModelingErrorCode 12 值全表＋modelingErrorCodeToken＋ModelingError 值类型＋modelingDiagCode 映射数据——当前仅 SchemaVersionUnsupported→MDL-READINESS-SCHEMA-UNSUPPORTED 已登记行，与"只登记有消费者条目"纪律一致）、DiagCodes.hpp（modelingCodeDescriptors 工厂恰 1 项＋registerModelingCodes）；三头零实现依赖可被下游解析（ErrorsTest.cpp/DiagCodesTest.cpp 直接 include 并以真实 StableCodeRegistry 注册验证通过） |

### 4.5 测试真实失败能力（验证验证者）—— **通过（2 个变异探针均如期变红，还原后全绿）**

- **探针 1（编译面语义变异）**：`src/ObjectTypes.cpp` `return "Tool";` → `return "Toolx";`，重编冒烟 `sdurws_ird_modeling_test` 后运行：**3 用例如期红**——`MdlObjTypes.SceneObjectRoleVocabularyFiveValues`、`RoleTokensAgreeWithPolicyConsumerMirror`（与 policy 消费侧逐值比对）、`RoleTokenRoundTripAndUnknownRejected`；其余 14 用例保持绿（红得"准"，恰好圈定受变异语义面）。
- **探针 2（源码面红线扫描）**：源文件尾部追加 `// #include <QtWidgets/QWidget>` 注释行，直接运行测试 exe：`MdlBuildRedLine.ProductFaceHasZeroQtIncludes_WP13T02_ACC1` **如期红**（运行期扫描源码树生效，与 ird_gates 4b 步同判据）。
- 还原（备份 cp 回＋touch 防 F-068 mtime 陷阱）＋重编后 **17/17 全绿恢复**；`git status` 确认 worktree A 源码树与送验 SHA 逐字节一致（探针零残留）。

### 4.6 架构红线（AGENTS §5 八条逐条自查）—— **通过**

1. **L2 计算内核零 Qt**：产品面 3 头＋3 TU 无任何 Qt include（亲读全文件＋运行期扫描用例绿）；链接面 PUBLIC 六目标全为 ird 单元目标；配置期 Qt FATAL 守卫在位。
2. **R-1 业务域互链禁止**：产品库仅链六条登记边；7 个业务域单元与 4 个表外平台单元零出现（BuildGraphContractTest.NoBusinessUnitOrExtraPlatformEdge 用例绿＋亲读 CMakeLists）。
3. **R-2 跨单元私有头禁止**：产品面消费的他单元头 runtime/Sources.hpp、diagnostics/DiagCodes.hpp 均实处对方 `include/sdurws/ird/<unit>/` 公共面（亲验 ls）；BuildRedLineTest.IncludeFaceRestrictedToSixRegisteredEdges 以文件系统可达性断言同判据。
4. **SA-02 框架零源码修改**：diff 零框架文件＋patches/ 零新登记（4.1）。
5. **R-4 名称前缀拼接/剥离**：产品面零 "RobWork" 字面量（运行期扫描用例绿——线性状态机剥注释口径与门禁 F-011 修复后一致）。
6. **PA-1 权威唯一**：robot-design token 权威留 runtime（using 引入不另设）；MDL-* 码注册权威留 diagnostics StableCodeRegistry（registerModelingCodes 仅转发）；词表所有者 modeling＋policy 消费镜像逐值同串对账；无越权实现他人语义。
7. **PA-2 不可变历史**：本任务无修订写路径改动（不适用，无违反）。
8. **不继承 old/ 实现**：全部代码按 units/modeling.md §3.3/§4.2/§4.5/§9.4/§9.5 从零编写，diff 零 old/ 引用。

### 4.7 注释规范（AGENTS §2）—— **通过**

- **公共头全查（3/3）**：ObjectTypes.hpp/Errors.hpp/DiagCodes.hpp 均有文件头（@file/@brief/设计依据含需求与任务号/背景说明"为什么"）、类与函数 Doxygen 中文契约注释（@param 方向与语义/@return/@throws；线程安全与确定性声明）；§2.5 高危信息：错误语义（错误面/诊断面两层分工、nullopt 契约）、确定性来源（switch 全枚举同码同串）、所有权（registry 调用方持有、值类型按值传递）、线程约束（并发只读安全/注册期单线程）逐项在位。物理单位/坐标系/rad 本任务为 token/词表登记面，不涉及物理量，无不适用缺失。
- **实现文件抽查（3/3 超过 ≥3 要求）**：ObjectTypes.cpp/Errors.cpp/DiagCodes.cpp 语义段落注释完备（转发表纪律、不可达兜底 return 的解释、映射阶段纪律逐值注解）。
- **测试文件（5/5）与 CMakeLists**：用例名带 `_WP13T02_ACCn` 追溯段＋IRD_TEST_INFO 需求/AT 登记＋断言处中文说明验证点；CMake 关键步骤（目标注册/守卫/宏）中文注释块完整。

### 4.8 文档同步＋findings 未关闭条目相关性 —— **通过**

- **单元卡同步**：units/modeling.md v0.3——§1.2 构建落位/代码落位行同步（"WP-13-T02 已落位"）、§3.2 六边"已随 WP-13-T02 登记完成注记"（含 dependency-graph.json 机器镜像刷新归 traceability 维护任务的口径）、§14.6 v0.3 变更记录逐条；DTB §2.14 T02 产出列"_plugin 注册"与卡 §3.2"随 WP-13-T15"的偏差按卡执行并登记（契约 note ② 预登记的既载偏差，DTB §5.4 增量修订消账归治理侧——非实现偏差，属"偏差已登记"合规路径）。
- **追踪矩阵/状态**：validate-task/validate-docs PASS（4.3）；contract-compile-log.md L216 WP-13-T02 行 ready；留痕目录齐备（4.10）。
- **findings.json 未关闭条目相关性核对**（open 235 条逐类筛查）：
  - **直接相关**：F-051/F-072（ird_gates SUB 形态一 vs 白名单 T-1 矛盾，major/open）——本任务新增 2 处同根因命中，实施者已按先例自登 **F-292**（major/open，处置归 WP-01 构建所有者，与 F-051 合并处置建议明确），闭环登记无静默积压。
  - **环境类再次印证**（本验收实际踩到、按 F-007/F-254 在册口径补参后通过）：F-007（全新树需 toolchain＋Qt 前缀＋ini.template.static——本次验收三件全部实证）、F-254（冒烟命令缺 Qt 前缀）、F-009/F-062（worktree 环境前置）。均为既有 open 条目，本任务无新增环境缺陷，不重复登记。
  - 其余 open 条目（提交标题家族、计数失准家族、ui/evidence/runtime 单元内瑕疵等）与本任务无相关性。

### 4.9 commit 质量 —— **通过（正文四段齐备、结论与独立复跑一致）；标题问题转登 F-293**

- 正文四段（改动内容/改动原因/验证/影响面与兼容性）齐备，验证段逐条与验收者独立复跑结果吻合（构建零错误、17/17＋5/5、ctest 2/2、门禁命中集 44→46、validate 双 PASS）——**无粉饰**；ird_gates exit 1 的在案原因在 commit 影响面段如实预声明。
- 标题 `[WP-13-T02] modeling 构建落位：…` 前缀后为名词短语，非动词开头——F-012 家族同型再现（建议级，已转登 **F-293**；正文齐备＋结论无粉饰故不构成阻断）。

### 4.10 留痕完整 —— **通过**

- gtest XML 4 件：integration/smoke × 17 用例（failures=0, errors=0）＋ integration/smoke × 5 用例（failures=0, errors=0）——亲验 grep `tests="/failures="/errors="`。
- ird-test-report.json 4 件：integration/smoke × 单元（summary total:17 passed:17 decisive:true）＋ contract（total:5 passed:5 decisive:true）——亲验 node 解析。
- 构建/测试/门禁/脚本日志：integration-build.log、integration-ctest.log、smoke-build.log（614 行）、smoke-configure.log、test-run 四件、gates base/branch 日志＋hit-set diff、validate-task/validate-docs.log 共 20 件落位 `traceability/builds/wp13-t02/`。
- phase-one-validation.log 自 CORE-T01 后流程演化为逐任务 `builds/<task>/` 目录留痕（该 log 未再追加属流程现状，非本任务缺位）；登记面由 contract-compile-log.md WP-13-T02 行承载（"phase-one-validation.log **或** resultNote"的或分支满足）。

### 4.11 偷懒/缩水实现扫描 —— **通过（含逐条证据表）**

1. **标记扫描**：diff 新增行 `TODO|FIXME|stub|placeholder|未实现|not implemented` 命中仅 1 处＝ErrorsTest.cpp L124 `ModelingError placeholder{};`——为缺省构造语义断言用例的**局部变量名**（验证 code 缺省＝DuplicateObjectId、params/detail 空的契约行为），非占位实现，不构成缩水。
2. **空实现检测**：三个 TU 全部为真实实现（switch 转发表/映射数据/描述符工厂＋注册转发），公共接口无空函数体、无恒返回默认值（nullopt 返回为卡面 §9.5 阶段纪律的**显式设计语义**且有注释与测试封闭性断言承载，非偷懒）。
3. **逐条证据表**：见 §2 的 4.4 表——acceptance 四条每条均给出具名测试/亲跑命令输出/文件行号，无一条仅凭"已实现"声明。
4. **测试强度**：断言校验行为与边界——地址相等（实体同一性而非仅值等）、全枚举机械比对（卡面字面自持清单 vs 实现）、词表外 nullopt 边界（空串/大小写变体/尾空白）、异常断言（DuplicateRegistrationRejected 捕获后核对 DiagnosticsErrorCode::DuplicateCode 再重抛）、真实 StableCodeRegistry 注册＋manifest 稳定；并与 4.5 变异联动实证测试会红。

## 3. 发现问题清单

| 级别 | 问题 | 处置 |
| --- | --- | --- |
| 阻断 | 无 | — |
| 建议 | 提交标题前缀后名词短语开头（F-012 家族同型再现） | 已转登 `traceability/findings.json` **F-293**（open，随家族统一处置） |
| 备注 | ird_gates 门禁红（基线 44＋本任务同根因扩员 2） | 实施者已自登 F-292（major/open），处置权归 WP-01 构建所有者，非本任务可修（ird_gates.cmake 不在 allowedFiles）——随验收 pass 不阻塞合入 |

## 4. 验收者环境说明

- 复现现场：detached worktree `D:\tmp_acc_wp13t02_a` @ 7fdfe8cf（记录回传另用 worktree B 于 origin/redesign-main）；集成构建树 A/build（VS17 2022 x64，RWS_BUILD_INDUSTRIALROBOT=ON，vcpkg toolchain＋Qt 6.11.1 前缀＋ini.template.static 辅助文件——F-007/F-254 在册口径）；冒烟树 `D:\tmp_acc_wp13t02_smoke`（独立目录，toolchain＋Qt 前缀）。
- 工具：cmake 3.x＋MSVC 19.42（VS2022 17.12）、ctest、pwsh 7（validate-*）、node 24（findings.json/ird-test-report 机器解析）。
- 变异探针与还原全程仅触碰 worktree A 内 `modeling/src/ObjectTypes.cpp`（备份→变异→还原→touch→git status 确认零残留）；冒烟树重编两轮，集成树未受探针污染（探针仅冒烟树重编，集成树二进制为纯送验 SHA 构建）。

## 5. 裁决

**verdict＝pass**。4.1～4.11 全项通过、无阻断级问题；建议级 1 条（F-293）已按 ACC §5 v1.2 转登 findings.json。本记录为合入段输入：所有者决策合入 `redesign-main` 时按收尾口径以本分支记录提交的 40 位 SHA 为不可变合并对象。
