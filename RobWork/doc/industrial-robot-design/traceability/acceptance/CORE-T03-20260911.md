# CORE-T03 验收记录（对抗式验收）

| 字段 | 值 |
| --- | --- |
| 任务 ID | CORE-T03（≙WP-03-T03，值来源与缺失值 Provenance/SourcedValue） |
| 契约 | tasks/foundation/CORE-T03.json（status: ready，随收尾翻转 done） |
| 验收对象 | 分支 `wp03-t03`，head＝`7b786f70c7377bf1c851f6def91e770cf2f75d50`（与 origin 一致——漂移检测 PASS），范围 `b92b1dc..7b786f7`（base 为所有者"调整队列"治理提交） |
| 尝试序号 | attempt=1（evidence 分支 `acc/CORE-T03/1`） |
| **verdict** | **pass**（4.1~4.11 全过；1 条建议级发现已登记 core.md v0.5 偏差行——非新增 findings） |
| 验收日期 | 2026-09-11 |
| 独立性声明 | PIPE v1.7 §0.4 单会话降级；补偿＝worktree A（detached @ head）亲手复现（31/31）＋"缺失转零"探针核验＋证据链前置自查（6 份日志 -f 入冻结对象——F-010 教训内化） |

---

## 逐项证据（4.1~4.11）

| # | 检查 | 结论与证据 |
| --- | --- | --- |
| 4.1 | 改动范围 | diff＝10 文件：core/{Provenance.hpp,Provenance.cpp,ProvenanceTest.cpp}＋core/CMakeLists＋units/core.md＋traceability 5——allowedFiles 内；forbiddenFiles 零触碰 |
| 4.2 | 可复现构建 | worktree A 全新配置 exit 0（ini 模板前置补齐）＋verify 目标构建 exit 0＋测试 31/31 PASSED（验收者亲手）；集成全量 exit 0 |
| 4.3 | verify 命令 | `cmake --build build --config Release --target sdurws_ird_core_test` exit 0 |
| 4.4 | acceptance 逐条 | ①UT-MISS 四态语义用例通过：FourStateMatrix 逐行（Provided 全可用／三非值态 value() 抛／Invalid 保留原串）＋EmptyRawInputRejected＋EqualitySemantics——两模式 XML；②"缺失不转零"不变量：MissingNeverBecomesZero（NotProvided/默认构造取值必抛＋tryValue 无值＋显式 0.0 与缺失不等）——类型层切断验证 |
| 4.5 | 失败能力 | "缺失转零"探针（tryValue 守卫替换为返回占位值）→MissingNeverBecomesZero 如期变红，恢复后 31/31 复绿（报告记录；探针零残留 grep 实证） |
| 4.6 | 红线 | 零 Qt；CR-01 遵守（core 公共类型零语义改写——P-1 校验为新增接口自身契约）；R-2 头根；SA-02 零框架改动 |
| 4.7 | 注释 | 公共头全查（四态表语义/P-1/无默认值语义/线程安全）；实现关键步（P-1/保留值/语法）注释；测试每用例追溯字段 |
| 4.8 | 文档同步 | core.md v0.5 偏差登记（methodTag 字符集放宽 [A-Za-z…——§4.3 卡内自例与原限定自相矛盾，实现取可用意图＋UT 同步，详设所有者裁决定格）；CR-01 消费约束指引（变更走 api-diff 流程）；findings 核对：F-010 前置自查（4.1）；F-004/F-008 无涉 |
| 4.9 | commit 质量 | 两提交四段式＋Acc-Covered（1,2 / 2）；调试留痕如实入报告 notes |
| 4.10 | 留痕完整 | 10 文件齐（日志 6＋XML×2＋report＋core.md）；ird-test-report 含探针与调试 notes |
| 4.11 | 偷懒扫描 | 零命中；Provenance 为实质实现（严格解析/校验/四态机）；测试断言行为与边界（含反例矩阵与不等断言） |

## 建议级发现（已处置，非悬置）

methodTag 字符集矛盾（§4.3 自例 vs 自身限定）→ 实现取可用意图＋core.md v0.5 登记＋UT 同步——**已按 DTB §5.4 完成**，无需另开 findings。

## 问题清单

阻断级：无。
