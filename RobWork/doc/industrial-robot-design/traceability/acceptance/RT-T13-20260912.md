# RT-T13 验收记录（对抗式验收，attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 ID | RT-T13（≙WP-06-T13，runtime 文档与门禁同步） |
| 契约 | tasks/foundation/RT-T13.json（status: ready，随收尾翻转 done） |
| 验收对象 | 分支 `wp06-t13`，head＝`a2bb6ff61b23b149a60efbc8d28fc32eb859f3a5`（40 位冻结对象；漂移检测 PASS——`git ls-remote origin refs/heads/wp06-t13` 实测＝该 SHA）；范围 `61daa56d4092237a8de9d402cde3984b9b376c61..a2bb6ff6`（base＝origin/redesign-main，4 个提交） |
| 尝试序号 | attempt=1（evidence 分支 `acc/RT-T13/1`，基于 origin/redesign-main＝61daa56d） |
| **verdict** | **pass**（4.1～4.11 全部通过、零阻断级问题；新增建议级发现 1 条——F-055 实施侧冒烟留痕未声明构建配置，已按 ACC §5 转登 findings.json，不阻塞合入） |
| 验收日期 | 2026-09-12 |
| 独立性声明 | 验收者为全新上下文会话（PIPE §0.4 子代理派发模式，无需降级声明），仅消费任务契约、设计文档、送验提交产物与留痕文件，未读取实施者任何过程叙述；构建、测试、探针、门禁、治理脚本全部由验收者亲手在独立 worktree 重跑，复现优先于采信（ACC §2 四条逐条满足）。对抗式立场：为"不通过"找证据——实测否决了实施留痕中"冒烟构建配置"的隐含口径（见 F-055）并逐条复核了登记件全部事实主张（均属实）。 |
| 验收环境 | Windows 10 x64（10.0.26200）/ Git Bash / VS2022（MSVC 19.42）/ Qt 6.11.1（CMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64）/ vcpkg 经典模式（toolchain 绝对路径 D:/10_Source_Repos/21_robot/RobWork/vcpkg/scripts/buildsystems/vcpkg.cmake）/ pwsh 7。复现现场＝`%TEMP%\ird-acc-rt-t13-a`（detached worktree @ a2bb6ff6，集成树全新配置）；冒烟树＝`%TEMP%\ird-acc-rt-t13-smoke`（独立从零目录）；记录回传＝`%TEMP%\ird-acc-rt-t13-b`（分支 acc/RT-T13/1）。验收日志在 worktree A 根与 %TEMP% 下，验收后随 worktree 删除——本记录为唯一证据载体。 |
| 环境前置说明（均为既有环境链问题、非本任务引入） | ①全新集成树配置需主仓同款参数：vcpkg toolchain＋Qt CMAKE_PREFIX_PATH（F-007 既有，本轮再次实证——无 toolchain 时 Boost NOTFOUND 且失败缓存残留会污染后续重配，须删 build/ 全新配置）；②`RobWork/RobWorkStudio/bin/RobWorkStudio.ini.template.static`／`ini.shared.in` 被 .gitignore `[Bb]in/` 规则吞掉不入 checkout，须从主仓手工复制（F-007 再次实证）；③从零集成树全量构建在 F-008 基线缺陷处失败（本轮实测 `: error ` 73 处＝RWSimulatorPlugin.vcxproj 71＋bt_plugin.rwplugin.vcxproj 2，industrialrobot 域 0 命中——与 RT-T12 attempt 2 判例逐字同型）；④运行集成测试 exe 需把 build/RobWork、RobWorkSim、RobWorkStudio 的 bin/Release 加入 PATH。 |

---

## 4.1 改动范围 —— 通过

- `git diff --name-status 61daa56d..a2bb6ff6` 共 **14 个文件**：`runtime/include/sdurws/ird/runtime/README.md`（M）＋`units/runtime.md`（M）＋traceability 12 件（A：builds/wp06-t13/ 下 11 件＋wp06-t13-gate-registrations.md）——全部落在契约 allowedFiles（`units/runtime.md`、runtime README.md、`traceability/**`）。
- forbiddenFiles（REQUIREMENTS.md／ARCHITECTURE.md）零触碰（14 文件清单反证）。
- 无未登记框架源码改动：diff 内无 `RobWork/RobWork/`、`RobWorkSim/`、`RobWorkStudio/src/`（industrialrobot 之外）任何文件；`patches/` 零新增（SA-02 不触发）。

## 4.2 可复现构建 —— 通过（集成按 F-008 闭合前"任务目标面从零"判例口径）

- **集成模式**（worktree A 全新配置）：`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=<vcpkg 绝对路径> -DCMAKE_PREFIX_PATH=<Qt6>` → 配置 exit 0；缓存 grep 实证 **`RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`**。全树 `cmake --build build --config Release -j` → exit 1，`: error ` 共 **73 处，全部**归属 RWSimulatorPlugin.vcxproj（71）＋bt_plugin.rwplugin.vcxproj（2）（RobWorkSim 域，F-008 基线缺陷；industrialrobot 域 0）——与 RT-T12 attempt 2 判例（73 处同分布）逐项一致。**任务目标面从零**：`cmake --build build --config Release --target sdurws_ird_runtime_test -j` → **exit 0、`: error ` 计数 0**。
- **集成测试**（验收者亲手运行，PATH 加三处 bin/Release）：**exit 0，261 例＝260 PASSED＋1 SKIPPED**（SKIPPED＝ContractSuite.ChildProcessWorker_RT_ID_1，按设计仅子进程执行的 worker 占位）。
- **冒烟模式**（独立从零目录 `%TEMP%\ird-acc-rt-t13-smoke`，vcpkg toolchain 绝对路径，`--config Release`）：配置 exit 0、构建 exit 0（唯一 warning＝MSB8029 临时目录增量提示，环境性质，与 RT-T12 记载一致）；测试 **exit 0，176/176 PASSED**。
- 实施侧留痕交叉核对：其 integration-build.log 产自主树存量增量树（F-054 标注纪律已落地——提交信息与 ird-test-report.json 显式标注树性质）；其 smoke-build.log 路径显示 Debug 配置（见 F-055——其报告未声明配置，数值主张真实但配置口径未声明；本验收者以 Release 从零复跑补强实证，176/176 绿，主张成立）。

## 4.3 契约 verify 命令 —— 通过

- 契约 verify 数组仅 1 条：`pwsh -File RobWork/scripts/industrialrobot/validate-docs.ps1`——验收者在 worktree A 亲手执行 → **PASS (20 units, 12 trace entries, 150 task files)，exit 0**（与实施侧 validate-docs.log 逐字一致）。
- 加跑（超出契约要求）：`validate-task.ps1 -TaskFile tasks/foundation/RT-T13.json` → **PASS (1 tasks)，exit 0**。
- `validate-state.ps1` 未执行——state.json 为编排者专属，本验收不涉流水线状态文件（派发说明第 2 条）。

## 4.4 acceptance 条目逐项 —— 全部通过

| 契约 acceptance | 验收者独立证据 | 结果 |
| --- | --- | --- |
| ① README 核对零偏差（§9→§12 指向核对） | README 第 4 行任务卡指向实测＝`units/runtime.md` **§12**（无第二指向）；磁盘 `ls include/sdurws/ird/runtime/*.hpp` 实测 **12 头**，与 README 模块清单逐头一致、无遗漏无多列（README-audit.md §3 复核成立）；落位登记段"§15.4 v0.4~v0.14＝RT-T03~T12"与 §15.4 变更记录行实测一致；builds/wp06-t01~t13 目录实测齐全；集成 261＝260＋1 skip／冒烟 176/176 与我方实跑逐数一致 | 通过 |
| ② §15.3 状态更新（P-RT-1~9 最新状态逐条） | runtime.md §15.3 九行逐条实测均含〔RT-T13 状态同步 2026-09-12〕标注；事实主张抽查全部属实：P-RT-1"core.md 现 v0.11 未冻结"（实测卡头 v0.11）、P-RT-2"ARCHITECTURE 现 v0.12"（实测卡头 v0.12）、P-RT-9"evidence.md P-EV-6 仍未消账"（实测 §15.3 P-EV-6 行原文在档）；九项裁决归属列均未变更、open 项未越权翻转（CORE-T10 文档任务纪律延续） | 通过 |
| ③ R-4 例外登记（名称解析器实现文件清单）提交 WP-01-T01 | 提交件 traceability/wp06-t13-gate-registrations.md §1 实测：清单＝NameMap.hpp＋NameMap.cpp（相对 IRD_ROOT 机器消费面建议回填块）；生效链 DTB §4.5 预登记行实测在档（第 657 行"预登记（清单随 WP-06-T13 提交转生效）"）；白名单 `IRD_R4_EXCEPTION_FILES=""` 占位＋注释实测在位；唯一性我方 grep 复核：产品面（include+src）"joinScopeLocal" 恰 2 文件命中（NameMap.hpp 注释声明侧＋NameMap.cpp 定义侧，L94）；例外范围＝文件级非单元级、不申请整单元豁免、O-12 独立性声明在案；引擎 4c 步 `if(NOT _unit STREQUAL "runtime")` 硬编码过渡态（ird_gates.cmake L455）与 §1.5 描述一致且明示"本提交不代行" | 通过 |
| ④ UT-BUILD 建议并入 CI（登记给 WP-01/WP-24） | 提交件 §2 实测：UT-BUILD＝test/BuildRedLineTest.cpp 四例（用例名与红线一一对应）；既有承载消账（gate-all.ps1 全量 _test 执行面＋ird_gates 构建期扫描）；**真实缺口我方实测证实**——ci/industrial-robot-windows.github-workflow.yml（L310/L413）与 .gitlab-ci.yml（L122/L132）run-tests 正则实测钉 `^sdurws_ird_core_test$`，runtime 等后续单元测试目标确未进 CI 执行面；登记请求分列 WP-01（执行面扩展）／WP-24（基线登记复核），接收方辖域文件（scripts/、cmake/）本 diff 零触碰 | 通过 |

## 4.5 测试真实失败能力 —— 通过（2 个负向探针，均即红→还原复绿）

| 探针 | 注入点（worktree A 临时修改，`git checkout --` 还原，还原后 `git status` 零跟踪改动） | 结果 |
| --- | --- | --- |
| 探针 A（文档门禁面——本任务 §12 行的守护检查） | units/runtime.md §12 RT-T13 行行首文本注入"（探针篡改）" | validate-docs **即红**：`Write-Error: phase index task row mismatch: RT-T13`；还原复绿 PASS（20 units, 12 trace entries, 150 task files）——本任务登记的"§12 行↔phase-one-task-index sourceRow 零偏差"由真实可红的门禁钉住 |
| 探针 B（R-4 例外锚点行为面——登记件 §1 所称"唯一实现点"） | NameMap.cpp L98 `joinScopeLocal` 返回值分隔符 `"."` → `"-"` | 重建后过滤器 `*NameMap*:ContractSuite.GoldenNameMap*` **即红**：exit 1、35 处 FAILED（NameMapBuildTest.BuildsAllScopesFromRichFixture_RT_NM_1 等）；还原复绿 **27/27 PASSED**——"joinScopeLocal 单点语义"断言非恒真，登记件例外清单的行为锚点真实有效 |
| 补充 | 探针 B 还原后全量集成套件复跑 | 260 PASSED＋1 SKIPPED（261），exit 0——终态零残留 |

## 4.6 架构红线（AGENTS §5 八条） —— 通过

1. **L2 零 Qt**：diff 全部新增行 Qt 头/QString/Q_OBJECT 扫描唯一命中＝登记件表格中用例名 `NoQtInclude_RT_BUILD_NFR_MNT_01` 的说明文字（非 include）；本任务零代码变更。
2. **业务域互链禁止（R-1）**：零代码变更；登记件不建立任何新依赖边。
3. **跨单元私有头禁止（R-2）**：diff 无任何源码 include 变化。
4. **框架零源码修改（SA-02）**：4.1 已证 patches/ 零新增、框架源码零触碰。
5. **不做前缀拼接/剥离（R-4）**：本任务恰为该红线的例外登记任务——登记范围＝文件级两文件、其余 runtime 产品面仍受扫描、不申请整单元豁免；红线语义在登记件 §1.1 中原文重述无误。
6. **权威唯一（PA-1）**：登记件明示"只提交清单与建议，接收方处置后才生效"，cmake/ 与 scripts/ 零触碰（diff 文件清单反证）；§15.3 九项裁决归属零变更、open 项不私裁。
7. **不可变历史（PA-2）**：runtime.md 修订为增量（v0.14→v0.15 追加式变更记录），无既有行改写（P-RT-1~9 行为追加标注非翻转）。
8. **不继承 old/ 实现**：零代码变更，不适用。

## 4.7 注释规范 —— 通过（文档任务：diff 无 .hpp/.cpp 代码文件）

- diff 内代码文件为零——"公共头全查、实现文件抽查 ≥3 个"的强制面空缺（以 4.1 十四文件清单为证）。
- 文档质量按 §2 精神核查：README.md 与 runtime.md 增量均为中文、解释"为什么"（如 P-RT 各条标注写明"仍 open＋实现侧事实消账"的区分理由）、含可追溯锚点（需求号/文档版本/路径）。
- 支撑抽查（登记件所引代码锚点）：NameMap.hpp 文件头 R-4 例外声明（L14）与 NameMap.cpp `joinScopeLocal` 的 Doxygen 中文注释（@brief/@param/@return、Scene 对象名示例）齐备——登记件 §1.6 锚点与代码实况一致。

## 4.8 文档同步＋findings 未关闭条目相关性 —— 通过

- **实现与任务卡零偏差**：§12 RT-T13 行四项完成条件与四条 acceptance 一一对应且有证据（见 4.4）；§14 追踪矩阵 NFR-MNT-07/R-4 行已同步指向提交件 §1；§15.4 v0.15 变更行逐条登记四项交付＋构建树口径；卡头 v0.15。无需 DTB §5.4 之外的偏差登记。
- **任务状态/追踪矩阵**：契约 status 翻转 done 归收尾段（PIPE §4.7⑥），非实施者责任；phase-one-task-index 的 RT-T13 行与单元卡行实测一致（探针 A 反证该检查真实有效）。
- **findings.json 未关闭条目逐条相关性核对**（open 类全部过目）：
  - **F-007／F-009（环境链：集成树参数未载明／gate-all 无法在 worktree 运行）**——本轮复现再次实证（toolchain＋Qt 前缀＋ini 模板手工复制）。与本任务相关但**不可消费**：修复面在 AGENTS.md §4.1／治理文档，不在 allowedFiles。维持 open。
  - **F-008（RWSimulatorPlugin 全新树构建失败）**——本轮实测同型同分布（73 处），判例口径下不阻断；修复面在 RobWorkSim 框架域（SA-02 patch 或上游），非本任务可消费。维持 open。
  - **F-019／F-051（ird_gates 红：testdata_lint 表外边＋runtime_test→testkit 边）**——本轮 ird_gates 实跑命中恰该 3 项，与登记件/报告"零新增"主张一致。登记件已如实转登记（§1.5 过渡态），修复面在 cmake/（WP-01 辖域），非本任务可消费。维持 open。
  - **F-032（validate-task planned 锚点弱覆盖）**——与本任务 verify（validate-docs）无交集；修复面在 scripts/。维持 open。
  - **F-043（重力投影方向测试敏感度）**——runtime 测试代码项；本任务零代码变更不可消费，留待后续 runtime 测试触面任务。维持 open。
  - **F-010（.gitignore 吞留痕日志）**——本任务已用 `git add -f` 正确交付 12 件留痕（规则面问题本身维持 open）。
  - **F-012 家族（F-023/024/026/027/030/034/037/039/040/042/044/045/047/048，提交标题名词短语）**——本任务 4 提交标题（登记／提交／登记／同步）全部动词开头，**零同型再现**。
  - **F-025/F-041（计数失实家族）**——本任务计数主张（261＝260＋1、176/176、清单 2 文件、CI 正则 2 模板 4 处）经我方逐一实测**全部吻合，零失实**。
  - **F-028/F-035（"ird_gates 未执行"失实家族）**——本任务实跑 ird_gates 并如实登记 exit=1＋命中 3 项既有登记，无同型失实。
  - **F-004/F-005/F-006/F-015~F-018/F-020/F-029/F-031/F-033/F-046**——他单元/横切项，与本任务无涉，维持 open（清单留档备 4.8 消账核对）。
  - **结论：无一条 open 发现可在本任务 allowedFiles 内顺手消账；本任务亦未新增同型违规。**

## 4.9 commit 质量 —— 通过

- 4 提交（8729b264→3c51a8e4→527ea1f9→a2bb6ff6）标题均为 `[WP-06-T13]` 前缀＋动词开头中文摘要（同步／登记／提交／登记）。
- 正文四段（改动内容／改动原因／验证／影响面与兼容性）逐提交齐备，均带 `Acc-Covered` 追溯尾注；验证段与留痕一致：a2bb6ff6 所称"命中恰 3 项既有登记 F-051/F-019、引擎自测 7 项全检出"经我方 ird_gates 实跑逐项复得；"MSB1009 相对路径首次失败已如实留痕"在 smoke-configure.log 在档（如实陈述非粉饰）；527ea1f9 所称 CI 正则缺口经模板实测证实。
- 无空话标题、无与留痕矛盾的表述；构建树性质口径显式区分（F-054 纪律已落地）。

## 4.10 留痕完整 —— 通过

- `traceability/builds/wp06-t13/` 实测 11 件齐备：integration-build.log／integration-test-run.log／sdurws_ird_runtime_test-integration.xml（`tests="261" failures="0" errors="0"`，1 skipped＝ContractSuite.ChildProcessWorker_RT_ID_1）／smoke-configure.log／smoke-build.log／smoke-test-run.log／sdurws_ird_runtime_test-smoke.xml（`tests="176" failures="0" errors="0"`）／validate-docs.log／validate-task.log／ird_gates.log／README-audit.md＋ird-test-report.json。
- ird-test-report.json（resultNote 载体）实测为有效 JSON（node 解析通过）：attempt=1、taskNature/reworkNote、summary.integration 261/260/0/1＋skippedNote、summary.smoke 176/176、exitCodes（irdGates=1 与命中语义一致）、acceptanceMapping 四条。
- phase-one-validation.log 未更新——该文件为 WP-03-T01 时代遗留聚合日志（git log 实测末次触碰 da407b61），现行口径以任务目录 ird-test-report.json 为聚合载体（RT-T12 等近期验收同判例），不构成缺口。
- 实施侧 XML／日志数值与我方独立复跑逐数一致；唯一口径缺口＝冒烟构建配置未声明（F-055，建议级）。

## 4.11 偷懒/缩水实现扫描 —— 通过

- ①diff 新增行 `TODO|FIXME|stub|placeholder|未实现|not implemented` 扫描**零命中**；无"触发式延后"豁免需列出。
- ②空实现检测：diff 零代码文件，无公共接口函数体可检——不适用。
- ③acceptance 逐条证据表：见 4.4——四条 acceptance 每条均有具名文件/行号/命令输出级证据，任一条不依赖"已实现"声明。
- ④测试强度：本任务零新增测试（如实登记为"既有用例全量复跑"——ird-test-report.json taskNature 明示，非缩水；测试交付归 RT-T01~T12 已验收范围）；其复跑面（261＋176）与登记数值经我方亲手实跑证实，且 4.5 双探针证明关键断言（文档门禁一致性、R-4 锚点行为）均非恒真。

---

## 问题清单

| 级别 | 编号 | 描述 | 处置 |
| --- | --- | --- | --- |
| 阻断 | 无 | — | — |
| 建议 | **F-055**（已转登 findings.json） | 实施侧冒烟留痕未声明构建配置：smoke-build.log 路径显示 `\testkit\Debug\`（全文 21 处 Debug、0 处 Release、无 --config 参数）＝MSBuild 默认 Debug 配置实跑；ird-test-report.json exitCodes 声明 smokeBuild=0 属实但未声明配置，与集成侧 Release 口径不对称。数值主张（176/176）真实（本验收者 Release 从零复跑 176/176 绿），无粉饰；仅留痕口径纪律问题（F-054 同族延伸：构建性质/配置显式标注） | 后续留痕提交在报告/提交信息中显式声明各模式构建配置；随下次触及留痕流程的任务顺手统一，不追溯 |
| 建议（既有，非新增） | F-007/F-009 再次实证 | 全新验收树需手工补 toolchain＋Qt 前缀＋ini 模板三件（本轮第 3 次实证）；gate-all 类脚本仍限主树运行 | 维持 open，归治理/AGENTS §4.1 辖域 |

## 验收者环境补充

- 配置失败两次的中间留痕（Boost 未提示、Qt 未提示）均为验收者自身环境复现过程，与送验对象无关；最终以主仓同款参数完成复现。
- ird_gates 经 worktree A 构建树 `--target ird_gates` 实跑成功（exit 1＝命中语义，非环境失败）；命中 3 项与我方解码输出及实施侧日志逐项一致。
- 验收后 worktree A 已还原零残留（`git status` 零跟踪改动实测）；两个 worktree 在本记录推送成功后删除。
