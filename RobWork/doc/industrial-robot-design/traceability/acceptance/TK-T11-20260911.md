# TK-T11 验收记录（对抗式验收）

| 字段 | 值 |
| --- | --- |
| 任务 ID | TK-T11（≙WP-02-T11，testkit 进程测试支撑实现：TestProcessRunner/EventWatch） |
| 契约 | tasks/foundation/TK-T11.json（status: ready，随收尾翻转 done） |
| 验收对象 | 分支 `wp02-t11`，head＝`10afd55552e428a6a0e3db85f5f9e0c3f4112a1c`（40 位冻结对象）；漂移检测：origin/wp02-t11 == wp02-t11 == 10afd55（零漂移）；范围＝redesign-main 分叉点 793fa60 起（2 提交：3dc8472 实现＋10afd55 留痕与编译放行登记） |
| 尝试序号 | attempt=1（evidence 分支 `acc/TK-T11/1`） |
| **verdict** | **pass**（4.1~4.11 全过；无阻断；建议级 2 项转登 findings F-024/F-025——提交标题风格与提交信息计数笔误，均不涉粉饰） |
| 验收日期 | 2026-09-11 |
| 独立性声明 | 全新上下文，只消费契约/单元卡/产物；未读实施叙述。补偿性对抗动作＝worktree A（detached @ 10afd55）亲手全新配置双模式构建＋全量测试（97/97×2）、两个定向缺陷探针（构建退出码显式核验防旧二进制假绿）、ird_gates/validate 双脚本亲手复跑并与送验留痕逐字比对 |

---

## 逐项证据（4.1~4.11）

| # | 检查 | 结论与证据 |
| --- | --- | --- |
| 4.1 | 改动范围 | `git diff --name-only 793fa60..10afd55`：19 文件＝6 个代码文件（CMakeLists.txt／ProcessRunner.hpp／src/ProcessRunner.cpp／test/ProcessRunnerTest.cpp／test/TestMain.cpp／tools/prochelper/main.cpp）全部位于 `testkit/**`（allowedFiles）＋13 个留痕文件（traceability/builds/wp02-t11/ 12 份＋contract-compile-log.md，DoD §4.2"留痕随实现提交"口径，TK-T10 同例）；forbiddenFiles（REQUIREMENTS.md/ARCHITECTURE.md）零触碰；patches/ 无新文件——SA-02 零框架源码改动 |
| 4.2 | 可复现构建 | worktree A（detached @ 10afd55，全新 build 树）：集成配置首次失败＝F-007 已登记环境缺口（RobWorkStudio/bin/RobWorkStudio.ini.template.static 为 git 未跟踪环境文件，worktree 天然缺失）→ 自主树复制该未跟踪文件（零被跟踪内容改动）→ 重配 exit 0 且缓存确认 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`（Qt CMAKE_PREFIX_PATH＋vcpkg toolchain）；`cmake --build … --target sdurws_ird_testkit_test` exit 0、error 行 0；测试 97/97 PASSED（验收者亲手运行）。冒烟：独立临时目录＋vcpkg toolchain 配置+构建 exit 0；测试 97/97 PASSED；运行时监听报告 summary total=97/passed=97/decisive=true（验收者亲手解析） |
| 4.3 | verify 命令 | 契约 verify＝`cmake --build build --config Release --target sdurws_ird_testkit_test`——在 worktree A 等价物（.acc-tk-t11-a/build）执行 exit 0（4.2 同一命令）；附带执行测试可执行文件全绿 |
| 4.4 | acceptance 逐条 | ①冻结签名零偏差：ProcessRunner.hpp 与 testkit.md §6.5 冻结块逐成员比对——ProcessSpec 四字段/ProcessExitKind 四态/ProcessOutcome 三字段/EventWatch 两方法/TestProcessRunner start/outcome/kill 签名逐字符一致；为承载实现追加的析构/禁拷贝/PImpl 私有指针已在文件头声明为实现细节级增量接缝（沿 TK-T08 v0.8 先例），冻结成员零变更；Windows API 限定经 grep 实证：库面（include/+src/）仅 src/ProcessRunner.cpp 含 windows.h（tools/prochelper 为 §3.6 安装排除面的测试设施可执行文件，非库面，头注释自证）；②四态＋EventWatch 用例：ProcessRunnerOutcome 4 用例（NormalExit/NonZeroExitCrashed/KillAndTree/TimeoutAndTree，其中树终止以"孙进程 marker 不出现"反向验证）＋EventWatchPath 7 用例（命中×2/超时×2/缺失文件/零时限/空 needle），两模式 97/97、XML failures=0（送验 XML tests=97 与验收者复现一致）；③T-1 复验：验收者亲手重跑 ird_gates 全量——命中 2 项＝IRD-GATE-SUB＋IRD-GATE-T1，双双指向 sdurws_ird_testdata_lint（F-019 既有，TK-T03 引入，TK-T10 验收已裁定非其变更面），与本任务送验 ird-gates.log 逐字一致；本任务变更面（prochelper 零链接边、ProcessRunner.cpp 无新增目标间边）零新增命中；TkBuild.ProductSourcesZeroTestkitInclude_T1 源码扫描用例两模式通过；零新外部依赖（std＋kernel32 默认链接，CMake 无新 target_link_libraries）。字面"全量运行零命中"目前唯一缺口＝F-019 既有登记项，延续 TK-T10 pass 先例不阻断本任务；④validate-docs（exit 0，PASS 20 units/12 trace/150 task files）与 validate-task（exit 0，PASS）均在 worktree A 亲手复跑；contract-compile-log.md 登记行 117＝TK-T11（branch wp02-t11、interUnit False、O-33 消账＋触发依据＋状态 ready）；DTB §4.2 O-33 行"已消账"（行 630）、§2 WP-02-T11 ≙TK-T11 映射恢复（行 179）、≙ 计数 73→74（行 744）均在冻结树核验 |
| 4.5 | 测试真实失败能力 | 探针一：outcome() 退出码判定反转（`code == 0`→`code != 0`）→ 重建 exit 0（显式核验）→ 8 用例如期变红（NormalExit/NonZeroExit/DoubleStart/EnvOverride/WorkingDir/EmptyWorkingDir/AwaitFileHits/AwaitLineHits）→ `git checkout` 精确还原（status 零残留）→ 重建 → 97/97 复绿。探针二：awaitFile 顶部注入恒真 `return true` → 4 用例如期变红（KillReportsKilledAndTerminatesWholeTree/TimeoutReports…/AwaitFileTimesOutAtDeadline/ZeroDeadlineChecks…）→ 还原 → 97/97 复绿。两探针证明四态判定与有界等待断言均真实联动实现行为，非恒真测试 |
| 4.6 | 架构红线 | ①L2/库面零 Qt＋零 gtest：grep 实证四个新文件无 Qt/gtest include（测试 TU 的 gtest 为 D-06 适配层惯例）；②R-1：testkit 链接面仍仅 core，新增文件零新链接边；③R-2：仅 include 本单元公共头（ProcessRunner/Fixture/TestPaths 均在 include/sdurws/ird/testkit/）；④SA-02：diff 无框架文件、patches/ 零新增；⑤R-4：无名称前缀拼接（不涉）；⑥PA-1/⑦PA-2/⑧old/ 恢复：不涉/不涉/全新实现。testkit 特有 T-1/T-2：见 4.4③，通过 |
| 4.7 | 注释规范 | 四个新增/大改文件全量审读：ProcessRunner.hpp 公共契约注释完整（冻结语义、行为契约、生命周期与线程安全、错误语义 Usage/EnvUnavailable 分类）；ProcessRunner.cpp 每函数 Doxygen 中文（@param 方向/单位 ms/@return/@throws），高危信息齐——线程约束（runner 单线程串行/EventWatch 可并发）、所有权（Impl 持有句柄、TempDir RAII、析构顺序敏感）、错误语义归类、确定性来源（环境块 map 排序→NFR-COR-02）、魔法数字来源（5ms 轮询间隔、5s 终止汇合上限、0xDEAD 哨兵码）、算法分步（start 六步序列"先挂起后绑定"竞态论证、四态判定路径）；prochelper/main.cpp 头注释＋逐函数说明（子命令表）；ProcessRunnerTest.cpp 每用例追溯 acceptance 条目＋§6.5 同步纪律声明。缺项未发现 |
| 4.8 | 文档同步 | 文档先行核验：单元卡/DTB/契约登记提交 dfb7558 是 base 793fa60 的祖先（`git merge-base --is-ancestor` 通过）——testkit.md v1.0（§9 TK-T11 行＋§10.1 启用表＋"阶段 A 不含"清单移出）、DTB §2.2 映射恢复、O-33 消账、契约生成全部先于实现落地，实现与任务卡零偏差（头文件增量接缝已在卡与头注释双侧声明）；findings.json 未关闭 17 项逐条核对——相关项：F-019（ird_gates 既有 2 命中，本任务零新增，维持 open 不由本任务消账）、F-007（全新树环境缺口，本次复现再次印证并按其口径处置），其余 15 项与本任务变更面无涉、无顺手可消账项；新增建议级 2 项（F-024/F-025）随本记录同批转登 |
| 4.9 | commit 质量 | 两提交均四段式（What/Why/How/影响面）＋Acc-Covered 标注；提交 3dc8472 标题"实现…"动词开头✓；提交 10afd55 标题"验证留痕与编译放行登记…"名词短语开头——F-012/F-023 同型再现，转登 F-024（建议级）；提交信息计数笔误（"16 个新用例/双路径 6"实为 17/7；"ird-test-report 三份"实为入库两份）——方向为少报、留痕文件本身准确（97/97 由 XML 与报告实数），转登 F-025（建议级）；验证结论逐条复现均为真，未发现粉饰 |
| 4.10 | 留痕完整 | 送验留痕 13 份齐：构建/测试/配置日志×8（integration-build/integration-test-run/smoke-configure/smoke-build/smoke-test-run/ird-gates/validate-docs/validate-task）＋双模式 gtest XML×2（均 tests=97 failures=0 errors=0）＋ird-test-report.json/-smoke.json（schema ird-test-report/1，summary 97/97/decisive=true——验收者 node 解析实证）＋contract-compile-log.md 登记行；ird-gates.log 与验收者复跑输出逐字一致；phase-one-validation.log 未更新——沿 TK-T10 先例（该日志止于 da407b6，foundation 任务以 builds/&lt;task&gt;/ 证据包替代），resultNote 归编排者专用未触碰 |
| 4.11 | 偷懒/缩水实现扫描 | ①TODO/FIXME/stub/placeholder/未实现/not implemented 扫描 diff 范围零命中（grep 退出码 1）；②无空实现——start 六步真实进程序列（CREATE_SUSPENDED→Job 绑定→Resume）、完整 Windows 引号转义规则、双 NUL 环境块合并、有界轮询等待均为实质代码；③acceptance 逐条证据表见 4.4（每条均有具名测试/命令输出/文件行号）；④测试强度：边界断言实载（duration GE/LT 双界、等待时长 GE deadline 且 LT 有界上界、零时限 LT 50ms 立即返回、树终止 marker 反向证明、Usage/EnvUnavailable 错误分类断言）＋4.5 双探针实证会红 |

## 问题清单

- 阻断级：无。
- 建议级（已随本记录转登 traceability/findings.json）：
  - **F-024**：提交 10afd55 标题名词短语开头，F-012/F-023 同型再现（纯风格，四段与验证结论齐备无粉饰）；
  - **F-025**：提交 10afd55 正文计数/份数笔误（新用例 16→实为 17；ird-test-report 三份→入库两份），方向为少报、留痕文件实数准确，属提交信息行文与留痕清点的一致性纪律项。

## 验收者环境说明

- 复现现场：`git worktree add --detach .acc-tk-t11-a 10afd55`（detached @ 冻结 SHA，非分支名）；集成树全新配置于 `.acc-tk-t11-a/build`；冒烟树独立于 `.acc-tk-t11-smoke`。
- 全部命令（配置/构建/测试/门禁/两脚本/两探针）由验收者本会话亲手执行，退出码逐次显式核验（探针注入与还原均显式核验重建退出码，防 TK-T07/T10 式旧二进制假绿）。
- 环境缺口处置（F-007 既有）：worktree 缺 git 未跟踪的 RobWorkStudio.ini.template.static——自主树复制该未跟踪文件后重配成功；未改动任何被跟踪产物（`git status` 零残留复核）。
- 送验对象冻结复核：验收全程 origin/wp02-t11 == 10afd55；验收者未向任务分支推送任何提交；pipeline/state.json 与 lock 未读写。
