# UI-T16 验收记录（attempt 2，2026-09-24）

| 字段 | 值 |
| --- | --- |
| 任务 | UI-T16 工作台宿主插件（sdurws_ird_ui_plugin——RobWorkStudio 集成入口＋开发期动态加载验证通道） |
| 送验对象（冻结） | `02d529370978816e8cf45be741d5920463bdbfec`（分支 wp10-t16；验收全程按 SHA，不按分支名） |
| diff 范围 | `e4d70ada744eb10df9bd203dcc59e3ae517b5f34..02d529370978816e8cf45be741d5920463bdbfec`（5 提交：562a18b4 → 79b3f8da → 6441c136 → d8af3a96 → 02d52937） |
| verdict | **pass**（无阻断级问题；建议级问题 3 条，已逐条转登 findings.json：F-321/F-322/F-323） |
| 前次验收 | attempt 1 verdict=fail（traceability/acceptance/UI-T16-20260924.md @ acc/UI-T16/1；阻断 B-1 宿主形态缺陷／B-2 失实复跑载体；F-318/F-319/F-320 已转登）——本记录同时核对其闭环状态（见 §4.8） |
| 验收者环境 | 全新上下文；独立 detached worktree `D:/10_Source_Repos/21_robot/_acc_t16_a` @ 02d52937（配置命令与全程日志在案）；记录分支 acc/UI-T16/2（基于 origin/redesign-main = b87b72f3）。MSVC 2022 x64＋Qt 6.11.1＋vcpkg classic（D:/10_Source_Repos/21_robot/RobWork/vcpkg） |

## 0. 分支漂移检查

- `git rev-parse origin/wp10-t16` = `02d529370978816e8cf45be741d5920463bdbfec`＝冻结 SHA。**零漂移**（ACC §3）。

## 1. 验收环境说明（含两处环境前置，如实登记）

1. **框架 bin 模板文件缺失**：仓库 .gitignore `[Bb]in/` 吞没 `RobWork/RobWorkStudio/bin/`，`RobWorkStudio.ini.template.static`（静态插件模式 configure 依赖，`RWS_USE_STATIC_LINK_PLUGINS` 框架默认 ON）不入库。全新 worktree 直接配置报错 configure_file 失败。处置：自主树复制该 gitignored 模板（环境前置，等价于开发者检出形态；与送验对象内容无关）。
2. **RobWorkSim 两框架目标编译失败（框架级，与本任务 diff 无关）**：全新配置下 `RWSimulatorPlugin`／`bt_plugin.rwplugin` 因 windows.h min/max 宏冲突（C4003→C2059/C2589，共 73 error）无法编译——`Math.hpp`/Eigen 处 `min`/`max` 被宏展开。主构建树的同名 vcxproj 为 2026-09-08 旧世代生成物（含 NOMINMAX），掩盖了该问题；基线 e4d70ada 检出同等失败（本任务 diff 触及的 88 个文件不含 RobWorkSim 任何文件——见 §4.1）。处置：验收构建以 `CL="-DNOMINMAX -DWIN32_LEAN_AND_MEAN"` 注入完成全量构建（exit 0），问题本体转登 **F-323**（建议级，治理/构建约定辖域）。
3. **宿主 GUI 冒烟的验收者独立复跑受限**：验收会话运行于非交互桌面（CopyFromScreen 报"句柄无效"、QClipboard 用例必红），框架亦无 CLI/ini 装载通道（`RobWorkStudio.ini.template.static` 的 UserPlugin 键为旧版遗留，现行 src 无读取代码；main.cpp 无 --rwsplugin）——verify 第 4 条的**亲手重跑在本验收环境不可达**。该条按 ACC §2.4 如实降级为"**留痕逐帧亲验＋磁盘物证核验＋门控签名比对**"（§4.3），不采信实施者运行结论本身；期间发现一处证据与表述矛盾（§4.9／F-321）。

## 4.1 改动范围 —— **通过**

- `git diff --name-only e4d70ada..02d52937`：88 个文件，逐一比对契约 allowedFiles：`industrialrobot/ui/**`（14 个源码/构建文件）＋`units/ui.md`＋`development-task-breakdown.md`＋`traceability/**`（72 个留痕/登记文件）——**0 个文件在 allowedFiles 之外**。
- forbiddenFiles（REQUIREMENTS.md／ARCHITECTURE.md）：零触碰（不在 diff 列表）。
- SA-02：`industrialrobot/patches/` 零新文件（`git diff -- …/patches/ | wc -l` = 0）；框架源码零改动（diff 内无 industrialrobot/ 之外源码路径）。

## 4.2 可复现构建 —— **通过**（含 §1 环境前置登记）

- **集成模式**：worktree A 全新配置 `cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=…/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64` → 缓存确认 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`；全量 `cmake --build build --config Release` **exit 0、error C/LNK/MSB 计 0**（日志 `_acc_t16_a/build-acc2.log`；首次全量运行暴露 §1-2 的 RobWorkSim 框架问题，注入 NOMINMAX 后通过）。产物在位：`build/RobWorkStudio/libs/Release/sdurws_ird_ui_plugin.dll`＋`build/RobWorkStudio/bin/Release/RobWorkStudio.exe`。
- **独立冒烟模式**：独立目录 `_acc_t16_smoke`，`cmake -S …industrialrobot -B … -DCMAKE_TOOLCHAIN_FILE=…vcpkg.cmake -DCMAKE_PREFIX_PATH=…Qt` → 配置 STATUS 行再现 "**industrialrobot.ui: standalone smoke mode——sdurws_ird_ui_plugin not registered**"（与送验 smoke-configure-rework.log 一致）；`cmake --build … --config Release` **exit 0、零错误**（`_acc_t16_a/smoke-build-acc.log`）。
- 送验留痕交叉核对：integrated-build-rework.log／smoke-build-rework.log 均 0 error 行、与主树产物路径一致，与本验收者复现结论不矛盾。

## 4.3 契约 verify 命令 —— **通过（含两项如实注记）**

| # | 命令 | 结果 |
| --- | --- | --- |
| 1 | `cmake --build build --config Release --target sdurws_ird_ui_plugin` | **成功**（exit 0，DLL 重链出） |
| 2 | `ctest --test-dir …ui -C Release -L "^ird$"` | **通过 2/2**（ui_test 1.75s＋contract_test 0.32s，100%） |
| 3 | `ctest … -L "^ird_gui$"` | **验收环境 33/34**：唯一失败＝`ParamTablePanelGuiTest.BatchPasteImpactDetail_UI_T08_ACC1`——**既有 UI-T08 剪贴板环境例**（非本任务新增），失败签名（粘贴 0 行/qt.qpa.mime 族）与送验前例 gui-clipboard-env-failure-detail.log 一致；本会话非交互桌面为必然失败条件。送验 ctest-ui-gui-rework.log 为带捕获时间戳头（2026-09-24 07:11:02）与逐用例耗时的全新捕获，1/1 通过——运行签名核验成立。UI-T16 新增 6 用例（UI-SPLIT 族）在本环境全过 |
| 4 | 宿主 RobWorkStudio.exe Plugins→Load plugin 冒烟 | **验收环境不可达**（§1-3 非交互桌面）。降级为留痕亲验：①控制台 host-smoke-studio-console.log 的 `[ird-ui-plugin]` 门控行与最终代码行为逐条对应（诊断栈就绪→装配完成→**Dock 已呈现**→两次宽度收束"目标 768 px，实际 1054 px（受内容最小宽度钳制…如实留痕）"→项目已打开→项目已创建）——其中 768/1054 与钳制措辞只能由最终 head 代码（qBound(420, 2/5·宽, 1024)＋钳制事实落盘）产出，**证明送验二进制真实运行到最终形态**；②磁盘物证：`tmp-wp10-t16-harness/proj-demo/lock` 心跳 2026-09-23T23:05:27Z（终录进程 pid 92980 存活期内刷新）＝G2 客观门控实证；`%APPDATA%/sdurws/ird-workbench.ini` 三旗标＋recentCommands=view.resetLayout,…＝G4 实证；③截图 01/02b/04/05/06b/10/12 逐帧亲验（任务栏时钟 7:03→7:05 与终录窗口吻合、标题栏 v26.9.24-wp10-t16）：五区工作台嵌入宿主主窗口、框架菜单/工具栏共存（02b 菜单含框架 9 项＋插件注入 8 项且三区开关勾选态与显示一致）、中央 RWStudioView3D 同帧共存、命令面板浮层、PM-11 状态投影（未打开项目→IRD演示项目切换在案）。**发现一处表述与证据矛盾**（新建项目"状态投影切换"，见 §4.9／F-321） |

## 4.4 acceptance 条目逐项证据 —— **通过**

| 契约 acceptance | 证据（验收者独立取得／亲验） |
| --- | --- |
| 1 插件目标落位＋壳拆两层＋harness 回归零变化＋禁止顶层 QMainWindow 嵌套 | ①ui/CMakeLists.txt diff：`sdurws_ird_ui_plugin` MODULE、`if(TARGET sdurws)` 集成树专属、AUTOMOC 仅该目标显式 ON——冒烟模式 STATUS"not registered"由验收者亲证；②IWorkbenchShell 拆分：WorkbenchContent.hpp 公共契约面（WorkbenchHostKind/两段装配 build/activate）＋WorkbenchShell 收敛 chrome（-1173 行语义主体搬移至 WorkbenchContent.cpp，文件头注逐条登记承袭口径）；③harness 回归：sdurws_ird_ui_gui_test 既有套件（WorkbenchShellGuiTest 7 用例等）验收者亲跑全过＋^ird$ 154/22 全过；harness 首页/--new/--open/PM-07 双开降级留痕（harness-*.png/.log，其 --new 流"项目已创建→项目已打开"双行俱全）；④无 QMainWindow 嵌套：UiPlugin.cpp buildDockBody 以插件本体（QDockWidget）为体栅格容器、setFloating(false)，代码零 QMainWindow 构造；ui.md §10.1 v1.10 增量注随落位提交（562a18b4）；⑤双模式构建零错误（§4.2） |
| 2 门禁命中集归一化比对 | 验收者亲跑 `cmake -DIRD_ROOT=<worktree> -P ird_gates.cmake`：**56 条命中，与送验 ird_gates-wp10-t16-rework.txt 归一化后逐行 IDENTICAL**（仅绝对路径前缀差异）；新增恰 4 条（SUB×2：ui→project 宿主集成边＋ui→ui 自边；R3×2：WorkbenchContent.cpp/_p.hpp——既有 ui 例外域文件级延伸），登记提交件 traceability/wp10-t16-gate-registrations.md＋DTB §4.5 登记行（diff 在案）；其余各码（R-1/R-2/R-4/R-5/T-1/T-2/GRAPH/LIB/SA02）零变化；ctest ^ird$ 2/2、^ird_gui$ 见 §4.3（ird-test-report-ui/contract/gui-rework.json：154/154、22/22、34/34，failures=0，testsuite 聚合+分套件双计核验） |
| 3 宿主交互验证留痕 | 见 §4.3 第 4 行逐帧亲验：五区嵌入＋框架菜单/工具栏共存＋RWStudioView3D 共存＋命令面板＋打开协议（lock 心跳物证）＋PM-11 投影＋布局记忆跨会话（ini 三旗标）＋恢复默认布局（recentCommands＋visibleLeft 复位）；动态加载边界（仅开发期通道，SA-01 静态白名单不变、正式装配归 WP-24-T03）在 ui.md §13 落位登记注⑤与返工登记注明文。**例外**：新建项目"创建后经打开协议进入/状态投影切换"子表述与在库证据矛盾——建议级 **F-321**（详见 §4.9），不动摇本条核心判据（新建协议入口可用＝创建协议产物落盘 6 项〔host-smoke-newproj/ HEAD/lock/objects/project.json/revisions/.staging 磁盘亲验，project.json+HEAD 内容核验〕＋显示名正确入档＋插件菜单/面板入口在案） |

## 4.5 测试真实失败能力 —— **通过**

两处实现变异（均在验收 worktree 内完成，事后 `git status` 零残留＋重建复绿）：

1. **变异 A**：WorkbenchContent.cpp build() 宿主控件校验 `if (m_deps.hostWidget == nullptr)` 置 `if (false && …)` → 重建后 `WorkbenchContentGuiTest.BuildValidationRejectsIncompleteDeps_UI_SPLIT` **转红**："宿主控件为空未被拒"（EXPECT_FALSE 失败，exit 1）。
2. **变异 B**：ShellSupport.cpp storeFlags() 尾部追加 `settings.setValue(kGroup/kKeyGeometry, "MUTATION-overwrite")` → 重建后 `WorkbenchContentGuiTest.EmbeddedMemoryFlagsOnlyBoundary_UI_SPLIT` **转红**（旗标半区边界断言捕获几何键被覆写，exit 1）。
3. 自然红例：剪贴板环境用例（§4.3）证明测试非恒真。
4. 还原后 `--gtest_filter=WorkbenchContentGuiTest.*`：**6/6 PASSED**。

## 4.6 架构红线（AGENTS §5 八条） —— **通过**

1. **L2 计算内核零 Qt**：ird_gates R-3 命中集亲跑核验＝既有 ui 例外域＋本任务登记的 2 文件，其余单元产品面零 Qt 命中；插件目标被引擎分类为 plugin 形态（Qt 链接合法）。
2. **业务域互链禁止（R-1）**：模型/需求/运动学/轨迹/动力学/选配/优化单元零参与；新增边仅 ui→project（登记）＋ui→ui。
3. **跨单元私有头禁止（R-2）**：新增/大改文件跨单元 include 全审计（§4.7 同步）：仅 `sdurws/ird/{project,diagnostics,ui}` **公共头**（include/sdurws/ird/…）；plugin/ 与 src/_p.hpp 均为本单元私有件，不外溢。
4. **框架零源码修改（SA-02）**：patches/ 零新文件（§4.1）；宿主 Dock 呈现缺陷的修复走插件侧事件循环重申（reassertEmbeddedPresentation），未改框架——根因链注释明确"两步都在框架侧，插件侧唯一落点"。
5. **不做名称前缀拼接/剥离（R-4）**：R-4 命中仅既有登记例外（project/src/ProjectStoreImpl.cpp），零新增。
6. **权威唯一（PA-1）**：命令判定全在注册表（插件 refreshHostMenuActions 只消费快照"本插件零判定"）；名称解析占位 NullUiNameResolver 不代行 runtime 语义；诊断文案走 diagnostics 码表。
7. **不可变历史（PA-2）**：修订面＝ui.md 增量注/版本行追加，无既有语义改写；失实 -r2 载体以"工作树删除＋留痕登记"处置，未改写历史提交。
8. **不继承 old/**：diff 内无 old/ 路径。

## 4.7 注释规范 —— **通过**

- **公共头全查**（3/3）：`WorkbenchContent.hpp`（文件头设计依据/背景/线程模型＋每方法 @brief/@param/@return/错误语义）、`UiPlugin.hpp`（生命周期/线程模型/所有权逐成员标注）、`DiagnosticsAssembly.hpp`（出参稳定性契约/装配序/异常语义）。
- **实现文件抽查 ≥3**（5 个）：UiPlugin.cpp（每段"为什么"＋px 单位＋根因链注释＋时序关键步标注）、WorkbenchContent.cpp（§10.1 逐条承袭注释＋校验分组理由）、DiagnosticsAssembly.cpp（装配四步序号注释）、WorkbenchShell.cpp（拆分口径登记）、ShellSupport.cpp（canConvert 类型钉住纪律注释）。§2.5 高危信息（单位/线程/所有权/错误语义）覆盖到位；测试文件用例名带需求/AT 追溯（IRD_TEST_INFO＋UI_SPLIT 后缀）。

## 4.8 文档同步＋findings 消账 —— **通过**

- **单元卡**：ui.md v1.10（落位登记）→ v1.11（返工登记＋落位登记注⑤勘误）随提交落位，§16.7 变更行两行在案；DTB §4.5 新增 SUB 登记行（2026-09-23）＋§2.11/§5.1 行（O-38 裁决承接）；需求语义零变化（REQUIREMENTS 未触碰，符合 forbiddenFiles）。
- **attempt 1 三条发现闭环核对**（登记于 acc/UI-T16/1，本分支 findings.json 不含——union 合并时由收尾统一翻转状态；此处核对其**证据闭环**）：
  - **F-318**（ui.md 表述强于实证）→ **已闭环**：v1.11 落位登记注⑤内嵌勘误明文（"本注初版所列…表述强于该批留痕实证…经返工修复与证据全量重录后方成立"），diff 行在案。
  - **F-319**（失实复跑载体，阻断）→ **已闭环**：ctest-ui-ird-rerun-r2.log／ctest-ui-gui-final-r2.log 已从工作树删除（head 树 0 个 -r2 文件，历史提交未改写）；替代载体 ctest-ui-{ird,gui}-rework.log 带捕获时间戳头＋ctest 原生运行签名（UpdateCTestConfiguration/Test project 行/逐用例耗时），验收者逐字节核验非拷贝。
  - **F-320**（装载环境前置未登记）→ **已闭环**：ui.md §13 返工登记注④明文 PATH 前置清单（框架 exe 目录＋vcpkg bin＋Qt bin＋Python 目录，objdump 实扫依据）＋装载门控客观判据（控制台 [ird-ui-plugin] 行＋Dev 日志目录）——验收者按该清单组建环境后框架可启动（§1-3 直启实测）。
- **新发现转登**（本记录分支 findings.json 顺延）：**F-321/F-322/F-323**，见 §5 问题清单。
- 任务状态/追踪矩阵：流水线状态归编排者收尾管理（state.acceptanceRecord），本记录不越权改动；需求追踪矩阵无需变更（需求语义零变化）。

## 4.9 commit 质量 —— **通过（一处建议级）**

- 5 提交逐一审读：标题均为 `[WP-10-T16]`＋动词开头；正文四段（改动内容/改动原因/验证/影响面）齐备，验证段与在库留痕逐条对照。
- **一处表述与证据矛盾（建议级 F-321，非粉饰——原始证据未被删改，就在同一留痕内）**：head 提交 02d52937 验证段及操作序列日志终版要点称"09＝新建『宿主冒烟项目』状态投影切换"；亲验证据链为：
  1. host-smoke-09-newproj-pm11-status.png 状态行仍为"IRD演示项目"（裁切放大核验，非目测误读）；
  2. host-smoke-studio-console.log 在"项目已创建（…显示名：宿主冒烟项目）"之后**无**第二条"项目已打开"行（亦无"打开失败"行）；
  3. 磁盘物证：host-smoke-newproj/lock 心跳**停于创建时刻** 2026-09-23T23:05:23.486Z 未再刷新，而同进程 proj-demo/lock 心跳 23:05:27.531Z 仍在跳动——会话从未持有新项目；
  4. G3 门控第二判据"控制台'项目已打开'在案=True"被此前 proj-demo 打开行**误满足**（门控定义缺陷）。
  - 定性：创建后经打开协议进入在该轮**未发生**；机制未决——harness 同序列（创建→打开）双行俱全（harness-new-open.log），指向插件场景特有路径（**先开 proj-demo 再新建＝带活动会话的切换/二次打开路径**，harness 与单元测试均未覆盖该形态）。是产品缺陷还是运行时序偶发，验收者无法在本环境定案（§1-3）——**建议登记 DTB §4 待所有者裁决产品语义**；不影响本条核心（创建协议本身成功且入口可用）。

## 4.10 留痕完整 —— **通过**

- gtest XML 三件（ui/contract/gui -rework.xml，154/22/34、failures=0、windows 平台、时间戳 2026-09-24T05:06 新鲜签名）＋ird-test-report 三件 -rework.json＋集成/冒烟构建日志＋ctest 全新捕获两件＋validate-task-rework.log（PASS 1 tasks）——均在库且核验。
- phase-one-validation.log／unit-status：由收尾环节按流程更新（编排者辖权），本验收不越权。
- 次要瑕疵（并入 F-322）：宿主 Dev 日志固化件为 [sampled-tail] 采样尾段（两实例条目拼接、行号重启），非全量原件且未标注采样口径。

## 4.11 偷懒/缩水实现扫描 —— **通过**

1. **TODO/FIXME/stub/未实现/not implemented 扫描**：diff 内零真实命中（"View3DPlaceholder"/"占位面板"均为 UI-T05 契约显式登记的既有占位组件引用——三维完整交互归 WP-10-T05 阶段 B，契约明文）。
2. **空实现检测**：公共接口无空函数体/纯默认返回；`open()/close()` 为契约定义的共存观测回调（O-38 裁决③让位形态， getView()/getWorkCellScene() 只读观测＋Dev 留痕——非缩水）。
3. **逐条证据表**：见 §4.4（acceptance 1/2/3 全覆盖，无一条无证据）。
4. **测试强度**：6 个新 UI-SPLIT 用例断言行为与边界（二次 build 拒绝/校验拒绝/旗标半区逐字节边界/损坏回退 Dev 诊断出线＋整段丢弃/覆写路由/让位页零交互），§4.5 变异实证非恒真。

## 5. 问题清单与转登

| 编号 | 级别 | 摘要 | 处置建议 |
| --- | --- | --- | --- |
| F-321 | 建议 | 宿主冒烟终录"新建后状态投影切换"与证据矛盾；G3 门控第二判据被既有行误满足；带活动会话的新建/切换路径行为未定性 | 实施侧下批 ui 触碰窗口重录该帧或如实降级表述；产品语义（切换路径）登记 DTB §4 待所有者裁决 |
| F-322 | 建议 | Dev 日志固化件为 [sampled-tail] 采样尾段非全量原件，未标注采样口径 | 留痕管线固化全量原件或显式标注采样窗口 |
| F-323 | 建议 | 全新工作树集成配置在 RobWorkSim 两框架目标 min/max 宏冲突编译失败（主树旧 vcxproj 掩盖；基线同等复现，与本任务 diff 无关） | DTB §5.1 构建约定登记 NOMINMAX 注入或修复框架 CMake（F-007/F-009/F-254 家族同辖域） |

## 6. 裁决

- **verdict = pass**：4.1~4.11 全部通过（含两项如实注记：verify 第 4 条验收环境不可达降级为证据级亲验；^ird_gui$ 验收环境 33/34 为既有环境例），无阻断级问题；F-321 的产品语义疑问属"语义拿不准"，按 ACC §6 建议登记 DTB §4 由所有者裁决（不阻塞本任务合入；若所有者裁定切换路径为缺陷，宜另立任务卡承接）。
- attempt 1 阻断项 B-1/B-2 与 F-318/F-319/F-320 均已闭环（§4.8）。
- 验收者未合入、未修改实施产物、未向任务分支推送任何提交。

*验收者：独立验收会话（attempt 2）；记录分支 acc/UI-T16/2 @ origin/redesign-main（b87b72f3）。*
