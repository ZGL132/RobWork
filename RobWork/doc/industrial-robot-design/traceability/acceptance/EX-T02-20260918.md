# EX-T02 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | EX-T02（任务身份与状态机 TaskTypes/StateMachine/Errors；≙WP-08-T03，分支 wp08-t02） |
| 契约 | tasks/foundation/EX-T02.json（status=ready；DOC-T12 编译放行版；requirements=TASK-01/NFR-REL-03） |
| 送验对象（冻结） | **2777bc0a7aba0b4e31c0d7e22c83b7ee605476fe**（40 位，全程按 SHA 验收） |
| diff 范围 | d11f20a25152cbec7937f736d3d3a7b6075aa143..2777bc0a7aba0b4e31c0d7e22c83b7ee605476fe（3 个提交：c1ee4c6c 实现＋9d12b610 留痕＋2777bc0a 单元卡同步；merge-base 实测＝base 自身） |
| 验收尝试 | attempt 1（state.attempts.accept=1 与编排者下发一致——pipeline/state.json 实读） |
| verdict | **pass**（4.1~4.11 全部通过，零阻断级；建议级 1 项——2777bc0a 提交标题名词开头，转登 F-208） |
| 验收日期 | 2026-09-18 |
| 记录回传 | 分支 **acc/EX-T02/1**（基于 origin/redesign-main＝d11f20a25152cbec7937f736d3d3a7b6075aa143），路径 traceability/acceptance/EX-T02-20260918.md，记录提交 SHA 由验收者向编排者回传三元组 |

## 分支漂移检查 —— 通过

- `git ls-remote origin`：`refs/heads/wp08-t02` = 2777bc0a7aba0b4e31c0d7e22c83b7ee605476fe ＝ 送验 SHA，**零漂移**（ACC §3）；`refs/heads/acc/EX-T02/1` 不存在（记录分支名无冲突）。

## 独立性与环境（ACC §2/§3）

- **全新上下文**：本会话只读代码、契约、单元卡、在库留痕与既有验收记录（后者仅用于协议条目判定先例引用——EX-T01-20260918-r2 的 4.10 resultNote 口径、F-200 环境前置复现方法），未消费实施者过程叙述；一切结论以下述亲手执行为准。
- **复现现场**：`git worktree add --detach D:/acc-tmp/ex-t02-repro 2777bc0a…`（detached @ SHA，非分支名）。集成树 `ex-t02-repro/build` 从零新建 configure：`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON`＋`-DCMAKE_TOOLCHAIN_FILE=<主仓>/vcpkg/scripts/buildsystems/vcpkg.cmake`＋`-DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`（worktree 不含未入库的 vcpkg/ 与 Qt——按主仓 build 缓存实测值镜像，F-007/F-098/F-062 环境链）；缓存 grep 实测 **`RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`**。首配曾失败于 `RobWorkStudio/src/CMakeLists.txt:58 configure_file`（`RobWorkStudio.ini.template.static` 不存在）——即 **F-200 已登记的环境前置**（bin/ 被 .gitignore 吞、文件不入库），自主仓补拷该**未跟踪框架辅助件**后 configure exit 0；该文件属框架基础设施、在 industrialrobot 验收面之外，复制动作不触碰送验源（git status 复证零改动，探针还原后全树 `git status --short` 0 行）。
- **冒烟现场**：`cmake -S industrialrobot -B D:/acc-tmp/ex-t02-smoke -DCMAKE_TOOLCHAIN_FILE=<主仓>/vcpkg/…/vcpkg.cmake`（独立缓存目录，带 toolchain——CORE-T01 G-2 教训），configure exit 0。
- **记录回传现场**：`git worktree add -b acc/EX-T02/1 D:/acc-tmp/ex-t02-record origin/redesign-main`。
- **复现优先于采信**：双模式配置/构建、四跑测试（两模式×两目标）、契约 verify 命令、ird_gates 复跑、双探针失效注入与还原、validate-task/validate-docs 复跑均为验收者亲手执行；在库留痕数值仅作交叉比对。

## 4.1 改动范围 —— 通过

`git diff --name-status d11f20a2..2777bc0a` 实证 **25 个变更文件**（3 M＋22 A），逐个落在契约 allowedFiles 内：

| 文件 | allowedFiles 依据 |
| --- | --- |
| execution/CMakeLists.txt（M）、execution/test/BuildRedLineTest.cpp（M）、execution/contract_test/IdentityVocabularyContractTest.cpp（A）、execution/include/sdurws/ird/execution/{Errors,StateMachine,TaskTypes}.hpp（A）、execution/src/{Errors,StateMachine,TaskTypes}.cpp（A）、execution/test/TaskStateMachineTest.cpp（A）——共 10 件 | `RobWork/RobWorkStudio/src/rwslibs/industrialrobot/execution/**` |
| units/execution.md（M） | 单元卡显式列入 |
| traceability/builds/wp08-t02/ 下 14 件（A） | `RobWork/doc/industrial-robot-design/traceability/**` |

- forbiddenFiles（REQUIREMENTS.md/ARCHITECTURE.md）零触碰——diff 文件清单零命中；
- 框架源码零改动：diff 中 industrialrobot/execution 与 doc/industrial-robot-design 之外**零文件**；patches/ 零新文件（SA-02 无登记义务）。

## 4.2 可复现构建 —— 通过（全新树任务目标面从零复现；全树口径受 F-008 基线缺陷阻挡，与本任务无关）

- **集成模式**（ex-t02-repro/build 全新配置）：configure exit 0、零 CMake Error，缓存 `:BOOL=ON` 实测。全树 `cmake --build build --config Release` 在框架 **RWSimulatorPlugin 71 处＋bt_plugin 2 处 error 处失败**——错误全部位于 RobWork/RobWorkSim 框架代码（Eigen `Parallelizer.h` C2589/C2059 与 `VelocityScrew6D.hpp` C2327 等 min/max 宏污染簇，含 `rw::rw::math` 双命名空间特征），与 F-008 登记逐字同族（其登记"71 处 error C"数字亦吻合）；**industrialrobot 面零错误**（73 处 error 中 0 个涉入）。该失败即 F-008 基线缺陷的又一次全新树复现，非本任务引入——按 RT-T02/T03、EV-T02 验收先例，任务相关目标面从零复现：
  `cmake --build build --config Release --target sdurws_ird_execution_test sdurws_ird_execution_contract_test` **exit 0**、error C/LNK/fatal 计数 **0**；两测试 exe 于 `build/RobWorkStudio/bin/Release/` 产出。
- **测试亲手执行**（非引用留痕）：集成模式 `sdurws_ird_execution_test` **31/31 PASSED** exit 0、`sdurws_ird_execution_contract_test` **9/9 PASSED** exit 0。
- **独立冒烟模式**（ex-t02-smoke 独立缓存）：configure exit 0；`cmake --build /d/acc-tmp/ex-t02-smoke --config Release`（全量默认目标）**exit 0**、错误计数 **0**；冒烟测试亲手执行 **31/31＋9/9 PASSED** exit 0。
- **送验留痕交叉比对**：4 份在库 gtest XML 实测 `tests="31"/"31"/"9"/"9"、failures="0"`——与本人四跑逐一相同；在库 integration-build.log 为**主仓存量增量树**产物（107 处 `10_Source_Repos` 路径），提交信息表述为"集成模式全量 Release 构建零 error 零 warning"，未宣称"从零/最强口径"，表述与留痕一致（F-036/F-038 口径复核）；本人全新树复现（任务目标面从零＋全树受 F-008 阻挡的事实记录）为其补强。

## 4.3 契约 verify 命令 —— 通过

契约 `verify` 数组仅一条：`cmake --build build --config Release --target sdurws_ird_execution_test`——验收者在 ex-t02-repro 根**逐字亲手执行**，exit 0、增量零 error（构建树该目标此前已从零建成）。

## 4.4 acceptance 条目逐项证据 —— 通过（5/5；ird_gates 无豁免需求——已实际执行）

| # | 契约条目 | 验收者亲手证据 |
| --- | --- | --- |
| 1 | EX-SM-1~7 全绿：九态逐转移矩阵＋§5.3 逐行断言；矩阵外拒绝（EX-SM-6 InvalidState、状态不变；受理前拒绝在状态机之外不产生 TaskRecord 不占状态） | 两模式四跑全绿含 `TableAndMatrixMatchSection53_EX_SM_Matrix_TASK01`（15 行表＋(触发,源态) 唯一性＋9×9 逐格 13 对合法边＋禁止转换点名＋检查点保留逐行＋能力守卫仅 T10 声明）、`Queued/Preparing/Running/PausedCancel…`＋`FourStateCancelExits`（EX-SM-1~4）、`ResumeCreatesNewAttempt`（EX-SM-5）、`MatrixOutsideRequestsRejected`（终态出边/跳步/越权/工厂触发误用，断言 code==InvalidState＋detail 前缀＋状态不变）、`FactoryPreconditions`（受理记录非 Queued/预绑定运行、恢复重建缺运行绑定均拒——"不产生 TaskRecord"的结构面）、`PauseUnsupportedExplicitFeedback`（EX-SM-7） |
| 2 | EX-SUB-1：三级身份并发唯一、同任务事件 FIFO；TaskId/RunId/AttemptId 值类型承接（execution 只分配与携带不重定义——TASK-03 五元组承接） | `TaskIdGenerateUniqueUnderConcurrency`＋`RunIdCoreGenerateUniqueUnderConcurrency`（8 线程×400 并发生成、set 去重计数断言）、`BindRunSetsAttemptOneAndGuards`（Queued 期 run 空/attempt=0、绑定置 1、Queued/重复/零值违约抛）、`SameTaskEventFifo`（6 转移事件序＝提交序＋五元组随 bindRun 完整化＋attempt=2）；契约面 `_contract_test` 9/9 绿：`NoRedefinitionOfCoreTypesInExecution`（四词表＋三身份类型定义形态 execution 树零命中）、`ScannerDetectsCoreDefinitionsSanity`（扫描器锚点自证——core 树定义真实存在）、`TaskIdIsExecutionOwnedPerSection41`（TaskId 定义于 execution 且 core 零定义——正反两面）、`CoreValueTypesUsableFromExecution`（链接半区实证） |
| 3 | 强制终止矩阵（forceTerminateCost 仅声明与呈现、不影响协议）；Queued/Preparing/Running/Paused 四态取消出口（ARCH §11.2-6） | `ForceKillMarksExplicitly_T13`（state=Failed＋termination=ForceTerminated＋EX-FORCE-TERMINATED 诊断＋T13 行 RetainLatest 声明）、`ForceTerminateCostDoesNotAffectProtocol_acceptance3`（Cheap/Expensive 两档终态/原因逐字段一致）、`FourStateCancelExits_ARCH_11_2_6`（四入口全经 Canceling 收敛 Canceled）、`PausedCancelRetainsCheckpoint`（T12 行 RetainLatest 声明断言——磁盘 manifest 断言归 EX-T03/T04，用例注释显式声明边界） |
| 4 | 能力声明承载（TASK-01）：三能力字段经 EvaluatorRuntimeCapabilities 注册表扩展声明（P-EX-7 处置：不私改 evidence EvaluatorDescriptor）；不支持暂停收到暂停显式反馈不静默（EX-SM-7 卡行禁止项） | `DeclareLookupMinimalFallback_PEX7_S55`（声明/同键重复拒绝/tryLookup 空/lookupOrMinimal 最小能力回退）；源码直核：TaskTypes.hpp 仅 include `evidence/Evaluator.hpp`（EvaluationKey）与 `evidence/Snapshot.hpp`——evidence 侧零改动（diff 无 evidence 文件）；`PauseUnsupportedExplicitFeedback_EX_SM_7_TASK01`（accepted=false＋state 不变＋feedback.code=="EX-CAPABILITY-UNSUPPORTED"＋context/cause/recommendedAction 非空＋零状态事件；supportsPause=true 对照半区进 Paused）——双探针亦证实该断言真实（4.5 探针①命中本用例） |
| 5 | P-EX-4（九态零新增）；P-EX-6（会话内存态、Queued 崩溃即消失）；P-EX-1（Draft 未冻结、冻结 diff 后增量同步） | `NineStatesNoAddition_PEX4_TASK01`（九 token 往返＋created/cancel-requested/rejected 拒绝＋受理即 Queued 首事件）；`QueuedRecordHasNoDiskIdentity_PEX6`（run 空/NotApplicable）＋`RecoveryScanAssignsInterrupted` 内"运行期矩阵到 Interrupted 零出边"断言＋`FactoryPreconditions` 恢复路径必须携带运行绑定（不存在排队任务的恢复路径）；include 面仅 core/evidence 公共头（4.6 直 grep）——P-EX-1"按卡现状签名消费、冻结 diff 后增量同步"口径在 CMakeLists/单元卡 §15.4 v0.4/ird-test-report.json 三处登记一致 |

## 4.5 测试真实失败能力 —— 通过（2 处失效注入，验收者独立设计、亲手执行；还原遵循 F-068 教训 touch 强制重编译）

| 探针 | 注入点（与实施者留痕探针不同点位） | 实测结果 |
| --- | --- | --- |
| ① 守卫声明损坏 | StateMachine.cpp 逐转移表 T10 行 `requiresSupportsPause` true→false（sed 精确替换） | **恰 2 用例红**：`TableAndMatrixMatchSection53_EX_SM_Matrix_TASK01`（"能力守卫声明仅 T10 持有"断言）＋`PauseUnsupportedExplicitFeedback_EX_SM_7_TASK01`（守卫失效后暂停被错误接受）；其余 29 绿 |
| ② 终态映射损坏 | TaskTypes.cpp `terminalOutcome` 的 `Completed→Completed` 改为 `Completed→Canceled` | **恰 2 用例红**：`TerminalOutcomeFrozenTable_S56`（冻结表逐对断言）＋`SameTaskEventFifo_EX_SUB_1`（终态映射收尾断言）；其余 29 绿 |

两探针各自 `git checkout --` 还原＋touch 源文件＋重建后 **31/31 复绿**，`git status --short` 0 行（零残留）。测试非恒真、注入即红，真实失败能力成立。

## 4.6 架构红线 —— 通过（AGENTS §5 八条逐条）

1. **L2 零 Qt**：execution 树（include/src/contract_test/test）`#include <Qt|<Q` 直 grep 仅命中 BuildRedLineTest.cpp 内红线扫描用例自身的模式字符串（扫描器输入，非 Qt 依赖）；配置期 CMake 守卫（`^Qt` FATAL）与 `NoQtInclude` 用例双保险，两模式构建成功＋用例绿实证。
2. **业务域互链（R-1）**：target_link_libraries 仅 `sdurws_ird_core/evidence/project` 三条 ARCH §3.5 登记边；配置期守卫对 `^sdurws_ird_` 通配白名单外链接 FATAL；ird_gates 复跑 17 命中中 **零 execution 项**。
3. **跨单元私有头（R-2）**：跨单元 include 直 grep 全集＝core/{DiagData,Errors,Evaluation,Events,Identity}.hpp＋evidence/{Evaluator,Snapshot}.hpp——全部位于他单元 `include/` 公共根；`../` 相对路径与 src/ 私有头 include 零命中。
4. **框架零源码修改（SA-02）**：4.1 已证 diff 零框架文件、patches/ 零新文件。
5. **不做名称前缀拼接（R-4）**：ird_gates R4 扫描 17 命中中唯一 R4 项为 project/ProjectStoreImpl.cpp（既有登记例外）；execution 树零 R4 命中。
6. **权威唯一（PA-1）**：TaskTypes/StateMachine/Errors 均为 execution 自有域（单元卡 §3.1/§4/§5 组成）；身份值类型只消费 core、能力声明为 evidence 侧注册表的 execution 扩展（P-EX-7 处置登记）、零 project 写路径/runtime 真值/policy 语义私建。
7. **不可变历史（PA-2）**：本交付无修订数据面；TaskRecord 可变字段的写入纪律（state 仅状态机、termination 终态一次写入、受理后不可变字段）由状态机强制并有 `FactoryPreconditions`/防御分支断言。
8. **不恢复 old/**：全新实现，零历史代码复制痕迹（实现逐条对照 units/execution.md §3.4/§4/§5 原文，注释标注出处）。

## 4.7 注释规范 —— 通过（公共头全查 3/3；实现文件抽查 3/3；测试/CMake 全查）

- **公共头 3 件全查**（Errors.hpp/StateMachine.hpp/TaskTypes.hpp）：文件头均有 @file/@brief＋设计依据（需求 ID＋卡章节＋契约条目）＋背景说明（如 TaskTypes.hpp 三级身份所有权边界、StateMachine.hpp 三条冻结规则、Errors.hpp 错误两分法）；类/结构体均有业务概念、生命周期、线程安全说明（§2.5 所有权/线程约束逐项在位：ITaskEventSink 不接管所有权、TaskStateMachine 非线程安全仅调度线程、EvaluatorRuntimeCapabilities 内部互斥两相并发、TaskRecord 单写者）。
- **实现文件 3 件全查**（≥3 要求满足）：StateMachine.cpp 逐转移表与卡 §5.3 逐行对照注释＋request() 五步分段讲解（每步业务含义＋失败路径）；TaskTypes.cpp 解析/生成逐步注释（大端装配、保留值重取、截断 64 字符防日志洪泛）；Errors.cpp switch 全枚举＋防御分支理由。函数注释含 @param [in] 方向、取值范围、异常与触发条件（@throws）。
- **§2.5 高危信息**：错误语义（调用方违约 fail-fast vs 可预期走 Ack 两分法，稳定码/ fail-fast token 区分——InvalidState/ContextClosed 不发稳定码）、确定性来源（thread_local 引擎、单一事实源转移表、表驱动断言）、单位/取值（percent∈[0,100] 无量纲、attempt 0＝保留值、checkpoint sequence ≥1）均在位。本单元无可物理量/坐标系/rad 载荷，相关条目自然 vacuous。
- **测试文件**：用例名携带需求/AT 追溯后缀（_EX_SUB_1/_EX_SM_6/_TASK01/_ARCH_11_2_6…），文件头与每用例注释说明验证的 acceptance 条目与替身边界声明（"不夸大覆盖面"——时序/进程/磁盘断言显式移交 EX-T03/T04/T06/T09）。
- **CMakeLists.txt**：中文注释块完整（目标注册、依赖红线守卫、ird_add_gtest 宏、/utf-8 编码纪律——含 core 落位 C2001 先例引用）。

## 4.8 文档同步与 findings 核对 —— 通过

- **实现与任务卡**：逐转移表 15 行、九态词表、错误码 19 值＋token、能力注册表、终态映射逐项与 units/execution.md §3.4/§4/§5 原文一致（本人对照实读）。**一处实现与卡的字面偏差已按 DTB §5.4 登记**：ArchivePhase 枚举卡 §3.1 组成表登记于 RunRegistry.hpp 行，实现落 TaskTypes.hpp（防头循环）——单元卡 §15.4 v0.4 行显式登记"一处头内归置登记"，值域语义零变更，卡头版本行/日期同步（v0.4/2026-09-18），不存在"代码先行、文档失实"。
- **任务状态/追踪**：契约 status=ready（翻转权在收尾，PIPE §4.7——非实施者可动）；ird-test-report.json 的 acceptanceCoverage 5 条逐条对账（本人逐条独立复核成立）。
- **findings.json 未关闭条目相关性核对**（读时点 207 条/177 open）：
  - **F-007/F-062/F-098/F-099/F-101/F-200（全新树环境链）**：本次验收**直接踩中 F-200**（ini 模板缺失卡 configure_file），按其登记形态自主仓补拷未跟踪辅助件解决；该链责任在构建环境治理（AGENTS §4.1 口径），非本任务可顺手消账，维持 open。
  - **F-008（RobWorkSim 插件从零构建失败）**：本次全树从零构建**直接复现**（73 error 全框架代码、零 industrialrobot 涉入）；维持 open，不影响本任务判定。
  - **F-019/F-051/F-072/F-115/F-173/F-202（ird_gates 基线命中群）**：本人复跑 17 命中与 wp08-t02 送验留痕及 base 基线同集（evidence/io/policy/project/runtime/diagnostics 各 test 目标→testkit、libzip/expat、testdata_lint、project R4），零新增、零 execution 项——基线红为既有登记事实，本任务无加重。
  - **F-010 族（.gitignore 吞留痕日志）**：本任务**未踩中**——builds/wp08-t02/ 14 件（9 日志＋4 XML＋report）经 `git ls-tree` @ 冻结 SHA 实证全部真实在库，无"声称在库而未入库"。
  - **F-065/F-207（留痕日志 GBK 双重编码）**：本任务 gates log 同带该系统性特征（『命中 17 项』呈乱码形态）；已登记，不再重复转登；机器可读性（IRD-GATE-* ASCII 码）未受损，本人逐项比对无歧义。
  - **F-201（§4.10 与流水线实践脱节）**：沿用其登记口径与 EX-T01 r2 先例——ird-test-report.json 即本任务 resultNote（见 4.10）。
  - **F-012 标题家族（open 的 F-012/023/024/026/027/030/034/037/039/040/042/044/045/047/048/059/076/079/082…）**：本任务 2777bc0a 为该家族**新实例**（[units] 前缀＋名词短语开头）——按 ACC §5 强制转登 **F-208**（见"问题清单"）。

## 4.9 commit 质量 —— 通过（含 1 项建议级，转登 F-208）

- **c1ee4c6c** `[WP-08-T03] 实现 execution 任务身份与九态状态机（TaskTypes/StateMachine/Errors）`：动词开头合规；正文四段（What 按文件列点/Why 对契约与陷阱处置/How 验证结论/影响面）齐备；验证段七项主张（构建/测试/verify/validate/门禁/探针/缩水扫描）**经本人逐项复现全部一致**，无粉饰。
- **9d12b610** `[WP-08-T03] 留痕入库：…`：动词短语开头合规；四段齐备；"九件留痕"清点与 `git ls-tree` 实测 14 件构成（9 日志＋4 XML＋report）一致。
- **2777bc0a** `[units] execution.md v0.4：EX-T02 落位同步——…`：正文四段齐备、验证段 validate-docs PASS 与本人复跑同数字（20 units, 12 trace entries, 150 task files）；但**标题前缀后以名词短语（'execution.md v0.4'）开头**，非动词开头——F-012 家族（[units] 前缀形态同 F-037/F-039/F-044/F-076/F-079）再现，建议级不构成阻断，转登 F-208。

## 4.10 留痕完整 —— 通过

- 在库清点（`git ls-tree` @ 2777bc0a）：traceability/builds/wp08-t02/ 恰 **14 件**＝4 份 gtest XML＋ird-test-report.json＋9 件日志（integration-build/smoke-configure/smoke-build/test-run/verify-target-build/gates-ird-gates-target/validate-task/validate-docs/test-fault-probe）——与 9d12b610 提交清单一致，零缺件（F-010 族未踩中）。
- **数值交叉比对（本人复跑 vs 在库）**：测试 31/31＋9/9＝四份 XML 与 report 一致✓；ird_gates 17 命中零 execution＝gates log 与我方复跑同集✓；validate-task `PASS (1 tasks)`/validate-docs `PASS (20 units, 12 trace entries, 150 task files)`＝我方复跑逐字一致✓；verify 命令 exit 0＝我方逐字执行一致✓；探针 6 用例红→还原 31 绿＝test-fault-probe.log 与我方双探针同型（点位不同）一致✓。
- **resultNote 语义**：沿 F-201 登记口径与 EX-T01 r2 先例——ird-test-report.json（ird-test-report/1 schema，runs 三节＋acceptanceCoverage 5 条对账）即本任务结果注记；phase-one-validation.log 为 2026-09-10 后停更的静态文档，判定满足「或」条件。

## 4.11 偷懒/缩水实现扫描 —— 通过（4 子项）

1. **标记扫描**：diff 新增行 `TODO|FIXME|stub|placeholder|未实现|not implemented` grep **零命中**（无契约标注的触发式延后项，故无豁免清单需求）。
2. **空实现检测**：公共接口逐函数实读——TaskId 五方法/CheckpointId/ProgressReport.make/EvaluatorRuntimeCapabilities 三方法（含 mutex 保护查重插入）/TaskStateMachine 全方法/isTerminalTaskState/terminalOutcome/toToken 均为实质实现，无空体/恒默认值返回。
3. **逐条证据表**：acceptance 5 条逐条具名测试＋命令输出＋文件行号见 4.4 表——5/5 有证据，无"已实现"声明依赖。
4. **测试强度**：55 用例断言校验行为与边界（并发唯一性计数、逐格矩阵、五元组完整性、异常码与状态不变复合断言、幂等不发事件等），非恒真非"只编译通过"；与 4.5 双探针联动证实会红。

## 问题清单

**阻断级：0 项。**

**建议级：1 项（已按 ACC §5 强制转登 findings.json）。**

- **F-208**（新登，编号顺延自 F-207）：2777bc0a 提交标题 `[units] execution.md v0.4：EX-T02 落位同步——…` 前缀后以名词短语开头，非动词开头（ACC §4.9/AGENTS §6.3 字面口径）——F-012 家族 [units] 前缀形态再现（先例 F-037/F-039/F-044/F-076/F-079）；正文四段齐备、验证结论与验收者亲手复现一致，纯风格瑕疵不构成阻断。处置建议同家族既有登记：实施者模板补动词开头示例（句式如『登记 execution.md v0.4 落位同步（…）』）；对已推送历史不追溯改写。

**既有发现踩中登记（不新登，已在册）**：F-200（worktree 配置缺未跟踪 ini 辅助件——本次复现并按登记形态绕过）、F-008（全树从零构建受框架插件宏污染阻挡——本次 73 error 复现、零 industrialrobot 涉入）、F-065/F-207（gates log 双重编码乱码——本次同型出现）。

## 裁决

**verdict = pass**。4.1~4.11 全部通过、零阻断级问题；建议级 F-208 已转登 findings.json，不阻塞合入。验收对象＝2777bc0a7aba0b4e31c0d7e22c83b7ee605476fe（非分支引用）；合入决策归所有者（ACC §6）。
