# WP-24-T01 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | WP-24-T01 冻结版本基线首版（owner 指示 2026-09-26 随 WP-24-T03a 提前启动） |
| 送验对象 | 分支 `wp24-t01` @ `96d5c5be`（base＝redesign-main @ 1b3a7371） |
| 验收方式 | 本会话受所有者指令执行验收＋合入（§2 独立性要求的例外授权如实登记：实施者同会话执行，对抗式复核以全新重跑＋变异探针替代跨会话独立性） |
| **verdict** | **pass**（4.1~4.11 逐项通过；无阻断级问题；建议级 2 项转登） |

## 4.1~4.11 逐项证据

| 项 | 结论 | 证据 |
| --- | --- | --- |
| 4.1 改动范围 | 通过 | base..head 共 6 文件（share/baseline.md 新增＋policy 四件＋units/policy.md）——本任务无 canonical 契约（owner 指示增量，登记载体＝DTB §2.25 行＋policy.md v0.15），比对登记范围一致；forbiddenFiles 面（其他单元产品码/框架源码）零触碰；patches/ 零新文件（SA-02） |
| 4.2 可复现构建 | 通过 | 验收者本会话亲手重跑：集成模式 policy 三目标构建零错误；`sdurws_ird_policy_test` 200/200＋`sdurws_ird_policy_contract_test` 23/23（gtest XML＋ird-test-report.json 已入库 traceability/builds/wp24-t01-acc/）；冒烟模式 policy_test 131/131（build_smoke_t15-app 树） |
| 4.3 契约 verify | 不适用（如实） | 无 canonical 契约（增量任务）；validate-docs PASS（20 units, 12 trace entries, 189 task files） |
| 4.4 acceptance 条目 | 通过 | 单元卡/DTB 行登记的验收标准两条：①基线字段齐备并与构建树一致——逐字段核对（框架 commit 31b8184 经 git log 实测；Qt 6.11.1/MSVC 17.12.0/生成器经缓存与 vswhere 实测；碰撞后端取值与代码字面量一致；许可证清单版本经 vcpkg ports 实测）；②policy CollisionBackendDescriptor 取值回填——代码单点常量＋两处调用切换实测（Compatibility.cpp:96 区段／RobWorkCollisionEvaluator.cpp 构造器） |
| 4.5 测试真实失败能力 | 通过 | 变异探针两步：①值变异 `"rw-31b8184"→"-PROBE"`——套件仍绿（如实登记：套件钉非空＋同源，不钉具体值——值权威在基线文档）；②契约探针 置空 `""`——`CollisionSessionDetector.UniqueEntryProducesBuiltinBackend`＋`O20Ppol5.BackendVersionConservativeStance` 两用例**正确变红**，还原后复绿。探针已还原，零残留 |
| 4.6 架构红线 | 通过 | ird_gates 引擎直跑 base(1b3a7371)..branch 归一化 diff＝空（零新增）；改动面无跨单元新增边（policy 内部＋文档）；R-4 零字面量 TU 纪律保持（RobWorkCollisionEvaluator.cpp 仅常量标识符引用） |
| 4.7 注释规范 | 通过 | 抽查 4 文件（CollisionEvaluator.hpp 常量块/Compatibility.cpp TU 头/RobWorkCollisionEvaluator.cpp 构造器注释/baseline.md 全文）：文件头/类/函数 Doxygen 中文齐备；非 owning/线程/确定性/变更纪律标注完整 |
| 4.8 文档同步 | 通过 | policy.md v0.15（P-POL-5 消账＋§15.4 行）＋DTB 引用；validate-docs PASS；findings.json 无本任务未闭合相关项（核对） |
| 4.9 commit 质量 | 通过 | `96d5c5be` 标题 `[WP-24-T01] 动词结尾中文摘要`；正文四段齐备（What/Why/How/影响面）；验证结论与留痕一致 |
| 4.10 留痕完整 | 通过 | gtest XML×2＋ird-test-report.json 入库 traceability/builds/wp24-t01-acc/；构建日志（本验收会话增量编译输出）与实施段日志在案 |
| 4.11 偷懒/缩水扫描 | 通过 | ①diff 范围 TODO/FIXME/stub/placeholder/未实现/not implemented 零命中（实测 grep）；②空实现：回填为单点常量＋两处调用切换，非空壳；③验收标准逐条证据见 4.4 行；④测试强度：非空断言＋同源断言为行为级（探针②实证会红） |

## 发现的问题（分级）

| 级别 | 描述 | 处置 |
| --- | --- | --- |
| 建议 | RW_VERSION 构建宏为配置期动态产物（日期＋分支名），基线已声明不作冻结取值——建议 WP-01-T03 治理面登记该宏的"非权威"地位，防后续误用 | 已在 baseline.md §1 登记；归档建议 |
| 建议 | 阶段 E 复核项（WIP 单元框架面刷新／tag 决策）为基线 v1.0 预留缺口——按设计即如此 | baseline.md §7 已登记待办 |

## 验收者环境说明

本会话（实施者同会话受权验收）：Windows 11 x64；VS2022 17.12.0（MSVC v143）；Qt 6.11.1；构建树 `build/`（RWS_BUILD_INDUSTRIALROBOT=ON 实测确认）；冒烟树 `build_smoke_t15-app`。所有构建/测试/门禁结论均为验收者本会话亲手重跑输出，未采信实施者运行结果。
