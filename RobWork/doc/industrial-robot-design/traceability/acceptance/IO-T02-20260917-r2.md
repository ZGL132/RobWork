# IO-T02 验收记录（acceptance record，attempt 2）

| 字段 | 值 |
| --- | --- |
| 任务 ID | IO-T02（SafePath＋BudgetGuard：路径穿越/预算防护；unit=io；masterWp=WP-E；≙DTB §2.12 WP-11-T03；requirements=NFR-SEC-01/NFR-SEC-02/PM-05；knownPitfalls=P-IO-1/P-IO-4/P-IO-6） |
| 送验对象（冻结） | **8c61896d3a1a6ec5a5eb17f1890de7734f899dd3**（分支 wp11-t02；diff 范围＝ce90896fa7c1fc43895148e3d302ca823d26f96f..8c61896d＝state.base..headSha，4 个提交：aafb66ac 实现＋eaa6aa99 文档/留痕＋3e0314f8 attempt-1 建议级补正＋8c61896d B-1 补证） |
| 验收尝试 | attempt 2（分支漂移检测：本次验收启动时 `git rev-parse origin/wp11-t02`＝8c61896d3a1a6ec5a5eb17f1890de7734f899dd3 与送验 SHA 全 40 位逐位一致，**零漂移**；evidence 分支 `acc/IO-T02/1` 已存在（attempt 1 fail 记录），本 attempt 按序号使用 `acc/IO-T02/2`，永不复用） |
| 验收环境 | 全新 detached worktree（`git worktree add --detach` @ 8c61896d＝`D:/10_Source_Repos/21_robot/_acc_io_t02_tmp/wt-a`）从零复现：集成 build 树（worktree A 内 `build/`）与独立冒烟 build 树（`_acc_io_t02_tmp/smoke`）均为本验收新建；记录回传 worktree（acc/IO-T02/2 基于 origin/redesign-main@ce90896f）。pwsh 7＋MSVC 2022 x64（19.42）＋vcpkg 经典模式（git 未追踪，worktree 天然不含，toolchain/安装树指主仓绝对路径）。环境前置（非实施缺陷，worktree 环境缺口）：①`RobWorkStudio.ini.template.static`／`RobWorkStudio.ini.shared.in` 被 `.gitignore:79` `[Bb]in/` 规则吞——自主仓补拷后配置零错误（与 attempt 1 同型前置）；②集成配置带 `-DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64 -DCMAKE_TOOLCHAIN_FILE=<主仓>/vcpkg/scripts/buildsystems/vcpkg.cmake`（与主构建树 CMakeCache 实测口径一致）。验收全程只消费产物与文档（契约/单元卡/在库留痕/先行验收记录/findings.json），未读取实施者过程叙述（ACC §2 四条逐项满足；attempt 1 验收记录仅作为流水线产物消费用于核对返工闭环，全部结论以本人复现为准） |
| verdict | **pass**（4.1～4.11 全部通过，无阻断级问题；attempt 1 阻断 B-1 已由 8c61896d 补证消解、建议级 S-1/S-2/S-3 已由 3e0314f8/8c61896d 补正消解——均经本人逐项复验；建议级余量：新登记 F-186＋F-185 保持 open〔已推送提交不改写〕＋O-1 所有者环节提请，均不阻塞合入） |
| 验收日期 | 2026-09-17 |

## 4.1 改动范围 —— 通过

- `git diff --name-status ce90896f..8c61896d` 实证 **24 个变更文件**（attempt 1 时为 15 件，返工追加 8 份 .log＋io.md 两笔补正）：
  - 产品代码 11 件（均在 `industrialrobot/io/` 下）：`io/CMakeLists.txt`（M）、`IoFwd.hpp`（M，增补 `IoResult<void>` 特化）、新增 `SafePath.hpp/.cpp`、`Budget.hpp/.cpp`、`IoDiagnostics.hpp/.cpp`、测试 3 件 `SafePathTest.cpp/BudgetTest.cpp/IoDiagnosticsTest.cpp`（A）；
  - 文档 1 件：`units/io.md`（M，卡头版本/日期＋§1.2 现状行＋§3.1 SafePath.hpp 行增记＋§15.5 v0.4 行含命名等价补记）；
  - traceability 12 件：`builds/wp11-t02/` 下 8 份 .log（8c61896d 强制入库）＋3 份 gtest XML＋`ird-test-report.json`。
- **逐文件比对契约 allowedFiles 三条通配**（`industrialrobot/io/**`、`units/io.md`、`traceability/**`）：**全部在册，零越界**。
- forbiddenFiles（REQUIREMENTS.md／ARCHITECTURE.md）**零触碰**（diff 无该两文件）。
- 无未登记框架源码改动：diff 零框架路径；`patches/` 零新文件（SA-02）；ird_gates SA-02 面实跑零新增命中（见 4.6）。

## 4.2 可复现构建 —— 通过（全新树从零双模式；集成全量树的基线插件失败按 F-008 判例归因）

- **集成模式**：worktree A 从零配置 **exit 0**（`-- Configuring done`／`-- Generating done`，缓存实测 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`）。
  - **契约目标级构建**（`cmake --build build --config Release --target sdurws_ird_io_test`）：**exit 0、`: error` 计数 0**，`build/RobWorkStudio/bin/Release/sdurws_ird_io_test.exe` 生成（日志 `_acc_io_t02_tmp/logs/verify-target-build-acc.log`——验收者过程日志留存于验收临时目录，按协议不入库，关键结论摘引于此）。
  - **全树全量构建**（`cmake --build build --config Release`）：exit 1，73 处 `: error` **全部**落於 `RWSimulatorPlugin.vcxproj`（71）与 `bt_plugin.rwplugin.vcxproj`（2），错误签名＝`rw/math/Math.hpp` 处 C4003→C2059/C2334/C2143 连锁——与 **F-008**（基线框架缺陷：全新集成树 RWSimulatorPlugin min/max 宏污染簇，base 959fb9b 对照同样失败）逐字吻合；**io/industrialrobot 路径零错误命中**。按 F-008 判例（TK-T01 登记）与 PRJ-T13～T16／IO-T01／IO-T02 attempt 1 五次先例：全新树全量构建失败属基线框架存量缺陷，本任务 diff 零框架触碰（4.1），不归本任务；DoD 第 1 条按"目标级零错误＋F-008 归因"口径判定。
- **独立冒烟模式**：独立临时目录从零配置＋全量构建（带主仓 vcpkg toolchain——AGENTS §4.1 口径）**exit 0、`: error` 计数 0**，`smoke/io/Release/sdurws_ird_io_test.exe` 生成。
- **测试执行（验收者亲手，非采信留痕）**：集成 exe **exit 0、20/20 PASSED**（164 ms）；冒烟 exe **exit 0、20/20 PASSED**（150 ms）。与在库三份 gtest XML（各 `tests="20" failures="0"`，`<testcase` 计 20、`<failure` 计 0）交叉一致。

## 4.3 契约 verify 命令 —— 通过

- 契约 verify＝`cmake --build build --config Release --target sdurws_ird_io_test`：①验收者直接执行 **exit 0 零 error**（4.2 同一命令独立复跑）；②`verify-task.ps1 -TaskFile tasks/foundation/IO-T02.json` 实跑 **PASS**（脚本解析仓库根＝worktree A、契约命令逐字执行、增量重建零 error）；③`validate-task.ps1` **PASS (1 tasks)**。
- 测试执行：双模式 20/20（4.2）；在库 `integration-test-run.log`／`smoke-test-run.log`（各 `[ PASSED ] 20 tests`）与复现一致。

## 4.4 acceptance 条目逐项证据 —— 通过（5/5；证据表见 4.11③）

1. **V09/V10/V11 全绿**（复现：IoSecTest 7 用例双模式全绿）：
   - `PathTraversalP4IllegalTableRejectedWithExactCodes`——§4.3.3 P-4 表 18 行逐条拒绝＋正确码（ESCAPE／PACK-ENTRY／RESERVED／TOO-LONG），含程序化边界（段长 256>255、总长 4097>4096——SP-9）；
   - `PathTraversalP4LegalRowsAndBatchDuplicateDetection`——合法行放行（relKey＝正斜杠＋折叠）＋折叠键查重（A/a→IO-PACK-DUPLICATE-ENTRY 整批拒绝、detail 全量含 index=2）＋最小序号定码＋空包合法；
   - `PathTraversalP5IllegalTableRejectedWithExactCodes`——P-5 表 10 行（净逃逸 `objects/../../evil.dll`、绝对/UNC 形态、保留名 `objects/nul`、尾随点、SP-3 区外首段）逐条拒绝＋P-5 缺 base／P-1 多传 base＝IO-FORMAT-INTERNAL（NFR-SEC-01 项目资源区逃逸反例拒绝面）；P-4/P-5 角色即"包解包成员＋项目内持久化资源引用"两翼，用户显式外部源（P-1）不在此限（下条）；
   - `SameProjectAltPathsEquivalentKeysAndSessionMutex`——同一项目三拼写（原生／全折叠＋尾随分隔符／正斜杠）equivKey 相等＋EquivKeyMutex 互斥（第二拼写 tryAcquire=false、heldCount=1）＋释放复用＋异项目不互斥（§4.2.4＋§9.10 TempArea 互斥经等价键）；
   - `SymlinkJunctionEscapeRejectedWithIoSecSymlink`——**真实 reparse point**（fs::create_symlink／create_directory_symlink／`mklink /J` junction 三形态均实际创建成功）指向资源区外一律 IO-SEC-PATH-SYMLINK（§9.12 连字符展开码）＋params 携带 offender 层级脱敏路径（V11 final-path 观测点）＋区内真实实体放行（防护不误伤）。
2. **V06 全绿**：`StreamBudgetRowsOverBudgetTriggersIoSecBudgetRowsWithBoundedMemory`——真实生成 10⁶ 行 CSV（约 12 MB）、行预算收紧 10⁵、恰在第 **100001** 笔检查点触发 IO-SEC-BUDGET-ROWS（三要素 actual=100001/limit=100000/unit=count）、**峰值提交内存增量 < 文件大小/2**（K32GetProcessMemoryInfo 动态解析实测采样）、账本 used 停在限额（失败笔不入账）。矩阵行名（IoCsv/StreamBudget）↔ gtest 套件映射经 `ird-test-report.json` testSuites 字段显式承载；CSV 读写器本体随 IO-T03（范围声明在 BudgetTest.cpp 文件头与 report，行检查点以最小流式路径承载——§11.2 IO-V06 行观测点全数落位，非缩水）。
3. **预算超限拒绝＋比较型三要素＋双保险＋账本可观测**：`BudgetTriadFileDepthTotalRejectWithComparativeParams`（FILE/-DEPTH/-TOTAL 分别触发，actual/limit/unit 齐、单位 bytes/levels/count 随维）；`DeclaredVsActualDoubleBookingKeepsLarger`（预检笔＋展开检补差＝max(声明,实际)、预检越限先拒不开、失败笔状态不变）；`ledger()` 快照断言（维度按声明序、失败笔不入账、关闭后可取终值）；scope 树回收（`ScopeTreeRollupToParentAndParentLimitEnforced`——子账回收至父、回收触发父超限则父子状态均不变、已关闭句柄 IO-FORMAT-INTERNAL）；饱和与比例（`SaturatingAccumulatorNeverWraps`／`ArchiveDualSidedBookingExpandAndRatio`——UINT64_MAX 饱和投影、交叉相乘无除法、压缩 0 展开>0 比例无穷大拒绝、压缩侧不回收展开侧回收）。§4.4"预检＋展开检双保险"语义逐项有断言。
4. **例外不扩大化**：`ExceptionNotWidenedUserSourceAndExportTargetUnrestricted`——P-1 经 symlink 入口放行（存在性准入＋equivKey＝最终实体路径＝实体直连同键）、不存在→IO-RES-NOT-FOUND（资源码非安全码，不虚报安全违规）、P-7 允许尚不存在；反例钉住同一路径换 P-5 角色即拒（规则集随角色切换，例外不因路径形态扩大）。其余通道（P-4/P-5/P-6）全部受 SafePath 管辖（4.1～三角色用例）；BudgetGuard 对 P-1 仍施加预算管辖（卡面口径，实现与用例一致）。
5. **P-IO-4/P-IO-6/P-IO-1 处置**：
   - **P-IO-4**＝`ProductDefaultValuesPinnedToCardSection451` 16 维 Draft 档位数值逐维字面钉住——验收者将 `Budget.cpp kDimTable`（L49-66）与 io.md §4.5.1 表（L411-426）逐维比对：**16 维默认值/硬上限逐一相等**（含 TempAreaBytes 硬上限＝编译期项"展开预算×2"＝32 GiB，磁盘项归临时区会话运行时收紧——卡面口径）；`PackImportChannelHardenedSpecRelaxForbidden` 四维禁放宽/tighten 只收紧 fail-fast/openScope 拒超硬上限。
   - **P-IO-6**＝`IoDiagCodeTable` 三用例（token 全枚举映射＋§9.12 全集 55 码双向机械比对、真实 `StableCodeRegistry` 注册含 seal/重复注册 DuplicateCode/内置 87 码合注册顺序无关 manifest）——验收者将测试字面清单与 io.md §9.12 表（L1410-1422）逐族比对：**55 码逐字一致**（含连字符展开 IO-SEC-PATH-SYMLINK；Ok/IO-CANCELLED 状态码不注册——§9.12 首行/UX-03）；码值未私定；diagnostics 单元文件零改动（allowedFiles 红线下正确形态）；**内置表收编确认随本记录提请 diagnostics 所有者**（问题清单 O-1）。
   - **P-IO-1**＝SafePath/BudgetGuard 以 `std::filesystem::path` 接收解析基点、io 公共头零 project 类型——`IoHeadersFreeOfQtAndCrossUnitIncludes` 运行期扫描绿＋头文件通读证实（SafePath.hpp L27-30 明示注入形态与裁决无关性）；io 公共头不因裁决结果变化。

## 4.5 测试真实失败能力 —— 通过（两枚实现侧失效注入均即红；还原后与送验对象逐字节一致、20/20 恢复）

- **探针 1（饱和加法有牙齿）**：`Budget.cpp` satAdd 退化为普通 `a + b`（L136 注入，注释标记 ACC-PROBE-1）→ 重建 → `BudgetTest.SaturatingAccumulatorNeverWraps` **FAILED**（回绕假象被 actual=饱和投影断言捕获）。
- **探针 2（大小写折叠有牙齿）**：`SafePath.cpp` foldCase 条件改 `if (false && …)`（L184 注入，标记 ACC-PROBE-2）→ 重建 → `IoSecTest.PathTraversalP5LegalRowsAccepted` **FAILED**（P-5 relKey 折叠断言捕获；注：`SameProjectAltPaths` 在此注入下仍绿——既有目录的 weakly_canonical 解析实盘大小写掩盖折叠缺失，折叠行为由 P-5 relKey 断言独立守卫，无恒真面）。
- 两探针后 `cp 备份还原`＋`git status --porcelain`（io 树零输出）＋`git diff --stat HEAD`（空）——**io 源码树与送验对象 8c61896d 逐字节一致**；重建后全量 **20/20 PASSED** 恢复。
- 测试强度（4.11④ 联动）：断言校验行为与边界（255/256、4096/4097、恰满+1、UINT64_MAX 饱和投影、压缩 0 展开>0 除零式逃逸、第 100001 行精确触发、重复注册 DuplicateCode、最小序号定码、真实 reparse point 三形态），无恒真、无"只编译通过"。

## 4.6 架构红线 —— 通过（AGENTS §5 八条逐条）

1. **L2 计算内核零 Qt**：新增 11 文件通读＋`grep -rnE "include <Qt|QString|QObject|QFile|QDir"` 全树扫描**零命中**（Win32 仅 `GetFileAttributesW`＋kernel32 动态解析——io 为 L2/L1 边界文件设施单元，非 Qt 面）；ird_gates R3 实跑零 io 命中；`IoHeadersFreeOfQtAndCrossUnitIncludes` 运行期扫描用例绿。
2. **业务域互链禁止（R-1）**：io 仅 PUBLIC 链 core＋diagnostics 两条 ARCH §3.5 登记边（CMakeLists L74-77 配置期守卫保持）；ird_gates R1 实跑零 io 命中；实跑 SUB/R1 命中集全为基线（见下）。
3. **跨单元私有头禁止（R-2）**：`grep -rhoE "#include <sdurws/ird/[a-z_]+/"` 全 io 树实测——仅 `ird/io/`（自）×12 与 `ird/diagnostics/`×2（DiagCodes.hpp **公共头**，io→diagnostics 登记边）；ird_gates R2 零命中。
4. **框架零源码修改（SA-02）**：4.1 实证零框架路径＋patches/ 零新文件＋gates SA-02 面零新增命中。
5. **不做名称前缀拼接/剥离（R-4）**：ird_gates R4 实跑命中仅 project 基线 1 处（ProjectStoreImpl.cpp——DTB §4.5 登记例外），io 零命中。
6. **权威唯一（PA-1）**：IO-* 码值以 §9.12 建议值产出 CodeDescriptor、经 diagnostics 公共 API（`IDiagnosticRegistry::registerCode`）注册——码表权威仍在 diagnostics::StableCodeRegistry，io 未私设注册表、未改动 diagnostics 单元任何文件；诊断文案键按 `diag.<code-lower>.*` 约定生成（值归 ui，未越权）。
7. **不可变历史（PA-2）**：不适用（io 单元无修订写路径）。
8. **不继承 old/（R-5）**：全文件零 old/ 引用，实现按卡面 §4.2/§4.3/§4.5/§9.1/§9.2/§9.12 从零编写。
- **ird_gates 实跑（验收者亲手，`cmake --build build --config Release --target ird_gates`）**：exit 1（SA-10"命中判构建失败"基线口径），去重后真实命中 **11 处＝基线既有全集**（SUB×9：diagnostics_contract/evidence×2/policy×2/project×2/runtime 的测试目标直链 testkit×8＋`testdata_lint→testkit` 表外边×1；T1×1：testdata_lint 链 testkit；R4×1：project 登记例外）——与送验报告声称的基线 11 处逐一对应，**零 io 命中、零新增**（日志中 R1/R3/R5/T2 等其余字样均系引擎自测 `fail_*` 按预期检出行与输出重复，非真实命中）。

## 4.7 注释规范 —— 通过（公共头 4 件全查＋实现 3 件全查＋测试 3 件全查，超抽查下限）

- **文件头**：9 件新增/大改文件全部具备（@file/@brief＋设计依据含卡面章节/需求 ID/契约 acceptance 编号＋"为什么"背景说明——如 SafePath.hpp 的 SA-14 集中防护论证、P-IO-1 注入锚点、Budget.hpp 的"预算为何归 io 不归 policy"（IO-D06）、IoDiagnostics.hpp 的"码值权威链"）。
- **类/函数 Doxygen 中文**：公共接口逐方法契约注释（前置/后置/错误/副作用/生命周期/线程/取消——ISafePathResolver、IBudgetGuard、EquivKeyMutex、BudgetSpec 逐项齐全）；实现文件语义段落注释标注算法步骤与卡面出处（normalizeCore ①~⑨ 步、词法消解净逃逸判据的 §4.2.2/§4.3.3 表行对照、closeScope 预演-提交两段、kDimTable 逐行数值来源）。
- **§2.5 高危信息**：物理单位（UTF-16 码元/字节/层级/计数/比例逐处标注——`maxTotalLength // 单位 UTF-16 码元` 型注释齐备）；坐标系/角度制不适用（io 无位姿量）；所有权（shared_ptr 共享语义、值拷贝归属、registry 调用方持有——本函数不接管 逐处声明）；线程约束（resolver 无状态并发、EquivKeyMutex 内部 shared_mutex、guard 单 scope 非并发＋ledger 并发只读、注册期单线程约定，逐类声明）；错误语义（调用方契约违约 fail-fast〔tighten/relax 抛 invalid_argument〕vs 环境错误诊断码，逐处注明归类）；确定性来源（A-Z 折叠不做本地化、饱和运算、十进制定点不经 locale、清单序＝表行序、枚举序＝表行序 static_assert，逐处注明）。
- 无过期注释、无复述式废注释、无中英混杂机翻腔（全查未发现）；魔法数字均有来源注释（32767/4096/255、16 维数值逐行注明表行）。

## 4.8 文档同步 ＋ findings.json 未关闭条目相关性 —— 通过（attempt 1 建议级偏差全部消解并经复验）

- **实现与任务卡偏差均有登记（DTB §5.4）**（io.md §15.5 v0.4 行实测在库）：①`sdurws_ird_io_test` 提前随本任务以真实用例登记（§12 IO-T01 禁止项语义＝无内容占位目标——本落位非预建空目标）；②§9.2 两处签名等价调整（openScope 增 parent 参数、增补 chargeArchive）；③`IoResult<void>` 特化；④BudgetLedger→BudgetLedgerSnapshot 命名等价补记。**设计正文（§2~§14）语义零变更**——实测 diff 仅卡头两行＋§1.2 现状行＋§3.1 SafePath.hpp 行增记＋§15.5 v0.4 行，相符。
- **attempt 1 建议级问题返工闭环复验（S-1/S-2/S-3 均消解）**：
  - S-1（卡头版本/日期未随迁，F-182）：3e0314f8 后卡头实测 `v0.4`＋`2026-09-17`（diff L7-8）——**已修复**；
  - S-3 前半（§3.1 增记声称未兑现，F-184）：§3.1 SafePath.hpp 行实测含 EquivKeyMutex——**已修复**；
  - S-3 后半（命名等价未登记）：§15.5 v0.4 行实测含"命名等价……补记"——**已修复**；
  - S-2（eaa6aa99 幻影清单，F-183）：8c61896d 将 8 份 .log 实际入库，冻结对象内清单与在库留痕一致——**已消解**（历史提交正文不改写，符合 AGENTS §6.3）；
  - S-4（eaa6aa99 标题名词开头，F-185）：已推送提交不改写——保持 open（纯风格，F-012 判例不阻断）。
- **任务状态/追踪矩阵**：契约 status=ready 待收尾翻转（收尾环节动作）；io.md §11.2/§12 行为本任务验收基准无修改需求；contract-compile-log.md 无本任务新行需求（未触发契约修订）。
- **findings.json 未关闭条目相关性核对（redesign-main 侧 160 条 open；attempt 1 evidence 分支侧另有 F-182~F-185）**：
  - **F-153/F-010（.gitignore `*.log` 吞 traceability 留痕）——本任务曾直接受害（B-1），经 8c61896d `git add -f` 入库后本任务面已消解；根治（.gitignore 辖域豁免）仍 open 归 governance，与在库 wp11-t01 先例同口径**；
  - **F-185/F-182/F-183/F-184**（attempt 1 登记于 acc/IO-T02/1，redesign-main 尚无）——**本次随本记录移植进 findings.json**（F-182/F-183/F-184 按复验结果置 fixed 并附返工提交号；F-185 保持 open），防 attempt 1 evidence 分支不合并导致登记丢失；
  - **F-008（全新树全量构建基线失败）**——本验收 4.2 实测第六次复现（RWSimulatorPlugin/bt_plugin 73 错误），归框架侧，与本任务产物无涉；
  - **F-179/F-180**（IO-T01 提交标题/卡头日期家族）——本任务 3e0314f8/8c61896d 标题动词开头合规、io.md 卡头日期已随迁，**未再现**；eaa6aa99 一例即 F-185 本体（已登记）；
  - F-120（批次范围缺陷）已 fixed；F-181（收尾落分支）属流水线治理，与本任务产物无涉；
  - 其余 open（core/project/policy/runtime/evidence/diagnostics 各家族）与本任务零交集。
- **新发现（建议级，转登 F-186）**：在库 `gates-ird-gates-target.log` 仅 3 行尾部片段（门禁引擎 GBK 控制台输出被直接捕获进 UTF-8 文件产生乱码；不含任何 IRD-GATE 命中明细行）——ird-test-report.json gates 字段声称"命中集＝基线 11 处（留痕 gates-ird-gates-target.log）"，从该日志本身无法核对明细（本验收经独立实跑确认 11 处基线零 io，**结论为真**，仅留痕载体弱）。

## 4.9 commit 质量 —— 通过（结论零粉饰；历史风格瑕疵 F-185 已登记不阻断）

- **aafb66ac**『实现 io 单元 SafePath＋BudgetGuard 与 IO-* 码表注册』：标题动词开头；正文四段齐备；验证段与验收者复现交叉一致（双模式零 error、20/20、门禁零新增——含"曾因 Psapi.lib 命中 1 处改 kernel32 动态解析后复扫通过"的过程如实陈述）；其中"集成模式全量构建零错误"系实施侧增量树实况（全新树全量失败为 F-008 基线，见 4.2，零 io 关联）——非粉饰。
- **eaa6aa99**『io.md v0.4 同步 IO-T02 落位并登记实施留痕』：四段齐备、验证结论复现为真；标题名词短语开头（F-185，已登记）；改动内容清单幻影（F-183）已由 8c61896d 消解（见 4.8）。
- **3e0314f8**『补正 io.md 卡头版本同步并兑现 §3.1 增记承诺（验收 S-1/S-3）』：标题动词开头、四段齐备、改动与 diff 逐一对应、Acc-Covered 字段如实标注"无（建议级补正）"。
- **8c61896d**『强制入库 8 份验证留痕日志补全证据链（验收阻断 B-1 返工）』：标题动词开头、四段齐备、8 份 .log 与在库 ird-test-report.json 按名引用逐一对上、"本提交零代码变更＋日志为实施段原生成文件"如实陈述；`git add -f` 形态与 IO-T01 先例一致。
- 四提交均未改写既有推送历史（返工以追加提交落位——AGENTS §6.3 合规）。

## 4.10 留痕完整 —— 通过（B-1 阻断已消解；冻结对象内证据链闭环）

- **`git ls-tree -r 8c61896d -- builds/wp11-t02/` 实测 12 个文件在库**：8 份 .log（integration-build／smoke-configure／smoke-build／integration-test-run／smoke-test-run／gates-ird-gates-target／validate-task／verify-task）＋3 份 gtest XML（integration／smoke／report-target，各 `tests="20" failures="0"`、`<testcase`=20、`<failure`=0）＋`ird-test-report.json`（ird-test-report/1：targets/gates/selfCheck/testSuites 四段齐全，20 用例↔acceptance↔§11.2 追溯完整）。
- **按名引用闭环**：ird-test-report.json gates/selfCheck 字段引用的 5 份日志全部在库；attempt 1 阻断（引用存在而文件不在库）已消除。
- **内容交叉一致**：integration-build.log（`: error` 计数 0）、integration/smoke-test-run.log（20/20）、validate-task.log（PASS）、verify-task.log（契约命令逐字＋PASS）均与验收者复现一致；gates 日志的载体弱化见 F-186（结论本身经实跑证实）。
- phase-one-validation.log／resultNote：不适用（与 IO-T01 同口径——phase-one 时代字段；功能等价物＝io.md §15.5 v0.4 行＋ird-test-report.json，均已更新在库）。

## 4.11 偷懒/缩水实现扫描 —— 通过（①②④零命中；③逐条证据表见 4.4）

- ① `git diff base..head | grep "^+" | grep -inE "TODO|FIXME|stub|placeholder|未实现|not implemented|待实现|XXX|HACK"` 唯一命中＝SafePath.cpp L184 注释 `// 后续字节不是 10xxxxxx——非法序列` 中 `10xxxxxx` 的 "xxx" 字样——UTF-8 连续字节记法的**假阳性**，非未完成标记；真实命中零。契约无"触发式延后"标注项；`IoDiagnostics.hpp` 文件头注明"逐行列定位助手随首个消费者（CSV 通道 IO-T03）落位，不预建无消费者能力"——§3.1 卡面既定落位边界声明，非缩水。
- ② 空实现检测：无——全部公共方法（resolver 两方法、guard 五方法、mutex 四方法、token/描述符/注册/三要素函数）均有实质实现（三个实现文件全读证实）；`Io.cpp` 保持 IO-T01 锚点翻译单元（契约既定形态，非空实现）。
- ③ 逐条证据表：见 4.4（5/5 条均有具名测试＋文件行号＋双模式复现结果＋卡面权威值比对；任一条无证据即 fail——本任务无此情形）。
- ④ 测试强度：见 4.5（边界/饱和/除零式逃逸/注册冲突/最小序号定码/真实 reparse point 断言＋两枚失效注入实证会红）。

## 发现的问题清单

| # | 级别 | 问题 | 依据 | 处置 |
| --- | --- | --- | --- | --- |
| S-5 | 建议 | 在库 gates-ird-gates-target.log 仅 3 行尾部片段（GBK 输出直捕进 UTF-8 文件产生乱码、无 IRD-GATE 命中明细行）——report 按名引用的留痕载体不足以自证明细（结论经本验收独立实跑证实为真：11 处基线零 io） | 4.8/4.10 节 | 转登 **F-186**（新）；后续任务门禁留痕含明细行/转码，归实施侧自觉＋governance 脚本侧规范化候选 |
| S-4' | 建议（沿袭） | eaa6aa99 标题名词短语开头（F-012/F-179/F-185 家族）——已推送提交不改写 | 4.9 节 | F-185 保持 open（本次移植登记）；T-IMPL 模板根治归 governance |
| O-1 | 所有者环节 | P-IO-6 后半"与 diagnostics 所有者收编确认"：实现侧已产出注册面＋真实注册表验证＋码值逐字 §9.12（本验收逐族比对证实）；IO-* 码进 diagnostics §4.6 内置装配清单属其所有者治理动作 | 4.4⑤ | 随本验收提请所有者裁决（登记 DTB §4）；不阻塞合入 |
| —— | 沿袭登记 | F-008（全新树全量构建基线失败）本验收第六次复现（RWSimulatorPlugin 71＋bt_plugin 2 处错误，io 零关联）——既有 open 登记不重复转登 | 4.2 节 | 归构建约定所有者（SA-02 patches 流程）；本任务零框架触碰不担责 |

## 验收者环境说明（复现要据）

- 复现命令序列（worktree A 内，除注明外）：①`git worktree add --detach D:/10_Source_Repos/21_robot/_acc_io_t02_tmp/wt-a 8c61896d`；②补拷 `RobWorkStudio.ini.template.static`／`.ini.shared.in`（.gitignore `[Bb]in/` 环境缺口）；③`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64 -DCMAKE_TOOLCHAIN_FILE=D:/10_Source_Repos/21_robot/RobWork/vcpkg/scripts/buildsystems/vcpkg.cmake`（exit 0，缓存 `:BOOL=ON` 实测）；④`cmake --build build --config Release`（exit 1——F-008 基线插件，io 零关联）＋`--target sdurws_ird_io_test`（exit 0 零 error）；⑤冒烟：`cmake -S RobWork/RobWorkStudio/src/rwslibs/industrialrobot -B ../smoke -DCMAKE_TOOLCHAIN_FILE=<主仓>/vcpkg/...`＋`--build --config Release`（exit 0 零 error）；⑥双模式测试 exe 直跑（各 20/20，gtest XML 留验收临时目录）；⑦`--target ird_gates` 实跑（exit 1＝SA-10 基线口径，去重真实命中 11 处全基线、零 io）；⑧validate-task／verify-task（双 PASS）；⑨两枚失效注入（Budget.cpp satAdd／SafePath.cpp foldCase）即红＋逐字节还原（`git status`/`git diff HEAD` 空）＋20/20 恢复。
- 验收者自身过程日志留存于 `_acc_io_t02_tmp/logs/`（临时目录，按协议不随记录入库）；关键输出已逐字摘引于本记录各节。
- 独立性：全新上下文；只读代码、契约、设计文档、在库留痕与先行验收记录（attempt 1 记录消费仅限核对返工闭环与 findings 移植——其结论均未采信，全部本人复现）；未读取实施者任何过程说明。
