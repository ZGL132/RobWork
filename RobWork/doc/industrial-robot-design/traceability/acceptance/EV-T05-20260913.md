# EV-T05 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | EV-T05 实现证据 Profile 与证明契约承载（unit=evidence，≙WP-05-T05） |
| 送验对象（冻结） | 203c29458b4b68bd9ef5197a61fe36edb62b005a（分支 wp05-t05 尖端；验收按 SHA 不按分支名） |
| diff 范围 | 56fd5d86e83c10fcd9e10dee60e1a7832fff1c0c..203c29458b4b68bd9ef5197a61fe36edb62b005a（3 提交，20 文件，+4638/−12） |
| 契约 | tasks/foundation/EV-T05.json（status=ready；verify 数组 1 条） |
| 验收日期 | 2026-09-13 |
| **verdict** | **PASS**（4.1～4.11 全部通过，无阻断级问题；建议级 1 条转登 findings.json F-062） |
| 验收者环境 | 全新上下文（PIPE §0.4 派发模式，无独立性降级）；复现现场＝detached worktree @ 203c2945（仓库根下 tmp-acc-ev-t05-a，验收后删除）；记录分支 worktree＝acc/EV-T05/1 @ origin/redesign-main（tmp-acc-ev-t05-b）；集成构建参数与主仓 build 树等价（vcpkg toolchain 绝对路径＋Qt 前缀＋RWS_BUILD_INDUSTRIALROBOT=ON，CMakeCache 实测 `:BOOL=ON`）；冒烟＝独立临时树＋toolchain。门禁/治理脚本回主仓只读运行（HEAD 恰为 203c2945，与送验对象一致；主仓工作树 state.json 未提交修改为编排者专属，未触碰） |

**独立性声明**：本验收为全新会话，只消费任务契约、设计文档、送验 diff 与留痕产物；未接触实施者过程叙述；构建与测试全部亲手重跑（复现优先于采信——实施者留痕仅作交叉比对基准）；全程以"为不通过找证据"的对抗立场执行，含双探针失败能力注入。

---

## 1. 逐项证据表（acceptance-protocol §4，4.1～4.11）

| # | 检查项 | 结论 | 证据 |
| --- | --- | --- | --- |
| 4.1 | 改动范围 | **通过** | `git diff --name-status 56fd5d86..203c2945`＝20 文件：4 个 evidence 单元文件（CMakeLists.txt 修改、Evidence.hpp/EvidenceTest.cpp 新增、Evidence.cpp 重写）⊂ 契约 allowedFiles `evidence/**`；15 个留痕文件 ⊂ `traceability/**`；units/evidence.md 在 allowedFiles 明列。forbiddenFiles（REQUIREMENTS.md/ARCHITECTURE.md）零触碰（diff 中无）；范围外文件 grep 反证＝"（无范围外文件）"；patches/ 无新文件（SA-02 无登记义务产生） |
| 4.2 | 可复现构建 | **通过** | 集成：worktree A 重新配置（configure exit=0、0 CMake Error，缓存实测 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`），`cmake --build build --config Release --target sdurws_ird_evidence_test -j` exit=0、`grep -cE "error C\|error LNK\|fatal error"`＝0。冒烟：独立树 tmp-acc-ev-t05-smoke（toolchain 绝对路径）configure exit=0＋build exit=0、0 error（验收日志 /tmp/acc-ev-t05-integration-configure.log、/tmp/acc-ev-t05-smoke-build.log，验收后随临时目录清除，结论以本行文字与实测数据留档）。环境前置说明见 §4 F-062：worktree 集成配置需补主树本地两件（toolchain/Qt 参数、bin/ini 模板资源——git 未跟踪），属已知环境缺口非实施缺陷 |
| 4.3 | 契约 verify 命令 | **通过** | 契约 verify 数组仅 1 条：`cmake --build build --config Release --target sdurws_ird_evidence_test`——亲手执行 exit=0 零 error（同 4.2 集成构建） |
| 4.4 | acceptance 条目 | **通过** | 逐条证据见 §2 表；ird_gates **已执行**（WP-01-T01 已落地，无需"门禁未建成"标注）：主仓复跑 exit=1＝3 处命中全部为既有（IRD-GATE-SUB runtime_test→testkit＝F-051；IRD-GATE-SUB testdata_lint→testkit＝F-019；IRD-GATE-T1 testdata_lint 链 testkit＝F-019），与送验留痕 ird-gates.log 逐项一致，evidence 变更面零新增命中 |
| 4.5 | 测试真实失败能力 | **通过** | 双探针（worktree A 注入、未提交、逐字还原）：①`validateProof` CollisionPairsMissing 检查改 `if (false && …)` → `EvidenceProof.CollisionPairsCounterexamplesRejected` FAILED（run exit=1）；②`validateCaseCoverageMatrix` 重复检查改 `if (false && …)` → `EvidenceCoverageMatrix.DuplicateAndUnknownReferenceRejected_EV_COV_2` FAILED（同次运行 2 failed / 0 passed）。还原后（`git checkout` + `grep -c 验收探针`＝0 零残留）重建复绿 83/83（restore build exit=0 / run exit=0）。两断言均非恒真 |
| 4.6 | 架构红线（八条） | **通过** | ①零 Qt：新增/变更 3 文件 grep Qt/Q_OBJECT/QString 零命中；②R-1：`target_link_libraries(sdurws_ird_evidence PUBLIC sdurws_ird_core)` 唯一链接边（evidence/CMakeLists.txt:54），无业务域互链；③R-2：include 面仅 `sdurws/ird/core/*`＋自单元公共头 `sdurws/ird/evidence/*`（include/ 下公共头，无私有头跨单元）；④R-4：无 RobWork 名称前缀拼接/剥离代码；⑤PA-1：权限归属正确——本交付只做"证据合不合格"校验，汇总裁定显式留给 EV-T06（Evidence.hpp 头注释与各校验器注释声明），诊断码权威注记归 diagnostics StableCodeRegistry（PA-1 注释留痕 Evidence.hpp:409-411）；⑥PA-2：未涉修订写路径；⑦SA-02：框架源码零改动（4.1 已证）；⑧无 old/ 历史实现恢复（全量新写） |
| 4.7 | 注释规范 | **通过** | 公共头全查：Evidence.hpp（1225 行）文件头（设计依据逐条目/背景说明/线程安全/实现形态）＋每个枚举/结构/函数 Doxygen 中文齐全，§2.5 高危信息：线程约束（纯值/可重入纯函数逐处标注）、错误语义（非抛出 try* 分轨＋issue 码表尾追加纪律）、确定性来源（检查序固定＝枚举声明序，NFR-COR-02 逐处标注）、所有权（IProducerRegistryView 调用方持有不接管，Evidence.hpp:785-786）。实现文件走读：Evidence.cpp 文件头（IRDPRF1 字节布局表）＋语义段注释（溢出安全求和的理由、presence 字节"缺失≠空"、检查序逐组锚点）。测试文件：EvidenceTest.cpp 文件头（范围说明＋EV-REG-3 替身边界声明）＋用例中文断言消息（含需求/AT 追溯）。CMakeLists.txt 中文注释块（diff 可见目标注册与源清单说明）。无复述式/过期/占位注释 |
| 4.8 | 文档同步 | **通过** | units/evidence.md 卡头 v0.5→v0.6＋§15.4 v0.6 行：12 项实现口径 I-1～I-12 逐条与代码核对一致（I-3 CollisionPair 正判定=代码 Evidence.cpp:519 `!pair.inCollision` 拒绝；I-4 两常量 kCoverageClaim*＝§6.3 原文短语；I-5 validateProof 第 4 参 expectedSliceId 无默认；I-6 IProducerRegistryView 最小注入端口；I-8 isValidProfileItemId 两段 kebab；I-9 IRDPRF1 非往返投影；I-10 NotApplicable 不计缺失；I-11 矩阵非法/漏验两面分离＝matrixLegal 与 coverageComplete 独立；I-12 CaseCoverageEntry.notApplicableReason 载体）。validate-task PASS（EV-T05.json）＋validate-docs PASS（20 units/12 trace/150 task files，主仓实测）。findings.json 未关闭条目相关性核对见 §3 |
| 4.9 | commit 质量 | **通过** | 3 提交均 `[WP-05-T05]`＋动词开头（实现/登记/登记）——F-048/F-059 家族形态零再现；正文四段（What/Why/验证/影响面）逐份齐备（git log -B 全文核对）；验证结论与验收者亲手复现逐项一致（双模式 83/83、verify exit=0、ird_gates 3 处既有命中、探针红/绿、validate-task PASS）——无粉饰 |
| 4.10 | 留痕完整 | **通过** | traceability/builds/wp05-t05/ 15 件全部入库：双模式 gtest XML（integration tests=83 failures=0；smoke tests=83 failures=0——与验收者实测 XML 逐项一致）；ird-test-report.json（schema ird-test-report/1；summary 83/83 两模式；逐用例 acceptance 追溯；irdGates 字段按 F-060 教训逐命中编号归属 F-019×2＋F-051×1；notExecuted 空）；integration-cache-check.txt（BOOL=ON）；构建/配置/verify/门禁/validate-task 日志；探针三件（red-build/red-run/restore-build）；`git add -f` 对抗 .gitignore 吞 .log（CORE-T02 教训，提交正文声明）。验收判据按"builds/ 留痕即 resultNote"口径（EV-T04 先例一致；phase-one-validation.log 为 CORE-T01 时代聚合载体已由 per-task builds/ 目录替代） |
| 4.11 | 偷懒/缩水扫描 | **通过** | ①diff 新增行 grep `TODO\|FIXME\|stub\|placeholder\|未实现\|not implemented` **零命中**；②空实现检测：Evidence.cpp 768 行逐行走读——全部 9 个公共函数有完整实现（两词形闸门为契约头 inline 完整实现），无空体/仅返回默认值的接口；③acceptance 逐条证据表见 §2，四条全有具名测试＋验收者实测命令输出；④测试强度：断言校验行为与边界（16 个 ProofIssueCode 反例全覆盖＋issue 下标定位＋检查序锚定＋溢出安全求和的 u64 边界＋零样本/降级标记边界），4.5 双探针证明非恒真 |

## 2. 契约 acceptance 逐条证据表

| acceptance 条目 | 结论 | 证据 |
| --- | --- | --- |
| ① EV-VER-6/7、EV-COV-2/3 用例通过 | **通过** | 具名用例（EvidenceTest.cpp 行号）：`MandatoryStateProofValid_EV_VER_6`:258＋`MandatoryStateProofMissingBasisRejected_EV_VER_6`:294（EV-VER-6 正反半边）；`NotApplicableNotCountedMissing_EV_VER_7`:604（EV-VER-7：NotApplicable 不计缺失＋Missing/Unverified 进缺失清单且状态区别保留＋无清单全量列出）；`DuplicateAndUnknownReferenceRejected_EV_COV_2`:889（EV-COV-2：重复/错误引用拒绝＋逐条目下标与 caseId 定位）；`ZeroSampleUndefined_EV_COV_3`:1027＋`DenominatorCompletenessAndDowngrade_EV_COV_3`:1056（EV-COV-3：零样本不定义＋分母完整性 90≠100 拒绝＋降级标记强制）。全部在验收者双模式 83/83 实测中通过 |
| ② validateProof 逐字段反例全部拒绝 | **通过** | ProofIssueCode 全部 16 值反例覆盖（8 个用例）：CategoryInvalid:311、BoundExpressionMissing:327、ContradictionExpressionMissing:347、MandatoryStateKindMissing/MandatoryStateObjectInvalid:363、MandatoryStateBasisMissing:294、CollisionPairsMissing/CollisionPairInvalid（保留值/A==B/否定判定）:383、ProducerKeyInvalid/ProducerNotRegistered/ContractVersionMismatch:417、SnapshotIdMissing/SnapshotIdMismatch/SliceIdMissing/SliceIdMismatch:443、CoverageClaimInvalid（跨类错配/自拟措辞/空声明）:475。每反例断言具体问题码而非仅非空 |
| ③ Profile 明细归需求 §8.1 表 4，不复制双账本（D-14） | **通过** | `commonRequiredItems()`（Evidence.cpp:157-186）仅内建三条通用必需项（common.snapshot-identity/case-coverage-matrix/mode-evidence-grade）；产品代码域明细 grep 反证：Evidence.hpp/Evidence.cpp 中 "kin.|trj." 命中 4 处全部为注释中的 §6.1 设计原文示例引用，零代码级域 Profile 实例；测试内 trj Profile 为域侧实例化模拟（设计要求的用法，非 evidence 内建）；具名用例 `CommonRequiredItemsMatchTable4GenericRows_D14`:729 钉住三条通用项内容与 C6 替代标志 false |
| ④ O-14 保守字面（覆盖矩阵分母＝快照冻结态必验工况集，schema 权威归 WP-14-T01） | **通过** | `validateCaseCoverageMatrix` 漏验核对（Evidence.cpp:677-703）分母严格取 `snapshot.caseSet.entries` 的 `enabled && mandatory` 冻结标记，不解释标记语义（注释明示 O-14 口径）；对照面在夹具 `standardCases()`（case-c-optional enabled 但非 mandatory，不进分母）；空必验集平凡完备 `EmptyRequiredSetTriviallyComplete_P_EV_7`:1008；单元卡 v0.6 §15.4 已按 DTB §5.4 登记该处置 |

## 3. findings.json 未关闭条目相关性核对（4.8，47 项 open 逐类结论）

| 条目 | 相关性结论 |
| --- | --- |
| F-019（testdata_lint 门禁边红×2） | **相关（门禁基线）**：本次主仓复跑实测命中清单含其 2 处，与送验留痕一致，零新增；仍 open 属治理侧（修复动作在 TK-T07 allowedFiles 之外），本任务无法也不应处置 |
| F-020（evidence.md §3.5 引用悬空） | **相关（文档面既有）**：本任务 diff 未触碰 §3.5 相关章节（仅卡头版本行＋§15.4 一行）；错误码内容本任务未改动（EvidenceErrorCode 表零变更），不随本任务消账，仍 open 待所有者裁决 |
| F-032（planned 契约锚点不强制） | 无关：治理脚本缺口，与本任务 ready 契约无涉 |
| F-043（RT-T06 重力测试敏感度） | 无关：runtime 域测试覆盖建议 |
| F-046（Snapshot.cpp 风格瑕疵） | **同单元但无关**：Snapshot.cpp 本任务零改动（diff 证实）；不构成对实施产物的验收障碍，仍 open |
| F-048/F-059（提交标题名词短语家族第 17/18/19 例） | **相关（已消除再现）**：本任务 3 提交均动词开头合规，家族零新增（4.9） |
| F-051（ird_gates 引擎与白名单矛盾） | **相关（门禁基线）**：本次复跑实测命中 1 处（runtime_test→testkit），与送验留痕一致；仍 open 归门禁引擎所有者 |
| F-055（冒烟留痕未声明构建配置） | **相关（已消除再现）**：本任务留痕显式标注 Release/全新树（ird-test-report.json targets 字段"两模式"＋提交正文"全新独立树 --clean-first"），纪律已按教训执行 |
| F-056（建议码裁决待所有者） | 无关：EV-T02 遗留裁决项，本任务未新增/改动任何 EvidenceErrorCode 值 |
| F-058（evidence.md AI 执行就绪节转义损毁） | **相关（既有未消账）**：该节本任务零改动（diff 反证）；属历史损毁非本任务引入，未顺手修复（修复超出本任务 allowedFiles 语义边界——单元卡正文仅 §15.4/卡头登记），仍 open |
| F-060（ird-test-report 门禁编号引用失准） | **相关（已消除再现）**：本任务 ird-test-report.json irdGates 字段逐命中编号归属（F-051×1＋F-019×2），提交正文明示"F-060 教训不笼统归并" |
| F-061（runtime 侧注释失实） | 无关：runtime/test 注释面，非本任务辖域 |
| 其余 35 项 open（F-004/005/006/007/008/009/010/012/015~018/023~031/033~036/038~042/044/045/047） | 逐项过筛：均属框架既有基线（F-008 全树构建缺陷）、治理脚本边界（F-004 gate-all 非主树）、历史提交风格家族（F-012 系）、以往任务留痕口径（F-023~047 家族），与 EV-T05 diff 无接触点；本任务零新增同类命中 |

## 4. 问题清单

**阻断级**：无。

**建议级（1 条，已按 ACC §5 转登 findings.json F-062，随本记录分支提交）**：

- **F-062（worktree/全新树集成构建环境前置未成文——F-004 同族扩展）**：AGENTS §4.1 登记的集成模式配置命令在非主树（验收 worktree 等）直接执行会失败于两件主树本地状态：①缺 `-DCMAKE_TOOLCHAIN_FILE=<vcpkg 绝对路径>` 与 `-DCMAKE_PREFIX_PATH=<Qt 路径>`（全新缓存下 Boost 查找失败——本次实测 configure exit=1 于 `Could NOT find Boost`；主仓 build/CMakeCache.txt 实证该两参数为主树实际配置）；②`RobWork/RobWorkStudio/bin/RobWorkStudio.ini.template.static`／`RobWorkStudio.ini.shared.in` 为 git 未跟踪的框架本地资源（`RWS_USE_STATIC_LINK_PLUGINS=ON` 时 configure_file 必需输入），worktree 检出后缺失即 CMake Error（本次实测）。补齐两件后 configure/build 全部零错误。建议：在 AGENTS §4.1 或 ACC §3 补一段"worktree 复现口径"（等价配置参数＋本地资源清单），或由治理侧将两 ini 资源纳入版本管理/生成机制。责任方：流程文档维护者＋框架构建配置所有者。severity=suggestion，status=open。

**核验过但判定不构成问题的观察**（留档防重复审查）：

- `validateProof` 的 CategoryInvalid 采用防御性范围比较（枚举越界值仅能经非法转换到达），测试以 `static_cast<ProofCategory>(3)` 覆盖——合法防御面，无缩水。
- `UnknownCaseReference` 归入"矩阵非法"类（matrixLegal=false）而 §6.6 原文措辞为条目"Invalid"语义——实现口径 I-11 已按 DTB §5.4 在单元卡 v0.6 登记（矩阵非法/漏验两面分离），登记在案即合规。
- `MissingMandatoryExecution` 的 issue.index 取 npos（caseId 有值）——漏验问题指向快照工况而非矩阵条目下标，npos 语义与契约头注释自洽。

## 5. 验收者实测数据汇总（复现优先于采信）

| 项目 | 验收者实测 | 送验留痕 | 一致性 |
| --- | --- | --- | --- |
| 集成 verify 目标构建 | exit=0，0 error | integration-verify.log exit 0 | 一致 |
| 集成 evidence 测试 | 83/83 PASSED（exit=0） | integration-test-run.log 83/83 | 一致 |
| 冒烟配置＋构建 | exit=0，0 error | smoke-build.log exit 0 | 一致 |
| 冒烟 evidence 测试 | 83/83 PASSED（exit=0） | smoke-test-run.log 83/83 | 一致 |
| gtest XML 计数 | 两模式 tests=83 failures=0 | XML 同值 | 一致 |
| 双探针 | 2 用例即红（exit=1）→还原复绿 83/83、零残留 | probe-red-run.log/probe-restore-build.log | 一致 |
| ird_gates | exit=1＝3 处既有命中（F-019×2＋F-051×1），evidence 面零命中 | ird-gates.log 14/15 | 一致 |
| validate-task / validate-docs | PASS / PASS | validate-task.log PASS | 一致 |
| 偷懒扫描 | diff 新增行零命中 | —（协议项目） | — |
