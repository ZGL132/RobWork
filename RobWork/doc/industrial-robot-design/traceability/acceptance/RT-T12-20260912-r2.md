# RT-T12 验收记录（attempt 2）

| 字段 | 值 |
| --- | --- |
| 任务 ID | RT-T12（runtime 测试替身与契约套件——WP-06-T12，unit=runtime） |
| 契约 | tasks/foundation/RT-T12.json（status=ready，branch=wp06-t12，verify＝`cmake --build build --config Release --target sdurws_ird_runtime_test`；acceptance 4 条；knownPitfalls P-RT-1；interUnit=true） |
| 送验对象（冻结） | `c044f10fc79078a6c9f1b10fe0bab1b387a6caf1`（分支 wp06-t12 尖端；base=`ba61f3db313c939de93db52010a2ba9f39b63a23`＝origin/redesign-main 尖端＝RT-T11 合入收尾提交；`git merge-base --is-ancestor` 实证 base 即 diff 起点；diffstat＝44 文件 +9362/−17） |
| 分支漂移检测 | 验收开始时与记录撰写前各做一次 `git fetch origin` ＋ `git rev-parse origin/wp06-t12`：两次均＝`c044f10fc79078a6c9f1b10fe0bab1b387a6caf1`（与送验 SHA 一致——ACC §3 冻结口径满足，无漂移） |
| 前次验收（返工输入） | attempt 1 verdict＝**fail**（阻断 B-1：黄金数据集字节完整性在 fresh 检出形态失配），不可变记录冻结于 evidence 分支 `acc/RT-T12/1` 提交 `18d4bd4bbb235105f04407cd9c083331aa9c4555` 的 `traceability/acceptance/RT-T12-20260912.md`。本轮仅把该记录当作**返工输入**用于核对 B-1 修复是否逐项成立；裁决对象仍是 `c044f10f` 的产物全貌，4.1～4.11 **全项重新出证**（ACC §5 fail→全量重验、不增量验收） |
| **verdict** | **pass**（4.1～4.11 全部通过、零阻断级问题；attempt 1 唯一阻断 B-1 在规定复现环境下实证关闭；无新增建议级发现——findings.json 无顺延登记，F-052/F-053/F-054 的 resolution 回填建议归合入段治理提交，见问题清单） |
| 独立性声明 | 验收者为全新上下文会话（PIPE §0.4 子代理派发模式），仅消费任务契约、设计文档、送验提交产物与留痕文件，未读取实施者任何过程叙述；attempt 1 记录仅作为阻断修复核对清单使用，不作为本轮任何检查项的通过依据。构建、测试、探针、门禁、字节核验全部由验收者亲手在独立 worktree 重跑，复现优先于采信（ACC §2 四条逐条满足，无需降级声明）。 |
| 验收方式 | 独立 worktree（detached @ 送验 SHA）亲手重配重跑双模式构建、契约 verify 命令、集成/冒烟测试、ird_gates 门禁与治理脚本；字节级核验＝6/6 manifest integrity 条目的 size＋SHA-256 实测＋blob↔检出 `cmp` 逐字节比对＋`tr -dc '\r'` CR 计数；动态探针＝2 处实现变异（Codec.cpp 身份域重新纳入 pathHint、BaseWorldTransform 逆平移符号）→精确变红→还原复绿＋1 处数据资产负向探针（model.json 人为 CRLF 化→lint 变红→还原）；静态审查＝GoldenDataLintTest.cpp（158 行）与 RuntimeTestDoubles.hpp（539 行）全文通读、ContractSuiteTest.cpp 文件头＋助手段＋5 个用例体抽查、test/README.md 全文、testdata/.gitattributes 全文、Codec.cpp/Snapshot.cpp/runtime/CMakeLists.txt 全部 diff hunk、8 个提交全文。 |
| 验收环境 | Windows 10 x64（10.0.26200）/ Git Bash / VS2022（MSVC 19.42.34433，SDK 10.0.22621）/ Qt 6.11.1（CMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64）/ vcpkg 经典模式（toolchain 绝对路径 D:/10_Source_Repos/21_robot/RobWork/vcpkg/scripts/buildsystems/vcpkg.cmake）/ 仓库本地 core.autocrlf=true（global 未设置——与 attempt 1 记录一致）/ pwsh 7（治理脚本）/ node（仅用于 JSON 解析自检）。复现现场＝`C:\Users\zgl18\AppData\Local\Temp\ird-acc-rt-t12-r2-a`（detached worktree @ c044f10f，集成树 build/ 全新配置；冒烟树＝`%TEMP%\ird-acc-rt-t12-r2-smoke` 独立从零目录），验收日志 15 件在其根下，验收后随 worktree 删除——本记录为准一证据载体；记录回传＝`%TEMP%\ird-acc-rt-t12-r2-b`（分支 acc/RT-T12/2，基于 origin/redesign-main）。 |
| 环境前置说明（复现者须知，均为既有环境链问题、非本任务引入） | ①全新集成树配置需主仓库同款参数（toolchain＋Qt 前缀——F-007 既有），且 `RobWork/RobWorkStudio/bin/RobWorkStudio.ini.template.static` 被 .gitignore 吞掉、目标目录 `bin/` 整体不入 checkout，须先 `mkdir -p` 再从主仓库手工复制（F-007 再次实证）。②从零集成树全量构建在 F-008 基线缺陷处失败（本轮实测 73 处 error＝RWSimulatorPlugin.vcxproj 71＋bt_plugin.rwplugin.vcxproj 2，ird 目标零命中）——与 F-036/F-038 及 RT-T02/T03/T06/T11 与 attempt 1 判例逐字同型；任务目标面从零构建（同契约 verify 命令）exit 0。③手工运行集成测试 exe 需把主库/RobWorkSim/RobWorkStudio 的 `bin/Release` 目录加入 PATH，否则缺 DLL。④本任务新增的 testdata 行尾钉死（`.gitattributes`）使 fresh checkout 不再发生 attempt 1 的 LF→CRLF 膨胀——本轮按 ACC §3 规定复现路径（`git worktree add --detach`）实测，见 4.3/4.4。 |

---

## 4.1 改动范围 —— 通过

- base..head 共 8 个提交：`97455085`（修复 RT-RES-3 路径不作身份）、`2ed1c558`（数据集交付）、`324ad96c`（替身＋契约套件＋lint）、`d8bcd441`（RT-STUB-0 声明＋v0.13 登记＋findings F-049~F-051）、`ff4b943c`（attempt 1 留痕）、`e59d3972`（**B-1 修复：testdata/.gitattributes＋F-053 注释更正**）、`a1744141`（runtime.md v0.14 登记）、`c044f10f`（attempt 2 留痕）；`git diff --name-status ba61f3db..c044f10f` 共 44 个文件、+9362/−17：
  - `runtime/**`（allowedFiles 第 1 条）：CMakeLists.txt（M）、src/Codec.cpp（M，RT-RES-3 修正）、src/Snapshot.cpp（M，同）、test/ContractSuiteTest.cpp（A）、test/GoldenDataLintTest.cpp（A）、test/RuntimeTestDoubles.hpp（A）、test/README.md（A）——7/7；
  - `testdata/**`（allowedFiles 第 2 条）：**.gitattributes（A，本轮新增）**＋golden/rt-fk-equation/1.0.0/ 4 件＋golden/rt-namemap-roundtrip/1.0.0/ 4 件＋tolerance/rt-runtime/v1.0.0.json——10/10；
  - `traceability/**`（allowedFiles 第 5 条）：builds/wp06-t12/ 留痕 25 件＋findings.json（M，F-049/F-050/F-051）——26/26；
  - `units/runtime.md`（allowedFiles 第 4 条，M，卡头 v0.13→v0.14＋§15.4 两行条目）。
- forbiddenFiles（REQUIREMENTS.md / ARCHITECTURE.md）：diff 中零出现。
- SA-02：diff 无任何框架源码文件；`industrialrobot/patches/` 树内仅既有 2 件（0001-rwslibs-entry-option.patch、PATCHES.md），零新增登记。
- 本任务零公共头改动（include/ 无 diff）——契约面零变化；产品实现仅 Codec.cpp/Snapshot.cpp 两处身份域修正。

## 4.2 可复现构建 —— 通过（集成按 F-008 闭合前"任务目标面从零"判例口径）

- **集成模式**（worktree A 全新配置）：`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=<vcpkg 绝对路径> -DCMAKE_PREFIX_PATH=<Qt6>` → 配置成功；缓存 grep 实证 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`（integration-acc-build.log 内 "=== cache check ===" 段）。全树 `cmake --build build --config Release -j` → exit 1，**73 处 error 全部**归属 RWSimulatorPlugin.vcxproj（71）＋bt_plugin.rwplugin.vcxproj（2）（RobWorkSim 域，F-008 基线缺陷；`: error `行按 vcxproj 归类统计，industrialrobot 域命中 0）——**任务目标面从零**：契约 verify 命令 `cmake --build build --config Release --target sdurws_ird_runtime_test` → **exit 0、`: error ` 计数 0**（verify-target-build.log）。
- **冒烟模式**（独立从零目录 `%TEMP%\ird-acc-rt-t12-r2-smoke`，带 vcpkg toolchain 绝对路径）：配置 exit 0、构建 exit 0、错误计数 0（smoke-acc-build.log；唯一 warning＝MSB8029 临时目录增量提示，环境性质，与 attempt 1 记载一致）。
- 构建面结论：两模式零错误（集成按判例口径）；实施侧 integration-rework-build.log 显式标注"主树增量树"（F-054 标注纪律已落地，见 4.9）。

## 4.3 契约 verify 命令与测试执行 —— 通过（fresh 检出形态下全绿，B-1 观测面复绿）

- 契约 verify 数组仅 1 条：`cmake --build build --config Release --target sdurws_ird_runtime_test`——验收者在 worktree A 逐字亲手执行（从零构建）**exit 0**。
- 其后亲手运行测试可执行（**本 worktree 即 ACC §3 规定复现路径 `git worktree add --detach` 的 fresh 检出形态**——attempt 1 B-1 的复现现场同构）：
  - 集成模式：**261 例＝260 PASSED＋1 SKIPPED＋0 FAILED**（exit 0；SKIPPED＝ContractSuite.ChildProcessWorker_RT_ID_1，按设计仅子进程执行——父用例 CrossProcessIdentityStable_RT_ID_1 实际派生子进程并以 141 ms 通过，占位跳过非缩水）；attempt 1 曾红的 5 个用例（ContractSuite.GoldenFkEquationDataset_RT_EQ_1、ContractSuite.GoldenNameMapRoundtripDataset_RT_NM_1、GoldenDataLint 3 例装载用例）本轮**全部 OK**（acc-integration-test-run.log、acc-integration-runtime-test.xml：`<testsuites tests="261" failures="0" ...>`）。
  - 冒烟模式：**176 例＝176 PASSED＋0 FAILED**（exit 0；acc-smoke-test-run.log、acc-smoke-runtime-test.xml），含 4 个 GoldenDataLint 用例——冒烟同样消费 worktree A 的 fresh 检出 testdata。
- **B-1 修复的字节级独立实证（验收者实测，不采信实施侧日志）**：
  - `git check-attr text eol`：testdata 下 json/mjs（含 requirements-ids.json）全部 `text: set`＋`eol: lf`；
  - 检出字节：model.json=1812、robot.json=1170（＝manifest 申报；attempt 1 时为 1867/1207）；两 manifest 的 **6/6 integrity 条目**（inputs/expected/generate ×2 数据集）size＋SHA-256 逐条实测全部匹配；
  - `git cat-file -p <blob> | cmp - <检出文件>` 逐字节一致；`tr -dc '\r' | wc -c`＝0（零 CR 字节）。
- 结论：attempt 1 阻断 B-1 在规定复现环境（fresh worktree 检出）下**已关闭**——关闭核对详见附录 A。

## 4.4 acceptance 条目逐项 —— 全部通过

| 契约 acceptance | 证据 | 结果 |
| --- | --- | --- |
| ① §11 验证矩阵逐条通过并留痕（AT-16/AT-18/AT-37 载体） | test/README.md §3 给出 §11 全表→测试文件落位（RT-T02～T12 累积）；本套件 13 用例补组合面缺口：RT-ID-1 跨进程（CrossProcessIdentityStable——父进程编译＋子进程经 gtest 过滤器再入，模型/快照/映射身份＋WC 帧名集 IRD_EXPECT_IDENTICAL，本轮实测 OK）、RT-ID-2/3、RT-BW-1/3/4/5（四消费替身 AT-37 载体）、RT-CAP-1/2、RT-RES-3、RT-EQ-1/RT-NM-1 数据集载体——**我方 261 例复现中全部通过（0 失败）**；留痕＝gtest XML×2＋run log×2＋ird-test-report.json（attempt=2 口径） | 通过 |
| ② RT-STUB-0 边界声明（替身不作 RobWork 算法正确性证明） | runtime/test/README.md §1 全文声明（替身只覆盖五个注入接口；RobWork/rwsim 本体与被测编译器/工厂不做替身；手写 FK 仅测试对照工具；未执行不得标注通过）＋RuntimeTestDoubles.hpp 文件头同款＋ContractSuiteTest.cpp 文件头复述——三处在案（本轮全文核读） | 通过 |
| ③ 数据集交付：rt-fk-equation/（analytic，附录 D 第 4 项）＋rt-namemap-roundtrip/（contract-fixture）＋容差档案 rt——lint 通过并随 CI 跑通 | 资产齐备（两 golden 数据集 inputs/expected/generate/manifest 全链＋tolerance/rt-runtime/v1.0.0.json；档案 id 以 rt-runtime 承接——F-050 偏差已按 DTB §5.4 登记）；**lint 用例（GoldenDataLint 4 例）在我方 fresh 检出形态的集成/冒烟两模式复现中全绿**；独立工具 sdurws_ird_testdata_lint 实跑零违规（实施侧 testdata-lint-run.log，字典 192 ID）；"随 CI 跑通"的可复现性由 testdata/.gitattributes 行尾钉死保障（本轮负向探针实证：行尾腐坏即红——见 4.5）。attempt 1 在本条的 fail 原因（B-1）已关闭 | 通过 |
| ④ 未执行不得标注通过 | 全部 261＋176 例由验收者亲手执行并留痕；实施侧 ird-test-report.json 为有效 UTF-8 JSON（node 解析通过；attempt=2、summary.integration 261/260/0/1＋skippedNote、summary.smoke 176/176、summary.rework 含 blocker/fix/mainTreeRerun/freshCheckoutSelfCheck/gates 五段）；实施侧 fresh 自证四件留痕（fresh-checkout-*.log）关键数值与我方独立实测一致（如 manifest-verify.log 的 6 条 SHA-256 前缀与我的 sha256sum 输出逐字相同）；无未执行标注通过的粉饰 | 通过 |

## 4.5 测试真实失败能力 —— 通过（2 处实现变异＋1 处数据负向探针，均即红→还原复绿）

| 探针 | 注入点（worktree A 临时修改，`git checkout --` 已还原，还原后 `git status` 无跟踪文件改动） | 结果 |
| --- | --- | --- |
| 探针 M1 | Codec.cpp L361 `writeResourceRef` 身份域开关 `if (!identityDomain)` 改为 `if (true)`（**反转 F-049 修复**——pathHint 重新进入身份域字节） | 过滤器重跑后 `ContractSuite.SourcePathHintIsNotIdentity_RT_RES_3` **FAILED**——证明 RT-RES-3 断言真实钉住"路径不作身份"规则 |
| 探针 M2 | BaseWorldTransform.cpp L442 `invertWorldBase` 逆平移 `* (-1.0)` 改 `* (1.0)`（−Rᵀt→＋Rᵀt） | 过滤器重跑后 `ContractSuite.NumericExampleThroughSnapshotView_RT_BW_1` **FAILED**＋既有 RT-T06 用例 `InverseAndPointTest.NumericExampleInverseRoundtrip_RT_BW_1` 同步 **FAILED**——§6.5 数值例断言非恒真且有双层防护 |
| 还原 | 两处修改 git checkout 还原→重建→同过滤器重跑 | **6/6 PASSED**（restore-run.log）——红由变异唯一导致 |
| 负向探针（数据资产面，B-1 机制敏感性） | model.json 人为 CRLF 化（1812→1867 字节，`sed -i 's/$/\r/'`） | `GoldenDataLint.*` 中恰 2 例 **FAILED**，失败消息＝attempt 1 的 B-1 签名逐字复现（"dataset-invalid: ... integrity.inputs/model.json: size 不符（期望 1812，实际 1867）"）→还原 1812 字节——完整性校验非恒真，且 `.gitattributes` 正是 fresh 检出形态全绿的决定性防护（B-1 因果链独立钉死） |

## 4.6 架构红线（AGENTS §5 八条） —— 通过

1. **L2 零 Qt**：diff 全部新增行与三个新测试文件 grep Qt 头/QString/Q_OBJECT 零命中（唯一"Qt"字样出现于既有 BuildRedLineTest 的零 Qt 扫描用例注释本身）；runtime 产品 include 面未扩大。
2. **业务域互链禁止（R-1）**：新增文件跨单元 include 仅 `<sdurws/ird/core/*>` 与 `<sdurws/ird/testkit/*>` 公共头；测试目标增链 sdurws_ird_testkit＝testkit.md §10.2 交接表 runtime 行消费面（测试侧依赖，R-1 不适用）；产品目标 sdurws_ird_runtime 链接面不变（仍 core＋L1 基线库，CMakeLists 文件末红线守卫在位）。
3. **跨单元私有头禁止（R-2）**：新增文件唯一非系统私有 include 为 `CompilerImpl.hpp`/`RuntimeTestDoubles.hpp`——均本单元内（测试与 src 同权先例，RT-T11 同款登记）。
4. **框架零源码修改（SA-02）**：diff 零框架文件、patches/ 零新增（4.1 已证）。
5. **名称前缀禁令（R-4）**：diff 无任何名称拼接/剥离；套件仅经 resolveObjectId/resolveRuntimeName 整串解析消费。
6. **权威唯一（PA-1）**：诊断断言全部经 registry 稳定码单点比对；数据集/档案只读消费，lint 走 testkit 装载单点，无第二套校验语义。
7. **不可变历史（PA-2）**：无修订/历史写路径触碰。
8. **不继承 old/**：无 old/ 引用。

**ird_gates 实跑**（worktree A 构建树 `cmake --build build --config Release --target ird_gates`）→ exit 1，命中**恰 3 项**：IRD-GATE-SUB×2（sdurws_ird_runtime_test→sdurws_ird_testkit【＝F-051 新登记】＋sdurws_ird_testdata_lint→testkit【＝F-019 既有】）＋IRD-GATE-T1×1（sdurws_ird_testdata_lint→testkit【＝F-019 既有】）；门禁自测 9 项全部按预期检出——与送验留痕（ird-gates-rework-run.log）及 attempt 1 记录逐项一致，**零未登记新增违规**。

## 4.7 注释规范 —— 通过

- **全文通读**：GoldenDataLintTest.cpp（158 行）、RuntimeTestDoubles.hpp（539 行）、test/README.md（82 行）、testdata/.gitattributes（27 行）。
- 文件头三件（@file/@brief＋设计依据编号＋背景说明）齐备且含中文"为什么"；RuntimeTestDoubles.hpp 每类/每函数 Doxygen 中文注释齐备，§2.5 高危信息全覆盖：线程约束（"非线程安全——仅测试线程访问"、CancelToggle 原子量跨线程注入）、所有权（"不拥有——调用方持有"）、确定性来源（"id/摘要由固定种子经 SHA-256 派生——跨进程字节一致"）、单位（rad/rad/s/rad/s²/kg/m 逐字段标注）、MSVC vector 校验误报的实现细节备注；ContractSuiteTest.cpp 每用例段前有矩阵依据块（IRD_TEST_INFO 需求/AT/档案三元组）＋单位与坐标系逐处标注（"单位 rad""p_world=(0.3,−0.2,1.5) m"）；GoldenDataLintTest.cpp 用例注释含需求追溯（NFR-COR-01/附录 D C4）；.gitattributes 注释完整解释 B-1 机理与 text eol=lf vs -text 的规则取舍（含覆盖面与未来二进制资产的豁免指引）。
- **修改文件**（Codec.cpp/Snapshot.cpp/runtime/CMakeLists.txt）hunk 注释完整解释"为什么"（身份域跳过 pathHint 的规则出处＋v0.13/v0.14 登记指针；cmd 子进程引号规则的 cmd 语义解释；两模式分工与 testkit 链接治理依据）。
- 公共头：本任务零公共头改动（无可查面）。
- attempt 1 建议级 S-1（F-053）的两处 rt→rt-runtime 注释残留已在 e59d3972 更正——本轮核读确认在位（ContractSuiteTest.cpp loadRtProfile 上方、GoldenDataLintTest.cpp 档案 lint 用例注释），注释与实参一致。

## 4.8 文档同步 —— 通过（含 findings 相关性逐条核对）

- **单元卡同步**：units/runtime.md v0.13（八面登记：§11 设施落位/契约套件用例清单/RT-RES-3 产品修正 F-049/数据集交付＋rt-runtime 偏差 F-050/RT-STUB-0 在案/RT-CAP-1 观测点口径/testkit 链接 F-051/执行证据）＋v0.14（返工登记：行尾钉死背景/规则取舍/自证口径、F-053 消账、F-054 标注纪律落地）——实现与任务卡的偏差全部按 DTB §5.4 登记在案，无"代码先行、文档失实"。
- **findings 登记**（送验对象 findings.json，51 条、43 open）：F-049（major，pathHint 入身份域——本任务修复，resolution 在案）、F-050（minor，档案 id 句法——rt-runtime 承接登记）、F-051（major，ird_gates 引擎与白名单矛盾——处置归 WP-01）。
- **findings.json 未关闭条目相关性核对（43 项 open 逐类过筛，本轮复验实测为准）**：
  - **F-007**（全新树配置前置）：本轮再次实证（ini 模板复制＋toolchain/Qt 前缀）——维持 open，环境链问题，与本任务 diff 无因果。
  - **F-008**（全新树全量构建失败）：本轮 73 error 同簇复现（RWSimulatorPlugin＋bt_plugin）——维持 open，非本任务引入。
  - **F-010**（.gitignore 吞 *.log）：实施侧 rework 提交以 git add -f 处置留痕入库——维持 open（治本仍缺）。
  - **F-012/F-023/F-024/F-026/F-027/F-030/F-034/F-037/F-039/F-040/F-042/F-044/F-045/F-047/F-048**（提交标题名词短语族）：本任务 8 个提交标题全部动词开头——**零复发**。
  - **F-028/F-035**（"门禁未建成"失实理由族）：本任务实跑 ird_gates 并逐项登记命中——零复发。
  - **F-015/F-016/F-017/F-018**（TK-T03 装载器偏差族）：F-018① 双前缀错误消息（"dataset-invalid: dataset-invalid:"）在我方负向探针输出中再次可见——维持 open（testkit 属他单元，非本任务可顺手消账）。
  - **F-019**（ird_gates 既有红）：本轮复现 2 项命中，口径不变——维持 open。
  - **F-032**（planned 契约锚点不检）：RT-T12 契约 status=ready，validate-task PASS——本任务未触发该缺口。
  - **F-036/F-038**（构建树性质标注族）：rework 留痕提交已显式区分"主树存量增量树/fresh detached worktree/独立从零冒烟"三类口径——F-054 纪律落地（状态回填见问题清单）。
  - **F-043**（重力投影转置方向敏感度缺口）：新增断言的重力例仍基于对称旋转 R_x(π)（R·g≡Rᵀ·g），转置方向依旧不被钉住——维持 open，建议后续以非对称旋转（如 R_y(π/2)）补一构型（沿 attempt 1 建议）。
  - **F-046**（Snapshot.cpp 风格）：本任务 diff 未触及对应区段，无变化。
  - 其余 open 项（F-004/005/006/009/020/025/029/031/033/041）与本任务无关联面。
- **追踪矩阵/契约状态**：契约 status 保持 ready（翻转归合入收尾，PIPE §4.7 同先例）；validate-docs PASS（20 units、12 trace entries、150 task files）；validate-task PASS。

## 4.9 commit 质量 —— 通过

- 8 个提交标题均 `[WP-06-T12]`＋动词开头（修复×2/交付/实现/登记×4）；正文四段（What/Why/验证/影响面）齐备；每提交带 `Acc-Covered` 行把提交映射到 acceptance 条目（超出基线要求的良好实践）。
- 验证段与留痕逐项一致且**与本验收者独立复现一致**：集成 261＝260 过＋1 设计性跳过、冒烟 176/176、ird_gates exit=1（3 命中已登记）、validate-task PASS、fresh 自证 manifest 6/6——我方全部复现吻合。
- attempt 1 建议级 S-2（F-054）已闭环：c044f10f 提交正文显式标注三类构建树性质（集成＝主树存量增量树；fresh 自证＝detached worktree 检出；冒烟＝独立从零目录）。
- 无粉饰发现；ird_gates 红如实写"exit=1 命中 3 项已登记"（诚实口径，零复发 F-028/F-035）。

## 4.10 留痕完整 —— 通过

- builds/wp06-t12/ 共 25 件：attempt 1 留痕 11 件原样保留＋rework 系列 14 件（integration-rework-build/test-run、smoke-rework-configure/build/test-run、fresh-checkout-baseline-red-run、fresh-checkout-fixed-integration-run、fresh-checkout-fixed-smoke-run、fresh-checkout-manifest-verify、fresh-checkout-negative-probe-run、ird-gates-rework-run、validate-task-rework-run、XML×3）；ird-test-report.json 更新为 attempt 2 口径（attempt/reworkNote 字段＋summary.rework 五段——JSON 有效性经 node 解析实证）；*.log 均已实际入库（git add -f，F-010 处置核实）。
- 实施侧 fresh 自证留痕与我方独立实测交叉一致：fresh-checkout-manifest-verify.log 的 6 条 declared/checkedout 字节＋SHA 前缀与我的 sha256sum/wc 输出逐字相同；fresh-checkout-baseline-red-run.log 含 11 处 [FAILED] 行（基线红对照，与我 4.5 负向探针的失败签名同型）。
- runtime.md v0.14 即本轮"实现落位＋证据指针"的文档留痕落点；phase-one-validation.log 口径沿用各任务 builds/ 目录登记（与本仓 WP-06 既有实践一致）。
- 验收者侧独立留痕（worktree A，随复现 worktree 删除、本记录为其准一载体）：integration-acc-build.log（重配＋缓存 grep＋全树构建尾部）、integration-acc-build-full.log（增量全树完整错误普查：71＋2 全归 RobWorkSim 域）、verify-target-build.log（契约 verify exit 0）、acc-integration-test-run.log＋acc-integration-runtime-test.xml（261/260/1）、acc-integration-final-run.log＋acc-integration-final.xml（还原后最终全量同结果）、mutation-build.log＋mutation-run.log（M1/M2 双红）、restore-build.log＋restore-run.log（6/6 复绿）、negative-probe-run.log（CRLF 负向探针双红）、smoke-acc-build.log＋acc-smoke-test-run.log＋acc-smoke-runtime-test.xml（176/176）、acc-ird-gates-build-run.log（3 命中）、acc-validate-task-run.log（PASS）、acc-validate-docs-run.log（PASS）。

## 4.11 偷懒/缩水实现扫描 —— 通过

1. **缩水词扫描**：diff 新增行 `TODO|FIXME|placeholder|未实现|not implemented` 零命中；`stub` 全部命中为 **RT-STUB-0**（契约登记 ID 与替身边界声明本身——契约显式语义，豁免有据）。
2. **空实现检测**：无公共接口空函数体；ContractSuite.ChildProcessWorker_RT_ID_1 的 GTEST_SKIP 为 §11 RT-ID-1 设计的子进程 Worker 入口（父用例派生时实际执行——CrossProcessIdentityStable 141 ms 通过即其行为证据），非占位缩水。
3. **acceptance 逐条证据表**：见 4.4——每条均具名用例/命令输出/文件行号，四条全部通过。
4. **测试强度**：断言校验行为与边界（跨进程身份逐字节/1e-15 位模式敏感/缓存 verdict+reasons/四消费方互等/能力位+诊断计数+subject 定位/往返逐字节）；4.5 双实现变异＋一数据负向探针实证非恒真。

---

## 附录 A：attempt 1 阻断项逐项关闭核对（B-1）

| attempt 1 阻断内容 | 返工方向建议 | 本轮关闭证据（验收者独立实测） |
| --- | --- | --- |
| 黄金数据集与容差资产以字节级完整性（sizeBytes＋SHA-256）交付文本文件，但未对 git 行尾归一化做任何防护：仓库本地 core.autocrlf=true＋上层 .gitattributes 未覆盖 json/mjs ⇒ fresh checkout（ACC §3 规定复现路径）把 testdata LF→CRLF，manifest 申报值失配，两数据集装载即抛 dataset-invalid（集成 5/261、冒烟 3/176 变红） | 首选在 allowedFiles 面内（testdata/**）新增目录级 .gitattributes 钉死行尾；修复后必须在**全新 worktree 检出形态**自证；manifest integrity 值若因重生成而变化须同步 | ①修复形态＝`testdata/.gitattributes` `* text eol=lf`（e59d3972，附完整中文规则说明）——恰为建议的首选方案，且在 allowedFiles 面内；②**fresh worktree 检出形态自证**（本轮复现现场即 `git worktree add --detach` 检出）：check-attr 全命中 text/eol=lf、6/6 integrity 条目 size＋SHA-256 实测匹配、blob↔检出 `cmp` 逐字节一致、CR 计数 0；③集成 261＝260 过＋1 设计性跳过、冒烟 176/176——**attempt 1 变红的全部 8 处观测面复绿**；④blob 内容零变化（数据集未重生成，manifest 无需变化；实施侧 `git add --renormalize` 零差异自证与 diff 面一致——testdata 下仅新增 .gitattributes）；⑤负向探针实证机制非恒真（CRLF 化即红、还原即绿）。**B-1 判定：已关闭。** |

## 问题清单

### 阻断级

无。

### 建议级（本轮无新增 findings 登记）

本轮未发现新的建议级问题，findings.json 无顺延登记（编号仍至 F-054）。以下为状态回填与维持 open 的处置注记：

| 事项 | 处置建议 | 责任方 |
| --- | --- | --- |
| F-052（major，attempt 1 登记的 B-1 根因本体，现存于 evidence 分支 acc/RT-T12/1 的 findings.json——任务分支基线早于该登记，故送验对象 findings.json 中无此条目） | 修复已随 e59d3972 落地并经本轮 attempt 2 全量复验通过——**建议合入段治理提交回填 F-052 resolution/status=fixed**（回填归合入段治理提交，验收者按分工不代置） | 合入段（所有者） |
| F-053（minor，attempt 1 建议级 S-1 注释残留） | 已随 e59d3972 更正并经本轮核读确认——**建议合入段回填 status=fixed**（runtime.md v0.14② 已登记消账落位） | 合入段（所有者） |
| F-054（minor，attempt 1 建议级 S-2 构建树性质标注纪律） | c044f10f 已显式标注三类树性质，纪律落地——**建议合入段回填 status=fixed**（runtime.md v0.14③ 已登记） | 合入段（所有者） |
| F-049/F-050/F-051（本任务登记于任务分支 findings.json，均 open＋resolution 在案） | F-049 修复已随任务落地，F-050 以 rt-runtime 承接——建议合入段一并回填；F-051 处置归 WP-01 门禁所有者，维持 open | 合入段（所有者）／WP-01 |
| F-043（重力投影转置方向敏感度） | 维持 open；建议后续以非对称旋转构型补钉（沿 attempt 1 建议，本任务新增重力断言仍为对称旋转 R_x(π)） | 后续任务 |

---

## 裁决

**verdict＝pass**：4.1～4.11 全部通过、零阻断级问题；attempt 1 唯一阻断 B-1 在 ACC §3 规定复现环境（fresh worktree 检出形态）下实证关闭（附录 A）。本验收针对送验对象 `c044f10fc79078a6c9f1b10fe0bab1b387a6caf1` 本身（按 SHA 冻结口径，分支漂移检测两次通过）。验收通过不改变合入决策归属：是否合入 `redesign-main` 由所有者依 PIPE §4.7 决策；F-052/F-053/F-054 等状态回填建议随合入段治理提交处理。
