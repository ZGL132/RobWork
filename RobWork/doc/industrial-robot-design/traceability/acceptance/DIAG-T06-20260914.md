# DIAG-T06 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 ID | DIAG-T06（≙WP-09-T03 范围设施面，diagnostics 单元：聚合器——CauseLink 原因链/聚合规则/稳定排序） |
| 契约 | `doc/industrial-robot-design/tasks/foundation/DIAG-T06.json`（status ready；branch wp09-t06；requirements ERR-01/EVI-01/NFR-COR-02；designRefs units/diagnostics.md §6.3~§6.5/§9.4；knownPitfalls P-DIAG-1；interUnit true） |
| 送验对象（冻结） | `fd3698d318117571416e2e8f69b3bcbf21143043`（验收全程复核：`origin/wp09-t06`＝该 SHA，**无分支漂移**） |
| diff 范围 | `1397654c357b06728435dc9f3c34905a5d6fe9b1`（base＝state.base＝redesign-main 尖端，与 merge-base 一致）..`fd3698d3`，共 2 提交（70df8723 实现＋fd3698d3 单元卡/留痕）、8 文件（+1781/−4） |
| verdict | **pass**（4.1~4.11 全部通过，无阻断级问题；新增建议级登记 2 项＝F-106/F-107，沿 F-081/F-103/F-104 家族既判先例不阻塞合入） |
| 验收日期 | 2026-09-14 |
| 验收者 | 独立验收会话（全新上下文；未消费实施叙述；对抗式立场——复现优先于采信，下述构建/测试/门禁/探针/脚本均为验收者亲手执行） |

---

## 一、逐项证据（ACC §4.1~4.11）

### 4.1 改动范围 —— 通过

- diff 文件清单（`git diff --name-status 1397654c..fd3698d3`，8 个）逐个比对契约 `allowedFiles`：
  - `RobWork/RobWorkStudio/src/rwslibs/industrialrobot/diagnostics/**`（4 个）：CMakeLists.txt（M，产品库增列 src/Aggregation.cpp、_test 增列 test/AggregationTest.cpp）、include/sdurws/ird/diagnostics/Aggregation.hpp（A，公共头）、src/Aggregation.cpp（A）、test/AggregationTest.cpp（A）；
  - `RobWork/doc/industrial-robot-design/units/diagnostics.md`（M，allowedFiles 第 2 项——卡头"构建落位"＋§14.4 v0.7 变更记录）；
  - `RobWork/doc/industrial-robot-design/traceability/**`（3 个，allowedFiles 第 3 项）：builds/wp09-t06/integration-gtest-diagnostics-test.xml（A）、integration-gtest-diagnostics-contract-test.xml（A）、ird-test-report.json（A）。
- `forbiddenFiles`（REQUIREMENTS.md / ARCHITECTURE.md）：diff 范围内零命中。
- SA-02（框架零源码修改）：diff 过滤 industrialrobot/doc 前缀后范围外文件计数 **0**；`industrialrobot/patches/` 零新文件零改动（`git diff --name-only <range> -- .../patches/` 空输出）。
- **范围越界：零。**

### 4.2 可复现构建 —— 通过（验收者亲跑；双模式零错误）

- **复现现场**：`git worktree add --detach <临时目录A> fd3698d318117571416e2e8f69b3bcbf21143043`（detached @ 送验 SHA，不用分支名）。
- **集成模式（干净构建树，从零重新配置）**：`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DRWS_USE_STATIC_LINK_PLUGINS=ON -DCMAKE_TOOLCHAIN_FILE=<仓库根>/vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`——配置 exit 0 零 CMake Error（Configuring done 53.9s），缓存确认 **`RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`**。worktree 无未跟踪本地辅助件 `RobWorkStudio.ini.template.static`（F-007/F-062/F-099 在册既有缺陷），自主树补拷后配置通过（环境修复，不触及送验产物——DIAG-T05 验收同先例）。
  - `cmake --build build --config Release --target sdurws_ird_diagnostics_test sdurws_ird_diagnostics_contract_test ird_gates`：两测试目标编译链接**零 error C／fatal error／error LNK（grep 计数 0）**；产物 `sdurws_ird_diagnostics.lib`（STATIC 增列 1 源文件）＋两测试 exe 全部落位 `build/RobWorkStudio/{libs,bin}/Release/`。口径沿 DIAG-T05 先例：构建两目标及其依赖链（core＋框架库），未做全树构建——诊断单元改动面完全覆盖。ird_gates 目标构建退出码 1＝门禁基线命中的预期行为（见 4.4）。
  - **测试执行（验收者亲跑）**：`sdurws_ird_diagnostics_test.exe` → **65/65 PASSED**（exit 0，29 ms）；`sdurws_ird_diagnostics_contract_test.exe` → **9/9 PASSED**（exit 0）——既有 54＋9 零回归＋新增 11 全过，与送验 ird-test-report.json 登记一致。
- **独立冒烟模式（另配临时目录，带 vcpkg toolchain——F-002/CORE-T01 G-2 口径）**：`cmake -S RobWork/RobWorkStudio/src/rwslibs/industrialrobot -B <临时冒烟目录> -DCMAKE_TOOLCHAIN_FILE=.../vcpkg.cmake` 配置 exit 0；`cmake --build ... --config Release` **exit 0 零 error（grep 计数 0）**；冒烟树两测试 exe 亲跑 → **65/65 PASSED＋9/9 PASSED**（exit 0），与送验留痕"双模式 65/65＋9/9"一致。

### 4.3 契约 verify 命令 —— 通过

契约 `verify` 数组仅一条，验收者在复现 worktree A 根目录逐字亲跑：

```
cmake --build build --config Release --target sdurws_ird_diagnostics_test   → exit 0
```

（独立再执行一次：增量链接 `sdurws_ird_diagnostics_test.exe` 产出，exit 0。）产物可直接运行（65/65，见 4.2）。

### 4.4 acceptance 条目逐项证据 —— 通过（4 条全部有具名测试与行号证据；ird_gates 如实登记基线命中而非虚标通过）

| # | acceptance 条目 | 证据 |
| --- | --- | --- |
| 1 | DT-AGG-1~4 用例通过：同根因稳定聚合（occurrences 为实际次数非截断）、聚合保留全部成员明细（不吞关键作用对象——DT-AGG-3，缺失项全量列出 EVI-01）、多工况稳定排序（orderKey 两次构建同序——NFR-COR-02）、不同作用对象绝不合并（DT-DUP-2 口径） | 亲跑双模式 65/65 全绿。**同根因聚合＋计数非截断**：`DtAgg1_SameCauseRootFoldsDerivedEntriesWithExactCounts`（AggregationTest.cpp:180——根因＋5 派生〔经 translate 链 causedBy→E1/E2/E4〕，阈值 3＜成员 5 → `EXPECT_EQ(group->occurrences, 5u)`(:227)、5 成员 entryId 逐个在场(:230-236)、根因组外自成呈现项（视图恰 2 项，:242-243）、两次聚合逐元素相等(:208-211)）。**成员明细全保留/缺失项全量列出**：`DtAgg1_MultiObjectFoldKeepsEverySubjectVisible`（:249——同码×5 subject 折叠为 1 项，5 对象 subject/localName 逐条按序比对(:275-280)）；`DtAgg3_MissingEvidenceMembersAllListed`（:450——6 缺失＋3 其他同批，缺失组 occurrences＝6、6 个缺失对象逐个在场(:483-491)、明细总数 9 不丢(:480)）。**稳定排序**：`DtAgg2_TwoBuildsFromShuffledInputProduceIdenticalOrder`（:318——两次独立构建均乱序插入，逐项 code/occurrences/severity/category/orderKey/members entryId 全比对(:361-372)、输出 orderKey 严格升序(:374-376)、视图 orderKey＝成员最早值 min 语义逐项复核(:377-394)、正序副本输入序无关性补面(:397-406)）。**不同作用对象绝不合并（DT-DUP-2 计数半区）**：`DtAgg1_MultiObject` 的 `occurrenceCount(entries, entries[0].dedupKey)==1`（:283-284——各对象键独立计数不互算）；目录/去重主半区在 DIAG-T04 CatalogFactoryTest（§11 分工），本套件承载聚合面口径 |
| 2 | 局部碰撞不自动升级任务不可行（DT-AGG-4——聚合仅呈现分组无判定权，无 infeasibility-proof 条目产生，C8 作用域；搜索未果口径 §8.1 C5 同表核对） | `DtAgg4_ConfigurationCollisionNeverEscalatesToInfeasible`（AggregationTest.cpp:507——5 条策略拒绝（构型级碰撞呈现，PolicyDenied 类码）＋4 条数据不足（C5 搜索未果呈现，EVI-SNAPSHOT-INCOMPLETE）同批聚合：恰 2 视图项、成员 9/9 保留(:530-535)；逐项断言 `category != InfeasibilityProof`(:541-542)且分类∈{PolicyDenied, DataInsufficient} 不私造(:543-545)、severity 保持 Warning 不升级(:548)；排序后分类集合精确断言恰两输入类(:556-563)；类型层面无证明字段——AggregatedEntry 8 字段无任何证明/不可行承载（Aggregation.hpp:197-246 逐字段，C8"无此能力"的机制承载） |
| 3 | DT-CHAIN-1 用例通过：错误转换派生条目 causedBy 链完整到根因（禁止翻译后只剩最后一层） | `DtChain1_TranslatedDerivativeChainWalksToRootCause`（AggregationTest.cpp:634——worker 崩溃场景：EX-WORKER-CRASHED 根因→translate 派生层 1（causedBy 根因，:653-654）→再 translate 派生层 2（causedBy 中间层，:655-656）；`CauseLink::chainOf` 自告知层遍历全链恰 3 层（中间层未被翻译吞掉，:663-667）、`rootOf` 抵达 EX-WORKER-CRASHED 根因(:669-675)；断链子集严格查询 Usage 不返回半截链(:686-687)、未知起点/空身份 Usage(:688-691)；聚合对断链子集容差＝独立呈现不伪造根因(:679-682) |
| 4 | P-DIAG-1 处置：AggregatedEntry 成员内嵌 core 契约值——core 冻结 diff 后增量同步，不私改 core | **编译期钉住**：AggregationTest.cpp:67-78 三条 `static_assert`（`MemberDetail::subject` 必须＝`std::optional<core::ObjectId>`——core.md v0.1 基线漂移即编译失败；entryId 类型；localName nullopt 语义）。**运行期佐证**：`DtPdiag1_MemberDetailEmbedsCoreContractValuesVerbatim`（:701——成员 subject 与源 record.subject 逐值相等，core 契约值原样内嵌不改写）。**core 零改动＝4.1 范围实证**（diff 零 core 文件；实现仅 include core 公共头 Identity.hpp，Aggregation.hpp:67 注释锚定 P-DIAG-1 基线口径） |

**ird_gates（如实登记）**：验收者亲跑 `cmake --build build --config Release --target ird_gates`（复现 worktree 集成树）→ exit 1，**命中 7 项与 wp09-t05 合入基线逐项同分布、零新增**：IRD-GATE-SUB×5（evidence_test/evidence_contract_test/policy_test/policy_contract_test/runtime_test → sdurws_ird_testkit 直链，F-072/F-051 族既有）＋IRD-GATE-SUB 表外边×1（sdurws_ird_testdata_lint → testkit，F-019）＋IRD-GATE-T1×1（sdurws_ird_testdata_lint 产品目标链 testkit，F-019）；引擎自测行（pass_clean/pass_r4comment＋fail_r1/t1/r5/sub/r3/r4/t2"按预期检出"）非命中。**diagnostics 相关命中＝0**（命中输出全文检索 diagnostics 计数 0）。exit 1＝基线既有命中的预期行为，处置权 WP-01 所有者——与送验 ird-test-report.json gates 段登记一致（如实登记基线命中，未私改门禁、未虚标通过）。

### 4.5 测试真实失败能力（验证验证者）—— 通过（两探针亲手构造、真实变红、还原复绿零残留）

验收者在复现 worktree 的 `src/Aggregation.cpp` 临时注入缺陷、重建重跑、逐字还原（`git checkout --`＋`git status` 零内容残留、grep `PROBE-TEMP` 计数 0；每次还原后 `touch` 强制重编规避 F-068 mtime 陷阱，最终 65/65 复绿）：

- **探针 1（occurrences 截断——计数语义）**：`buildGroupView` 中 `view.occurrences = view.members.size();` 改为 `view.members.size() > 2u ? 2u : view.members.size()`（模拟"≥N 截断"）→ **3 用例真实变红**：`DtAgg1_SameCauseRootFoldsDerivedEntriesWithExactCounts`（occurrences==5 断言）、`DtAgg1_MultiObjectFoldKeepsEverySubjectVisible`（==5）、`DtAgg3_MissingEvidenceMembersAllListed`（==6）——acceptance 1"实际次数非截断"的断言非恒真。
- **探针 2（断链返回半截链——DT-CHAIN-1 观测面）**：`CauseLink::walkChain` 断链分支 `throw DiagnosticsError(Usage, "diagnostics/causelink/chain-broken-at-set-boundary")` 改为 `return chain;` → `DtChain1_TranslatedDerivativeChainWalksToRootCause` **真实变红**（1 FAILED——"断链子集的严格链查询应 Usage"负断言失败）：半截链缺陷确实被测试抓住，"禁止翻译后只剩最后一层"有机器观测面。
- 两探针证明断言校验真实行为边界、非恒真；探针期间中间构建未计入任何结论。

### 4.6 架构红线（AGENTS §5 八条逐条）—— 通过

1. **L2 计算内核零 Qt**：新增 3 文件 `grep "#include <Q"` 计数 0/0/0（hpp/cpp/test）；ird_gates 输出零 diagnostics 命中；diagnostics/CMakeLists.txt 配置期红线守卫（既有）未触碰。
2. **业务域互链禁止（R-1）**：产品库链接边未变（diff 仅增列源文件，PUBLIC 依赖仍仅 `sdurws_ird_core`）；ird_gates SUB/R1 零 diagnostics 命中。
3. **跨单元私有头禁止（R-2）**：ird include 全集＝core 公共头 {Identity.hpp（Aggregation.hpp:67）、DiagData.hpp（测试）}＋本单元公共头 {Aggregation/Catalog/DiagCodes/Errors/Factory}（`include/sdurws/ird/` 公共层，grep 实证）；detail 层引用计数 0/0/0；本任务未新增跨单元引用。
4. **框架零源码修改（SA-02）**：见 4.1——diff 零框架文件、patches/ 零改动。
5. **不做名称前缀拼接/剥离（R-4）**：本任务无名称解析语义（聚合器与 RobWork 名称面无关）；grep 名称前缀类引用 0 命中；ird_gates R-4 零 diagnostics 命中。
6. **权威唯一（PA-1）**：聚合器定位为纯查询视图（Aggregation.hpp:20-25 文件头专段——条目生命周期与去重折叠归 DiagCatalog，聚合零写路径）；occurrences 语义注释明示"目录侧去重折叠计数归 DiagCatalog——PA-1，聚合器不重计"（Aggregation.hpp:230-232），`occurrenceCount` 为纯事实计数不执行折叠（Aggregation.cpp:442-455）；码值只用既登记码（测试经 registerBuiltinCodes，无私造码值）；无 ui/命令语义越入。
7. **不可变历史（PA-2）**：aggregate 纯函数无写路径——`DtAgg5` 断言输入集合逐字节未变（AggregationTest.cpp:731-739）；聚合视图不入目录不持久化（AggregatedEntry 无 entryId，hpp:180 注释）；原始证据全保留（members 明细，PA-2 的呈现面延伸）。
8. **不继承 old/**：实现按单元卡 §6.3/§6.4/§9.4 从零编写；grep old/ 零引用（old/ 当前磁盘亦缺失——单元卡 §1.2 登记同源）。

### 4.7 注释规范（AGENTS §2）—— 通过（公共头全查＋实现/测试全读，抽查面全覆盖）

- **公共头全查（1/1，Aggregation.hpp 350 行）**：文件头设计依据逐条（§6.3/§6.4/§6.5/§9.4＋需求 ERR-01/EVI-01/NFR-COR-02＋C5/C8 作用域＋契约 knownPitfalls）；"为什么聚合是纯查询视图"背景专段（PA-1 权威归属论证）；聚合无工程判定权专段（C8/C5——类型层面无字段可承载）；P-DIAG-1 处置锚点专段；实现口径登记四项摘要（与单元卡 §14.4 v0.7 呼应）；线程安全专段（const 纯函数、无共享可变状态、进程级共享）。类/函数级：CauseLink 命名空间三种关系语义边界（causedBy 可遍历/relatedTo 无链序/supersedes 不合并）；rootOf/chainOf 逐参数方向、断链/环/未知起点四类 Usage 触发条件与理由（"半截链会让'翻译后只剩最后一层'缺陷从观测面消失"）；AggregationRule 三值逐值、AggregationScope.minGroupSize 取值域与默认值语义（2 与目录侧 ≥3 的边界切割）；AggregatedEntry 逐字段业务含义＋P-DIAG-1 锚点；IDiagnosticAggregator 契约表逐行（合法/非法调用）；DiagnosticAggregator 分组判据算法注释（键一容差遍历/键二任务分区/单组化/折叠/排序确定性论证）。
- **实现文件抽查（1/1 全读，Aggregation.cpp 457 行）**：文件头确定性来源专段（std::map 字典序、无哈希序指针序——NFR-COR-02 实现纪律）；severityRank 显式级别秩映射表及"枚举声明序≠级别序"理由（§4.3＋P-DIAG-3）；buildIndexAndValidate 检查序①②③逐步业务依据；walkToReachableAncestor 容差遍历与环防御（跳数上限＝集合大小的数学论证）；GroupKey 六分量全序键设计说明（kind 分量防碰撞、bool 分量隔离默认值比较）；buildGroupView 第一~七步逐步语义（排序/组码/明细/计数/最高严重/共同分类/共同作用域）；walkChain 严格版与容差版差异论证（断链抛 Usage 防半截链的理由）。§2.5 高危信息：错误语义（Usage＝调用方违约 fail-fast vs 容差呈现的边界逐处标注）、线程（纯函数并发安全）、确定性来源逐处标注、所有权（输入只读不接管——hpp @param 注明）。
- **测试文件抽查（1/1 全读，AggregationTest.cpp 801 行）**：文件头 acceptance 1~4 映射表＋用例名后缀＝矩阵行编号说明；线程约束专段（单线程理由——无互斥面可测不私建）；每 TEST 注释锚定矩阵行/需求/陷阱；断言处中文说明（"不因聚合短路""跨任务条目被并入他组""聚合产出了不可行证明语义（DT-AGG-4 反例命中）"等）。无中英混杂机翻腔、无过期注释、无裸 TODO。
- **单位/坐标系/角度制式**：本任务无物理量计算面（纯条目分组/排序/计数），不适用——非缺项。

### 4.8 文档同步 —— 通过

- 单元卡 `units/diagnostics.md` v0.6→v0.7：卡头"构建落位"字段实测刷新（Aggregation.hpp/cpp/测试文件/11 用例/双模式 65/65＋9/9 落位清单）＋§14.4 v0.7 变更记录——**四项实现口径按 DTB §5.4 登记**（①聚合键成员归属单组化〔含链断裂容差口径与 §9.4 前置的对应论证〕；②根因不入组成员；③共同值投影与计数语义〔severity 级别秩/category/scopeKind nullopt 口径/occurrences 每成员计 1/视图 orderKey min 语义〕；④违约面 Usage 清单＋occurrenceCount 纯查询＋relatedTo/supersedes 不参与判定）＋接线登记（aggregatedUnder 消费随 ui/reporting；目录侧自动聚合阈值归 DIAG-T09）＋P-DIAG-1 处置锚点。**§9.4 两方法签名与 Aggregation.hpp 逐字比对一致**（验收者亲核 aggregate/occurrenceCount 签名与契约表四行——Aggregation.hpp:285-286/301-302 与 diagnostics.md:975-978 一致；AggregationScope 形状卡未定→实现口径④登记，DTB §5.4 合规，非未登记偏差）。
- 契约流水线状态：`state.json` 的 DIAG-T06 队列项（branch wp09-t06、headSha、attempt 1）在案，翻转归收尾（PIPE §4.7⑤——非实施者职责）。
- validate-task：验收者亲跑 `validate-task.ps1 -TaskFile <契约>` → **PASS (1 tasks)**，exit 0；validate-docs → **PASS (20 units, 12 trace entries, 150 task files)**，exit 0（fd3698d3 正文两声称均复现准确）。
- **findings.json 未关闭条目相关性核对（open 条目逐家族排查，登记册共 105 条/86 open）**：与本任务直接相关的既有 open 条目——**F-081/F-083/F-085/F-087/F-088/F-090/F-102/F-103 家族**（声称入库实际未入库：本任务 fd3698d3 正文声称的各类 .log 全部未入库＝该家族第 9 例且再现 .gitignore 根因，新登记 **F-106**；改进面：2 份集成 gtest XML 本例已入库）；**F-010**（.gitignore:87 全局 *.log 吞留痕——本例根因再现，并入 F-106）；**F-012/F-100/F-104 标题家族**（fd3698d3 标题名词"文档"开头——同型再现，新登记 **F-107**）；**F-007/F-062/F-098/F-099/F-101**（全新树构建环境链：本次复现再次实证 toolchain/Qt 前缀/ini 模板三件套必要——按 DIAG-T05 验收口径一次配通，处置权构建约定所有者，无可随手消账项）；**F-019/F-051/F-072**（ird_gates 基线红：本次 7 命中同分布再印证，零 diagnostics 命中，处置权 WP-01 所有者）；**F-065**（留痕日志中文乱码：本次 ird_gates 输出复现同现象——环境编码面，与本任务产物无关）；**F-046/F-063/F-093/F-105 微观卫生家族**：本任务新增源码扫描零残留注释/零冗余 include，无再现、无新登记。其余 open 条目属 evidence/runtime/policy/io/testkit 等其他单元，与本任务无相关性、无静默积压。

### 4.9 commit 质量 —— 通过（附标题风格再现与留痕清单出入——转登 F-107/F-106，不构成数值粉饰）

- `70df8723` `[WP-09-T06] 实现 diagnostics 聚合器（CauseLink/聚合规则/稳定排序）`：标题动词开头、≤50 字；正文四段齐备（What 按文件逐项/Why 逐条需求与陷阱映射/验证含构建·测试·门禁真实结论/影响面"仅新增文件＋CMake 增列、目标名/别名/include 根不变"）；`Acc-Covered: 1, 2, 3, 4` 尾注。
- `fd3698d3` `[DIAG-T06] 文档同步与验证留痕（单元卡 v0.7＋双模式证据）`：正文四段齐备、如实说明纯文档与留痕性质；**标题以名词"文档"开头非动词**——F-012/F-100/F-104 家族同型再现（转登 **F-107**，建议级）。
- **出入（建议级，转登 F-106）**：fd3698d3 正文声称入库"integration/smoke 构建·配置·测试运行日志、契约 verify 命令日志、ird_gates 输出、validate-task PASS、gtest XML 两份、ird-test-report.json"——git ls-tree @fd3698d3 实证 builds/wp09-t06/ 仅 **3 件**（2 份集成 gtest XML＋ird-test-report.json），正文声称的全部 .log 零入库（.gitignore:87 全局 `*.log` 吞——F-010 根因再现，未 add -f）；ird-test-report.json 本例无 logs/gtestXml 索引字段（gates 字段提及 gates-current-wp09t06.log 一处且该文件不在库）。**改进面**：2 份集成 gtest XML 本例已实际入库（F-103"首次全部缺失"未复重）。**数值主张（双模式 65/65＋9/9、既有 54＋9 零回归、门禁 7 命中基线逐码一致零 diagnostics、validate-task/validate-docs PASS）经验收者亲手复现全部准确，测试结果无粉饰。**
- 注记：送验构建日志未入库故其实施构建树口径无从核对（F-103 同症）——验收者已以独立从零配置构建补强（集成＋冒烟双模式 exit 0 零 error，见 4.2），口径缺口不掩盖。

### 4.10 留痕完整 —— 字面部分未达成（建议级——F-106；沿 F-081→F-103 家族八例既判先例不阻断）

- **在库**（git ls-tree @fd3698d3，builds/wp09-t06/ 共 3 件）：`ird-test-report.json`（schemaVersion ird-test-report/1，含 per-target 构建结论、per-suite 逐用例 trace〔11 新用例逐条映射 acceptance/需求/矩阵行〕、gates 基线对比、双模式结果——结构化 resultNote 载体本身完整准确）＋**2 份集成 gtest XML**（integration-gtest-diagnostics-test.xml 65 用例、integration-gtest-diagnostics-contract-test.xml 9 用例，`DtDiag2`/`ConfirmableProjectStubTest` 等既有用例在案——XML 入库较 DIAG-T05 改进）。
- **未入库而提交正文声称在案（各 .log）**：integration/smoke 构建·配置·测试运行日志、verify 命令日志、ird_gates 输出、validate-task PASS 留痕——全部缺失（F-010 根因，见 4.9）。**证据本体不缺**：双模式 65/65＋9/9、ird_gates 7 命中、validate-task/validate-docs PASS 由验收者从零亲手复现一致（本记录 4.2/4.3/4.4/4.8）。
- `phase-one-validation.log`：历史载体（DIAG-T03/T04/T05 验收记录同结论），本任务留痕按现行惯例落 `builds/wp09-t06/`，ird-test-report.json 即结构化 resultNote 载体——符合"phase-one-validation.log **或** resultNote 已更新"的替代口径。

### 4.11 偷懒/缩水实现扫描 —— 通过

- ①**标记扫描**：diff 新增行 `TODO|FIXME|placeholder|not implemented|未实现` 计数 **1**——唯一命中为入库 gtest XML 内既有用例名 `DtDiag2_ParamSchemaPlaceholderConsistency`（DIAG-T04 产物、ParamSchema 占位一致性**验证用例**的数据行，非实现缩水标记，所在文件为留痕数据而非代码）；`stub` 命中 9 处全部为 XML 内既有用例名/套件名（ConfirmableProjectStubTest/DtCfm*/ConsumerStub*——DIAG-T05 产物）与单元卡既有文本；**新增源码 Aggregation.hpp/cpp/test 三文件零命中**，无需豁免条目。
- ②**空实现检测**：公共接口全部真实实现——aggregate 为五步算法（入口阈值检查→buildIndexAndValidate 三重前置→规则投影→std::map 分组〔三类全序键〕→阈值折叠＋buildGroupView 七步投影＋稳定排序，Aggregation.cpp:377-440）；occurrenceCount 为四元组全等线性扫描（:442-455）；CauseLink::rootOf/chainOf 经 walkChain 严格链遍历（:321-369）。无空函数体/仅返回默认值的契约函数。
- ③**逐条证据表**：见 4.4——acceptance 4 条全部具名测试＋文件行号级证据，无一条仅凭"已实现"声明。
- ④**测试强度**：断言校验行为与边界（occurrences 精确值 5/6/9 非阈值截断、成员 entryId/subject/localName 逐个在场、跨任务分区归属严格、分类集合精确两值、Usage 违约面 7 类负断言〔entryId 0/重复/Dev 级/阈值 0/断链/未知起点/空身份〕、纯函数输入逐字节未变、阈值 2/3 双向行为、orderKey min 语义逐项复核）；无恒真断言；4.5 双探针实证非恒真。

---

## 二、裁决

**verdict ＝ pass**

- 4.1~4.11 全部通过（4.10 的留痕入库缺口与 4.9 的标题风格为建议级，沿 F-081→F-103 家族八例既判先例不阻断），无阻断级问题：范围零越界、双模式构建/测试/门禁/脚本全部亲手复现成功（集成 65/65＋9/9、冒烟 65/65＋9/9、ird_gates 7 命中基线同分布零 diagnostics、validate-task/validate-docs PASS）、红线零命中、注释无缺项、4.11 零命中（新增源码面）、双探针实证测试真实失败能力、无数值粉饰。
- 新增建议级登记 2 项：**F-106**（fd3698d3 声称入库的各 .log 留痕全部未入库——F-081/F-102/F-103 家族第 9 例；.gitignore:87 `*.log` 根因再现＝F-010；改进面：2 份集成 gtest XML 已入库；证据本体不缺，验收者双模式亲跑一致）；**F-107**（fd3698d3 标题名词"文档"开头——F-012/F-100/F-104 家族再现）。
- 环境类既有条目 F-007/F-062/F-098/F-099/F-101/F-019/F-051/F-072/F-065 本次复现再次实证（ini 模板补拷后零错误配置构建；ird_gates 基线 7 命中同分布；门禁输出乱码同现象），已在册、不重复登记。
- 本验收通过的是 **SHA fd3698d318117571416e2e8f69b3bcbf21143043**，非分支引用；验收者未修改任何实施产物（探针注入/还原均在临时 worktree 内、最终零残留）、未向 wp09-t06 推送任何提交。

## 三、问题清单

| 级别 | 描述 | 处置 |
| --- | --- | --- |
| 阻断 | 无 | — |
| 建议 | **F-106**：fd3698d3 正文声称入库"构建·配置·测试运行日志＋verify 日志＋ird_gates 输出＋validate-task PASS"等 .log 留痕，实际 builds/wp09-t06/ 仅 3 件（2 集成 gtest XML＋ird-test-report.json）——F-081/F-102/F-103 家族第 9 例；.gitignore:87 全局 `*.log` 吞且未 add -f（F-010 根因再现）；ird-test-report.json 本例无 logs/gtestXml 索引字段。数值主张全部复现准确、无粉饰；2 份集成 gtest XML 已入库（较 T05 改进） | 已转登 findings.json（F-106，open）；修复方向同 F-103（gitignore 例外/强制 add -f）；不阻塞合入 |
| 建议 | **F-107**：fd3698d3 标题"[DIAG-T06] 文档同步与验证留痕…"以名词"文档"开头非动词——F-012/F-100/F-104 家族再现（70df8723 动词开头合规） | 已转登 findings.json（F-107，open）；不阻塞合入 |

## 四、验收者环境说明

- 复现 worktree（worktree A）：`D:/10_Source_Repos/21_robot/acc-diag-t06-a`（detached @ fd3698d3）；集成树＝主树缓存口径从零配置（`RWS_BUILD_INDUSTRIALROBOT=ON`〔缓存 grep 确认〕、`RWS_USE_STATIC_LINK_PLUGINS=ON`、vcpkg toolchain、Qt `CMAKE_PREFIX_PATH`；`RobWorkStudio.ini.template.static` 自主树补拷——F-007/F-062/F-099 既有缺陷的环境修复，不触及送验产物）。冒烟树：`D:/10_Source_Repos/21_robot/acc-diag-t06-smoke`（独立目录＋vcpkg toolchain，显式 Release）。
- 工具链：VS 2022（MSVC，生成器 Visual Studio 17 2022，-A x64）、CMake 4.3.1、vcpkg 经典模式（gtest 1.18.0 @ installed/x64-windows）、pwsh（validate-task.ps1 / validate-docs.ps1）。
- 探针注入/还原均在 worktree A 内：两轮注入（occurrences 截断、walkChain 断链返回半截链）各自重建重跑取证后 `git checkout --` 还原＋touch 强制重编（规避 F-068 mtime 陷阱），最终 `git status` 零内容残留、grep `PROBE-TEMP` 计数 0、65/65 复绿。
- 验收记录与 F-106/F-107 登记落 `acc/DIAG-T06/1` 分支（基于 origin/redesign-main @ 1397654c）并推送；验收者日志随临时 worktree 清理，本记录内数值均为亲测。
