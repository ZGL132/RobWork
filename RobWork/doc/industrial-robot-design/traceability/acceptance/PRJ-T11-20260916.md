# PRJ-T11 验收记录（attempt 1，2026-09-16）

| 字段 | 值 |
| --- | --- |
| 任务 | PRJ-T11 确认放行流（交互回调编排与留痕）——tasks/foundation/PRJ-T11.json |
| 送验对象（冻结） | cdb242646be12eab64678cb93794be9efeef4d51（分支 wp04-t11；验收后复核 origin/wp04-t11 == 该 SHA，无分支漂移） |
| diff 范围 | 900d7eb74f36bd1c100c771dba6475c71054eb01（state.base）..cdb242646be12eab64678cb93794be9efeef4d51，共 4 提交（bfe2d5d1 实现 → 151e7d6b 用例 → 46c58061 单元卡 v0.13 → cdb24264 留痕） |
| **verdict** | **pass**（4.1～4.11 全部通过、无阻断级问题；4 条建议级问题转登 findings.json F-157/F-158/F-159/F-160） |
| 验收环境 | 验收者全新上下文（未消费实施叙述）；复现现场＝detached worktree @SHA（D:\\10_Source_Repos\\21_robot\\acc-tmp\\prj-t11-a）；集成模式全新从零配置＋构建，独立冒烟另配临时目录（vcpkg toolchain 绝对路径）；MSVC 2022 x64 / Release |

## 4.1 改动范围 —— 通过

- `git diff --stat 900d7eb7..cdb24264`：15 文件，全部落在契约 `allowedFiles` 内——`industrialrobot/project/**`（src/ConfirmationFlow.{hpp,cpp} 新增、src/CommandServiceImpl.{hpp,cpp}、src/win32/StoreLock.{hpp,cpp}、test/CommandServiceTest.cpp、test/StubHandlers.hpp、CMakeLists.txt）、`units/project.md`（3 行）、`traceability/builds/wp04-t11/**`（5 件）。
- `forbiddenFiles`（REQUIREMENTS.md/ARCHITECTURE.md）零触碰（diff name-only 反证在档）；范围外文件清单为空。
- SA-02：diff 范围无框架源码文件，`patches/` 无新文件——无未登记框架改动。

## 4.2 可复现构建 —— 通过（全新树从零，任务目标面）

- 集成模式：worktree A 全新从零配置（`-G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON`＋vcpkg toolchain＋Qt prefix——主树缓存口径；**缓存确认 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`**）。`sdurws_ird_project_test` 与 `sdurws_ird_project_contract_test` 两目标构建 **exit 0、`: error` 计数 0**（验收者日志 acc-tmp/build-integration.log、build-contract-integration.log）。
  - 环境备注：首次配置失败于 `RobWorkStudio.ini.template.static` 缺失——该文件被 `.gitignore:79 [Bb]in/` 吞掉不入库、仅存在于主树（**F-062 已登记的同族环境缺口，非实施缺陷**）；从主树复制后配置零错误。
- 独立冒烟模式：独立临时目录（`-DCMAKE_TOOLCHAIN_FILE=<repo>/vcpkg/scripts/buildsystems/vcpkg.cmake`——AGENTS §4.1 口径）配置＋全量构建 **exit 0、错误 0**（acc-tmp/configure-smoke.log、build-smoke.log）。
- 结论：两模式零错误，与送验报告 builds[].exitCode=0 一致；本验收为全新树从零构建（强于主树增量口径）。

## 4.3 契约 verify 命令 —— 通过

- 契约 verify 仅一条：`cmake --build build --config Release --target sdurws_ird_project_test`——在 worktree A 集成树（全新从零）亲手执行 **exit 0**（初次构建即本命令；探针前后另重建 3 次均成功）。

## 4.4 acceptance 条目逐项证据 —— 通过（全部亲手执行）

| # | 契约 acceptance | 证据（验收者亲手复现） |
| --- | --- | --- |
| 1 | PRJ-TX-2 四路（硬断言/无 interaction/interaction 拒绝/绑定失效）——四路均无修订、command.json 无新记录；诊断字段完整 | 集成 219/219、冒烟 219/219 含 `CommandConfirmTx2.Path1~Path4`＋`BindingVerify.EachMemberMismatch_DetectedAndClassified` 全绿。Path1 断言 HardAssertFailed＋ERR-01 四字段非空＋修订计数不变（CommandServiceTest.cpp:848-871）；Path2 无交互→ConfirmationsUnresolved＋findings Pending 回传（:875-890）；Path3 拒绝→ConfirmationsRejected（:894-910）；Path4 经 PerturbingProbe 四成员任一注入→ConfirmationsUnresolved＋Pending＋无修订＋reportDev 维度定位＋零用户级码（:916-941）；BindingVerify 纯函数级四成员逐一扰动各自定性正确（:946-989）。 |
| 2 | 确认绑定与取消路径：占执行槽、零事务资源、可被关闭取消（无永久等待）；interaction 生命周期（挂起/恢复/丢失——InteractionLost） | `ConfirmWait_HoldsSlot_SecondSubmitQueued`（:999-1034，挂起期间第二提交 300ms 超时不得完成、放行后串行 seq 递增）、`ConfirmWait_ZeroTransactionResources`（:1039-1062，挂起时 .staging 无 tx 目录＋修订无增长）、`SessionClose_CancelsConfirmWait_NoPermanentWait`（:1069-1109，requestClose 非阻塞返回在途数 1→回调 ui 拆除抛出→Aborted(InteractionLost)→票据释放排空收尾 closed()=true——无永久等待）、`SuspendThenUserCancel_RejectedContextUnharmed`（:1114-1139，取消后上下文可继续提交）、`Callback_SynchronousOnSubmitterThread`（:1143-1163，回调线程==提交线程）。丢失面＝SessionClose 用例 throwOnRelease（§5.3.3 抛出→interaction-lost 同路径）。 |
| 3 | 确认留痕入 command.json（confirmations 结构）；UX-03 承接（回调纯接口供数齐备、文案不在 project 生成） | `Confirmations_PersistedToCommandJson_RoundTrip`（:1174-1217，磁盘 command.json 五字段与重算值逐字段同源一致＋credential ISO-8601 24 字节 Z 尾＋cid-/rev- 形态＋`codec::dump(parse(x))==磁盘字节` canonical round-trip）、`NoFindings_ConfirmationsFieldOmitted`（:1221-1240，空表缺省省略）、`Callback_ReceivesComparativeData_Ux03`（:1246-1273，比较三要素 2m/1m 齐备到达回调＋project 确认流零用户级诊断——文案边界 NFR-REL-05）。 |
| 4 | project 不依赖 Widgets（ICommandInteraction 纯接口）；P-PR-7 不私改签名；P-PR-6 诊断经 IDiagnosticsSink 注入不直链 diagnostics | ①独立 grep 扫描新增/改动文件零 Qt include（`ird_gates` QT 维度＋单元内 `BuildRedLineTest.NoQtInclude_DT_BUILD_NFR_MNT_01` 在 219 集内通过——源码扫描自动覆盖新增文件）；②diff 范围无 include/ 公共头文件（git diff name-only 反证）＝ICommandInteraction/IModelCompilePort 签名零改动（P-PR-7）；③CMakeLists 无新增链接边、契约测试 `LinkageContractTest`（contract 15/15 集内）通过＝零 diagnostics 链接边，Path4 断言 devContains("绑定复核失配")＋reports.empty()（P-PR-6——reportDev 注入通道）。 |
| — | （ird_gates 执行状态） | 已建成（WP-01-T01 落地），本任务**实跑**：命中 9 项＝7×IRD-GATE-SUB＋1×IRD-GATE-T1＋1×IRD-GATE-R4，与 F-145 基线/wp04-t08~t10 逐项一致＝**零新增命中**（worktree A 实跑 exit 1＝设计行为；自测 9 项全部按预期）。送验报告 gates.irdGates 与验收者实测一致。 |

测试执行：集成 `sdurws_ird_project_test` **219/219 PASSED**、`sdurws_ird_project_contract_test` **15/15 PASSED**；冒烟两目标同数字 **219/219＋15/15**——与送验报告 claims 逐字一致（acc-tmp/gtest-integration.xml、contract-integration.xml、gtest-smoke.xml、contract-smoke.xml 四份 XML 为验收者亲跑产物）。

## 4.5 测试真实失败能力 —— 通过（双探针即红、还原复绿）

- 探针 1（submit 级编排）：`recheckConfirmations` 短路为恒 `return std::nullopt`（绑定复核形同虚设）→ 重建后 `CommandConfirmTx2.Path4_BindingMismatch_RejectedUnresolvedNoRevision` **FAILED**（绑定失效拒绝路径依赖真实复核，非恒真）。
- 探针 2（留痕落盘）：`commitPlan.command.confirmations = std::move(confirmations)` 注释短路 → `CommandConfirmPersistence.Confirmations_PersistedToCommandJson_RoundTrip` **FAILED**（留痕断言依赖真实落盘）；同轮 Path4 恢复 OK（探针 1 还原干净）。
- 全部还原后重建＋全量复跑 **219/219 PASSED**；`git status` 对 industrialrobot/ 零差异（实施产物无残留改动；本验收全程未向任务分支推送任何提交）。

## 4.6 架构红线（AGENTS §5 八条）—— 通过

1. L2 零 Qt：project 全部源/头零 Qt include（扫描零命中；BuildRedLineTest 门内通过）。
2. R-1 业务域互链：无 modeling/requirements/kinematics/trajectory/dynamics/selection/optimization 依赖；跨单元仅 core 公共头消费。
3. R-2 私有头：include 面仅 `sdurws/ird/core/*` 公共头＋本单元头；ConfirmationFlow.hpp 为 src/ 私有实现头不出 include/。
4. SA-02：diff 无框架文件、patches/ 零新增。
5. R-4：本任务改动文件零 R4 命中（ird_gates 唯一 R4 为 ProjectStoreImpl.cpp T08 既有字面量，本任务 diff 零触碰该常量）。
6. 权威唯一（PA-1）：确认留痕写路径在 project（§4.4.4 所有者）；摘要算法唯一路径 core::ContentDigester（CR-02）；确认流诊断码零私造（diagnostics §4.6 无确认流用户码→走 reportDev，CR-08）；名称处理未触碰。
7. PA-2：confirmations 随修订一次性持久化，无覆写路径；编码演进以 magic 版本号约定（PA-2 精神，注释明示）。
8. 无 old/ 恢复与历史实现继承（diff 零 old/ 引用）。

## 4.7 注释规范 —— 通过

- 公共头：本任务**零公共头改动**（include/ 无 diff）——契约面无新增待查项。
- 新增/大改文件全查＋实现文件抽查（≥3）：`ConfirmationFlow.hpp`（272 行，文件头设计锚点＋实现口径①～⑤登记＋每类型/函数 Doxygen 中文＋线程安全声明＋编码表字节序/出席旗标/枚举序等确定性来源全列）、`ConfirmationFlow.cpp`（编码表逐字段注释＋magic 演进语义＋大端理由＋bad_alloc 例外语义）、`CommandServiceImpl.{hpp,cpp}`（S4/S5/S6 增量编排逐段注释：复核时机锚点 diagnostics §5.5 要点⑦前后半、失配归类 §6.7＋实现口径④、D-10 接缝语义）、`StoreLock.{hpp,cpp}`（formatIsoMilli Doxygen＋"不得参与内容寻址"确定性边界＋gmtime_s 回退语义）。
- §2.5 高危信息：物理单位（比较值注释带单位 m）、rad/deg（本任务无角度量——比较值经 unit token 显式）、所有权（setConfirmationProbe 非 owning 注释）、线程约束（IConfirmationProbe 仅命令槽内调用、GatedInteraction 用例串行口径）、错误语义（实现口径④ Rejected 归类＋CR-08 不私造）、确定性来源（编码表枚举定值/大端/无浮点文本化）——全部在位。

## 4.8 文档同步与 findings 相关性 —— 通过（3+1 条建议级转登）

- 实现与任务卡零语义偏差；实现口径①～⑤（findingDigest 编码表、policyContentId 阶段 A 来源、复核时机、失配归类、探针接缝）按 DTB §5.4 于 ConfirmationFlow.{hpp,cpp} 文件头＋单元卡 §15.4 v0.13 行双向登记。`validate-task` PASS、`validate-docs` PASS（20 units, 12 trace entries, 150 task files——与 46c58061 声明逐字一致，验收者实跑）。
- 契约状态 ready→（合入段由所有者翻转 done，非验收者动作）。
- **findings.json 未关闭条目相关性核对**（86 条 open，最新编号 F-156）：
  - F-156（LockContractTest.CrashThenConcurrentTakeover 偶发）：本验收 3 轮 contract test（集成/冒烟/复跑）均 15/15 全绿，未复现偶发——继续观察。
  - F-145（门禁基线 9 命中）：实测命中集合与登记逐项一致，零新增——复核成立。
  - F-062（worktree 复现环境前置）：本次复现再次踩中（ini 模板缺失）——已有登记，无需新条目。
  - 其余 open 条目（F-012 提交标题家族、F-019/F-051 门禁登记矛盾、F-152/F-153 幻影清单、F-155 卡头日期等）与本任务代码无语义冲突，但**本任务在三个家族中再现**（见问题清单 G-1～G-3，转登 F-157～F-159）；另零行为影响杂项转登 F-160。
- 可顺手消账项：无（F-157~F-159 均为纪律面，随模板/流程补救统一处置）。

## 4.9 commit 质量 —— 通过（含 G-1/G-2 建议级）

- 四提交正文均为四段式（What/Why/How/影响面）＋`Acc-Covered:` 行；实现+测试+文档+留痕四提交序列可独立回溯。
- 验证结论与留痕一致性：bfe2d5d1/151e7d6b 声称的"两模式 219/219＋15/15、门禁 9 命中零新增、validate-task PASS"经本验收逐项复现全部为真；46c58061 的 validate-docs 数字逐字一致；cdb24264 的两模式结论与门禁结论复现为真。
- 标题合规：bfe2d5d1『实现确认放行流…』、151e7d6b『新增确认放行流用例组…』动词开头合规；**cdb24264 与 46c58061 为 F-012 家族同型名词短语开头（G-2 → F-158）**。
- 计数出入：cdb24264 正文『全套执行证据（16 文件）』与实际入库 5 件不符（.log 全部被 .gitignore 吞掉未入库——**G-1 → F-157**，方向多声称、结论性数据无粉饰）。

## 4.10 留痕完整 —— 通过（附 G-1 缓解面说明）

- gtest XML 4 份（project/contract × integration/smoke）＋ird-test-report.json（acceptance 逐条对账＋builds/gates 段）**在库**（git ls-tree 实证）。
- 构建日志：报告引用的 11 份 .log 未入库（.gitignore:87 `*.log` 命中，check-ignore 实证）——证据本体（XML＋report）在库且结论经验收者独立复现为真；幻影清单按家族判例转登（F-157），缓解建议随 F-153（git add -f 先例或内联明示）。
- phase-one-validation.log：仓库自 CORE-T01 后无任务更新（grep 'PRJ-T' 零命中），ird-test-report.json＋builds/ 为既成留痕载体（PRJ-T01~T10 先例）——按"或"口径满足。

## 4.11 偷懒/缩水实现扫描 —— 通过

- ①diff 范围 `TODO|FIXME|XXX|placeholder|not implemented|未实现` 扫描**零命中**（无"触发式延后"豁免需求）。
- ②空实现：本任务无公共接口新增；全部新增函数有完整实现体（ConfirmationFlow 摘要/冻结/复核、编排两复核点、留痕落盘、探针），无仅返回默认值的空壳。
- ③acceptance 逐条证据表：见 4.4——四条全部有具名测试＋行号级断言映射，无"已实现"声明依赖。
- ④测试强度：断言校验行为与边界（磁盘地面事实修订/对象/.staging 计数、canonical round-trip 字节级、线程 ID 相等、future 超时、Pending/Rejected/Aborted 状态机、reportDev 通道与用户码双面），非恒真；与 4.5 双探针联动实证会红。

## 问题清单（全部建议级，无阻断）

| # | 级别 | 发现 | 转登 |
| --- | --- | --- | --- |
| G-1 | 建议 | 幻影清单家族再现（F-148/F-152 之后）＋F-010 根因（F-153 第八次后）再踩中：cdb24264 声称 16 文件/11 份 .log，ls-tree 实证入库 5 件，全部 .log 被 .gitignore:87 吞掉且正文未内联明示；结论性数据复现为真、无粉饰 | F-157 |
| G-2 | 建议 | 提交标题家族再现（F-012 系，一次两例）：cdb24264『[governance] PRJ-T11 验证留痕：…』（[governance] 车道第三例、"PRJ-Txx 验证留痕"逐字模板再现）＋46c58061『[units] project.md v0.13：…』（[units] 文件名词短语车道再现）；两实现提交动词开头合规 | F-158 |
| G-3 | 建议 | 卡头日期停留家族再现（F-155 之后第八跳）：project.md 卡头『日期』行停留 2026-09-09，§15.4 v0.13 行为 2026-09-16——版本半边落地、日期半边未同步 | F-159 |
| G-4 | 建议 | 零行为影响杂项（F-046/F-063 同型）：CommandServiceTest.cpp:1112 注释笔误"ReRejected"；CommandServiceImpl.cpp recheckConfirmations 循环内 currentBase lambda 每 finding 重复构造同一值（可提循环外） | F-160 |

## 裁决

**pass** —— 4.1～4.11 全部通过，无阻断级问题；建议级 G-1～G-4 已转登 findings.json（F-157～F-160，随本 evidence 分支提交）。验收者不执行合入；送验对象冻结于 cdb242646be12eab64678cb93794be9efeef4d51。
