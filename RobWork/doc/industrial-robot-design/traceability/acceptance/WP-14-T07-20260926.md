# WP-14-T07 验收记录（attempt 1，2026-09-26）

| 字段 | 值 |
| --- | --- |
| 任务 | WP-14-T07（实现工艺模板、工位镜像、四类阵列与需求集批量撤销；单元 requirements） |
| 送验对象（冻结 SHA） | `16eefa15648c62bdd77207197abeb1d7c4476e97`（origin/wp14-t07 == 16eefa15，零分支漂移；本地 wp14-t07 分支引用未被触碰） |
| base | `1b3a73712cd8305a9476c855297ea240c06412a3`（== origin/redesign-main 当前尖端——T07 尚未合入，口径自洽） |
| diff 范围 | `1b3a7371..16eefa15`（2 提交：fdbb5367 实现 → 16eefa15 卡同步＋留痕） |
| 验收环境 | detached worktree @ 16eefa15（复现现场 D:/acc_t07/wt-a）；集成全新构建树 D:/acc_t07/build-int；独立冒烟构建树 D:/acc_t07/build-smoke；base 侧临时 worktree @ 1b3a7371（D:/acc_t07/wt-base，门禁比对后即删）——全部由验收者亲手执行，未采信实施者运行结果 |
| **verdict** | **pass**（4.1~4.11 全部通过；无阻断级问题；建议级 2 项 F-373/F-374 已转登 findings.json，不阻塞合入） |

---

## 4.1 改动范围 —— 通过

- `git diff --name-status 1b3a7371..16eefa15`：30 个变更文件（9 M＋21 A），逐一对照契约 `allowedFiles` 三前缀**零越界**——
  - `requirements/**` 9 个：CMakeLists.txt（M）；新增 TemplateArray.hpp/.cpp＋TemplateArrayTest.cpp（3 A）；修改 DiagCodes.hpp/.cpp＋Editor.hpp/.cpp＋DiagCodesTest.cpp（5 M）；
  - `units/requirements.md` 1 个（M）；
  - `traceability/builds/wp14-t07/` 20 个留痕文件（全部 A）。
- `forbiddenFiles`（REQUIREMENTS.md/ARCHITECTURE.md）：diff 范围内零出现——零触碰。
- SA-02：`patches/` 零改动（diff 范围无 patches 文件）；requirements/ 与文档路径之外零文件触碰（无任何框架源码改动）。

## 4.2 可复现构建 —— 通过（两模式零错误，验收者全新构建树亲手复现）

- **集成模式**：worktree 检出后缺 gitignored 模板（F-007/F-062 既有登记事实）——自主复制主检出 `RobWork/RobWorkStudio/bin/`（RobWorkStudio.ini.template.static／ini.shared.in／bin/none/ini.template）后配置：
  `cmake -S <worktreeA>/RobWork -B D:/acc_t07/build-int -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=<vcpkg>/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`（裸命令缺 Boost——主树缓存同口径两参数，AGENTS §4.1 未载明的既有缺口）
  → configure exit 0；`grep RWS_BUILD_INDUSTRIALROBOT CMakeCache.txt` → **`RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`**（验收树实测）。构建 `sdurws_ird_requirements`＋`sdurws_ird_requirements_test`＋`sdurws_ird_requirements_contract_test`（Release）三目标 **exit 0 零错误**。
  构建前置 `tasklist` 核查无 RobWorkStudio.exe/msbuild 残留进程——未动用 taskkill。
- **独立冒烟模式**：`cmake -S <worktreeA>/…/industrialrobot -B D:/acc_t07/build-smoke -DCMAKE_TOOLCHAIN_FILE=<vcpkg>/vcpkg.cmake -DCMAKE_PREFIX_PATH=<Qt6>` → configure exit 0（与送验 smoke-configure.log 同款 "standalone smoke mode" 横幅）；全量构建 exit 0（日志中 14 处 "error" 字样均为 Errors.cpp/ErrorsTest.cpp 文件名，非编译错误）；还原变异后两测试 exe 直接运行 **141/141＋5/5 全绿**。
- 两条构建树均为本次验收从零配置，未复用任何既有构建树。

## 4.3 契约 verify 命令 —— 通过（3/3 逐条亲手执行）

| # | 命令（契约原文） | 结果 |
| --- | --- | --- |
| 1 | `cmake --build build --config Release --target sdurws_ird_requirements` | 执行（验收树路径代入）→ exit 0，零错误（验收者在全新构建树已按 F-358/F-367/F-368/F-371 家族在案口径自行补建两测试目标并执行，见 F-373 转登） |
| 2 | `ctest --test-dir build/RobWorkStudio/src/rwslibs/industrialrobot/requirements -C Release -L "^ird$"` | 执行 → **2/2 passed（100%）**（sdurws_ird_requirements_test、sdurws_ird_requirements_contract_test；ird label 合计 0.52s） |
| 3 | `pwsh -File RobWork/scripts/industrialrobot/validate-docs.ps1` | 执行（worktreeA 根起跑）→ **PASS（20 units, 12 trace entries, 189 task files）**，exit 0 |

补充：`validate-task.ps1 -TaskFile WP-14-T07.json` 亦由验收者复跑 → **PASS (1 tasks)**。

## 4.4 acceptance 条目逐项证据 —— 通过（6/6；具名证据均为验收者实跑产出）

| # | 契约 acceptance | 证据（验收者实测） |
| --- | --- | --- |
| 1 | 镜像坐标/姿态正确（V-06 黄金断言）＋不可镜像规则处置（V-07） | `ReqTemplateArray.MirrorGoldenNormalY/Z_WP14T07_ACC1`：位置 (1,2,3)→(1,−2,3)/(1,2,−3) 轴对齐反射逐位断言；姿态解析闭式（法向 Y→(−roll,+pitch,−yaw)、法向 Z→(−roll,−pitch,+yaw)，1e-12 rad）——TemplateArray.cpp reflectRpy 以 M·R·M 反射共轭＋Z-Y-X 提取实现（含万向锁 roll'=0 确定约定）。`MirrorPointAtTargetAndToolRollFree_WP14T07_ACC1`：目标点反射 (0,1,0)→(0,−1,0)、滚转区间反手性 [−0.5,0.9]→[−0.9,0.5] rad。`MirrorUnmirrorableRulePending_WP14T07_ACC1`：AlignGeometryNormal/AlignFrame 两源镜像→**规则 kind 与目标/特征逐字段保留原样（不降级 Fixed）**＋参数快照 orientation-pending 标记＋恰 2 条 REQ-DERIVE-MIRROR-PENDING warning。全绿实测 |
| 2 | 四类阵列生成（数量/间距/角度逐一断言＋溯源完整＋源零回写 I-REQ-10） | `LinearArrayCountAndSpacing`（0.5 m×3 条位置逐一断言）、`RectangularArrayGrid`、`CircularArrayRadiusAndAngle`（θᵢ=start+step·i 极坐标定位）、`PolylineArrayArcLength`（条数=floor(总弧长/间距) 派生）——四构型全绿；`LinearArrayMultiSourceProvenance`＋夹具 `assertProvenanceShape`（generatorId/linked=true/instanceId 非空/kind 键）＋`assertDerivedIndependent`（独立 ObjectId＋generation 同批同 instanceId＋源条目出口零改写）——溯源完整与零回写结构化断言。全绿实测 |
| 3 | 六类工艺模板＋重生成冲突＋解除关联＋产物可手改 | `TemplateSixKindsGoldenDefaults_WP14T07_ACC3`：六类黄金表逐类断言（网格 2×1/3×1/2×2/1×1/1×1/2×1、generatorId 词表、ProcessTag 就近映射、validateTaskPoint 全链通过、source=UserProvided 非 DerivedReadOnly、首格=origin）；`TemplateApplyAndRegenerateReplace`＋`RegenerateConflictKeepsHandModified_WP14T07_ACC3`（手改容差 1e-3→5e-3 m 后重生成→恰 1 冲突＋REQ-DERIVE-REGENERATE-CONFLICT、替换面仅未手改条、应用后手改值 5e-3 原样保留不静默覆盖）；`UnlinkDisablesRegenerationAndAllowsHandEdit_WP14T07_ACC3`（linked→false 后重生成不替换＋产物可手改）。全绿实测 |
| 4 | 批量整体回滚＋删除源提示＋再派生无环 | `BatchApplyUndoRedoAtomic_WP14T07_ACC4`：2 条镜像批次单次 applyEdit→edits==1（恰一次入栈）→一次 undoLocal 整体回滚（3→1 条）→无可再撤（栈空）→一次 redoLocal 整体重做（Editor.cpp 批次路径单快照单 push——代码 Editor.cpp ⑤步）；`DeleteSourceWithLinkedDerivedWarns_WP14T07_ACC4`：删除被引用源→**允许**（accepted）＋恰 1 条 REQ-DERIVE-SOURCE-REMOVED 且 context 含 `linked-derived=1`＋派生条目 P1-M 独立留存；`ReDerivationAcyclicNoLivePropagation_WP14T07_ACC4`：对派生条目再镜像→新批次新 instanceId、溯源链 source-id 留痕、改源零传播（一次性参数快照非活性链接）。全绿实测 |
| 5 | 局部撤销与项目级撤销分离＋不绕过命令 | `LocalUndoAndCommandPathSeparation_WP14T07_ACC5`：①编辑器批次应用/undoLocal/redoLocal 全程端口 objectRefs 零变化（零修订）；②草稿经 apply-requirement-set prepare→Planned＋根＋点集两写入＋快照逆命令（inversePayload 解码为 Restore、点集逆槽字节==基线 canonical 字节位级保真——PA-2）＋prepare 零副作用；③两编辑器/命令路径互不串扰＋同载荷重放同计划（确定性）。全绿实测 |
| 6 | 双模式构建零错误＋ctest 全绿＋留痕＋ird_gates 零命中 | 见 4.2/4.3/4.10 与下方 ird_gates 专段——全部验收者实跑达成 |

**ird_gates base..head 归一化比对（编排者口径，验收者独立复算）**：引擎直跑 `cmake -DIRD_ROOT=<两侧各自源根> -P cmake/ird_gates.cmake`（base..head 间引擎文件零改动，diff 空）——head 侧 exit=1 且 **127 行** IRD-GATE 记录、base 侧 exit=1 且 **127 行**；绝对路径占位归一化后排序 **diff 为空（IDENTICAL_HIT_SETS）——零新增门禁命中**。验收者归一化产物与送验附件 `ird_gates_base/head_hits_normalized.txt` 逐行一致（仅行尾 CRLF/LF 差异，`--strip-trailing-cr` 后 diff 空）——送验门禁证据与实测吻合。两侧 exit=1 均为 DTB §4.5 登记的存量例外既有状态（requirements 测试目标 SUB 直链 testkit＝F-359 在案、ui R3 族、io/modeling LIB 族），非本任务引入。

## 4.5 测试真实失败能力 —— 通过（3 处实质变异全部真变红，还原后复绿）

变异窗口＝worktreeA 源树＋冒烟构建树增量重建；每次变异→具名测试真实变红→`git checkout --` 还原→三处全部还原后再重建全量复跑（T14 教训时序）；还原后 worktreeA `git status --porcelain -- RobWork/` 计数 0（零残留），HEAD 恒为 16eefa15。

| 变异 | 注入点 | 变红测试（实测输出） |
| --- | --- | --- |
| M1 镜像反射公式翻转 | TemplateArray.cpp `reflectPosition`：`p − 2.0·d·n` → `p + 2.0·d·n`（三分量） | `MirrorGoldenNormalY_WP14T07_ACC1`＋`MirrorGoldenNormalZ_WP14T07_ACC1` **FAILED**（位置黄金逐位断言失配）——2 tests ran, 2 failed |
| M2 阵列间距倍增 | TemplateArray.cpp Linear 分支：`spacingM·i` → `spacingM·i·2.0` | `LinearArrayCountAndSpacing_WP14T07_ACC2` **FAILED**（间距/位置逐一断言失配）——1 failed |
| M3 删除提示抑制 | Editor.cpp 删除保护扫描：`if (linkedDerived > 0)` → `if (false && linkedDerived > 0)` | `DeleteSourceWithLinkedDerivedWarns_WP14T07_ACC4` **FAILED**（REQ-DERIVE-SOURCE-REMOVED 零产出）——1 failed |

三次还原后全量复跑：**141/141＋5/5 全绿**（冒烟树实测留档）。测试非恒真、有真实检出力。

## 4.6 架构红线（AGENTS §5 八条）—— 通过

1. **L2 零 Qt**：T07 新增/修改六产品文件 grep `#include.*Qt|Q_OBJECT` 零命中；TemplateArray 链接面仅库内既有边（门禁 R3 零新增佐证）。
2. **业务域互链（R-1）**：新增产品面跨单元 include 零命中（grep 证）；门禁比对 R1 命中 base=head（1=1，既有 modeling_plugin 一处）。
3. **跨单元私有头（R-2）**：TemplateArray.hpp 跨单元 include 仅 `core/DiagData.hpp`（include/sdurws/ird/core/ 公共头——登记边）；Editor.cpp→DiagCodes.hpp 为本单元内头；测试面消费 project 公共头（CommandService.hpp/QueryPort.hpp 实存于 project/include/）为 test/** 允许域。门禁 R2 零命中两侧一致。
4. **框架零修改（SA-02）**：见 4.1——patches/ 与框架零改动。
5. **名称前缀（R-4）**：T07 产品文件 grep `RobWork` 字面零命中；门禁 R4 命中 base=head（1=1，既有）。
6. **权威唯一（PA-1）**：模板/镜像/阵列为纯函数草稿生成辅助（零工作集写副作用）；落草稿唯一入口＝编辑器 applyEdit(EditBatch)；项目级写入仅经 apply-requirement-set 命令通道（ACC5 用例结构化自证）——不绕过命令、不私设第二写路径。
7. **不可变历史（PA-2）**：局部撤销/重做零修订（端口字节级断言）；项目级逆命令为快照逆槽（逆槽字节==基线前版 canonical 字节位级保真断言）；编辑器无任何 project 写端口参数。
8. **不继承 old/ 实现**：T07 文件从零编写；diff 无 old/ 路径。

## 4.7 注释规范（AGENTS §2）—— 通过

- **公共头全查**：`TemplateArray.hpp`（435 行）文件头中文 Doxygen 完整——设计依据（卡 §3.3/§7.1/§7.2/§9.5/§4.7/§4.3/§3.4＋REQ-07/REQ-11/AT-24/NFR-COR-01/02/03/D-REQ-4＋契约 acceptance 1~5）＋"草稿生成辅助"背景段（为什么模板不绕过命令——PA-1）＋I-REQ-10 服务面执行说明；每个类型/函数/成员带 `@brief`/方向/单位（m/rad/无量纲）/坐标系语义（refFrame 系）/校验边界/线程安全/确定性声明；三参数快照键值语义逐键登记（非活性链接说明）。`Editor.hpp` 新增 applyEdit(EditBatch) 重载注释完整（批量原子性 @post、三段校验链、线程约束仅 UI 线程）；`DiagCodes.hpp` 三新码逐码 §9.6 行号＋语义＋消费者登记。
- **实现文件抽查 ≥3**：`TemplateArray.cpp`（1396 行：文件头"确定性来源"段——%.17g 无损往返/strtod 全消费/IEEE754 确定运算/冲突清单升序；反射几何逐步注释——反射等距性/M²=I 对合性/黄金闭式逐轴登记/万向锁分支推导；四构型校验与快照键注释逐段）；`Editor.cpp`（批次入口五步注释——快照回滚底座/替换名 §5.1 保护同源/名称三面核对/先校验后写入/恰一次入栈；删除提示分支带"允许＋知情登记"语义注）；`DiagCodes.cpp`（三码登记共同口径段——warning 级/ResourceMissing 分类依据/文案键 P-DIAG-9 约定；表尾追加纪律注）。均达标。
- **测试文件**：文件头黄金断言口径段（轴对齐反射逐位/解析闭式 1e-12 依据）＋每用例中文说明对应 acceptance 编号（ACC1~ACC5）＋IRD_TEST_INFO 携带 REQ-06/07/09/11、AT-24、PA-2 追溯字段；夹具断言辅助（assertProvenanceShape/assertDerivedIndependent）带 I-REQ-10 中文注。
- F-372（Capture.cpp 起草式注释）本任务未触及该文件，无再现面。

## 4.8 文档同步＋findings 消账核对 —— 通过（1 项表头邻位残留转登 F-374；F-369 经实测确认已补正）

- 单元卡 `units/requirements.md` §14.6 **v0.7**（2026-09-26）登记与实文逐项对查为真：①§9.5 ITemplateArrayService 代码块随落位修订（applyMirror 源入参 `std::vector<TaskPoint>`/ArrayParams 内嵌 sources/regenerate instanceId 以 string 承载/unlinkGenerator 自由函数——DTB §5.4 等价调整随卡登记，与实现逐字一致）；②§9.6 表 T07 两码标〔已落位〕＋表尾增登 REQ-DERIVE-SOURCE-REMOVED 行（实现期增登先例——PLAN-MISSING 同款；增登合法性成立：该提示分支原 14 码无承载面）；③DiagCodes 工厂清单 13→16 表尾追加＋DiagCodesTest 分批封闭断言随附同步（kStagedSection96Codes 恰 16 项＋descriptors.size()==16 断言×5 处＋T07DeriveDescriptorsMatchSection96Rows 逐字段新用例）；④卡头「文档版本」行 **v0.5→v0.7**（行 7，显式注明"v0.6 表头行漏同步已随本版补正——F-369"）——F-369 所指缺陷在送验对象中经实测消除（卡头 v0.7 == §14.6 末行 v0.7），建议治理收尾时将 F-369 置 fixed。
- **残余（转登 F-374）**：卡头「日期」行（行 8）仍为「2026-09-26（v0.5）；2026-09-22（v0.1）」——日期值 2026-09-26 对 v0.7 仍准确（同日），但版本括注停留 v0.5，F-123/F-133/F-369 同族邻位残留（纯标注滞后）。
- 追踪矩阵/状态：validate-docs PASS（12 trace entries）；任务状态翻转归治理收尾（PIPE 口径），本次不涉及（契约 status 保持 ready——note 原文"放行归治理会话"）。
- **findings.json 未关闭条目相关性核对**（以 worktreeA 检出版为基准，最大号 F-372）：
  - **F-369**：本任务已实测补正（见上——消账依据在案，建议收尾置 fixed）；
  - **F-359**（requirements 测试目标 SUB 直链 testkit 存量命中）：门禁比对确认 base=head 零变化，未再引入；
  - **F-363**（"IRDREQO" 魔数钉子测试，所有者指定"T04+ 顺手落"）：本任务未触 Codec.cpp/CodecTest，未消账亦无新增积压责任，仍在案；
  - **F-370/F-041 散文计数家族**：本任务两提交与留痕的全部散文计数（141/141、5/5、ReqTemplateArray 17/17、ctest 2/2、门禁 127=127、20 件留痕）经验收者逐项实测**全部准确**——无再现；
  - **F-361/F-364/F-366 标题格式家族**：两提交均 `[WP-14-T07]`＋动词开头（"实现…"/"同步…"）——无再现；
  - **F-372**（Capture.cpp 注释行文）：本任务未触及该文件——无再现面；
  - **F-300/F-358/F-367/F-368/F-371 家族**（契约 verify 缺测试目标构建行）：本任务契约同型缺口再现→**F-373 转登**；
  - F-007/F-009/F-062（worktree/全新树环境前置）：本次复现属实（bin 模板缺失＋集成配置需 toolchain/Qt 前缀），既有条目已覆盖，不重复登记。
  - 无静默积压。

## 4.9 commit 质量 —— 通过（无粉饰）

- fdbb5367 `[WP-14-T07] 实现工艺模板、工位镜像、四类阵列与需求集批量撤销`／16eefa15 `[WP-14-T07] 同步单元卡落位登记与验证留痕（双模式构建/测试/门禁证据）`——两提交均 `[任务编号]` 前缀＋动词开头（F-361 家族零再现），正文 What/Why/验证/影响面四段齐备＋Acc-Covered 对账行（1,2,3,4,5／6）。
- 防粉饰核对：提交正文声称 UT 141/141、ReqTemplateArray 17/17、contract 5/5、ctest 2/2、门禁 IDENTICAL 127=127、双模式零错误、validate 双 PASS——与验收者独立实测逐项一致（送验 XML ReqTemplateArray tests="17" failures="0"、报告 total=141/passed=141 与 total=5、门禁附件与复算逐行相同）；无计数失准、无粉饰。

## 4.10 留痕完整 —— 通过

`traceability/builds/wp14-t07/` 20 件全部入库且计数与验收者独立复现一致：两 gtest XML（tests=141/failures=0、tests=5/failures=0——与本次实跑同值；ReqTemplateArray tests="17"）、ird-test-report 两份（total=141/passed=141、total=5，用例级明细）、集成构建日志、ctest 日志、冒烟 configure/构建日志（主检出＋worktree 双份复核）、两测试 exe 运行日志（主检出＋worktree 双份）、validate-docs/validate-task 日志、门禁 base/head 引擎原始输出各一份＋归一化命中集各一份＋比对结论 md（直方图与验收者复算吻合、附件归一化文件与验收者产物逐行一致）。gtest XML 原样留存、无手工转抄痕迹（F-346/348 家族无再现）。任务状态翻转/resultNote 归治理收尾，与 T05/T06 同口径。

## 4.11 偷懒/缩水实现扫描 —— 通过

- ① diff 新增行（requirements/**＋units/requirements.md）`TODO|FIXME|未实现|not implemented` 扫描**零命中**；`stub|placeholder` 大小写不敏感亦零命中（门禁原始输出附件中的 `View3DPlaceholder.cpp`/`StubHandlers.cpp` 等字样为既有 ui 单元文件名在门禁扫描输出中的引用，非本任务代码）；无"触发式延后"豁免需求。
- ② 空实现检测：applyTemplate（校验链＋参数快照全量＋行主序网格派生全实质）、applyMirror（同系核对＋法向单位化＋逐规则处置五分支全实质）、applyArray（四构型校验/几何/快照全实质——Polyline 弧长参数化＋段内插值实质）、regenerate（批次定位→快照全量逆→确定性重算→逐名称配对→冲突诊断五步全实质）、unlinkGenerator、编辑器 applyEdit(EditBatch)（五步全实质）——无仅返回默认值的桩。
- ③ 逐条证据表：见 4.4——acceptance 6 条逐条注明具名测试/命令输出/实文位置，零缺证。
- ④ 测试强度：断言校验行为与边界（黄金逐位/1e-12 闭式、逐字段溯源形状、恰 N 条诊断＋context 键内容、整批回滚栈空断言、逆槽字节位级保真、源零回写值相等断言、负向拒绝面——非法参数/空批次/重复名/引用保护）；4.5 三处变异实证测试会红，非恒真。

---

## 发现问题清单

**阻断级**：无。

**建议级（已转登 traceability/findings.json）**：

- **F-373**：F-300/F-358/F-367/F-368/F-371 家族新例——tasks/foundation/WP-14-T07.json verify 数组仅 3 行（库名构建＋ctest＋validate-docs），缺 sdurws_ird_requirements_test／sdurws_ird_requirements_contract_test 显式构建行；验收者已按家族在案口径自行补建并执行（141/141＋5/5），建议契约模板统一补构建行（与 F-367/F-368/F-371 合并落地面）。
- **F-374**：F-123/F-133/F-369 同族邻位残留——units/requirements.md 卡头「日期」行版本括注停留 v0.5（行 8），未随 v0.7 推进；日期值本身对 v0.7 仍准确（同日 2026-09-26），纯标注滞后、validate-docs 不校验该维度。

## 裁决

**verdict＝pass**。4.1~4.11 全部通过、无阻断级问题；建议级 2 项（F-373/F-374）已转登 findings.json，不阻塞合入。合入决策归所有者（本验收者不合入、不改实施产物、未向任务分支 wp14-t07 推送任何提交；送验对象冻结于 16eefa15648c62bdd77207197abeb1d7c4476e97）。

## 验收者环境说明

- 复现现场：`git worktree add --detach` @ 16eefa15（base 1b3a7371）；送验前核验 origin/wp14-t07 == 16eefa15（零漂移）；主检出被并发会话持有（wp24-t01），验收者全程未切换主检出分支、未在主检出执行任何 git 写操作；集成/冒烟构建树均为本次新建；base 侧临时 worktree（门禁比对用）比对后即删；记录经 acc/WP-14-T07/1 分支推送回传。
- 环境前置（F-007/F-062 既有登记事实）：worktree 检出后自主复制 bin/ 三件 ini 模板；集成与冒烟配置均带 vcpkg toolchain＋Qt6 前缀（主树缓存同款 D:/software/Qt/6.11.1/msvc2022_64）。
- 本机无 RobWorkStudio.exe 进程锁干扰，全程未动用 taskkill；ird_gates 以引擎直跑（cmake -P）口径比对（两侧 exit=1 为 DTB §4.5 存量例外既有状态，未采信 VS 目标口径）；变异测试共用冒烟源树，严格"先还原再全量"时序。
- 本机代理不稳定风险按编排者口径以显式 refspec `git push origin HEAD:refs/heads/acc/WP-14-T07/1` 推送并 `git ls-remote` 核验 SHA 一致。
