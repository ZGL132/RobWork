# PRJ-T15 验收记录（acceptance record）

| 字段 | 值 |
| --- | --- |
| 任务 ID | PRJ-T15（契约测试与崩溃注入落地 PRJ-TX-1～14；unit=project；masterWp=WP-D/WP-04-T15） |
| 送验对象（冻结） | a52dbd9388c8241e8a1e92775ef83f2c2eae1fef（分支 wp04-t15；diff 范围＝13fdfb753c21aa377ec46ec7853f17ead2e9cb5d..a52dbd93，state.base..headSha） |
| 验收尝试 | attempt 1（分支漂移检测：git fetch 后 origin/wp04-t15 == a52dbd93，零漂移） |
| 验收环境 | 全新 detached worktree（git worktree add --detach acc_prjt15_repro @ a52dbd93）从零配置复现；冒烟独立目录 acc_prjt15_smoke；记录回传 worktree（acc/PRJ-T15/1 基于 origin/redesign-main@13fdfb75）；pwsh 7＋MSVC 2022 x64＋vcpkg 经典模式（主仓库 vcpkg 目录——git 未追踪，worktree 天然不含） |
| verdict | **pass**（4.1～4.11 全部通过；无阻断级问题；建议级 5 条转登 findings.json F-170～F-174） |
| 验收日期 | 2026-09-17 |

## 4.1 改动范围 —— 通过

- git diff --name-only 13fdfb75..a52dbd93 实证 16 文件：project 单元 10（CMakeLists.txt＋test/ 下 9：ProcessCrashContractTest.cpp、ProcessLockContractTest.cpp、Tx11Tx13SuiteTest.cpp、Tx15Child.{cpp,hpp}、Tx2DiagnosticRecordContractTest.cpp、TestMainReport.cpp、LinkageContractTest.cpp 修订、LockContractTest.cpp 修订）＋traceability/builds/wp04-t15/ 5 份 ird-test-report*.json＋units/project.md 1——逐文件比对契约 allowedFiles 三条通配（project/**、units/project.md、traceability/**）**全部在册**。
- forbiddenFiles（REQUIREMENTS.md／ARCHITECTURE.md）零触碰（diff 无该两文件）。
- 无未登记框架源码改动：diff 零 RobWork/RobWork/、RobWorkSim/、RWS 其他源码路径；patches/ 无新文件（SA-02）；**diff 零 core/ 路径文件**（acceptance 4 的 P-PR-1 零 core 修改实证）；零产品源码变更（src/ 下无文件）。

## 4.2 可复现构建 —— 通过（全新树从零，任务目标面；PRJ-T01～T14 判例口径）

- 集成模式：worktree A 全新从零配置，缓存确认 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`（configure 日志 Configuring done＋Generating done，exit 0）。
  - 环境备注（F-007/F-098/F-099/F-062 既有登记，非实施缺陷）：①worktree 不含 vcpkg——toolchain 指主仓库 vcpkg/scripts/buildsystems/vcpkg.cmake；②Qt prefix 补 CMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64＋CMAKE_CXX_FLAGS=-DNOMINMAX -DWIN32_LEAN_AND_MEAN -DWIN32（主树缓存同口径）；③RobWorkStudio.ini.template.static 被 [Bb]in/ gitignore 吞掉——自主树复制后第三次配置零错误（前两次失败即 F-007 所述必卡点复现）。
- 契约 verify 两目标构建：sdurws_ird_project_test **exit 0、": error" 计数 0**；sdurws_ird_project_contract_test **exit 0、error 计数 0**（仅 Tx15Child.cpp park token 宽→窄转换的既有模板实例化提示，非错误）。
- 独立冒烟模式：独立目录从零配置（vcpkg toolchain——AGENTS §4.1 口径，exit 0）＋全量构建 **exit 0、": error" 计数 0**。
- 全框架全量集成构建未单独重跑——RobWorkSim RWSimulatorPlugin 全新从零失败为 **F-008 已登记基线框架缺陷**（base 对照同样失败；PRJ-T13/T14 验收两次独立实证），本任务 diff 零框架触碰，按既往判例不归本任务、不构成阻断。

## 4.3 契约 verify 命令 —— 通过

逐条独立执行（worktree A 集成树，验收者亲手）：
1. `cmake --build build --config Release --target sdurws_ird_project_test` → **exit 0**（error 计数 0）；
2. `cmake --build build --config Release --target sdurws_ird_project_contract_test` → **exit 0**（error 计数 0）。
（首轮全新树构建即此二命令；探针还原后再次重建复绿均 exit 0。）

## 4.4 acceptance 条目 —— 通过（四条逐项证据）

| # | 契约条目 | 证据（验收者亲手复现＋代码亲读） |
| --- | --- | --- |
| 1 | §11 表 PRJ-TX-1～14 逐项执行通过并留痕（gtest XML＋ird-test-report.json；每用例注明需求/AT；未执行不得标注通过） | **contract_test 28/28 PASSED**（既有 18 零回归＋新增 10）＋**unit_test 254 PASSED＋1 SKIPPED**（既有 253 零回归＋TX-13 过＋TX-11 显式跳过），集成与冒烟两模式亲手各执行一轮全部一致；TX 逐行对账（ird-test-report.json acceptanceMapping）：TX-1/TX-2/TX-3＝CommandContractTx1.*/Tx2DiagnosticContract.*/CommandContractTx3.*（contract）；TX-4＝TxEngineTest F1～F7＋F8 进程内（unit）＋F8 真进程五挡（contract，本任务）；TX-5/TX-6＝Walkthrough.*/UndoRedoServiceTest.*/DraftServiceTest.*；TX-7＝LockContractTest.*（T03 原语层）＋Tx7ProcessLock.*（本任务）；TX-8＝ArchiveContractTest.*＋Tx8ArchiveProcessKill.*（本任务）；TX-9/TX-10＝ProjectStoreTest.Tx9_*/TxEngineTest F2/F4/F7；TX-11＝Tx11CopyConsistency **SKIPPED 如实登记**（不计通过）；TX-12＝RevisionIndexTest；TX-13＝Tx13ReadOnlyAllWrites.*（本任务）；TX-14＝BuildRedLineTest.*＋LinkageContractTest.*。IRD_TEST_INFO 需求/AT 追溯：四份新增测试文件亲读在案（如 Tx13 用例 IRD_TEST_INFO{PM-07}、Tx2 用例 IRD_TEST_INFO{MDL-06,SA-15}/{AT-01}）；留痕面＝5 份 ird-test-report*.json 在库（用例级明细四份 listenerReport＋任务级汇总，durationMs 差异证实两次独立运行非复制） |
| 2 | testkit 消费边界用例化声明在案；产品目标不链 testkit（T-1 红线） | **FaultInterceptor 经 src/win32/IFileOps 接缝**＝Tx15Child.cpp ParkFileOps（D-10 形态亲读确认：生产窄接口 IFileOps、测试侧 fake 装饰 Win32FileOps，faultpoint ID 取 IFileOps.hpp 冻结登记）；**TempDir**＝F8/TX-7/TX-8/TX-13 四个新夹具；**IRD_TEST_INFO/IRD_EXPECT_IDENTICAL**＝Tx2DiagnosticRecordContractTest/Tx11Tx13SuiteTest 消费（AssertMacros 展开于消费方 TU）；**TestRecordListener**＝双 main 安装（TestMainReport.cpp 自有 main＋LockContractTest main 增量——gtest_main→gtest 换轨亲读确认）；**产品目标零 testkit 边**＝LinkageContractTest.NoTestkitEdge_DT_BUILD_T1 双侧断言（产品链接块零 testkit＋两测试目标链接块显式登记消费）＋ird_gates 实跑 **T1 产品面零新增命中**（唯一 T1 命中＝既有 testdata_lint 基线项 F-019） |
| 3 | TestProcessRunner 接入的 F8/PRJ-TX-7/8 真进程场景按 testkit §10.1 节奏触发；前置未就绪如实标注 | **亲手执行 3 轮**（首轮全量＋复跑 2 轮 filter 组）：F8 参数化五挡（T1/T2T3/T4 未提交族＋T5/T6 已提交族——EventWatch.awaitFile 等停靠标记→TestProcessRunner.kill→outcome.Killed→重启 D-02 接管→RecoveryReport 逐项＋字节不变）＋Tx7ProcessLock 三场景（第二实例只读＋holder=父 PID／存活不接管 D-03＋强杀接管锁记录重写／崩溃后双竞争者恰一胜者——winner=Killed/loser=Exited 形态互证）＋Tx8ArchiveProcessKill（批次残留/manifest 不在/listRuns 空/恢复报告干净）——**3 轮 9/9 全绿零竞态**（F-156 偶发用例 CrashThenConcurrentTakeover_SingleWinner 未再现）；TK-T11 已 ready（sdurws_ird_testkit 库两模式构建在案——消费触发成立）；TX-11 前置未就绪＝GTEST_SKIP 登记阻塞原因（另存为/包实体阶段 B WP-04-T17/T18），skipped 不计通过——不带病前进 |
| 4 | P-PR-7/P-PR-4/P-PR-1 处置 | P-PR-7＝双编译 IModelCompilePort 桩（CommandContractTx3 既有三路在 28/28 内执行；**§5.3.6 单侧冻结签名零改动＝diff 无任何公共头文件**，4.1 实证）；P-PR-4＝归档线程模型进程内模拟（ArchiveContractTest.Ppr4_* 既有）＋真进程半边不预设 RunRegistry 对接细节（execution 详设定稿后二次对齐——与单元卡 v0.17 行口径一致）；P-PR-1＝用例构造的 core 数据（DiagnosticRecord/ConfirmableFinding/凭据/身份/事件）随 v0.1 基线消费，**零 core 文件改动**（diff 核对）＋Tx2 用例四路记录过 checkDiagnosticRecord 机器校验 |

ird_gates：验收者实跑（cmake --build build --config Release --target ird_gates）——**命中 11 项＝9×IRD-GATE-SUB＋1×IRD-GATE-T1＋1×IRD-GATE-R4**，与 wp04-t14 基线 9 项（F-145 后）比对**新增恰 2 项**：sdurws_ird_project_test→sdurws_ird_testkit、sdurws_ird_project_contract_test→sdurws_ird_testkit（本任务契约明文的 testkit 消费链接边——DTB §4.5 既有例外行『各 _test/_contract_test 经 testkit/policy 替身消费』同型实例；实例行增补归 WP-01-T03，随验收请求转呈，转登 F-173）；其余 9 项与基线逐项一致（F-019 的 testdata_lint 两项＋F-145 的 R4 ProjectStoreImpl.cpp 既有字面量——本任务 diff 未触碰该文件）；exit 1 属 SA-10 设计（命中即非零）；门禁自测 9 项（pass_clean/pass_r4comment＋7 项 fail 按预期检出）全部符合。validate-task.ps1 -TaskFile …PRJ-T15.json → **PASS (1 tasks)，exit 0**（验收者亲跑）；validate-docs.ps1 → **PASS (20 units, 12 trace entries, 150 task files)**。

## 4.5 测试真实失败能力 —— 通过（双探针即红、还原复绿）

- 探针 1：Tx11Tx13SuiteTest.cpp ① 段 submit 断言期望 `LockHeldByOther`→`ContextClosed` 反转 → Tx13ReadOnlyAllWrites.TraversesEveryWriteEntry_AllRejectedWithStableDiag **FAILED**（Expected <00-00-00-00> vs Actual <09-00-00-00>——断言有牙齿，非恒真）。
- 探针 2：ProcessCrashContractTest.cpp kBoundaries 参数表 `{"T5", true}`→`{"T5", false}`（提交点边界误标未提交族）→ TransactionBoundaries/F8ProcessCrash.KillAtBoundary_RecoveryReportAndByteInvariants/**T5_Uncommitted FAILED**（tip 期望 r0 实际已前移——参数化断言族真实区分已提交/未提交边界语义）。
- 逐次 `git checkout --` 还原＋重建：Tx13 **1/1 PASSED**、T5_Committed **1/1 PASSED** 复绿；`git status --porcelain RobWork/` 零输出（复现现场与送验 SHA 逐字节一致）。注：探针注入后对 .cpp touch 再构建，规避 F-068/F-075 增量构建时间戳陷阱。

## 4.6 架构红线 —— 通过

- **L2 零 Qt**：diff 全部新增 `#include` 亲扫（36 条）——零 Qt 头（`#include <Q*` 计数 0）。
- **R-1 业务域互链禁止**：链接面仅 sdurws_ird_project→core（既有唯一单元边）＋两测试目标→sdurws_ird_testkit（T-1 允许形态——仅测试目标，任务契约 acceptance 2 明文消费面＋NoTestkitEdge 双侧断言钉住产品面）；无任何业务域单元目标引用。
- **R-2 跨单元私有头禁止**：跨单元 include 全走公共路径（sdurws/ird/core/{Events,Identity}.hpp、sdurws/ird/testkit/{ContractCheck,Fault,Fixture,ProcessRunner}.hpp＋gtest/{AssertMacros,RecordListener}.hpp）；本单元 src/ 私有头（Codec/ObjectStore/RevisionIndex/TxEngine/win32/{AtomicFile,IFileOps}）仅测试目标消费（R-2 允许——同单元测试，CMake PRIVATE include 注入，既有先例同款）。
- **R-4 名称前缀拼接/剥离**：diff 零命中（ird_gates R4 唯一命中＝F-145 既有 ProjectStoreImpl.cpp 字面量，本任务 diff 未触碰）。
- **SA-02 框架零修改**：4.1 实证。
- **PA-1 权威唯一**：零产品源码变更，纯测试与留痕/单元卡登记；写路径/真值/名称解析零触碰。
- **PA-2 不可变历史**：F8 已提交挡断言"r0 文件逐字节不变——提交只增不改"即 PA-2 崩溃面测试化（亲读确认）。
- **不继承 old/**：无。

## 4.7 注释规范 —— 通过

- **公共头**：本任务零公共头新增（include/ 下无文件）；测试侧头 Tx15Child.hpp 全查合规——文件头五要素齐备（@file/@brief、设计依据 §12/§7.6/§11/testkit §6.5/§10.1、背景说明"为什么子进程与本测试可执行文件同体"、参数传递纪律含环境变量唯一权威登记、线程与生命周期）；runChild 逐参数 [in]/返回值/退出码语义注释完整。
- **实现文件抽查（要求 ≥3，实查 6——本任务全部新增文件）**：ProcessCrashContractTest.cpp（文件头：F8/TX-8 设计依据＋与 TxEngineTest 零重复声明＋停靠协作协议；kBoundaries 逐参数注释；夹具/用例锚定注释齐）、ProcessLockContractTest.cpp（三场景逐用例锚定 PM-07/D-03/D-02/§9.1/§9.9＋"不判据声明"——失败者 holder 字段竞态不判据的论证）、Tx15Child.cpp（分派表/停靠语义/错误语义退出码表逐项＋ParkFileOps pre/post 停靠时序论证）、Tx2DiagnosticRecordContractTest.cpp（allowTransient=true 依据＋P-PR-1 处置声明）、Tx11Tx13SuiteTest.cpp（错误码清单口径——redo 前置违约不私造稳定码 CR-08 论证）、TestMainReport.cpp（为什么替换 gtest_main＋监听器所有权"非 owning——官方 recommended 做法"）。
- **AGENTS §2.5 高危信息表**：所有权（监听器静态存储期/TempDir 析构清理/store 保活至进程终局）、线程（单线程入口纪律/子进程单线程/ parkForever 可中断等待）、错误语义（子进程退出码 0/3/4/5/8/9 逐值含义＋"失败一律非零退出码表达——不留假阳性通道"）、确定性（宽字符环境变量传参纪律——"窄 argv 经 ANSI 代码页会破坏非 ASCII 路径"）、单位/坐标系（本任务无物理量，不适用）。均齐备。

## 4.8 文档同步 —— 通过（含 findings 未关闭条目相关性核对）

- **实现与任务卡零偏差**：acceptance 四条逐项对账（见 4.4）全部落位；TX-11 的 skipped 处置、TX-13 错误码集合＝设计清单、testkit 消费面清单与契约 note（interUnit=true 判定）逐字对应。**零设计变更**（§11 表为用例体实现、§7.6 F8/§9.x 语义全部照既有文档锚定）。
- **单元卡同步**：units/project.md §15.4 v0.17 行在位（2026-09-17），卡头文档版本行 v0.16→v0.17 已同步；v0.17 行声明的测试计数（contract 28/28＋unit 254+1 skipped＋真进程 3 轮稳定）、门禁基线比对（11＝9＋2×SUB）、P-PR 三处置——与验收者实测**逐项一致**。
- **findings.json 未关闭条目相关性核对（148 条 open）**：①**F-156**（LockContractTest.CrashThenConcurrentRecover 家族偶发）——与本任务同单元并发锁面直接相关，验收者 3 轮真进程复跑未再现（本记录 4.5/4.4-3 留证）；②**F-145**（ird_gates R4 1 命中基线）——实测仍在、零新增；③**F-019**（testdata_lint 的 T1/SUB 命中）——实测仍在，消账归口门禁白名单任务，非本任务辖域；④**F-122/F-133/…/F-168 卡头日期行家族**——本任务再现第十二例（卡头日期停留 2026-09-09，§15.4 v0.17 行＝2026-09-17；版本半边已同步）——转登 **F-170**；⑤**F-007/F-098/F-099/F-062 环境坑**——本次验收再次踩中（ini 模板/vcpkg/Qt/NOMINMAX 口径，已登记有处置建议，不新登）；⑥**F-068/F-075 探针增量陷阱**——本验收按其建议 touch 规避；⑦F-115 先例（diagnostics_contract_test→testkit 新增 SUB 的处置先例）——本任务 2×SUB 同型处置（如实登记＋增补归 WP-01-T03），转登 **F-173**。
- 追踪矩阵（trace-matrix.json）自 WP-00-T02 冻结后非任务级登记面（先例同口径）；任务状态/队列由编排者收尾更新。

## 4.9 commit 质量 —— 通过（附建议级）

- a8b01ac3 标题「[WP-04-T15] 落地 PRJ-TX 契约测试与崩溃注入用例体（真进程 F8/TX-7/TX-8＋TX-2/TX-13/TX-11）与 testkit 消费接入」——**动词开头合规**；正文四段（What/Why/验证/影响面）齐备，逐文件改动清单与 diff 一致；验证结论（两模式 28/28＋254+1 skipped、真进程 3 轮稳定、gates 11＝9＋2、validate-task PASS）经本验收逐项亲手复现**全部为真**；Acc-Covered: 1,2,3,4 在位。
- a52dbd93 标题「[WP-04-T15] 验证留痕与单元卡同步（PRJ-T15 落地登记 v0.17）」——前缀后以名词短语「验证留痕」开头非动词开头（建议级，**F-012 家族再现**，转登 **F-171**）；正文四段齐备、验证结论实测为真（无结论性粉饰）；但正文『改动内容』声称入库的"双模式构建日志…gtest XML 四份＋运行日志四份、ird-gates.log、validate-task.log"与实际入库不符——提交树 traceability/builds/wp04-t15/ 仅 5 份 ird-test-report*.json（见 4.10——转登 **F-172**）。

## 4.10 留痕完整 —— 通过（附保留：日志/XML 幻影引用，转登 F-172）

- **在库**：任务级 ird-test-report.json（acceptance 四条逐项对账＋builds/testRuns/gates/validateTask 全字段）＋四份 listenerReport 明细（unit/contract × smoke/integration——用例级 name/status/durationMs，与 gtest XML 等价承载，两模式 durationMs 差异证实真实独立运行）。
- **未入库（幻影引用）**：ird-test-report.json testRuns 声明的 4 份 gtest XML 与 4 份 runLog、builds 数组 4 条 log（integration-build/verify-target-build/smoke-configure/smoke-build）、gates.log（ird-gates.log）、validate-task.log——提交树均不存在。根因拆分：*.log 七份被 .gitignore:87 全局 `*.log` 吞（**F-010 根因第九次踩中**——F-081/F-083/F-085/F-087/F-088/F-090/F-153 之后，处置建议仍为 traceability/** 负向豁免或 .txt 后缀约定）；4 份 gtest XML **不在 ignore 规则内**（check-ignore 实证）——属实施者生成后未提交。等价明细已由 listenerReport JSON 在库承载，无结论性失实；按 F-166（PRJ-T14 同型）判例为建议级不阻断。
- phase-one-validation.log 未更新——PRJ-T12/T13/T14 先例同口径（该 log 为阶段一专用；本任务 resultNote 载体＝任务级 ird-test-report.json，已更新且完整）。

## 4.11 偷懒/缩水实现扫描 —— 通过

- **①标记扫描**：diff 全文 `TODO|FIXME|XXX|HACK|not implemented|未实现|待实现|占位实现` 扫描——零有效命中（唯一"占位实现"字样位于 units/project.md §15.4 **v0.16 历史行**文本内，为 PRJ-T14 缺陷留证的历史记载，非本任务未实现标记）；`stub` 命中 10 处全部为既有测试桩类型名（StubSink/StubInteraction/StubHandlers——PRJ-T02 起既有基础设施）。契约显式登记的豁免：PRJ-TX-11 用例体＝前置未就绪显式登记（acceptance 3 明文允许——GTEST_SKIP 如实标注，另存为/包实体阶段 B WP-04-T17/T18 承接）。
- **②空实现检测**：本任务零产品源码变更；新增测试函数体无空函数/仅返回默认值实现（六个文件逐行通读）。
- **③逐条证据表**：见 4.4 acceptance 四条逐项表——每条均有具名测试＋验收者亲手复现结果，任一条不缺。
- **④测试强度**：断言校验行为与边界（错误码精确匹配、字节级不变断言、退出码形态互证、恰一胜者原子性）——非恒真、非"只编译通过"（4.5 双探针实证即红）；真进程用例 3 轮稳定无竞态；与 4.5 联动抽改验证会红。

## 问题清单（建议级，全部转登 findings.json，无阻断级）

| # | 级别 | 摘要 | findings 编号 |
| --- | --- | --- | --- |
| G-1 | 建议 | units/project.md 卡头『日期』行停留 2026-09-09（§15.4 v0.17 行＝2026-09-17；版本半边 v0.17 已同步）——F-122/F-133/…/F-168 家族第十二次再现 | F-170 |
| G-2 | 建议 | a52dbd93 标题『验证留痕与单元卡同步…』前缀后名词开头非动词开头——F-012 家族再现（实现提交 a8b01ac3 合规） | F-171 |
| G-3 | 建议 | a52dbd93 正文与 ird-test-report.json 声称入库的 gtest XML×4/构建·门禁·validate-task 日志/运行日志×4 实际未入库（*.log 被 .gitignore:87 吞＝F-010 第九次踩中；XML 不在 ignore 内属未提交）——幻影清单家族再现；等价明细以四份 listenerReport JSON 在库承载，无结论性粉饰 | F-172 |
| G-4 | 建议 | ird_gates 新增 2×SUB 命中（project_test/contract_test→sdurws_ird_testkit）＝契约明文消费面，DTB §4.5 例外行实例增补归 WP-01-T03——转呈跟踪（F-115 处置先例同型） | F-173 |
| G-5 | 建议 | ProcessCrashContractTest.cpp 两处 startChild 以 `wchar_t exePath[MAX_PATH]` 固定缓冲取自身路径，GetModuleFileNameW 截断情形（返回 nSize-1）未检测——build 树路径未超 MAX_PATH，零当前行为影响微瑕（F-160/F-169 同型） | F-174 |

## 验收者环境与可复现性说明

- 复现现场：`git worktree add --detach acc_prjt15_repro a52dbd93`；集成配置三踩 F-007 环境点后成功（命令与参数见 4.2）；verify 两目标、双模式测试执行（集成 254+1/28＋冒烟 254+1/28）、真进程组 3 轮、ird_gates、validate-task、validate-docs、双探针注入/还原——全部验收者亲手执行。
- 验收者运行留痕留存于验收临时目录（/tmp/prjt15_*.log：cfg/build/smoke_build/unit_run/contract_run/ct_round2/ct_round3/gates/probe1_build/probe2_build/restore）；库内证据面以送验 5 份 ird-test-report*.json 为准（其实测数据与验收者复现逐项一致）。
- 本记录与 findings.json（F-170～F-174 追加）经 acc/PRJ-T15/1 分支 commit＋push 回传；验收者未向任务分支 wp04-t15 推送任何提交、未修改送验产物、未执行合入。
