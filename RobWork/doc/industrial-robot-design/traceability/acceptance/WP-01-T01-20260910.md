# WP-01-T01 对抗式验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | WP-01-T01 实现依赖红线与补丁门禁 ird_gates（unit=build，DTB §2.2） |
| 送验对象 | headSha `511d58f04165ab196da2a374b87a23e9593353e0`（分支 wp01-t01，base `ba87bdf67355128da575ada93f2112eae211a72d`） |
| 裁决 | **pass**（4.1～4.11 全过；建议级 1 项转登 findings F-009，不阻塞合入） |
| 独立性声明 | **降级模式（PIPE v1.7 §0.4）**：所有者指令"禁止使用子智能体"，本验收与实施同会话顺序执行。补偿措施：复现全部亲手重跑（独立 worktree 全新配置，不复用实施构建树与日志结论）、对抗式清单 4.1～4.11 不缩减、以 headSha 冻结产物为唯一裁决输入；发现的问题如实转登 |
| 验收者环境 | Windows 10.0.26200 x64、Git Bash、VS2022＋CMake 4.3、pwsh、vcpkg 经典模式（x64-windows）；worktree A（detached @ 511d58f）复现＋worktree B（acc/WP-01-T01/1 @ origin/redesign-main）记录回传 |

---

## 逐项证据（acceptance-protocol §4，4.1～4.11）

### 4.1 改动范围 —— PASS

base..headSha 共 10 个变更文件，逐一核对契约 allowedFiles：

| 文件 | 判定 |
| --- | --- |
| industrialrobot/cmake/ird_gates.cmake（新） | allowedFiles `cmake/**` ✓ |
| industrialrobot/cmake/ird_gates_whitelist.cmake（新） | 同上 ✓ |
| industrialrobot/CMakeLists.txt（改） | allowedFiles 显式列出 ✓ |
| doc/.../ARCHITECTURE.md（改，仅 §3.5 一行） | allowedFiles 显式列出（契约注明专为承载 O-21 补登）✓ |
| doc/.../development-task-breakdown.md（改，仅 O-21 行） | allowedFiles 显式列出 ✓ |
| doc/.../traceability/builds/wp01-t01/ 五件（新） | allowedFiles 未列——属契约 `outputs` 含 `traceability-update` 的治理留痕面，沿用 TK-T01 先例（其验收同样放行 traceability/builds/wp02-t01/）；建议级观察随记录留档 |

forbiddenFiles 零触碰（REQUIREMENTS.md、src/rws/**、src/rwslibs/ 下 industrialrobot 之外路径均未出现在 diff）；patches/ 零新增（SA-02：无登记外框架改动，工作区核对见 4.3/4.6）。

### 4.2 可复现构建（验收者亲手重跑） —— PASS

**集成模式（worktree A 全新配置，非实施构建树）**：
```
$ cmake -S <accA>/RobWork -B <accA>/build -G "Visual Studio 17 2022" -A x64 \
    -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_PREFIX_PATH=<Qt6.11> \
    -DCMAKE_TOOLCHAIN_FILE=<vcpkg> -DVCPKG_TARGET_TRIPLET=x64-windows
  → configure exit=0；缓存确认 RWS_BUILD_INDUSTRIALROBOT:BOOL=ON
$ cmake --build <accA>/build --config Release --target ird_gates → exit=0
```
- 首次配置失败（RobWorkStudio.ini.template.static 缺失）＝**F-007 所述预存环境缺口的再次复现**（该文件为未跟踪遗留物，.gitignore `[Bb]in/` 吞掉，全新 worktree 天然缺失；框架 RobWorkStudio/src/CMakeLists.txt:58 硬性需要）。处置：自主树复制该未跟踪文件后配置成功——环境修复，非改动送验对象；F-007 继续开放。
- 未做全新树全量构建：本任务 diff 不含任何 C++ 编译目标（ird_gates 为纯 cmake -P 门禁），全量构建的预存缺陷面（F-008 RobWorkSim 宏污染）与本任务零共享翻译单元，参照 TK-T01 对照实验结论不重复占用验收资源；实施侧主构建树全量 Release exit=0 日志已随任务提交（builds/wp01-t01/integration-build-full.txt）。

**冒烟模式（worktree A 全新临时目录，带 vcpkg toolchain）**：configure exit=0（standalone smoke mode）＋ `--target ird_gates` exit=0。

### 4.3 契约 verify 命令（逐条独立执行） —— PASS

1. `cmake --build build --config Release --target ird_gates`：A 树亲手执行 exit=0；自测 8/8（pass_clean 通过＋fail_r1/t1/r5/sub/r3/r4/t2 全部按预期检出对应命中码）＋真实树零命中——子运行退出码与命中码由引擎双向断言，目标 exit=0 即全过。
2. `pwsh -File RobWork/scripts/industrialrobot/validate-docs.ps1`：A 树亲手执行 → `PASS (20 units, 12 trace entries, 139 task files)`。

### 4.4 acceptance 条目逐条核对 —— PASS

| 契约验收条目 | 证据 |
| --- | --- |
| ①R-1~R-5＋T-1/T-2＋SA-02 全部实现（cmake -P＋git，零 Python） | 引擎检查项与命中码一一对应（cmake/ird_gates.cmake 文件头表）；`grep -ci python` 于引擎与数据文件零命中；SA-02 工作区核对在 A 树运行（git status porcelain 框架路径干净＋PATCHES.md 登记文件存在性核对） |
| ②对骨架与已落位目标现状零命中 | 三次运行日志（builds/wp01-t01/）：集成/冒烟/直接运行均零命中；INTERFACE 占位目标以变量名生成、无链接边，引擎按"跳过＋注释声明"处理，无误报 |
| ③白名单数据源＝§3.5 全量边＋dependency-graph.json 一致；O-21 消账 | 白名单 21 条边＝§3.5 原有 20 条＋补登 testkit→core（ARCH §3.5 新行，O-21 状态已回登 DTB）；GRAPH 检查双向核对（图⊆白名单＋多出边须有出处登记），验收者另行以伪造图实测双向命中（见 4.5） |
| ④R-4 例外登记册按 O-12 结论填充；未裁决前空册＋占位说明 | `IRD_R4_EXCEPTION_FILES`/`UNITS` 为空＋注释载明 O-12 未裁决、runtime 清单随 WP-06-T13、policy 行待确认（ird_gates_whitelist.cmake）；DTB §4.5 人工册对应行未被越权改动 |
| ⑤例外只能经 DTB §4.5 登记册；未登记例外检出即失败（含用例验证） | 引擎自测 7 个违例夹具全部检出（含 R-4 字面量夹具在空登记册下被检出）——证明"未登记例外＝失败"的机制真实存在 |

### 4.5 测试真实失败能力（验证验证者） —— PASS

- 门禁自测本身即"构造应失败输入确认会红"的机制化：7 个违例夹具在验收重跑中全部命中对应 IRD-GATE 码，干净夹具通过（防恒真/防恒红双向验证）。
- 验收者附加两项独立反向实验：①伪造 dependency-graph.json（仅含 core→ui 边）→ 引擎按预期输出 IRD-GATE-GRAPH 双向命中并退出码 1；②首次集成自测运行曾因引擎"跳过模式匹配绝对路径"缺陷整批假阴性——修复后全过（该缺陷由自测自身抓出，证明其检出能力真实）。

### 4.6 架构红线（AGENTS §5 逐条） —— PASS

本任务 diff 为 cmake 脚本＋文档：零 Qt 头（纯脚本无 C++）；无跨单元 include；无业务域互链（白名单数据与 §3.5 一致，GRAPH 核对通过）；无 RobWork 名称前缀拼接（引擎自身源码经 R-4 扫描零命中）；权威归属（PA-1）：名称/单位/诊断文案未越权实现，例外裁决未私裁（R-4 空册留 O-12）。

### 4.7 注释规范（AGENTS §2，CMake 脚本按 §2.7） —— PASS

三个新增/修改文件全查（数量不足抽查下限 3 即全查）：文件头均有设计依据与追溯块（需求号/章节/任务号）；每个检查步骤有语义段落中文注释（解释"为什么"与业务含义，如跳过模式相对路径化的假阴性实录、变量名目标的诚实边界、SA-02 核对范围声明）；退出语义/命中码/输入参数逐项说明（错误语义显式化）。合规。

### 4.8 文档同步 —— PASS

- 单元卡：本任务横切（unit=build），无对应单元卡需修订；DTB §2.2 卡面输出物 `ird/cmake/ird_gates.cmake` 与实际落位 `industrialrobot/cmake/ird_gates.cmake` 为同一目录的简写对应（契约 allowedFiles 口径为准），不构成偏差。
- 任务状态/追踪：契约 status 翻转与 history 追加随收尾治理提交执行（PIPE §4.7⑤，属合入段动作，不在此刻要求）。
- findings.json 未关闭条目相关性核对：F-004（AUTOMOC 空转，与本任务零新 C++ 目标无关）；F-005/F-006（单元卡转义损毁，core/testkit 卡辖域）；**F-007（全新集成树配置缺口）——本次验收现场再次实证（ini.template.static 缺失卡 :58），环境修复后通过，继续开放**；F-008（RobWorkSim 全新树构建失败——本次未触发，未做全新树全量构建的理由见 4.2）。新发现 **F-009**（.gitignore 全局 *.log 吞没 traceability 留痕日志）随本记录转登。

### 4.9 commit 质量 —— PASS

4 个提交（9e21536/5c0cc60/6674c1b/511d58f）标题均 `[WP-01-T01]`＋动词开头；正文四段齐备；验证段与留痕一致——其中 6674c1b 声称五件留痕实际仅一件入库（.gitignore 吞没）为**失实陈述**，其自身随后以 511d58f 如实修正并说明原因；验收判定：修正及时、原因如实、未粉饰结论本身，不构成 4.9 fail（教训转登 F-009 处置建议：留痕入库前 `git status` 核对）。

### 4.10 留痕完整 —— PASS

ird-test-report.json（六项验证结构化报告）＋四份运行/构建日志落位 traceability/builds/wp01-t01/（.txt 后缀——F-009）；gtest XML 不适用（无新 C++ 测试目标，自测内建于 cmake 目标并以日志留痕）；phase-one-validation.log 属旧单阶段流程口径，本流水线任务以 builds/<branch>/ 承载（TK-T01 同例）。

### 4.11 偷懒/缩水扫描 —— PASS

①diff 范围 `TODO|FIXME|stub|placeholder|未实现|not implemented` 零命中（R-4 空登记册为契约验收第 ④ 条显式要求，非缩水）；②空实现检测：cmake 脚本无"空函数体"概念，各检查段均有真实逻辑（自测反向验证其行为存在）；③逐条证据表见 4.4——五条 acceptance 全部有具名证据；④测试强度：自测断言"期望退出码＋期望命中码"双条件，非恒真（fail_r4 首轮曾抓出真缺陷，见 4.5）。

---

## 问题清单

| 级别 | 编号 | 内容 | 处置 |
| --- | --- | --- | --- |
| 建议 | F-009（新登） | .gitignore 全局 `*.log` 吞没 traceability/** 留痕日志（本次四份日志需改 .txt 才入库；6674c1b 险些构成"留痕缺失"误报） | 转登 findings.json；建议构建约定所有者为 traceability/** 加负向豁免或明文化 .txt 约定 |
| 留档 | — | traceability/builds/** 不在契约 allowedFiles 字面内，依 outputs 的 traceability-update＋TK-T01 先例放行 | 建议后续契约编译时将 evidence 目录显式纳入 allowedFiles（CCP 修订建议，随本记录留档） |
| 复现实录 | — | F-007 环境缺口在验收现场二次实证（worktree A 配置首败） | 已在 4.2/4.8 注明；F-007 继续 open，责任方构建约定所有者 |

## 裁决

**pass**——无阻断级问题；建议级 F-009 已按 v1.2 强制转登。验收记录经 §3 回传流程落在本 evidence 分支（acc/WP-01-T01/1）并推送，三元组 {branch, path, commit} 交付编排者。
