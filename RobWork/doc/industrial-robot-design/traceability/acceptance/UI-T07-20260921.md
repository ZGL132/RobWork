# 验收记录 —— UI-T07（统一工程策略入口与策略编辑适配器·阶段 A：摘要只读卡）· attempt 1

| 字段 | 值 |
| --- | --- |
| 任务 | UI-T07（contract：tasks/foundation/UI-T07.json，schema ird-task/1，status=ready；interUnit=true——直接持有 policy::IPolicyProvider/EngineeringPolicySet（C-10）对端类型投影与 project 命令端口（C-4）路径说明，本任务即 O-31 触发面之一，O-31 已裁决 2026-09-19 随整链放行；requirements UX-08/ARC-05；designRefs ui.md §13/§4.6/§6.7＋policy.md §10.6；knownPitfalls O-31） |
| 送验对象 | **f918798695e5c9199f042ab8ad9d778d54735611**（40 位冻结；分支 wp10-t07；本地 `git rev-parse wp10-t07`＝该 SHA 逐字一致。**远程漂移核对说明**：`origin/wp10-t07` 远程跟踪引用不存在（远程无该分支引用），且验收执行期间 github.com 网络不可达（本验收一次 `git ls-remote origin wp10-t07` 探测失败——`schannel: failed to receive handshake, SSL/TLS connection failed`，与编排者登记的代理环境事实一致）——远程比对无法执行，按 ACC §3 v1.3 口径以冻结 SHA 为验收对象（验收通过的是该 SHA，不是分支引用）；此事实如实登记，不构成 §3 定义的"发现 origin ≠ headSha"型漂移证据） |
| diff 范围 | 5bfc84107e0ee87704b10c22342ded5909ac5191..f918798695e5c9199f042ab8ad9d778d54735611（base 与 `origin/redesign-main` 逐字一致；range 内 2 提交：7a688a65 实现＋测试、f9187986 单元卡 v0.9＋验证留痕归档；17 个变更文件） |
| 验收尝试 | attempt 1（state.attempts.accept 序号 1；本验收为全新上下文，只消费产物、契约、设计文档与在库留痕＋既往**验收**记录（UI-T06 同日先例，仅取环境口径），未读取实施者过程叙述；全部构建/测试/门禁/治理脚本/变异试验由验收者亲手执行） |
| 验收环境 | 复现 worktree A＝`git worktree add --detach acc-ui-t07/wt-a` @ f9187986（6154 文件检出后 `git rev-parse HEAD`＝f9187986 实证）；首配前按 F-098/F-099 既有登记自主树复制未追踪 `RobWorkStudio.ini.template.static`＋`RobWorkStudio.ini.shared.in` 两件（bin/ 被 .gitignore 吞没的框架 configure 硬依赖——F-098/F-099 环境链本次再次亲历，非送验对象缺陷）；集成 build 树全新配置：`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=<主仓>vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64 -DCMAKE_CXX_FLAGS="-DNOMINMAX -DWIN32_LEAN_AND_MEAN -DWIN32"`——**CONFIGURE_EXIT=0**，`grep RWS_BUILD_INDUSTRIALROBOT build/CMakeCache.txt` → `:BOOL=ON` 实证；独立冒烟 build 树＝`acc-ui-t07/smoke`（vcpkg toolchain＋Qt 前缀——AGENTS §4.1 口径）；GUI 测试执行＝`QT_QPA_PLATFORM=windows`＋`QT_PLUGIN_PATH=<Qt>/plugins`（ui.md §12.2 既有登记口径）；ird_gates 集成模式（MSBuild 面）输出为 GBK 双重编码乱码＋行合并（F-065/F-207 家族——IRD-GATE 码与目标名可辨），最终比对以**脚本模式**（`cmake -DIRD_ROOT=<industrialrobot 根> -P cmake/ird_gates.cmake`，exit 1＝设计内）干净行序归一化后逐项一致（见 4.6）；验收者日志存 acc-ui-t07/logs/（验收临时目录，随 worktree 生命周期处置，关键结论逐条摘录于本记录）；记录回传 worktree B＝`git worktree add -b acc/UI-T07/1` @ origin/redesign-main |
| verdict | **pass**（4.1～4.8/4.11 通过；4.9/4.10 按 F-081/F-266/F-264 家族既有建议级先例判定不阻塞：两提交的**验证结论全部经验收者亲手复现准确**——双模式构建/三测试/门禁 40＝39＋1/两治理脚本无一虚报；两条新发现为建议级，已随本 evidence 分支转登 findings.json **F-268/F-269**。4.9/4.10 的字面缺口＝13 件 .log 留痕被根 .gitignore 吞未入库（在库 7 件本体完整）＋1 处标题名词短语第 15 例，均按 POL-T06（F-081）/UI-T06（F-266/F-267）先例以建议级登记并留返工输入） |

## 4.1 改动范围 —— 通过

- `git diff --name-status 5bfc8410..f9187986` 实证 **17 个变更文件**，逐个对照契约 allowedFiles 三模式：`ui/**` 8 件（CMakeLists.txt M、include/ 2 件〔PolicySummaryCard.hpp A 271 行、UiProjections.hpp M〕、src/ 3 件〔PolicySummaryCard.cpp A 384 行、WorkbenchShell.cpp M、WorkbenchShell_p.hpp M〕、test/ 1 件〔PolicySummaryCardTest.cpp A 453 行〕、gui_test/ 1 件〔PolicySummaryCardGuiTest.cpp A 428 行〕）＋`units/ui.md`（M，v0.8→v0.9 增量修订）＋`traceability/**` 8 件（builds/wp10-t07/ 7 件 A＋wp10-t07-gate-registrations.md A）——**全部在 allowedFiles 内，零越界**。
- forbiddenFiles（REQUIREMENTS.md/ARCHITECTURE.md）零触碰（17 文件列表实证，两文件均不在 diff 内；本验收对照需求原文 UX-08 与 policy.md §10.6 以只读方式提取，未改动）；契约文件 tasks/foundation/UI-T07.json 不在 diff 内＝未被任务侧改动（status=ready 维持，合入收尾翻转——PIPE §4.7 既有口径）。
- 框架源码（RobWork/、RobWorkSim/、RobWorkStudio/ 其余源码）零改动：`git diff --name-only base..head -- RobWork/RobWork RobWork/RobWorkSim RobWork/RobWorkStudio/src`（排除 industrialrobot）**零输出**；`patches/` 范围内零变更（head 树 patches/ 仅既有两件 0001-rwslibs-entry-option.patch＋PATCHES.md，与 base 树逐文件一致——SA-02 无登记需求）。

## 4.2 可复现构建 —— 通过（集成全新树重配＋全量构建＋独立冒烟；`: error` 计数双零）

| 步骤 | 结果 |
| --- | --- |
| 集成重配 | worktree A 全新配置 **exit 0**（补齐 vcpkg toolchain＋Qt 前缀＋CXX flags＋两件未追踪 ini 辅助件后通过——前三者为 F-098/F-099 既有登记口径，AGENTS §4.1 字面命令在全新树不可配，验收者亲历同款）；缓存 grep `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON` 实证 |
| 集成全量构建 | `cmake --build build --config Release` **BUILD_EXIT=0、`: error` 计数 0、`: fatal error` 计数 0**（RobWork 全库→RobWorkSim→RobWorkStudio→industrialrobot 全单元全新编译）；产物 `build/RobWorkStudio/bin/Release/` 下 `sdurws_ird_ui_test.exe`（1032192 B）＋`sdurws_ird_ui_contract_test.exe`（303616 B）＋`sdurws_ird_ui_gui_test.exe`（787968 B）全部在位（另全部 ird 单元测试目标同批产出） |
| 独立冒烟模式 | `cmake -S …industrialrobot -B acc-ui-t07/smoke -G "Visual Studio 17 2022" -A x64 -DCMAKE_TOOLCHAIN_FILE=<vcpkg> -DCMAKE_PREFIX_PATH=<Qt>` 配置 exit 0；`cmake --build --config Release` **exit 0、错误计数 0**；`smoke/ui/Release/` 下三测试可执行在位并亲跑（见 4.3——40/3/15 全绿） |

## 4.3 契约 verify 命令 —— 逐条亲手执行；全部成功

契约 verify 数组仅 1 条，在 worktree A 根目录按字面独立执行：

| 命令 | 结果 |
| --- | --- |
| `cmake --build build --config Release --target sdurws_ird_ui_test` | **exit 0**（VERIFY_EXIT=0、`: error` 计数 0） |

附加测试真实执行（验收者亲手，非采信送验留痕；日志存 acc-ui-t07/logs/）：

- 集成树三测试可执行直接运行（QT_QPA_PLATFORM=windows＋QT_PLUGIN_PATH）：**模型层 40/40、契约 3/3、GUI 15/15 全部 PASSED**（亲跑 gtest XML ird_ui_test.acc.xml/ird_contract_test.acc.xml/ird_gui_test.acc.xml＋console 留痕；计数与在库三份 gtest XML〔tests=40/3/15、failures=0〕及三份 ird-test-report.json〔total=40/3/15、passed 同值、failed/skipped=0、decisive=true〕逐值一致；PolicyCardModel 十用例＋PolicySummaryCardGuiTest 五用例逐一在亲跑 XML 中在列且零失败）。
- 冒烟树三测试可执行另跑 **40/3/15 全部 PASSED**（S_MODEL=0/S_CONTRACT=0/S_GUI=0——"集成＋冒烟双模式同口径"的提交声称经验证属实）。
- 治理脚本亲跑（worktree A 树内）：`validate-task.ps1 -TaskFile <契约绝对路径>` → **PASS (1 tasks)**；`validate-docs.ps1` → **PASS (20 units, 12 trace entries, 150 task files)**——与提交正文声称逐字一致。

## 4.4 acceptance 条目 —— 3/3 逐条证据（未执行项零存在；ird_gates 已随 WP-01-T01 落地故为"已执行"）

| # | 契约 acceptance 要点 | 证据（验收者亲手） |
| --- | --- | --- |
| 1 | 阶段 A 摘要只读卡（§6.7）：数据＝IPolicySummarySource（即 IPolicyProvider 投影面）返回 EngineeringPolicySet 公开字段投影（碰撞域启用/安全间距/过滤对计数/行程上限阈值/判定阈值，含「显式不适用」态）；显示值经单位换算投影，不改变策略内容身份；摘要只读、显示开关与计算开关分组异名并说明影响范围（POL-ID-3） | ①逐字段投影承载：`PolicyThresholdProjection{present,siValue}`＋`PolicySummaryProjection` 五类字段逐项展开（UiProjections.hpp:273-362 diff 亲读——碰撞域启用位＋启用域 token〔policy.md §4.3 冻结 token 直用〕/安全间距 SI m/近限位比·条件数无量纲/行程上限 SI rad/必检·过滤对计数；present=false＝「显式不适用」直读投影，头内注释逐字段锚定 policy.md §4.3/§4.4）。②五类字段行＋换算：`LoadedProjectionRows_FiveFieldFamilies_UI_T07_ACC1` 亲跑通过——键序冻结九行逐行断言（碰撞"启用（self、environment、tool）" :167、安全间距 0.3 m→"300 mm" :174、碰撞对"必检 2 对·过滤 1 对" :179、行程上限 4π rad→"720 deg" :185、近限位比"0.85"/条件数「显式不适用」 :191/:195）；换算实现点 PolicySummaryCard.cpp:127-143（`core::convert` 一次乘除——显示投影不回写 SI 真值，KIN-12 同案）＋`ThresholdConversionViaCoreUnits_UI_T07_ACC1`（mm/deg/无量纲三路数值＋容差、present=false 空态不填充、量纲违约 `EXPECT_THROW(core::CoreError)` :276-277）与 `SiDefaultsIdentityDisplay_KIN12_UI_T07_ACC1`（m/rad 恒等形态）亲跑通过——"显示值经单位换算投影，不改变策略内容身份"实证。③显式不适用不发明数值：`ExplicitNotApplicable_NoInventedNumbers_UI_T07_ACC1`——四阈值槽位全「显式不适用」且 `containsDigit` 反断言夹带数字（:288-310）；未装载态 `UnavailablePlaceholder_NoFabrication_UI_T07_ACC1`——"工程策略→未装载"占位＋策略字段行缺席断言（:317-336）。④分组异名＋影响范围：`GroupTitlesDistinctAndScoped_POLID3_UI_T07_ACC1`——两标题互异非空、"计算权威"/"不属策略字段"语义下限断言（:103-122）；行级归属断言（前 7 行 PolicyAuthority 组/后 2 行 SessionDisplay 组，:199-204）＋`DisplaySeparationNotes_POLID3_UI_T07_ACC1`（边界说明点明"不存在于工程策略字段"与"不改变计算与策略内容身份"，:406-425）。⑤摘要只读＋GUI 同源：`CardIsReadOnly_NoEditableControls_UI_T07_ACC1`——卡子树七族可编辑控件全族扫描零命中＋唯一按钮＝修改策略入口（GUI :320-344）；行文本与 `policySummaryRows` 输出逐行同源（`LoadedProjection_RendersModelRows_UI_T07_ACC1_ACC3` 观测点②，GUI :271-283）。 |
| 2 | 策略编辑经①命令端口提交（P-POL-9 适配器宿主＝ui）：编辑表单归阶段 B；ui 不持有判定权与计算开关权威（ARC-05/N-3：权威归 policy/EngineeringPolicySet） | ①卡面路径说明：`kPolicyEditPathNote`＝"修改策略经命令端口提交生成新修订（权威归策略，ui 只读）"（PolicySummaryCard.hpp:168-169 契约常量单点）经 edit-path 行呈现（实现 :236-237）；`EditPathNote_CommandPort_UI_T07_ACC2` 亲跑通过——行值与常量全等＋"命令端口"/"新修订"关键词断言＋行归属计算权威组（:375-395）。②表单归阶段 B、不虚构能力：`kPolicyEditDeferredNotice`＝"该入口将在后续版本提供（编辑表单随阶段 B 交付）"（hpp:171-174）；GUI `EditEntry_ShowsDeferredNotice_UI_T07_ACC2` 亲跑通过——点击后提示可见且为契约原文、卡面仍零 QLineEdit、**端口零触达（替身 callCount 不变 :380-381）**（GUI :355-382）——阶段 A 无表单即零提交面。③ui 零判定权/零计算开关权威：卡数据输入唯一来源＝`IPolicySummarySource`（实现 :297 `source.summary()`——只读取快照渲染，零回写零判定；按钮回调体仅 setText/show 延期提示 :339-349）；无项目态/停用态均按对端投影事实呈现不自行推断。 |
| 3 | O-31 处置（已裁决 2026-09-19——随整链放行）：C-10 按 ui.md v0.4 裁决经 ui::IPolicySummarySource 自有端口承载（ShellWiring.policySource，L5 适配 policy::IPolicyProvider——§6.7 策略摘要只读面语义不变）；C-4 命令面经 ui 自有命令网关端口＋值投影；产品面零对端链接/include（NoCrossUnitInclude_O31_UI_BUILD 守卫常驻）；摘要只读、显示开关与计算开关分组异名（POL-ID-3）语义零变化 | ①C-10 端口承载：`IPolicySummarySource`/`ShellWiring.policySource` 为 UI-T03 既有交付（UiPorts.hpp:103-108 亲读——本任务 diff 未触碰该头），卡经壳装配消费（WorkbenchShell.cpp:396-400 `createPolicySummaryCard(*m_wiring.policySource,…)`）；GUI `LoadedProjection_RendersModelRows_UI_T07_ACC1_ACC3` 观测点③端口消费计数 ≥1（替身计数＝注入承载证据，GUI :256-257）＋`RefreshOnContextInjection_UI_T07_ACC3`（投影变更→presentProjectContext→端口重拉＋行更新 :393-426）亲跑通过——L5 真适配归装配层任务、ui 测试以可控替身承载（ui.md §3.1 口径）。②C-4 命令面：零新 C-4 面——编辑路径以 kPolicyEditPathNote 钉住"经命令端口提交生成新修订"（acc2 证据），IUiCommandGateway 已随 UI-T06 冻结（本任务 diff 零触碰 UiPorts.hpp/ICommandRegistry.hpp）。③零对端 include/链接：验收者独立 grep——ui 产品面（src＋include）`sdurws/ird/` include 仅 core（13）/diagnostics（9）/ui（22）三目录，policy/project 等 15 业务域**零命中**；`target_link_libraries(sdurws_ird_ui PUBLIC sdurws_ird_core/sdurws_ird_diagnostics/Qt6::Core/Gui/Widgets)`（CMakeLists.txt:133-138 亲读——零对端链接）；`NoCrossUnitInclude_O31_UI_BUILD` 守卫用例随 sdurws_ird_ui_test 40/40 亲跑通过（亲跑 XML 含该用例）。④POL-ID-3 分组异名语义零变化：§6.7 语义以两分组词表单点承载（acc1 ④证据），UI-T03 计数聚合改派生方法后语义可复现（`AggregatesDerivedFromPerFieldFacts_UI_T07_ACC1` 全提供 3/1、全不提供 0/4、计数控恒等＝4，:347-363 亲跑通过）。 |

## 4.5 测试真实失败能力 —— 通过（两处实现变异注入应失败输入，均真实变红；试验后已还原并复绿、worktree 字节级还原）

在验收沙箱 worktree A（git 管控下，两轮试验后 `git status --porcelain` 零输出＝与送验对象逐字节一致）注入两处变异：

| # | 变异 | 重建后亲跑结果 |
| --- | --- | --- |
| 1 | `PolicySummaryCard.cpp` formatPolicyThreshold 显式不适用短路守卫改 `if (false && !threshold.present)`（「显式不适用」呈现轨被拆除） | `--gtest_filter=…ExplicitNotApplicable_NoInventedNumbers_UI_T07_ACC1:…ThresholdConversionViaCoreUnits_UI_T07_ACC1` → **双用例 FAILED**（MUT1_EXIT=1；失败点即承重断言：absent.present 应为 false、不适用行值夹带数字） |
| 2 | `WorkbenchShell.cpp` refreshPolicySummaryCard 钩子调用改 `if (false && m_refreshPolicyCard)`（上下文注入触发的端口重拉被拆除） | `--gtest_filter=PolicySummaryCardGuiTest.RefreshOnContextInjection_UI_T07_ACC3` → **FAILED**（MUT2_RUN=1；失败信息"上下文注入未触发端口重拉"——恰为承重断言） |

两处变异还原后重建（RESTORE_BUILD_EXIT=0）复跑：变异 1 双用例 **PASSED**（RESTORE_RUN_EXIT=0）；变异 2 还原后 GUI 全量 **15/15 PASSED**（RESTORE_RUN=0）。三组承重断言均为「显式不适用语义」与「端口注入刷新语义」本身——非恒真、非"只编译通过"。

## 4.6 架构红线（AGENTS §5 八条）—— 逐条通过

1. **L2 计算内核零 Qt**：本任务仅改 ui 单元（R-3 全产品唯一 Qt Widgets 例外单元，DTB §4.5 预登记）；core/evidence/policy/runtime 等零触碰（17 文件清单实证）；ird_gates 脚本模式亲跑（exit 1＝设计内）归一化命中集与在库 gates-hit-set.txt **逐项一致（36/36 码＋目标对 IDENTICAL）**：R3 命中 14 处＝链接 3（Qt6::Core/Gui/Widgets——既有）＋产品面文件 11，其中本任务恰增 1 处 `ui/src/PolicySummaryCard.cpp`（F-259 同型机器面滞后，与提交件 wp10-t07-gate-registrations.md 登记行逐字一致；40＝wp10-t06 基线 39＋1 经与在库命中集全量归一化比对证实），PolicySummaryCard.hpp/UiProjections.hpp/测试面零命中。
2. **业务域互链禁止（R-1）**：ui 产品面 include 扫描（src＋include 全量 grep）——`sdurws/ird/` 命中仅 core/diagnostics/ui 三目录，15 业务域**零命中**；本任务新增边＝零（卡消费面只见 ui 自有端口/投影＋core Units 表内登记边）；链接面 CMakeLists.txt:133-138 亲读零对端链接。
3. **跨单元私有头禁止（R-2）**：零跨单元 include（同上 grep——全部 `include/sdurws/ird/<unit>/` 公共路径）；ui 内部消费私有头 `WorkbenchShell_p.hpp` 仅为同单元内 include（PolicySummaryCard.cpp:30），私有面零跨单元暴露；新公共头仅 PolicySummaryCard.hpp 落位 include/sdurws/ird/ui/。
4. **框架零源码修改（SA-02）**：diff 内零框架文件、patches/ 与 base 逐文件一致（4.1 实证）。
5. **不做名称前缀拼接/剥离（R-4）**：ui diff 新增代码行 `RobWork` 字面量 **0 命中**（grep 计数仅命中 diff 文件头 `+++ b/…` 行——非代码内容）；ird_gates 复跑 R4 项仅 project 既有 1 处（与基线同值）。
6. **权威唯一（PA-1）**：策略内容权威归 policy（卡为只读投影、零回写——submit/write 面 ui 侧不存在，acc2/acc3 证据）；单位换算权威归 core Units（formatPolicyThreshold 经 core::convert 单一入口）；行装配/文案权威单点在 PolicySummaryCard 契约头与 policySummaryRows 纯函数（GUI 只渲染——同源测试钉住）；壳零策略语义（仅持刷新钩子重拉端口快照）。
7. **不可变历史（PA-2）**：本任务零修订数据触碰（17 文件清单——无 project/ 修订面文件）；UI 对策略只持投影快照不回写。
8. **不恢复 old/**：diff 新增行 `old/` 引用 **0 命中**（计数实证）；实现全部从零编写。

## 4.7 注释规范（AGENTS §2）—— 通过（新增公共头 PolicySummaryCard.hpp 全查＋大改头 UiProjections.hpp diff 全读＋实现 3 件（PolicySummaryCard.cpp 全文、WorkbenchShell.cpp/_p.hpp diff）＋两测试文件全文——覆盖"公共头全查＋实现抽查 ≥3"）

- **文件头**：新增/大改 8 件均有中文 `@file/@brief`＋设计依据（需求 UX-08/ARC-05、POL-ID-3、ui.md §6.7/§4.6/§4.2/§11.4、policy.md §10.6、任务契约 acceptance 1~3、O-31 裁决逐条对应）＋背景说明（"为什么卡的文案与分组要在公共头钉死""为什么显式不适用是语义内容""为什么刷新是清空重建""为什么聚合改派生方法"等"为什么"层注释齐备）。
- **类/函数 Doxygen**：公共头逐结构体/逐函数 `@brief/@param [in]/@return/@throws/@note` 语义完整（PolicySummaryGroup 两枚举值语义、PolicySummaryRow 四字段、formatPolicyThreshold 三参方向/量纲约束/异常条件、policySummaryRows 装配规则全列、PolicyThresholdProjection 不带 origin 的权威论证）；实现文件私有函数（formatDisplayNumber/thresholdValueText/makeRow/buildPolicyCardContent）均有职责注释。
- **§2.5 高危信息表**：物理单位（SI m/rad/无量纲 "1"/mm/deg 逐处显式——阈值单位由字段位置固定并注明出处）；角度制式（行程上限 rad 显式，非度）；坐标系（本任务无坐标系面——如实无缺失）；所有权（卡容器/子控件所有权移交 Qt 对象树、刷新钩子生命周期 ≤ 容器、QPointer 悬垂防护逐处注明）；线程约束（"纯函数＋编译期固定数据——任意线程可调用""装配与刷新只在 UI 线程调用（§3.4 M-1）"逐处注明）；错误语义（量纲违约 fail-fast 抛 CoreError＝调用方错误、logic_error 双处为构建期事实破坏 fail-fast、显式不适用＝对端语义内容非错误）；确定性来源（to_chars locale 无关＋6 位有效数字呈现精度注释、行序装配冻结序 NFR-COR-02、同输入两次装配全等测试钉住）。
- **魔法数字**：呈现精度 6 位有效数字（"呈现投影非工程比较值"来源注释）、layout margins 4、addStretch(1)、缓冲 40 字节——逐个注明依据。
- **反例扫描**：零复述式注释、零无编号 TODO（唯一"MUTATION"注释为验收者变异试验所加、已还原）、注释与代码一致（CMakeLists AUTOMOC 关闭理由随源集 8→9 文件同步更新——无过期注释残留）。

## 4.8 文档同步 —— 通过（零偏差＋增量修订已登记＋findings 相关性逐条核对）

- **单元卡增量修订**：`units/ui.md` v0.8→v0.9（diff 实证五落点：文档头版本行、§3.3 公共头布局增行 PolicySummaryCard.hpp、§12.3 用例清单增行 UI-POL-1、§13 UI-T07 行后"阶段 A 落地登记"注、§16.7 变更记录增 v0.9 行——既有语义内容零改动）；v0.9 行登记的承载形状（卡交付/契约头/C-10 逐字段细化/计数改派生/测试 40＋3＋15/门禁 39→40＋gate-all 环境面 3 项既有失败如实登记/编辑表单归阶段 B 维持 §13 原文）与实现逐一亲读比对一致。实现与任务卡零未登记偏差（acceptance 逐条对应见 4.4）。
- **追踪同步**：validate-task PASS＋validate-docs PASS 亲跑同值（4.3）；§12.3 UI-POL-1 行（前置/操作/预期/观测点）与交付 GUI 测试形态一致；契约 status=ready 维持（合入收尾翻转——PIPE §4.7 口径）。
- **findings.json 未关闭条目相关性核对（232 条 open 逐类筛查，与本任务相关者如下；顺手消账项为零）**：
  - **F-227/F-259**（ird_gates 机器面滞后：R3 例外链接/头包含白名单未回填，owner=WP-01-T03）：本任务新增 1 处 R3 命中即 F-259 同型（新实现 TU 的 Qt 头包含——R-3 例外本体），已按 wp10-t02~t06 先例以登记提交件 wp10-t07-gate-registrations.md 顺延登记（责任方 WP-01-T03，不重复开立请求条款）——维持 open 正确（白名单文件不在本任务 allowedFiles，本任务无回填义务）。
  - **F-261/F-263/F-264/F-267**（提交标题名词短语家族第 11~14 例）：本任务 f9187986 标题同型再现（**第 15 例**，见 4.9）——家族性复发，随本验收转登 **F-269** 顺延跟踪。
  - **F-010/F-077/F-081/F-266**（.gitignore 吞 .log／留痕归档虚报家族）：本任务 **F-266 同型复踩**（f9187986 正文声称归档的 13 件 .log——集成构建日志、冒烟配置/构建/三测试控制台日志、validate-task 日志、ird_gates 双模式原始输出——被根 .gitignore:87 规则静默跳过未强制入库，在库仅 7 件〔3 XML＋3 report＋gates-hit-set.txt〕；另 gate-registrations.md 首节"本目录 ird_gates.log/ird_gates-script.log"为在库悬空引用；**数值主张本身无一虚报**：全部经本验收亲手复现准确）——随本验收转登 **F-268** 顺延跟踪（按 F-266/POL-T06 先例判定建议级：证据本体在库且全量复现）。
  - **F-260**（UI-T03 行为面缺口：折叠/门面违约轨）：属 UI-T03 交付测试集；本任务零触碰 resizeEvent 折叠面，与本任务 diff 无交集，维持 open 正确。
  - **F-265**（WorkbenchShell_p.hpp 行尾半角括号笔误，owner=UI 后续任务顺手还原）：本任务大改同文件但该行（现 :402，cat -A 字节级复核行尾仍为半角 `)`）未还原——维持 open 正确（非本任务 acceptance 义务；登记 owner 为"后续 UI 任务顺手"，建议随收尾提示下序 UI 任务消账）。
  - **F-201**（§4.10 条文载体与流水线实践脱节）：循其登记口径以 `traceability/builds/wp10-t07/` 在库 7 件为 4.10 判定载体（见 4.10——载体件数缺口即 F-268）。
  - **F-065/F-207**（gates 留痕 GBK 双重编码乱码）：本验收亲跑 ird_gates 集成模式输出同型乱码＋行合并（IRD-GATE 码与目标名可辨、以脚本模式归一化后完成比对）——既有登记持续适用，环境/工具链面非本任务新增缺陷。
  - **F-098/F-099**（全新树配置口径缺口）：本验收再次亲历（ini 两件＋toolchain/Qt/CXXFLAGS）——与本任务零代码关联，维持 open 正确（本记录环境段再留实证）。
  - 其余 open 条目与本任务 17 文件面无关联（逐类比对零交集）。

## 4.9 commit 质量 —— 按家族先例通过（2 提交四段式齐备、验证结论全数复现准确；1 处标题名词短语＝F-267 家族第 15 例转登 F-269；1 处归档清单与在库树不一致＝F-266 家族复踩转登 F-268）

- **7a688a65** `[WP-10-T07] 实现统一工程策略入口阶段 A：右栏策略摘要只读卡与呈现契约`：动词开头（"实现"）合规；四段（What/Why/验证/影响面）齐备（含 Acc-Covered: 1,2,3）；正文验证声称（双模式零错误、40＋3＋15 双模式同口径、ird_gates 40＝39＋恰增 1 处 R3、validate-task PASS）与本验收亲跑**逐值一致**；影响面段如实声明（PolicySummaryProjection 细化为首消费既定动作、右栏占位标签被真实卡取代为契约显式设计、sdurws_ird_ui 链接面零变化）。
- **f9187986** `[WP-10-T07] ui 单元卡 v0.9 增量修订与验证留痕归档（文档同步＋证据）`：①**前缀后以名词短语『ui 单元卡』开头——不合 ACC §4.9/AGENTS §6.3『动词开头』字面**，F-012/F-232~F-242/F-247/F-258/F-261/F-263/F-264/F-267 家族**第 15 例**（纯风格瑕疵）——转登 **F-269**（建议级，随 F-247 家族统一跟踪）。②正文"验证（How）"与"改动内容"声称的留痕归档清单与在库树不一致——13 件 .log 被根 .gitignore:87 规则静默跳过未强制入册（F-010/F-077/F-081/F-266 家族复踩，**数值主张本身无一虚报**：全部经本验收亲手复现准确）——转登 **F-268**（建议级，按 F-081/POL-T06/F-266 先例"证据本体完整且全量复现"口径不阻塞；返工输入＝git add -f 回填 13 件或循 F-081 裁决①/③）。两提交正文四段齐备、如实区分已执行/未执行（ird_gates 与测试计数均与在库 XML/报告/命中集逐值相符；gate-all 环境面 3 项既有失败在 gate-registrations.md 逐项如实登记非粉饰）。

## 4.10 留痕完整 —— 按 F-201/F-081/F-266 先例通过（证据本体 7 件在库且全数复现；13 件日志在盘未入库＝F-268，回填责任已登记）

- **在库载体**：`traceability/builds/wp10-t07/` **7 件**（双模式同口径 gtest XML×3：tests=40/3/15、failures=0 亲读且用例名与源码一一对应；ird-test-report.json×3：decisive=true、passed=40/3/15、failed/skipped=0 亲读；gates-hit-set.txt：87 行归一化命中集——与本验收脚本模式复跑逐项一致）＋`traceability/wp10-t07-gate-registrations.md`（触发事实/恰增 1 处登记行/零实质违反声明/gate-all 环境面 3 项既有失败逐项核实齐备；"40＝基线 39＋1"结论与本验收复跑一致）。在库 XML 计数与提交正文、本验收亲跑三方一致。
- **缺口（F-268 登记处置）**：提交正文声称归档的 13 件 .log（integrated-build-ui/smoke-configure/smoke-build/三测试 console/smoke-contract·gui·model-console/validate-task/ird_gates/ird_gates-script/gate-all）实际**未入库**——实施主工作树该目录 `git status --ignored --porcelain` 实证 13 件在盘未跟踪（根 .gitignore:87 `*.log` 吞没、未按 UI-T05 先例 `git add -f`）；按 F-081/POL-T06/F-266 先例（F-077 POL-T04 口径）以本验收亲跑证据（4.2/4.3/4.6 全套：集成/冒烟配置与构建日志、三测试 console＋XML、门禁双模式复跑、validate-task/validate-docs——存验收临时目录并摘录于本记录）替代裁判依据；回填或根因修复循 F-268 resolution 裁决。gate-registrations.md 对"本目录 ird_gates.log/ird_gates-script.log"的在库悬空引用同入 F-268。
- phase-one-validation.log 尾行停早期任务、state.json 无 resultNote 字段——循 **F-201** 既有登记口径，以 `traceability/builds/wp10-t07/`＋登记提交件为判定载体。
- 验收者自跑留痕（集成/冒烟配置与构建日志、三测试 gtest XML＋console、门禁双模式复跑输出、变异试验构建/运行日志、validate 脚本输出）存于验收临时目录 acc-ui-t07/logs/，关键结论已逐条摘录于本记录；临时目录处置不随本记录效力变化。

## 4.11 偷懒/缩水实现扫描 —— 通过（扫描零实质命中；豁免逐条列据；逐条证据表见 4.4）

- **①标记扫描**：diff 新增行 `TODO|FIXME|XXX|HACK|stub|未实现|not implemented` **零命中**（grep exit 实证）；`placeholder`（大小写不敏感）命中均为非交付物本体：测试用例/变量名（Unloaded_RendersPlaceholder、placeholder 变量——测试语义本身）、`src/View3DPlaceholder.cpp`（UI-T05 既有交付物文件名引用——注释与 diff 语境）；「占位/延期说明」（"修改策略…"入口反馈、显示控件阶段说明）＝**契约显式形态**（ui.md §11.4 不虚构业务能力＋§13 UI-T07 行"编辑表单归阶段 B"原文＋§16.7 v0.9 登记；触发有真实点击反馈与端口零触达断言——非空实现、不虚构业务能力），豁免依据＝单元卡原文＋实现亲读，逐条列出如上。
- **②空实现检测**：全部公共接口函数有真实实现（siDefaults/find 失败 fail-fast、formatPolicyThreshold 换算＋守卫＋异常、policySummaryRows 九行装配全分支、policySummaryGroupTitle 全枚举 switch、createPolicySummaryCard 控件树构建＋刷新闭包、壳 refreshPolicySummaryCard/shutdown 钩子清理——实现全文亲读）；`PolicyThresholdDisplay{}` 空态返回＝"present=false 呈现显式不适用"的设计语义而非空实现；零"仅返回默认值"的空壳公共接口。
- **③逐条证据表**：契约 acceptance 3 条逐条证据见 4.4 表——每条均有具名测试、命令输出或文件行号，零"已实现"空声明。
- **④测试强度**：断言校验行为与边界（逐字段行值全等＋键序冻结＋分组归属逐行；换算三路数值＋容差＋量纲违约 EXPECT_THROW；不适用双断言：文案全等＋containsDigit 反断言防夹带数值；未装载形态负向断言策略字段行缺席；GUI 可编辑控件七族扫描＋唯一按钮断言；点击后端口零触达计数断言；上下文注入触发重拉计数＋行更新断言）；**非恒真、非"只编译通过"**——与 4.5 联动抽改实现两处（显式不适用轨拆除/端口重拉拆除）均真实变红且失败点恰为承重断言，其余用例不受扰。

## 发现的问题清单（建议级 2 条，无阻断级）

| 编号 | 级别 | 摘要 | 处置 |
| --- | --- | --- | --- |
| F-268 | suggestion | F-010/F-077/F-081/F-266 家族复踩：提交 f9187986 正文声称 traceability/builds/wp10-t07/ 归档 13 件 .log（集成构建日志、冒烟配置/构建/三测试控制台日志、validate-task 日志、ird_gates 双模式原始输出等），实际全部被根 .gitignore:87 吞、未按 UI-T05 先例 git add -f（在库仅 7 件：3 XML＋3 report＋gates-hit-set.txt）；wp10-t07-gate-registrations.md 首节"本目录 ird_gates.log/ird_gates-script.log"在库悬空引用（F-081 同型）。数值主张全部经验收者亲手复现准确（构建零错误/40＋3＋15 双模式/门禁 40＝39＋1/validate PASS）——证据本体完整，方向为"声称在库而实际未入库" | 已随本 evidence 分支转登 findings.json（回填 13 件或按 F-081/F-266 裁决①/③闭环；按家族先例不影响验收） |
| F-269 | suggestion | F-264/F-267 家族第 15 例：提交 f9187986 标题前缀后以名词短语『ui 单元卡』开头，不合『动词开头』字面（同提交 7a688a65 以动词『实现』开头合规）；正文四段齐备、验证结论经独立复跑证实，纯风格瑕疵 | 已随本 evidence 分支转登 findings.json（随 F-247 家族统一跟踪，不单独返工） |

## 裁决

**verdict＝pass**：4.1～4.8 与 4.11 全部通过；4.9/4.10 存在的两处缺口（标题名词短语第 15 例、13 件日志未入库＋2 处悬空引用）均为**既有家族性建议级问题**（F-266/F-264 先例明确："不影响验收——证据本体完整且全量复现"），两提交的全部验证结论经本验收亲手复现无一虚报，无阻断级问题。F-268/F-269 已按 ACC §5 强制转登本 evidence 分支 `traceability/findings.json`，不阻塞合入。验收通过对象＝**f918798695e5c9199f042ab8ad9d778d54735611**（非分支引用）；后续收尾按 PIPE §4.7 由所有者执行（建议随收尾一并裁决 F-268 的日志回填路径与 F-265 的顺手还原时点）。

验收者合规声明：本会话未合入任何分支、未修改实施产物（worktree A 变异试验后经 git 还原至与送验对象逐字节一致）、未向任务分支 wp10-t07 推送任何提交；本记录唯一写入面＝evidence 分支 acc/UI-T07/1（验收记录＋findings.json 两条转登）。
