# UI-T18 验收记录（attempt 1，2026-09-24）

| 字段 | 值 |
| --- | --- |
| 任务 | UI-T18 Dock 拓扑拆分＋状态行迁移宿主状态栏（O-43 裁决③承接；契约 tasks/foundation/UI-T18.json） |
| 送验对象（冻结） | `d7e4a24390550626ddd0c54f700622fc90db7e83`（分支 wp10-t18；验收全程按 SHA——分支漂移检测：本地 wp10-t18 与 origin/wp10-t18 均与之逐位一致，通过） |
| diff 范围 | `feb09471b12d78fcb4e30f269afc03258f1b71ad..d7e4a24`（3 提交：0e54c27b 实现／3cb5e96e merge base 对齐派发基线／d7e4a24 r3 收尾留痕；32 个变更文件＝ui 产品面 8＋units/ui.md 1＋doc 树 traceability 20＋**仓库根 traceability 3〔越界，见 4.1/阻断 B-1〕**） |
| 复现现场 | 全新 detached worktree @d7e4a24（仓库外 `acc-ui-t18/wt-head`）；base 侧另配 detached worktree @feb0947（`wt-base`，ird_gates 基线复跑用）；集成模式全新 configure（显式 `-DRWS_BUILD_INDUSTRIALROBOT=ON`，grep 确认缓存 `:BOOL=ON`）；独立冒烟模式另配全新树（vcpkg toolchain＋Qt 前缀） |
| 验证脚本 | validate-task PASS（验收者亲跑，worktree 内；输出 24 件随本 evidence 分支入库 `builds/wp10-t18/acc-attempt1/`） |
| **verdict** | **fail（阻断 2 项：B-1——范围越界，3 个留痕文件落在契约 allowedFiles 外的仓库根 `traceability/`；B-2——契约 verify 第 4 条宿主 GUI 冒烟未执行〔所有者窗口问询未获答复，在库亦无宿主冒烟证据〕。其余 4.2／4.5／4.6／4.7／4.9／4.10／4.11 通过，4.8 通过〔附建议级勘误〕；实现面与测试面零缺陷检出——154/22/35 全绿＋门禁 63↔63 双向差集 0＋双突变验证者有效性成立）** |

---

## 0. 环境说明（复现前提，影响后续验收者）

- **F-323 两个子项本轮再次 100% 复现**（与 UI-T16-r2/UI-T17 两轮验收一致）：①configure 前须把主工作树 `RobWork/RobWorkStudio/bin/RobWorkStudio.ini.template.static` 手工补置到 worktree 同路径（.gitignore `[Bb]in/` 吞件）；②RobWorkSim NOMINMAX 宏冲突须以 `CL="-DNOMINMAX"` 环境变量全量构建（Git Bash 下 `/D` 形式会被 MSYS 路径转换破坏）。
- 集成模式依赖口径（沿 UI-T17-r2 §0 登记）：全新树集成 configure 带 vcpkg toolchain＋`CMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`（与主构建树 CMakeCache 一致）。本验收 configure 一次通过，`RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`（acc-attempt1/acc-config-head.log）。
- 本验收**未执行任何交互式宿主冒烟**：验收者按派发指令以 AskUserQuestion 向所有者请求 3~5 分钟 GUI 冒烟窗口（多 Dock 拓扑＋宿主状态栏唯一真机核验），**未获答复**——按派发指令"暂不方便则按所有者答复在记录中如实登记执行状态并按证据裁决"处理，verify 4 与 acceptance 宿主呈现半区**不标注通过**（ACC §4.4 纪律），登记为阻断 B-2。
- git fetch（代理 127.0.0.1:17890 拒连）本记录分支创建时失败——record worktree 基于本地 `origin/redesign-main` 引用（feb09471，与本地 redesign-main 逐位一致）；推送在本记录完成时重试，若持续失败按 AGENTS §6.3 停在原地报告。

---

## 1. 逐项核查（acceptance-protocol §4，4.1～4.11）

### 4.1 改动范围 —— **不通过（阻断 B-1：范围越界 3 文件）**

- `git diff --name-status feb0947..d7e4a24`：32 个变更文件逐一对照契约 `allowedFiles`（`ui/**`、`units/ui.md`、`RobWork/doc/industrial-robot-design/traceability/**` 三项）：
  - **在 allowedFiles 内 29 件**：ui 产品面 8（include/WorkbenchContent.hpp、src/WorkbenchContent.cpp、src/WorkbenchContent_p.hpp、src/WorkbenchShell.cpp、src/WorkbenchShell_p.hpp、plugin/UiPlugin.cpp、plugin/UiPlugin.hpp、gui_test/WorkbenchContentGuiTest.cpp）＋units/ui.md 1＋doc 树 `traceability/builds/wp10-t18/` 20 件（r3 全套留痕）。
  - **越界 3 件（B-1）**：`traceability/builds/wp10-t18/gtest-ui-contract.xml`、`gtest-ui-gui.xml`、`gtest-ui-model.xml`——位于**仓库根** `traceability/`，不匹配契约任何 allowedFiles 模式（`RobWork/doc/industrial-robot-design/traceability/**` 的目录前缀不同；git tree 实测 mode 100644 普通文件，非符号链接/junction 别名）。由 0e54c27b 引入，d7e4a24 仍未清理。
  - 越界件内容核查：三件 XML 的 gtest 时间戳为 **16:24**（r2 代捕获），而在库权威 -r3 件为 **19:02**——即送验提交自带了一组**过期的 r2 代中间产物**于规范留痕树之外。这与 d7e4a24 提交信息自述"r2 轮中间件（未完成的冒烟日志等）已清理，r3 为准"**自相矛盾**（r2 代 XML 恰未清理）。
  - **同类前科在案**：units/ui.md §16.7 v1.13-r2 行明文登记过同一缺陷的更正——"①证据位置更正（batch 1 留痕误置仓库根 traceability/——**allowedFiles 外**，r2 迁入 doc 规范路径）"。本任务为已知类缺陷复发。
- `forbiddenFiles`（REQUIREMENTS.md / ARCHITECTURE.md / development-task-breakdown.md）：零触碰（均不在 diff）——本项通过。
- SA-02：diff 无 `industrialrobot/patches/` 变更＝无框架补丁登记需求；无任何框架源码路径（RobWork/RWS 框架层、RobWorkSim）变更；ird_gates SA-02 工作区核对（IRD_ENABLE_GIT 缺省 ON）head↔base 命中集逐位一致（见 4.6）为旁证。

### 4.2 可复现构建 —— **通过**

- 集成模式（全新树重新配置后）：`CL="-DNOMINMAX" cmake --build build --config Release --parallel 8`——**exit=0，`error C/LNK/MSB/fatal` 计数 0**；sdurws_ird_ui.lib／sdurws_ird_ui_plugin.dll（2,727,936 B）／RobWorkStudio.exe（2,083,328 B）及全部框架目标产出（acc-attempt1/acc-integration-build.log，348 KB 全量日志）。
- 独立冒烟模式：`cmake -S <industrialrobot> -B <tmp/smoke-build> -DCMAKE_TOOLCHAIN_FILE=<repo>/vcpkg/...`（另加 Qt 前缀）configure 零 CMake Error（acc-smoke-configure.log exit=0）＋全量构建 **exit=0、error 计数 0**（acc-smoke-build.log，含 sdurws_ird_ui_test.exe 等测试目标产出）——与实施段 smoke-build-r3.log（SMOKE_BUILD_EXIT=0）结论一致。

### 4.3 契约 verify 命令 —— **verify 1～3 通过（验收者独立执行）；verify 4 未执行（阻断 B-2）**

| # | 命令 | 结果 | 证据（acc-attempt1/） |
| --- | --- | --- | --- |
| 1 | `cmake --build build --config Release --target sdurws_ird_ui sdurws_ird_ui_plugin sdurws_ird_ui_app sdurws_ird_ui_test sdurws_ird_ui_gui_test sdurws_ird_ui_contract_test` | **成功** exit=0，六目标产出齐备 | acc-verify1-targets.log |
| 2 | `ctest --test-dir build/RobWorkStudio/src/rwslibs/industrialrobot/ui -C Release -L "^ird$"` | **成功** 2/2（ui_test＋contract_test） | acc-ctest-ird.log |
| 3 | `ctest --test-dir build/RobWorkStudio/src/rwslibs/industrialrobot/ui -C Release -L "^ird_gui$"` | **成功** 1/1（gui_test） | acc-ctest-gui.log |
| 4 | 框架 RobWorkStudio.exe 经 Plugins→Load plugin 加载后呈多 Dock 拓扑（主 Dock＋属性右 Dock＋任务底部 Dock）且宿主状态栏唯一；启动日志＋截图＋操作序列留痕 | **未执行**——所有者窗口问询未获答复（§0）；在库 `builds/wp10-t18/` 21 件留痕全部为单元/GUI 测试与构建门禁证据，**无任何宿主冒烟件**（实施段契约 note 已如实登记"未执行…随验收段补录"）。按 ACC §4.4 不标注通过 → **阻断 B-2** | 本节＋4.4 |

- 三测试 exe 直跑（验收者独立 PATH 注入、全新 XML）：模型 **154/154**、契约 **22/22**、GUI **35/35**，failures=0（acc-gtest-{model,contract,gui}.xml＋控制台日志）——与实施段 -r3 登记（154/22/35）逐位一致。
- ird_gates 命中集归一化比对（验收者独立复跑：head 与 base 各自 detached worktree、同引擎 `cmake -DIRD_ROOT=... -P cmake/ird_gates.cmake`；归一化沿用送验自带 normalize-gate-log.ps1〔27 行，验收者逐行读过：抽 IRD-GATE 行→行内拆分→剥扫描根→排序去重〕）：**head 63 ↔ base 63，双向差集 0**（acc-hitset-head.txt / acc-hitset-base.txt / `diff` 空）；且与实施段在库 gate-hitset-{head,base}-r3.txt **逐字节一致**（双 diff 皆空）。本任务零 CMake/链接面变更的声明成立。

### 4.4 契约 acceptance 三条逐项 —— **代码/测试面全部实证；宿主呈现半区随 B-2 未获实证**

**acceptance 1（契约面变更落位）——代码/测试面通过（宿主端到端半区随 B-2）**：

| 证据项 | 证据（验收者独立复核） |
| --- | --- |
| IWorkbenchContent 移除 statusBarWidget() | 公共头 diff：`QStatusBar* statusBarWidget()` 纯虚出口删除＋`class QStatusBar` 前置声明删除；WorkbenchContent_p.hpp override 同步删除 |
| 增双状态观测钩子 | `setStatusTextObserver(std::function<void(const QString&)>)`／`setStatusMessageObserver(std::function<void(const QString&, int timeoutMs)>)` 两纯虚——公共头 Doxygen 契约完整（@param [in]、单位 ms、0＝驻留、生命周期≤实例、UI 线程、空＝清除语义） |
| PM-11 永久文本 formatProjectStatusText 唯一权威现取现投 | src/WorkbenchContent.cpp refreshStatusBar（L1147~1153）：`text` 现取→m_statusTextObserver 投影；标题观察者同源同文本（PM-11 双面） |
| 瞬态消息 showMessage 语义原样移交 | showStatusFeedback（L1247~1256）：文本＋超时 ms 零加工直投观察者；判空收口唯一 |
| 内容层 6 处瞬态消息点统一收敛 | grep 实测 call sites 恰 6 处（L893 resetLayout 恢复默认布局／L947 延期占位说明／L1052、L1057 手册打开／缺失／L1203、L1207 未知命令与禁用原因）——diff 逐处由 `m_statusBar->showMessage` 改为 `showStatusFeedback`，无第 7 处旁路 |
| 内容层零状态栏 Widget | m_statusBar/m_statusLabel 成员与 `new QStatusBar` 装配代码全删；shutdown 置空清单同步；QStatusBar 字样仅存历史说明注释与 p.hpp 一处**死包含**（见 4.7/F-334，非 Widget） |
| §10.1 v1.14 增量注⑦与实现逐字一致 | ui.md L1084 注⑦逐句与上列代码事实对照成立（出口移除／双钩子／6 处收敛／零 Widget） |
| 测试落位 | WorkbenchContentGuiTest.cpp `TwoPhaseAssemblyContract_UI_SPLIT` 增状态出口观测断言（PM-11 投影非空＋瞬态消息含"未知命令"）——本验收 35/35 XML 内，且经 4.5 双突变证明非恒真 |

**acceptance 2（多 Dock 拓扑落位）——代码/测试面通过（宿主呈现半区随 B-2）**：

| 证据项 | 证据 |
| --- | --- |
| 主 Dock＝命令条＋项目导航纵排（Left 区） | buildDockBody 重构：栅格删除，mainLayout 纵排 addWidget(topBar)＋addWidget(left)；`setWidget(body)`（插件本体 Dock 即主 Dock） |
| 『IRD 属性与诊断』右 Dock＋『IRD 任务和状态』底 Dock | m_propsDock（objectName ird_props_dock）／m_tasksDock（ird_tasks_dock）创建、setWidget(rightWidget/bottomWidget)；addDockWidget(Right/Bottom) 延后至 reassertEmbeddedPresentation 装载自证拍（与主 Dock 同受 restoreState 隐藏根因链支配→一并 `show()` 重显——代码 L684~697） |
| 中央让位页保持已构建不挂载 | centralWidget() 不再进入任何布局（O-38 裁决③——宿主中央 RWStudioView3D 唯一所有三维） |
| 三区可见性目标改绑 Dock 本体 | setRegionVisibilityTarget(Left→this／Right→m_propsDock／Bottom→m_tasksDock)——区域开关作用面升格为 Dock 本体 |
| PM-14 旗标重施 | reassertEmbeddedPresentation：重显后 `setVisible(m_content->regionVisible(Right/Bottom))` 按用户意愿位重施（用户跨会话隐藏不被夺回）；主 Dock 不重施（v1.11"G1 装载即呈现"口径，注释申明理由）——本任务收尾提交 d7e4a24 的主旨，代码事实成立 |
| 尺寸初值钳制 | resizeDocks：右 `qBound(300, width/5, 480)`／底 `qBound(190, height/4, 340)`；[geometry] Dev 行改载三 Dock 尺寸/可见性＋宿主状态栏可见性（v1.11 要素 3 的对位迁移） |
| 布局记忆三区键零变化（PM-14） | diff 未触及 WorkbenchContent.cpp 布局记忆键区（loadFlags/storeFlags 路径零变更 hunks）——键零变化成立 |
| WorkbenchShell 自有状态栏等价迁移 | WorkbenchShell.cpp L329~340：`m_window->statusBar()` 自建永久位 QLabel（objectName 同名 ird_status_project_text＋stretch 1）接 setStatusTextObserver／showMessage 转发接 setStatusMessageObserver；呈现行为等价由 WorkbenchShellGuiTest 既有断言承载（L461~474：可写"演示项目"/只读"演示项目（只读）"/脏"演示项目*（只读）"三态断言，本验收 35/35 全绿内） |
| v1.11 状态行钉底修复的语义等价迁移逐项对照 | ui.md §13 落位登记注④三要素逐项（恒可见→宿主状态栏本身恒可见／纵排防挤出→承载结构消解／[geometry] 排障→三 Dock＋宿主状态栏事实）——与代码逐项对照成立 |

**acceptance 3（门禁与留痕）——门禁/ctest/留痕通过；宿主 GUI 冒烟执行状态＝未执行（B-2）**：

- ird_gates 双向差集 0（4.3）；ctest ^ird$ 2/2＋^ird_gui$ 1/1；gtest XML＋ird-test-report.json（三件 decisive=true）＋构建/门禁/冒烟日志在库齐备（4.10）。
- "宿主 GUI 冒烟…如实登记执行状态"——登记纪律成立（实施段/提交信息/落位登记注⑥⑧均如实写明未执行、随验收段补录）；但 verify 4 本体未执行且本验收未获窗口，宿主拓扑与状态栏唯一性**零真机实证**→随 B-2 阻断。

### 4.5 测试真实失败能力 —— **通过（双突变两红一复绿，验收者自有 worktree 内）**

1. 突变 M1：WorkbenchContent.cpp refreshStatusBar `if (m_statusTextObserver)` → `if (false)` → 重建 gui_test → `TwoPhaseAssemblyContract_UI_SPLIT` **FAILED**，断言消息精确命中（"PM-11 状态文本未经理观察者投影（UI-T18 契约面失效）"）（acc-mut1-build.log／acc-mut1-run.log）。
2. 突变 M2：还原 M1 后同法瘫痪 showStatusFeedback 路由（`if (m_statusMessageObserver)` → `if (false)`）→ 同用例 **FAILED**（"瞬态消息未经理观察者投影（UI-T18 契约面失效）"）（acc-mut2-build.log／acc-mut2-run.log）。
3. 还原后复跑**全量 GUI 套件 35/35 PASSED**（acc-gtest-gui-restore.xml／acc-restore-run.log）；`git status --porcelain` 空＝送验对象零残留。
4. 两断言分别锚定 UI-T18 契约面的两个新出口（永久投影／瞬态路由），红→绿闭环证明测试真实校验行为而非恒真。

### 4.6 架构红线（AGENTS §5 八条）—— **通过**

1. L2 零 Qt：diff 仅触及 ui 单元（R-3 唯一例外单元）＋文档/留痕；新增 include 唯一为 `<QDockWidget>`（ui 单元内，合法）；无任何计算内核文件被触及。
2. 业务域互链禁止（R-1）：无新增跨单元 include（4.11 表旁证：全 diff 仅 1 条新增 include）；ui→project 既有 C-3/C-5 端口适配面（PortAdapters）本任务零改动；gates SUB/R1 命中集 head↔base 逐条一致＝无新增表外边。
3. 跨单元私有头禁止（R-2）：零新增跨单元 include。
4. 框架零源码修改（SA-02）：4.1（无 patches 新文件、无框架路径改动）；gates SA-02 工作区核对 head↔base 一致。
5. 名称前缀拼接/剥离（R-4）：gates R4 命中 head↔base 各自集合内逐位一致（既有命中，非本任务引入）。
6. 权威唯一（PA-1）：状态文本权威仍在内容装配层 formatProjectStatusText（插件/壳只渲染投影）；Dock 拓扑与可见性旗标＝纯呈现面；命令入口语义未移（本任务未触及命令网关）。
7. 不可变历史（PA-2）：本任务不触碰修订面（diff 无 project/revision 相关文件）。
8. 未恢复 old/ 实现：多 Dock 拓扑与状态投影全部按 ui.md §10.1 v1.14＋O-43 ③从零实现。

### 4.7 注释规范 —— **通过（附三处建议级勘误，见 F-333/F-334）**

- **公共头全查（WorkbenchContent.hpp）**：两个新虚函数 Doxygen 契约完整——@brief 职责、@param [in] 方向、入参单位（超时 ms、0＝驻留语义）、生命周期（≤本实例）、线程（UI 线程）、不注册＝无投影的语义边界；v1.14 登记注内联。契约文档级合格。
- **实现文件抽查（全 8 件 diff 逐块读过，≥3 件要求满足）**：showStatusFeedback（判空收口点"为什么"、与 setTitleTextObserver 同纪律）、buildDockBody 多 Dock 拓扑注释块（三 Dock 分工／addDockWidget 延后时序的根因链／红线不变申明）、PM-14 旗标重施注释块（为什么重施／为何主 Dock 不重施）、尺寸钳制注释（§4.4 最小尺寸出处）——§2.4"解释为什么"标准达成；成员注释含所有权（窗口树托管）与线程语境。
- §2.5 高危信息表抽查：单位（ms）✔、所有权（Qt 父子树/窗口树）✔、线程（UI 线程回调）✔、错误语义（宿主状态栏不可得→降级留痕不吞错）✔、确定性（regionVisible 模型位重施次序）✔。
- **建议级勘误（不阻塞）**：①三文件头未随 UI-T18 增量更新——UiPlugin.hpp @brief 仍写"任务 UI-T16 立项，UI-T17 增量扩展"且设计依据缺 §13 UI-T18 行/v1.14（**该文件恰为 F-326 上轮修复对象，同族复发**）；UiPlugin.cpp 设计依据同缺；WorkbenchContent.hpp 设计依据缺 v1.14 契约面变更引用（本次契约面变更恰落在该头）→ **F-333**。②WorkbenchContent_p.hpp L36 `#include <QStatusBar>` 成死包含（成员已删；d7e4a24 对 .cpp 做了"死包含字面收口"而 .hpp 漏收）→ **F-334**。③尺寸钳制注释写"钳制到内容最小宽 280 px 以上/最小高 160 px 以上"，代码下界实为 300/190（高于 §4.4 最小值，行为无碍，注释数字与代码不同义）——随 F-333 触碰窗口顺带对齐。

### 4.8 文档同步＋findings 相关性 —— **通过（附一处登记口径勘误 F-332）**

- units/ui.md v1.14：文档版本行＋§10.1 v1.14 增量注⑦⑧＋§13 UI-T18 行＋落位登记注＋§16.7 v1.14 行，与代码/留痕逐项对照成立（含注⑥"未执行如实登记＝宿主 GUI 冒烟"）；v1.11 钉底语义等价迁移对照（注④）与代码事实一致。
- **登记口径勘误（建议级 F-332）**：§16.7 v1.14 行⑦仍写"ird_gates 命中 **43 条**与基线一致零新增"、⑥仍写"集成**五目标**"——d7e4a24 提交信息自述"注⑥修正两处与实测证据不一致的口径（命中数 43→归一化 63/63、五目标→六目标）"，注⑥确已修正，但 §16.7 平平行**漏改**（在库 gate-hitset-*-r3.txt 与本验收独立复跑均为 63↔63；"零新增"结论本身正确，仅计数与目标数两处口径残留旧值）——同文件内注⑥与 §16.7 行口径不一致。
- 契约任务状态仍为 ready／追踪矩阵翻转：归收尾环节（编排者辖权），非实施者偏差。
- validate-task PASS（验收者亲跑）。
- **F-318~F-330 逐条相关性复核**（F-318~F-320/F-324/F-326/F-327 原件登记于 acc/UI-T16/1、acc/UI-T17/1 evidence 分支的 findings.json，未并入 redesign-main 副本；redesign-main 副本现有 F-001~F-317＋F-321~F-330）：

| 条目 | 现状态 | 与本任务送验面的关联复核 |
| --- | --- | --- |
| F-318/F-319 | open @ acc/UI-T16/1 | UI-T16 文档表述/失实载体问题，实质已由 ui.md v1.11 兑现（UI-T16-r2/UI-T17-r2 两轮复核结论同）；非本任务辖权。建议随 findings 清理判定（两分支登记未并入主线副本的问题第三轮在案）。 |
| F-320 | open @ acc/UI-T16/1 | 装载通道环境前置（PATH＋--rwsplugin 首通道）——本轮无宿主装载执行，无新证据；B-2 返工补录时须按其清单自证。**保持 open**。 |
| F-321 | open @ redesign-main | 切换路径状态投影——本任务 diff 未触及 UiSessionController/切换协议（4.1 文件清单可证），无新证据亦无冲突。**保持 open**。 |
| F-322 | open @ redesign-main | Dev 日志采样尾段口径——本任务在库留痕未新增宿主 Dev 日志固化件，无叠加证据。**保持 open**。 |
| F-323 | open @ redesign-main | 全新工作树可复现性——**本验收第三轮 100% 复现两子项**（§0），且按既定口径成功完成全量构建（处置路径持续有效）。**保持 open**（框架级修复仍缺）。 |
| F-324 | open @ acc/UI-T17/1 | ini 通道 FormatError——本轮未使用该通道，无新证据。**保持 open**。 |
| F-325 | open @ acc/UI-T17/1 | 取证管线脆弱——B-2 补录时直接适用（截图判据＋优雅退出＋日志全量固化方向）。**保持 open**。 |
| F-326 | fixed @ redesign-main | 上轮文件头修复成立；**本任务对 UiPlugin.hpp 出现同族新发**（登记 F-333，不构成对 F-326 闭环的翻案）。 |
| F-327 | open @ redesign-main | 适配器直测延期——本任务未触及 StorePortAdapter/SerialTaskExecutor，延期理由（零 CMake/链接面变更）继续成立。**保持 open**。 |
| F-328/F-329/F-330 | open @ redesign-main | UI-T17 登记勘误/证据卫生/重开 UX 语义——本任务 diff 未触及 v1.13-r4 注文与会话控制器，无新证据；F-328 所请勘误窗口仍未兑现（连续三轮在案，宜随收尾批次统一处置）。**保持 open**。 |

- 本验收无可随手消账项；新增建议级 F-331~F-334 随本 evidence 分支转登（见"二、问题清单"）。

### 4.9 commit 质量 —— **通过（范围 3 提交全查）**

- **0e54c27b**（实现主提交）：标题 `[WP-10-T18] Dock 拓扑拆分＋状态行迁移宿主状态栏（O-43 裁决③承接）`——动词开头、≤50 字；四段齐备（What 按文件列要点／Why 引 O-43＋DTB v0.19 行／验证含"未执行（如实登记）：宿主 GUI 冒烟"／影响面含 SA-02 与布局记忆键不变）。验证段"ird_gates 命中 43 条"为当时归一化口径（其后 r3 重录为 63/63 并在收尾提交中更正登记）——非粉饰，演进留痕连续可考。
- **3cb5e96e**（merge base 对齐）：无信息量合并提交（align 派发基线，UI-T17 先例 656b7f30 同型），可接受。
- **d7e4a24**（收尾提交）：四段齐备；验证段每一条（六目标 BUILD_EXIT=0／冒烟 SMOKE_BUILD_EXIT=0／154·22·35 decisive／ctest 2/2＋1/1／gates 63/63 diff 空／validate-task PASS／宿主冒烟仍未执行）经本验收逐件对得上（4.2/4.3/4.10）；"r2 轮中间件已清理"与仓库根 3 件 r2 代 XML 残留相矛盾（B-1 反证，已入 4.1）；文末 `Acc-Covered: 1,2,3` 与 acceptance 三条的实证状态相符（宿主半区已在各条内如实限定）。
- 诚实度：无粉饰——所有未执行项明示"未执行"。

### 4.10 留痕完整 —— **通过**

- 在库 `doc 树 traceability/builds/wp10-t18/` 共 **21 件**逐一核对：gtest XML 三件（154/22/35，failures=0，head 行 timestamps 19:02 与 r3 口径一致）＋三 console＋ird-test-report 三件（`"decisive": true` 各一）＋integration-build-r3.log（末行 BUILD_EXIT=0）＋smoke-build-r3.log（SMOKE_BUILD_EXIT=0）＋ctest 双标签两件＋ird-gates 两件＋gate-hitset 三件（head/base 各 63 行、diff 空——与验收者独立复跑逐字节一致）＋validate-task-r3.log（PASS）＋normalize-gate-log.ps1（归一化引擎，27 行已读）。
- **所有 .log/.xml 真实入库**（无 .gitignore 吞件痕迹——git 对象在库，worktree 检出齐全）。
- 实施段环境声明与证据自洽：integration/smoke 日志内路径为实施 worktree（`.wt-wp10-t18`），产物尺寸与验收者重建件同数量级。
- resultNote 等价物＝ui.md §13 落位登记注⑥（验证结果汇总）已更新；phase-one-validation.log 沿 UI-T14 以来 ui 单元惯例不再逐任务追加（近期四个 ui 任务均如此，本验收沿用该口径，不判缺陷）。
- 验收段自身留痕随本 evidence 分支入库：`builds/wp10-t18/acc-attempt1/` 24 件（configure/全量构建/冒烟 configure＋构建/verify1/ctest 双/gtest 三套＋复绿/gates 两侧日志＋两归一化集/两突变 build＋run）。

### 4.11 偷懒/缩水实现扫描 —— **通过**

1. `TODO|FIXME|XXX|HACK|stub|placeholder|not implemented|未实现` 扫描（产品面 diff 新增行）：**零命中**。
2. 空实现检测：新增函数全部有实质体（双观测钩子＝存储＋判空转发，为契约显式形态；showStatusFeedback＝判空收口路由；buildDockBody/reassert 增量均为实质逻辑）；无"仅返回默认值"的契约外空壳。
3. 逐条证据表：见 4.4（acceptance 三条逐项，宿主呈现半区如实标注 B-2 未获实证——不采信"已实现"声明本身，代码/测试/门禁三层独立复核）。
4. 测试强度：4.5 双突变双红＋复绿；WorkbenchShellGuiTest 三态 PM-11 断言（可写/只读/脏）校验等价迁移的行为与边界；断言均校验行为非恒真。
5. 诚实边界核对：契约 note 预登记的宿主 GUI 冒烟未执行＝实施段如实申报，与本验收查证一致（在库零宿主冒烟件）。

---

## 2. 阻断项详述（返工输入）

### B-1 范围越界：仓库根 `traceability/` 3 件（4.1）

- **事实**：`traceability/builds/wp10-t18/gtest-ui-{contract,gui,model}.xml`（git mode 100644，0e54c27b 引入、d7e4a24 在库）不在契约 allowedFiles 任何模式内；内容为 16:24 r2 代捕获，与在库 19:02 的 -r3 权威件并存，构成规范树外的过期证据副本；与 d7e4a24 提交信息"r2 轮中间件已清理"矛盾；units/ui.md v1.13-r2 行已登记同类缺陷为"allowedFiles 外"并做过更正——已知类缺陷复发。
- **返工**：wp10-t18 返工提交 `git rm` 三件即可（纯留痕残留，零产品影响；**不得**以修改契约 allowedFiles 方式迁就——契约面收窄/扩大须所有者裁决）。若所有者有意设立仓库根 traceability/ 为规范留痕位，则须先修订契约与治理口径，再重新送验。
- **定级依据**：ACC §5 fail 判据首项"范围越界"；4.1 通过标准为"每个变更文件都在 allowedFiles 内"的机械判据。

### B-2 宿主 GUI 冒烟未执行（4.3 verify 4／4.4 acceptance 2、3 宿主半区）

- **事实**：契约 verify 第 4 条要求框架 RobWorkStudio.exe 真机加载后核验多 Dock 拓扑与宿主状态栏唯一并留痕（启动日志＋截图＋操作序列）。实施段如实登记未执行（契约 note 诚实边界，约定验收段补录）；验收段已按派发指令问询所有者窗口（UI-T17 先例＝所有者人工操作），**未获答复**；在库 21 件留痕无任何宿主冒烟件。本任务与 UI-T17-r2 的关键差异：后者有在库 r4 取证＋所有者确证可供裁定，本任务**零宿主证据**，无裁定基础。
- **返工**：按 UI-T17 先例由所有者人工操作补录（--rwsplugin 直载通道＋F-320 PATH 前置清单＋F-325 截图判据/优雅退出教训）：①Plugins→Load plugin（或 --rwsplugin）装载后核验主 Dock＋属性右 Dock＋任务底部 Dock 三区拓扑；②核验宿主状态栏唯一（插件区无第二状态栏；PM-11 文本与瞬态消息出现在宿主状态栏）；③PM-14 旗标重施核验（隐藏右/底 Dock→重载→保持隐藏）；④启动日志＋逐帧截图＋操作序列入 `builds/wp10-t18/`；完成后重新发起**全量**验收（attempt 2）。
- **定级依据**：ACC §4.3"逐条独立执行，全部成功"＋§4.4"未执行的不得标注通过"；UI-T17 attempt 1 先例（B-1 同型判阻断）。

---

## 3. 问题清单

### 阻断级

- **B-1** 范围越界（§2）——仓库根 traceability/ 3 件 r2 代 XML，allowedFiles 外。
- **B-2** 宿主 GUI 冒烟未执行（§2）——verify 4 零真机实证，窗口问询未获答复。

### 建议级（已随本 evidence 分支转登 traceability/findings.json，编号自 F-331 起）

- **F-331**（＝B-1 的登记面，blocking 沿 F-319 先例入册）：仓库根 traceability/ 越界件 3 件（r2 代 16:24 捕获；v1.13-r2 已更正过的同类缺陷复发；与"r2 中间件已清理"的提交表述矛盾）。
- **F-332** ui.md §16.7 v1.14 行口径未随注⑥修正同步：⑦"命中 43 条"应为归一化 63/63、⑥"集成五目标"应为六目标（契约 verify[0] 命令面）；"零新增"结论本身经独立复跑证实。随下批 ui/文档触碰窗口勘误。
- **F-333** 三文件头未随 UI-T18 增量更新（UiPlugin.hpp @brief/设计依据、UiPlugin.cpp 设计依据、WorkbenchContent.hpp 设计依据）——F-326 同族复发（UiPlugin.hpp 即上轮修复对象）；类/函数级注释完备零行为影响。随下批 ui 触碰窗口对齐（顺带把尺寸钳制注释的 280/160 与代码下界 300/190 对齐表意）。
- **F-334** WorkbenchContent_p.hpp L36 死包含 `<QStatusBar>`（d7e4a24 对 .cpp 做了死包含字面收口，私有头漏收）。随下批 ui 触碰窗口删除。

---

## 4. 裁决

- **verdict = fail**。4.2／4.5／4.6／4.7／4.9／4.10／4.11 通过；4.8 通过（附 F-332 勘误）；4.1 不通过（B-1）；4.3 verify 1~3 通过、verify 4 未执行（B-2）；4.4 代码/测试面通过、宿主呈现半区随 B-2。
- 两项阻断相互独立，任一即足以 fail（ACC §5）；B-1 为机械判据无裁量空间，B-2 按 4.4 纪律不得标注通过。
- 实现面与测试面质量本身零缺陷检出：契约面变更、多 Dock 拓扑、PM-14 重施、harness 等价迁移的代码审查逐行通过；154/22/35 全绿＋门禁 63↔63 差集 0＋双突变验证——返工面仅 B-1（删 3 件）与 B-2（宿主冒烟补录），外加 4 条建议级勘误可随返工窗口顺带。
- 本验收未触碰 wp10-t18／redesign-main／main 与编排者辖权文件（traceability/pipeline/state.json）；未向送验分支推送任何提交；未执行任何交互式宿主冒烟（窗口未授予）。
- 返工后按 ACC 全量重验（attempt 2）；合入决策按 ACC §6 归所有者。

## 5. 验收者环境

Windows x64，Git Bash＋pwsh 7，VS 2022 x64，Qt 6.11.1（D:/software/Qt/6.11.1/msvc2022_64），vcpkg 经典模式（仓库根；集成树与冒烟树均经其 toolchain 解析）。复现现场＝仓库外双 detached worktree（head @d7e4a24／base @feb0947，`D:/10_Source_Repos/21_robot/acc-ui-t18/`）；集成构建树与冒烟构建树均全新配置；F-323 两子项按既定口径处置（ini 模板手工补置＋CL="-DNOMINMAX"）。宿主 GUI 冒烟未执行（所有者窗口问询无答复）。验收者全部独立产物 24 件随本 evidence 分支入库 `builds/wp10-t18/acc-attempt1/`。
