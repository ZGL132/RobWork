# UI-T16 独立验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | UI-T16——工作台宿主插件（RobWorkStudio 集成入口＋开发期动态加载验证通道，sdurws_ird_ui_plugin） |
| 契约 | tasks/foundation/UI-T16.json（schemaVersion ird-task/1，status ready） |
| 送验对象（冻结） | `6441c13617a4c8701827d2ac8a8cb46e2a7e4e87`（分支 wp10-t16 尖端；**逐字核对 `origin/wp10-t16` == 该 SHA，零漂移**） |
| diff 范围 | `e4d70ada744eb10df9bd203dcc59e3ae517b5f34..6441c13617a4c8701827d2ac8a8cb46e2a7e4e87`（3 个提交：562a18b4 实现 / 79b3f8da 宿主装载修复＋冒烟重录 / 6441c136 收尾更正） |
| 验收尝试 | attempt 1（state.attempts.accept 序号；evidence 分支 acc/UI-T16/1） |
| **verdict** | **fail**（阻断级问题 B-1、B-2；建议级问题 S-1~S-3 已转登 findings.json F-318~F-320） |
| 验收日期 | 2026-09-24 |
| 记录回传 | 分支 `acc/UI-T16/1`（基于 origin/redesign-main @ b87b72f3）；路径 `traceability/acceptance/UI-T16-20260924.md`；记录提交 SHA 见编排者收到的三元组 |

## 0. 验收环境（ACC §3）

- **复现 worktree A**＝`git worktree add --detach` @ 6441c136（全新从零）；集成 build 树全新配置：`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=<主仓>vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64 -DCMAKE_CXX_FLAGS="-DNOMINMAX -DWIN32_LEAN_AND_MEAN -DWIN32"`——配置 exit 0、缓存 grep `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON` 实证。配置前自主复制两件 git 未追踪 ini 辅助件（`RobWorkStudio.ini.template.static/.shared.in`，`.gitignore:79 [Bb]in/` 吞没的环境件、框架 configure_file 硬依赖——F-007/F-009 既有登记环境链，非送验对象缺陷）。验收者另亲历一次按 AGENTS §4.1 字面裸命令（无 toolchain）配置失败（缺 Boost——F-098/F-099 既有登记同款），补 toolchain 后从零成功。
- **冒烟 build 树**＝worktree A 内独立目录 `build-smoke`（vcpkg toolchain＋Qt 前缀）。
- **ctest/GUI 运行环境**＝`QT_QPA_PLATFORM=windows`＋`QT_QPA_PLATFORM_PLUGIN_PATH=<Qt>/plugins/platforms`＋框架/Qt DLL PATH（ui.md §12.2 纪律——环境由运行方供给）。
- **ird_gates 复现**＝双 detached worktree（head @ 6441c136、base @ e4d70ada）各以 `cmake -DIRD_ROOT=<该树 industrialrobot> -P cmake/ird_gates.cmake` 亲跑，路径前缀归一化＋sort -u 后逐行 diff。
- **宿主装载复现尝试**＝验收者以 worktree A 全新构建产物（`build/RobWorkStudio/libs/Release/sdurws_ird_ui_plugin.dll`）三次装载尝试：①`--rwsplugin` 命令行；②Plugins→Load plugin 对话框（键入路径至 DLL 同级目录）；③`--ini-file` 的 `[Plugins] UserPlugin` 启动装载——框架主窗口（菜单/工具栏/中央三维视图）三次均正常起窗，但三次均未达成装载门控（无 `[ird-ui-plugin]` 控制台输出、无 `ird-ui-plugin-logs/` 目录生成）：前两次缺 python313.dll/boost dll 的 PATH 前置（插件 DLL 导入表面含 `python313.dll`、`boost_filesystem-vc143-mt-x64-1_92.dll`——strings 实扫），补齐后仍未装载（QPluginLoader 静默失败，原因未深究）。**该通道的装载环境前置在契约/单元卡均无登记，异机复现受阻——转建议级 S-3/F-320**。验收者对宿主形态缺陷的判定不依赖本次装载复现：以送验对象自载留痕的逐帧亲验为准（见 4.4/4.11）。
- 验收者全程只消费产物、契约、设计文档与在库留痕，未读取实施者过程叙述（ACC §2）；构建/测试/门禁/验证脚本全部亲手重跑。

## 一、逐项证据（ACC §4，4.1～4.11）

### 4.1 改动范围 —— 通过

- 范围内变更 74 文件（`git diff --name-only e4d70ada..6441c136`）：12 件在 `industrialrobot/ui/**`（allowedFiles 第 1 项）、60 件在 `traceability/**`、`units/ui.md` 与 `development-task-breakdown.md` 各 1 件——全部落在契约 allowedFiles 四模式内。
- forbiddenFiles（REQUIREMENTS.md/ARCHITECTURE.md）零触碰（不在变更清单）。
- 框架零源码修改（SA-02）：`git diff --name-only` 对框架源码面＝空集；`industrialrobot/patches/` 在范围内零 diff（无新增登记＝无框架改动，自洽）。

### 4.2 可复现构建 —— 通过（两模式全新树从零，零错误）

- **集成模式**（worktree A 内新建 `build/`）：`cmake --build build --config Release` **全量退出码 0、`: error`/`: fatal error` 计数 0**（RobWork 全库→RobWorkSim→RobWorkStudio→industrialrobot 全单元→ui 三测试目标＋sdurws_ird_ui_plugin 全新编译；产物 `build/RobWorkStudio/bin/Release/sdurws_ird_ui{,_test,_contract_test,_gui_test}.exe` 与 `build/RobWorkStudio/libs/Release/sdurws_ird_ui_plugin.dll` 全部生成）。
- **独立冒烟模式**（worktree A 内 `build-smoke/`）：配置＋全量构建 **exit 0、错误计数 0**；`ui/Release/` 下 `sdurws_ird_ui.lib`＋三测试 exe＋运行期 DLL 就位；插件目标按登记不注册（构建日志显式 STATUS 行"standalone smoke mode——sdurws_ird_ui_plugin not registered"，与 ui.md §13 落位登记注①逐字一致）。

### 4.3 契约 verify 命令 —— 通过（逐条独立亲跑）

| # | 契约 verify | 验收者亲跑结果 |
| --- | --- | --- |
| 1 | `cmake --build build --config Release --target sdurws_ird_ui_plugin` | exit 0，`sdurws_ird_ui_plugin.dll` 链接产出（Automatic MOC 目标级生效实证） |
| 2 | `ctest --test-dir build/RobWorkStudio/src/rwslibs/industrialrobot/ui -C Release -L "^ird$"` | **2/2 通过**（sdurws_ird_ui_test 1.86s＋sdurws_ird_ui_contract_test 0.40s），exit 0 |
| 3 | `ctest --test-dir build/RobWorkStudio/src/rwslibs/industrialrobot/ui -C Release -L "^ird_gui$"` | **1/1 通过**（ctest 级）；用例级直跑 exe＝**34/34 全过**（`[ PASSED ] 34 tests`，桌面解锁态剪贴板可用） |
| 4 | 框架 RobWorkStudio.exe Plugins→Load plugin 宿主冒烟 | **验收者未能在自有环境复现装载成功**（见 §0 环境段——装载环境前置未登记，S-3/F-320）；以送验对象自载留痕＋逐帧亲验代复现，结论见 4.4 条目 3——**该条目不成立** |

### 4.4 acceptance 条目逐项核对 —— 条目 1/2 通过，条目 3 不成立

| # | acceptance 要点 | 验收者证据 | 结论 |
| --- | --- | --- | --- |
| 1 | 插件目标落位（RobWorkStudioPlugin 派生 DLL 零计算逻辑；AUTOMOC 仅该目标；harness 行为回归零变化；禁止 QMainWindow 嵌 Dock；ui.md §10.1 增量登记；双模式零错误） | ①目标注册与 MODULE/AUTOMOC per-target ON 亲验（verify 1＋CMakeLists 插件段）；②UiPlugin.cpp 无任何 QMainWindow 构造，Dock 体栅格安放内容装配面（buildDockBody 逐行读）；③IWorkbenchShell.hpp 契约头零 diff（范围内不在变更清单——门面签名零变化实证）；④壳拆两层后 harness 回归零变化：首页态/新建→打开/PM-07 双开降级三态留痕亲验（harness-homepage.png 五区全可见、harness-new-open.log "项目已创建→已打开（可写：是）"、harness-pm07-instance2-readonly.log "可写：否（降级只读）"＋harness-pm07-dual-open-readonly.png 标题「IRD演示项目（只读）」＋顶栏只读徽标＋状态栏投影亲验）＋既有 GUI 套件亲跑全绿；⑤ui.md v1.10 §3.3 增列＋§10.1 增量注＋§13 落位登记注＋§16.7 行在库实证；⑥双模式零错误（4.2） | **通过** |
| 2 | 门禁：命中集归一化比对——新增仅限插件登记面；其余各码零变化；ctest 两标签回归通过＋报告级留痕 | ird_gates 双树亲跑归一化 diff：**恰增 4 条唯一命中**（SUB×2：`ui->project`/`ui->ui` 均指 sdurws_ird_ui_plugin；R3×2：ui/src/WorkbenchContent.cpp、ui/src/WorkbenchContent_p.hpp——既有 ui 例外域文件级延伸），其余 R-1/R-2/R-4/R-5/T-1/T-2/GRAPH/LIB/SA02 逐行零变化；52→56 总数吻合；DTB §4.5 SUB 登记行＋traceability/wp10-t16-gate-registrations.md 提交件与命中逐条对得上；ctest ^ird$ 2/2＋^ird_gui$ 1/1（34/34）亲跑通过；ird-test-report.json（210/209/1）与三件 gtest XML（154/22/34，failures 与登记一致）亲核 | **通过** |
| 3 | 宿主交互验证留痕：五区工作台嵌入宿主主窗口＋框架菜单/工具栏/RWStudioView3D 共存；Ctrl+Shift+P 宿主可用；新建/打开协议经壳入口（PM-11 状态栏投影）；布局记忆宿主生效；留痕齐备＋动态加载边界登记 | 送验对象自载证据逐帧亲验：**①五区面板在宿主主窗口不可见**（host-smoke-04/06b/09 截图亲验：宿主窗口仅框架菜单/工具栏＋中央三维视图，无顶栏/左栏/右栏/底部；对照同内容装配面的壳模式基线 harness-homepage.png 五区全可见；host-smoke-02b 的 Plugins 菜单左栏√/右栏√/底部√勾选态与实际不显示自相矛盾——亲验一致）；**②PM-11 状态栏投影未出现**（host-smoke-06b/09 放大核验，三维视图直达窗口下缘，无状态栏条带）；**③打开/新建协议完成性未实证**（操作序列日志门控 G2：proj-demo lock 心跳打开前后同值 12:06:42；G3 标注"!! 门控 G3 未确认：新建协议 4s 内未见目标目录落盘"，收尾核查目标目录仍为空）；**④布局记忆跨会话演示落空**（菜单导航 Down×15 实落第 16 可选条目＝右栏，ini visibleRight=false 佐证；左栏开关未有效演示、恢复默认布局未入 recentCommands）；旗标半区持久化路径本身已证（G4a ini 落盘＋二次装载读取）。已实证项（验收者确认）：G1 装载门控（双实例 [ird-ui-plugin] 控制台输出＋Dev 日志）、菜单/工具栏共存（host-smoke-02b 插件注入条目与框架条目同菜单）、命令面板可弹出且命令可执行（ini recentCommands=project.open,project.new 落盘）、RWStudioView3D 共存（宿主中央三维在位）。动态加载边界（仅开发期通道、SA-01 不变）登记如实 | **不成立**（契约明文要求"五区工作台嵌入宿主主窗口""PM-11 状态栏投影""布局记忆在宿主窗口生效"均未达成实证）→ **阻断 B-1** |

### 4.5 测试真实失败能力 —— 通过（两变异注入，均精准变红、还原复绿）

在 worktree A 逐次变异→重编 gui_test→亲跑→`git checkout --` 还原→复跑复绿（每轮后 `git status --porcelain` 确认源树洁净）：

| 变异 | 注入点 | 预期红用例 | 实测 |
| --- | --- | --- | --- |
| M1 | `WorkbenchContent.cpp` build() 宿主控件空校验置死（`if (false)`） | BuildValidationRejectsIncompleteDeps_UI_SPLIT | **FAILED（5/6 红 1）**，还原后 6/6 复绿 |
| M2 | `ShellSupport.cpp` loadFlags 损坏判别置死（版本不识别/类型不符不再判 Corrupt） | EmbeddedCorruptMemoryFallsBackWithDevDiagnostic_UI_SPLIT | **FAILED（5/6 红 1）**，还原后 6/6 复绿 |

结论：新增 UI-SPLIT 族断言校验真实行为与边界（两段装配时序契约/必填校验拒绝/损坏整段丢弃＋Dev 诊断），非恒真；会红能力由两例红实证。

### 4.6 架构红线（AGENTS §5 八条）—— 通过

- **L2 零 Qt**：ui 为界面例外单元（NFR-MNT-01）；产品库源集（src/＋include/）外无新增 Qt 面；插件目标 Qt 链接走 plugin 分类面（ird_gates 亲跑确认 R3 链接面命中零新增）。
- **R-1/R-2 业务域互链与私有头**：ui 产品库源集对 `sdurws/ird/<其他业务单元>` 的 include **亲 grep＝空集**；project 公共头引用仅存在于 `app/`（O-31 既有特权边）与 `plugin/UiPlugin.*`（本次 SUB×2 登记面）——引用全为 `include/sdurws/ird/<unit>/` 公共头（DiagCodes.hpp/StoreTypes.hpp/ProjectStore.hpp）＋框架 `rws/RobWorkStudio.hpp`；WorkbenchContent_p.hpp 留在 src/（R-2）。
- **R-4 名称前缀**：新增/改动源面拼接/剥离扫描零命中。
- **SA-02 框架零修改**：4.1 已证（patches 零 diff＋框架源码零触碰；插件经框架公开基类与 IID 机制接入）。
- **权威唯一**：打开协议推进仍全在 UiSessionController（插件仅注入触发编排处理器——§11.5 归属正确的装配期覆写）；命令判定仍全在 CommandRegistry（插件菜单动作只转发统一提交路径，逐行读实证）；名称/单位/诊断文案未在 ui 另立权威。
- **PA-2 不可变历史**：本任务无修订写路径触碰（ui 单元无修订语义）。
- **不恢复 old/ 实现**：全部实现按 O-38 裁决与 ui.md v1.10 增量从零编写（搬移语义登记逐行对应，无 old/ 目录引用）。

### 4.7 注释规范 —— 通过

- **公共头全查**：`WorkbenchContent.hpp`（319 行）文件头五要素齐备（设计依据/背景/线程模型/生命周期）；每个类/结构体/枚举/函数有 Doxygen 中文契约注释，参数方向/单位（px）/所有权（非 owning target）/错误语义（返回值轨 fail-fast）/线程约束逐条标注——契约级文档质量。
- **实现文件抽查 ≥3**：UiPlugin.cpp（装配四步注解、fail-fast 上抛分支、让位观测语义、QPluginLoader 生命周期与 Qt 父子树纪律）、WorkbenchContent.cpp（文件头搬移与行为保持声明——两处分派逐条登记）、WorkbenchShell.cpp（收敛 chrome 后文件头重写、实现期口径登记承袭）、DiagnosticsAssembly.cpp（两宿主共用装配序列逐步注解）、ShellSupport.cpp（loadFlags/storeFlags 损坏判别口径与 QSettings 按键保留语义）——§2.5 高危信息（单位/坐标系/线程/错误语义/确定性）无缺项。
- 79b3f8da 对 buildDockBody 失败分支"误导性注释"的修正属实（现注释与宿主冒烟实证一致）。

### 4.8 文档同步＋findings 相关性 —— 通过（附建议级 S-1）

- 实现与任务卡零偏差（宿主差异面均按 §10.1 v1.10 增量登记：布局旗标半区/折叠随形态关闭/会话入口覆写面/让位页——逐条与代码对得上）；ui.md v1.10（版本行＋§3.3＋§10.1 增量注＋§13 落位登记注＋§16.7）与 DTB §4.5 SUB 登记行随落位提交在库；validate-task（worktree A 亲跑）＝**PASS (1 tasks)**；validate-docs（亲跑）＝**PASS (20 units, 12 trace entries, 187 task files)**。
- 任务状态/追踪矩阵：契约状态翻转归收尾（PIPE §4.7）——不要求随送验对象携带（先例同）。
- **findings.json 未关闭条目相关性核对（open 260 条，最大编号 F-317）**：
  - **F-007/F-008/F-009/F-254**（全新树配置环境链族）：验收者本次全部亲历复现（ini 模板缺件/裸命令缺 Boost/冒烟 Qt 前缀），与本任务零代码关联，无可随手消账项；
  - **F-259**（R3 机器面豁免滞后，WP-01-T03 辖权）：本任务 R3×2 新命中为其同族延续，实施侧已沿 wp10-t03 §1 同型请求增量登记（gate-registrations §2），非静默积压；
  - **F-201**（phase-one-validation.log 载体脱节）：本任务该日志零提及，按既有登记口径以 `traceability/builds/wp10-t16/` 为 4.10 实质载体；
  - **F-207/F-231/F-309/F-315**（留痕编码族）：在库留痕主体为 UTF-8 洁净（integrated-build.log 零乱码命中）；仅 `ctest-ui-last.log` 时间戳列"中国标准时间"呈 GBK 双重编码乱码（"й׼ʱ"型）——同族再现，随家族统一处置，不另立新条；
  - 其余 open 条目属其他单元/横切面，与本任务无相关性。

### 4.9 commit 质量 —— 562a18b4/79b3f8da 通过；6441c136 不通过（**阻断 B-2**）

- `562a18b4`（实现）：标题动词开头合规；四段齐备；其验证段声称（双模式零错误/gates 恰增 4/ctest 回归/harness 三态复测）经验收者独立复跑**全部为真**；对宿主冒烟因锁屏中断的 WIP 边界如实标注。
- `79b3f8da`（宿主装载修复＋证据重录）：四段齐备；hostWidget 漏注入的缺陷定位、修复与证据重录的声称经验收者对照代码（UiPlugin.cpp:172 hostWidget=this 注入＋同点位注释）与在库截图/日志逐条核实**为真**。
- `6441c136`（收尾更正）：**主旨属实且值得肯定**——对 79b3f8da 四项宿主交互表述强于实证的更正登记，经验收者对截图/日志逐帧亲验全部成立（见 4.4 条目 3）。**但其验证段声称"新增收尾会话 03:39~03:41 独立复跑实录：ctest-ui-ird-rerun-r2.log（^ird$ 2/2）、ctest-ui-gui-final-r2.log（^ird_gui$ 33/34……）"与留痕载体不符**：①`ctest-ui-ird-rerun-r2.log` 与 79b3f8da 所载 `ctest-ui-ird-rerun.log` **逐字节一致**（diff 为空；时长 1.81/0.16 sec 逐位相同）；②`ctest-ui-gui-final-r2.log` 与 `ctest-ui-gui-final.log` **逐字节一致**（时长 3.40/3.43 sec 逐位相同，且无 ctest 时间戳头等新运行签名）；③`ctest-ui-gui-final-r2.log` 实际内容为 ctest 级"100% tests passed"（与 34/34 一致），与提交信息所称该文件载"33/34、唯一失败"**自相矛盾**（33/34 之轮若真，ctest 级必报 Failed）。即"独立复跑实录"实为既有日志的字节级拷贝，验证结论与留痕不一致——ACC §4.9"发现粉饰即不通过"，**阻断 B-2**（更正主旨的诚实性不受此影响，但复跑声称本身失实，必须随返工更正）。

### 4.10 留痕完整 —— 通过（载体齐备；宿主交互证据的实质缺口已在 4.4 条目 3 计）

- gtest XML 四件（ui_test 154/0、contract_test 22/0、gui_test 34/1、gui 单例 rerun 1/1）＋ird-test-report.json 五件（总 210/209/1 与 XML 逐位吻合；gui-rerun/contract 单件齐备）＋双模式构建日志（integrated/smoke/fix-rebuild）＋validate-task.log 全部落位 `traceability/builds/wp10-t16/`。
- 宿主冒烟留痕（host-smoke-01~12 截图、双实例控制台 stdout/stderr、操作序列日志含收尾更正登记、插件 Dev 日志固化副本、缺陷轮证据）齐备且自洽——**证据链真实呈现了缺陷**（这正是 4.4 条目 3 判不成立的依据，而非留痕缺失）。
- phase-one-validation.log/resultNote 未更新——按 F-201 既有登记口径以 builds 目录为实质载体（4.8 已注）。

### 4.11 偷懒/缩水实现扫描 —— 通过

- ①diff 新增行 `TODO|FIXME|stub|placeholder|未实现|not implemented` 扫描：**恰 1 命中**＝"顶层形态的占位面板（ird_view3d_placeholder）回归由 WorkbenchShellGuiTest 承载"——系 UI-T05 落位登记的**既有组件名**（View3DPlaceholder 占位面板，契约显式形态）而非未完成标记，豁免依据成立。
- ②空实现检测：公共契约面（IWorkbenchContent 九组方法）实现约 1350 行非空；插件回调 open()/close() 为"只读观测留痕（零操作本体）"——O-38 裁决③让位形态的契约显式声明（acceptance 原文"RWStudioView3D 共存"的最小接入），非缩水。
- ③acceptance 逐条证据表：见 4.4——条目 1/2 全条有具名证据，条目 3 核心断言无证据支撑（且反证成立）→ 已计阻断 B-1。
- ④测试强度：新增六用例 47 断言校验行为与边界；与 4.5 联动两变异实证会红。

## 二、问题清单与裁决

### 阻断级（verdict=fail 依据）

| # | 问题 | 证据 | 返工指令 |
| --- | --- | --- | --- |
| **B-1** | **acceptance 3 未达成**：嵌入式宿主形态集成缺陷——①五区工作台面板在宿主主窗口不可见（区域开关勾选态与显示不符）；②PM-11 状态栏投影未出现；③打开协议 lock 心跳未刷新（G2）、新建协议目标目录未落盘（G3）；④左栏开关/恢复默认布局未获有效演示（菜单导航落空至右栏） | 4.4 条目 3 全部证据（host-smoke-01~12 截图、操作序列日志门控行、ini 键值）均经验收者逐帧亲验 | 修复嵌入式宿主面板可见性与状态行承载（形态缺陷方向：Dock 体栅格/框架 addDockWidget 安放/框架 QtMainWindowState 相互作用），随后以同一操作序列全量重录宿主交互证据（G1~G4 客观门控全过＋逐帧截图），并同步勘误 ui.md §13 落位登记注⑤（S-1/F-318） |
| **B-2** | **6441c136 验证段"独立复跑实录"与留痕不符**：两件 -r2 日志为既有日志逐字节拷贝（时长逐位相同、无新运行签名），且 final-r2 内容（ctest 级 100% Passed）与提交信息"33/34 唯一失败"表述矛盾 | 4.9 全部证据（diff 逐字节一致实证＋文件内容引文） | 随返工提交如实更正：如需保留复跑声称，以带 `Start testing` 时间戳头的全新 ctest 捕获重录 -r2 载体；否则改述为"未复跑"。禁止以拷贝冒充新运行记录 |

### 建议级（已转登 findings.json，F-318~F-320 顺延）

| # | 发现 | 处置建议 | 责任方 |
| --- | --- | --- | --- |
| S-1/F-318 | ui.md §13 UI-T16 落位登记注⑤与 §16.7 v1.10 行的宿主交互表述（五区嵌入/PM-11 投影/布局记忆/恢复默认布局）强于实证——收尾更正只落在操作序列日志，单元卡两处未随更正同步 | 随 B-1 返工落位勘误（与更正后证据对齐） | 实施者 |
| S-2/F-319（blocking） | 即 B-2 的登记面：收尾提交复跑声称的载体失实（字节级拷贝＋内容矛盾）——跨任务跟踪防再现 | 随 B-2 返工消账；记忆点：ctest 捕获必须含时间戳头，复用旧日志必须显式标注"同轮留存副本" | 实施者 |
| S-3/F-320 | 宿主装载通道环境前置未登记：插件 DLL 导入表面牵入 `python313.dll`/`boost_filesystem-vc143-mt-x64-1_92.dll`（strings 实扫），验收者全新 worktree 构建树三次装载尝试（--rwsplugin/Load plugin 对话框/ini UserPlugin）均未复现装载成功（无 [ird-ui-plugin] 输出、无 Dev 日志目录）——通道在异机/净环境的可复现性受阻 | 随返工在 ui.md §13/契约 note 登记装载环境前置（PATH 需求清单）与装载门控客观判据（控制台输出＋Dev 日志目录），返工重录时逐项自证 | 实施者 |

## 三、裁决

- **verdict＝fail**（阻断 B-1、B-2；依据 ACC §5：acceptance 条目不成立＋4.9 验证结论与留痕不一致）。
- 实施者按本记录返工后**重新发起全量验收**（新 attempt 序号，全量重跑不做增量验收）；送验新 SHA 由新一轮验收请求携带。
- 本记录与 F-318~F-320 转登落 `acc/UI-T16/1` 分支推送 origin（记录三元组由验收者回传编排者）；验收者未合入任何实施产物、未向任务分支推送任何提交。

## 四、验收者环境说明（复现命令关键行）

- 集成构建：`cmake --build build --config Release`（exit 0；错误计数 0）
- 冒烟构建：`cmake --build build-smoke --config Release`（exit 0；错误计数 0）
- 契约 verify 1~3：见 4.3 表（全部 exit 0）
- 门禁：`cmake -DIRD_ROOT=<tree>/industrialrobot -P cmake/ird_gates.cmake`（head 56 命中/base 52 命中；归一化 diff＝恰增 SUB×2＋R3×2）
- 变异 M1/M2：见 4.5 表（均 FAILED→还原复绿 6/6）
- validate-task/validate-docs：PASS（pwsh，worktree A 上下文亲跑）
- 宿主装载三次尝试：见 §0（均未过 G1 门控——S-3/F-320）
