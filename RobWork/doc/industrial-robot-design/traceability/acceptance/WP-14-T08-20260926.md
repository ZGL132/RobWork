# WP-14-T08 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | WP-14-T08 实现 requirements 插件界面（四面板与编辑→草稿→应用链路） |
| **verdict** | **pass**（4.1~4.11 全部通过；无阻断级问题；建议级 2 项 F-375/F-376 已转登 findings.json，不阻塞合入） |
| 送验对象（冻结） | 678222d8ba235b5b9af0f7245948291b90e23f16（两提交：42cce92a 实现＋678222d8 文档留痕） |
| base | 17ff09969db3d4adf9a07b12491c61c22498a148（WP-14-T07 合入收尾） |
| 验收日期 | 2026-09-26 |
| 复现现场 | detached worktree `D:/10_Source_Repos/21_robot/tmp_acc_wp14t08_A` @ 678222d8（集成 `D:/acc_t08/build-int`、冒烟 `D:/acc_t08/build-smoke`）；base 侧门禁比对临时 worktree `D:/10_Source_Repos/21_robot/tmp_acc_wp14t08_base`（比对毕已删） |
| 独立性 | 全新上下文，未消费实施叙述；构建/测试/门禁/变异全部验收者亲手重跑；主检出（redesign-main @ e448d47e，另一 wp24 会话持有未提交改动）零触碰、零 git 写操作 |

**分支漂移核查（ACC §3）**：`git ls-remote origin refs/heads/wp14-t08` ＝ `678222d8ba235b5b9af0f7245948291b90e23f16`，与送验 headSha 逐字节一致——零漂移，非阻断。

---

## 4.1 改动范围 —— 通过

`git diff --name-status 17ff0996..678222d8`：43 文件（25 个 requirements 源码/测试/CMake＋16 个 traceability/builds/wp14-t08/** 留痕＋1 个 units/requirements.md），**逐文件均落在契约 allowedFiles 三个 glob 内**：

- `RobWork/RobWorkStudio/src/rwslibs/industrialrobot/requirements/**`：CMakeLists.txt（M）、contract_test/BuildGraphContractTest.cpp（M）、plugin/ 22 新文件、test/PluginPanelTest.cpp（A）、test/TestMainReport.cpp（M）；
- `RobWork/doc/industrial-robot-design/traceability/builds/wp14-t08/**`：16 件留痕（A）；
- `RobWork/doc/industrial-robot-design/units/requirements.md`（M，卡头 v0.7→v0.8＋§14.6 v0.8 行）。

**forbiddenFiles 零触碰**：REQUIREMENTS.md／ARCHITECTURE.md 不在 diff 中。**SA-02**：diff 无任何框架源码（RobWork/RWS/RobWorkSim）文件，`patches/` 零新文件——无未登记框架改动。

## 4.2 可复现构建 —— 通过（验收者全新构建树，零错误）

- **环境前置**（F-007/F-062 既有登记事实）：worktree 检出缺 gitignored 模板——从主检出**只读复制** `RobWork/RobWorkStudio/bin/` 三件（RobWorkStudio.ini.template.static／RobWorkStudio.ini.shared.in／none/RobWorkStudio.ini.template）后配置；两条构建命令均带 `-DCMAKE_TOOLCHAIN_FILE=vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`（T07 验收同款口径）。
- **集成模式**：`cmake -S <worktreeA>/RobWork -B D:/acc_t08/build-int -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON …` 配置成功；`grep RWS_BUILD_INDUSTRIALROBOT build-int/CMakeCache.txt` → `:BOOL=ON`。逐目标构建（Release）：sdurws_ird_requirements、sdurws_ird_requirements_plugin（→ plugin.lib 产出）、sdurws_ird_requirements_test、sdurws_ird_requirements_contract_test——全部零错误。
- **独立冒烟模式**：`cmake -S <worktreeA>/…/industrialrobot -B D:/acc_t08/build-smoke -DCMAKE_TOOLCHAIN_FILE=… -DCMAKE_PREFIX_PATH=…` 配置成功（"standalone smoke mode" 横幅）；全量构建 Debug＋Release 各一轮，错误模式计数 0、退出码 0。
- 结论留痕：本文档即验收者命令实录；无构建日志外置需求（送验侧日志见 `traceability/builds/wp14-t08/`，与验收者实测一致，见 4.10）。

## 4.3 契约 verify 命令 —— 通过（3/3 逐条亲手执行）

| # | 契约 verify 原文 | 验收者执行 | 结果 |
| --- | --- | --- | --- |
| 1 | `cmake --build build --config Release --target sdurws_ird_requirements_plugin` | 于验收者集成树 D:/acc_t08/build-int 同参执行 | 成功零错误，`sdurws_ird_requirements_plugin.lib` 产出 |
| 2 | `ctest --test-dir build/RobWorkStudio/src/rwslibs/industrialrobot/requirements -C Release -L "^ird$"` | 于 D:/acc_t08/build-int 同参执行 | **2/2 通过**（sdurws_ird_requirements_test 0.47s＋sdurws_ird_requirements_contract_test 0.18s，100% tests passed） |
| 3 | `pwsh -File RobWork/scripts/industrialrobot/validate-docs.ps1` | 于 worktreeA（送验 SHA 树）从仓库根执行 | **PASS (20 units, 12 trace entries, 189 task files)** |

补充（契约 verify 数组缺两测试目标构建行——F-300 家族既有缺陷，转登 **F-375**）：验收者在全新集成树自行补建 `sdurws_ird_requirements_test`＋`sdurws_ird_requirements_contract_test` 后执行 ctest——验收者侧正常动作，结论不受影响。

## 4.4 acceptance 条目逐项证据 —— 通过（6/6）

1. **四面板落位并挂接**——通过。对象树：`buildRequirementTree`（plugin/PanelTreeModel.cpp）根→四分组（工位→区域→工况→计划固定序）→条目，节点锚＝ObjectId、显示 name 走 `ui::ensureNoInternalIdentity`（UX-02 守卫复用非私写）；具名测试 `PluginPanel.RequirementTreeBuildsRootFourGroupsAndEntries_ACC1`（行数 1+4+条目、分组计数、锚反查、全零 id 不定位、哈希名 fail-fast 全断言）。工位面板：`stationFieldsFor` 六分量约束/容差（m/rad 显式单位）/三段/五规则 `orientationRuleParamKeys(kind)` 显隐表/来源徽标六态/FormEditCommon 批量装配面（`stationQuantitySpecs`）——测试 `StationInspectorRowsAndSourceBadges_ACC1`＋`StationFormKindDrivesParamVisibility_ACC1`。区域面板：计数-间距双模式采样（规范化经 IWorkRegionService 现算）/覆盖率目标/预览几何（8 角点＋格线）——测试 `RegionPanelDualModeSamplingAndCoverage_ACC1`＋`RegionPreviewGeometryOutlineAndGrid_ACC1`；结果着色零实现（归 KIN-07，契约豁免口径）。工况面板：负载四态占位/必验清单预览经 `deriveRequirementProfile`（P-EV-9 单点）——测试 `ConditionPanelRowsAndMustListPreview_ACC1`。校验面板：R0~R9 恒 10 层计数/逐项行 subject 跳转锚/预览-正式固定文案——测试 `ValidationPanelLayersCountsJumpAndSemantics_ACC1`。装配期一次注册：`requirementsPanelRegistration()`（§10.9 形状承载——P-REQ-8 暂持，登记 R-REQ-1 增量同步义务于卡 §14.6 v0.8②；modeling P-MDL-8 同款先例），测试 `PanelRegistrationAndDomainCommandsMatchCard_ACC5` 断言 pluginId/titleKey/stage/三能力。面板不互读控件、不缓存权威数据：`refreshPanel` 全现取重投影（RequirementsPanelWidget.hpp L109-117 注释与实现一致），会话态零 RequirementWorkingSet 成员（结构性防第二真值，RequirementsUiModule.hpp L69-77）。
2. **编辑→草稿→应用链路**——通过。接受分支：`submitEntryEdit`→`onEditApplied`＋`notifySessionDirty`＋draftStatus.dirty/edits 断言（测试 `SubmitEditAcceptRefreshesAndMarksDirty_ACC2`）；拒绝分支：工作集字节不变＋`DuplicateName` 就地原因＋零脏标记（测试 `SubmitEditRejectKeepsValueWithInPlaceReason_ACC2`——UX-03/05/07）。信封：`buildDraftCommand("requirements")` 五对象槽装配（根槽 allocateNew 按会话根身份/集合槽按根引用表挂载态；expectedRevision/commandType=kCmdApplyRequirementSet/payloadFormatVersion/payloadCanonical 逐字段断言；首应用根槽 allocateNew 翻转断言；域外 moduleId→nullopt）——测试 `BuildDraftCommandAssemblesApplyEnvelope_ACC2`。P-REQ-6：测试 `DraftCommandDoesNotLocallyGateOnBlocking_ACC2`——会话报告含 Blocking 时信封仍组装（prepare 现场重估），呈现面如实投 DataInsufficient/不伪造可行。字段编辑零计算逻辑：合法性裁决唯一在编辑器域校验链（PanelEditFlow.hpp 文件头声明与实现一致；ACC3 源面扫描测试在案）。
3. **插件零计算逻辑＋链接面恰三条**——通过。CMakeLists.txt L250 单行链接语句：`target_link_libraries(sdurws_ird_requirements_plugin PUBLIC sdurws_ird_requirements sdurws_ird_ui Qt6::Core Qt6::Gui Qt6::Widgets)`——本单元计算库＋ui＋Qt 三件套，无任何其他业务域单元；配置期守卫（同文件 L454-494）R-1/零 Qt/零 gtest 三重断言；测试 `PluginSourcesNoQtInModelsNoBusinessCrossIncludes_ACC3` 源面扫描（呈现模型零 Qt/零表外 include）；验收者独立 grep 全部 plugin/*.cpp|*.hpp 的 `#include`：Qt 头仅 RequirementsPanelWidget.{hpp,cpp}（Q_OBJECT widget 载体）＋测试文件 QKeySequence，跨单元仅 core/project/ui 公共头（include/sdurws/ird/ 路径），零跨单元私有头。ird_gates R-3 增量零命中（41→41）。
4. **两级撤销与辅助链路**——通过。L-R4：`LocalUndoTracker`（入栈-净深记账——draftStatus 差值计数推不出栈态的精确事实源）＋`TwoLevelUndoView` 两独立标签/两处独立控件；测试 `TwoLevelUndoSeparation_ACC4`（undo/redo 工作集回退、edits 计数差值语义、标签分离断言）。L-R10：`ImportWizardFlow` 五步（SourceSelected→Mapped→Previewed→Applied＋步骤违约 fail-fast）；测试 `ImportWizardFlowFiveStepsToDraft_ACC4`（映射自动识别/单位预览 m 缺省/逐行部分成功/错误行定位/O-36 全零待分配/行级入草稿）。L-R11：`exportCopyPrompt`——测试 `ExportCopyPromptSuccessAndFailureKeepsOldFile_ACC4`（成功提示携路径＋"不影响项目"；失败"原文件保持完好"＋旧文件**字节级**比对不变）。L-R12：`stationFieldsFor(writable)` 行门控＋命令面 readOnlyAllowed 恰 3 条真——测试 `ReadOnlyModeGate_ACC4`（Editable→ReadOnlyGrey 逐行、灰显不复活、浏览/预览/副本导出/捕获/拾取可用族计数=3）；草稿源：`RequirementsDraftSource` 真实继承 `ui::IModuleDraftSource`（P-REQ-4——ui/IDraftController.hpp L353-389 冻结面四方法逐一 override，磁盘核对存在），保存/恢复往返测试 `DraftSourceRoundtrip_ACC4`。
5. **九条域命令登记＋SA-16 零默认键＋V-22 边界**——通过。测试 `PanelRegistrationAndDomainCommandsMatchCard_ACC5` 逐行断言九条 id/readOnlyAllowed/ownerUnit/titleKey/scope/bindable/defaultShortcut 空（对照 units/requirements.md §9.8 命令表 L845-850 逐条一致：import-csv/json=false、export-copy/capture-tcp/pick-feature=true、mirror-stations/create-array/apply-template/regenerate-linked=false）；两套命名空间分离断言（点分 CommandId vs 无点 project token）。`HotkeyRegistrationConflictFreeViaBindingTable_ACC5`：真实 ui::CommandRegistry（白名单含 requirements）＋真实 GlobalShortcutRegistry 九键全 Ok/同键 Conflict/空键 NotBindable。V-22：`traceability/builds/wp14-t08/v22-gui-flow-registration.md` 如实登记 8 用例设计流程＋AGENTS Windows 规程（QT_QPA_PLATFORM=windows 逐个绝对路径），**明示"已设计、未执行"＋envUnavailable（锁屏无交互桌面），不记绿灯**——边界如实，无粉饰。
6. **双模式构建＋ctest 全绿＋留痕＋ird_gates**——通过。见 4.2（集成＋冒烟双树零错误）、4.3（ctest 2/2）、4.10（留痕落位）与下方门禁专段（恰 +2 sanctioned 命中）。

## 4.5 测试真实失败能力 —— 通过（3 处实质变异全部真变红，还原后复绿）

变异窗口＝worktreeA 源树＋集成构建树增量重建；每次变异→具名测试真实变红→`git restore` 还原；三处还原后全量重建复跑 161/161＋5/5 全绿，`git status --short` 零残留（送验对象未被改动——变异仅存在于验收者临时 worktree）。

| # | 变异位置与内容 | 被验证测试 | 结果 |
| --- | --- | --- | --- |
| M1 | RequirementsUiModule.cpp L137：`rootSlot.allocateNew = !m_session.rootObjectId.has_value()` → 恒 `false`（破坏首应用取号语义） | PluginPanel.BuildDraftCommandAssemblesApplyEnvelope_ACC2 | **FAILED**（1 FAILED TEST） |
| M2 | PanelEditFlow.cpp L82：`projectUndoLabel` 改与草稿级标签同文（破坏两级分离呈现） | PluginPanel.TwoLevelUndoSeparation_ACC4 | **FAILED**（1 FAILED TEST） |
| M3 | PanelStationModel.cpp L215-221：只读门控循环条件失效（`false &&`——writable=false 不降级灰显） | PluginPanel.ReadOnlyModeGate_ACC4 | **FAILED**（exit 1） |

验收者过程留痕（如实登记）：M3 首次编辑破坏花括号配对导致编译失败，管道 `tail` 掩蔽退出码致旧二进制通过——属验收者操作缺陷非测试恒真；修正变异语法重编后真实变红。教训与 T14 时序教训同录：变异验证必须确认"重建确实发生"再判读。

## 4.6 架构红线 —— 通过（八条逐查）

1. **L2 计算内核零 Qt**：requirements 计算库本任务零改动（既有零 Qt 不变）；新增呈现模型 9 文件（plugin/ 目录，不在门禁产品面扫描域 include/**＋src/**——CMakeLists L200-203 登记的文件域隔离口径）零 Qt include（验收者独立 grep 证实）；Qt 仅 Q_OBJECT widget（RequirementsPanelWidget）与测试。ird_gates R-3 base..head 41→41 零增量。
2. **业务域互链禁止（R-1）**：插件链接面恰 `requirements＋ui＋Qt`（CMake L250 单行钉住）；源码 include 零 modeling/kinematics/trajectory/dynamics/selection/optimization；门禁 R-1 增量恰 1 条＝插件→本单元计算库自边（卡 §3.2 明文插件链接面的引擎解析结果，与 modeling WP-13-T15 先例 modeling→modeling 同型，**sanctioned**——契约与单元卡 §14.6 v0.8③登记为准）。
3. **跨单元私有头禁止（R-2）**：全部跨单元 include 均为 `sdurws/ird/<unit>/` 公共头（core：Identity/DiagData/Units；project：CommandService；ui：ICommandRegistry/IDraftController/IWorkbenchShell/UiTypes/UiText/FormEditCommon）；私有面仅同目录 plugin/*.hpp（目标内 PRIVATE）。
4. **框架零源码修改（SA-02）**：diff 无框架文件、patches/ 零新增（4.1 已证）。
5. **R-4 名称前缀**：门禁 R-4 3→3 零增量（既有 project 单侧命中非本任务引入）；新增代码无 RobWork 名称拼接/剥离。
6. **权威唯一（PA-1）**：修订/写路径未在插件实现（草稿唯一写目标＝IRequirementEditor；应用经 buildDraftCommand→命令端口，取号归 prepare/project）；运动学/就绪判定归计算库 checker（插件只呈现——P-REQ-6 边界有测试钉住）；共享策略/单位文案归 core/Units＋ui/FormEditCommon（quantity specs 复用）；命令入口经 ui CommandRegistry（测试用真实 registry 注册）。
7. **不可变历史（PA-2）**：插件零直接修订写面（Capture/Import 域侧接口无 project 端口——T06 已验收的结构保证未破坏）；撤销产生新状态不入历史（undoLocal 为编辑器草稿级、项目级归 project UndoRedoService——呈现分离）。
8. **不恢复 old/**：diff 内 `old/` 引用扫描零命中。

## 4.7 注释规范 —— 通过

- 全部 22 个新增 .hpp/.cpp 逐一机器核查：`@file` 文件头**22/22 在场**（含设计依据/契约追溯/背景说明段落）。
- 公共契约注释全查（RequirementsUiModule.hpp、PanelEditFlow.hpp、PanelCommandCatalog.hpp、RequirementsPanelWidget.hpp、CMakeLists.txt）：类注含业务概念/生命周期/线程约束/所有权（非 owning 标注遍布）；函数注含 @brief/@param 方向/@return/@throws 及触发条件、线程约束行、确定性声明；错误语义二分（调用方错误 fail-fast vs 域诊断码）多处显式（如 buildDraftDocument 的 std::logic_error 装配违约、adoptRestoredDocument 的草稿数据缺陷不吞）。
- 实现文件抽查 ≥3（实查 7 个）：PanelTreeModel.cpp（UX-02 守卫理由/O-36 全零不定位/确定性文本化 locale 无关来源）、PanelStationModel.cpp（单位 m/rad 显式、显隐表即数据、格式化确定性 %.6f 裁尾）、PanelValidationModel.cpp（层名固定词表确定性来源、P-REQ-6 边界文案出处）、PanelRegionModel.cpp、PanelEditFlow.cpp、RequirementsUiModule.cpp（五对象槽语义逐段、PA-1 取号出处）、PanelCommandCatalog.cpp（卡表行序＝登记序、SA-16 结构保证理由）——均达标。
- §2.5 高危信息抽查：物理单位（m/rad 随行标注）、坐标系/线程约束（仅 UI 线程＋PanelUiThreadGuard）、所有权（非 owning 引用逐处标注）、错误语义（域码 vs 异常二分）、确定性来源（排序键＝工作集规范序 I-REQ-1 等）——在位。

## 4.8 文档同步 —— 通过（含 findings 相关性核对）

- 单元卡 units/requirements.md 随送验同步 v0.8（§14.6 新行：11+11 文件构成/P-REQ-4·6·8 处置/构建图 +2 命中登记与 sanctioned 依据/R-REQ-1 增量同步义务/V-22 仅登记边界）；卡头版本行 v0.7→v0.8、**日期行同步推进（F-374 所指缺陷实测已修复——「2026-09-26（v0.8）」，可随收尾消账）**。卡 §3.1/§3.2/§11 T08 行落位标记由 v0.8 行承载（卡文自声明，行文核对一致）。
- 构建图契约测试翻转（_plugin 必须存在）随附同步：翻转理由、单元卡登记出处、modeling T15 先例三要素齐备（BuildGraphContractTest.cpp 头注＋断言注释）——**同步合法性成立**（先例在案、登记在场）。
- 实现与任务卡零偏差声明核对：未发现语义偏差；P-REQ-8 同形暂持属已登记的 R-REQ-1 机制（非未登记偏差）。
- **findings.json 未关闭条目相关性核对（305 条 open，基准 max=F-374）**：①F-358/F-367/F-368/F-371/F-373 家族（契约 verify 数组缺测试目标构建行）——本契约 WP-14-T08.json 同患（verify 仅 3 行）→ 转登 **F-375**；②F-370/F-354/F-360 散文计数家族——本任务散文"14 用例"与实测 20 不符（详见 F-376）→ 转登 **F-376**；③F-363（requirements 魔数无测试钉住）为 T03 期发现、本任务无 Codec 改动，无消账义务；④**F-374 由本任务顺手消账**（卡头日期行已随 v0.8 推进——登记属实，建议治理收尾时置 fixed）；⑤F-359（requirements 测试目标 testkit SUB 直链）仍在门禁存量中（base 侧已在），非本任务新增。
- 任务契约状态翻转/resultNote/state.json 归治理收尾（PIPE §4.7），与 T02~T07 同口径——本任务 allowedFiles 未触及，合规。

## 4.9 commit 质量 —— 通过

- 42cce92a：`[WP-14-T08] 实现 requirements 插件界面（四面板与编辑→草稿→应用链路）`——动词开头；四段齐备（改动内容按文件模块列要点/改动原因逐条对应 acceptance 与 knownPitfalls/验证含双模式构建＋ctest 6 轮＋门禁比对＋脚本 PASS/影响面声明 contract_test 翻转与门禁 +2）；验证结论与验收者实测**全部吻合**（161/161、2/2、PASS×2、+2 sanctioned）。
- 678222d8：`[WP-14-T08] 同步 requirements 单元卡 v0.8 与任务验证留痕`——动词开头；四段齐备；"纯文档与留痕"与 diff 相符。
- 瑕疵（建议级，随 **F-376** 转登）：两提交正文与部分留痕散文称 PluginPanelTest "14 用例"，实测 20（实现提交版本即 20——起草期计数未更新；机器产物三方一致且为少报方向，无粉饰）。

## 4.10 留痕完整 —— 通过

`traceability/builds/wp14-t08/` 16 件全部入库且与验收者独立复现一致：两 gtest XML（**tests="161" failures="0"** / **tests="5" failures="0"**——PluginPanel suite tests="20"，与验收者全新构建树实跑逐值相同）、ird-test-report 两份（161 条/5 条用例级明细，outcome 全 passed）、集成构建日志两份、ctest.log、冒烟 configure/build 日志、ird_gates base/head 引擎原始输出两份＋归一化命中集两份＋比对结论文档（与验收者复算吻合，见专段）、v22-gui-flow-registration.md、verification-summary.md。gtest XML 原样留存无手工转抄痕迹（F-346/348 家族无再现——计数以 XML 原文 grep 取证）。任务状态翻转/resultNote 归治理收尾（T05/T06/T07 同口径）。

## 4.11 偷懒/缩水实现扫描 —— 通过

- ① diff 新增行（requirements/**）`TODO|FIXME|stub|placeholder|未实现|not implemented` 扫描**零命中**（验收者 grep 实证）；无"触发式延后"豁免需求——V-22 GUI 不启动为契约 acceptance 5 原文明文边界且 envUnavailable 如实登记，非缩水。
- ② 空实现检测：抽查全部公共函数（buildDraftCommand/readonlyProjections/submitEntryEdit/submitBatchEdit/twoLevelUndoView/requirementsDomainCommands/applyReadOnlyGate/stationFieldsFor/buildRequirementTree/ImportWizardFlow 全方法/exportCopyPrompt/DraftSource 四方法）均有实质实现与域接口调用，无"仅返回默认值"占位。
- ③ 逐条证据表：见 4.4（六条 acceptance 逐条注明具名测试/命令输出/文件行号——任一无证据即 fail 的情形未出现）。
- ④ 测试强度：断言校验行为与边界（字节级工作集不变、O-36 全零不定位、冲突检出拒绑、部分成功逐行定位、旧文件字节级完好、SA-16 空默认键）；非恒真——4.5 三处变异真实变红为结构性自证。

## ird_gates base..head 归一化比对（编排者口径，验收者独立复算）

引擎直跑 `cmake -DIRD_ROOT=<各自源根> -P cmake/ird_gates.cmake`（base＝17ff0996 临时 worktree、head＝worktreeA；引擎文件 base..head 零改动）——两侧 exit=1（DTB §4.5 存量例外既有状态）。归一化（绝对路径→`<IRD_ROOT>`＋[ird_gates] 行提取＋排序计数）后 diff 恰 **2 行新增**：

```
+ IRD-GATE-R1: 业务域目标互链：sdurws_ird_requirements_plugin → sdurws_ird_requirements（ARC-02；R-1 无例外）
+ IRD-GATE-SUB: 表外依赖边 requirements->ui（sdurws_ird_requirements_plugin → sdurws_ird_ui）不在 ARCH §3.5 白名单
```

汇总行「命中 60 项」→「命中 62 项」（+2 恰为上述两条）。**逐条核对 sanctioned**：两条均为插件目标链接面（CMake L250 单行）的引擎解析结果，链接面＝units/requirements.md §3.2 明文"插件目标→本单元计算库＋sdurws_ird_ui＋Qt"，单元卡 §14.6 v0.8③ 与送验比对附件同口径登记为治理面回填项（归 WP-01-T03）；与 modeling 插件既有两行（modeling→modeling/modeling→ui，base 侧在案）同型——**契约与治理登记为准，验收者独立裁决：sanctioned，非违规越界**。R-3 零增量（41→41，插件文件域隔离成立）。

## 发现问题清单

| 级别 | 编号 | 摘要 |
| --- | --- | --- |
| 阻断 | 无 | —— |
| 建议 | F-375 | 契约 verify 数组缺两测试目标构建行（F-300 家族新例）——验收者补建后执行，结论不受影响 |
| 建议 | F-376 | 散文计数"14 用例" vs 实测 20（F-370 家族新例；机器产物一致且为少报方向，无粉饰） |

## 裁决

**verdict＝pass**。4.1~4.11 全部通过、无阻断级问题；建议级 2 项（F-375/F-376）已转登 findings.json，不阻塞合入。合入决策归所有者（本验收者不合入、不改实施产物、未向任务分支 wp14-t08 推送任何提交；送验对象冻结于 678222d8ba235b5b9af0f7245948291b90e23f16，origin/wp14-t08 与之零漂移）。

## 验收者环境说明

- 主检出（redesign-main @ e448d47e）由另一并发会话持有（wp24 系未提交改动在场）——本验收全程未切换其分支、未执行任何主检出 git 写操作（fetch 仅更新远程跟踪引用）、未修改主检出任何文件。
- 残留进程检查：tasklist 核查 RobWorkStudio.exe 零命中后才开始集成构建（无进程锁树干扰）。
- 临时 worktree：A（head 复现）与 base（门禁比对，用毕即删）；本记录落 acc/WP-14-T08/1 分支 worktree B 并推送（三元组见编排者回执）。
