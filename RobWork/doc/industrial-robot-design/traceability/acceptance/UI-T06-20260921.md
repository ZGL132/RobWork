# 验收记录 —— UI-T06（CommandRegistry＋GlobalShortcutRegistry＋命令面板）· attempt 1

| 字段 | 值 |
| --- | --- |
| 任务 | UI-T06（contract：tasks/foundation/UI-T06.json，schema ird-task/1，status=ready；interUnit=true——消费 core IEventSubscription/diagnostics IDiagObserver 登记边＋O-31 C-4 端口/投影承载；requirements UX-13/PM-14；designRefs ui.md §13/§3.5/§7/§10.3/§10.4） |
| 送验对象 | **cdeb53e8c58445462fb2b8d788e5563a9dee9a26**（40 位冻结；分支 wp10-t06；`git fetch` 后 `git rev-parse origin/wp10-t06`＝该 SHA 逐字一致——无分支漂移，ACC §3 冻结判据通过） |
| diff 范围 | 8cba86ce9f2a97724c4780e9fc3c2a8f7c0f405b..cdeb53e8c58445462fb2b8d788e5563a9dee9a26（＝state.base..headSha；base 与 `origin/redesign-main` 逐字一致；range 内 2 提交：3830e164 实现＋测试、cdeb53e8 单元卡 v0.8＋验证留痕归档） |
| 验收尝试 | attempt 1（state.attempts.accept 序号 1；本验收为全新上下文，只消费产物、契约、设计文档与在库留痕，未读取实施者过程叙述；全部构建/测试/门禁/治理脚本/变异试验由验收者亲手执行） |
| 验收环境 | 复现 worktree A＝`git worktree add --detach acc-ui-t06/wt-a` @ cdeb53e8（6141 文件检出后 `git rev-parse HEAD`＝cdeb53e8 实证）；集成 build 树全新配置：`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=<主仓>vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64 -DCMAKE_CXX_FLAGS="-DNOMINMAX -DWIN32_LEAN_AND_MEAN -DWIN32"`——**CONFIGURE_EXIT=0**（41.6 s），`grep RWS_BUILD_INDUSTRIALROBOT build/CMakeCache.txt` → `:BOOL=ON` 实证；首配前按 F-099 既有登记自主树复制未追踪 `RobWorkStudio.ini.template.static`＋`RobWorkStudio.ini.shared.in` 两件——bin/ 被 .gitignore 吞没的框架 configure 硬依赖（F-098/F-099 环境链在本次验收再次亲历、非送验对象缺陷）；冒烟 build 树＝独立目录 acc-ui-t06/smoke（vcpkg toolchain＋Qt 前缀——AGENTS §4.1）；GUI 测试执行＝`QT_QPA_PLATFORM=windows`＋`QT_PLUGIN_PATH=<Qt>/plugins`（wp10-t03 gate-registrations §2 既有登记口径）；ird_gates 输出为 GBK 双重编码乱码（F-065/F-207 家族——IRD-GATE 码与路径可辨、命中总数以退出报告"39 处命中"为准，非送验对象缺陷）；记录回传 worktree B＝`git worktree add -b acc/UI-T06/1` @ origin/redesign-main |
| verdict | **pass**（4.1～4.8/4.11 通过＋4.9/4.10 按 F-081/F-264 家族既有建议级先例判定不阻塞：两提交的**验证结论全部经验收者亲手复现准确**——构建/测试/门禁/脚本无一虚报；两条新发现为建议级，已随本 evidence 分支转登 findings.json **F-266/F-267**，不阻塞合入。4.9/4.10 的字面缺口＝9 件 .log 留痕被根 .gitignore 吞未入库＋1 处标题名词短语，均按 POL-T06（F-081）/UI-T05（F-264）先例以建议级登记并留返工输入） |

## 4.1 改动范围 —— 通过

- `git diff --name-status base..head` 实证 **26 个变更文件**，逐个对照契约 allowedFiles 三模式：`ui/**` 18 件（CMakeLists.txt M、include/ 5 件〔ICommandRegistry.hpp A 549 行、IGlobalShortcutRegistry.hpp A 289 行、UiTypes.hpp A、IWorkbenchShell.hpp M、UiPorts.hpp M、UiProjections.hpp M〕、src/ 6 件〔CommandRegistry.cpp A、GlobalShortcutRegistry.cpp A、CommandPalette.cpp A、CommandPalette_p.hpp A、WorkbenchShell.cpp M、WorkbenchShell_p.hpp M、ShellSupport.cpp M〕、test/ 3 件〔CommandRegistryModelTest.cpp A、ShortcutRegistryModelTest.cpp A、BuildRedLineTest.cpp M〕、gui_test/ 1 件〔CommandPaletteGuiTest.cpp A〕）＋`units/ui.md`（M，v0.7→v0.8 增量修订）＋`traceability/**` 7 件（builds/wp10-t06/ 6 件 A＋wp10-t06-gate-registrations.md A）——**全部在 allowedFiles 内，零越界**。
- forbiddenFiles（REQUIREMENTS.md/ARCHITECTURE.md）零触碰（26 文件列表实证，两文件均不在 diff 内；本验收对照需求原文 UX-13/PM-14 以只读方式提取，未改动）；契约文件 tasks/foundation/UI-T06.json 不在 diff 内＝未被任务侧改动（status=ready 维持，合入收尾翻转——PIPE §4.7 既有口径）。
- 框架源码（RobWork/、RobWorkSim/、RobWorkStudio/ 其余源码）零改动：`git diff --name-only base..head -- RobWork/RobWork RobWork/RobWorkSim RobWork/RobWorkStudio/src` 排除 industrialrobot 后**零输出**；`patches/` 范围内零变更（head 树 patches/ 仅既有两件——SA-02 无登记需求）。

## 4.2 可复现构建 —— 通过（集成全新树重配＋全量构建＋独立冒烟；`: error` 计数双零）

| 步骤 | 结果 |
| --- | --- |
| 集成重配 | worktree A 全新配置 exit 0（补齐 vcpkg toolchain＋Qt 前缀＋CXX flags＋两件未追踪 ini 辅助件后通过——前三者为 F-098/F-099 既有登记口径，AGENTS §4.1 字面命令在全新树不可配，验收者亲历同款）；缓存 grep `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON` 实证 |
| 集成全量构建 | `cmake --build build --config Release` **exit 0（BUILD_EXIT=0）、`: error`/`: fatal error` 计数 0/0**（RobWork 全库→RobWorkSim→RobWorkStudio→industrialrobot 全单元全新编译）；产物 `build/RobWorkStudio/bin/Release/` 下 `sdurws_ird_ui_test.exe`（944128 B）＋`sdurws_ird_ui_contract_test.exe`（303616 B）＋`sdurws_ird_ui_gui_test.exe`（712192 B）全部在位 |
| 独立冒烟模式 | `cmake -S …industrialrobot -B acc-ui-t06/smoke -DCMAKE_TOOLCHAIN_FILE=<vcpkg> -DCMAKE_PREFIX_PATH=<Qt>` 配置 exit 0；`cmake --build --config Release` **exit 0、错误计数 0**；`smoke/ui/Release/sdurws_ird_ui_test.exe` 在位并亲跑（见 4.3） |

## 4.3 契约 verify 命令 —— 逐条亲手执行；全部成功

契约 verify 数组仅 1 条，在 worktree A 根目录按字面独立执行：

| 命令 | 结果 |
| --- | --- |
| `cmake --build build --config Release --target sdurws_ird_ui_test` | **exit 0**（VERIFY_EXIT=0、`: error` 计数 0） |

附加测试真实执行（验收者亲手，非采信送验留痕；日志存验收临时目录 acc-ui-t06/logs/）：

- 集成树三测试可执行直接运行：**模型层 30/30、契约 3/3、GUI 10/10 全部 PASSED**（gtest XML 亲跑产物 ird_ui_test.acc.xml/ird_contract_test.acc.xml/ird_gui_test.acc.xml＋console 留痕；计数与在库三份 gtest XML〔tests=30/3/7→10、failures=0〕及三份 ird-test-report.json〔total=30/3/10、passed 同值、failed/skipped=0、decisive=true〕逐值一致）。
- `ctest -C Release`（ui 构建子目录，带 GUI 环境变量）**exit 0、3/3 通过（ird×2＋ird_gui×1）**。
- 冒烟树 `sdurws_ird_ui_test.exe` 另跑 **30/30 PASSED**。
- 治理脚本亲跑：`validate-task.ps1 -TaskFile <契约绝对路径>` → **PASS (1 tasks)**；`validate-docs.ps1`（worktree A 树内）→ **PASS (20 units, 12 trace entries, 150 task files)**——与提交正文声称逐字一致。

## 4.4 acceptance 条目 —— 3/3 逐条证据（未执行项零存在；ird_gates 已随 WP-01-T01 落地故为"已执行"）

| # | 契约 acceptance 要点 | 证据（验收者亲手） |
| --- | --- | --- |
| 1 | UI-CMD-1/2、UI-HKY-1/2/3：重复命令注册/重复快捷键绑定在注册边界拒绝（DuplicateId/Conflict，不覆盖不静默）并出比较型诊断（UI-CMD-DUPLICATE/UI-HOTKEY-CONFLICT，冲突键＋已占用命令在 context、数值比较字段标「不适用」——SA-16/ARCH §11.2-1）；插件私占全局快捷键（WindowShortcut/ApplicationShortcut 作用域 QShortcut）静态扫描命中即违例（UI-HKY-3，NFR-MNT-07） | ①重复命令注册拒绝：`DuplicateAndInvalidRegistrationRejected_UI_CMD_1` 亲跑通过——重复 id（不同 owner）返回 `RegistrationResult::DuplicateId`（CommandRegistryModelTest.cpp:162-164）、注册表仍仅 1 条（:165 `query({}).size()==1` 不覆盖不追加）、UI-CMD-DUPLICATE 经 Dev 通道出线且 Dev 码不入目录（:166-167 devLog seen＋目录计数 0）；实现点 CommandRegistry.cpp:517-522（含 owner 白名单 :509-514、id 句法 :231-256 点分段＋段字符 [A-Za-z0-9-]＋必含点——词形措辞差已按 ui.md §16.7 v0.8 登记）；变异试验证实承重（4.5 变异 1）。②重复快捷键拒绝＋比较型诊断：`ConflictRejectedWithDiagnostic_UI_HKY_1` 亲跑通过——`registerDefault("test.beta","Ctrl+S")` 返回 `Conflict{existingCommand=test.alpha}`（ShortcutRegistryModelTest.cpp:174-178）、`lookup` 仍指向 alpha＋绑定表仅 1 条（:179-181 不覆盖）、UI-HOTKEY-CONFLICT 入目录恰 1 条（:184）、**比较型字段双 NotApplicable 断言**（:191-193 `comparison->actual/expected.quantity.state()==FieldState::NotApplicable`）与实现点 GlobalShortcutRegistry.cpp:380-382（`SourcedValue<double>::notApplicable()` 双侧——ERR-01 非数值判定不伪造数值）互证、**冲突键＋已占用命令在 context**（实现 :386-387 context 文案携带键 PortableText＋占用命令 id＋被拒命令；测试经诊断条目存在性＋计数佐证出线）；冲突流订阅/退订幂等（:195-199 probe.notified）。③UI-CMD-2 未知拒绝＋UI-HKY-1b 前置违约：`UnknownSubmitRejectedWithDiagnostic_UI_CMD_2`（未注册提交 accepted=false＋UI-CMD-UNKNOWN 入目录 1 条＋处理器零调用＋文案键 diag.ui-cmd-unknown.title，:211-226）与 `PreconditionsUnknownAndNotBindable_UI_HKY_1b`（UnknownCommand/NotBindable/空键四路拒绝、零表变更零目录条目，:202-225）亲跑通过。④UI-HKY-3 静态扫描：`NoGlobalShortcutPrivatization_SA16_UI_BUILD` 亲跑通过——注释剥离后全产品面扫描"QShortcut＋全局作用域"同现，唯一豁免面 `src/GlobalShortcutRegistry.cpp`（BuildRedLineTest.cpp diff :329-372）＋**正向控制**（豁免文件必须真实携带 QShortcut＋Qt::WindowShortcut 创建形态，防扫描条件被掏空）；实际创建点唯一（GlobalShortcutRegistry.cpp:352-353 `new QShortcut(binding.key, m_shortcutParent)`＋`setContext(Qt::WindowShortcut)`）。注：UI-CMD-DUPLICATE 为 Dev 级码（ui.md §3.5/§6.2——Dev 不入目录），context（id/owner）在 Dev 消息文本承载（实现 :518-520）；比较型「不适用」诊断属用户级 UI-HOTKEY-CONFLICT——与单元卡 §3.5 逐码语义一致，非偏差。 |
| 2 | 命令面板（§7.4/§10.3）：名称/关键字模糊搜索与键盘导航；未绑定命令经面板可达；rebind/解绑为用户级设置持久化（PM-14，§7.3）；实际 QShortcut 对象仅由 GlobalShortcutRegistry 创建（唯一注册点，N-11 无第二套） | ①模糊搜索唯一实现点在注册表（§7.4 模型层可断言）：`PaletteFuzzyMatchingRules_UX13` 亲跑通过——关键字命中加权置顶（"面板"→workbench.commandPalette，:431-433）、词边界前缀（"新"→project.new，:436-438）、子序列大小写不敏感（"pjnw"，:441-445）、未命中空集（:448）、空查询全量（:451-452）；实现点 CommandRegistry.cpp:172-220（fieldScore 权阶 60/50/30/10＋关键字 +20/标题 +10、命中内加权非假命中）＋:412-457（paletteSnapshot 可见性过滤＋stable_sort 稳定序＋近期置顶去重＋limit 截断）。②稳定序/上限/近期/回放：`PaletteStableOrderLimitAndRecents_UX13_PM14`（同输入同序、limit=2 截两行、submit 接受即置顶、未知不计、restoreRecentUsed 丢弃未注册＋去重收敛，:462-495）亲跑通过。③GUI 键盘流端到端：`PaletteOpensViaDefaultShortcut_UI_PALETTE_1`（Ctrl+Shift+P 物理键→isVisible=true→Esc 关闭）、`PaletteFilterNavigateExecute_UI_PALETTE_2`（未绑定命令 scheme.switch 文本行可达＋无项目写命令"禁用＋不可用"说明行＋过滤"恢复"→view.resetLayout 置顶＋Enter 经 registry.submit 派发——左栏复位观测面 +12）亲跑通过（QTest 键注入）；键盘导航实现 CommandPalette.cpp:260-312（keyPressEvent Esc/Enter/Tab＋eventFilter ↑↓/Enter/Esc 全键盘无鼠标）。④rebind/解绑持久化（PM-14）：`RebindUnbindPersistence_UI_HKY_2`（冲突拒绝原绑定保持、改绑 User 来源＋回调载荷、旧键释放双向唯一、解绑总允许且命令仍面板可达、同键幂等，:231-280）＋GUI `UserRebindPersistsAcrossSessions_UI_PALETTE_3`（设置注入 Ctrl+J 改绑→会话 A 改绑键生效/被覆盖默认键失效→shutdown 兜底落盘→会话 B 复验生效——持久化闭环，:246-330）亲跑通过；壳侧写路径经 UiSettingsWriter 后台落盘线程（WorkbenchShell_p.hpp kShortcutGroup/kShortcutKey :119-127＋loadUserShortcutBindings/persistShortcutsAsync）。⑤唯一注册点：见条目 1 ④扫描＋正向控制；五入口统一收口 registry.submit（壳集成 WorkbenchShell.cpp:555-663 亲读——登记 19 命令、seal 收口、attach 主窗口、面板处理器 open；ShellCommandBoard 登记职能移除——ShellSupport.cpp diff -35 行至 -121 行＋替代登记注）。 |
| 3 | O-31 处置（已裁决 2026-09-19——随整链放行）：§10.3 CommandOutcome.revisionResult 的 project::CommandResult（C-4）按 ui.md v0.4 裁决经 ui 自有命令网关端口＋命令结果值投影承载（L5 适配，§3.2 冻结基准），产品面零对端链接/include（NoCrossUnitInclude_O31_UI_BUILD 守卫常驻）；命令语义（唯一写路径经 project、ui 零业务判定）零变化；P-UI-10 处置维持：UI-CMD-*/UI-HOTKEY-CONFLICT 码值按 §3.5 建议值向 diagnostics StableCodeRegistry 注册，最终码值以 diagnostics 码表收编表现为准（不私定码值） | ①投影承载面：`CommandOutcome.revisionResult` 类型＝`std::optional<CommandResultProjection>`（ICommandRegistry.hpp:321，头内注释逐条标注 O-31 裁决出处与"投影≠重定义"）；`CommandEnvelopeProjection`/`CommandResultProjection` 落 UiProjections.hpp（diff 亲读——status 四值/newRevision Committed 唯一非空/reason token 不镜像枚举，逐字段锚定 project.md §5.3.1）；`IUiCommandGateway` 端口落 UiPorts.hpp（diff 亲读——注释明示注册表不调用本端口、L5 适配、ui 零业务判定）。②零对端 include：验收者独立 grep `sdurws/ird/` 于 ui 产品面（src＋include）——命中仅 **core/diagnostics/ui 三目录**，modeling/requirements/…/runtime 等 15 业务域**零命中**；`NoCrossUnitInclude_O31_UI_BUILD` 守卫用例随 sdurws_ird_ui_test 30/30 亲跑通过（build/ird_ui_test.acc.xml 含该用例——动态枚举产品面文件，新增 TU 自动入扫）；链接面 CMakeLists.txt 亲读——`sdurws_ird_ui` 链接 core＋diagnostics＋Qt6::Core/Gui/Widgets 三件（R-3 例外），**零对端链接**；Qt6::Test 仅 gui_test 测试目标（diff :316-332）。③投影语义实测：`SubmitSemanticsParamsExceptionProjection_UI_T06_ACC3` 亲跑通过——处理器回填 `CommandResultProjection{Committed, newRevision 非空}` 原样回传（:367-384）、参数不透明搬运（:342-355）、处理器异常捕获不穿透＋NOT-EXECUTABLE＋Dev 明细（:357-365）。④P-UI-10 码值收编实测：模型层两套件 DiagHarness 均以真实 diagnostics 全链承载——`registerBuiltinCodes`＋`uiDiagnosticCodeDescriptors()` 逐条 `StableCodeRegistry.registerCode`（CommandRegistryModelTest.cpp:91-99、ShortcutRegistryModelTest.cpp:100-106），九码描述符定义点 ShellSupport.cpp:247-275（UI-CMD-DUPLICATE/UNKNOWN/NOT-EXECUTABLE/UI-HOTKEY-CONFLICT 在列），注册表出线走 `IDiagnosticFactory::create`＋`sink.append` 唯一入口（CommandRegistry.cpp:609-625、GlobalShortcutRegistry.cpp:366-395）——码值权威归 diagnostics 码表，ui 未私定码值。 |

## 4.5 测试真实失败能力 —— 通过（两处实现变异注入应失败输入，均真实变红；试验后已还原并复绿）

在验收沙箱 worktree A（git 管控下，试验后 `git status --porcelain`/`git diff` 零输出＝与送验对象逐字节一致）注入两处变异：

| # | 变异 | 重建后亲跑结果 |
| --- | --- | --- |
| 1 | `CommandRegistry.cpp` registerInternal 重复 id 拒绝分支条件改 `if (false && …)`（UI-CMD-1 拒绝轨被拆除） | `--gtest_filter=DuplicateAndInvalidRegistrationRejected_UI_CMD_1` → **FAILED**（恰该用例红，失败点即承重断言：:164 DuplicateId 相等、:165 表仅 1 条、:186 非法描述符不入表；MUT1_EXIT=1） |
| 2 | `GlobalShortcutRegistry.cpp` bindInternal 同键占用拒绝分支条件改 `if (false && …)`（UI-HKY-1 冲突拒绝被拆除） | `--gtest_filter=ConflictRejectedWithDiagnostic_UI_HKY_1` → **FAILED**（恰该用例红，失败点：:176 Conflict kind 相等、:177 existingCommand.has_value；MUT2_EXIT=1） |

两处变异还原后重建（exit 0）复跑 **30/10/3 全绿**（REBUILD_EXIT=0）。两组承重断言均为注册边界拒绝语义本身——非恒真、非"只编译通过"。

## 4.6 架构红线（AGENTS §5 八条）—— 逐条通过

1. **L2 计算内核零 Qt**：本任务仅改 ui 单元（R-3 全产品唯一 Qt Widgets 例外单元，DTB §4.5 预登记）；core/evidence/policy/runtime 等零触碰（26 文件清单实证）；ird_gates 亲跑（集成树 `--target ird_gates`，exit 1＝设计内）自报"39 处命中"，与在库 wp10-t05 基线（gates-hit-set.txt，33 处）逐码比对**恰增 6 处**——全部为本任务新增产品面文件（两公共头 ICommandRegistry/IGlobalShortcutRegistry＋四实现 TU CommandRegistry/GlobalShortcutRegistry/CommandPalette/CommandPalette_p）的 R3 头包含命中（F-259 同型机器面滞后，与提交件 wp10-t06-gate-registrations.md 六行表逐行一致）；其余 33 处（SUB 测试目标族/LIB io libzip/expat/R3 链接 3＋既有 4 文件/R4 project 1/R1/R5/T1/T2）零变化；UiTypes.hpp 纯标准库零命中。
2. **业务域互链禁止（R-1）**：ui 产品面 include 扫描（src＋include 全量 grep）——`sdurws/ird/` 命中仅 core/diagnostics/ui 三目录，15 业务域**零命中**；本任务新增边＝ui 自有 C-4 端口/投影（O-31 承载，非对端链接）；`ArchEdgesCoreDiagnosticsOnly_UI_CTR`＋`TestkitOnlyLinkedByTestTargets_UI_CTR` 契约用例亲跑通过。
3. **跨单元私有头禁止（R-2）**：零跨单元 include（同上 grep）；ui 内部新增 `CommandPalette_p.hpp` 置 src/（不进 include/——亲读），公共面仅三头落位 include/sdurws/ird/ui/。
4. **框架零源码修改（SA-02）**：diff 内零框架文件、patches/ 零变更（4.1 实证）。
5. **不做名称前缀拼接/剥离（R-4）**：ui diff 新增行 `RobWork` 字面量 **0 命中**（grep 计数实证）＋ird_gates 复跑 R4 项仅 project 既有 1 处（与基线同值）。
6. **权威唯一（PA-1）**：命令登记/呈现/路由权威归 ui 注册表（SA-16 唯一入口——五入口统一 submit，壳命令板登记职能按既有登记移除）；命令**执行**权威归 project（IUiCommandGateway 注释明示"唯一写路径经 project、ui 零业务判定"；注册表只派发处理器不触碰领域对象——submit 实现亲读）；快捷键表权威单点（m_byKey 双向唯一映射）；诊断码值权威归 diagnostics（P-UI-10 实测形态）。
7. **不可变历史（PA-2）**：本任务零修订数据触碰（26 文件清单——无 project/ 修订面文件）；UI 对命令结果只持投影快照不回写。
8. **不恢复 old/**：diff 新增行 `old/` 引用 **0 命中**（计数实证）；实现全部从零编写。

## 4.7 注释规范（AGENTS §2）—— 通过（新增 3 公共头全查＋3 实现 TU＋面板私有头＋2 模型测试＋1 GUI 测试＋BuildRedLineTest 改动全读，覆盖"公共头全查＋实现抽查 ≥3"）

- **文件头**：新增/大改 10 件均有中文 `@file/@brief`＋设计依据（需求 UX-13/PM-14/NFR-MNT-07、ui.md §3.5/§7.2~§7.6/§10.3/§10.4 逐节标注、O-31 裁决与任务契约 acceptance 1~3 逐条对应）＋背景说明（"为什么命令注册表在 ui 而命令执行在 project""为什么需要唯一注册点红线——N 张键表则冲突检测/改绑/呈现全部失真""为什么是别名而不是强类型"等"为什么"层注释齐备）。
- **类/函数 Doxygen**：公共头逐结构体/逐函数 `@brief/@param [in]/@return/@throws/@note` 语义完整（CommandDescriptor 十二字段逐个注释、RegistrationResult/HotkeyResult 词表逐枚举、谓词签名纪律"只消费快照零 IO——NFR-PERF-01 <1 ms"）；实现文件类/私有函数均有职责注释。
- **§2.5 高危信息表**：线程约束（两注册表"全部方法 UI 线程、非线程安全——§3.4 M-1"、UiSettingsWriter 后台落盘线程禁访 Widget、冲突流"UI 线程同步派发"逐处注明）；所有权（工厂 unique_ptr 所有权即刻移交、deps 共享指针所有权在外、QShortcut Qt 父子树管理、观察者"生命周期须短于本注册表"明示）；错误语义（RegistrationResult/HotkeyResult 值轨＋诊断观测面分离、Dev 码不入目录 §6.2、factory/sink 可空＝显式声明场景、create 抛错不吞 fail-fast、处理器异常捕获不穿透）；确定性来源（模糊匹配稳定排序锚 registrationOrder、bindings 排序 Default 装配序/User 改绑序、restoreRecentUsed 去重保序、键 PortableText 规范形跨平台稳定）。
- **魔法数字**：kMaxRecent 20＝§7.4"最近 20 条"、id 上限 128＝防御上限注释、面板 520×360 与顶部 60 偏移＝§4.1/§4.4 视觉锚、margins 8＝单列紧凑布局——逐个注明来源。
- **物理单位/坐标系/角度制式**：本任务为纯界面命令设施，无物理量（快捷键序列以 PortableText 规范形承载并注明跨布局稳定语义——如实记录非缺失）。
- **反例扫描**：零复述式注释、零无编号 TODO、注释与代码一致（AUTOMOC 关闭理由随源集 5→8 文件同步更新；ShellSupport.cpp 头注随命令板移除同步改写——无过期注释残留）。

## 4.8 文档同步 —— 通过（零偏差＋增量修订已登记＋findings 相关性逐条核对）

- **单元卡增量修订**：`units/ui.md` v0.7→v0.8（文档头版本行＋§3.1 测试目标表 gui_test 链接列增 Qt6::Test＋§3.3 公共头三行落位注＋§12.3 UI-PALETTE-1/2/3 用例行＋§16.7 v0.8 行——diff 实证纯增量落点、既有语义内容零改动）；v0.8 行登记的承载形状（三公共头、C-4 端口/投影、seal/presentContext/registered/bindable 增量、CommandHandler C++17 签名适配〔std::span→const vector&——D-01 钉死 C++17，契约语义零变化〕、19 命令最小集与默认绑定表、命令 id 词形措辞差登记、壳集成取代命令板、面板形态主窗口浮动子面板、测试 30＋3＋10、门禁 33→39）与实现逐一亲读比对一致。实现与任务卡零未登记偏差（acceptance 逐条对应见 4.4）。
- **追踪同步**：validate-task PASS＋validate-docs PASS 亲跑同值（4.3）；§12.3 三行（前置/操作/预期/观测点）与交付 GUI 测试形态一致；契约 status=ready 维持（合入收尾翻转——PIPE §4.7 口径）。
- **findings.json 未关闭条目相关性核对（230 条 open 逐类筛查，与本任务相关者如下；顺手消账项为零）**：
  - **F-227/F-259**（ird_gates 机器面滞后：R3 例外链接/头包含白名单未回填，owner=WP-01-T03）：本任务新增 6 处 R3 命中即 F-259 同型（新增产品面文件的 Qt 头包含——R-3 例外本体），已按 wp10-t02/t03/t05 先例以登记提交件 wp10-t06-gate-registrations.md 顺延登记（责任方 WP-01-T03，不重复开立请求条款）——维持 open 正确（白名单文件不在本任务 allowedFiles，本任务无回填义务）。
  - **F-261/F-263/F-264**（提交标题名词短语家族第 11/12/13 例）：本任务 cdeb53e8 标题同型再现（**第 14 例**，见 4.9）——家族性复发，随本验收转登 **F-267** 顺延跟踪。
  - **F-010/F-077/F-081**（.gitignore 吞 .log／留痕归档虚报家族）：本任务 **F-081 同型复踩**（声称归档 15 件、实际入库 6 件、9 件 .log 被根 .gitignore:87 规则静默跳过且未按 UI-T05 先例 `git add -f`；另 gate-registrations.md 首节"本目录 gates-branch-run.log"为在库悬空引用）——随本验收转登 **F-266** 顺延跟踪（按 F-081 POL-T06 先例判定建议级：证据本体在库且数值主张全部复现准确）。
  - **F-260**（UI-T03 行为面缺口：折叠/门面违约轨）：属 UI-T03 交付测试集；本任务零触碰 resizeEvent 折叠面，命令门面（commandAvailability→ShellCommandAvailability 映射）按提交正文如实声明保持形态——与本任务 diff 无交集，维持 open 正确。
  - **F-265**（WorkbenchShell_p.hpp 行尾半角括号笔误，owner=UI 后续任务顺手还原）：本任务大改同文件但该行（现 :379）未还原（cat -A 字节级复核仍为 `)`）——维持 open 正确（非本任务义务，无消账）。
  - **F-201**（§4.10 条文载体与流水线实践脱节）：循其登记口径以 `traceability/builds/wp10-t06/` 在库 6 件为 4.10 判定载体（见 4.10——载体件数缺口即 F-266）。
  - **F-065/F-207**（gates 留痕 GBK 双重编码乱码）：本验收亲跑 ird_gates 输出同型乱码（IRD-GATE 码与路径可辨、命中总数可辨）——既有登记持续适用，环境/工具链面非本任务新增缺陷。
  - **F-098/F-099**（全新树配置口径缺口）：本验收再次亲历（ini 两件＋toolchain/Qt/CXXFLAGS）——与本任务零代码关联，维持 open 正确（本记录环境段再留实证）。
  - 其余 open 条目与本任务 26 文件面无关联（逐类比对零交集）；F-081 悬空引用家族本任务新增 1 处（已并入 F-266）。

## 4.9 commit 质量 —— 按家族先例通过（2 提交四段式齐备、验证结论全数复现准确；1 处标题名词短语＝F-264 家族第 14 例转登 F-267；1 处归档清单与在库树不一致＝F-081 家族复踩转登 F-266）

- **3830e164** `[WP-10-T06] 实现 CommandRegistry＋GlobalShortcutRegistry＋命令面板`：动词开头合规；四段（What/Why/验证/影响面）齐备（含 Acc-Covered: 1,2,3）；正文验证声称（双模式零错误、30/30＋3/3＋10/10、ird_gates 39＝33＋恰增 6 处 R3、validate-task PASS）与本验收亲跑**逐值一致**；影响面段如实声明 ShellCommandBoard 取代与门面映射保持。
- **cdeb53e8** `[WP-10-T06] ui 单元卡 v0.8 增量修订与验证留痕归档（文档同步＋证据）`：①**前缀后以名词短语『ui 单元卡』开头——不合 ACC §4.9/AGENTS §6.3『动词开头』字面**，F-012/F-232~F-242/F-247/F-258/F-261/F-263/F-264 家族**第 14 例**（纯风格瑕疵）——转登 **F-267**（建议级，随 F-247 家族统一跟踪）。②正文"验证（How）"声称的留痕归档清单（15 件）与在库树（6 件）不一致——9 件 .log 被根 .gitignore:87 规则静默跳过未强制入册（F-010/F-081 家族复踩，**数值主张本身无一虚报**：全部经本验收亲手复现准确）——转登 **F-266**（建议级，按 F-081/POL-T06 先例"证据本体完整且全量复现"口径不阻塞；返工输入＝git add -f 回填 9 件或循其裁决①/③）。两提交正文四段齐备、如实区分已执行/未执行（ird_gates 与测试计数均与在库 XML/报告逐值相符）。

## 4.10 留痕完整 —— 按 F-201/F-081 先例通过（证据本体 6 件在库且全数复现；9 件日志在盘未入库＝F-266，回填责任已登记）

- **在库载体**：`traceability/builds/wp10-t06/` **6 件**（双模式 gtest XML×3：tests=30/3/10、failures=0 亲读；ird-test-report.json×3：decisive=true、passed=30/3/10、failed/skipped=0 亲读）＋`traceability/wp10-t06-gate-registrations.md`（触发事实/六行命中表/零实质违反声明/引擎自检记录齐备；"39＝基线 33＋恰增 6 处 R3"结论与本验收复跑逐码一致）。在库 XML 计数与提交正文、本验收亲跑三方一致。
- **缺口（F-266 登记处置）**：提交正文声称归档的 9 件日志（smoke-configure/smoke-build/integrated-build-ui_test/ctest.log/三测试 console.log/gates-branch-run.log/validate-task.log）实际**未入库**——主工作树该目录 `git status --ignored --porcelain` 实证 9 件在盘未跟踪（根 .gitignore:87 `*.log` 吞没、未按 UI-T05 先例 `git add -f`）；按 F-081/POL-T06 先例（F-077 POL-T04 口径）以本验收亲跑证据（4.2/4.3/4.6 全套：集成/冒烟构建日志、三测试 console＋XML、ctest、ird_gates 39 命中复跑、validate-task/validate-docs——存验收临时目录并摘录于本记录）替代裁判依据；回填或根因修复循 F-266 resolution 三选一裁决。gate-registrations.md 对"本目录 gates-branch-run.log"的在库悬空引用同入 F-266。
- phase-one-validation.log 尾行停早期任务、state.json 无 resultNote 字段——循 **F-201** 既有登记口径，以 `traceability/builds/wp10-t06/`＋登记提交件为判定载体。
- 验收者自跑留痕（集成/冒烟配置与构建日志、三测试 gtest XML＋console、ctest、门禁复跑、变异试验构建/运行日志）存于 worktree A 外验收临时目录（acc-ui-t06/logs/），关键结论已逐条摘录于本记录；临时目录随 worktree 生命周期消亡，不影响本记录为证据载体。

## 4.11 偷懒/缩水实现扫描 —— 通过（扫描零实质命中；豁免逐条列据；逐条证据表见 4.4）

- **①标记扫描**：diff 新增行 `TODO|FIXME|XXX|HACK|stub|未实现|not implemented` **零命中**（grep exit 实证）；`placeholder`（大小写不敏感）2 处命中均为非交付物本体：`src/View3DPlaceholder.cpp`（UI-T05 既有交付物文件名——CMakeLists 源集行）、`m_filter->setPlaceholderText(...)`（Qt API 名——面板过滤输入框提示文案，真实实现）。`占位说明处理器`（WorkbenchShell.cpp 17 项归属命令）＝**契约显式形态**（ui.md §11.4/§14.1 阶段 A 登记：触发有状态栏反馈＋messageKey＋进近期使用，非空实现、不虚构业务能力，接线随归属任务）——豁免依据＝单元卡 §11.4/§16.7 v0.8 原文＋实现亲读（非空处理器体），逐条列出如上。
- **②空实现检测**：全部公共接口函数有真实实现（注册协议/谓词求值/模糊投影/近期管理/seal/attach/detach/restoreUserBindings/订阅句柄 RAII/面板 open/repopulate/键盘导航——三实现 TU 全文亲读）；零"仅返回默认值"的空壳公共接口；HotkeyResult 默认构造即 Conflict（"未显式构造即拒绝"安全缺省）为设计面而非空实现。
- **③逐条证据表**：契约 acceptance 3 条逐条证据见 4.4 表——每条均有具名测试、命令输出或文件行号，零"已实现"空声明。
- **④测试强度**：断言校验行为与边界（重复拒绝三重断言：返回值/表不变/诊断出线且 Dev 不入目录；冲突拒绝五重：kind/existingCommand/lookup/表条数/诊断计数＋NotApplicable 双断言；模糊规则四权阶逐档＋未命中空集＋稳定序双跑比对＋上限截断＋近期置顶/回放收敛；GUI 物理键触发序列＋改绑跨会话往返含被覆盖默认键失效反断言）；**非恒真、非"只编译通过"**——与 4.5 联动抽改实现两处（重复拒绝拆除/冲突拒绝拆除）均真实变红且失败点恰为承重断言，其余用例不受扰。

## 发现的问题清单（建议级 2 条，无阻断级）

| 编号 | 级别 | 摘要 | 处置 |
| --- | --- | --- | --- |
| F-266 | suggestion | F-010/F-077/F-081 家族复踩：提交 cdeb53e8 正文声称归档 15 件留痕、实际入库 6 件（9 件 .log 被根 .gitignore:87 吞、未按 UI-T05 先例 git add -f）；gate-registrations.md 首节在库悬空引用 gates-branch-run.log（F-081 同型）。数值主张全部经验收者亲手复现准确（构建零错误/30＋3＋10/门禁 39＝33＋6/validate PASS）——证据本体完整，方向为"声称在库而实际未入库" | 已随本 evidence 分支转登 findings.json（回填 9 件或按 F-081 裁决①/③闭环；按 POL-T06 先例不影响验收） |
| F-267 | suggestion | F-264 家族第 14 例：提交 cdeb53e8 标题前缀后以名词短语『ui 单元卡』开头，不合『动词开头』字面；正文四段齐备、验证结论经独立复跑证实，纯风格瑕疵 | 已随本 evidence 分支转登 findings.json（随 F-247 家族统一跟踪，不单独返工） |

## 裁决

**verdict＝pass**：4.1～4.8 与 4.11 全部通过；4.9/4.10 存在的两处缺口（标题名词短语第 14 例、9 件日志未入库＋1 处悬空引用）均为**既有家族性建议级问题**（F-264/F-081 先例明确："不影响验收——证据本体完整且全量复现"），两提交的全部验证结论经本验收亲手复现无一虚报，无阻断级问题。F-266/F-267 已按 ACC §5 强制转登本 evidence 分支 `traceability/findings.json`，不阻塞合入。验收通过对象＝**cdeb53e8c58445462fb2b8d788e5563a9dee9a26**（非分支引用）；后续收尾按 PIPE §4.7 由所有者执行（建议随收尾一并裁决 F-266 的日志回填路径）。
