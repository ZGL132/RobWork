# IO-T01 验收记录（acceptance record）

| 字段 | 值 |
| --- | --- |
| 任务 ID | IO-T01（构建落位：io 占位转真实库；unit=io；masterWp=WP-E；≙DTB §2.12 WP-11-T02；outputs=implementation＋traceability-update，本任务无测试目标——acceptance 3 显式登记，测试整备随 IO-T06/T07） |
| 送验对象（冻结） | d175a4e4b8b1de79dbb12de6a3a1cddcd2742625（分支 wp11-t01；diff 范围＝2cad0cc59b844db8f13ea402ab243418a7083875..d175a4e4，3 个提交）。基线说明：送验头处 state.json 已被前一任务收尾清理（base=null），diff 基线取 `git merge-base origin/redesign-main d175a4e4`＝2cad0cc5（PRJ-T16 合入收尾提交），即任务分支分叉点＝state.base 等价物 |
| 验收尝试 | attempt 1（分支漂移检测：`git fetch origin wp11-t01` 后 origin/wp11-t01 == d175a4e4 全 40 位逐位一致，零漂移；acc/IO-T01/1 分支名此前不存在——ls-remote 实测空） |
| 验收环境 | 全新 detached worktree（`git worktree add --detach` @ d175a4e4）从零复现：集成 build 树与独立冒烟 build 树均为本验收新建独立目录；记录回传 worktree（acc/IO-T01/1 基于 origin/redesign-main@2cad0cc5）；pwsh 7＋MSVC 2022 x64（MSBuild 17.12.6）＋vcpkg 经典模式（主仓库 `RobWork/vcpkg`——git 未追踪，worktree 天然不含，toolchain 指主仓路径）。环境前置按 F-007/F-098 既有登记处置（非实施缺陷）：①toolchain 指主仓 vcpkg；②`CMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`＋`CMAKE_CXX_FLAGS=-DNOMINMAX -DWIN32_LEAN_AND_MEAN -DWIN32`；③`RobWorkStudio.ini.template.static` 被 [Bb]in/ gitignore 吞（worktree 实测缺文件）——自主树补拷后配置零错误。验收全程只消费产物与文档（契约/单元卡/留痕/先行验收记录），未读取实施者过程叙述 |
| verdict | **pass**（4.1～4.11 全部通过；无阻断级问题；建议级 2 条转登 findings.json F-179、F-180） |
| 验收日期 | 2026-09-17 |

## 4.1 改动范围 —— 通过

- `git diff --name-status 2cad0cc5..d175a4e4` 实证 **17 个变更文件**：
  - 产品代码 5 件：`industrialrobot/CMakeLists.txt`（M，io 移出 INTERFACE 占位循环＋`add_subdirectory(io)` 注册＋状态注释同步）＋ `io/CMakeLists.txt`、`io/include/sdurws/ird/io/IoFwd.hpp`、`io/include/sdurws/ird/io/IoError.hpp`、`io/src/Io.cpp`（均 A）；
  - 文档 1 件：`units/io.md`（M，+5/−4——§1.1 构建落位行、§1.2 io 构建骨架行、§15.5 v0.3 变更记录、卡头版本行）；
  - traceability 11 件：`builds/wp11-t01/` 9 份日志（A）＋`io-pio3-dependency-probe.md`（A，108 行）＋`contract-compile-log.md`（M，+1 行）。
- **逐文件比对契约 allowedFiles 四条通配**（`industrialrobot/io/**`、`industrialrobot/CMakeLists.txt`、`units/io.md`、`traceability/**`）：**全部在册，零越界**。
- **逐提交核验**：881f67ff＝产品代码 5 件、8d45c17c＝traceability 11 件、d175a4e4＝units/io.md——三提交各自也零越界。
- forbiddenFiles（REQUIREMENTS.md／ARCHITECTURE.md）零触碰（diff 无该两文件）。
- 无未登记框架源码改动：diff 零 RobWork/RobWork/、RobWorkSim/、RWS 其他源码路径；patches/ 零新文件（SA-02）；ird_gates 实跑 SA-02 工作区核对零命中（worktree 干净）。

## 4.2 可复现构建 —— 通过（全新树从零双模式）

- **集成模式**：worktree A 全新从零配置 **exit 0**（`-- Configuring done (29.9s)`＋`-- Generating done`，缓存实测 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`），`cmake --build build --config Release --target sdurws_ird_io`（＝契约 verify 命令，见 4.3）**exit 0**、`: error` 计数 0（日志唯一 "error" 字样＝diagnostics 源文件名 `Errors.cpp`）、依赖链 sdurw_math→sdurws_ird_core→sdurws_ird_diagnostics→**sdurws_ird_io.lib 全新编译生成**。
- **独立冒烟模式**：独立目录从零配置（带 vcpkg toolchain——AGENTS §4.1 口径）＋**全量构建**（全部已落位单元产品库＋测试目标）**exit 0、`: error` 计数 0**，`sdurws_ird_io.lib` 生成；全日志目标名扫描仅 `sdurws_ird_io`，**无 sdurws_ird_io_test/_contract_test 目标**（acceptance 3 不预建口径的构建面实证）。
- 全框架全量集成构建未单独重跑——F-008 基线框架缺陷判例（PRJ-T13/T14/T15/T16 验收四次独立实证），本任务 diff 零框架触碰（4.1），不归本任务、不构成阻断；父 CMakeLists 变更的配置面影响已由全新配置成功（Configuring done 零错误、无别名/目标冲突）覆盖。

## 4.3 契约 verify 命令 —— 通过

契约 verify 数组仅一条，验收者亲手在 worktree A 执行：

1. `cmake --build build --config Release --target sdurws_ird_io`（worktree A 自身 build 树）→ **exit 0**，sdurws_ird_io.lib 生成。

附加（§3 校验器面）：`validate-docs.ps1` → **PASS (20 units, 12 trace entries, 150 task files)，exit 0**；`validate-task.ps1 -TaskFile …IO-T01.json` → **PASS (1 tasks)，exit 0**——与实施留痕 builds/wp11-t01/validate-docs.log、validate-task.log 计数逐字一致。

## 4.4 acceptance 条目 —— 通过（四条逐项证据）

| # | 契约条目 | 证据（验收者亲手复现＋磁盘实测） |
| --- | --- | --- |
| 1 | 双模式构建零错误：sdurws_ird_io 由 INTERFACE 占位升级 STATIC（C++17、PUBLIC 链 RWS::ird::core＋RWS::ird::diagnostics——io.md §3.3），目标名与别名 RWS::ird::io 不变；L1 文件/XML 第三方目标待 P-IO-3 冻结后登记（本任务不引入）；新建 io/CMakeLists.txt 并在父 CMakeLists 注册；IoFwd/IoError 公共头就位，不在骨架预建占位实现 | **双模式构建 exit 0**（4.2 实测，全新树）。**io/CMakeLists.txt 逐行核对**：`add_library(sdurws_ird_io STATIC src/Io.cpp)`（目标名不变）；`target_compile_features(PUBLIC cxx_std_17)`＋`CXX_STANDARD 17/CXX_STANDARD_REQUIRED/EXTENSIONS OFF`；`target_link_libraries PUBLIC sdurws_ird_core sdurws_ird_diagnostics`（恰两条，§3.3 登记边）；**零 L1/第三方链接登记**（文件内显式声明＋全文无 find_package(libzip/expat/pugixml)）；别名 `RWS::ird::io` 仅在非独立模式定义（`if(NOT CMAKE_CURRENT_SOURCE_DIR STREQUAL CMAKE_SOURCE_DIR)`——集成模式成立；全新集成配置成功＝无重复定义）。**父 CMakeLists**：io 移出 IRD_MODULES 占位循环、`add_subdirectory(io)` 位于 core/diagnostics/project 之后（链接次序约束满足）。**IoFwd.hpp/IoError.hpp 在库**（include/sdurws/ird/io/），Io.cpp 为纯注释锚点翻译单元、零接口实现（§4.11②同步核证） |
| 2 | 红线扫描零命中：仅 io→core/io→diagnostics 两条登记边；零 Qt/零 Widgets、无私有头出 include、无业务单元表外边、产品目标不链 testkit | **ird_gates 验收者实跑**（worktree A 集成树 `--target ird_gates`，rc=1＝SA-10 命中即非零设计）：去重后真实命中**恰 11 项**＝IRD-GATE-SUB×9（diagnostics_contract/evidence×2/policy×2/project×2/runtime 八个测试目标直链 testkit＋testdata_lint→testkit 表外边）＋IRD-GATE-T1×1（testdata_lint）＋IRD-GATE-R4×1（project/src/ProjectStoreImpl.cpp——wp04-t08 既有，本任务 diff 未触碰）——**与 PRJ-T16/wp04-t15 验收基线 11 项同构，零新增；命中行零 `sdurws_ird_io`/`ird/io` 字样**（grep 实测 0）。白名单 `ird_gates_whitelist.cmake:36-37` "io->core"/"io->diagnostics" 两行在册；dependency-graph.json:17-18 同两条边（治理预登记，IRD-GATE-GRAPH 零命中）。**零 Qt/Widgets**：diff 新增 `#include` 共 8 处全部为标准库/本单元头（cstdint/string/utility/vector/functional＋IoError.hpp），无任何 Qt 头；引擎自测 9 项（pass_clean/pass_r4comment＋fail_r1/t1/r5/sub/r3/r4/t2）全部按预期。**无私有头出 include**：io/include/ 仅 IoFwd/IoError 两个公共头，唯一跨文件 include 为同单元 IoFwd→IoError |
| 3 | 不预建空测试目标：sdurws_ird_io_test/_contract_test 随 IO-T06/T07 落地，本任务不注册；verify 由占位的『构建 sdurws_ird_io_test』修正为构建产品目标 sdurws_ird_io（偏差登记 traceability/contract-compile-log.md） | **两测试目标零处出现**：io/CMakeLists.txt 无 add_subdirectory(test)/无 gtest 接入（文件内注释块显式登记不预建依据）；冒烟全量构建日志零 `_io_test`/`_io_contract_test` 字样；io/test/、io/contract_test/ 目录不存在。**verify 偏差登记在案**：contract-compile-log.md 第 174 行（2026-09-17 IO-T01 行）——占位 verify 指向 IO-T06 才登记的测试目标不可执行、修正为产品目标构建、实施段按修正口径执行，与本验收 4.3 实测一致；契约本体 verify 字段已是修正后命令（`--target sdurws_ird_io`），二元可判。**未虚标测试证据**：全部留痕与提交正文如实声明"本任务无测试目标"，无任何 ird-test-report//gtest XML 伪造 |
| 4 | P-IO-3 处置（R-IO-1）：完成 ZIP/XML 候选库 vcpkg 可用性验证并留痕；选型冻结归 WP-11 评审，不私裁引入任何依赖 | **验收者逐条复演探针命令**（主仓 vcpkg，全部命中）：①端口在册＝libzip 1.11.4／miniz 3.1.2／expat 2.8.3／pugixml 1.16（`vcpkg search` 精确行实测）；②本机安装＝libzip:x64-windows 1.11.4（bzip2＋default-aes 特性）、zlib 1.3.2#2、bzip2 1.0.8#6、xerces-c 3.3.0#1 在装；miniz/expat/pugixml **未装**；③downloads 缓存＝仅 `nih-at-libzip-v1.11.4.tar.gz` 有（miniz/expat/pugixml 无——"首装需网络"的离线风险输入与留痕结论一致）；④CMake 接入形态＝`share/libzip/` config 齐备，`libzip-targets.cmake` 实测 `add_library(libzip::zip SHARED IMPORTED)`；⑤RobWork 自带 XML＝`rw/core/DOMParser.hpp`＋`rw/core/BoostXMLParser.hpp` 在库，`build/CMakeCache.txt:972` `XERCESC_LIBRARY=…/xerces-c_3.lib`，`sdurw_loaders_LIB_DEPENDS` 缓存行含该 lib。**留痕在库**：`traceability/io-pio3-dependency-probe.md`（108 行：方法可复现命令、候选矩阵、结论三档、边界声明三 条）＋`builds/wp11-t01/io-pio3-probe-raw.log`。**零引入实证**：CMakeLists 零 L1/第三方目标、`vcpkg list` 状态与既有环境一致（验证只读）、miniz/expat/pugixml 无安装动作痕迹；冻结权表述（WP-11 评审）在留痕/CMakeLists/单元卡三处一致，未私裁 |

## 4.5 测试真实失败能力 —— 通过（本任务零测试代码，改用门禁＋契约校验器双失效注入；均即红、还原后逐字节一致）

本任务按契约不建测试目标（acceptance 3），"任选断言构造失败输入"形态不适用，改用**验证者失效注入**（验证验证者，每枚探针后 `git status --porcelain` 零输出＋被改文件与备份 `cmp` 逐字节一致）：

- 探针 1（门禁 R3 有牙齿）：向 worktree A 的 `io/src/Io.cpp` 追加 `#include <QString>` → `cmake -DIRD_ROOT=… -P cmake/ird_gates.cmake` **exit 1 且报 IRD-GATE-R3**（零 Qt 红线真实检出）——还原后送验对象逐字节一致。
- 探针 2（契约校验器有牙齿）：IO-T01.json 变异副本（taskId→"IO-T01-X"）→ validate-task.ps1 **抛 `invalid taskId: IO-T01-X` 拒收**。
- 测试强度评估随 IO-T06/T07（测试整备任务）验收时按其用例执行，本任务无恒真测试面。

## 4.6 架构红线 —— 通过（AGENTS §5 八条逐条）

1. **L2 计算内核零 Qt**：新增 4 文件 include 面 8 处全标准库/自单元（4.4②）；ird_gates R3 零命中＋R3 失效注入探针证实检出能力（4.5）。
2. **业务域单元互链禁止（R-1）**：io 仅 PUBLIC 链 core＋diagnostics 两条 ARCH §3.5 登记边；io/CMakeLists 内置配置期守卫（两面读取 LINK_LIBRARIES＋INTERFACE_LINK_LIBRARIES，非白名单 `sdurws_ird_*` 即 FATAL_ERROR）；ird_gates SUB 检查零 io 命中。
3. **跨单元私有头禁止（R-2）**：io/include/ 仅两公共头；零跨单元 include（core/diagnostics 消费按 P-PR-1 同款口径留给首个消费任务——IoFwd/IoError 为纯标准库头，实测零 ird 头 include）；ird_gates R2 零命中。
4. **框架零源码修改（SA-02）**：4.1 实证零框架路径＋SA-02 门禁零命中＋patches/ 零新文件。
5. **不做名称前缀拼接/剥离（R-4）**：ird_gates R4 唯一命中＝既有 ProjectStoreImpl.cpp（本任务未触碰）；新增文件零拼接/剥离语义（仅注释提及 RobWork 名词）。
6. **权威唯一（PA-1）**：IoError.hpp 显式登记 IO-\* 稳定诊断码注册权威＝diagnostics StableCodeRegistry（P-IO-6 锚点，"io 不越权自建注册表"）；本任务不自建单位/诊断文案面。
7. **不可变历史（PA-2）**：本任务无修订/历史写路径，N/A。
8. **不继承 old/**：diff 无 old/ 引用（old/ 磁盘本不存在）；IoError.hpp 等仅按 io.md 卡面从零承载。

## 4.7 注释规范 —— 通过（新增产品文件 4 件全查＝100% 覆盖，超出"抽查 ≥3"）

- **文件头**：4 件均有 `@file/@brief`＋"设计依据"（需求/单元卡/契约条目定位）＋"背景说明"（解释为什么）三段式中文文件头注释；Io.cpp 的"为什么是注释文件而不是零源码"（CMake STATIC 至少一翻译单元纪律）为典型"解释为什么"样本。
- **类/函数 Doxygen**：IoErrorCode 枚举（57 值逐一注释稳定 token 与依据章节）、IoError 结构（生命周期/所有权/params 保序确定性/敏感性约束）、IoCancelToken（所有权＝调用方实现、幂等复位语义）、IoProgress（单位语义/单调性/total=0 含义）、IoResult（ok 判定契约＋explicit bool 防误用）——公共接口注释构成完整契约文档。
- **§2.5 高危信息**：所有权（IoCancelToken 裸指针"调用方持有，io 不接管"）、线程约束（纯值类型并发只读安全；令牌实现方约束；"io 不创建线程"）、错误语义（格式/安全/资源/包过程四族归类、Cancelled＝状态非错误不落诊断）、确定性（枚举顺序＝§9.12 表行序、表尾追加纪律、params 构造序、kAccessVersion 编译期常量、stage 稳定英文短语）——逐项在位。
- **物理单位/坐标系/rad**：本任务交付面为类型契约头与锚点翻译单元，无物理量/位姿/角度实体，N/A（IoProgress 的 done/total 单位语义已按"随 stage 注明"显式登记）。
- **枚举计数复核**：IoErrorCode 实测 57 个枚举成员，与头注释"57 值"及 io.md §9.12 建议值表一致（验收者计数复现）。
- **CMakeLists**：目标注册、依赖红线守卫、别名条件、编码 /utf-8 等关键步骤均有中文注释块，含魔法语义说明（如 `NOTFOUND` 字面串不误报原理），符合 AGENTS §2.7 口径。

## 4.8 文档同步 —— 通过

- **单元卡增量修订在案**：io.md v0.2→v0.3（卡头版本行＋§15.5 变更记录行）——v0.3 行逐点核对磁盘现实：STATIC/C++17/PUBLIC 链两条边、io/CMakeLists.txt 新建、IoFwd/IoError 就位、src/Io.cpp 锚点、测试目标不预建、P-IO-3 探针留痕路径——**验收者 ls/grep 逐项证实与磁盘一致，零失实陈述**；设计正文（§2~§14）零变更声明与 diff 事实一致（io.md 变更 +5/−4 仅卡头/§1.1/§1.2/§15.5）。
- **实现与任务卡零未登记偏差**：verify 命令偏差系治理放行时（2026-09-15）已登记于契约 acceptance 3 与 compile-log，本提交按登记执行并消账（contract-compile-log.md:174）；allowedFiles 内文件全部按卡产出，无卡外表。
- **任务状态**：契约 status 维持 ready 未翻转——正确（翻转权＝所有者合入收尾 PIPE §4.7⑤）；unit-status.json/phase-one-task-index.json 未触碰与先行落位任务（DIAG-T02）同口径（设计卡状态行≠构建落位状态）。
- **findings.json 未关闭条目相关性核对（157 条 open）**：①io 直接相关＝F-029（IO-T07 dependsOn 补正——owner＝wp11-t07 领取者，本任务无处置义务）、R-IO-1/P-IO-3（io.md §15.2/§15.3——**本任务正是其处置项**，可用性验证完成并留痕，冻结权正确保留 WP-11）；②F-120（PRJ 段队首死锁）已 fixed，其 IO-T01 连带放行已兑现（本验收即产物）；③F-012 家族（提交标题动词开头）——**本任务两笔提交再现，转登 F-179**；④F-175 家族（卡头日期行）——**io.md 卡头"日期"行停留 2026-09-10（版本行已 v0.3@2026-09-17），第十四次再现，转登 F-180**；⑤F-007/F-008/F-009/F-010（环境/gitignore 类）与本任务产物无涉（builds/wp11-t01/ 的 9 份 .log 虽受 `*.log` 规则辖域〔验收者以未跟踪探针文件实测规则活跃〕，但已实际入库且内容完整，F-010 的既有处置不受影响）；⑥其余 open 条目（core/project/policy/runtime/evidence/diagnostics 各单元与流水线治理面）与本任务零交集。

## 4.9 commit 质量 —— 通过（建议级 1 条转登 F-179）

- 三提交标题均带 `[WP-11-T02]` 前缀（≙契约所引 DTB 卡，契约 branch=wp11-t01 系卡内序号口径——契约 note 显式登记，先例 EX-T01）。
- **正文四段齐备**（What/Why/验证/影响面）且逐笔核对其验证结论与留痕及验收者复现一致：集成/冒烟零错误、门禁 11 命中零新增、libzip/框架 XML 就绪度结论、validate 双 PASS——**零粉饰、零虚标**；8d45c17c 显式声明"本任务无测试目标……不伪造测试通过状态"（AGENTS §4.2 如实口径）。
- `Acc-Covered` 尾注逐笔映射 acceptance 条目（881f67ff→1,2；8d45c17c→3,4；d175a4e4→1〔traceability-update〕）。
- **风格瑕疵（建议级）**：881f67ff 与 d175a4e4 两笔标题名词开头（"io 单元构建落位…"、"io.md 同步…"），非动词开头；8d45c17c（"登记…"）合规——F-012/F-023/F-024/F-026/F-027/F-100/F-176 家族再现，判例口径（F-012：纯风格瑕疵不阻断）→ 转登 F-179。

## 4.10 留痕完整 —— 通过

- `traceability/builds/wp11-t01/` **9 件全部在库**且与本验收实测交叉一致：verify-target-build.log（verify 命令零错误——本验收 4.3 复现一致）、integration-build.log（169 行全量零错误）、smoke-configure/smoke-build.log（toolchain 口径零错误——本验收复现一致）、gates-ird-gates-target.log（11 命中＋自测 9 项——本验收同构复现）、gates-ird-gates-base-compare.log（base 2cad0cc5 对比同分布——本验收以"命中集全为 diff 未触碰文件"独立佐证零新增）、io-pio3-probe-raw.log（探针原始输出——本验收逐条复演一致）、validate-task/validate-docs.log（PASS——计数逐字一致）。
- **gtest XML＋ird-test-report.json：不适用且未虚标**——本任务无测试目标系契约 acceptance 3 显式登记（测试整备随 IO-T06/T07，届时按 DTB §5.5 接入并产出 XML/报告）；DIAG-T02（含测试落位任务）先例的 XML/报告件与本任务卡面产物列无关。
- phase-one-validation.log 未更新——与 phase-one 后全部任务同口径（该文件自 WP-03 时代后未再追加，grep 零 PRJ-*/DIAG-* 条目）；resultNote 字段系 phase-one 时代契约字段（IO-T01.json 无此字段），其功能等价物＝contract-compile-log.md IO-T01 修订行＋io.md §15.5 v0.3 变更记录行，均已更新。

## 4.11 偷懒/缩水实现扫描 —— 通过

- **①关键词扫描**：diff 新增行 `TODO|FIXME|stub|placeholder|未实现|not implemented` **零命中**（grep exit 1 实测）。Io.cpp"有意留空"＝契约显式安排的豁免：acceptance 1 原文"不在骨架预建占位实现"＋io.md §12 IO-T01 行产物定义＋core/policy/diagnostics/project 四单元落位同先例（DIAG-T02 验收同款豁免判例）——依据成立，非缩水。
- **②空实现检测**：公共接口面＝两类型契约头（inline 值定义＋纯虚接口＋带逻辑的 explicit operator bool）——无"空函数体/仅返回默认值冒充实现"；Io.cpp 零代码且无任何声明冒充能力（NFR-MNT-04：无消费者的能力不预建）。
- **③逐条证据表**：见 4.4——四条 acceptance 全部有命令输出/文件行号/磁盘实测支撑，任一条未采信"已实现"声明。
- **④测试强度**：本任务无测试面（契约规定）；以 4.5 双探针证实门禁与契约校验器的失效检出能力（防"扫描恒绿"），产品行为测试强度留待 IO-T06/T07 验收评估。

## 问题清单

| 级别 | 编号 | 摘要 | 去向 |
| --- | --- | --- | --- |
| 阻断 | 无 | — | — |
| 建议 | F-179 | 881f67ff/d175a4e4 两笔提交标题名词开头，非动词开头（F-012 家族再现；正文四段齐备、零粉饰） | findings.json 已登记 |
| 建议 | F-180 | io.md 卡头"日期"行停留 2026-09-10，版本行已 v0.3（2026-09-17）——F-175 家族（卡头日期行不同步）第十四次再现 | findings.json 已登记 |

观察（不构成问题登记）：io.md §15.2 R-IO-1 行文本未随验证完成翻转状态——该行陈述的是风险与缓解机制（"vcpkg 可用性验证前置到 IO-T01"），无失实；完成事实已由 §15.5 v0.3 变更记录、§1.2 落位行与探针留痕三处承载，读面自洽。

## 验收者环境说明

- 复现现场：`git worktree add --detach D:/10_Source_Repos/21_robot/acc-iot01-a d175a4e4`（验收后删除）；集成树 `acc-iot01-a/build`、冒烟树 `acc-iot01-smoke`、验收日志 `acc-iot01-acc-logs/`（均在仓库外，不入库）。
- 记录回传：`git worktree add -b acc/IO-T01/1 D:/10_Source_Repos/21_robot/acc-iot01-b origin/redesign-main`（本记录与 findings.json F-179/F-180 同提交推送）。
- 门禁中文输出在 GBK 终端捕获呈 mojibake（编码面既有现象），命中判定均按 ASCII 稳定码 IRD-GATE-\* 与目标名进行，不受影响。
- 本验收未修改实施产物、未向任务分支 wp11-t01 推送任何提交；送验对象冻结 d175a4e4 全程未变。
