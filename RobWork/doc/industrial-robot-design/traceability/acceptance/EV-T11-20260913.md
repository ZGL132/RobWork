# EV-T11 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | EV-T11（evidence 测试替身与契约套件，WP-05-T11；契约 tasks/foundation/EV-T11.json） |
| 送验对象（冻结） | `cef36791c392d7edd091bcb9819eb98b937762ea`（分支 wp05-t11 尖端；验收全程按 SHA，不按分支名） |
| diff 范围 | `4395bdabc6905cfdbaa36028fac90404c95d22fc..cef36791c392d7edd091bcb9819eb98b937762ea`（2 个提交：88801be9 实现＋cef36791 留痕） |
| 验收尝试 | attempt 1（验收记录输出路径 traceability/acceptance/EV-T11-20260913.md，无同日前置记录，不加 -rN 序号） |
| 验收日期 | 2026-09-13 |
| 裁决 | **pass**（4.1～4.11 全部通过；阻断级问题 0；建议级问题 1 项→转登 F-073） |

## 0. 独立性声明（ACC §2）

本验收为全新上下文会话（PIPE §0.4 派发模式，无需降级声明）：未消费实施者的任何过程叙述/辩护文本，仅读取任务契约、设计文档、送验 diff 与留痕产物；全部构建与测试由验收者本人在独立现场亲手重跑（复现优先于采信）；立场为对抗式核查（逐项为"不通过"找证据，每项均给出通过命令输出或失败反证）。验收者未修改任何实施产物、未向任务分支推送任何提交。

## 1. 逐项证据（ACC §4，4.1～4.11）

### 4.1 改动范围——通过

`git diff --name-status 4395bdab..cef36791`：36 个变更文件（11 个 industrialrobot 源/资产＋24 个 traceability 留痕＋1 个 units/evidence.md），逐一对契约 `allowedFiles` 核对：

- `evidence/**`：CMakeLists.txt（M）＋test/ 5 个新文件——在列；
- `testdata/**`：golden/ev-slice-fixture/1.0.0/ 4 个新文件＋tolerance/ev-slice/v1.0.0.json——在列；
- `units/evidence.md`（M）——在列；
- `traceability/**`：builds/wp05-t11/ 22 个留痕＋findings.json（F-072 登记）——在列。

`forbiddenFiles`（REQUIREMENTS.md／ARCHITECTURE.md）零触碰（不在 diff 中）；diff 无 `patches/` 文件＝无未登记框架源码改动（SA-02）。**零越界。**

### 4.2 可复现构建——通过（全树口径按 F-008 判例以任务目标面为准）

- 复现现场：`git worktree add --detach <临时目录A> cef36791…`（detached @ SHA）。
- 集成模式**重新配置**：`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=<vcpkg 绝对路径> -DCMAKE_PREFIX_PATH=<Qt>`；grep 缓存确认 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`、`GTest_DIR=…/vcpkg/installed/x64-windows/share/gtest`。
- 全树 Release 构建：73 处 error **全部**位于 RWSimulatorPlugin（71）＋bt_plugin（2）＝既有基线缺陷 **F-008** 判例范围，与 industrialrobot 无涉；范围外 error 计数＝0。按判例口径以任务目标面构建为准。
- 任务目标面：`--target sdurws_ird_evidence_test` 与 `--target sdurws_ird_evidence_contract_test` 两次构建均 **exit 0、零 error 零新增告警**（验收者日志 acc-verify-test.log / acc-verify-contract.log）。
- 独立冒烟模式：全新独立树 `<临时目录>/ird-acc-ev-t11-smoke`，带 vcpkg toolchain 绝对路径，全量构建 **exit 0 零 error**（MSB8029 中间目录位于临时目录的告警为验收环境现象，非交付缺陷）。
- 环境前置说明：worktree 配置需补两件主树未跟踪框架资源（`RobWork/RobWorkStudio/bin/RobWorkStudio.ini.template.static`／`ini.shared.in`，被 .gitignore `[Bb]in/` 忽略）与 toolchain/Qt 参数——与既有 open 条目 **F-062** 完全一致（环境缺口非本任务产物缺陷，本验收再次实测复现）。

### 4.3 契约 verify 命令——通过

契约 `verify`＝`cmake --build build --config Release --target sdurws_ird_evidence_test`，验收者在 worktree A 集成树亲手执行：**exit 0，日志零 error**。两测试目标随后在集成树亲手运行：`sdurws_ird_evidence_test` **205/205**、`sdurws_ird_evidence_contract_test` **14/14**，均 exit 0（gtest 输出：`[==========] Running 205 tests from 59 test suites.`／`Running 14 tests from 3 test suites.`，`[  PASSED  ]` 全量）。

### 4.4 acceptance 条目——通过（逐条证据）

1. **§11 验证矩阵逐条通过并留痕**：`evidence/test/README.md` §3 给出 §11 全矩阵→文件落位映射（EV-T02～T11 累积）；集成测试二进制 `--gtest_list_tests` 实测矩阵组分布：EV_ENV 29／EV_VER 22／EV_DEP 15／EV_CPA 15／EV_CUR 13／EV_SNAP 10／EV_REG 10／EV_COV 6／EV_ID 6／EV_INV 2／EV_ERR 3／EV_CASESET 4／EV_SLICE* 2／CR-01 5（contract 目标）等；EV-T11 新增端口面（EvaluatorPortVerdict 9＋Context 2＋Currentness 1＋DoubleBoundary 2）与契约面（EnvelopeAccessorContract 4＋SliceFixtureContract 5）全部两模式通过（4.3）；留痕＝提交的四份 gtest XML（205/0、14/0 双模式）＋ird-test-report.json 逐用例 trace 表＋builds/wp05-t11/ 全套日志。
2. **替身边界声明（EV-REG-3）**：`evidence/test/README.md` §1 全文声明（"替身输出仅验证 evidence 契约，不构成 IK/动力学/碰撞检测等业务算法正确性证明"）＋EvidenceTestDoubles.hpp 文件头同款声明＋机检面 `ScriptedDoubleBoundary.BoundaryDeclarationDocumentedInReadme_EV_REG_3` 通过（断言三锚点『仅验证』『不构成』『EV-REG-3』在案）。
3. **替身数据不得冒充真实证据**：README §1.4（替身只存活测试进程内存、不写归档/缓存/manifest）＋机检 `ScriptedDoubleBoundary.DoubleLivesOnlyInTestSources_EV_REG_3` 通过（产品源码 src/＋include/ 替身符号零命中，反向自证替身头确在测试面——扫描非空转）。
4. **数据集交付**：`testdata/golden/ev-slice-fixture/1.0.0/`（manifest＝contract-fixture 类按 ird-golden-manifest/1 登记＋inputs/slice.json＋expected/slice-identity.json＋generate/make_ev_slice_fixture.mjs）＋`testdata/tolerance/ev-slice/v1.0.0.json`（零宽容差档案）。验收者亲跑 lint 工具：`sdurws_ird_testdata_lint` 扫描 **3 个数据集版本，违规 0**（lint-ok: ev-slice-fixture/1.0.0；注：须显式 `--dict` 指定 requirements-ids.json——既有 F-016 缺省路径缺陷，非本任务引入）。`_contract_test` 内 GoldenDataset::load 全链（解析→schema→SHA-256 完整性→交叉校验）与 EV-ID-1 数据集载体（三身份＋双形态编码逐字节对照＋parse(expected)==重建切片）用例通过；envelope 访问器特化（EvidenceEnvelopeAccessors/DraftEnvelopeAccessors 五面）按 testkit §10.2 交接交付于 `_contract_test` 并与产品 validateCombination 双面/分工面如实对账。
5. **O-14 未决保守字面**：EV-COV 契约用例按 §6.6 字面构造——覆盖分母＝快照冻结必验集（`standardCases()` 三工况字面），未私造工况 schema；`MarkersCarriedVerbatim_O14_EV_CASESET` 等字面用例在案；处置按契约 note 编入（DOC-T07 放行口径）。

`ird_gates`：**已建成并执行**（不适用"门禁未建成"豁免）——验收者实测 5 处命中＝3 项既有（F-019×2：testdata_lint 表外依赖边＋T1 产品目标链 testkit；F-051×1：runtime_test→testkit）＋2 项新增（evidence_test/evidence_contract_test→sdurws_ird_testkit 直链），新增两项已由实施侧登记 **F-072**（ird_gates_whitelist T-1 段明文允许而引擎 SUB 形态未豁免的引擎/白名单矛盾，与 F-051 同根因，处置归 WP-01 构建所有者）；产品目标链接面零新增违规。

### 4.5 测试真实失败能力——通过（验收者双探针）

- 探针 1：`EvaluatorPortSuiteTest.cpp` 用例 `GateBlocksScriptedProof_EV_VER_1` 断言改为错误期望（EngineeringInfeasible）→ 冒烟树重建后运行 **exit 1**，gtest 报 `Expected equality of these values`（L497）＋`[  FAILED  ]`——非恒真。
- 探针 2：`SliceFixtureContractTest.cpp` 用例 `RebuiltSliceMatchesExpectedIdentityAndEncoding_EV_ID_1` 期望值换为 baseline 身份 → 重建后 **exit 1**，FAILED（L377）——身份逐字节对照真实有效。
- 恢复：两处逐字还原后重建复绿（两用例 exit 0）；`git status --porcelain` 于 worktree A **零残留**（探针未污染送验现场）。

### 4.6 架构红线——通过（AGENTS §5 八条逐条）

1. L2 零 Qt：新增 5 个测试文件 Qt include 扫描零命中（唯一命中为 BuildRedLineTest.cpp 注释文字"零 Qt"）；产品目标 `sdurws_ird_evidence` 链接面不变（仅 sdurws_ird_core），CMake 配置期红线守卫保持。
2. R-1 业务域互链：新文件 include 仅 core/evidence/testkit 三类公共头；testkit 为测试支撑设施（非业务域单元），其消费边＝testkit §10.2 交接交付物本体；门禁引擎对该边的命中已登记 F-072（引擎/白名单一致性，owner WP-01，非实现越权）。
3. R-2 跨单元私有头：全部 `<sdurws/ird/<unit>/…>` 公共头形式；唯一本地 include 为同目录 `EvidenceTestDoubles.hpp`（测试设施，非跨单元）。
4. SA-02 零框架源码修改：diff 无框架文件、无 patches/ 新文件。
5. R-4 名称前缀：diff 无 RobWork 名称拼接/剥离逻辑。
6. PA-1 权威唯一：汇总/编解码/校验语义未越权（诊断 token 均按"建议码、码值权威归 StableCodeRegistry"表述）；模型身份/单位/文案零私设。
7. PA-2 不可变历史：diff 无修订覆盖/改写语义。
8. 不恢复 old/：无 old/ 引用。

### 4.7 注释规范——通过

新增/大改文件 6 个全部逐一通读（EvidenceTestDoubles.hpp 376 行、EvaluatorPortSuiteTest.cpp 1083 行、EnvelopeAccessorContractTest.cpp 439 行、SliceFixtureContractTest.cpp 471 行、test/README.md 87 行、make_ev_slice_fixture.mjs 346 行）：文件头均带设计依据（§11/§9.3/§10.2/契约 acceptance 编号）与"替身边界声明"；类/函数 Doxygen 中文注释齐备；§2.5 高危信息逐项在案（线程约束"单线程"声明、确定性"按构造序回放、无时间/随机源"、错误语义"域自选轨/取消抛出轨"与建议码归属、单位/取值范围标注、所有权/生命周期标注）。无缺项。

### 4.8 文档同步＋findings 相关性——通过

- 单元卡 evidence.md **v1.2** 按 DTB §5.4 登记实现落位（§11 设施行状态更新＋§15.4 变更记录 v1.2 条目——与交付逐项对应，本验收未发现声明与实现偏差）；契约 status 保持 ready（翻转 done 归收尾环节，PIPE §4.7）；validate-task 治理脚本验收者亲跑 **PASS**。
- findings.json open 条目（30 项）与本任务相关性逐条核对，结论：
  - **相关并已由本任务面验证**：F-019/F-051/F-072（门禁边——实测 5 处命中与归属一致）；F-016（lint 工具字典缺省路径——本验收实测命中，以 `--dict` 显式路径绕行后全绿）；F-062（worktree 构建环境前置未成文——本验收实测命中两件主树本地资源）；F-065（留痕日志中文双重编码——wp05-t11 日志延续同现象，证据效力不受影响）；F-008（全树构建基线缺陷——本验收 73 处 error 全落其范围）。
  - **相关但非本任务义务**：F-058（evidence.md 模板转义损毁——本任务仅按 DTB §5.4 增量修订 v1.2，损毁节非任务面，未顺手消账，留档不静默积压）；F-020/F-032（evidence.md §3.5 锚点／validate-task 锚点检查范围——文档治理面）。
  - **不相关**：F-004/F-005~F-018/F-023~F-015 系其余条目（他单元实现/文档/提交风格家族；EV-T11 diff 零产品源码触碰，F-046/F-063/F-064/F-070/F-071 等所指文件均不在本 diff）。

### 4.9 commit 质量——通过

两个送验提交均 `[WP-05-T11]` 前缀＋动词开头，正文四段（What/Why/验证/影响面）齐备；验证段主张与验收者独立复现逐项一致（205/205＋14/14 双模式、lint 3 数据集零违规、ird_gates 5 处命中归属、探针红→绿→零残留）。发现两处散文计数与实测有小幅出入（详见问题清单→F-073，F-041 同型）：**方向为笔误级少计/错计、留痕文件本身（XML/报告汇总 total=219）准确，无粉饰**——不构成 4.9 的"验证结论与留痕不一致"阻断（结论本身真实），降为建议级。

### 4.10 留痕完整——通过

`traceability/builds/wp05-t11/` 23 个文件随送验提交入库：gtest XML 四份（_test smoke/integration 各 tests="205" failures="0"；_contract_test smoke/integration 各 tests="14" failures="0"）＋ird-test-report.json（ird-test-report/1，total 219/passed 219，逐用例 trace＋失败能力自证＋证据路径清单）＋构建日志（集成任务面两份、冒烟配置/构建三份）＋ird-gates.log／validate-task.log／validate-docs.log＋探针四件套（red-build/red-run/restore-build/residue-check）。AGENTS §4.2 三件套（gtest XML＋ird-test-report.json＋构建日志）齐备；phase-one-validation.log 未随本任务更新——与 EV-T03～T10 先例一致（逐任务结果记录以 builds/<task>/ird-test-report.json 为载体，ACC 4.10"或 resultNote"口径）。

### 4.11 偷懒/缩水实现扫描——通过

① diff 范围 `TODO|FIXME|XXX|stub|placeholder|未实现|not implemented|HACK` 扫描：唯一命中为 `SliceFixtureContractTest.cpp` L193 `ADD_FAILURE() << "夹具数据集含未实现的依赖 kind: "`——未知 kind 的 **fail-fast 守卫消息**（数据集与实现漂移即显性失败的防线），非偷懒标记；无契约"触发式延后"豁免需要引用。② 空实现检测：新增文件全部为实质测试断言/替身行为/数据资产（公共接口零新增——本任务交付面在测试与数据，无空壳）。③ acceptance 逐条证据表见 4.4（五条全部有具名测试/命令输出/文件行号）。④ 测试强度：断言校验行为与边界（字段级证明校验、逐字节身份对照、透传观测、双面对账），非恒真——4.5 双探针实证会红。

## 2. 问题清单

**阻断级：无。**

**建议级（转登 findings.json）：**

- **F-073（本次新增登记）**：EV-T11 提交信息与 ird-test-report.json 散文计数失准——①实现提交与报告称 `EvaluatorPortSuiteTest.cpp`"15 用例／EvaluatorPortVerdict 8"＋_test 目标"既有 190 用例"、实现提交影响面段称"既有 195 用例"；实测该文件 **14** 个 TEST（EvaluatorPortVerdict **9**＋Context 2＋Currentness 1＋DoubleBoundary 2）、_test 既有 **191**（191＋14＝205）、_contract_test 既有 5（5＋9＝14，此面报告数字准确）。F-041 同型（计数笔误、方向无粉饰——XML/报告汇总 total=219 与实测完全一致，数字差错仅散文）。零行为影响，登记留档供计数纪律自检（F-041/F-025 先例同口径）。

**既有 open 条目处置提示（非新增）**：F-016/F-062/F-065 在本验收实测中再次复现（见 4.8），建议 WP-01 侧随 F-072/F-051 门禁引擎修正一并裁决；F-058（evidence.md 模板损毁）仍 open，未顺手消账（非本任务 allowed 语义面）。

## 3. 验收者环境说明

- 复现现场：worktree A＝`<TEMP>/ird-acc-ev-t11-a`（detached @ cef36791…；集成构建树含缓存确认）；冒烟树＝`<TEMP>/ird-acc-ev-t11-smoke`（独立配置，vcpkg toolchain 绝对路径 `D:/10_Source_Repos/21_robot/RobWork/vcpkg/scripts/buildsystems/vcpkg.cmake`）；记录回传＝worktree B（`acc/EV-T11/1` @ origin/redesign-main）。治理脚本（validate-task）与门禁复核在送验同内容检出（wp05-t11＝cef36791＝origin/wp05-t11，无分支漂移）只读运行。
- 验收者自有日志（临时目录，关键结论已摘录于本记录）：acc-configure3.log／acc-build.log／acc-verify-test.log／acc-verify-contract.log／acc-ird-gates.log／acc-probe1-*.log／acc-probe2-*.log／acc-restore-build.log／acc-residue-check.txt／acc-testdata-lint2.log／acc_evidence_test-integration.{log,xml}／acc_evidence_contract_test-integration.{log,xml}／冒烟同套。
- 分支漂移检测：本地 wp05-t11＝origin/wp05-t11＝cef36791c392d7edd091bcb9819eb98b937762ea（验收全程未变）。
