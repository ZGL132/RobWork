# 验收记录 —— WP-10-T15（UI-T15 工作台验证 harness）· attempt 2（返工后全量重验）

| 字段 | 值 |
| --- | --- |
| 任务 | UI-T15（contract：tasks/foundation/UI-T15.json，schema ird-task/1，status=ready——047336c6 治理纠正后的形态；interUnit=true；requirements UX-09/PM-07/PM-10/PM-11；designRefs units/ui.md §10.1/§10.5/§5.2/§3.1） |
| 送验对象 | **101a8d4aabb9ca892cde61defae201a88ac5b860**（40 位冻结）。漂移检测：`git rev-parse origin/wp10-t15`＝该 SHA，**零漂移**。返工提交＝571fe4a7（修复）＋101a8d4a（复测留痕＋F-288 补齐）共 2 提交 |
| 4.1 diff 基线 | **047336c667d263f93085beab9c2f3c544ffe38d4..101a8d4a**（返工起点＝契约治理纠正提交，按验收请求指令）；全任务范围（95dd9323 merge-base..101a8d4a）另作红线/门禁对照，attempt 1 全量范围见 acc/WP-10-T15/1@7ccc1fad 记录 |
| 验收尝试 | attempt 2（全新上下文；**全量重验**——不采信 attempt 1 的任何通过结论，4.1~4.11 全部亲历重建；只消费契约、设计文档、在库留痕与 attempt 1 验收记录〔仅取其阻断项 B-1 的返工要求口径〕；未读取实施者过程叙述） |
| 验收环境 | 复现 worktree A＝`git worktree add --detach <tmp>/wtA` @ 101a8d4a（detached @ SHA）。环境链按在册处置（F-007/F-200 ini 模板复制＋F-008/F-098 NOMINMAX 三定义＋F-254 冒烟 Qt 前缀）：集成树全新配置 `cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=<主仓>vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64 -DCMAKE_CXX_FLAGS="-DNOMINMAX -DWIN32_LEAN_AND_MEAN -DWIN32"`——CONFIGURE_EXIT=0、缓存 grep `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON` 实证、configure 日志 `: error` 计数 0。冒烟树＝`<tmp>/build-smoke`（同 toolchain＋Qt 前缀）。ird_gates 以脚本模式对送验树与 base detached worktree（@ 95dd9323）**双态亲跑**并路径归一化比对。harness 交互取证全程**零 QT_\* 环境变量**（qt.conf 机制）。记录回传 worktree B＝`git worktree add -b acc/WP-10-T15/2 <tmp>/wtB` @ origin/redesign-main（0863a243）。 |
| verdict | **pass**（4.1~4.11 全部通过、无阻断级问题；attempt 1 阻断项 B-1 经修复后由本验收以**双可写意图实例亲跑 2 轮＋变异因果性试验 M3** 复现为真）。建议级问题 3 项（F-295/F-296/F-297）已随本记录转登 findings.json，不阻塞合入 |

## 4.1 改动范围 —— 通过

- **返工范围** `git diff --name-status 047336c6..101a8d4a` 实证 **37 个变更文件**，逐一对号入座 allowedFiles：`ui/app/{HarnessMain.cpp,PortAdapters.cpp,PortAdapters.hpp}` 3 件 M（属 `ui/**`）＋`traceability/builds/wp10-t15/` 33 件（A/M/R060/R055 重命名——属 `traceability/**`；R060/R055＝attempt 1 命名漂移件 ct-*-ui.log 改名为契约 note 登记的 ctest-ui-*.log）＋`units/ui.md` 1 件 M（v1.7→v1.8）。
- forbiddenFiles（REQUIREMENTS.md/ARCHITECTURE.md）零触碰（37 文件清单实证）。
- **全任务范围**（95dd9323..101a8d4a）grep 反证：除 `ui/**` 与 `doc/industrial-robot-design/**` 外零文件（含框架源码、vcpkg、patches——`patches/` 路径 diff 行数＝0，SA-02 无登记需求）。契约文件 tasks/foundation/UI-T15.json 系 attempt 1 范围内实施者自带（F-289 登记项），其 status=done 自翻已由基线提交 047336c6〔governance〕纠正为 ready，本验收范围（047336c6..head）内该文件零变更。

## 4.2 可复现构建 —— 通过（集成全新树重配＋全量构建＋独立冒烟；`: error` 计数双零）

| 步骤 | 命令（验收者亲手执行于 worktree A） | 结果 |
| --- | --- | --- |
| 集成配置 | §头表环境链命令 | CONFIGURE_EXIT=0；缓存 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON` 实证；`: error` 计数 0 |
| 集成全量构建 | `cmake --build build --config Release` | **BUILD_EXIT=0；`: error` 计数 0**；sdurws_ird_ui_app.exe＋qt.conf（构建期生成物）＋ui 三测试目标产出（bin/Release 实证） |
| 冒烟配置＋构建 | `cmake -S RobWork/RobWorkStudio/src/rwslibs/industrialrobot -B <tmp>/build-smoke -G "Visual Studio 17 2022" -A x64 -DCMAKE_TOOLCHAIN_FILE=<vcpkg> -DCMAKE_PREFIX_PATH=<Qt>`；`cmake --build … --config Release` | 配置 exit=0；**SMOKE_BUILD exit=0；`: error` 计数 0**；app＋三测试 exe 产出 |

## 4.3 契约 verify 命令 —— 通过（前 3 条逐条独立执行＋第 4 条亲跑；冒烟 exe 全量复跑）

| 契约 verify 行 | 验收者执行 | 结果 |
| --- | --- | --- |
| `cmake --build build --config Release --target sdurws_ird_ui_app` | worktree A 原样执行 | **exit=0** |
| `ctest --test-dir build/.../ui -C Release -L "^ird$"` | 同上（ui 叶子目录口径） | **2/2 Passed，exit=0**（变异试验前后共 3 轮全过） |
| `ctest --test-dir build/.../ui -C Release -L "^ird_gui$"` | 同上＋`QT_QPA_PLATFORM=windows`＋插件路径（§12.2 口径） | **1/1 Passed，exit=0**（2 轮全过） |
| `sdurws_ird_ui_app.exe --help（装配外冒烟）` | **零 QT_\* 环境变量**直跑（集成树与冒烟树各 1 轮） | **exit=0**，中文用法打印正常（--readonly/--open/--new/--devlog-dir 四形态齐全） |

- 用例级计数（验收者直跑三 exe 控制台输出）：集成树 ui_test **154/154**、ui_contract_test **22/22**、ui_gui_test **28/28**；冒烟树三 exe **154/154＋22/22＋28/28**——与在库留痕（gtest XML 六件＋ird-test-report 六件，见 4.10）逐件一致。
- 脚本口径：`validate-task.ps1`＝**PASS (1 tasks)**；`verify-task.ps1`＝**PASS UI-T15**（exit 0。注：其第 4 条 verify 行以裸 exe 名调用无法解析路径——脚本健壮性局限，见观察项；本验收以全路径亲跑补证，不影响结论）。

## 4.4 acceptance 条目逐项核对 —— ①②③ 全部通过

### ① 目标与装配面落位＋双模式构建零错误 —— 通过

- 目标落位：ui/CMakeLists.txt `add_executable(sdurws_ird_ui_app app/HarnessMain.cpp app/PortAdapters.cpp)`（_app 后缀首次启用；qt.conf 生成块在位——本验收集成/冒烟两树产物均带 qt.conf 且零环境变量可启动）；双模式构建零 error（4.2）。
- 适配器族＋装配序列：StoreFactoryPortAdapter/StorePortAdapter/ProjectDiagnosticsBridge 翻译规则锚定对端契约（PortAdapters.cpp token 表逐枚举、StoreError 折叠＋detail 透传）；HarnessMain 诊断栈两段式 seal（registry→registerBuiltinCodes 87 码＋ui 九码→seal；factory→seal，175-210 行亲读）＋ShellWiring 必填四端口＋UiSessionController 打开编排（296-400 行亲读）。
- v0.1 诚实桩五件全部在位且零虚构（逐桩核对见 4.11②）。

### ② ird_gates 命中集三方比对恰增 2 条唯一 SUB —— 通过

验收者亲跑脚本模式（引擎自测 9 用例全过：fail_r1/r3/r4/r5/sub/t1/t2＋pass_clean/pass_r4comment）：

| 比对面 | 结果 |
| --- | --- |
| base worktree 亲跑（95dd9323） | **47** 条唯一命中 |
| 送验树亲跑（101a8d4a） | **49** 条唯一命中 |
| 亲跑 base↔亲跑 head 路径归一化 diff | **恰增 2 行，全部 IRD-GATE-SUB：`ui->project（sdurws_ird_ui_app → sdurws_ird_project）`＋`ui->ui（sdurws_ird_ui_app → sdurws_ird_ui）`**；R-1/R-3/R-4/R-5/T-1/T-2/GRAPH/LIB/SA02 命中集逐行零变化（R3×17/SUB×26/R4×1/LIB×2/T1×2 等全部与 base 同行——仅文件路径前缀差异） |
| 亲跑 head ↔ 在库 ird_gates-wp10-t15.txt | **49 条命中行逐行全等**（归一化后 diff 为空） |
| 亲跑 base ↔ 在库 ird_gates-base.txt | **47 条命中行逐行全等**（归一化后 diff 为空） |

DTB 登记行亲证：§4.5 SUB 例外行（development-task-breakdown.md:663——O-31 裁决＋§2.11 卡行，生效 2026-09-22，`sdurws_ird_ui` 产品库链接块仍仅 core/diagnostics＋Qt）＋§2.11 WP-10-T15 卡行（:314）。产品面零对 project include（grep ui/src/ ui/include/ 零命中亲证）。在库新增件 ird_gates-hit-set-diff.txt 与亲跑 diff 同型（45a46,47 恰增两 SUB 行）。

### ③ harness 交互验证留痕 —— **通过（含 attempt 1 阻断项 B-1 场景）**

验收者亲跑取证（全部**零 QT_\* 环境变量**；进程存活以 HasExited/Get-Process 实证）：

| 场景 | 验收者执行 | 结果 |
| --- | --- | --- |
| `--help` | 零环境变量直跑（集成＋冒烟双树） | exit=0——通过 |
| 无参＝无项目首页态（PM-10） | 启动→10 s 存活→强杀；stdout 捕获 | 「诊断栈就绪」「工作台壳已就位」＋窗口存活；在库 harness-homepage.png 亲验（标题/状态栏"未打开项目"、三入口、四写钮禁用、最近项目空表）——**通过** |
| `--new` 创建真实项目→标准打开协议进入会话（PM-11） | `--new <scratch>/demo-project 演示工程A`→12 s→存活校验 | 「项目已创建…经打开协议进入」「项目已打开…（可写：是）」；落盘实证 `HEAD`/`objects/`/`revisions/`/`project.json`/`lock`/`.staging`；在库 harness-project-state.png 亲验（标题与状态栏"演示工程A"、四写钮使能）——**通过** |
| **双开同名项目第二实例降级只读（PM-07——可写：否）〔B-1 修复复现〕** | **双可写意图实例亲跑 2 轮**：实例 1 `--new` 持锁存活（可写：是）→实例 2 普通 `--open` 同目录（无 --readonly）；第 2 轮另加 `--readonly` 第三实例对照与脏锁（强杀后残留锁）变体 | **两轮实例 2 全部存活进入会话且「可写：否（降级只读——见横幅）」**（15 s 存活校验，无 0xC0000409/std::terminate）；dev 日志含 ird.harness.diagbridge 具名拒绝记录（code=PRJ-LOCK-HELD、rejection=diagnostics/subject-missing、F-290 归属文案——与 PortAdapters.cpp 纪律实现逐字同构）；--readonly 对照实例正常「可写：否」；脏锁变体实例 1 正常取锁「可写：是」（PRJ-RECOVERY 恢复路径）——**通过** |
| `--open` 打开既有项目 | 上两行场景即含（同一协议路径） | 通过 |

在库留痕逐件亲验为真：harness-lock2.log（双可写场景描述＋两实例 stdout＋dev 拒绝记录＋结论段，与亲跑输出同构）、harness-lock2-banner.png（第二实例窗口：标题/状态栏「演示工程A（只读）」＋工具条只读标示＋四写钮禁用）、harness-homepage.png、harness-project-state.png、harness-open.log、harness-noenv.log。attempt 1 的「留痕场景误配」已在单元卡 §13 登记注勘误（v1.8）并在库件全面刷新为修正场景。

## 4.5 测试真实失败能力 —— 通过（三处变异真实变红；还原后全绿；零残留；含修复因果性试验）

变异均在集成树执行（头文件变异按 F-075 口径 `--clean-first` 强制重编；还原后同法重建）：

| 变异 | 内容 | 结果 |
| --- | --- | --- |
| M1 | UiSessionController.hpp:171 `forceWaitThreshold{120 * 1000}`→`{1200 * 1000}`（P-UI-8 冻结值） | clean 重建后 **4 FAILED**：FourLevelDefenseForceConfirm＋TForce2GiveUpBounded＋PUi8DefaultsConservativeAndConfigurable＋ForceConfirmWithoutWiringFailsFast |
| M2 | StatusProjection.cpp:152 `case StatusWord::ResultsStale: return 5;`→`return 6;`（§6.3 优先级链） | 增量重编后 **1 FAILED**：SevenStatePriorityChain_UISTG3_UI_T04_ACC1 |
| M3（修复因果性） | PortAdapters.cpp 桥纪律变异：SubjectMissing 移入上抛组（本组 case 注释剔除）→重建 app→重跑 PM-07 双开 | **实例 2 不再进入会话**：stdout「打开编排异常终止（harness 装配类缺陷）：diagnostics/subject-missing: 用户级码缺 subject（PRJ-LOCK-HELD）…」＋错误页阻塞（受控路径，非 0xC0000409）——**证明①桥的分组纪律是 PM-07 降级存活的原因要素；②HarnessMain 兜底 catch 对装配类异常按设计转为可观测错误页（不会退回 std::terminate 进程死亡）** |
| 还原 | 三处 `git checkout --`＋重建 | **154/154 PASSED**；`git status --porcelain` 零残留（三次均实证） |

## 4.6 架构红线（AGENTS §5 八条）—— 通过

1. **L2 零 Qt**：app 目标为可执行（非产品库；Qt 链接合法）；ui 产品库链接块仍仅 core/diagnostics＋Qt（门禁 SUB 仅增 app 目标两行＋在库守卫断言亲历）；harness 源码在 app/（门禁产品扫描域外——R3 命中集与 base 逐行一致实证）。
2. **业务域互链（R-1）**：ui 产品面 grep `ird/project|ird/execution|ird/policy|ird/runtime|ird/evidence|ird/modeling|…` 零命中（亲证）；对 project 的协作仅在 app 目标（DTB §4.5 登记的装配层特权边）；零 modeling/requirements/kinematics/trajectory/dynamics/selection/optimization 触碰。
3. **跨单元私有头（R-2）**：app 所 include 他单元头（project/ProjectStore.hpp、StoreTypes.hpp、QueryPort.hpp、diagnostics/Errors.hpp、Catalog.hpp）全部位于各单元 `include/sdurws/ird/<unit>/` 公共面（ls 实证）。
4. **SA-02**：全任务范围 diff 内零框架源码；patches/ 零新文件。
5. **R-4 名称前缀**：三新文件 grep "RobWork" 零命中（亲证）；门禁 R4 命中仅既有 project/src/ProjectStoreImpl.cpp 一处（与 base 同）。
6. **PA-1 权威唯一**：打开编排触发归装配层（HarnessMain 注释明示「状态机推进归控制器」）；打开五步协议语义留在 project（适配器只做值翻译）；诊断码/文案权威在 core/diagnostics（桥只注入宿主标识，不做文本反解＝零虚构——rework 注释亲读）。
7. **PA-2 不可变历史**：新代码零修订读写路径（--new/--open 经 ProjectStoreFactory 对端契约）。
8. **不恢复 old/**：全部新写。

## 4.7 注释规范 —— 通过（三文件全读＋CMake 增量块与在库留痕件核阅，超抽查线）

- **文件头**：PortAdapters.hpp（设计依据＋O-31 装配层论证＋所有权/线程约束）、HarnessMain.cpp（定位/用法/线程模型/与产品装配层关系）——均含"代码之外的知识"；PortAdapters.cpp 头注同构（attempt 1 已核，rework 未触碰头部）。
- **rework 增量注释逐段亲读**：桥 report 的两分组纪律注释逐 case 说明业务含义与依据（attempt 1 B-1/F-290/UiPorts.hpp C-3 锚点）；拒绝消息含码/拒绝 token/工厂 detail/缺口归属四要素；HarnessMain 兜底注释说明第一道/第二道防线关系与 exit 5 语义；StoreFactoryPortAdapter 注更正横幅数据源（readOnlyCause/lockHolder）与诊断目录两通道独立——与实现逐行相符。
- **§2.5 高危信息**：单位/坐标系（本任务无物理量计算面）；所有权（桥构造 invalid_argument fail-fast、unique_ptr 独占＝INV-SES-3 保活）；线程约束（UI 线程 M-1）；错误语义三分类（装配缺陷 fail-fast／对端 StoreError 折叠／对端记录契约缺口具名上报后协议继续——两路都不静默）；确定性（token 表逐字取自对端词表）。未发现复述式/占位式/机翻式/过期注释。

## 4.8 文档同步 —— 通过；findings.json 未关闭条目逐类过筛零静默积压

- **单元卡**：units/ui.md v1.7→**v1.8**（§13 UI-T15 落位登记注勘误〔场景误配句如实更正——attempt 1 在库 lock2 留痕实为 --readonly 第二实例所产〕＋**UI-T15 返工登记注**〔根因链/桥两分组纪律/编排兜底/v0.1 诚实边界增补〔PRJ-\* 项目级事件条目在缺口裁决前不入目录、横幅数据源＝打开结果自带锁事实〕/双可写复测口径〕＋§16.7 v1.8 行）——登记内容与实现形状逐条比对相符（本验收 4.4/4.5 亲历为证）。
- **DTB**：§2.11 WP-10-T15 卡行＋§4.5 SUB 例外行在位（4.4② 已核）。
- **契约状态**：status=ready（047336c6 治理纠正）——与「待验收」阶段相符；done 翻转归收尾（PIPE §4.7）。
- **findings.json 未关闭条目相关性核对**（231 条 non-fixed 逐类过筛）：
  - **F-288**（本任务直接相关，attempt 1 登记）：四类缺口零件已随 101a8d4a **全部实入库**（gtest XML 六件＋ird-test-report 六件＋integrated-build.log＋validate-task.log——本验收逐件验真，见 4.10）；ctest 日志按契约 note 文件名落位（ctest-ui-ird.log/ctest-ui-gui.log），ct-*-ui.log 旧件删除（R060/R055 改名，历史可溯）——**材料缺口已闭合**，登记状态仍 open（翻转归治理，建议随本记录消账）。
  - **F-289**（契约自翻 done）：已由 047336c6 纠正为 ready——材料处置完成，状态 open 待治理随本验收 pass 消账。
  - **F-290**（project PRJ-LOCK-HELD 缺 subject——跨单元契约缺口）：**如实保持 open**（裁决权在 project/diagnostics 所有者）；harness 侧按本返工纪律具名上报不静默吞（亲跑 dev 日志为证），不掩盖缺口——处置正确。
  - **F-291**（AUTOMOC 注释计数）：本返工零触碰 ui/CMakeLists.txt（diff 实证），现状「十五个源文件」vs src/ 19 个 TU 未恶化——在案。
  - F-278/F-280/F-281/F-282/F-285（文件尾换行家族）：UiPorts.hpp/UiSessionController.hpp 本任务零触碰（全范围 diff 实证），尾字节仍 0x50 'P'——在案未恶化。
  - F-007/F-062/F-098/F-099/F-101/F-254（全新树环境链）：本验收亲历并按登记口径绕过（ini 模板复制＋NOMINMAX 三定义＋Qt 前缀），一次配置成功，无新形态。
  - F-008（RobWorkSim 全新树构建失败）：按 NOMINMAX 口径规避，集成全量构建零 error——与本任务 diff 零交集。
  - F-251（顶层 ctest 空转）：本契约 verify 用 ui 叶子目录口径，未踩雷。
  - F-259（门禁 4b Qt 类头命中 3 处）：在 37 条 R3 命中集内与 base 同行——无变化。
  - F-068/F-075（验收探针增量陷阱家族）：本验收 M1 按 clean-first 口径规避；并新遇同族新形态（见 F-297 转登）。
  - 其余 open 条目（F-012 标题家族、F-081 幻影清单家族、F-0xx 各单元发现等）与本任务范围（ui/app 装配面）无涉。**历史发现零静默积压。**

## 4.9 commit 质量 —— 通过（两提交标题动词开头＋正文四段齐备；验证结论与亲测一致）

- 571fe4a7「修复 harness PM-07 第二写者双开进程终止：诊断桥拒绝显式纪律与打开编排兜底」、101a8d4a「刷新验证留痕：PM-07 双可写实例复测证据与 F-288 缺口补齐」——均 [WP-10-T15] 前缀＋动词开头；What/Why/验证/影响面四段齐备。
- 逐条比对亲测：修复内容与 diff 逐行相符✓、双可写复测留痕与亲跑同构✓、F-288 四类零件在库验真✓、门禁三方比对全等✓、ctest 双标签 2/2＋1/1✓。
- 例外（建议级，转登 **F-296**）：101a8d4a 正文两处散文计数与实际入库件数不符（「ird-test-report.json 五件（集成 model/contract/gui＋冒烟 model/contract/gui）」——括注列 6 件、在库实为 6 件；「冒烟模式测试六件（-smoke 后缀 XML/JSON/console）」——-smoke 后缀文件实为 3 XML＋3 console，JSON 三件名属 ird-test-report 系）。方向均为**少报/枚举口径含混**、无粉饰，全部实质结论（154＋22＋28 全过、decisive=true、failed=0）经亲测为真——F-025/F-249/F-256 散文计数家族新例。

## 4.10 留痕完整 —— 通过（F-288 四类缺口全部闭合且逐件验真）

- **在库验真**（builds/wp10-t15/ 返工后 33 件）：gtest XML 六件（集成＋冒烟 × ui_test/ui_contract_test/ui_gui_test——文件头 `tests="154|22|28" failures="0"`、真实测试套件名、时间戳 2026-09-22）＋ird-test-report 六件（summary.decisive=true、total/passed/failed＝154/154/0、22/22/0、28/28/0 ×双模式）＋integrated-build.log（`: error` 计数 0、BUILD_EXIT=0）＋validate-task.log（PASS）＋ctest-ui-ird.log（2 tests）/ctest-ui-gui.log（1 test）＋harness 六件（4.4③ 亲验）＋门禁三件（4.4② 三方比对全等）＋smoke-configure/smoke-build.log（exit 0）。
- 契约 note 所列留痕文件名与在库实际**一致**（attempt 1 的命名漂移已消解——ct-*-ui.log 改名 ctest-ui-*.log 且 note 口径自此名实相符）。
- phase-one-validation.log：F-201 在案（家族性停更，实践以 resultNote/留痕目录替代）——非本任务特有。

## 4.11 偷懒/缩水实现扫描 —— 通过

- ①rework 代码 diff `TODO|FIXME|XXX|HACK|placeholder|not implemented|未实现|待实现` 扫描**零命中**（范围 047336c..head 的 industrialrobot/ 下增量）。
- ②空实现检测：仅五桩为恒值实现，全部为契约 note 显式登记的 v0.1 诚实桩——逐桩重读零虚构注释在位（DraftQueryPortStub「磁盘草稿行集如实恒空」、SessionTaskPortStub「无执行引擎＝无任务……false＝未受理」、UnloadedPolicySource「'未装载'与'装载了空策略'是两个事实」、NullUiNameResolver「不虚构名称」、HarnessAboutSource「版本值零虚构」）——通过。
- ③acceptance 逐条证据表：见 4.4（①②③均具名证据，无一条无证据）。
- ④测试强度：ui 三套件断言行为与边界（M1/M2 变异真实变红；非恒真）；app/ 装配面自身零测试承载（attempt 1 在案观察——本返工以交互取证＋M3 因果性试验补强其质量证据，harness 开发工具定位下可接受，观察项维持）。

## 发现问题清单

### 阻断

- 无。

### 建议级（已随本记录转登 findings.json，编号 F-295~F-297）

- **F-295**（注释精度）：PortAdapters.cpp 桥纪律 switch 的收尾注释「工厂错误码全表 11 值在上方两个分组穷举」与代码形态不符——switch 显式列出 7 值（CodeUnknown/Usage 上抛组＋CodeDeprecated/SubjectMissing/ComparisonMissing/ParamSchemaMismatch/ContextMissing 降级组），其余 4 值（DuplicateCode/CategoryMismatch/InvalidState/BindingMismatch）落入兜底 `throw;`。行为语义自洽（4 值在 create() 路径不可达，兜底上抛＝装配缺陷组语义，新增值误入时经 HarnessMain 兜底转可观测错误页、非静默）——但「穷举」表述失准；且「编译期由 -Wswitch 提示补行」在 MSVC 下不成立（-Wswitch 为 GCC/Clang 语义；MSVC 等价 C4061/C4062 默认关闭）。建议：注释改写为「11 值按语义二分：降级组显式列出，其余（含未来新增值）归上抛组」或显式列全 11 case；工具链声明改按 MSVC 口径。责任方：ui 实施者（下个触碰 app/ 的任务顺手）。
- **F-296**（F-025/F-249/F-256 散文计数家族新例）：101a8d4a 正文「ird-test-report.json 五件（集成…＋冒烟…）」括注列 6 件、在库 6 件；「冒烟模式测试六件（-smoke 后缀 XML/JSON/console）」实际 -smoke 后缀为 3 XML＋3 console（JSON 三件名属 ird-test-report 系）。方向＝少报/枚举口径含混，实质结论经亲测全真。责任方：ui 实施者（留痕类提交正文枚举与实际入库件数核对）。
- **F-297**（F-068/F-075 验收环境陷阱家族新形态）：`cmake --build --target sdurws_ird_ui_test --clean-first` 的 Clean 阶段波及共享 bin/Release 下未参与该目标的兄弟测试 exe（sdurws_ird_ui_contract_test.exe/sdurws_ird_ui_gui_test.exe 消失，后续 ctest 报 Not Run）——验收者重建两目标后复绿。根因未深查（MSBuild Clean 范围与共享输出目录交互），先例登记供后续验收会话规避：clean-first 后重建全部三个 ui 测试目标再跑 ctest。责任方：构建/治理（如复现可考虑各测试目标独立输出目录）。

### 观察项（不立 finding）

- verify-task.ps1 第 4 条 verify 行（`sdurws_ird_ui_app.exe --help`）以裸 exe 名调用无法解析（脚本对可执行产物的路径解析局限）——本验收以全路径亲跑补证（exit 0）；脚本总体 PASS 不受影响。
- HarnessMain 兜底 catch 的 QMessageBox::critical 为模态阻塞——无人值守自动化场景下进程停在错误页直至人工关闭（控制台行先于对话框输出，证据不丢失）；开发工具定位下可接受。
- F-288/F-289 的登记状态翻转（open→fixed）归治理侧——材料闭合已由本验收逐件验真。
- 在库 integrated-build.log 产自主工作树（D:/10_Source_Repos/...，checkout 同 SHA）——SHA 相同内容等价，本验收已在独立 worktree 全新树复现零 error，口径无碍。

## 裁决

- **verdict＝pass**（4.1~4.11 全部通过；阻断级问题零项；attempt 1 阻断项 B-1 修复经双可写实例亲跑 2 轮＋M3 因果性变异验证为真）。
- 建议级 3 项（F-295/F-296/F-297）已按 ACC §5 转登 traceability/findings.json（编号顺延自 F-294），不阻塞合入。
- 验收者未合入任何内容、未修改送验产物、未向 wp10-t15 推送任何提交；本记录与 findings 转登落于 acc/WP-10-T15/2 分支（基于 origin/redesign-main@0863a243）。

## 记录三元组

| 字段 | 值 |
| --- | --- |
| branch | acc/WP-10-T15/2 |
| path | RobWork/doc/industrial-robot-design/traceability/acceptance/WP-10-T15-20260922-r2.md |
| commit | 本分支（acc/WP-10-T15/2）载入本记录的提交＝evidence commit（40 位 SHA 由验收者回传编排者；收尾按 PIPE 双漂移检测 origin ref==该 SHA 后按 SHA 合并） |
