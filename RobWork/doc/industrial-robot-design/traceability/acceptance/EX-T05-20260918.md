# EX-T05 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | EX-T05（调度线程与事件/进度分发 Scheduler/EventBus；≙WP-08-T06，分支 wp08-t05） |
| 契约 | tasks/foundation/EX-T05.json（status=ready；DOC-T12 编译放行版，note 记 requirements/designRefs/allowedFiles/acceptance/branch 回填） |
| 送验对象（冻结） | **e28dc691696334083deb97e73c09e8bbd69c7e2c**（40 位，全程按 SHA 验收） |
| diff 范围 | d8253f2d8ec9b0496108e5ceb1e015539b0266db..e28dc691696334083deb97e73c09e8bbd69c7e2c（单提交 e28dc691「[WP-08-T06] 实现 EX-T05 调度线程与事件/进度分发（Scheduler/EventBus）」） |
| 验收尝试 | attempt 1（acc/EX-T05/1） |
| verdict | **pass**（4.1~4.11 全部通过，零阻断级；建议级 2 项——F-212/F-213 新登） |
| 验收日期 | 2026-09-18 |
| 记录回传 | 分支 **acc/EX-T05/1**（基于 origin/redesign-main＝d8253f2d8ec9b0496108e5ceb1e015539b0266db），路径 traceability/acceptance/EX-T05-20260918.md，记录提交 SHA 由验收者向编排者回传三元组 |

## 分支漂移检查 —— 通过

- `git fetch origin` 后实测：`git rev-parse origin/wp08-t05` = e28dc691696334083deb97e73c09e8bbd69c7e2c ＝ 送验 SHA，**零漂移**（ACC §3）。

## 独立性与环境（ACC §2/§3）

- **全新上下文**：本会话只消费契约、验收协议、单元卡、diff、在库留痕与既有 findings 登记册；未读取实施者过程叙述（ird-test-report.json 与单元卡 §15.4 v0.7 行属送验产物，仅作主张与在库证据核对，一切结论以下述亲手执行为准）。
- **复现现场**：`git worktree add --detach D:/10_Source_Repos/21_robot/tmp-acc-ex05/wt-a e28dc691696334083deb97e73c09e8bbd69c7e2c`（detached @ SHA）。集成树 wtA/build 从零新建，配置三轮达成：①裸配置失败于 Boost NOTFOUND → 补 `-DCMAKE_TOOLCHAIN_FILE=<主仓>/vcpkg/scripts/buildsystems/vcpkg.cmake`；②失败于 `RobWorkStudio.ini.template.static` 缺失（**F-007 再次实证**：.gitignore `[Bb]in/` 规则吞掉该配置必需件，fresh 树必卡 RobWorkStudio/src/CMakeLists.txt:58——自主仓工作树补拷，环境件不入版本控制）；③configure exit 0，缓存 grep 实测 **`RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`**，另带 `-DCMAKE_PREFIX_PATH=D:/software/QT/6.11.1/msvc2022_64`（F-007 同条登记的 Qt 前缀件）。以上环境操作均为验收侧自备，未触碰送验对象。
- **记录回传现场**：`git worktree add -b acc/EX-T05/1 D:/10_Source_Repos/21_robot/tmp-acc-ex05/wt-b origin/redesign-main`。
- **复现优先于采信**：双模式配置/构建、四跑测试、契约 verify 命令、ird_gates 复跑、validate-task/validate-docs 复跑、双探针失效注入均为验收者亲手执行；送验提交内留痕数值仅作交叉比对（比对结果一致）。

## 4.1 改动范围 —— 通过

`git diff --name-status d8253f2d..e28dc691` 实证 **22 个变更文件**（9 M＋13 A），逐个落在契约 allowedFiles 三个模式内：

| 文件组 | allowedFiles 依据 |
| --- | --- |
| execution/ 下 9 件：CMakeLists.txt（M）、include/…/EventBus.hpp（A）、Scheduler.hpp（M）、StateMachine.hpp（M）、src/EventBus.cpp（A）、Scheduler.cpp（M）、StateMachine.cpp（M）、test/SchedulerEventBusTest.cpp（A）、contract_test/EventBusDeliveryContractTest.cpp（A） | `RobWork/RobWorkStudio/src/rwslibs/industrialrobot/execution/**` 显式列入 |
| units/execution.md（M，+1 行＝§15.4 v0.7 落位登记） | 单元卡显式列入 |
| traceability/builds/wp08-t05/ 下 12 件（A，gtest XML×6＋构建/门禁/校验日志×5＋ird-test-report.json） | `traceability/**` 显式列入 |

- forbiddenFiles（REQUIREMENTS.md/ARCHITECTURE.md）零触碰——diff 文件清单零命中；
- 框架源码（SA-02）：diff 中 `industrialrobot/` 与 `doc/industrial-robot-design/` 之外零文件；patches/ 零新文件（无登记义务）。

## 4.2 可复现构建 —— 通过

- **集成模式**（wtA/build 从零配置，缓存 `:BOOL=ON` 实测）：
  - 契约目标 `--target sdurws_ird_execution_test` **exit 0**（＝4.3 的 verify 逐字执行，同时构建了 sdurws_ird_execution 库与 EventBus.cpp 新翻译单元）；`--target sdurws_ird_execution_contract_test` **exit 0**；产物 `build/RobWorkStudio/bin/Release/sdurws_ird_execution_test.exe`、`…_contract_test.exe` 实存。
  - 全量 `cmake --build build --config Release` 失败于 **RWSimulatorPlugin**（RobWorkSim 框架插件，rw/math/Math.hpp min/max 宏污染簇，73 处 error C）——此为 **F-008 已登记的基线框架缺陷**（open，base 959fb9b 对照同样失败），与本任务 diff 零相关（4.1 已证 diff 不触碰 RobWorkSim）；ird 目标不依赖该插件，不影响本任务构建面判定。
  - 测试亲手执行：`sdurws_ird_execution_test` **89/89 PASSED exit 0**、`sdurws_ird_execution_contract_test` **19/19 PASSED exit 0**（run-test-integration.txt/run-contract-integration.txt）。
- **独立冒烟模式**（tmp-acc-ex05/smoke 独立缓存）：`cmake -S …/industrialrobot -B <smoke> -DCMAKE_TOOLCHAIN_FILE=<主仓>/vcpkg/…/vcpkg.cmake` configure **exit 0**；`cmake --build --config Release` **exit 0**；测试亲手执行 **89/89＋19/19 PASSED exit 0**（run-test-smoke.txt/run-contract-smoke.txt）。
- 与送验提交内留痕交叉比对：`traceability/builds/wp08-t05/` 四份 XML 头 `tests="89"/"19"/"89"/"19"、failures="0"、errors="0"`、timestamp 2026-09-18T13:48——与本人四跑数值一致，无粉饰迹象。

## 4.3 契约 verify 命令 —— 通过

契约 `verify` 数组仅一条：`cmake --build build --config Release --target sdurws_ird_execution_test`——验收者在 wtA 根**逐字亲手执行**，exit 0（见 4.2）。无其他条目。

## 4.4 acceptance 条目逐项证据 —— 通过

| # | 契约条目（摘） | 验收者亲手证据 |
| --- | --- | --- |
| 1 | EX-SUB-1/2 全绿：并发提交各获唯一 TaskId、无重复、同任务事件 FIFO（TASK-03 五元组分配经 §4.3 在提交受理时完成、TASK-01 状态机受理入口）；过期快照拒绝（EX-SNAPSHOT-STALE＋无 TaskRecord——§6.3 V2） | `ConcurrentSubmitAssignsUniqueTaskIds_EX_SUB_1`（6 线程×12 并发→72/72 受理、canonical 文本集合等势零重复、每任务恰一个 Queued 事件、事件五元组前缀＝受理时绑定快照锚定三元组）＋`SameTaskEventFifoFollowsTransitionOrder_EX_SUB_1`（内联走链 Queued→Preparing→Running→Completed 严格四帧）＋`StaleSnapshotRejectedWithNoRecord_EX_SUB_2`＋`MissingClosureObjectRejected_EX_SUB_2`（闭包整体过期/单对象缺失均 EX-SNAPSHOT-STALE＋task 空＋零事件＋tryTask 为 nullopt）——四跑全绿；实现核对 Scheduler.cpp submit()（L114~188：互斥段内 validateSubmission→TaskId::generate→T1 事件→双优先级入队，L148 受理段分配） |
| 2 | 事件 FIFO 与投递线程契约用例通过（EventBus 为 core IDomainEventBus 参考实现——§10.4；九态事件流＋ResultArchived 按序投递）；进度对外节流 ≤10 Hz（D-07 实现参数非需求值） | contract_test 7 用例四跑全绿：`NineStateStreamAndResultArchivedDeliveredInOrder`（八态生命周期流＋ResultArchived 混投，投递序与发布序**逐帧 operator== 全等**、末帧 ResultArchived/前一帧 Completed 家族序锚）＋`ConcurrentPublishLosesNothing`（4×250 并发恰 1000 帧）＋RAII 退订/不死锁（future 2 s 有界等待）/订阅序/waitForIdle；节流＝`ProgressThrottledToTenHz_D07`（ManualClock 虚拟推进：t0/t100/t200 放行、t+50/t+150 丢弃）＋disabled 对照面；EventBus.hpp/cpp 实读与 §10.4 逐条对应 |
| 3 | UI 线程零计算结构断言就位（NFR-PERF-01：>1 s 转后台——内联门槛＝可预测 <1 s 调度线程内联；§11 EX-BLD-1/T05 结构断言承载，提交/控制非阻塞） | `SubmitAndQueryNonBlockingDuringComputation_NFR_PERF_01`：阻塞执行体占用调度域 tick 内联段时，第二任务 submit 经 future 2 s 有界等待就绪且受理、tryTask 即返 Queued——锁只保护簿记不覆盖计算的结构证明；实现核对 dispatchOne()（Scheduler.cpp L531~547：锁外执行段＋异常兜底重锁走 T9） |
| 4 | 队列/优先级/预算/提交验证就位：双优先级 FIFO 不抢占（D-04）；PM-07 只读拒绝启动（§6.3 V3 消费 store.writable() 的预留失败路径变体，写权限判定归 project 不私判） | `PriorityAndFifoDispatchOrder_D04`（逐拍观测 Interactive 先于先提交 Background、同级 I1→I2、B1→B3、在途不受后续优先级影响）＋`InlineDispatchFollowsPriorityThenFifo`（执行体调用序行为面）＋`PerProjectFormalCapQueuesThirdTask`（同项目正式上限/Preview 不计/队头阻塞）＋`QueueCapacityRejectsOverflow_V4`＋`ReadOnlyStoreRejectsFormalSubmit_PM_07`（**真实 ProjectStore ReadOnly 打开**：Verified/Quick 拒绝＋EX-STORE-READ-ONLY、Preview 跳过照常受理）；实现核对 validateSubmission() V1~V4 逐条（Scheduler.cpp L194~298）与 store.writable() 消费点 |
| 5 | UX-10 只承载进度阶段投影（七态状态词与映射归 ui，execution 不新增状态词）；P-EX-1 处置：消费 core v0.1 Draft 未冻结契约，冻结 diff 后按影响面增量同步 | `PhaseTokenPassedThroughVerbatim_UX_10`（worker 域 token 逐字符透传、事件流只含 TaskStatusChanged——进度不入事件 core D-09）；StateMachine.cpp updateProgress（L352~366）注释钉住透传纪律；P-EX-1：EventBus.hpp/实现零重定义 core 事件家族与总线接口（只 include sdurws/ird/core/Events.hpp），增量同步义务登记于文件头与单元卡 §15.4 v0.7 |

## 4.5 测试真实失败能力 —— 通过（2 处失效注入，亲手执行）

| 注入 | 改动（wtA 内、注入后即还原） | 预期 | 实测 |
| --- | --- | --- | --- |
| FIFO 反转 | EventBus.cpp publish：`m_queue.push_back(event)` → `push_front` | 九态流逐帧全等断言应红 | 重编 contract 目标 exit 0 → `--gtest_filter='*NineStateStream*:*SamePublisherFifo*'`：**1 FAILED**（NineStateStreamAndResultArchivedDeliveredInOrder）——序断言有检出力 |
| V3 权限反转 | Scheduler.cpp：`if (!m_collab.store->writable())` → `if (m_collab.store->writable())` | PM-07 拒绝断言应红 | 重编 test 目标 exit 0 → `--gtest_filter='*ReadOnlyStoreRejectsFormalSubmit*'`：**1 FAILED**（ReadOnlyStoreRejectsFormalSubmit_PM_07）——结构化拒绝码断言有检出力 |

- 两次注入后 `git checkout --` 还原，`git status --porcelain`（排除 untracked 构建目录）确认工作树回到送验 SHA 原状；还原后重编复跑全量 **89/89 PASSED**——复现对象未被污染。

## 4.6 架构红线（AGENTS §5 八条） —— 通过

1. **L2 零 Qt**：execution 全目录 grep `#include <Q|Q_OBJECT|QWidget` 零命中（唯一命中为 BuildRedLineTest.cpp 自身的扫描逻辑字符串）；新增文件 include 面仅 std＋core/evidence/project 公共头；
2. **业务域互链（R-1）**：grep `ird/{modeling,requirements,kinematics,trajectory,dynamics,selection,optimization}` 零命中；execution→evidence/project 为 ARCH §3.5/单元卡 §3.5 已登记的编译链接边（CMakeLists 注释与 EX-T01 登记一致，本任务 diff 未改链接边）；
3. **跨单元私有头（R-2）**：Scheduler.cpp 全部 include 为 `sdurws/ird/{core,evidence,project,execution}/…` 公共头路径；evidence/project 类型在公共头内前向声明、.cpp 才 include 公共头；
4. **SA-02**：4.1 已证框架零改动、patches/ 零新文件；
5. **名称前缀拼接（R-4）**：grep 拼接/剥离模式零命中；ird_gates R4 复跑零 execution 新增（见 4.3 补充）；
6. **权威唯一（PA-1）**：V1"属当前上下文"经 ISubmissionGuard 注入不私判、V3 写权限消费 store.writable()、V2 闭包经 evidence 注入面、能力经注册表推导——调度器只消费结论；
7. **不可变历史（PA-2）**：本任务不触修订写路径（归 project）；EventBus 事件不持久化（core D-09）；
8. **不继承 old/**：全部新增实现，无 old/ 引用。

## 4.7 注释规范（AGENTS §2） —— 通过

- **公共头全查**：EventBus.hpp（209 行）与 Scheduler.hpp（643 行）——文件头设计依据（单元卡章节/需求 ID/契约条目逐条）、类注释（业务概念/生命周期/线程安全）、函数 Doxygen（@param 方向与语义/@return/@throws）、§2.5 高危信息（线程约束、所有权"非所有权裸指针＋存活期纪律"、错误语义两分法、实现参数非上游值、确定性来源 ManualClock 同源纪律）全齐备；
- **实现文件抽查（≥3）**：EventBus.cpp（锁结构关键点注释＋逐语义段"为什么"）、Scheduler.cpp（V1~V4 逐条对应清单行、内联段锁外安全性推理、队头阻塞语义出处）、StateMachine.cpp 增量 updateProgress（终态防御与透传纪律）——均为中文、解释设计依据而非复述代码；
- **测试文件**：两测试文件文件头声明替身边界与"断言不 sleep"纪律，用例名带需求/AT 追溯（_EX_SUB_1/_D04/_D07/_PM_07/_UX_10/_NFR_PERF_01 后缀）；
- **CMakeLists.txt**：关键步骤（落位登记、目标增列、测试用例组说明）中文注释块，随 diff 增量同步；
- 反面示例扫描：无复述式/占位式/过期注释；魔法数字均有出处（100 ms 节流窗＝≤10 Hz 实现参数 D-07、15 s 兜底阈值＝10 s 协议窗＋5 s 余量等）。

## 4.8 文档同步与 findings 核对 —— 通过

- 单元卡 execution.md +1 行＝§15.4 v0.7 EX-T05（≙WP-08-T06）落位登记：契约头三件（EventBus 新增＋Scheduler/StateMachine 扩展）、四处归置/边界登记（内联登记段随 EX-T06、Ports 两接口改随 T06/T08、capability 受理时推导、archiveless 校验待词表）、P-EX-1 处置与测试计数（89＝70+19、19＝12+7）——与 diff 实物逐项一致，**未发现未登记偏差**；
- 契约 acceptance/branch 与 DTB §2.9 WP-08-T06 行（需求列 TASK-01/03、UX-10；完成条件＝事件 FIFO 用例＋UI 不做计算）对得上；
- **findings.json 未关闭条目相关性核对（ACC v1.2）**：open 条目约 170 项中，按 EX-T05/Scheduler/EventBus/调度器/进度节流关键词检索，**零条**直接涉本任务实现语义或送验范围；F-007（fresh 集成树配置参数＋ini.template.static 被 .gitignore 吞掉）在本验收复现现场**再次实证**（4.2 环境段）——维持 open，由治理任务消账；F-008（RWSimulatorPlugin 基线构建失败）在本次全量构建中再次命中——同样与送验对象无关（4.2 已说明）。

## 4.9 commit 质量 —— 通过

- 标题 `[WP-08-T06] 实现 EX-T05 调度线程与事件/进度分发（Scheduler/EventBus）`：任务编号＋动词开头中文摘要；
- 正文四段齐备：What（按文件/模块列要点）、Why（契约/单元卡章节/需求 ID）、How（双模式构建、89/89＋19/19 两模式、ird_gates 零新增、validate-task PASS、留痕位置）、影响面与兼容性（链接边零变更、既有用例保留、归置偏差登记）；
- 验证结论与在库留痕一致（本验收四跑交叉比对无出入），无粉饰；`Acc-Covered: 1,2,3,4,5` 尾注。

## 4.10 留痕完整 —— 通过

- 送验提交内 `traceability/builds/wp08-t05/` 12 件：gtest XML×4（顶层两模式两目标＋gtest_reports×2）、integration-build.log、smoke-build.log、smoke-configure.log、gates-ird-gates-target.log、validate-task.log、ird-test-report.json——齐备且数值与亲手复跑一致；
- `ird-test-report.json`（ird-test-report/1 schema）：targets×3、acceptanceMapping acc1~acc5 逐条具名、gates、evidenceFiles、honestyNotes——即 4.10"resultNote 已更新"的现行承载（phase-one-validation.log 机制已由其取代，EX-T04 先例同口径，本任务 diff 无该文件与先例一致）。

## 4.11 偷懒/缩水实现扫描 —— 通过

- ① diff 新增行 `TODO|FIXME|stub|placeholder|未实现|not implemented` 扫描：**代码文件零命中**；仅 2 处命中在文档留痕（ird-test-report.json honestyNotes 的"零伪造"正向声明、单元卡 §15.4 v0.7 行"防 stub"归置说明）——非实现缩水标记；
- ② 公共接口空实现：EventBus.hpp 零 inline 业务体（实现全在 .cpp）；Scheduler.hpp 仅常量/配置结构与接口声明；无"空函数体/仅返回默认值"的契约接口；
- ③ acceptance 逐条证据表见 4.4——五条全部具名测试＋实现行号双承载，零"已实现"裸声明；
- ④ 测试强度：断言校验具体状态词/稳定码/逐帧全等/有界等待，非恒真；ManualClock 虚拟推进＋waitForIdle＋future 有界等待替代 sleep；与 4.5 联动双探针实证会红。

## 发现的问题清单

**阻断级：无。**

**建议级（已按 ACC §5 转登 traceability/findings.json，编号顺延至 F-212/F-213）：**

1. **F-212**：`EventBusDeliveryContractTest.SamePublisherFifoPreservesPublishOrder` 用 1000 帧同值事件，断言面为"零丢失＋事件 id 唯一"，未直接断言投递序与发布序对应（文件内注释自证：序由 NineStateStream 用例的异值流逐帧全等钉住）。标题承诺 FIFO 而断言承载在另一用例——建议后续给该用例逐帧可区分载荷（如 attempt 序号递增）使序断言自持。不阻塞（序语义已被更强断言覆盖＋4.5 探针证明异值流会红）。
2. **F-213**：`SchedulerEventBusTest.ProgressThrottledToTenHz_D07` 只断言终态可见帧为 t200——该断言无法区分"窗内帧丢弃"与"窗内帧延迟到窗外应用"两种实现（两者终态帧相同）。节流丢弃语义现由实现审阅承载（reportProgress 只把放行帧入 m_pendingProgress）。建议补一条中间时刻断言（如 t+50ms tick 后可见帧仍为 t0）钉住"窗内帧不可见"。不阻塞（契约要求为对外频率上限，现断言已覆盖 ≤10 Hz 可见面）。

## 裁决

**verdict = pass**：4.1~4.11 全部通过，零阻断级问题；建议级 F-212/F-213 已转登 findings.json，不阻塞合入。送验对象 e28dc691696334083deb97e73c09e8bbd69c7e2c 可提交所有者进入合入决策（PIPE §4.7）。

## 验收者环境说明

- Windows 10（10.0.26200）x64、Git Bash、MSVC 2022（17.12.6）、CMake 4.3、Qt 6.11.1 msvc2022_64、vcpkg（主仓经典模式，gtest 1.18.0）；
- 复现现场：`D:/10_Source_Repos/21_robot/tmp-acc-ex05/wt-a`（detached @ e28dc691，集成树 build/＋冒烟树 ../smoke）；记录回传：`D:/10_Source_Repos/21_robot/tmp-acc-ex05/wt-b`（分支 acc/EX-T05/1）；
- 亲手执行命令与输出文件（验收会话临时区，tmp-acc-ex05/）：configure.log、integration-build.log（全量构建 F-008 失败点留证）、verify-target-build.log、verify-contract-build.log、run-test/contract-{integration,smoke}.txt、smoke-configure.log、smoke-build.log、my-gates.log、fi1/fi2-build.log、fi2-restore.log；
- ird_gates 复跑口径：exit 1＝既有登记例外（与送验留痕一致）；命中集合与 base（wp08-t04/gates-ird-gates-target.log）排序 diff 为空、"ird_execution" 字样 0 命中——零新增。
