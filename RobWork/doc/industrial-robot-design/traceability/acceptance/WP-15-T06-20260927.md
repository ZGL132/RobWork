# WP-15-T06 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | WP-15-T06 实现区域采样与覆盖率评估器（kin.region-coverage） |
| 契约 | tasks/foundation/WP-15-T06.json（status=ready，dependsOn WP-15-T04） |
| 送验对象（冻结） | `2df969fb5601d1df264abb3f43f45d2a801981bc`（单提交；分支 wp15-t06 不参与复现） |
| diff 范围 | `0b8bcd734b8283b0c286af8cd16af50f79306c2f..2df969fb5601d1df264abb3f43f45d2a801981bc`（state.base..headSha） |
| **verdict** | **pass**（4.1~4.11 全部通过；无阻断级问题；建议级 3 项已转登 findings.json：F-395/F-396/F-397） |
| 验收环境 | Windows 10 x64；VS 2022 x64；Qt 6.11.1 msvc2022_64（CMAKE_PREFIX_PATH）；vcpkg 经典模式（仓库根，installed/x64-windows）；pwsh 7。复现现场＝`git worktree add --detach` @ 2df969fb（送验 SHA，非分支名）；worktree 缺 gitignored `RobWork/RobWorkStudio/bin/RobWorkStudio.ini.template.static`（及 .shared.in），已从主检出**只读复制**后 configure（T15/T16/T02~T05 实录同型现象）。构建前 tasklist 无 RobWorkStudio.exe/msbuild 残留进程；主检出零写操作（一切写操作在自建临时 worktree）。 |
| 证据目录 | `traceability/builds/wp15-t06-acc/`（本 evidence 分支，28 件：两模式 configure/build 日志、gtest XML×6、运行日志、门禁双侧直跑日志＋路径归一化直方图、变异测试 4 日志、还原复绿 5 件、validate 两日志） |

## 4.1 改动范围 —— PASS

- `git diff --name-only 0b8bcd73..2df969fb` 实测 25 文件：kinematics 下 8 个（CMakeLists.txt＋新增 Sampling.hpp/.cpp、Coverage.hpp、Evaluators.hpp/.cpp 追加、两测试用例文件）＋`traceability/builds/wp15-t06/` 16 件＋`units/kinematics.md`——全部落在契约 allowedFiles 三模式内。
- forbiddenFiles（REQUIREMENTS.md/ARCHITECTURE.md）零触碰（diff 文件清单实证）；diff 内无 `patches/` 新文件＝SA-02 无未登记框架改动（框架源码零触碰）。

## 4.2 可复现构建 —— PASS

- **集成模式**（全新构建目录＋worktree A 源树）：`cmake -S <worktreeA>/RobWork -B <acc>/build_int -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=<repo>/vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64` → 配置成功 0 error（integration-configure.log）；缓存 grep 实证 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`；`cmake --build build_int --config Release --target sdurws_ird_kinematics sdurws_ird_kinematics_test sdurws_ird_kinematics_contract_test` → **exit 0，`error C`/`error LNK` 计数 0**（integration-build.log）。
- **独立冒烟模式**（另一全新目录）：`cmake -S <worktreeA>/RobWork/RobWorkStudio/src/rwslibs/industrialrobot -B <acc>/build_smoke`（vcpkg toolchain＋Qt6 prefix 在案——CORE-T01 G-2 口径）→ 配置/全量构建 **exit 0、0 错误**（smoke-configure.log/smoke-build.log，含 `sdurws_ird_kinematics.lib` 与两测试 exe 产物行 346/355/367）。

## 4.3 契约 verify 命令 —— PASS（3/3 逐条亲测）

| 契约 verify 行 | 验收者执行 | 结果 |
| --- | --- | --- |
| `cmake --build build --config Release --target sdurws_ird_kinematics` | 验收者自建集成树逐字执行（目标名一致） | exit 0，产物 `sdurws_ird_kinematics.lib`（verify1-build-product-lib.log） |
| `ctest --test-dir build/RobWorkStudio/src/rwslibs/industrialrobot/kinematics -C Release -L "^ird$"` | 同上 | **2/2 passed**（`100% tests passed, 0 tests failed out of 2`，Label `ird`＝2 tests） |
| `pwsh -File RobWork/scripts/industrialrobot/validate-docs.ps1` | worktree A 内执行 | **PASS（20 units / 12 trace / 189 task files）**（validate-docs.log） |

- 契约 verify 数组缺两测试目标构建行（F-300/F-358/…/F-381 家族在本契约的新实例，见 F-397）——验收者在全新构建树自行补建 `sdurws_ird_kinematics_test`/`sdurws_ird_kinematics_contract_test` 并执行（4.2 集成构建三目标行），属验收者侧正常补全动作。
- 另执行 `validate-task.ps1 -TaskFile <WP-15-T06.json>` → **PASS**（validate-task.log）。

## 4.4 acceptance 条目逐项证据 —— PASS（5/5，均验收者亲测）

| # | 契约 acceptance | 证据（具名测试＋验收者实测） |
| --- | --- | --- |
| 1 | 固定计数 100/60 双口径、整数计数无浮点容差（分母＝计划样本总数） | `KinRegionCoverage.FixedCountGoldenReach60_WP15T06_ACC1`：planned/reached＝位置 100/60、位姿 100/60（RegionCoverageTest.cpp:354-362）；`PositionExistenceVsPoseGlobal_WP15T06_ACC1`：位置 60/100（存在性）与位姿 60/200（全局口径 D=2 方向反例）分离实证（:404-409）。CoverageResult **无比率字段**（Coverage.hpp:105-128 全文核查——结构保证「绝不输出 0%/100%」）。两用例在两模式均 PASS。 |
| 2 | 降级保留分母＋零样本→DataInsufficient＋KIN-COVERAGE-ZERO-SAMPLES、绝不 0%/100% | `DataInsufficientKeepsDenominatorDowngrades_WP15T06_ACC2`：planned 保持 100、reached 50、downgraded=true（分母守恒——computeCoverage 守恒式自检 Sampling.cpp:454-466）；`ZeroSampleEvaluateDiagAndMaterial_WP15T06_ACC2`：{0,5,4} 乘积 0→`KIN-COVERAGE-ZERO-SAMPLES` 诊断（注册码常量断言，禁拼码）＋载荷标记 downgraded=1/两轴 defined=0＋证据行仍在（:446-487）。诊断码在 DiagCodes.hpp:113/116 注册（§9.6 行 13/14）。**变异 A 实证**：篡改 downgraded 计算（去掉 DataInsufficient 子句）→ 两具名用例真实变红（mutationA-run.log）。 |
| 3 | sampleSetIdentity＝SHA-256(planContentIdentity‖预算/种子 canonical)（evidence §4.1.4）；对账不一致→KIN-SAMPLE-IDENTITY-MISMATCH→证据缺失，绝不沿用 | `SampleSetIdentityFormulaAndDeterminism_WP15T06_ACC3`（测试侧独立组装 canonical 字节复算＋同输入稳定＋异 seed 必异）＋`IdentityMismatchDropsEvidence_WP15T06_ACC3`（三变体：篡改身份/快照无 ref＝未冻结/篡改分母→均 KIN-SAMPLE-IDENTITY-MISMATCH＋零 payload＋零证据行；对照 matched→正常产出，:548-597）；对账**先于生成**（Evaluators.cpp evaluate 步骤 1 对账：2137-2193 → 步骤 2~5 才 runRegionCoverageComputation）。契约侧 `IdentityReconciliationContractTrack_WP15T06_ACC3` 同轨。**变异 B 实证**：身份公式 canonical 魔数写入字节数 7→8 → ACC3 公式用例真实变红（mutationB-run.log）。⚠ 登记布局与实现的 1 字节偏差见 F-395。 |
| 4 | 复评不增删更换样本：同 (plan,budget,seed) 同集同序（黄金锁定）；sampleIndex 双射；取消 partial→NotRun 保留、重跑同集 | `GoldenGeneratorsAndSameOrderRerun_WP15T06_ACC4`：Grid 体心（格心 0.25/0.75 独立参考）、斐波那契螺旋（独立公式复算 1e-15）、roll 均分（−π/2/＋π/2）、Random 同 seed 同序列/异 seed 异序列＋重跑逐位对齐（:605-654）；`SampleIndexAlignsDenominatorBijection_WP15T06_ACC4`：双射核查（缺项/重复→logic_error；computeCoverage Sampling.cpp:411-424）；`CancellationPartialNotRunRerunSameSet_WP15T06_ACC4`：取消→NotRun 保留、重跑同集 reached 60 复原；`DeterminismPayloadBitwise_WP15T06_ACC4`（契约）：payload 逐位一致。 |
| 5 | 逐样本评估接线：IK 位置/位姿分流＋碰撞缺检测器 DataInsufficient＋镜像独立计数＋collision-models Conditional | `PerSampleIkWiringPositionVsPose_WP15T06_ACC5`（位置样本姿态容差 π rad 落值 Evaluators.cpp:2369-2377、位姿样本查询容差）；`CollisionDemandWithoutDetectorDataInsufficient_WP15T06_ACC5`（要求在场缺会话→该样本 DataInsufficient＋collisionNotEvaluated＋整体降级；无要求无会话→不降级对照，:799-846——KIN-05 承接）；`MirrorPlanSamplesIndependent_WP15T06_ACC5`（镜像计划独立计数、分母相加）；契约 `DescriptorMatchesSection43Row_WP15T06_ACC5`（九条依赖含 collision-models Conditional——Evaluators.cpp:2586-2589，无碰撞布尔开关）。 |

测试计数留痕（验收者亲测，与送验宣称一致）：集成 62/62＋43/43（gtest XML `tests="62" failures="0"`/`tests="43" failures="0"`；T06 具名 12＋9 条）；冒烟同数 62/62＋43/43；ird-test-report.json summary 62/62（12 条 WP15T06）、ird-test-report-contract.json 43/43（9 条 WP15T06）——**计数三源（控制台/XML/报告）一致**。测试文件 TEST 宏计数实证：RegionCoverageTest.cpp=12、RegionCoverageEvaluatorContractTest.cpp=9、IkTest.cpp=18、TaskPointsBatchEvaluatorContractTest.cpp=14。

## 4.5 测试真实失败能力 —— PASS（2 处实质变异，全部真红，已还原）

| 变异 | 内容 | 结果 |
| --- | --- | --- |
| A（降级保留分母语义） | Sampling.cpp computeCoverage 的 `downgraded` 计算删除 `dataInsufficient>0` 两子句（保留零样本 defined 子句） | 重建后 `DataInsufficientKeepsDenominatorDowngrades_WP15T06_ACC2` 与 `CollisionDemandWithoutDetectorDataInsufficient_WP15T06_ACC5` **双双 FAILED**（mutationA-build.log/mutationA-run.log，exit 1） |
| B（样本集身份公式） | Sampling.cpp:149 canonical 魔数写入 `magic + 7` → `magic + 8` | `SampleSetIdentityFormulaAndDeterminism_WP15T06_ACC3` **FAILED**（mutationB-build.log/mutationB-run.log，exit 1）——同时实证该用例钉住的是实现侧 7 字节布局（见 F-395） |
| 还原 | 两处均 `git checkout -- src/Sampling.cpp` 还原（`git status --porcelain` 计数 0），重建三目标后全量复跑 | **62/62＋43/43 复绿**（final-restore-rebuild.log、final-test-run.log、final-contract-run.log、final_*_test.xml）。变异窗口与冒烟构建时序隔离（冒烟先行完成——T14 教训兑现）。 |

## 4.6 架构红线 —— PASS（八条自查）

1. **L2 零 Qt**：新增/改动产品文件（Sampling.hpp/.cpp、Coverage.hpp、Evaluators.hpp/.cpp）include 面零 Qt 头（grep 实证）；产品测试 KinBuildRedLine.ProductFaceHasZeroQtIncludes 在 62 例中 PASS。
2. **业务域互链（R-1）**：新文件 include 仅 core/evidence/runtime（登记边）＋本单元公共头＋rw/math；grep 非{kinematics,core,evidence,runtime,policy,execution,diagnostics}的 sdurws include 零命中；req 对象 schema 以宿主解析投影值（SamplingPlan 等）承载，未 include requirements 单元任何头。
3. **跨单元私有头（R-2）**：全部 include 走 `include/sdurws/ird/<unit>/` 公共面。
4. **框架零源码修改（SA-02）**：4.1 已证（无 patches 新文件、框架源码零触碰）。
5. **R-4 名称前缀**：ird_gates R4 命中 base/head 均 3 条（同项目存量），零新增（直方图一致）；产品面 RobWork 字面量测试 PASS。
6. **权威唯一（PA-1）**：覆盖率计算唯一实现点 computeCoverage（Sampling.cpp:407）；评估器只产素材不裁定（downgraded/incomplete 为素材标记，判定归 evidence——Coverage.hpp:96-101 注释与 §7.2 一致）；单位/诊断码经 DiagCodes 注册常量（§9.6 行 13/14），无拼码。
7. **不可变历史（PA-2）**：本任务无修订写路径触点（纯计算与证据素材）。
8. **不继承 old/ 实现**：新增文件全部从需求/卡面实现，无 old/ 路径引用。

## 4.7 注释规范 —— PASS（公共头全查＋实现文件抽查 ≥3）

- **公共头全查**：Sampling.hpp（785 行）/Coverage.hpp（170 行）文件头 Doxygen 齐备（设计依据逐条含需求 ID/卡章节/契约 acceptance；背景说明把五值词表映射口径、无比率字段的结构保证讲透）；类/结构体逐个注明业务概念、值语义、线程安全；函数逐个 @brief/@param 方向＋单位（m、rad、无量纲）/取值范围/@return/@throws；§2.5 高危信息实测在位——物理单位（RegionBox 单位 m、kFibonacciGoldenAngle/kPi rad 字面锁定）、坐标系（基座系 {B} 显式标注）、角度制式（rad 显式）、确定性来源（splitmix64 state=seed·2³²＋k、黄金角字面锁定、枚举序 x 最慢 z 最快）、所有权（视图非 owning）、错误语义（装配期 fail-fast vs 评估期结构化诊断分轨）。Evaluators.hpp T06 表尾段（:764-1047）同规范。
- **实现文件抽查**：Sampling.cpp（生成黄金逐步讲解＋分支边界注释＋magic 数字来源——黄金角 2.39996322972865332223＝π(3−√5) 注明）、Evaluators.cpp T06 段（:1874-2607，评估主流程逐步编号＋每分支业务场景＋KIN-05 承接说明）、RegionCoverageTest.cpp/RegionCoverageEvaluatorContractTest.cpp（用例名带 WP15T06＋ACC 编号，IRD_TEST_INFO 需求/AT 追溯）——全部合格。
- 两处注释/登记与实现不符的**建议级缺陷**（非缺失必填项，按 §2.6「过期/错误注释」反面例转登）：**F-395**（sampleSetIdentity canonical 魔数 8 字节登记 vs 7 字节实现——见 4.8）、**F-396**（generateDirections 头注「N=1 退化 +Z」实为 +X）。

## 4.8 文档同步 —— PASS（含 findings 相关性核对＋F-394 督办兑现核查）

- **单元卡 v0.6 落位登记**：§14.6 v0.6 行（:981）六部分登记（①Sampling 值模型＋黄金＋sampleSetIdentity canonical 布局、②Coverage 双口径六计数＋IRDCV01 载荷布局、③评估器九条依赖/主流程/样本状态映射登记〔含 NotApplicable 覆盖缺口显式登记——F-146 家族口径〕、④测试 12＋9 计数与证据、knownPitfalls O-37/O-38/P-KIN-3/P-KIN-7 处置、F-394 兑现）；卡头版本行/日期行同步 v0.6（:7-8）；§3.3 布局表/§4.3 行/§11 T06 行（:843 完成注记）/构建落位行（:15 T06 公共面）均已同步。实现与登记逐项对照一致（评估键 kebab `kin-region-coverage` 随卡登记、载荷 token 点形保留、checkpointGranularity=Sample、Phase="solve-sample"、Rotation3D 外联符号手写矩阵积规避随卡登记——Sampling.cpp:300-311 实证）。
- **测试计数类散文与实测一致**（12/9/18/14 四处与 TEST 宏计数逐一吻合——含下条督办兑现后的纠正值）。
- **F-394 督办兑现核查（三处纠正全部落卡）**：①v0.3 ⑧句（:982 行内）已由『零新增命中』改为『＋1 项 sanctioned 命中（Eigen PRIVATE——O-40；附件同目录）』（F-387 消账）；②v0.4 ⑥句（:979 行内）已由『KinIk 15 新用例』改为『18 新用例』并补列用例名（实测 IkTest.cpp TEST 计数=18 吻合——F-390 消账）；③v0.5 ⑤句（:980 行内）已由『12 新契约用例』改为『14 新契约用例』并拆分枚举（线程数 1↔4 等价与分片连续区间两项分列；实测 TaskPointsBatchEvaluatorContractTest.cpp TEST 计数=14 吻合——F-393 消账）。状态翻转（open→fixed）沿 F-394/F-380 先例留治理收尾簿记，本记录为兑现凭据。
- **findings.json 未关闭条目相关性核对**（worktree A 检出，open 共 324 条）：与 kinematics 相关的 F-380（DiagCodes 散文计数——处置已随 T05 兑现、状态翻转待治理）、F-383（evidence §9 增补义务——本任务未触及 evidence 卡）、F-386（KIN-NO-DEVICE 消费面——未触及）、F-389（KIN-RESIDUAL-EXCEEDED 自然触发用例——本通道逐样本走 IK 内部残差复验面，未触及该比较型诊断生产面）、F-391（去重反范数数据——未触及）、F-382（kinematics 两测试目标 SUB 存量命中——本任务未新增门禁命中，见 4.2 门禁比对）均与本任务改动面无交集，维持 open 无静默积压；F-387/F-390/F-393/F-394 随本任务兑现（见上）。**建议级转登 3 项**：F-395/F-396/F-397（见 findings.json，编号自 F-394 后顺延）。
- 契约状态/追踪矩阵/编排者 state.json 簿记属收尾面（PIPE §4.7），不在送验对象义务内（与 T02~T05 口径一致）。

## 4.9 commit 质量 —— PASS

- 标题 `[WP-15-T06] 实现区域采样与覆盖率评估器 kin.region-coverage`——任务前缀＋动词开头中文摘要合规。
- 正文四段齐备：What（按文件列要点）/Why（KIN-04 R3/R8＋EVI-01＋knownPitfalls 处置）/验证（构建、62/62＋43/43、ctest 2/2、validate 双 PASS、门禁零新增）/影响面（仅新增＋表尾追加＋CMake 注册）。**验证结论与验收者亲测逐项一致、无粉饰**（计数/门禁/validate 全部对上；Acc-Covered: 1~5 与契约 acceptance 对应）。

## 4.10 留痕完整 —— PASS

- 送验提交内 `traceability/builds/wp15-t06/` 16 件留痕实测在位：两模式构建/运行日志、gtest XML×2（tests=62/43 failures=0 原样）、ird-test-report 双份（62/62＋43/43）、ird_gates 双侧日志＋归一化直方图＋比对附件、base-head.diff。验收者侧复现留痕 28 件落 `traceability/builds/wp15-t06-acc/`（本 evidence 分支）。
- DTB WP-15-T06 行（:392）在案；任务完成状态随收尾簿记（PIPE §4.7）由编排者执行——与 T02~T05 既有口径一致。

## 4.11 偷懒/缩水实现扫描 —— PASS

1. TODO/FIXME/stub/placeholder/未实现/not implemented 扫描：diff 全部 8 个源码文件零命中（grep 实证）；无契约标注「触发式延后」豁免事项。
2. 空实现检测：新增公共接口（generateSampleSet/computeCoverage/sampleSetIdentity/生成器四函数/WorkspaceSampler 三方法/工厂）函数体全部为实质实现（4.4/4.5 的变异测试反证测试与实现均非恒真/空壳）；无仅返回默认值的桩。
3. 逐条证据表：见 4.4——acceptance 1~5 每条均有具名测试＋验收者实测输出，任一条无证据即 fail 的情形未发生。
4. 测试强度：断言校验行为与边界（黄金数值独立复算、双射违例 logic_error、对账三变体、取消 watermark、载荷逐字节解析、禁拼码断言），4.5 双变异真红实证非恒真。

## 裁决

**verdict＝pass**：4.1~4.11 全部通过，无阻断级问题。建议级 3 项（不阻塞合入）已按 ACC §5 强制转登 `traceability/findings.json`：

- **F-395**：sampleSetIdentity canonical 布局登记（hpp:450「"IRDSSID1" 8 字节 ASCII magic」＋单元卡 §14.6 v0.6「"IRDSSID1"8B」）与实现（Sampling.cpp:149 `magic + 7` 实写 7 字节 "IRDSSID"）存在 1 字节偏差；测试黄金复算以同款 `magic + 7` 镜像实现而其注释宣称复算 "IRDSSID1"；变异 B 实证用例钉住 7 字节布局。风险面＝P-KIN-3 三方一致义务的对齐基准失真（evidence 冻结按登记实现将得到不同摘要→运行期对账显性失败——安全网在，但登记义务本身须纠正）。建议：下次触及统一为「7 字节 magic "IRDSSID"＋u32 codecVersion」口径（与 IRDPM01/IRDIK01/IRDBP01/IRDCV01 七字符惯例一致、不改已交付摘要值）或实现改 8 字节并同步测试黄金与卡面；**evidence 冻结 canonical 格式前必须消账**。owner：kinematics 实施会话＋evidence 卡冻结会话。
- **F-396**：Sampling.hpp:529 generateDirections 函数注「N=1 时退化为 +Z 单方向」与实现公式不符（z₀=1−1/1=0、r=1、φ=0→**+X**；Sampling.cpp:229 实现处注释正确）——公共头注释方向错误（AGENTS §2.6 错误注释反面例）。建议下次触及纠正为 +X。owner：kinematics 实施会话。
- **F-397**：tasks/foundation/WP-15-T06.json verify 数组仅 3 行（产品库构建＋ctest＋validate-docs），缺两测试目标构建行——F-300/F-358/F-367/F-368/F-371/F-373/F-375/F-381 家族新例（验收者已自行补建执行，62/62＋43/43 实测）。建议后续契约治理批量补齐该家族。owner：契约编译/治理会话。

## 附：验收者侧与实施侧证据的口径差异说明

- 实施留痕门禁比对（wp15-t06/ird-gates-base-head-compare.md）宣称「直方图 145 行、去重条目 69、引擎自计 69」——验收者复测（独立 base worktree @0b8bcd73 临时检出＋head worktree，`cmake -DIRD_ROOT=... -DIRD_SELFTEST_DIR=... -P cmake/ird_gates.cmake` 引擎直跑）：双侧原始命中行 69/69、去重后唯一条目 69/69（每条计数 1）、引擎自计 69/69，路径归一化直方图 **base..head 完全一致＝零新增零消除**（ird_gates_base.log/ird_gates_head.log/base-hits-pathnorm.norm/head-hits-pathnorm.norm）。实施附件的「145 行」为其日志全文行数口径（含引擎横幅与自测行），与本验收 69 命中口径不矛盾（三套计数同注：69 原始/69 去重/69 引擎自计）。ird_gates exit 1＝存量 69 命中既有状态（DTB §4.5 存量例外未清零，非本任务新增）。
- 本机代理（127.0.0.1:17890）不稳定：记录分支推送采用显式 refspec `git push origin HEAD:refs/heads/acc/WP-15-T06/1` 并以 `git ls-remote` 核实远端 SHA 一致。
