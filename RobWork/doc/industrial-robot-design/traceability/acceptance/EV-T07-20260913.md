# EV-T07 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | EV-T07 实现 evidence 结果包络（Envelope）——契约 tasks/foundation/EV-T07.json |
| 送验对象（冻结 SHA） | 3e4e099c8c1792b79da3f4b7b15a306f6b2e4ca0（分支 wp05-t07 尖端） |
| diff 范围 | cb15319f8d7d6d7bfe839c695132fe18efd1584b..3e4e099c8c1792b79da3f4b7b15a306f6b2e4ca0（3 提交：32a90593 实现 / 18c9cea8 留痕 / 3e4e099c 单元卡登记） |
| verdict | **pass**（4.1～4.11 全部通过，无阻断级问题；建议级 1 条已转登 findings F-064） |
| 验收日期 | 2026-09-13 |
| 记录分支 | acc/EV-T07/1（本文件所在提交） |

## 独立性声明（ACC §2）

本验收为全新上下文会话：未消费实施者任何过程叙述/辩护文本，仅消费任务契约、设计文档（units/evidence.md §7/§11/§12）、送验 diff 与留痕产物；全部构建与测试由验收者亲手在 ACC §3 规定复现现场（detached worktree @SHA）重跑，未采信实施者运行结果；检查以"为不通过找证据"的对抗式立场执行（含 3 个独立设计的失败/冗余探针）。派发模式＝PIPE §0.4 子代理（无需降级声明）。

## 验收环境

- 复现现场：`git worktree add --detach %TEMP%\ird-acc-ev-t07-a 3e4e099c…`（送验 SHA 检出，4773 文件）。
- 集成模式：worktree A 内重新配置（`-DRWS_BUILD_INDUSTRIALROBOT=ON`，grep CMakeCache 实证 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`，`CMAKE_TOOLCHAIN_FILE`＝vcpkg 绝对路径、`CMAKE_PREFIX_PATH`＝D:/software/Qt/6.11.1/msvc2022_64——主树缓存同参数）；两件 git 未跟踪 ini 模板（RobWorkStudio.ini.template.static / .shared.in，F-007/F-062 登记的 .gitignore [Bb]in/ 吞没效应）自主树复制后配置成功（exit 0）。
- 冒烟模式：独立临时目录 `%TEMP%\ird-acc-ev-t07-smoke`，`-DCMAKE_TOOLCHAIN_FILE=D:\10_Source_Repos\21_robot\RobWork\vcpkg\scripts\buildsystems\vcpkg.cmake`（绝对路径），配置 exit 0。
- ird_gates 回主仓库只读复跑（主仓工作树 HEAD＝cb15319f＝送验 base，industrialrobot 源码无未提交修改——命中所指目标均先于本任务存在，反证"既有"成立）。
- 验收环境特有警告（非送验对象问题）：worktree A 建于 %TEMP% 下，MSBuild 报 MSB8029（中间目录位于临时目录可能影响增量生成）——本验收为一次性全新构建，无增量复用，结论不受影响。

## 逐项证据（ACC §4，4.1～4.11）

### 4.1 改动范围 — 通过

- `git diff --stat cb15319f..3e4e099c`＝27 文件（4345 insertions, 3 deletions）：evidence/CMakeLists.txt（+40/-3）、evidence/include/sdurws/ird/evidence/Envelope.hpp（新 525 行）、evidence/src/Envelope.cpp（新 492 行）、evidence/test/EnvelopeContractTest.cpp（新 338 行）、evidence/test/EnvelopeTest.cpp（新 716 行）、traceability/builds/wp05-t07/ 21 件留痕、units/evidence.md（+2/-1）。
- 逐文件对照契约 allowedFiles：`evidence/**` ✓（5 个文件）、`units/evidence.md` ✓、`traceability/**` ✓（21 件留痕）——**全部在 allowedFiles 内**；`industrialrobot/CMakeLists.txt` 未触碰（仅 evidence 子目录 CMake）。
- forbiddenFiles（REQUIREMENTS.md / ARCHITECTURE.md）：diff 零触碰 ✓。
- SA-02：diff 内无 `patches/` 文件、无框架源码改动（`git diff --name-status` 全清单核对）✓。

### 4.2 可复现构建 — 通过

- 集成模式（全新 worktree 从零配置＋构建——F-008 判例口径下任务目标面）：配置 exit 0（缓存 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON` grep 实证）；`cmake --build build --config Release --target sdurws_ird_evidence_test sdurws_ird_evidence_contract_test -j` exit 0 零 error（`grep -cE "error C[0-9]+|: error"`＝0；产物 build/RobWorkStudio/bin/Release/ 两测试 exe 落位）。
- 冒烟模式：配置 exit 0；`cmake --build /tmp/ird-acc-ev-t07-smoke --config Release -j` exit 0 全量零 error（core/evidence/policy/runtime/testkit 五单元库＋全部测试目标从零编译，8 个 exe 落位）。
- 实施侧留痕对照：integration-cache-check.txt＝`RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`；ird-test-report.json notes 按 F-036/F-054/F-055 纪律显式区分"集成＝仓库根增量树／冒烟＝全新独立树，配置 Release"。

### 4.3 契约 verify 命令 — 通过

契约 verify 数组仅一条：`cmake --build build --config Release --target sdurws_ird_evidence_test`——验收者在 worktree A 逐字执行，exit 0、error 计数 0。

### 4.4 acceptance 条目逐项证据 — 通过（2/2）

| # | 契约 acceptance 条目 | 证据 |
| --- | --- | --- |
| 1 | 表 3 全组合矩阵用例通过（EV-ENV-1/2）：非法组合逐一构造拒绝、Preview 构造边界拒绝 | EnvelopeTest.cpp 26 用例逐一对应：合法四格＝CompletedFeasibleConstructsAndComputesPayloadDigest（L333，含载荷摘要与手工 SHA-256 重算逐字节比对）、CompletedEngineeringInfeasibleWithValidProof（L364，validateProof 字级通过）、CompletedEngineeringInfeasibleWithMustRecordOnly（L388）、CompletedDataInsufficientWithFullMissingList（L405）、CanceledDiagnosticOnlyEnvelopeKeepsReusableFalse（L421，EV-ENV-2）、FailedAndInterruptedDiagnosticOnlyConstruct（L437）；非法格逐一拒绝＝PreviewRejectedUnconditionally（L456）、PreviewIsFirstError（L470，首错序）、CompletedNotApplicable（L487）、CanceledFeasible（L497）、DataInsufficientEmptyMissing（L506）、FeasibleWithMissing（L517）、EngineeringInfeasibleWithoutBasis（L527）、FailedWithPayload（L537）、CanceledWithSatisfiedEvidence（L547）、CanceledWithMissingItems（L560）、PartialDataReusableTrue（L569，EV-ENV-2）、TaskIdentityInvalid（L578）、EvaluationKeyInvalid（L588）、InputIdentityZero（L598）、CompletedManifestNotCoherent（L608）＋快照绑定面 3 例（L622/L645/L665）＋确定性 2 例（L688/L710）；每例经 expectRejected 断言稳定错误码 EnvelopeIllegalCombination＋消息含字段名。实测：集成 133/133＋冒烟 133/133（XML 顶格 tests="133" failures="0"，送验两份 XML 与验收者独立 XML 同值） |
| 2 | CR-01：仅消费 core Evaluation 词表（无本地枚举定义）；_contract_test 断言 token 字面量与 core::toToken 输出一致 | 新目标 sdurws_ird_evidence_contract_test（§11 跨单元契约面首次落地）5/5 两模式通过：TokenLiteralsEqualCoreToTokenOutputs（ContractTest L225，三词表 11 值逐一 EXPECT_STREQ）、TokenRoundTripThroughCoreParsers（L250）、EnvelopeTokensFlowThroughCoreVocabulary（L273，包络承载枚举回流）、NoLocalVocabularyEnumDefinitionInProductSources（L300，include/＋src/ 源码扫描）、EnvelopeHeaderConsumesCoreEvaluationHeader（L329，include 面断言）；另有 6 组 static_assert（L99-117）编译期钉死包络三轴字段类型＝core 词表类型。验收者 grep 复核：产品源码 `enum class` 仅 EnvelopeIssueCode（校验问题观测码——非三轴词表）与 ProducerProcess（产出位置——设计未定名类型），mode/outcome/engineeringStatus 三轴字段类型全部为 core:: 枚举 |

ird_gates 自查（AGENTS §6.3，实现类任务——本任务实际执行，非"未执行"标注）：验收者主仓实测 `cmake --build build --config Release --target ird_gates`——命中 3 项均为既有（IRD-GATE-SUB sdurws_ird_runtime_test→sdurws_ird_testkit＝F-051；IRD-GATE-SUB testdata_lint→testkit 表外边＋IRD-GATE-T1 testdata_lint 链 testkit＝F-019），**evidence 面零新增命中**；引擎自测 9 项（pass_clean/pass_r4comment/fail_r1/fail_t1/fail_r5/fail_sub/fail_r3/fail_r4/fail_t2）全部符合预期；exit 1 系既有命中所致，与送验 ird-gates.log 及提交声明（F-019 两项＋F-051 一项——按 F-060 教训逐条归属、不笼统归并）一致；主仓基线（不含本任务代码）同样 3 处命中，反证非本任务引入。

### 4.5 测试真实失败能力（验证验证者）— 通过（探针 2 个，均为验收者独立设计）

- **探针 1（Preview 构造边界/表 3 行 4）**：将 Envelope.cpp L137 `if (draft.mode == core::EvaluationMode::Preview)` 注入 `false &&` 短路（＝Preview 放行，表 1 禁止形态）→ 重构建 exit 0 → `EnvelopeMatrix.PreviewRejectedUnconditionally_EV_ENV_1` 与 `EnvelopeMatrix.PreviewIsFirstErrorAmongMultipleViolations_EV_ENV_1` **2 用例变红**（FAILED 输出在案）。
- **探针 2（EV-ENV-2 复用锁）**：还原探针 1（`git checkout --`＋`git diff --stat`＝0 复核）后将 L184 `if (draft.partialData.has_value() && draft.partialData->reusable)` 注入 `false &&`（＝reusable==true 放行，"恒 false"锁失效形态）→ 重构建 exit 0 → `EnvelopeMatrix.PartialDataReusableTrueRejected_EV_ENV_2` **变红**。
- 全部还原（`git status --porcelain -- RobWork/`＝0、"false &&" grep 计数 0）后重构建复绿 **133/133＋5/5 PASSED**（验收者独立 XML：/tmp/acc-ev-t07-evidence-int.xml、/tmp/acc-ev-t07-contract-int.xml）。两探针分别钉住 acceptance 1 的两个断言族，证明非恒真。
- **探针 3（冗余 include 验证，4.7 卫生面）**：从 Envelope.hpp 删除 `#include <sdurws/ird/evidence/Slice.hpp>` 后双目标仍零错误编译（exit 0）——实证该 include 冗余（转登 F-064），随即恢复。

### 4.6 架构红线（AGENTS.md §5 八条）— 通过

| 红线 | 证据 |
| --- | --- |
| 1. L2 计算内核零 Qt | `grep -rn "Qt" evidence/include/ evidence/src/` 零命中；CMakeLists 无 Qt 链接（仅 core＋GTest 于测试目标） |
| 2. 业务域互链禁止（R-1） | 送验 4 文件 `#include <sdurws/ird/...>` 面仅 core（DiagData/Digest/Evaluation/Identity）与 evidence 自身公共头；无 modeling/kinematics 等业务域单元；CMake 链接面 sdurws_ird_evidence 仅 sdurws_ird_core（提交声明与 CMake diff 一致） |
| 3. 跨单元私有头禁止（R-2） | 全部 include 走 `include/sdurws/ird/<unit>/` 公共头路径；契约测试目标链 sdurws_ird_evidence（本单元产品库＋GTest::gtest_main——T-1 白名单允许形态） |
| 4. 框架零源码修改（SA-02） | 4.1 已证：diff 无框架文件、patches/ 无新登记 |
| 5. 不做名称前缀拼接/剥离（R-4） | `grep "RobWork"` 于 src/Envelope.cpp 与两测试文件仅命中文档注释（无代码级拼接/剥离） |
| 6. 权威唯一（PA-1） | 三轴词表归 core（CR-01 三防线）；Must 违例识别复用 EV-T06 kDiagMustViolation 码面不私造 token（Envelope.cpp L118-123 注释明示"码值权威归 diagnostics"）；工况内容不解释（"域事实，PA-1"，Envelope.hpp CaseScope 注释）；缓存判定面不越权（EV-ENV-2 的 judgeCacheHit 半边显式声明归 EV-T09——§12 依赖序） |
| 7. 不可变历史（PA-2） | ResultEnvelope 无 setter，make() 唯一生产者（SA-13 构造边界；operator== 注释"历史包络按 §7.2 永不回写"）；确定性用例 MakeFromEqualDraftsGivesEqualEnvelopes 钉住可复算性 |
| 8. 不继承/恢复 old/ | diff 无 old/ 路径文件；实现全部从设计文本新写（单元卡 v0.8 落位登记逐条对照） |

### 4.7 注释规范（AGENTS.md §2）— 通过

- **公共头全查（Envelope.hpp，525 行）**：文件头含设计依据（§7.1/§7.2、TASK-02/ERR-01/CON-04/EVI-01/RPT-05、CR-01/CR-02、契约 acceptance 逐条）＋"为什么构造边界是结果可信性的总闸"背景说明＋实现形态与线程安全声明；7 个结构体/枚举逐一有业务概念、值语义、线程安全注释；4 个函数（isMustViolationRecord/validateCombination×2/make）均有 @brief/@param [in] 方向与持有说明/@return/@throws（make：EvidenceError 触发条件）/复杂度/确定性（检查序固定、NFR-COR-02）。
- **实现文件（Envelope.cpp，492 行）**：文件头实现纪律四条；validateCombination 三段逐步注释（①无条件边界/②Completed 行/③非 Completed 行，每分支注明业务场景与设计出处）；make 三步注释（校验→组装→摘要计算，CR-02 说明"不接受申报值"原因）；错误语义明确（异常轨单一稳定码＋issue 码逐格）。
- **测试文件抽查**：两文件头均含设计依据、需求/AT 追溯与替身边界声明（EV-REG-3 同源——AcceptAllClosureSource/ScriptedProducerRegistry 声明"仅验证 evidence 构造边界契约"）；每个 TEST 注释注明验证的需求/表 3 格位。
- **CMakeLists.txt**：新增契约目标段含中文注释块（§11 分工、三防线说明）；库源文件逐行注释对应任务。
- §2.5 高危信息抽查：单位（evaluatorContractVersion"无量纲"显式）、所有权（@param"调用方持有，本函数不接管"）、线程约束（"非线程安全的组装中间态，仅组装线程持有"）、错误语义（异常轨/issue 轨分轨说明）、确定性来源（枚举顺序＝检查序、值只能表尾追加）——齐备。

### 4.8 文档同步＋findings 相关性 — 通过

- 实现与任务卡零语义偏差；实现口径 E-1～E-6（未定名类型最小成形/payload 摘要 make 重算/凭据两段式/kDiagMustViolation 复用/清单绑定一致性/EvaluationKey std::string 承载 D-5 同款）已按 DTB §5.4 于 units/evidence.md §15.4 v0.8 变更记录逐条登记；卡头"文档版本"行 v0.7→v0.8 同步推进（F-057 消账纪律，不复发）。
- validate-task 三查（验收者主仓亲跑）：`validate-task: PASS (1 tasks)`；EV-T07 契约 designRefs §7/§12 锚点真实存在。
- **findings.json 未关闭条目逐条相关性核对**（open 条目全量）：
  - F-004（AUTOMOC 空转）：构建面既有，本任务 evidence 目标零 Qt 实证不变——无涉，继续 open。
  - F-005/F-006/F-058（单元卡"AI 执行就绪补充"转义损毁家族）：evidence.md 该节本次 diff 未触碰（仅卡头版本行＋§15.4 一行），损毁既有非本任务引入——未顺手修（属 DOC 治理批量任务），继续 open。
  - F-007/F-009/F-062（worktree/非主树环境前置）：本验收实际踩中 ini 模板缺件（按其口径补齐后成功），互证有效——继续 open，归流程文档/框架配置所有者。
  - F-008（RWSimulatorPlugin 全树缺陷）：按判例以任务目标面构建，未影响本验收——继续 open。
  - F-010（.gitignore 吞 *.log）：实施侧已按 CORE-T02 教训 `git add -f` 强制入库（21 件留痕全在库实证）；负向豁免本处置仍 open——与本任务无新发现。
  - F-012 家族（提交标题名词短语，F-012/023/024/026/027/030/034/037/039/040/042/044/045/047/048/059）：**本任务三提交标题均动词开头（实现/登记/登记），家族问题未复发**；家族处置仍 open。
  - F-015～F-018/F-043/F-046/F-061（testkit/runtime 域缺陷）：域外，无涉。
  - F-019/F-051（ird_gates 既有命中）：本次复跑实证 3 处命中仍在且与基线逐项一致、零新增——继续 open，归 WP-01 构建所有者。
  - F-020（evidence.md §3.5 锚点缺位）：EV-T07 契约 designRefs＝§7/§12 不受影响（validate-task PASS 实证）；本任务未触及 Errors.hpp——继续 open，归 evidence 详设所有者。
  - F-025/F-041/F-060（计数/归属准确性）：本任务提交与 ird-test-report 数字经验收者逐份清点一致（31＝26＋5、133＋5、476＝61＋133＋5＋4＋176＋97、门禁命中按 F-019×2＋F-051×1 逐条归属）——未复发。
  - F-028/F-035（"门禁未建成"过时措辞）：本任务如实执行 ird_gates 并登记真实结论（"ird_gates 门禁自查已执行"）——未复发。
  - F-029/F-031/F-032/F-033（契约编译/治理面）：域外，无涉。
  - F-036/F-038/F-054/F-055（构建树性质/配置标注纪律）：本任务 ird-test-report notes 显式区分增量树/全新树＋Release 配置——纪律已吸收，未复发。
  - F-056（EvidenceErrorCode 建议码超清单）：本任务未新增诊断建议码（异常轨复用既有 EnvelopeIllegalCombination；EnvelopeIssueCode 为 issue 观测面枚举、非诊断码面，不触码值权威）——继续 open，归 diagnostics 所有者。
  - F-063（Verdict.hpp 风格微瑕）：同单元相邻文件，本任务 diff 未触及 Verdict.hpp——未消账（正常），继续 open。

### 4.9 commit 质量 — 通过

- 三提交标题均 `[WP-05-T07]`＋动词开头（实现/登记/登记）——F-012 家族问题未复发。
- 正文四段齐备（What 按文件列要点/Why 对应契约 acceptance 与 CR 条款/How 验证结论/影响面）；`Acc-Covered: 1,2` 尾注。
- 验证结论与留痕对账（验收者逐份清点）：新增用例 31＝EnvelopeTest 26＋ContractTest 5 ✓；evidence 133＝既有 107＋新增 26 ✓；冒烟全量 476＝61＋133＋5＋4＋176＋97 ✓（六份 XML 顶格计数逐一核对）；ird_gates 3 命中归属 F-019×2＋F-051×1 ✓；未发现粉饰/失实。

### 4.10 留痕完整 — 通过

- traceability/builds/wp05-t07/ 21 件全部在库：gtest XML 7 份（evidence 集成/冒烟、contract 集成/冒烟、core/runtime/testkit/policy 冒烟——tests/failures 属性逐份与声明一致）＋ird-test-report.json（notExecuted=[]、contractVerifyCommand 登记、failureCapabilitySelfCheck、irdGates 归属）＋构建日志（integration-build-evidence.log/smoke-build-full.log/probe-red-build/probe-red-run/probe-red-run.xml/probe-restore-build）＋smoke/integration 测试运行日志＋integration-cache-check.txt＋probe-residue-check.txt（PROBE 残留计数 0）＋ird-gates.log＋validate-task.log（PASS）。
- *.log 按 F-010 教训 git add -f 强制入库（实施提交 18c9cea8 正文自述，实际入库清单核对一致）。

### 4.11 偷懒/缩水实现扫描 — 通过

- ①TODO/FIXME/stub/placeholder/未实现/not implemented 扫描：送验 4 个新增代码文件 `git grep` 零命中；无"触发式延后"豁免声明需核对。
- ②空实现检测：make()（校验＋组装＋摘要三步实质实现）、validateCombination 两重载（表 3 逐格检查实现）、isMustViolationRecord 有实质体；公共接口无空函数体/仅返回默认值形态。
- ③acceptance 逐条证据表：见 4.4——两条均有具名测试＋命令输出＋文件行号，无"已实现"声明依赖。
- ④测试强度：断言校验行为与边界——非法组合逐一断言稳定错误码＋消息含字段名（非仅"不抛异常"）、载荷摘要与手工 SHA-256 重算逐字节比对、首错顺序断言（issues.front()）、确定性序列逐条比对（码＋消息文本）、快照绑定面用界内/越界对照（上下文无关面不误报断言）——非恒真、非"只编译通过"；与 4.5 双探针联动实证测试会红。

## 问题清单

| 级别 | 编号 | 描述 | 处置 |
| --- | --- | --- | --- |
| 阻断 | 无 | — | — |
| 建议 | G-1（转登 findings F-064） | Envelope.hpp L67 `#include <sdurws/ird/evidence/Slice.hpp>` 冗余（头内无 Slice 实体引用；验收者删除后双测试目标仍零错误编译实证）——零行为影响卫生面微瑕，F-053 同型 | 建议随下次触及 Envelope.hpp 的任务顺手移除（避免独立格式化提交）；已转登 findings.json |

## 裁决

**verdict＝pass**：4.1～4.11 全部通过，无阻断级问题；建议级 1 条（G-1/F-064）不阻塞合入。验收者未合入、未修改实施产物、未向 wp05-t07 推送任何提交（送验对象冻结于 3e4e099c）。
