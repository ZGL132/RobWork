# RT-T10 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 ID | RT-T10（实现编译缓存纯判定 CacheKey 分层键——WP-06-T10，unit=runtime） |
| 契约 | tasks/foundation/RT-T10.json（status=ready，branch=wp06-t10，verify＝`cmake --build build --config Release --target sdurws_ird_runtime_test`；acceptance 3 条；knownPitfalls CR-05） |
| 送验对象（冻结） | `73ba2d05779a8340a2cfe8e87e19f0217232e4c9`（分支 wp06-t10 尖端；base=`d6b969f08924f1620bb783e87dd33eb9773a420a`＝origin/redesign-main 尖端＝RT-T09 合入收尾提交） |
| 分支漂移检测 | `git ls-remote origin wp06-t10`＝`73ba2d05779a8340a2cfe8e87e19f0217232e4c9`（与送验 SHA 一致——ACC §3 冻结口径满足；注：本机 git 代理 127.0.0.1:17890 当机，检测经直连（`-c http.proxy=`）完成，共 5 次代理重试后改直连 1 次成功） |
| **verdict** | **pass**（4.1～4.11 全部通过，无阻断级问题；建议级 1 条已转登 findings.json F-047） |
| 验收方式 | 全新上下文，独立 worktree（detached @ 送验 SHA）亲手重配重跑双模式构建与契约 verify 命令；静态审查＝逐行通读全部新增/修改源码文件并对照 units/runtime.md §9.4/§10.0 原文；动态探针＝2 处缺陷注入（与实施者探针不同点）→精确还原→全量复绿。不消费实施叙述。 |
| 验收环境 | Windows 10 x64 / Git Bash / VS2022（Visual Studio 17 2022 生成器 x64）/ Qt 6.11.1（CMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64）/ vcpkg 经典模式（仓库根，toolchain 绝对路径）/ CMake 4.3 / Windows PowerShell 5.1（治理脚本；pwsh 7 在本机仅有 Store 存根，AGENTS §4.1 允许 5.1）。复现现场＝`D:\10_Source_Repos\21_robot\_acc_rt_t10_a`（detached worktree @ 73ba2d05，集成树 build/＋冒烟树 build-smoke/ 均全新从零配置，验收后可删）；记录回传＝`D:\10_Source_Repos\21_robot\_acc_rt_t10_b`（分支 acc/RT-T10/1）。 |
| 环境前置说明（复现者须知） | ①全新集成树配置需主仓库同款参数：`-DCMAKE_TOOLCHAIN_FILE=<root>/vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`（缺 toolchain 则 Boost 找不到、配置失败——本次实测复现）；且 `RobWork/RobWorkStudio/bin/` 下两个被 .gitignore 忽略的本地模板（`RobWorkStudio.ini.template.static`、`RobWorkStudio.ini.shared.in`）须手工复制（目标目录本身也须 mkdir）——**F-007/F-009 的再次实证**，非本任务引入、不阻塞。②从零集成树手工运行测试 exe 需把 `build/RobWork/bin/Release`、`build/RobWorkStudio/bin/Release`、vcpkg `installed/x64-windows/bin` 加入 PATH（boost/gtest DLL），否则 exit 127——ctest 入口无此问题，环境链备注。 |

---

## 4.1 改动范围 —— 通过

- base..head 共 3 个提交：`5a1a0137`（实现 CacheKey 分层键＋判定表 judge）、`2d75a839`（runtime.md v0.11 登记）、`73ba2d05`（验证留痕）；`git diff --stat d6b969f..73ba2d05` 共 19 个文件、+3296/−5：
  - `runtime/**`（allowedFiles 第 1 条）：CMakeLists.txt（M）、include/sdurws/ird/runtime/CacheKey.hpp（A，350 行）、src/CacheKey.cpp（A，326 行）、test/CacheKeyTest.cpp（A，493 行）、include/sdurws/ird/runtime/NameMap.hpp（M，+16）、src/NameMap.cpp（M，±1）——6/6；
  - `doc/industrial-robot-design/traceability/builds/wp06-t10/**`（allowedFiles 第 4 条）：留痕 12 件（A）；
  - `doc/industrial-robot-design/units/runtime.md`（allowedFiles 第 3 条，M，+5/−5＝卡头版本行＋§3.1 CacheKey 行＋§15.4 v0.11 条目）。
- forbiddenFiles（REQUIREMENTS.md / ARCHITECTURE.md）：diff 中零出现。
- SA-02：diff 无任何框架源码文件；`industrialrobot/patches/` 无新文件。
- 对既有 NameMap.hpp/.cpp 的修改为纯常量化（新增 `kNameMapRuleVersion=1` 单点常量，`m_ruleVersion` 默认值与 `buildRuntimeNameMap` 赋值改取该常量——值不变、消除两处字面量 1），落位依据＝units/runtime.md §15.4 v0.11⑥ 登记（4.8 项核对）。

## 4.2 可复现构建 —— 通过

- **集成模式**（worktree A 全新配置＋全新 build/ 从零构建）：`cmake -S <A>/RobWork -B <A>/build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=<vcpkg> -DCMAKE_PREFIX_PATH=<Qt6>` → `Configuring done (58.4s)`；缓存确认 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`（acc-logs/integration-configure.log）。构建 `cmake --build build --config Release --target sdurws_ird_runtime_test` → **BUILD_EXIT=0**，`error C[0-9]|LNK…|fatal error` 计数＝0（日志 acc-logs/integration-build.log；CacheKey.cpp/CacheKeyTest.cpp 均在该构建中编译）。
- **冒烟模式**（独立目录 build-smoke，带 vcpkg toolchain 绝对路径重配置）：`cmake --build build-smoke --config Release` → **exit=0** 零错误（acc-logs/smoke-build.log）；测试执行 **172/172 PASSED**（acc-logs/smoke-test-run.log，`[==========] 172 tests from 46 test suites ran`，与送验留痕 smoke XML tests="172" failures="0" 一致）。
- 结论：两模式均零错误；与提交声明"集成零错误、冒烟 172/172 零回归"一致。模式分工（CacheKey.cpp/CacheKeyTest.cpp 仅集成模式编译——消费 src/Snapshot.cpp 集成编译单元的 `snapshotidentity::computeWorkCellCompileIdentity` 公式单点）按 DTB §5.4 登记于 runtime.md §15.4 v0.11⑦（v0.8②/v0.9⑧/v0.10⑥ 同款先例），**口径正当**：本验收核对 CMakeLists 确认两文件确在 `TARGET sdurw_kinematics` 条件内、CacheKey.hpp 冒烟模式确无 TU include（grep 全冒烟源码零命中）、冒烟 172 零回归成立。

## 4.3 契约 verify 命令 —— 通过

- 契约 verify 数组仅 1 条：`cmake --build build --config Release --target sdurws_ird_runtime_test`——验收者在 worktree A **逐字亲手执行**（从零构建）exit=0（即 4.2 集成构建）；构建产物运行 **220/220 PASSED**（`[==========] 220 tests from 52 test suites ran`＝既有 213＋新增 7，与送验留痕 integration-test-run.log 逐字一致；日志 acc-logs/integration-test-run.log、acc-logs/restore-test-run.log 两轮均 220/220）。
- 治理脚本（Windows PowerShell 5.1）：`validate-task.ps1 -TaskFile RT-T10.json` → **PASS (1 tasks)**；`verify-task.ps1` 先 `-DryRun`（RepoRoot 归约正确＝D:\10_Source_Repos\21_robot\RobWork，dryrun 命令与契约 verify 逐字一致）→ 正式运行 **PASS RT-T10**（脚本内部执行同一构建命令于主树——主树恰为同一送验提交 wp06-t10 尖端，构建 exit 0）。

## 4.4 acceptance 条目逐项 —— 通过（具名测试均在我执行的 220/220 中 PASSED）

| 契约 acceptance | 证据（具名测试，CacheKeyTest.cpp） | 结果 |
| --- | --- | --- |
| RT-CACHE-1～4 分层键与判定表全部用例通过 | RT-CACHE-1＝`BuildsTwoLayerKeysDeterministic_RT_CACHE_1`（两层键齐备＋8 条指纹＋D-04 分层：requestDynamicWorkCell 不入 WC 键＋同输入重复生成逐字节等值＋**WC 键与 §9.1 公式单点同参数输出逐字节相等的复用钉住**）、`ComponentPerturbationPreciseReasons_RT_CACHE_1`（7 类编译器/环境侧分量逐项扰动→键变＋reasons 恰为单条该分量词：contract/compiler/baseline/namemap-rule/codec/wc-options；能力级别扰动→WC 键不变＋有向 DWC 两词 dwc-not-requested/dwc-missing-in-cached；3 类内容侧扰动〔质量 kg 值/工具 TCP m 平移/几何资源字节〕→全部恰为 model-changed）、`FingerprintSetPinsPureContentKey_RT_CACHE_1`（指纹集合恰 8 条、名字序＝编码域序——策略/种子/线程/HEAD 无条目，CON-06 结构钉住）；RT-CACHE-2＝`WorkCellOnlyReuseNotFullReuse_RT_CACHE_2`（缓存 nullopt/请求要求→WorkCellOnlyReuse，显式断言 ≠FullReuse）；RT-CACHE-3＝`OldCompilerContractRejected_RT_CACHE_3`（契约版本 1 vs 2 双向 Incompatible＋contract-changed）；RT-CACHE-4＝`HeadAdvanceDoesNotInvalidate_RT_CACHE_4`（消费面未变→模型字节等值→请求键与缓存键全等→FullReuse，reasons 空；键路径无 HEAD/时钟输入——只断言 runtime 侧，与单元卡 RT-CACHE-4 行"当前性归 evidence computeCurrentness"口径一致且测试注释如实声明） | 7/7 PASSED |
| 存储归 execution（本任务只做纯判定） | 模块面核查：CacheKey.hpp/.cpp 仅有 buildKey/judge 纯函数＋值类型，**零缓存容器/命中查询/淘汰 API**（逐行通读确认；头注释 L29-33 声明存储边界＝§9.4 首句原文）；SkippedNoPhysics 落账 nullopt 规则属 execution 的注释声明面（CacheKey.hpp L120-125）——边界与契约 acceptance 2 与 v0.11② 登记一致 | 通过 |
| 部分/失败产物不得作为完整命中（§9.4 判定表反例）用例通过 | `UnfinalizedKeyNeverHits_RT_CACHE_1`：未 finalize 键（workCellKey 全零）请求侧/缓存侧双向 Incompatible(key-invalid)＋伪造部分键（WC 键合法＋在场 DWC 键全零）Incompatible(key-invalid)——判定门先于一切比对（CacheKey.cpp L243-256）；配合 RT-CACHE-2 的"WorkCellOnlyReuse 非 FullReuse"反例 | PASSED |

## 4.5 测试真实失败能力（验证验证者）—— 通过（2 处注入，验收者亲手执行，与实施者探针不同点）

| 探针 | 注入（worktree A 临时编辑，显式核验构建退出码） | 结果 | 还原 |
| --- | --- | --- | --- |
| 探针 A | CacheKey.cpp judge 第 1 步键有效性门 `if (!requestedValid \|\| !cachedValid)` 改为 `if (false && …)` → 重编 **exit=0**（新二进制） | **恰好 1 例变红**：`UnfinalizedKeyNeverHits_RT_CACHE_1`（CacheKeyTest 过滤器 6 PASSED/1 FAILED——key-invalid 词与伪造部分键拒绝面被真实钉住，非恒真） | 精确还原（git diff 仅剩探针 B 一行时核验） |
| 探针 B | CacheKey.cpp buildKey 分量指纹名 `"base-world-rule"` 篡改为 `"base-world-rule-x"` → 重编 **exit=0** | **恰好 1 例变红**：`FingerprintSetPinsPureContentKey_RT_CACHE_1`（分量名序钉住生效——混入指纹即红） | 精确还原 |
| 复绿 | `git diff 73ba2d05 -- <runtime>`＝空（与送验 SHA 逐字节一致，显式核验）→ 全量重建 **exit=0** → **220/220 PASSED**（acc-logs/restore-build.log、restore-test-run.log） | 通过 | — |

注：实施者留痕中的探针（RT-CACHE-2 行 verdict 注入 FullReuse→恰 2 例红）为其实施面自证；本验收按对抗原则另选两处不同注入点，独立证明测试敏感度。probe-cache-red.log 的 2 FAILED 结论与提交声明一致（留痕核对，不构成采信依据）。

## 4.6 架构红线（AGENTS §5 八条）—— 通过

1. **L2 零 Qt**：新增/修改 6 文件 Qt include 扫描零命中（`include <Qt|include <Q` ×6＝0）；runtime/CMakeLists.txt 末尾 Qt 链接 FATAL_ERROR 守卫在配置期通过；ird_gates 的 R-3 自测项亦过。
2. **业务域互链禁止（R-1）**：跨单元 industrialrobot include 扫描（modeling/requirements/kinematics/trajectory/dynamics/selection/optimization/evidence/policy/project/io/execution/ui/reporting/testkit/diagnostics）零命中；唯一单元依赖＝`sdurws_ird_core`（CMakeLists L171＋ird_gates R-1 零新增命中）。
3. **跨单元私有头禁止（R-2）**：CacheKey.hpp/.cpp/CacheKeyTest.cpp 的 include 面＝标准库＋`sdurws/ird/core/Digest.hpp`（core 公共头）＋`sdurws/ird/runtime/*`（本单元公共头）＋同目录测试夹具 `CanonicalModelFixture.hpp`（单元内）——零跨单元头。
4. **框架零源码修改（SA-02）**：见 4.1（diff 无框架文件、patches/ 零新文件）。
5. **不做名称前缀拼接（R-4）**：CacheKey 无任何名称生成/拼接行为（名称唯一权威仍在 NameMap——本任务反而在 NameMap 增设 `kNameMapRuleVersion` 单点，供键分量引用，未另写第二来源）。
6. **权威唯一（PA-1）**：WC 层键复用 `snapshotidentity::computeWorkCellCompileIdentity` 单点（不写第二套编码——RT-CACHE-1 用例以逐字节相等钉住）；nameMapRuleVersion 取 `kNameMapRuleVersion` 单点、baseWorldRuleVersion 恒在公式单点内部取 `kBaseWorldRuleVersion`、codecVersions 取各编码头常量组装（`codecVersionsFromCodecHeaders`）、基线版本取 `RobWorkBaselineVersion::capture()`——各分量单一权威，无字面量重写；判定语义只在 `judgeCompileCacheCompatibility` 单点。
7. **不可变历史（PA-2）**：CacheKey 全为纯值/纯函数（构造后只读的 builder、无状态的 judge），无任何修订/历史写路径。
8. **不继承 old/**：新增文件无 old/ 引用（grep 零命中）。

## 4.7 注释规范（AGENTS §2）—— 通过

- **公共头全查**（2 个改动头逐行通读）：CacheKey.hpp（350 行，文件头含设计依据〔§9.4/§3.1/CON-04/CON-05/CR-05/任务契约〕＋存储边界＋实现层增补背景＋两模式口径＋线程安全声明；3 类型 2 函数 1 接口 1 类全部中文 Doxygen——`@param [in]` 方向、`@return` 语义、`@throws RuntimeError/InputInvalid` 触发条件、noexcept 约束下的设计理由、verdict 三值与 reasons 稳定词汇表逐词释义）；NameMap.hpp 增量（`kNameMapRuleVersion` 完整 Doxygen：单一权威理由＋消费方纪律＋升版路径）。
- **实现文件抽查**（全读 ≥3）：src/CacheKey.cpp（326 行——四步 buildKey 逐步注释〔算法第几步/数学业务含义/失败走向〕、judge 三步与判定表行对应、分量编码大端/长度前缀规则、构造期 fail-fast 理由）、test/CacheKeyTest.cpp（493 行——文件头用例↔需求追溯、每用例设计依据段、断言失败信息面向人工 review）、runtime/CMakeLists.txt（RT-T10 增列块中文注释含模式分工理由）——全部合规。
- **§2.5 高危信息逐项**：物理单位（测试内 mass 4.5 注明 kg、TCP 平移 0.05 注明 m）；坐标系（TCP/法兰语境注明）；所有权/线程约束（每类型注明"值语义纯结构；线程安全"／builder 构造后只读可并发）；错误语义（构造期 InputInvalid＝调用方错误 fail-fast vs judge 纯函数不抛——归属逐处注明）；确定性来源（大端定长/长度前缀/字段序＝编码域分量序、指纹"同分量同字节即可"、零键保留值语义）——全部在场。

## 4.8 文档同步 + findings 相关性 —— 通过

- units/runtime.md 随提交升 **v0.11**：卡头版本行、§3.1 模块表 CacheKey 行落位标注、§15.4 v0.11 登记条目（七要点：①分量指纹表增补〔§9.4 草图两字段之上、只增不改〕②DWC 键链式派生③生成器构造入参选型④reasons 稳定词汇⑤未 finalize 键判定面表示⑥kNameMapRuleVersion 常量单点⑦两模式编译分工）。逐条比对实现与登记：指纹分量名/序、判定表行序、reasons 词汇、链式派生公式、构造校验规则均一一对应——**零未登记偏差**。§9.4 契约原文（草图两字段名/类型/语义、判定表六行、分量表九行）未被改动。
- 状态同步：phase-one-validation.log L48 `RT-T10.json: validate-task: PASS (1 tasks)`（本验收亲手重跑一致）；契约 status=ready 待合入收尾翻转（PIPE 口径，非偏差）。
- **findings.json（送验对象内）相关性核对**：46 条、open 38 条。逐类核对与本任务因果：环境链 F-004/F-007/F-008/F-009/F-010（F-007/F-009 本验收**再次实证**，见页首环境说明；F-004 空转 MOC 在本次构建输出再次可见）、门禁基线 F-019（本次独立复现命中仍恰为 SUB/T1 两项、零新增——与本任务留痕声明一致）、提交标题家族 F-012/F-023/F-024/F-026/F-027/F-030/F-034/F-035/F-037～F-040/F-042/F-044/F-045（**本任务两份提交再现同型——已转登 F-047**，见问题清单）、其余 DOC/TK/EX/IO/RT 早期条目均无 RT-T10 因果。无"可随本任务顺手消账"条目。

## 4.9 commit 质量 —— 通过（附 1 条建议级，见问题清单）

- 三提交正文四段齐备（改动内容按文件列点／改动原因对应需求 CON-04/CON-05＋任务卡＋DTB §5.4／验证含构建退出码·测试计数·探针·门禁·validate／影响面），无空话。
- 验证段结论与验收者亲手复现**逐项一致**：集成 220＝213＋7、冒烟 172/172、探针恰 2 例红（probe-cache-red.log）→复绿 220、ird_gates 命中＝F-019 既有 2 项零新增、validate-task PASS——无粉饰。
- 标题：实现提交 `5a1a0137`『[WP-06-T10] 实现编译缓存纯判定（CacheKey 分层键＋判定表 judge）』动词开头合规；两份提交（`2d75a839`『文档同步：…』、`73ba2d05`『验证留痕：…』）以名词短语开头——F-012 家族第 15/16 例（先例均判纯风格瑕疵不构成阻断），转登 **F-047**，不计入 4.9 阻断。

## 4.10 留痕完整 —— 通过

- `traceability/builds/wp06-t10/` 12 件落位：integration-build.log、integration-test-run.log（尾行 220 PASSED）、ird-test-report.json（schema ird-test-report/1：targets/modeMechanics/testSuites 逐例 trace/failureCapabilityProbe/gates/conclusion 全段，与代码逐条比对相符）、gtest XML 双份（integration tests="220" failures="0"／smoke tests="172" failures="0"）、probe-build.log、probe-cache-red.log（恰 2 FAILED：ComponentPerturbationPreciseReasons_RT_CACHE_1／WorkCellOnlyReuseNotFullReuse_RT_CACHE_2——与提交声明一致）、smoke-configure/build/test-run.log、ird-gates-run.log、validate-task-run.log。
- 留痕与本验收复现数字全部一致（220/172/探针 2 红/门禁 2 命中）；ird_gates 以"命中集＝既有登记 2 项、零新增"口径如实登记（未虚标全绿）；无"未执行标注通过"情形。

## 4.11 偷懒/缩水实现扫描 —— 通过

- ① diff 新增行 `TODO|FIXME|stub|placeholder|未实现|not implemented` 扫描（产品代码范围）＝**零命中**，无需豁免列表。
- ② 空实现检测：公共接口无空函数体/仅默认值返回——`codecVersionsFromCodecHeaders`/构造器/`buildKey`（四步完整实现）/`judgeCompileCacheCompatibility`（三步判定表全行）全部有实质实现；`IRuntimeCompileCacheKey` 纯虚声明属 §9.4/§10.0 契约本身。
- ③ acceptance 逐条证据表：见 4.4（三条均具名测试＋XML 计数＋验收者亲手执行）。
- ④ 测试强度：断言校验行为＋边界（逐分量扰动×10、reasons 精确词序敏感、有向 DWC 双向、双向 key-invalid、伪造部分键、复合键与公式单点逐字节互证）；非恒真——4.5 双探针证明恰一例红。

## 问题清单

### 阻断级
无。

### 建议级（已转登 findings.json F-047）
- **F-047**：F-012 家族再现——RT-T10 两份送验提交标题以名词短语开头（`73ba2d05`『[WP-06-T10] 验证留痕：…』＝F-034/F-040/F-042/F-045 已判同形态；`2d75a839`『[WP-06-T10] 文档同步：…』＝F-037/F-039/F-044/F-045 同前缀形态）——家族第 15/16 例（实现提交 `5a1a0137` 动词开头合规）；正文四段齐备、验证结论与验收者复现一致，纯风格瑕疵不构成阻断。随本验收记录转登于 findings.json（evidence 分支内）。

### 环境观察（已有 open 登记，本次再次实证，不新增）
- F-007/F-009：全新集成树/worktree 配置需 vcpkg toolchain＋Qt CMAKE_PREFIX_PATH＋RobWorkStudio/bin 两个 .gitignore 忽略的 ini 模板（且目标目录需手工创建）——本验收手工补齐后复现成功。
- F-004：`Automatic MOC for target sdurws_ird_runtime(_test)` 空转 MOC 现象在本次构建输出可见（实证零 Qt 依赖，无副作用）。
- F-019：ird_gates 基线红（SUB/T1 两项 testkit 相关边）在本验收全新树独立复现，命中集与送验声明一致、零新增——RT-T10 无门禁新债。
- F-008：全树从零构建受 RobWorkSim 插件缺陷阻挡为已知基线事实——本验收按先例口径执行**目标面从零构建**（sdurws_ird_runtime_test 及其依赖），零错误。

## 裁决

**pass** —— 4.1～4.11 全部通过，无阻断级问题。送验对象 `73ba2d05779a8340a2cfe8e87e19f0217232e4c9` 达成 RT-T10 契约 DoD（以本记录与 evidence 分支 acc/RT-T10/1 为凭）；合入决策归所有者。
