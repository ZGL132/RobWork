# CORE-T02 验收记录（对抗式验收）——attempt 2：pass

| 字段 | 值 |
| --- | --- |
| 任务 ID | CORE-T02（≙WP-03-T02，身份与摘要 Identity/Digest/SHA-256） |
| 契约 | tasks/foundation/CORE-T02.json（status: ready，随收尾翻转 done） |
| 验收对象 | 分支 `wp03-t02`，head＝`b2a176617560675bf9c2c7e6beb1d78ff740861e`（与 origin 一致——漂移检测 PASS），范围 `aae0e4b..b2a1766` |
| 尝试序号 | attempt=2（evidence 分支 `acc/CORE-T02/2`）；attempt=1 fail 记录＝`acc/CORE-T02/1` @ `c2dbc86`（阻断级 B-1：4 份日志被 .gitignore 吞——返工输入 lastFailureRecord 已按 §4.5 传递） |
| **verdict** | **pass**（B-1 已修复实证；4.1~4.11 全过；1 条建议级 F-009 撞号观察随收尾治理处理） |
| 验收日期 | 2026-09-10 |
| 独立性声明 | PIPE v1.7 §0.4 单会话降级（同会话顺序执行）；补偿＝worktree 亲手复现＋两个失败能力探针＋机器 ls-tree/check-ignore 实证 |

---

## 返工核验（attempt=1 B-1 的修复实证）

- `git ls-tree -r b2a1766 -- builds/wp03-t02/`：**9 文件**（JSON＋XML×2＋**5 份日志**）——B-1 缺失的 integration-build/integration-fullbuild/integration-test-run/smoke-configure/smoke-build 全部入库；
- 返工提交 `b2a1766` 零代码改动（纯 `git add -f` 证据补交，符合 attempt-1 处置建议的 allowedFiles 约束）。

## 逐项证据（4.1~4.11）

| # | 检查 | 结论与证据 |
| --- | --- | --- |
| 4.1 | 改动范围 | 13 文件＝core/** 8＋CMakeLists 1＋units/core.md 1＋traceability/** 3——allowedFiles 内；forbiddenFiles 零触碰；patches/ 零新增（SA-02） |
| 4.2 | 可复现构建 | worktree（detached @ b2a1766）全新配置 exit 0＋verify 目标构建 exit 0＋测试 17/17（本 attempt 亲手）；实施侧双模式日志随对象入库 |
| 4.3 | verify 命令 | `cmake --build build --config Release --target sdurws_ird_core_test` exit 0（worktree 内） |
| 4.4 | acceptance 逐条 | ①UT-ID-T 六用例＋UT-ID-D 七用例全过（含 FIPS 空串/abc/两分组向量）——两模式 XML；②token 往返与保留值行为＝§4.1/§4.2 一致（往返/全零保留/att-0 拒绝/严格解析逐条钉住） |
| 4.5 | 失败能力 | 两层实证：实施侧 K 常量扰动→向量全红、大写放行注入→严格性变红；验收侧错误前缀篡改→IdentityStrictParse 变红；均清除后复绿 |
| 4.6 | 红线 | 零 Qt（新增头零 Q include）、R-2（公共头仅 include/，detail 命名空间承载内部）、CR-02（摘要唯一实现归 core 本任务）、SA-02 零框架改动 |
| 4.7 | 注释 | 公共头全查（文件头设计依据/类与函数 Doxygen/线程与确定性标注）；实现抽查（严格解析逐步注释、SHA-256 分步 FIPS 条款对照、宏两处成对警告）；§2.5 高危信息（单位无涉、线程安全声明、错误语义前缀、确定性来源）齐备 |
| 4.8 | 文档同步 | core.md v0.4 登记三处（接口零偏差＋Errors.hpp 首消费者最小契约＋实现取舍）；findings 核对：F-004（AUTOMOC 空转——本目标同样存在，留构建所有者）；**F-009 撞号**（.gitignore 条目与 gate-all 条目重号）→ 建议收尾治理重编号 F-010；F-008 与本任务目标复现无关（verify 目标从零构建成功） |
| 4.9 | commit 质量 | 三提交均 `[WP-03-T02]` 四段式＋Acc-Covered 标注（1,2 / 2 / 2 补全）；验证结论与留痕一致 |
| 4.10 | 留痕完整 | 9 文件齐（日志×5＋XML×2＋report＋）；ird-test-report 含两探针自检记录；ird_gates 随全量构建零命中 |
| 4.11 | 偷懒扫描 | diff 全量 TODO/FIXME/stub/placeholder/未实现/not implemented 零命中；公共接口全部真实逻辑（SHA-256/严格解析/FNV 均为实质实现）；测试断言行为与边界（非恒真——4.5 实证） |

## 契约 acceptance 逐条

1. UT-ID-T/UT-ID-D 全过（含 FIPS 向量）——17/17 双模式 ✓；
2. token 往返与保留值行为与 §4.1/§4.2 一致（五元组规范化序列化）——往返/保留值/严格解析用例组 ✓。

## 问题清单

阻断级：无。建议级：F-009 撞号（收尾治理重编号，数据修复）＋.gitignore 例外规则补登（F-009 所有者既定处置）。

## 环境与复现说明

- 记录经 acc/CORE-T02/2 回传；worktree 均已清理；fetch 一次 SSL 瞬时中断重试恢复。
