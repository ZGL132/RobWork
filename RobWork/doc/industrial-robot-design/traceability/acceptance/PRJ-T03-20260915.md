# PRJ-T03 验收记录（attempt 3，2026-09-15）

| 字段 | 值 |
| --- | --- |
| 任务 | PRJ-T03（project 单元写锁与路径 StoreLock/PathCanonical；契约 tasks/foundation/PRJ-T03.json；需求 PM-07/PM-08；knownPitfalls P-PR-6） |
| **verdict** | **pass**（4.1～4.11 全部通过；零阻断级问题；建议级 2 项已转登 findings.json F-127～F-128） |
| 送验对象（冻结） | `65520d6a769bc6a66f5580e8d4790136fa29829a`（40 位）；分支漂移检测：`git rev-parse origin/wp04-t03`＝该 SHA（fetch 后实测），零漂移——验收全程按该 SHA 的 detached worktree 进行，不按分支名 |
| diff 范围 | `c332d52b27e9b4ff032e4851a03ab3e26153d37a..65520d6a`（state.base..headSha；`origin/redesign-main` 尖端恰为该基线，即纯任务分支增量；3 个提交：a3ac8fa5 实现＋ad01b11b 单元卡 v0.5＋65520d6a 留痕归档） |
| attempt 说明 | 尝试序号 3 由编排者随验收请求下发（state.attempts.accept＝3）；attempt 1/2 均未产生任何落盘记录（remote/local 均无 acc/PRJ-T03/* 分支、origin/redesign-main 的 traceability/acceptance/ 无同日同名文件、state.lastFailureRecord＝null）——本文件为该任务首份验收记录，文件名按编排者下发路径未加 -r 序号 |
| 验收者环境 | 全新上下文；复现现场＝`git worktree add --detach D:/10_Source_Repos/21_robot/.acc-tmp/prj-t03-a 65520d6a`；集成模式全新配置（vcpkg toolchain＋Qt 前缀＋`RWS_USE_STATIC_LINK_PLUGINS=ON` 与主树缓存一致；`RobWorkStudio.ini.template.static/.shared.in` 两件 git 未跟踪本地资源自主树复制——F-007/F-062 登记件，验收者本轮亲历复踩：首轮裸配置在 Boost 解析与 RobWorkStudio/src/CMakeLists.txt:58 两处失败，按登记口径补齐后通过）；冒烟模式另配独立临时目录 `.acc-tmp/prj-t03-smoke`；CMake 4.3＋MSVC 19.42/VS2022 x64；pwsh 治理脚本 |
| evidence | 分支 `acc/PRJ-T03/3`（本文件＋findings.json F-127～F-128 同支承载；commit 三元组见文末） |

---

## 1. 逐项证据（ACC §4.1～4.11）

### 4.1 改动范围 —— PASS

- `git diff --name-status c332d52b..65520d6a` 共 16 文件，逐一比对契约 `allowedFiles` 三模式：
  `project/**`×10（CMakeLists.txt、include/sdurws/ird/project/StoreTypes.hpp、src/win32/{ILockOps.hpp,PathCanonical.cpp,PathCanonical.hpp,StoreLock.cpp,StoreLock.hpp}、test/{LockContractTest.cpp,PathCanonicalTest.cpp,StoreLockTest.cpp}）✓；`traceability/**`×5（builds/wp04-t03/ 下 ird-test-report.json＋双模式 gtest XML 四份）✓；`units/project.md`（显式列名）✓。
- `forbiddenFiles`（REQUIREMENTS.md / ARCHITECTURE.md）零触碰 ✓。
- `industrialrobot/patches/` 零新文件；框架源码（RobWork/RWS/RobWorkSim）零改动（diff 全清单核对，无任何清单外路径）——SA-02 无登记义务产生。SDK 基线提升（`_WIN32_WINNT=0x0601`）仅发生在 project 单元两个新翻译单元的编译定义层（ILockOps.hpp/PathCanonical.cpp 各自先行于 windows.h 的三行序，文件头注释注明"SA-02 框架零修改，不可动全局"），属单元内部实现、非框架改动 ✓。

### 4.2 可复现构建 —— PASS（验收者全新树亲手复现，不采信实施日志）

- **集成模式**：`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=<vcpkg> -DCMAKE_PREFIX_PATH=<Qt> -DRWS_USE_STATIC_LINK_PLUGINS=ON` → exit 0（`-- Configuring done (22.7s)`＋`-- Generating done`，零 CMake Error）；缓存 grep 确认 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`。
  - 契约 verify 目标 `sdurws_ird_project_test` 从零构建 exit 0、`error` 行计数＝0（产物 `sdurws_ird_project_test.exe`＋gtest DLL 拷贝步骤执行）。
  - `sdurws_ird_project_contract_test` 构建 exit 0、error 计数＝0。
  - 两测试可执行文件运行：**38/38＋10/10＝48/48 全绿**（gtest 输出实测，XML 落验收者证据目录）。
- **独立冒烟模式**：`-S industrialrobot -B <独立临时目录> -DCMAKE_TOOLCHAIN_FILE=vcpkg…`（AGENTS §4.1＋CORE-T01 G-2 口径）配置 exit 0（"standalone smoke mode"）；`--config Release` 全量构建 exit 0、error 计数＝0；两测试 exe 运行 **38/38＋10/10 全绿**。
- 口径说明：全新树**全量**集成构建受 F-008 基线框架缺陷阻挡（既有登记，非本任务引入），本次复现覆盖任务目标面（project→core 链＋两测试目标＋门禁＋治理脚本），与 PRJ-T01/T02 以来既往验收口径一致；本轮未加 `-DCMAKE_CXX_FLAGS`（PRJ-T02 记录载明的 NOMINMAX 系口径）而任务面构建/测试零错误，证据自足。

### 4.3 契约 verify 命令 —— PASS

- 契约 `verify` 数组 2 条，验收者在 detached worktree 根**逐字原样执行**：
  ① `cmake --build build --config Release --target sdurws_ird_project_test` → **exit 0**；
  ② `cmake --build build --config Release --target sdurws_ird_project_contract_test` → **exit 0**。
- `verify-task.ps1 -TaskFile <契约>` 实跑 → **verify-task: PASS PRJ-T03，exit 0**（内部执行同一命令集，双口径一致）。

### 4.4 acceptance 条目逐条 —— PASS（4/4 有对应证据；无虚标）

| # | 契约条目 | 验收者证据（全部亲手复跑，两模式一致） |
| --- | --- | --- |
| 1 | PRJ-TX-7 锁用例：第二实例只读打开＋持锁 PID 提示（PM-07）、心跳卡顿不接管（D-03）、崩溃后双实例并发接管仅一胜者（D-02） | `LockContractTest` 5 用例两模式 10/10 通过：`SecondInstance_ReadOnlyDegrade_ReportsHolderPid_PM07`（真双进程：子进程持锁→父进程 writable 打开 HeldByOther 不等待＋`lastHolder().pid`＝子进程实际 PID＋PRJ-LOCK-HELD 上下文含该 PID；子进程退出后锁即时可取）、`StalledHeartbeat_NeverEnablesTakeover_D03`（子进程心跳构造 2 h 前＋内容冻结实测，父进程两次获取均拒）、`CrashThenConcurrentTakeover_SingleWinner_D02`（TerminateProcess 后两竞争子进程 manual-reset 事件同时放行、各单次获取，Exit Code 恰 `{0,3}` 各一＋锁记录 PID＝胜者——"恰一胜者"以排序后向量逐值断言，不给双胜/双败留解释空间）；进程内佐证 `StalledHolder_NotTakenOver_OverTime_D03`／`StaleHeartbeat_IsNeverWriteAuthorityCriterion_SA17_D03` |
| 2 | PRJ-TX-13（锁部分）：只读上下文遍历全部写入口一律拒绝＋稳定诊断（PRJ-LOCK-HELD 含持有 PID）；失权三道防线（§9.6 写前权威检查——SA-17：心跳不作写权限判据） | `SecondInstance_WriteEntries_UniformlyRejected_TX13`（本阶段锁面写入口＝门卫 `requireWriteAuthority`＋锁重写 `rewriteHeartbeat`，遍历拒绝；三次拒绝共 3 条 PRJ-LOCK-HELD、上下文含持有 PID 321、拒绝方零写入副作用——锁文件字节前后一致）。范围注记：命令/草稿/归档写入口本阶段尚不存在（PRJ-T08+ 落位），§9.6① 明文该入口统一经本门卫承接——非缩水，机制即设计指定的唯一防线。三道防线逐条在位：`Release_RejectsWrites_AuthorityLost_ThenReacquirable`（①状态机＋context-closed→PRJ-WRITE-AUTHORITY-LOST 映射＋OS 锁即时可再取）、`ProbeFailure_LatchesLostWrite_SecondLine`（②探测失败即失权＋锁存不可逆——注入撤销后仍拒绝）、`IoErrorClassification_ThreeCodesLatch_OthersNot`（③三码 32/6/5 各锁存＋反例磁盘满 112 不锁存）、`HeartbeatWriteFailure_ClassifiedAsAuthorityLoss`（③经真实写入口通道）；心跳非判据双向证明：`StaleHeartbeat_…SA17_D03`（远古心跳既不撤销持有者权限也不授予第二实例权限，拒绝诊断＝PRJ-LOCK-HELD 而非心跳派生码） |
| 3 | PathCanonical 用例：路径规范化与同项目多路径打开（§9.3）；锁文件永不删除重建（§9.4 防锁对象分裂——D-03） | `PathCanonicalTest` 7 用例两模式 14/14 通过（幂等＋`\\?\` DOS 形态／大小写·正斜杠·尾分隔符·冗余段四拼写族收敛／相对路径 cwd 收敛／8.3 短名〔卷禁用时如实降级 SUCCEED 不伪造〕／区分度反例／不存在与空串错误码 2/87／`SameProjectMultiPathOpen_SecondWritableRefused` 进程内 isSelf）＋`LockContractTest.SameProjectMultiPathOpen_CrossSpelling_Refused`（跨进程大写变体拒绝）；D-03 双观测面：`StoreLockTest.Heartbeat_RewritesInPlace_FileIdentityStable_D03`（≥4 次心跳前进后卷序号/文件索引/创建时间三元组＋长度 113 不变）＋`LockContractTest.LockFileIdentity_StableAcrossCrashTakeover_D03`（崩溃-接管全链身份逐字段不变） |
| 4 | P-PR-6 处置：锁诊断经 §5.0 IDiagnosticsSink 适配器产出 core::DiagnosticRecord（注入式先行、不直链 diagnostics 库）；PRJ-* 码值收编 diagnostics.md §4.6 | 公共头 `StoreTypes.hpp`＝§5.0 原文形态 `IDiagnosticsSink{report(core::DiagnosticRecord)/reportDev}`（project 定义、外部实现、注入可空——sink 可空路径由 PathCanonicalTest null sink 口径实证）；产码点码值仅 `PRJ-LOCK-HELD`/`PRJ-WRITE-AUTHORITY-LOST`，与 diagnostics.md §4.6 行 284/379 收编清单逐字一致（验收者亲查该文件）；`StoreLockTest.Diagnostics_ProducedThroughCoreRecordFactory_PPR6`（make 工厂 C-3：context/cause/recommendedAction 非空逐记录断言＋码值逐字）；构建图面：`ProjectLinkage.DiagnosticsEdgeInjectionNotLink_DT_BUILD` 两模式通过＋验收者亲跑 ird_gates 输出中 project 零提及（grep 计数＝0）——注入式先行、零 diagnostics 链接边 |

`ird_gates` 执行说明（ACC §4.4"未执行的不得标注通过"）：门禁已建成，验收者**实际执行** `cmake --build build --config Release --target ird_gates` → exit 1＝**8 处命中**，目标名去重清单与送验报告登记基线一致：SUB×6（sdurws_ird_diagnostics_contract_test / evidence_test / evidence_contract_test / policy_test / policy_contract_test / runtime_test 直链 testkit）＋SUB 表外边×1（testdata_lint→testkit）＋T1×1（testdata_lint 产品目标链 testkit）——与 F-115（DIAG-T10）登记的 8 项基线逐项同分布，**零新增、零 sdurws_ird_project 条目**（门禁输出中 "project" 出现次数＝0）；R1/R3/R4/R5/T2 token 为引擎自测"按预期检出"行。exit 1＝基线既有命中的预期行为（SA-10/DTB §4.5 登记义务随任务流转，F-115 已登记），非本任务问题。

治理脚本（验收者亲跑）：`validate-task.ps1` → **PASS (1 tasks)** exit 0；`verify-task.ps1` → **PASS PRJ-T03** exit 0；`validate-docs.ps1` → **PASS (20 units, 12 trace entries, 150 task files)** exit 0。

### 4.5 测试真实失败能力 —— PASS（2 组实现级探针注入即红；均还原复绿）

探针打在 `src/win32/StoreLock.cpp` 实现体（一次性 worktree 内；每轮 Edit 引入新 mtime、增量重编译、滤跑目标用例、`git checkout --` 还原并确认工作树干净、全量重跑复绿——F-068/F-075 口径）：

| 探针 | 注入 | 结果 |
| --- | --- | --- |
| 1 | 失权三码表移除 `ERROR_INVALID_HANDLE(6)`（防线③分类表破坏） | `IoErrorClassification_ThreeCodesLatch_OthersNot` **红**（`[FAILED]`，classifyIoError(6) 后 `held()` 仍 true——锁存断言真实）；还原复绿 |
| 2 | PID 定宽编码 `%010u`→`%09u`（113 字节定宽布局破坏） | `Acquire_WritesPidRecord_FixedWidth` **红**（StoreLockTest.cpp L290 定宽长度断言真实失败）；还原复绿 |
| 还原后 | `git checkout --`＋重编＋全量重跑 | **38/38 复绿** ✓ |

### 4.6 架构红线 —— PASS（AGENTS §5 八条逐条）

1. **L2 零 Qt**：project 全目录 `#include <Q…/Qt…` 扫描唯一命中为 BuildRedLineTest.cpp 内红线扫描器自身的字符串字面量（扫描逻辑，非 Qt 头）；新增六文件零 Qt；CMake 配置期守卫（`^Qt…` FATAL）＋`ProjectBuild.NoQtInclude_DT_BUILD_NFR_MNT_01` 两模式通过 ✓。
2. **R-1 业务域互链禁止**：产品库唯一单元边 project→core（CMake LINK_LIBRARIES/INTERFACE 两面读取的配置期守卫实测通过）；其余协作全走注入（IDiagnosticsSink/ILockOps）；`ProjectLinkage` 5 用例两模式通过＋ird_gates 零 project 条目 ✓。
3. **R-2 私有头不出单元**：新增公共头仅 StoreTypes.hpp 位于 `include/sdurws/ird/project/`；ILockOps/StoreLock/PathCanonical 头均在 `src/win32/`（`ProjectBuild.PublicHeaderPathLayout_DT_BUILD_R2` 直接证据）；跨单元 include 仅 `<sdurws/ird/core/DiagData.hpp>`——core 的 include/ 公共头（验收者路径实测 `core/include/sdurws/ird/core/DiagData.hpp`），且为契约 acceptance 4 明文要求的消费面 ✓。
4. **SA-02 框架零修改**：diff 无框架文件、patches/ 零新文件（见 4.1）✓。
5. **R-4 名称前缀拼接/剥离**：新增源码零 RobWork 名称操作逻辑（grep 实证）✓。
6. **PA-1 权威唯一**：锁语义为 project 单元 §9 设计内职责；PRJ-* 码值逐字取自 diagnostics.md §4.6 收编清单（不私造码）；DiagnosticRecord 无 severity 字段——权限/Warning、权限/Error 映射保留在 diagnostics 码表侧，project 只产记录载荷 ✓。
7. **PA-2 不可变历史**：本任务不触及修订/写路径历史面 ✓。
8. **不恢复 old/ 实现**：零 old/ 引用；全部实现按 units/project.md §9 从零编写（对照文档逐节核对）✓。

### 4.7 注释规范 —— PASS（公共头全查＋实现文件抽查 6 个）

- **公共头全查**：`StoreTypes.hpp`——文件头（设计依据 §3.1/§5.0/§9.2/契约 acceptance 2/4＋P-PR-6 背景说明）、类/结构 Doxygen 中文描述、生命周期与所有权（非 owning sink）、线程约束（实现方须按 §9.8 串行化）、错误语义、字段单位（PID＝OS 标识、心跳＝ISO-8601 UTC）全部在位。
- **实现文件抽查**：`StoreLock.hpp`（三条铁律＋三道防线＋范围声明＋线程约束＋错误语义）、`StoreLock.cpp`（每步语义段注释：三码表常量来源、撕裂容忍定位策略、心跳线程"无权限状态写通路"的结构性说明、锁外 join 的死锁说明、锁内核与公开版分体的 EDEADLK 依据）、`ILockOps.hpp`（窄接口背景＋故障点冻结＋线程/错误语义）、`PathCanonical.hpp/.cpp`（逐步注释＋竞争窗口处置＋towlower 与 NTFS 口径一致性说明）、`CMakeLists.txt`（增量段中文注释块注明出处与红线依据）、三个测试文件（每用例"锚定"注释注明 acceptance/需求/AT 追溯）——§2.5 高危信息表逐项覆盖（所有权/线程/错误语义双分类/确定性说明〔utcNowIsoMilli 明示不得参与 CON-05 身份计算〕/字节宽度单位）。无中英混杂机翻腔、无复述式废注释、无过期注释。

### 4.8 文档同步＋findings 消账核对 —— PASS

- 单元卡增量：`units/project.md` §15.4 v0.5 行（ad01b11b）——产物清单、§9.1～§9.6 兑现、P-PR-6 处置、三项实现口径登记（113 字节定宽布局／SDK 基线按 TU 提升／初始心跳调用方时钟供给——§9 未定义判据按 DTB §5.4 登记而非私裁）、测试结论（两模式 48/48）与验收者独立复现**逐项一致**；本文契约语义零变更 ✓。追踪矩阵无本任务登记义务（PRJ-T01/T02 同判例；契约 outputs 的 traceability-update 由 builds/wp04-t03/ 承载）。
- **findings.json 未关闭条目相关性核对**（105 项 open，逐族排查）：
  - **F-007**（全新集成树配置参数未载明＋ini 模板被 .gitignore 吞）：本验收**亲历复踩**（裸配置 Boost 失败→补 toolchain；RobWorkStudio/src/CMakeLists.txt:58 失败→复制两件本地资源）——保持 open，非本任务辖域。
  - **F-008**（全新树全量框架构建失败）：构成本次 4.2 目标面口径的原因之一——保持 open。
  - **F-010 / F-125**（.gitignore 吞留痕日志／幻影清单家族）：**本任务再现**（送验提交声称的 10 份 .log 全部未入库，见 4.10；F-125 处置建议③"PRJ-T02 的日志随 PRJ-T03 送验补交"亦未执行）→ 转登 **F-127**。
  - **F-012 / F-126 家族**（提交标题非动词开头）：65520d6a 标题"PRJ-T03 验证留痕归档…"以任务名词开头 → 转登 **F-128**。
  - **F-115**（ird_gates 8 命中基线登记义务悬置）：本任务门禁命中集与该基线逐项同分布、零新增——登记义务仍归 WP-01-T03/所有者，保持 open。
  - **F-123**（CoreEdgePresent 敏感性缺口）：本任务触及 project/CMakeLists.txt（增列源文件/测试目录/GTest 换链）但守卫文本未改动；且本任务后该边已承载真实头消费（StoreTypes.hpp→DiagData.hpp），缺口形态不变——保持 open，PRJ-T04 收口建议不变。
  - **F-124**（"冒烟模式无别名"表述失准）：涉及的 alias 条件块不在本任务 diff 触及行内——保持 open。
  - 其余 open 条目（policy/runtime/evidence/diagnostics 各族）与本任务无交叠。

### 4.9 commit 质量 —— PASS（附建议级 F-128）

- 三提交逐一核对：
  - a3ac8fa5 `[WP-04-T03] 实现 project 写锁与路径规范化（StoreLock/PathCanonical）`——动词开头 ✓；四段齐备；"实施期两处缺陷由测试真实失败驱动修复"的声明与 ird-test-report.json 留痕互证（死锁拆分锁内核/事件生存期竞速），且经 4.5 探针证实该两组用例确有致红能力——非虚构叙事。
  - ad01b11b `[units] project.md v0.5：…`——单元卡前缀规范车道（AGENTS §6.2）✓。
  - 65520d6a `[WP-04-T03] PRJ-T03 验证留痕归档（…）`——前缀后以任务名词开头非动词 → **F-012/F-126 家族再现，转登 F-128**（纯风格；正文四段齐备＋`Acc-Covered: 1,2,3,4` 对账行在位）。
- 验证段结论逐项比对验收者复现：两模式 38/38＋10/10 ✓、ird_gates 8 命中零 project ✓、validate-task PASS ✓、冒烟 toolchain 绝对路径 ✓——**全部为真**；"集成模式全量构建 exit 0"因构建日志未入库（F-127）无法核对树身份，但其可复核面均被独立证实，无结论性粉饰。

### 4.10 留痕完整 —— PASS（附建议级 F-127）

- 入库核验（`git ls-tree 65520d6a -- builds/wp04-t03/`）：`ird-test-report.json`＋双模式 gtest XML 四份——**ird-test-report.json 在库且与实现/测试逐一对应**（四段式验收自证＋acceptance 1～4 逐条 evidence 映射＋回归 48/48），其 acceptance 对账表承担 resultNote 载体，按 ACC §4.10"或"分支判定成立（PRJ-T01/T02 同判例）；`phase-one-validation.log`/resultNote 未更新——同上判例不阻断。
- 幻影清单家族再现：提交正文声称的"双模式构建/配置日志＋测试运行日志＋validate-task.log＋verify-target-build.log＋gates-ird-gates-target.log"等 10 份 .log **实际入库 0 份**（实施工作树本地存在、命中 `.gitignore:87 *.log`——check-ignore 实证）；ird-test-report.json 的 gates 字段引用 validate-task.log/gates-ird-gates-target.log 亦为库外幻影引用 → **F-127**（F-081/F-102/F-103/F-106/F-108/F-111/F-113/F-114/F-125 家族第 16 例）。按家族判例（证据本体不缺＋数值主张全部复现为真）登记建议级、不阻断；构建/测试可复现性已由验收者亲手建立（4.2/4.3）。

### 4.11 偷懒/缩水实现扫描 —— PASS

1. **标记扫描**：diff 新增行 `TODO|FIXME|stub|placeholder|未实现|not implemented|XXX|HACK` 零命中（grep 实测 exit 1）；无契约标注"触发式延后"项需要豁免列表。
2. **空实现检测**：公共接口无空体——IDiagnosticsSink 为纯虚契约（§5.0 原文形态，实现由外部注入）；Win32LockOps 六方法全部为真实 Win32 薄封装（CreateFileW/SetFilePointerEx/SetEndOfFile/WriteFile/FlushFileBuffers/GetHandleInformation/CloseHandle 逐行在位）；StoreLock 状态机/心跳线程/撕裂容忍解析均为实质实现（731 行实现文件逐段审读）。
3. **逐条证据表**：契约 acceptance 1～4 逐条证据见 4.4 表——每条均有具名测试＋验收者两模式实测输出＋（第 4 条）文件行号级码值核对，**无任何一条缺证据**。
4. **测试强度**：断言校验行为与边界而非恒真——三码分类含反例（112 不锁存）、"恰一胜者"以 `{0,3}` 排序向量逐值断言、文件身份三元组跨崩溃-接管逐字段比较、撕裂/垃圾字节容忍读、陈旧心跳双向判据、8.3 短名卷禁用如实降级不伪造通过；与 4.5 联动：两组实现级探针注入即红（同文件第 290 行定宽断言、分类表封闭性断言均实证存活）。

---

## 2. 裁决

**verdict = pass**（4.1～4.11 全部通过，零阻断级问题；不阻塞合入）

建议级问题 2 项，已按 ACC §5 强制转登 `traceability/findings.json`（编号顺延自 F-126）：

| 编号 | 摘要 | 严重级 |
| --- | --- | --- |
| F-127 | 幻影清单家族再现（F-081 系第 16 例，F-125 后同日同单元再例）：65520d6a 正文声称入库"双模式构建/配置日志＋测试运行日志＋validate-task.log＋verify-target-build.log＋gates-ird-gates-target.log"等 10 份 .log，实际入库 0 份（本地存在、.gitignore `*.log` 吞没——check-ignore 实证）；ird-test-report.json gates 字段同持库外幻影引用。F-125 处置建议③（PRJ-T02 日志随本任务补交）未执行。数值主张经验收者 detached worktree 从零复现全部为真，按家族判例不阻断 | suggestion |
| F-128 | 提交标题家族再现（F-012 系，F-121/F-126 同日再例）：65520d6a 标题"[WP-04-T03] PRJ-T03 验证留痕归档…"前缀后以任务名词开头非动词开头；纯风格瑕疵，四段齐备、Acc-Covered 对账在位、结论与留痕一致 | suggestion |

## 3. 验收者环境说明

- 复现现场：`git worktree add --detach D:/10_Source_Repos/21_robot/.acc-tmp/prj-t03-a 65520d6a…`（detached @SHA）；冒烟构建树＝`.acc-tmp/prj-t03-smoke`（独立目录、vcpkg toolchain 绝对路径）；记录回传：`git worktree add -b acc/PRJ-T03/3 D:/10_Source_Repos/21_robot/.acc-tmp/prj-t03-b origin/redesign-main`。
- 集成配置参数（与主树缓存一致；F-007/F-062 口径）：`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=D:/10_Source_Repos/21_robot/RobWork/vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64 -DRWS_USE_STATIC_LINK_PLUGINS=ON`；`RobWorkStudio.ini.template.static/.shared.in` 自主树复制入 worktree（git 未跟踪本地资源）——裸配置两次失败（Boost 不可解析／RobWorkStudio/src/CMakeLists.txt:58 configure_file 缺文件）后按登记口径补齐，亲历证实 F-007 描述仍然成立。
- 探针方法：实现级注入须重编译——Edit 修改源文件（新 mtime）→ 增量构建 → 滤跑目标用例 → `git checkout --` 还原 → 确认工作树干净 → 全量重跑复绿（规避 F-068/F-075 mtime 陷阱）。
- 本记录证据日志（configure/build/test-run/gates/探针原始输出）留存于验收者临时证据目录（随 worktree 清理）；持久证据以本记录内联关键输出＋送验提交自带 5 件留痕（已逐一核对实际入库与内容真实性）为准。
- 独立性声明（ACC §2）：全新上下文；仅消费契约/设计文档/送验产物与留痕（含既往验收记录作为判例口径引用），未消费实施者叙述；构建与测试全部亲手重跑；对抗式立场下逐项取证（实测门禁、双探针改红、码值逐字核对、幻影清单比对、红线机械扫描）。
- 实施产物零改动声明：验收全程未向 `wp04-t03` 推送任何提交、未修改送验对象内容（4.5 探针仅落在一次性 detached worktree 内并已还原）；本分支仅承载本记录与 findings.json 两条建议级登记。

## 4. 记录三元组（回传给编排者）

```
branch: acc/PRJ-T03/3
path:   RobWork/doc/industrial-robot-design/traceability/acceptance/PRJ-T03-20260915.md
commit: （见本分支推送后记录提交的 40 位 SHA——由验收会话随最终报告输出）
```
