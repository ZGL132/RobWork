# WP-13-T09 验收记录（attempt 2）

| 字段 | 值 |
| --- | --- |
| 任务 ID | WP-13-T09（实现 DH↔显式转换五状态判定与等价验证） |
| 送验对象（冻结） | `12284f7280db2ccbdcc692a43583544c4b291984`（分支 wp13-t09 尖端；漂移检查：`git rev-parse origin/wp13-t09`＝该 SHA，无漂移——本记录全程按该 SHA 复现，不按分支名） |
| diff 范围 | `0bb40848d219d117df8f80a8c963d9044077960b..12284f7280db2ccbdcc692a43583544c4b291984`（3 提交：b0cf4479 实现＋7e8d3c36 文档同步/留痕＋12284f72 attempt 1 B-1 返工） |
| 前次验收 | attempt 1（2026-09-25，verdict=**fail**，阻断 B-1＋建议 G-1~G-4→F-337~F-341）：记录 `acc/WP-13-T09/1` 分支 `traceability/acceptance/WP-13-T09-20260925.md`（送验对象 7e8d3c36，未合入 redesign-main） |
| 验收分支／记录 | `acc/WP-13-T09/2`，本文件 |
| verdict | **pass**（4.1~4.11 全部通过；无阻断级问题；无新增建议级问题——F-337/F-339/F-340/F-341 经亲跑复现确认已由返工修复，F-338 按登记口径维持 open） |
| 验收日期 | 2026-09-25 |

## 0. attempt 1 阻断项（B-1/F-337）修复复现 —— 已修复

attempt 1 阻断项：`contract_test/CommandPipelineContractTest.cpp:288` 的 T08 时代"T09 行不预建"钉住断言（`registry.find("MDL-DH-NOT-EXPRESSIBLE")==nullptr`）未随 T09 三码合法登记同步，冻结源码全新构建 contract_test 8/9 FAILED、在库 9/0 XML 不可复现。

本次亲跑复现（全新 detached worktree @12284f72，全新配置全新构建）：

- 返工 diff（7e8d3c36..12284f72）核对：钉住断言改为在册断言——T09 三码经 `DiagCodes.hpp` 常量同源对账（`kMdlDhNotExpressible/kMdlDhApproximate/kMdlDhAnalysisFailed`）＋`ownerUnit=="modeling"` 逐码断言＋`NOT-EXPRESSIBLE.confirmable=false`＋`APPROXIMATE.requiresComparison=true`；缺席断言仅余 T13/T18 行（`MDL-EXPORT-FAILED`/`MDL-21-COUPLING-STAGE-LOCKED`）；测试更名 `StableCodesRegisteredWithoutPrebuilding_WP13T09`（CommandPipelineContractTest.cpp:263，文件头注/用例注分期口径同步）。
- 亲跑 `sdurws_ird_modeling_contract_test.exe`：**9/9 PASSED**（集成全新构建，见 4.3）——与在库重录件 `integration-modeling-contract-test.xml`（9/0，timestamp 2026-09-25T02:34:51）一致，attempt 1 的留痕矛盾消解。

## 1. 逐项证据（ACC §4，4.1~4.11）

### 4.1 改动范围 —— 通过

- `git diff --name-only 0bb40848..12284f72` 共 **17 文件**，逐一比对契约 `allowedFiles`：modeling 单元 11 文件（CMakeLists.txt、include 的 DhConvert/CommandHandlers/DiagCodes 三头、src 的 DhConvert/CommandHandlers/DiagCodes 三件、contract_test/CommandPipelineContractTest.cpp、test 的 DhConvertTest/DhConvertEquivalenceTest/DiagCodesTest 三件）⊆ `modeling/**`；traceability/builds/wp13-t09/ 5 件留痕 ⊆ `traceability/**`；`units/modeling.md` 在列。
- `forbiddenFiles`（REQUIREMENTS.md/ARCHITECTURE.md）零触碰（同名 grep 零命中）；diff 无框架源码文件、无 `patches/` 新文件（SA-02 通过）。

### 4.2 可复现构建 —— 通过（双模式零错误）

- **复现现场**：`git worktree add --detach /d/10_Source_Repos/21_robot/acc-t09-r2-A 12284f72…984`；ini 模板环境件（`RobWorkStudio.ini.template.static`/`.shared.in`）自主树复制（F-007/F-098/F-099 在案口径）。
- **集成模式**（全新配置）：`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=<repo>/vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64 -DCMAKE_CXX_FLAGS="-DNOMINMAX -DWIN32_LEAN_AND_MEAN -DWIN32"` → CONFIGURE_EXIT=0；缓存确认 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`、`RWS_USE_STATIC_LINK_PLUGINS:BOOL=ON`（与主树口径一致）。
  - `cmake --build build --config Release --target sdurws_ird_modeling --parallel 8` → **exit 0**，`error C|error LNK|: error|error MSB` 计数 **0**（日志 acc-t09-r2-build1.log，901 行，BUILD1_EXIT=0）；
  - `sdurws_ird_modeling_test` → **exit 0** 计数 0（build2.log，BUILD2_EXIT=0）；
  - `sdurws_ird_modeling_contract_test`（F-338 interim 口径补建）→ **exit 0** 计数 0（build3-contract.log，BUILD3_EXIT=0）。
- **独立冒烟模式**（独立临时目录 `/d/10_Source_Repos/21_robot/acc-t09-r2-smoke`，`-S …/industrialrobot`＋vcpkg toolchain＋Qt CMAKE_PREFIX_PATH，AGENTS §4.1 修正口径）：配置 exit 0；全量构建 **SMOKE_BUILD_EXIT=0**、错误计数 **0**（smoke-build.log 640 行）。

### 4.3 契约 verify 命令 —— 6 条逐条亲手执行，全部成功

| # | 命令 | 结果 |
| --- | --- | --- |
| 1 | `cmake --build build --config Release --target sdurws_ird_modeling` | exit 0，零错误 |
| 2 | `cmake --build build --config Release --target sdurws_ird_modeling_test` | exit 0，零错误 |
| 3 | `ctest --test-dir build/RobWorkStudio/src/rwslibs/industrialrobot/modeling -C Release -L "^ird$"` | **2/2 PASSED**（modeling_test＋contract_test，exit 0）；补亲跑两 exe：`sdurws_ird_modeling_test` **159/159**、`sdurws_ird_modeling_contract_test` **9/9**（含更名后 `StableCodesRegisteredWithoutPrebuilding_WP13T09`）——与在库 integration-modeling-test.xml（159/0）、integration-modeling-contract-test.xml（9/0）逐件一致 |
| 4 | `cmake --build build --config Release --target ird_gates` | exit 1＝存量命中（门禁按设计命中即失败；MSBuild 捕获有编码污染——F-336 在案）；按 F-336 建议采用**引擎直跑口径**取证：`cmake -DIRD_ROOT=<…> -P cmake/ird_gates.cmake`，head 侧（worktree @12284f72）**119 条命中**、base 侧（detached worktree @0bb40848）**119 条命中**，路径归一化后**多重集 diff 为空＝零新增命中**（与在库登记口径一致） |
| 5 | `pwsh -File RobWork/scripts/industrialrobot/validate-task.ps1 -TaskFile …/WP-13-T09.json` | **PASS** (1 tasks)，exit 0 |
| 6 | `pwsh -File RobWork/scripts/industrialrobot/validate-docs.ps1` | **PASS** (20 units, 12 trace entries, 189 task files)，exit 0 |

- 冒烟补充亲跑：`sdurws_ird_modeling_test` **128/128**、`sdurws_ird_modeling_contract_test` **5/5** PASSED——与在库 smoke-modeling-test.xml（128/0）、smoke-ird-test-report.json（128 条）一致（gated 子集：DhConvertEquivalenceTest/CommandHandlersTest 等随 TARGET sdurw_kinematics gating 不编入冒烟，CMakeLists.txt 有据）。

### 4.4 acceptance 条目 —— 五条均有对应通过证据（全部为本次亲跑复现）

| 契约 acceptance | 证据（亲手复现，集成模式 159＋9 内具名用例） |
| --- | --- |
| 1 无损展开/零位对齐/基座混入拒绝 | `MdlDhConvert.DhToExplicitAccumulatesCardFormula_WP13T09_ACC1`（独立参考展开逐级累乘对照）、`DhToExplicitZeroAlignment_WP13T09_ACC1`（q=0 显式位姿==DH 派生位姿——θ_offset 与 zeroOffset 分离后两者同入几何）、`DhToExplicitRejectsEmptyAndMixedBase_WP13T09_ACC1`（ChainEmpty＋DegenerateBase 两态拒绝，DhConvert.cpp:922/928 契约检查）——3 用例 PASSED |
| 2 五状态样例/结构检查先行/逐关节逐项上界 | `ExplicitToDhNotExpressibleTerminal_WP13T09_ACC2`（prismatic 等结构违例终判不进求解）、`ExplicitToDhExactOnDhDerivedChain_WP13T09_ACC2`（六轴 UR 类非平凡链逐关节 δθ/δd/δa/δα 逐项 ≤1e-9 回收＋deviations 逐项 ≤1e-9＋Exact 无诊断）、`ExactNonUniqueCoincidentAxes/ParallelAxes_ACC2/3`——PASSED；`MDL-DH-NOT-EXPRESSIBLE` 三码经真实 StableCodeRegistry 在册断言（contract_test 9/9 内） |
| 3 退化族字典序/确定性 | freeCoordinates 字典序 `{0,4}`（θ1/θ2）显式断言（DhConvertTest.cpp:526-527）＋钉中性值 `EXPECT_DOUBLE_EQ(…thetaOffset, 0.0)`（:528-529/571）＋解集证人 `ASSERT_GE(solutionSet.size(), 2U)`（:531）；`ApproximateNotAuthority`（E=Σ(轴角+原点)>0 :622、比较型三要素、不产权威资格）；`AnalysisFailedNotSemantic`（1e200 溢出样本、parameters 空、Diverged、恰一条 ANALYSIS-FAILED 诊断）；确定性 `EXPECT_TRUE(result == repeat)` 逐字段相等两处（:537/:580）——PASSED |
| 4 verifyEquivalent/CompileProbe/roundtrip | `VerifyEquivalentAcceptsSameChain_WP13T09_ACC4`（maxPositionDeviation ≤1e-9 断言 DhConvertEquivalenceTest.cpp:355、探针调用计数 `EXPECT_EQ(probe.calls, 2)` :359）、`VerifyEquivalentRejectsDeviatedChain`（1e-6 注入 → maxPositionDeviation >1e-9 :392）、`RoundtripDoubleConsistency`（参数级＋FK 级双重一致 :400/:430）——PASSED；测试替身以 runtime 公共头（Compiler/CanonicalModel/Description/Errors/Sources.hpp）装配 S5，零私有头 include（R-2 亲查）；该装配取舍已登记单元卡 §14.6 v0.10 ⑥（DTB §5.4 已登记偏差，非私设） |
| 5 权威切换独立领域命令 | 域门四道：`AuthoritySwitchDomainGatePreparesCandidate`（候选 authority=StandardDH＋dhDerived 逐关节等于判定解 :460-468）、`…RefusesPendingEdits`（C-6 先决断、门②拒绝后探针 0 调用 :502——门序短路）、`…RefusedWhenVerificationFails`；命令 prepare 面：`AuthoritySwitchPreparesInsideCommandPrepare`（prepare 计算解重编码）、`…RejectsNotExpressibleWithTerminalDiag`（C-3 终判拒绝）、`…RejectsMixedEdits`、`NonSwitchPayloadUnaffectedByGate`（非切换载荷零影响）、`…WithoutServicesFailsFast`（装配缺失 fail-fast）——PASSED |

无"未执行标通过"项；`ird_gates` 门禁已落地（WP-01-T01），本次按引擎直跑口径真实执行（见 4.3 第 4 条）。

### 4.5 测试真实失败能力 —— 通过（两处变异均致红，注入后已还原）

在 detached worktree A 内对 `src/DhConvert.cpp` 注入（各自重建→亲跑→还原，还原后 `git diff HEAD --stat`＝0 行确认源码与送验 SHA 一致，重跑复绿 159/159）：

- **M1**：`dhToExplicit` 步① 基座混入拒绝分支条件置假（`if (chain.mixedInBaseTransform.has_value())` → `if (false && …)`）→ **1 FAILED**：`DhToExplicitRejectsEmptyAndMixedBase_WP13T09_ACC1`（恰为该拒绝面的具名用例）；
- **M2**：`dhStepTransform` 卡面公式平移向量 `(a,0,d)` → `(d,0,a)`（DhConvert.cpp:233）→ **5 FAILED**：ACC1 累乘公式/零位对齐＋Exact 回收＋重合轴/平行轴 ExactNonUnique（展开公式破坏沿传播链被三层断言捕获）。

断言校验行为与边界，非恒真；变异面与断言面一一对应。

### 4.6 架构红线 —— 通过（AGENTS §5 八条逐条）

1. **L2 零 Qt**：modeling 产品面（include//src/）grep `Qt/Q_OBJECT/QWidget` 零命中（唯一命中为 Errors.hpp 注释中的需求名 NFR-MNT-01）；门禁命中集比对零新增（Qt 面命中全部为 ui 单元存量，head=base）。
2. **R-1 业务域互链**：include//src/ 无 `ird/<requirements|kinematics|trajectory|dynamics|selection|optimization>` include；产品链接面＝六条登记边（core/diagnostics/project/runtime/policy/io，CMakeLists.txt target_link_libraries＋配置期 FATAL 守卫＋ird_gates_whitelist 同源）；门禁零新增。
3. **R-2 跨单元私有头**：新公共头仅在 `include/sdurws/ird/modeling/`；对 runtime 的消费全部经公共头（DhConvert.hpp:80-82 与 DhConvertEquivalenceTest.cpp:32-36 亲查，CompilerImpl.hpp 等私有头零 include）。
4. **SA-02 框架零修改**：diff 无框架文件、patches/ 零新文件（4.1）。
5. **R-4 名称前缀**：diff 内无 "RobWork" 名称前缀拼接/剥离逻辑（grep 拼接/剥离/prefix 零命中）。
6. **PA-1 权威唯一**：转换器为纯函数服务；verifyEquivalent 经注入探针只读走 S1~S5（不发布快照、不产生修订——CompileProbe 契约注）；切换判定/验证在命令 prepare 内执行、候选以 prepare 计算解重编码（CommandHandlers.cpp ③.5 门，:866 起）；写路径仍归 project。
7. **PA-2 不可变历史**：本任务无修订写面（拒绝面"验证失败不产生修订"有测试钉住）。
8. **knownPitfalls P-MDL-8**：未触发——buildCanonicalModel 分段入口签名未变（集成模式真实编译链接通过亲证；单元卡 §14.6 v0.10 ⑦ 登记）。

### 4.7 注释规范 —— 通过

- **公共头全查**：`DhConvert.hpp`（新增 647 行）——文件头（设计依据/三方法背景/容差纪律"附录 D 唯一书写点"/确定性 NFR-COR-01/02/线程安全 §3.4）＋全类型全方法 Doxygen 中文注释；§2.5 高危信息逐项在位：物理单位（rad/m 逐字段，:92-101/155-157/266-268）、坐标系（origin=T_parent_joint、core.md §4.6 读法）、角度制式（"单位 rad（不是度）"语义逐处显式）、所有权（CompileProbe 非 owning、调用方存活保证 :344-346）、线程约束（无共享可变状态/可重入）、错误语义（值面 DhErrorCode 与判定面五态互斥 :112-116；三码产码唯一书写点）、确定性来源（工程阈值 vs 过程常数两类分立，cpp 文件头"实现纪律"段）。`CommandHandlers.hpp` T09 增量（HandlerServices 注入位＋③.5 门）与 `DiagCodes.hpp` T09 三码常量块注释完整。
- **实现文件抽查 ≥3**：①`DhConvert.cpp`（文件头实现纪律＋逐步算法注"步①/步②/步③"、两阶段求解"相位 1/相位 2"逐段、解析种子帧相位数学含义、择优规则）；②`CommandHandlers.cpp` ③.5 权威切换门逐段（触发条件/C-6 投影①②/受权威模式管辖字段不比对的语义）；③`DiagCodes.cpp` T09 三码登记块逐字段口径注（confirmable/requiresComparison/retryable 的卡面依据）。三件均达标。
- **CMakeLists.txt** 关键步骤（目标注册/六边守卫/gating/宏）中文注释块：达标。
- attempt 1 的 F-339 修复点亲查：`DiagCodes.hpp:181` @brief 已改"当前 20 项"，与工厂 return 实点计数 20 项（DiagCodes.cpp:546-551 逐名点数：8+9+3）及 `DiagCodes.cpp:15` 文件头注一致。

### 4.8 文档同步＋findings 相关性 —— 通过

- **实现与任务卡零未登记偏差**：`IDhExplicitConverter` 三方法签名与卡 §9.4.7 原文逐参一致（亲查对照）；§7.5 判定树/§7.6 切换条件与实现结构一致；实现细化（两阶段求解相位 2 解析种子、自由坐标语义注〔平行轴样例自由坐标为末级 θ——实现细化非语义变更〕、零位折叠、链级 Description 与 T12 分工、命令面 C-6 投影、探针 S5 装配取舍、P-MDL-8 处置）全部登记于单元卡 §14.6 v0.10；返工增量登记于 §14.6 v0.11（B-1/F-339/F-340/F-341 消账＋F-338 维持 open＋重验留痕数字）——文档头 v0.11。
- 契约状态字段仍为 `ready`（翻转归收尾 PIPE §4.7⑤，非实施面缺陷）。
- **findings F-337~F-341（attempt 1 同任务转登条目）闭环状态逐条核对**（登记册副本在 acc/WP-13-T09/1，随该 evidence 分支待收尾并流；本次已把五条按核对结论补登本分支登记册——见 §3）：
  - **F-337（blocking，B-1）→ 已修复（本次亲跑复现）**：全新构建 contract_test 9/9（见 §0/4.3），在库 9/0 XML 可由送验源码复现——阻断消解；
  - **F-339 → 已修复**：DiagCodes.hpp:181 计数注释 19→20，与工厂实点 20 项一致（4.7）；
  - **F-340 → 已修复**：集成侧 ird-test-report.json 重收为全量 168 条（summary 168/168＝modeling_test 159＋contract_test 9），与两份在库 gtest XML 逐件可对账；
  - **F-341 → 已修复**：DhConvert.cpp IRD_DH_DEBUG 调试块已清除（grep `IRD_DH_DEBUG|fprintf` 零命中），非有限初值分支语义保留＋补业务含义注释；
  - **F-338 → 维持 open（按登记口径，属预期）**：契约 verify 数组缺 `sdurws_ird_modeling_contract_test` 构建行，所有者为契约编译/治理侧（契约文件 CCP 冻结产物，实施侧不私改）；本次验收按 interim 口径补建该目标后全量执行（ctest 2/2）——不阻塞本次验收。
- **findings F-318~F-336 其余在册条目逐条核对**（与本次送验面关联性）：
  - F-318~F-320：登记册无独立 id 条目（F-321 resolution 注明 acc/UI-T16/1 已占用）——无关联；
  - F-321~F-325、F-327~F-330：UI-T16/T17/T18 宿主冒烟取证管线/文档勘误/UX 语义族——与 modeling/DH 转换域无关联，无消账面；F-331~F-334 在 acc/UI-T18/1 分支登记册（主线册无独立条目），其处置由 F-335 条目承载；
  - F-326：已 fixed（主线登记册在案）；
  - F-335（F-331~F-334 消账未回写）/F-336（ird_gates MSBuild 捕获截断/编码污染）：所有权治理侧——本次验收按 F-336 建议采用**引擎直跑口径**完成命中集比对（head=base 多重集 diff 空），两案维持 open、本任务无可消账面；
  - **与送验面相关的存量**：F-302（modeling pugixml LIB 词表命中）本次 head 侧仍在（IRD-GATE-LIB 1 条，head=base 存量、O-40 已登记依赖，消账归词表维护方）；F-317（留痕收取管线不完整）的 T09 复发即 F-340，已随返工修复；F-007/F-062/F-098/F-099/F-101（全新树配置环境链）本次再次实证（ini 模板复制＋toolchain＋Qt 前缀＋CXX flags 全套后配置构建零错误），维持 open、所有权 AGENTS 维护方；F-293/F-298/F-299/F-303/F-308/F-313（提交标题家族）本三提交均为 `[WP-13-T09]` 动词开头，未再现。
  - 无 open 条目声称由本任务修复而实际未修复——无静默积压。

### 4.9 commit 质量 —— 通过

- 三提交 `b0cf4479`（实现）/`7e8d3c36`（文档同步＋留痕）/`12284f72`（返工 B-1＋消账＋重验留痕）——均 `[WP-13-T09]` 前缀＋动词开头；正文四段（What/Why/验证/影响面）齐备，代码与测试/文档/留痕同提交。
- 计数自洽亲核：159＝139＋20（MdlDhConvert 9＋MdlDhEquivalence 6＋MdlDhCommand 5，与测试文件 TEST() 逐一对应）；集成侧报告 168＝159＋9；冒烟 128＋5 与 CMake gating 逻辑一致。
- 验证段结论与本次复现一致：159/159＋9/9（集成亲跑）、128/128＋5/5（冒烟亲跑）、ctest 2/2、双 PASS、门禁零新增命中——**无粉饰**。

### 4.10 留痕完整 —— 通过

- 在库留痕五件（traceability/builds/wp13-t09/）：`integration-modeling-test.xml`（159/0）、`integration-modeling-contract-test.xml`（9/0，**attempt 1 不可复现件已重录，本次亲跑一致**）、`ird-test-report.json`（summary 168/168，含两 exe 全量用例级明细）、`smoke-modeling-test.xml`（128/0）、`smoke-ird-test-report.json`（128 条）——五件与本次全新复现逐件对账一致（attempt 1 的 B-1 留痕矛盾消解，F-340 修复到位）。
- 构建日志与 validate 日志不在在库要求内（五件留痕口径）；phase-one-validation.log/resultNote：本任务未涉及（同 T08 先例）。

### 4.11 偷懒/缩水实现扫描 —— 通过

- ① 关键字扫描：diff 新增行 `TODO|FIXME|XXX|HACK|stub|placeholder|not implemented|未实现|待实现` **零命中**；`占位` 5 处均为注释语义（报告字段缺省值说明——NFR-COR-03"未对照不伪造数值"），非 stub 实现（DhConvert.hpp:391/:394、DhConvert.cpp 输出面等，逐处核过上下文）。
- ② 空实现检测：`IDhExplicitConverter` 三方法与 `prepareAuthoritySwitch` 均实质实现（DhConvert.cpp 1679 行：展开累乘/两阶段 LM/雅可比秩/字典序定值/FK 对照/四道门）；`dhToExplicit` 的 diags 输出参数为契约预留位（头注 :449-454 明确"本方法两态均不产诊断——无登记码不得产诊断"语义，非缩水）。
- ③ acceptance 逐条证据表：见 4.4（每条具名测试＋本次亲手复现结论，任一条无证据即 fail——本表五条全部有证据）。
- ④ 测试强度：断言校验行为与边界（逐关节逐项 1e-9、字典序钉值 EXPECT_DOUBLE_EQ、逐字段确定性相等、探针调用计数 2/0、E>0、1e-6 注入拒绝）——无恒真、非"只编译通过"；与 4.5 联动两处变异实测致红。
- attempt 1 的 F-341 修复点亲查：DhConvert.cpp `IRD_DH_DEBUG`/`fprintf` 零命中，调试块已清除。

## 2. 问题清单

### 阻断级

无。

### 建议级（新增转登）

无——本次验收未发现新的建议级问题（F-342 起编号未动用）。

### 存量条目处置注记（本分支登记册已同步，见 §3）

| 编号 | 本次核对结论 | 处置 |
| --- | --- | --- |
| F-337（blocking） | 返工 12284f72 已修复，亲跑复现 9/9 | status=fixed（本分支登记册，fixedAt 2026-09-25，resolution 引本记录） |
| F-339 | DiagCodes.hpp:181 已改 20 项，与工厂实点一致 | status=fixed（同上） |
| F-340 | 集成侧 ird-test-report.json 重收 168 条全量 | status=fixed（同上） |
| F-341 | IRD_DH_DEBUG 调试块已清除 | status=fixed（同上） |
| F-338 | 契约 verify 缺 contract_test 构建行——所有者契约编译/治理侧 | 维持 open（按登记口径，属预期；本次按 interim 口径补建后全量执行 ctest 2/2） |

## 3. 登记册同步说明

本 evidence 分支基于 `origin/redesign-main`（0bb40848），其 `traceability/findings.json` 止于 F-336——attempt 1 转登的 F-337~F-341 在 `acc/WP-13-T09/1` 分支的登记册副本中、尚未并流主线。为使本分支登记册自洽并固化闭环结论，本次随验收记录把 F-337~F-341 五条按 attempt 1 原文补登至本分支登记册，并按 §2 核对结论置 F-337/F-339/F-340/F-341＝fixed（附 fixedAt 与本次复现出处）、F-338 维持 open。收尾按 SHA 合并两证据分支时，登记册以本副本（含状态翻转）为准归并即可。

## 4. 验收者环境说明

- Windows 10（10.0.26200 x64）；Visual Studio 17 2022（MSVC）；pwsh 7；Qt 6.11.1（msvc2022_64）；vcpkg 经典模式（主树 `vcpkg/installed/x64-windows`，gtest/pugixml 已装）。
- 复现全程按送验 SHA `12284f72…984` detached worktree（`/d/10_Source_Repos/21_robot/acc-t09-r2-A`）执行；集成配置＝AGENTS §4.1＋F-007/F-062/F-098/F-099 环境链修正口径（vcpkg toolchain＋Qt CMAKE_PREFIX_PATH＋CMAKE_CXX_FLAGS 对齐主树缓存＋ini 模板环境件自主树复制）；构建以 `--parallel 8` 执行（与契约命令语义等价）。
- 冒烟模式另配独立临时目录（`acc-t09-r2-smoke`，toolchain＋Qt 前缀）；门禁取证按 F-336 建议采用引擎直跑口径（`cmake -DIRD_ROOT=<…> -P ird_gates.cmake`），base 侧另建 detached worktree @`0bb40848` 直跑同脚本后做归一化（反斜杠→正斜杠＋worktree 前缀替换）多重集比对（head 119＝base 119，diff 空）。
- 4.5 变异注入在临时 worktree 内实施并已还原（`git diff HEAD --stat`＝0 行复核）；测试亲跑设 `QT_QPA_PLATFORM=windows`。
- 独立性声明（ACC §2）：验收者未读取实施者任何过程叙述；消费的输入限于送验 SHA 产物、任务契约、设计文档链、在库留痕与 attempt 1 验收记录（返工输入与 findings 核对的法定输入）；全部结论以亲手复现输出为准；未向 `wp13-t09` 分支推送或修改任何内容，未合入任何分支。
