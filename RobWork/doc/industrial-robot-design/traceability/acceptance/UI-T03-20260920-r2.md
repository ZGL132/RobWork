# 验收记录 —— UI-T03（工作台壳与五区布局：WorkbenchShell/布局记忆/无项目首页/状态栏）· attempt 2

| 字段 | 值 |
| --- | --- |
| 任务 | UI-T03（contract：tasks/foundation/UI-T03.json，schema ird-task/1，status=ready；interUnit=true——装配面消费 core IDomainEventBus＋diagnostics IDiagnosticSink/IRedactionService 表内边；requirements UX-09/PM-10/PM-11/PM-14） |
| 送验对象 | **e6ac9d11f51fc9b483d074f3b541f2c2ccbad911**（40 位冻结；分支 wp10-t03；`git fetch` 后 `git rev-parse origin/wp10-t03`＝该 SHA 逐字一致——无分支漂移，ACC §3 冻结判据通过） |
| diff 范围 | efed7256c618c583ede69493b2202ceb05ff0494..e6ac9d11f51fc9b483d074f3b541f2c2ccbad911（＝state.base..headSha；base 与 `origin/redesign-main` 逐字一致；range 内 3 提交：d402cfef 实现＋测试、dd9ff626 单元卡 v0.5＋门禁登记件＋留痕、e6ac9d11 返工补交 14 件 .log） |
| 验收尝试 | attempt 2（state.attempts.accept＝2；attempt 1 fail＝B-1 留痕被 .gitignore 吞没——本验收为全新上下文，只消费产物、契约、设计文档与在库留痕，未读取实施者过程叙述，亦未采信 attempt 1 记录中的任何未复核结论；全部构建/测试/门禁/治理脚本/变异试验由验收者亲手执行） |
| 验收环境 | 复现 worktree A＝`git worktree add --detach D:/10_Source_Repos/21_robot/acc-ui-t03-r2-wtA` @ e6ac9d11（集成 build 树全新配置：`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=<主仓>vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64 -DCMAKE_CXX_FLAGS="-DNOMINMAX -DWIN32_LEAN_AND_MEAN -DWIN32"`——配置 exit 0，`grep RWS_BUILD_INDUSTRIALROBOT build/CMakeCache.txt` → `:BOOL=ON` 实证；首配曾因 `RobWorkStudio.ini.template.static` 缺件失败——`bin/` 整目录被 `.gitignore:79 [Bb]in/` 吞没的 git 未追踪本地辅助件、框架 configure_file 硬依赖，F-099 既有登记环境链，自主树复制 `RobWorkStudio.ini.template.static`＋`RobWorkStudio.ini.shared.in` 两件后从零重配成功——非送验对象缺陷）；冒烟 build 树＝独立目录 `acc-ui-t03-r2-smoke`（vcpkg toolchain＋Qt 前缀——AGENTS §4.1）；GUI 测试执行＝`QT_QPA_PLATFORM=windows`＋`QT_PLUGIN_PATH=<Qt>/plugins`（不设 QT_PLUGIN_PATH 时首跑 exit 127 平台插件不可达——GUI 环境供给缺口同 `traceability/wp10-t03-gate-registrations.md` §2 既有登记，验收者亲历同款）；门禁 base 对照＝worktree B（detached 语义下的 `acc/UI-T03/2` 工作树 @ efed7256，兼任本记录回传分支工作树）；记录回传 worktree B＝`git worktree add -b acc/UI-T03/2` @ origin/redesign-main |
| verdict | **pass**（4.1～4.11 全部通过、无阻断级问题；两条新发现均为建议级，已随本 evidence 分支转登 findings.json **F-260**/**F-261**，不阻塞合入） |

## 4.1 改动范围 —— 通过

- `git diff --name-status base..head` 实证 **37 个变更文件**，逐个对照契约 allowedFiles 三模式：`ui/**` 11 件（CMakeLists.txt M、gui_test/BuildPlacementTest.cpp M、gui_test/TestMainReport.cpp M、gui_test/WorkbenchShellGuiTest.cpp A、include 三公共头 A、src 三件 A＋src/WorkbenchShell_p.hpp A、test/ShellModelTest.cpp A）＋`units/ui.md`（M，v0.5 增量修订）＋`traceability/**` 25 件（builds/wp10-t03/ 23 件 A＋findings.json M＋wp10-t03-gate-registrations.md A）——**全部在 allowedFiles 内，零越界**。
- forbiddenFiles（REQUIREMENTS.md/ARCHITECTURE.md）零触碰（37 文件列表实证，两文件均不在 diff 内）。
- 框架源码（RobWork/、RobWorkSim/、RobWorkStudio/ 其余源码）零改动；`patches/` 范围内零变更（SA-02 无登记需求——37 文件列表实证）。

## 4.2 可复现构建 —— 通过（集成全新树重配＋全量构建＋独立冒烟；`: error` 计数 0）

| 步骤 | 结果 |
| --- | --- |
| 集成重配 | worktree A 全新配置 exit 0（补齐 vcpkg toolchain＋Qt 前缀＋CXX flags＋两件未追踪 ini 辅助件后通过；前四者为 F-098/F-099 既有登记口径——AGENTS §4.1 字面命令在全新树不可配，验收者亲历同款；ini 缺件＝F-099 环境链，非送验对象缺陷）；缓存 grep `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON` 实证 |
| 集成全量构建 | `cmake --build build --config Release` **exit 0、`: error`/`: fatal error` 计数 0**（RobWork 全库→RobWorkStudio→industrialrobot 全单元全新编译；日志 `acc-integrated-build.log`；产物 `build/RobWorkStudio/bin/Release/sdurws_ird_ui{,_test,_contract_test,_gui_test}` 全部在位） |
| 独立冒烟模式 | `cmake -S …industrialrobot -B acc-ui-t03-r2-smoke -DCMAKE_TOOLCHAIN_FILE=<vcpkg> -DCMAKE_PREFIX_PATH=<Qt>` 配置 exit 0；`cmake --build --config Release` **exit 0、错误计数 0**；`ui/Release/` 下 `sdurws_ird_ui.lib`＋三测试 exe＋运行期 DLL（Qt6Core/Gui/Widgets、gtest.dll）全部就位（与在库 smoke-configure.log/smoke-build.log 产物面同值） |

## 4.3 契约 verify 命令 —— 逐条亲手执行；两条全部 exit 0

在 worktree A 根目录按契约字面逐条独立执行：

| 命令 | 结果 |
| --- | --- |
| `cmake --build build --config Release --target sdurws_ird_ui_test` | exit 0 |
| `cmake --build build --config Release --target sdurws_ird_ui_gui_test` | exit 0 |

附加测试真实执行（验收者亲手，非采信送验留痕）：三测试可执行文件直接运行 **8/8＋3/3＋6/6 全部 PASSED**（gtest XML 留痕于验收临时目录；`sdurws_ird_ui_gui_test` 按 §12.2 以 windows 平台单实例运行）；`ctest -C Release -L ird` **3/3 通过（ird×2＋ird_gui×1）**——与在库 ctest.log 及三份 gtest XML（tests=8/3/6、failures=0）计数逐值一致。治理脚本亲跑：`validate-task.ps1 -TaskFile <契约绝对路径>` → **PASS (1 tasks)**；`validate-docs.ps1` → **PASS (20 units, 12 trace entries, 150 task files)**——与在库 validate-task.log/validate-docs.log 逐字一致。

## 4.4 acceptance 条目 —— 4/4 逐条证据（未执行项零存在）

| # | 契约 acceptance 要点 | 证据（验收者亲手） |
| --- | --- | --- |
| 1 | UI-WB-1/2/3、UI-SES-1：五区 Dock 创建与跨会话布局记忆、view.resetLayout 恢复出厂、损坏回退＋UI-LAYOUT-RESTORE-FAILED（Dev，不阻塞启动）、无项目首页三入口与项目作用域命令禁用（命令面板仍可达）；§4.1~§4.5 用例经 ctest 留痕 | `WorkbenchShellGuiTest` 四具名用例亲跑全过：`FiveZoneLayout_PersistsAcrossSessions_UI_WB_1`（五区可见性矩阵＋改形→shutdown→同设置目录重建→位形/浮动/几何还原〔±8 px〕＋设置文件存在＋.rwdesign 零写入三重断言）、`ResetLayout_RestoresFactory_UI_WB_2`（乱序→resetLayout→三区恢复＋浮窗清除＋Top/Central 恒在）、`CorruptLayoutMemory_FallsBackWithDevDiagnostic_UI_WB_3`（版本 999＋乱码注入→initialize true〔不阻塞〕→Dev 日志含码＋目录零命中〔§6.2 双面〕→layout 组三键整段丢弃）、`NoProjectHomepage_CommandGating_UI_SES_1`（首页页 index 0＋三入口可见可点＋4 条项目作用域命令 enabled=false 且 reasonKey 非空＋4 条会话/视图命令恒可达＋未登记 id registered=false）；ctest ird_gui 标签亲跑通过（§12.3 四例＋2 辅助例与 ui.md §12.3 行逐一对应） |
| 2 | 状态栏 PM-11 格式：`<显示名>[*][（只读）]`；*＝present∨会话脏；（只读）＝writable=false；布局/最近项目（PM-14）持久化用户级、绝不写入 .rwdesign | 模型层 `StatusBarFormat_PM11_Matrix_UI_T03_ACC2` 六行矩阵亲跑全过（无项目/基线/磁盘草稿/会话脏/只读/组合序）＋GUI 层 UI_SES_1 控件端到端三段翻转断言（`演示项目`→`演示项目（只读）`→`演示项目*（只读）`，标题栏同格式）；PM-14：UI_WB_1 设置文件存在＋.rwdesign 三重零命中断言亲跑通过；`RecentProjects_CapDedupPersist_PM_10_PM_14`（上限 10 裁尾/去重置顶/规范化同键/失效保留/移除/跨实例读回）亲跑全过 |
| 3 | 最小可用布局（§4.4）三要素恒在＋低于最小尺寸侧栏折叠；Shell 装配门面按 §10.1（initialize 恰好一次、子模型访问、mainWindow 唯一 Widget 出口、有界 shutdown） | 三要素恒在：UI_WB_1/2 中 Top/Central regionVisible 恒真断言＋UI_SES_1 状态栏标签（ird_status_project_text）在位断言亲跑通过；门面：initialize 返回值断言（UI_WB 系全部 ASSERT_TRUE）、shutdown 报告（completed/layoutPersisted——UI_WB_1）、shutdown 后无第二 Widget 出口（公共头亲读：唯一 `QWidget*` 出口＝`mainWindow()`，其余五区均私有——ARC-02 收口）、有界 shutdown（drainAndStop 同步 join——WorkbenchShell.cpp:221-238 亲读）。**折叠行为仅有实现面证据**（`updateCollapseBySize`＝WorkbenchShell.cpp:551-569＋阈值常量 :77-80），无行为用例——§4.4 该子句证据强度低于具名测试，转登 **F-260**（建议级；ui.md §12.3 权威用例行无此行，交付用例集「四例＋2 辅助例」已经单元卡 v0.5 登记，不构成缩水） |
| 4 | O-31 处置：ShellWiring 持 ui 自有端口（IPolicySummarySource/IUiNameResolver）；产品面零对 project/evidence/execution/policy/runtime 的链接或 include（NoCrossUnitInclude_O31_UI_BUILD 守卫常驻）；装配面消费 core＋diagnostics 表内边；P-UI-5 按 CF-1 | 验收者独立双重复核：①源码面——`grep -rn 'sdurws/ird/(project\|evidence\|execution\|policy\|runtime\|…)'` 于 ui 产品面（src/＋include/）**零命中**，include 面仅 Qt（R-3 例外单元）＋core（Events/Identity）＋diagnostics（Catalog/DiagCodes/Redaction/Logging）＋ui 自有头；②机器面——`sdurws_ird_ui_contract_test` 三用例亲跑全过（含 `NoCrossUnitInclude_O31_UI_BUILD`）；③门禁面——ird_gates base/head 双侧脚本模式亲跑，命中集 diff **恰为 3×R3**（ui 三新源文件 Qt 头——R-3 例外头包含半区机器面滞后，与 F-259 登记逐字同因，非跨单元边）；P-UI-5：本任务零 ICommandRegistry 面（CF-1 口径——ui 自有头随 UI-T06），ui→core 边照常（CMake 链接行亲读） |

## 4.5 测试真实失败能力 —— 通过（两处断言注入应失败输入，双层真实变红；试验后已还原并复绿）

在验收沙箱 worktree A（git 管控下，试验后 `git diff` 零输出＝与送验对象逐字节一致）注入两处变异：

| # | 变异 | 重建后亲跑结果 |
| --- | --- | --- |
| 1 | `UiProjections.hpp` `formatProjectStatusText` 中 `if (context.drafts.anyUnapplied())` 改 `if (false && …)`（PM-11 `*` 追加失效） | `sdurws_ird_ui_test` → **`ShellModel.StatusBarFormat_PM11_Matrix_UI_T03_ACC2` FAILED**（矩阵三行断言红）；`sdurws_ird_ui_gui_test` → **`NoProjectHomepage_CommandGating_UI_SES_1` FAILED**（状态栏控件端到端红）——模型层＋GUI 层双面均非恒真 |
| 2 | `WorkbenchShell.cpp` `restorePersistedLayout` Corrupt 分支移除 `emitDev(…UI-LAYOUT-RESTORE-FAILED…)`（保留回退与丢弃，仅删诊断出线） | `sdurws_ird_ui_gui_test` → **`CorruptLayoutMemory_FallsBackWithDevDiagnostic_UI_WB_3` FAILED**，且失败点恰为观测点断言 `WorkbenchShellGuiTest.cpp(368): m_devLog->seen("UI-LAYOUT-RESTORE-FAILED")`——诊断断言真实承重，非搭车变红 |

两处变异各自只红预期用例、其余用例保持 PASSED（定位精确）；还原后重建复跑 **8/3/6 全绿**，沙箱与送验对象零差异。

## 4.6 架构红线（AGENTS §5 八条）—— 逐条通过

1. **L2 计算内核零 Qt**：本任务仅改 ui 单元（R-3 全产品唯一 Qt Widgets 例外单元，DTB §4.5 预登记）；core/evidence/policy/runtime 等零触碰（37 文件清单实证）；ui 产品面 Qt 头包含＝R-3 例外头包含半区，ird_gates 3×R3 命中已按 F-259 登记回填请求（机器面滞后，非实质违反——基线对照亲跑实证既有 29 处零新增零移除）。
2. **业务域互链禁止（R-1）**：ui 产品面对 modeling/requirements/kinematics/trajectory/dynamics/selection/optimization/workflow/project/evidence/execution/policy/runtime/io/reporting 的 include **零命中**（grep 实证）；跨单元协作仅 core/diagnostics 两条 ARCH §3.5 登记边＋Qt（链接行＝CMakeLists.txt:106-111 亲读）。
3. **跨单元私有头禁止（R-2）**：私有头 `WorkbenchShell_p.hpp` 位于 `src/`（未进 include/——亲读实证）；ui 消费的 core/diagnostics 头均为其 `include/sdurws/ird/<unit>/` 公共头。
4. **框架零源码修改（SA-02）**：diff 内零框架文件、patches/ 零变更（4.1 实证）。
5. **不做名称前缀拼接/剥离（R-4）**：ui 产品面 `"…RobWork…"` 字面量 **零命中**（grep 实证）＋契约测试 `NoRobWorkPrefixLiteral_R4_UI_BUILD` 亲跑通过；名称呈现经 IUiNameResolver 端口（resolveObjectId localName 语义——runtime 权威不劫持，UiPorts.hpp 亲读）。
6. **权威唯一（PA-1）**：PM-11 状态栏格式为 ui §2.2 主责（formatProjectStatusText 单一权威纯函数——UiProjections.hpp:194-213 亲读）；命令注册表类型未在本任务预建（P-UI-5/CF-1——归 UI-T06 ui 自有头）；诊断码值权威仍归 diagnostics 码表（ui 仅描述符供体——StableCodeRegistry 注册契约用例亲跑通过）；壳不实现 project/runtime 语义（提交路径只路由＋占位说明——§11.4 不虚构能力）。
7. **不可变历史（PA-2）**：本任务零修订数据触碰（37 文件清单——无 project/ 修订面文件）。
8. **不恢复 old/**：零 old/ 引用（diff 实证）。

## 4.7 注释规范（AGENTS §2）—— 通过（公共头 3 件全查＋私有头＋实现 2 件抽查）

- **文件头**：8 件新增文件（3 公共头＋私有头＋2 实现＋2 测试＋CMake 增量块）均有中文 `@file/@brief`＋设计依据（ui.md 章节/需求 ID/契约条目逐条标注）＋背景说明（如 IWorkbenchShell.hpp 门面装配方向、UiPorts.hpp "为什么是端口而不是 include 对端"）。
- **类/函数 Doxygen**：公共头逐函数 `@brief/@param [in]/@return/@note/@throws` 语义完整（含失败行为与错误归类——如 initialize 返回值轨 fail-fast、shutdown 幂等后置）；`WorkbenchShellImpl` 全部私有方法逐个中文职责注释（WorkbenchShell_p.hpp:406-425）。
- **§2.5 高危信息表**：物理单位（px 尺寸常量 ：82-86 逐个注明来源 §4.4 原文数值）；线程约束（UI 线程纪律＋后台落盘线程"禁触 Widget"——WorkbenchShell_p.hpp 线程模型段/UiSettingsWriter 类注）；所有权（wiring 共享引用所有权在 L5——ShellWiring 结构注；unique_ptr 所有权移交——工厂注）；错误语义（调用方违约＝返回值轨 fail-fast、环境错误＝Dev 码走日志通道——含"Dev 码不入目录"的 diagnostics §6.2 依据）；确定性（登记序＝§7.1 表行序稳定排序锚——ShellSupport.cpp:48-53）；角度制式/坐标系本任务无此类物理量（纯界面单元）。
- **魔法数字**：全部集中常量区并注来源（kCollapseWidth=1280/§4.4 原文、kMaxRecentProjects=10/PM-10 原文、kLayoutFormatVersion=1/版本判别基准）。
- **反例扫描**：零"复述代码"式注释、零无信息量 TODO、零过期注释（实现与 ui.md v0.5 同批登记）。

## 4.8 文档同步 —— 通过（零偏差＋增量修订已登记＋findings 相关性逐条核对）

- **单元卡增量修订**：`units/ui.md` v0.4→v0.5（文档头版本/日期、§10 O-31 注 v0.5 段——UiPorts/UiProjections 形状逐条冻结、§10.1 v0.5 增量冻结块〔ShellWiring.devLog＋facets＋阶段 A 口径，代码块与实现逐字对齐——亲读比对一致〕、§16.7 v0.5 变更行）——契约 acceptance 4 明文的"形状随首消费冻结并增量修订登记"已兑现，实现与任务卡零未登记偏差。
- **门禁登记提交件**：`traceability/wp10-t03-gate-registrations.md`（§1 R-3 机器面回填请求＋§2 gate-all 执行面缺口＋§3 findings 登记＋§4 声明）在库亲读确认；`findings.json` 增 **F-259**（suggestion、open、owner=WP-01-T03）——与本验收独立复现的 3×R3 diff 逐字同因。
- **追踪同步**：validate-task PASS＋validate-docs PASS 亲跑同值（4.3）。
- **findings.json 未关闭条目相关性核对（225 条 open 逐类筛查，与本任务相关者如下，均可消账项为零）**：
  - **F-259**（本任务实施端自登记）：即上述 3×R3——维持 open 正确（owner=WP-01-T03，白名单文件不在本任务 allowedFiles，WP-10-T02 F-227 同口径）；
  - **F-010**（.gitignore `*.log` 吞留痕）：attempt 1 阻断 B-1 的根因、本任务返工提交 e6ac9d11 以 `git add -f` 补交 14 件而非改 .gitignore——处置正确（.gitignore 不在 allowedFiles，例外规则归 F-010 所有者）；**本验收在冻结对象上清点 23 件留痕全部在库**，F-010 系统性处置仍 open 正确；
  - **F-098/F-099**（全新树配置口径缺口）：本验收再次亲历（ini 两件缺件＋toolchain/Qt/CXXFLAGS 参数）——与本任务零代码关联，维持 open 正确（本记录环境段再留实证）；
  - **F-227**（WP-10-T02 门禁机器面 8 处命中，同 owner=WP-01-T03）：与本任务 F-259 同族同归口，不随本任务消账；
  - 其余 open 条目（F-004~F-258 族）与本任务 diff 无关联（逐类与本任务 37 文件面比对，零交集）。

## 4.9 commit 质量 —— 通过（3 提交四段式齐备；1 处标题名词短语＝既有家族新例，转登 F-261）

- **d402cfef** `[WP-10-T03] 实现工作台壳与五区布局（…）`：动词开头合规；四段（What/Why/验证/影响面）齐备；正文声称（8/8、6/6、3/3、ctest 3/3、门禁 diff＝3×R3）与本验收亲跑逐值一致。
- **dd9ff626** `[WP-10-T03] ui 单元卡 v0.5 增量修订与门禁登记提交件（文档同步＋留痕）`：**前缀后以名词短语『ui 单元卡』开头——不合 ACC §4.9/AGENTS §6.3『动词开头』字面**，F-012/F-232~F-242/F-247/F-258 家族第 11 例（纯风格瑕疵；正文四段齐备、validate 双 PASS 与留痕清点经验收者独立复跑证实）——转登 **F-261**（建议级，循 F-258 先例不阻塞）。
- **e6ac9d11** `[WP-10-T03] 返工补交 14 件 .log 验证留痕（B-1：…）`：动词开头合规；四段齐备；声称"14 件补交、零代码变化、不 amend 已推送历史"——与冻结对象实测一致（23 件在库清点、d402cfef/dd9ff626 未被改写）。

## 4.10 留痕完整 —— 通过（23 件在库逐件清点；§4.10 条文载体循 F-201 登记口径）

- `traceability/builds/wp10-t03/` **23 件全部在库**（`git ls-tree -r` 清点）：gtest XML×3（tests=8/3/6、failures=0——亲读）＋ird-test-report.json×3（summary decisive=true、passed=8/3/6、failed/skipped/notRun=0——亲读）＋console 日志×3＋集成构建日志×2＋冒烟 configure/build×2＋ctest.log（3/3 通过亲读）＋门禁六件（base/分支脚本模式日志＋骨架命中集×2＋hit-set diff——diff 恰 3 行 R3 亲读）＋validate-task/validate-docs×2——与三提交正文声称的全部执行面一一对应、无虚标。
- attempt 1 B-1（14 件 .log 被 .gitignore 吞没）已由 e6ac9d11 补齐：本验收对冻结对象直接清点为唯一判据（不采信补交叙述），23/23 在库。
- phase-one-validation.log 尾行停 CORE-T01、state.json 无 resultNote 字段——循 F-201 既有登记口径，以 `traceability/builds/wp10-t03/` 23 件为 4.10 判定载体。
- 验收者自跑留痕（集成/冒烟构建日志、三测试 gtest XML＋console、ctest、门禁双侧、变异试验日志）存于验收临时目录，关键结论已逐条摘录于本记录；临时目录随 worktree A 生命周期消亡，不影响本记录为证据载体。

## 4.11 偷懒/缩水实现扫描 —— 通过（扫描零命中；豁免逐条列据；逐条证据表见 4.4）

- **①标记扫描**：diff 新增行 `TODO|FIXME|XXX|HACK|stub|未实现|not implemented` **零命中**；`placeholder` 字样命中均为变量/对象名（`stagePlaceholder`/`kStagePlaceholder`/`ird_stage_placeholder`）——其本体是**契约显式设计**而非未完成实现：units/ui.md §4.1"阶段 A：占位面板（含『本阶段将在后续版本提供』说明，不虚构业务能力）"＋§11.4 不虚构能力原文＋ui.md v0.5 §16.7⑤ 登记"占位说明＝§4.1/§11.4 契约显式设计，非未完成实现"——豁免依据三条齐备，逐条列出如上。
- **②空实现检测**：公共接口全部有真实实现或属"由 L5 适配"的纯虚端口声明（IUiNameResolver/IPolicySummarySource 为 O-31 注入面——契约明文）；`WorkbenchShellImpl` 十一个虚函数全部非平凡实现（亲读）；`formatProjectStatusText`/`createWorkbenchShell`/`uiDiagnosticCodeDescriptors` 均有实体；无"仅返回默认值"的公共函数。未接线命令的触发反馈＝状态栏占位说明（submitShellCommand——契约 §14.1 阶段 A 边界的显式形态，非空实现）。
- **③逐条证据表**：契约 acceptance 4 条逐条证据见 4.4 表——每条均有具名测试或文件行号，零"已实现"空声明。
- **④测试强度**：断言校验行为与边界（可见性矩阵/几何 ±8 px 容差/上限 10 裁尾/规范化去重同键/失效保留/损坏三键整段丢弃/Dev 双面出线）；与 4.5 联动抽改实现两处均真实变红（非恒真、非只编译通过）。唯一强度缺口＝§4.4 尺寸折叠与 §10.1 违约轨（二次 initialize/shutdown 后空返回）无行为用例——转登 **F-260**（建议级）。

## 发现的问题清单（均建议级，无阻断级）

| 编号 | 级别 | 摘要 | 处置 |
| --- | --- | --- | --- |
| F-260 | suggestion | UI-T03 交付测试集未覆盖两处行为面：§4.4"低于最小尺寸（1280×720）侧栏折叠"（仅实现面证据 WorkbenchShell.cpp:551-569——UI-WB 系用例均在阈值之上运行）与 §10.1 门面违约轨（二次 initialize 返回 false、shutdown 后调用安全空返回——仅正向路径被断言） | 已随本 evidence 分支转登 findings.json（owner＝UI-T11/UI-T14 实施者：会话控制器与契约测试套件落位时补行为用例）；不阻塞本任务（ui.md §12.3 权威用例行无此两行，交付用例集经单元卡 v0.5 登记） |
| F-261 | suggestion | F-012/F-232~F-242/F-247/F-258 家族第 11 例：提交 dd9ff626 标题前缀后以名词短语『ui 单元卡』开头，不合『动词开头』字面；正文四段齐备、验证结论经验收者独立复跑证实，纯风格瑕疵 | 已随本 evidence 分支转登 findings.json（随 F-247 家族统一跟踪，不单独返工） |

## 裁决

**verdict＝pass**：4.1～4.11 全部通过、无阻断级问题；F-260/F-261 均建议级、已按 ACC §5 强制转登本 evidence 分支 `traceability/findings.json`，不阻塞合入。验收通过对象＝**e6ac9d11f51fc9b483d074f3b541f2c2ccbad911**（非分支引用）；后续收尾按 PIPE §4.7 由所有者执行。
