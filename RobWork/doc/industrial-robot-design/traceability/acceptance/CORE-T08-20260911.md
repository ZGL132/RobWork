# CORE-T08 验收记录（对抗式验收）

| 字段 | 值 |
| --- | --- |
| 任务 ID | CORE-T08（≙WP-03-T08，事件契约 Events） |
| 契约 | tasks/foundation/CORE-T08.json（status: ready，随收尾翻转 done） |
| 验收对象 | 分支 `wp03-t08`，head＝`1b4fa4dcd1ef855f4bc5a31aaf2a86063b9c5701`（与 origin 一致——漂移检测 PASS），范围 `73edcde..1b4fa4d` |
| 尝试序号 | attempt=1（evidence 分支 `acc/CORE-T08/1`） |
| **verdict** | **pass**（4.1~4.11 全过；无阻断、无新增建议级发现） |
| 验收日期 | 2026-09-11 |
| 独立性声明 | PIPE v1.7 §0.4 单会话降级；补偿＝worktree A（detached @ head）亲手复现（63/63）＋投递禁用探针实证＋证据链前置自查（6 份日志 -f 入冻结对象——F-010 教训内化） |

---

## 逐项证据（4.1~4.11）

| # | 检查 | 结论与证据 |
| --- | --- | --- |
| 4.1 | 改动范围 | diff＝10 文件：core/{Events.hpp,Events.cpp,EventsTest.cpp}＋core/CMakeLists＋units/core.md＋traceability 5——allowedFiles 内；forbiddenFiles 零触碰；参考总线不进产品链接面（T-1 生产面扫描继续全过） |
| 4.2 | 可复现构建 | worktree A 全新配置 exit 0（ini 模板前置补齐）＋verify 目标构建 exit 0＋测试 63/63 PASSED（验收者亲手）；集成全量 exit 0 |
| 4.3 | verify 命令 | `cmake --build build --config Release --target sdurws_ird_core_test` exit 0 |
| 4.4 | acceptance 逐条 | ①四类载荷构造与 kind 一致（工厂按备选类型构造＋访问守卫错配抛 core/events/kind-payload）；make 错配抛错＝访问守卫语义（按类型工厂使错配不可构造——更优形态，契约语义不弱化）；事件值拷贝/移动/相等（payload 深比较反例）；参考总线 FIFO/退订幂等/退订后不投递/多订阅全收——两模式 XML；②五元组 TASK-03（TaskStatusChanged/ResultArchived 携带 TaskIdentity）✓ |
| 4.5 | 失败能力 | 投递禁用探针（publish 入口直接 return）→FifoDeliveryOrder 如期变红，恢复后 63/63 复绿（报告记录；前两轮注入 inconclusive 如实留痕——CRLF 匹配失效与 sed 结构破坏，行号 sed 显式验证后重做） |
| 4.6 | 红线 | 零 Qt；接口归 core＋实现归 execution·ui（§4.9 责任边界——core 不实现生产总线）；R-2 头根；SA-02 零框架改动 |
| 4.7 | 注释 | 公共头全查（四类事件/责任边界/防 DTO 膨胀/R1 无过滤/事件不持久化）；实现快照遍历注释；测试每用例追溯 |
| 4.8 | 文档同步 | core.md v0.10 落位登记（接口零偏差＋参考总线不进产品面＋四条边界说明）；findings 核对：F-010 前置自查；F-004（同源累计）；F-008 无涉 |
| 4.9 | commit 质量 | 两提交四段式＋Acc-Covered（1,2 / 2）；三轮探针 inconclusive 调试过程如实入报告 |
| 4.10 | 留痕完整 | 10 文件齐（日志 6＋XML×2＋report＋core.md） |
| 4.11 | 偷懒扫描 | 零命中；参考总线为实质实现（FIFO 快照遍历/幂等退订）；接口仅声明＝§4.9 明文责任边界（实现归 execution·ui——非契约豁免即任务形态本身） |

## 问题清单

阻断级：无。建议级：无新增。
