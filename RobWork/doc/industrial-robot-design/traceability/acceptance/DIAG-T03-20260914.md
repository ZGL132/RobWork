# DIAG-T03 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 ID | DIAG-T03（≙WP-09-T03，diagnostics 单元：错误类型与稳定码注册表 Errors/DiagCodes/StableCodeRegistry） |
| 契约 | `doc/industrial-robot-design/tasks/foundation/DIAG-T03.json`（status ready；branch wp09-t03） |
| 送验对象（冻结） | `ce618ea464dd200a72a8916df05d373f385f9b8c`（= `origin/wp09-t03` 尖端，验收全程复核无分支漂移） |
| diff 范围 | `a6315152e377b98e17a4a72bd4d33e339aa08882`（state.base＝redesign-main 尖端）..`ce618ea4`，共 2 提交、20 文件（+2952/−7） |
| verdict | **pass**（4.1~4.11 全部通过，无阻断级问题；无新增建议级登记项） |
| 验收日期 | 2026-09-14 |
| 验收者 | 独立验收会话（全新上下文；未消费实施叙述；复现优先于采信——下述构建/测试/门禁/探针均为验收者亲手执行） |

---

## 一、逐项证据（ACC §4.1~4.11）

### 4.1 改动范围 —— 通过

- diff 文件清单（`git diff --name-only a6315152..ce618ea4`，共 20 个）逐个比对契约 `allowedFiles`：
  - `RobWork/RobWorkStudio/src/rwslibs/industrialrobot/diagnostics/**`：CMakeLists.txt、include/.../DiagCodes.hpp、include/.../Errors.hpp、src/DiagCodes.cpp、src/Errors.cpp、test/StableCodeRegistryTest.cpp（6 个）；
  - `RobWork/doc/industrial-robot-design/units/diagnostics.md`（1 个，allowedFiles 第 2 项）；
  - `RobWork/doc/industrial-robot-design/traceability/builds/wp09-t03/**`（13 个留痕文件，allowedFiles 第 3 项）。
- `forbiddenFiles`（REQUIREMENTS.md / ARCHITECTURE.md）：`git diff --name-only <range> | grep -E 'REQUIREMENTS\.md|ARCHITECTURE\.md'` 零命中。
- SA-02（框架零源码修改）：diff 范围内无 `RobWork/`、`RobWorkSim/`、`RobWorkStudio/` 框架源码改动；`patches/` 目录零新文件（`git diff --stat <range> -- '**/patches/'` 空输出）。
- **范围越界：零。**

### 4.2 可复现构建 —— 通过（验收者亲跑；环境差异如实说明）

- **复现现场**：`git worktree add --detach /d/10_Source_Repos/21_robot/acc-tmp/diag-t03-repro ce618ea4...`（detached @ 送验 SHA）。
- **集成模式（干净构建树，重新配置）**：`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64 -DCMAKE_TOOLCHAIN_FILE=.../vcpkg.cmake`，配置 exit 0；缓存确认 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`。日志：`acc-tmp/acc-logs/integration-configure.log`。
  - **送验对象构建面零错误**：`sdurws_ird_diagnostics`（STATIC）、`sdurws_ird_diagnostics_test`、`sdurws_ird_diagnostics_contract_test`、core 依赖链全部编译链接成功。
  - **环境差异（非本任务缺陷，如实登记）**：干净 worktree 全量 `cmake --build build --config Release` 以 exit 1 结束，73 处 error **全部**位于框架目标 `RobWorkSim/src/rwsimlibs/plugins/RWSimulatorPlugin.vcxproj`（rw 框架头 VelocityScrew6D.hpp/Math.hpp 的 min/max 宏污染簇——C4003 先兆→C3878/C7510/C2760/C2039 连锁），与 `traceability/findings.json` **F-008** 登记的基线缺陷逐字吻合（F-008 已实证 base 959fb9b 对照同样失败；另有 F-007/F-009 登记同环境链）。本任务 diff 与 RobWorkSim/rw 零交集，错误归属框架基线，处置权框架/构建所有者，不构成送验对象阻断。
- **独立冒烟模式**：`cmake -S RobWork/RobWorkStudio/src/rwslibs/industrialrobot -B <临时目录> -DCMAKE_TOOLCHAIN_FILE=vcpkg/scripts/buildsystems/vcpkg.cmake`（带 toolchain——CORE-T01 验收 G-2 教训口径）配置 exit 0、构建 exit 0。日志：`acc-logs/smoke-configure.log`、`acc-logs/smoke-build.log`。
- **测试执行（验收者亲跑，双模式）**：
  - 集成：`sdurws_ird_diagnostics_test.exe` → **22/22 PASSED**（exit 0）；`sdurws_ird_diagnostics_contract_test.exe` → **3/3 PASSED**（exit 0）。日志：`acc-logs/integration-test-run.log`、`integration-contract-run.log`。
  - 冒烟：同两可执行文件（冒烟树产物）→ **22/22 PASSED**＋**3/3 PASSED**（exit 0）。日志：`acc-logs/smoke-test-run.log`、`smoke-contract-run.log`。
  - 与送验留痕 `traceability/builds/wp09-t03/ird-test-report.json` 的 regression 字段（integration 25/25、smoke 25/25）一致。

### 4.3 契约 verify 命令 —— 通过

契约 `verify` 数组仅一条，逐字亲跑：

```
cmake --build build --config Release --target sdurws_ird_diagnostics_test   → exit 0
```

（干净 worktree 集成树内执行；产物 `build/RobWorkStudio/bin/Release/sdurws_ird_diagnostics_test.exe` 成功生成并可直接运行。）

### 4.4 acceptance 条目逐项证据 —— 通过（4 条全部有证据；ird_gates 未标注"通过"而是如实登记基线命中）

| # | acceptance 条目 | 证据 |
| --- | --- | --- |
| 1 | DT-REG-1~5 用例通过；码表与 §4.6 逐项一致（PRJ 10/RT 14/POLICY 23/EVI 7/EX 18/RPT 8/DIAG 7 全量收编，码值不重排）；manifest() 两次计算稳定 | 亲跑 22/22 全绿。`DtReg1_BuiltinTableRegistersFullyAndManifestStable`（StableCodeRegistryTest.cpp:177）：registeredCodes 计数断言 10/14/23/7/18/8/7、87 码逐码 find 命中、manifest 两次逐条目＋digest 相等、digest 非全零；`DtReg1_BuiltinTableValuesMatchDesignVerbatim`（:220）：87 码与测试内嵌 §4.6 原文清单逐位比对（双来源交叉钉住）；`DtReg1_ManifestIndependentOfRegistrationOrder`（:234）：逆序注册同 manifest＋首尾条目字典序断言。DT-REG-2~5 用例见第 2 条 |
| 2 | 重复注册拒绝（不覆盖不静默）、未知码构造拒绝、paramSchema 缺参/版本冲突/废弃 tombstone 解析用例过（NFR-MNT-03；PA-2） | `DtReg2_DuplicateRegistrationRejectedNotOverwritten`（:269，同码同/异元数据均 DuplicateCode＋find 首版不被覆盖）、`DtReg2_TextKeyCollisionRejected`（:312）、`DtReg3_UnknownCodeFindNullptrAndOwnershipConflictRejected`（:347，未知前缀/跨前缀 CodeUnknown＋find nullptr）、`DtReg5_ParamSchemaValidation`（:427，空串/非数组/悬空逗号/大写/重复参数名 5 类拒绝＋`[]` 与 `["pid","host"]` 合法面）、`DtReg5_VersionConflictIsDuplicateRejection`（:297）、`DtReg5_DeprecateTombstoneKeptAndNavigable`（:486，tombstone 保留＋supersededBy 导航＋删除禁止＋摘要变化）、`DtReg5_TombstoneDescriptorRegistrableAndManifestConsistent`（:535，两进程同清单含 tombstone 摘要一致）——全绿，且经 4.5 探针实证会红 |
| 3 | P-DIAG-3 处置：DiagnosticCategory（15 值）/DiagnosticSeverity（4 值）为实现承载词表随码元数据登记，不新增工程状态轴值 | `DiagCodes.hpp:76-115` 词表 15/4 值＝单元卡 §4.3 原文（枚举注释逐值锚点）；`DtPdiag3_Category15Severity4TokensFrozen`（:594）15/4/3 token 逐值断言＋15 token 互异；`DtPdiag3_VocabularyCarriedByBuiltinCodeMetadata`（:639）Dev 码计数＝11（EX 4＋EVI 2＋DIAG 4＋PRJ 1）＋flags 全表扫描；单元卡 §14.4 v0.4 变更记录登记"不新增轴值，需求侧如冻结正式枚举走需求变更"。分类/严重逐码落值依据（压缩串展开规则 R1~R4＋全部消歧点）在单元卡 §4.6 表后注记与 `DiagCodes.cpp` 文件头/逐码注释双重登记 |
| 4 | P-DIAG-1/P-DIAG-9 处置：消费契约以 core.md v0.1 为基线；titleKey/detailKey 文案键随码表登记且注册期唯一性校验，文案值归 ui/文案资源（键/值分离——UX-02） | `DtPdiag1_CodeFieldIsCoreDiagCode`（:676）`static_assert` 钉住 `CodeDescriptor::code`/`supersededBy` 类型＝`core::DiagCode`（core 冻结后增量同步、不私改 core）；`DtPdiag9_TextKeysFollowConventionAndAreUnique`（:689）87 码 174 键命名约定逐码断言＋唯一性＋偏离约定键 Usage 拒绝；实现侧 `registerCode` 检查④（DiagCodes.cpp:702-718）键非空/跨码唯一（DuplicateCode）/键形约定（Usage）；`DiagCodes.hpp:174-179` 字段注释明示"值归 ui/文案资源，本设施不携带" |

**ird_gates（如实登记，未执行不标注）**：验收者亲跑 `cmake --build build --config Release --target ird_gates`（干净 worktree 集成树）→ exit 1，**命中 7 项全部为既有基线族**：IRD-GATE-SUB×5（evidence_test/evidence_contract_test/policy_test/policy_contract_test/runtime_test → sdurws_ird_testkit 直链，F-072 族）＋IRD-GATE-SUB 表外边×1（sdurws_ird_testdata_lint → testkit）＋IRD-GATE-T1×1（sdurws_ird_testdata_lint 产品目标链 testkit，F-019 族）。与送验留痕 `gates-current-wp09t03.log` 及 wp09-t02 基线命中集合逐项一致；**diagnostics 相关命中＝0**（输出中零 diagnostics 字样）。exit 1＝基线既有命中的预期行为，处置权 WP-01 所有者——**零新增命中**。

### 4.5 测试真实失败能力（验证验证者）—— 通过（两探针亲手构造）

验收者在复现 worktree 的实现文件 `src/DiagCodes.cpp` 临时注入缺陷、重建重跑、逐字还原（还原后 `git status` 零残留，22/22 复绿）：

- **探针 1（重复注册拒绝短路）**：第一轮仅短路检查⑧码重复（`if (false && m_codes.find(...))`）——DtReg2 两用例**未变红**：同码再注册先被检查④文案键唯一性（NFR-MNT-03 第二检查点）以同一错误码 DuplicateCode 兜底拒绝（防御深度，非测试恒真）。第二轮同时短路检查④（`if (false && m_textKeys.find(...))`）——`DtReg2_DuplicateRegistrationRejectedNotOverwritten` 与 `DtReg2_TextKeyCollisionRejected` **真实变红**（FAILED 2 tests）。
- **探针 2（词表 token 篡改）**：`severityToken(Info)` 返回值 `"info"`→`"bogus"`——`DtPdiag3_Category15Severity4TokensFrozen` **真实变红**（FAILED 1 test）。
- 注：探针 1 第一轮的"未变红"经分析为实现的冗余防线所致（两检查点语义重叠、错误码面一致），已在送验留痕 `ird-test-report.json.realFailureEvidence` 有相同观察与登记，不构成缺陷。

### 4.6 架构红线（AGENTS §5 八条逐条）—— 通过

1. **L2 计算内核零 Qt**：diff 新增 include 全集扫描＝std 头＋gtest（仅测试目标）＋`sdurws/ird/core/{DiagData,Digest}.hpp`＋本单元头，零 Qt；CMake 配置期守卫（CMakeLists.txt:212 Qt 匹配即 FATAL_ERROR）随配置通过；既有 `NoQtInclude_DT_BUILD` 用例亲跑通过。
2. **业务域互链禁止（R-1）**：产品库仅 PUBLIC 链 `sdurws_ird_core`（CMakeLists.txt:74）；配置期守卫（:203-221）＋ird_gates R-1 零 diagnostics 命中。
3. **跨单元私有头禁止（R-2）**：跨单元 include 仅 `include/sdurws/ird/core/` 公共头；`PublicHeaderPathLayout_DT_BUILD`/`NoCrossUnitInclude` 用例通过。
4. **框架零源码修改（SA-02）**：见 4.1——diff 零框架文件、patches/ 零新文件。
5. **不做名称前缀拼接/剥离（R-4）**：本任务无名称解析语义；ird_gates R-4 零命中（引擎自测 fail_r4 按预期检出，证明检查在工作）。
6. **权威唯一（PA-1）**：码值权威＝StableCodeRegistry（本单元职责本体）；文案键登记/文案值归 ui（P-DIAG-9 键值分离）；未越权实现他单元语义。
7. **不可变历史（PA-2）**：deprecate 只置位不删除（tombstone 永存，`DtReg5_DeprecateTombstoneKeptAndNavigable` 验证"删除禁止"）；码值一经登记不重排（`DtReg1_BuiltinTableValuesMatchDesignVerbatim`）。
8. **不继承 old/**：实现按单元卡 §4.5/§9.1 从零编写（old/ 不在磁盘，无引用）。

### 4.7 注释规范（AGENTS §2）—— 通过

- **公共头全查（2/2）**：`Errors.hpp`（156 行）——文件头设计依据/背景/错误语义总纲/线程安全/确定性齐备；枚举逐值中文 Doxygen（含"随 DIAG-T04/T05 消费"的分工标注）；函数 @param/@return/@throws 完整。`DiagCodes.hpp`（447 行）——文件头含 P-DIAG-1/3/9 处置锚点、线程安全（注册期单线程/seal 后只读无锁的论证）、CodeDescriptor 16 字段逐字段约束（必填/默认值/语义/出处）、五方法接口契约注释完整。
- **实现/测试文件抽查（4 个，≥3 达标）**：`Errors.cpp`（token switch 全枚举逐值注释＋不可达分支说明）、`DiagCodes.cpp`（883 行：文件头 canonical 编码表＋压缩串展开规则 R1~R4；registerCode 验证链①~⑧逐步中文注释含业务出处 NFR-MNT-03/SA-15/PA-2；magic 数字 7＝magic 长度、0xFF 掩码等均有说明）、`StableCodeRegistryTest.cpp`（739 行：每 TEST 注释引用矩阵行/acceptance 原文，断言处中文说明）、`diagnostics/CMakeLists.txt`（关键步骤中文注释块：gtest 接入/红线守卫/宏定义理由）。
- **§2.5 高危信息**：错误语义（调用方违约 fail-fast vs 设施失败）在 Errors.hpp 头注释明示；线程约束逐类注明；所有权（find 返回指针生命周期、StableCodeRegistry 禁拷贝/禁移动的理由）注明；本任务无物理单位/角度量。无中英混杂机翻腔、无过期注释、无裸 TODO。

### 4.8 文档同步 —— 通过

- 实现与任务卡**零偏差、且两处实现口径均按 DTB §5.4 登记于单元卡 §14.4 v0.4**：①"运行期"判据＝实现类 `seal()` 装配原语（§9.1 未定义判据，引 evidence EV-T09 先例；IDiagnosticRegistry 五方法签名不扩——非接口偏差）；②注册期验证链检查序固定（键唯一性 DuplicateCode 先于键形约定 Usage）。DT-REG-3/4/5 的工厂/translate 路径留待 DIAG-T04（§11 任务分工），本任务自证注册表侧判定原语——契约 acceptance 第 2 条的"未知码构造拒绝"在本任务交付面（注册表＋错误码面）内有用例承载，边界声明在测试文件头与单元卡均有登记。
- 单元卡同步四处：卡头"构建落位"、§1.2 构建骨架行、§4.6 表后"逐码展开登记"注记（压缩串机械展开规则 R1~R4＋消歧点显式化——码值集合零改动）、§14.4 变更记录 v0.4。
- **findings.json 未关闭条目相关性核对（82 项 open）**：与本任务直接相关的是环境/门禁类既有条目——**F-007**（全新树配置参数未载明＋ini.template.static 被 .gitignore [Bb]in/ 吞掉）、**F-008**（RWSimulatorPlugin 全新树构建失败，登记 71 处 error，本次复现实测 73 处、症状与文件一致）、**F-009**（gate-all 非主树不可全量运行）、**F-010**（.gitignore *.log 吞留痕——本任务第二提交以 `add -f` 入库 8 份日志，沿用 wp09-t02 先例）。四条均为框架/构建治理类，处置权不在本任务 allowedFiles 内，**无可随手消账项，也无静默积压**；本次验收复现再次印证 F-007/F-008（见 4.2），不重复登记。其余 open 条目属其他单元（testkit/evidence/policy 等），与本任务无相关性。验收者无新增建议级问题需转登（见"三、问题清单"）。

### 4.9 commit 质量 —— 通过

- `45fe61fc` `[WP-09-T03] 实现 diagnostics 稳定码注册表与错误类型（Errors/DiagCodes）`：标题动词开头、≤50 字；正文四段齐备（What 按文件列改动/Why 引契约与 knownPitfalls/验证含构建测试门禁真实结论＋覆盖边界声明/影响面"仅 diagnostics 单元内新增"）；"探针失败能力自证""ird_gates 7 命中＝基线零新增（exit 1 为基线预期）"等表述与留痕一致，无粉饰。
- `ce618ea4` `[WP-09-T03] 补齐验证留痕日志（.gitignore *.log 例外入库——wp09-t02 同先例）`：四段齐备，如实说明"无新增执行"（纯留痕补录）；`Acc-Covered: 1,2,3,4` 尾注与仓库流水线惯例一致。

### 4.10 留痕完整 —— 通过

- `traceability/builds/wp09-t03/` 落位 13 文件：gtest XML×4（test/contract × integration/smoke）、`ird-test-report.json`（schemaVersion ird-test-report/1，含 per-suite 用例 trace、regression、gates 基线对比、realFailureEvidence、日志索引）、构建/测试/配置/validate-task/verify/ird_gates 日志×8＋smoke-configure.log。
- `phase-one-validation.log`：自 WP-05 起即为历史载体（grep wp05~wp09 均零条目——wp09-t02 先例亦未更新），本任务留痕按现行惯例全部落 `builds/wp09-t03/`，符合"phase-one-validation.log **或** resultNote 已更新"的替代口径（ird-test-report.json 即结构化 resultNote 载体）。
- 验收者亲跑的双模式测试输出与送验 XML 数值一致（22/22＋3/3，两模式）。

### 4.11 偷懒/缩水实现扫描 —— 通过

- ①**标记扫描**：diff 新增内容 `TODO|FIXME|stub|placeholder|未实现|not implemented` 命中 3 行，全部位于 `StableCodeRegistryTest.cpp:401-404`——`makeValidDescriptor("RT-PLACEHOLDER")` 为 DT-REG-4 用例**故意构造的未注册码测试数据**（验证"异常文本作码拒绝"路径），非实现缩水标记，零豁免需要。
- ②**空实现检测**：公共接口（IDiagnosticRegistry 五方法、token/categoryToken/severityToken/retryKindToken、isValidDiagCodeSyntax、builtinCodeDescriptors/registerBuiltinCodes）全部有真实实现——registerCode 为 8 步验证链、manifest 为逐字段 canonical 编码＋core::ContentDigester SHA-256、内置表 87 码逐码登记，无空体/仅返回默认值的契约函数。
- ③**逐条证据表**：见 4.4——acceptance 4 条全部具名测试/文件行号级证据，无一条仅凭"已实现"声明。
- ④**测试强度**：断言校验行为与边界（错误码面逐值、首版不被覆盖、计数 10/14/23/7/18/8/7、Dev 计数 11、token 互异、字典序首尾、tombstone 摘要变化、幂等 seal），无恒真断言；4.5 探针实证非恒真。

---

## 二、裁决

**verdict ＝ pass**

- 4.1~4.11 全部通过，无阻断级问题。
- 干净 worktree 全量集成构建踩 F-007/F-008 既有基线缺陷（73 处 error 全在框架 RWSimulatorPlugin 目标）——归属框架基线、与本任务 diff 零交集，不构成送验对象阻断；送验对象全部构建目标与两模式测试由验收者亲手复现成功。
- **无新增建议级问题需要转登 findings.json**（本次编号顺延自 F-102 起，未动用——发现的全部现象均已被 F-007/F-008/F-009/F-010 既有 open 条目覆盖，且实施者留痕已如实登记基线命中与防御深度观察）。
- 本验收通过的是 **SHA ce618ea464dd200a72a8916df05d373f385f9b8c**，非分支引用；验收者未修改任何实施产物、未向 wp09-t03 推送任何提交。

## 三、问题清单

| 级别 | 描述 | 处置 |
| --- | --- | --- |
| 阻断 | 无 | — |
| 建议 | 无新增。既有相关 open 条目：F-007/F-008/F-009（全新树构建环境链——本次复现再次实证，F-008 实测 73 处 vs 登记 71 处，同根因同症状）、F-010（.gitignore 吞留痕——本任务按先例 add -f 处置，根修仍开放） | 均已在册，处置权框架/构建所有者（WP-01），本任务无可消账项 |

## 四、验收者环境说明

- 复现 worktree（worktree A）：`/d/10_Source_Repos/21_robot/acc-tmp/diag-t03-repro`（detached @ ce618ea4），集成树配置参数＝主树缓存口径（Qt CMAKE_PREFIX_PATH `D:/software/Qt/6.11.1/msvc2022_64`＋vcpkg toolchain，`RWS_BUILD_INDUSTRIALROBOT:BOOL=ON` 复核）；ini.template.static 因 F-007 既有缺陷需自主树补拷（环境修复，不触及送验产物）。冒烟树：`acc-tmp/smoke-diag-t03`（独立目录＋toolchain）。验收者日志/XML：`acc-tmp/acc-logs/`、`acc-tmp/acc-xml/`（验收后随临时目录清理，本记录内数值均为亲测）。
- 工具链：VS 2022（MSVC 19.4x，生成器 Visual Studio 17 2022，A x64）、CMake、vcpkg 经典模式（gtest 1.18.0 @ installed/x64-windows）、pwsh（validate-task.ps1）。
- 探针注入/还原均在 worktree A 内，最终 `git status` 零内容残留（仅一次行尾 CRLF 警告，已 checkout 还原）。
