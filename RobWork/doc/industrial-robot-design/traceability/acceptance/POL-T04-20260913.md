# POL-T04 验收记录（对抗式验收）

| 字段 | 值 |
| --- | --- |
| 任务 ID | POL-T04（≙WP-07-T04，policy 策略解析器与校验器——resolvePolicy 七段管线＋IPolicyValidator/IPolicyValidationContext） |
| 契约 | tasks/foundation/POL-T04.json（status: ready，随收尾翻转 done；requirements ARC-05/ERR-01；designRefs units/policy.md §5.1/§5.2/§9.2/§12；verify 1 条；acceptance 2 条；knownPitfalls P-POL-4） |
| 验收对象 | 分支 `wp07-t04`，head＝`2df7eb2cdfd2b81ebe0b28ddf5c856b562047c6e`（40 位冻结对象）；漂移检测：验收起止两次核验 origin/wp07-t04 == 2df7eb2c（零漂移）；范围＝`042e8db1b8b655a373a4a3913148fc56ff8ea36d..2df7eb2cdfd2b81ebe0b28ddf5c856b562047c6e`（＝state.base..headSha，3 提交：实现＋留痕登记＋单元卡 v0.5） |
| 尝试序号 | attempt=1（evidence 分支 `acc/POL-T04/1`） |
| **verdict** | **pass**（4.1~4.11 全过；无阻断级问题；建议级 3 项新发现已转登 findings.json F-076/F-077/F-078——均为提交标题风格/留痕清单口径/单元卡既有转义损毁，零行为影响，不阻塞合入） |
| 验收日期 | 2026-09-13 |
| 独立性声明 | 全新上下文，只消费契约/单元卡/代码/留痕产物；未读实施叙述。补偿性对抗动作＝worktree A（detached @ 2df7eb2c）亲手全新配置双模式构建＋全量测试（97/97×2）、两个独立缺陷探针（均为验收者自行设计的 .cpp 变异，与实施侧自证位点不同：①角色词表核对调用禁用②③段唯一默认填充禁用，还原后复绿＋git status 零残留核验）、ird_gates 集成树亲跑（命中 5 项与送验留痕逐字一致）、单元卡 §5.1/§5.2/§9.2/§11/§12 权威原文与实现逐点比对、留痕文件清单逐份 git ls-tree 清点 |

---

## 逐项证据（4.1~4.11）

| # | 检查 | 结论与证据 |
| --- | --- | --- |
| 4.1 | 改动范围 | `git diff --name-status 042e8db1..2df7eb2c`：15 文件＝4 个代码/构建文件（policy/CMakeLists.txt＋policy/include/sdurws/ird/policy/PolicyParsing.hpp＋policy/src/PolicyParsing.cpp＋policy/test/PolicyParsingTest.cpp）全部位于 `policy/**`（allowedFiles）＋10 个留痕文件（traceability/builds/wp07-t04/，DoD §4.2 留痕口径，属 `traceability/**`）＋units/policy.md（allowedFiles 显式列出，+1 行＝§15.4 v0.5 变更记录）；forbiddenFiles（REQUIREMENTS.md/ARCHITECTURE.md）零触碰（路径过滤 diff 为空）；框架源码（RobWork/RobWorkSim/RobWorkStudio 非 industrialrobot 部分）零触碰；patches/ 无新文件——SA-02 零框架源码改动。3 提交＝dad8667e（实现）＋272aeeaf（留痕）＋2df7eb2c（单元卡 v0.5） |
| 4.2 | 可复现构建 | worktree A（`git worktree add --detach` @ 2df7eb2c，全新 build 树）：按 F-007/F-062 已登记口径两步处置（①配置命令补 `-DCMAKE_TOOLCHAIN_FILE=…/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`——自主树 CMakeCache 读取；②复制主树 git 未跟踪的 RobWorkStudio.ini.template.static 与 RobWorkStudio.ini.shared.in 至 wtA bin/——未改动任何被跟踪产物）→ 配置 exit 0 且缓存确认 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`（grep 实证）；`cmake --build build --config Release --target sdurws_ird_policy_test` exit 0、`error C`/`: error` 零行（日志唯一告警＝core/Units.cpp C4099×1，F-071 既有存量在全新树再现，本 diff 未触碰 core；policy 面零告警）；测试 97/97 PASSED（验收者亲手运行，`[==========] 97 tests from 18 test suites ran`）。冒烟：独立临时目录 acc-pol-t04-smoke＋vcpkg toolchain 配置 exit 0＋构建 exit 0 零 error；测试 97/97 PASSED（均验收者亲手执行） |
| 4.3 | verify 命令 | 契约 verify（单条）＝`cmake --build build --config Release --target sdurws_ird_policy_test`——在 worktree A 集成树亲手独立复跑 exit 0（即 4.2 集成构建，逐字命令）；附带运行测试可执行文件 97/97 全绿；`verify-task.ps1 -DryRun` PASS（RepoRoot 归约至 wtA、dryrun 命令行与契约逐字一致）；validate-task PASS、validate-docs PASS（20 units, 12 trace entries, 150 task files） |
| 4.4 | acceptance 逐条 | ①"POL-PARSE-1~6 错误分类表全量反例通过（不短路）"：验收者亲手双模式各 97/97（XML tests=97 failures=0×2）；§5.2 错误分类表 12 行反例逐行核对全部落位——行 1 未知字段→RoleVocabularyTypoRejectedAsUnknownField（SCHEMA-UNKNOWN-FIELD＋localName 定位 mandatoryPairs[1].first）、行 2 版本→Future（比较型实际/期望代＋升级指引断言）/Unknown 两用例＋FutureVersionDoesNotInterpretFields（版本门即返回）、行 3 非有限→NaN/±Inf 两用例（原文保留断言 cause 含 "nan"/"inf"）、行 4 越域→NegativeSafetyClearance（比较型三要素 actual=-0.01/expected=0.0/单位 m）＋NearLimitRatio 1.5（expected=1.0/单位 "1"）＋ZeroTravelLimit＋ConditionNumber 0.5 四用例、行 5 必填缺省→RequiredMissingWhenEnabledWithoutClearance_Acceptance2、行 6 单位→力单位 N（cause 转发 core/units/dimension 原文）＋未注册 token "parsec" 两用例、行 7 重复→ExactDuplicateListsBothEntries（cause 列重复双方理由）＋RoleExpansionDuplicateWithinList（展开面重复）、行 8 冲突→直接对端＋Role 展开（Tool 角色命中 toolObj）＋Group 同名三用例、行 9 组引用→UndefinedGroupReferenceRejected（cause 含 "ghost"、localName=…[1].second）＋DefinedGroupReferencePublishes 反向、行 10 对象缺失→规则端/适用范围端/双缺失三用例（subject 绑定缺失对象 ID——POL-PARSE-6 断言）、行 11 空域启用→EmptyDomainsWithEnabledRejected（＋EmptyDomainsWithDisabledPublishes 反向：enabled=false 空域合法）、行 12 告知性→TravelLimitDefaultFilledFromAppendixD_Acceptance2（INFO 码＋随发布对象 validationDiagnostics 附带断言）；不短路实证：MultipleThresholdErrorsCollectedWithoutShortCircuit（四槽位四诊断按 struct 序逐位断言）＋AllStageErrorsCollectedInDeterministicOrder（跨五检查面 7 诊断按段序逐位断言码面与 localName）；POL-PARSE-1~6 编号行（§11 矩阵）逐行有具名 `_POL_PARSE_N` 用例（PARSE-5 行"未知键"面由 RoleVocabularyTypo 用例＋总反例中的 UNKNOWN-FIELD 位承载——覆盖完整，仅后缀标注组织差异）。②"O-10/P-POL-2 保守口径：默认解析仅附录 D 第 11 项（4π，origin=DefaultAppendixD）；无冻结默认项保持 nullopt＝显式不适用，不发明数值、不设第二默认"：TravelLimitDefaultFilledFromAppendixD_Acceptance2（siValue 位级等于 4π＋origin=DefaultAppendixD＋POLICY-INFO-DEFAULT-APPLIED 诊断）＋UnfrozenDefaultsStayAbsent_Acceptance2（近限位比/条件数未提供保持 nullopt、诊断为空——不发明数值）＋RequiredMissingWhenEnabledWithoutClearance_Acceptance2（enabled 且间距缺失即拒绝）＋DefaultAppliedAndExplicitValueShareIdentity（默认与显式 4π 同 contentIdentity、诊断集不同）＋ExplicitTravelLimitKeepsExplicitOriginWithoutInfo（显式 4π 标 Explicit 无告知）；卡面口径无漂移核对（knownPitfalls P-POL-4 陷阱）：policy.md L355/§5.1③/L1186 D-05 与契约 acceptance 2 逐字同口径。ird_gates（WP-01-T01 已建成，如实执行）：验收者亲跑 `cmake --build build --config Release --target ird_gates`——命中 5 项（IRD-GATE-SUB×4：evidence_test/evidence_contract_test/runtime_test→testkit＋testdata_lint→testkit 表外边；IRD-GATE-T1×1：testdata_lint 产品目标链接 testkit；对照 findings.json 归属＝F-019×2＋F-051×1＋F-072×2），全部不在本任务 allowedFiles 内＝base 既有、policy 面零新增，与送验 ird_gates-current-wp07t04.log 逐字一致（base 快照 log 同 5 项——GATE_DELTA_ZERO 证实）；引擎自测 9 项全部按预期；ird-test-report.json gateAll 节含 base 对照方法；gate-all.log 16/17（唯一失败＝ird_gates 既有 5 命中） |
| 4.5 | 测试真实失败能力 | 探针一（验收者设计：PolicyParsing.cpp L577-578 两处 checkRoleVocabulary 调用禁用——①段词表核对机制变异）→ RoleVocabularyTypoRejectedAsUnknownField＋AllStageErrorsCollectedInDeterministicOrder 如期变红（2 failed / 0 passed of 2）。探针二（验收者设计：③段唯一默认填充条件 `false && …`——O-10 唯一默认机制变异）→ TravelLimitDefaultFilledFromAppendixD_Acceptance2＋DefaultAppliedAndExplicitValueShareIdentity 如期变红（2 failed / 3 passed of 5）。两探针均为 .cpp 变异、首次重建即红（F-068/F-075 增量陷阱本轮未触发——非头文件注入形态）；证明词表反例断言与 acceptance 2 默认口径断言均真实联动实现行为、非恒真。探针还原（git checkout）＋touch 强制重编译（F-068 处置纪律）→ 97/97 复绿；`git status --porcelain` 零残留核验（送验对象未被污染）。与实施侧自证相互独立、位点不同 |
| 4.6 | 架构红线 | ①零 Qt：grep 实证新增三文件（PolicyParsing.hpp/.cpp/PolicyParsingTest.cpp）include 面无 Qt 头（标准库＋core 公共头＋policy 自有头）；②R-1 无业务域互链：PolicyParsing 对外 include 仅 sdurws/ird/core/{DiagData,Digest,Identity,Errors,Provenance,Units}.hpp＋policy 自有公共头 {Errors,PolicyInput,PolicySet}——零其他业务域单元；policy 库链接面本任务零改动（CMakeLists diff 仅增 src/PolicyParsing.cpp 与 test/PolicyParsingTest.cpp 两行，链接节未动）；③R-2 跨单元私有头：上述 include 全部位于各单元 include/sdurws/ird/<unit>/ 公共面；④R-4 无名称前缀拼接/剥离（解析器不触名称解析——runtimeName 槽位显式留空 nullopt 并注释"归⑥端口/会话侧"）；⑤R-5 无 RobWork 名称拼接（grep 零命中；ird_gates R-4/R-5 扫描在 5 项既有命中外零新增佐证）；⑥PA-1 权威唯一：内容身份唯一经 PolicyCodec::contentIdentity（CR-02 单点）、对象字节版本不越权计算（sourceVersion 恒 nullopt＋注释明示归 POL-T05 Provider）、单位换算唯一经 core convert（SA-12——文件内零换算因子字面量）、阈值域窗唯一经 PolicyThreshold::make；⑦PA-2 不可变历史不适用（无修订写路径——解析为纯函数）；⑧SA-02 零框架改动（见 4.1）；无 old/ 恢复（全新实现）。AGENTS §5 八条逐条过 |
| 4.7 | 注释规范 | 公共头 PolicyParsing.hpp 全查（315 行）：文件头含设计依据（§5.1/§5.2/§9.2/§12＋ARC-05/ERR-01/NFR-COR-02/03＋契约 acceptance 逐条）＋背景说明（管线位置/调用方/纯函数契约/输入非法 vs 调用方违约双轨分工）＋线程安全声明；每类/每函数 Doxygen 中文（@param [in] 方向、@return、@throws 触发条件逐一——resolvePolicy 两个 PolicyObjectInvalid 抛点分列）；§2.5 高危信息齐备——物理单位（m/无量纲 "1"/rad 逐域显式——L131-143 siUnitToken）、坐标系（不涉位姿）、角度制式（rad 显式"不是度"）、所有权（input/context"调用方持有，本函数不修改/保证存活至返回"）、线程约束（逐类"纯函数/无状态可重入"声明）、错误语义（诊断轨 vs fail-fast 双轨逐条标注）、确定性来源（发射序＝管线段序/段内承载序/"C" locale 数字格式）；魔法数字有来源（4π＝附录 D 第 11 项、五值词表＝§6.1 同源）。实现 PolicyParsing.cpp 全查（876 行）：文件头实现纪律 5 条＋错误语义＋线程/确定性声明；每函数/每语义段中文注释（版本门即返回的 why、阈值检查序"非有限先于单位换算"的分类保真 why、覆盖等价解析期可检子集与会话期完整复核的两层防御 why、emplace 承载的 const 约束 why）。测试 PolicyParsingTest.cpp 全查（1070 行）：文件头设计依据＋替身边界声明（POL-TD-1 精神）＋用例名带追溯编号＋断言处中文注明验证条款。policy/CMakeLists.txt 增量注释中文齐备。缺项未发现 |
| 4.8 | 文档同步 | units/policy.md §15.4 v0.5 变更记录（+1 行）登记实现口径 ①~⑩，验收者逐点与实现/权威章节比对一致：①七段管线与发射序＝runParsePipeline 段序与 hasError 错误级判定（core DiagnosticRecord 无严重级字段——固定映射口径与 Errors 面一致）；②"未知字段"落点＝角色词表核对为类型层活性检测面＋字节层等价拒绝归 codec decode（契约注释与 v0.5 登记一致）；③版本门即返回＝行 2 处置语义（测试 FutureVersionDoesNotInterpretFields 钉住）；④覆盖等价解析期可检子集＝同 kind 同载荷＋Object×Role 经 context.objectRole，Object/Role×Group 归会话期同码复核（§7.2 两层防御——实现 endsCoverEquivalent 与登记逐字对应）；⑤空域不填默认＝§5.1④ 权威语义（测试 EmptyDomainsWithEnabledRejected）；⑥RULE-CYCLE 活性面＝未定义组核对（扁平组 schema 无嵌套——实现注释与登记一致）；⑦信息级诊断仅 POLICY-INFO-DEFAULT-APPLIED（不发明码）；⑧sourceVersion 恒 nullopt（PA-1）；⑨emplace 消费约束兑现；⑩阈值检查序（测试 MultipleThresholdErrors… 按 struct 序钉住）。语义零偏差声明属实；§12 POL-T04 行产物清单（PolicyParsing.hpp/.cpp：resolvePolicy/IPolicyValidator/IPolicyValidationContext）与交付一致；契约 status 翻转归收尾（PIPE §4.7⑥）不算偏差。findings.json 未关闭条目逐条核对相关性：F-019/F-051/F-072（ird_gates 既有 5 命中——本次亲跑再证实仍在，处置权在 cmake/ 不在本任务 allowedFiles，不可顺手消账）；F-004（MOC 空转——构建日志再现）；F-007/F-008/F-009/F-062（worktree/全新树环境前置——本次按已登记口径处置：toolchain/Qt 前缀＋两件 ini 模板复制）；F-071（core C4099——全新树再现×1）；F-065（留痕日志中文双重编码——送验 gate-all.log 门禁段再现，验收复核不受阻）；F-012 家族＋F-025/F-041 家族在送验提交有新例（转登 F-076/F-077）；F-058 家族排查扩展至 policy.md（转登 F-078）；P-POL-4（上游 Draft 漂移陷阱——契约 knownPitfalls）：O-10/P-POL-2 引用口径与 policy.md L355/§5.1③/L1186 逐字一致，本任务无漂移须同步 |
| 4.9 | commit 质量 | 3 提交逐一核查：dad8667e 标题"[WP-07-T04] 实现策略解析器与校验器（resolvePolicy 七段管线＋PolicyValidator）"——前缀＋动词"实现"开头✓；正文四段齐备（What 逐文件/Why 对应 acceptance＋设计依据/验证 五项/影响面 R-1/R-2/SA-02 合规声明）；验证结论与验收者实测一致（双模式 97/97✓、ird_gates base 对比零增量✓——本次亲跑证实）。272aeeaf 标题"[WP-07-T04] 登记 POL-T04 验证留痕（双模式构建/测试 XML/门禁对比/测试报告）"——动词"登记"开头✓；正文四段齐备；验证结论与留痕一致（XML tests=97 与 integration/smoke-test-run 对应声明——文件名清单失准转登 F-077，数值主张本身准确）。2df7eb2c 标题"[units] policy.md v0.5：POL-T04 实现落位登记（DTB §5.4 增量修订）"——前缀后名词短语开头（F-012 家族同型，转登 F-076，纯风格）；正文四段齐备（What 登记口径①~⑩逐条/Why DTB §5.4/验证 如实声明纯文档增量/影响面 零接口变更）。未发现粉饰：全部数值主张（97/97、5 项命中、GATE_DELTA_ZERO）经验收者亲手复现证实 |
| 4.10 | 留痕完整 | 送验留痕 10 份 git ls-tree 确认全部入库：集成/冒烟构建与配置日志×4＋双模式测试运行日志×1（integration-test-run.log）＋gtest XML×2（integration/smoke 均 tests=97 failures=0，与验收者亲手复跑一致）＋ird-test-report.json＋gate-all.log＋ird_gates-base/current 对比 log×2；ird-test-report.json（schema ird-test-report/1）含逐用例 44 条 trace 登记（suite/设计锚点/modes）、既有 53 用例承接说明、contractVerifyCommand、gateAll 节（16/17 判定＋base 对照方法）——即 resultNote 载体已更新；smoke-build.log 为 Release 配置（\Release\ 路径可辨，F-055 口径合规）；phase-one-validation.log 未更新——沿 TK-T10/TK-T11/POL-T03 先例（foundation 任务以 traceability/builds/<task>/ 证据包替代）。瑕疵：提交正文清单列出不存在的 smoke-test-run.log（建议级——F-077，证据本体不缺：冒烟测试运行由 smoke XML 承载） |
| 4.11 | 偷懒/缩水实现扫描 | ①TODO/FIXME/stub/placeholder/未实现/not implemented 扫描 diff 新增行零命中（grep 退出码 1）；契约无"触发式延后"标注故无豁免诉求；②空实现检测：公共接口全部有实逻辑——resolvePolicy/runParsePipeline（七段逐一实判定：fail-fast 复检/版本门/词表核对/逐槽位归一与域窗/默认填充/成对覆盖等价 O(n²) 判定/组引用/对象存在性/空域拒绝/contentIdentity 发布）、PolicyValidator::validate（共享管线单实现——两入口诊断逐字一致的机制保证）、sceneObjectRoleTokens（词表访问器）、IPolicyValidationContext 三方法为纯虚接口（契约本体，非缩水）；③acceptance 逐条证据表见 4.4（两条均有具名测试＋断言逻辑＋双模式 XML 通过记录）；④测试强度：断言校验行为与边界（诊断码/localName/subject/比较型三要素逐位断言、origin 来源三分、位级 4π 等值、端序规范化、集合升序去重、跨段 7 诊断发射序）＋4.5 双探针实证会红——非恒真、非"只编译通过" |

## 问题清单

- 阻断级：无。
- 建议级（3 项，均已转登 findings.json）：
  - **F-076**：2df7eb2c 标题"[units] policy.md v0.5：…"前缀后名词短语开头，非动词开头——F-012 家族再复现一例；另两份送验提交动词开头合规；纯风格瑕疵。
  - **F-077**：272aeeaf 留痕清单失准——正文称"新增 10 件"却列出 11 个文件名，其中 smoke-test-run.log 不存在于送验提交（F-025/F-041 同型）；证据本体不缺（冒烟测试运行由 sdurws_ird_policy_test-smoke.xml tests=97 failures=0 承载），无粉饰。
  - **F-078**：units/policy.md"AI 执行就绪补充/单元任务退出条件"两节存在 F-005/F-006/F-058 家族同型转义损毁（L1259 `$(policy.ToUpper())` 未展开、L1260 "locked"应为 blocked、L1271 "alidate-docs.ps1"缺首字母）——家族第四份单元卡实例；既有损毁非本任务引入（送验 diff 对 policy.md 仅 +1 行，反证在档）。
- 既有 open 项延续（不新增编号，本任务留痕/验收过程再现）：F-019/F-051/F-072（ird_gates 既有 5 命中，本次验收者亲跑再证实）；F-004（MOC 空转）；F-007/F-008/F-009/F-062（worktree/全新树环境前置，本次按已登记口径处置）；F-071（core/Units.cpp C4099 全新树再现）；F-065（留痕日志中文双重编码再现，不影响复核）；F-010（*.log 留痕——本任务留痕已实际入库，根因治理项维持 open）；F-068/F-075（探针增量编译陷阱——本轮两探针均 .cpp 变异未触发，无新形态）。

## 验收者环境说明

- 复现现场：`git worktree add --detach D:/10_Source_Repos/21_robot/acc-pol-t04-wt-a 2df7eb2cdfd2b81ebe0b28ddf5c856b562047c6e`（detached @ 冻结 SHA，非分支名）；集成树全新配置于 wtA/build（缓存确认 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`）；冒烟树独立于 D:/10_Source_Repos/21_robot/acc-pol-t04-smoke。
- 全部命令（配置/构建/测试/verify/ird_gates/validate-task/validate-docs/verify-task DryRun/双探针）由验收者本会话亲手执行；探针注入与还原均经 `git status` 零残留＋97/97 复绿双重核验（防旧二进制假绿，按 F-068 处置纪律 touch 后重建）。
- 环境缺口处置（F-007/F-062 既有，两步）：①配置命令补 `-DCMAKE_TOOLCHAIN_FILE=vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`（自主树 CMakeCache 读取）；②复制主树 git 未跟踪的 RobWorkStudio.ini.template.static 与 RobWorkStudio.ini.shared.in 至 wtA bin/——未改动任何被跟踪产物。
- 送验对象冻结复核：验收起止两次核验 origin/wp07-t04 == 2df7eb2c（零漂移）；验收者未向任务分支推送任何提交；实施工作树（主树 wp07-t04 检出）未触碰；pipeline/state.json 与 lock 未读写。
- 本记录落 evidence 分支 `acc/POL-T04/1` 并推送，commit SHA 见验收请求回执三元组；随附 findings.json 变更＝F-076/F-077/F-078 三条建议级转登（本记录同分支同提交）。
