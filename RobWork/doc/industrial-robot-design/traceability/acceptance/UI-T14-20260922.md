# UI-T14 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | UI-T14——ui 契约测试套件（§12 全用例） |
| 契约 | tasks/foundation/UI-T14.json（schemaVersion ird-task/1） |
| 送验对象（冻结） | `4f74ec22a200565c74da81b7aa679d8228f4c7d8`（分支 wp10-t14 尖端；验收全程按 SHA，不按分支名） |
| diff 范围 | `11534184449b295252f346e460ced8306d935d0a..4f74ec22`（base＝origin/redesign-main 尖端＝UI-T13 合入收尾；range 内 2 提交：`cf408f24` 实现＋`4f74ec22` [units] ui.md v1.6 登记） |
| 裁决 | **pass**（4.1~4.11 全通过；建议级问题 1 项转登 F-287，不阻塞合入） |
| 验收日期 | 2026-09-22 |
| 验收者 | 独立上下文验收会话（ACC §2 四条独立性要求声明遵守：未消费实施叙述，全部结论以亲手复现的产物与证据为准） |

复现现场：detached worktree @ `4f74ec22`（临时目录 A）；门禁基线另设 detached worktree @ base；本记录落于 `acc/UI-T14/1` 分支（worktree B，基于 `origin/redesign-main`）。

---

## 4.1 改动范围 —— 通过

- `git diff --name-only base..head` 共 **21 文件**：ui 单元 5（`ui/CMakeLists.txt`、`contract_test/PeerBridgeContractTest.cpp` 758 行新增、`contract_test/Section12RegistryTest.cpp` 169 行新增、`test/AboutDialogModelTest.cpp` +67、`test/TaskPresentationModelTest.cpp` +147）＋traceability 留痕 15（`builds/wp10-t14/` 14 件＋`wp10-t14-gate-registrations.md`）＋`units/ui.md` 1——全部落在契约 `allowedFiles`（`ui/**`、`units/ui.md`、`traceability/**`）内。
- `forbiddenFiles`（REQUIREMENTS.md/ARCHITECTURE.md）零触碰；同 diff 范围对框架源码（RobWork/RobWorkStudio/RobWorkSim 产品源码区）与 `patches/` 的检索零命中——**无未登记框架改动（SA-02 干净）**。

## 4.2 可复现构建 —— 通过（集成定向口径零 error；冒烟零 error；框架全仓构建的既知事实如实登记）

- **集成模式重新配置**：临时目录 A 全新构建树 `cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=<vcpkg> -DCMAKE_PREFIX_PATH=<Qt6>`；缓存 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON` grep 确认。主树 build 缓存同口径（`CMAKE_TOOLCHAIN_FILE`＝vcpkg、`CMAKE_PREFIX_PATH`＝Qt6——AGENTS §4.1 命令行未含 toolchain 参数，但主树集成构建实际带之，按主树实际口径复现）。
- **复现环境两处既知卡点（非本任务引入，家族在案）**：①全新 worktree 缺 `RobWorkStudio/bin/RobWorkStudio.ini.template.static`（.gitignore 吞掉、F-007 家族——主树补拷同款绕过，UI-T13 验收先例）；②Boost 定位需 vcpkg toolchain（同主树口径）。
- **集成定向构建（仓库既定集成口径——先例任务 integration-build.log 均为 `--target <ird 目标>`，非全仓 ALL_BUILD）**：`cmake --build build --config Release --target sdurws_ird_ui_test sdurws_ird_ui_contract_test sdurws_ird_ui_gui_test ird_gates`——三测试目标编译链接零 error（三 exe 产出）；`ird_gates` 目标 exit 1＝门禁"命中即失败"设计行为（47 处基线命中，全部为 DTB §4.5 已登记例外，见 4.4 门禁段）。验收者日志：`a_int_targeted_build.log`（验收工作目录留存，未入库——不向任务分支推送的约束；下同）。
- **全仓 ALL_BUILD 的如实登记**：验收者另跑过一次全仓 `cmake --build build --config Release`，框架 RobWorkSim `RWSimulatorPlugin` TU 在 `rw/math/Math.hpp` 报 73 处语法错误——**全部位于框架只读区**（base..head 对框架零改动，该 TU 的编译输入与 base 逐字节相同，故此失败在 base 同样存在，与本任务改动无因果关系）；主树集成树从未构建过该目标（无产物，先例日志口径亦为定向构建）。判定：框架区既知事实，不属本任务送验面，不影响 4.2 结论。
- **独立冒烟模式**：`cmake -S <A>/RobWork/RobWorkStudio/src/rwslibs/industrialrobot -B <冒烟临时目录> -DCMAKE_TOOLCHAIN_FILE=<vcpkg> -DQt6_DIR=<Qt6>`＋`cmake --build ... --config Release`——**exit 0、零 error**，三测试 exe 产出。（第一次冒烟构建 exit 1 系验收者自身操作竞态：变异检查 M3 临时修改 ui/CMakeLists.txt 恰与后台冒烟构建重叠触发重配置命中守卫——还原后重跑 exit 0，与交付物无关。）
- **双模式测试亲跑**：集成树 154/154＋22/22＋27/28（唯一失败＝`ParamTablePanelGuiTest.BatchPasteImpactDetail_UI_T08_ACC1`，验收者本机复现同根因 `OleSetClipboard: COM error 0x800401d0: OpenClipboard`——v1.4 已登记的宿主剪贴板占用环境例，与本任务零耦合，testkit §7.5 不记为通过）；冒烟树 154/154＋22/22＋27/28（同一环境例）——与送验留痕 6 份 ird-test-report.json 逐份同口径一致。

## 4.3 契约 verify 命令 —— 通过（实质执行成立；逐字口径空转＝F-251/F-252 既有开放条目，如实登记不重复转登）

| 命令 | 验收者亲跑结果 |
| --- | --- |
| `ctest --test-dir build -C Release -L "^ird$"`（逐字） | exit 0＋`No tests were found!!!`——**空转**，与 F-251 登记的框架 CTestTestfile 链缺失（顶层未 enable_testing）逐字吻合；F-251 resolution ② 明文预告"UI-T14 契约同款命令将同族踩中"，实例已被该开放条目覆盖，不另开新号 |
| `ctest --test-dir build -C Release -L "^ird_gui$"`（逐字） | 同上空转 |
| `ctest --test-dir <ui 叶子构建目录> -C Release -L "^ird$"`（修正口径，沿 RPT-T11 验收先例） | **2/2 Passed**（sdurws_ird_ui_test 1.61s＋sdurws_ird_ui_contract_test 0.04s），exit 0 |
| 同上 `-L "^ird_gui$"`（§12.2 完整环境：QT_QPA_PLATFORM=windows＋QT_PLUGIN_PATH＋绝对路径单实例） | exe 级 Failed＝27 passed＋1 failed（同一剪贴板环境例）——与实施者留痕声称逐字一致 |

实质执行另以三 exe 直接运行双模式四跑佐证（4.2 段），非零个测试真实执行且结论可复核。

## 4.4 acceptance 条目逐项证据 —— 通过

| # | acceptance 条目 | 证据（验收者亲手复现） |
| --- | --- | --- |
| 1 | ctest `ird`/`ird_gui` 标签注册与 §12.3 全族具名落位＋ird-test-report.json 留痕＋UI-EXEC-1 登记 | 标签：`ui/CMakeLists.txt` 尾部 `set_tests_properties(... LABELS "ird")`/`LABELS "ird_gui"`（ird＝test＋contract_test、ird_gui＝gui_test）——叶子目录 ctest 双命令实测标签过滤生效（4.3 表）。§12.3 全族：`Section12Registry.AllAcceptanceFamilyCasesRegistered_UI_T14` 在 154 内亲跑 PASSED——十一承接族（WB×3/SES×7/DRF×4/CMD×7/HKY×3/STG×3/TSK×5/PLG×2/DIA×1/PERF×1/LCY×1）＋UI-EXEC-1 逐编号存在性审计通过（审计文件自排除防恒真、缺失全量列示；V3D/PALETTE/POL/FRM/HELP 族由已合入任务文件承载、不属承接族——与契约 scope 口径一致）。留痕：6 份 ird-test-report.json（集成 model/contract/gui＋冒烟 model/contract/gui）每例绑定 TestRecord（含 requirementIds/atIds），outcome 计数 154/22/27+1 双模式一致。GUI 环境不满足不得绿灯：唯一失败例 outcome=failed 如实落痕（非绿灯）。UI-EXEC-1 执行限制具名化：`QueuePositionWordingOnlyWithoutNumbers_UI_EXEC_1` PASSED＋M2 变异即红（4.5） |
| 2 | UI-HKY-1/UI-STG-3/UI-WB-1 用例通过留痕；GUI 按 §12.2 Windows 流程执行 | 验收者亲跑 `[ OK ]`：`ShortcutRegistryModel.ConflictRejectedWithDiagnostic_UI_HKY_1`（＋`_UI_HKY_1b`）、`StatusWordModel.SevenStateMappingTableRows_UISTG3_UI_T04_ACC1`/`SevenStatePriorityChain_UISTG3_UI_T04_ACC1`、`WorkbenchShellGuiTest.FiveZoneLayout_PersistsAcrossSessions_UI_WB_1`。§12.2 纪律：QT_QPA_PLATFORM=windows（未用 offscreen）、绝对路径单实例、GUI exe 与模型测试分开命令执行——验收者全程遵守并与留痕记录一致 |
| 3 | O-31 处置（测试面直链＋FaultInterceptor 桥接＋产品面守卫不变） | 直链：`ui/CMakeLists.txt` `target_link_libraries(sdurws_ird_ui_contract_test PRIVATE sdurws_ird_ui sdurws_ird_project sdurws_ird_execution sdurws_ird_diagnostics sdurws_ird_testkit GTest::gtest)`——与 ui.md §3.1 行 v0.4 原文"落位直链面"一致，**F-228 随之消解**（该条 status=fixed 于 2026-09-19，本任务兑现"project/execution 直链随 UI-T14 落地登记"句）。桥接：`PeerBridgeContractTest` 四用例在 22 内亲跑 PASSED（直链登记钉/C-6 InteractionL5Adapter＋FaultInterceptor 故障注入＋双侧成员函数指针 static_assert 镜像钉/ITaskScheduler 伪造＋投影桥/RedactionPolicy 值面钉）。产品面守卫不变三道防线亲证：①配置期守卫——M3 变异向 `sdurws_ird_ui` 链接块注入 `sdurws_ird_project` 后重新配置即 `CMake Error at ui/CMakeLists.txt:226: ui 依赖红线（O-31/R-1/T-1）……发现 sdurws_ird_project`（FATAL_ERROR，还原后配置恢复 exit 0）；②`PeerBridge.ContractTestDirectLinkFaceRegistered_O31_UI_T14` PASSED（产品库链接块零对端零 testkit 文本钉）；③`NoCrossUnitInclude_O31_UI_BUILD`（BuildRedLineTest，154 内）PASSED。门禁：验收者同引擎 `cmake -P cmake/ird_gates.cmake` 对 head 与 base 双 worktree 独立复跑，路径归一化去重 diff＝**恰增 3 条唯一 SUB 命中**（contract_test→project/execution/diagnostics，即 O-31 裁决测试面既定形态）、其余逐码零变化（base 95＝LIB×8/R-1×1/R-3×37/R-4×3/R-5×1/SUB×39/T-1×5/T-2×1；head 101）——与 `gates-hit-set.txt`/`gates-hit-set-diff.txt`/`wp10-t14-gate-registrations.md` 留痕完全一致 |

## 4.5 测试真实失败能力 —— 通过（三处变异，全部即红；超出 ≥2 要求）

| 变异 | 构造（产品源码，验收 worktree 内、还原后未推送） | 结果 |
| --- | --- | --- |
| M1 | `UiText.cpp:129` 插件装配失败标签 `"装配失败"`→`"未装配"` | `AboutModel.PluginAssemblyFailureDegradationIsolatesRow_UI_PLG_1` **FAILED**（重建后即红） |
| M2 | `UiText.cpp:167` 排队文案 `"排队中（等待资源）"`→追加数字 `"…0"` | `TaskPresentationModelTest.QueuePositionWordingOnlyWithoutNumbers_UI_EXEC_1` **FAILED**（零数字纪律真实检出） |
| M3 | 产品库 `sdurws_ird_ui` 链接块注入 `sdurws_ird_project` | CMake 重新配置即 **FATAL_ERROR**（O-31 配置期守卫 `ui/CMakeLists.txt:226` 触发）——守卫非摆设 |

三处变异均已还原（`git status` 干净、还原后重新配置 exit 0），worktree A 为验收临时现场，随记录推送后销毁，不影响送验对象。

## 4.6 架构红线（AGENTS §5 八条）—— 通过

1. **L2 零 Qt**：本任务变更面全在 ui 单元（R-3 例外本体＋其测试目标）——新增两契约 TU 零 `#include <Q…>`（grep 实证），计算内核单元零触碰。
2. **R-1 业务域互链**：零 business-domain 单元引用；contract_test 直链的 project/execution/diagnostics 属 L4 支撑单元且为 O-31 裁决测试面既定形态（4.4③ 门禁 diff 唯一增量即此 3 条，逐条与裁决登记一致）。
3. **R-2 私有头**：新增 include 全集＝`sdurws/ird/{project,execution,diagnostics,ui,testkit}` 公共头（`include/sdurws/ird/<unit>/` 路径 ls 实证 6/6）＋gtest＋std——零私有头跨单元。
4. **SA-02 框架零改动**：4.1 已证（patches/ 无新文件）。
5. **R-4 名称前缀**：新增代码零 RobWork 名称拼接/剥离（门禁 R-4 码 base↔head 零变化——唯一既有 project 面命中两侧同在）。
6. **权威唯一（PA-1）**：本任务零产品行为变化（实现提交自述＋验收者 diff 复核：产品库源文件/CMake 链接块逐字未动；变更面＝测试代码＋测试目标链接面＋文档留痕）；测试对名称解析/单位/诊断文案只消费不自裁（Section12Registry 只读扫描源码树；PeerBridge 词表映射表以注入承载并注明"形状钉不钉词表权威"）。
7. **PA-2 不可变历史**：零修订数据结构触碰、零写路径。
8. **不恢复 old/**：零 old/ 引用。

## 4.7 注释规范 —— 通过（本任务零新增公共头、零新增实现 TU；测试/CMake 全查）

- 两个新契约 TU 文件头均具备：`@file/@brief`＋设计依据逐条锚定（契约 acceptance、ui.md §3.1/§12.1/§12.3、testkit.md §6.4/§2.4、project.md §5.3.3、先例 TU）＋"为什么这样设计"的机制解释（如 PeerBridge 头部"为什么形状漂移由测试面捕获能成立"段）＋线程模型声明（单线程 gtest 串行/QCoreApplication 由 main 构造）。
- 类/函数/fixture：每个替身类与 TEST 均有中文职责注释；关键步骤注释解释业务含义（如 FaultInterceptor 故障缝选点理由、mutable 修约说明、Queued 快照 run 留零值的 P-EX-6 口径）。
- §2.5 高危信息：线程约束（"测试串行纪律下无并发——Fault.hpp 线程约束"）、所有权（`peer_` 非 owning 注释）、错误语义（端口"纯查询不抛"折叠契约、fail-fast 面）、确定性/判据来源（UI-PERF-1 判定＝心跳时间戳与刷新耗时，1.1 s 限时等待为场景构件非判据——§12.2"不使用固定 sleep 判据"纪律逐句对应）；物理量均带单位语境（25 ms 采样/200 ms 阈值/50 ms 刷新周期/>1 s 慢查询，且注明"阈值即用例值不放宽"）。
- CMakeLists.txt 增量块沿用单元既定注释密度（直链面依据/O-31 守卫不变声明/新 TU 注册理由逐段中文注释）。
- 追溯字段：新用例均带 `IRD_TEST_INFO`（如 UI_PLG_1→SA-01/UX-14、EXEC_1→TASK-01、UI_T14 系列→SA-15/NFR-PERF-01/NFR-SEC-07/NFR-MNT-01）。

## 4.8 文档同步与 findings 核对 —— 通过

- **实现与任务卡零偏差**：ui.md v1.6（§13 UI-T14 落位登记注＋§16.7 v1.6 行，4 行新增）与实现逐项对得上（17→22 契约＝4＋1、151→154 模型＝3、GUI 27/28、ctest 双口径、门禁恰增 3 条唯一 SUB——验收者逐项独立复现一致）；`units/ui.md` 修订属契约 allowedFiles 且为表尾追加增量、既有章节零改动。
- **findings.json 核对（open 250 条）**：与本任务直接相关且本次处置——①**F-251/F-252**（open，ctest `--test-dir build` 空转口径）：本契约 verify 命令逐字复跑空转实证（4.3），实例被 F-251 resolution ② 明文预告并覆盖（"同步 UI-T14 契约同款命令"待构建约定所有者），家族在案**不重复开号**；②**F-228**（fixed）：直链兑现随本任务落地，ui.md §3.1 行措辞已区分直链/传染——消账闭环维持；③**F-007**（RobWorkStudio.ini 模板 .gitignore 吞没）：验收复现并以主树补拷同先例绕过，家族在案不重复登记；④**F-010/F-283/F-284 家族**（.gitignore `*.log` 吞留痕日志）：本任务复踩（4.10/问题清单），转登 **F-287**。其余抽核（F-259/F-278 家族、F-041 家族等）属他任务/治理域或已随本任务面自然覆盖——无"可顺手消账而未消账"条目。
- 任务状态/追踪矩阵：状态翻转归收尾（PIPE §4.7⑤ 先例），不在送验对象范围；契约 note 的 DOC-T13 编译回填字段与实际一致（branch=wp10-t14、requirements UX-03/09/10/13、designRefs §12.1~12.4）。

## 4.9 commit 质量 —— 通过（唯一瑕疵属 F-287，见 4.10）

- `cf408f24`：标题 `[WP-10-T14] 实现 ui 契约测试套件：…` 动词开头；正文四段齐备（What 按文件/模块列要点、Why 锚 acceptance 1/3＋ui.md §3.1/§12、How 双模式构建＋154/22/27-28＋ctest 叶子口径＋门禁逐码比对＋validate-task、影响面＝产品库零改动）；`Acc-Covered: 1、3` 在位。
- `4f74ec22`：标题 `[units] ui.md v1.6：…`；四段齐备；验证段如实转引。
- **验证结论与留痕一致性**：全部关键数字（154/154、22/22、27/28、恰增 3 条唯一 SUB、validate-task PASS）经验收者独立复现全部为真，**无粉饰**。唯一出入＝该提交声称归档的原始 .log 未入库等三处清单失准（纯归档面、结论面不受影响）——转登 F-287（4.10）。

## 4.10 留痕完整 —— 通过（结论面完整；原始日志缺席转登 F-287）

- 在库留痕 15 件：ird-test-report×6（双模式×三目标，每例 TestRecord）＋gtest XML×5（`sdurws_ird_ui_{contract_test,contract_test-smoke,gui_test,gui_test-smoke,ui_test-smoke}.xml`）＋gates-hit-set×3＋gate-registrations.md。验收者据在库留痕逐项独立复现成功（构建/测试/门禁/validate-task 全对上）——结论面完整。
- `ird-test-report-gui.json` 对环境失败例如实记 `outcome=failed`（含 COM 0x800401d0 reason），不记通过——testkit §7.5 口径满足。
- 提交信息与 gate-registrations.md 引用的原始日志（`ird_gates-integration/base/smoke.log`、ctest 双标签日志、控制台日志、构建日志两件、`validate-task.log`）实际零入库（.gitignore:87 `*.log` 全局规则，家族根因）→ 转登 **F-287**；同提交"ird-test-report.json 五件"实为 6 件、"三目标 gtest XML（双模式）"实际在库 5/6（集成 `sdurws_ird_ui_test.xml` 缺席）——同条并载。
- `phase-one-validation.log` 为 WP-03 时代聚合日志，UI 系先例（UI-T11/T12/T13 同态）未逐任务更新，非本任务引入。

## 4.11 偷懒/缩水实现扫描 —— 通过

1. **标记扫描**：diff 新增行 `TODO|FIXME|stub|placeholder|未实现|not implemented|XXX|HACK` 全量 grep——**零命中**（无契约"触发式延后"豁免需要列出）。
2. **空实现检测**：本任务零新增公共接口函数；四用例断言面逐条核非恒真（存在性审计断言、值透传断言、故障注入路径断言、零数字字符逐字符断言、心跳间隙/刷新耗时实测断言）；审计用例自排除防自匹配恒真（文件头明示＋实现落实）。
3. **acceptance 逐条证据表**：见 4.4——三条全部有具名测试/命令输出/文件行号证据，零条"仅声明"。
4. **测试强度**：与 4.5 联动——三处产品源码变异（文案改义/数值注入/红线增边）分别使对应用例或配置守卫即红，排除恒真与"只编译通过"型测试。

---

## 发现的问题清单

### 阻断级

无。

### 建议级（已转登）

1. **F-287**（suggestion）：F-010/F-283/F-284 家族复踩（UI 系连续第 9 例）——留痕提交 `4f74ec22` 正文声称归档 `ird_gates-integration/base/smoke.log`×3、ctest 双标签日志、控制台日志、构建日志两件、`validate-task.log`，实际 .log 零入库（.gitignore:87 全局 `*.log`，未 add -f）；同声称面另有"ird-test-report.json 五件"实为 6 件、gtest XML 在库 5/6（集成 `sdurws_ird_ui_test.xml` 缺席）两处清单失准。验证结论本身（双模式零 error、154＋22＋27/28、门禁恰增 3 条唯一 SUB、validate-task PASS）经本验收独立复现全部属实，不构成粉饰；处置建议与家族同口径：治理一次性解决 .gitignore 根因（负例规则或 .txt 惯例成文）后自动消账。

### 已有开放条目复踩（不另开号）

- F-251/F-252：契约 verify ctest 逐字口径空转——F-251 resolution ② 明文点名 UI-T14 同款命令，实例如预期复踩；本验收以叶子目录口径（沿 RPT-T11 先例）证非零个测试真实执行。处置权在构建约定所有者（DTB §5.1 辖域）。
- F-007：RobWorkStudio.ini 模板 .gitignore 吞没致全新 worktree 配置卡死——复现并以主树补拷绕过（UI-T13 同款）。

## 裁决

**pass**。4.1~4.11 全部通过，无阻断级问题；建议级 1 项（F-287）已按 ACC §5 转登 findings.json（随本记录分支入库），不阻塞合入。送验对象＝SHA `4f74ec22a200565c74da81b7aa679d8228f4c7d8`；后续收尾按 PIPE §4.7 由所有者决策。

## 验收者环境说明

- Windows 10 x64（win32 10.0.26200）、Git Bash、CMake 4.3＋VS2022 17.12.6 msbuild、Qt 6.11.1 msvc2022_64、vcpkg x64-windows（gtest 动态库）。
- 复现现场：detached worktree @ `4f74ec22`（临时目录 A，含独立 build 树）＋独立冒烟构建目录＋detached base worktree（门禁基线）＋worktree B（本记录）。验收者原始运行日志留存于验收工作目录未入库（遵守"不修改实施产物、不向任务分支推送"约束；入库留痕以送验对象自带 15 件为准）。
- 全程未向 wp10-t14 推送任何提交、未修改送验对象内容；变异检查全部在验收 worktree 内完成并还原。
