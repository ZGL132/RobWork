# WP-13-T11 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | WP-13-T11 实现基座安装姿态编辑（预设＋任意欧拉角）——modeling 单元 |
| 送验对象（冻结 SHA） | `049828737a5b02f49cec0315dafaf18fd80d33f1`（分支 wp13-t11 尖端；2 提交：287dc703 实现＋04982873 文档同步） |
| diff 范围 | `bf9c995d3fe41bbe783b245883d74a93941c79de..049828737a5b02f49cec0315dafaf18fd80d33f1` |
| 验收尝试 | 1（分支 acc/WP-13-T11/1） |
| **verdict** | **pass**（4.1~4.11 全部通过，无阻断级问题；建议级 3 条已转登 findings.json：F-345/F-346/F-347） |
| 验收日期 | 2026-09-25 |

## 0. 独立性声明与环境（ACC §2/§3）

- 验收者为全新上下文会话，未消费实施者任何过程叙述；只读任务契约（`tasks/foundation/WP-13-T11.json`）、设计文档（`units/modeling.md` v0.13 @ 送验 SHA、`units/runtime.md` §6.2、`REQUIREMENTS.md` MDL-22/AT-37 原文）与在库留痕；构建与测试全部亲手重跑（复现优先于采信），全程为"不通过"找证据。
- **复现现场**：`git worktree add --detach <临时目录A> 04982873`（detached @ SHA）；base 侧对照现场：detached worktree @ `bf9c995d`（gates 引擎直跑比对用）；记录回传现场：`git worktree add -b acc/WP-13-T11/1 <临时目录B> origin/redesign-main`。
- **分支漂移检查**：`origin/wp13-t11`（04982873…33f1）== 送验 headSha，**无漂移**。
- **集成模式**（A 内全新配置，主树缓存同口径）：`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DCMAKE_TOOLCHAIN_FILE=<repo>/vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DRWS_USE_STATIC_LINK_PLUGINS=ON` → "Configuring done/Generating done" 零错误；缓存确认 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`。环境链缺口按在案口径处置：①`RobWorkStudio/bin/` 两份 ini 模板为 gitignore 本机环境件，自主树复制进 worktree 后配置通过（F-007/F-062/F-098/F-099 口径，属环境供给非送验对象改动——A 内源树 `git status` 全程零改动，变异测试还原后亦然）；②RobWorkSim 框架插件两目标（RWSimulatorPlugin/bt_plugin.rwplugin）在全新 worktree 因 windows.h min/max 宏冲突编译失败（C4003→级联 73 error）——**F-323 已登记的框架级缺口逐字复现**（错误目标、根因、数量均一致），INDUSTRIALROBOT 全部目标零波及（构建日志 grep 计数 0），本任务 diff 不触达 RobWorkSim；按 T10 先例逐目标构建契约涉及目标。
- **独立冒烟模式**（另一临时目录，`-S …/industrialrobot`＋vcpkg toolchain＋Qt CMAKE_PREFIX_PATH，AGENTS §4.1 修正口径）：配置 "Generating done" 零错误；全量构建 exit 0、`error C|error LNK` 计数 0。
- **门禁口径**：按 F-336 建议采用引擎直跑 `cmake -DIRD_ROOT=<…industrialrobot> -P cmake/ird_gates.cmake`（IRD_SELFTEST_DIR 用缺省 TEMP——见 §4.8 末与 F-347 的操作防呆教训）。
- **宿主 GUI 冒烟**：本任务契约 verify 与 acceptance 均为计算库面（modeling 无界面目标），无 GUI 冒烟项——无未执行项。
- 验收者环境：Windows 10.0.26200 x64；Visual Studio 17 2022（MSVC 19.42）；Qt 6.11.1（msvc2022_64）；vcpkg 经典模式（主树 `vcpkg/installed/x64-windows`）。

## 1. 逐项证据（ACC §4，4.1~4.11）

### 4.1 改动范围 —— 通过

`git diff --name-status bf9c995d..04982873` 共 22 文件：modeling 单元 4（CMakeLists.txt、include/…/Template.hpp、src/Template.cpp、test/BasePlacementEditTest.cpp〔新增〕）＋`traceability/builds/wp13-t11/` 16 件留痕＋`traceability/wp13-t11-prt4-crosscheck.md`＋`units/modeling.md`。逐文件对照契约 `allowedFiles`：modeling `**` 通配、`units/modeling.md` 明列、`traceability/**` 通配——**全部在册**；`forbiddenFiles`（REQUIREMENTS.md/ARCHITECTURE.md）零触碰；diff 无 `patches/` 路径（SA-02 无未登记框架改动）、无 `RobWork/RobWork/`、`RobWorkSim/`、`RobWorkStudio/`（industrialrobot 之外）任何文件。

### 4.2 可复现构建 —— 通过

- 集成模式（重配后逐目标，契约涉及面）：`sdurws_ird_modeling`/`sdurws_ird_modeling_test`/`sdurws_ird_modeling_contract_test` 三目标 exit 0、编译/链接错误计数 0（产物 `sdurws_ird_modeling.lib`＋两 exe 落位 build/RobWorkStudio/…/Release/）。全量 `cmake --build build --config Release` 在 RobWorkSim 框架插件处失败＝F-323 在案框架缺口（与本任务无关、基线同现），INDUSTRIALROBOT 零波及。
- 独立冒烟模式：全新目录配置＋构建 exit 0、错误计数 0；`sdurws_ird_modeling_test` 亲跑 **139/139 PASSED**＋`sdurws_ird_modeling_contract_test` **5/5 PASSED**（与在库 smoke 报告 144=139＋5 一致）。

### 4.3 契约 verify 命令 —— 逐条执行完毕（6 条）

| # | 命令 | 结果 |
| --- | --- | --- |
| 1 | `cmake --build build --config Release --target sdurws_ird_modeling` | exit 0，零错误，lib 落位 |
| 2 | `cmake --build build --config Release --target sdurws_ird_modeling_test` | exit 0，零错误，exe 落位 |
| 3 | `ctest --test-dir build/RobWorkStudio/src/rwslibs/industrialrobot/modeling -C Release -L "^ird$"` | **2/2 PASSED**；补亲跑契约 exe（F-338/F-344 interim 口径——T11 契约 verify 同样缺该目标构建行，见 F-345）：**9/9 PASSED**；`sdurws_ird_modeling_test` 亲跑 **184/184 PASSED**（含 `MdlBasePlacementEdit` 9 条全过）——与在库 `ird-test-report.json`（193/193）、`smoke-ird-test-report.json`（144/144）逐件一致 |
| 4 | `cmake --build build --config Release --target ird_gates` | exit 1＝**存量命中（门禁按设计命中即 FATAL——基线侧同样 exit 1）**；引擎直跑口径取证：head（worktree @04982873）与 base（detached worktree @bf9c995d）直方图 **逐码一致**（IRD-GATE-SUB 57/R3 41/LIB 10/T1 5/R4 3/T2 1/R5 1/R1 1，发射 119 次）→**归一化多重集 diff 为空＝零新增命中**，与在库 `ird-gates-branch-hits.txt`/`ird-gates-comparison.txt` 逐件一致 |
| 5 | `pwsh -File …/validate-task.ps1 -TaskFile …/WP-13-T11.json` | **PASS** (1 tasks)，exit 0 |
| 6 | `pwsh -File …/validate-docs.ps1` | **PASS** (20 units, 12 trace entries, 189 task files)，exit 0 |

### 4.4 acceptance 条目 —— 四条均有对应证据（全部为本次亲跑复现，具名用例）

| 契约 acceptance | 证据（亲手复现） |
| --- | --- |
| 1 BasePlacement 编辑 UT（V-13，MDL-22/AT-37）：三预设＋custom（customEaa 必填 rad）＋basePosition（m）；apply-robot-design 原子持久化单命令单修订；词表与 runtime 一致 | `ApplyThreePresetsAndPosition_WP13T11_ACC1_V13`（inverted/wall/ground 逐预设接受、preset 直写、位置 m 以 UserProvided Provided、非 Custom 时 EAA 保持 NotProvided、每次恰一条变更记录）；`ApplyCustomEaaEdit_WP13T11_ACC1_V13`（EAA rad Provided＋切离 Custom 复位 NotProvided 非静默）；`PresetTokenVocabularyIsRuntimeEnum_WP13T11_ACC1`（**static_assert 类型级钉住** BasePlacement::preset 即 runtime::InstallationPresetToken——平行词表必编译失败）；`AtomicPersistenceViaApplyRobotDesign_WP13T11_ACC1_V13`（prepare 产出**恰一根写入**＋requiresDualCompile＋inverse 逆载荷＝前一版根字节〔PA-2〕＋写入字节解码逐字段一致＋二次编辑仍单写入） |
| 2 未配置默认地面（V15-04）＋I-MDL-7 非法组合拒绝 | `DefaultGroundTemplatePath_WP13T11_ACC2_V15_04`（ground＋零位 m、provenance UserProvided＋methodTag=template/generic-6r、inverted 显式选择不被覆盖）；`DefaultGroundImportPath_WP13T11_ACC2_V15_04`（URDF 映射层填 ground＋defaults 清单含 basePlacement/ground/import-mapped）；`IMdl7IllegalCombinationsRejected_WP13T11_ACC2`（a 缺 EAA→CustomEaaMissing；b 零 EAA R=I→PresetIdentityRotation〔runtime InputInvalid 同口径〕；c/d **容差边界** 1×10⁻⁶ rad 接受·1×10⁻¹³ rad 拒绝〔1×10⁻¹² 判定的两侧实测〕；e/f NaN/Inf→ValueNotFinite；g 非 Custom 携带 EAA→`std::invalid_argument` fail-fast；**全部拒绝路径工作集字节不变**断言） |
| 3 P-RT-4 交叉核对留痕（O-16 兑现行） | `Prt4PresetAxisSingleAuthority_WP13T11_ACC3`：三预设编辑产物经 Description.base 同款映射交 runtime 解析——inverted=R_x(π) diag(1,−1,−1)、wall=R_y(π/2)、ground=I 与 runtime.md §6.2 冻结值逐元素一致，且与 `installationPresetRotation()` 输出逐元素相等；位置分量原样透传（m）。留痕文件 `traceability/wp13-t11-prt4-crosscheck.md` 在库并核读：唯一权威产出点、modeling 只存参数不存矩阵（`RobotDesign.hpp:281-294` BasePlacement 结构仅 preset/customEaa/basePosition 三字段——结构面核实；`Codec.cpp:695-698` 编码仅三字段、无矩阵字节）、§7.7 引用失真卡缺陷注记在案 |
| 4 编译单一字段（MDL-22/M-11） | `SingleFieldCompileIntoRWorldBase_WP13T11_ACC4`：custom EAA（0,0,π/2 rad）→runtime 解析 R 与手算 R_z(π/2)=[[0,−1,0],[1,0,0],[0,0,1]] 逐元素 1×10⁻¹² 一致＋平移=basePosition；`checkWorldBaseTransform`/`checkPresetConsistency` 对编译产物全部通过；**字节面单字段证据**（编码字节变化＋往返解码 basePlacement 一致＋摘除 basePlacement 后其余字段与编辑前全量相等）；modeling 侧不计算/不缓存/不二次叠加＝结构面（无矩阵字段可承载）＋本用例直接断言（R 全由 runtime 规则面从三参数解析） |

### 4.5 测试真实失败能力 —— 通过（两处变异均致红，注入后已还原）

在 detached worktree A 内注入（各自重建→亲跑→`git checkout --` 还原；还原后 `git status --porcelain`＝零改动、重跑复绿 **MdlBasePlacementEdit 9/9**）：

- **M1**：`Template.cpp:804` 恒等拒绝分支禁用（`if (identity)`→`if (false && identity)`）→ **1 FAILED**：`MdlBasePlacementEdit.IMdl7IllegalCombinationsRejected_WP13T11_ACC2`（零 EAA 与 1×10⁻¹³ rad 两侧本应拒绝的输入被接受——I-MDL-7 后半映射层拒绝面真实有效）；
- **M2**：`Template.cpp:873` 变更摘要提交禁用（`ws.changes.push_back(std::move(record))` 注释）→ **1 FAILED**：`MdlBasePlacementEdit.ApplyThreePresetsAndPosition_WP13T11_ACC1_V13`（"一次编辑恰一条变更记录"断言落空——UX-05 粒度断言真实有效）。

### 4.6 架构红线 —— 通过（AGENTS §5 八条逐条）

1. **L2 零 Qt**：本任务 diff 四个源文件 grep `Qt` 零命中；门禁命中集 head=base 零新增（Qt 面命中全部为 ui 单元存量）。
2. **R-1 业务域互链**：modeling include/src grep `ird/(requirements|kinematics|trajectory|dynamics|selection|optimization)` 零命中；跨单元消费仅 runtime 公共规则面（六类端口之外的既有登记边——Template.cpp 新增 include `runtime/BaseWorldTransform.hpp` 走 modeling→runtime 既有登记边，门禁 LIB/SUB 命中集零新增佐证）。
3. **R-2 跨单元私有头**：新增 include 均为公共头（`include/sdurws/ird/runtime/BaseWorldTransform.hpp`）；无 `src/` 深路径、无相对路径跨单元包含。
4. **SA-02 框架零修改**：diff 无 industrialrobot 之外文件、patches/ 无新文件（见 4.1）。
5. **R-4 名称前缀**：Template.cpp 无 RobWork 名称拼接/剥离逻辑（名称解析归 runtime——本任务未触名称面）。
6. **PA-1 权威唯一**：建模只存参数（BasePlacement 三字段）；R_world_base 唯一由 runtime 编译（ACC4 用例）；EAA→R 换算唯一调用 runtime::rotationFromCustomEaa（Template.cpp 注释与实现一致，无私设第二套换算）；未新增任何诊断稳定码（DiagCodes 未触——§9.5 登记纪律）。
7. **PA-2 不可变历史**：ACC1 原子持久化用例断言 inverse 逆载荷携带前一版根字节；diff 无任何改写历史路径。
8. **不恢复 old/**：diff 无 old/ 引用。

### 4.7 注释规范 —— 通过（公共头全查＋实现/测试/CMake 全查——本任务改动面共 4 文件）

- **公共头 `Template.hpp` T11 增段（603~742 行）全查**：每个类型/函数 Doxygen 中文齐备——`BasePlacementEditErrorCode`（四值逐值带业务语义与检查序）、`basePlacementEditErrorCodeToken`（纯函数/确定性声明）、`BasePlacementEditError`（detail 面与呈现文案分层）、`BasePlacementEditValue`（整体替换语义、单位 rad/m 显式、"★ modeling 只存参数不存矩阵"P-RT-4 纪律段）、`applyBasePlacementEdit`（①~⑥检查序逐条、@param 方向、@return 两态语义、@throws 触发条件、**非线程安全（编辑态仅 UI 线程）**、确定性声明）。
- **实现 `Template.cpp` T11 段（718~875 行）**：文件头 @brief 同步 T11；①~⑦语义段落注释齐备——契约违约 fail-fast 与"用户可输入面走值面拒绝"的错误语义区分、1×10⁻¹² 容差来源（与值模型 I-MDL-7 同源，非自设魔数）、候选构造先校验后提交的强保证来源、切离 Custom 复位 NotProvided 的显式语义（NFR-COR-03）、摘要不含数值字面的确定性理由（NFR-COR-02/locale）。
- **测试 `BasePlacementEditTest.cpp`（845 行）**：文件头 ACC1~ACC4 逐条对照＋设计依据＋线程安全声明；每用例中文说明验证哪条需求/AT（IRD_TEST_INFO 追溯字段与在库合并报告一致——9 条全部带 MDL-22，7 条带 AT-37/V15-04 等）；夹具注释解释"为何命令路径选移动关节"（冒烟两模式可编译的工程理由）；期望值独立抄写不引实现常量（kPiHalf、手算矩阵）。
- **`CMakeLists.txt`**：两处 T11 增列注释块（源文件与测试目标）说明落位理由与两模式编译口径。
- §2.5 高危信息表逐项：物理单位（rad/m 逐字段显式）✓、坐标系（世界系标注）✓、角度制式（"单位 rad——不是度！"）✓、所有权/生命周期（ModelingWorkingSet 引用语义）✓、线程约束（UI 线程）✓、错误语义（调用方违约 fail-fast vs 用户输入面拒绝 vs 防御闸三层区分）✓、确定性来源（纯函数、无 locale 格式化）✓。

### 4.8 文档同步 —— 通过（零偏差；findings 未关闭条目逐条核对）

- `units/modeling.md` 升 v0.13：§3.3 表 Template.hpp 行 T11 落位＋Parts.hpp 行归属订正（**DTB §5.4 偏差登记**——basePlacement 为根对象字段、编辑域内核与逐轴编辑流同址 Template.hpp；契约内原卡注记"卡内多处引用 §7.7 失真"的处置注记随交叉核对留痕在案）＋§15 v0.13 变更记录。DTB §2.14 T11 行（需求 MDL-22/AT-37、完成条件三句、禁止项"不在基座系参数化 g"）与契约 acceptance 逐条对得上；追踪矩阵为 WP-00-T02 冻结产物、MDL-22→T11 映射已正确（无需逐任务改矩阵，T10 同先例）。
- **findings.json 未关闭条目相关性核对（275 open 逐族过筛，F-318~F-344 全数核对）**：
  - **F-338/F-344（契约 verify 缺 contract_test 构建行家族）**：**与本次送验面直接相关——T11 契约 verify 数组同样只有 modeling/modeling_test 两行，家族在本任务复发**（interim 口径补建后全量执行，ctest 2/2＋契约 9/9）→ 转登新条目 **F-345**；
  - **F-342（DiagCodes.hpp @brief 计数失实）**：同单元在册——本任务**未触 DiagCodes**（无新稳定码增登），其 owner 指定的"随 DiagCodes 增登动作同步"顺手消账窗口未触发，维持 open 属预期，无关联动作欠账；
  - **F-343（T10 散文计数失准）**：同单元在册——owner 指定"后续 modeling 文档同步窗口勘误 §14.6 v0.12 ⑦"；T11 文档同步（v0.13）实际触碰 modeling.md 但未顺带勘误该散文计数（窗口错过、非本任务送验面缺陷、权威留痕数字全对）——维持 open，此处留痕提醒后续窗口；
  - **F-336（ird_gates VS 捕获路径缩减/编码污染）**：方法论相关——本次按其建议采用引擎直跑口径取证，比对结论不受影响；
  - **F-321~F-325/F-327~F-330/F-335（ui/宿主冒烟/取证管线/流水线合并域）**：与 modeling 计算库送验面**无关联**；
  - **F-323（RobWorkSim NOMINMAX＋ini 模板不入库）**：本次全新 worktree 复现其前半（73 error 逐字吻合），industrialrobot 零波及——已在册，无新证据需追加登记。
- **F-347（新）操作教训随录**：验收者首轮门禁直跑误将 IRD_SELFTEST_DIR 指向源树 testkit/test，脚本自测夹具生成清掉了该目录跟踪文件并连累冒烟构建 C1083——`git checkout` 还原后以缺省 TEMP 夹具目录重跑，结论不变（head=base 直方图一致）、双源树还原纯净。脚本缺"SELFTEST_DIR 不得落在 IRD_ROOT 内"防呆断言→ 转登 **F-347**。

### 4.9 commit 质量 —— 通过（一处表述失实转登 F-346）

- `287dc703` 标题 `[WP-13-T11] 实现基座安装姿态编辑（三预设＋任意欧拉角）＋UT`＋正文四段（What/Why/验证/影响面）齐备；`04982873` 同格式。数字与本次亲跑一致：集成 184/184＋9/9（合并 193/193）、冒烟 139/139＋5/5（合并 144/144）、validate 双 PASS、gates 零新增命中。
- **一处失实（建议级，F-346）**：`04982873` 正文"集成 gtest XML 两件…留痕"与实际提交物不符——`traceability/builds/wp13-t11/` 提交的是 testkit JSON 报告（ird-test-report/1 形态逐案明细）与逐 exe 日志，**gtest JUnit XML 原件未提交**（T09/T10 先例均提交 XML 原件；本次全量 diff grep `\.xml$` 零命中）。逐案数据本身在库、自洽且可由当前源码复现（本次亲跑 193/193 逐件一致），非数据缺失、非粉饰；已推送提交不可改写——本条即留档。

### 4.10 留痕完整 —— 通过（按在案口径；gtest XML 原件缺口并入 F-346）

- 在库 `traceability/builds/wp13-t11/` 16 件：集成/冒烟 `ird-test-report.json` 合并全量（193/193、144/144，summary 与逐案记录数自洽）＋分 exe JSON 报告（184/9/139/5）＋构建日志（零 error）＋ctest 日志＋gates 三件——与本次亲手复现结果逐件一致（复现优先于采信已满足）。
- `phase-one-validation.log`/resultNote 载体：按 F-2xx 在案口径（§4.10 条文与流水线实践脱节；任务级留痕实际落位 traceability/builds/）以 builds/wp13-t11/ 判定，T09/T10 同判例。
- gtest XML 原件未落库（T09/T10 先例均有）——建议级，并入 F-346（与 4.9 表述失实同条：同一载体缺口的两个侧面）。

### 4.11 偷懒/缩水实现扫描 —— 通过

1. diff 新增行 grep `TODO|FIXME|stub|placeholder|未实现|not implemented|以后完善` **零命中**；契约无"触发式延后"标注，无豁免需要列出。
2. 空实现检测：新增公共接口 `applyBasePlacementEdit`/`basePlacementEditErrorCodeToken` 函数体完整（七步检查序＋全枚举 switch token 表）；无"仅返回默认值"的空壳。
3. **逐条证据表**：见 §4.4——契约 acceptance 四条逐条具名用例＋命令输出，无一条无证据。
4. 测试强度：断言校验行为与边界（1×10⁻⁶/1×10⁻¹³ rad 容差两侧实测、NaN/Inf 逐分量、fail-fast 异常断言、拒绝路径全工作集字节相等、手算矩阵逐元素、单命令恰一根写入、逆载荷字节逐位等于前版）——非恒真、非"只编译通过"；与 4.5 两处变异联动证实测试会红。

## 2. 问题清单

**阻断级：无。**

**建议级（已转登 findings.json，编号顺延自 F-344）：**

| 编号 | 摘要 | 责任方 |
| --- | --- | --- |
| F-345 | T11 契约 verify 数组缺 `sdurws_ird_modeling_contract_test` 构建行（F-300/F-304/F-310/F-314/F-338/F-344 家族在 T11 契约复发）；interim 口径＝补建该目标后全量执行（本次 ctest 2/2＋契约 9/9 即该口径） | 契约编译/治理侧（CCP 冻结产物，走 DTB §5.4 契约修订补行） |
| F-346 | T11 留痕缺 gtest JUnit XML 原件（T09/T10 先例均有；提交物为 testkit JSON 报告＋日志），且 04982873 正文"集成 gtest XML 两件"表述与实际提交物不符——逐案数据在库、自洽、可复现，非数据缺失非粉饰；已推送提交不可改写，本条即留档 | 实施者侧（后续留痕窗口补录 XML 原件或经治理侧登记 JSON 报告为等价载体） |
| F-347 | ird_gates.cmake 的 IRD_SELFTEST_DIR 无防呆校验（可与 IRD_ROOT 重叠；自测夹具生成会清理/写入该目录——本次验收复现源树跟踪文件被清事故 1 例，还原后结论不变）；建议脚本启动时断言 SELFTEST_DIR 不在 IRD_ROOT 内 | 治理脚本所有者（WP-01-T01 ird_gates 辖域） |

## 3. 裁决

**verdict＝pass**：4.1~4.11 全部通过且无阻断级问题；建议级 F-345/F-346/F-347 已转登，不阻塞合入。送验对象为 SHA `049828737a5b02f49cec0315dafaf18fd80d33f1`（非分支引用）。合入决策归所有者（本验收者不合入）。
