# TK-T03 验收记录（对抗式验收）

| 字段 | 值 |
| --- | --- |
| 任务 ID | TK-T03（≙WP-02-T03，黄金数据集清单模型与完整性校验 Dataset） |
| 契约 | tasks/foundation/TK-T03.json（status: ready，随收尾翻转 done） |
| 验收对象 | 分支 `wp02-t03`，head＝`ea7f6cafbb3ddc879722aab28d4ead95d5866145`（与 origin 一致——漂移检测 PASS），范围自 redesign-main 分叉点起 |
| 尝试序号 | attempt=1（evidence 分支 `acc/TK-T03/1`） |
| **verdict** | **pass**（4.1~4.11 全过；无阻断、无新增建议级发现） |
| 验收日期 | 2026-09-11 |
| 独立性声明 | PIPE v1.7 §0.4 单会话降级；补偿＝worktree A（detached @ head）亲手复现（30/30）＋SHA 探针实证＋证据链前置自查（10 份证据文件 -f 入冻结对象——F-010 教训内化） |

---

## 逐项证据（4.1~4.11）

| # | 检查 | 结论与证据 |
| --- | --- | --- |
| 4.1 | 改动范围 | diff＝testkit {Dataset.hpp/Dataset.cpp/DatasetTest.cpp}＋CMakeLists＋tools/testdata/main.cpp＋testdata/requirements-ids.json＋units/testkit.md＋traceability——allowedFiles 内；forbiddenFiles 零触碰 |
| 4.2 | 可复现构建 | worktree A 全新配置 exit 0（ini 模板前置补齐）＋verify 目标构建 exit 0＋测试 30/30 PASSED（验收者亲手）；lint 工具随全量构建 exit 0 |
| 4.3 | verify 命令 | `cmake --build build --config Release --target sdurws_ird_testkit_test` exit 0 |
| 4.4 | acceptance 逐条 | ①TK-MAN 全字段装载（附录 A.2 形态 manifest——schemaVersion/datasetId/kind/scenarioCategory/coveredRequirements/toleranceProfile/inputs/expected/parameters/units/frames/referenceSource/producer/edgeCases/integrity/generator/history 全部校验后装载）✓；②反例矩阵五变体（schema 未知主版本/datasetId 句法/单位未注册/缺 edgeCases/integrity 未覆盖）消息均含字段路径 ✓；③TK-INT 篡改一字节→SHA 不符、追加字节→size 不符（先于 SHA 报告）✓；④lint：datasetId/目录一致（DatasetIdDirMismatch 用例）＋需求 ID 字典逐项校验（lint 端到端实测：KIN-99 拒绝 exit 1、KIN-01 通过 exit 0）＋字典种子 192 ID 交付 ✓ |
| 4.5 | 失败能力 | SHA 检查探针（实际哈希替换为登记值）→篡改用例如期变红，恢复后 30/30 复绿（报告记录；探针零残留） |
| 4.6 | 红线 | 零 Qt；T-2（core ContentDigester＋JsonLite 自有＋std）；SA-02 零框架改动；lint 工具不进产品安装面（§3.6） |
| 4.7 | 注释 | 公共头全查（四步流程/P-1 类似字段约束/落位偏差声明）；实现关键步（字段路径错误出口/完整性顺序/白名单解析）；lint 工具用法与退出码 |
| 4.8 | 文档同步 | testkit.md v0.4 落位登记（接口零偏差＋toleranceProfile 落位偏差＋lint/字典交付＋TK-MAN 变体目录对齐说明）；P-TK-7 遵守（字典随需求修订同步——不私改语义）；findings 核对：F-010 前置自查（10 份证据 -f 入库）；F-004/F-008 无涉 |
| 4.9 | commit 质量 | 两提交四段式＋Acc-Covered（1,2,3,4 / 4）；调试留痕如实（四轮反例矩阵迭代——datasetId 目录一致校验顺序对齐） |
| 4.10 | 留痕完整 | 10 份证据文件齐（日志 6＋XML×2＋report＋core/testkit 卡外另有 validate 记录）；ird-test-report 含探针与四条取舍 notes |
| 4.11 | 偷懒扫描 | 零命中；清单校验/完整性为实质实现；lint 端到端实测（非仅编译）；测试含反例矩阵（五变体＋篡改/size） |

## 问题清单

阻断级：无。建议级：无新增。
