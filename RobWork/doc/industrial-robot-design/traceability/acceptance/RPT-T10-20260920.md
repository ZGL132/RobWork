# RPT-T10 验收记录（acceptance record）

| 字段 | 值 |
| --- | --- |
| 任务 ID | RPT-T10（实现证据包组装——Bundle＋IArchiveWriter 注入契约与 FakeArchiveWriter；unit=reporting；masterWp=WP-H；interUnit=true；卡内行≙WP-12-T05；依赖 RPT-T05；实现任务——全量清单验收） |
| 送验对象（冻结） | 4110786ab67144385fe487f5be103f19c9f9eeae（分支 wp12-t10；diff 范围＝87aceec6ba649b4b344214d50b6d105444f11113..4110786ab67144385fe487f5be103f19c9f9eeae＝state.base..state.headSha；range 内含 55e34f9d（实现）／0c95de0e（验证留痕入库）／4110786a（留痕 .log -f 补交）三个提交） |
| 验收尝试 | attempt 1（分支漂移检测：fetch 后 `git rev-parse` 实测 `origin/wp12-t10` == 4110786ab67144385fe487f5be103f19c9f9eeae 全 40 位逐字一致，零漂移；`acc/RPT-T10/1` 分支名此前不存在；state.attempts.accept==1 与请求一致——state.json 亲读实证） |
| 验收环境 | 复现 worktree A＝`git worktree add --detach D:/10_Source_Repos/21_robot/rpt-t10-acc-a` @ 4110786a（集成 build 树为本验收全新配置）；冒烟 build 树＝独立目录 `rpt-t10-acc-a/build-smoke`（本验收新建）；记录回传 worktree B＝`git worktree add -b acc/RPT-T10/1 D:/10_Source_Repos/21_robot/rpt-t10-acc-b` origin/redesign-main@87aceec6；MSVC 2022 x64＋vcpkg 经典模式（toolchain 绝对路径指向主仓库 vcpkg——worktree 不含 vcpkg，只读第三方）＋Qt 6.11.1（D:/software/Qt）。集成配置＝`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=<主仓>vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`，配置 exit 0、`CMake Error` 计数 0、缓存 grep `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON` 实证；配置前自主树复制两件未追踪 ini 辅助件（RobWorkStudio.ini.template.static/.shared.in——F-007/F-200 既有登记环境链的预防性处置，非送验对象缺陷）。验收全程只消费产物、契约、设计文档与在库留痕，未读取实施者过程叙述；结论全部出自验收者亲手重跑 |
| verdict | **pass**（4.1～4.11 全部通过；无阻断级问题；建议级 4 条已转登 findings.json F-246/F-247/F-248/F-249） |
| 验收日期 | 2026-09-20 |

## 4.1 改动范围 —— 通过

- `git diff --stat 87aceec..4110786a` 实证 **25 个变更文件**（6 件 reporting 代码/头/测试/CMake＋18 件 traceability/builds/wp12-t10/ 留痕＋1 件 units/reporting.md；5529 insertions/4 deletions，全部新增或增列，无删除、无重命名）。
- **allowedFiles 逐文件比对（越界零命中）**：`git diff --name-only 87aceec..4110786a | grep -v -E '^RobWork/RobWorkStudio/src/rwslibs/industrialrobot/reporting/|^RobWork/doc/industrial-robot-design/units/reporting\.md$|^RobWork/doc/industrial-robot-design/traceability/'` **零输出**——6 件命中 `reporting/**`、18 件命中 `traceability/**`、1 件命中 `units/reporting.md`，25/25 全部在册。
- forbiddenFiles（REQUIREMENTS.md／ARCHITECTURE.md）**零触碰**（diff 无该两文件）。
- 无未登记框架源码改动（SA-02）：diff 零框架路径（`RobWork/RobWork/src/`、`RobWorkStudio/src/` 非 rwslibs 面）；`patches/` 目录变化 **0 件**。
- 涉及文件与契约产物列（『Bundle.hpp/.cpp（＋IArchiveWriter 契约与 Fake）』）一致：Bundle.hpp/.cpp＋test/FakeArchiveWriter.hpp 均为 `reporting/**` 内新增；CMakeLists.txt 仅增列源文件与两测试文件（M，21 行变化全为注释块＋3 处增列）。

## 4.2 可复现构建 —— 通过（集成全新树重配＋独立冒烟树，双模式零错误）

- **集成模式**（worktree A 全新 `build/`，配置 exit 0 后缓存 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON` grep 实证）：契约 verify 两目标 `cmake --build build --config Release --target sdurws_ird_reporting_test`／`…_contract_test` **均 exit 0，`error C[0-9]/error LNK/fatal error` 计数 0**（含 RobWork→RobWorkStudio→industrialrobot 依赖链从零编译；产物 `build/RobWorkStudio/bin/Release/` 两测试 exe 就位）。
- **独立冒烟模式**（独立目录 build-smoke，`-DCMAKE_TOOLCHAIN_FILE=<主仓>vcpkg`＋Qt prefix 显式参数——AGENTS §4.1 必带口径）：配置 **exit 0**＋全量构建（`cmake --build build-smoke --config Release`，覆盖 industrialrobot 全部目标）**exit 0、错误计数 0**；`build-smoke/reporting/Release/` 两测试 exe 就位并亲跑全绿（见 4.3）。
- 说明：本验收未重跑"集成全树"口径（契约 verify 字面即两目标级；送验 integration-build.log 的声称口径亦已限定"verify 两目标零错误"，无 RPT-T09 F-245 式全树表述越界）；全新树全树口径当前受 F-008/F-235 框架基线缺陷阻断属既有登记事实，与送验对象无关。

## 4.3 契约 verify 命令 —— 通过（两条逐条亲手执行，测试真实运行）

worktree A 按契约字面逐条独立执行，**两条全部 exit 0**：

1. `cmake --build build --config Release --target sdurws_ird_reporting_test` → exit 0（acc-int-build1.log）
2. `cmake --build build --config Release --target sdurws_ird_reporting_contract_test` → exit 0（acc-int-build2.log）

**测试真实运行（非只构建，双模式四跑全绿）**：

- 集成模式亲跑两 exe → `[==========] 164 tests from 30 test suites ran`＋`[PASSED] 164 tests`；`58 tests from 21 test suites`＋`[PASSED] 58 tests`——零失败；
- 冒烟模式亲跑两 exe → 同为 **164＋58 全绿**。

**与送验留痕逐字一致**：在库四份 gtest XML 顶格统计（单元 164/0 failures、契约 58/0 failures × integration/smoke，`<failure` 标记计数全 0）与验收者亲跑一致；ird-test-report.json 声称的 164/164＋58/58 双模式全绿经亲跑证实（164＝RPT-T09 基线 150＋新增 14、58＝51＋新增 7，与 `grep -c ^TEST`/用例清点一致）。

**附加治理脚本（worktree A 上下文亲手复跑）**：`validate-task.ps1 -TaskFile …RPT-T10.json` → **PASS (1 tasks)**；`validate-docs.ps1` → **PASS (20 units, 12 trace entries, 150 task files)**；`verify-task.ps1 -TaskFile …RPT-T10.json -DryRun` → **DRYRUN PASS**（dryrun 回显的两条命令与契约 verify 字面逐字一致——交叉印证）；`validate-state.ps1` → **PASS (phase=idle, queue=16, history=126, tasks indexed=150)**。均与在库 validate-task.log/validate-docs.log 结论一致。

**ird_gates 独立复跑＋三方归一化比对**：验收者以 `cmake -DIRD_ROOT=<worktree A>/…/industrialrobot -DIRD_ENABLE_GIT=OFF -P cmake/ird_gates.cmake` 直跑 head 树——**原始 IRD-GATE- 行 61、规范摘要"命中 27 项"、exit 1**（与送验双日志同构）；命中集路径归一化后与送验 branch 日志 diff 为空、送验 branch 与 base 日志 diff 亦为空（**三方 IDENTICAL 实证——GATES-HIT-SET-IDENTICAL 独立成立**）；27 项命中全部为既有登记归属族（testkit 直链 SUB 族/io libzip·expat LIB 词表族/ui Qt R3 例外族/project R4 字面量/T1 testdata_lint·ui_gui_test——逐条清点），**reporting 目标相关命中 0**（grep 实证）——RPT-T10 diff 零新增门禁命中。gate-all.ps1 冒烟步缺 Qt 前缀为既有环境项（gates-all-note.txt 已如实登记，wp12-t07~t09 先例口径）。

## 4.4 acceptance 条目 —— 通过（五条逐项证据）

| # | 契约条目 | 证据（验收者亲读源码＋亲手复跑） |
| --- | --- | --- |
| 1 | RP-BUN-1 组装清单完整（bundle.json 六字段＋snapshot/results/reproduction 条目＋复现要素） | 实现＝Bundle.cpp:456-555（bundle.json 六字段逐字：schemaVersion "ird-evidence-bundle/1"/reportId/contentIdentity/sourceRuns/generatedAtUtc/totalDigest——kBundleSchemaVersion Bundle.hpp:189）；snapshot/<run>/snapshot.json refs-only 四列（snapshotId/sliceId/inputBaselineId/caseSetDigest——Bundle.cpp:459-472）；results/<run>/result.json＝envelopeDigest＋证据清单（itemId/status/digest——Satisfied 携摘要 presence 纪律，Bundle.cpp:203-224）＋复现要素 ReproductionBlock 逐字段（Bundle.cpp:177-198）；reproduction.json 逐运行 versions＋configurations（Bundle.cpp:487-512）；totalDigest＝IRDRPTB1 大端编码域＋core ContentDigester 唯一算法、排除 bundle.json 自身（D-04——Bundle.cpp:519-535）。测试＝`BundleTest.AssemblesCompleteBundleWithAllEntries`（条目枚举恰 4＋bundle.json 六字段逐项＋三类条目内容逐列＋目标文件存在——亲跑通过）＋`TotalDigestIsDeterministicAcrossAssembles`（generatedAtUtc 变化不入摘要——亲跑通过）。**"不自算"纪律**（NFR-COR-04 数据源 §7.6）：版本列取 report.reproduction()（evidence ReproductionBlock 值拷贝——contract static_assert BundleCrossUnitContractTest.cpp:88-90 类型恒等）；种子/线程数/容差经 BundleConfigRef 引用承载（configKindToken＋contentIdentity——域 schema 归域不解释，Bundle.hpp:43-55 文件头专节＋Bundle.cpp:489-508 实现一致） |
| 2 | 要素缺失拒绝＋缺省 runs | 实现＝收集阶段零 IO 先全量收集（Bundle.cpp:339-433）：五项事实逐项核对、缺失"<要素>@<runId>"全量列出不短路（:418-433——BundleIncomplete，writer 从未 open＝不产出半包零临时）；复现块 productVersion/evidenceContractVersion 必填字段逐字段完整性核对（:363-373）；runs 空＝缺省取报告 resultRefs（:297-303）、越界/重复＝Usage fail-fast（:304-329）。测试＝`MissingEnvelopeRejectsWithBundleIncompleteBeforeAnyWrite`（逐项定位＋零会话零目标——亲跑通过）＋`MissingElementsAreListedExhaustively`（五项全缺 detail 五项逐在列——亲跑通过）＋`IncompleteReproductionBlockIsRejectedFieldByField`（必填字段置空逐字段列出、完整字段不在清单——亲跑通过）＋`EmptyRunsDefaultsToReportResultRefs`（缺省与显式全集同 totalDigest——亲跑通过）＋`EmptyConfigurationListIsLegalButNulloptIsMissing`（空集合法 vs nullopt 缺位分界——亲跑通过） |
| 3 | 取消/失败清理（IArchiveWriter 异常/失败→清理临时、目标不变；协作取消→检查点逐条目；FakeArchiveWriter 随任务交付） | 实现＝失败三轨透传 Failed（open/addEntry/finish 各自 try-catch——Bundle.cpp:566-631，错误原码保留不吞错）；取消三检查点（入轨 :334-337／收集逐运行 :397-402／写出逐条目 :581-586＋finish 前 :608-612）→Canceled 全空形态（UX-03）；放弃路径＝writer 析构 RAII 清理（Bundle.hpp:57-63"放弃路径"节——§9.6 三方法签名零改动的契约澄清，单元卡 §14.4 v0.13 登记）。测试＝`CancelBeforeCollectionReturnsCanceledWithNoSession`＋`CancelDuringCollectionReturnsCanceledBeforeOpen`＋`CancelDuringWriteCleansTempAndLeavesTargetUntouched`（临时文件存在→writer 属主析构后已清理＋目标不变——独立存续期构造亲跑通过）＋`WriterFailureAtAddEntryFailsWithTargetUntouched`＋`WriterFailureAtFinishCleansTempAndKeepsTarget`（临时即时清理＋目标不变——亲跑通过）＋`RetryAfterWriterFailureSucceeds`（故障清除同请求重装成功——亲跑通过）。**FakeArchiveWriter 随任务交付**：test/FakeArchiveWriter.hpp（真实临时文件承载生命周期＋open/addEntry/finish 三类故障注入＋RAII 放弃路径＋替身边界声明"不构成 ZIP 容器格式正确性证明"在案——具名替身收口归 RPT-T11 与卡行 T11 产物列原文一致） |
| 4 | 格式与通道边界（ird-evidence-bundle/1 独立格式；不复用 .rwpack；数据契约归 evidence/打包组装归 reporting/ZIP 归 io 注入；条目名纯相对正斜杠 SP-2） | 实现＝kBundleSchemaVersion "ird-evidence-bundle/1" 独立字面（Bundle.hpp:189）；条目名全部由 runId 规范文本构造（字符集受限天然满足 SP-2——Bundle.cpp:54-65）；ZIP 字节封装只经注入 IArchiveWriter（reporting 定义、io 薄适配——Bundle.hpp:107-181；零 zip 实现特征）。测试＝`BundleSchemaVersionIsIndependentFormat`（≠ird-report-json/1——亲跑通过）＋`EntryNameViolatesSp2IsRejected`（绝对/盘符/反斜杠/上溯/空名五形态拒绝＋拒绝零副作用＋重复名拒绝——亲跑通过）＋`EntryAddressingIsRelativeAndProjectMirrorFree`（条目名相对正斜杠＋零 HEAD/lock/.staging 项目镜像要素——亲跑通过）＋`NoSecondZipImplementationInReportingSources`（reporting 源码树 zlib/minizip/libzip 等特征扫描零命中——IRD_REPORTING_UNIT_ROOT 注入实证在编译定义 CMakeLists.txt:262/315，测试在 58 条亲跑清单内）＋§9.6 三签名 static_assert（BundleCrossUnitContractTest.cpp:70-84——签名漂移即编译失败）。数据契约归 evidence：Bundle.hpp/Bundle.cpp 只消费 evidence 公共类型（Envelope.hpp/Snapshot.hpp）零重定义（P-RPT-9——static_assert 钉死） |
| 5 | P-RPT-7/P-RPT-9 处置 | **P-RPT-7**：IArchiveWriter 为 reporting 定义的注入契约（Bundle.hpp:103-181 契约注释逐条——open 同卷临时区/addEntry 只增 SP-2/finish 原子替换；"io 未暴露通用 ZIP 写出公共接口前测试经 FakeArchiveWriter 注入、不私建第二套 ZIP 实现"原文落位）＋源码扫描测试常驻自证（acceptance 4 行）＋diff 零 io/ 文件触碰（4.1 实证——薄适配落 io 归后续，未越权实现）。**P-RPT-9**：evidence ReproductionBlock/ResultEnvelope/EvidenceItemStatus 消费以 v0.1 Draft 为基线——类型恒等 static_assert 三处（reproduction() 返回类型 :88-90、tryEnvelope 签名 :94-97、EvidenceItemStatus 五值词面全表 :104-108）＋词面稳定互异测试（ItemStatusTokensAreStableAndDistinct——kebab 词面逐值钉字）亲跑通过；evidence 侧追加枚举值时全枚举 switch 无 default 编译期暴露遗漏（Bundle.cpp:67-79） |

## 4.5 测试真实失败能力 —— 通过（两处实现变异即红、还原复绿）

在 worktree A（本验收沙箱，任务分支零触碰）注入变异，增量重建后亲跑单元测试 exe：

1. **变异 1（Bundle.cpp:370）**：复现块完整性核对 `if (reproduction->evidenceContractVersion.empty())` → `if (false)`（完整性核对被短路）→ `BundleTest.IncompleteReproductionBlockIsRejectedFieldByField` **变红**（detail 期望含 "reproduction.evidenceContractVersion@…" 实不含）——acceptance 2"复现块不完整逐字段列出"红能力成立。**163 PASSED / 1 FAILED**。
2. **变异 2（Bundle.cpp 写出循环＋finish 前两处取消检查点）**：`cancel->isCancelled()` 判定 → `if (false)`（两处一并移除）→ `BundleTest.CancelDuringWriteCleansTempAndLeavesTargetUntouched` **变红**（取消被无视→finish 照常发布→目标存在断言失败）——acceptance 3"协作取消→清理临时、目标不变"红能力成立。**162 PASSED / 2 FAILED**。附注：仅移除写出循环一处检查点时测试保持绿——finish 前检查点兜住同一取消保证（检查点链纵深防御的行为结果），验收者随即加强变异（两处齐移）实证红能力，非测试恒真。
3. `git checkout` 还原＋重建 exit 0 后 **164/164 复绿**（首次复跑被变异运行残留的临时目标文件 ird-bundle-test-cancel-write.zip 干扰误红一次——即 G-1/F-246 登记的测试预清理卫生项；清残后复绿）。`git status --porcelain`（industrialrobot 面）空（沙箱洁净）。

## 4.6 架构红线 —— 通过（AGENTS §5 八条逐条）

1. **L2 计算内核零 Qt**：五件新文件＋两测试文件 include 面亲读＋`grep 'include <Qt|include <Q|QWidget|QString'` **零命中**——include 面＝std＋gtest（测试）＋`sdurws/ird/{core,evidence,reporting}/…` 公共头；ird_gates R3 检查 reporting 零命中（4.3 三方比对）。
2. **业务域互链禁止（R-1）**：`target_link_libraries(sdurws_ird_reporting …)` 零改动（CMakeLists diff 仅增列源文件——四条登记边 core/evidence/diagnostics/project 保持）；ird_gates SUB 检查 reporting 零命中双证；跨单元协作全走注入契约（IReportResolver/IReportResultSource/IEvidenceBundleSource/IArchiveWriter/IReportIoFactory——§3.3 注入模式）。
3. **跨单元私有头禁止（R-2）**：新公共头位于 `include/sdurws/ird/reporting/`；include 面仅他单元公共头（core/DiagData、core/Digest、core/Identity、evidence/Envelope、evidence/Snapshot——公共路径实证）；`"../test/FakeArchiveWriter.hpp"` 为**单元内**测试替身相对包含（不出 include/、不跨单元——T07/T09 同先例）。
4. **框架零源码修改（SA-02）**：diff 零框架路径、patches/ 零新文件（4.1）。
5. **不做 RobWork 名称前缀拼接/剥离（R-4）**：新代码零名称解析面；ird_gates R4 命中仅 project 单元既有登记 1 处（三方命中集等同——4.3），本任务零新增。
6. **权威唯一（PA-1）**：打包格式与组装归 reporting（§7.6 要素边界——本任务正是该权威的落地）；复现要素数值不自算（evidence 复现块值拷贝＋配置 canonical 引用——4.4 条 1 实证）；ZIP 字节封装归 io（注入契约、io 未就位前 Fake 注入——不越权私建）；JSON 序列化经注入 io 工厂（SA-12 不自建序列化器——canonicalBytes 闭包 Bundle.cpp:443-452）；命令入口/单位/诊断文案零触碰。
7. **不可变历史（PA-2）**：证据包为外部交付物（外部目标原子替换——finish 协议）；项目内历史零触碰（零项目写路径——makeAtomicTarget 不可达判红 UnreachableIoFactory 常驻自证）；totalDigest 排除生成时刻（D-04——同输入同摘要，历史可重建验证）。
8. **不继承/不恢复 old/**：diff 无 old/ 引用。

## 4.7 注释规范 —— 通过（公共头全查＋实现/测试文件抽查超配）

- **公共头 Bundle.hpp（全查，本任务唯一新公共头）**：文件头（L1-78）设计依据逐条锚定 §7.6/§9.6/§9 章首/§3.3/§13＋需求 RPT-03/NFR-COR-04/NFR-COR-02/PM-05/UX-03＋任务契约 acceptance 1~5；专节四篇（"谁拥有什么"要素边界、"不自算"纪律、放弃路径 RAII 澄清〔DTB §5.4 登记〕、线程契约）；每个 struct/class/enum/函数/常量 Doxygen 中文齐备（IArchiveWriter 三方法逐条前置后置与异常轨、EvidenceBundleRequest 字段语义含缺省与越界、BundleStatus 三形态约定、BundleConfigRef 与 evidence 投影关系、IEvidenceBundleSource 三方法 nullopt 语义、装配器六步编排明细含 totalDigest 规范域逐字节）；§2.5 高危信息：所有权（五注入引用借用语义＋可写引用显式标注 :499-502）、线程（会话单线程/并发只读/令牌可见性 :65-73）、错误语义（契约违约 fail-fast vs 环境失败 ReportError 两轨 :133-136）、确定性（canonical 域/排序键/排除 generatedAtUtc :75-77）——必填项零缺失（单位/坐标系/rad：本任务纯字节/文本/身份面，无几何量——N/A 合理）。
- **实现文件抽查（≥3 件，实查 4 件）**：①Bundle.cpp 文件头（L1-31 实现口径五项 DTB §5.4 登记＋线程/确定性声明）＋全函数段落注释（每步"第 N 步"编号、每分支业务场景、魔法数字来源——"IRDRPTB1"族格式魔数与 ReportCodec 同族的来历、20 字符 ISO-8601 与 32 缓冲、缺失清单"<要素>@<runId>"形态、检查点必须落在 finish 之前的理由）；②test/FakeArchiveWriter.hpp 文件头替身边界声明（§2.3 契约文档级）＋三类注入逐项注释＋线程约束（非线程安全替身）；③CMakeLists.txt 增列段中文注释块（RPT-T10 增列说明——设计依据与陷阱处置逐条，12 行）；④contract_test/BundleCrossUnitContractTest.cpp 文件头契约声明五条＋逐 static_assert 中文说明。
- **测试文件（全查）**：BundleTest.cpp 用例注释逐条标注验证的 acceptance 条目/RP-BUN-1 观测点（AGENTS §2.7 追溯字段）＋替身边界声明＋确定性夹具说明（禁随机——同输入同摘要前提）。

## 4.8 文档同步 —— 通过（偏差已按 DTB §5.4 登记；findings 未关闭条目已核对）

- **单元卡 v0.12→v0.13**（55e34f9d 内同步提交）：文档头版本行＋§14.4 v0.13 落位登记行（验收者逐行亲读）；**四项实现口径登记与实现对照核实**（①条目编址＝snapshot/<runId>/snapshot.json 等 ZIP 条目化——与 Bundle.cpp:54-65/459-555 一致；②reproduction.json 形状＝逐运行 versions＋configurations 不聚合不改写——与 :487-512 一致；③EvidenceItemStatus 五值包内词面映射——与 :67-79 一致；④放弃路径＝析构 RAII 承载、§9.6 签名零改动——与 Bundle.hpp:57-63 一致）；另登记 ④withEvidenceBundle 导出编排接线不在本任务面（§11 涉及文件列"同名文件"口径内——Export.hpp 偏差④的显式 Usage 边界维持，接线随集成任务收口）——**零未登记偏差**（验收者以 acceptance 五条逐条反查实现，无超出登记的偏差）。
- **任务状态/追踪矩阵**：validate-docs PASS（亲跑同值）；契约 status 翻转归收尾段（PIPE §4.7，与既有任务同口径；F-201 已登记该条文与实践脱节）。
- **findings.json 未关闭条目相关性核对（v1.2）**：origin/redesign-main 版本现册 **245 条（open 213）**，逐族核对与本任务的关系——
  - **F-012 家族**（辅助提交标题名词短语开头）：**本任务再现 2 例**（0c95de0e/4110786a——见 4.9/G-2）——已转登 **F-247**（编号自 F-245 顺延）；
  - **F-041/F-073/F-235/F-236/F-238/F-243/F-025 计数失准族**：本任务再现 3 处（c2"17 个文件" vs 实入库 7 件；c3"十份" vs 实 11 件；ird-test-report/c1"各 61 处 IRD-GATE- 命中"——独立命中实为 27 项，gates-all-note.txt 自身口径准确）——**G-3 转登 F-249**；
  - **F-010/F-223/F-244 幻影清单族**（.gitignore `*.log` 吞留痕）：**根因再次踩中**——0c95de0e 目录式 add 时 11 件 .log 被吞（实入库 7 件——`git show --name-status` 逐件实证），4110786a 以 `git add -f` 补交并如实说明（不 amend 已推送历史合规）；本验收 18/18 件全部在库（零证据缺失），自纠型再现——**G-4 转登 F-248**；
  - **F-007/F-200 环境链**：验收复现再次预防性复制 ini 双辅助件——既有登记再证实，非交付面问题，零新增登记；
  - **F-008/F-235/F-036/F-038/F-245 全树口径族**：本任务送验报告**未做**集成全树口径声称（verify 目标口径、冒烟"全树"实为 industrialrobot 冒烟树全目标——口径成立）——零复踩、零新增；
  - **F-201**（ACC §4.10 resultNote 条文与流水线实践脱节）：本任务同样以收尾段翻转登记——既有登记继续有效；
  - **F-019**（门禁基线红）：亲历复现（27 项既有命中，reporting 零命中）——既有登记继续有效；
  - 其余 open 条目（core/testkit/evidence/project/io/execution/policy/runtime/diagnostics/ui 各单元家族）与本任务交付面（reporting 证据包组装层）无交叉；**无可随本任务顺手消账项**。

## 4.9 commit 质量 —— 通过（四段式齐备、验证结论经独立复跑证实；标题风格 2 例建议级）

- **55e34f9d** `[RPT-T10] 实现证据包组装（Bundle＋IArchiveWriter 注入契约）`——**动词开头合规**；四段齐备（What 按文件/函数分点、Why 逐条对应 acceptance 1~5＋P-RPT-7/P-RPT-9 处置＋要素边界、How 构建与测试与门禁与 validate 真实结论、影响面含 withEvidenceBundle 边界指引）；`Acc-Covered: 1,2,3,4,5`。验证声称逐项经本验收独立复跑证实（双模式构建零错误、164＋58 全绿、门禁命中集归一化 diff 为空、validate 双 PASS）——"各 61 处既有命中"措辞＝计数口径瑕疵（见 G-3/F-249），命中集实质结论"零新增"独立成立。
- **0c95de0e** `[RPT-T10] 验证留痕入库：门禁/构建/测试运行/治理自查证据（wp12-t10）`——四段齐备；标题前缀后以名词短语开头（F-012 家族——G-2/F-247）；正文声称"17 个文件"与该提交实入库 7 件不符（11 件 .log 被 .gitignore:87 吞——正文如实列出了目录应含内容，次提交即补交自曝，非掩饰；方向为多报，见 G-3/F-249）。
- **4110786a** `[RPT-T10] 验证留痕补交：构建/测试运行 .log 十份 -f 入库（wp12-t10）`——四段齐备（Why 含 .gitignore 根因与 wp12-t07~t09 先例引用——诚实自曝被吞事实）；标题同族瑕疵（同 G-2）；"十份"与实入库 11 件差 1（少报——同 G-3/F-249 计数族）。
- **粉饰排查**：提交正文声称的双模式测试计数（164＋58——XML 顶格＋亲跑双证）、门禁归一化 diff 为空（验收者三方比对独立成立）、validate 双 PASS（亲跑同值）、留痕均为真实执行原始输出（四份 XML/运行日志/构建日志与亲跑结论全一致）——**逐项相符，无虚标通过、无粉饰**；计数偏差与被吞补交均由提交链自身如实呈现。

## 4.10 留痕完整 —— 通过

- gtest XML×4＋ird-test-report.json（schema `ird-test-report/1`，targets/tests/gates 三段、acceptance 逐用例对应说明）＋构建日志三件（integration-build.log/smoke-configure.log/smoke-build.log）＋测试运行日志四件＋门禁双树日志＋gates-hit-set-diff.txt＋gates-all-note.txt＋validate 双日志——**18 件全部落位在库**（4.1/4.9 实证）。
- 在库四份 XML 顶格 tests/failures 属性与验收者双模式亲跑结论一致（164/58 × 2，零失败）；ird-test-report.json 的 notes 段（新增 14＋7 用例逐组列举）与源码 `TEST(` 清点一致（14＋7）。
- gates-all-note.txt 如实登记 gate-all.ps1 冒烟步环境失败（脚本内部未传 Qt 前缀）与 DoD 门禁承担口径（ird_gates 双树直跑＋双树原始日志＋归一化 diff 在库）——无掩饰；双树直跑命令与"原始行 61/归一化 diff 为空"口径与验收者亲历完全一致。
- resultNote/status 更新随收尾段（PIPE §4.7）；`phase-one-validation.log` 未随本任务更新（该文件停于 2026-09-10 CORE-T01 时代）——F-201 已登记的条文与实践脱节，此处如实注明，不构成新的粉饰。

## 4.11 偷懒/缩水实现扫描 —— 通过

- **①TODO/FIXME/stub/placeholder/未实现/not implemented**：diff 范围 reporting 全部代码/测试文件 diff 全文扫描（grep 六词族）**零命中**（无豁免条款需列出）。
- **②空实现检测**：`EvidenceBundleAssembler::assemble`（Bundle.cpp:256-638——六步编排全量逻辑：请求校验五路/取消三检查点/收集零 IO 五项事实/缺失全量拒绝/条目集内存定版＋canonical 序列化/totalDigest 大端编码域/写出会话三轨失败透传）为完整实现；FakeArchiveWriter 为契约明文允许的注入替身且边界声明在案（真实临时文件承载生命周期——非空壳）；bundleItemStatusToken 的 `return "unknown"` 为全枚举 switch 后防御位（不可达——与 token() 家族同款，非桩）。
- **③逐条证据表**：见 4.4——acceptance 五条每条均有具名测试＋亲跑结论＋实现行号，任一条无证据即 fail 的反例不存在。
- **④测试强度**：4.5 两处实现变异实测红（1 例即时红＋1 例加强变异后红——附带发现检查点链纵深防御的行为冗余，非测试恒真）＋还原复绿；断言校验行为与边界（六字段逐项值核对/条目枚举恰 4 与次序/缺失清单逐项字符串定位/零会话零临时零目标三重断言/临时文件存在→析构消失的时序观测/Canceled 全空形态/error 码逐枚举/重复名与 SP-2 五形态拒绝/拒绝路径零副作用——"非法调用后合法条目仍可写"）——非恒真、非"只编译通过"。

## 发现的问题清单

| 级别 | 编号 | 内容 | 处置 |
| --- | --- | --- | --- |
| 建议 | G-1 | BundleTest 固定临时路径用例无预清理：`CancelDuringWriteCleansTempAndLeavesTargetUntouched`（及同族用例）使用共享 temp 目录固定名目标路径，但用例入口不防御性清理目标——验收 4.5 变异红运行遗留 `ird-bundle-test-cancel-write.zip` 后，实现还原正确、测试仍误红一次（BundleTest.cpp:904/910 目标不存在断言被残留文件触发）；干净环境下四跑全绿无此问题。建议：用例入口 `std::filesystem::remove(request.target)` 预清理，或夹具为每用例分配独占子目录 | **已转登 findings.json F-246**（建议 RPT-T11 替身与契约套件收口时一并处置；不阻塞合入） |
| 建议 | G-2 | 两辅助提交 0c95de0e／4110786a 标题前缀后以名词短语开头（"验证留痕入库：…"/"验证留痕补交：…"），不合 ACC §4.9/AGENTS §6.3"动词开头"字面——F-012/F-232~F-242 家族再现（家族新增第 17/18 例，RPT 系连续九任务复踩）。两正文四段齐备、验证结论经验收者独立复跑全部证实（无粉饰），纯风格瑕疵不构成阻断；主提交 55e34f9d 标题合规 | **已转登 findings.json F-247**（与家族合并处置；不追溯改写已推送历史；不阻塞合入） |
| 建议 | G-3 | 计数口径精度三处（F-041/F-025/F-243 散文计数家族再现，方向混杂、无证据缺失）：①0c95de0e 正文"留痕目录（17 个文件）"与该提交实入库 7 件不符（被吞 .log 未计入——且 7＋11＝18，"17"与最终在库数亦不合）；②4110786a 正文"十份 .log"实入库 11 件（少报 1）；③ird-test-report.json gates 段与 55e34f9d 正文"各 61 处 IRD-GATE- 命中"——61 为原始日志行数（含流式＋摘要两遍重复与自测提及），**独立命中实为 27 项**（日志自身"命中 27 项"与 gates-all-note.txt"原始 IRD-GATE- 行各 61 处"口径准确在案）——与 F-243 对 wp12-t09 的同字段裁定同型再现 | **已转登 findings.json F-249**（纯措辞/计数精度问题；不阻塞合入） |
| 建议 | G-4 | F-010/F-223/F-244 根因（.gitignore 第 87 行全局 `*.log` 与留痕入库要求结构性冲突）再次踩中：0c95de0e 目录式 add 时 11 件 .log 被忽略规则吞掉（实入库 7 件），4110786a 以 `git add -f` 补交——**自纠型再现第 2 次连续**（验收前已闭环、18/18 件在库零缺失、提交正文如实说明），未构成验收 fail；系统性处置（.gitignore 负例规则 `!traceability/**/*.log` 或留痕 .txt 后缀惯例成文）仍未落地 | **已转登 findings.json F-248**（并入 F-223 系统性处置；不阻塞合入） |
| 环境注记 | —— | 复现期间 ini 双辅助件缺失（bin/ 被 .gitignore 吞）为 F-007/F-200 家族既有登记环境链的再证实（本验收预防性复制后一次配置成功）；非本任务交付面问题，零新增登记 | 已在 4.8 逐族核对留痕 |

## 验收者环境说明

- 复现与记录均在临时 worktree（rpt-t10-acc-a／rpt-t10-acc-b，冒烟树 rpt-t10-acc-a/build-smoke）完成；验收者对任务分支 wp12-t10 **零提交、零推送、零文件修改**（4.5 变异在沙箱 worktree A 内注入并已还原，industrialrobot 面 `git status --porcelain` 复核洁净）。
- 构建树：集成 `rpt-t10-acc-a/build`（全新配置，`RWS_BUILD_INDUSTRIALROBOT:BOOL=ON` 缓存确认）；冒烟 `rpt-t10-acc-a/build-smoke`（独立目录＋vcpkg toolchain＋Qt prefix）。测试执行：两模式各 2 目标亲跑（本轮共 4 次全绿运行＋2 次变异红运行＋2 次复绿运行）；ird_gates head 树直跑＋与送验 base/branch 两日志归一化三方比对（基线树未另行建 worktree——送验 base 日志与 branch 日志经本次三方比对互相钉住，且 gates-all-note.txt 载明 base 日志产自送验时已清理的 detached worktree）。
- 记录落位：本文件随 F-246/F-247/F-248/F-249 转登一并提交于 `acc/RPT-T10/1` 分支并推送 `origin`（ACC §3 记录回传）；三元组 {branch: acc/RPT-T10/1, path: RobWork/doc/industrial-robot-design/traceability/acceptance/RPT-T10-20260920.md, commit: <本次 evidence 提交 SHA>} 回传编排者。
