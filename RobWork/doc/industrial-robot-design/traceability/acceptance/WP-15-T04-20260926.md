# WP-15-T04 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | WP-15-T04 实现多初值 IK 与解集去重排序（kin.task-point-ik） |
| 送验对象（冻结） | fb67389f65ee04413efa051949729e2ec8022e6d（按 SHA 验收，不按分支名） |
| diff 范围 | f56256f6d831bffd3b4026739f25608aba41bf65..fb67389f65ee04413efa051949729e2ec8022e6d（state.base..headSha；两实施提交 a7580ddd「求解」＋fb67389f「过滤去重排序」） |
| **verdict** | **pass**（4.1～4.11 全部通过，零阻断级问题；建议级 4 条转登 findings.json：F-389～F-392） |
| 验收者环境 | 全新上下文；主检出只读（redesign-main @f56256f6，并发会话占用中——未切换分支、未做任何 git 写操作、未改任何文件）；复现现场＝detached worktree @fb67389f（自建临时目录），集成模式独立 build 树从零配置，冒烟模式另配独立 build 树；自查与测试执行全部亲手完成，未采信实施者运行结果。分支漂移检查（ACC §3）：`git ls-remote origin refs/heads/wp15-t04`＝fb67389f65ee04413efa051949729e2ec8022e6d——与冻结 SHA 一致，零漂移。gitignored 模板缺失自主检出只读复制后 configure（RobWorkStudio.ini.template.static＋RobWorkStudio.ini.shared.in——T02~T09 验收实录现象，F-007 在案） |
| 环境参数 | 集成/冒烟均带 `-DCMAKE_TOOLCHAIN_FILE=vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`（主树 build/CMakeCache.txt 实证配置口径——F-062/F-098 在案）；无 toolchain 裸命令首配失败于 FindBoost（F-007 家族既有现象）——删树带全参数重配成功；集成缓存确认 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`。残留进程检查：构建前 tasklist 无 RobWorkStudio.exe/测试 exe/msbuild/cmake 残留 |
| 证据附件 | 验收者复跑产物原样留存于本分支 `traceability/builds/wp15-t04-acc/`（26 件：gtest XML 六份＝集成/冒烟两模式×两目标＋变异还原复绿终态两份、ird_gates base/head 引擎日志两份、归一化命中表四份〔原始行/去重条目两口径×两侧〕、集成构建/集成配置/冒烟构建/冒烟配置日志四份、validate-docs/validate-task 日志两份、变异构建/运行日志八份〔M1 范数化/M1b 签名去重/M2 排序键序/M3 过滤顺序〕） |

---

## 4.1 改动范围 — 通过

- `git diff --name-status f56256f6..fb67389f`＝32 个文件，逐一对契约 allowedFiles：
  - `kinematics/**` 14 个：CMakeLists.txt（M）；include 四处（Bounds.hpp/Ik.hpp/SolutionSet.hpp 新增，Evaluators.hpp/KinTypes.hpp 表尾修改——5 文件）；src 七处（Bounds.cpp/CanonicalCodec.hpp/Ik.cpp/KinTypes.cpp/SolutionSet.cpp 新增，Evaluators.cpp 修改）；test/IkTest.cpp、contract_test/TaskPointIkEvaluatorContractTest.cpp 新增——allowedFiles 第 1 条内；
  - `units/kinematics.md` 1 个（v0.3→v0.4）——allowedFiles 第 2 条内；
  - `traceability/builds/wp15-t04/` 17 个——allowedFiles 第 3 条内。
- `forbiddenFiles`（REQUIREMENTS.md/ARCHITECTURE.md）：diff 零触碰。
- SA-02：patches/ 路径 diff＝0 文件——框架零源码修改、无未登记改动。

## 4.2 可复现构建 — 通过（两模式独立树从零）

- **集成模式**：worktree A（detached @fb67389f）→ `cmake -S <A>/RobWork -B <独立目录> -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON`（附 toolchain/Qt 前缀）exit 0（43.3s），缓存 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON` 确认；`cmake --build … --config Release --target sdurws_ird_kinematics sdurws_ird_kinematics_test sdurws_ird_kinematics_contract_test` exit 0，日志 `error C|error LNK|fatal error` 计数＝0（附件 acc-integration-build.log/acc-integration-configure.log）。
- **独立冒烟模式**：另一独立目录 `cmake -S <A>/RobWork/RobWorkStudio/src/rwslibs/industrialrobot -B <独立目录>`（vcpkg toolchain＋Qt 前缀——AGENTS §4.1 口径）配置 exit 0；`cmake --build --config Release` 全树 exit 0、零 error（附件 acc-smoke-configure.log/acc-smoke-build.log）；两测试 exe 冒烟实测 **45/45＋20/20** 通过（acc-kin-test-smoke.xml/acc-kin-contract-smoke.xml 原样留存）。

## 4.3 契约 verify 命令 — 通过（3/3 亲手执行）

| # | 契约命令 | 结果 |
| --- | --- | --- |
| 1 | `cmake --build build --config Release --target sdurws_ird_kinematics` | exit 0（独立 build 树按字面 target 构建） |
| 2 | `ctest --test-dir build/RobWorkStudio/src/rwslibs/industrialrobot/kinematics -C Release -L "^ird$"` | exit 0，`100% tests passed, 0 tests failed out of 2`（标签 ird 计 2 用例） |
| 3 | `pwsh -File RobWork/scripts/industrialrobot/validate-docs.ps1` | exit 0，`validate-docs: PASS (20 units, 12 trace entries, 189 task files)` |

- 家族注记（引用 F-381，不另立新号）：verify 数组含 ctest 行但缺 `_test`/`_contract_test` 两测试目标显式构建行与 ird_gates 行——ctest 所依赖的测试 exe 与门禁引擎直跑由验收者在独立构建树自行补建补跑（家族在案口径，属验收者侧正常动作）。
- 附验（协议 §3 脚本面）：`validate-task.ps1 -TaskFile <契约>` exit 0 `PASS (1 tasks)`（附件 acc-validate-task.log/acc-validate-docs.log）。

## 4.4 契约 acceptance 逐条 — 通过（5/5 有证据）

| # | 契约条目 | 证据（验收者亲手复现） |
| --- | --- | --- |
| 1 | 多初值 IK 去重（去重对象＝构型；逐轴阈值 1e-6、continuous 直接比较无跨周取模；硬过滤顺序①残差复验②限位③碰撞未启用跳过＋collisionNotEvaluated——V-04/AT-03 反例） | `SamePoseDistinctConfigurationsRetained`（三连杆双分支同位姿两构型均保留——solutions=2）；`DedupMergesWithinPerAxisTolerance`（两初值逐轴差 5e-7 合并为 1＋convergedCount=2＋filteredCount=0〔去重≠硬过滤统计分离〕＋代表 sourceInitIndex=0）；`DedupThresholdIsPerAxis`（deduplicateSolutions 唯一实现点直测：全轴≤阈值合并/单轴 0.8>0.7 保留/阈值边界 0.5 闭式比较/管线级双分支回归）；`FilterOrderLimitBeforeCollision`（全碰撞替身＋越限候选——记录 reason=JointLimit 而非 Collision，顺序即语义）。实现面：Ik.cpp 第 3 步①FK 新鲜复验→②逐轴直接比较（L593-599，无取模）→③会话判空跳过＋collisionNotEvaluated（L450/L622-625）。反证面：变异 M1 揭示该测试反范数数据缺口→F-391（建议级） |
| 2 | 五类结局铁律（枚举无任务级不可行值；结局 2/3 必附 SearchExhaustedRecord〔预算/初值数/迭代统计/逐解过滤〕→DataInsufficient 素材；换初值/扩预算同冻结输入可翻转——V-06/V-07/V-09；§8.1 C5/C8 不得输出不可行） | `MultiInitNoConvergenceCarriesSearchRecord`（结局 2＋searchRecord.initialGuessesTried 断言）；`AllCollisionFilteredYieldsSearchRecordNotInfeasible`（结局 3＋searchRecord 必附＋无 proofMaterial——不出不可行）；`ExpandedInitialValuesFlipOutcome`（同目标同容差小预算→结局 2，扩初值→SolutionsFound 且残差≤容差）；`ConvergesOnReachableTarget`（结局 1）；`CancellationYieldsNoTerminalFields`（取消非结局零终局字段）。契约测试：`SearchExhaustedDeliversRecordAndDiag`（evidence::SearchExhaustedRecord 交付＋KIN-SEARCH-EXHAUSTED 诊断）、`EvaluateSuccessProducesBoundPayload`（结局 1 有 payload 无 searchRecord 反差断言）。枚举面：KinTypes.hpp `IkOutcomeKind` 恰五值（读码——无任务级不可行值，D-KIN-7） |
| 3 | 稳定排序四键逐位可复现（margin 降序→manip 降序→‖q−referenceQ‖₂ 升序→sourceInitIndex 兜底；referenceQ 显式入身份禁隐式读会话姿态；去重＝成对容差比较而 Signature 精确编码仅记录键——I-KIN-3；V-04 两次输出逐位比对） | `SolutionSetViewSortsByFourKeys`（五解手工黄金逐位断言四键逐级裁决＋同集合重复构造视图幂等）；`ReferenceQEntersIdentityAndDeterminesBytes`（同请求两次 payload 逐位一致＋仅改 referenceQ 身份字节必变＋解本身不受影响）；`RepeatedSolveBitwiseIdentical`；`SignaturesDifferWhileDedupEquivalent_IKIN3`（1e-9 rad 差：签名不同〔精确编码〕＋去重等价〔容差〕两向语义）。实现面：IkRequest.referenceQ 显式字段（Ik.hpp L255——请求组装唯一来源，无会话姿态读取面）；sortSolutions/deduplicateSolutions 唯一实现点（NFR-MNT-04）。变异 M2（键序交换）翻红 2 用例证非恒真（见 4.5） |
| 4 | 解析工作半径界限（Bounds.hpp §3.3 T04 行；超界产 AnalyticBoundExceeded 证明素材〔推导输入＋expectedSliceId 绑定〕——仅素材不裁定归 evidence validateProof；非法目标/容差 fail-fast KIN-TARGET-ILLEGAL；结果绑定五元组＋要求值分离——V-10 前半联合观测） | `AnalyticBoundExceededMaterialValidatable`（素材经 evidence validateProof 零问题自证——category/boundExpression/producer 键＋契约版本/snapshotId+sliceId 绑定/覆盖声明五查通过）；`AnalyticBoundDeliversProofMaterial`（output.proof 承载）。实现面：Bounds.cpp 三角形不等式（Σorigin 模长＋Σprismatic 行程＋TCP 偏置——L135-171）＋`%.17g` 确定推导文本＋makeAnalyticBoundMaterial 仅组装不裁定（Ik.cpp 结局 5 零迭代早返 L452-461）；KIN-TARGET-ILLEGAL fail-fast 轨：`IllegalRequestFailsFast`（NaN 目标/容差≤0/维度违例→invalid_argument）＋`AssemblyFailsFastOnIllegalQuery`（装配期四反例）；KinematicsErrorCode 零追加（Errors.hpp 零改动——diff 实证；依据＝§9.2 IIkSolver 无 @错误 行域错误名可收编＋solve() 无 Expected 通道——卡 §14.6 v0.4 ③ 登记）。绑定面：`EvaluateSuccessProducesBoundPayload`（payload kindToken/canonical 字节/SHA-256 摘要重算三面断言）＋encodeTaskPointIkPayloadCanonical（IRDIK01 v1——身份五元组+referenceQ+task 五元组+evaluationKey/契约版本内嵌、要求值分离——Evaluators.cpp 读码） |
| 5 | 按卡 §11『求解/过滤去重排序两提交』拆分实施提交，验收一次全量执行（gtest XML＋ird-test-report.json 留痕） | 两提交：a7580ddd「实现多初值 IK 求解与解析工作半径界限」＋fb67389f「落位硬过滤去重排序与 kin.task-point-ik 评估器」——与卡 §11 T04 行规模列拆分一致；验收者全量亲跑两模式 45/45＋20/20（四份 XML 原样留存）＋ctest 2/2；送验内留痕 17 件清点与宣称一致（见 4.10） |

## 4.5 测试真实失败能力（验证验证者） — 通过（4 处实质变异：3 处真红＋1 处揭示测试强度缺口；全部还原复绿）

变异均在 worktree A 产品源上实施→增量重建→跑具名测试→`git restore`→全程日志留痕（acc-mut*-*.log）。时序纪律：全部变异在冒烟构建与冒烟测试**完成之后**实施、还原经 `git status` 确认干净后才跑终态全量（T14 教训合规）：

| 变异 | 内容 | 结果 |
| --- | --- | --- |
| M1 | deduplicateSolutions 逐轴阈值比较→欧氏范数（SolutionSet.cpp） | **未翻红**：`DedupThresholdIsPerAxis_WP15T04_ACC1` 保持绿——该测试的反范数断言数据无效（注释宣称逐轴差 (0.5,0.5) 会区分范数 0.707>0.7，实际数据 b={0.6,5.0} 第二轴差为 0、范数 0.5≤0.7 与逐轴判定同值）→ 建议级 F-391（测试强度缺口，非恒真——M1b 证明其容差路径真实会红） |
| M1b | 容差比较→签名精确相等（I-KIN-3 违例方向——去重对象退化为逐位相同编码） | **真红 3 用例**：DedupMergesWithinPerAxisTolerance／DedupThresholdIsPerAxis／SignaturesDifferWhileDedupEquivalent_IKIN3（`[FAILED ] 3 tests`） |
| M2 | sortSolutions 键序篡改——manipulability 提到 minimumJointMargin 之前 | **真红 2 用例**：SolutionSetViewSortsByFourKeys／SolutionSetViewWorstByAndFiltered |
| M3 | Ik.cpp 硬过滤顺序篡改——③碰撞提前到②限位之前 | **真红 1 用例**：FilterOrderLimitBeforeCollision（reason Collision≠JointLimit——顺序语义被具名断言钉住） |

恢复后终态：`git status` 产品源零差异，重建三目标 exit 0，45/45＋20/20 全绿（acc-kin-test-int-final.xml/acc-kin-contract-int-final.xml 原样留存）。

## 4.6 架构红线 — 通过（AGENTS §5 八条逐条）

1. **L2 零 Qt**：kinematics 全单元 grep `include.*Qt`＝仅 1 命中且为 BuildRedLineTest.cpp 内的守卫正则字符串自身（红线测试的扫描模式文本）——真实 Qt include 零命中。
2. **R-1 业务域互链**：产品面跨单元 include 仅 core/diagnostics/runtime/evidence 四个 L2/L3 单元公共头；零 policy include（碰撞经**本单元自有最小端口** IKinCollisionSession——P-KIN-7/O-37 宿主注入先例同款，真实④端口会话组装归 T07，R-POL-2 不变）；无 modeling/requirements/trajectory/dynamics/selection/optimization 触碰；ird_gates SUB 直方图 base↔head 零新增（去重口径 SUB 37→37）。
3. **R-2 跨单元私有头**：消费的他单元头逐一核实位于各单元 `include/sdurws/ird/<unit>/` 公共面（core 6 处/evidence 6 处/runtime 8 处/diagnostics 1 处——全公共）；本单元私有头 CanonicalCodec.hpp 置 src/ 内、仅本单元两 TU（Evaluators.cpp/KinTypes.cpp）include——跨单元不可达。
4. **R-4 名称前缀拼接/剥离**：新增源面 `RobWork` 字符串拼接/剥离扫描＝0 命中；TCP 解析经 findObject 精确匹配（FrameUnresolved 拒绝模糊匹配——Bounds.cpp/Ik.cpp 双侧同规则）。
5. **PA-1 权威唯一**：限位真值经 evaluationIntervals(view) 同快照提取（唯一实现点）；码值经 DiagCodes 注册常量（kKinResidualExceeded 等——禁字符串拼码，拼码处 throw logic_error 防线）；判定词表零输出（EvaluationOutput 仅 payload/diagnostics/searchRecord/proof 素材——只产素材不裁定，结局裁定归 evidence validateProof）。
6. **PA-2 不可变历史**：本任务无修订写路径接触面（纯计算库＋评估器形态）。
7. **SA-02 框架零修改**：4.1 patches 零触碰。
8. **old/ 不恢复**：实现全部新建（Ik/SolutionSet/Bounds/CanonicalCodec/KinTypes 增量等无历史源码恢复迹象；旧代码仅卡 §2.5 功能对照）。

## 4.7 注释规范 — 通过（AGENTS §2 全项）

- 公共头全查（Ik.hpp 377 行/SolutionSet.hpp 203 行/Bounds.hpp 173 行新增＋Evaluators.hpp/KinTypes.hpp 表尾增量）：文件头中文 Doxygen（设计依据含卡 § 行号/需求 ID/O 项/契约 acceptance 出处＋背景说明）＋类/结构体/函数 @brief/@param [in] 方向与单位/＠return 语义完整；§2.5 高危信息逐项在位——物理单位（m/rad/逐轴 rad|m/λ 无量纲）、坐标系（目标位姿基座系 {B}——T_base_tcp）、角度制式 rad 显式、所有权与生命周期（modelView/collisionSession 非 owning——调用方持有并保证 solve() 期间存活；解集视图构造后不可变）、线程约束（IkSolver 无状态可重入/会话并发只读/探针线程安全义务）、错误语义（调用方 fail-fast 轨 vs 评估期结构化素材轨分轨注明＋KIN-TARGET-ILLEGAL 语义锚）、确定性来源（splitmix64 seed 派生序列/单线程定序/`%.17g` 定点/f64 位模式直写）。
- 实现文件抽查 ≥3：Ik.cpp（第 0~5 步逐步注释＋每分支业务场景＋KIN-SOLVER-INTERNAL fail-fast 不静默＋结局 2/3 阶段①幸存数区分判据）、SolutionSet.cpp（四键逐键注释＋配对预计算理由＋per-axis/无取模语义＋组内代表先到先留）、Bounds.cpp（三角形不等式数学含义＋逐段贡献单位＋素材五要素对照 evidence validateProof 五查）、KinTypes.cpp（IRDSIG01 布局逐字段＋±0 位级区分说明）——合格。
- 测试代码：用例名带 WP15T04_ACCx 追溯字段＋IRD_TEST_INFO 需求/AT 登记＋中文断言说明（读码核实）——合格。

## 4.8 文档同步 — 通过（建议级 2 条转登 F-389/F-390）

- 单元卡 v0.4（§14.6 行 978）六项登记逐项与实现核对一致：①KinTypes 表尾追加（五类结局枚举/值模型族/configurationSignature IRDSIG01/referenceQ 入身份——卡面 §6.1 未穷尽字段随行登记）；②Bounds 三角形不等式公式与 Bounds.cpp 逐条一致＋素材字段面（claimToken/coverageClaim/producer/snapshotId+sliceId）；③Ik.cpp 实现口径 14 项（λ=1e-3 LLT/FK 复算唯一委托 FkEvaluator/探针每 16 次＋初值边界/初值策略三分式含 continuous 名义区间 [−π,π]/硬过滤顺序与首命中阶段记录/去重先到先留/结局判定落值＝阶段①幸存数区分 2/3＋Collision 记录→4/结局 5 先于迭代/intervals 同快照义务/IKinCollisionSession 最小端口/KIN-TARGET-ILLEGAL fail-fast＋**KinematicsErrorCode 零追加依据**〔§9.2 无 @错误 行可收编＋solve() 无 Expected 通道——§9.6 不预建无消费者条目；Errors.hpp 零改动经 diff 实证〕）与实现逐条对号；④SolutionSet 视图四操作＋worstBy 三度量稳定语义；⑤评估器族错误分轨/结局映射/模式敏感性/评估键 kebab 形态（§4.3 行 307 随版标注——evidence 冻结闸门 isValidEvaluationKey 有具名测试钉住）；⑥测试与留痕数字（45/45＋20/20 与验收者亲测一致）。卡头版本行 v0.4/日期行 2026-09-26 同步（F-369/374/F-133 教训已吸收）。
- 偏差登记判定：实现与契约/单元卡零未登记偏差——V-04 载体改平面 3R（2R 全位姿逆解唯一故双构型反例需三连杆）已随 v0.4 ⑥ 登记；kebab 键形偏差随 §4.3 行＋v0.4 ⑤ 双登记且有先例（T03 kin-pose-metrics 同款）；Bounds 归 T04 落点偏差随 v0.3⑦ 消歧后兑现。
- **findings.json 未关闭条目相关性核对（4.8 指定项）**：kinematics 相关 open 8 条逐条盘点——F-380 触发条件（下次触及 DiagCodes.hpp/DiagCodesTest.cpp）本任务未发生（diff 实证零触碰该两面）；F-384 触发面 Errors.hpp 本任务零改动（该零改动本身即 T04 契约依据，改动反而需重新评估）；F-381 家族本任务实例见 4.3 注记（引用不另立号）；F-382 sanctioned 台账本任务零新增 SUB 命中（双侧直方图逐码同值）；F-383（evidence §9 增补悬账）本任务延续 T03 义务 c 口径（零 evidence 侧未落卡类型依赖——IKinCollisionSession 自有最小端口读码核实）；F-385/F-386/F-387 为 T03 遗留非本任务产物，其中 F-386 同族缺口在本任务复现（KIN-RESIDUAL-EXCEEDED——转登 F-389）。无静默积压。
- 发现（建议级 F-390）：卡 v0.4 ⑥『KinIk 15 新用例』与实测不符——本批新增 IkTest.cpp 计 18 个 TEST（提交 1 时 8＋提交 2 增 10；base 侧 T03 留痕 XML 实证 _test 目标 27 例无 KinIk 套件，head 45＝27＋18），枚举漏列 FilterOrderLimitBeforeCollision/InitialValueStrategiesDeterministic/DedupMergesWithinPerAxisTolerance 三具名用例——散文计数家族（方向少报、附件 XML 与提交正文 45/45＋20/20 准确）。

## 4.9 commit 质量 — 通过（建议级 1 条转登 F-392）

- 两提交正文四段（What/Why/验证/影响面）齐备；验证段主张逐项与验收者亲测一致：集成构建零错误、ctest 2/2、45/45＋20/20 两模式、validate-task PASS（1 tasks）/validate-docs PASS（20 units/12 trace/189 task files 同数字）、ird_gates 引擎直跑三套计数同值零新增零消除（附件逐格一致）——无粉饰。
- 影响面段『两测试目标用例数扩容（35→45、11→20）』经时点实证为**提交 2 自身增量口径**（提交 1 时 _test 目标＝base 27＋IkTest 8＝35、contract 目标 11；head 45/20）——准确非粉饰。
- 标题：两提交均动词开头（『实现…』『落位…』）、任务编号以括注形式出现在标题内（『（WP-15-T04 提交 N…）』），但未用 `[WP-15-T04]` 前缀形态——AGENTS §6.3/ACC §4.9 字面要求不符；F-012/F-069/F-385 标题家族再现（动词开头、编号可见、无粉饰）→ 建议级 F-392。

## 4.10 留痕完整 — 通过

- 送验提交内 traceability/builds/wp15-t04/ 实际入库 17 件（ls 清点）：base-head.diff、base/head_hits.norm、integration-build.log、ird-gates-base-head-compare.md、ird-test-report.json＋ird-test-report-contract.json、ird_gates base/head 引擎日志、run-integrated/run-smoke 测试运行日志×4、双目标 gtest XML（tests=45/failures=0、tests=20/failures=0——与验收者两模式复跑逐一致）、smoke-configure/smoke-build 日志——**声称与入库一致，零幻影清单**（F-102/F-111/F-125 家族本任务零复现）。
- ird-test-report.json：summary total 45/passed 45/failed 0/decisive true＋45 条目；contract 报告 20/20 同构——计数与 gtest XML 与验收者亲测三方一致（F-342 家族核对口径）。
- phase-one-validation.log：本任务属阶段 B 单元实现批次，留痕以 builds/wp15-t04/ 证据集为口径（与已合入的 T02/T03 同形）——ird-test-report.json 即结果账本（resultNote 面齐备）。

## 4.11 偷懒/缩水实现扫描 — 通过（建议级 2 条转登 F-389/F-391）

- ①diff 新增行扫描：TODO/FIXME/placeholder/未实现/not implemented＝0 命中；`stub` 6 命中均为测试替身类名（StubProducerRegistry/StubCollisionSession——test double 命名惯例，非空实现标记），无契约豁免条款需要列出。
- ②空实现检测：产品面无空函数体/仅返回默认值的接口实现——Ik.cpp 681 行（DLS 迭代/硬过滤三段/结局判定）、SolutionSet.cpp 199 行（四键排序/逐轴去重/视图四操作）、Bounds.cpp 252 行（界限推导/确定文本/素材组装）、Evaluators.cpp 增量段（descriptor/装配校验/评估主流程/canonical 编码）实质实现完整；`return {}` 空语义扫描无产品面命中。
- ③逐条证据表：见 4.4——acceptance 5/5 条条有具名测试/命令输出/文件行号。
- ④测试强度：45＋20 用例断言行为与边界（双分支同位姿保留、逐轴阈值与边界闭式、过滤顺序语义、结局翻转、searchRecord 字段、取消零素材、身份字节敏感、四键逐位、validateProof 五查自证、摘要重算、诊断码常量）；非恒真——变异 M1b/M2/M3 证明 6 个具名用例真实会红。发现（建议级）：M1 变异揭示 `DedupThresholdIsPerAxis` 的反范数断言数据无效（变异后保持绿——注释宣称的区分能力未被数据承载）→ F-391；KIN-RESIDUAL-EXCEEDED 生产路径无自然触发用例且缺口未随卡登记 → F-389（F-146/F-043/F-386 覆盖面家族）。

## 门禁专段（ird_gates 引擎直跑 base..head 归一化比对 — 零新增零消除）

验收者以 cmake -P 引擎直跑两棵 detached worktree（base＝f56256f6 临时 worktree，用毕即删；head＝fb67389f worktree A），归一化（剥 worktree 绝对路径前缀）后逐行 diff，三套计数并列（T02 验收确立口径/F-378 教训）：

| 计数口径 | base f56256f6 | head fb67389f | 增量 |
| --- | --- | --- | --- |
| ①IRD-GATE 标记行总数（含引擎自测行 2——fail_r5/fail_t2 按预期检出文本，非命中） | 145 | 145 | 0 |
| ①′纯命中行（去自测行） | 143 | 143 | 0 |
| ②去重条目（引擎『命中 69 项』清单） | 69 | 69 | 0 |
| ③引擎自计数（`命中 N 项`/`存在 N 处命中`） | 69 | 69 | 0 |

- 原始行逐码直方图（两侧同值）：LIB 12／R1 7／R3 41／R4 3／R5 1（自测）／SUB 75／T1 5／T2 1（自测）；去重口径逐码（两侧同值）：LIB 6／R1 3／R3 20／R4 1／SUB 37／T1 2。
- 归一化 diff：原始行口径与去重条目口径**均为空**（acc-base/head-hits.norm、acc-base/head-dedup.norm 四件附件）——零新增零消除，与送验附件 ird-gates-base-head-compare.md 逐格一致（验收者独立重跑，F-387 关注的登记不一致本轮未复现）。
- VS 目标 ird_gates 退出码 1＝存量命中既有状态（DTB §4.5 存量例外未清零）；kinematics 相关 SUB 命中（sdurws_ird_kinematics_test/_contract_test→sdurws_ird_testkit）为 F-382 在案存量、双侧同在零新增。

## 发现问题清单

| 级别 | 编号 | 摘要 |
| --- | --- | --- |
| 阻断 | 无 | — |
| 建议 | F-389 | KIN-RESIDUAL-EXCEEDED 比较型诊断生产路径已落位（appendResidualDiags＋硬过滤①ResidualRecheck 轨）但无自然触发用例（迭代内收敛与新鲜复验消费同一确定性 FkEvaluator——复验失败在注入面不可达），且单元卡 §14.6 v0.4 ⑤ 仅登记『生产面』未按家族口径登记覆盖缺口（F-146/F-043/F-386 家族） |
| 建议 | F-390 | 卡 §14.6 v0.4 ⑥『KinIk 15 新用例』与实测 18 不符（漏列三个具名用例）——F-025/F-041/F-073/F-387 散文计数家族（方向少报、附件与提交正文实测数准确） |
| 建议 | F-391 | DedupThresholdIsPerAxis_WP15T04_ACC1 反范数断言数据无效——变异测试实证：逐轴阈值→欧氏范数变异后测试保持绿（数据第二轴差 0，注释宣称的 (0.5,0.5) 区分场景未落地）；建议 T13 黄金数据集随批补真双轴差用例 |
| 建议 | F-392 | 两送验提交标题未用 `[WP-15-T04]` 前缀形态（任务编号以括注形式在标题内）——F-012/F-069/F-385 标题家族再现（动词开头合规、正文四段齐备、结论无粉饰） |

## 裁决

**verdict＝pass**。4.1～4.11 全部通过，无阻断级问题；4 条建议级随本记录转登 findings.json（F-389～F-392，编号顺延自送验侧最大号 F-388），不阻塞合入。送验对象＝fb67389f65ee04413efa051949729e2ec8022e6d 本身；分支漂移检查（ACC §3）：`git ls-remote origin refs/heads/wp15-t04`＝fb67389f65ee04413efa051949729e2ec8022e6d——与冻结 SHA 一致，零漂移。

---

*验收者：WP-15-T04 attempt 1 独立验收会话（全新上下文，2026-09-26）；本记录与 F-389~F-392 转登经 acc/WP-15-T04/1 分支推送，收尾按 commit SHA 合并。*
