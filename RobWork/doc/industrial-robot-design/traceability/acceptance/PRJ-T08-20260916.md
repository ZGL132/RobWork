# PRJ-T08 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | PRJ-T08 打开与恢复（ProjectStoreFactory/ProjectStoreImpl） |
| 单元 | project |
| 送验对象（冻结） | 3e19fc6798d6c0285fe9d544dcebd895159fccb2（origin/wp04-t08 尖端＝该 SHA，无分支漂移） |
| diff 范围 | cc49f39fce2115f0991179bb436d40f2b0c75ddf..3e19fc6798d6c0285fe9d544dcebd895159fccb2（state.base..headSha；merge-base 实测＝base，4 提交 12 文件） |
| 验收会话 | 全新上下文（ACC §2 四条独立性逐条适用：未消费任何实施叙述，只读代码/契约/设计文档/留痕；构建测试亲手重跑；为"不通过"找证据立场） |
| 复现现场 | `git worktree add --detach <临时目录A> 3e19fc67`（detached @ SHA）；独立冒烟目录另配；全部验证在临时 worktree 内完成 |
| **verdict** | **pass**（4.1～4.11 全部通过；零阻断级问题；建议级 6 项 F-141～F-146 随本记录转登 findings.json，不阻塞合入） |
| 日期 | 2026-09-16 |

---

## 1. 逐项证据（ACC §4，4.1～4.11）

### 4.1 改动范围 — 通过

- `git diff --stat base..head`＝12 文件：project 单元 6（CMakeLists.txt、include/ProjectStore.hpp、include/StoreTypes.hpp、src/ProjectStoreImpl.{hpp,cpp}、test/ProjectStoreTest.cpp）＋traceability/builds/wp04-t08/ 5（ird-test-report.json＋四份 gtest XML）＋units/project.md 1——全部落在契约 allowedFiles 三个通配内（`project/**`、`traceability/**`、`units/project.md`）。
- forbiddenFiles（REQUIREMENTS.md/ARCHITECTURE.md）零触碰（diff 文件清单实证）。
- SA-02：`git diff base..head -- RobWork/patches vcpkg` 输出为空——patches/ 无新登记、无框架源码改动。

### 4.2 可复现构建 — 通过（任务目标面从零零错误；全树中止＝F-008 基线既有）

- 集成模式重新配置（worktree A 内全新 build/）：`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON`＋vcpkg toolchain＋Qt 前缀（F-062 登记的 worktree 复现三件前置——toolchain/Qt 前缀/两件未跟踪 ini 模板——验收时逐件补齐，主树 CMakeCache 实证参数同源）；缓存 `grep ^RWS_BUILD_INDUSTRIALROBOT build/CMakeCache.txt`＝`RWS_BUILD_INDUSTRIALROBOT:BOOL=ON` 确认。
- 全树 Release 构建（`cmake --build build --config Release`）：EXIT=1，73 处 error 行全部位于 RobWorkSim `RWSimulatorPlugin`（71）与 `bt_plugin`（2）——F-008 登记的基线框架缺陷（min/max 宏污染簇，全新树必现、主树存量增量掩盖；其 resolution 明示 ired 任务不受影响），与 industrialrobot 零关联。
- 任务目标面从零构建零错误：`--target sdurws_ird_project_test` EXIT=0 零 error（acc-verify-target-build.log）；`--target sdurws_ird_project sdurws_ird_project_contract_test` EXIT=0 零 error（acc-other-targets-build.log）。
- 独立冒烟模式（另一临时目录，带 `-DCMAKE_TOOLCHAIN_FILE=<repo根>/vcpkg/scripts/buildsystems/vcpkg.cmake`——AGENTS §4.1/CORE-T01 G-2 口径）：configure EXIT=0（acc-smoke-configure.log）、Release 构建 EXIT=0 零 error（acc-smoke-build.log），两测试 exe 产出。

### 4.3 契约 verify 命令 — 通过

- 契约 verify 唯一一条 `cmake --build build --config Release --target sdurws_ird_project_test`：验收者亲手执行 EXIT=0，产物 `sdurws_ird_project_test.exe` 生成。
- `verify-task.ps1 -TaskFile tasks/foundation/PRJ-T08.json`：`verify-task: PASS PRJ-T08`（acc-verify-task.log）。

### 4.4 acceptance 条目逐条 — 通过（四条均有具名测试证据；一条覆盖面建议转登 F-146）

| # | 契约 acceptance 原文要点 | 证据（ProjectStoreTest 18 用例，XML 与控制台双留痕，验收者亲手重跑全绿） |
| --- | --- | --- |
| 1 | PRJ-TX-9：Closed 后迟到写拒绝＋稳定诊断；闭包外修订不可见；激活前失败不影响当前项目 | `Tx9_ClosedLateWrite_RejectedWithStableDiag`（ContextClosed＋PRJ-WRITE-AUTHORITY-LOST＋HEAD 字节不变）、`Tx9_DrainingRejectsWrite_ThenDrainsInFlight`（Draining 拒写→票据释放排空→回调恰一次）、`Tx9_UncommittedRevision_InvisibleAfterReopen`（植入闭包外修订＋悬挂对象→tryRevision 空＋对照腿可见＋danglingObjectCount>0）、`Tx9_OpenFailure_DoesNotTouchCurrentProject`（完成条件具名用例：open 旧格式 B 抛 FormatLegacy 后 A 仍可写可提交 HEAD 前进）。全写入口（archive.begin/submit、draft.save）尚未存在（T10/T12/T14 落位面）——写入口代表＝executeCommit 内部写通道，口径已按 DTB §5.4 登记（单元卡 v0.10＋ird-test-report.json implementationNotes＋文件头），全量用例归 PRJ-T15 复验：登记成立，视为有证据。 |
| 2 | 旧格式/未来版本稳定拒绝＋升级指引；恢复报告断言（PM-06/PM-08） | `LegacyFormat_StableReadOnlyRejection`（FormatLegacy＋PRJ-FORMAT-LEGACY＋原文件字节不动）、`SchemaFuture_RejectedWithUpgradeGuidance`（SchemaFuture＋detail 三键 document/supported/upgrade＋PRJ-SCHEMA-FUTURE）、`RecoveryReport_StagingResidueAndOrphanDrafts`（.staging 残留/孤儿草稿/.bak 三现场逐字段＋PRJ-RECOVERY-IGNORED-UNCOMMITTED/PRJ-RECOVERY-ORPHAN-DRAFT＋报告快照与 sink 同源）、`RecoveryReport_HeadIntegrityFailure_StopsOpen`（④闭包完整性失败＝StoreCorrupt＋PRJ-STORE-CORRUPT，打开中止）。 |
| 3 | open/createNew/关闭排空（Draining）与失权后写拒绝；只读打开提供查询 | `CreateNew_SkeletonIdentityAndReopen`（骨架五要素＋身份面＋幂等关闭）、`Open_ReopenClosedProject_IdentityPreserved`、`Tx9_DrainingRejectsWrite_ThenDrainsInFlight`（PM-03 存储侧排空）、`ReadOnlyOpen_ProvidesQueriesAndRejectsWrites`（PM-07 只读查询面＋LockHeldByOther＋PRJ-LOCK-HELD）、`WritableOpen_DegradesToReadOnlyWhenHeld`（进程外持锁模拟→降级＋持有 PID 可读→恢复对照腿）、`InProcessSecondWritableOpen_Rejected`（§9.3 防自我双写）。**失权后写拒绝的上下文级直接用例缺位**（门卫第②道 requireWriteAuthority→WriteRejected 的三行委托未端到端钉住；原语级语义已由 StoreLockTest 5+ 用例〔PRJ-T03 交付〕覆盖）——建议级，转登 F-146，随 PRJ-T15 复验补用例。 |
| 4 | P-PR-1/P-PR-6 处置 | `Ppr1_CoreIdentityBaselineConsumed`（prj-/rev- 规范文本形态＋与磁盘 round-trip——core Identity v0.1 基线，零 core 修改实证：diff 无 core 文件）、`Ppr6_SinkInjectionOptionalWithSameCodes`（sink 注入双通道同源；null sink 报告数据面照常；码值逐字＝diagnostics.md §4.6 收编清单）。零 diagnostics 链接边由 LinkageContractTest 10/10（验收者重跑）维持。 |

### 4.5 测试真实失败能力 — 通过（双探针注入即红、还原复绿）

- 探针 1（状态门卫）：将 `ProjectStoreImpl::executeCommit` 的 `m_state != Active` 改为恒假（禁用 §9.6① 门卫）→重建→`--gtest_filter=ProjectStoreTest.Tx9_*`：**`Tx9_ClosedLateWrite_RejectedWithStableDiag` 与 `Tx9_DrainingRejectsWrite_ThenDrainsInFlight` 双双 FAILED**（迟到写不再拒绝）——测试对门卫行为真实敏感。
- 探针 2（闭包可见性）：将 `loadCommittedState` 的 `uncommitted.count(dirName)` 跳过改为恒不跳过（闭包外修订注册进会话索引）→重建→**`Tx9_UncommittedRevision_InvisibleAfterReopen` FAILED**——可见性纪律被测试真实钉住。
- 两探针还原后 `git diff` 为空、全量重跑 169/169 复绿（acc-revert-rerun.log）。探针仅存在于验收者临时 worktree，未触碰送验对象与任务分支。

### 4.6 架构红线（AGENTS §5 八条逐条） — 通过（1 项门禁嫌疑转登 F-145）

1. L2 零 Qt：project 单元 grep Qt 头＝0 命中（源码扫描实证）。
2. R-1 业务域互链：`target_link_libraries(sdurws_ird_project PUBLIC sdurws_ird_core)` 唯一产品链接边；跨单元 include 仅 `sdurws/ird/core/{DiagData,Events,Digest,Identity}.hpp`（core 为基座单元，非业务域）。
3. R-2 跨单元私有头：新头 `ProjectStoreImpl.hpp` 置于 src/（私有）；对外仅 include/ 公共头；测试消费同单元私有头沿 TxEngineTest 先例。
4. SA-02：见 4.1（零框架改动）。
5. R-4 名称前缀拼接/剥离：代码无任何 RobWork 名称解析/拼接逻辑；**ird_gates IRD-GATE-R4 启发式命中 1 处**＝ProjectStoreImpl.cpp:104 `kCreatedWithToolVersion = "RobWork-IndustrialRobot/0.1"`——语义核查为 §4.2 createdWithToolVersion 数据字段（创建工具版本透传串，随 project.json 持久化），非名称解析行为，红线本体未违反；但门禁要求"例外须 DTB §4.5 登记"而 DTB 不在本任务 allowedFiles 内→转登 F-145 归所有者登记（major）。
6. PA-1 权威唯一：打开/恢复语义全部落在 project 单元卡 §5.1/§7.4/§8.7 范围内，无越权代行 runtime/policy 语义。
7. PA-2 不可变历史：实现只读既有修订（闭包外仅不装载不删除）；新增内容经 createNew 组装/提交七步，无覆盖写路径。
8. 不恢复 old/：diff 无 old/ 路径。

### 4.7 注释规范 — 通过

- 公共头全查：`ProjectStore.hpp`（267 行）/`StoreTypes.hpp`（399 行）——文件头设计依据/背景说明齐备，每个类型/函数中文 Doxygen（契约级：生命周期、所有权〔非 owning 注入指针、unique_ptr 交付〕、线程模型〔§9.8〕、错误语义〔稳定码封闭集＋错误二分〕、确定性〔NFR-COR-02 排序〕逐项在位）。
- 实现文件抽查（≥3）：`ProjectStoreImpl.cpp`（1341 行——文件头实现口径登记八项、每个函数 Doxygen＋语义段注释＋魔法数字来源〔如 256 MiB＝§4.6、心跳 30 s＝§9.4〕）、`ProjectStoreImpl.hpp`（锁序纪律、票据 RAII 语义）、`ProjectStoreTest.cpp`（每用例锚定行：需求/任务卡/AT 口径、夹具口径登记三项）、`project/CMakeLists.txt`（PRJ-T08 增列两段中文注释块）。§2.5 高危信息（单位/坐标系/rad/所有权/线程/错误语义/确定性）在适用处逐项在位（字节预算、OS PID、ISO-8601 UTC、非 owning 生存期义务、m_lifecycleMutex/m_writerMutex 锁序、稳定码归类、字典序确定性）。

### 4.8 文档同步 — 通过（3 项家族未消账/未同步转登 F-141/F-143/F-144）

- 实现与任务卡零未登记偏差：落位口径差异（端口访问器随 T09～T14 增量挂载、StorePath 不落位、写入口代表、实现口径八项）全部按 DTB §5.4 以单元卡 v0.10 §15.4 行＋源码头注登记——"先落访问器只能 stub"的规避本身即 NFR-MNT-04 纪律的落实。
- 单元卡 v0.10 登记内容与代码逐项核对一致（打开协议步骤、错误码清单、测试分组计数 4/4/6/2＋边界 2＝18 用例实测相符）。
- findings.json 未关闭条目相关性核对（v1.2 义务）：F-035（实现类必须实跑 ird_gates）本任务未执行→F-144；F-140（卡头日期行同步＋家族注明）本任务未执行→F-143；F-138（evidence 清单只列库内文件）本任务再现→F-141；F-139（提交标题动词开头）本任务再现→F-142；F-007/F-062（worktree 环境前置）验收复现时再次命中（按既有登记处置，不重复登记）；F-008/F-019/F-051（门禁/全树基线红）验收实跑数据与既有登记一致。
- 契约 status 维持 ready：翻转 done 归合入收尾（PIPE §4.7⑤），非验收项。追踪矩阵（trace-matrix.json/unit-status.json）为治理侧维护面，先例（PRJ-T07 diff）同口径，不构成缺漏。

### 4.9 commit 质量 — 通过（标题家族问题转登 F-142；无粉饰实证）

- 4 提交正文四段（What/Why/验证/影响面）全部齐备，Acc-Covered 行在位（185c221d 以"Acc-Covered: 4"登记文档侧承载）。
- 验证结论与留痕逐项比对真实：169/169＋10/10、XML tests/failures 属性、validate-task PASS——验收者独立复现**全部为真**；3e19fc67 对上一提交 XML 缺失根因（MSYS 路径传 Windows 程序致 gtest_output 未落盘而退出码 0）的教训登记属实且方向为自曝缺陷（反粉饰）。
- 标题：5cb6bc9e「实现 project 打开与恢复（…）」动词开头合规；69ca3e2c/3e19fc67 以任务名词开头（F-139 已判"PRJ-Txx 验证留痕归档/留痕补齐"逐字模板第六/七连例）、185c221d 为 [units] 车道文件名词短语（F-037 系同型）→转登 F-142（家族建议级，既往判例均不阻断）。

### 4.10 留痕完整 — 通过（库外 .log 混列转登 F-141）

- 库内实证（git ls-tree @3e19fc67）：ird-test-report.json（ird-test-report/1 全 schema）＋四份 gtest XML（tests=169 failures=0 ×2 模式；tests=10 failures=0 ×2 模式）。
- 验收者复跑产物与送验 XML 交叉核对：顶格计数 169/0、169/0、10/0、10/0 与验收者双模式重跑逐字一致；ProjectStoreTest 套件 18 用例计数相符。
- ird-test-report.json builds[] 引用的 7 份 .log（integration-build/smoke-configure/smoke-build/四份测试控制台日志等）**不在库内**（根 .gitignore `*.log`＝F-010 根因），提交正文未内联明示、report 无 localOnlyNote 区分→F-141（家族建议级）。验收者以本地重跑补强了这些日志的结论真实性（构建零错误/测试全绿均为亲手复现）。
- phase-one-validation.log 未更新（该文件自 WP-03 时代后未再滚动）；resultNote 等价物＝builds/wp04-t08/ird-test-report.json，沿本单元 T01～T07 先例，视为满足 4.10 的"或 resultNote"半边。

### 4.11 偷懒/缩水实现扫描 — 通过

- ①TODO/FIXME/placeholder/未实现/not implemented：diff 四个产品文件 grep 零命中。`stub` 仅 1 处＝ProjectStore.hpp:35 注释，内容为解释**为何不落**端口访问器 stub（随 T09～T14 增量挂载）——契约 note（DOC-T10 编译放行）与单元卡 §12/§3.1 显式登记的触发式延后，豁免依据成立。
- ②空实现检测：公共接口（ProjectStore 7 方法＋工厂 2 方法＋ICloseObserver）在 ProjectStoreImpl 全部有实质实现；无返回默认值的空函数体。
- ③逐条证据表：见 4.4——四条 acceptance 全部有具名测试/命令输出/文件行号，无"已实现"声明式证据。
- ④测试强度：断言校验行为与边界（稳定码逐字、字节级不变断言、对照腿防恒真、fail-fast 与环境错误二分）；4.5 双探针实证改实现即红。

### 附：ird_gates 实跑结论（F-035 处置建议口径——验收者代行）

`cmake --build build --config Release --target ird_gates`：exit 1，**9 处命中**＝基线 8（IRD-GATE-SUB×6：diagnostics_contract_test/evidence_test/evidence_contract_test/policy_test/policy_contract_test/runtime_test→sdurws_ird_testkit，F-051 家族；testdata_lint→testkit 的 SUB＋T1 两处，F-019）＋**新增 1（IRD-GATE-R4：project/src/ProjectStoreImpl.cpp，本任务引入，见 F-145）**。门禁自测 9 项（pass_clean/pass_r4comment/fail_r1/t1/r5/sub/r3/r4/t2）全部按预期。与 PRJ-T07 验收基线（8 命中、零 project 项）比对：新增命中恰为本任务文件 1 处。脚本三套（validate-task PASS / validate-docs PASS〔20 units, 12 trace, 150 task files〕/ verify-task PASS）均亲手复跑通过。

---

## 2. 发现的问题清单

**阻断级：无。**

建议级（已逐条转登 traceability/findings.json，编号顺延自 F-140）：

| 编号 | 严重度 | 摘要 |
| --- | --- | --- |
| F-141 | suggestion | F-138 家族第 15 例：69ca3e2c 正文声称入库 10 项证据，ls-tree 实证仅 5 件（7 份 .log 被 .gitignore 吞）；ird-test-report.json builds[] 库外 .log 与库内文件混列无 localOnlyNote |
| F-142 | suggestion | F-012 家族（F-139 同字面模板再 现）：三份提交标题名词开头（69ca3e2c/3e19fc67/185c221d） |
| F-143 | suggestion | F-133/F-134/F-140 家族第五例：卡头日期行停留 2026-09-09 未随 v0.10 推进；F-140 处置建议未执行未注明；另 §15.4 v0.10 行插于 v0.9 行之前（行序倒置） |
| F-144 | suggestion | F-028/F-035 家族：实现类任务未实跑 ird_gates，报告以"WP-01-T01 落地前口径"为由标注未执行（理由失实——门禁已建成且本单元前序任务均有门禁结论） |
| F-145 | major | **新增门禁命中**：IRD-GATE-R4 命中 ProjectStoreImpl.cpp:104 `kCreatedWithToolVersion = "RobWork-IndustrialRobot/0.1"`（§4.2 createdWithToolVersion 数据字段，红线本体未违反，属门禁例外登记缺位——例外登记归 DTB §4.5 所有者；不登记则后续任务门禁基线 8→9 持续红） |
| F-146 | suggestion | acceptance 3"失权后写拒绝"上下文级端到端用例缺位（门卫第②道 requireWriteAuthority→WriteRejected 三行委托未直接钉住；原语级已由 StoreLockTest 覆盖）——建议随 PRJ-T15 复验补用例 |

## 3. 验收者环境说明

- 复现现场：`git worktree add --detach <临时目录A> 3e19fc6798d6c0285fe9d544dcebd895159fccb2`；冒烟独立目录；记录回传：`git worktree add -b acc/PRJ-T08/1 <临时目录B> origin/redesign-main`（attempt=1，取自 state.attempts.accept）。
- worktree 集成配置按 F-062/F-007 既有口径补齐三件前置（vcpkg toolchain 绝对路径、Qt 前缀 `D:/software/Qt/6.11.1/msvc2022_64`、两件未跟踪 ini 模板自主树复制）——属已登记环境缺口，不影响送验对象评价。
- 全树构建中止于 F-008 基线缺陷（RobWorkSim 插件，73 error 零个涉及 industrialrobot）；任务目标面（sdurws_ird_project＋_test＋_contract_test 及依赖）从零构建零错误、双模式测试 169/169＋10/10 全绿——DoD 第 1 条按 F-036/F-038 确立的"任务目标面全新树从零"口径判定满足。
- 本记录与 F-141～F-146 转登构成 evidence 提交（同支 acc/PRJ-T08/1），推送后由编排者写入 state.acceptanceRecord；验收者未合入、未修改实施产物、未向任务分支推送任何提交（送验对象冻结于 3e19fc67）。
