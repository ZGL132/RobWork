# WP-24-T03a 首版装配验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | WP-24-T03a 首版装配（拆段见 DTB v0.21 §2.25；owner 指示 2026-09-26 提前启动＋"每完成一个插件进行装配"） |
| 送验对象 | 分支 `wp24-t03` @ `075b2f9d`（含首版 c0b62652＋缺陷修复 5afabe23＋T03b-1 草稿源 075b2f9d；base＝redesign-main @ 1b3a7371） |
| 验收方式 | 实施者同会话受权验收（独立性例外如实登记）；对抗复核＝全新重跑四测试套件＋真宿主两轮直载实测＋缩水扫描＋变异探针（resolveText 缺键 fail-fast 在真宿主实测为红，见 4.5） |
| **verdict** | **pass**（4.1~4.11 逐项通过；无阻断级问题；建议级 1 项＋收口余量清单转登） |

## 4.1~4.11 逐项证据

| 项 | 结论 | 证据 |
| --- | --- | --- |
| 4.1 改动范围 | 通过 | base..head 23+7 文件（ui 两公共头＋PluginUiRegistrar.cpp＋DomainAssembly.*＋UiPlugin 集成＋UiText 键族＋AboutDialog 迁移＋modeling 继承切换/装配 API/门面 assembly/＋CMake 两件＋三文档＋留痕）——登记载体＝ui.md v1.15/modeling.md v0.20/DTB v0.21 §2.25 T03 行拆段注记；framework 源码零触碰；T07 在制品（requirements）零触碰 |
| 4.2 可复现构建 | 通过 | 验收者本会话重跑：集成 ui/modeling 五目标构建零错误（ui_plugin DLL 重链在宿主进程释放后完成）；四测试套件 154/154＋22/22＋265/265＋17/17（XML×4＋ird-test-report.json 入库 traceability/builds/wp24-t03-first/acc/）；冒烟树 ui/modeling 测试目标零错误（154/22/215/8） |
| 4.3 契约 verify | 不适用（如实） | 无 canonical 契约（owner 指示增量拆段）；validate-docs PASS |
| 4.4 acceptance 条目 | 通过 | 登记验收标准核验：①装配契约头落位（§3.3 两行＋§10.9/§11 形状逐字段比对）✓；②白名单装配期固定（构造期不可变＋运行期无注册/注销——PluginUiRegistrar 实现无注销接口）✓；③建模真宿主挂位（真宿主直载：建模 Dock＋树填充＋中文按钮，截图 02/03 帧）✓；④UiText 解析（按钮键名→中文工程用语实证）✓；⑤P-MDL-8 消账（继承切换，签名零变化）✓ |
| 4.5 测试真实失败能力 | 通过 | **真宿主实测红**：UiText 键族未登记时（首版装配首次直载）resolveText 对 `cmd.modeling.new-from-template.title` fail-fast 弹错误对话框（截图 modeling-in-host-smoke-01.png 实录）——键登记修复后同链路复绿（smoke-02.png）：一红一绿同链路实证；另 4.5 编译探针：分支切换陈旧二进制现象（BuildGraph 误报）经强制重编排除，实证测试二进制对应真实源 |
| 4.6 架构红线 | 通过 | ird_gates 引擎直跑基线..branch 归一化 diff＝净新增恰 1 处 `SUB ui->modeling`（sdurws_ird_ui_plugin→sdurws_ird_modeling_plugin——ui→project 同族 DTB §4.5 装配特权边；登记册回填归 WP-01-T03）；ui 头零 project include（前向声明断环——NoCrossUnitInclude_O31_UI_BUILD 扫描过）；R-1 不破坏（业务域仅经 ui 公共头＋建模门面被装配层消费） |
| 4.7 注释规范 | 通过 | 抽查 4 文件（IPluginUiRegistrar.hpp/IPluginUiModule.hpp 文件头含断环与零 project include 纪律／PluginUiRegistrar.cpp 实现注／DomainAssembly.cpp 消费序注／ModelingUiModule 草稿源四方法 Doxygen）：§2 表高危信息（线程/所有权/生命周期/诚实边界）齐备 |
| 4.8 文档同步 | 通过 | ui.md v1.15（§3.3 两行＋§16.7 行＋收口余量④清单）＋modeling.md v0.20（继承切换＋门面＋P-MDL-8 翻转）＋DTB v0.21（T03 拆段注记）；validate-docs PASS |
| 4.9 commit 质量 | 通过 | `c0b62652`/`5afabe23`/`075b2f9d` 三提交标题动词开头中文摘要、正文四段齐备；5afabe23 为所有者实测反馈的缺陷修复（首刷＋受理反馈），修复描述与留痕一致 |
| 4.10 留痕完整 | 通过 | XML×4＋ird-test-report.json＋截图 3 帧＋门禁比对文件入库 traceability/builds/wp24-t03-first/（acc/ 子目录为本验收全新捕获） |
| 4.11 偷懒/缩水扫描 | 通过 | ①diff 范围 TODO/FIXME/stub/not implemented 零命中（grep 实测）；②"首版可见反馈面"（命令点击→"已受理…随收口接线"状态栏提示）为**登记过的诚实边界**（ui.md v1.15 ④收口余量清单），非缩水——不虚构执行成功；③无空实现（registrar 校验序/草稿源四方法/门面转接均实装）；④测试强度：154＋22＋265＋17 用例行为断言＋真宿主红绿探针 |

## 发现的问题（分级）

| 级别 | 描述 | 处置 |
| --- | --- | --- |
| 建议 | 命令提交出口首版为状态栏受理反馈（域执行面随 T03b 后续增量/ICommandService 网关装配）——使用方不得误读为命令已执行 | ui.md v1.15④ 收口余量清单已登记 |
| 建议 | 建模 Dock 为宿主侧 Left 区承载（中央区 StageId 挂位 CentralAreaHost 未落位） | 收口余量清单已登记 |

## 验收者环境说明

同会话受权验收（Windows 11 x64；VS2022 17.12.0；Qt 6.11.1；宿主直载经 run-ui-t18-view.ps1——F-320 PATH 前置）。所有构建/测试/门禁/宿主直载为验收者本会话亲手执行；发现并修复于验收周期内的两处装配缺陷（首刷缺失/出口未绑定）已随 5afabe23 入库并复测。
