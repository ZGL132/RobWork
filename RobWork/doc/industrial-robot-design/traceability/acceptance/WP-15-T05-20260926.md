# WP-15-T05 实施验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | WP-15-T05 实现批量任务点验证评估器（kin.task-points-batch） |
| 单元 | kinematics |
| 送验对象（冻结） | 70dbaa11243a8eb395ed877f1663379544cbe791（提交 2：文档/留痕；提交 1：4dee67fc 实现） |
| base | 1ae740cfab4e78187bc386c0550d224293802f2a（origin/redesign-main @ 验收时点） |
| diff 范围 | 1ae740cfab4e78187bc386c0550d224293802f2a..70dbaa11243a8eb395ed877f1663379544cbe791（28 文件，+8219/−34） |
| 验收日期 | 2026-09-26 |
| 验收者 | 独立上下文验收会话（全新上下文，未消费实施叙述；全部构建/测试/门禁/变异亲手复跑） |
| **verdict** | **pass**（4.1~4.11 全部通过；零阻断级；建议级 2 条已转登 findings.json F-393/F-394） |
| 验收者证据集 | traceability/builds/wp15-t05-acc/（34 件——本提交随附） |
| 验收环境 | Windows 10 x64；VS 2022（17.12.6）x64；Qt 6.11.1 msvc2022_64；vcpkg 经典模式（installed/x64-windows）；pwsh 7。复现现场＝`git worktree add --detach` @ 70dbaa11（送验 SHA，非分支名）；worktree 缺 gitignored `RobWork/RobWorkStudio/bin/RobWorkStudio.ini.template.static`，已从主检出**只读复制**后 configure（src/CMakeLists.txt:53-59 引用，T15/T16/T02~T09 实录同型现象）。构建前 tasklist 无 RobWorkStudio.exe/msbuild 残留进程。 |

---

## 4.1 改动范围 —— 通过

- `git diff --name-status 1ae740cf..70dbaa11` 实测 28 文件，逐一对号契约 allowedFiles：
  - `RobWork/RobWorkStudio/src/rwslibs/industrialrobot/kinematics/**` ×10（CMakeLists.txt 修改；Evidence.hpp/.cpp、TaskPointsBatchTest.cpp、TaskPointsBatchEvaluatorContractTest.cpp 新增；DiagCodes.hpp/.cpp、Evaluators.hpp/.cpp、DiagCodesTest.cpp 表尾追加/修改）；
  - `RobWork/doc/industrial-robot-design/traceability/**` ×17（builds/wp15-t05/ 留痕集）；
  - `RobWork/doc/industrial-robot-design/units/kinematics.md` ×1。
- forbiddenFiles（REQUIREMENTS.md / ARCHITECTURE.md）**零触碰**（不在 diff 文件清单）。
- SA-02：`industrialrobot/patches/` 无新增文件、无任何 patches 触碰——无未登记框架改动。

## 4.2 可复现构建 —— 通过（两模式均零错误，验收者亲手重配重跑）

- **集成模式**（全新构建目录，worktree A 源树）：`cmake -S RobWork -B <acc>/build_int -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=<repo>/vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64` → 配置成功（integration-configure.log）；缓存 grep 实证 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`；`cmake --build build_int --config Release --target sdurws_ird_kinematics sdurws_ird_kinematics_test sdurws_ird_kinematics_contract_test` → **exit 0、`error C`/`error LNK` 计数 0**（integration-build.log，962 行）。
- **独立冒烟模式**（另一全新目录）：`cmake -S RobWork/RobWorkStudio/src/rwslibs/industrialrobot -B <acc>/build_smoke -G "Visual Studio 17 2022" -A x64 -DCMAKE_TOOLCHAIN_FILE=.../vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`（toolchain 参数在案——CORE-T01 G-2 口径）→ 配置成功（smoke-configure.log）；全量 `cmake --build build_smoke --config Release` → **exit 0、零错误**（smoke-build.log，含 `sdurws_ird_kinematics.lib` 与两测试 exe 产物行）。
- 冒烟侧两测试目标复跑：**50/50＋34/34 全过**（smoke_sdurws_ird_kinematics_test.xml `tests="50" failures="0"`；smoke_sdurws_ird_kinematics_contract_test.xml `tests="34" failures="0"`）。

## 4.3 契约 verify 命令 —— 3/3 逐条亲手执行通过

| 契约 verify 条目 | 验收者执行口径 | 结果 |
| --- | --- | --- |
| `cmake --build build --config Release --target sdurws_ird_kinematics` | `build` 为主检出锁定构建树（另一并发会话在用，验收者不可写）——按 F-300/358 家族既定口径在验收者全新集成构建树以同参数同目标构建（含依赖链全量重配） | exit 0，零错误（integration-build.log） |
| `ctest --test-dir build/RobWorkStudio/src/rwslibs/industrialrobot/kinematics -C Release -L "^ird$"` | 同上构建树上逐字执行（仅 --test-dir 指向验收者树） | **exit 0：2/2 通过**（_test 50/50＋_contract_test 34/34，integration-ctest.log） |
| `pwsh -File RobWork/scripts/industrialrobot/validate-docs.ps1` | worktree A 内逐字执行 | **PASS（20 units / 12 trace / 189 task files）**（validate-docs.log） |

附带核对：`validate-task.ps1 -TaskFile tasks/foundation/WP-15-T05.json` → **PASS（1 tasks）**（validate-task.log）——证实送验留痕宣称。

## 4.4 acceptance 条目逐项证据 —— 5/5 通过（每条具名证据；未执行项无）

| # | 契约 acceptance 条目 | 验收者证据（具名测试/文件行号/命令输出） |
| --- | --- | --- |
| 1 | 批量展开与三态素材（appliesTo 过滤、disabled/None→NotApplicable 显式、去重、悬空引用→InputInvalid——V-12；per-item 五态独立、只算不判） | ①`ExpansionAppliesToNotApplicableAndDedup_WP15T05_ACC1`（contract_test L336-388）：AllStations/Stations/None 三工况×启用/停用两点＝5 工作项断言（Stations 清单外 p2 不产项、p1 三次登记去重为一项、None 两项 NotApplicable 且 reason 非空、计数守恒 5=5/0）；②`DanglingStationReferenceYieldsInputInvalid_WP15T05_ACC1`（L395-446）：悬空对象产 InputInvalid 工作项＋端到端 Evidence 行 `status=Invalid`＋`invalidReason.code == kKinPointRefDangling`（§9.6 行 16 消费面）；③`PerItemStatesIndependent_WP15T05_ACC1`（L453-503）：脚本求解器四结局→CandidateFound/NoConvergence/AllFiltered/**BoundExceeded** 四态互不污染＋逐态 presence（bestSolution/searchRecord/proofMaterial）；④实现复核 Evaluators.cpp L1444-1513（悬空引用先于 enabled/None 判定——数据缺陷不被停用吞掉，ERR-01）＋L1494-1510（三路预终结显式 reason）；⑤「不是工程判定」：EvaluationOutput 无任何判定字段，素材面齐备（Evidence.cpp build L260-369 全读——「只产不判」纪律落实，判定归 aggregateVerdict） |
| 2 | 必验工况覆盖/NotRun/组装前自检三重复核（无伪完成；EVI-02） | ①`CancellationMarksNotRunAndIncomplete_WP15T05_ACC2`（L557-603）：6 项 3 批、批 2 起点取消→processed=2/notRun=4/incomplete=true/completedBatch=1、NotRun 项 reason 全非空、caseCompletion.notRun=true；②`CaseCompletionMatrixMaterial_WP15T05_ACC2`（L510-554）：无取消全 executed、停用工况 notApplicable；③组装前自检双实现：评估器侧 Evaluators.cpp L1749-1756（totalWorkItems≠processed+notRun→`std::logic_error`）＋组装器侧 Evidence.cpp `validateComputationInvariants`（build() L271 复核）；④取消收尾 Evaluators.cpp L1672-1690（已解项终态保留、仅未派发项回写 NotRun——部分完成批不丢失） |
| 3 | 证据组装（EvidenceItem/SearchExhaustedRecord/DomainVerdictInputs REQ-06/payload canonical §5.6——§8.2 五行；搜索未果不产生违例——REQ-06） | ①`BuilderOutputShapeAndVerdictInputs_WP15T05_ACC3`（L610-699）：4 逐项行＋1 搜索未果汇总行、搜索聚合 budget=10/guesses=2、mustViolations/shouldViolations 各 1（Must/Should 分流、搜索未果零违例——C5/C8）、payload token/digest 一致性、完整批无 KIN-RESULT-INCOMPLETE；②`ProofProducerReboundToBatchKey_WP15T05_ACC3`（L706-732）：proof.producer==批量评估键＋契约版本、snapshotId/sliceId 保持请求原值（D-09 expectedSliceId 绑定）；③`PayloadHeaderBindingLayout_WP15T05_ACC1`（test/TaskPointsBatchTest.cpp L398-450）：IRDBP01 载荷 §5.6 绑定逐字段解析；④组装唯一组装点：评估器不自拼证据行（Evaluators.cpp L1376-1386：装配校验＋runBatchComputation＋builder.build 三段），NFR-MNT-04 落实 |
| 4 | execution 通道消费（能力声明三件套/取消周期查询每批至少一次/批粒度进度/黄金批大小 256——D-KIN-6；V-22 本单元侧） | ①`CapabilityDeclarationValues_WP15T05_ACC4`（L969-985）：supportsPause=false（R1 如实声明）＋checkpointGranularity=Batch＋forceTerminateCost=Cheap＋纯函数同值；②`PerBatchCancelProbeProgressAndWatermark_WP15T05_ACC4`（L739-783）：cancelCalls≥批数（每批至少一次）、progress 恰 3 次 phase="solve-batch" 单调至 100、watermark (k,3) 逐批递增序列；③`DefaultBatchSizeIs256_WP15T05_ACC4`（L789-818）：常量 256 断言＋300 项→2 批（256+44）实例化；④实现复核 Evaluators.cpp L1586-1670（批间查询点＋项起点探针承载 ARCH §4.4 的 2 s 停止派发界；watermark 推进只在批完成后） |
| 5 | 确定性（(pointOid,conditionId) 全序/连续区间分片/合并按全序——同线程数逐位、异线程数等价；§8.4；V-21） | ①`TotalOrderIndependentOfInjectionOrder_WP15T05_ACC5`（L825-875）：两种注入序 items 逐位同序＋caseSubset 规范化同序＋payload canonical 字节相等；②`ThreadCountEquivalence_WP15T05_ACC5`（L882-921）：threadCount 1↔4 payload 字节相等＋证据行序一致＋80 次替身调用计数守恒；③`ShardsAreContiguousRanges_WP15T05_ACC5`（L928-962）：替身记录线程 id——全序 [0..3]/[4..7] 各自同线程、两线程相异（连续区间断言）；④实现复核 Evaluators.cpp L1515-1517（全序排序唯一入口）、L1596-1649（分片=连续区间、各线程只写全序槽位、异常按分片序重抛首错） |

## 4.5 测试真实失败能力（变异测试）—— 通过（3 处实质变异全部真红；还原后全量复绿）

全部变异在验收者 worktree A 源树实施，每处变异后仅重建契约测试目标并跑具名用例，随后 `git checkout --` 还原；最终还原态重建后全量 50/50＋34/34 复绿（final-restore-rebuild.log、final_*_test.xml）。变异窗口与冒烟构建时序隔离（冒烟先行完成，T14 教训兑现）。

| 变异 | 实质篡改内容 | 期望变红用例 | 实测 |
| --- | --- | --- | --- |
| M1 | 移除 `runBatchComputation` 步骤 2 全序排序 `std::sort(workItems…workOrderLess)`（Evaluators.cpp L1517——篡改 §8.4 全序语义本体） | `TotalOrderIndependentOfInjectionOrder_WP15T05_ACC5` | **真红**（RUN exit 1，`[FAILED]`，mut1-sort-remove-run.log）；还原后该用例复绿（mut1-restore-run.log exit 0） |
| M2 | 跳过批 watermark 上报 `checkpointSink->batchWatermark(...)`（Evaluators.cpp L1659-1661——切断 P-KIN-7 最小端口接线） | `CancellationMarksNotRunAndIncomplete_WP15T05_ACC2`＋`PerBatchCancelProbeProgressAndWatermark_WP15T05_ACC4` | **双用例真红**（RUN exit 1，mut2-watermark-skip-run.log）；还原后双用例复绿（mut2-restore-run.log exit 0） |
| M3 | 黄金批大小常量 `kBatchDefaultBatchSize` 256U→128U（Evidence.hpp L89——篡改 D-KIN-6 锁定值） | `DefaultBatchSizeIs256_WP15T05_ACC4` | **真红**（RUN exit 1，mut3-batchsize-run.log）；还原后终态全量复绿（50/50＋34/34，final_*_test.xml） |

结论：相关断言锁行为而非恒真——全序/检查点端口/黄金常量三面均具真实失败能力。

## 4.6 架构红线（AGENTS §5 八条逐条）—— 通过

1. **L2 零 Qt**：新文件 6 个（Evidence.hpp/.cpp、Evaluators 增段、两测试文件、DiagCodes 增行）`grep -ci "qt"` 全 0；CMakeLists diff 无 Qt 触碰。
2. **R-1 业务域互链禁止**：新代码跨单元 include 仅 core/evidence/execution 公共头（`grep "#include <sdurws"` 清单逐行核对——无 requirements/modeling 等业务域单元头）；req 对象 schema 以 BatchTaskPoint/BatchCondition 宿主投影注入（Evidence.hpp 文件头注明纪律依据）。
3. **R-2 跨单元私有头禁止**：非系统 include 仅本单元 src/ 内共享 `CanonicalCodec.hpp`（同单元私有面合规）＋其余全为 `include/sdurws/ird/<unit>/` 公共头。
4. **SA-02 框架零修改**：见 4.1（patches/ 零触碰）。
5. **R-4 名称前缀拼接/剥离**：新代码零名称解析——对象一律 ObjectId（core::ObjectId.toCanonical 仅作诊断文本），名称解析未越权（归 runtime）。
6. **PA-1 权威唯一**：评估器/组装器纯计算（不写项目/不产生修订/不归档——归档与检查点写出经宿主端口 IBatchCheckpointSink 最小投影，P-KIN-7 处置如登记）；诊断码全部经 DiagCodes.hpp 常量（禁拼码——Evidence.cpp include 注释明示），无 DX-xxx 数字码。
7. **PA-2 不可变历史**：本任务零修订/历史写入面。
8. **不继承 old/ 实现**：diff 无 old/ 路径；实现自零按卡 §7.1/§8.2/§8.3/§8.4 编写。
- 附加：**ird_gates 门禁零新增零消除**（见下）；DiagCodesTest 封闭断言 15→16 码同步（kSection96FullTable/IsError/CategoryRows 三表＋行序逐位），卡 §9.6 行 16 与实现/测试三面对齐。

### ird_gates 引擎直跑 base..head 归一化比对（验收者独立复跑）

- 口径：`cmake -DIRD_ROOT=<industrialrobot 源根> -DIRD_SELFTEST_DIR=<tmp> -P cmake/ird_gates.cmake` 双侧直跑（base 临时 worktree @1ae740cf 已删，结论以归一化直方图附件佐证——base-hits-linenorm.norm/head-hits-linenorm.norm）；引擎自测（干净夹具＋7 类植入违例检出）双侧通过。
- 命中行归一化（剥绝对路径前缀＋剥 (Lnn) 行号＋剥 R-n/§n 编号）后 sort|uniq 直方图 **diff 为空——逐位一致：零新增命中、零消除命中**。
- 三套计数同注（F-378 口径）：①原始 IRD-GATE 发射行数 base=69/head=69；②归一化直方图去重条目 base=69/head=69（每条发射恰一行）；③引擎自报 `[ird_gates] 命中 69 项` base/head 同值。三套一致、增量均 0。
- 两侧 ird_gates 退出码均 1＝**存量命中既有状态**（DTB §4.5 登记例外未清零，本任务无清零义务）；无新增命中故无 sanctioned 核对事项。送验留痕附件（ird-gates-base-head-compare.md，145/69/69 口径声明）与本验收者独立结果同向一致。

## 4.7 注释规范（AGENTS §2）—— 通过

- **公共头全查**：`Evidence.hpp`（600 行全读）——文件头设计依据/背景说明/R-1 纪律/线程安全/确定性段齐备；每个枚举/结构/接口中文 Doxygen（业务概念、生命周期、非 owning 所有权、并发只读）；物理量单位逐字段显式（targetInBase 平移 m/旋转 rad、两容差 m/rad、批大小/线程数无量纲、referenceQ rad|m、去重阈值 rad|m 逐轴）；确定性来源（同输入同字节、定宽小端）；错误语义（调用方 fail-fast vs 评估期结构化素材分轨）；BatchItemStatus 七态与卡面五值偏差的对照与理由逐条注明（引用 §14.6 v0.5）。`Evaluators.hpp` T05 段（L474-759）同规范（BatchQuery 注入面纪律/descriptor 落值/工厂 O-37/能力声明 §8.3 原文对照、runBatchComputation @throws 两轨）。
- **实现文件抽查 ≥3**：`Evidence.cpp` 文件头（组装口径五点登记＋确定性来源）＋build()/makeItemEvidenceRow/编码函数逐步注释；`Evaluators.cpp`（装配校验/展开/全序/分批/取消/检查点/自检各段——「当前步骤＋业务含义＋失败走向」齐全，魔法数字全解释：256=D-KIN-6、探针粒度=V-22 批间查询点）；`DiagCodes.cpp` 行 16 增块（分类/级别/动作族落值依据逐条）；`DiagCodesTest.cpp` 同步段。测试文件头与用例注均含需求/AT 追溯（WP15T05_ACCn 显式编号）。
- §2.5 高危信息表七项（单位/坐标系 {B}/rad|m 制式/所有权/线程约束/错误语义/确定性来源）在抽查面全部在注。
- 反面示例零命中：无复述式废注、无 TODO 占位、无过期注释、无中英机翻腔。

## 4.8 文档同步 ＋ findings 未关闭条目相关性 —— 通过（附 2 条建议级转登）

- **卡面同步五项逐一核对**（units/kinematics.md diff）：①卡头版本 v0.4→v0.5（F-369/F-374 教训兑现）；②§9.6 表尾追加行 16 `KIN-POINT-REF-DANGLING`（error——V-12 语义、任务列 T05）；③§4.3 kin.task-points-batch 行标注实现键 kebab 词形；④§11 T05 行标注已完成；⑤§14.6 v0.5 五项登记（值模型/评估器执行口径/组装器口径/偏差登记/测试结果）。
- **per-item 状态增补两值（BoundExceeded/NotRun）的契约依据**：DTB §5.4 偏差登记路径——§14.6 v0.5 ④逐条给出理由（结局 5 素材承载态的三态完备性论证；NotRun 为 §7.1/§9.2 @post 明文要求的取消终态），余五值逐字保留；实现/测试/卡面三面一致（变异 M1 面亦证实全序与状态机测试真实约束）。登记合法。
- **F-380 处置兑现（验收者以代码复核）**：base DiagCodes.cpp 实测 `DiagnosticSeverity::Error`=6/Warning`=9（散文误写 4/11——F-380 原判定属实）；head 实测 7/9 且三处散文（DiagCodes.hpp 严重级别列注、DiagCodesTest 两处）纠正为「T02 面 6 error＋9 warning、T05 后 7 error＋9 warning」；`grep "4 error＋11 warning"` 全单元**零残留**；DiagCodesTest 封闭断言 16 码三表同步。处置属实，findings.json 状态翻转（open→fixed）留待治理收尾簿记（登记于 F-394 owner）。
- **未关闭条目相关性核对**（findings.json @head 计 385 条、max F-392）：
  - F-380：本任务兑现（见上）；
  - F-387（v0.3 ⑧散文「零新增命中」少报 sanctioned）／F-390（v0.4 ⑥「15 新用例」实为 18）：owner 指令均为「下次触及卡面时纠正」——**本任务触及该卡但未顺带执行**，两处散文原样（不阻断；转登 F-394 督办）；
  - F-386/F-389/F-391（KIN-NO-DEVICE 守卫/KIN-RESIDUAL-EXCEEDED 自然触发用例/去重反范数数据）：消费面本任务未触及（批量通道诊断面收敛为 KIN-RESULT-INCOMPLETE/KIN-POINT-REF-DANGLING 两码，残差对比走要求值行而非诊断），维持 open、无消账空间；
  - 其余 300+ open 条目与本任务（kinematics 批量通道）无消费面交集。
- **本验收转登（建议级，编号顺延 max F-392→F-393/F-394，随本提交入 findings.json，经 ConvertFrom-Json 校验 387 条）**：
  - **F-393**：卡 §14.6 v0.5 ⑤「KinTaskPointsBatchEval 12 新契约用例」与实测不符——实际 14 个 TEST（base 契约目标 20 例、head 34 例＝20＋14；枚举亦仅列 13 项），散文计数家族新例（少报方向、XML 与实现提交正文「14 新用例」准确、无粉饰）；
  - **F-394**：F-387/F-390 的「下次触及卡面时纠正」指令在本任务触及卡面时未兑现（v0.3 ⑧/v0.4 ⑥散文原样）——消账督办登记＋F-380 状态翻转归治理收尾。
- 契约/追踪矩阵：契约 JSON 本 diff 未触碰（状态翻转属 PIPE §4.7 收尾动作）；validate-task PASS、validate-docs PASS。

## 4.9 commit 质量 —— 通过

- 两提交标题均为 `[WP-15-T05]` ＋动词开头（『实现批量任务点验证评估器 kin.task-points-batch』『同步单元卡 v0.5 登记与验证留痕』）——F-392 所载 T04 标题家族问题本批已纠正。
- 正文四段齐备（What 按文件/模块列点；Why 对号契约 acceptance/卡章节/F-380；How 含两模式构建/ctest/XML 计数/ird_gates 三套计数/validate 双脚本；影响面含红线不变式声明）；`Acc-Covered: 1,2,3,4,5` 消账标注（实现提交）。
- **验证段与验收者亲测逐项一致，零粉饰**：50/50＋34/34（我方 XML 同值）、ctest 2/2、ird_gates 零新增零消除（我方直方图 diff 空）、validate 双 PASS、F-380「三处散文纠正」（我方 grep 零残留复核）。代码与测试/文档/留痕同一提交链（4dee67fc 实现＋70dbaa11 文档留痕），可独立回溯。

## 4.10 留痕完整 —— 通过

- 送验留痕集 `traceability/builds/wp15-t05/`（20 件）随送验提交在库：gtest XML ×2（tests=50/34、failures=0、与我方独立运行同值）、ird-test-report.json 双份（total=50/passed=50；total=34/passed=34）、集成构建/ctest 日志、冒烟配置/构建日志、四份运行日志、ird_gates 比对附件（compare.md＋两侧日志＋归一化命中表 base/head_hits.norm＋base-head.diff 0 行＝归一化空 diff 的证据本体）。
- 验收者独立证据集 `traceability/builds/wp15-t05-acc/`（34 件）随本记录提交：两模式 gtest XML ×4、变异还原复绿终态 XML ×2、ird_gates base/head 直跑日志 ×2＋验收者口径归一化直方图 ×2、集成/冒烟配置与构建日志 ×4、validate-docs/validate-task 日志 ×2、变异构建/运行日志 ×8、终态复绿日志与运行日志 ×5。
- phase-one-validation.log/resultNote：本任务属阶段 B 单元实现批次，按 T02/T03/T04 既定口径以 ird-test-report.json 为结果账本（resultNote 面齐备）。
- 本记录计数全部来自附件原样文件与实测输出（XML `tests=` 属性、直方图行数、引擎自报行），无手工转抄。

## 4.11 偷懒/缩水实现扫描 —— 通过

- ①diff 新增行 `TODO|FIXME|HACK|stub|placeholder|未实现|not implemented` 扫描**零命中**（`TODO(WPnn)` 合法形态亦无）；留痕件 `base-head.diff` 0 字节为「归一化命中表空 diff」的证据本体（compare.md 明示语义），非占位桩。
- ②空实现检测：新增公共接口（IKinematicEvidenceBuilder.build/KinematicEvidenceBuilder 实现、runBatchComputation、taskPointsBatchCapability、encodeBatchPayloadCanonical、IBatchCheckpointSink 端口）实现体全部为实质逻辑（4.4 逐条行为证据＋4.5 变异真红佐证），无「仅返回默认值」面；IBatchCheckpointSink 为纯虚端口属卡面登记的最小消费接口（P-KIN-7 处置），非缩水。
- ③acceptance 逐条证据表见 4.4（5/5 全有具名证据，无「已实现」声明依赖）。
- ④测试强度：断言校验行为与边界（三态素材计数、presence 纪律、取消计数脚本、watermark 序列、线程 id 连续性、payload 字节等价），非恒真——4.5 三处实质变异全部真红实证。

---

## 发现问题清单

### 阻断级

无。

### 建议级（已转登 findings.json，不阻塞合入）

| 编号 | 一句话 | owner |
| --- | --- | --- |
| F-393 | 卡 §14.6 v0.5 ⑤「12 新契约用例」实为 14（20＋14=34 XML 实证）——散文计数家族新例（少报） | kinematics 实施会话（下次触及卡面纠正） |
| F-394 | F-387/F-390 的「下次触及卡面纠正」指令在本任务触及卡面时未兑现——消账督办；F-380 代码面已兑现、状态翻转归治理收尾 | kinematics 实施会话＋治理收尾会话 |

## 裁决

**verdict = pass**：4.1~4.11 全部通过、零阻断级问题；建议级 2 条已按 ACC v1.2 §5 转登 findings.json（387 条，max F-394）。送验对象 70dbaa11243a8eb395ed877f1663379544cbe791 可进入所有者合入环节；本验收者不合入、不修改实施产物、未向任务分支推送任何提交（送验对象保持冻结；origin/wp15-t05 复核＝70dbaa11，零分支漂移）。
