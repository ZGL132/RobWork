# UI-T13 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | UI-T13（WP-10-T13）诊断/确认/任务呈现模型与 CommandInteractionBridge |
| 送验对象（冻结） | `6b7ffbc33e9c5bc597d2d68b252548f386925212`（分支 `wp10-t13`；base `7b62d221b338db7fd01a07724da0b7e580b12628`＝state.base＝UI-T12 合入后） |
| 冻结核对 | 验收首查 `origin/wp10-t13`＝`wp10-t13`＝送验 SHA，零分支漂移；验收全程按 SHA（detached worktree），未向任务分支推送任何提交，未修改任何实施产物 |
| 验收依据 | acceptance-protocol.md v1.5（ACC §2 独立性／§3 执行环境／§4 清单 4.1~4.11）；全新上下文，只消费产物与证据，构建、测试、门禁、validate-task 全部亲手重跑 |
| **裁决** | **pass**（4.1~4.11 全部通过，零阻断级问题；3 条建议级问题已按 F-284/F-285/F-286 转登 findings.json，不阻塞合入） |
| 验收日期 | 2026-09-22 |

## 一、逐项证据（ACC §4，4.1~4.11）

### 4.1 改动范围 —— 通过

- `git diff --name-status 7b62d221..6b7ffbc3` 共 27 文件：ui 单元 14（`ui/CMakeLists.txt`、3 公共头 `IDiagnosticPresentationModel.hpp`/`ITaskPresentationModel.hpp`/`CommandInteractionBridge.hpp`、2 既有头增量 `UiPorts.hpp`/`UiProjections.hpp`、3 实现 `DiagnosticPresentation.cpp`/`TaskPresentation.cpp`/`CommandInteractionBridge.cpp`、`UiText.cpp` 键族增量、3 模型测试、1 契约测试）＋traceability 留痕 12＋`units/ui.md` 1——全部落在契约 `allowedFiles`（`ui/**`、`units/ui.md`、`traceability/**`）内。
- `forbiddenFiles`（REQUIREMENTS.md／ARCHITECTURE.md）零触碰；diff 内框架源码（RobWork/RWS）与 `patches/` 零文件——SA-02 无登记需求。
- 范围内提交 2 个：`e727fa26`（[WP-10-T13] 实现＋测试＋CMake 同一提交）、`6b7ffbc3`（[units] ui.md v1.5 落位登记＋留痕归档）。

### 4.2 可复现构建 —— 通过（双模式从零亲手复现）

- **集成模式**：worktree A（`D:/10_Source_Repos/21_robot/wt-acc-uit13-a`，`git worktree add --detach @ 6b7ffbc3`）。全新配置 `cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=<主树>/vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`——第二次配置 EXIT=0（Configuring done，零 CMake Error）；缓存 grep 实证 **`RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`**。首次配置失败根因＝worktree 缺主树 git 未跟踪本地辅助件 `RobWorkStudio.ini.template.static`／`.ini.shared.in`（.gitignore `[Bb]in/` 吞没——F-007 在册缺陷，UI-T11/T12 验收同先例），自主树补拷至 worktree `RobWork/RobWorkStudio/bin/`（环境修复，不触及送验产物）后配置通过。
  - `cmake --build build --config Release --target sdurws_ird_ui_test` → **EXIT=0**；`--target sdurws_ird_ui_contract_test` → **EXIT=0**；另建 `sdurws_ird_ui_gui_test` → **EXIT=0**。三日志 `error C[0-9]` grep 计数均 **0**（日志 `acc-uit13-build-ui-test.log`/`-ui-contract.log`/`-gui.log`，验收者工作目录留存）。
- **独立冒烟模式**：`acc-uit13-smoke`（源＝worktree A 同 SHA 的 `industrialrobot/` 目录；带 vcpkg toolchain 绝对路径＋Qt6_DIR，AGENTS §4.1 口径）——配置 EXIT=0、全量构建 **SMOKE_BUILD_EXIT=0**（error 计数 0），产出全部单元目标含 `ui/Release/sdurws_ird_ui_test.exe` 与 `sdurws_ird_ui_contract_test.exe`。
- 测试 exe 验收者亲跑（Qt bin 挂 PATH）：集成树 `sdurws_ird_ui_test.exe` → **151/151 PASSED**；`sdurws_ird_ui_contract_test.exe` → **17/17 PASSED**；冒烟树两 exe → **151/151＋17/17 PASSED**——与送验留痕（ird-test-report.json 151/0、contract 17/0、smoke 151/0＋17/0）同口径一致。

### 4.3 契约 verify 命令 —— 通过（逐条独立执行）

1. `cmake --build build --config Release --target sdurws_ird_ui_test`：exit 0（集成树亲跑）。
2. `cmake --build build --config Release --target sdurws_ird_ui_contract_test`：exit 0（集成树亲跑）。

### 4.4 acceptance 条目 —— 通过（5/5，逐条具名证据）

| 契约 acceptance 条目 | 具名证据（均在验收者亲跑 151＋17 内 PASSED；行号＝测试文件实测行） |
| --- | --- |
| ①UI-DIA-1＋诊断呈现（§9.1）：过滤/稳定排序/聚合折叠呈现、原因链展开、动作映射表全路由不直调领域服务、三要素数值＋单位同显与四态占位、恢复横幅 PM-15、Tier-U 面板 flush 仅关闭路径、脱敏仅配置界面、ui 不改诊断事实、取消无错误呈现 UX-03 | 过滤/折叠：`QueryForwardsFilterAndStableOrder_UI_DIA_1`（DiagPresentationModelTest.cpp:238——查询透传＋稳定序）、`DedupFoldShowsOccurrences_UI_DIA_1`:265（occurrences 计数呈现）；原因链：`ExpandChainWalksCausedBy_UI_DIA_1`:284（经 chainOf 缝真实上溯）＋`ExpandChainDegradesWithoutSeam_UI_DIA_1`:317（未注入退化"自身即根"零虚构——缝已按 §16.7 v1.5 单侧冻结登记，契约 acceptance ⑤ 同条 sanction）；动作映射：`ActionMappingTableFrozen_UI_DIA_1`:341＋`ActionMappingCoversAllCategoryTokens_UI_DIA_1`:364（§4.4 全动作族 totality——实现 DiagnosticPresentation.cpp:44~85 逐 token 注释出处，未知族兜底 None）；呈现映射：`PresentationRouteCancelHasNoErrorSurface_UX_03`:398（取消→NoErrorPresentation）；三要素：`ComparisonValuesUnitCoDisplayAndPlaceholders_UI_DIA_1`:423（Provided＝"数值 单位"同显、NotApplicable/Invalid/NotProvided 占位不伪造——实现 :134~177 四态逐支）；恢复横幅：`RecoveryBannerSummaryAndActions_PM_15`:477（一句话按紧迫度择一＋固定三动作）；日志面板：`UserLogPanelSnapshotAndDevJump_UI_DIA_1`:510（模型无 flush 面——`assembleUserLogPanel`/`UserLogPanelSnapshot` 结构性无 flush 成员，Dev 级仅跳转位）；脱敏：`RedactedPathForwardsService_NFR_SEC_07`:557（只读转发＋装配缺服务 fail-fast）；订阅 Marshal：`SubscribeMarshalToUiThreadAndUnsubscribe_M_1`:595（目录通知线程→UI 线程＋退订丢在途）；pending 只读经端口：`PendingConfirmationsViaPortOnly_UI_CMD_4`:536 |
| ②UI-CMD-4~7（§9.2/§9.3 确认流）：Bridge 实现 ui 自有 IUiCommandInteraction（C-6 外翻形态）、命令线程同步回调→Marshal→阻塞等待、桥内零写入口、isAlive 探针→Aborted(interaction-lost)、未确认不提交、输入变化失效标注、关闭窗口 Aborted 无用户诊断、ui 只交 credentials、principal 会话缓存 P-UI-4 | O-31 结构自证＋任意线程探针：`BridgeImplementsUiInteractionPort_O31`（InteractionTaskContractTest.cpp:184——`is_base_of` 静态断言＋基面虚派发＋旁路线程读）；Marshal 线程契约：`PresenterInvokedOnUiThreadOnly_M_1`:213（`callThread == QThread::currentThread()`）；确认凭据：`ConfirmedFlowProducesCredentials_UI_CMD_5`（CommandInteractionBridgeTest.cpp:189——凭据与 findings 一一对应＋注入时钟 confirmedAtUtc）；拒绝无凭据：`RejectedFlowYieldsNoCredentials_UI_CMD_4`:224（nullopt——对端 Rejected(confirmations-rejected) 无修订语义由端口契约承载 UiPorts.hpp 增注）；空集短路：`EmptyFindingsShortCircuitWithoutDialog`:243；拆除唤醒：`DismantleWakesBlockedCommand_UI_CMD_7`:261（旁路线程拆除→等待方 nullopt＋短路径复验）；无超时：`NoAutoTimeoutWhileWaiting_P_DIAG_6`:299（50 轮泵送仍未返回＝P-DIAG-6）；失效标注：`RevisionCommitAnnotatesStaleInput_UI_CMD_6`:340（代次判别＋实时标注通道＋批级横幅键）；补全双层：`EnrichmentFillsRecordFieldsAndDevFold_UI_CMD_5`:379＋`DefaultAssemblyWithoutEnricherZeroFabrication`:423（缺行零虚构；Dev 折叠区只带命令类型 token 零哈希——UX-02）；装配 fail-fast＋principal 缓存：`FactoryFailFastAndPrincipalCached_P_UI_4`:156；桥内零写入口＝结构事实（CommandInteractionBridge.cpp 全文只触碰 presenter 与内部等待状态，零端口调用——验收者逐行复核）；线程模型四步＝实现 :158~274 逐步注释（条件变量等待谓词＝决议∨拆除、决议优先——§9.2 关闭窗口行"用户意图已表达→照常放行"） |
| ③UI-TSK-1~5（§9.4/§9.5 任务呈现）：9 态短标签＋当前置顶、200 ms 轮询三规则、能力启用反馈不静默、强杀独立确认、崩溃重跑入口、归档子标、当前性徽标无「正式通过」、后台只读 P-UI-7、UI 线程不阻塞等待 | 进度三规则：`ProgressMonotonicDropRegressionAndIdempotent_UI_TSK_5`（TaskPresentationModelTest.cpp:228——单调/回退丢弃/幂等）＋`PollingDropsRegressionWithDevLog_UI_TSK_5`:258（轮询回退丢弃＋Dev 留痕）；置顶与标签：`CurrentTaskFirstOrderingAndStateLabels_UI_TSK_1`:304；即发即忘＋强杀确认链：`ControlRequestsFireAndForget_UI_TSK_1`:327（requestForceTerminate 未确认→显式拒绝不发对端）；暂停反馈双层：`PauseOnUnsupportedTaskGivesExplicitFeedback_TASK_01`:364（端口反馈透传＋`ui.task.pause.unsupported` 兜底——不静默）；后台只读：`BackgroundTasksReadOnlyAfterClose_P_UI_7`:402（backgroundRejected＋reasonKey）；崩溃重跑：`WorkerCrashRowHasRerunEntry_UI_TSK_2`:435（Failed/Interrupted→rerunAvailable）；归档子标：`CompletedBadgeCarriesArchiveSublabel_UI_TSK_3`:466（ArchiveFailed→警告位）；当前性徽标：`CurrentnessBadgeSupersededWithReasons_UI_TSK_4`:493（搬运不计算＋原因逐条＋词表只有"当前/已过期"零「正式通过」）；装配 fail-fast：`FactoryFailsFastWithoutTaskPort`:524；契约面到达序：`TaskPortControlOrderAndProgressProtocol`（InteractionTaskContractTest.cpp:239）＋`ControlRequestsAreFireAndForget_NFR_PERF_02`:276＋`FindingQueryPortFeedsBridgeEnrichment`:308；默认 200 ms＝`kDefaultProgressPollInterval{200}`（ITaskPresentationModel.hpp:368 显式常量）；UI 线程不阻塞等待＝接口结构事实（控制四请求全部即发即忘返回 Ack，无等待形态——验收者逐行复核） |
| ④sdurws_ird_testkit_qt 首消费者理由登记（卡 §13 行原文、ui.md §12.1） | ui.md §12.1（:1470）原文在位：「`sdurws_ird_testkit_qt` 启用登记（testkit.md §3.5/§10.1）：本单元为首个消费者（WP-10-T11 需 Qt Test 辅助：事件循环驱动、控件探针），理由已在 UI-T13 任务卡登记；该目标禁止进入生产计算库依赖（testkit 红线）」；§16.7 v1.5 行（:1769）同句登记；契约卡 §13 行（tasks/foundation/UI-T13.json acceptance[3]）原文一致；§13 UI-T13 落位登记注（ui.md:1578 ⑥段）复述登记——三处同源零失实 |
| ⑤O-31 处置（已裁决随整链放行：对端类型经 ui 自有端口＋投影承载、产品面零对端链接/include、P-PR-7 冻结点实现、P-UI-4/P-UI-7 建议口径、P-DIAG-6 承接维持） | 零 include：六个新增/触碰产品文件 include 全集＝std＋core（DiagData/Identity/Evaluation/Events/Provenance）＋diagnostics（Catalog/Confirmable/DiagCodes）＋ui 自有头——零 project/execution/evidence/policy/runtime/业务域（验收者逐文件 grep 实证，正则扫描零命中）；零链接：`target_link_libraries(sdurws_ird_ui PUBLIC sdurws_ird_core sdurws_ird_diagnostics Qt6::Core Qt6::Gui Qt6::Widgets)`（CMakeLists.txt:197~202）；常驻守卫 `NoCrossUnitInclude_O31_UI_BUILD` 亲跑 PASSED（151 内）；门禁验收者独立复跑：逐码计数与留痕 hit-set 完全一致（95 条：LIB×8/R-1×1/R-3×37/R-4×3/R-5×1/SUB×39/T-1×5/T-2×1），R3 恰增 4＝两个新实现 TU 的 Marshal Qt 面（ui 本身是 R-3 例外本体目标，F-259 同型机器面滞后，登记件 wp10-t13-gate-registrations.md 在位），其余七码零变化；P-PR-7：线程模型按 project.md §5.3.3 冻结点实现（端口形状逐字镜像——UiPorts.hpp IUiCommandInteraction 注释逐条对照 §9.2 类块），P-DIAG-6＝`NoAutoTimeoutWhileWaiting_P_DIAG_6` 测试钉住；P-UI-4＝principal 构造采集缓存（`FactoryFailFastAndPrincipalCached_P_UI_4`）；P-UI-7＝`BackgroundTasksReadOnlyAfterClose_P_UI_7` |

- 未执行项：无——五条 acceptance 全部有具名测试与命令输出；`ird_gates` 非 acceptance 行，已随 4.6 亲跑（非"未执行"项）。

### 4.5 测试真实失败能力 —— 通过（两处变异构造，均如期变红，已还原复绿）

1. **MUT-1**（`src/TaskPresentation.cpp` mergeProgressDecision 回退分支 `DropRegression`→`Accept`，删除单调规则效果）：重建后 `--gtest_filter=TaskPresentationModelTest.ProgressMonotonic*:TaskPresentationModelTest.PollingDrops*` → **2 FAILED**（ProgressMonotonicDropRegressionAndIdempotent_UI_TSK_5、PollingDropsRegressionWithDevLog_UI_TSK_5）——§9.4 进度单调规则真实被测。
2. **MUT-2**（`src/DiagnosticPresentation.cpp:103` diagnosticRouteFor 的 `Canceled` 分支 `NoErrorPresentation`→`FailureWithObjectFocus`）：重建后同 filter → **PresentationRouteCancelHasNoErrorSurface_UX_03 FAILED**——UX-03"正常取消无错误呈现"真实被测。
3. 还原与复绿：两处变异 `git checkout --` 还原（`git status --porcelain` 零残留）→ 重建 exit 0 → 全量复跑 **151/151 全绿**。

### 4.6 架构红线（AGENTS §5 八条）—— 通过

1. **L2 零 Qt**（ui 为界面单元，适用形态＝模型层头文件零 Qt＋Widgets 仅 GUI 面）：三个新公共头（IDiagnosticPresentationModel/ITaskPresentationModel/CommandInteractionBridge .hpp）零 `Q` include（grep 计数 0，Marshal 上下文以私有嵌套类前置声明承载）；Qt 面仅在两个实现 TU 的 Marshal 半区（QMetaObject/QObject——ui 属 R-3 例外本体目标）；TaskPresentation.cpp 零 Qt。
2. **R-1 业务域互链**：无 modeling/requirements/kinematics/trajectory/dynamics/selection/optimization 依赖；对 execution/project/evidence 的协作全部经 ui 自有端口（IUiTaskPresentationPort/IUiCommandInteraction/IUiFindingQueryPort——C-8/C-6/C-9 首消费冻结）＋值投影（TaskViewProjection 族，语义锚不重定义）；SUB 命中 39 项与 base 一致零新增。
3. **R-2 跨单元私有头**：include 只指向 `include/sdurws/ird/<unit>/` 公共头（core×5、diagnostics×3——均公共面）；验收者正则扫描六个新增/触碰文件零私有头命中。
4. **SA-02 框架零修改**：diff 零框架文件、patches/ 零新增（见 4.1）。
5. **R-4 名称前缀**：新增产品文件 `RobWork` 字面量 grep 计数 0（命中仅为注释引用需求编号形态的零星——逐条核对均为文档注释非代码语义）；名称解析语义未在本单元实现。
6. **PA-1 权威唯一**：呈现模型零再加工（过滤/去重/稳定序全在 sink——query 透传；当前性徽标搬运不计算；九态/归档相位只投影不重算）；确认判定权不在 ui（只交 credentials）；任务状态权威在 execution（模型唯一"计算"＝§9.4 明文的呈现纪律：进度乱序处置＋清单排序）。
7. **PA-2 不可变历史**：本任务零修订数据结构触碰、零写接口（诊断只读、任务控制即发即忘、确认只产凭据不留痕）。
8. **不恢复 old/**：diff 与新增文件 `old/` 引用 grep 计数 0。

### 4.7 注释规范 —— 通过（公共头全查＋实现文件抽查 3/3）

- **公共头全查（3/3）**：三个新公共头均有完整文件头注释（设计依据逐条锚定 ui.md §9/§10.7/§10.8/§3.4/§3.5＋需求编号＋契约卡）；每个类/结构体/枚举有 Doxygen 中文职责说明（业务概念、生命周期归属、线程约束——如 CommandInteractionBridge 类注释四步线程模型、IUiCommandInteraction 端口"为什么自有端口"的 O-31 依赖倒转论证）；每个函数 `@brief`＋参数 `[in]` 方向语义＋返回值三态语义＋`@throws` 触发条件（如 createDiagnosticPresentationModel 的 fail-fast 双校验）；§2.5 高危信息齐备：单位（percent [0,100] 无量纲显式标注、ms 轮询周期常量）、线程（任意线程原子读/UI 线程调用/命令执行线程同步回调逐处标注）、所有权（Deps 指针非 owning＋生存期契约、观察者析构前先退订）、错误语义（调用方错误 fail-fast vs 环境错误空集＋Dev 日志）、确定性来源（taskRowLess 稳定全序 NFR-COR-02、locale::classic 数值面孔）。
- **实现文件抽查（3/3）**：DiagnosticPresentation.cpp（517 行）、CommandInteractionBridge.cpp（402 行）、TaskPresentation.cpp（551 行）——文件头四要素齐备；每个语义段有"为什么"级注释（如 requestConfirmations 四步逐步注释＋死锁规避论证"长操作绝不持锁，否则等待谓词拿不到互斥量＝死锁"：CommandInteractionBridge.cpp:188~191；expandChain 四步算法注释 :356~412；magic number 均有出处——6 位有效数字与 UiText 格式约定同源 :141~143）。
- 无过期注释/机翻腔/复述代码形态命中（验收者通读复核）。

### 4.8 文档同步 —— 通过

- **单元卡增量修订（DTB §5.4）**：ui.md v1.4→v1.5，三处登记齐全——§3.3 公共头布局三行落位增注（:221~223）＋UiText 键族⑧增注（:228）；§13 UI-T13 行后落位登记注（:1578，七段全量登记①~⑦）；§16.7 v1.5 行（:1769）。登记内容与实现逐项对照零失实（验收者抽查 6 组声明均实证：三头落位、端口冻结、键族 32 键、测试计数、门禁恰增 4、GUI 27/28）。
- **偏差登记**：①expandChain 链查询缝（diagnostics §9.7 投影不携带 causedBy——L5 经 CauseLink::chainOf 供给，未注入退化"自身即根"）已按单侧冻结登记于 §16.7 v1.5＋契约 acceptance ⑤＋头文件 deps.chainOf 注释——sanctioned deviation，非隐性缩水；②§10.7/§10.8 原文对端类型（execution::TaskSnapshot 等）按 O-31 机制以 ui 投影承载，接口"形状"随首消费冻结并随本版增量修订登记——同上 sanctioned。零未登记偏差。
- **任务状态/追踪矩阵**：契约状态翻转与 state.json 属收尾（PIPE §4.7）编排者职责，不在送验对象内；trace-matrix.json 为 DOC 期需求覆盖矩阵（generatedAt 2026-09-10，先例同口径不逐任务更新）；需求语义零变化（REQUIREMENTS.md 未动——4.1 实证）。
- **findings.json 未关闭条目相关性核对（ACC §4.8 v1.2）**：登记册共 283 条、open 247 条。与本任务相关且本次处置如下：**F-259**（R-3 例外头包含半区机器面滞后，责任方 WP-01-T03）——本任务新增 2 实现 TU 恰增 4 处 R3 命中，属该家族新例，已按先例登记提交件 wp10-t13-gate-registrations.md 并在 §16.7 v1.5 复述，家族责任方不变，不另开新号；**F-278/F-280/F-281**（UiPorts.hpp/UiSessionController.hpp 尾换行缺失）——本任务增量修改 UiPorts.hpp 后尾换行仍未恢复（尾字节 0x50 'P' 实证），构成家族复踩，转登 **F-285**；**F-007**（RobWorkStudio.ini 模板被 .gitignore 吞、全新 worktree 配置必卡）——本次验收实测复现并以同先例方式绕过（主树补拷），家族在案不重复登记；**F-010/F-283 家族**（.gitignore `*.log` 吞留痕日志）——本任务提交信息与登记件引用的原始日志未入库（详见 4.10），转登 **F-284**。其余 open 条目与本任务改动面零相关（抽核 F-004/F-005/F-006/F-008/F-012/F-015/F-016 等均属他单元/治理域）。无"可随本任务顺手消账"而未消账的条目。

### 4.9 commit 质量 —— 通过

- `e727fa26`：标题 `[WP-10-T13] 实现诊断/确认/任务呈现模型与 CommandInteractionBridge`（动词开头、≤50 字）；正文四段齐备（What 按文件/模块列要点／Why 逐条对应契约 acceptance 1~5＋knownPitfalls 处置／验证 含双模式构建、门禁恰增 4 的如实披露、151＋17＋27/28 含 1 项既有环境失败的如实定性／影响面 "既有行为零变化＋冻结形状未动"）。
- `6b7ffbc3`：标题 `[units] ui.md v1.5：UI-T13 落位登记…＋门禁与测试留痕归档`；四段齐备；验证段如实转引前一提交测试结论。
- **验证结论与留痕一致性**：151/17/27-28 三组数字与留痕 XML/report 及验收者独立复跑完全一致；门禁"恰增 4 处 R3"与验收者独立复跑逐码一致；GUI 失败定性（UI-T12 已登记剪贴板占用、与本任务零耦合）经验收者本机复现同根因（COM 0x800401d0 OpenClipboard 失败——详见 4.10），**无粉饰**。唯一瑕疵＝该提交声称归档的控制台/gates/validate 日志实际未入库（.gitignore 吞没）——已转登 F-284（建议级，结论本身不受影响）。

### 4.10 留痕完整 —— 通过（结论面完整；原始日志缺席已转登 F-284）

- `traceability/builds/wp10-t13/` 实际入库 11 件：gtest XML×3（sdurws_ird_ui_test.xml＝tests 151/failures 0；contract＝17/0；gui＝28 总/1 failure）＋ird-test-report.json×5（模型集成 151/151 decisive:true、契约集成 17/17、GUI 27/28、模型冒烟 151/151、契约冒烟 17/17）＋gates-hit-set×3（hit-set.txt 逐码计数、hit-set-diff.txt 3c3 R3 33→37、files-diff.txt 恰增两新 TU）。
- 验收者独立复现比对：模型/契约双模式 151＋17 全绿一致；GUI 27/28 且失败同一用例同一根因；门禁逐码 95 条完全一致；validate-task 亲跑 **PASS (1 tasks)**（worktree A，`pwsh -File RobWork/scripts/industrialrobot/validate-task.ps1 -TaskFile <契约>`）。
- 提交信息与 gate-registrations.md 引用的 `ird_gates-integration/base/smoke.log`、控制台日志×5、`validate-task.log` 未实际入库（.gitignore `*.log`——F-010 家族根因）→ 转登 **F-284**；hit-set×3＋XML×5＋report×5 已足以支撑结论复核（验收者据此逐项复现成功），结论面完整。
- `phase-one-validation.log` 为 WP-03 时代聚合日志，未逐任务更新——UI 系先例（UI-T11/T12 同态），非本任务引入的缺失；per-task 留痕本体在 builds/wp10-t13/ 与本记录。

### 4.11 偷懒/缩水实现扫描 —— 通过

- ①**标记扫描**：diff 全量 `TODO|FIXME|stub|placeholder|未实现|not implemented|XXX|HACK` 增量行扫描——产品源码（src/include/CMakeLists）**零命中**；命中仅为两类合法形态：测试替身类名（StubPresenter/RedactionStub/FindingQueryStub/UserLogStub/StubTaskPort/StubCurrentness——testkit 惯例的可控替身，非未实现代码）与留痕 JSON/XML 内既有测试 ID 的 "Placeholder" 字样（UI-T05 占位视图等设计内行为名）。
- ②**空实现检测**：接口全部方法有实实现（query/expandChain/pendingConfirmations/subscribe/redactedPath、控制四请求、pollOnce、requestConfirmations 等——验收者通读三个实现 TU 全文确认）；无"仅返回默认值"的契约未声明空实现；装配缺失路径的空集/nullopt 返回均为注释显式声明的零虚构语义（非偷懒）。
- ③**逐条证据表**：见 4.4 表格——五条 acceptance 每条注明具名测试（文件:行）＋实现文件行号＋命令输出，任一条无证据即 fail 的红线未触发。
- ④**测试强度**：抽读 6 个测试体（DismantleWakesBlockedCommand 旁路线程拆除唤醒、NoAutoTimeoutWhileWaiting 50 轮泵送观测、RevisionCommitAnnotatesStaleInput 三段时序、PresenterInvokedOnUiThreadOnly 线程身份断言、ComparisonValuesUnitCoDisplay 四态逐一、ControlRequestsFireAndForget 强杀未确认拒绝）——断言校验行为与边界（多线程会合、一次性语义、代次判别、线程身份、四态占位逐字符串），非恒真、非"只编译通过"；与 4.5 联动的两处抽改变红实证测试真实失败能力。

## 二、问题清单

**阻断级：无。**

**建议级（3 条，已按 F-284/F-285/F-286 转登 findings.json，编号顺延自 F-283）：**

1. **F-284**（suggestion）：F-010/F-283 家族复踩（UI 系连续第 8 例）——UI-T13 留痕提交 6b7ffbc3 的提交信息与 traceability/wp10-t13-gate-registrations.md 引用 `builds/wp10-t13/` 下原始日志（ird_gates-integration/base/smoke.log、控制台日志×5、validate-task.log），实际入库仅 11 件（gtest XML×3＋ird-test-report×5＋gates-hit-set×3），.log 零入库；验证结论本身（双模式零 error、151＋17＋27/28、门禁恰增 4 处 R3、validate-task PASS）经本验收独立复现全部属实，不构成粉饰。处置建议与家族同口径：治理一次性解决 .gitignore 根因后自动消账。
2. **F-285**（suggestion）：F-278/F-280/F-281 家族复踩——UiPorts.hpp 于 UI-T13 增量修改（五端口首消费冻结增注）后文件尾换行仍未恢复（尾字节 0x50 'P' 实证）；本任务 6 个新增文件尾换行全部正常（0x0a 实证）。处置建议：随 F-278 家族一次性处置。
3. **F-286**（suggestion）：TaskPresentation.cpp `refresh()` 注释与实现不一致——注释声称"refresh 不回退显示——乱序快照不得倒退进度条，与 pollOnce 同纪律"（:371~373），但同 attempt 内较低 percent 的新快照会无条件覆盖展示值（merge 以 nullopt 基线调用恒 Accept，既有展示进度仅在"新快照无进度"时回填，:373~386）；§9.4 单调规则的规范语义写的是轮询合并路径（pollOnce 实现正确且有测试钉住），刷新路径不受契约句约束，但注释声明强于实现，后续维护者可能据注释依赖该守恒。处置建议：要么在 refresh 并入时以既有展示值为基线调 mergeProgressDecision（AttemptReset 仍重置），要么修正注释收窄声明范围。

## 三、验收者环境说明

- 复现现场：`git worktree add --detach D:/10_Source_Repos/21_robot/wt-acc-uit13-a 6b7ffbc3`（集成模式）；冒烟树 `D:/10_Source_Repos/21_robot/acc-uit13-smoke`（源取同 SHA industrialrobot/ 目录）；记录回传 worktree `D:/10_Source_Repos/21_robot/wt-acc-uit13-b`（分支 `acc/UI-T13/1` ← `origin/redesign-main`）。
- 工具链：VS 2022（Visual Studio 17 2022 / x64）、Qt 6.11.1（D:/software/Qt/6.11.1/msvc2022_64）、vcpkg 经主树绝对路径 toolchain（worktree 无 git 未跟踪的 vcpkg/，环境事实与先例同）；RobWorkStudio bin 自举模板件自主树补拷（F-007 在案缺陷绕过，不触及送验产物）。
- 测试执行：模型/契约测试以 QCoreApplication 级直跑（Qt bin 挂 PATH）；GUI 测试按 ui.md §12.2 口径（`QT_QPA_PLATFORM=windows`＋`QT_PLUGIN_PATH` 指向 Qt 插件目录、绝对路径单进程运行）；ird_gates 以 `cmake -DIRD_ROOT=<worktree industrialrobot> -DIRD_SELFTEST_DIR=<临时目录> -P cmake/ird_gates.cmake` 脚本模式亲跑。
- 验收者原始运行日志留存于验收工作目录（`acc-uit13-configure2.log`/`acc-uit13-build-*.log`/`acc-uit13-gui-run2.log`/`acc-uit13-gates-run.log`/`acc-uit13-smoke-*.log`），未入库（遵守"不修改实施产物、不向任务分支推送"约束；入库留痕以送验对象自带 11 件为准）。
- 变异验证（4.5）在 detached worktree A 本地进行并当场还原，任务分支 `wp10-t13` 与远程零触碰。
