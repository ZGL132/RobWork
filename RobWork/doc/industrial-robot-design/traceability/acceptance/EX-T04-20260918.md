# EX-T04 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | EX-T04（≙WP-08-T05）RunRegistry 与迟到结果两段式接纳 |
| 单元 | execution |
| 送验对象（冻结） | `4adc54f2b3e7ed37dae14552cdfda5fab9f7c04c`（分支 wp08-t04） |
| base | `dbc741799d7bb5f946d265ed463abd0c126c5aea`（＝origin/redesign-main 尖端） |
| commit 范围 | `dbc74179..4adc54f2`，共 3 个提交：743cefc4（实现）、bc646c24（留痕）、4adc54f2（单元卡同步） |
| 分支漂移检查 | `origin/wp08-t04 == 4adc54f2b3e7…`（推送后无追加提交）——通过 |
| **verdict** | **pass**（4.1～4.11 全过、零阻断；1 项建议级转登 F-211） |
| 验收日期 | 2026-09-18（attempt 1；记录分支 `acc/EX-T04/1`） |
| 独立性声明 | 全新上下文会话，只消费契约/设计文档/代码/在库留痕产物，未消费实施者叙述；构建、测试、门禁、探针全部亲手复跑（ACC §2） |

## 验收者环境说明

- 复现现场：`git worktree add --detach <临时目录A> 4adc54f2b3e7…`（detached @ SHA，非分支名）。
- 集成模式重新配置：`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=<仓库根>/vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`；配置后 grep 缓存确认 **`RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`**。两个环境前置按既有 findings 处置先例补齐：①F-007（配置参数未载明 toolchain/Qt 路径）；②F-098/F-099/F-200 族（`RobWorkStudio/bin/RobWorkStudio.ini.template.static`＋`ini.shared.in` 为 git 未跟踪本地辅助件，自主树复制后配置通过——全新树不复制必卡 `RobWorkStudio/src/CMakeLists.txt:58`，属基线环境缺口、非本任务引入）。
- 冒烟模式：独立临时目录，`cmake -S …/industrialrobot -B <临时目录> -DCMAKE_TOOLCHAIN_FILE=<仓库根>/vcpkg/scripts/buildsystems/vcpkg.cmake`（带 toolchain——CORE-T01 G-2 口径）。
- 诊断码核对、findings 相关性核对均在冻结 worktree 内完成。

## 4.1 改动范围 —— 通过

`git diff --name-status dbc74179..4adc54f2`：共 22 个变更文件，逐一对照契约 `allowedFiles`：

- `RobWork/RobWorkStudio/src/rwslibs/industrialrobot/execution/**`：CMakeLists.txt（M）、contract_test/ArchiveCollaborationContractTest.cpp（A）、include/…/execution/Ports.hpp（A）、include/…/execution/RunRegistry.hpp（A）、src/Admission.cpp（A）、src/RunRegistry.cpp（A）、test/RunRegistryAdmissionTest.cpp（A）——7 件，全部在允许面内；
- `RobWork/doc/industrial-robot-design/units/execution.md`（M）——在允许面内；
- `RobWork/doc/industrial-robot-design/traceability/builds/wp08-t04/**`（A，14 件）——在 `traceability/**` 允许面内。

`forbiddenFiles`（REQUIREMENTS.md／ARCHITECTURE.md）零触碰（diff 中不存在）。SA-02：`git diff --name-only dbc74179..4adc54f2 -- '*patches*'`＝0 件，无未登记框架源码改动（集成构建中失败的 2 个 RobWorkSim 框架项目见 4.2——为基线既有缺陷 F-008，不在本 diff 内）。

## 4.2 可复现构建 —— 通过

- **集成模式（冻结 worktree 重新配置）**：配置 exit 0、缓存 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON` 确认。全 solution 构建 exit 1，但 73 处 error **全部**集中于 `RobWorkSim/rwsimlibs/bullet/bt_plugin.rwplugin.vcxproj`（2）与 `RobWorkSim/rwsimlibs/plugins/RWSimulatorPlugin.vcxproj`（71）——与开放发现 F-008（全新集成树框架基线缺陷：Math.hpp min/max 宏污染）逐字同型，`grep -icE "industrialrobot|ird_"` 对 error 行＝**0**，本 diff 零框架文件。工业产品目标链（ird_core/evidence/project/testkit/runtime/execution＋两测试目标）在 fresh 树零错误构建成功。实施者留痕 `integration-build.log` 记录的是主树（`D:\…\RobWork\build\`）全量构建 exit 0——与 fresh 树表现不矛盾（F-008 仅现于全新树），无粉饰迹象。
- **集成模式（主构建树口径）**：契约 verify 两命令在本冻结 worktree 的 fresh 树逐条执行亦零错误（见 4.3）。
- **独立冒烟模式**：`smoke-configure` exit 0＋`cmake --build --config Release` exit 0、`grep -icE "error C[0-9]|error LNK|fatal error"`＝0；两测试可执行文件产出。
- 结论：双模式构建可复现、零错误（交付面口径）；结论留本验收执行日志（临时目录，不随记录入库——本记录以文字结论与在库留痕交叉印证为准）。

## 4.3 契约 verify 命令 —— 通过（2/2）

逐条亲手执行（fresh worktree 集成树）：

1. `cmake --build build --config Release --target sdurws_ird_execution_test` → **exit 0**，零编译错误，产出 `sdurws_ird_execution_test.exe`；
2. `cmake --build build --config Release --target sdurws_ird_execution_contract_test` → **exit 0**，零编译错误，产出 `sdurws_ird_execution_contract_test.exe`。

## 4.4 acceptance 条目逐项证据 —— 通过（5/5；ird_gates 已实际执行非豁免）

验收者亲手运行两测试目标（集成＋冒烟双模式）：`sdurws_ird_execution_test` **70/70 PASSED**、`sdurws_ird_execution_contract_test` **12/12 PASSED**（gtest XML 自留存档；与在库四份 XML 的 tests/failures 聚合一致：70/0 与 12/0）。逐条：

| # | 契约 acceptance 条目 | 验收者亲手证据 |
| --- | --- | --- |
| 1 | EX-REG-1~8 全绿（未知 RunId 拒绝＋零归档写；五元组逐字段独立断言；陈旧 AttemptId 拒绝；EX-REG-4 项目切换后归档至 A 原 runDir；EX-REG-7 重投递幂等；EX-REG-8 内容冲突拒绝；不按当前会话丢弃合法迟到结果） | ①`UnknownRunRejectedWithNoArchiveWrite_EX_REG_1`（RunRegistryAdmissionTest.cpp:684——Rejected(UnknownRun)＋EX-REGISTRY-UNKNOWN-RUN 诊断＋beginCalls==0＋results 目录不存在）；②`FiveTupleFieldMismatchRejectedPerField_EX_REG_2`（:713——project/branch/revision/attempt 逐字段独立失配断言；**本验收探针 1 注入步 2 revision 失明后该用例转红**，见 4.5）；③`StaleAttemptsAllRejected_EX_REG_3`（:750）＋`BindingMismatchAtSecondCheck`（:779）＋`KindOutsideAllowedSetRejected`（:810）；④EX-REG-4＝contract `LateResultArchivesToRegisteredRunDirAfterProjectSwitch_EX_REG_4`（ArchiveCollaborationContractTest.cpp:424——真实双 ProjectStore：B 打开存活后 r1 迟到接纳→manifest 落 **A 原 runDir**、`EXPECT_FALSE(fs::exists(B/results/<run>))` 绝不进 B、listRuns 按原修订可见且 manifest 五元组＝登记原五元组、A 上下文保持存活）；⑤EX-REG-7＝`FinalEnvelopeRedeliveryIdempotent_EX_REG_7`（:871——三连投递、DuplicateIgnored、事件恰一次；**探针 2 翻转指纹比对后转红**，见 4.5）；⑥EX-REG-8＝`FinalEnvelopeContentConflictRejected_EX_REG_8`（:918）＋`ResultBatchAdmissionAndConflict_EX_REG_8`（:947）＋contract `BatchContentConflictViaProjectPort_EX_REG_8`（:479——真实 project 端口 D-14 全链路）；⑦"不按当前会话丢弃"＝classifyArrival 全程零"当前项目"查询（RunRegistry.cpp:166-289 全读——仅查登记表，结构性保证）＋EX-REG-4 行为面 |
| 2 | EX-ORT-1：CON-02 三态正交（四轴互不推导逐行断言）；HEAD 不参与接纳判定；取消/失败/中断结果不入正式 | `CompletedWithDataInsufficientIsAdmitted_EX_ORT_1`（:988——Completed＋DataInsufficient 合法组合）＋`TerminatedRegistrationsRejectFinalEnvelope_EX_ORT_1`（:823——Canceled/Failed/ForceTerminated 三终结因拒绝＋零归档；**步 7 三值列举经查 §9.2 原文（units/execution.md:820-821）一致，Interrupted 不入拒绝集非私裁**）＋`SupersededCurrentDoesNotBlockOrRewrite_EX_ORT_1`（:1047）＋`HeadChangeNotInvolvedInAdmission_EX_REG_6`（:1022——HEAD 前进后接纳照常）；不入正式＝AdmissionOutcome.envelope 仅 Admitted 非空（RunRegistry.hpp:394-405）＋拒绝路径零端口调用（Admission.cpp:180-190） |
| 3 | 九步接纳＋名称反解编排就位（先反解后接纳 CON-06；INameResolverAdapter 注入零 runtime 编译边；envelope 组装复用 evidence 校验器单点不复制） | `RuntimeNameResolutionGatesAdmission`（:1075——任一名称不可解→NameUnresolved 拒绝＋反解器未装配 fail-closed）＋`UndecodablePayloadRejected`（:1129）＋`ConstructionFailureFailsTask_EX_ORT_1`（:1151——make 拒绝→T9）；单点复用＝contract `EnvelopeAssemblyReusesEvidenceValidators`（ArchiveCollaborationContractTest.cpp:535——接纳产物与 evidence API 直接组装产物 `EXPECT_EQ(*out.envelope, expected)` **全字段相等**＋payload digest 一致〔CR-02 重算〕——结构性证明零复制）；零表外边＝Ports.hpp/RunRegistry.hpp include 面无 runtime/policy 头（4.6） |
| 4 | P-EX-5/P-PR-4 处置：归档协作线程模型按 §9.5 单侧冻结（调度线程调用＋writer 互斥串行化＋完成事件幂等无上限＋abandon 三时机），与 PRJ-T14 冻结文本互为对端 | RunRegistry.hpp 文件头/IRunArchiveGateway/ResultAdmission 类注（:46-52、:161-165、:606-612）与 Admission.cpp 文件头（:30-32）冻结纪律在注：全部归档端口调用仅调度线程、不另持项目写锁；完成事件幂等无上限＝EX-REG-7 用例三连投递实证；abandon 三时机＝`ArchiveRetryExhaustionAbandonsKeepsCompleted_EX_ARC_5`（:1203）＋A7 两分支（:1230/:1252）＋批次迟到 Completed abandon（Admission.cpp:727-737）；单元卡 §15.4 v0.6 行登记与 PRJ-T14 §5.6/§9.8 互为对端起点（units/execution.md:1252） |
| 5 | AT-10 场景载体就位（PM-13 切换场景——EX-REG-4/§9.4 S7）；P-EX-1 处置：消费 evidence/project 公共头现状签名，冻结 diff 后增量同步 | AT-10 载体＝EX-REG-4 双项目切换契约用例（:424——§9.4 S7 排布注解在案）；P-EX-1＝新增代码仅消费 core/evidence/project 公共头（include 面全查，4.6）＋单元卡 §15.4 v0.6 增量同步承诺登记＋4adc54f2 提交正文"文档侧落点"说明 |

ird_gates：**已实际执行**（非豁免）——本人在冻结 worktree 复跑 `--target ird_gates`：命中 **17 项**（与送验日志声称一致）；在库 wp08-t04 门禁日志与 base 的 wp08-t03 门禁日志做 IRD-GATE 码提及序列比对＝**41 处码提及多重集完全相等**（零新增命中）。

## 4.5 测试真实失败能力 —— 通过（双探针独立注入，本地 worktree 内、还原后零残留）

| 探针 | 注入点 | 预期红 | 实测 |
| --- | --- | --- | --- |
| 1 | RunRegistry.cpp classifyArrival 步 2 revision 比对置恒假（`if (false && …)`——ACC-PROBE-1） | `FiveTupleFieldMismatchRejectedPerField_EX_REG_2` 应失守 | 重建 exit 0 → 运行该用例 **FAILED**（revision 失配不再拒绝——检出生效） |
| 2 | Admission.cpp admitFinalEnvelope 幂等指纹比对翻转（`==`→`!=`——ACC-PROBE-2） | `FinalEnvelopeRedeliveryIdempotent_EX_REG_7` 与 `FinalEnvelopeContentConflictRejected_EX_REG_8` 应失守 | 重建 exit 0 → **2 FAILED**（幂等/冲突双向均失守——D-14 防线检出生效） |

还原：`git checkout --` 两文件，`git status` 确认零残留；重建后全量复跑 **70/70 PASSED**。结论：断言非恒真，行为与边界校验真实（与在库 test-fault-probe.log 的两探针点位互补，结论一致）。

## 4.6 架构红线（AGENTS §5 八条） —— 通过

1. **L2 零 Qt**：execution 全目录 `grep -rinE "#include <Qt|Q_OBJECT|QString"`＝0 命中；
2. **R-1 业务域互链**：diff 未触碰 modeling/requirements/kinematics/trajectory/dynamics/selection/optimization；execution 的 include 面＝core/evidence/project（ARCH §3.5 登记边）＋本单元公共头，runtime/policy 零编译依赖（P-EX-3 经 Ports.hpp 注入兑现，SA-10 无表外边）；
3. **R-2 私有头**：全部 `#include <sdurws/ird/<unit>/…>` 公共头；core formatDigest 属 detail 命名空间未跨单元消费（Admission.cpp:53-73 自行 hex 编码并注明 R-2 纪律）；
4. **SA-02 框架零修改**：见 4.1（patches/ 零新增）；
5. **R-4 名称前缀拼接**：diff 新增行 `RobWork` 字面量拼接/剥离扫描＝0 命中（ird_gates R4 检查亦仅报基线既有 project 侧 1 处例外项，与本任务无关）；
6. **PA-1 权威唯一**：判定＝evidence aggregateVerdict、构造＝evidence ResultEnvelope::make（SA-13）、归档＝project IResultArchivePort、反解＝runtime ⑥端口经注入适配、诊断码＝diagnostics StableCodeRegistry（新代码引用的 8 个稳定码 EX-REGISTRY-UNKNOWN-RUN/EX-REGISTRY-MISMATCH/EX-STALE-ATTEMPT/EX-CHANNEL-PROTOCOL-ERROR/EX-SNAPSHOT-STALE/EX-ARCHIVE-FAILED/EX-ARCHIVE-AUTHORITY-LOST/PRJ-ARCHIVE-CONFLICT 经查全部已收编 diagnostics/src/DiagCodes.cpp）；implementation 未私建第二持久化路径（admitNonArchivedKind 的三类到达放行＋归置边界已登记单元卡 §15.4 v0.6）；
7. **PA-2 不可变历史**：内容冲突拒绝不覆盖（指纹幂等/冲突拒绝双道＋D-14 归档侧比对），归档位置取登记原值不重新推导（A8）；
8. **不恢复 old/**：实现按 §9/§10.5 从零编写，无历史实现痕迹。

## 4.7 注释规范 —— 通过

- **公共头全查**：`RunRegistry.hpp`（703 行）与 `Ports.hpp`（188 行）——文件头均有设计依据（需求/架构/任务编号追溯）＋背景说明＋线程约束；每个类型/函数 Doxygen 中文齐备：参数 `[in]` 方向与语义、返回值含义与失败行为、异常触发条件（ExecutionError fail-fast 面逐条列明）；§2.5 高危信息：线程约束逐类注明（仅调度线程——P-PR-4 域）、所有权注明（"非所有权引用——调用方保证存活期覆盖"RunRegistry.hpp:634）、错误语义二分注明（结构化拒绝 vs 异常 fail-fast——Ports.hpp:162-165）、确定性来源注明（UtcClockFn/DelayFn 注入、SHA-256 经 core::ContentDigester 唯一实现点 CR-02——Admission.cpp:55-57）；单位注明（Config 退避"单位 ms"——RunRegistry.hpp:619-620）；本任务无物理量/坐标系/角度制式承载面（纯编排逻辑，n/a 合理）。
- **实现文件抽查（≥3）**：`Admission.cpp`（801 行全读——每语义段注释含九步步号/数学业务含义/失败走向；魔法数字 2 次/1 s→4 s 退避注明 D-11 出处与"非上游值"登记）、`RunRegistry.cpp`（375 行全读——步 1~7 逐段＋步 2/步 4 三分支细则自洽化说明）、`RunRegistryAdmissionTest.cpp`（文件头替身边界声明＋断言处需求/AT 追溯＋"不 sleep"纪律注明，符合 §2.7 测试注释要求）；`CMakeLists.txt` 关键步骤中文注释块同步更新（目标增列依据逐条注明任务号）。

## 4.8 文档同步＋findings 消账 —— 通过

- **实现与任务卡零未登记偏差**：`units/execution.md` v0.6（4adc54f2）卡头落位行＋§15.4 变更记录行与实现逐项对应；四处实现口径/归置增量（RejectReason 表尾追加四值、AdmissionOutcome 成形、mode/archivePhase 镜像字段、IEvaluationOutputDecoder 接缝、validateCombination 先于 make 的次序细化、三类非归档到达放行边界）全部按 DTB §5.4 登记，无"代码先行、文档失实"。
- **追踪/状态同步**：validate-task `PASS (1 tasks)`、validate-docs `PASS (20 units, 12 trace entries, 150 task files)`——本人在冻结 worktree 复跑，与在库日志逐字一致。
- **findings.json 未关闭条目相关性核对**（开放共 180 条，逐条按 execution/WP-08/EX 族筛出 8 条相关）：
  - **F-012 家族（F-208/F-210 直接前例）**：本任务 4adc54f2 为**新实例**（[units] 前缀＋"execution.md v0.6"名词短语开头）——按 ACC §5 强制转登 **F-211**（见"问题清单"）；
  - **F-207（gates 日志捕获侧双编码乱码）**：本任务 `gates-ird-gates-target.log` 同型受累（实测文件含乱码标记；IRD-GATE-* ASCII 码可 grep，机器可读性未损）——沿用 F-207 已登记口径，无新条目；`ird-test-report.json` 本身经 node 解析验证为干净 UTF-8（`锛` 零命中、正确全角括号在、JSON 可解析），验收者先前"疑似乱码"系终端截断渲染假象，特此更正留档；
  - **F-201（§4.10 与流水线实践脱节）**：沿用其登记口径与 EX-T01r2/EX-T02/EX-T03 先例——ird-test-report.json 即本任务 resultNote（见 4.10）；
  - **F-007/F-008/F-098（全新树环境前置族）**：本验收现场再次实证（ini 辅助件复制＋RobWorkSim 全量构建失败）——均已有 open 条目在案，无新增实例、无恶化；
  - **F-205/F-209（分支防线事故族）**：与本任务实施/送验对象无涉（编排侧流程缺陷），本验收未观察到新实例；
  - **F-031（EX-T09 契约 AT-34 迁注）**：属未来任务契约修订项，与 EX-T04 无涉。

## 4.9 commit 质量 —— 通过（含 1 项建议级，转登 F-211）

- **743cefc4** `[WP-08-T05] 实现 execution RunRegistry 与迟到结果两段式接纳（九步接纳/归档协作）`：动词开头合规；正文四段齐备（What 按文件列点/Why 对 acceptance 与需求 ID/How 七项验证/影响面）；验证段主张（双模式构建、70/70＋12/12、ird_gates 与 wp08-t03 集合 diff 为空、validate-task PASS、双探针）经本人逐项复现全部一致，无粉饰。
- **bc646c24** `[WP-08-T05] 留痕入库：…`：动词开头合规；四段齐备；留痕清单与在库 13 件一一对应（见 4.10）。
- **4adc54f2** `[units] execution.md v0.6：EX-T04 落位同步——…`：**前缀后名词短语开头，非动词开头**——ACC §4.9/AGENTS §6.3 字面要求未满足，F-012 家族同型再现（先例 F-037/F-039/F-044/F-076/F-079/F-208/F-210）；四段齐备、validate-docs PASS 经复跑逐字一致，纯风格瑕疵不构成阻断 → 转登 **F-211**。

## 4.10 留痕完整 —— 通过

- 在库清点（冻结 worktree @ 4adc54f2 实存）：`traceability/builds/wp08-t04/` 恰 **13 件**＝4 份 gtest XML（integration/smoke × test/contract_test）＋ird-test-report.json＋8 件日志（integration-build/smoke-configure/smoke-build/verify-target-build/gates-ird-gates-target/validate-task/validate-docs/test-fault-probe）——与 bc646c24 提交信息清单逐件对应，零缺件（F-010 族未踩中）。
- **数值交叉比对（本人复跑 vs 在库）**：测试 70/70＋12/12＝在库四份 XML（tests=70/70/12/12、failures=0）与 report 一致✓；ird_gates 命中 17 项与在库日志一致、与 wp08-t03 码提及多重集相等✓；validate-task/validate-docs PASS 复跑逐字一致✓；verify 两命令 exit 0 逐字执行一致✓；探针 2 项红/还原复跑全绿＝test-fault-probe.log 与本人双探针同结论（点位互补）一致✓。
- phase-one-validation.log：本任务未更新——沿用 F-201 登记口径（ird-test-report.json 即 resultNote 承载，EX-T01r2/EX-T02/EX-T03 三先例一致），报告含 acceptanceCoverage 五条逐项映射（结构经 node 解析确认）。

## 4.11 偷懒/缩水实现扫描 —— 通过

1. **词表扫描**：base..head 全部新增行（含文档/留痕）`TODO|FIXME|stub|placeholder|未实现|not implemented`＝**0 命中**（无豁免需求）；
2. **空实现检测**：公共接口（IRunRegistry/IResultAdmission/三注入接口）全部有 final 实现（RunRegistry/ResultAdmission）；无返回默认值的占位函数体（setter 三件为注入接缝，标准形态）；admitNonArchivedKind 具有真实行为（核对面放行＋开发留痕），其"持久化归各自组件"边界已在单元卡登记；
3. **逐条证据表**：见 4.4——契约 acceptance 5 条全部有具名测试/亲手命令输出/文件行号，零"无证据"条目；
4. **测试强度**：断言校验行为与边界（拒绝原因码逐项、端口调用计数、文件存在性/逐字节、事件恰一次、状态/阶段两轴投影），非恒真——4.5 双探针实证会红。

## 问题清单

| 级别 | 编号 | 描述 | 处置 |
| --- | --- | --- | --- |
| 阻断 | （无） | —— | —— |
| 建议 | **F-211**（转登 findings.json） | F-012 家族同型再现：4adc54f2 提交标题 `[units] execution.md v0.6：…` 前缀后名词短语开头，非动词开头；四段齐备、验证结论经复现一致，纯风格瑕疵 | 与 F-012 家族合并处置（提交模板补动词开头示例）；不追溯改写已推送历史 |

## 裁决

**pass**——4.1～4.11 全部通过、零阻断级问题；建议级 1 项已按 ACC §5 强制转登 findings.json（F-211，编号顺延自 F-210）。本验收通过的是冻结 SHA `4adc54f2b3e7ed37dae14552cdfda5fab9f7c04c`，不是分支引用；合入由所有者按 PIPE §4.7 决策。
