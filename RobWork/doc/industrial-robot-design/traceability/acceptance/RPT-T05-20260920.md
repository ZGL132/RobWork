# RPT-T05 验收记录（acceptance record）

| 字段 | 值 |
| --- | --- |
| 任务 ID | RPT-T05（实现报告构建器——会话状态机/锚定/资格与当前性快照/冻结；unit=reporting；masterWp=WP-H；interUnit=true——消费 project IProjectQueryPort＋evidence 资格/当前性投影公共头，产出 IReportResultSource 注入契约；实现任务——全量清单验收） |
| 送验对象（冻结） | d14eab5c595702a41c72815a8ae76b7f299b26c7（分支 wp12-t05；diff 范围＝02ed302656ff338b362602990a293318aa427b0e..d14eab5c…，state.base..headSha；range 内含 6a9aba31（实现）/d24adf7d（文档同步）/d14eab5c（验证留痕）三个提交） |
| 验收尝试 | attempt 1（分支漂移检测：`git fetch` 后 `origin/wp12-t05` ＝ 本地 `wp12-t05` ＝ d14eab5c… 全 40 位逐字一致，零漂移；base 02ed3026 ＝ `origin/redesign-main` 尖端即送验分支合并基——`git log --oneline -1 02ed3026` 实证为 RPT-T04 合入收尾提交；`acc/RPT-T05/1` 分支名此前不存在，`git worktree add -b` 成功） |
| 验收环境 | 复现 worktree A＝`git worktree add --detach D:/acc_tmp/acc-rpt-t05-a` @ d14eab5c（detached@SHA，非分支名）；记录回传 worktree B＝`acc/RPT-T05/1` 基于 origin/redesign-main@02ed3026；pwsh 7＋MSVC 2022 x64＋vcpkg 经典模式（toolchain 绝对路径指向主仓库 vcpkg——worktree 天然不含）；集成配置＝主树缓存实证口径：`-G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=<主仓>vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`（与主 build/CMakeCache.txt 实测缓存逐字同口径），并自主树复制 `RobWork/RobWorkStudio/bin/RobWorkStudio.ini.template.static` 一件 git 未追踪本地辅助件到 worktree A 同位（F-007 环境链同款，bin/ 被 .gitignore 吞——环境补救，非送验对象缺陷）；**本验收集成模式按契约 verify 两目标（含其框架依赖链）构建交付面，未跑全量 ALL_BUILD**——RPT-T04 验收 F-235 已登记全新树全量构建的 RobWorkSim/RWSimulatorPlugin 环境性编译错误（构建约定所有者调查中），本验收不重蹈该已知环境坑、以"契约 verify 目标＋冒烟树 industrialrobot 全单元"为可复现口径；冒烟模式独立目录 `D:/acc_tmp/acc-smoke-t05` 全量构建（AGENTS §4.1 toolchain＋Qt 前缀口径）；实施工作树仍检出任务分支 wp12-t05（ACC §3 预判成立，detached worktree 复现正确）；验收全程只消费产物、契约、设计文档与在库留痕，未读取实施者过程叙述；结论全部出自验收者亲手重跑 |
| verdict | **pass**（4.1～4.11 全部通过；无阻断级问题；建议级 2 条转登 findings.json F-236/F-237） |
| 验收日期 | 2026-09-20 |

## 4.1 改动范围 —— 通过

- `git diff --name-status 02ed3026..d14eab5c` 实证 **23 个变更文件**（4 A＋1 M＝工业产品面，17 A＝留痕面，1 M＝单元卡）：①`reporting/CMakeLists.txt`（M）②③④⑤`reporting/{contract_test/BuilderCrossUnitContractTest.cpp, include/sdurws/ird/reporting/Builder.hpp, src/Builder.cpp, test/BuilderTest.cpp}`（A）⑥～㉒`traceability/builds/wp12-t05/` 17 件留痕 ㉓`units/reporting.md`（M，v0.8）。
- **allowedFiles 逐文件比对**：①～⑤命中 `industrialrobot/reporting/**`；⑥～㉒命中 `traceability/**`；㉓命中 `units/reporting.md`——**23/23 全部在册，零越界**。
- forbiddenFiles（REQUIREMENTS.md／ARCHITECTURE.md）**零触碰**（diff 无该两文件）。
- 无未登记框架源码改动（SA-02）：diff 零 `RobWork/RobWork/`、RobWorkSim、RobWorkStudio 其他 src 路径（`git diff --name-only <range> -- RobWork/patches/ RobWork/RobWork/ RobWorkSim/ RobWorkStudio/src` 除 industrialrobot/reporting 外空输出亲验）；`patches/` 零新文件。
- 逐提交核验：6a9aba31＝①～⑤（实现本体）、d24adf7d＝㉓（文档同步）、d14eab5c＝⑥～㉒（验证留痕）。

## 4.2 可复现构建 —— 通过（两模式零错误；集成口径见环境段说明）

- **集成模式**（worktree A 内新建 `build/`，全新配置 `Configuring done`＋`Generating done`，缓存确认 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`——grep 亲验）：
  - 契约 verify 两目标构建（`--target sdurws_ird_reporting_test`／`_contract_test`，含框架依赖链 sdurw_math 等）**各自 exit 0、零编译错误**（日志中 "error" 命中 4 行均为 `Errors.cpp`/`ErrorsIdentityTest.cpp` 文件名列表行，非编译错误——逐行甄别）；**reporting 新增面（Builder.cpp/BuilderTest.cpp/BuilderCrossUnitContractTest.cpp）零警告**——unit 日志 54 条 warning 逐行归因：40 条框架 rw/math 既有（Eigen C4996/Quaternion/VectorND 等）＋14 条既有 `core/src/Units.cpp` C4099（Quantity struct/class 混用——RPT-T04 验收已登记同款，Units.cpp 不在本任务 diff 面），industrialrobot 面警告文件清单亲数零 reporting 文件；contract 日志零 warning。
  - 集成全量 ALL_BUILD 未跑（F-235 已登记的全新树 RobWorkSim 环境错误，见环境段口径）——送验交付面零错误可复现成立。
- **冒烟模式全量构建**（独立目录 `D:/acc_tmp/acc-smoke-t05`，toolchain＋Qt 前缀——AGENTS §4.1 口径）：配置 exit 0＋全量构建 **exit 0、`error C|error LNK|fatal error` 计数 0**；`reporting/Release/sdurws_ird_reporting{,_contract_test}.exe` 产物就位；smoke-build.log warning 命中 reporting 计数＝0（仅既有 project/test 等 DraftServiceTest C4834 等，非本任务 diff 面）。
- 首配前 ini.template.static 复制（F-007 环境链）记录于环境段——非送验对象缺陷。

## 4.3 契约 verify 命令 —— 通过（两条逐条亲手执行）

在 worktree A 根目录按契约字面逐条独立执行（4.2 即其输出；此为命令原文复述）：

1. `cmake --build build --config Release --target sdurws_ird_reporting_test` → exit 0
2. `cmake --build build --config Release --target sdurws_ird_reporting_contract_test` → exit 0

附加治理脚本（worktree A 上下文亲手复跑，与送验留痕一致）：`verify-task.ps1 -TaskFile doc/industrial-robot-design/tasks/foundation/RPT-T05.json` → **verify-task: PASS RPT-T05**（脚本内部并触发 reporting 面全部目标重构建，均成功）；`validate-task.ps1 -TaskFile …/RPT-T05.json` → **PASS (1 tasks)**；`validate-docs.ps1` → **PASS (20 units, 12 trace entries, 150 task files)**——与在库 `builds/wp12-t05/validate-task.log`/`validate-docs.log` 一致。

**ird_gates 门禁亲手复跑（双树命中集归一化 diff）**：branch 侧（worktree A，`cmake -DIRD_ROOT=<A>/industrialrobot -P cmake/ird_gates.cmake`）IRD-GATE 输出 61 行（含 27 项既有登记命中＋引擎自测段）＋FATAL 汇总"27 处命中"（命中即 FATAL 为引擎设计语义，命中集对比为口径）；base 侧（worktree B@02ed3026 同命令）同为 61 行/27 项；两侧 IRD-GATE 行提取后按 `<ROOT>` 归一化路径做 `sort`＋`diff` → **GATES-HIT-SET-IDENTICAL（diff 为空）**——零 reporting 新增命中，与送验留痕 `gates-hit-set-diff.txt` 结论一致；27 项既有命中全部为 io/ui/project/testkit 等既有登记面（R4 命中 1 处＝project/ProjectStoreImpl.cpp，base 同款），reporting 构建器新增目标/文件未引入任何命中行。

## 4.4 acceptance 条目 —— 通过（六条逐项证据）

| # | 契约条目 | 证据（验收者亲手复现＋代码亲读） |
| --- | --- | --- |
| 1 | 构建反例矩阵（§4.6 逐行）：B∧C 专属＝LevelConflict；C∧零 C 章节选中＝ScopeInsufficient＋RPT-SCOPE-INSUFFICIENT＋建议生成 B 级（非静默降级——P-RPT-5）；Preview 结果＝SourceMissing；Canceled/Failed/Interrupted 入正式结论＝构建边界拒绝（TASK-02）；证据引用绑定不符＝EvidenceRefInvalid | `Builder.cpp` 亲读五拒绝点逐条标注 acceptance 1：①B 级携带 C 专属覆盖→LevelConflict（:505-512 请求面）②C 级两路同码面范围判定（:1087-1108 内容路 checkSectionScope＋选择路 selectedCSections==0；makeScopeInsufficientDiagnostic 携 P-RPT-5 契约词）③Preview 拒绝（:617-627——D-16，码 SourceMissing＋诊断）④非 Completed 结果入结论条目→DataInvalid（:929-934——TASK-02）⑤证据绑定四查（:949-990——itemId 在清单/状态一致/Satisfied 摘要一致/工况范围不越界，均 EvidenceRefInvalid）。测试亲跑全绿：`BLevelWithCExclusiveOverride_RejectedLevelConflict`（BuilderTest.cpp:860）/`CLevelWithNoCSectionContent_RejectedScopeInsufficient`（:874——断言诊断码在案＋recommendedAction 含"显式确认""B 级"契约词）/`CLevelWithPopulatedCSection_Builds`（:902 正例对照）/`CLevelUserDeselectAllCSections_RejectedScopeInsufficient`（:926 选择路）/`PreviewResultRejected_SourceMissing`（:949）/`CanceledResultBoundIntoEntry_RejectedDataInvalid`（:968）/`CanceledResultUnbound_PresentAsStatusAndDiagnostics`（:984 未绑定仅状态与诊断呈现）/`EvidenceRefMismatchVariants_Rejected`（:1013 三变体：清单外/摘要不符/——）/`EntryCaseScopeOutsideResult_Rejected`（:1031）——**双模式 78/78 全绿亲跑** |
| 2 | RP-MDL-2 引用不可解析拒绝：未 finalize/修订不存在/Preview 逐项 SourceMissing（listRuns 仅 finalize 口径——§7.2 步②）；无报告对象、查询端口桩/FakeSink 零调用（零项目写断言） | `Builder.cpp` 亲读：①tryRevision 缺失→SourceMissing＋诊断（:545-557）②请求 runId∉listRuns finalize 清单→SourceMissing＋诊断（:561-581）③envelope 解码失败→SourceMissing＋诊断（:603-613）。测试 `UnresolvableReferences_RejectedSourceMissing`（BuilderTest.cpp:1062——三反例逐段断言）亲跑绿；"无报告对象"＝`expectFailure` 断言 `outcome.report==nullptr` 贯穿全部失败用例（:563-568）；"查询端口桩零调用"＝FakeQueryPort 反向断言面（:277-312——构建器不可调用的其余方法一律抛 logic_error，调用即测试失败）＋调用日志白名单断言（:781-783）；"失败路径结果源零调用"＝`ZeroProjectWrites_OnFailureAndSuccess` 断言 `envelopeCalls.empty()`（:1117）亲跑绿 |
| 3 | 资格与当前性快照（RP-CUR-3/RP-STATE-1~3 映射）：资格经 evidence 纯检查取得并冻结进 ResultRefSnapshot.eligibility——不自算、不缓存跨报告复用（§6.2）；当前性不可判定＝status=nullopt＋RPT-CURRENTNESS-UNEVALUABLE 诊断、不默认 Current（P-EV-4——§6.3）；失败/取消结果仅诊断/状态呈现 | `Builder.cpp` 亲读：资格经 `m_resultSource->eligibilityOf()` 注入消费、只冻结 eligible 位（:649-663——不自算的结构面：构建器不持有 VerdictInput/AnalysisSnapshot 等事实面）；当前性 `status=nullopt` 原样冻结＋unevaluableNote 必填＋诊断入并（:654-705——"不默认 Current"注释与代码一致，无任何默认 Current 分支）。测试 `EligibilityFrozenFromPureCheck_QueriedPerBuild`（:1151——两次构建 eligibilityCalls 1→2，不缓存实证）/`UnevaluableCurrentness_NotDefaultCurrent`（:1178——断言 status 无值＋诊断码＋报告级并集携带）亲跑绿；**4.5 探针 2 对此条做了真实失败验证（注入"默认 Current"即红）**；`CanceledResultUnbound`（:984——RP-STATE-1 构建器层映射）亲跑绿；具名 RP-* 用例体随 RPT-T11 收口＝契约 acceptance 3 原文明文（"本任务交付构建器层用例（局部夹具）"），不构成缩水 |
| 4 | 零项目写验证：Resolving~Verifying 全程零项目写（§7.1——生成失败不修改原项目）；唯一写点在 Publishing（随 RPT-T09）；锚定视图纪律（tryRevision＋listRuns 构建时刻快照——HEAD 前进/迟到结果不入已锚定视图） | **结构保证亲读**：`ReviewReportBuilder` 成员仅 `const IProjectQueryPort*`/`IReportResultSource*`/`const SectionRegistry*`/`const IDiagnosticRegistry*` 四个只读借用（Builder.hpp:592-595）——类中不存在任何写端口引用，查询端口消费面经 FakeQueryPort 反向断言锁定为 head/tryRevision/listRuns 三只读方法；**测试**：`ZeroProjectWrites_OnFailureAndSuccess`（成功路径 callLog 恰 3 次、失败路径同白名单）＋`BuildsBLevelReport`（:776-778 head/tryRevision/listRuns 各恰一次）＋`AnchoredViewIsolatedFromLateRuns`（:1123——listRunsCalls==1 且迟到结果追加后 `resultRefs().size()==1` 不变）亲跑绿；`ResultOrderNormalized`（:809——集合语义规范化 identity 无关请求序）亲跑绿 |
| 5 | 会话状态机（§7.1）：七态＋两终态、终态后不可变；冻结时刻＝build() 返回；取消检查点（章节/结果）与 RAII 清理（正常取消非错误——UX-03）；会话单线程单测锁定 | `Builder.hpp:172-182` 九值枚举（主链七态＋Failed/Canceled）亲读与 §7.1 图原文一致；`Builder.cpp:365-397` advanceTo 唯一后继表（严格逐步——跳段/回退/终态推进一律 Usage fail-fast）、`:399-418` fail/cancel 终态不可变、`:420-429` RAII 析构守卫（非终态未 release 析构转 Canceled——清理语义在构建器仅内存态下的落位）；冻结时刻＝`ReviewReport::make` 后 `session.release()` 即返回（:1205-1215）。测试 `SevenStateHappyPath_AdvancesStrictly`（:582——token 逐字锁定）/`IllegalJumpRejected_UsageFailFast`（:604 跳段/回退/跨三段）/`TerminalStatesImmutable`（:620 三终态上推进/二次失败/取消全拒）/`FailFromAnyNonTerminalStage`（:654 六非终态逐边）/`DestructorGuardSmoke`（:682）/`CancelAtResultCheckpoint`（:1239——结果源零调用）/`CancelBetweenSections`（:1255——检查点序按词表序观察，后继章节零投影）/`ContentIdentityStableAcrossRebuilds`（:786 内容幂等——冻结语义）亲跑绿；单线程约定＝头文件契约声明（:52-53/204-205）＋全部用例单线程运行，本验收确认其与 §9 约定一致 |
| 6 | 跨单元依赖就绪声明（EX-T08 先例）：EV-T08/PRJ-T09 公共头可用；envelope 解码缺位（R-3）经 IReportResultSource 注入隔离——不自建解码器、不私建编译边 | include 面亲读（Builder.hpp:78-83）：`evidence/Currentness.hpp`（EV-T08 交付面）/`evidence/{Envelope,Snapshot,Verdict}.hpp`/`project/QueryPort.hpp`（PRJ-T09 交付面）全部公共头；`BuilderCrossUnitContractTest.cpp` 亲读：5 组 static_assert 类型恒等（currentnessOf 返回＝evidence::CurrentnessResult/tryEnvelope 返回＝optional<evidence::ResultEnvelope>/资格对成员＝evidence::EligibilityCheck/build 接口面＋实现面双断言）＋`RptPresentationCodesRegisteredInStableCodeRegistry`（RPT-CURRENTNESS-UNEVALUABLE/RPT-SOURCE-MISSING/RPT-SCOPE-INSUFFICIENT 对 StableCodeRegistry find 断言——码值权威 PA-1）＋`DecodeIsolatedViaResultSource_NoOwnDecoder`（解码隔离——:306）亲跑绿（18/18）；eligibilityOf/tryReproduction 两落位增量按 DTB §5.4 登记于单元卡 §14.4 v0.8（本表 4.8 项核对），契约语义零变更；LinkageContractTest 既有四条编译边常驻用例保持全绿（18 条 contract 测试亲跑） |

## 4.5 测试真实失败能力 —— 通过（两处断言注入即红、还原复绿）

在 worktree A（本验收沙箱，实施分支零触碰）注入两处应失败输入，冒烟树重建后亲跑：

1. **TASK-02 拒绝点旁路**（探针 1）：`Builder.cpp` 非 Completed 结果拒绝条件 `if (boundEnvelope.outcome != core::TaskOutcome::Completed)` → `if (false && …)`（取消/失败/中断结果放行入结论条目）→ **恰 1 个测试 FAILED：`ReviewReportBuilderTest.CanceledResultBoundIntoEntry_RejectedDataInvalid_RPT05_ACC1`**（精准命中目标断言）。
2. **P-EV-4 违约注入**（探针 2）：`Builder.cpp` 当前性快照赋值 `item.currentness.status = currentnessResult.status;` → 不可判定时默认 `Current`（违反 §6.3/P-EV-4"不得默认 Current"）→ **恰 1 个测试 FAILED：`ReviewReportBuilderTest.UnevaluableCurrentness_NotDefaultCurrent_RPT05_ACC3`**（精准命中）。

还原两处注入后 `git status`/`git diff` 实证产品源码零残留（worktree A 干净）、重建复绿 exit 0（冒烟 78/78）。**测试非恒真**：断言对实现行为真实敏感，非"只编译通过"。

## 4.6 架构红线 —— 通过（AGENTS §5 八条逐条）

1. **L2 计算内核零 Qt**：本任务 4 个新增源文件 diff 新增行 grep `include.*<Qt|Q_OBJECT|QWidget|QtCore` **零命中**；reporting 为 L2 计算单元、非 ui 例外（include 面亲读——全部 std＋ird 公共头）。
2. **业务域互链禁止（R-1）**：diff 新增行 grep `ird/(modeling|requirements|kinematics|trajectory|dynamics|selection|optimization)/` **零命中**；跨单元消费仅经 core/evidence/diagnostics/project 公共头（ARCH §3.5 白名单 `reporting->core/evidence/diagnostics/project` 四行既有在位——ird_gates SUB/R1 复跑零新增命中佐证）。
3. **跨单元私有头禁止（R-2）**：include 面全部为 `include/sdurws/ird/<unit>/` 公共头（Builder.hpp:72-88 亲读清单）；单元内私有头仅 `#include "ReportCodec.hpp"`（Builder.cpp:48——src/ 相对路径同单元，R-2 允许，注释明示）。
4. **框架零源码修改（SA-02）**：diff 零框架路径、patches/ 零新文件（4.1）；ird_gates SA-02 工作区核对在门禁复跑中通过（27 项命中集与 base 逐行一致）。
5. **不做名称前缀拼接/剥离（R-4）**：新增行零 RobWork 字面量；ird_gates R4 复跑命中仅既有 1 处（project/ProjectStoreImpl.cpp——base 同款，非本任务）。
6. **权威唯一（PA-1）**：级别×章节判定权威单点在 Sections（构建器以 `frozenReportLevelRule()` 注入 ReviewReport::make——Builder.cpp:1209）；RPT-\* 码面全部引用 diagcodes 登记常量（`diagcodes::kSourceMissing`/`kScopeInsufficient`/`kCurrentnessUnevaluable`）且契约测试对 StableCodeRegistry find 断言（引用登记非二次定义）；码表元数据经 IDiagnosticRegistry 查询、缺席时以默认元数据承载不私设第二权威（Builder.cpp:136-156 注释逐条）。
7. **不可变历史（PA-2）**：构建器对 project 仅三只读方法（tryRevision 锚定不可变视图）；报告对象 ReviewReport 为不可变值对象（make 唯一生产者）；零修订/写路径触碰。
8. **不继承不恢复 old/**：diff 新增行 grep `old/` **零命中**。

## 4.7 注释规范 —— 通过（公共头全查＋实现/测试全读 3 件超抽查下限）

- **公共头全查（1 件新增）**：`Builder.hpp`（600 行）——文件头设计依据（§7.1/§7.2/§9.1/§9 章首/§6.2/§6.3/§4.6/§3.3＋需求 ID 四件＋契约 acceptance 1~6）＋背景说明（构建器三条红线位置）＋IReportResultSource 落位增量来龙去脉（DTB §5.4）＋线程契约段＋确定性声明齐备；每个类/结构体（ReportCancelToken/ReportProgress/IReportProgressSink/ReportBuildStage/ReportBuildSession/ReviewMetadataSeed/ReportBuildRequest/ReportBuildOutcome/ReportEligibilityChecks/IReportResultSource/IReviewReportBuilder/ReviewReportBuilder——12 个）均有业务概念/生命周期/线程约束说明；每个函数 @brief/@param 方向与语义/@return/@throws 齐备。
- **§2.5 高危信息逐项**：物理单位（done/total "无单位纯计数"显式注明——本任务无数值计算量，单位面不适用处零伪造）；坐标系/角度（不适用——零位姿/角度数据，未虚构注释）；所有权与生命周期（构造引用借用语义"存续期覆盖全部 build() 调用"/outcome.report unique_ptr 归调用方/progress.stage 静态存储期"实现方不得留存指针"/failure() "不外借引用 beyond 会话生命周期"——逐处明示）；线程约束（build 并发安全/Session 非线程安全单线程/注入源并发只读由实现方承诺/取消令牌可见性义务——类注＋函数注双面）；错误语义（Usage fail-fast＝调用方违约 vs SourceMissing/ScopeInsufficient 等稳定码＋诊断＝来源数据错误、取消＝UX-03 error 缺席——三类归因清晰）；确定性来源（结果集按 runId 规范文本排序进身份/生成时间者 ReportId 不入身份 §4.4 排除列/token 静态存储期——逐处标注）。
- **实现/测试全读（3 件 ≥3 要求）**：`Builder.cpp`（1226 行全读——文件头设计依据/错误二分/线程契约/确定性段＋实现要点步①~⑦对照＋每拒绝点 acceptance N 标注＋复杂步骤逐步讲解：锚定纪律/快照一致性/绑定四查/两路范围判定/演化种子重算比对）；`BuilderTest.cpp`（1384 行全读——替身边界声明整段：合法包络经 evidence make() 构造、Preview 受控越界样本为例外并注明理由；每用例注释声明验证哪条 acceptance）；`BuilderCrossUnitContractTest.cpp`（338 行全读——契约声明①~④段＋static_assert 双面断言的成员指针原理注释）。
- 零反面示例命中：无复述式注释/无占位 TODO（4.11 扫描佐证）/无中英机翻腔/无过期注释。

## 4.8 文档同步 —— 通过

- `units/reporting.md` v0.8 落位登记（d24adf7d）：文档版本行 v0.7→v0.8＋构建落位行追加 RPT-T05 落地子句＋§14.4 v0.8 变更行——登记内容与实现亲读比对一致（会话状态机九值/IReviewReportBuilder 契约表/ReportCancelToken/ReportProgress 零 io 类型 P-RPT-1/IReportResultSource 四方法/构建编排步①~⑦要点/测试面/替身边界/CMakeLists 仅增列）；**两处落位增量（eligibilityOf/tryReproduction）已按 DTB §5.4 在 §14.4 v0.8 行明文登记**（"§9.1 原文两方法＋落位增量两方法……契约语义零变更"），非静默偏差；§14.4 v0.8 行另登记三处范围边界（框架章节内建投影随数据源任务收口/coverageSummary 零计数承载待 RP-COV-1/externalResourceSummary 空集承载）——均为有契约依据的范围登记，非缩水；"本文契约语义零变更"属实（§7/§9 原文零改动——diff 仅 3 行落位登记）。
- 分支/文件口径：契约 branch=wp12-t05 与实际一致；CMakeLists 改动仅增列源文件与两测试文件（目标名/别名/四条链接边/编译选项零改动——diff 亲读实证）。
- **findings.json 未关闭条目相关性核对（v1.2）**：与 reporting/RPT 系相关的 open 条目逐条核对——**F-230**（RPT 契约任务号前缀双轨）：本任务三提交沿用 `[RPT-T05]` 前缀＝登记的可用等价形态，一致未越轨；**F-232/F-233/F-234**（RPT 留痕/文档提交标题名词短语开头家族）：本任务 d24adf7d/d14eab5c 两提交**同族复踩**（第 6/7 例）→ 转登 F-237；**F-041/F-073 家族**（散文计数失准）：本任务**复踩**（三处散文计数 23/26 与实测 28 不符，详见 4.10/G-1）→ 转登 F-236；**F-081/F-083/F-085/F-087/F-088/F-090/F-113 家族**（留痕 .log 被 .gitignore 吞/悬空引用）：本任务 17 件留痕**全部实际入库**（git ls-tree 实证 17 件含 11 件 .log——实施侧 git add -f 成功规避，零复踩）；**F-235**（全新树 RobWorkSim 环境错误）：本验收以"契约 verify 目标＋冒烟全量"口径绕开未重蹈（未采证不判定其现状——非本任务可消账项，维持 open 交构建约定所有者）；**F-066**（EVI 建议码超出清单）：RPT-\* 呈现码三件已在 diagnostics 收编表登记（契约测试亲跑证实），非同族问题。

## 4.9 commit 质量 —— 通过（三提交逐份核对；标题风格 2 处家族复踩降建议级）

- **6a9aba31**"实现报告构建器（Builder）：会话状态机/锚定/资格与当前性快照/冻结"——`[RPT-T05]`＋动词开头合规；正文四段（What 按文件列要点/Why 对契约与需求 ID 逐条/How 双模式＋测试＋门禁＋脚本/影响面＋Acc-Covered: 1,2,3,4,5,6）齐备；并如实登记一处测试缺陷修正（CancelBetweenSections 观察点按词表序校正）。
- **d24adf7d**"reporting 单元卡 v0.8：RPT-T05 实现落位登记（文档同步）"／**d14eab5c**"验证留痕：双模式构建/测试执行/门禁/治理自查证据（wp12-t05）"——正文四段齐备、验证结论与留痕一致，但**标题以名词短语开头**（F-232/F-233/F-234 家族第 6/7 例，实现提交合规、两辅助提交复踩——家族先例均判纯风格瑕疵不构成阻断）→ 转登 F-237，不追溯改写已推送历史（AGENTS §6.3）。
- 验证结论与留痕一致性抽查：XML 顶格 tests="78"/"18"、failures="0"（与提交"78/18"一致，验收者双模式亲跑吻合）；ird_gates"27 项既有命中、命中集 diff 为空"（验收者亲手复跑双树 61 行归一化 diff 为空——吻合）；validate"双 PASS"（复跑吻合）；gates-hit-set-diff.txt"GATES-HIT-SET-IDENTICAL"（复跑吻合）——零粉饰。唯一失准处＝测试用例散文计数（23/26 vs 实测 28），见 4.10/G-1，属 F-041/F-073 家族笔误非粉饰（执行数据真实、散文计数既未虚增通过数也未掩盖失败）。

## 4.10 留痕完整 —— 通过

- `traceability/builds/wp12-t05/` **17 件全部实际入库**（git ls-tree 实证；含 11 件 .log——git add -f 规避 .gitignore `*.log` 规则成功，F-081 家族零复踩）：双模式 gtest XML×4＋构建/配置/测试运行/门禁（base/branch/diff 三件）/validate-task/validate-docs 日志＋ird-test-report.json（d14eab5c 正文"十六件留痕"＋报告单列＝17 件，计数一致）。
- gtest XML 顶格核对：unit tests="78" failures="0" errors="0"（integration＋smoke 双份）；contract tests="18" failures="0" errors="0"（双份）——与提交声称一致；验收者双模式亲跑 78/78＋18/18 全绿（4.2/4.3）。
- 新增用例清点：BuilderTest.cpp **实测 28 个 TEST**（ReportBuildSessionTest 5＋ReviewReportBuilderTest 23，grep ^TEST 亲数）＋BuilderCrossUnitContractTest.cpp 3 个；78＝RPT-T04 交付面 50＋28（XML 自洽）、18＝15＋3；ird-test-report.json（ird-test-report/1）双模式构建结论＋逐用例 acceptance 追溯＋acceptanceCoverage 六条消账映射在位（亲读）。
- **计数失准发现（G-1→F-236）**：ird-test-report.json notes/提交 6a9aba31 正文/单元卡 v0.8 三处散文计数"二十三条（5＋18）"、d14eab5c 正文"新增 26 用例"，与实测 28 条不符（三处均小于实测——新增多于声称，无缩水；四份 XML 与亲跑真实）；ird-test-report.json 自身 targets 段"run-pass（两模式 78/78）"与 XML 一致——执行数据真实、散文计数笔误，F-041/F-073 家族复踩，建议级不阻断。
- 治理脚本结论已亲手复跑核验（4.3）；phase-one-validation.log 无 RPT 条目＝RPT-T03 起收尾先例同口径（收尾走 pipeline/state.json＋契约翻转，该 log 非本任务载体）；验收者亲跑证据存于本记录（4.2/4.3/4.5/ird_gates 复跑）。

## 4.11 偷懒/缩水实现扫描 —— 通过

1. **标记扫描**：diff 新增行 grep `TODO|FIXME|stub|placeholder|未实现|not implemented`（-i）**零命中**；无 `TODO(WP-nn)` 登记项（契约亦无触发式延后标注——"随 RPT-T06/T07/T09/T11/T12 收口"各处均为契约 acceptance 原文与单元卡 §11 任务拆分链明文的任务边界：渲染/校验/发布/具名用例/框架章节数据源分属后续任务，非偷懒豁免）。
2. **空实现检测**：公共接口函数体逐个亲读——token（全枚举 switch）/ReportBuildSession 四方法＋析构守卫（全实现）/ReviewReportBuilder::build（1226 行实现全读——无空体无占位；coverageSummary/externalResourceSummary 两字段以零计数/空集承载均有 §14.4 v0.8 范围登记与"不私设规则"的 PA-1 依据，非空实现）——零"仅返回默认值"式偷懒。
3. **逐条证据表**：见 4.4 六条——每条均有具名测试（双模式亲跑全绿）＋文件行号证据，任一条不缺。
4. **测试强度**：断言校验行为与边界（状态机逐边/五拒绝点逐条＋错误码断言/绑定四查三变体/调用计数恰一次与零调用白名单/契约词内容断言/幂等 identity 断言/检查点序观察）——非恒真；4.5 双探针实证注入即红（1+1 精准红）、还原复绿。

## 发现的问题（分级清单）

- **阻断级：无。**
- **建议级 2 条（已转登 findings.json，F-236/F-237）**：
  - **G-1（→F-236）**：测试用例散文计数失准——提交 6a9aba31 正文/单元卡 v0.8/ird-test-report.json notes 三处写"二十三条（5＋18）"、d14eab5c 正文写"新增 26 用例"，实测 28 条（grep ^TEST；XML 78＝50＋28 自洽）——F-041/F-073 家族复踩。三处均小于实测＝新增多于声称、无缩水；四份 XML 与双模式亲跑（78/78＋18/18）真实无虚，纯散文计数笔误。处置：RPT-T06+ 以实测/XML 顶格差值为计数口径；不追溯改写已推送提交。
  - **G-2（→F-237）**：d24adf7d/d14eab5c 两提交标题以名词短语开头——F-012/F-232/F-233/F-234 家族第 6/7 例（RPT 系连续四任务复踩）；正文四段齐备、零粉饰，纯风格瑕疵。处置：F-012 建议②（实施者模板补动词开头示例与负例）落地前预计持续再现。

## verdict

**pass** —— 4.1～4.11 全部通过，无阻断级问题；建议级 2 条（G-1/G-2）转登 findings.json F-236/F-237，不阻塞合入。送验对象＝d14eab5c595702a41c72815a8ae76b7f299b26c7（该 SHA 通过验收，非分支引用）。

验收者环境与独立性声明：全新上下文，仅消费产物/契约/设计文档/在库留痕；构建与测试全部由验收者在 detached worktree@SHA 亲手重跑（不采信实施者运行结果）；全程零实施叙述输入；未向 wp12-t05 推送任何提交、未修改任何实施产物（探针注入均在验收沙箱 worktree A 内完成并还原，git status/diff 零残留实证）；验收记录与 findings 转登落 `acc/RPT-T05/1` 分支并推送（ACC §3 记录回传）。
