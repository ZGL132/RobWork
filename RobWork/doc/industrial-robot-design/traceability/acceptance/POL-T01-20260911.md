# POL-T01 验收记录（对抗式验收）

| 字段 | 值 |
| --- | --- |
| 任务 ID | POL-T01（≙WP-07-T01，policy 构建落位） |
| 契约 | tasks/foundation/POL-T01.json（status: ready，随收尾翻转 done） |
| 验收对象 | 分支 `wp07-t01`，head＝`432f9fa8f7a5eaeb5cd496d8a20e0b4976605ddc`（与 origin 一致——漂移检测 PASS），范围 `053ba83..432f9fa` |
| 尝试序号 | attempt=1（evidence 分支 `acc/POL-T01/1`） |
| **verdict** | **pass**（4.1~4.11 全过；无阻断、无新增建议级发现） |
| 验收日期 | 2026-09-11 |
| 独立性声明 | PIPE v1.7 §0.4 单会话降级；补偿＝worktree A（detached @ head）亲手复现（4/4）＋证据链前置自查（6 份日志 -f 入冻结对象——F-010 教训内化） |

---

## 逐项证据（4.1~4.11）

| # | 检查 | 结论与证据 |
| --- | --- | --- |
| 4.1 | 改动范围 | diff＝10 文件：policy/{CMakeLists,src/Policy.cpp,test/BuildRedLineTest.cpp}＋industrialrobot/CMakeLists＋traceability 6——allowedFiles 内；forbiddenFiles 零触碰 |
| 4.2 | 可复现构建 | worktree A 全新配置 exit 0（ini 模板前置补齐）＋verify 目标构建 exit 0＋测试 4/4 PASSED（验收者亲手）；集成全量 exit 0 |
| 4.3 | verify 命令 | `cmake --build build --config Release --target sdurws_ird_policy_test` exit 0 |
| 4.4 | acceptance 逐条 | ①两模式构建零错误（4/4）；红线扫描零命中：无 Qt（NoQt 用例）、无私有头出 include/（落位期零头＋保留位锚定）、依赖图仅 core＋RobWork 边（NoCrossUnit 白名单 {core, policy}＋配置期守卫三层）；②gtest 按 §5.5 接入（find_package REQUIRED＋GTest::gtest_main＋ird_add_gtest）——CR-07 ✓ |
| 4.5 | 失败能力 | R-5 清单漏登探针（清单宏隐藏 proximity＋重配重编）→BaselineLibsPinned 如期变红，恢复后 4/4 复绿（报告记录；探针零残留 grep 实证；首版探针误改链接目标致断链已修正设计并如实留痕） |
| 4.6 | 红线 | 零 Qt；唯一单元边＝core（CR-04：runtime 消费经 IPolicyNameContext 注入而非直依赖——本任务零 runtime 链接实证）；R-5 proximity 直链＝登记册生效行唯一许可（链接清单四库含 proximity）；SA-02 零框架改动 |
| 4.7 | 注释 | CMake 关键决策块全注释（R-5 例外标注＋冒烟判定说明＋分号坑沿用 RT-T01 留痕）；测试每用例设计依据；占位翻译单元说明 |
| 4.8 | 文档同步 | 零偏差（§3.4/§12 POL-T01 行逐项对应）；findings 核对：F-010 前置自查（4.1 实证）；F-004 AUTOMOC 空转（本目标同样存在——留构建所有者，累计第五处）；F-008 无涉（verify 目标从零构建成功） |
| 4.9 | commit 质量 | 两提交四段式＋Acc-Covered（1,2 / 2）；-f 补交提交说明 |
| 4.10 | 留痕完整 | 9 文件齐（日志 6＋XML×2＋report）；ird-test-report 含探针记录与四条取舍 notes（含 P-POL-2 nullopt 语义指向 POL-T02） |
| 4.11 | 偷懒扫描 | 零命中；占位 Policy.cpp＝任务行明文产物形态（T01 落位，真实接口随 POL-T02+）；测试断言行为与边界（R-5 四库钉住＋冒烟空串双分支强断言，防恒真） |

## 问题清单

阻断级：无。建议级：无新增。
