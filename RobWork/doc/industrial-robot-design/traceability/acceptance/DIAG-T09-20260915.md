# DIAG-T09 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 ID | DIAG-T09（≙WP-09-T04 范围，diagnostics 单元：诊断生命周期与容量——目录清理策略〔容量护栏/分级淘汰/软溢出/DIAG-CATALOG-OVERFLOW〕＋宿主留痕形状〔envelope/CommandRecord 对接数据〕） |
| 契约 | `doc/industrial-robot-design/tasks/foundation/DIAG-T09.json`（status ready；branch wp09-t09；requirements TASK-02/CON-02/NFR-REL-03；designRefs units/diagnostics.md §6.1/§6.2；knownPitfalls P-DIAG-7；interUnit true；dependsOn DIAG-T04/T07——均已 pass-merged） |
| 送验对象（冻结） | `4d4bf83c11bfe6c626efe6d5d25084beff4671a4`（验收期间 live 复核：`origin/wp09-t09`＝该 SHA，**无分支漂移**——ls-remote 实测；验收前段两次 SSL 握手失败，evidence 分支 fetch 成功后网络恢复补测） |
| diff 范围 | `87c1f06b490f3ccb32f2efd11b95951256761d8d`（base＝state.base＝origin/redesign-main 尖端，与 headSha 父提交一致）..`4d4bf83c`，共 1 提交、11 文件（+2143/−13） |
| verdict | **pass**（4.1~4.11 全部通过，无阻断级问题；新增建议级登记 1 项＝F-113〔报告字段悬空引用未入库 .log——F-087/F-090 家族再现〕，沿 F-081→F-111 家族既判先例不阻塞合入） |
| 验收日期 | 2026-09-15 |
| 验收者 | 独立验收会话（attempt 1，全新上下文；未消费实施叙述；对抗式立场——复现优先于采信，下述构建/测试/门禁/探针/脚本均为验收者亲手执行） |

---

## 一、逐项证据（ACC §4.1~4.11）

### 4.1 改动范围 —— 通过

- diff 文件清单（`git diff --name-status 87c1f06b..4d4bf83c`，11 个，+2143/−13）逐个比对契约 `allowedFiles`：
  - `RobWork/RobWorkStudio/src/rwslibs/industrialrobot/diagnostics/**`（5 个，allowedFiles 第 1 项）：CMakeLists.txt（M——注释登记 DIAG-T09 落位＋`test/CatalogLifecycleTest.cpp` 增列 `_test`、`test/CatalogHostTraceStubTest.cpp` 增列 `_contract_test`；`target_link_libraries`/`find_package` 面 diff 零触碰——链接边不变）、include/…/Catalog.hpp（M，公共头——CatalogCapacityConfig＋五方法＋kCatalogInternalChannel＋IDevLogSink）、src/Catalog.cpp（M，+281——容量护栏/分级淘汰/溢出登记，无新增翻译单元）、test/CatalogLifecycleTest.cpp（A，9 用例）、test/CatalogHostTraceStubTest.cpp（A，5 用例）；
  - `RobWork/doc/industrial-robot-design/units/diagnostics.md`（M，allowedFiles 第 2 项——卡头 v0.9→v0.10 构建落位刷新＋§14.4 v0.10 变更记录行，共 7 行）；
  - `RobWork/doc/industrial-robot-design/traceability/**`（5 个，allowedFiles 第 3 项）：builds/wp09-t09/ 下 integration-gtest-diagnostics-test.xml（A，tests=107 failures=0）、integration-gtest-diagnostics-contract-test.xml（A，tests=14 failures=0）、smoke-gtest-diagnostics-test.xml（A，tests=107 failures=0）、smoke-gtest-diagnostics-contract-test.xml（A，tests=14 failures=0）、ird-test-report.json（A，base 字段＝87c1f06b 与送验范围一致）。
- `forbiddenFiles`（REQUIREMENTS.md / ARCHITECTURE.md）：diff 范围内零命中。
- SA-02（框架零源码修改）：diff 过滤 industrialrobot/doc 前缀后范围外文件计数 **0**（`git diff --name-only … | grep -cv` 实测）；`industrialrobot/patches/` diff 零触碰（空输出——无新登记必要）。
- **范围越界：零。**

### 4.2 可复现构建 —— 通过（验收者亲跑；双模式零错误）

- **复现现场**：`git worktree add --detach D:/10_Source_Repos/21_robot/acc-diag-t09-a 4d4bf83c11bfe6c626efe6d5d25084beff4671a4`（detached @ 送验 SHA，不用分支名）。
- **集成模式（干净构建树，从零重新配置）**：`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DRWS_USE_STATIC_LINK_PLUGINS=ON -DCMAKE_TOOLCHAIN_FILE=<主仓>/vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`——配置 exit 0 零 CMake Error（Configuring done 38.4s），缓存确认 **`RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`**。worktree 无未跟踪本地辅助件 `RobWorkStudio.ini.template.static`/`ini.shared.in`（F-007/F-062/F-099 在册既有缺陷），自主树补拷至 worktree 内 `RobWork/RobWorkStudio/bin/` 后配置通过（环境修复，不触及送验产物——DIAG-T05~T08 验收同先例）。
  - `cmake --build build --config Release --target sdurws_ird_diagnostics_test sdurws_ird_diagnostics_contract_test`：两测试目标编译链接**零 error C／fatal error／error LNK（grep 计数 0）**，Catalog.cpp/CatalogLifecycleTest.cpp/CatalogHostTraceStubTest.cpp 均有编译活动；产物 exe 落位 `build/RobWorkStudio/bin/Release/`。口径沿 DIAG-T05~T08 先例：构建两目标及其依赖链（diagnostics＋core＋框架库），未做全树构建——本任务改动面（diagnostics 单元）完全覆盖。
  - **测试执行（验收者亲跑）**：`sdurws_ird_diagnostics_test.exe` → **107/107 PASSED**（exit 0，459 ms）；`sdurws_ird_diagnostics_contract_test.exe` → **14/14 PASSED**（exit 0，12 ms）——既有 98＋9 零回归＋新增 14 全过，与送验 ird-test-report.json 登记一致。
- **独立冒烟模式（另配临时目录 `D:/10_Source_Repos/21_robot/acc-acc-t09-smoke`，带 vcpkg toolchain——F-002/CORE-T01 G-2 口径）**：`cmake -S RobWork/RobWorkStudio/src/rwslibs/industrialrobot -B <冒烟目录> -DCMAKE_TOOLCHAIN_FILE=.../vcpkg.cmake` 配置 exit 0；`cmake --build ... --config Release` **exit 0 零 error（grep 计数 0）**；冒烟树两测试 exe 亲跑 → **107/107 PASSED＋14/14 PASSED**（exit 0），与送验留痕"双模式 107/107＋14/14"一致。

### 4.3 契约 verify 命令 —— 通过

契约 `verify` 数组仅一条，验收者在复现 worktree A 根目录逐字亲跑：

```
cmake --build build --config Release --target sdurws_ird_diagnostics_test   → exit 0
```

（增量链接产出 sdurws_ird_diagnostics_test.exe，exit 0；产物可直接运行 107/107，见 4.2。）另亲跑 `verify-task.ps1 -TaskFile <契约>` → **verify-task: PASS DIAG-T09**，exit 0（脚本内执行同条 verify 命令）。

### 4.4 acceptance 条目逐项证据 —— 通过（3 条全部有具名测试与行号证据；ird_gates 如实登记基线命中而非虚标通过）

| # | acceptance 条目 | 证据 |
| --- | --- | --- |
| 1 | DT-LIFE-1~3/5 用例通过：目录容量与分级淘汰（Info 最旧淘汰，Warning/Error/活动任务关联保留＋DIAG-CATALOG-OVERFLOW）、失败/取消后诊断保留（终结原因必附——NFR-REL-03）、归档失败报告（EX-ARCHIVE-FAILED 用户可见，关闭不等待）、持久化诊断不因当前性改写（字节不变——CON-02/DT-LIFE-5） | 亲跑双模式 107/107＋14/14 全绿（送验 XML 四份同数据）。**DT-LIFE-1**：`DtLife1_TieredEvictionKeepsWarningErrorAndActiveTask`（CatalogLifecycleTest.cpp:239——capacity=4 逐轮注入：最旧已消费 Info（info1/info2）淘汰腾位、Error/Warning 消费后仍保留（:284-296 在场集合断言）、活动任务关联 Info 带任务锚定在场（:289-296）、溢出事实恰两条 DIAG-CATALOG-OVERFLOW 且通道＝diag/catalog、消息含 `evicted=1`（:300-317）、append 恰一次变更通知＝6 次（:320-321））；`DtLife1_UnconsumedInfoSurvivesDespiteAge`（:328——更旧未消费 Info 压力下保留、同容量已消费 Info 正常淘汰＝判据②压过"最旧优先"（:352-353））；`DtLife1_LinkReferencedInfoProtectedThenReleasable`（:361——被 causedBy 引用的 Info 链保护不淘汰（:384）、引用者淘汰后下次压力解除保护（:398）＝引用计数快照多遍收敛的两步观测）；`DtLife1_RetentionProtectedSoftOverflowStillAppends`（:406——已消费 Error 占满无候选：新条目照常追加 size=3>capacity=2（:429）、溢出登记含 `retention-protected=1`＋`evicted=0`（:431-435）＝证据完整性优先）。**DT-LIFE-2**：`DtLife2_FailedTaskDiagnosticsRetainedWithTerminationReason`（:509——失败任务终结注销保护后按任务查询两条俱全（:531）、终结原因 EX-TASK-REJECTED＝Error 必附＋根因 EX-WORKER-CRASHED 链保留（:534-544）——NFR-REL-03"终结原因必附"）；`DtLife2_CanceledTaskInfoRecordOnlyNoErrorDiagnostics`（:553——取消任务恰一条 RT-CANCELLED Info、actionKind=none（:579-582）、中断任务 EX-TASK-INTERRUPTED Info＋rerun-interrupted（:585-591）、全目录零 Error 级条目（:595-598）——UX-03/D-16/TASK-02）。**DT-LIFE-3**：`DtLife3_ArchiveFailureUserVisibleAndFacilitiesIntact`（:657——EX-ARCHIVE-FAILED＝Error/ExecutionFailed/retry-task 随终态入目录且用户可见（:687-708）、既有诊断未受影响＋容量护栏在失败路径照常运转（:709）、溢出登记经**真实 LoggingPipeline＋FileLogFileOps** 落盘 Tier-D（dev-diagnostics.log 含 DIAG-CATALOG-OVERFLOW）且 Tier-U 零命中（:713-720））；`DtLife3_CloseAfterArchiveFailureIsBounded`（:728——目录析构 <1 s＋flush(deadline=3 s) <4 s 双墙钟上界（:746-758）＝关闭无阻塞点 PM-03）。**DT-LIFE-5**（契约测试套件）：`DtLife5_PersistedBytesInvariantUnderCurrentnessProjection`（CatalogHostTraceStubTest.cpp:309——Superseded 投影后重读记录逐值不变（:353-354）＋持久化字节逐字节不变（:355）＋SHA-256 摘要不变（:356，core ContentDigester——CR-02 唯一哈希路径））；`DtLife5_UpdateIsSupersedingEntryAndOldEntryUntouched`（:370——更新＝新条目＋supersedes、旧条目投影逐字段原样（projectionEqual 全字段比对 :401）、supersedes 指向不存在条目 Usage 拒绝（:414-420）） |
| 2 | 宿主留痕形状（envelope/CommandRecord 对接数据）冻结：诊断随宿主持久化的编码归宿主所有者（envelope canonical 归 evidence、CommandRecord 归 project），本任务只交付对接数据形状，不私改宿主编码（§4.2/§6.2） | **结构性证明**：diff 零 evidence/project 文件（4.1）、零宿主编码设施新增（产品库仅 Catalog.* 扩容）；桩编码器（StubEvidenceEnvelopeStore/StubProjectCommandStore）定义在测试侧（CatalogHostTraceStubTest.cpp:142/:231——P-DIAG-8 同款桩验证纪律，文件头 :26-31 专段论证"测试侧编码器＝diagnostics 零宿主编码设施新增的结构性证明"）。三个形状用例：`HostTrace_EnvelopeShapeIsRecordValuesWithoutSessionIdentity`（:429——两会话异 entryId（:452-453 前置自检）对同一 record 值持久化，宿主字节逐字节一致＋摘要一致（:457-459）＝"entryId 不入 envelope"反例钉住）；`HostTrace_CommandRecordBindsRevisionNotEntryId`（:468——绑定字段＝修订身份非 entryId（:491）、记录值同源（:493））；`HostTrace_DevCodesNeverEnterHostPersistence`（:501——Dev 码宿主桩拒绝且拒绝路径零字节写入（:516-518）——historical=true 才允许，TASK-02/§6.1） |
| 3 | P-DIAG-7 处置：目录容量默认 10,000 条为可配工程默认（P-DIAG-7 登记），WP-23 校准，不作为需求语义 | `DtPdiag7_DefaultsPinnedAndConfigurableSurface`（CatalogLifecycleTest.cpp:444——①默认值钉住：capacityConfig()=={enabled=true, maxEntries=10000} 与 §14.3 登记值逐项断言（:449-452）；②0 容量 Usage 且被拒配置不部分生效（:455-460）；③enabled=false 关闭护栏＝12 条注入零淘汰零溢出登记（:463-472）；④缩容延迟生效：缩至 2 不立即淘汰、下一次 append 压力一次腾位且淘汰序＝入目录序（:476-497））。**默认值登记三处一致**：CatalogCapacityConfig 结构体注释（Catalog.hpp:457-464）、单元卡 §14.3 P-DIAG-7 行、单元卡 §14.4 v0.10 陷阱处置段 |

**ird_gates（如实登记）**：验收者亲跑 `cmake --build build --config Release --target ird_gates`（复现 worktree 集成树）→ exit 1，**命中 7 项与 wp09-t08 合入基线逐项同分布、零新增**：IRD-GATE-SUB×5（evidence/evidence_contract/policy/policy_contract/runtime 测试目标 → sdurws_ird_testkit 直链，F-072/F-051 族既有）＋IRD-GATE-SUB 表外边×1（sdurws_ird_testdata_lint → testkit，F-019）＋IRD-GATE-T1×1（sdurws_ird_testdata_lint 产品目标链 testkit，F-019）；引擎自测行（pass_clean/pass_r4comment＋fail_r1/t1/r5/sub/r3/r4/t2"按预期检出"）非命中。**diagnostics 相关命中＝0**（命中输出全文检索 diagnostics 计数 0）。exit 1＝基线既有命中的预期行为，处置权 WP-01 所有者——与送验 ird-test-report.json gates 段登记一致（如实登记基线命中，未私改门禁、未虚标通过）。

### 4.5 测试真实失败能力（验证验证者）—— 通过（两探针亲手构造、真实变红、还原复绿零残留）

验收者在复现 worktree 临时注入缺陷、重建重跑、逐字还原（`git checkout` 语义的编辑回退＋`touch` 强制重编规避 F-068 mtime 陷阱；还原后 `git status` 对 diagnostics 目录零修改、grep `PROBE-TEMP` 计数 0；最终 107/107＋14/14 复绿）：

- **探针 1（消费确认失守——DT-LIFE-1 淘汰判据②观测面）**：`Catalog.cpp` `DiagCatalog::markConsumed` 的 `m_impl->consumedIds.insert(entryId);` 改为 `(void)entryId;`（任何条目永不进入"已消费"集合）→ **5 例真实变红**：`DtLife1_TieredEvictionKeepsWarningErrorAndActiveTask`、`DtLife1_UnconsumedInfoSurvivesDespiteAge`、`DtLife1_LinkReferencedInfoProtectedThenReleasable`、`DtPdiag7_DefaultsPinnedAndConfigurableSurface`（缩容腾位半区）、`DtLife3_ArchiveFailureUserVisibleAndFacilitiesIntact`（infoCount=3≠2）——分级淘汰的"已消费"判据有机器观测面，非恒真。
- **探针 2（0 容量 Usage 校验击穿——P-DIAG-7 配置契约观测面）**：`Catalog.cpp` `configureCapacity` 的 `if (config.enabled && config.maxEntries == 0)` 改为 `if (false && …)`（0 容量不再拒绝）→ `DtPdiag7_DefaultsPinnedAndConfigurableSurface` **精确变红**（1 FAILED——"maxEntries==0：未抛出异常（应拒绝并抛 DiagnosticsError）"）：配置不变量契约有机器观测面。
- 两探针证明断言校验真实行为边界、非恒真；探针期间中间构建未计入任何结论。

### 4.6 架构红线（AGENTS §5 八条逐条）—— 通过

1. **L2 计算内核零 Qt**：改动四面（Catalog.hpp/Catalog.cpp/CatalogLifecycleTest.cpp/CatalogHostTraceStubTest.cpp）`grep "#include <Q"` 计数 0/0/0/0；ird_gates 输出零 diagnostics 命中；diagnostics/CMakeLists.txt 配置期红线守卫（既有）未触碰（diff 仅注释＋测试文件增列）。
2. **业务域互链禁止（R-1）**：产品库链接边未变（diff 零 `target_link_libraries` 改动，PUBLIC 依赖仍仅 `sdurws_ird_core`）；ird_gates SUB/R1 零 diagnostics 命中。
3. **跨单元私有头禁止（R-2）**：新文件 ird include 全集＝core 公共头 {DiagData.hpp、Identity.hpp、Digest.hpp}＋本单元公共头 {Catalog/DiagCodes/Errors/Factory/Logging}（include/sdurws/ird/ 公共层，grep 实证）；`detail::codeLower` 来自本单元 src/EntryDetail.hpp（**同单元**私有头——R-2 只禁跨单元，允许）；对 IRedactionService 用前置声明＋shared_ptr 保持最小依赖面；测试桩不依赖产品私有头（taskCanonical 自持拼接——CatalogHostTraceStubTest.cpp:213 注文明示）。
4. **框架零源码修改（SA-02）**：见 4.1——diff 零框架文件、patches/ 零改动。
5. **不做名称前缀拼接/剥离（R-4）**：本任务无名称解析语义（容量护栏/留痕形状与 RobWork 名称面无关）；grep 名称前缀类引用 0 命中；ird_gates R-4 零 diagnostics 命中。
6. **权威唯一（PA-1）**：`setTaskActive` 只承载保护登记、任务状态权威归 execution（Catalog.hpp:605-622 注释＋Catalog.cpp:172-175 实现——不判定任务真状态）；`markConsumed` 消费确认由消费方显式登记（目录不感知 ui/reporting 呈现时机）；溢出码 DIAG-CATALOG-OVERFLOW 复用登记表既有码（DiagCodes.cpp 内置表——T03 收编），消息首 token＝稳定码，无私造码值；无 project/runtime/policy 语义越入（归档编排/abandon 归 project——DtLife3 只承载诊断半区并有注文明示）。
7. **不可变历史（PA-2）**：目录为会话态（非修订历史）；条目构造后不可变（无 setter）、"更新"＝新条目＋supersedes 旧条目保留（DtLife5 用例钉住）；淘汰＝整条删除绝不部分改写（Catalog.cpp:271-273 注释"CON-02 在淘汰面的对偶"）；清理永不触碰已持久化诊断（CatalogCapacityConfig 注释 :451-453）。
8. **不继承 old/**：实现按单元卡 §6.1/§6.2/§9.7 从零编写；grep old/ 零引用。

### 4.7 注释规范（AGENTS §2）—— 通过（公共头全查＋实现文件全读＋测试全读，抽查面全覆盖）

- **公共头全查（1/1，Catalog.hpp 758 行）**：文件头设计依据逐条（§4.1/§4.2/§6.2/§6.4/§9.7＋需求映射＋任务契约）；P-DIAG-7 处置锚点专段（:25-28）；信封"为什么不是继承 core"背景专段（:30-36）；subject 边界口径专段；线程安全专段（:55-57）。**DIAG-T09 新增段注释完备**：CatalogCapacityConfig 逐字段（enabled＝关闭后无溢出登记的语义；maxEntries 单位"条，非字节"＋≥1＋0 非法理由——§2.5 单位/边界）；五方法 Doxygen 逐个（configureCapacity 快照切换/缩容延迟语义/Usage 触发条件；capacityConfig 观测面；markConsumed 的"为什么需要显式确认"专段＋advisory 语义＋entryId==0 Usage；setTaskActive 登记语义＋PA-1 论证＋终结不立即淘汰；attachDevLogSink 非拥有指针＋静默降态＋死锁面纪律——§2.5 所有权/线程/错误语义全在位）；kCatalogInternalChannel 对齐 diag/redaction 命名族论证。
- **实现文件全读（1/1，Catalog.cpp 736 行）**：文件头实现口径登记（淘汰判据合取/引用计数快照/软溢出/出口吞错纪律）；evictForCapacity 算法逐步注释（每遍快照构建→自最旧标记→多遍收敛→不死循环论证）；applyEvictions 三表一次性重建＋O(n²) 规避＋"删除整条绝不部分改写"论证；append 步骤①~⑥编号注释（Dev 拒绝/空 id/重复 id/去重折叠/链接完整性/容量护栏——每步业务场景与触发条件）；溢出登记锁内捕获锁外派发＋吞错理由；configureCapacity/markConsumed/setTaskActive/attachDevLogSink 每处分支与幂等语义注释。
- **测试文件全读（2/2）**：CatalogLifecycleTest.cpp（761 行）——文件头 acceptance 映射＋线程约束专段＋自持纪律说明；每 TEST 注释锚定矩阵行/需求/契约 acceptance；断言处中文说明（"未达上限不得产生溢出登记""缩容不立即淘汰（宁暂超限不越已消费判据）""软溢出不得丢弃新条目（证据完整性）"等）。CatalogHostTraceStubTest.cpp（521 行）——文件头 P-DIAG-8 处置＋结构性证明专段；桩类逐方法语义注释（"只改标记，不改持久化字节——CON-02 的宿主侧纪律"）；14 用例与 ird-test-report.json per-suite trace 逐条对应。
- **CMakeLists.txt**：DIAG-T09 落位注释两处（产品库"扩容无新增翻译单元"＋两测试文件增列与 §10 矩阵对应）。
- **修改文件新增段**：单元卡 diagnostics.md 卡头构建落位＋§14.4 v0.10 行（实现口径六项登记，见 4.8）。
- **单位/坐标系/角度制式**：本任务无物理量计算面（容量单位"条非字节"已显式标注、墙钟时限均带单位与上界语义；无位姿/角度语义），不适用——非缺项。
- 注记：Catalog.cpp:283 一处注释续行使用 `///`（应为 `//` 的文档注释误用）——零行为影响风格微瑕，低于 F-063/F-093 同类已判"非缺必填项"阈值，不登记不阻断。

### 4.8 文档同步 —— 通过

- 单元卡 `units/diagnostics.md` v0.9→v0.10：卡头"构建落位"字段实测刷新＋§14.4 v0.10 变更记录行——**实现口径六项按 DTB §5.4 登记**（①"已消费"判据＝markConsumed 显式确认＋advisory 语义；②活动任务保护＝集合登记而非状态判定（PA-1）；③淘汰算法＝引用计数快照＋多遍收敛＋applyEvictions 三表重建；④软溢出＝护栏让位于证据完整性＋两种溢出均登记；⑤配置语义＝快照切换＋缩容延迟生效＋v0.7 自动聚合触发面澄清（§9.7 无消费面不预建——NFR-MNT-04）；⑥宿主留痕形状＝对接数据形状冻结，零宿主编码设施新增）。**与实现逐项比对一致**（淘汰判据三合取＝isEvictableCandidate；引用计数快照＝evictForCapacity；软溢出 retention-protected 标记＝OverflowNotice；0 容量 Usage＝configureCapacity；消息首 token＝稳定码＋capacity/evicted/retention-protected/size＝append 溢出段）。§11 DIAG-T09 行（验证方式 DT-LIFE-1~3/5、产物 Catalog.* 扩展）兑现；§10 DT-LIFE-1/2/3/5 行各有承载用例。
- 契约流水线状态：`state.json` currentTask＝DIAG-T09、phase awaiting_acceptance、attempts.accept＝1 在案，翻转归收尾（PIPE §4.7⑤——非实施者职责）；ird-test-report.json base 字段与 state.base 一致。
- 治理脚本：验收者亲跑 `validate-task.ps1 -TaskFile <契约>` → **PASS (1 tasks)**，exit 0；`validate-docs.ps1` → **PASS (20 units, 12 trace entries, 150 task files)**，exit 0；`verify-task.ps1` → **PASS DIAG-T09**，exit 0。
- **findings.json 未关闭条目相关性核对（open 条目 93/112 逐家族排查）**：
  - **F-087/F-090 悬空引用家族**：本任务 4d4bf83c 的 ird-test-report.json gates/selfCheck 字段引用 4 件 .log（gates-ird-gates-target.log/integration-build.log/verify-task.log/validate-task.log）未入库（git ls-tree 实证仅 5 件）——**同型再现，新登记 F-113**（建议级）；提交正文本身干净（只声称在库的 XML×4＋report）；
  - **F-012/F-100/F-104/F-107/F-109/F-112 标题家族**：4d4bf83c 标题"[WP-09-T09] 实现 diagnostics 诊断生命周期与容量…"动词开头**合规，未再现**；
  - **F-110（Logging.cpp "ILoggFileOps" 拼写）**：处置建议"随下次触碰 Logging.cpp 的提交顺手更正"——本任务 diff **未触碰 Logging.cpp**（改动面＝Catalog.*/CMakeLists/新测试/单元卡/留痕），无可顺手消账面，继续 open（非落空——无条件触发）；
  - **F-007/F-009/F-062/F-098/F-099/F-101（全新树构建环境链）**：本次复现再次实证（ini 模板补拷后零错误配置构建；gate-all 未运行——F-009 在册环境受限），按 DIAG-T05~T08 验收口径一次配通，无可随手消账项；
  - **F-019/F-051/F-072/F-097（ird_gates 基线红）**：本次 7 命中同分布再印证，零 diagnostics 命中，处置权 WP-01 所有者；
  - **F-065（留痕日志中文乱码）**：本次 ird_gates 输出复现同现象（环境编码面，与本任务产物无关）；
  - **F-068/F-075（探针 mtime/头文件陷阱）**：本次验收主动以 touch 强制重编规避，未再现误判；
  - 其余 open 条目属 core/testkit/runtime/evidence/policy/io 等其他单元及 DOC/TK 家族文档面，与本任务无相关性、无静默积压。

### 4.9 commit 质量 —— 通过（附全量构建主张未独立复现的注记——数值面全部复现准确）

- `4d4bf83c`（唯一送验提交）`[WP-09-T09] 实现 diagnostics 诊断生命周期与容量（目录清理策略/宿主留痕形状）`：标题动词开头（"实现"）、≤50 字、任务编号前缀在位；正文四段齐备（What 按文件逐项含五方法语义与测试用例分族清单/Why 逐条 acceptance 与需求映射/验证含双模式 107/107＋14/14·ird_gates 7 命中基线·validate-task/verify-task PASS 真实结论/影响面"纯扩展既有行为零改动＋宿主零改动"）；`Acc-Covered: 1, 2, 3` 尾注与 acceptance 三条一一对应。**留痕声称与 ls-tree 逐件一致**（正文仅声称"两模式 gtest XML×4＋ird-test-report.json"——恰为在库 5 件，无 F-111 型幻影清单）。
- **数值主张复现核对**：双模式 107/107＋14/14 ✓、既有 98＋9 零回归 ✓（送验 XML＋验收者亲跑一致）、ird_gates 7 命中＝wp09-t08 基线逐码一致零 diagnostics ✓、validate-task PASS ✓、verify-task PASS ✓——全部准确，无粉饰。
- 注记（如实登记，非粉饰指控）：正文声称"集成模式全量构建（cmake --build build --config Release）exit 0"——验收者按 DIAG-T05~T08 先例只复现了两测试目标及其依赖链的构建（本任务改动面全覆盖），未独立复现全树构建（F-009 环境受限＋远超改动面）；该主张的引用留痕 integration-build.log 不在库（并入 F-113）。核心口径（本任务交付面的构建/测试/门禁/脚本）已全部亲手复现一致。

### 4.10 留痕完整 —— 字面部分未达成（建议级——F-113；沿 F-081→F-111 家族既判先例不阻断）

- **在库**（git ls-tree @4d4bf83c，builds/wp09-t09/ 共 5 件）：`ird-test-report.json`（schemaVersion ird-test-report/1，base 与送验范围一致，含 per-target 构建结论、gates 基线对比、selfCheck 探针能力说明、per-suite 逐用例 trace〔14 新用例逐条映射 acceptance/需求/矩阵行，断言值与源码逐项核对一致〕——结构化 resultNote 载体本身完整准确）＋**双模式 gtest XML 四份**（integration/smoke × diagnostics-test 107 用例/diagnostics-contract-test 14 用例，tests=107|14 failures=0 errors=0，DtLife1~5/DtPdiag7/HostTrace 全部在案）。
- **未入库而报告字段引用（各 .log）**：ird-test-report.json 的 gates 字段（gates-ird-gates-target.log、integration-build.log）与 selfCheck.validateTask 字段（verify-task.log、validate-task.log）共 4 件悬空引用——F-087/F-090 家族再现，转登 **F-113**（建议级）。**证据本体不缺**：双模式 107/107＋14/14、ird_gates 7 命中、validate-task/verify-task PASS 由验收者从零亲手复现一致（本记录 4.2/4.3/4.4/4.8）。
- `phase-one-validation.log`：历史载体（DIAG-T03~T08 验收记录同结论），本任务留痕按现行惯例落 `builds/wp09-t09/`，ird-test-report.json 即结构化 resultNote 载体——符合"phase-one-validation.log **或** resultNote 已更新"的替代口径。

### 4.11 偷懒/缩水实现扫描 —— 通过

- ①**标记扫描**：产品面四文件（Catalog.hpp/Catalog.cpp/CatalogLifecycleTest.cpp/CatalogHostTraceStubTest.cpp）`TODO|FIXME|placeholder|未实现|not implemented` 计数全 0（grep -c 实证）；`stub` 命中仅存在于测试文件名/桩类名（CatalogHostTraceStubTest/StubEvidenceEnvelopeStore/StubProjectCommandStore——**P-DIAG-8 桩验证纪律的契约要求形态**，DIAG-T05 ConfirmableProjectStubTest 同先例，非实现缩水标记，逐条豁免依据＝单元卡 §10 P-DIAG-8＋契约测试目标定位）；产品文件 stub 计数 0；diff 全文扫描仅有的 2 处 `Placeholder` 命中＝既有用例名 DtDiag2_ParamSchemaPlaceholderConsistency（DIAG-T04 产物，留痕 XML 数据行非代码标记）。
- ②**空实现检测**：公共接口全部真实实现——容量护栏六步（append 步骤⑥：超限腾位→软溢出标记→条目登记）、分级淘汰算法（evictForCapacity 多遍收敛＋applyEvictions 三表重建，Catalog.cpp:221-296）、消费确认/活动任务保护集合语义、溢出开发诊断组装与派发（Catalog.cpp:419-443）、五方法入口校验（0 容量 Usage/0 id Usage/幂等集合操作）。无空函数体/仅返回默认值的契约函数。
- ③**逐条证据表**：见 4.4——acceptance 3 条全部具名测试＋文件行号级证据，无一条仅凭"已实现"声明。
- ④**测试强度**：断言校验行为与边界（逐 entryId 在场/消失断言、溢出消息逐字段 evicted=1/retention-protected=1、默认值 10000 逐项、墙钟上界 <1 s/<4 s、字节级/摘要级三重不变、两会话异 id 同字节反例、Dev 拒绝零字节写入）；无恒真断言；4.5 双探针实证非恒真。

---

## 二、裁决

**verdict ＝ pass**

- 4.1~4.11 全部通过（4.10 的报告字段悬空引用为建议级 F-113，沿 F-081→F-111 家族既判先例不阻断），无阻断级问题：范围零越界、双模式构建/测试/门禁/脚本全部亲手从零复现成功（集成 107/107＋14/14、冒烟 107/107＋14/14、ird_gates 7 命中基线同分布零 diagnostics、validate-task/validate-docs/verify-task PASS）、红线零命中、注释无缺项、4.11 零命中（新增源码面）、双探针实证测试真实失败能力、无数值粉饰。
- 新增建议级登记 1 项：**F-113**（4d4bf83c 的 ird-test-report.json gates/selfCheck 字段悬空引用 4 件未入库 .log——F-087/F-090 家族再现、F-010 根因；提交正文本身干净；数值主张全部复现准确、无粉饰）。
- F-110 消账注记：本任务 diff 未触碰 Logging.cpp，"随下次触碰顺手更正"的建议无条件触发——继续 open，非落空。
- 环境类既有条目 F-007/F-009/F-019/F-051/F-062/F-065/F-068/F-072/F-075/F-097/F-098/F-099/F-101 本次复现再次实证（ini 模板补拷后零错误配置构建；ird_gates 基线 7 命中同分布；门禁输出乱码同现象；gate-all 环境受限），已在册、不重复登记。
- 本验收通过的是 **SHA 4d4bf83c11bfe6c626efe6d5d25084beff4671a4**，非分支引用；验收者未修改任何实施产物（探针注入/还原均在临时 worktree 内、最终零残留）、未向 wp09-t09 推送任何提交。

## 三、问题清单

| 级别 | 描述 | 处置 |
| --- | --- | --- |
| 阻断 | 无 | — |
| 建议 | **F-113**：4d4bf83c 的 traceability/builds/wp09-t09/ird-test-report.json gates 字段（gates-ird-gates-target.log、integration-build.log）与 selfCheck.validateTask 字段（verify-task.log、validate-task.log）引用 4 件 .log 均未入库（ls-tree 实证仅 5 件）——F-087/F-090 悬空引用家族再现（.gitignore『*.log』根因＝F-010，家族累计第 12 例）；提交正文干净（仅声称在库 5 件）。数值主张全部复现准确、无粉饰 | 已转登 findings.json（F-113，open）；修复方向同 F-087/F-090（gitignore 例外/强制 add -f/改 .txt 后缀/报告字段只引用在库文件）；不阻塞合入 |

## 四、验收者环境说明

- 复现 worktree（worktree A）：`D:/10_Source_Repos/21_robot/acc-diag-t09-a`（detached @ 4d4bf83c）；集成树从零配置（`RWS_BUILD_INDUSTRIALROBOT=ON`〔缓存 grep 确认〕、`RWS_USE_STATIC_LINK_PLUGINS=ON`、vcpkg toolchain、Qt `CMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`；`RobWorkStudio.ini.template.static`＋`ini.shared.in` 自主树补拷至 worktree 内 `RobWork/RobWorkStudio/bin/`——F-007/F-062/F-099 既有缺陷的环境修复，不触及送验产物）。冒烟树：`D:/10_Source_Repos/21_robot/acc-acc-t09-smoke`（独立目录＋vcpkg toolchain）。
- 工具链：VS 2022（MSVC，生成器 Visual Studio 17 2022，-A x64）、vcpkg 经典模式（gtest 1.18.0 @ installed/x64-windows）、pwsh（validate-task/validate-docs/verify-task.ps1）。
- 探针注入/还原均在 worktree A 内：两轮注入（markConsumed 不登记消费确认、configureCapacity 0 容量校验短路）各自重建重跑取证后逐字还原＋touch 强制重编（规避 F-068 mtime 陷阱），最终 `git status` 零 tracked 修改、grep `PROBE-TEMP` 计数 0、107/107＋14/14 复绿。
- 分支漂移检查：验收中段两次 `ls-remote` SSL 握手失败（DIAG-T08 验收同现象）；`fetch origin redesign-main` 成功后网络恢复，补测 `origin/wp09-t09`＝4d4bf83c（**无漂移**），并核实远程尚无 `acc/DIAG-T09/1` 分支（无 evidence 分支重名）。
- 验收记录与 F-113 登记落 `acc/DIAG-T09/1` 分支（基于 origin/redesign-main @ 87c1f06b）并推送；验收者日志随临时 worktree 清理，本记录内数值均为亲测。
