# EX-T09 验收记录（attempt 2）

| 字段 | 值 |
| --- | --- |
| 任务 | EX-T09（契约测试套件落地——FakeWorker/契约与故障注入；≙WP-08-T10） |
| 送验对象（冻结） | `8a5a05fbb77679c399fa23fd293be59bc3d9911a`（40 位；分支 wp08-t09 尖端；验收全程按该 SHA，不按分支名） |
| diff 范围 | `2ca48dace52a9f67aa570ef219aa828b6936f8eb..8a5a05fbb77679c399fa23fd293be59bc3d9911a`（state.base..headSha；base＝origin/redesign-main 尖端） |
| attempt | 2（state.attempts.accept＝2；attempt 1＝acc/EX-T09/1 @ 93819ac0，verdict=fail，唯一阻断 B-1＝9 件 .log 留痕未入库） |
| 复现现场 | `git worktree add --detach D:/10_Source_Repos/21_robot/acc-tmp/ex-t09-r2/repro 8a5a05fb…`（detached @ SHA）；集成树 build/ 与冒烟树均为本次从零新建，无实施构建树残留 |
| 记录回传 | 分支 **acc/EX-T09/2**（基于 origin/redesign-main＝2ca48dac…），路径 `traceability/acceptance/EX-T09-20260919-r2.md`，记录提交 SHA 由验收者向编排者回传三元组 |
| 验收日期 | 2026-09-19 |
| 验收者环境 | Windows 10 x64（10.0.26200）；VS 2022 Community（MSBuild 17.12.6）；CMake 4.3；pwsh 7（治理脚本）；Node 24（记录侧 JSON 解析） |
| **verdict** | **pass**（4.1~4.11 全部通过；零阻断级问题；建议级问题 4 项——F-224/F-225 新登＋F-222/F-223 由 acc/EX-T09/1 镜像转登，均不阻塞合入） |

---

## 独立性声明与环境前置（ACC §2/§3）

- **全新上下文**：本会话只读产物——代码、契约 `tasks/foundation/EX-T09.json`、单元卡 `units/execution.md`、在库留痕、`pipeline/state.json` 与 attempt 1 验收记录（后者仅用于①B-1 返工范围核对、②F-222/F-223 镜像保真、③F-068 探针方法论规避——EX-T01-20260918-r2 同款先例），未消费实施者过程叙述；一切结论以下述亲手执行为准。
- **复现现场**：集成树全新配置（F-007/F-062/F-098/F-200 已登记环境链，按 EX-T08 验收先例口径补齐）：`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=<主仓>/vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64 -DCMAKE_CXX_FLAGS="-DNOMINMAX -DWIN32_LEAN_AND_MEAN -DWIN32"` → **exit 0**，缓存 `grep RWS_BUILD_INDUSTRIALROBOT build/CMakeCache.txt` → **`RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`** 实测；未跟踪辅助件（`RobWorkStudio.ini.shared.in`/`.template.static`）自主仓补拷（bin/ 被 .gitignore 排除——F-007/F-200 口径，验收会话自备不入版本控制）。冒烟树另建 `acc-tmp/ex-t09-r2/smoke-build`（源＝worktree A 的 industrialrobot/，带 vcpkg toolchain 绝对路径——CORE-T01 G-2 教训）。
- **操作披露（纯验收侧，与送验对象无关）**：①首配未带 toolchain 失败于 Boost NOTFOUND（F-007/F-009 所指环境链），按先例补齐后一次通过；②gtest `--gtest_output=xml:` 以 POSIX 风格路径（/d/…）传参时 Windows 版 gtest 静默不落盘，改 Windows 风格路径（D:/…）后四份 XML 全部落位。
- **记录回传现场**：`git worktree add -b acc/EX-T09/2 D:/10_Source_Repos/21_robot/acc-tmp/ex-t09-r2/record origin/redesign-main`。

## 分支漂移检查 —— 通过（ACC §3 送验对象冻结）

- `git rev-parse origin/wp08-t09 wp08-t09` → 两引用均＝`8a5a05fbb77679c399fa23fd293be59bc3d9911a`＝送验 headSha，**零漂移**。

## 4.1 改动范围 —— 通过

- diff 文件清单（`git diff --name-status 2ca48dac..8a5a05fb`）：**33 个文件**＝execution 单元 11 个（CMakeLists.txt 改；test/ 新增 ContractSuiteFacilities.hpp、ContractSuiteFacilitiesTest.cpp、TestMainReport.cpp；contract_test/ 新增 ArchiveFaultContractTest.cpp、RecoveryProcessContractTest.cpp、RecoveryChild.hpp/.cpp、ContractTestMain.cpp，修改 LinkageContractTest.cpp、WorkerProcessContractTest.cpp）＋units/execution.md 1 个＋traceability/builds/wp08-t09/ 留痕 20 个＋traceability/findings.json 1 个。
- 机械过滤（三前缀正则 `execution/**`、`units/execution.md`、`traceability/**`）：**33/33 全部命中 allowedFiles，越界文件空集**。
- `forbiddenFiles`（REQUIREMENTS.md、ARCHITECTURE.md）：diff 中**零触碰**（机械核对空集）。
- SA-02：diff 中无 industrialrobot/ 之外任何框架源码文件；`patches/` 零改动（`git diff --name-only <base>..<head> -- …/patches/` 计数 0）——**零未登记框架改动**。

## 4.2 可复现构建 —— 通过（双模式零错误，验收者亲手）

- **集成模式**：全新配置（见环境前置）后全量 `cmake --build build --config Release` → **final-exit=0**，`error C[0-9]|error LNK[0-9]|error MSB[0-9]|fatal error` 计数 **0**（全量日志留验收临时目录 build-integrated.log；F-008 基线插件域本次亦零错误——三宏基线口径下全新树一次通过）。
- **独立冒烟模式**：另配全新目录（带 vcpkg toolchain）配置 **exit 0**（Configuring done/Generating done，零 error；smoke-config.log）＋全量构建 **final-exit=0**、错误计数 **0**（smoke-build.log）；两测试目标与 worker exe 均产出。
- 在库留痕交叉比对：`build-smoke-config.log`/`build-smoke-full.log` 与本验收者复跑结论一致（另见 4.10 对摘录型日志的载体观察 F-225）。

## 4.3 契约 verify 命令 —— 通过（2/2 逐条亲手）

- verify[1]：`cmake --build build --config Release --target sdurws_ird_execution_test` → **exit 0**（exe 产出）。
- verify[2]：`cmake --build build --config Release --target sdurws_ird_execution_contract_test` → **exit 0**（exe 产出）。

## 4.4 acceptance 条目逐项核对 —— 通过（5/5 有证据，验收者亲手）

| # | 契约 acceptance | 验收证据（均为验收者亲手执行/逐行核对） |
| --- | --- | --- |
| 1 | §11 矩阵全组逐项执行留痕＋落位双测试目标 | ①组覆盖：两目标源码中 EX_ 具名断言扫描——EX-SUB×8/EX-SM×13/EX-WKR×7/EX-CHN×5/EX-CKP×5/EX-CCH×3/EX-REG×11/EX-ORT×4/EX-ARC×8/EX-RCV×1/EX-RES×3 全组在案，EX-BLD 由 `ExecutionBuild.*` 七用例＋`ExecutionLinkage` 两用例承载，**12 组全覆盖**；②执行留痕（亲手）：集成 149/149＋38/38、冒烟 149/149＋38/38，四份 gtest XML（tests=149/38、failures=0×4）＋两份 ird-test-report.json（passed=149/38、failed=0）全部 PASSED；③目标分工：CMakeLists `sdurws_ird_execution_test`（test/* 纯逻辑替身）与 `sdurws_ird_execution_contract_test`（contract_test/* 跨单元＋真进程）两目标源清单逐行核对与 §11 分工原文一致；④真进程稳定性：契约套件连跑 3 次 38/38（与实施声称一致）。 |
| 2 | 替身边界声明在案＋faultPointId 命名登记 | `test/ContractSuiteFacilities.hpp` 文件头逐项声明（ScriptedEvaluator 双端形态/FakeWorkerMain＝worker exe 内置脚本宿主不另造第二 worker/FaultInterceptor 接缝/ManualClock/TempDir+DeterministicEnv＋R-7/A-9"不构成业务算法正确性证明"）；faultPointId 七项登记（execution/process-launcher/create、channel/send-frame、channel/recv-frame、archive/{begin,write-batch,finalize,abandon}）由 `ContractSuiteFacilitiesTest.FaultPointIdRegistryMatchesSection11Handover` 具名断言钉住（本验收亲跑通过）；EX-ARC-1 的 FaultInterceptor 实接线（FaultArchivePort.shouldFire(kFaultArchiveWriteBatch)+FaultLog 断言）逐行核对在案。 |
| 3 | TestProcessRunner 按 TK-T11 触发接入真进程场景 | EX-RCV-1＝`RecoveryProcessContractTest`（TestProcessRunner start→EventWatch.awaitFile→kill→重启开库 D-02→listRuns 空→预留批次字节断言→T14 重建＋EX-TASK-INTERRUPTED）亲跑 PASSED；EX-WKR-2 真进程面由 EX-T06 期 WorkerProcessContractTest 既有用例承载（38/38 内亲跑通过）；testkit §10.1 触发登记兑现（ProcessRunner/Fault/RecordListener 均为本目标实际链接消费）；无"前置未就绪"用例（TK-T11 已交付）。 |
| 4 | P-EX-8 处置：空实现占位＋断言只针对收编 18 项码值 | `NullExecutionDiagnosticsSink` 空实现两方法（project §3.2 同模式）；`registeredExStableCodes()` 18 项**与权威源逐字一致**（本验收者从 diagnostics 单元 `src/DiagCodes.cpp` 提取全部 EX-* 码排序比对：两集合相等）；`isRegisteredExStableCode` 自检闸内嵌于 CollectingSink.hasReportCode（清单外码值即测试自身失败——具名断言亲跑通过）；全 diff 无私定新码值（EX-* 字面量扫描仅上述清单成员）。 |
| 5 | AT-10/11/13/34 载体就位＋ManualClock 纪律零 sleep 断言 | AT 登记：ird-test-report.json 中 AT-10×6/AT-11×1/AT-13×2/AT-34×2（源码 IRD_TEST_INFO 七处亲点）；ManualClock 虚拟推进（EX-ARC-4 形态二 advance(6000) 越阈、无 sleep 断言；时钟敏感用例退避经 DelayFn 记录器断言值非真实等待）；真进程泵循环中的有界 Sleep/轮询均为 I/O 到达观测窗口非时序判据（逐处注释声明）。 |
| 附 | ird_gates | 验收者亲跑 `--target ird_gates`：exit 1（既有登记例外红＝设计行为），唯一命中集合 **18 项**与在库 `gates-t09-hits-sorted.txt` 逐项吻合，其中 execution 两测试目标→sdurws_ird_testkit 恰为本任务契约明文新增（F-221 所指"新增恰 2、移除 0"独立证实）。 |

## 4.5 测试真实失败能力 —— 通过（2 个实现侧失效注入，均即红＋还原复绿）

| 探针 | 注入点（实现侧，非测试侧） | 预期 | 实测 |
| --- | --- | --- | --- |
| A | `src/StateMachine.cpp` forRecoveryInterrupted 删除 EX-TASK-INTERRUPTED 诊断注入 | EX_RCV_1 红 | **红**：exit 1，`RecoveryProcessContractTest.cpp(236) hasInterruptedCode` 断言失败，恰 1 FAILED |
| B | `include/.../RunRegistry.hpp` archiveBackoffCap {4000}→{3999} | EX_ARC_1 红 | **红**：exit 1，`ArchiveFaultContractTest.cpp(584) delays[1]≠4000ms` 断言失败，恰 1 FAILED |
| 还原 | 两处均 `git checkout --`＋`touch`（规避 F-068 mtime 陷阱）后重建 | 复绿 | **复绿**：契约套件 38/38 PASSED；`git status` 全程零残留（还原后空输出） |

- 方法论披露：探针 B 首次取 {4001}（上限不约束 1000×4=4000 序列，可观测行为不变、测试仍绿）——属探针设计失误而非测试恒真，改 {3999} 后即红；如实记录不粉饰。
- 两探针证明：恢复重建诊断语义与 D-11 有界退避值均被测试真实钉住（非恒真、非"只编译通过"）。

## 4.6 架构红线 —— 通过（AGENTS §5 八条逐条）

1. **L2 零 Qt**：diff 新增 include 面全量扫描（机械提取 41 条 `#include`）——零 Qt 头；产品库/worker 链接面零变更；`ExecutionBuild.NoQtInclude_DT_BUILD_NFR_MNT_01` 亲跑通过。
2. **R-1 业务域互链禁止**：产品目标链接面不变（core/evidence/project 三条登记边）；新增仅两测试目标→sdurws_ird_testkit（T-1 允许形态，契约 acceptance 明文消费边——ird_gates 亲跑命中集合证实无其他新增）；配置期 FATAL 守卫与 `UnitEdgesRegisteredOnly` 用例双重钉住。
3. **R-2 跨单元私有头**：新增 include 仅 `<sdurws/ird/{core,evidence,project,testkit}/…>` 公共头＋本单元相对包含＋std/gtest/windows.h（测试 TU）；ContractSuiteFacilities.hpp 置于 test/ 私有包含根不入 include/ 公共根（`PublicHeaderPathLayout_DT_BUILD_R2` 亲跑通过）。
4. **SA-02 框架零修改**：见 4.1（patches/ 零触碰）。
5. **R-4 名称前缀拼接/剥离**：diff 中无 RobWork 名称拼接/剥离逻辑（真进程场景经 IRD_EXECUTION_WORKER_EXE 编译定义注入路径——非名称解析）。
6. **PA-1 权威唯一**：测试仅观测与断言；码表只读镜像＋自检闸（不私定语义）；登记/判定/归档语义全部消费真实实现（RunRegistry/ResultAdmission/ProjectStore），零越权实现。
7. **PA-2 不可变历史**：diff 无修订/历史写面触碰。
8. **不继承 old/**：diff 无 old/ 引用。

## 4.7 注释规范 —— 通过（公共设施头全查＋实现文件抽查 6 件）

- **公共头全查（本 diff 唯一新头）**：`test/ContractSuiteFacilities.hpp`——文件头设计依据（单元卡 §11/§12/§13＋testkit §6.4/§6.5/§6.6/§7.2/§7.3＋契约 acceptance 2/4/5）、替身边界声明全项（acceptance 2 落文件面）、P-EX-8 处置口径、线程约束（测试单线程非线程安全）、构建边界（T-1）；类/函数 Doxygen 中文齐备（ManualClock 用法纪律与 ms 单位、NullExecutionDiagnosticsSink 占位背景与裁决归属、ScriptedOutputDecoder FIFO/耗尽 fail-closed 语义）；faultPointId 七常量逐个注明接缝含义。
- **实现/测试文件抽查（≥3 达成，实查 6）**：ArchiveFaultContractTest.cpp（文件头设计依据/与既有用例零重复声明/替身边界＋夹具确定性上下文＋D-11 退避值"断言值"注释＋缓存跳过对照闭环注释）；RecoveryProcessContractTest.cpp（场景边界如实声明——project 侧呈现面归 PM-08 不私改对端；步序注释"算法第几步/失败走到哪"）；RecoveryChild.hpp/.cpp（停靠协议/错误语义＝非零退出码显性失败无假阳性通道/每步业务含义）；ContractTestMain.cpp＋TestMainReport.cpp（双形态协议时序——子进程拦截先于 InitGoogleTest；静态存储期监听器所有权依据）；ContractSuiteFacilitiesTest.cpp（逐用例设计依据＋断言失败文案中文业务语义）。
- **§2.5 高危信息表**：物理单位（退避 ms、2 s/10 s 协作窗、5 s 兜底阈值为"实现参数非上游值"显式声明）；坐标系/角度制式（本 diff 纯编排/存储语义，不涉——无隐式约定处）；所有权（FaultArchivePort 对 real 端口 null 删除器非所有权语义逐处注明；gtest 监听器静态存储期依据）；线程约束（设施单线程声明、gtest 串行纪律）；错误语义（子进程非零退出码＝环境错误显性失败；dev/stable 诊断边界——deverror stableCode 空＋通道名前缀）；确定性来源（脚本 FIFO 应答序、faultPointId 登记序固定、ManualClock 单调、镜像清单按码表序誊录）。**必填项零缺失**。

## 4.8 文档同步与 findings.json 消账核对 —— 通过

- **单元卡增量修订**：units/execution.md §15.4 v0.11 行（diff 实证）逐件登记落位（§11 矩阵缺口六行/testkit 设施接入与 T-1 收窄重述/替身边界与 faultPointId/P-EX-8/AT 载体/ird_gates 登记＋F-221 转登）；卡头版本 v0.5→v0.11 追平（F-175 家族微瑕消账）；契约语义零变更（本文纯落位登记）。实现与任务卡零未登记偏差（本验收逐条 acceptance 对照无出入）。
- **findings.json 消账核对（ACC §4.8 v1.2；基于 redesign-main 版 220 条＋任务分支 F-221 全量过筛 191 open）**：
  - **F-221**（本任务实施侧自查转登：ird_gates 新增 2 命中）：本验收亲跑门禁独立证实"新增恰 2、移除 0"——如实登记非缺位，实例行增补归 WP-01-T03，保持 open（门禁所有者）。
  - **F-031**（EX-T09 契约 acceptance 5 括注 AT-34 出处失准）：属契约文本辖区（契约文件不在本任务 allowedFiles，实施者无从修正），登记所有者含契约编译治理所有者——保持 open，非本任务义务；实体主张（AT-34 载体＝§11 EX-SM-3 行）已由本验收证实就位。
  - **F-212/F-213/F-216/F-219**（EX-T05/T07 测试敏感度建议）：涉案文件（EventBusDeliveryContractTest/ResourceGovernanceTest/CheckpointCacheTest 等）本 diff 零触碰，无顺手消账义务；本任务 4.5 双探针证明新增用例具真实红能力。
  - **F-217 家族**（NOMINMAX 无包裹 define→C4005）：**本任务新增文件同型扩员**（RecoveryProcessContractTest/ArchiveFaultContractTest——fresh 集成树实测各 +2 C4005）→ 新登 **F-224**。
  - **F-222/F-223**（attempt 1 转登：293ef150 标题名词开头／.gitignore *.log 结构性冲突）：acc/EX-T09/1 未合入、redesign-main 版无该两条——本记录分支**镜像转登**（与 attempt 1 版同号同义，EX-T01-20260918-r2 F-206 镜像先例；PIPE §4.7③ 并集取一）；F-222 所指事实本验收复核成立（293ef150 标题"单元卡…"名词开头，353b3362"落地…"与 8a5a05fb"补交…"动词开头合规）；F-223 的 EX-T09 实例（B-1）已由 8a5a05fb add -f 补交消账、结构性根源保持 open。
  - **F-008/F-007/F-062/F-200**（worktree/基线构建环境链）：本验收再次复现并按登记口径处置（见环境前置），无新义务；F-008 本次带三宏基线未复现失败，与登记一致。
  - **无可随本任务顺手消账的 open 条目**（其余各家族与 execution 契约套件零交集）。

## 4.9 commit 质量 —— 通过（附镜像登记的既有风格瑕疵）

- **353b3362**「[WP-08-T09] 落地 execution 契约测试套件（§11 矩阵补齐＋testkit 设施接入）」：动词"落地"开头合规；正文四段齐备；验证段数字（149/149＋38/38、双模式零错误）经本验收者亲手复跑全部证实。
- **293ef150**「[WP-08-T09] 单元卡 v0.11 落位登记＋门禁/测试证据留痕」：正文四段齐备、留痕清单与在库 20 件一致；唯一瑕疵＝"单元卡"名词开头（F-012 家族——attempt 1 已转登 F-222，本分支镜像登记；纯风格不阻断）。
- **8a5a05fb**「[WP-08-T09] 补交九件验证 .log 留痕修复 B-1（Attempt 2 返工）」：动词"补交"开头合规；正文四段齐备；验证段**如实声明**"构建与测试本提交未重跑（依据验收记录返工输入）+ 零代码目标变更"——非粉饰；本验收以送验 SHA 全量重跑补足该实证（4.2/4.3/4.4 全绿）。
- **数字口径核对**：8a5a05fb 正文"gates exit 1＋19 命中"＝原始命中行计数（LIB io 因 libzip/expat 各一行）；归一化 token 口径 18 行——两口径均自洽且与在库清单/本验收亲跑一致，非失实。
- 提交粒度：实现＋测试＋CMake＋单元卡同链三提交、留痕补交独立成提交且正文载明勘误来源——符合"一次修改一次提交、验证如实"口径。

## 4.10 留痕完整 —— 通过（attempt 1 阻断项 B-1 已修复）

- **B-1 消账核实**：attempt 1 阻断的 9 件 .log（构建日志×3/运行日志×4/门禁原始日志×1/validate-task.log×1）现全部在库（`git diff --name-status` 20 件 builds/wp08-t09/ 均为 A；`ls -la` 实测 20 文件非零字节）——提交正文声称 20 件与在库**完全对齐**。
- 在库 XML 汇总亲验：四份 gtest XML tests=149/149/38/38、failures=0×4；两份 ird-test-report.json（passed=149/38、failed=0、skipped=0）且两模式版本真实分歧（byte 2624 起为 duration 差异——非复制件）。
- validate-task：在库 `PASS (1 tasks)`；本验收者在 worktree A 亲跑 **exit 0＝PASS (1 tasks)**。
- gtest XML＋ird-test-report.json 并存机制：两测试目标自有 main 安装 RecordListener（本验收亲跑产出同构双产物）。
- 载体质量观察：在库三件构建日志为筛选摘录（无错误计数/最终状态行，详见 F-225）——结论经本验收独立复跑证实为真，仅载体弱，不构成返工项。

## 4.11 偷懒/缩水实现扫描 —— 通过

- **①标记扫描**：diff 新增行 `TODO|FIXME|stub|placeholder|未实现|not implemented` 扫描——唯一命中＝测试名 `NullExecutionDiagnosticsSinkIsEmptyPlaceholder_PEX8`；豁免依据：契约 acceptance 4＋单元卡 §15.3 P-EX-8 明文要求的"空实现占位"本体（契约显式标注的占位，非偷懒）。
- **②空实现检测**：新增代码空/默认值函数体仅 NullExecutionDiagnosticsSink.report/reportDev 两方法——同上契约明文豁免（占位语义逐行注释）；无未声明的空实现公共接口。
- **③逐条证据表**：见 4.4 表——契约 acceptance 五条逐条给出具名测试/命令输出/文件行号，任一无证据即 fail 的口径下五条全部有证据。
- **④测试强度**：4.5 双实现侧探针即红（T14 诊断语义、D-11 退避值均被钉住）；断言校验行为与边界（恰 3 次写调用、退避序列值、FaultLog 命中数、检查点字节逐位一致、manifest 有无、缓存入账对照闭环）——非恒真、非"只编译通过"。

---

## 问题清单（分级）

| # | 级别 | 问题 | 处置 |
| --- | --- | --- | --- |
| G-1 | 建议 | F-217 家族同型再现：本任务新增 RecoveryProcessContractTest/ArchiveFaultContractTest 两文件无包裹 #define NOMINMAX/WIN32_LEAN_AND_MEAN，fresh 集成树实测 +4 处 C4005（零行为影响） | **新登 F-224**（findings.json，随本记录分支提交） |
| G-2 | 建议 | 在库三件构建留痕为筛选摘录（无错误计数/最终状态行），"零错误"从日志自身不可自证——本验收独立复跑证实为真 | **新登 F-225**（同上） |
| G-3 | 建议（沿袭） | 293ef150 标题名词开头（F-012 家族）；.gitignore *.log 与留痕要求结构性冲突（EX-T09 实例已消账、根源未除） | F-222/F-223 **镜像转登**（acc/EX-T09/1 未合入——同号同义，收尾并集取一） |
| G-4 | 所有者环节 | F-031（EX-T09 契约 acceptance 5 括注 AT-34 出处失准）登记在案未修正——契约文本辖区（不在本任务 allowedFiles），实体主张已就位无实害 | 保持 open（契约编译治理所有者）；不阻塞合入 |
| —— | 沿袭登记 | F-008/F-007/F-062/F-200 环境链本验收再次按登记口径处置；F-221 门禁基线新增 2 命中经独立复跑证实如实 | 保持 open（各登记所有者）；无本任务新义务 |

## 裁决

**verdict＝pass**：4.1~4.11 全部通过、零阻断级问题；attempt 1 唯一阻断 B-1 已消账（9 件 .log 在库、留痕半区补全）。建议级 G-1~G-3 已按 ACC §5 转登/镜像登记 findings.json，G-4 为所有者环节既有登记——均不阻塞合入。本验收未修改实施产物（探针注入已逐字节还原，worktree A `git status` 干净）；未向任务分支 wp08-t09 推送任何提交；送验对象保持冻结于 8a5a05fbb77679c399fa23fd293be59bc3d9911a。
