# WP-15-T08 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | WP-15-T08（实现会话姿态承载与设默认命令门面——KIN-06/14） |
| 单元 | kinematics |
| 送验对象（冻结） | 6b5b0fa69e26096095b11679ef6c1f711cf35408（单提交，即 `origin/wp15-t08` 尖端——分支漂移检测：`git rev-parse origin/wp15-t08` ＝ 该 SHA，零漂移） |
| diff 范围 | 1b166a7f9a4529eda48dbbaed69a0b186e0c3aec..6b5b0fa69e26096095b11679ef6c1f711cf35408（base＝redesign-main 1b166a7f） |
| 验收日期 | 2026-09-27 |
| 验收者 | 独立上下文对抗式验收会话（全新会话，未消费实施叙述） |
| **verdict** | **pass**（4.1～4.11 全部通过，零阻断级问题；建议级 2 条转登 findings.json：F-401/F-402） |

## 环境说明（复现现场）

- worktree A：`D:/10_Source_Repos/21_robot/.acc-t08/wtA`（detached @ 6b5b0fa69e26096095b11679ef6c1f711cf35408）。worktree 缺 gitignored 两件模板（`RobWorkStudio.ini.template.static`＋`RobWorkStudio.ini.shared.in`——F-007/F-062/F-099 在册既有缺陷），自主检出只读补拷至 worktree 内 `RobWork/RobWorkStudio/bin/` 后配置通过（环境修复，不触及送验产物，DIAG-T05～T09 验收同先例）。残留进程检查：`tasklist` 无 RobWorkStudio.exe（零冲突）。
- 集成模式：**从零重新配置**于验收者自建临时树 `D:/10_Source_Repos/21_robot/.acc-t08/build-integ`（`cmake -S <wtA>/RobWork -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=<主仓>/vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`；Configuring done 63.6s 零 CMake Error；缓存 grep 确认 **`RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`**）。不触碰主检出 `build/`。
- 冒烟模式：独立树 `D:/10_Source_Repos/21_robot/.acc-t08/build-smoke`（`cmake -S <wtA>/RobWork/RobWorkStudio/src/rwslibs/industrialrobot`＋vcpkg toolchain＋同款 Qt6 前缀；配置 8.2s 零错误；`cmake --build ... --config Release` 全树 **exit 0 零错误**）。
- base 侧 worktree：`D:/10_Source_Repos/21_robot/.acc-t08/wtBase`（detached @ 1b166a7f）——仅用于 ird_gates 引擎直跑比对（无需构建），用毕即删。
- 主检出零写操作：全程未切换主检出分支、未在主检出执行 git 写操作、未修改主检出任何文件（模板只读复制来源除外）。

## 4.1 改动范围 —— 通过

`git diff --name-status 1b166a7f..6b5b0fa`：共 30 文件，逐一比对契约 `allowedFiles`：

- `RobWork/RobWorkStudio/src/rwslibs/industrialrobot/kinematics/**`×8（CMakeLists.txt M；Commands.hpp/.cpp、CommandFacadeContractTest.cpp、CommandsTest.cpp A；DiagCodes.hpp/.cpp、Sampling.hpp、DiagCodesTest.cpp M）✓
- `RobWork/doc/industrial-robot-design/units/kinematics.md` M ✓
- `RobWork/doc/industrial-robot-design/traceability/builds/wp15-t08/**`×21 A ✓（evidence 目录 19 件＋base_hits.norm＋head_hits.norm＋base-head.diff）

`forbiddenFiles`（REQUIREMENTS.md/ARCHITECTURE.md）零触碰 ✓；`patches/` 无新文件（SA-02 无登记外框架改动；ird_gates SA-02 检查 head 侧零命中佐证）✓。

## 4.2 可复现构建 —— 通过（两模式零错误，验收者亲手从零重配）

- 集成：`--target sdurws_ird_kinematics` exit 0 → `sdurws_ird_kinematics.lib`；补建 `sdurws_ird_kinematics_test`／`sdurws_ird_kinematics_contract_test` 各 exit 0（验收日志：`.acc-t08/acc-integ-build-{kin,test,ctest}.log`）。
- 冒烟：全树构建 exit 0 零 error（`.acc-t08/acc-smoke-build.log`；三 kinematics 目标 `.lib`＋两 `.exe` 产出行在案）。

## 4.3 契约 verify 命令 —— 3/3 逐条亲手执行成功

| # | 命令 | 结果 |
| --- | --- | --- |
| 1 | `cmake --build build --config Release --target sdurws_ird_kinematics` | exit 0（验收者临时集成树等价执行） |
| 2 | `ctest --test-dir build/RobWorkStudio/src/rwslibs/industrialrobot/kinematics -C Release -L "^ird$"` | **100% tests passed, 0 failed out of 2**（sdurws_ird_kinematics_test 0.30s＋sdurws_ird_kinematics_contract_test 0.25s） |
| 3 | `pwsh -File RobWork/scripts/industrialrobot/validate-docs.ps1`（worktree A 内） | **PASS (20 units, 12 trace entries, 189 task files)** |

额外（非 verify 数组）：`validate-task.ps1 -TaskFile <契约>` → PASS (1 tasks)。verify 数组缺两测试目标构建行属 F-397/F-399 家族复发 → 建议级转登 **F-401**；验收者已在全新构建树自行补建并执行（82/53 实测，不构成阻断）。

## 4.4 acceptance 条目逐项证据 —— 4/4 通过

| # | 契约条目 | 证据（具名测试/输出/行号，全部验收者复跑） |
| --- | --- | --- |
| 1 | 会话姿态（L-K4）零修订/零失效/不入身份；requestIdentity 不变（V-18；KIN-06/AT-04） | ①`KinSessionPose.SessionOpsProduceZeroGatewayTraffic_WP15T08_ACC1`：set/resetHome/clear 全序列后计数网关 `submitCalls==0`＋`fetchCalls==0`＋无信封（修订计数恒 0 的运行期证明；CommandsTest.cpp:328~349）；②`SessionPoseNotInRequestIdentity_WP15T08_ACC1`：会话姿态多次变化前后 IkRequestIdentity 六要素逐字段相等＋referenceQ 变化对照组（身份灵敏度面，:357~392）；③类型面结构保证：KinSessionPose 零端口/事件/身份字段（Commands.hpp:109~167）；④`SetGetRoundtripAndClear`／`ResetToHomeWritesSuppliedHome`／`NonFiniteQFailsFast`（NFR-COR-03 fail-fast） |
| 2 | 设默认 TCP/设备（L-K9）经①端口提交 modeling `apply-robot-design`（KIN-14/AT-27；P-KIN-5 知会登记） | ①`SetDefaultTcpAssemblesApplyRobotDesignEnvelope_WP15T08_ACC2`：token 同串＋版本 2＋branch/tip 绑定＋补丁透传＋**手排金标载荷逐位一致**＋Committed 回显新修订（:405~451）；②`PayloadFrameDecodesByIndependentReader_WP15T08_ACC2`（契约）：**IRDMCP2 金标逐位**＋测试内独立解码器严格回解（magic 7B/版本/mode=Apply/单根槽/allocateNew=0/oid/token/根字节透传/removals 空段/无尾随字节，ContractTest.cpp:166~216）；③`EncodeDeterministicBytes`（NFR-COR-02）；④失败=零修订＋诊断：`SetDefaultTcpPatcherFailureZeroRevisionZeroSubmit`（恰一条 KIN-ROOT-BYTES-ILLEGAL、subject=根、submitCalls==0，:478~501）＋`SetDefaultTcpNoDeviceRootZeroSubmit`（两轨 KIN-NO-DEVICE，:508~540）；⑤`setProjectDefaultDevice`＝根指定修订（槽字节=基线原样——零内容变更零引用触碰，:547~573）；R1 语义登记随卡 §14.6 v0.8①-c；P-KIN-5 知会登记卡 v0.8③ |
| 3 | 只读 writable=false 拒绝＋诊断透传不自裁（PA-1）；L-K5 回填仅显示默认；D-KIN-4 禁隐式读会话；门面无自有 schema | ①`ReadOnlySessionRejectionPassthrough_WP15T08_ACC3`：提交交①端口判定（submitCalls==1）＋NotCommitted 零修订＋对端诊断**原样透传**（:631~663——变异 M2 实证该测试钉住透传语义）；②L-K5：`isSet()`/`jointConfiguration()` 为唯一显示回填读点（Commands.hpp:147~159 注）；③D-KIN-4：acceptance 1 之②身份对照测试；④`NoOwnDomainCommandSchema_WP15T08_ACC3`：命令常量封闭集恰 1（FacadedModelingCommand）＋全公共头零带引号 `"kinematics.` 字面（ContractTest.cpp:229~260） |
| 4 | O-35 不适用性登记；无点 token 服从裁决；§9.8 七条仅 ui 词表 | ①`O35NoDotTokenAndValuePinning_WP15T08_ACC4`：`apply-robot-design`/`robot-design` 同串＋正则 `^[a-z0-9-]{3,64}$`＋零点双保险（ContractTest.cpp:141~159）；②卡 §14.6 v0.8② O-35 不适用性登记行；③§9.8 `kinematics.*` 七条为 ui CommandId 词表登记（卡 :748 起；落位随 T12 插件，本单元公共头零字面——契约测试扫描 2 复核） |

## 4.5 测试真实失败能力（变异测试）—— 通过（2 处实质变异均真实变红，已还原）

- **M1 载荷信封编码字节**：`src/Commands.cpp` 段 3 `putU32(out, 0U)` → `putU32(out, 1U)`（mode Apply→Restore）→ 重编译后 `SetDefaultTcpAssemblesApplyRobotDesignEnvelope_WP15T08_ACC2`、`EncodeDeterministicBytes_WP15T08_ACC2`、`PayloadFrameDecodesByIndependentReader_WP15T08_ACC2` 三具名测试 **FAILED**（金标逐位比对与独立解码器真实钉住字节布局——非恒真）。`git restore` 还原。
- **M2 可写性透传语义**：`assembleAndSubmit` 尾部改为 `out.diagnostics.clear()`（吞对端诊断）→ `ReadOnlySessionRejectionPassthrough_WP15T08_ACC3` **FAILED**（透传不吞的断言真实钉住 PA-1 语义）。`git restore` 还原。
- 还原后全量重跑：**82/82＋53/53 全绿**（先还原再跑全量——T14 教训时序；变异窗口与冒烟构建时序隔离：冒烟树三 kinematics 目标在 M1 前已编完，两变异仅触及 .cpp 不触头文件，零污染）。

## 4.6 架构红线 —— 通过（八条逐条）

1. **L2 零 Qt**：kinematics include//src 产品面 grep `include.*Qt`/`<Q` 零命中；ird_gates R3 的 20 条命中全部属 ui 单元（kinematics 零 R3）✓
2. **R-1 禁互链**：零 modeling/project include（grep 仅注释提及）；门面经自有投影接口 IKinProjectCommandGateway＋宿主注入（O-37 纪律）✓
3. **R-2 私有头**：仅 include core/runtime 本单元公共头（DiagData/Identity/Errors/KinTypes/runtime Errors）；gates R2 零命中 ✓
4. **SA-02**：diff 无框架文件；gates SA-02 零命中 ✓
5. **R-4**：gates R4 命中仅 project 单元存量 1 条（base 同款），kinematics 零命中 ✓
6. **权威唯一（PA-1）**：writable 判定、命令断言、编解码、诊断词表均透传不自裁（M2 变异实证钉住）✓
7. **不可变历史**：门面只经①端口产生新修订，无任何直接写路径 ✓
8. **不恢复 old/**：无 old/ 引用 ✓
ird_gates 引擎直跑 base..head 比对：**零新增零消除**（见 4.11③ 附件核对）——新增命中为零，无 sanctioned 例外需核对。

## 4.7 注释规范 —— 通过

- **Commands.hpp**（新公共头，全查）：文件头设计依据/背景说明/线程安全/确定性齐备；每类/每函数中文 Doxygen（@brief/@param [in]/@return/@throws）；§2.5 高危信息全项：单位（rad/m）、坐标系、所有权（gateway/patcher 非 owning、宿主持有）、线程约束（KinSessionPose 仅 UI 线程；gateway 并发安全）、错误语义（调用方违约 fail-fast vs 数据侧 KIN-ROOT-BYTES-ILLEGAL 诊断轨逐点标注）、确定性来源（框架编码纯位组装）。
- **Commands.cpp**（新实现，全查）：执行序逐步骤编号注释＋分支业务语义＋魔法数零存在（常量具名）；诊断产码经在册常量禁拼码注明。
- **抽查 ≥3 实现文件**：CommandFacadeContractTest.cpp／CommandsTest.cpp（替身说明＋逐用例 IRD_TEST_INFO 需求/AT 追溯）／DiagCodes.cpp（行 17 逐码分类/重试族落值注释）／Sampling.hpp（F-395/F-396 纠正点注释带督办编号溯源）——全部合格。

## 4.8 文档同步与 findings 相关性 —— 通过

- 单元卡 v0.8（卡头版本行同步）：§14.6 五项＋督办兑现＋测试登记逐项与实现一致（diff 实证）；§9.6 表尾追加行 17 `KIN-ROOT-BYTES-ILLEGAL`（error，:742）；§11 T08 行置已完成；封闭断言 16→17 随附同步合法（DiagCodesTest 全表断言同步扩行，8 error＋9 warning——在册"表尾追加随附同步"纪律）；§3.3 布局表 Commands.hpp 行 T08；构建落位行补 T08。
- **F-395/F-396 督办兑现实证（逐字节）**：head Sampling.hpp:451 登记改「"IRDSSID"（7 字节 ASCII magic……F-395 督办纠正）」与 Sampling.cpp:148~149 实写 `magic+7` 对齐 ✓；:532 改「N=1 时退化为 +X……F-396 督办纠正」与实现公式（Sampling.cpp:229）一致 ✓。**建议治理收尾将 F-395/F-396 置 fixed**（本记录即兑现证据）。
- findings.json 未关闭条目相关性：F-386/F-389/F-391（消费面/评估器面）本任务 diff 未触及（无 Evaluators.cpp/FkTest.cpp 变更），维持 open 无遗留义务；F-394 已随 T06/T07 兑现（F-400 在案）；F-398 计数口径纪律本任务已遵守（三套计数分列、口径注明捕获模式）；F-397/F-399 家族在本契约 verify 数组复发 → 转登 **F-401**；比对附件口径②组成 → 转登 **F-402**（建议级，见下）。

## 4.9 commit 质量 —— 通过

标题 `[WP-15-T08] 实现 kinematics 会话姿态写入口与设默认命令门面（KIN-06/14）`（动词开头）；正文四段（What/Why/验证/影响面）齐备；验证段数字逐项与产物实证一致：82/82＋53/53（base..head XML 差值＋13/＋3＝base 已交 XML 69/50 实算吻合；base 冒烟 63 以临时 worktree 实测的声明与 T07 F-399 记录一致）；gates 三套计数（直方图 145/去重 76/自计 69）与附件文件实测一致；无粉饰。

## 4.10 留痕完整 —— 通过

evidence 目录 `traceability/builds/wp15-t08/` 19 件：集成/冒烟构建日志×2＋四运行日志＋gtest XML×4＋ird-test-report.json×4＋gates 附件×4（base-head.diff/base_hits.norm/head_hits.norm/compare.md）。XML 计数（82/53/76/53，failures 全 0）与验收者独立复跑逐一相符；ird-test-report.json summary（82/82、53/53、76/76）与 XML 同源一致。`phase-one-validation.log` 未更新——T06/T07 同先例（早期任务口径，验证留痕由 evidence 目录承载，符合 4.10"或 resultNote"替代面精神）。

## 4.11 偷懒/缩水扫描 —— 通过

- ①代码 diff（kinematics/**）`TODO|FIXME` 零命中；`stub|placeholder|not implemented|未实现` 零命中（唯一 "stub" 语义为测试桩码 PRJ-TEST-READONLY 的文档化说明，非未实现标记；具名契约豁免无需援引）。
- ②空实现检测：公共接口全部有实质函数体（KinSessionPose 五方法、门面两方法＋组装尾部、编码函数逐段组装）；无仅返回默认值的占位。
- ③逐条证据表：见 4.4（四条 acceptance 全部具名测试＋行号证据，零"已实现"声明采信）。
- ④测试强度：断言校验行为与边界（计数网关零流量、身份逐字段＋对照组、金标逐位、独立解码器封闭回解＋无尾随字节、fail-fast 零副作用外泄）；**4.5 变异实证非恒真**（2 处变异 4 用例次真实变红）。
- gates 比对附件独立复核（验收者自建双侧 worktree 引擎直跑）：base/head 引擎自计 **69/69**、`^IRD-GATE-` 原始行 **69/69**、锚定去重（sort -u）**69/69**；路径归一化后逐行 diff **IDENTICAL**（逐码直方图 base=head：R1 3、R3 20、R4 1、T1 2、SUB 37、LIB 6）——**零新增零消除**（退出码 1＝存量既有状态，DTB §4.5 未清零，与送验口径一致；送验附件 145 行/去重 76 的口径组成见 F-402——不影响零增量结论）。

## 发现问题清单

**阻断级：无。**

**建议级（已转登 findings.json，编号顺延自 F-400）：**

- **F-401**：`tasks/foundation/WP-15-T08.json` verify 数组仅 3 行，缺 `sdurws_ird_kinematics_test`／`sdurws_ird_kinematics_contract_test` 两测试目标构建行——F-300/F-358/F-367/F-368/F-371/F-373/F-375/F-381/F-397/F-399 家族复发（本批 WP-15 契约逐单形态）；验收者已在全新构建树自行补建执行（集成 82/53、冒烟 76/53 实测），不构成阻断。
- **F-402**：`builds/wp15-t08/ird-gates-base-head-compare.md` 口径②「去重条目 76」的组成含 7 条引擎自测检出裸码 token（IRD-GATE-R1/R3/R4/R5/SUB/T1/T2 各 1——`grep -o "IRD-GATE-XXX.*"` 无锚定捕获把自测行「自测 fail_rX：按预期检出 IRD-GATE-RX」一并计入），命中本体去重实为 69（与引擎自计同值）；三计数各自可复现、零增量结论不受影响（非 F-398 的误标形态），建议后续附件口径②排除自测检出行或注明组成（76＝69 命中＋7 自测 token）。

**非问题备忘**：测试桩码 `PRJ-TEST-READONLY`（CommandsTest.cpp:646）仅测试夹具承载且注释显式声明"桩码、稳定码权威归 project/diagnostics 注册表"——透传语义不要求在册，无登记违规。

## 裁决

**verdict＝pass**。4.1～4.11 全部通过、无阻断级问题；建议级 F-401/F-402 随本记录转登，不阻塞合入。F-395/F-396 督办兑现经逐字节实证属实，建议治理收尾置 fixed。验收者未合入、未修改实施产物、未向任务分支推送任何提交（送验对象冻结于 6b5b0fa69e26096095b11679ef6c1f711cf35408）。
