# IO-T06 验收记录（attempt 2）

| 字段 | 值 |
| --- | --- |
| 任务 ID | IO-T06（≙WP-11-T05/T07：包导入导出 io 侧设施＋固化中转 Package/TempArea/AtomicFile） |
| 送验对象（冻结） | `de10bec9ad496a66dd4327e2e61f0e410df6dde6`（40 位；分支 wp11-t06） |
| diff 范围 | `0c778f51c281892c5037ea31f8cbb2c3db8ace14..de10bec9ad496a66dd4327e2e61f0e410df6dde6`（4 笔提交：ae8a15a1 产品库段 / 82aa5376 测试面 / 16d7fee9 文档留痕段 / de10bec9 返工留痕落库） |
| 契约 | `tasks/foundation/IO-T06.json`（status ready） |
| attempt | 2（attempt 1 记录＝`traceability/acceptance/IO-T06-20260917.md`，分支 acc/IO-T06/1 @ a0a212b0，verdict fail——阻断项 B-1〔4.9/4.10 留痕幻影清单〕；本次按协议全量重跑，非增量验收） |
| **verdict** | **pass**（4.1~4.11 全部通过；无阻断级问题；建议级 F-200/F-201 转登本记录 §问题清单并在本分支 findings.json 同步登记） |
| 验收者环境 | 全新上下文会话；Windows x64 / VS2022 17.12（MSBuild 17.12.6+db5f6012c）/ CMake＋vcpkg 经典模式（D:/10_Source_Repos/21_robot/RobWork/vcpkg）/ Qt 6.11.1 msvc2022_64 |
| 复现现场 | `git worktree add --detach <临时目录A> de10bec9`；集成模式全新配置（`-G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON`＋vcpkg toolchain＋Qt CMAKE_PREFIX_PATH——与主构建树缓存口径一致；配置后缓存确认 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`，CMake Error 计数 0）；独立冒烟另配临时目录（toolchain 绝对路径） |
| 分支漂移检测 | `git fetch origin wp11-t06 && git rev-parse origin/wp11-t06` ＝ `de10bec9ad496a66dd4327e2e61f0e410df6dde6` ——与送验 SHA 一致，无漂移 |

---

## §4.1 改动范围 —— 通过

- `git diff --name-status 0c778f51..de10bec9` 计 33 个变更文件：io 源码 13 个（`M CMakeLists.txt`、`A contract_test/PackageContractTest.cpp`、`A include/.../{AtomicFile,Package,TempArea}.hpp`、`M include/.../ZipChannel.hpp`、`A src/{AtomicFile.cpp,IoPlatform.hpp,Package.cpp,TempArea.cpp}`、`M src/{ResourceIo.cpp,ZipChannel.cpp}`、`A test/PackageTest.cpp`）＋ `units/io.md` ＋ traceability 19 个（builds/wp11-t06/ 16 份＋findings.json）。
- 逐文件对照契约 allowedFiles 三个模式（`io/**`、`units/io.md`、`traceability/**`）：**全部命中，零越界**。
- forbiddenFiles（REQUIREMENTS.md、ARCHITECTURE.md）：diff 中零触碰。
- SA-02：diff 无 `industrialrobot/patches/` 下任何文件——无未登记框架源码改动。

## §4.2 可复现构建 —— 通过（附环境说明）

- **集成模式（worktree A 全新配置构建树）**：配置零 error（`RWS_BUILD_INDUSTRIALROBOT:BOOL=ON` 确认）；全量 Release 构建中，**io 单元及全部 industrialrobot/送验面目标编译链接零错误**。全解决方案构建退出码 1，73 处 error **全部**位于 RobWorkSim 两个插件（`RWSimulatorPlugin.vcxproj` 的 Math.hpp min/max 宏污染簇＋`bt_plugin.rwplugin.vcxproj` 的 Eigen 同型污染）——与 findings.json **F-008**（已登记 open 基线框架缺陷："RobWorkSim RWSimulatorPlugin 在全新集成树从零构建失败……base 959fb9b 对照复现同样失败〔71 处 error C〕，主树为存量增量产物掩盖"）完全同因，与本任务改动面无关（送验 diff 未触碰 RobWorkSim/框架）。io 相关依赖链（sdurw_math/sdurw/sdurws_ird_core/diagnostics/io/io_test/io_contract_test）全部构建成功。
- **独立冒烟模式（独立临时目录，带 vcpkg toolchain）**：配置成功＋全量 Release 构建退出码 0、错误计数 0。
- **契约 verify 两命令（4.3 与本项合并复现）**：`cmake --build build --config Release --target sdurws_ird_io_test` exit=0；`--target sdurws_ird_io_contract_test` exit=0（worktree A 集成树逐条执行）。

## §4.3 契约 verify 命令 —— 通过

两条 verify 命令逐条独立亲手执行（见 §4.2 末条），均 exit 0。

## §4.4 acceptance 条目逐项 —— 通过（逐条证据表见 §4.11③）

| 契约 acceptance 条目 | 对应用例 | 验收者实测 |
| --- | --- | --- |
| 1：V12/V13/V14 全绿 | ZipBombRatio/ZipBombOverExpand（PackageTest.cpp:533/570）、DuplicateEntries/CaseDiffering（:604/651）、TamperedPayload（:688） | 双模式实跑全绿；码面/ledger 快照/条目级定位断言逐条核对与用例源码一致 |
| 2：V18~V23 全绿 | ExportConcurrentWrite×2（Contract:421/469）、ImportCancel（PackageTest:734）、DiskFull（Contract:502）、CleanupFailed（Contract:543）、ExportCommitFailure/PreservesPrevious（Contract:602/625） | 双模式实跑全绿 |
| 3：V29/V31/V32 全绿 | ManifestReferencingMissing/MissingRequiredMirror（PackageTest:778/815）、BackgroundNonBlocking（Contract:658）、CommittedVsStaged（Contract:720） | 双模式实跑全绿 |
| 4：失败不留目标目录总断言＋io 无 rename/无 .staging 读 | assertNoTarget（PackageTest.cpp:417）/expectNoTarget（Contract:405）挂接全部失败样例；`grep` 证实 Package.cpp 零 rename/MoveFile 调用（仅注释声明"本文件无 rename"）；类型层面 Package.hpp 无发布 API | 断言真实执行于 116/8 用例中 |
| 5：contract_test 目标登记＋P-IO-1/O-09/P-IO-2/P-IO-7 处置 | CMakeLists.txt:264 `add_executable(sdurws_ird_io_contract_test contract_test/PackageContractTest.cpp)`（真实用例非空目标）；ISnapshotFileSource 注入（公共头零 project/runtime 类型——include 扫描零跨单元头）；P-IO-2＝IO-T05 已落位零新码（io.md §15.5 v0.9 原文）；P-IO-7＝§7.8 schema 未预写（Package.cpp 文件头注第 29~31 行） | 核对一致 |

ird_gates 口径：门禁已建成（WP-01-T01 后），非"未执行"情形。验收者复跑 `cmake --build build --target ird_gates`：退出码 1＝15 处登记命中（基线 14＋`sdurws_ird_io_contract_test→libzip::zip` 1 处＝F-197），**命中集合与留痕 gates-ird-gates-target.log 逐项一致**（仅扫描根路径前缀不同）。命中均属 DTB §4.5/findings 登记例外，非本任务新增未登记项。

## §4.5 测试真实失败能力（验证验证者） —— 通过

在 worktree A 对实现注入两处缺陷（源码已恢复并重建复绿，`git status` 确认 io 源码与送验对象一致）：

1. **哈希复算短路**：Package.cpp ⑤哈希复算 `if (actual != row.sha256)` 改 `if (false && ...)` → 增量构建后 `--gtest_filter=IoPackTest.TamperedPayloadHashMismatchLocatedPerEntry` **FAILED 1**（篡改包不再被拒，测试如期变红）。
2. **cleanup 假成功**：TempArea.cpp cleanup 删除真实实现体前直接置非活动并 return 成功 → `--gtest_filter="IoTempTest.*:IoPackTest.ExportImportRoundtrip*"` **FAILED 2**（CreateMarksAndCleanupIsIdempotent＋ExportImportRoundtripRestoresPayloadBytewise——目录未删被断言捕获）。

恢复源码重建后同过滤器 5/5 PASSED。两处注入证明测试校验行为与边界，非恒真。

## §4.6 架构红线（AGENTS §5 八条） —— 通过

- **L2 零 Qt**：`grep -rn "#include <Qt|#include <Q|QtCore|QtGui|QtWidgets"` 于 io 全部 src/include/test/contract_test 零命中（唯一匹配为 SafePathTest.cpp:479 红线扫描用例自身的字符串字面量，非 include）；CMakeLists.txt:317 配置期 Qt 链接守卫在位。
- **R-1 业务域互链**：跨单元 include 扫描仅 `sdurws/ird/(core|diagnostics|io)/`，零命中业务域单元；配置期依赖守卫（CMakeLists.txt:308~325）在位；ird_gates R-1 复跑零新增命中。
- **R-2 跨单元私有头**：相对 include 仅 `IoPlatform.hpp`（io 单元 src/ 内私有头，单单元内消费——合法）；无跨单元私有头。
- **SA-02 框架零修改**：diff 无 patches/ 登记、无框架源码文件。
- **R-4 名称前缀拼接**：ird_gates R4 扫描 io 源码零命中（15 项命中中 R4 仅 project 既有登记 1 处）。
- **PA-1 权威唯一**：发布语义归 project——Package.hpp 类型层面无 rename/发布 API（N-7），Package.cpp 零 rename 调用路径（grep 证实）；io 只做 ⑧前目标存在性预检。
- **PA-2 不可变历史**：本任务不触修订数据（io 只镜像 payload 树）。

## §4.7 注释规范（AGENTS §2） —— 通过

- **公共头全查（3/3）**：Package.hpp（457 行）/TempArea.hpp（214 行）/AtomicFile.hpp（202 行）——文件头（@file/@brief＋设计依据〔单元卡章节＋需求 ID＋任务卡〕＋背景说明"为什么"）齐备；类/结构体/函数全部 Doxygen 中文注释；§2.5 高危信息表逐项在位（物理量带单位〔如 PackFileEntry.size 字节〕、路径坐标系/角色声明、枚举序＝状态图序、所有权与生命周期〔pimpl 值语义共享/调用方持有引用不接管〕、线程约束〔会话单线程/管理器进程级并发安全/io 不创建线程〕、错误语义〔IO-* 稳定码＋调用方违约 fail-fast 面〕、确定性来源〔IO-D11 时间戳统一 createdAtUtc/manifest 字典序/canonical JSON〕）。
- **实现文件抽查（4/4，≥3 达标）**：Package.cpp（1767 行——九步/六步协议逐步注释＋分支边界＋魔法数字来源〔如 100:1 比例硬限、4 GiB DWORD 上限〕）、TempArea.cpp（三步骤注释＋RAII 兜底说明＋"绝不误删用户目录"边界）、AtomicFile.cpp（§4.6 协议分步＋TOCTOU 双保险注释）、IoPlatform.hpp（私有头纪律 R-2 声明＋线程安全/确定性注）。

## §4.8 文档同步 —— 通过

- 实现与任务卡零偏差；六项等价调整/增补全部按 DTB §5.4 登记（io.md §15.5 v0.9 行第⑥点：verifyThrough 尾随 cancel/progress、PackageExportOptions 四元数据字段、availableBytes、AtomicTarget.write＋adopt/impl 通道、稳定版本重读恰一次；ZipEntryInfo 表尾增补两字段）。
- 单元卡版本 v0.8→v0.9；§1.2 构建落位行、§3.1/§3.3 目标行同步；文末落位进度行更新。
- 契约 JSON 本身零修改（status 维持 ready 待收尾翻转——正确）。
- **findings.json 未关闭条目相关性核对（v1.2 要求）**：主线基线（0c778f51）171 项 open。与 io/本任务相关者：**F-194**（ResourceIo.cpp 缺 `<mutex>`——suggestion）已随本任务 16d7fee9 顺手消账（实读 ResourceIo.cpp:59-61 显式 include＋消账注释；送验对象 findings.json 置 fixed/fixedBy=16d7fee9）——顺手消账落实；**F-196**（XML-SYNTAX 码表收编确认）归 diagnostics 所有者，非本任务辖域、无静默积压；**F-197**（ird_gates 词表未含 libzip/expat——本任务 contract_test 链接新增 1 处 LIB 命中）由实施段自登记 open，归门禁所有者，不在 allowedFiles 内、未越权自裁——处置正确。另：r1 验收登记的 F-198/F-199（在途于 acc/IO-T06/1 分支，未合入主线）同为 open 建议级，收尾合并时与 F-197/F-200/F-201 一并进入主线追踪。

## §4.9 commit 质量 —— 通过

4 笔提交（ae8a15a1/82aa5376/16d7fee9/de10bec9）逐份实读：标题均为 `[WP-11-T05/T07]` ＋动词开头中文摘要；正文四段（改动内容/改动原因/验证/影响面）齐备；验证结论（116/116＋8/8 双模式、ird_gates 15 处分列、validate-task/verify-task PASS）与留痕文件及验收者复现逐数值一致。attempt 1 的 B-1（提交信息所列 .log 因全局 `*.gitignore` 静默忽略未入库＝幻影清单）已在 de10bec9 修复：12 份 .log 经 `git add -f` 真实在库（worktree A `ls` 逐一确认 16 文件），且提交信息明示返工口径——无粉饰。

## §4.10 留痕完整 —— 通过（附口径说明）

- gtest XML ×4（io-test/io-contract-test × integration/smoke：tests=116/8、failures=0）＋ird-test-report.json（schemaVersion ird-test-report/1，rework 字段登记返工口径）＋构建/测试/验证日志 12 份——全部真实落库于 `traceability/builds/wp11-t06/`，数值与验收者双模式复现一致。
- **phase-one-validation.log 或 resultNote 未更新**：log 尾行停在 CORE-T01（2026-09-10），state.json 无 resultNote 字段——但此为 CORE-T01 之后全部已合入任务的一贯实践口径（协议 4.10 条文与流水线实践脱节），非本任务特有缺失；任务级留痕已完整落位于 builds/wp11-t06/。判不阻断，条文脱节转登建议级 **F-201**。
- 环境说明（验收者侧）：全新 worktree 集成配置首次失败（`RobWorkStudio.ini.template.static` 不存在）——该文件与 `RobWorkStudio.ini.shared.in` 为**未跟踪**环境模板（`git ls-files` 为空），仅物理存在于主工作树；从主树拷贝后配置成功。属框架既有环境依赖（与 F-007 同族不同因），非送验对象缺陷，转登建议级 **F-200**。

## §4.11 偷懒/缩水实现扫描 —— 通过

- ① `grep -rniE "TODO|FIXME|stub|placeholder|未实现|not implemented"` 于 io 全部 diff 面（src/include/test/contract_test/CMakeLists）**零命中**（亦无契约标注的触发式延后条目需豁免）。
- ② 空实现检测：makePackageImporter/makePackageExporter（Package.cpp:1757/1762）真实构造 Impl；IPackageImporter/IPackageExporter/ITempAreaManager/IAtomicFileWriter 全方法有真实逻辑（九步/六步协议、EquivKeyMutex 互斥、ReplaceFileW 原子替换——实读证实）；无仅返回默认值的契约接口。
- ③ acceptance 逐条证据表：见 §4.11③（=§4.4 表）——5 条 acceptance 全部有具名测试/命令输出/文件行号证据，零"已实现"空声明。
- ④ 测试强度：22 个新用例断言码面（EXPECT_EQ error.code）、条目级 params 定位、会话状态机终态、文件系统副作用（零落盘/零残留/逐字节内容）、脱敏形态（`\\\\?\\` 前缀缺失）、线程 id、UI 打点间隙 <2s；并与 4.5 联动以双注入证实会红。

---

## 问题清单

**阻断级：无。**

**建议级（已登记本分支 findings.json，编号自主线 F-196 与在途 F-197/F-198/F-199 之后顺延为 F-200 起）：**

| 编号 | 内容 | 责任方 |
| --- | --- | --- |
| F-200 | 全新 checkout/worktree 的集成模式配置依赖两个未跟踪文件 `RobWork/RobWorkStudio/bin/RobWorkStudio.ini.template.static` 与 `RobWorkStudio.ini.shared.in`（`git ls-files` 为空、仅主工作树物理存在）——fresh 树 `cmake -S RobWork -B build` 配置期 `CMake Error: File ...does not exist` 直接失败；验收以从主工作树拷贝两文件修复。与 F-007（配置参数未载明）同族不同因。建议：两模板入库或由 CMake configure 期生成 | 构建约定所有者（框架侧文件，SA-02 约束下先登记再动） |
| F-201 | ACC §4.10 条文"phase-one-validation.log 或 resultNote 已更新"与流水线实践脱节：log 自 CORE-T01（2026-09-10）后无任何任务追加，state.json schema 无 resultNote 字段——条文两个载体均无实际维护路径，任务级留痕实际落于 traceability/builds/<branch>/。建议 ACC/PIPE 文档所有者修订该条为"任务级留痕目录落位"口径，消除验收判定的历史歧义 | ACC/PIPE 文档所有者 |

**在途引用（非本次新登）**：F-197（送验对象 16d7fee9 自登记，ird_gates 词表）、F-198/F-199（r1 记录 a0a212b0 登记：writeBytesToFile DWORD 截断边界、V19 仅展开阶段样例）——均 open，随各自分支收尾合并入主线追踪。

## 裁决

**verdict ＝ pass**。4.1~4.11 全项通过、无阻断级问题；建议级 F-200/F-201 不阻塞合入（ACC §5）。送验对象维持冻结为 `de10bec9ad496a66dd4327e2e61f0e410df6dde6`；本验收未向任务分支 wp11-t06 推送任何提交、未修改任何实施产物（worktree A 中两处注入已恢复并复绿验证）。
