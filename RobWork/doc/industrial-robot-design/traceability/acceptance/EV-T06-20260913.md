# EV-T06 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | EV-T06 实现 evidence 五级汇总判定（Verdict）——契约 tasks/foundation/EV-T06.json |
| 送验对象（冻结 SHA） | e266e09f5322804c8222a6ba6582adc63b5acb8d（分支 wp05-t06 尖端） |
| diff 范围 | 88d07b8c3481e4ed11291585debc11f948829835..e266e09f5322804c8222a6ba6582adc63b5acb8d（3 提交：806ce921 实现 / e8f029f9 留痕 / e266e09f 单元卡登记） |
| verdict | **pass**（4.1～4.11 全部通过，无阻断级问题；建议级 1 条已转登 findings F-063） |
| 验收日期 | 2026-09-13 |
| 记录分支 | acc/EV-T06/1（本文件所在提交） |

## 独立性声明（ACC §2）

本验收为全新上下文会话：未消费实施者任何过程叙述/辩护文本，仅消费任务契约、设计文档（units/evidence.md）、送验 diff 与留痕产物；全部构建与测试由验收者亲手在 ACC §3 规定复现现场（detached worktree @SHA）重跑，未采信实施者运行结果；检查以"为不通过找证据"的对抗式立场执行（含 2 个独立设计的失败能力探针）。派发模式＝PIPE §0.4 子代理（无需降级声明）。

## 验收环境

- 复现现场：`git worktree add --detach %TEMP%\ird-acc-ev-t06-a e266e09f…`（送验 SHA 检出，4747 文件）。
- 集成模式：worktree A 内重新配置（`-DRWS_BUILD_INDUSTRIALROBOT=ON`，grep CMakeCache 实证 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`，`CMAKE_TOOLCHAIN_FILE`＝vcpkg 绝对路径、`CMAKE_PREFIX_PATH`＝D:/software/Qt/6.11.1/msvc2022_64——主树缓存同参数）；两件 git 未跟踪 ini 模板（RobWorkStudio.ini.template.static / .shared.in）按 F-062 口径自主树复制后配置成功（exit 0）。
- 冒烟模式：独立临时目录 `%TEMP%\ird-acc-ev-t06-smoke`，`-DCMAKE_TOOLCHAIN_FILE=D:\10_Source_Repos\21_robot\RobWork\vcpkg\scripts\buildsystems\vcpkg.cmake`（绝对路径），配置 exit 0。
- ird_gates 回主仓库只读复跑（主仓工作树 HEAD＝e266e09f、industrialrobot 源码无未提交修改）。

## 逐项证据（ACC §4，4.1～4.11）

### 4.1 改动范围 — 通过

- `git diff --stat 88d07b8c..e266e09f`＝20 文件（4500 insertions, 3 deletions）：evidence/CMakeLists.txt（+9/-3）、evidence/include/sdurws/ird/evidence/Verdict.hpp（新 667 行）、evidence/src/Verdict.cpp（新 732 行）、evidence/test/VerdictTest.cpp（新 1149 行）、traceability/builds/wp05-t06/ 15 件留痕、units/evidence.md（+2/-1）。
- 逐文件对照契约 allowedFiles：`evidence/**` ✓（4 个代码文件）、`industrialrobot/CMakeLists.txt`（本 diff 未触碰，仅 evidence 子目录 CMake）✓、`units/evidence.md` ✓、`traceability/**` ✓（15 件留痕）——**全部在 allowedFiles 内**。
- forbiddenFiles（REQUIREMENTS.md / ARCHITECTURE.md）：diff 零触碰 ✓。
- SA-02：`git diff --stat 88d07b8c..e266e09f -- patches/` 为空（无新 patch 登记＝无框架源码改动）；patches/ 目录仅既有 0001-rwslibs-entry-option.patch＋PATCHES.md ✓。

### 4.2 可复现构建 — 通过

- 集成模式：配置 exit 0（缓存三项关键参数 grep 实证，见环境节）；`cmake --build build --config Release --target sdurws_ird_evidence_test -j` exit 0 零 error（全新 worktree 从零配置＋任务目标面构建——F-008 判例口径；产物 build/RobWorkStudio/bin/Release/sdurws_ird_evidence_test.exe）。
- 冒烟模式：配置 exit 0；`cmake --build /tmp/ird-acc-ev-t06-smoke --config Release -j` exit 0 全量零 error（五单元库＋测试全部从零编译）。
- 实施侧留痕对照：integration-cache-check.txt 第 1 行 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`、smoke-build.log 全部产物路径为 `...\build-smoke-wp05-t06\*\Release\...`（Release 配置实跑——F-055 纪律已吸收）。

### 4.3 契约 verify 命令 — 通过

契约 verify 数组仅一条：`cmake --build build --config Release --target sdurws_ird_evidence_test`——验收者在 worktree A 逐字执行，exit 0（增量重链接成功）。

### 4.4 acceptance 条目逐项证据 — 通过（4/4）

| # | 契约 acceptance 条目 | 证据 |
| --- | --- | --- |
| 1 | EV-VER-1~8、EV-COV-1/4 五级优先级正反例全部通过 | VerdictTest.cpp 24 用例逐一对应：EV-VER-1＝VerdictGate.GateBlocksProof（L457）、EV-VER-2＝SearchExhaustedThenFeasibleAfterReplan（L485）、EV-VER-3/4＝VerdictConfigCollision 两例（L533/L561）、EV-VER-5＝ReplannedPathKeepsFeasible（L594）、EV-VER-6 正反例＝VerdictMandatoryProof 两例（L618/L638）、EV-VER-7＝NotApplicableNotCountedMissing（L662）、EV-VER-8 Must/Should 两例（L711/L731）、EV-COV-1＝MissingMandatoryExecutionBlocked（L754）、EV-COV-4 两例（L780/L807）；实测集成 107/107＋冒烟 107/107（XML 顶格 tests="107" failures="0"，两份送验 XML 同值、验收者独立 XML 同值）。EV-VER-6 的 validateProof 字段级半边与 EV-COV-2/3 校验器半边已随 EV-T05 落地（§12 依赖序，EvidenceTest 83 例回归通过内）——范围声明与单元卡 §12 行一致，非缩水 |
| 2 | 缺失项全量列出（不抽样） | 用例 VerdictMissingCompleteness.GateFailuresListedExhaustively_Acceptance2（L844：②级快照身份 2 字段＋漏验 2 工况＝4 条全量断言）＋EvidenceGapsListedExhaustively_Acceptance2（L869：④级 2 缺口全量）；实现面②级固定检查序 a~f 六组成全量收集不短路（Verdict.cpp L119-246）、④级 a~e 五条件全量收集不短路（L284-368） |
| 3 | 汇总不得凭状态字段采信证明（D-9） | 用例 VerdictD9.ProofPresenceNeverTrustedWithoutFieldValidation_D9（L891：越界类别冒充→字段级拒绝→DataInsufficient）＋VerdictMandatoryProof.InvalidProofFallsToDataInsufficient_EV_VER_6（L638）；实现面③命中严格以 `validateProof(...)空` 为前置（Verdict.cpp L465-471）；验收者探针 1 实证（见 4.5） |
| 4 | O-14 未决，保守字面（分母＝快照冻结态 RequiredCaseSet） | 实现 L207 `validateCaseCoverageMatrix(input.coverage, snapshot)` 分母恒取快照＋L359-367 `enabled && mandatory` 字面消费不解释（P-EV-7 空必验集保守落④级）；用例 VerdictPitfalls 两例（L917 空必验集→DataInsufficient 不虚通过、L944 全 NotApplicable→②级字面拦截）＋GateFailuresListedExhaustively 中非 mandatory 工况 C 不进分母断言（L860-865） |

ird_gates 自查（AGENTS §6.3，实现类任务）：验收者主仓实测 `cmake --build build --config Release --target ird_gates`——命中 3 项均为既有（IRD-GATE-SUB sdurws_ird_runtime_test→sdurws_ird_testkit＝F-051；IRD-GATE-SUB testdata_lint→testkit＋IRD-GATE-T1 testdata_lint 链 testkit＝F-019），**evidence 面零新增命中**；引擎自测 9 项（pass_clean/pass_r4comment/fail_r1/fail_t1/fail_r5/fail_sub/fail_r3/fail_r4/fail_t2）全部符合预期；exit 1 系既有命中所致，与送验 ird-gates.log 及提交声明（F-019 两项＋F-051 一项，F-060 教训逐项归属）一致。

### 4.5 测试真实失败能力（验证验证者）— 通过（探针 2 个，均为验收者独立设计）

- **探针 1（D-09 面）**：将 Verdict.cpp ③级命中条件 `if (input.proof.has_value() && proofIssues.empty())` 篡改为 `if (input.proof.has_value())`（＝凭存在性采信证明，D-09 禁止形态）→ 重构建 → `VerdictMandatoryProof.InvalidProofFallsToDataInsufficient_EV_VER_6` 与 `VerdictD9.ProofPresenceNeverTrustedWithoutFieldValidation_D9` **2 用例变红**（实测 FAILED 输出在案）→ 证明 D-9 断言非恒真、确实钉住字段级采信语义。
- **探针 2（P-EV-7 面）**：将 ④级空必验集检查 `if (!hasMandatoryCase)` 篡改为 `if (false)`（＝空必验集放行，P-EV-7 保守处置失效形态）→ 重构建 → `VerdictPitfalls.EmptyRequiredCaseSetIsDataInsufficient_P_EV_7` **变红**。
- 探针 1 与探针 2 分次注入分别归因（探针 2 注入时先还原探针 1 并 grep 复核单点残留）；全部还原（`git checkout --`＋`git status` 干净）后重构建复绿 **107/107 PASSED**，`grep -c PROBE-ACC`＝0 零残留。

### 4.6 架构红线（AGENTS.md §5 八条）— 通过

1. **L2 零 Qt**：Verdict.hpp/Verdict.cpp/VerdictTest.cpp `grep "#include <Qt|#include <Q"` 零命中；evidence CMake 无任何 Qt 链接 ✓。
2. **业务域互链禁止（R-1）**：`target_link_libraries(sdurws_ird_evidence PUBLIC sdurws_ird_core)`（CMakeLists L58）——唯一单元边＝core，无其他业务域目标 ✓。
3. **跨单元私有头禁止（R-2）**：三个新文件 include 面全部为公共头（`sdurws/ird/core/{DiagData,Digest,Evaluation,Identity}.hpp`＋`sdurws/ird/evidence/*` 本单元公共头），无 `src/` 私有头 ✓。
4. **SA-02**：见 4.1，零框架改动 ✓。
5. **R-4 名称前缀**：Verdict.cpp 无任何名称拼接/剥离（makeVerdictDiag 注释显式声明 localName/runtimeName 置空、名称反解归 runtime⑥端口——L39/52-55）✓。
6. **权威唯一（PA-1）**：就绪判定消费不生产（ReadinessSummary 注释 L131-133）、域判定透传定级（DomainVerdictInputs L180-182）、清单不重写（③附加义务 L478-482"清单标记义务在评估器侧，汇总器不重写清单"）、建议码不承担注册职责（L34-36"码值权威归 diagnostics StableCodeRegistry，PA-1"）✓。
7. **不可变历史（PA-2）**：aggregateVerdict/两资格/基准检查全为纯函数无写回（§7.2"永不回写"纪律于 checkFormalPassEligibility 注释 L544-545 显式承诺）✓。
8. **不恢复 old/**：diff 无 old/ 引用，实现全部新写 ✓。

### 4.7 注释规范（AGENTS.md §2）— 通过

本任务新增/大改文件共 4 个，全部逐一核查（超出"公共头全查＋实现抽查 ≥3"要求）：

- **Verdict.hpp（公共头，契约文档）**：文件头（L1-105）含设计依据逐章节引用、契约 acceptance 对应、背景说明（"为什么汇总值得独立契约头"）、knownPitfalls 处置、I-1~I-9 实现口径索引、线程安全声明；每个 struct/class 均有业务概念＋生命周期＋线程安全三要素；每个函数（含 IProfileRegistryView::findProfile）有 @brief/@param 方向与取值语义/@return/线程约束；§2.5 高危信息全覆盖（所有权 L397-398"调用方持有，本单元不接管所有权"、线程约束 L399/497、错误语义 kDiag* 常量逐个注明建议值归属、确定性 L490-492"missingItems 顺序＝检查序，可用下标断言"）。
- **Verdict.cpp（实现）**：文件头实现纪律四条；每个函数 Doxygen 完整；**每个语义段落有注释**（②级 a~f、④级 a~e 逐段说明业务含义与出处）；决策表⑤级逐分支注释（L552-583）；防御分支说明进入条件与保守方向（L534-536"宁可数据不足不可虚通过"）。
- **VerdictTest.cpp**：文件头声明替身边界（EV-REG-3）；每个 TEST 带"验证哪条需求/AT/acceptance"注释；夹具函数逐个说明用途与设计依据。
- **evidence/CMakeLists.txt**：新增源列与测试列均带职责注释块（引用章节与任务号）。

未发现 §2.6 反面示例（无复述式/占位/机翻/过期注释）。微瑕 1 处（L506 注释块续行行首多一空格，纯排版零语义）转登 F-063，不构成"缺必填项"。

### 4.8 文档同步＋findings 未关闭条目相关性 — 通过

- **单元卡同步**：units/evidence.md 卡头版本行 v0.6→v0.7 与 §15.4 v0.7 变更记录行同步推进（F-057 同型问题未复发，卡头行＝§15.4 末行）；v0.7 行完整登记 I-1~I-9 实现口径、24 新增/107 两模式/445 冒烟/探针/门禁零新增——与实测逐项吻合（validate-docs 在送验侧 PASS，本侧以 validate-task PASS＋卡面核对佐证）。
- **偏差登记**：实现相对 §6.4.1 伪代码的三处落位选择（I-1 签名投影、I-2 增补三字段、I-5 区域证据分流）均按 DTB §5.4 在单元卡 v0.7 登记，语义有 §7.2/§6.4.3/§6.3 依据，非私改设计。§11 矩阵行未逐行标注实现状态——沿 EV-T03~T05 既有实践且已在提交 e266e09f 影响面段显式声明（EV-T12 统一核对），非静默偏差。
- **追踪矩阵**：无需求语义变化（requirements 四项均既有），矩阵无需更新；契约 status 翻转归编排者收尾（PIPE §4.7），非实施者责任面。
- **findings.json 未关闭条目逐条相关性核对**（open 43 条）：
  - **F-019/F-051（门禁边红，major）**：本次实测 3 处命中即此两项、零新增；cmake/ 不在 allowedFiles，**不随本任务消账**，保持 open 归 WP-01 构建所有者。
  - **F-056（建议码超出 §13 清单）**：本任务 kDiag* 补登 8 值（EVI-OUTCOME-NOT-COMPLETED 等）为 F-056 同模式扩展，已在代码注释与单元卡 v0.7⑨ 登记"码值权威归 diagnostics StableCodeRegistry，注册时裁决"——**同族扩展关联**，diagnostics 注册时与 F-056 四值一并收编；F-056 保持 open。
  - **F-020（evidence.md §3.5 缺位）**：本任务 designRefs（§6.4/§6.5/§6.6/§12）锚点全部存在（validate-task PASS）；F-020 涉及的 §3.5 增设与 EV-T02 designRefs 回锚仍归 evidence 详设所有者，**保持 open**；其处置建议②（§3.5 析出成节时全表收编）应纳入本任务 8 个补登建议码。
  - **F-005/F-006/F-058（单元卡转义损毁家族）**：evidence.md"AI 执行就绪补充"节损毁（L1112 `$(evidence.ToUpper())`、L1113 `locked`、L1124 `alidate-docs`）在送验版本**仍在**；本次 diff 未触碰该节（反证在档）——非本任务交付面，保持 open 归 DOC 治理任务。
  - **F-012 家族（F-023/024/025/026/027/030/034/036/037/038/039/040/041/042/044/045/047/048/059/060，提交标题/计数/树性质标注）**：本任务**零复现**——三提交标题均动词开头（登记/登记/实现）；计数实测吻合（24 新增＝TEST 清点、107/107、445＝61+107+4+176+97）；集成增量树/冒烟全新树显式区分且冒烟标注 Release（F-036/F-054/F-055 纪律已吸收）；ird_gates 命中按 F-060 教训逐项归属（F-019×2＋F-051×1）；门禁实际执行并登记真实结论（F-028/F-035 纪律已吸收）。家族处置（模板补救）仍 open，不随本任务关闭。
  - **F-004/F-007/F-008/F-009/F-010/F-062（构建与环境前置）**：本次验收实测仍需 F-062 所述前置（toolchain/Qt 绝对路径＋两件 ini 模板复制）方能 worktree 配置——与 F-062 描述一致，保持 open；F-008 按判例以任务目标面构建规避；F-010 本体（.gitignore 豁免）未落地，本任务留痕以 git add -f 入库（16 件实际全在 diff 中）。
  - **F-015~F-018（testkit）/F-021/F-013/F-014（编排）/F-029/F-031/F-032/F-033（契约编译治理）/F-043/F-046/F-048/F-051 关联面/F-061（runtime 侧）**：与本任务变更面无交集，不相关，维持原状。
- 结论：无历史发现被静默积压；无本任务可顺手消账的条目（相关条目辖域均在 allowedFiles 之外）。

### 4.9 commit 质量 — 通过

三提交逐一核对：

| 提交 | 标题（动词开头） | 四段 | 验证结论与留痕一致性 |
| --- | --- | --- | --- |
| 806ce921 | 实现 evidence 五级汇总判定… ✓ | What/Why/验证/影响面 ✓ | 双模式构建/107 计数/探针/门禁归属——实测全部吻合 |
| e8f029f9 | 登记双模式构建与测试证据… ✓ | ✓ | 16 件留痕实际入库（diffstat 清点＝16）；增量树/全新树/Release 标注准确 |
| e266e09f | 登记单元卡 v0.7… ✓ | ✓ | 卡头与 §15.4 同步属实；validate-docs PASS 声明与卡面一致 |

无粉饰：验证段明确区分增量树（集成）/全新树（冒烟）与既有命中（门禁），无"最强口径"类失准表述（F-036 家族纪律已落地）。

### 4.10 留痕完整 — 通过

- traceability/builds/wp05-t06/ 16 件全部实际入库（diffstat 逐件在案）：cache-check 1＋集成 4（库构建/测试构建/测试运行/verify）＋gtest XML 2（integration/smoke 均 tests="107" failures="0" errors="0"，顶格实核）＋冒烟 3（configure/build/test-run）＋探针 4（red-build/red-run/restore-build＋…）＋ird-gates.log＋validate-task.log＋ird-test-report.json。
- ird-test-report.json（schemaVersion ird-test-report/1）：24 用例逐条具名 trace（acceptance 1~4 归属逐条标注）、门禁命中逐项归属 F-019/F-051、构建树性质与配置声明齐备——本任务以该报告承载 resultNote 职责（AGENTS §4.2 三件套：XML＋报告＋构建日志齐备；phase-one-validation.log 为跨任务汇总账本，随收尾流程更新，EV-T03~T05 同口径）。

### 4.11 偷懒/缩水实现扫描 — 通过

1. **TODO/FIXME/stub/placeholder/未实现/not implemented 扫描**：diff 新增行 grep 零命中（实测 EXIT=1）；无契约显式"触发式延后"标注，无需豁免列举。
2. **空实现检测**：Verdict.cpp 全部 7 个函数（makeVerdictDiag/evidenceGapStatusText/collectCommonGateFailures/collectDataInsufficient/aggregateVerdict/checkFormalPassEligibility/checkReviewRecordEligibility/checkComparisonBaselinesConsistent）均有完整实体，无空体/仅返回默认值的公共接口。
3. **acceptance 逐条证据表**：见 4.4——4/4 条均有具名测试＋实现行号＋探针佐证，无"已实现"声明依赖。
4. **测试强度**：断言校验行为与边界（status/hitLevel/missingItems 逐项/诊断码/unmetConditions token/trace 六槽求值态），非恒真；4.5 双探针实证测试会红；决策表正例 FullySatisfiedInputIsFeasible 提供全链绿对照面。

## 问题清单

### 阻断级

无。

### 建议级（已转登 findings.json）

- **F-063**（suggestion，open）：Verdict.hpp 两处零行为影响风格微瑕——①L506 注释块续行行首多一个空格（与块内其余行 `// ` 对齐不一致）；②VerdictTest.cpp EV-VER-3 用例（L541-542）`domainSideOnly` 局部变量构造后未使用（注释自述"仅为注释性对照"——读代码者须靠注释理解其不进入聚合，未来接入 clang-tidy unused-variable 类检查会报）。两处均可随下次触及该两文件的任务顺手规整，不单开任务。

## 裁决

**verdict＝pass**：4.1～4.11 全部通过，无阻断级问题；建议级 1 条（F-063）不阻塞合入。送验对象 e266e09f5322804c8222a6ba6582adc63b5acb8d 可提交所有者进入合入决策。
