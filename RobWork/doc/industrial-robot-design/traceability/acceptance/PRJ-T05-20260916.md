# PRJ-T05 验收记录（attempt 2，2026-09-16）

| 字段 | 值 |
| --- | --- |
| 任务 | PRJ-T05（project 单元对象库 ObjectStore：内容编址/只增发布/LRU；契约 tasks/foundation/PRJ-T05.json；需求 CON-01；knownPitfalls P-PR-1/CR-02） |
| **verdict** | **pass**（4.1～4.11 全部通过；零阻断级问题；建议级 3 项已转登 findings.json F-131～F-133） |
| 送验对象（冻结） | `b6cc2352a5d9b7963198748184365fff1e101e20`（40 位）；分支漂移检测：`git fetch origin` 后 `git rev-parse origin/wp04-t05`＝该 SHA（实测逐字节一致），零漂移——验收全程按该 SHA 的 detached worktree 进行，不按分支名 |
| diff 范围 | `fd1b1971de9b9ba5d0ef4812b6a89726d0bf4695..b6cc2352`（state.base..headSha；`origin/redesign-main` 尖端恰为该基线，即纯任务分支增量；3 个提交：9438eb0e 实现＋7f058bde 单元卡 v0.7＋b6cc2352 留痕归档） |
| attempt 说明 | 尝试序号 2 由编排者随验收请求下发（state.attempts.accept＝2；attempt 1 未产出已推送的验收记录——origin/redesign-main 与全部本地/远程分支均无 PRJ-T05 同日记录，`git log --all --diff-filter=A -- "*PRJ-T05-2026*"` 实测为空），故本文件为基础名 `PRJ-T05-20260916.md`（同日无既有同名文件，无需 -r2 序号）；evidence 分支按 attempt 取 `acc/PRJ-T05/2`，`git ls-remote --heads origin "acc/PRJ-T05/*"` 实测为空——无重名冲突 |
| 验收者环境 | 全新上下文（未消费任何实施会话叙述，只读契约/设计文档/留痕产物）；复现现场＝`git worktree add --detach D:/10_Source_Repos/21_robot/acc-PRJ-T05-attempt2/wtA b6cc2352a5d9b7963198748184365fff1e101e20`；集成模式全新配置（vcpkg toolchain 用主树绝对路径＋Qt `CMAKE_PREFIX_PATH`＝`D:/software/Qt/6.11.1/msvc2022_64`；`RobWorkStudio.ini.template.static/.shared.in` 两件 git 未跟踪本地资源自主树复制——F-007/F-062/F-099 登记件，验收者本轮亲历复踩：首轮裸配置在 `RobWorkStudio/src/CMakeLists.txt:58` configure_file 失败〔bin/ 目录被 .gitignore 忽略、模板文件不在 git 对象内〕，按登记口径补齐后重配 exit 0）；冒烟模式另配独立临时目录 `build-smoke-acc`（带 vcpkg toolchain——AGENTS §4.1/CORE-T01 G-2 口径）；CMake＋MSVC VS2022 x64；pwsh 治理脚本。注：本轮未加 `-DCMAKE_CXX_FLAGS=-DNOMINMAX…` 与 `RWS_USE_STATIC_LINK_PLUGINS=ON`（较 PRJ-T04 验收配置少两项对齐参数），配置与目标闭包构建/测试运行均零错误通过——不影响结论，特此留痕 |
| evidence | 分支 `acc/PRJ-T05/2`（本文件＋findings.json F-131～F-133 同支承载；commit 三元组见文末） |

---

## 1. 逐项证据（ACC §4.1～4.11）

### 4.1 改动范围 —— PASS

- `git diff --name-status fd1b197..b6cc235` 共 10 文件，逐一比对契约 `allowedFiles`：
  `project/**`×4（CMakeLists.txt〔增列 src/ObjectStore.cpp＋test/ObjectStoreTest.cpp〕、src/ObjectStore.cpp〔新增〕、src/ObjectStore.hpp〔新增，src/ 私有实现头——R-2〕、test/ObjectStoreTest.cpp〔新增〕）✓；`traceability/**`×5（builds/wp04-t05/ 下 ird-test-report.json＋双模式 gtest XML 四份）✓；`units/project.md`（契约显式列名）✓。无任何清单外路径。
- `forbiddenFiles`（REQUIREMENTS.md / ARCHITECTURE.md）零触碰 ✓。
- `industrialrobot/patches/` 零新文件（patches/ 下仅既有 0001-rwsdefs-entry-option.patch 与 PATCHES.md，base..head 零变化）；框架源码（RobWork/RWS/RobWorkSim）零改动——SA-02 无登记义务产生 ✓。

### 4.2 可复现构建 —— PASS（验收者全新树亲手复现，不采信实施日志）

- **集成模式**：worktree A 内 `cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON`（补 F-007 环境链参数，见环境说明）→ **exit 0**（`-- Configuring done`＋`-- Generating done`，零 CMake Error）；缓存 grep 确认 **`RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`**。
  - 契约 verify 目标 `sdurws_ird_project_test` 从零构建 **exit 0**、`error C|error LNK|fatal error` 计数＝**0**；构建覆盖面＝依赖闭包从零编译：框架侧 sdurw_core/sdurw_common/sdurw_math＋sdurws_ird_core＋sdurws_ird_project＋测试目标（`*.vcxproj ->` 产物行实测 6 项）。
  - `sdurws_ird_project_contract_test` 构建 **exit 0**、错误计数＝0。
  - 两测试可执行文件运行：**109/109＋10/10＝119/119 全绿**（gtest 输出实测，XML 落验收者证据目录 acc-evidence/）。
- **独立冒烟模式**：`-S industrialrobot -B build-smoke-acc -DCMAKE_TOOLCHAIN_FILE=<仓库根 vcpkg 绝对路径>`（AGENTS §4.1＋CORE-T01 G-2 口径）配置 **exit 0**；Release 全量构建 **exit 0**、错误计数＝0；Debug 全量构建 **exit 0**、错误计数＝0；Release 配置下测试 exe 运行 **109/109 全绿**。
- 口径说明：全新树**全树**构建受 F-008 基线框架缺陷阻挡（既有登记，非本任务引入），本次复现覆盖任务依赖闭包面（框架 math/common/core＋project→core 链＋两测试目标＋门禁＋治理脚本），与 PRJ-T01～T04 既往验收口径一致。

### 4.3 契约 verify 命令 —— PASS

- 契约 `verify` 数组唯一一条：`cmake --build build --config Release --target sdurws_ird_project_test`——验收者在全新配置的 worktree A 构建树中独立执行，**exit 0**、错误计数 0（见 4.2）。无其他条目。

### 4.4 acceptance 条目 —— PASS（四条逐项均有具名证据；ird_gates/validate-task 亲手复跑）

| # | 契约 acceptance 条目 | 证据（验收者亲查/亲跑） |
| --- | --- | --- |
| 1 | 内容编址（objects/<oid>/<cv>）与只增发布/同对象共享去重/LRU 缓存预算单测通过 | `ObjectStoreTest` 11 用例两模式全绿：`Publish_CreatesContentAddressedPath_CON01`（编址路径 objects/<obj-规范文本>/<64hex>）、`Publish_ReturnsCv_MatchesCoreDigesterAndCodecEntry_CR02`（发布返回＝core 直算＝codec 入口三方相等）、`Publish_SameContent_SharesSingleFileUnchanged_CON01`（共享单文件＋字节/mtime 零变化）、`Publish_SameContentDifferentOid_NoCrossObjectSharing_CON01`、`Publish_NewVersion_AlongsidesOldBothReadable_CON01`、`Publish_EmptyPayload_RoundTrip_FipsEmptyVector_CON01`（FIPS 空串向量）、`Publish_AllZeroOid_FailFastInvalidArgument`、`Cache_EvictsLeastRecentlyUsed_WithinBudget`（cacheLruOrder 逐键断言）、`Cache_OversizeNotCached_ExactFitCached`（≤ 口径边界）、`Cache_EvictionDoesNotAffectHeldCopies_PA3`、`ConcurrentReads_WhilePublishing_ThreadSafe`（3 读线程×200 次×并发 20 发布） |
| 2 | 篡改检测（读取侧摘要校验失败拒绝 store-corrupt）；对象文件不可变（PA-2） | `TryObject_TamperedContent_RejectedStoreCorrupt`（等长单字节翻转，tryObject/object/scan 三通道检出）、`TryObject_TamperedSize_RejectedStoreCorrupt`（追加篡改＝size 半边）、`Publish_ExistingTargetDigestMismatch_RejectsStoreCorruptNoOverwrite`（发布侧既有文件摘要不符拒绝且不覆盖）、`Read_VerifiedOnce_FreeOfRecheckWithinContext_PinnedSemantic`（免复检语义双向钉住：同上下文免复检交付/重开上下文检出）、`PublishNewVersion_OldVersionAndReadersNeverOverwritten_PA2`（旧版本字节/mtime 不变＋旧读者隔离＋旧版本重发布走共享）——两模式全绿；检测能力真实性由 4.5 突变实验实证 |
| 3 | 引用存在性语义（tryObject 空 optional 不抛；有引用无对象→store-corrupt 材料/有对象无引用→悬挂对象开发诊断；负载原样存储 D-10/N-9） | `TryObject_MissingObject_ReturnsNulloptNoThrow_CON01`（文件缺/目录缺两形态）、`Object_MissingReferenced_ThrowsStoreCorrupt`（§5.2 错误表）、`Read_BinaryPayloadPreservedByteExactly_D10`（全 256 字节值＋非法 UTF-8 逐位保真＋磁盘半边逐字节比对）、`Scan_AllReferencedPresent_NoFindings`、`Scan_MissingReferenced_ReportedNoThrow`、`Scan_DanglingObject_ReportedAsDevDiagnostic_NotDeleted`（CapturingSink 断言 channel/count＋文件不删除）、`Scan_CorruptReferenced_FullAndExistenceModes`（全量档/存在性档两档）、`Scan_MixedFindings_AllBuckets_DeterministicReport`（四桶一次扫描＋两次扫描逐键相等）、`Scan_EmptyStore_NoFindings_InvalidRefFailsFast`、`TryObject_AllZeroKeys_FailFastInvalidArgument`——两模式全绿 |
| 4 | P-PR-1 处置（core 消费以 core.md v0.1 为基线不私改 core）＋CR-02 处置（摘要只经 core ContentDigester 不私设第二哈希路径） | `Publish_ReturnsCv_MatchesCoreDigesterAndCodecEntry_CR02`、`CoreContract_AddressingPath_SingleHashViaCore_CR02_PPR1`（发布返回值/文件名解析回读/目录名解析回读/core 直算四方相等，含 64KiB 跨分块负载）、`CoreContract_StrictCanonicalParse_ConsumedAsBaseline`（大写 hex/错 tag 拒绝）两模式全绿；**源码面亲查**：ObjectStore.cpp 全文零哈希计算——cv 一律经 `codec::contentVersionOf`，目录/文件名派生与读回解析全走 core 公共 API（toCanonical/tryFromCanonical，ObjectStore.cpp L154-172/L546-553）；include 面＝core 公共契约头 Identity.hpp/Digest.hpp（ObjectStore.hpp L77-78）；4.1 diff 核对 core 目录零文件＝零 core 修改 |

- **ird_gates**（WP-01-T01 已落地，须亲跑）：验收者亲建构建树执行 `cmake --build build --config Release --target ird_gates` → **exit 1（命中为预期）**，命中集合＝**IRD-GATE-SUB×7**（diagnostics_contract/evidence/evidence_contract/policy/policy_contract/runtime 各 _test 直链 sdurws_ird_testkit＋testdata_lint→testkit 表外边）＋**IRD-GATE-T1×1**（testdata_lint 链接 testkit），全部为其他单元既有登记命中（DTB §4.5 减免通道），**project 单元零命中、零新增**——与 wp04-t03/wp04-t04 基线（PRJ-T04 验收记录 4.4 所载 8 项同分布）逐项一致；门禁自检项（pass_clean/pass_r4comment/fail_r1/r3/r4/r5/sub/t1/t2）全部按预期输出。
- **validate-task**：`pwsh -File scripts/industrialrobot/validate-task.ps1 -TaskFile doc/industrial-robot-design/tasks/foundation/PRJ-T05.json` → **PASS（1 tasks）**，exit 0（亲手）。

### 4.5 测试真实失败能力 —— PASS（验证验证者，2 处突变均精确变红）

在临时 worktree A 中对实现做两处独立削弱后重建重跑（实验修改用后即还原并复绿，未触碰送验对象 SHA，未向任何分支推送）：

1. **摘要校验短路**（readVerified 中 `if (!(actual == cv))` → `if (false && …)`）：`--gtest_filter=ObjectStoreTest.TryObject_TamperedContent_RejectedStoreCorrupt` → **FAILED**（篡改检出断言真实有效，非恒真）。
2. **LRU 逐出短路**（cacheInsert 中 `while (…)` → `while (false && …)`）：`--gtest_filter=ObjectStoreTest.Cache_EvictsLeastRecentlyUsed_WithinBudget` → **FAILED**（预算/逐出断言真实有效）。

两突变并存单次运行：`[  FAILED  ] 2 tests`＋`[  PASSED  ] 0 tests`（acc-45-mutation-run.log 实测）。还原两处修改后重建，全量 **109/109 复绿**（acc-test-run-integration-restored.log）——证明送验实现本身全绿非实验残留。

### 4.6 架构红线（AGENTS §5 八条逐条） —— PASS

1. **L2 计算内核零 Qt**：ObjectStore.{hpp,cpp}/ObjectStoreTest.cpp include 面 grep 实测零 Qt（唯一命中为 BuildRedLineTest.cpp 红线扫描器自身的模式串）；ird_gates 零 project 项 ✓。
2. **业务域互链禁止（R-1）**：链接面仅 `sdurws_ird_core`（CMakeLists target_link_libraries 亲读）；CMake 配置期守卫（R-1/T-1 FATAL 断言）在位；ird_gates 亲手复跑 project 零命中 ✓。
3. **跨单元私有头禁止（R-2）**：ObjectStore.hpp 置于 src/（私有实现头，不出 include/）；测试目标 PRIVATE include src/ 为同单元允许形态；跨单元 include 仅 core 公共头（Digest.hpp/Identity.hpp/DiagData.hpp）✓。
4. **框架零源码修改（SA-02）**：4.1 已证 ✓。
5. **不做名称前缀拼接/剥离（R-4）**：ObjectStore.{hpp,cpp} 全文无任何 RWS/RobWork 名称处理逻辑（全文亲读）✓。
6. **权威唯一（PA-1）**：对象库不解析清单——scanObjects 契约注释明确"引用集由调用方组装——权威唯一 PA-1：清单语义归 RevisionIndex"（ObjectStore.hpp L317-319）；悬挂/不合规发现仅开发诊断 reportDev，用户级文案归 diagnostics（P-PR-6 链路不动）；单位/诊断稳定码沿 StoreErrorCode 封闭集，不自造码段 ✓。
7. **不可变历史（PA-2）**：只增发布为结构保证（唯一写动作 publishNew rename、目标已存在即失败不覆盖）＋用例钉住（acceptance 2 组）✓。
8. **不继承 old/ 历史**：diff 全清单无 old/ 引用，实现从零编写 ✓。

### 4.7 注释规范（AGENTS §2） —— PASS（公共接口全查＋实现文件抽查 3 个）

- **新增接口头全查**：`src/ObjectStore.hpp`（395 行）——文件头（设计依据 §4.6/§4.4.6/§7.1/§3.4＋需求 CON-01/PA-2/D-10/N-9＋契约 acceptance＋"为什么对象库独立成类"背景＋CR-02/P-PR-1 摘要路径纪律＋错误二分三分类＋线程约束＋所有权生命周期）；每个 struct/class Doxygen 中文背景说明（ObjectKey/ObjectKeyHash〔含 0x9e3779b97f4a7c15 黄金比例常数的来源解释〕/ObjectScanReport〔四桶各自映射语义〕/ObjectCacheStats〔记账口径〕/ObjectStore〔生命周期归 PRJ-T08 持有〕）；每个函数 @brief＋执行序分步＋@param 方向/单位/默认值＋@return＋@throws 分类（fail-fast vs StoreError 稳定码）；§2.5 高危信息逐项在位：单位（字节/256 MiB/预算）、线程（读并发/写调用方串行化/互斥范围）、所有权（sink 非 owning 可空、深拷贝值语义）、错误语义（数据侧/调用方/环境三分类＋映射表）、确定性（报告排序键 NFR-COR-02）。
- **实现文件抽查（≥3）**：①`src/ObjectStore.cpp`（642 行全文亲读）——文件头回指设计依据、错误映射三函数逐条映射依据（ERROR_DISK_FULL=70/ERROR_DISK_QUOTA_EXCEEDED=395/ERROR_ACCESS_DENIED=5 原生码常量注释）、publishObject 五步执行序每步业务含义与失败去向、EXISTS→消失竞态有界重试的窗口说明、防御性契约声明（core canonical 失真→logic_error）、免复检语义与"摘要计算在锁外"的并发理由、扫描 error_code 版 increment 的异常轨道规避、逐出记账口径——无复述式废注释、无中英混杂机翻腔；②`test/ObjectStoreTest.cpp`（996 行抽查 500+ 行：文件头陷阱处置自证＋夹具辅助＋Publish/Read/Immutability/Cache/Scan 组）——每用例锚定注释注明 acceptance/§ 章节/需求追溯、断言理由（如"断言表达式整体加括号：ObjectKey{a,b} 内的逗号会被宏当参数分隔符"）；③`project/CMakeLists.txt`——PRJ-T05 落位注释块（编址/只增/校验/LRU/扫描语义面＋R-2 处置＋CR-02 唯一入口）与测试增列注释块，关键步骤中文注释块完整。
- **微瑕留档（不转登、不阻断）**：ObjectStore.hpp L240 注释续行缺 ` * ` 前缀（Doxygen 块内行格式不一致，publishObject @param payloadBytes 的第二行；编译与语义无影响）。

### 4.8 文档同步＋findings 消账核对 —— PASS（同步零偏差；消账核对 3 项再现转登 F-131～F-133）

- **单元卡同步**：units/project.md §15.4 新增 v0.7 行（L1377）与实现逐项一致（五段语义/实现口径三项登记/CR-02/P-PR-1 处置/实现期缺陷留证/28 用例分布/两模式 109/109/门禁零新增）；§12 任务行（L1237）`ObjectStore.*` 产物与实际落位文件一致；实现与任务卡零偏差——实现口径三项登记（错误映射表/发布侧强制实算/双通道交付）属 §4.6 未定义判据的 DTB §5.4 落值登记，非语义偏离；`validate-task` PASS（亲手，见 4.4）。
- **findings.json 未关闭条目相关性核对**（open 109 条逐族核对，与本任务相关者如下）：
  - **F-128/F-130/F-012 族（提交标题名词开头）**：**再现**——b6cc2352 标题『[WP-04-T05] PRJ-T05 验证留痕归档（…）』前缀后以任务名词开头（F-128 同字面形态）；7f058bde 标题『[units] project.md v0.7：…』以文件名词短语开头（F-130 第二例 ae9596db 同型）——一次两例，转登 **F-131**。
  - **F-129/F-125/F-127 族（幻影清单/库外日志引用）**：**再现（明示形态）**——b6cc2352 正文**主动明示**『10 个 .log 证据文件因 .gitignore *.log 规则不入库、以工作区文件形态留痕』（较 a148a99e 的隐匿形态诚实度提升），但 ird-test-report.json 的 gates 字段（gates-ird-gates-target.log/validate-task.log）与 buildModes 字段（四份构建日志）仍为库外路径引用（`git ls-tree` 实证 builds/wp04-t05/ 仅入库 5 件），后来者按图索骥不可得——根因同 F-010（.gitignore:87 *.log），按家族判例登记建议级，转登 **F-132**。
  - **F-123（project.md 卡头版本/日期行失同步）**：**恶化再现**——本任务触及 units/project.md（追加 v0.7 行）但卡头『文档版本』行停留 **v0.4（PRJ-T02…）**、『日期』行停留 **2026-09-09**，而 §15.4 末行已是 v0.7（2026-09-16）——F-123 处置建议『随下次触及的任务把卡头行推进』在 PRJ-T03（v0.5）/PRJ-T04（v0.6）/本任务（v0.7）连续三次触及时均未执行，失同步差距由 1 个版本扩大到 3 个；v0.7 登记内容本身完整准确（少报方向），按 F-123 判例建议级，转登 **F-133**。
  - **F-124（"冒烟模式无别名"注释失准）**：本任务触及 project/CMakeLists.txt 但别名注释块（L129 附近）不在本次触及 hunks 内、未更正——与 PRJ-T04 判定同口径，**保持 open**。
  - **F-122（CoreEdgePresent 断言敏感度缺口）**：本任务未改变构建图（链接面不变），处置窗口（PRJ-T04 顺带）已过仍未收口——非本任务范围，**保持 open**（提示：已连续两个 project 任务未触发该处置建议，所有者宜显式指定收口任务）。
  - **F-115/F-019/F-051/F-072（ird_gates 基线命中与登记义务悬置）**：本任务门禁命中仍为 8 项基线（零新增、零 project 项，亲手复跑实证）——登记义务仍归所有者，**保持 open**。
  - **F-008（全新树全树构建失败）**：构成本次 4.2 目标面口径的原因之一——**保持 open**。
  - **F-007/F-062/F-099（环境链：vcpkg/ini 模板/Qt 前缀）**：验收者本轮亲历复踩与登记口径一致（首轮 bin/ 模板缺失 configure_file 失败、补齐后 exit 0）——**保持 open**。
  - **F-065（留痕日志中文乱码）**：本轮 ird_gates 控制台重定向输出再现 GBK 乱码现象（控制台代码页问题，命中集合可辨识、不影响判定）——同源，**保持 open**。
  - 其余 open 条目（policy/runtime/evidence/diagnostics/testkit/io 各族）与本任务无交叠。

### 4.9 commit 质量 —— PASS（四段齐备、无粉饰；标题家族问题转登 F-131）

- `9438eb0e`『[WP-04-T05] 实现 project 对象库（ObjectStore：内容编址/只增发布/LRU）』——动词开头合规；正文四段（What 按文件列要点/Why 需求与契约 ID/验证/影响面）齐备；验证结论（两模式 109/109、门禁零新增、validate-task PASS、实现期缺陷由测试真实失败驱动修复）经本验收逐项独立复现**全部为真**；Acc-Covered 对账行在位。
- `7f058bde`『[units] project.md v0.7：PRJ-T05 对象库落位登记』——正文四段齐备、登记内容与实现一致；标题以文件名词短语开头（→ F-131）。
- `b6cc2352`『[WP-04-T05] PRJ-T05 验证留痕归档（构建/门禁/测试执行证据）』——正文四段齐备且**诚实明示** 10 份 .log 因 .gitignore 不入库（无隐瞒，见 4.8/F-132 定性）；标题以任务名词开头（→ F-131）；"本提交零代码变更"声明与 diff 实际一致（纯 traceability/** 新增）。
- 三提交均未发现验证结论与留痕不符的粉饰情形。

### 4.10 留痕完整 —— PASS（入库件亲验；.log 工作区形态按家族判例登记）

- **gtest XML×4 入库亲验**：builds/wp04-t05/ 下 `sdurws_ird_project_test-{integration,smoke}.xml` 头部实测 `tests="109" failures="0" errors="0"`、`sdurws_ird_project_contract_test-{integration,smoke}.xml` 实测 `tests="10" failures="0" errors="0"`（timestamp 2026-09-16T04:58~04:59，与验收者亲手复现数值一致）。
- **ird-test-report.json 入库**：schemaVersion/targets/testSuites（28 用例逐用例 trace 到 acceptance 与 § 章节）/gates/buildModes/acceptanceCoverage 五段结构与库内 XML 数值互洽。
- **构建日志**：10 份 .log 因根 .gitignore *.log 规则不入库（实施正文明示的工作区留痕形态；`git ls-tree` 实证零 .log）——建议级，见 F-132；其结论性内容（构建 exit 0/测试全绿/门禁 8 命中/validate-task PASS）已由验收者亲手复现全部为真。
- **ird_gates/validate-task 留痕**：验收者亲手复跑（见 4.4），命中集与 PASS 结论与留痕记载一致。
- **phase-one-validation.log/resultNote 维度**：phase-one-validation.log 无 PRJ-T01～T05 任一任务行（foundation 系任务自 PRJ-T01 起均按 builds/wp04-tNN/ 留痕判例执行，PRJ-T04 验收同口径 PASS）——沿用既有判例，不构成本任务缺口。

### 4.11 偷懒/缩水实现扫描 —— PASS

1. **标记扫描**：`git diff fd1b197..b6cc235 | grep -inE "TODO|FIXME|stub|placeholder|未实现|not implemented"` → **零命中**（grep exit=1 实测）；无"触发式延后"声明需豁免。
2. **空实现检测**：ObjectStore.hpp 声明的全部方法（publishObject/tryObject/object/scanObjects/cacheStats/cacheLruOrder＋私有辅助）在 ObjectStore.cpp 均有实质实现（642 行全文亲读：无空函数体、无仅返回默认值的占位、无恒真分支）。
3. **acceptance 逐条证据表**：见 4.4——四条均有具名用例＋亲手运行结果，任一条无证据即 fail 的情形未出现。
4. **测试强度**：断言校验行为与边界非恒真——LRU 逐键序断言（cacheLruOrder 全序比对）、篡改双向（等长翻转/追加）、免复检语义双向钉住（同上下文交付新字节/重开检出）、D-10 全 256 字节值逐位 memcmp＋磁盘半边逐字节比对、四桶混合扫描＋两次扫描逐键相等（确定性）、并发 3×200 读零失真；**4.5 突变实验实证**两处削弱即精确变红——测试集具备真实失败能力，非"只编译通过"。

## 2. 发现的问题清单

| 级别 | 编号 | 摘要 | 处置 |
| --- | --- | --- | --- |
| 阻断 | 无 | —— | —— |
| 建议 | F-131 | 提交标题家族再现（F-012 系，一次两例）：b6cc2352 以任务名词开头（F-128 同字面）、7f058bde 以文件名词短语开头（F-130 第二例同型） | 转登 findings.json；不追溯改写 |
| 建议 | F-132 | 幻影清单家族再现（F-129 后第 12 例，**明示形态**）：正文主动声明 .log 不入库，但 ird-test-report.json 六处日志引用仍为库外路径；根因同 F-010 | 转登 findings.json；处置同 F-129①（.gitignore 否定规则或 .txt 后缀） |
| 建议 | F-133 | project.md 卡头版本/日期行失同步恶化（F-123 后连续第三次未执行）：卡头停 v0.4/2026-09-09，§15.4 已至 v0.7/2026-09-16 | 转登 findings.json；随下次触及顺手更正或批量修正批次 |
| 留档 | —— | ObjectStore.hpp L240 注释续行缺 ` * ` 前缀（格式微瑕，无语义影响） | 随下次触及该文件顺手更正，不单开条目 |

## 3. 裁决

**verdict＝pass**。4.1～4.11 全部通过、零阻断级问题；建议级 3 项（F-131～F-133）已转登 `traceability/findings.json`（本 evidence 分支同支承载），不阻塞合入。验收者不执行合入——按 ACC §6 由所有者决策。

## 4. 记录三元组与自证

- branch：`acc/PRJ-T05/2`（基于 origin/redesign-main＝fd1b1971）
- path：`RobWork/doc/industrial-robot-design/traceability/acceptance/PRJ-T05-20260916.md`
- commit：（见本文件提交后由验收者回填至编排者输出，40 位 SHA）
- 验收者证据目录（本地工作区，不入库）：`D:/10_Source_Repos/21_robot/acc-PRJ-T05-attempt2/acc-evidence/`（亲手 gtest XML×4、测试运行 log×4、ird_gates log、validate-task log、4.5 突变实验 log×2）＋构建/配置 log×6（configure-integration{,2}/build-verify-target/build-contract-test/smoke-configure/smoke-build-{release,debug}）
