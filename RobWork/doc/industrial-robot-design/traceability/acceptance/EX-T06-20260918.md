# EX-T06 验收记录（attempt 1，2026-09-18）

| 字段 | 值 |
| --- | --- |
| 任务 | EX-T06（≙WP-08-T07）：WorkerLauncher 与通道（ProcessLauncher/ChannelPair/JobScope/worker） |
| 契约 | tasks/foundation/EX-T06.json（schemaVersion ird-task/1，branch wp08-t06） |
| 送验对象（冻结） | 22044b7778d007289ba9eb1d9c6199808bdde200 |
| base | b727e50d426f44dfb84dce43ffb70360b815caa6（＝origin/redesign-main 尖端） |
| diff 范围 | b727e50d..22044b77（31 文件：+8465/−23） |
| **verdict** | **pass**（4.1~4.11 全部通过，零阻断级；建议级 1 项已转登 findings.json F-214） |

## 独立性声明（ACC §2）

本验收为全新上下文：未消费实施者任何过程叙述，仅读代码、契约、设计文档与在库留痕；构建与测试全部由验收者亲手在冻结 SHA 的独立 worktree 重跑；以"为不通过找证据"的对抗立场逐项核查（4.5 双探针构造失败输入、4.11 逐条证据表）。

## 3. 送验对象冻结与分支漂移检查 —— 通过

- `git fetch origin` 后 `git rev-parse origin/wp08-t06` ＝ `22044b7778d007289ba9eb1d9c6199808bdde200`（与送验 headSha 逐字节一致）——**零分支漂移**；
- `origin/redesign-main` ＝ `b727e50d426f44dfb84dce43ffb70360b815caa6`（与编排者下发的 base 一致）；
- 复现现场：`git worktree add --detach D:/tmp-acc-ex-t06/repro 22044b77…`（detached @ SHA，非分支名）。

## 4.1 改动范围 —— 通过

`git diff --name-status b727e50d..22044b77` 全量 31 文件逐一对照契约 allowedFiles：

| 范围 | 文件数 | 判定 |
| --- | --- | --- |
| `RobWork/RobWorkStudio/src/rwslibs/industrialrobot/execution/**` | 16（新增 13＋修改 3：CMakeLists.txt、LinkageContractTest.cpp、Ports.hpp） | ✅ allowedFiles 第 1 条 |
| `RobWork/doc/industrial-robot-design/traceability/builds/wp08-t06/**` | 14（全部新增留痕） | ✅ allowedFiles 第 3 条 |
| `RobWork/doc/industrial-robot-design/units/execution.md` | 1（§15.4 增 v0.8 行） | ✅ allowedFiles 第 2 条 |

- forbiddenFiles（REQUIREMENTS.md、ARCHITECTURE.md）：diff 中**零出现**；
- 框架源码：diff 零触碰（RobWork/RobWorkStudio/RobWorkSim 框架目录零文件）；`patches/` 目录 diff 零文件——SA-02 无登记义务触发；
- 过滤测试：`git diff --name-only … | grep -v execution/ | grep -v doc/industrial-robot-design/` 输出为空——**无任何范围外文件**。

## 4.2 可复现构建 —— 通过

- **集成模式**（worktree A 内从零配置，`RWS_BUILD_INDUSTRIALROBOT:BOOL=ON` 缓存实测确认）：
  - 环境还原两处（与 F-007 已登记口径一致）：①`RobWorkStudio.ini.template.static`/`.shared.in` 被 .gitignore `[Bb]in/` 规则吞掉，从主仓复制该两件环境文件；②按主构建树缓存口径带 `-DCMAKE_TOOLCHAIN_FILE=<主仓>/vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`，并按主树同值 `-DUSE_Python=OFF -DBUILD_sdurws_pythoneditor=OFF`（缓存 diff 仅此两布尔项）；
  - 契约两目标（即 4.3 的 verify 命令）`--target sdurws_ird_execution_test` **exit 0**、`--target sdurws_ird_execution_contract_test` **exit 0**；产物 `build/RobWorkStudio/bin/Release/sdurws_ird_execution_test.exe`、`…_contract_test.exe`、`…_execution_worker.exe` 实存；
  - 全量 `cmake --build build --config Release` 失败于 **RWSimulatorPlugin**（RobWorkSim 框架插件，rw/math/Math.hpp `min` 宏污染簇，73 处 error C）——**F-008 已登记的基线框架缺陷**（open），与本任务 diff 零相关（4.1 已证 diff 不触碰 RobWorkSim；EX-T04/EX-T05 两次验收同点复现，同口径不阻断）；ird 目标不依赖该插件；
  - `--target ird_gates` 亲手执行：命中分布与送验日志**逐码全等**（LIB×8、R1×1、R3×1、R4×3、R5×1、SUB×23、T1×3、T2×2），命中集合编码归一 diff 为空，**零 execution 相关命中**（零新增实证）；
- **独立冒烟模式**（`D:/tmp-acc-ex-t06/smoke` 独立缓存）：`cmake -S …/industrialrobot -B <smoke> -DCMAKE_TOOLCHAIN_FILE=<主仓>/vcpkg/…/vcpkg.cmake` configure **exit 0**；`cmake --build --config Release` **exit 0**（含 `sdurws_ird_execution_worker.exe`）；
- **测试亲手执行（四跑全绿）**：
  | 模式 | 单元测试 | 契约测试（真进程） |
  | --- | --- | --- |
  | 集成 | **113/113 PASSED exit 0**（1623 ms） | **27/27 PASSED exit 0**（1942 ms） |
  | 冒烟 | **113/113 PASSED exit 0**（1507 ms） | **27/27 PASSED exit 0**（2142 ms） |
- 与送验在库留痕交叉比对：`traceability/builds/wp08-t06/` 四份 XML 顶层 `tests="113"/"27"`、failures/errors 均无——与本人四跑数值一致，无粉饰迹象。

## 4.3 契约 verify 命令 —— 通过

契约 `verify` 数组两条，验收者在 worktree A 根**逐条逐字亲手执行**：

1. `cmake --build build --config Release --target sdurws_ird_execution_test` → **exit 0**；
2. `cmake --build build --config Release --target sdurws_ird_execution_contract_test` → **exit 0**（含 worker exe 依赖目标连带构建）。

## 4.4 acceptance 条目逐项证据 —— 通过

| # | 契约条目（摘） | 验收者亲手证据 |
| --- | --- | --- |
| 1 | EX-WKR-1~3 全绿：崩溃只失败当前任务（EX-WKR-2——仅当前任务 Failed＋EX-WORKER-CRASHED、主进程存活、其他任务不受影响、最近检查点保留、崩溃 worker 永不回池——NFR-REL-02/D-02）；卡死经心跳失联走强杀（EX-WKR-3——D-06 实现参数非需求值、诊断码区分）；正常完成走九步接纳（EX-WKR-1——握手 manifest 比对 CON-06） | 契约测试四跑全绿：`WorkerCrashFailsOnlyItsOwnTask_EX_WKR_2`（真进程 0xC0000005：A 走 T9＋EX-WORKER-CRASHED 判定诊断、检查点字节崩溃前后逐字节相等、B 照常 Admitted＋T8、新任务 pid ≠ 崩溃 pid、A 恒 Dead、list() 存活面）＋`HeartbeatLossTriggersHungAndForceKill_EX_WKR_3`（ManualClock 虚拟推进 3×100 ms 窗、EX-WORKER-HUNG＋detail 含 "heartbeat-loss" 不含 "evaluationTimeout"、真实 TerminateJobObject 后 isAlive()==false、Dead 永不回池）＋`WorkerCompletesViaHandshakeAndNineStepAdmission_EX_WKR_1`＋`HandshakeManifestMismatchRejectsDispatch`（握手失配拒绝派发面）——D-06 默认值钉值见 `HeartbeatPolicyDefaultsAreD06Parameters`（5 s×3 实现参数非需求值） |
| 2 | EX-CHN-1：通道消息全携五元组（IRDCHN/1 帧自定界＋seq 序控——§6.5/D-15；乱序/重复按 seq 重排去重，断裂→协议错误→Failed＋EX-CHANNEL-PROTOCOL-ERROR） | 单元测试四跑全绿：`OutOfOrderFramesReorderBySeq_EX_CHN_1`（乱序注入按 seq 重排交付）＋`DuplicatesAndBackwardsAreDroppedForDevDiagnostic_EX_CHN_1`＋`WindowOverflowIsProtocolError_EX_CHN_1`＋`GapTimeoutIsProtocolError_EX_CHN_1`（ManualClock 缺口超时断裂）＋帧自定界三用例（跨 chunk 边界/坏 magic 版本拒/超长拒）；真进程半区 `SequenceGapBreaksChannelAndFailsTask_EX_CHN_1`（真进程缺口→协议错误→任务失败）；五元组全程随行：帧头 168B 定长身份块编解码＋每帧 identity 校验（ChannelProtocol.cpp 实读） |
| 3 | 流式分批回传（ResultBatch——§6.5、NFR-PERF-03 不整体装载）；worker 临时目录隔离（§6.4——不写项目正式目录、结果经主进程登记核对） | `ResultBatchesStreamInOrderBeforeFinal`（三批独立到达、索引 {1,2,3} 按序、逐批 "BATCH:<i>" 载荷完整、通道不合并批次）＋单元面 `FragmentRoundtripLargeMessage`/`EmptyMessageStillProducesOneFragment`（DispatchRequest/FinalOutput 恒分片布局）；`WorkerTempDirIsolatedAndCleanedUp`（真进程写 %TEMP%\ird-worker-<pid>-<run>-<attempt>\artifact.bin 实存、项目 results/ 树递归扫描零泄漏、正常退出后临时目录清理实 measurement） |
| 4 | R-3 处置：Windows 进程/作业/管道行为逐项对照 Microsoft Learn 口径（§6.6）真进程实证不超诺；P-EX-3 处置：快照物化经 IExecutionModelService 注入消费，零编译依赖不私建 execution→runtime 边 | `LauncherClosesInheritedHandleDuplicates_R3`（真进程 launch 后 `hasOpenChildHandleDuplicates`==false——句柄继承副本防泄漏实证）＋`SupervisorForceKillTakesPrecedence`/`ConventionSetMapsPerContractTable`/`OutsideConventionSetIsCrashedIncludingAccessExceptions`（退出码约定集表逐格）；不超诺声明实读（JobScope.hpp/ProcessLauncher.hpp 文件头"不超诺"段——只承诺 Microsoft Learn 明文语义，不承诺 breakaway 防御与 DLL 分离清理）；P-EX-3：Ports.hpp 增 IExecutionModelService/MaterializedDispatch/ModelPrepareResult/PrepareOptions（值传递、字节对 execution 不透明），include 扫描 execution 单元零 `sdurws/ird/runtime/` 头、ird_gates 零新增命中（无 SA-10 表外边）——契约 acceptance 4"如架构补登边可平滑直连"的注入形态成立 |

未执行项标注：无——四条 acceptance 全部有亲手执行证据；`ird_gates` 已随 WP-01-T01 落地，本次亲手执行（见 4.2）。

## 4.5 测试真实失败能力 —— 通过（双探针红/还原复跑全绿）

| 探针 | 注入点（worktree A 内，探后 `git checkout --` 还原） | 预期红 | 实测 |
| --- | --- | --- | --- |
| 1 | WorkerSupervisor.cpp `classifyWorkerExitCode` default 分支 `return Crashed` → `return NormalCompletion` | 约定集外分类用例必须红 | `ExitClassificationTest.OutsideConventionSetIsCrashedIncludingAccessExceptions` **FAILED**（1/3 红） |
| 2 | ChannelProtocol.cpp `FrameAssembler::feed` magic 校验 `memcmp(...)!=0` → `if (false)` | 坏魔数拒绝用例必须红 | `ChannelProtocolFrameTest.AssemblerRejectsBadMagicAndVersion` **FAILED**（1/2 红） |

还原后重建复跑：**113/113＋27/27 全绿**，`git status`（execution 单元）干净——送验代码零残留改动。测试非恒真实证完毕。

## 4.6 架构红线 —— 通过

AGENTS.md §5 八条逐条（对 diff 新增行与全单元实扫）：

1. **L2 计算内核零 Qt**：execution 全单元 `#include` 扫描零 Qt 头（唯一 "Q" 命中为既有 BuildRedLineTest.cpp 注释文本）；worker 目标 `WIN32_EXECUTABLE ON` 无控制台——✅
2. **业务域互链禁止（R-1）**：include 扫描零 modeling/requirements/kinematics/trajectory/dynamics/selection/optimization 头——✅
3. **跨单元私有头禁止（R-2）**：跨单元 include 仅 `<sdurws/ird/core/…>`、`<sdurws/ird/evidence/…>`、`<sdurws/ird/project/…>` 公共路径；win32 三件私有头（ChannelPair/JobScope/ProcessLauncher）物理隔离于 `src/win32/` 且仅被本单元 src 消费、不出 `include/`；无相对路径跨目录 include——✅
4. **框架零源码修改（SA-02）**：diff 零框架文件、patches/ 零新增——✅（4.1 已证）
5. **不做 RobWork 名称前缀拼接/剥离（R-4）**：diff 新增行 "RobWork" 字面量/前缀操作扫描零命中——✅
6. **权威唯一（PA-1）**：监督器"不越权声明"（WorkerSupervisor.hpp 文件头：不理解状态机、不接触接纳、不写盘——事件流在"通道消息→结构化事件"处切分）；检查点持久化编排归 EX-T08（EX-WKR-2 只断言通道面字节完好）；worker 临时目录清理失败经通道开发诊断回传——✅
7. **不可变历史（PA-2）**：本任务不触碰修订/写路径——不适用且无违反迹象——✅
8. **不继承 old/ 历史**：全部实现为按单元卡从零编写（文件头设计依据逐条锚定 §6.4~§6.6），无 old/ 引用——✅

## 4.7 注释规范 —— 通过

- **公共头全查（3 件）**：ChannelProtocol.hpp（458 行——文件头含设计依据/背景"为什么不引第三方 IPC"/字节序纪律/线程安全声明；每常量注明来源与单位〔ms/字节〕；每函数 @param 方向+违约语义+返回失败行为；FrameSequencer 注明实现参数非上游值）；WorkerSupervisor.hpp（464 行——文件头含线程模型逐方法标注〔仅调度线程/并发只读〕、注入时钟纪律、"不越权声明"；每结构生命周期与池纪律注明）；Ports.hpp 增量段（IExecutionModelService 四件——P-EX-3 处置背景、值传递不可变性、线程约束逐条）；
- **实现文件抽查（4 件 ≥3）**：ChannelProtocol.cpp（文件头+小端 helper 逐段注释）、WorkerSupervisor.cpp（实现要点对照头注释+退出码表逐行中文）、win32/JobScope.hpp 与 ProcessLauncher.hpp（KILL_ON_JOB_CLOSE Microsoft Learn 口径引文、"不超诺"专段、五步启动时序注释）；
- **§2.5 高危信息表**：物理单位（ms/字节，如 `readerSliceMs`/`dispatchFragmentBytes`）、线程约束（每方法/每成员标注）、错误语义（fail-fast vs 协议错误面逐点注明）、确定性来源（LE 字节序/SHA-256 同源/ManualClock）、坐标系与角度制本任务无涉（通道协议域）；魔法数字均有来源（168B＝4×36+24、23B 帧头、64 KiB 折中等）——**缺项为零**。

## 4.8 文档同步 —— 通过

- 单元卡 `units/execution.md` §15.4 增 **v0.8** 行：落位登记（两公共头＋Ports 扩展＋win32 三件＋worker 宿主）＋**实现登记四处**（launch 握手异步读法、worker 链接形态＝共享源直编＋白名单边、ErrorReport dev 通道、ChannelProtocol 组成行增序控/身份核对 helper）＋P-EX-3/P-EX-1/R-3 处置留痕——实现与登记逐条对得上（验收者对照代码逐项实读一致），属 DTB §5.4 合规登记，非"代码先行、文档失实"；
- WorkerAssignment 增 manifestDigest 字段、Ports.hpp 归置修订均按"增量登记"先例（EX-T03 evaluationTimeout 同口径）落账；
- 治理脚本亲手复跑：`validate-docs.ps1` **PASS**（20 units, 12 trace entries, 150 task files）＝送验 validate-task.log 同数字；`validate-task.ps1 -TaskFile EX-T06.json` **PASS**；
- **findings.json 未关闭条目相关性核对（ACC v1.2）**：open 条目（约 190 项）按 EX-T06/execution/worker/通道/IRDCHN 关键词检索：F-212/F-213（EX-T05 验收建议，归属"execution 单元所有者——EX-T09 或冻结 diff 增量同步窗口"）、F-031（EX-T09 契约括注补正，归属 wp08-t09 领取者）、F-211（EX-T04 提交标题风格）——**零条**属本任务送验范围或可随本任务顺手消账（本任务 diff 未触碰 EventBusDeliveryContractTest.cpp/SchedulerEventBusTest.cpp/EX-T09 契约）；F-007（ini.template.static 被 .gitignore 吞掉）与 F-008（RWSimulatorPlugin 基线失败）在本次复现现场再次实证，维持 open。

## 4.9 commit 质量 —— 通过（附 1 项建议级）

范围两个提交逐一核查：

1. `d19839d0` `[WP-08-T07] 实现 EX-T06 worker 启动链与 IRDCHN/1 通道（监督器/协议/Win32 设施/worker 宿主）`——任务编号＋**动词开头**合规；正文四段（What 按文件列要点/Why 契约与单元卡章节＋需求 ID/How 双模式构建＋113/113＋27/27＋ird_gates 零新增＋validate-task PASS＋留痕位置/影响面）齐备；验证结论与亲手复跑一致；`Acc-Covered: 1,2,3,4` 尾注；
2. `22044b77` `[WP-08-T07] EX-T06 文档同步与验证留痕（单元卡 v0.8 登记＋双模式证据）`——标题以名词"EX-T06"开头（非动词），F-012 家族同型（先例 F-205/F-208/F-210/F-211，均判建议级不阻断）；正文四段齐备，validate-docs PASS 数字与本人复跑逐字一致；`Acc-Covered: 无新增` 如实尾注；
- 无粉饰：两提交声明"两模式 113/113＋27/27 全绿"经本人四跑证实；ird_gates"零新增命中"经命中集合逐码比对证实。

## 4.10 留痕完整 —— 通过

- 送验提交内 `traceability/builds/wp08-t06/` 14 件：gtest XML×4（两模式两目标，顶层 `tests="113"/"27"`、零 failure/error）、两目标两模式运行日志×4、build-integrated-full.log（181 行，零 error）、build-smoke-config/full.log、gates-ird-gates-target.log（与本人复跑逐码全等）、validate-task.log（PASS）、ird-test-report.json；
- `ird-test-report.json`（ird-test-report/1 schema）：targets 逐条、acceptanceMapping、honestyNotes（含"零跳过零失败零伪造"、真进程 3 次复跑、链接形态登记、EX-T08 边界声明）——即 4.10"resultNote 已更新"的现行承载（EX-T04/EX-T05 先例同口径，phase-one-validation.log 机制已由其取代，本 diff 无该文件与先例一致）。

## 4.11 偷懒/缩水实现扫描 —— 通过

- **① 标记扫描**：diff 新增行 `TODO|FIXME|stub|placeholder|未实现|not implemented` 扫描**零命中**（代码与文档均零）；"阶段 A 脚本替身评估器"为契约 §3.4 明文的阶段 A 交付边界（非缩水——替身边界在 worker/main.cpp 文件头与 ird-test-report.json honestyNotes 双声明）；
- **② 空实现检测**：`return {}` 命中全部为带注释的协议错误闩守卫路径（FrameAssembler/MessageReassembler failed 分支——错误面语义非空实现）；公共头零 inline 业务体；实现文件 853/1149/783 行实质承载；
- **③ acceptance 逐条证据表**：见 4.4——四条全部具名测试＋实现实读双承载，零"已实现"裸声明；
- **④ 测试强度**：断言校验具体退出码（0xC0000005）、稳定码（EX-WORKER-HUNG/EX-WORKER-CRASHED）、pid 不复用、字节级检查点保留、批次序 {1,2,3} 逐批全等、磁盘实存/清理实 measurement；ManualClock 虚拟推进替代 sleep（EX-WKR-3 350 ms 虚拟越窗）；与 4.5 双探针联动实证会红——非恒真、非"只编译通过"。

## 发现的问题清单

**阻断级：无。**

**建议级（已按 ACC §5 转登 traceability/findings.json，编号顺延至 F-214）：**

1. **F-214**：文档提交 22044b77 标题 `[WP-08-T07] EX-T06 文档同步与验证留痕（…）` 以任务号名词"EX-T06"开头，非动词开头——ACC §4.9/AGENTS §6.3 字面要求未满足；F-012 家族同型再现（先例 F-012/F-023/F-024/F-026/F-027/F-030/F-205/F-208/F-210/F-211，一贯判建议级不阻断；同任务另一提交 d19839d0"实现…"合规）。正文四段齐备、验证段数字经验收者亲手复跑逐项一致，纯风格瑕疵。处置建议：提交模板补动词开头示例（如"同步 EX-T06 文档与验证留痕（…）"）；对已推送历史不追溯改写（AGENTS §6.3）。

## 裁决

**verdict = pass**：4.1~4.11 全部通过，零阻断级问题；建议级 F-214 已随本记录转登 findings.json，不阻塞合入。送验对象 22044b7778d007289ba9eb1d9c6199808bdde200 可提交所有者进入合入决策（PIPE §4.7）。

## 验收者环境说明

- Windows 10（10.0.26200）x64、Git Bash、MSVC 2022（17.12.6）、CMake 4.3、Qt 6.11.1 msvc2022_64、vcpkg（主仓经典模式，gtest 1.18.0）；
- 复现现场：`D:/tmp-acc-ex-t06/repro`（detached @ 22044b77，集成树 build/ 从零配置）；冒烟：`D:/tmp-acc-ex-t06/smoke`（独立缓存＋vcpkg toolchain）；记录回传：`D:/tmp-acc-ex-t06/record`（分支 acc/EX-T06/1，基于 origin/redesign-main＝b727e50d）；
- 亲手执行命令与输出（验收会话临时区 `D:/tmp-acc-ex-t06/`）：acc-integrated-full.log/acc-integrated-full2.log（全量构建 F-008 失败点留证）、verify 两命令回显 exit 0、acc-ird-execution-{test,contract-test}-{integrated,smoke}.xml（四跑）、gates-raw.log＋gates-wt.txt/gates-sub.txt（命中集合 diff 空）、探针两轮红/还原回显；
- 探针后 worktree A 已 `git checkout --` 还原并复跑全绿，`git status`（execution 单元）干净；两 worktree 于本记录推送成功后由验收者删除。
