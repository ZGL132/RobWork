# RPT-T07 验收记录（acceptance record）

| 字段 | 值 |
| --- | --- |
| 任务 ID | RPT-T07（实现一致性检查器——多格式逐字段一致；unit=reporting；masterWp=WP-H；interUnit=false；≙DTB WP-12-T04；实现任务——全量清单验收） |
| 送验对象（冻结） | 07bb4a78ac4215e07db73ac8be42c09ed6b3d4af（分支 wp12-t07；diff 范围＝a0a8b99dd5382fcdd90a41d05c478f76b98e0c9c..07bb4a78ac4215e07db73ac8be42c09ed6b3d4af；origin/redesign-main 实测停于 a0a8b99d＝base，range 内含 82a5ff10（实现）／f42c36e9（单元卡 v0.10 同步）／07bb4a78（验证留痕）三个提交） |
| 验收尝试 | attempt 1（分支漂移检测：`git fetch` 后 `origin/wp12-t07` == 07bb4a78ac4215e07db73ac8be42c09ed6b3d4af 全 40 位逐字一致，零漂移；`acc/RPT-T07/1` 分支名此前不存在） |
| 验收环境 | 复现 worktree A＝`git worktree add --detach D:/10_Source_Repos/21_robot/acc-rpt-t07-repro` @ 07bb4a78（集成 build 树为本验收全新配置）；冒烟 build 树＝独立目录 `D:/10_Source_Repos/21_robot/acc-rpt-t07-smoke`（本验收新建）；记录回传 worktree B＝`git worktree add -b acc/RPT-T07/1 D:/10_Source_Repos/21_robot/acc-rpt-t07-record` origin/redesign-main@a0a8b99d；MSVC 2022 x64（VS 17.12）＋vcpkg 经典模式（toolchain 绝对路径指向主仓库 vcpkg——worktree 天然不含 vcpkg，`git ls-files vcpkg` 零输出实证）＋Qt 6.11.1（D:/software/Qt）。集成配置参数＝主树缓存实证口径：`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=<主仓>vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`，缓存确认 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`；初配两次失败（无 toolchain 时 Boost 不命中；缺 `RobWorkStudio.ini.template.static`——bin/ 整目录被 .gitignore `[Bb]in/` 吞）均为 F-007/F-098/F-099/F-200 既有登记环境链（本验收再次亲历），自主树复制 bin/ 两件未追踪辅助件后配置成功——非送验对象缺陷。冒烟配置同因补 Qt prefix（F-007 同源）。验收全程只消费产物、契约、设计文档与在库留痕，未读取实施者过程叙述；结论全部出自验收者亲手重跑 |
| verdict | **pass**（4.1～4.11 全部通过；无阻断级问题；建议级 1 条已转登 findings.json F-240） |
| 验收日期 | 2026-09-20 |

## 4.1 改动范围 —— 通过

- `git diff --stat a0a8b99..07bb4a78` 实证 **25 个变更文件**（7 A reporting 代码＋测试、1 M reporting/CMakeLists.txt、1 M units/reporting.md、16 A traceability/builds/wp12-t07/ 留痕）。
- **allowedFiles 逐文件比对（越界零命中）**：`git diff --name-only a0a8b99..07bb4a78 | grep -v -E '^RobWork/RobWorkStudio/src/rwslibs/industrialrobot/reporting/|^RobWork/doc/industrial-robot-design/units/reporting\.md$|^RobWork/doc/industrial-robot-design/traceability/'` **零输出**——7 件代码命中 `reporting/**`、17 件留痕命中 `traceability/**`、1 件命中 `units/reporting.md`，25/25 全部在册。
- forbiddenFiles（REQUIREMENTS.md／ARCHITECTURE.md）**零触碰**（diff 无该两文件）。
- 无未登记框架源码改动（SA-02）：diff 零框架路径；`patches/` 目录变化 **0 件**。
- Render.cpp 修改＋RenderText.hpp 新增均在 `reporting/**` 内（正向映射抽取为共享私有头的等价重构——单元卡 §14.4 v0.10 已登记，见 4.8）。

## 4.2 可复现构建 —— 通过（集成全新树＋独立冒烟树，两目标零错误）

- **集成模式**（worktree A 全新 `build/`，重新配置后缓存 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`）：契约 verify 两目标依次 `cmake --build build --config Release --target …` **均 exit 0，`error C[0-9]/error LNK/fatal error` 计数 0**（含 RobWork→RobWorkStudio→industrialrobot reporting 依赖链全新编译；产物 `build/RobWorkStudio/bin/Release/sdurws_ird_reporting_test.exe`＋`sdurws_ird_reporting_contract_test.exe`）。
- **独立冒烟模式**（独立目录，源＝worktree A 的 industrialrobot，`-DCMAKE_TOOLCHAIN_FILE=<主仓>vcpkg`＋Qt prefix）：配置 exit 0＋全量构建 **exit 0、错误计数 0**；`reporting/Release/` 下两测试 exe 就位。
- 说明：本验收未做"全树从零全量构建"口径——基线框架缺陷（F-008/F-235 既有登记：RobWorkSim RWSimulatorPlugin 全新树必败）使该口径当前不可达，送验留痕（integration-build.log/smoke-build.log）与近三任务（RPT-T05/T06）同为 verify 目标级零错误口径，本验收复现同口径成立；本任务 diff 零框架触碰（4.1 实证），框架既有缺陷不归本任务。

## 4.3 契约 verify 命令 —— 通过（两条逐条亲手执行，测试真实运行）

worktree A 按契约字面逐条独立执行，**两条全部 exit 0**：

1. `cmake --build build --config Release --target sdurws_ird_reporting_test` → exit 0
2. `cmake --build build --config Release --target sdurws_ird_reporting_contract_test` → exit 0

**测试真实运行（非只构建）**：集成模式亲跑两 exe → `[PASSED] 108 tests`＋`[PASSED] 38 tests`（failures=0 errors=0）；冒烟模式亲跑两 exe → 同为 108＋38 全绿。**与送验留痕四份 gtest XML 顶部统计（108/38 × integration/smoke，failures=0 errors=0）逐字一致**。

附加治理脚本（worktree A 上下文亲手复跑）：`validate-task.ps1 -TaskFile tasks/foundation/RPT-T07.json` → **PASS (1 tasks)**；`validate-docs.ps1` → **PASS (20 units, 12 trace entries, 150 task files)**——与在库 `builds/wp12-t07/validate-task.log`/`validate-docs.log` 一致。

**ird_gates 独立复跑**（worktree A 集成树 `cmake --build build --config Release --target ird_gates`）：exit 1＝既有命中集（F-019 既有红基线，非本任务引入）；命中经 token 频次归一（IRD-GATE-* 码×目标名）与在库 `gates-ird-gates-branch.log` **逐项相等**（LIB×8/R3×7/R4×3/SUB×35/T1×5＋自测段，差异仅为在库日志头部提及行对 testdata_lint/ui_gui_test 的名字出现次数——非命中行）；**reporting 目标零命中**（grep 实证）——送验 `gates-hit-set-diff.txt` 的 GATES-HIT-SET-IDENTICAL 结论独立成立。注：验收者捕获的门禁控制台输出存在 GBK 乱码（F-065/F-231/F-207 既有登记家族，环境级），已按 token 归一规避，不影响结论。

## 4.4 acceptance 条目 —— 通过（五条逐项证据）

| # | 契约条目 | 证据（验收者亲读源码＋亲手复跑） |
| --- | --- | --- |
| 1 | RP-CONS-1 逐字段一致与坏样本定位 | **合法三格式一致＋fieldCount 断言**：contract_test `ConsistencyThreeFormatTest.LegalReport_Consistent_FieldCount_RPT07_ACC1`（真渲染 HTML/JSON/CSV 三工件→回读逐字段一致→`fieldCount == matrix.size()`（13 单元格）→reader 工厂记账恰 1）亲跑通过。**三态坏样本定位到 (format, fieldKey, dimension)**：①HTML 删 data-field 属性→unit `DeleteDataFieldAttribute_MissingLocated_RPT07_ACC1`（missing 定位到具体 fieldKey——提取器以行内序位单元格重建键，Consistency.cpp:307-312）；删整格→`DeleteWholeValueCell_MissingLocated_RPT07_ACC1`；②CSV 改一值→contract `CsvChangeOneValue_ValueLocated_RPT07_ACC1`（Csv value 维度，expected/actual 逐字断言）；③JSON 删限定语→contract `JsonDeleteQualifier_QualifierLocated_RPT07_ACC1`（Json qualifier 维度）。**不短路**：contract `MismatchesListedExhaustively_NotShortCircuit_RPT07_ACC1`（CSV＋JSON 双坏样本→恰 2 条全量列出）亲跑通过；实现侧 `compareField` 逐维度全查不 return（Consistency.cpp:968-1025）、矩阵驱动外层循环无短路出口（:1126-1140）。**fieldCount 断言**另见 unit `HtmlPlusJsonMirror_Consistent_FieldCount_RPT07_ACC1`。以上 8 例双模式亲跑全绿 |
| 2 | RP-CONS-2 CSV 转义 roundtrip（NFR-SEC-03） | contract `ConsistencyCsvRoundtripTest.NastyValueSamples_RoundtripIdentical_NoEscapeIntoStructLayer_RPT07_ACC2`：样例集 **=（`=command;shutdown`）＋（`+增量`）−（`-0.5`）@（`@地址`）' 前缀（`'引号前缀`）/中文（含「」与 N·m）/引号（`他说："你好"`）/换行（`line1\nline2`）** 8 样本经 13 字段夹具导出→reader 回读→与源矩阵逐字符一致（`EXPECT_TRUE(result.consistent)`＋逐条 mismatch ADD_FAILURE 反证）。**转义不进结构化层双自证**：文件层断言 `'=command;shutdown`/`''引号前缀`/`"line1\nline2"` 转义形在字节中存在；结构层断言 `findCell(matrix,…)->valueRepr == "=command;shutdown"` 恒原文。**空串样例的缺席列承载**（SourcedValue::invalid 拒绝空串——core 契约）以文本字段 unit/qualifier 空列＋`plus->unit` 无值/`qualifier` 空集断言钉住（§8.3 空值≠"不适用"）。**io fake 边界**：roundtrip 消费测试注入的 FakeCsvWriter/FakeCsvReader 可逆对（P-RPT-1 裁决前既定形态——acceptance 5 明文允许"测试注入/L5 装配"；替身边界声明在测试文件头 L16-24＋单元卡 §14.4 v0.10；io 真实转义正确性归 io 验证矩阵 IO-V01/V02）——非偏差 |
| 3 | 七元组逐字段比对（§8.5 判定规则） | **value/unit/qualifier 三格式全比对**：`compareField` 的 value（:978-980）/unit（:983-985）/qualifier（:993-1009，token 序结构化比对）无格式分派——三格式恒查。**限定语丢失＝mismatch**：unit `DeleteQualifierSpan_QualifierLocated_RPT07_ACC3`（HTML span 删除）＋contract `JsonDeleteQualifier…`＋`CsvStatusColumnChange` 之外的 qualifier 面亲跑通过——§6.4"跳过限定语维度"禁止项的执行面。**status**：CSV 原生 token（contract `CsvStatusColumnChange_StatusLocated_RPT07_ACC3`）＋HTML 中文显示名经共享表反查（unit `ChangeStatusDisplay_StatusLocated_RPT07_ACC3`，"可行"→engineering-infeasible token 失配）；JSON 字段镜像结构上不携带该维度不比对——与"跳过限定语"不同质，**已按 DTB §5.4 冻结登记于单元卡 §14.4 v0.10 ②**（4.8 判定合规）。**ref 仅 CSV**：contract `CsvResultRefColumnChange_RefLocated_RPT07_ACC3`（result_ref 列篡改定位）。**missing 全格式**：矩阵驱动成员检查（Consistency.cpp:1131-1136）＋HTML 双形态 missing 用例。**数值 to_chars 文本规范形逐字符比对（D-09）**：`valueRepr` 为 RPT-T06 落位的 to_chars 文本形，比对为字符串 `!=`（:978）非浮点再解析——JSON 解析器数值路径仅用于计数量字段（:613-662 注释明示"不承载比对值"），亲读核实 |
| 4 | 纯函数纪律＋平凡通过＋parse-failed | **同输入同结论/不修改任何输入**：unit `ConsistencyPurityTest.SameInputSameResult_InputsUnmodified_RPT07_ACC4`（注入坏样本后两次 check 结果五字段逐项相等＋HTML/JSON 字节逐位不变）＋contract `ConsistencyPurityContractTest.SameInputSameResult_ThreeFormats_RPT07_ACC4`（三格式全路径同断言）亲跑通过；check 全程 const 只读（Consistency.cpp:1038-1145 亲读）。**artifacts<2 平凡通过＋注记**：unit `ZeroArtifacts_TrivialPassWithNote`/`SingleArtifact_TrivialPassWithNote`（consistent=true＋mismatches 空＋fieldCount=5＋notes 恰 1 条含 artifacts=0/1）亲跑通过。**回读失败→ConsistencyMismatch（dimension=parse-failed）错误轨**：HTML 结构截断（unit `HtmlTruncatedInsideEntryRow`）/JSON 截断（contract `JsonTruncated`）/CSV 无标识行（`CsvGarbageWithoutMarker`）/CSV 引号结构破坏（`CsvUnclosedQuoteStructure`）/CSV 工厂适配缺位（`CsvReaderFactoryMissing`，不静默当一致）五例均断言错误码 ConsistencyMismatch＋detail 含 parse-failed——亲跑通过 |
| 5 | P-RPT-1 处置 | **公共头零 io 类型**：Consistency.hpp include 面亲读＝仅 std＋reporting 自有公共头（Render.hpp/ReportModel.hpp）；contract `ConsistencyHeaderDisciplineTest.PublicHeaderZeroIoIncludes_RPT07_ACC5` 源码扫描（`sdurws/ird/io/` 与 `#include <Qt` 双否定断言）亲跑通过。**CSV 回读零字节解析、不私建第二套解析器**：src/Consistency.cpp 亲读——extractCsv（:846-933）仅做列名映射与多值列拆分，零 CSV 文法处理；`check()` 的 CSV 路径经注入 `m_csvReaders->makeCsvReader()`（:1108-1119）取 reader 回读；产品代码无任何 escape/unescape/引号化实现（NFR-SEC-03 转义唯一实现归 io）。**签名零改动预留**：IReportCsvReader/IReportCsvReaderFactory 与 io ICsvReader 投影同形（头注释 L206-213 登记裁决补边后直连口径） |

## 4.5 测试真实失败能力 —— 通过（两处实现变异即红、还原复绿）

在 worktree A（本验收沙箱，任务分支零触碰，还原后 `git status` 干净）对 src/Consistency.cpp 注入两处应失败变异，增量重建后亲跑：

1. **变异 1：禁用 qualifier 维度比对**（`if (extracted.qualifier != sourceTokens)` → `if (false && …)`）→ **3 例变红**：unit `DeleteQualifierSpan_QualifierLocated_RPT07_ACC3`、contract `JsonDeleteQualifier_QualifierLocated_RPT07_ACC1`＋`MismatchesListedExhaustively_NotShortCircuit_RPT07_ACC1`（期望 2 条实得 1 条）。§6.4 硬约束维度的红能力成立。
2. **变异 2：还原 1 后禁用 value 维度比对**（`if (extracted.value.value_or(std::string()) != cell.valueRepr)` → `if (false && …)`）→ **3 例变红**：contract `CsvChangeOneValue_ValueLocated_RPT07_ACC1`＋`MismatchesListedExhaustively…`＋`ConsistencyPurityContractTest.SameInputSameResult_ThreeFormats_RPT07_ACC4`。value 维度红能力成立。

两处变异还原后重建 exit 0、108＋38 **全绿复现**（无 F-068 时间戳残留——复绿以真实重编译验证）。测试非恒真、非"只编译通过"。

## 4.6 架构红线 —— 通过（AGENTS §5 八条逐条）

1. **L2 计算内核零 Qt**：reporting 全单元（include/src/test/contract_test）真实 Qt include **零命中**（grep 的 4 处命中均为红线测试内的否定断言字符串字面——test/BuildRedLineTest.cpp:135/141、contract_test 两处 `EXPECT_EQ(…find("#include <Qt"), npos)`）；CMakeLists diff 零链接变更（链接边仍为 RPT-T06 前登记的四条 PUBLIC）。
2. **业务域互链禁止（R-1）**：Consistency.hpp 仅 include reporting 自有公共头＋std；本任务零新增跨单元边；ird_gates 独立复跑 **reporting 目标零命中**（4.3）。
3. **跨单元私有头禁止（R-2）**：新文件 include 面干净（4.4#5）；src/RenderText.hpp 为单元内私有共享头（不出 include/）——登记于单元卡 §3.1/§14.4 v0.10。
4. **框架零源码修改（SA-02）**：diff 零框架路径、patches/ 零新文件（4.1）。
5. **不做 RobWork 名称前缀拼接/剥离（R-4）**：新增源码 grep 零命中；ird_gates R4 检查对本任务文件零命中（R4 唯一命中为 project 单元既有登记 F-145，与本任务无关）。
6. **权威唯一（PA-1）**：一致性判定归 reporting（§8.5 本单元语义）；未越权实现 io 转义（归 io）、未自建 CSV 解析器、未实现名称解析/修订写路径。
7. **不可变历史（PA-2）**：本任务零修订/写路径触碰（check 为只读纯函数）——不适用即合规。
8. **不继承/不恢复 old/**：diff 无 old/ 引用。

## 4.7 注释规范 —— 通过（公共头全查＋实现文件 4 件抽查）

- **公共头 Consistency.hpp（全查，342 行）**：文件头（L1-64）设计依据逐条锚定 §8.5/§9.4/§3.3＋需求 RPT-02/NFR-SEC-03/NFR-DEP-04＋契约 acceptance 1~5＋"检查器在导出链中的位置"背景说明＋维度覆盖表＋notes 偏差登记＋线程契约/上界；每类型注释含业务概念/生命周期（借用指针、调用方持有）/线程安全（L109-133/145-156/169-178/195-200/218-239/251-258/282-302/322-338）；函数注释 @brief/@param 方向/@return/@throws/复杂度齐备（如 readTables L222-236、check L286-301）。**§2.5 高危信息**：所有权（借用逐处标注）、线程约束（工厂无状态并发只读、reader 单线程会话 L217-218）、错误语义（Usage fail-fast vs ConsistencyMismatch 错误轨 vs mismatch 入列三分；detail 为开发诊断面经 diagnostics 脱敏 L290-296）、确定性来源（比对序＝矩阵序→工件输入序→维度序，NFR-COR-02，Consistency.cpp:30-31）——**必填项零缺失**（物理单位/坐标系/角度制式：本单元为文本/枚举比对面，无数值物理量计算，N/A 合理）。
- **实现文件抽查（4 件 ≥3）**：①src/Consistency.cpp 全读——文件头失败语义边界（parse-failed 结构层 vs mismatch 内容层）是"为什么"级注释；check() 六步注释（L1040-1144）逐步给出业务含义与失败去向；魔法数字均注明来源（如 `cellPos = cellEnd + 5; // "</td>" 长度`）；②src/RenderText.hpp 全读——文件头说明"为什么抽成本头"（双表失步→假不一致）与互逆性证明；③test/ConsistencyTest.cpp 与 contract_test/ConsistencyCrossUnitContractTest.cpp 全读——文件头含替身边界/fake 边界声明，**用例名带 RPT07_ACCx 追溯字段**（AGENTS §2.7 测试注释规范）。
- Render.cpp diff 注释、CMakeLists.txt 增补注释块（RPT-T07 增列逐件说明）在位。

## 4.8 文档同步 —— 通过（实现与任务卡零未登记偏差；findings 未关闭条目已核对）

- **单元卡 v0.9→v0.10**（f42c36e9）：§3.1 构建落位追加 Consistency.hpp 落位段（含 RenderText.hpp 私有共享头登记、"其余契约头随 RPT-T09+"推进）；§14.4 v0.10 行**逐项登记三类事项（DTB §5.4 口径）**：①ConsistencyResult 增补 notes 成员（§9.4 原文形状无注记位而 acceptance 4"平凡通过＋注记"须有承载——实现与原文的唯一形状偏差，已登记）；②比对维度覆盖冻结（status/ref 的格式面裁定——"携带却不查"与"结构上不输出"不同质论证在案）；③parse-failed/mismatch 失败语义边界裁定。三项均与实现逐字对得上（4.4/4.7 亲读核实）——**零未登记偏差**。
- **任务状态/追踪矩阵**：validate-docs PASS（12 trace entries）＋validate-task PASS（锚点校验过）；state.json 的 status/resultNote 翻转归收尾段（PIPE §4.7，与既有任务同口径；F-201 已登记 §4.10 条文与流水线实践的该脱节）。
- **findings.json 未关闭条目相关性核对（v1.2）**：现册 **open 207 条**，逐族核对与本任务的关系——
  - **F-230**（[RPT-Txx] vs [WP-12-Txx] 前缀双轨口径）：本任务沿用 `[RPT-T07]` 前缀（契约 branch=wp12-t07、卡行 ≙WP-12-T04），属已登记双轨口径的沿例，无新增发现；
  - **F-012/F-232~F-239 家族**（辅助提交标题名词短语开头）：**本任务再现**（见 4.9/G-1）——已按强制转登要求登记 **F-240**（编号自 F-239 顺延）；
  - **F-010/F-223/F-081 等幻影清单族**（.gitignore `*.log` 吞留痕）：本任务 `git add -f` 入库（提交正文明示该口径）且 **17/17 件全在库**（`git diff --stat` 逐件实证）——家族零复踩；
  - **F-041/F-073/F-236/F-238 计数失准族**：本任务散文计数（十七件留痕、新增 14＋13 条、单元 108＝94＋14、契约 38＝25＋13）经验收者 `ls | wc -l`、`grep -c "^TEST("`、XML 顶格属性与双模式亲跑**全部准确**——零复踩；
  - **F-007/F-098/F-099/F-200 环境链**（toolchain/Qt prefix/ini 辅助件）：验收复现期间再次亲历（见环境段），属既有登记的再证实，非本任务交付面问题；
  - **F-235/F-008**（全新树全量构建必败）：本验收据此采用 verify 目标级零错误口径（与送验留痕及 RPT-T05/T06 先例一致）——已在 4.2 说明；
  - **F-065/F-207/F-231**（门禁日志 GBK 乱码）：验收者捕获输出同征（4.3 注），既有登记继续有效；
  - 其余 open 条目（core/testkit/evidence/project/io/execution/policy/runtime/diagnostics/ui 各单元家族）与本任务交付面（reporting 一致性检查器）无交叉，无可随本任务顺手消账项。

## 4.9 commit 质量 —— 通过（四段式齐备、验证结论经独立复跑证实；标题风格 1 条建议级）

- **82a5ff10** `[RPT-T07] 实现一致性检查器（多格式逐字段一致）`——动词开头合规；四段齐备。
- **f42c36e9** `[RPT-T07] reporting 单元卡 v0.10：RPT-T07 实现落位登记（文档同步）`——四段齐备；标题前缀后以名词短语开头（F-012 家族，见 G-1）。
- **07bb4a78** `[RPT-T07] 验证留痕：双模式构建/测试执行/门禁/治理自查证据（wp12-t07）`——四段齐备；标题同族瑕疵（同 G-1）。
- **验证结论与留痕一致性（粉饰排查）**：提交正文声称的十七件留痕（ls 实证 17/17）、gtest 108/38（XML 顶格＋亲跑双证）、新增 14＋13（`grep -c "^TEST("` 实证）、gates 双树 diff 为空（token 归一比对实证）、`git add -f` 口径（明示非隐匿）——**逐项相符，零粉饰、零夸大**。

## 4.10 留痕完整 —— 通过

- gtest XML×4＋ird-test-report.json（schema `ird-test-report/1`）＋构建日志（integration-build.log/smoke-configure.log/smoke-build.log）＋测试运行日志×4＋门禁双树日志＋gates-hit-set-diff.txt＋validate 双日志——**17 件全部落位在库**（4.1/4.9 实证）。
- 在库四份 XML 顶部 tests/failures/errors 属性与验收者双模式亲跑结论逐字一致（108/38 × 2，零失败零错误）。
- resultNote/status 更新随收尾段（PIPE §4.7）——F-201 已登记该条文与实践的脱节，此处如实注明。

## 4.11 偷懒/缩水实现扫描 —— 通过

- **①TODO/FIXME/stub/placeholder/未实现/not implemented**：diff 范围新增行扫描**零命中**（无豁免条款需列出）。
- **②空实现检测**：公共接口 `FieldConsistencyChecker::check` 为完整实现（Consistency.cpp:1038-1145，六步全量逻辑）；构造/提取器/比对函数全有实现体；测试替身（NullCsvReader/FakeCsv* 族）为契约明文允许的注入形态且边界声明在案——非空实现偷懒。
- **③逐条证据表**：见 4.4——acceptance 五条每条均有具名测试＋亲跑结论，任一条无证据即 fail 的反例不存在。
- **④测试强度**：4.5 两处实现变异实测变红（qualifier 维度 3 例、value 维度 3 例）＋还原复绿；断言校验行为与边界（定位四元组逐字段、fieldCount、不短路计数、字节逐位不变、错误码＋detail 词）——非恒真。

## 发现的问题清单

| 级别 | 编号 | 内容 | 处置 |
| --- | --- | --- | --- |
| 建议 | G-1 | 两辅助提交 f42c36e9／07bb4a78 标题前缀后以名词短语开头（"reporting 单元卡 v0.10：…"/"验证留痕：…"），不合 ACC §4.9/AGENTS §6.3"动词开头"字面——F-012/F-232/F-233/F-234/F-237/F-239 家族再现（家族第 10/11 例，RPT 系连续六任务复踩）。正文四段齐备、验证结论经验收者独立复跑全部证实（无粉饰），纯风格瑕疵不构成阻断 | **已转登 findings.json F-240**（与家族合并处置；不追溯改写已推送历史；不阻塞合入） |
| 环境注记 | —— | 复现期间初配两连败（Boost/ini 模板）与冒烟 Qt prefix 需求均为 F-007/F-098/F-099/F-200 既有登记环境链的再证实；门禁捕获乱码为 F-065/F-231 家族——均非本任务交付面问题，零新增登记 | 已在 4.8 逐族核对留痕 |

## 验收者环境说明

- 复现与记录均在临时 worktree（acc-rpt-t07-repro／acc-rpt-t07-record）完成；验收者对任务分支 wp12-t07 **零提交、零推送、零文件修改**（4.5 变异在沙箱内注入并已还原，`git status` 复核干净）。
- 构建树：集成 `acc-rpt-t07-repro/build`（全新配置，`RWS_BUILD_INDUSTRIALROBOT:BOOL=ON` 缓存确认）；冒烟 `acc-rpt-t07-smoke`（独立目录＋vcpkg toolchain＋Qt prefix）。测试执行：两模式各 2 目标亲跑（本轮共 4 次全绿运行＋2 次变异红＋1 次复绿运行）。
- 记录落位：本文件随 F-240 转登一并提交于 `acc/RPT-T07/1` 分支并推送 `origin`（ACC §3 记录回传）。
