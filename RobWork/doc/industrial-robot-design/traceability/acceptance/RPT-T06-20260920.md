# RPT-T06 验收记录（acceptance record）

| 字段 | 值 |
| --- | --- |
| 任务 ID | RPT-T06（实现渲染器：FieldMatrix 提取＋HTML/JSON/CSV 三格式；unit=reporting；masterWp=WP-H 卡内行 ≙WP-12-T04；interUnit=true；实现任务——全量清单验收） |
| 送验对象（冻结） | 6dc1b140ead7b6e100772d4bbac4640a83efed2a（分支 wp12-t06；diff 范围＝d74f92194a2e409a95c0be76cd6c8124b0958ca1..6dc1b140ead7b6e100772d4bbac4640a83efed2a，state.base..headSha；state.base＝RPT-T05 合入收尾提交 d74f9219，与 `git merge-base origin/redesign-main 6dc1b140` 实测一致；range 内含 a11b6038／8ebebc41／6dc1b140 三个提交） |
| 验收尝试 | attempt 1（分支漂移检测：`git fetch` 后 `origin/wp12-t06` == 6dc1b140ead7b6e100772d4bbac4640a83efed2a 全 40 位逐字一致，零漂移；`acc/RPT-T06/1` 分支名此前不存在） |
| 验收环境 | 复现 worktree A＝`git worktree add --detach D:/tmp-acc-rpt-t06-a` @ 6dc1b140，全新从零（集成 build 树、冒烟 build 树均为本验收新建独立目录，无缓存复用）；基线门禁 worktree C＝detached @ d74f9219（ird_gates 基线复跑用）；记录回传 worktree B＝`acc/RPT-T06/1` 基于 origin/redesign-main@d74f9219。pwsh 7＋MSVC 2022 x64＋vcpkg 经典模式（toolchain 绝对路径指向主仓库 vcpkg——worktree 天然不含）。集成配置参数＝主树缓存实证口径（F-098/F-099 既有登记，非实施缺陷）：`-G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=<主仓>vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64 -DCMAKE_CXX_FLAGS="-DNOMINMAX -DWIN32_LEAN_AND_MEAN -DWIN32"`，并自主树复制 `RobWorkStudio.ini.template.static`＋`RobWorkStudio.ini.shared.in` 两件 git 未追踪本地辅助件（bin/ 被 .gitignore `[Bb]in/` 吞——F-099 实证同款）。集成配置后缓存确认 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`。冒烟配置＝`-S <A>/RobWork/RobWorkStudio/src/rwslibs/industrialrobot -B D:/tmp-acc-rpt-t06-smoke`＋同 toolchain＋同 Qt prefix。验收全程只消费产物、契约、设计文档与在库留痕，未读取实施者过程叙述；结论全部出自验收者亲手重跑（本记录引用的送验留痕仅作交叉比对对象，不替代亲手复现） |
| verdict | **pass**（4.1～4.11 全部通过；无阻断级问题；建议级 2 条转登 findings.json F-238/F-239——均沿既有家族判例，不阻塞合入） |
| 验收日期 | 2026-09-20 |

## 4.1 改动范围 —— 通过

- `git diff --name-status d74f9219..6dc1b140` 实证 **23 个变更文件**（4 A＋1 M＝reporting 交付面 5 件、17 A＝traceability/builds/wp12-t06/ 留痕、1 M＝units/reporting.md）：①`reporting/CMakeLists.txt`（M）②`reporting/contract_test/RenderCrossUnitContractTest.cpp`（A）③`reporting/include/sdurws/ird/reporting/Render.hpp`（A）④`reporting/src/Render.cpp`（A）⑤`reporting/test/RenderTest.cpp`（A）⑥~㉒`traceability/builds/wp12-t06/` 17 件留痕 ㉓`units/reporting.md`（M，v0.8→v0.9）。
- **allowedFiles 逐文件比对**：①~⑤命中 `RobWork/RobWorkStudio/src/rwslibs/industrialrobot/reporting/**`；⑥~㉒命中 `traceability/**`；㉓命中 `units/reporting.md`——**23/23 全部在册，零越界**（边界 grep：不在 reporting/**、traceability/**、units/reporting.md 三者之内的变更文件＝零输出）。
- forbiddenFiles（REQUIREMENTS.md／ARCHITECTURE.md）**零触碰**（diff 无该两文件）。
- 无未登记框架源码改动（SA-02）：diff 零框架源码路径（仅 industrialrobot/reporting 与 doc 链），`patches/` 零新文件（grep 零输出）。
- **逐提交核验**（三提交各零越界）：a11b6038＝①~⑤（实现本体）、8ebebc41＝㉓（单元卡同步）、6dc1b140＝⑥~㉒（验证留痕）。

## 4.2 可复现构建 —— 通过（两模式全新树从零，零错误）

- **集成模式**（worktree A 内新建 `build/`，重配后缓存确认 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`）：`cmake --build build --config Release` **全量构建 exit 0、": error"/"fatal error" 计数 0**（RobWork 全库→RobWorkSim→RobWorkStudio→industrialrobot 全单元全新编译；产物实测 `build/RobWorkStudio/bin/Release/sdurws_ird_reporting{,_test,_contract_test}.exe` 在位）。
- **独立冒烟模式**（独立目录 `D:/tmp-acc-rpt-t06-smoke`，源＝worktree A 的 industrialrobot，带 vcpkg toolchain＋Qt prefix——AGENTS §4.1 口径）：配置＋全量构建 **exit 0、错误计数 0**；`reporting/Release/` 下 `sdurws_ird_reporting_test.exe`＋`sdurws_ird_reporting_contract_test.exe` 在位并亲跑。
- **测试亲跑（验收者亲手执行，非采信送验日志）**：集成模式 94/94＋25/25 全绿（exit 0）；冒烟模式 94/94＋25/25 全绿（exit 0）——与送验四份 gtest XML 顶格属性（tests="94" failures="0" errors="0"、tests="25" failures="0" errors="0"）逐项一致。
- 本任务 diff 零框架触碰（4.1 实证），框架既有缺陷（F-008 系）不归本任务。

## 4.3 契约 verify 命令 —— 通过（两条逐条亲手执行）

在 worktree A 根目录按契约字面逐条独立执行，**两条全部 exit 0**：

1. `cmake --build build --config Release --target sdurws_ird_reporting_test` → exit 0
2. `cmake --build build --config Release --target sdurws_ird_reporting_contract_test` → exit 0

附加治理脚本（worktree A 上下文亲手复跑）：`validate-task.ps1 -TaskFile doc/industrial-robot-design/tasks/foundation/RPT-T06.json` → **PASS (1 tasks)**；`validate-docs.ps1` → **PASS (20 units, 12 trace entries, 150 task files)**——与在库 `builds/wp12-t06/validate-task.log`/`validate-docs.log` 逐字一致；`verify-task.ps1 -TaskFile … -DryRun` → DRYRUN PASS（解析出的两条 verify 命令与契约逐字一致）。

## 4.4 acceptance 条目 —— 通过（六条逐项证据）

| # | 契约条目 | 证据（验收者亲手复现＋在库留痕交叉比对） |
| --- | --- | --- |
| 1 | RP-CONS-3 确定性：同报告同格式二次渲染字节相同（三格式各自——稳定排序/数值 to_chars 最短往返/行尾 HTML/JSON LF）；二次渲染字节比对为 Verifying 步组成部分（§8.5 步③） | **亲跑三条确定性用例全绿**：`HtmlRenderTest.ByteDeterministic_SecondRenderIdentical_RPT06_ACC1`（二次渲染 bytes 逐字节相等＋digest 相等）、`JsonRenderContractTest.SchemaVersion_FourStateTokens_CanonicalBytes_RPT06_ACC5`（二次渲染字节相同）、`CsvRenderContractTest.ByteDeterministic_SecondRenderIdentical_RPT06_ACC1`。**实现面亲读**：`Render.cpp:511` 条目 stable_sort 按 entryKey、`:528` 诊断 sort 按〔章节 order→码→局部名→来源运行→次数〕组合键、`:556` 证据按 itemId、`:573` 结果按 runId 规范文本、`:318-330` toCharsShortest＝to_chars 最短往返（locale 无关）、`:612` HTML 输出断言零 `\r`（LF）。§8.5 步③字节比对归导出链 RPT-T09——渲染器侧义务（同输入同字节纯函数性）由上列用例承载，契约 note 明文两任务共享验证面互引，无缺口 |
| 2 | RP-CONS-4 Unicode/单位/复杂结构引用：UTF-8 无 BOM；单位经 UnitPreference 冻结集、换算经 core 唯一入口（KIN-12 纯投影）；曲线类以引用呈现不复制 | **亲跑**：`HtmlRenderTest.Html5StaticContract…`（首三字节非 EF BB BF——无 BOM 断言）、`FieldMatrixTest.UnitPreferenceConflict_Rejected_DataInvalid_RPT06_ACC2`（字段单位与冻结集同量纲冲突→DataInvalid）、`FieldMatrixTest.ValueProjection…`（1.5 m SI→mm 冻结集投影＝1500，经 core tryConvert）、`FieldMatrixTest.CurveRefSection_ValueIsReferenceText_NotCopy_RPT06_ACC2`（引用文本 "run-<hex>#payload.curves[0].torque"）。**实现面亲读**：`Render.cpp:663` 换算唯一走 `core::tryConvert`（全文件零第二换算实现——grep 实证）、`:654` 冻结集冲突即 DataInvalid、`:659` SI 基线缺位即 DataInvalid 不静默（P-RPT-9 同步面） |
| 3 | RP-SAN-1 诊断脱敏双保险：渲染时再次过脱敏（exportSafeSummary 双保险）；用户级报告不含调用栈/内存地址/未脱敏本机路径；身份规范文本仅追溯区块/附录（UX-02） | **亲跑（真实 RedactionService 凭证，非替身）**：`HtmlRenderTest.SanitizationDoubleInsurance_NoRawPathNoAddress_RPT06_ACC3`（夹具诊断定位名含二级目录“机密”＋0x 地址——渲染输出零命中、RootOnly 一级目录与文件名按策略保留）、`CsvRenderContractTest.DiagnosticsAppendix_SanitizedLocalName_RPT06_ACC3`（CSV 诊断附表 local_name 同口径）、`HtmlRenderTest.IdentityCanonicalText_ConfinedToTraceBlocks_RPT06_ACC3`（完整规范身份仅在追溯区，正文缩略形＋锚值恒为 fieldKey）。**实现面亲读**：`Render.cpp:591` redactText＝IRedactionService User 档、HTML/JSON/CSV 全部人读自由文本经 appendRedacted/redactText（grep 计 15 处消费点），值文本不过脱敏（Invalid 保留原文 NFR-COR-03——注释明示）。**4.5 变异注入反证**：脱敏旁路注入后上两用例即红（见 4.5）——脱敏断言真实有效 |
| 4 | FieldMatrix 单次投影：三渲染器以同一 FieldMatrix 为值来源；矩阵确定性单测锁定 | **亲跑毒化矩阵三格式验证**：`RenderValueSingleSourceTest.PoisonedMatrix_ThreeFormats_EmitCellValue_RPT06_ACC4`——矩阵值改写为毒化标记后，HTML（`data-field="…">POISON…`）、JSON、CSV 输出均携带毒化值且零 "1500"（未被重算）＝D-07/AT-22 结构性前提实证；`FieldMatrixTest.SameReportSameMatrix…`（两次提取逐单元格全等＋遍历序三段锁定：章节 order→条目 entryKey→字段存储序，7 单元格逐位核对）；`HtmlRenderTest.MatrixReportMismatch_Rejected_NoPartialArtifact`（缺/多单元格双向 DataInvalid 且失败无部分产物外泄）。**实现面亲读**：MatrixIndex（`Render.cpp:453`）双向完整性 consume/assertFullyConsumed——HTML/JSON 每字段按键消费、CSV 逐行消费＋全消费断言，值不可能出自矩阵以外 |
| 5 | 三格式格式契约：HTML5 静态零外部依赖零脚本（样式内联、data-field/data-qualifier 锚——D-08）；JSON ird-report-json/1 canonical（顶层 schemaVersion、四态显式 token、未知字段拒绝——NFR-DEP-04 执行侧）；CSV 列序冻结主表 11 列＋诊断/覆盖附表（#rwcsv1、空值与“不适用”区分） | **亲跑**：`HtmlRenderTest.Html5StaticContract…`（零 `<script`/`http(s)://`/`<link`/`src=`/`@import`、样式内联、DOCTYPE html5、meta charset、LF 无 BOM）、`HtmlRenderTest.DataFieldAnchors…`（逐矩阵单元格 data-field 锚在场＋data-qualifier 三 token）、`JsonRenderContractTest.SchemaVersion…`（顶层 "schemaVersion": "ird-report-json/1"＋四态 token＋限定语数组字段）、`CsvRenderContractTest.MainTableElevenColumns…`（主表 11 列冻结序逐列断言＋诊断/覆盖附表列序＋方言行恰 3 次＋“未提供”/空字段区分）。**实现面亲读**：`Render.cpp:2114` firstUnknownJsonTopLevelKey＝kJsonTopLevelKeys 17 键冻结集（头文件 17 项逐一清点核实）装配自检、`:2190-2218` 三表列序冻结常量、`:2248` 三文档空行分隔拼接（单工件形状——单元卡 §14.4 v0.9 登记） |
| 6 | 陷阱处置：P-RPT-1 公共头零 io 类型；P-RPT-6 不留 PDF 桩；P-RPT-9 core v0.1 Draft 基线 | **P-RPT-1 亲跑**：`RenderHeaderDisciplineTest.PublicHeaderZeroIoIncludes_RPT06_ACC6`（源码扫描 Render.hpp 零 `sdurws/ird/io/` include＋零 Qt include）＋`JsonRenderContractTest.WriterViaFactory_SessionSequence`（工厂记账：恰 1 次 makeJsonWriter、零 CSV、零落盘）。**亲读**：Render.hpp 接口只出现 reporting/core 类型（IReportIoFactory/CsvWriter/JsonWriter/OutputTarget/ReplacePolicy/ReportCsvCell/ReportJsonDom——§9.5 落位偏差暂载 Render.hpp 已按 DTB §5.4 登记单元卡 §14.4 v0.9，v0.6 同型先例；链接面仍四条登记边，零 io 编译边——CMakeLists diff 实证）；**P-RPT-6 亲读＋亲跑**：ReportRenderFormat 三值无 PDF 词位、RenderOptions 零 PDF 专属字段（Render.hpp:94-106/151-162）、`HtmlRenderTest.WrongFormatRequest_UsageFailFast`（他格式请求＝Usage fail-fast——格式无关抽象、新格式＝新实现）；**P-RPT-9 亲读**：core UnitToken/tryConvert/Digest256/ContentDigester 公共面消费、SI 基线缺位 DataInvalid 不静默、`static_assert(RenderArtifact.digest == core::Digest256)` 类型恒等常驻自证（契约测试 ：611） |

**ird_gates 独立复现**（对 acceptance 6 及红线 4.6 的机器面证据）：head 侧（worktree A `cmake -DIRD_ROOT=<A>/industrialrobot -DIRD_ENABLE_GIT=OFF -P cmake/ird_gates.cmake`）命中 **61 项**；base 侧（worktree C @ d74f9219 同命令）命中 **61 项**；归一化 IRDRoot 后逐行 diff 为空＝**GATES-HIT-SET-IDENTICAL**，且 grep "reporting" 零命中（零新增零移除）——与送验 `gates-hit-set-diff.txt` 主张一致，且与在库 `gates-ird-gates-base.log`/`gates-ird-gates-branch.log` 归一化后逐行一致（61=61）。

## 4.5 测试真实失败能力 —— 通过（两处变异注入即红、还原复绿）

在 worktree A（本验收沙箱，任务分支零触碰）对 `src/Render.cpp` 注入两处应失败变异，重建后亲跑：

1. **值投影/确定性断言**：`toCharsShortest` 顶部注入 `return "MUTATED-ACC-PROBE";`（破坏数值文本规范形）→ `sdurws_ird_reporting_test` **FAILED 3 例**（`FieldMatrixTest.ValueProjection_FourStateWords_ToChars_AndUnitConversion_RPT06_ACC2`、`HtmlRenderTest.DataFieldAnchors…`、`HtmlRenderTest.IdentityCanonicalText…`）、`sdurws_ird_reporting_contract_test` **FAILED 1 例**（`CsvRenderContractTest.MainTableElevenColumns…`）——恰为值投影依赖断言。
2. **脱敏双保险断言**：`redactText` 注入 `return std::string(raw);`（脱敏旁路）→ unit **FAILED**：`HtmlRenderTest.SanitizationDoubleInsurance_NoRawPathNoAddress_RPT06_ACC3`；contract **FAILED**：`CsvRenderContractTest.DiagnosticsAppendix_SanitizedLocalName_RPT06_ACC3`——HTML/CSV 两面脱敏断言均真实敏感。

还原两处注入后 `git status --porcelain` 干净（工作树回到 6dc1b140 内容）、重建 exit 0、94/94＋25/25 复绿。**测试非恒真**：断言对输入真实敏感。

## 4.6 架构红线 —— 通过（AGENTS §5 八条逐条）

1. **L2 计算内核零 Qt**：diff 零触碰 core/evidence/policy/runtime 及业务域源码；reporting 新增文件 grep Qt 零命中（唯一命中为契约测试断言零 Qt 的自证文本）；reporting 四条链接边全部为 ARCH §3.5 登记边（core/evidence/diagnostics/project——ird_gates_whitelist.cmake:38-41），零 Qt 链接。
2. **业务域互链禁止（R-1）**：reporting 不在业务域单元清单内且零业务域单元链接 reporting；gates 命中集 61 项中零 reporting（4.4 实证）。
3. **跨单元私有头禁止（R-2）**：新增文件全部 include `sdurws/ird/<core|diagnostics|evidence|reporting>/…` 公共头路径，逐一核实目标文件在各单元 `include/sdurws/ird/<unit>/` 下存在（11/11 PUBLIC OK）；src/ReportCodec.hpp 式私有实现头零新增。
4. **框架零源码修改（SA-02）**：diff 零框架路径、patches/ 零新文件（4.1）。
5. **不做名称前缀拼接/剥离（R-4）**：gates R4 扫描命中仅既有 project 一处（基线既有归属），零 reporting 命中；Render.hpp/.cpp 无 RobWork 字面量（4.11① 扫描同源证据）。
6. **权威唯一（PA-1）**：渲染器零写路径、零名称解析、零第二单位换算实现（KIN-12 投影走 core tryConvert）、零诊断码创建（只读消费 DiagCodes 词表）、模板版本随渲染器不可调用方选（§9.3 原文）；io canonical 行为归 io 适配（SA-12 不建第二编码器——HTML 转义为模板渲染职责，注释明示 SA-12 约束面是 CSV/JSON）。
7. **不可变历史（PA-2）**：三提交线性追加（a11b6038→8ebebc41→6dc1b140）、无改写；origin/wp12-t06 == headSha 亦证无强推。
8. **不继承 old/ 历史实现**：diff 零 old/ 引用；实现全部按单元卡 §8/§9.3 原文新写。

## 4.7 注释规范 —— 通过（公共头全查＋实现与测试抽查）

- **公共头**：`Render.hpp`（736 行）全文通读——文件头（设计依据三源逐条＋背景说明“两条铁律”＋io 注入契约族落位说明＋P-RPT-6 处置＋线程契约＋确定性来源五要素）；每个结构/枚举/类/函数均有 Doxygen 中文描述（FieldCell 逐成员说明含 fieldKey 三段唯一性与 AT-22 join key 语义、RenderOutcome 成功/失败两形态语义、IReportRenderer 契约逐条〔前置/后置/错误/取消/线程/副作用/合法与非法调用〕、io 族每个方法的会话语义与生命周期约束）；§2.5 高危信息：单位（valueRepr 显示单位语义）、所有权（注入引用 borrow“调用方持有”三处明示）、线程约束（单线程写入序/并发只读安全逐类注明）、错误语义（Usage/DataInvalid/RenderFailed/TemplateVersion 四码归类与触发条件）、确定性来源（稳定排序/to_chars/无 locale 依赖）齐备。
- **实现文件**：`Render.cpp`（2401 行）全文通读——文件头与公共头一致且只记实现口径；每个语义段（词表映射表、确定性工具、前置校验、MatrixIndex、排序原语、脱敏、投影、限定语推导、三渲染器主体）有中文段落注释说明“为什么”；复杂逻辑（矩阵同源双向验证、单位一致性闸、CSV 三文档拼接）逐步讲解；魔法数字有来源（0xFFFF 章节序兜底、64 字节 to_chars 缓冲上限、digest 前 12 hex——§8.3 原文）。
- **测试文件 2 件全读**（RenderTest.cpp 832 行＋RenderCrossUnitContractTest.cpp 640 行）：文件头设计依据/替身边界声明/确定性夹具纪律三条齐备；每用例注释标注验证的 acceptance 条目与 § 号（RPT06_ACC1~6 后缀）；fixture 函数 Doxygen 中文齐备；FakeCsvWriter/FakeJsonWriter 边界声明在案（最小确定性替身——P-RPT-1 裁决补边后直连签名零改动）。
- **CMakeLists.txt**：本任务仅增列三个源文件（diff 9 行），既有中文注释块（目标注册/依赖红线守卫/门禁交互）未触碰、原样在位。

## 4.8 文档同步 —— 通过

- **实现与任务卡偏差**：零未登记偏差。四类落位事项（§9.5 io 注入契约族暂载 Render.hpp＋IReportOutputTarget::write 字节通道形状补全／FieldCell.entryKey 呈现承载增列／覆盖附表列序冻结零数据行〔不伪造逐工况状态——ERR-01〕／限定语两词推导面〔external-validation-incomplete、downgraded-reference-value 关联键未入 §4 模型〕）全部在单元卡 §14.4 v0.9 行按 DTB §5.4 口径登记（亲读核实）；§3.1 构建落位行追加 Render.hpp 落位段与“其余契约头随 RPT-T07+”推进在位；单元卡 §11 RPT-T06 行产物列（`Render.hpp/.cpp`（FieldMatrix 提取＋HTML/JSON/CSV 渲染；注入 io 工厂））与实际交付逐项对应。RP-STATE-1~3 具名用例归 RPT-T08＝契约 note 明文（渲染侧承载位＝§8.2① 前置断言门已在本任务落地），非缺口。
- **任务状态/追踪同步**：契约状态翻转归收尾（PIPE §4.7，本验收不代行）；单元卡 v0.9 与 validate-docs/validate-task 双 PASS（4.3 亲跑一致）在位。
- **findings.json 未关闭条目相关性核对**（基线 205 条 open 逐族过筛，与 reporting/留痕相关者）：
  - **F-236**（RPT-T05 散文计数家族，owner＝reporting 实施者“后续 RPT-T06+ 提交与报告的散文计数自清点核对”）：对本任务全部散文计数逐项实测清点——RenderTest 16 条（grep ^TEST 实测 16）、契约测试 7 条（实测 7）、单元 94＝既有 78＋16、契约 25＝既有 18＋7、“新增 23 用例”“既有 96 用例”均与 XML 顶格与验收者双模式亲跑一致。**本体计数零复踩**；唯一残留＝留痕提交 6dc1b140 汇总短语“九件留痕”与实际入库 17 件不符（见发现 G-1→F-238）。家族维持 open，无消账义务。
  - **F-012/F-232/F-233/F-234/F-237（标题名词短语家族）**：本任务再现 2 例（8ebebc41/6dc1b140，见 G-2→F-239）——家族延续，登记不消账。
  - **F-010/F-081/F-083/F-085/F-087/F-088/F-090/F-102/F-103/F-106/F-108/F-111/F-113/F-114/F-118/F-125/F-127/F-129/F-132/F-136（.log 幻影清单/未入库家族）**：本任务 **零复踩**——17 件留痕（含 11 份 .log）全部实际入库（git ls-tree 实证），ird-test-report.json 的全部文件引用逐一解析在库（含跨目录引用 tasks/foundation/RPT-T06.json、wp12-t05/ird-test-report.json 均存在）——`git add -f` 先例（wp09-t03/t04）正确沿用。家族 open 维持（根因 .gitignore:87 全局规则未修，与本任务无消账关系）。
  - **F-098/F-099（全新树配置口径/ini 模板缺件）**：本验收再次亲历（补两件辅助件＋toolchain/Qt/CXX_FLAGS 后从零配置成功），与本任务零代码关联、AGENTS.md 不在 allowedFiles——维持 open 正确（本记录环境段已再留实证）。
  - 其余 open 条目（RT/PRJ/POL/EX/UI 等单元内部事项、CI 扩展、dataset 等）与本任务交付面（reporting 渲染器）无关联，无需本任务处置。

## 4.9 commit 质量 —— 通过（三提交逐份核对；两处建议级见 G-1/G-2）

| 提交 | 标题 | 四段正文 | 验证结论与留痕一致性 |
| --- | --- | --- | --- |
| a11b6038 | `[RPT-T06] 实现报告渲染器（FieldMatrix 单次投影＋HTML/JSON/CSV 三格式）`（动词“实现”开头——合规） | What/Why/验证/影响面＋Acc-Covered 1~6 齐备 | 实现面描述与本验收亲读逐项一致（矩阵序/四态词/资格门/17 键冻结集/脱敏范围） |
| 8ebebc41 | `[RPT-T06] reporting 单元卡 v0.9：RPT-T06 实现落位登记（文档同步）`（名词短语开头——G-2，建议级） | 四段齐备 | validate 双 PASS 亲跑一致；四类偏差登记与 §14.4 v0.9 原文逐项一致 |
| 6dc1b140 | `[RPT-T06] 验证留痕：双模式构建/测试执行/门禁/治理自查证据（wp12-t06）`（名词短语开头——G-2，建议级） | 四段齐备 | “94/25 两模式零失败零错误”与 XML 顶格及验收者亲跑一致；“61 项既有登记命中零 reporting 新增”与验收者双树复跑一致 |
| 6dc1b140 汇总计数 | —— | “九件留痕”与实际入库 17 件不符（8 个要点组） | **G-1（建议级→F-238）**：要点组内件数（四份 XML＋四份运行日志＋双 gates 日志＋双 validate 日志）已如实列明、全部实际在库（ls-tree 17/17），方向为少报，零幻影零缺失，无粉饰成分 |

无粉饰：门禁 61 项命中如实登记为既有归属并附双树原始留痕；测试计数与 XML 顶格逐项一致。

## 4.10 留痕完整 —— 通过

- 在库留痕（送验提交 6dc1b140 内 `traceability/builds/wp12-t06/` 17 件，逐件在库亲验）：`git ls-tree -r` 实证 17 件＝ird-test-report.json＋四份 gtest XML（integration/smoke × unit/contract_test，顶格 tests=94/25、failures=0、errors=0，亲读）＋11 份 .log（integration-build、smoke-configure、smoke-build、integration-test-run-unit/contract、smoke-test-run-unit/contract、gates-ird-gates-base/branch、validate-task、validate-docs——**全部实际入库，幻影家族零复踩**）＋gates-hit-set-diff.txt。
- ird-test-report.json（ird-test-report/1 schema）亲读：目标级结论（build-pass 两模式＋run-pass 94/94、25/25）、门禁段（GATES-HIT-SET-IDENTICAL、61 项既有）、治理段（validate 双 PASS）、acceptanceCoverage 1~6 逐条证据指针——与验收者亲手复现的全部数值一致。
- **验收者自跑留痕**（本记录证据，存验收临时目录，随 worktree 生命周期说明于环境段）：集成/冒烟两模式双测试目标 gtest 运行日志四份、ird_gates 基线/变更后原始日志两份、两轮变异注入/还原构建与运行日志、治理脚本输出——关键结论已逐条摘录于本记录各节。
- `phase-one-validation.log`/resultNote：本任务留痕载体＝builds/wp12-t06/ 目录（RPT-T01~T05 及近期任务同款先例），未更新 phase-one-validation.log 不构成缺口。

## 4.11 偷懒/缩水实现扫描 —— 通过

1. **标记扫描**：diff 范围四个源文件＋CMakeLists 全文 `TODO|FIXME|stub|placeholder|未实现|not implemented`（含中文“未实现”）**零命中**（grep 计数 0）。契约显式“触发式延后”豁免逐条列出依据：①覆盖附表零数据行＝单元卡 §14.4 v0.9 明文登记（§4 模型 CaseCoverageSummary 为聚合、逐工况矩阵未入模型——ERR-01 不伪造，数据面随模型扩展）；②限定语两词（external-validation-incomplete/downgraded-reference-value）不推导＝同登记行（关联键未入 §4 模型——不虚构关联）；③RP-STATE-1~3 具名用例体归 RPT-T08＝契约 note 原文（§8.2① 前置断言门为其渲染侧承载位，本任务已落地并用例钉住）。三处豁免均有契约/单元卡原文依据，非偷懒。
2. **空实现检测**：公共接口函数（extractFieldMatrix/token/三渲染器 render/io 族工厂）实现全部在 Render.cpp 且为实质实现（矩阵提取 O(R·S·E·F) 单次遍历、HTML 文档结构装配、JSON DOM 装配＋17 键自检、CSV 三文档逐行落表）；RenderOptions 契约载体形与 MemoryReportOutputTarget commit 恒成功均有类注契约依据（§9.3/§9.5 原文）——非未声明空实现。
3. **acceptance 逐条证据表**：见 4.4 表格——六条每条给出具名测试/亲跑命令输出/文件行号，无一条仅凭“已实现”声明。
4. **测试强度**：23 用例（16＋7）均为行为断言——字节级确定性比对（3 格式二次渲染逐字节相等）、毒化矩阵值单源（三格式零重算＋“1500”零命中反向断言）、真实 RedactionService 滤除断言（正/负双向：0x 地址零命中＋RootOnly 一级目录在场）、错误路径断言（DataInvalid/Usage/TemplateVersion 分类）、工厂记账面（调用次数断言）；非恒真、非“只编译通过”。**4.5 变异注入两例即红**（3＋1 例、1＋1 例，恰命中被变异语义的断言）实证敏感度；还原复绿实证测试与实现同步。

## 发现的问题（分级清单）

- **阻断级：无。**
- **建议级 G-1（已转登 findings.json F-238）**：留痕提交 6dc1b140 正文汇总短语“新增 traceability/builds/wp12-t06/ 九件留痕”与实际入库 **17 件**不符（8 个要点组；“九件”在件数/组数任一口径下均不成立）。F-041/F-073/F-236 散文计数家族再现（本任务其余散文计数——16/7/94/25/23/96——实测全部准确，仅此一处汇总短语失准）。方向为少报：要点组已完整枚举全部 17 件且全部实际在库（ls-tree 17/17 实证），零幻影零缺失，无粉饰。处置建议：随 F-236 家族合并处置（报告/提交散文计数以 ls-tree/XML 实测为准）；不追溯改写已推送提交；不阻塞合入。
- **建议级 G-2（已转登 findings.json F-239）**：8ebebc41“[RPT-T06] reporting 单元卡 v0.9：…”与 6dc1b140“[RPT-T06] 验证留痕：…”两提交标题以名词短语开头（实现提交 a11b6038 合规）——F-012/F-232/F-233/F-234/F-237 家族再现（RPT 系第 8/9 例）。两提交正文四段齐备、验证结论与验收者亲测一致，纯风格瑕疵不构成阻断。处置建议：沿 F-237 合并处置路径（实施者模板补动词开头示例与负例）；不追溯改写已推送历史；不阻塞合入。

## verdict

**pass** —— 4.1～4.11 全部通过，无阻断级问题；建议级 2 条（G-1→F-238、G-2→F-239）随本记录转登 findings.json，均沿既有家族判例判为不阻塞。合入决策归所有者（本验收者不合入、不改实施产物、未向 wp12-t06 推送任何提交）。
