# RT-T09 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 ID | RT-T09（快照与工厂 Snapshot——WP-06-T09，unit=runtime） |
| 契约 | tasks/foundation/RT-T09.json（status=ready，branch=wp06-t09，verify＝`cmake --build build --config Release --target sdurws_ird_runtime_test`） |
| 送验对象（冻结） | `13072b12d67aa972e94d58a207478e484515e583`（分支 wp06-t09 尖端；base=9bd4579b94a101dc104bd536cb7e9acc42d8a543＝origin/redesign-main 尖端） |
| 分支漂移检测 | `git ls-remote origin wp06-t09`＝13072b12…（与送验 SHA 一致——ACC §3 冻结口径满足） |
| **verdict** | **pass**（4.1～4.11 全部通过，无阻断级问题；建议级 1 条已转登 findings.json F-046） |
| 验收方式 | 全新上下文，独立 worktree（detached @ 送验 SHA）亲手重配重跑双模式构建与契约 verify 命令；静态审查＝逐行通读全部新增/修改源码文件；动态探针＝2 处缺陷注入→精确还原→全量复绿。不消费实施叙述。 |
| 验收环境 | Windows 10 x64 / Git Bash / VS2022（Visual Studio 17 2022 生成器 x64）/ Qt 6.11.1（CMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64）/ vcpkg 经典模式（仓库根，toolchain 绝对路径）/ pwsh 7（治理脚本）。复现现场＝`D:\10_Source_Repos\21_robot\.acc-tmp\rt-t09-repro`（worktree，验收后可删）。 |
| 环境前置说明（复现者须知） | 全新集成树配置需主仓库同款参数：`-DCMAKE_TOOLCHAIN_FILE=<root>/vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`；且 `RobWork/RobWorkStudio/bin/` 下两个被 .gitignore 忽略的本地模板（`RobWorkStudio.ini.template.static`、`RobWorkStudio.ini.shared.in`）须手工复制，否则 RobWorkStudio 配置失败——已登记发现 **F-007/F-009**（open，构建约定所有者辖域）的再次实证，非本任务引入、不阻塞。 |

---

## 4.1 改动范围 —— 通过

- base..head 仅 1 个提交：`13072b12 [WP-06-T09] 实现快照与工厂（RuntimeSnapshot/IRuntimeModelView/工厂/materialize）`；`git diff --name-status 9bd4579b..13072b12` 共 20 个文件：
  - `runtime/**`（allowedFiles 第 1 条）：CMakeLists.txt（M）、Adapter.hpp（M）、BaseWorldTransform.hpp（M）、Resource.hpp（M）、Compiler.hpp（A）、Snapshot.hpp（A）、src/Snapshot.cpp（A）、test/SnapshotTest.cpp（A）——8/8；
  - `doc/industrial-robot-design/traceability/builds/wp06-t09/**`（allowedFiles 第 4 条）：留痕 11 份（A）；
  - `doc/industrial-robot-design/units/runtime.md`（allowedFiles 第 3 条，M）。
- forbiddenFiles（REQUIREMENTS.md / ARCHITECTURE.md）：diff 中零出现。
- SA-02：diff 无任何框架源码文件；`industrialrobot/patches/` 无新文件。
- 对三个既有头（Adapter/BaseWorldTransform/Resource）的修改均为纯增量实体（RobWorkBaselineVersion / kBaseWorldRuleVersion / ResourceBytes+ResourceReadError+IRuntimeResourceProvider），不改既有声明，落位依据＝units/runtime.md §15.4 v0.10① 登记（4.8 项核对）。

## 4.2 可复现构建 —— 通过

- **集成模式**（worktree A 全新配置）：`cmake -S <A>/RobWork -B <A>/build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=<vcpkg> -DCMAKE_PREFIX_PATH=<Qt6>` → `Configuring done (44.8s)`；缓存确认 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`（验收日志 acc-integration-configure3.log）。构建 `--config Release --target sdurws_ird_runtime_test` exit=0，`error C[0-9]/LNK/MSB/fatal` 计数＝0，Snapshot.cpp/SnapshotTest.cpp 编译零警告（日志 acc-integration-build.log）。
- **冒烟模式**（独立目录 rt-t09-smoke，带 vcpkg toolchain 绝对路径重配置）：`cmake --build ... --config Release` exit=0 零错误（acc-smoke-build.log）；测试执行 **172/172 PASSED**（acc-smoke-test-run.log，与送验留痕 smoke XML 一致）。
- 结论：两模式均零错误；与提交声明"集成零错误零警告、冒烟 172/172 零回归"一致。

## 4.3 契约 verify 命令 —— 通过

- `cmake --build build --config Release --target sdurws_ird_runtime_test`：验收者亲手执行成功（即 4.2 集成构建，exit=0）。
- 治理脚本：`validate-task.ps1 -TaskFile RT-T09.json` → **PASS (1 tasks)**；`verify-task.ps1`（先 `-DryRun` 核对解析与环境：RepoRoot 归约正确、dryrun 命令与契约 verify 一致）→ 正式运行 **PASS RT-T09**（脚本内部执行同一构建命令）。
- 构建产物运行：**213/213 PASSED**（＝既有 199＋新增 14，与留痕 integration-test-run.log 逐字一致；日志 acc-integration-test-run.log）。

## 4.4 acceptance 条目逐项 —— 通过（具名测试均在我执行的 213/213 中 PASSED）

| 契约 acceptance | 证据（具名测试，SnapshotTest.cpp） | 结果 |
| --- | --- | --- |
| RT-SNAP-1～4、RT-CPX-4：并发/隔离/迟到用例通过 | RT-SNAP-1＝`ConcurrentReadOnlyView_RT_SNAP_1`（8 线程×200 迭代并发只读＋每线程私有 State 写入互扰检查）；RT-SNAP-2＝`WorkerMaterializeRoundtrip_RT_SNAP_2`（物化重建身份逐字节相等＋可执行）；RT-SNAP-3＝`StaleSnapshotSurvivesProjectSwitch_RT_SNAP_3`（A 快照迟到反解用 A 映射、B 映射无 A 旧名、上下文释放迟到请求 ContextReleased fail-fast）；RT-SNAP-4＝`ReferenceReleaseLifecycle_RT_SNAP_4`（weak_ptr 过期时序——MSVC 无 ASAN 的等价观测，§15.4 v0.10 登记）；RT-CPX-4＝`CancelAtStageBoundary_RT_CPX_4`（段边界取消→Cancelled＋RT-CANCELLED 非 error＋无半成品＋重入重试成功）＋`BadAllocBecomesResourceBudget_RT_CPX_4`（bad_alloc→ResourceBudget＋RAII 清理＋重试成功） | 全部 PASSED |
| CR-05：值供给样例与 EV-T04 联合断言一致 | `Cr05ValueSupplyEnvironmentEntries_CR_05`：快照暴露 modelIdentity/nameMapIdentity/robworkBaselineVersion＝模型/映射/采集点计算值（值传递同一性）＋规范文本 fromCanonical 往返＋同一样例重复供给三值逐字节一致（EV-T04 跨单元联合断言的锚点）＋基线文本非空无空白 | PASSED |
| materialize 编码与工厂创建（§9.5）往返用例通过 | `MaterializeCodecRoundtripAndValidation_RT_9_5`（encode→parse 逐字段相等＋确定性＋截断/坏 magic/尾随字节/空载荷四类防御拒绝）＋`WorkerMaterializeRoundtrip_RT_SNAP_2`＋`WorkerIdentityMismatchRejected_D_13`（篡改预期身份→Failed＋诊断含期望/实得身份；篡改模型字节→parse 身份复核拒绝） | 全部 PASSED |

## 4.5 测试真实失败能力（验证验证者）—— 通过（2 处注入，验收者亲手执行）

| 探针 | 注入（worktree A 临时编辑，显式核验构建退出码） | 结果 | 还原 |
| --- | --- | --- | --- |
| 探针 1 | Snapshot.cpp materialize 的 modelIdentity D-13 核对条件改为 `if (false && …)` → 重编 exit=0 | **恰好 1 例变红**：`WorkerIdentityMismatchRejected_D_13`（SnapshotTest 过滤器 13 PASSED/1 FAILED——失败面精确，非恒真） | 精确还原 |
| 探针 2 | snapshotidentity::encodeIdentityDomain 移除 `appendU64(out, f.revisionSeq)` → 重编 exit=0 | **恰好 1 例变红**：`SnapshotIdentityDeterministicAndObservabilityExcluded_RT_ID_1`（revisionSeq 扰动断言失效即红——证明"入"字段覆盖是被钉住的） | 精确还原 |
| 复绿 | `git status --porcelain`＝空（与送验 SHA 逐字节一致）→ 全量重建 exit=0 → **213/213 PASSED** | 通过 | — |

## 4.6 架构红线（AGENTS §5 八条）—— 通过

1. **L2 零 Qt**：新增/修改源文件 Qt include 扫描零命中（grep `#include <Qt|#include <Q` ×7 文件＝0）；CMake 依赖红线守卫（runtime/CMakeLists.txt 末尾 Qt 目标 FATAL_ERROR 断言）在配置期通过。
2. **业务域互链禁止（R-1）**：跨单元 industrialrobot include 扫描（modeling/requirements/kinematics/trajectory/dynamics/selection/optimization/evidence/policy/project/io/execution/ui/reporting/testkit）零命中；唯一单元依赖＝`sdurws_ird_core`（CMakeLists L158＋配置期守卫）。
3. **跨单元私有头禁止（R-2）**：公共头只 include `sdurws/ird/{core,runtime}/*`；Snapshot.cpp 对同单元私有头（src/WorkCellCompiler.hpp、src/DynamicWorkCellCompiler.hpp）的消费属单元内合法（R-2 只禁跨单元）；对 S6/S7 产物在公共面仅前置声明。
4. **框架零源码修改（SA-02）**：见 4.1（diff 无框架文件、patches/ 零新文件）。
5. **R-4 名称前缀拼接**：无拼接行为——S8 交叉校验（Snapshot.cpp `collectActualRuntimeNames`＋`crossCheckRuntimeNames`）以"映射输出 ↔ WC 实际名逐一相等"为纪律，名称唯一来源＝映射。
6. **权威唯一（PA-1）**：运动学真值/名称归 runtime 本职（快照/工厂即 runtime 交付物）；诊断码经 `registryCode()` 单点取串（不私裁码值）；基座—世界读取经 BaseWorldTransform 规则单点（`worldToBase` 直读模型、`baseToWorld`/`gravityBase` 走规则函数，未写第二套投影）；基线/规则版本常量单点（`RobWorkBaselineVersion::capture`、`kBaseWorldRuleVersion`）。
7. **不可变历史（PA-2）**：RuntimeSnapshot 构造后只读（禁拷贝/禁移动、无 setter、shared_ptr<const> 发布——Snapshot.hpp L350-356、Snapshot.cpp 构造门禁）。
8. **不继承 old/**：无 old/ 引用。

## 4.7 注释规范（AGENTS §2）—— 通过

- **公共头全查**（4 个新增/改动头逐行通读）：Snapshot.hpp（680 行）、Compiler.hpp（257 行）、Adapter.hpp 增量（RobWorkBaselineVersion）、Resource.hpp 增量（三实体）、BaseWorldTransform.hpp 增量（kBaseWorldRuleVersion）——文件头均含设计依据（需求 ID＋单元卡锚点＋任务卡）、类/函数中文 Doxygen 完整（@param [in] 方向、@return 失败行为、@throws 触发条件）。
- **实现文件抽查 ≥3**（全读）：src/Snapshot.cpp（1191 行）、test/SnapshotTest.cpp（903 行）、runtime/CMakeLists.txt（268 行）——全部合规。
- **§2.5 高危信息逐项**：物理单位（gBase[2]==-9.81 注明 m/s² 等）、坐标系（T_world_base/T_base_world、世界系/基座系逐处注明）、角度制式（关节位 rad 语境）、所有权（ResourceBytes 借持视图"缓冲由 provider 持有"／快照 shared_ptr 归调用方／注入指针调用方持有）、线程约束（类注释逐个注明线程安全面：工厂无状态可重入、快照只读共享、State 每线程拷贝）、错误语义（调用方违约 fail-fast 异常轨 vs 环境错误稳定码诊断轨——UnknownObject/ContextReleased 与 12 注册码的归属逐处注明）、确定性来源（大端/长度前缀/字段序＝§9.1 表行序、身份排除面、常量单点）——全部在场。

## 4.8 文档同步 + findings 相关性 —— 通过

- units/runtime.md 随提交升 **v0.10**：卡头版本行、§3.1 模块表 Adapter/Resource 两行落位标注、§15.4 v0.10 登记条目（六要点：Compiler.hpp 契约面先行〔RT-T11 交付十段链实现〕、快照身份含 revisionSeq 的字段表口径、worker 物化"对象字节＝IRDCANO 编码"形态与编译器版本面值传递、工厂编排与发布门禁、CR-05 值供给样例、两模式编译分工）。逐文件比对实现与登记：一致，零未登记偏差。
- 状态同步：phase-one-validation.log L47 `RT-T09.json: validate-task: PASS`（本验收亲手重跑一致）。
- **findings.json（送验对象内）相关性核对**：45 条，open 37 条（F-004～F-010、F-012、F-015～F-020、F-023～F-045 中未关闭者）。逐类核对：均无 RT-T09 因果——提交标题名词短语家族（F-012/F-023/F-024/F-026/F-027/F-030…）、TK-T03 lint/testdata、DOC 契约回填、环境类（F-004 AUTOMOC 空转——本验收构建输出再次见 "Automatic MOC for target sdurws_ird_runtime" 空转现象，实证仍在；F-007/F-008/F-009/F-010 环境链——F-007/F-009 本验收**再次实证**，见页首环境说明）。无"可随本任务顺手消账"条目。本验收新增建议级 1 条（F-046，见 §问题清单）。

## 4.9 commit 质量 —— 通过

- 标题 `[WP-06-T09] 实现快照与工厂（RuntimeSnapshot/IRuntimeModelView/工厂/materialize）`：任务前缀＋动词开头，合规。
- 正文四段齐备（改动内容按文件列点／改动原因对应需求 ARC-03、CON-06、任务卡与 CR-05／验证／影响面与兼容性）；验证段结论（集成 213＝199＋14、冒烟 172、探针禁用 D-13 恰一例红后复绿、ird_gates 命中集与 wp06-t08 基线相同零新增）与本验收亲手复现**逐项一致**，无粉饰。

## 4.10 留痕完整 —— 通过

- `traceability/builds/wp06-t09/` 11 份落位：integration-build.log、integration-test-run.log（尾行 213 PASSED）、ird-test-report.json（schema ird-test-report/1，目标级 result＋14 用例逐条 trace，与代码逐条比对相符）、gtest XML 双份（integration tests=213 failures=0 / smoke tests=172 failures=0）、probe-build.log、probe-d13-red.log（恰好 WorkerIdentityMismatchRejected_D_13 一例 FAILED——与我的探针 1 独立同结论）、smoke-configure/build/test-run.log。
- 留痕与我复现的数字全部一致；无"未执行标注通过"情形（ird_gates 以"命中集＝既有登记 2 项、零新增"口径如实登记于 ird-gates-run.log）。

## 4.11 偷懒/缩水实现扫描 —— 通过

- ① diff 新增行 `TODO|FIXME|stub|placeholder|未实现|not implemented` 扫描：仅 2 处命中"stub"＝RT-STUB-0 替身边界**声明文本**（§11 要求的测试文档纪律，非缩水）；TODO/FIXME/placeholder/未实现/not implemented 零命中，无需豁免列表。
- ② 空实现检测：公共接口无空函数体/仅默认值返回——Compiler.hpp 为 §10.0 契约面（纯虚声明，属契约本身；其十段链产品实现归 RT-T11 的分阶段落位已按 DTB §5.4 登记，非本任务缩水）；工厂 create/materialize、快照视图、身份/物化编码均有完整实现；测试替身 `ScriptedCompiler::compile()` 返回 Failed 并注释声明"不实现（不伪造十段链语义）"——正当替身边界。
- ③ acceptance 逐条证据表：见 4.4（三条均具名测试＋XML 计数＋验收者亲手执行）。
- ④ 测试强度：断言校验行为＋边界（身份"入"字段逐项扰动、WC 键分量逐项扰动、防御拒绝四类、DWC 两成因、取消/重试往返）；非恒真——4.5 双探针证明恰一例红。

## 问题清单

### 阻断级
无。

### 建议级（已转登 findings.json F-046）
- **F-046**：src/Snapshot.cpp `SnapshotAssembler::assemble` 方法体（约 L951-1030）整体缩进比照类方法层级浅一级（4 空格，应为 8 空格），L1086-1088 `return SnapshotAssembler::assemble(...)` 实参续行对齐亦有出入；L543-548 `if (std::equal(...)) { /* 空 then */ } else { return …; }` 为空 then 分支反模式。纯风格瑕疵，零行为影响；建议 RT-T11 触碰该文件时顺手规整（避免独立格式化提交）。

### 环境观察（已有 open 登记，本次再次实证，不新增）
- F-007/F-009：全新集成树/worktree 配置需 vcpkg toolchain＋Qt CMAKE_PREFIX_PATH＋RobWorkStudio/bin 两个 .gitignore 忽略的 ini 模板——本验收手工补齐后复现成功。
- F-004：`Automatic MOC for target sdurws_ird_runtime(_test)` 空转 MOC 现象在构建输出可见（实证零 Qt 依赖，无副作用）。

## 裁决

**pass** —— 4.1～4.11 全部通过，无阻断级问题。送验对象 13072b12d67aa972e94d58a207478e484515e583 达成 RT-T09 契约 DoD（以本记录与 evidence 分支 acc/RT-T09/1 为凭）；合入决策归所有者。
