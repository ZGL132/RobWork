# WP-15-T03 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | WP-15-T03 实现 FK 与位姿指标评估器（kin.pose-metrics） |
| 送验对象（冻结） | 5551d4a4f71591975ccccbbfd64ada349e07d0ef（按 SHA 验收，不按分支名） |
| diff 范围 | 2a708d2b1a8a6551c826e4d3e139549dedbbc662..5551d4a4f71591975ccccbbfd64ada349e07d0ef（state.base..headSha） |
| **verdict** | **pass**（4.1～4.11 全部通过，零阻断级问题；建议级 4 条转登 findings.json：F-384～F-387） |
| 验收者环境 | 全新上下文；主检出只读（并发会话占用中，未做任何 git 写操作）；复现现场＝detached worktree @5551d4a4（自建临时目录），集成模式独立 build 树从零配置，冒烟模式另配独立 build 树；验收者自查与测试执行全部亲手完成，未采信实施者运行结果。gitignored 的 RobWorkStudio.ini.template.static 自主检出只读复制后 configure（T02~T09 验收实录现象，F-007/F-099 在案） |
| 环境参数 | 集成/冒烟均带 `-DCMAKE_TOOLCHAIN_FILE=vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`（主树 build/CMakeCache.txt 实证配置——F-062/F-098 环境链口径）；集成缓存确认 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON` |
| 证据附件 | 验收者复跑产物原样留存于本分支 `traceability/builds/wp15-t03-acc/`（13 件：六份 gtest XML＝集成/冒烟两模式×两目标＋变异后复绿终态两份、ird_gates base/head 引擎日志两份、归一化命中表两份、集成构建/冒烟配置/冒烟构建日志三份） |

---

## 4.1 改动范围 — 通过

- `git diff --name-only 2a708d2b..5551d4a4`＝32 个文件，逐一对契约为：
  - `kinematics/**` 12 个（CMakeLists.txt、include 四头〔Errors/Evaluators/Fk/KinTypes〕、src 三 TU〔Errors/Evaluators/Fk〕、test 三件〔ErrorsTest/FkTest/KinFkFixture〕、contract_test 一件〔PoseMetricsEvaluatorContractTest〕）——allowedFiles 第 1 条内；
  - `traceability/**` 19 个（builds/wp15-t03/ 18 件＋findings.json）——allowedFiles 第 3 条内；
  - `units/kinematics.md` 1 个——allowedFiles 第 2 条内。
- `forbiddenFiles`（REQUIREMENTS.md/ARCHITECTURE.md）：diff 零触碰（按文件名过滤 diff 输出＝0 行）。
- SA-02：`*patches*` 路径 diff＝0 文件——框架零源码修改、无未登记改动。

## 4.2 可复现构建 — 通过（两模式独立树从零）

- **集成模式**：worktree A（detached @5551d4a4）→ `cmake -S <A>/RobWork -B <独立目录> -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON`（附 toolchain/Qt 前缀），缓存确认 ON；`cmake --build … --config Release --target sdurws_ird_kinematics sdurws_ird_kinematics_test sdurws_ird_kinematics_contract_test` exit 0，日志 `error C|error LNK|fatal error` 计数＝0（附件 integration-build.log）。注意：无 toolchain 的裸命令首配失败于 FindBoost（F-007/F-062/F-101 在案）——删树后带全参数重配成功，与本家族教训一致。
- **独立冒烟模式**：另一独立目录 `cmake -S <A>/…/industrialrobot -B <独立目录> -DCMAKE_TOOLCHAIN_FILE=vcpkg/...`（AGENTS §4.1 / CORE-T01 G-2 口径）→ 构建 exit 0、零 error（附件 smoke-configure.log/smoke-build.log）；两测试 exe 冒烟实测 27/27＋11/11 通过（XML 原样留存）。
- 残留进程检查：构建前 `tasklist` 无 RobWorkStudio.exe，无锁树冲突。

## 4.3 契约 verify 命令 — 通过（3/3 亲手执行）

| # | 契约命令 | 结果 |
| --- | --- | --- |
| 1 | `cmake --build build --config Release --target sdurws_ird_kinematics` | exit 0（独立 build 树执行，字面 target 单跑复验通过） |
| 2 | `ctest --test-dir build/RobWorkStudio/src/rwslibs/industrialrobot/kinematics -C Release -L "^ird$"` | exit 0，`100% tests passed, 0 tests failed out of 2`（标签 ird 计 2 用例） |
| 3 | `pwsh -File RobWork/scripts/industrialrobot/validate-docs.ps1` | exit 0，`validate-docs: PASS (20 units, 12 trace entries, 189 task files)` |

- 家族注记（引用 F-381，不另立新号）：T03 契约 verify 数组较 T02 已补 ctest 行，但仍缺 `_test`/`_contract_test` 两测试目标的显式构建行——ctest 所依赖的测试 exe 由验收者在两棵独立构建树自行补建（家族在案口径，属验收者侧正常动作）。
- 附验（协议 §3 脚本面）：`validate-task.ps1 -TaskFile <契约>` exit 0 PASS (1 tasks)。

## 4.4 契约 acceptance 逐条 — 通过（5/5 有证据）

| # | 契约条目 | 证据（验收者亲手复现） |
| --- | --- | --- |
| 1 | FK 指标链路解析黄金对照（D-KIN-2 统一尺度、σmax/σmin、w=∏σ、归一化裕量；附录 D 第 4/9 项容差；先行解析算例 UT、T13 收口全量） | `KinFk` 15 用例：PlanarTwoLinkZeroPose/QuarterPose 解析位姿、PlanarTwoLinkJacobianAnalytic（零位解析列＋π/2 位独立参考逐元素）、RevPrismSingularValuesAnalytic（σ={√(L²+1),1} 全解析、条件数=√(L²+1) 解析锚、n<6→w=0）、SixAxisAgainstIndependentReference（位姿/雅可比逐元素＋Σσ²=‖J‖_F²＋∏σ=w 恒等式＋降序＋条件数首末比）、AllFixedChainDegenerateMetrics（dof=0：+∞/isFinite=false 不静默截断）、MarginNormalization（中点 1/限位 0/工作范围超限 −1/无界 +∞）；容差 kPoseTol/kGoldenRel/kGoldenAbs 与附录 D 第 4/9 项声明同源（夹具）。真失败能力见 4.5（M1/M2/M3 具名测试真红） |
| 2 | 基座—世界唯一来源＋q_authoritative 零叠加（V-02 本单元侧输入/输出面；FK→IK→FK 闭环属 T04/T13 承载） | Fk.cpp:299 `tcpInWorld` 唯一经 `runtime::composeWorldBaseTcp(view.worldToBase(), tcpInBase)`——全单元无第二处基座变换代数（grep 实证唯一调用点）；BaseWorldInvertedMount_V02（倒挂安装：独立 4×4 复合逐元素对照＋"倒挂下 world≠base"防空转对照断言）；AuthoritativeQZeroOffsetNotAdded（zeroOffset 0→0.5 双模型字节级同输出——FK 消费 q 即权威值）。FK→IK→FK 闭环需 IK 对端（T04），按卡 §10.1"解析算例黄金仅覆盖 FK/解析可验证面"分批承载，与契约 acceptance 2 括注"本单元侧输入/输出面"一致——非缩水 |
| 3 | 非法输入 fail-fast 两分语义＋结构化错误素材 | IllegalQDimensionRejected（3≠2，params expected-dof/actual-dof 保序断言）、IllegalQNonFiniteRejected（NaN，params nonfinite-index）——不钳制不置零（实现先维度后有限性两检独立可辨）；NoTcpTwoBranches（悬空 oid→NoTcp、非空键不命中→FrameUnresolved、精确匹配正例）；评估期结构化素材 StructuredErrorMaterialNoTcp（无工具/悬空两分支→KIN-NO-TCP error 级诊断、零 payload 零证据、subject 携带指名工具身份、码经 kinematicsDiagCode 映射取注册常量禁拼码）；OverLimitQComputableWithNegativeMargin（q1=3.5>2.97 FK 照常可算＋负裕量＝调用方违例素材——两分语义）。NoDevice 分支的承载方式见 F-386（建议级） |
| 4 | kin.pose-metrics 评估器形态（descriptor 五条 Required/三模式/stateless；宿主注入工厂闭包 create() 无参——O-37 裁决形态；确定性同字节） | DescriptorMatchesSection43Row（五条依赖 key/kind/Required 逐项＋无 applicability＋Preview/Quick/Verified 三模式＋stateless＋profile{kin,1,零值}域不申报）；KeyWordFormGateCompliant（实现键 kin-pose-metrics 过 evidence 冻结闸门 isValidEvaluationKey——卡面点形键的机械偏差已随卡 §14.6 v0.3 登记并经编排者放行口径核可）；HostInjectionFactoryProducesEvaluator（工厂闭包捕获 (视图,查询)、create() 无参签名、产出实例 descriptor 同值、payload token/canonical 字节/SHA-256 摘要重算三面齐全）；AssemblyFailFastRejectsIllegalQuery（空视图/维度/NaN/badKey 四反例 invalid_argument）；DeterministicBytesAcrossCallsAndModes（同实例/跨实例/跨模式同字节）。O-37 义务链核验：governance-log.md:207 closed＋DTB §4.2:638 已裁决行＋卡 §14.6 v0.3 ⑥ 放行记录——三面一致 |
| 5 | O-40/Eigen 处置（vcpkg 经典模式、PRIVATE 仅计算库、版本登记） | CMakeLists.txt：`find_package(Eigen3 CONFIG REQUIRED)`＋`target_link_libraries(sdurws_ird_kinematics PRIVATE Eigen3::Eigen)`——仅产品库 PRIVATE；公共三头（Fk/KinTypes/Evaluators）零 Eigen 类型（grep 实证，Eigen include 仅 src/Fk.cpp 单 TU）；vcpkg 实测 `Package: eigen3 / Version: 5.0.1`（installed/x64-windows/status＋Eigen3ConfigVersion.cmake 双证）——与卡 §14.6 v0.3 ⑤ 登记一致。版本登记落点为单元卡 §14.6 而非契约 note 字面（契约 tasks/*.json 不在 allowedFiles、实施者不可改——登记位置偏差按 DTB §5.4 已随卡面落账，验收者判定合规） |

## 4.5 测试真实失败能力（验证验证者） — 通过（3 处实质变异，全部真红，恢复后复绿）

变异均在 worktree A 的 src/Fk.cpp 上实施→增量重建两测试目标→跑具名测试→`git restore`＋touch 强制重编（F-068 mtime 陷阱规避）→重建全绿：

| 变异 | 内容 | 结果 |
| --- | --- | --- |
| M1 | 条件数 `smax / smin` → `smax * smin`（L360） | `KinFk.SixAxisAgainstIndependentReference_WP15T03_ACC1` 真红（1 FAILED；RevPrism 用例不红系 σmin=1 时积商同值——数学预期，非测试失效） |
| M2 | 归一化裕量 `/ half` → `/ (hi - lo)`（去掉半程归一化，L418） | `KinFk.MarginNormalization_WP15T03_ACC1` 真红（1 FAILED） |
| M3 | 跳过基座—世界复合 `composeWorldBaseTcp(...)` → `m.tcpInWorld = m.tcpInBase`（L299） | `KinFk.BaseWorldInvertedMount_V02_WP15T03_ACC2` 真红（1 FAILED） |

恢复后终态：`git status` 产品源零差异，重建后 27/27＋11/11 全绿（acc-kin-test-int-final.xml/acc-kin-contract-int-final.xml 原样留存）。

## 4.6 架构红线 — 通过（AGENTS §5 八条逐条）

1. **L2 零 Qt**：kinematics src/include/test/contract_test 全文 grep `#include <Qt|<Q` ＝0 命中（构建日志 Automatic MOC 为 F-004 在案的顶层空转，非 Qt 依赖）。
2. **R-1 业务域互链**：产品面跨单元 include 仅 core/diagnostics/runtime/evidence（+测试面 testkit——T-1 允许形态）——无 modeling/requirements/trajectory/dynamics/selection/optimization；CMake PUBLIC 链六条＝§3.2 登记边；ird_gates SUB 直方图 base↔head 逐码比对 SUB=75→75 零新增边。
3. **R-2 跨单元私有头**：消费的 13 个他单元头逐一核实位于各单元 `include/sdurws/ird/<unit>/` 公共面（13/13 PUBLIC OK）；测试夹具自持 id 派生副本而非 include runtime/test 内部头（R-2 精神，夹具头注明）。
4. **R-4 名称拼接**：kinematics 源内 `RobWork` 字符串拼接/剥离扫描＝0 命中；TCP 解析经 findObject/nameMap 语义（FrameUnresolved 拒绝模糊匹配）。
5. **PA-1 权威唯一**：基座—世界真值只读 view.worldToBase()；码值权威经 DiagCodes 注册常量（禁拼码）；判定词表零输出（EvaluationOutput 仅 payload/diagnostics——只算不判）。
6. **PA-2 不可变历史**：本任务无修订写路径接触面（纯计算库＋评估器形态）。
7. **SA-02 框架零修改**：4.1 patches 零触碰。
8. **old/ 不恢复**：实现全部新建（diff 无历史源码恢复迹象；旧代码仅 §2.5 功能对照）。

## 4.7 注释规范 — 通过（建议级 1 条转登 F-384）

- 公共头全查（Fk.hpp/KinTypes.hpp/Evaluators.hpp 新增＋Errors.hpp 增量）：文件头中文 Doxygen（设计依据含需求/任务卡/O 项出处）、类/结构体/函数 @brief、AGENTS §2.5 高危信息逐项在位——物理单位（m/rad/无量纲/混合量纲说明）、坐标系（{B}/{W} 读法 core §4.6）、rad/deg 显式、所有权（view 调用方持有/非 owning 注入指针）、线程约束（无状态可重入/并发只读）、错误语义（调用方 fail-fast 轨 vs 环境结构化素材轨分轨注明）、确定性来源（定序固定/无并行归约/无时钟、canonical f64 位模式）。
- 实现文件抽查 ≥3：Fk.cpp（五步主流程逐步注释＋每分支业务场景＋Eigen/rw 边界纪律）、Evaluators.cpp（分轨语义/映射纪律/不可达路径 fail-fast）、Errors.cpp（映射两行登记纪律）——合格。
- 发现（建议级，F-380 同族"注释随代码更新"）：Errors.hpp 两处过期残留未随 T03 映射行同步——L165-166 段头注"映射阶段纪律见文件头注——本任务零映射行"、L182-183 `@return 已登记稳定码文本（当前全表 4 值均 nullopt…）`与同文件已更新的文件头（"WP-15-T03 …登记两行"）及实现（NoDevice/NoTcp 返回注册常量）矛盾。数据/断言/实现均正确（ErrorsTest 真红能力经 T02 验收 M 系探针在案、本次读码核实），纯注释面 → F-384。
- 测试代码：用例名带 WP15T03_ACCx 追溯字段＋IRD_TEST_INFO 需求/AT 登记＋中文说明，合格。

## 4.8 文档同步 — 通过（建议级 2 条转登 F-385/F-387）

- 单元卡 v0.3（§14.6 行 978）：九项登记逐项与实现核对一致——①KinTypes 两实体落点；②D-KIN-2 实现口径（奇异值/条件数/可操作度/裕量归一化公式与 Fk.cpp 逐条一致）＋两模式可链接纪律；③评估器形态＋错误分轨＋键词形偏差登记；④Errors 映射两行＋KIN-NEAR-SINGULAR 产生面归属（§9.6 行 731 随版标注）＋NoDevice 不可达性论证；⑤O-40 兑现（Eigen 5.0.1——vcpkg status 实测同值）；⑥O-37 编排者 2026-09-26 放行裁决记录（义务 a/b/c 交代）；⑦§11 T03 行 Bounds 归属消歧（行 839）；⑧测试与留痕数字；⑨F-380 触发条件未触发的处置说明（核实：diff 零触碰 DiagCodes.hpp/DiagCodesTest.cpp——属实）。卡头版本行/日期行同步 v0.3/2026-09-26（F-369/374 教训已吸收）。
- 评估键 kebab 化：卡 §4.3 行 306 随版标注（实现键 kin-pose-metrics＋闸门依据＋T04~T07 同规则预告）——有依据、随卡标注、词形经 evidence 冻结闸门测试钉住。
- **F-383 核对（4.8 指定项）**：实施端自登条目质量合格——title 与 governance-log.md:207（O-37 closed 2026-09-22）、DTB §4.2:638（裁决行原文）、卡 §14.6 v0.3 ⑥（放行记录）三面一致；owner/resolution 字段齐备；与本任务相关性成立（evidence §9 增补义务悬账正是本任务 knownPitfalls O-37 的随附义务 b）——维持 open 合理，无需本任务消账。
- 开放发现相关性盘点（不静默积压）：F-380 触发条件本任务未发生（⑨ 登记）；F-381 家族本任务实例见 4.3 注记（引用不另立号）；F-382 sanctioned 台账本任务零新增 SUB 命中（SUB 75→75）；F-012 标题家族本任务两提交均动词开头（无新实例——但缺前缀见 F-385）。
- 偏差登记判定：实现与契约/单元卡零未登记偏差（键 kebab 化、Bounds 归 T04、版本登记落点均已随卡登记）。

## 4.9 commit 质量 — 通过（建议级 1 条转登 F-385）

- 两提交（0ece5504 实现、5551d4a4 登记）正文四段（What/Why/验证/影响面）齐备；验证段主张逐项与验收者亲测一致：集成/冒烟 27/27＋11/11、ctest 2/2、validate-docs PASS（20 units/12 trace/189 task files 同数字）、ird_gates base..head +1 sanctioned 命中（与验收者引擎直跑比对一致，见门禁专段）——无粉饰。
- 标题：两提交均动词开头（"实现…"/"登记…"）但缺 `[WP-15-T03]` 前缀——AGENTS §6.3/ACC §4.9 字面要求不符；与 F-069（EV-T09）判例同形态（动词开头、任务编号在正文可见、无粉饰）→ 建议级 F-385。

## 4.10 留痕完整 — 通过

- 送验提交内 traceability/builds/wp15-t03/ 实际入库 18 件（ls 清点）：双目标 gtest XML（tests=27/failures=0、tests=11/failures=0——与验收者两模式四轮复跑逐一致）、ird-test-report.json（total 27/passed 27/decisive true）＋ird-test-report-contract.json（11/11）、集成/冒烟构建与配置日志、四份测试运行日志、ird_gates base/head 引擎日志＋归一化命中表两份＋比对附件、validate-docs/validate-task 日志——**声称与入库一致，零幻影清单**（F-102/F-111/F-125 家族本任务零复现；.log 按先例 add -f 入库）。
- phase-one-validation.log：本任务属阶段 B 单元实现批次，留痕以 builds/wp15-t03/ 证据集为口径（与已合入的 T02 同形）——ird-test-report.json 即结果账本（resultNote 面齐备）。

## 4.11 偷懒/缩水实现扫描 — 通过（建议级 1 条转登 F-386）

- ①diff 新增行 `TODO|FIXME|stub|placeholder|未实现|not implemented` 扫描＝0 命中（无豁免条款需要列出）。
- ②空实现检测：产品面无空函数体/仅返回默认值的接口实现（Fk.cpp 495 行/Evaluators.cpp 287 行实质实现——SVD/编码/校验逻辑完整；`return {}` 扫描零命中）；KinTypes.hpp 为纯类型头（无 TU）已随卡与 CMake 注登记。
- ③逐条证据表：见 4.4——acceptance 5/5 条条有具名测试/命令输出/文件行号。
- ④测试强度：27+11 用例断言行为与边界（解析黄金逐元素、params 保序、字节级确定性、+∞ 退化面、负裕量、四反例 fail-fast、诊断码/subject 精确断言、摘要重算）；非恒真——4.5 三处变异证明具名测试真实会红。发现（建议级）：`NoDeviceGuardDocumentedUnreachable_WP15T03_ACC3` 为仅 SUCCEED() 的文档化用例（无行为断言，命名形似行为测试）——NoDevice 结构化素材路径因此无直接运行断言（类型系统不可达：CanonicalModelBuilder 禁空链，守卫为注入视图面契约防线；映射行已由 ErrorsTest 覆盖、不可达性论证随卡 §14.6 v0.3 ④）→ F-386（F-146/F-043 测试覆盖面建议家族）。

## 门禁专段（ird_gates 引擎直跑 base..head 归一化比对 — 零未登记新增）

验收者以 cmake -P 引擎直跑两棵 detached worktree（T02 验收确立口径），归一化（剥绝对路径＋剥汇总行）后逐行 diff，三套计数并列（F-378 口径）：

| 计数口径 | base 2a708d2b | head 5551d4a4 | 增量 |
| --- | --- | --- | --- |
| ①直方图行数（grep 按码计） | 143 | 145 | +2 行 |
| ②去重条目（归一化后 sort -u） | 143 | 145 | +2 条（本任务归一化不复合同串——去重=行数，如实列明） |
| ③引擎自计数（`命中 N 项`） | 68 | 69 | +1 项 |

- 直方图逐码：LIB 10→12（+2）、R1 7→7、R3 41→41、R4 3→3、R5 1→1、SUB 75→75、T1 5→5、T2 1→1——与送验附件 ird-gates-base-head-compare.md 逐格一致（验收者独立重跑）。
- 唯一新增条目（diff 全文 2 行）：`IRD-GATE-LIB: 目标 sdurws_ird_kinematics 链接未知目标族 Eigen3::Eigen（…先登记 DTB 再扩词表）`——验收者独立定性为 **sanctioned**：DTB §4.2 O-40 行（2026-09-22 所有者批次授权：Eigen——kinematics 计算库 PRIVATE，rw::math 无 SVD）＋契约 knownPitfalls 携带 O-40＋acceptance 5 字面一致（PRIVATE 仅计算库）；白名单扩词表属治理面待办（不在 allowedFiles，与 F-382 同池顺延）。零其他新增命中；VS 目标 ird_gates 退出码 1 系存量命中既有状态（DTB §4.5 存量例外未清零）。

## 发现问题清单

| 级别 | 编号 | 摘要 |
| --- | --- | --- |
| 阻断 | 无 | — |
| 建议 | F-384 | Errors.hpp 两处过期注释残留（段头注"本任务零映射行"、@return"当前全表 4 值均 nullopt"）与已登记的 NoDevice/NoTcp 映射行矛盾——AGENTS §2.6 过期注释（F-380 同族）；实现与断言正确 |
| 建议 | F-385 | 两送验提交标题缺 `[WP-15-T03]` 前缀（F-069 变体家族再现；动词开头合规、正文四段齐备、结论无粉饰） |
| 建议 | F-386 | KIN-NO-DEVICE 结构化素材路径无直接运行断言（类型系统不可达＋守卫防线在案）＋NoDeviceGuardDocumentedUnreachable 为仅 SUCCEED() 的文档化用例——测试承载方式建议（F-146/F-043 家族） |
| 建议 | F-387 | 卡 §14.6 v0.3 ⑧ 句尾"ird_gates…零新增命中"与同附件/提交正文的"+1 项 sanctioned 命中（Eigen PRIVATE）"表述不一致——散文计数家族（F-025/F-041 同族，方向少报、附件与提交正文口径准确） |

## 裁决

**verdict＝pass**。4.1～4.11 全部通过，无阻断级问题；4 条建议级随本记录转登 findings.json（F-384～F-387，编号顺延自送验侧最大号 F-383），不阻塞合入。送验对象＝5551d4a4f71591975ccccbbfd64ada349e07d0ef 本身；分支漂移检查（ACC §3）：`git ls-remote origin refs/heads/wp15-t03`＝5551d4a4f71591975ccccbbfd64ada349e07d0ef——与冻结 SHA 一致，零漂移。

---

*验收者：WP-15-T03 attempt 1 独立验收会话（全新上下文，2026-09-26）；本记录与 F-384~F-387 转登经 acc/WP-15-T03/1 分支推送，收尾按 commit SHA 合并。*
