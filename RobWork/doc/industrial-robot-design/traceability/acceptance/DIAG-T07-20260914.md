# DIAG-T07 验收记录（attempt 2）

| 字段 | 值 |
| --- | --- |
| 任务 ID | DIAG-T07（≙WP-09-T04 范围设施面，diagnostics 单元：两级日志——管线/节流采样/轮转/WorkerLogBatch 重放/ILogFileOps 接缝） |
| 契约 | `doc/industrial-robot-design/tasks/foundation/DIAG-T07.json`（status ready；branch wp09-t07；requirements NFR-REL-05/UX-02；designRefs units/diagnostics.md §7.1~§7.5/§9.6；knownPitfalls P-DIAG-7/P-EX-8；interUnit true） |
| 送验对象（冻结） | `cf30112f45169c29b431556228762221587ce594`（验收全程复核：`origin/wp09-t07`＝该 SHA，**无分支漂移**） |
| diff 范围 | `dcf229573f6f53cfce94ec6f92e14d20065d6587`（base＝state.base＝redesign-main 尖端，与 merge-base 一致）..`cf30112f`，共 2 提交（6603b9df 实现＋cf30112f 单元卡/留痕）、10 文件（+3314/−5） |
| verdict | **pass**（4.1~4.11 全部通过，无阻断级问题；新增建议级登记 3 项＝F-108/F-109/F-110，沿 F-081/F-102/F-103/F-106/F-012/F-100/F-104/F-107 家族既判先例不阻塞合入） |
| 验收日期 | 2026-09-14 |
| 验收者 | 独立验收会话（attempt 2，全新上下文；未消费实施叙述；对抗式立场——复现优先于采信，下述构建/测试/门禁/探针/脚本均为验收者亲手执行） |

---

## 一、逐项证据（ACC §4.1~4.11）

### 4.1 改动范围 —— 通过

- diff 文件清单（`git diff --name-status dcf22957..cf30112f`，10 个）逐个比对契约 `allowedFiles`：
  - `RobWork/RobWorkStudio/src/rwslibs/industrialrobot/diagnostics/**`（4 个）：CMakeLists.txt（M，仅注释更新＋`src/Logging.cpp` 增列产品库＋`test/LoggingTest.cpp` 增列 `_test`——逐 hunk 核对，无其他改动）、include/sdurws/ird/diagnostics/Logging.hpp（A，公共头）、src/Logging.cpp（A）、test/LoggingTest.cpp（A）；
  - `RobWork/doc/industrial-robot-design/units/diagnostics.md`（M，allowedFiles 第 2 项——卡头"构建落位"＋§14.4 v0.8 变更记录）；
  - `RobWork/doc/industrial-robot-design/traceability/**`（5 个，allowedFiles 第 3 项）：builds/wp09-t07/ 下 integration-gtest-diagnostics-test.xml（A）、integration-gtest-diagnostics-contract-test.xml（A）、smoke-gtest-diagnostics-test.xml（A）、smoke-gtest-diagnostics-contract-test.xml（A）、ird-test-report.json（A）。
- `forbiddenFiles`（REQUIREMENTS.md / ARCHITECTURE.md）：diff 范围内零命中。
- SA-02（框架零源码修改）：diff 过滤 industrialrobot/doc 前缀后范围外文件计数 **0**；`industrialrobot/patches/` 零新文件零改动（`git diff --name-only <range> -- .../patches/` 空输出）。
- **范围越界：零。**

### 4.2 可复现构建 —— 通过（验收者亲跑；双模式零错误）

- **复现现场**：`git worktree add --detach D:/10_Source_Repos/21_robot/.acc-worktrees/diag-t07-r2-repro cf30112f45169c29b431556228762221587ce594`（detached @ 送验 SHA，不用分支名）。
- **集成模式（干净构建树，从零重新配置）**：`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DRWS_USE_STATIC_LINK_PLUGINS=ON -DCMAKE_TOOLCHAIN_FILE=<仓库根>/vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`——配置 exit 0 零 CMake Error（Configuring done 7.1s），缓存确认 **`RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`**。worktree 无未跟踪本地辅助件 `RobWorkStudio.ini.template.static`（F-007/F-062/F-099 在册既有缺陷），自主树补拷至 `RobWork/RobWorkStudio/bin/` 后配置通过（环境修复，不触及送验产物——DIAG-T05/T06 验收同先例）。
  - `cmake --build build --config Release --target sdurws_ird_diagnostics_test sdurws_ird_diagnostics_contract_test`：两测试目标编译链接**零 error C／fatal error／error LNK（grep 计数 0）**， Logging.cpp/LoggingTest.cpp 均有编译活动；产物 `sdurws_ird_diagnostics.lib`（STATIC 增列 1 源文件）＋两测试 exe 落位 `build/RobWorkStudio/{libs,bin}/Release/`。口径沿 DIAG-T05/T06 先例：构建两目标及其依赖链（core＋框架库），未做全树构建——本任务改动面（diagnostics 单元）完全覆盖。
  - **测试执行（验收者亲跑）**：`sdurws_ird_diagnostics_test.exe` → **77/77 PASSED**（exit 0，310 ms）；`sdurws_ird_diagnostics_contract_test.exe` → **9/9 PASSED**（exit 0）——既有 65＋9 零回归＋新增 12 全过，与送验 ird-test-report.json 登记一致。
- **独立冒烟模式（另配临时目录 `D:/10_Source_Repos/21_robot/.acc-worktrees/diag-t07-r2-smoke`，带 vcpkg toolchain——F-002/CORE-T01 G-2 口径）**：`cmake -S RobWork/RobWorkStudio/src/rwslibs/industrialrobot -B <冒烟目录> -DCMAKE_TOOLCHAIN_FILE=.../vcpkg.cmake` 配置 exit 0；`cmake --build ... --config Release` **exit 0 零 error（grep 计数 0）**；冒烟树两测试 exe 亲跑 → **77/77 PASSED＋9/9 PASSED**（exit 0），与送验留痕"双模式 77/77＋9/9"一致。

### 4.3 契约 verify 命令 —— 通过

契约 `verify` 数组仅一条，验收者在复现 worktree A 根目录逐字亲跑：

```
cmake --build build --config Release --target sdurws_ird_diagnostics_test   → exit 0
```

（`sdurws_ird_diagnostics_test.vcxproj` 增量链接产出，exit 0；产物可直接运行 77/77，见 4.2。）另亲跑 `verify-task.ps1 -TaskFile <契约>` → **verify-task: PASS DIAG-T07**，exit 0（脚本内执行同条 verify 命令）。

### 4.4 acceptance 条目逐项证据 —— 通过（3 条全部有具名测试与行号证据；ird_gates 如实登记基线命中而非虚标通过）

| # | acceptance 条目 | 证据 |
| --- | --- | --- |
| 1 | DT-LOG-1~5 用例通过：两级分流（Tier-U 无 Dev 行/无调用栈/无内部哈希——NFR-REL-05/UX-02）、关联 ID 与全局稳定序（同线程 FIFO）、写失败注入（DIAG-LOG-WRITE-FAILED 不阻塞调用方、flush 有界——ILogFileOps 接缝）、日志禁用不影响正式诊断（§7.8）、WorkerLogBatch 乱序/断裂重放合并 | 亲跑双模式 77/77 全绿（送验 XML 四份同数据：tests=77/9 failures=0 两模式）。**DT-LOG-1 两级分流**：`DtLog1_TwoTierSplitKeepsUserFileCleanOfDevLinesAndInternalPatterns`（LoggingTest.cpp:390——用户级 Error 消息故意携带 0x 地址＋64 位十六进制哈希＋栈帧文本，Tier-U 文件恰 2 行全 "U" 无 Dev 行(:431-434)、模式扫描三禁止零命中＋[ADDR]/[HASH]/at [FRAME] 占位符在场(:437-442)、code=PRJ-LOCK-HELD 稳定码保留(:444)；Tier-D 恰 3 行全量镜像且保留未过滤原文(:448-458)——§7.1"Tier-U ⊆ Tier-D"）。**DT-LOG-2 关联 ID 与稳定序**：`DtLog2_ConcurrentWritesKeepSameThreadFifoAndGlobalStableOrder`（:469——4 线程×150 并发写，回读 600 行零丢失(:501)、seq 全文件严格单调(:505-507)、各线程 entryId 子序列严格 1..150＝同线程 FIFO(:511-524)、tag/ids 关联字段逐行在场）。**DT-LOG-3 写失败注入**：`DtLog3_WriteFailureInjectedStaysNonBlockingAndBounded`（:536——FaultFileOps 抛异常轨注入用户文件追加＋冲刷失败：log 三条 EXPECT_NO_THROW 不阻塞不抛(:548-557)、flush 返回 false 有界失败(:561-562)、dev 文件出现 DIAG-LOG-WRITE-FAILED Dev 行且指明 file=user(:567-580)、failHits 计数＝appendHits 零成功写入＋用户文件未创建(:581-584)——故障注入经 ILogFileOps 接缝自持替身，不链 testkit）。**DT-LOG-4 禁用独立性**：`DtLog4_DisabledLoggingLeavesFormalDiagnosticsIntact`（:596——enabled=false：reportDev 路由进禁用 logger 安全降级(:613-614)、工厂 create＋目录 append 正常产出且 snapshot 恰一条 PRJ-STORE-CORRUPT(:624-633)、两日志文件零产生(:636-639)、flush 即时成功(:635)）。**DT-LOG-5 重放合并**：`DtLog5_WorkerBatchReplayRestoresOrderAndFillsChannelGap`（:651——场景 A 乱序投递 2→1→0 恢复 b0→b1→b2 落盘(:699-700)、重复批幂等单行(:702)＋陈旧批 Dev 计数行(:703-704)、worker 行补注 wk=7＋ids=task 五元组(:706-712)；场景 B final=2 越过缺口 1：恰一条 EX-CHANNEL-PROTOCOL-ERROR Dev 补条(:715-731)、位置严格在 b0 之后 b2 之前(:733-742)）。机制承载行为面：节流窗汇总 occurrences=4＋采样首/每 N/末条（DtLogThrottleAndSamplingWindowBehavior:754）、队列满丢最旧 Debug/Trace＋Dev 计数行 Error 保全（DtLogOverflow...:850）、轮转世代移位（DtLogRotation...:899）、截断 4 KiB＋UTF-8 边界（DtLogTruncation...:941）、入口违约 7 类 Usage（DtLogCallerViolations...:975） |
| 2 | P-DIAG-7 处置：轮转（默认 5×2 MiB/文件）与 flush 窗口（N=256/T=2 s）为可配工程默认（P-DIAG-7 登记），WP-23 性能验收时校准，不作为需求语义 | `DtPdiag7_DefaultsAreConfigurableEngineeringDefaults`（LoggingTest.cpp:1022——默认值逐项断言 rotateFiles=5/rotateMaxBytes=2 MiB/flushEveryLines=256/flushInterval=2 s/sampleEveryDebug=100/throttleWindowSeconds=10/enabled 缺省开(:1025-1032)＝单元卡 §14.3 登记值；可配置面整值替换 configure 后行为生效(:1039-1050)）——"缺省可用＋可配置"两面自证，与单元卡 §14.4 v0.8 陷阱处置登记一致；LogSinkConfig 头文件注释逐字段锚定 P-DIAG-7（Logging.hpp:206-211） |
| 3 | P-EX-8 处置：worker 日志回传只定 WorkerLogBatch 载荷语义（§7.5）；帧类型/通道帧表归 execution §6（§12.2 交接），不私定对端帧格式 | `DtPex8_HeaderDefinesPayloadOnlyNoExecutionFrameContract`（LoggingTest.cpp:1063——源码扫描钉住：WorkerLogBatch struct 在场(:1076)、`#include <sdurws/ird/execution` 头/实现双零命中(:1078-1081)＝R-1/R-2、FrameType/FrameHeader/ChannelFrame 帧类型词汇零命中(:1083-1085)）；验收者独立复核 Logging.hpp include 全集（4 个：core 公共头 DiagData/Identity＋本单元公共头 Catalog/Confirmable）与 Logging.cpp（1 个：本单元 Errors.hpp）——零 execution 侧引用 |

**ird_gates（如实登记）**：验收者亲跑 `cmake --build build --config Release --target ird_gates`（复现 worktree 集成树）→ exit 1，**命中 7 项与 wp09-t06 合入基线逐项同分布、零新增**：IRD-GATE-SUB×5（evidence/evidence_contract/policy/policy_contract/runtime 测试目标 → sdurws_ird_testkit 直链，F-072/F-051 族既有）＋IRD-GATE-SUB 表外边×1（sdurws_ird_testdata_lint → testkit，F-019）＋IRD-GATE-T1×1（sdurws_ird_testdata_lint 产品目标链 testkit，F-019）；引擎自测行（pass_clean/pass_r4comment＋fail_r1/t1/r5/sub/r3/r4/t2"按预期检出"）非命中。**diagnostics 相关命中＝0**（命中输出全文检索 diagnostics 计数 0）。exit 1＝基线既有命中的预期行为，处置权 WP-01 所有者——与送验 ird-test-report.json gates 段登记一致（如实登记基线命中，未私改门禁、未虚标通过）。

### 4.5 测试真实失败能力（验证验证者）—— 通过（两探针亲手构造、真实变红、还原复绿零残留）

验收者在复现 worktree 的 `src/Logging.cpp` 临时注入缺陷、重建重跑、逐字还原（`git checkout --`＋`touch` 强制重编规避 F-068 mtime 陷阱；还原后 `git status` 零内容残留、grep `PROBE-TEMP` 计数 0；最终 77/77 复绿）：

- **探针 1（Tier-U 哈希过滤阈值击穿——NFR-REL-05/UX-02 观测面）**：`maskUserPatterns` 模式 2 阈值 `j - i >= 32` 改为 `>= 65`（64 位十六进制哈希不再命中过滤）→ `DtLog1_TwoTierSplitKeepsUserFileCleanOfDevLinesAndInternalPatterns` **真实变红**（DiagLogging 套件 1 FAILED 11 PASSED——"内容哈希必须被过滤"断言失败）：用户级无内部哈希的防线非恒真。
- **探针 2（断裂补条击穿——DT-LOG-5 观测面）**：`processBatch` 收尾批断裂分支删除 `emitGapLine(key, s, c)` 调用（缺口不再补 EX-CHANNEL-PROTOCOL-ERROR 行）→ `DtLog5_WorkerBatchReplayRestoresOrderAndFillsChannelGap` **真实变红**（1 FAILED——"恰一条断裂补条"断言失败）：§7.5"批次丢失→主进程补一条 Dev 诊断"有机器观测面。
- 两探针证明断言校验真实行为边界、非恒真；探针期间中间构建未计入任何结论。

### 4.6 架构红线（AGENTS §5 八条逐条）—— 通过

1. **L2 计算内核零 Qt**：新增 3 文件 `grep "#include <Q"` 计数 0/0/0（hpp/cpp/test）；ird_gates 输出零 diagnostics 命中；diagnostics/CMakeLists.txt 配置期红线守卫（既有）未触碰（diff 仅源文件增列）。
2. **业务域互链禁止（R-1）**：产品库链接边未变（diff 仅增列源文件，PUBLIC 依赖仍仅 `sdurws_ird_core`——target_link_libraries 亲核）；ird_gates SUB/R1 零 diagnostics 命中；execution 侧零引用（DtPex8 源码扫描运行期钉住）。
3. **跨单元私有头禁止（R-2）**：新文件 ird include 全集＝core 公共头 {DiagData.hpp、Identity.hpp}＋本单元公共头 {Catalog/Confirmable/DiagCodes/Errors/Factory/Logging}（`include/sdurws/ird/` 公共层，grep 实证）；detail 层引用计数 0；本任务未新增跨单元引用。
4. **框架零源码修改（SA-02）**：见 4.1——diff 零框架文件、patches/ 零改动。
5. **不做名称前缀拼接/剥离（R-4）**：本任务无名称解析语义（日志管线与 RobWork 名称面无关）；grep 名称前缀类引用 0 命中；ird_gates R-4 零 diagnostics 命中。
6. **权威唯一（PA-1）**：日志定位为非权威运维快照（§7.8；Logging.hpp:24-26 文件头专段"绝不参与项目事实重建"）；DtLog4 实证禁用日志不影响正式诊断（目录/工厂路径与日志零耦合）；taskCanonical 注释明示"仅日志呈现，不是身份计算（身份归各所有者单元，PA-1）"（Logging.cpp:266-267）；码值只用既登记码（DIAG-LOG-WRITE-FAILED/EX-CHANNEL-PROTOCOL-ERROR——§4.6 内置表原文引用，无私造码值）；无 project/runtime/policy 语义越入。
7. **不可变历史（PA-2）**：日志管线无任何修订/宿主写路径（写面仅两级日志文件＋轮转覆盖——§7.8 生命周期，与修订历史无交集）。
8. **不继承 old/**：实现按单元卡 §7.1~§7.5/§9.6 从零编写；grep old/ 零引用。

### 4.7 注释规范（AGENTS §2）—— 通过（公共头全查＋实现/测试全读，抽查面全覆盖）

- **公共头全查（1/1，Logging.hpp 578 行）**：文件头设计依据逐条（§7.1~§7.6/§7.8/§9.6＋需求 NFR-REL-05/UX-02＋契约 acceptance 1~3）；"为什么两级日志由一条记录＋分流产出"背景专段（NFR-REL-05 两级语义漂移防范论证）；P-DIAG-7/P-EX-8 陷阱处置锚点专段（契约 knownPitfalls 逐项）；线程安全专段（ILogger 全方法任意线程＋析构例外＋全局稳定序＝入队序）；确定性专段（节流窗用注入时钟、有界等待用 steady_clock 墙钟的理由）。类/函数级：LogTier/LogLevel 逐值语义（≠诊断 severity 映射、采样适用面）；CorrelationIds 六项逐字段＋"至少一个非空"判定原语；LogRecord 生命周期（返回即入队确认、worker 回传保留原时间戳）；LogSinkConfig 逐字段默认值＋P-DIAG-7"修改默认值不构成需求偏差"；WorkerLogBatch 载荷字段语义逐项（seq 从 0 连续、final 收尾触发断裂判定）；ILogFileOps 四方法 @param [in] 逐个＋"允许抛异常、管线消化"实现契约；ILogger 四方法与 §9.6 契约表逐行（合法/非法调用、@throws Usage 触发条件逐类）；LoggingPipeline 五步管线逐步注释＋worker 重放三支行为（期望/越过/陈旧）＋失败语义专段。
- **实现文件抽查（1/1 全读，Logging.cpp 1337 行）**：文件头实现口径六项登记（与单元卡 §14.4 v0.8 九项呼应）；truncateForLog UTF-8 续字节回退逐步注释；maskUserPatterns 三模式逐步＋优先级论证（地址优先于哈希防二次匹配）；appendOne"为什么不持常驻句柄"三点论证（低容量/fclose 即落盘/Windows 句柄共享）；rotateFile 世代移位方向注释（自高位向低位防覆盖）；runLoopInner"持锁不变量"论证（wait 返回恒持锁、每轮末尾重取的理由——EPERM 陷阱）；队列满策略分支逐支（丢最旧 Debug/Trace→无可丢丢新行）；processBatch 三支序号语义逐步；finalize 四步收尾序（缓冲排空→节流汇总→采样尾条→冲刷）。§2.5 高危信息：错误语义（入口 Usage fail-fast vs 环境面降级不抛的边界逐处标注——validateRecordBasics/validateConfig 注释）、线程（互斥只护队列与配置、日志线程私有无锁化逐处标注）、确定性来源（节流窗记录时间戳＝注入时钟）、所有权（clock/ops 调用方持有引用、按值接管语义——hpp @param 注明）。
- **测试文件抽查（1/1 全读，LoggingTest.cpp 1086 行）**：文件头 acceptance 1~3 映射表＋用例名后缀＝矩阵行编号说明；故障注入形态专段（自持替身不链 testkit——T-1 纪律）；线程约束专段（DtLog2 多线程观测面、ManualClock 单线程读取理由）；每 TEST 注释锚定矩阵行/需求/陷阱；断言处中文说明（"防线不依赖调用方自觉""消除主线程灌入竞态""轮转覆盖＝旧档消亡是设计语义不是丢行"等）。无中英混杂机翻腔、无过期注释、无裸 TODO。
- 唯一瑕疵：Logging.cpp:1273 注释将接缝名误拼为"ILoggFileOps"（应为 ILogFileOps）——纯拼写瑕疵转登 **F-110**，不构成 §2.5 必填项缺失。
- **单位/坐标系/角度制式**：本任务无物理量计算面（时间戳 UTC 已逐处标注 epochMillis/毫秒；无位姿/角度语义），不适用——非缺项。

### 4.8 文档同步 —— 通过

- 单元卡 `units/diagnostics.md` v0.6→v0.8（v0.7 为 DIAG-T06 已合入登记）：卡头"构建落位"字段实测刷新（Logging.hpp/cpp/测试文件/12 用例/双模式 77/77＋9/9 落位清单）＋§14.4 v0.8 变更记录——**九项实现口径按 DTB §5.4 登记**（①序号＝日志线程写入序；②镜像写入 Tier-U⊆Tier-D；③Tier-U 内部模式过滤清单〔0x→[ADDR]/≥32 hex→[HASH]/栈帧→[FRAME]〕与 T08 脱敏接线点；④节流窗记录时间戳判定与采样末条关闭落盘；⑤断裂判定时机＝final 批或关闭排空；⑥内部行旁路采样；⑦日志线程顶界兜底；⑧FileLogFileOps 逐行开-写-关＋flushFile 恒真；⑨队列满无可丢行时丢新行）＋陷阱处置（P-DIAG-7 可配工程默认、P-EX-8 只定载荷语义）＋接线登记（IRedactionService 随 T08、IDevLogSink 自本任务可装配、订阅回调维持同步派发）。**§9.6 四方法签名与 Logging.hpp 逐字比对一致**（log/logDev/flush(deadline)/configure——Logging.hpp:424/436/447/460 与单元卡 §9.6 原文逐 token 一致，仅形参名 level/config 补名不构成签名偏差）；§7.2 LogRecord 字段表/§7.5 WorkerLogBatch 四字段逐项一致；§10 DT-LOG-1~5 矩阵行与 12 用例逐行有承载（DT-LOG-4"宿主持久化桩验证"半区归 project 落地后的编排测试——本套件承载目录/工厂半区，与 §11"同名文件"分工一致）。
- 契约流水线状态：`state.json` currentTask＝DIAG-T07、phase awaiting_acceptance、attempts.accept＝2 在案，翻转归收尾（PIPE §4.7⑤——非实施者职责）。
- 治理脚本：验收者亲跑 `validate-task.ps1 -TaskFile <契约>` → **PASS (1 tasks)**，exit 0；`validate-docs.ps1` → **PASS (20 units, 12 trace entries, 150 task files)**，exit 0；`verify-task.ps1` → **PASS DIAG-T07**，exit 0。
- **findings.json 未关闭条目相关性核对（open 条目逐家族排查，登记册共 107 条/86 open）**：与本任务直接相关的既有 open 条目——**F-081/F-102/F-103/F-106 家族**（声称入库实际未入库：cf30112f 正文声称的各类 .log 全部未入库＝该家族第 10 例且再现 .gitignore 根因，新登记 **F-108**；改进面：双模式 gtest XML 四份本例已入库）；**F-010**（.gitignore:87 全局 *.log 吞留痕——本例根因再现，并入 F-108）；**F-012/F-100/F-104/F-107 标题家族**（cf30112f 标题名词"文档"开头——同型再现，新登记 **F-109**）；**F-007/F-062/F-098/F-099/F-101**（全新树构建环境链：本次复现再次实证 toolchain/Qt 前缀/ini 模板三件套必要——按 DIAG-T05/T06 验收口径一次配通，处置权构建约定所有者，无可随手消账项）；**F-019/F-051/F-072**（ird_gates 基线红：本次 7 命中同分布再印证，零 diagnostics 命中，处置权 WP-01 所有者）；**F-065**（留痕日志中文乱码：本次 ird_gates 输出复现同现象——环境编码面，与本任务产物无关）；**F-068**（探针还原 mtime 陷阱：本次验收主动以 touch 强制重编规避，未再现误判）。微观卫生家族（F-046/F-063/F-093/F-105 型）：本次发现 1 处注释接口名拼写瑕疵，新登记 **F-110**；其余源码零残留注释、零冗余 include。其余 open 条目属 evidence/runtime/policy/io/testkit 等其他单元，与本任务无相关性、无静默积压。

### 4.9 commit 质量 —— 通过（附标题风格再现与留痕清单出入——转登 F-108/F-109，不构成数值粉饰）

- `6603b9df` `[WP-09-T07] 实现 diagnostics 两级日志（管线/节流采样/轮转/WorkerLogBatch 重放）`：标题动词开头、≤50 字；正文四段齐备（What 按文件逐项/Why 逐条需求与陷阱映射〔NFR-REL-05/UX-02、§7.1~§7.5/§9.6、acceptance 1~3〕/验证含构建·测试·门禁真实结论〔双模式 77/77＋9/9、既有 65＋9 零回归、ird_gates 7 命中基线零新增、validate/verify-task PASS〕/影响面"仅新增文件＋CMake 增列、既有诊断行为零改动"）；`Acc-Covered: 1,2,3` 尾注。正文"（≙WP-09-T04…）"与单元卡 §11 行（DIAG-T07→WP-09-T04）一致；分支号 wp09-t07 与标题 [WP-09-T07] 沿既定映射口径（契约 note 登记），与前五例诊断任务提交同惯例。
- `cf30112f` `[DIAG-T07] 文档同步与验证留痕（单元卡 v0.8＋双模式证据）`：正文四段齐备、如实说明纯文档与留痕性质；**标题以名词"文档"开头非动词**——F-012/F-100/F-104/F-107 家族同型再现（转登 **F-109**，建议级）。
- **出入（建议级，转登 F-108）**：cf30112f 正文声称入库"integration/smoke 构建日志、测试运行日志、契约 verify 命令日志（verify-target-build.log）、ird_gates 输出、validate-task PASS、gtest XML 四份、ird-test-report.json"——git ls-tree @cf30112f 实证 builds/wp09-t07/ 仅 **5 件**（双模式 gtest XML 四份＋ird-test-report.json），正文声称的全部 .log 零入库（.gitignore:87 全局 `*.log` 吞——F-010 根因再现，未 add -f；ird-test-report.json gates 字段引用 gates-current-wp09t07.log 一处且该文件不在库）。**改进面**：双模式 gtest XML 四份本例已实际入库（F-106 时代仅集成两份）。**数值主张（双模式 77/77＋9/9、既有 65＋9 零回归、门禁 7 命中基线逐码一致零 diagnostics、validate-task/verify-task PASS）经验收者全新 worktree 从零配置亲手复现全部准确，测试结果无粉饰。**
- 注记：送验构建日志未入库故其实施构建树口径无从核对（F-103 同症）——验收者已以独立从零配置构建补强（集成＋冒烟双模式 exit 0 零 error，见 4.2），口径缺口不掩盖。

### 4.10 留痕完整 —— 字面部分未达成（建议级——F-108；沿 F-081→F-106 家族九例既判先例不阻断）

- **在库**（git ls-tree @cf30112f，builds/wp09-t07/ 共 5 件）：`ird-test-report.json`（schemaVersion ird-test-report/1，含 per-target 构建结论、gates 基线对比、selfCheck 探针能力说明、per-suite 逐用例 trace〔12 新用例逐条映射 acceptance/需求/矩阵行，断言值与源码逐项核对一致〕——结构化 resultNote 载体本身完整准确）＋**双模式 gtest XML 四份**（integration/smoke × diagnostics-test 77 用例/diagnostics-contract-test 9 用例，tests=77|9 failures=0 errors=0 时间戳 2026-09-14T23:00~23:01，DtLog1~5/DtPdiag7/DtPex8 十二新用例全部在案——较 F-106 改进）。
- **未入库而提交正文声称在案（各 .log）**：integration/smoke 构建·配置·测试运行日志、verify-target-build.log、ird_gates 输出（gates-current-wp09t07.log）、validate-task PASS 留痕——全部缺失（F-010 根因，见 4.9）。**证据本体不缺**：双模式 77/77＋9/9、ird_gates 7 命中、validate-task/validate-docs/verify-task PASS 由验收者从零亲手复现一致（本记录 4.2/4.3/4.4/4.8）。
- `phase-one-validation.log`：历史载体（DIAG-T03~T06 验收记录同结论），本任务留痕按现行惯例落 `builds/wp09-t07/`，ird-test-report.json 即结构化 resultNote 载体——符合"phase-one-validation.log **或** resultNote 已更新"的替代口径。

### 4.11 偷懒/缩水实现扫描 —— 通过

- ①**标记扫描**：新增源码三文件（Logging.hpp/Logging.cpp/LoggingTest.cpp）`TODO|FIXME|stub|placeholder|not implemented|未实现` 计数 **0**；diff 范围内留痕文件命中 30 处全部为既有用例名/套件名（integration/smoke-…-contract-test.xml 内 ConfirmableProjectStubTest×14——DIAG-T05 产物、integration/smoke-…-test.xml 内 DtDiag2_ParamSchemaPlaceholderConsistency×1——DIAG-T04 产物，均为验证用例的数据行非实现缩水标记，所在文件为留痕数据而非代码）；无需豁免条目。
- ②**空实现检测**：公共接口全部真实实现——LoggingPipeline 五步管线（renderRecord：截断→级别过滤→节流→采样→写入＋flush 窗口，Logging.cpp:700-744）、节流窗管理（throttleSuppressed/emitThrottleSummary :751-800）、采样（samplingDrop :807-820）、写入失败语义（writeLine/flushDirty :844-915）、worker 重放三支（processBatch/acceptBatch/drainReady/emitGapLine :998-1089）、四步关闭收尾（finalize :1093-1134）、FileLogFileOps 四方法（appendOne 开-写-关/rotateFile 世代移位 :314-395）。flushFile 恒真为**契约已登记的实现口径⑧**（逐行开-写-关下无持久脏缓冲——单元卡 §14.4 v0.8 明文，非未声明空实现）。无空函数体/仅返回默认值的契约函数。
- ③**逐条证据表**：见 4.4——acceptance 3 条全部具名测试＋文件行号级证据，无一条仅凭"已实现"声明。
- ④**测试强度**：断言校验行为与边界（行数精确 2/3/4/600、占位符在场＋原文零命中的正反双断言、seq 严格单调逐行、同线程 FIFO 子序列逐项、failHits＝appendHits 零成功写入、补条位置严格介于 b0/b2、采样恰 4 行＝首/每 N/末条、节流恰 3 行＝首/汇总 occurrences=4/窗后首条、轮转 .2 不存在＋活动/一代档各恰一行、截断 4 KiB＋3 字节边界对齐、Usage 违约 7 类错误码面、P-DIAG-7 七项默认值逐项断言）；无恒真断言；4.5 双探针实证非恒真。

---

## 二、裁决

**verdict ＝ pass**

- 4.1~4.11 全部通过（4.10 的留痕入库缺口与 4.9 的标题风格为建议级，沿 F-081→F-106 家族九例既判先例不阻断），无阻断级问题：范围零越界、双模式构建/测试/门禁/脚本全部亲手从零复现成功（集成 77/77＋9/9、冒烟 77/77＋9/9、ird_gates 7 命中基线同分布零 diagnostics、validate-task/validate-docs/verify-task PASS）、红线零命中、注释无缺项（1 处拼写瑕疵转登）、4.11 零命中（新增源码面）、双探针实证测试真实失败能力、无数值粉饰。
- 新增建议级登记 3 项：**F-108**（cf30112f 声称入库的各 .log 留痕全部未入库——F-081/F-102/F-103/F-106 家族第 10 例；.gitignore:87 `*.log` 根因再现＝F-010；改进面：双模式 gtest XML 四份已入库；证据本体不缺，验收者双模式亲跑一致）；**F-109**（cf30112f 标题名词"文档"开头——F-012/F-100/F-104/F-107 家族再现）；**F-110**（Logging.cpp:1273 注释接口名误拼"ILoggFileOps"——微观卫生）。
- 环境类既有条目 F-007/F-062/F-098/F-099/F-101/F-019/F-051/F-072/F-065/F-068/F-010 本次复现再次实证（ini 模板补拷后零错误配置构建；ird_gates 基线 7 命中同分布；门禁输出乱码同现象），已在册、不重复登记。
- 本验收通过的是 **SHA cf30112f45169c29b431556228762221587ce594**，非分支引用；验收者未修改任何实施产物（探针注入/还原均在临时 worktree 内、最终零残留）、未向 wp09-t07 推送任何提交。

## 三、问题清单

| 级别 | 描述 | 处置 |
| --- | --- | --- |
| 阻断 | 无 | — |
| 建议 | **F-108**：cf30112f 正文声称入库"构建/测试运行日志＋verify-target-build.log＋ird_gates 输出＋validate-task PASS"等 .log 留痕，实际 builds/wp09-t07/ 仅 5 件（双模式 gtest XML 四份＋ird-test-report.json）——F-081/F-102/F-103/F-106 家族第 10 例；.gitignore:87 全局 `*.log` 吞且未 add -f（F-010 根因再现）；ird-test-report.json gates 字段引用 gates-current-wp09t07.log 不在库。数值主张全部复现准确、无粉饰；双模式 XML 四份已入库（较 F-106 改进） | 已转登 findings.json（F-108，open）；修复方向同 F-103/F-106（gitignore 例外/强制 add -f）；不阻塞合入 |
| 建议 | **F-109**：cf30112f 标题"[DIAG-T07] 文档同步与验证留痕…"以名词"文档"开头非动词——F-012/F-100/F-104/F-107 家族再现（6603b9df 动词开头合规） | 已转登 findings.json（F-109，open）；建议模板固化标题口径；不阻塞合入 |
| 建议 | **F-110**：Logging.cpp:1273 注释将接缝名误拼为"ILoggFileOps"（应为 ILogFileOps）——纯注释拼写瑕疵，行为与接口拼写均正确 | 已转登 findings.json（F-110，open）；随下次触碰该文件的提交顺手更正；不阻塞合入 |

## 四、验收者环境说明

- 复现 worktree（worktree A）：`D:/10_Source_Repos/21_robot/.acc-worktrees/diag-t07-r2-repro`（detached @ cf30112f）；集成树＝主树缓存口径从零配置（`RWS_BUILD_INDUSTRIALROBOT=ON`〔缓存 grep 确认〕、`RWS_USE_STATIC_LINK_PLUGINS=ON`、vcpkg toolchain、Qt `CMAKE_PREFIX_PATH`；`RobWorkStudio.ini.template.static` 自主树补拷至 worktree 内 `RobWork/RobWorkStudio/bin/`——F-007/F-062/F-099 既有缺陷的环境修复，不触及送验产物）。冒烟树：`D:/10_Source_Repos/21_robot/.acc-worktrees/diag-t07-r2-smoke`（独立目录＋vcpkg toolchain，显式 Release）。
- 工具链：VS 2022（MSVC，生成器 Visual Studio 17 2022，-A x64）、vcpkg 经典模式（gtest 1.18.0 @ installed/x64-windows）、pwsh（validate-task/validate-docs/verify-task.ps1）。
- 探针注入/还原均在 worktree A 内：两轮注入（maskUserPatterns 哈希阈值 32→65、processBatch 删除 emitGapLine 调用）各自重建重跑取证后 `git checkout --` 还原＋touch 强制重编（规避 F-068 mtime 陷阱），最终 `git status` 零内容残留、grep `PROBE-TEMP` 计数 0、77/77 复绿。
- 验收记录与 F-108/F-109/F-110 登记落 `acc/DIAG-T07/2` 分支（基于 origin/redesign-main @ dcf22957）并推送；验收者日志随临时 worktree 清理，本记录内数值均为亲测。
