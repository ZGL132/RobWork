# PRJ-T07 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | PRJ-T07（事务引擎 TxEngine：七步状态机/.staging/恢复扫描；≙WP-04-T07） |
| 送验对象（冻结） | 448d2e4f715d58fee7ba3a9183a631c405fc9912（分支 wp04-t07 尖端） |
| diff 范围 | da7d3647c61122d58242bacfcfcc1926bbb78828（origin/redesign-main，即 merge-base）..448d2e4f，共 4 提交（856544ef 实现 / e61ade92 单元卡 v0.9 / 65e05bee 留痕归档 / 448d2e4f 留痕补齐） |
| **verdict** | **pass**（4.1～4.11 全部通过，无阻断级问题；建议级 3 项，已按 ACC §5 转登 findings.json＝F-138/F-139/F-140） |
| 验收者环境 | 全新上下文；detached worktree@448d2e4f 从零配置复现（C:\Users\zgl18\AppData\Local\Temp\acc-prj-t07-repro）；独立冒烟树（acc-prj-t07-smoke，带 vcpkg toolchain）；集成配置参数＝主树缓存口径（toolchain 绝对路径＋Qt CMAKE_PREFIX_PATH＋RWS_BUILD_INDUSTRIALROBOT=ON＋RWS_USE_STATIC_LINK_PLUGINS=ON＋ini 辅助件两份复制——F-062/F-099/F-101 环境链前置）；全部构建与测试由验收者亲手执行 |
| 记录分支 | acc/PRJ-T07/1（本文件所在分支，commit 见推送后 state.acceptanceRecord） |

---

## 4.1 改动范围——通过

- `git diff --name-only da7d3647..448d2e4f`＝14 文件，逐项落在契约 allowedFiles：project/** 8 件（CMakeLists.txt、src/TxEngine.{hpp,cpp}、src/win32/{AtomicFile.cpp,AtomicFile.hpp,IFileOps.hpp}、test/{AtomicFileTest.cpp,TxEngineTest.cpp}）＋traceability/** 5 件（builds/wp04-t07/ 下 ird-test-report.json＋4 份 gtest XML）＋units/project.md 1 件。
- forbiddenFiles（REQUIREMENTS.md/ARCHITECTURE.md）：`git diff --name-only da7d3647..448d2e4f -- <两文件>` 零输出——零触碰。
- SA-02：patches/ 目录零改动（同法验证零输出）——无未登记框架源码修改。

## 4.2 可复现构建——通过（附环境注记）

- **集成模式**：worktree A 从零配置 exit 0（`RWS_BUILD_INDUSTRIALROBOT:BOOL=ON` 缓存确认）；industrialrobot 交付面定向构建零 error——产物行实证 `sdurws_ird_core.lib`→`sdurws_ird_project.lib`→`sdurws_ird_project_test.exe`→`sdurws_ird_project_contract_test.exe` 全部链接成功。
- **环境注记（非本任务问题，阻断豁免依据）**：全量 `cmake --build`（含框架全部目标）以 exit 1 终止，73 处 error 全部位于框架 RobWorkSim 目标 RWSimulatorPlugin.vcxproj（rw/math/Math.hpp、VelocityScrew6D.hpp 的 min/max 宏污染簇，C2059/C2334/C2143/C2238）——与 findings **F-008** 已登记的基线框架缺陷同形态（该记录载明 base 959fb9b 对照复现同样失败、主树 Debug 存量 RWSimulatorPlugin.dll 佐证"存量掩盖"，本次验收者实测主树存量产物在位）。本任务 diff 零框架文件，因果上不可能引入；构建日志证实 MSBuild 在该框架目标失败时 industrialrobot 目标尚未开始编译。交付面口径（industrialrobot 全目标）零错误成立，冒烟模式（全单元树）零错误成立，不构成 ACC §5 的"构建不可复现"阻断。
- **独立冒烟模式**：另配临时目录＋`-DCMAKE_TOOLCHAIN_FILE`（F-002 教训口径）配置＋构建 exit 0（SMOKE_EXIT=0）。
- **测试复现**：冒烟树 151/151＋10/10 全绿；集成树 151/151＋10/10 全绿（与送验 4 份 XML 的 tests="151" failures="0"、tests="10" failures="0" 逐一一致；TxEngineTest 套件 18 用例在位）。

## 4.3 契约 verify 命令——通过

- 契约 verify 仅一条：`cmake --build build --config Release --target sdurws_ird_project_test`——验收者在复现树 build 上执行，exit 0（增量重建并产出测试可执行文件）。
- 附加治理脚本：validate-task PASS (1 tasks)；validate-docs PASS (20 units, 12 trace entries, 150 task files)。
- ird_gates（`--target ird_gates`）实测执行：命中 8 项＝IRD-GATE-SUB×7（diagnostics_contract_test/evidence_test/evidence_contract_test/policy_test/policy_contract_test/runtime_test 直链 testkit＋testdata_lint→testkit 表外边）＋IRD-GATE-T1×1（testdata_lint），零 project 项、零新增命中——与 report 声称及 F-019/wp04-t06 基线描述一致（门禁因基线命中按设计 exit 1，非构建错误）。

## 4.4 acceptance 逐条核对——通过（ird_gates 已真实执行，非"未执行"）

| 契约条目 | 证据（验收者实测） |
| --- | --- |
| 1. PRJ-TX-4（F1～F8 进程内部分）边界注入矩阵＋任一步失败已发布内容字节不变＋.staging 忽略＋恢复诊断 | TxEngineTest 具名用例 F1/F2/F3/F4/F5/F6/F6b/F7（§7.6 F1～F7 逐边界，故障经 IFileOps 接缝注入：kCreateDirectories/kWriteChunk/corruptOnClose/kPublishNew/kReplaceExisting/FakeBus/kRemoveTree）＋内存段两用例（Commit_InconsistentTipUpdate_FailFastZeroDiskWrites、Commit_UnregisteredAuthoritativeRef_FailFast——零磁盘副作用断言）；字节比对断言（snapshotCommitted/expectCommittedUnchanged）随各用例执行全绿；恢复诊断断言（PRJ-RECOVERY-IGNORED-UNCOMMITTED 用户级码＋devContains）在 F2/F4/F5/F7/F8 系列全绿 |
| 2. 崩溃/kill 后已提交修订与 HEAD 字节不变（AT-13 载体，进程级复验归 PRJ-T15）；提交点＝HEAD 原子切换 | F8_AfterPrepare/F8_AfterStage（旧态字节不变严格全等）、F8_AfterHeadSwitch（新态字节不变：HEAD＝newHead 逐字段＋manifestDigest 对磁盘字节重算比对）、F5（T4 腿）、HappyPath（T7 控制腿）全绿；契约与 acceptance 注记的"进程级 TestProcessRunner kill 归 PRJ-T15"口径与测试文件头注一致（PRJ-T15 尚未实施，无超前声明） |
| 3. 「已发布但未提交」识别（闭包外对查询不可见）＋.staging/tmp 清理（D-09） | F4/F5 用例的 uncommittedRevisions＋tryRevision(nullopt)＋闭包排除断言全绿；StagingTmp_CleanedByScan_EngineArtefactsStayInTxDir（tmp 整树清理＋产物限事务目录＋项目根无散落 head.new）全绿；RecoveryScan_HealthyStore_NoFindings 基线全绿 |
| 4. P-PR-1 处置（清单摘要经 core ContentDigester，零 core 修改） | HeadManifestDigest_MatchesDirectCoreDigester：core::ContentDigester 直算磁盘 manifest.json 字节 ↔ HEAD.manifestDigest ↔ 对象文件名三方同值断言全绿；diff 核查零 core/ 文件触碰（4.1） |

## 4.5 测试真实失败能力——通过（2 探针，均实证会红）

- 探针 1（F1 腿）：TxEngine.cpp commit() 第 1 步 createDirectories 失败分支移除 throw（吞错）→重建→`TxEngineTest.F1_PrepareFails_StagingDirCreate` **FAILED**（期望的 WriteRejected 未抛出）。
- 探针 2（P-PR-1 腿）：`newHead.manifestDigest = manifestDigestHex` 改为 metadataDigestHex（错值）→重建→`TxEngineTest.HeadManifestDigest_MatchesDirectCoreDigester` **FAILED**。
- 还原：两探针逐字还原后 `git status --porcelain`（industrialrobot/ 范围）零输出；按 F-068 教训 touch 源文件强制重编→全量复跑 151/151＋10/10 **复绿**。

## 4.6 架构红线——通过

- 零 Qt：diff 新增 include 全集＝标准库＋windows.h＋gtest（仅测试）＋`sdurws/ird/core/Events.hpp`＋本单元头（PersistenceFormat/StoreTypes/私有头）——QtCore/QtWidgets/QtGui 零命中。
- R-1 业务域互链：跨单元依赖仅 project→core（Events.hpp 公共头）——project 非业务域计算单元，唯一单元边与 PRJ-T01 以来的登记一致；ird_gates 实测 SUB/T1 命中集合与基线一致（零新增边）。
- R-2 私有头：TxEngine.hpp 位于 src/（头文件自注"不入 include/"）；对 core 仅 include 公共头路径 `sdurws/ird/core/`。
- R-4：TxEngine 零名称前缀拼接/剥离逻辑（名称解析未涉足）。
- 权威唯一（PA-1）：诊断码＝PRJ-RECOVERY-IGNORED-UNCOMMITTED/PRJ-STORE-CORRUPT（diagnostics.md §4.6 收编清单，代码注释自证"不私造码"）；摘要一律经 codec::contentVersionOf（core ContentDigester——CR-02 唯一入口）；写路径归 project 本职。
- 不可变历史（PA-2）：只增发布（publishNew 不覆盖）、失败悬挂不回滚、HEAD 原子切换——实现与 §7.1 一致。
- SA-02 框架零修改（见 4.1）；old/ 无恢复痕迹（diff 全部新写文件）。

## 4.7 注释规范——通过

- 公共契约面（本任务新增均为私有实现头＋测试，仍按公共头标准全查）：TxEngine.hpp 文件头（设计依据/背景/错误二分/线程约束/所有权——§2.5 高危信息全项：单位〔chunk 64KiB〕、所有权〔五指针非 owning 逐一点名〕、线程〔commit 串行化＋tx-id 原子仅保证编号〕、错误语义〔调用方 invalid_argument vs 环境 StoreError 稳定码 vs 数据 StoreCorrupt 三分〕、确定性〔NFR-COR-02 排序〕）。
- 实现文件抽查（≥3）：TxEngine.cpp（全读——七步各段标注 §7.1 步号、错误映射表注明来源、魔法数字 ERROR_DISK_FULL=70/ERROR_ACCESS_DENIED=5 注释、报告确定性排序注释）、win32/IFileOps.hpp（增量两方法＋faultpoint 登记注释完整）、win32/AtomicFile.cpp（createDirectories/removeTree 薄封装注释完整）、TxEngineTest.cpp（文件头＋18 用例逐一中文锚定注释：需求/AT/契约条目）、AtomicFileTest.cpp（增量转发注释）、CMakeLists.txt（增量注释块）——无缺项、无过期注释、无中英机翻腔。

## 4.8 文档同步＋findings 消账核对——通过（附 1 项建议级：F-140）

- 单元卡 project.md v0.9 登记（§15.4）与实现逐项对应：七步协议、恢复扫描①④⑤、损坏分流纪律、对象发布不经 publishObject 的口径、修订身份随 CommitPlan 带入、kMetadataTypeToken、IFileOps 接缝增补、P-PR-1 处置、测试统计（18 用例/151/151/10/10）——与代码及实测一致；实现口径登记（DTB §5.4）四项在卡内显式落位；契约语义零变更（§7 原文未动，diff 仅 3 行）。validate-docs PASS；validate-task PASS。
- 任务状态/追踪矩阵：契约 status=ready（翻转归收尾段 PIPE §4.7，非实施侧职责）。
- **findings 未关闭条目相关性核对**（open 116 条，含 project/PRJ 字样者 29 条）：
  - **F-019**（ird_gates 基线红）：本任务实测 8 命中＝基线集合，零 project 项——状态不受本任务影响，修复动作在 testkit/治理侧，不可随本任务消账；
  - **F-010**（.gitignore 吞留痕日志根因）：本任务再次命中（10 份 .log 未入库）——根因未收口，随 F-138 登记继续累积；
  - **F-123/F-133/F-134**（卡头日期行失同步）：本任务再现且 F-134 两条建议（两行均推进＋注明消账进展）全部未执行——恶化，转登 **F-140**；
  - 其余 open 条目与本任务 diff 无涉，无可顺手消账项（消账需文档/代码动作超出本任务 allowedFiles 或属其他任务辖域）。

## 4.9 commit 质量——通过（附 1 项建议级：F-139 标题家族）

- 856544ef（实现）：标题"实现 project 事务引擎…"动词开头合规；正文四段（What/Why/验证/影响面）齐备；Acc-Covered: 1,2,3,4 在位；验证结论（151/151、10/10、8 命中零新增、exit 0、validate-task PASS）经验收者亲手复现逐项为真。
- 65e05bee（留痕归档）：标题以任务名词"PRJ-T07"开头（F-128→F-130→F-131→F-135 同字面模板**五连例再现**）；正文声称 15 项证据、git ls-tree 实证仅 5 件入库（10 份 .log 未入库，正文未内联明示）——幻影清单家族再现，转登 **F-138**。
- 448d2e4f（留痕补齐）：标题同为任务名词开头；正文**有**内联说明".log 按仓库惯例留工作树不入库"，两份 XML 的补齐原因（首提时生成失败未落盘）如实陈述——诚实度合格，标题瑕疵并入 F-139。
- e61ade92（[units] 车道）：文件名词短语开头（F-037/F-079/F-092/F-130 同型），并入 F-139。
- 无粉饰：全部数值主张实测为真；"引擎零缺陷修改、失败全为测试侧期望错误"的实现期缺陷陈述与 report 一致，无掩盖形态。

## 4.10 留痕完整——通过（附 F-138 建议）

- 库内五件自洽：ird-test-report.json（targets/gates/validateTask/verify/acceptance 逐条到具名测试）＋双模式 gtest XML 四份（151/151＋10/10、TxEngineTest 18 套件、failures=0）——数字与验收者四轮实测（冒烟树/集成树/探针复绿/送验 XML）一致。
- `.log` 十份仅工作区未入库：ird-test-report.json 的 evidence.committed 将其与库内 5 件混列且无 localOnlyNote 字段（较 F-136 先例的缓解措施退步）——已转登 F-138；结论性证据（XML/JSON）在库完整，不构成 4.10 阻断。
- 契约 verify 与 validate-task 结论均经验收者重放为真。

## 4.11 偷懒/缩水实现扫描——通过

- ① `TODO|FIXME|stub|placeholder|未实现|not implemented`（大小写不敏感）扫描 diff 范围（industrialrobot/）：**零命中**；无"触发式延后"豁免需列出。
- ② 空实现检测：TxEngine 全部方法有实质实现（七步编排/扫描/自校验逐段落地）；IFileOps 纯虚＝设计接缝（契约声明）；AtomicFile 增量两方法为 std::filesystem 薄封装（幂等语义注释在位）——无未声明的空实现/仅返回默认值。
- ③ acceptance 逐条证据表：见 4.4——四条全部到具名测试/命令输出，零"已实现"裸声明。
- ④ 测试强度：字节级快照比对（严格全等/悬挂残留两档）、故障注入 fake（装饰真实实现＋occurrence 计数）、闭包排除＋tryRevision 不可见断言、core 直算三方比对、O-15 水位单调断言——非恒真、非"只编译通过"；4.5 探针实证断言真实敏感。

---

## 问题清单

### 阻断级

无。

### 建议级（已按 ACC §5 转登 traceability/findings.json，编号顺延自 F-137）

| 编号 | 摘要 | 登记位置 |
| --- | --- | --- |
| F-138 | 幻影清单家族第 14 例：65e05bee 声称 15 项证据实际入库 5 件；10 份 .log 未入库（.gitignore:87＝F-010 根因）；evidence.committed 混列且无 localOnlyNote（较 F-136 缓解措施退步）；结论性内容实测无粉饰 | findings.json |
| F-139 | 提交标题家族（F-012 系）：65e05bee/448d2e4f 任务名词开头（同字面模板五连例）＋e61ade92 [units] 车道名词开头；实现提交动词开头合规；纯风格瑕疵不阻断 | findings.json |
| F-140 | 卡头日期行家族（F-123→F-133→F-134 后第四次再现且 F-134 建议全部未执行）：卡头版本行 v0.9 已同步、日期行停留 2026-09-09（§15.4 v0.9＝2026-09-16）；消账进展未注明 | findings.json |

## 裁决说明

- 4.2 的全量集成构建 exit 1 归因于 findings F-008 已登记的框架基线缺陷（RobWorkSim RWSimulatorPlugin，全新树必现、主树存量掩盖），与本任务 diff 零交叠；industrialrobot 交付面与冒烟模式零错误、测试四轮复现一致——不构成"构建不可复现"阻断。
- verdict＝**pass**：可进入所有者合入环节（本验收者不合入、不修改实施产物、未向 wp04-t07 推送任何提交；送验对象保持冻结于 448d2e4f）。
