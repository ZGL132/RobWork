# WP-13-T07 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | WP-13-T07 实现六轴模板创建与参数化编辑（unit=modeling，interUnit=false；knownPitfalls=O-27） |
| 送验对象（冻结） | 分支 `wp13-t07` 尖端 `7156ff13f7fc49f076663c6716a53c27252d677d`（验收全程按该 SHA） |
| 分支漂移检查 | 验收启动时 `git fetch origin` 后 `origin/wp13-t07` = `7156ff13…` ＝ 送验 SHA ＝ 本地 `wp13-t07`，**零漂移**（ACC §3） |
| diff 范围 | `5e9719aae0bbb53520561974bdbd9100961973c2..7156ff13f7fc49f076663c6716a53c27252d677d`（3 个提交：a1c13dcc 判定提取与诊断码登记＋abde6724 模板实现＋7156ff13 文档同步与留痕；base＝验收请求指定，即 wp13-t06 合入收尾提交、`origin/redesign-main` 尖端） |
| **verdict** | **pass**（4.1～4.11 全部通过，无阻断级问题；建议级 4 项转登 findings F-312/F-313/F-314/F-315） |
| 验收执行方式 | 验收者全新上下文独立复现：detached worktree @ 送验 SHA 亲手重配重跑全部构建/测试/脚本/门禁与两处变异注入；未采信实施者任何运行结果（ACC §2 四条） |
| 复现现场 | `D:/10_Source_Repos/21_robot/acc-t07-A`（detached @ 7156ff13，集成模式 build＋冒烟模式 build-smoke-acc）；`D:/10_Source_Repos/21_robot/acc-t07-C`（detached @ 5e9719aa，门禁 base 对照专建＋变异备份）；记录回传于 `D:/10_Source_Repos/21_robot/acc-t07-B`（分支 `acc/WP-13-T07/1`） |
| evidence 分支 | `acc/WP-13-T07/1`（基于 `origin/redesign-main` = 5e9719aa；记录 commit＝本分支尖端 40 位 SHA，由编排者登记三元组） |

---

## 4.1 改动范围 —— 通过

`git diff --name-status 5e9719aa..7156ff13` 共 34 文件，逐一对契约 `allowedFiles` 核对：

- `RobWork/RobWorkStudio/src/rwslibs/industrialrobot/modeling/**`：12 文件（CMakeLists.txt 改；include/…/{DiagCodes,Errors,Import}.hpp 改、Template.hpp 新增；src/{DiagCodes,Errors,Import}.cpp 改、Template.cpp 新增；test/{DiagCodesTest,ErrorsTest}.cpp 改、TemplateTest.cpp 新增）——全在 modeling/** 内。
- `RobWork/doc/industrial-robot-design/units/modeling.md`（改，升 v0.8）——在 allowedFiles。
- `RobWork/doc/industrial-robot-design/traceability/**`：21 文件（builds/wp13-t07/ 留痕：21 件＝配置/构建/ctest/双模式用例运行日志＋gtest XML 四件＋ird-test-report 四件＋门禁直跑 base/branch 与命中集 diff＋validate-task/validate-docs 日志）——在 allowedFiles `traceability/**` 内。验收者 `git diff --name-only … -- traceability/builds/wp13-t07 | wc -l` 实测 **21**，与提交 7156ff13 正文「留痕 21 件」一致（本任务该计数无出入；出入项见 4.9 用例计数）。
- **forbiddenFiles 零触碰**：REQUIREMENTS.md、ARCHITECTURE.md 不在 diff；任务契约 WP-13-T07.json 未被触碰（status 翻转归治理收尾——F-289 反例未再现）。
- **SA-02**：diff 无 modeling 之外任何框架源码文件；`industrialrobot/patches/` 无新文件（base..head 对 patches 路径 diff 为空）；diff 零 vcpkg 文件。

## 4.2 可复现构建 —— 通过（两模式均验收者亲手从零重配重跑）

- **集成模式**（acc-t07-A，源＝detached worktree @ 7156ff13）：
  - 配置：`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON` ＋ 主树 vcpkg toolchain ＋ Qt 前缀（与主树缓存同参）。结果 `Configuring done`；缓存确认 **`RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`**。
  - 环境前置（非本任务产物缺陷，F-007/F-009/F-200 已登记同族，本次再次实证）：①worktree 内无 `vcpkg/`（不入库）——toolchain 指向主树 vcpkg 复用其已装包；②`RobWorkStudio/bin/RobWorkStudio.ini.template.static` 被 `.gitignore` 的 `[Bb]in/` 规则吞掉不入库，首次配置报 `CMake Error: File … does not exist`——从主树复制该 gitignored 环境件后配置通过。
  - 任务相关目标构建（验收者亲手）：`sdurws_ird_modeling`、`sdurws_ird_modeling_test`、`sdurws_ird_modeling_contract_test` 三目标退出码均 **0**，MSVC `error C/LNK/fatal` 计数 **0**。
  - 验收者亲跑两 exe：`sdurws_ird_modeling_test` → **119/119 PASSED**（含 `MdlTemplate.*` 15 条全过）；`sdurws_ird_modeling_contract_test` → **5/5 PASSED**。与在库留痕 sdurws_ird_modeling_test-integration.xml（`tests="119" failures="0"`）、ird-test-report-integration.json（summary 119/0）一致。
- **独立冒烟模式**（acc-t07-A/build-smoke-acc，`-S RobWork/RobWorkStudio/src/rwslibs/industrialrobot` ＋ `-DCMAKE_TOOLCHAIN_FILE=<主树 vcpkg>/scripts/buildsystems/vcpkg.cmake` ＋ Qt `CMAKE_PREFIX_PATH`——AGENTS §4.1 与 F-254 修正口径）：配置 done（退出码 0）；全量构建退出码 **0**、错误计数 **0**（全部 20 单元测试 exe 齐产出）。冒烟亲跑：`sdurws_ird_modeling_test` → **119/119 PASSED**；`sdurws_ird_modeling_contract_test` → **5/5 PASSED**。

## 4.3 契约 verify 命令 —— 逐条亲手执行（6/6 成功；门禁按"零新增命中"口径判定）

| # | 命令 | 结果 |
| --- | --- | --- |
| 1 | `cmake --build build --config Release --target sdurws_ird_modeling` | 退出码 0，零错误 |
| 2 | `cmake --build build --config Release --target sdurws_ird_modeling_test` | 退出码 0，零错误 |
| 3 | `ctest --test-dir build/RobWorkStudio/src/rwslibs/industrialrobot/modeling -C Release -L "^ird$"` | **2/2 passed**（unit＋contract_test）。验收者另亲补跑了契约清单未列的 `--target sdurws_ird_modeling_contract_test`（退出码 0）——契约 verify 缺该构建目标行与 F-300（T04）/F-304（T05）/F-310（T06）同型**第四例**，转登 **F-314**；非实现缺陷，验收者已亲证三目标齐构建） |
| 4 | `cmake --build build --config Release --target ird_gates` | 门禁引擎运行后按 SA-10 以退出码 1 终止——命中集为**存量基线红**（52 处存量命中，在案 F-019/F-115/F-173/F-221/F-227/F-292/F-302 等族）。验收者专建 base 对照（detached worktree @ 5e9719aa）**两侧均以 `cmake -DIRD_ROOT=<绝对路径> -DIRD_SELFTEST_DIR=<临时> -P cmake/ird_gates.cmake` 直跑同脚本**（configure-free）：base 侧 `IRD-GATE-` 行 111 vs head 侧 111，路径归一化（剥离 worktree 前缀）后 `sort+diff` **多重集 diff 为空＝零新增命中**——与送验留痕 gates-hit-set-diff.txt「零新增」结论一致，且为验收者独立复现。modeling 面相关命中恰 3 条均为在案存量：IRD-GATE-LIB pugixml::pugixml（F-302）＋IRD-GATE-SUB ×2 两条 modeling 测试目标直链 testkit（F-292） |
| 5 | `pwsh -File RobWork/scripts/industrialrobot/validate-task.ps1 -TaskFile …/WP-13-T07.json` | `validate-task: PASS (1 tasks)`，退出码 0 |
| 6 | `pwsh -File RobWork/scripts/industrialrobot/validate-docs.ps1` | `validate-docs: PASS (20 units, 12 trace entries, 186 task files)`，退出码 0 |

## 4.4 acceptance 条目逐条证据 —— 5/5 有对应证据（行号以送验 SHA 内容为准，验收者亲读亲跑）

| acceptance 条目 | 证据 |
| --- | --- |
| 1. Template.hpp/.cpp 落位（卡 §3.3 表 T07 行）：IRobotDesignTemplateFactory 按卡 §9.4.2 签名（listTemplates/createDraft）；模板清单三行——generic-6r（6×Revolute、Explicit、R1 可用）/generic-7r（enabled=false，P-03 七轴数值未冻结仅登记不启用）/custom-chain（逐轴定义）；createDraft 产出满足 I-MDL-1～12 的工作集、不触达 project、不产生修订 | Template.hpp 落位 `include/sdurws/ird/modeling/`（卡 §3.3 表 T07 行；卡 v0.8 §3.3/§14.6 同步登记）。签名对照：listTemplates＝Template.hpp:303 `virtual std::vector<TemplateDescriptor> listTemplates() const`；createDraft＝:359-362 `(const TemplateId&, runtime::InstallationPresetToken, std::string_view, std::vector<core::DiagnosticRecord>&) const`——与卡 §9.4.2 一致（落位形态偏差：直返→TemplateOutcome 两态，已按 DTB §5.4 随卡 §14.6 v0.8 ④a 登记，见 4.8）。清单三行：`ListTemplates_ThreeRowsPerSection51_WP13T07_ACC1`（TemplateTest.cpp:172-207）逐行断言 id/axisCount/authority/预设三值/enabled（六轴 true：186；七轴 false：194——note 含 "P-03"：196；custom-chain axisCount=nullopt：201）＋行序＝卡表行序＋清单确定性（206）。I-MDL-1~12：`CreateDraft_Generic6R_WorkingSetInvariants_WP13T07_ACC1`（:215-270）调 `checkInvariants` 零违例（230-233）＋6 关节/7 连杆/链序（236-243）＋BasePlacement 预设与零位地面带模板来源标记（257-269）。不触达 project/不产生修订：工厂纯函数面（Template.cpp 无文件/时钟/环境访问，验收者 include 面与逐行核对；`CreateDraft_DeterministicTempHandles_WP13T07_ACC1`（:324-343）同输入两次创建工作集逐字段相等——纯函数性直接证据）。 |
| 2. 七轴登记不启用 UT（V-02，O-27/P-03）：listTemplates 返回 enabled=false；createDraft(generic-7r)→TemplateDisabled 错误码＋提示诊断，不静默替换为六轴、无半成品 | `CreateDraft_Generic7R_DisabledBlocked_WP13T07_ACC2`（:356-395）：outcome.ok()==false（367）＋code==TemplateDisabled（368）＋错误 params 含 template-id=generic-7r／freeze-gate=P-03（374-383）＋**恰一条** MDL-TEMPLATE-DISABLED 提示诊断且与 DiagCodes.hpp `kMdlTemplateDisabled` 常量同源对账（387-389）＋与 TEMPLATE-RANGE 语义不混用（390-391）＋错误态 `outcome.get()` 抛 logic_error＝无半成品/无静默替换（394）。实现面：enabled 门先于建链（Template.cpp:436-453）——拒绝路径无任何七轴建链动作（:493-503 七轴不可达建链分支，含实现缺陷护栏）；诊断码登记链完整（DiagCodes.cpp:232-250 描述符表尾追加＋Errors.cpp:87-88 映射行＋DiagCodesTest 工厂封闭性八码同步）。4.5 变异实证：旁路 enabled 门后本用例**变红**。 |
| 3. 六轴默认参数表 T-MDL-1 UT（设计默认值——D-MDL-7，非上游冻结需求值）：逐轴 axis/zeroOffset/bounds/workingRange/maxVel/maxAcc 按卡 §5.1 表值（单位 m/rad/rad·s⁻¹/rad·s⁻²）；J6 行程 [−2π,+2π] 恰在 4π 阈值边界（附录 D 第 11 项）默认通过行程校验兼作边界用例；数值锁定随 WP-13-T16 mdl-template-6r 黄金数据集 | `SixAxisDefaults_TableValues_WP13T07_ACC3`（:406-433）：测试内**独立抄写**卡 §5.1 表（expectedTable() :110-125 与实现各自转写互为核对）后六行×九列（type/axis xyz/zeroOffset/bounds 双端/maxVel/maxAcc）位级精确比对（不用容差——两侧同为 π 整倍数字面量）＋每行自洽（qmin<qmax：428；速度/加速度有限：430）。`SixAxisDefaults_J6TravelAtThresholdBoundary_WP13T07_ACC3`（:442-457）：J6 qmin=−2π/qmax=+2π 位级＋行程 `qmax−qmin` 位级恰等 4π（454-456——附录 D 第 11 项"含于合规侧"边界事实；阈值常量归 policy 不本地私设，ARC-05）。`CreateDraft_JointFieldsFromTable_WP13T07_ACC3`（:465-503）：草稿逐关节字段来自表值（473-485）＋workingRange=NotApplicable（:240-242 ACC1 内）＋材料种子钢取 §5.3 默认表单源对账（489-496——实现不写第二处 7850 字面量）＋物性 mass/com/inertia 全 NotProvided 不猜测（499-501）。4.5 变异实证：篡改 J3 表值后 TableValues 用例**变红**。 |
| 4. 创建→编辑链路 UT（MDL-01、AT-20/AT-01 建模侧）：generic-6r→createDraft（BasePlacement 预设＋来源标记 Template）→逐轴编辑（类型/轴线/零位/限位/速度/加速度——§5.2 字段级/批量变体、批量产出单条变更摘要）→buildChangeSummary；4/5 轴与含 prismatic 链创建入口阻止＋提示（§6.4 维度二判定复用、§2.1 创建列 ❌）；应用链路经 apply-robot-design 随 WP-13-T08 集成、V-01 全链随 WP-13-T16 收口 | 创建起点：ACC1（BasePlacement 预设＋来源标记 template/<id>，:263-269）。字段级编辑：`CreateEditChain_FieldLevelEdits_WP13T07_ACC4`（:516-589）——轴线/零位/限位/类型四字段接受路径（528-556，每次恰一条变更记录）＋拒绝面四类（非有限 562-567/零轴 569-573/坏区间 575-578/Continuous 冲突 580-583）＋**拒绝后工作集与基线 `operator==` 逐字段相等（字节不变）且记录数不变**（585-588）。权威锁：`CreateEditChain_AuthorityLockedAxis_WP13T07_ACC4`（:596-614，C-1 复用 authorityEditGuard 单一判定——StandardDH 态 axis 拒绝＋工作集不变）。批量变体：`CreateEditChain_BatchAndChangeSummary_WP13T07_ACC4`（:622-666）——4 行接受＋1 行拒绝（appliedCount=4/rejectedRows=[4]/lastError=ValueNotFinite）、拒绝行保留原值不连带回滚（646-649）、**恰一条**批量变更记录（652-655，UX-05）、buildChangeSummary 确定性格式＋空集空串（658-665）。速度/加速度编辑面：无 schema 落点（卡 §4.3-A 未登记，v0.4 ③c"不发明字段"纪律）——数值面由 SixAxisJointSpec 承载（Template.hpp:142-149 类型注）并在 ACC3 钉住表值；偏差已随卡 §14.6 v0.8 ④e 登记（4.8），字段级编辑随 schema 澄清表尾追加——**契约允许的登记偏差**，非缩水。创建入口阻止：`CreationEntryGuard_BeyondRangeBlocked_WP13T07_ACC4`（:675-723）——5 轴链阻止＋reason 含 "4/5"（684-690）＋含 prismatic 链阻止（694-701）＋6/7 轴放行无诊断（704-716）＋**与 judgeChainCapability 同源对照逐字段相等**（720-722——§6.4 单一实现实证）。应用链路 T08/V-01 T16 收口＝契约明示的触发式延后（acceptance 4 原文），豁免依据成立。 |
| 5. 几何生成辅助两条 UT（§5.2 v0.2）：连杆占位圆柱生成（相邻关节原点连线、确定性纯函数）与碰撞引用复制辅助（视觉引用→同资源碰撞引用）——产物均为普通 GeometryRef 同权同校验、来源 GeometricEstimate（methodTag 区分）；不引入网格重画/凸包简化算法（上游未授权） | `PlaceholderCylinder_SegmentGeometry_WP13T07_ACC5`（:736-806）：+z 连线恒等旋转＋中心=中点（742-757）；−z 特例 diag(1,−1,−1)（760-766）；一般方向 (3,−4,0) 的 R·z0=单位方向与 R 正交性均 1×10⁻¹²（768-793）＋产物 kind=Primitive（746）＋来源 GeometricEstimate＋methodTag "link-placeholder-cylinder"（797-799）＋同输入两次调用产物逐字段相等（802-805 确定性）。输入面：`PlaceholderCylinder_RejectsBadInput_WP13T07_ACC5`（:812-832）半径零/负/NaN、起终点重合、空资源键五类 fail-fast。碰撞复制：`CopyVisualToCollision_SameResourceRef_WP13T07_ACC5`（:840-874）同 resourceRefId＋位姿逐元素同原引用＋同 kind（856-868）＋来源 methodTag "collision-copy" 与占位圆柱区分（871-873）；不重画/不凸包简化＝实现即复制语义无任何网格算法（Template.cpp:803-813 验收者逐行核对——上游未授权边界守住）。 |

未执行项标注：无——全部 acceptance 面均执行；`ird_gates` 按 4.3 如实记录为"存量基线红＋验收者独立比对零新增命中"，不标注"零命中"。

## 4.5 测试真实失败能力 —— 通过（两处变异实证后复原）

| # | 变异（Template.cpp） | 预期红用例 | 实测 |
| --- | --- | --- | --- |
| M1 | J3 行 bounds `{-kPi, kPiHalf}` → `{-kPiHalf, kPiHalf}`（表值篡改） | `SixAxisDefaults_TableValues_WP13T07_ACC3` | 重编译后运行：`[  FAILED  ] …SixAxisDefaults_TableValues_WP13T07_ACC3`——**红**（表值独立抄写比对真实生效） |
| M2 | enabled 门 `if (!descriptor->enabled)` → `if (false)`（七轴静默放行旁路） | `CreateDraft_Generic7R_DisabledBlocked_WP13T07_ACC2` | 重编译后运行：generic-7r 落入不可达建链护栏抛 logic_error，用例 `error: C++ exception … thrown` ＋ `[  FAILED  ] …ACC2`——**红**（V-02 拒绝面真实生效） |
| 复原 | 恢复原始 Template.cpp（`git diff` 空＝与送验 SHA 逐字节一致）＋touch 强制重编（防 F-068/F-075 增量陷阱） | 全套 | `119 tests … [  PASSED  ] 119 tests.`——**复原绿** |

两处变异均证明测试校验行为与边界而非恒真；送验对象在验收全程保持冻结（变异仅在验收者私有 worktree A 内进行并已复原）。

## 4.6 架构红线 —— 逐条自查通过（AGENTS §5 八条）

1. **L2 计算内核零 Qt**：modeling 全目录 `grep "#include <Qt|#include <Q"` 零命中；ird_gates R3 零新增（存量 R3 命中全部在 ui 单元）。
2. **业务域互链（R-1）**：Template.hpp include 面＝core（DiagData/Provenance/Digest）＋modeling 本单元（Errors/Import/RobotDesign/PropertyEstimation/DiagCodes）＋runtime（Errors.hpp 的 Expected 模板——Codec/PropertyEstimation 已有先例的登记边公共设施）；ird_gates 目标链零新增命中（4.3#4 实证 base..head 集合不变）——无第二份跨单元产品依赖。
3. **跨单元私有头（R-2）**：新增/改动文件 include 全部为 `include/sdurws/ird/<unit>/` 公共头；`ird/modeling/src/`、相对路径私有头扫描零命中。
4. **框架零修改（SA-02）**：4.1 已证 diff 无框架文件、patches/ 无新增。
5. **不做名称前缀拼接（R-4）**：R4 门禁唯一命中为 project 单元存量（ProjectStoreImpl.cpp，在案）；modeling 新增代码零 "RobWork" 字面量操作。
6. **权威唯一（PA-1）**：模板工厂只产出草稿工作集（不触 project、不产生修订——4.4#1 纯函数面证据）；诊断码/文案登记于 DiagCodes（core/diagnostics 面）；命令入口未越权（无 apply/命令面代码）。
7. **不可变历史（PA-2）**：变更记录 append-only（ModelingChangeRecord 注＋appendChangeRecord 仅 push_back）；拒绝路径工作集字节不变（ACC4 实测）。
8. **不继承 old/ 历史**：新增/改动文件 `old/` 引用扫描零命中；实现为按卡面从零编写（ judgeChainCapability 为同单元内行为保持提取，ImportTest 既有断言钉住——非 old/ 恢复）。

## 4.7 注释规范 —— 通过（公共头全查＋实现文件抽查 4 个，超 ≥3 下限）

- **公共头全查**（Template.hpp 660 行逐段＋DiagCodes.hpp/Errors.hpp/Import.hpp diff 逐块）：文件头注含设计依据（卡 §5.1/§5.2/§9.4.2/§6.4/§3.3、需求 MDL-01/04/22、P-03/O-27、任务契约）＋背景说明（工厂在 PM-01 创建流程中的位置）＋P-03 纪律＋线程安全/确定性声明；每个类型/函数有 Doxygen 中文 @brief＋@param 方向/单位（rad、rad·s⁻¹、rad·s⁻²、m、无量纲轴）＋@return/@throws＋错误语义归类（调用方违约 fail-fast vs 业务拒绝值面）；§2.5 高危信息表逐项在位（单位/坐标系——连杆系/geom 系注明；rad 制式显式；所有权——纯值/调用方所有；线程约束——ModelingWorkingSet 非线程安全仅 UI 线程；错误语义——TemplateDisabled→MDL-TEMPLATE-DISABLED 映射链注明的归类；确定性——临时句柄 SHA-256 派生同键同句柄）。
- **实现文件抽查**：Template.cpp（816 行逐段——文件头实现总纪律、每函数注释含设计出处、魔法数 π 字面量来源注、Rodrigues 展开逐步推导注、步①~步⑤检查序设计意图注）、TemplateTest.cpp（比对口径独立抄写注、每用例头注含 acceptance/需求/AT 追溯）、DiagCodes.cpp（T07 行描述符逐字段对齐 §9.5 增登行的注释）、Errors.cpp（映射两行的阶段纪律注释）——均无复述式废注、无中英机翻腔、无过期注释；TODO 均带任务编号或不存在。
- **测试追溯字段**：15 个 MdlTemplate 用例全部携带 `IRD_TEST_INFO(需求列, AT 列)`（testkit 登记设施），用例名含 `WP13T07_ACCn` 段——4.11③ 逐条证据表可机械对照。

## 4.8 文档同步 —— 通过（实现与任务卡零偏差；七项偏差/澄清已按 DTB §5.4 登记；findings 相关性已核对）

- **单元卡增量修订**：modeling.md 升 v0.8（卡头 ：7；§3.3 T07 行 ：257；§5.1 清单表 ：444-446 与 T-MDL-1 表 ：448-456；§9.4.2 签名 ：975-999；§9.5 表尾增登 MDL-TEMPLATE-DISABLED 行 ：1185——表行序追加于表尾符合登记簿纪律；§14.6 v0.8 变更记录行 ：1504 含实现落位面＋②judgeChainCapability 提取＋③码面登记＋**④七项偏差与澄清逐条登记**＋⑤验证留痕汇总）。验收者逐项核对实现与卡 v0.8 ④a~④g：两态落位/ModelingWorkingSet 放置/确定性临时句柄/custom-chain 种子/速度加速度边界/几何种子边界/名与原点种子——与代码一一相符，**无未登记偏差**。
- **需求/任务追溯面**：契约 requirements（MDL-01/MDL-04）在卡 §12 追溯矩阵行（:1373/:1376）任务列已含 T07（编译期既有，无需本任务改动）；验收（V-01/V-02）行承载一致。
- **ird_gates 命中集多重集比对**：验收者独立复现 base..branch 零新增（4.3#4）——实施侧留痕 gates-direct-{base,branch}.log（各 122 行）与 gates-hit-set-diff.txt 的方法与结论均复现成立。
- **findings.json 未关闭条目相关性核对（ACC v1.2，254 条 open 全量过目）**——与本任务相关且本任务面出现的存量条目：
  - **F-292**（modeling 两测试目标直链 testkit SUB 存量）：本任务门禁复现中原样在册，零新增——**不消账**（处置归门禁词表治理）。
  - **F-302**（sdurws_ird_modeling 链 pugixml::pugixml LIB 存量）：同上原样在册——**不消账**（O-40 依赖已登记，词表暴露归治理）。
  - **F-300/F-304/F-310 家族**（契约 verify 缺 contract_test 构建行）：本任务契约 **第四例再现**——转登 **F-314**。
  - **F-012/F-293/F-298/F-299/F-303/F-308 家族**（送验提交标题名词开头）：本任务 7156ff13 **再现**——转登 **F-313**。
  - **F-207/F-231/F-309 家族**（gates 留痕双重编码乱码）：在库 gates-ird-gates-branch.log **再现**——转登 **F-315**。
  - **F-041/F-080/F-236/F-249/F-256/F-311 家族**（散文计数失准）：本任务用例计数 13 vs 实测 15 **再现**——转登 **F-312**（4.9 详述）。
  - **F-305/F-306/F-307**（Import.cpp 既有面：kind 复用/mesh 承载形态/阅读地图步序注）：本任务对 Import.cpp 为纯函数提取（行为保持、4.4/4.6 已证），上述面未被触及也未被恶化——**不顺手消账不构成积压**（非本任务因果面），留原状跟踪。
  - **F-007/F-009/F-200**（worktree/全新树环境前置）：本次验收再次实证（vcpkg 与 ini 模板件两处），结论与在案条目一致——**不消账**，为环境链家族再证。
- **流水线状态**：契约 status 仍为 ready（翻转归治理收尾——正确）；state.json 不在送验 diff 内（实施侧无越权簿记——F-289/F-294 反例未再现）。

## 4.9 commit 质量 —— 通过（四段式齐备、验证结论与验收者独立复现逐项吻合；两处建议级转登）

- 三笔提交（a1c13dcc → abde6724 → 7156ff13）均含 `[WP-13-T07]` 前缀＋四段正文（What/Why/验证/影响面），附 `Acc-Covered` 行；代码与测试同提交（abde6724 含 Template.hpp/.cpp＋TemplateTest.cpp＋CMakeLists），留痕与文档同步单独成笔（7156ff13）——与 T05/T06 既定验收先例同型。
- **验证段与留痕一致性**（验收者逐条对照独立复现结果）：「119/119＋契约 5/5」＝验收者亲跑一致；「ird_gates 零新增（52＝52）」＝验收者归一 diff 空一致；「validate-task/validate-docs 双 PASS」一致；「冒烟带 vcpkg toolchain＋Qt 前缀」与 smoke-configure.log/复现口径一致——**无粉饰**。留痕件数「21 件」实测一致（本任务该家族面上无出入）。
- 建议级两处：①7156ff13 标题前缀后以名词「单元卡」开头非动词开头（正文四段齐备）——F-012 家族再现，转登 **F-313**；②abde6724/7156ff13 与卡 §14.6 v0.8 均称「新增 13 条 MdlTemplate 用例」，验收者 `--gtest_list_tests` 实测 MdlTemplate 段 **15 条**（119＝base 104＋15；TemplateTest.cpp 在两提交均为 15 个 TEST 宏）——方向少报、无证据缺失，F-041 家族再现，转登 **F-312**。两项均不构成"验收粉饰"（无失败隐瞒、无结论伪造），按 T06 先例（F-308/F-311）建议级处置。

## 4.10 留痕完整 —— 通过

- 在库留痕 21 件（builds/wp13-t07/）：gtest XML 四件（modeling/contract × integration/smoke，`tests="119" failures="0"` ／ `tests="5" failures="0"`——与验收者亲跑一致）；ird-test-report.json 四件（integration summary 119/0 decisive=true、contract-integration 5/0、smoke 同）；集成与冒烟配置/构建/ctest/用例运行日志；门禁直跑 base/branch 日志＋命中集 diff；validate-task/validate-docs PASS 日志——**落位齐备且内容与验收者独立复现一致**。
- `phase-one-validation.log 或 resultNote` 条款：phase-one-validation.log 不存在（该文件早已不在追踪树）；ird-test-report.json 无 resultNote 字段——本项为 ACC §4.10 条文与流水线实践脱节（**F-201 已在案**），按在库实践以 gtest XML＋ird-test-report.json＋构建/脚本日志全链留痕判定，与 T04/T05/T06 验收先例同口径；不构成缺项。
- 在库 gates-ird-gates-branch.log 存在双重编码乱码（F-207/F-231/F-309 家族再现）——**载体编码问题非数据问题**，门禁结论经验收者绕开该载体直跑独立成立；转登 **F-315**。

## 4.11 偷懒/缩水实现扫描 —— 通过

1. **违例标记扫描**：diff 范围新增行 `TODO|FIXME|HACK|XXX|stub|placeholder|not implemented|未实现|待实现|占位实现` 扫描——除契约 acceptance 5 本身要求的"占位圆柱/占位原语"特性命名（makeLinkPlaceholderCylinder/kPlaceholderCylinderRadius/"placeholder-radius" 等）外**零命中**；无契约外触发式延后。
2. **空实现检测**：公共接口 15 个函数/方法（工厂两方法、编辑流两变体、两 token 函数、buildChangeSummary、sixAxisTemplateDefaults、creationEntryGuard、judgeChainCapability、几何辅助两函数等）实现体全部为完整逻辑（验收者逐行通读 Template.cpp 816 行＋Import.cpp 提取段）；无"仅返回默认值"函数；错误路径/拒绝路径均实质实现。
3. **逐条证据表**：4.4 表 5/5 条均有具名测试（15 条 MdlTemplate 按 ACC1~5 全覆盖）＋文件行号＋验收者亲跑输出——无"已实现"声明依赖。
4. **测试强度**：断言校验行为与边界（表值位级比对、拒绝字节不变、确定性逐字段相等、恰一条记录/诊断、阈值边界 4π、正交 1×10⁻¹²）；非恒真——4.5 两处变异实证会红；与既有用例回归共存（119 全绿含 104 条存量）。

## 发现的问题清单

**阻断级**：无。

**建议级（4 项，已逐条转登 traceability/findings.json，编号顺延自 F-311）**：

| 编号 | 摘要 | 家族 |
| --- | --- | --- |
| F-312 | 提交与卡 §14.6 v0.8 称「新增 13 条 MdlTemplate 用例」，实测 15 条（119＝104＋15）——少报、无粉饰 | F-041/F-080/F-236/F-249/F-256/F-311 |
| F-313 | 7156ff13 标题「单元卡 v0.8 文档同步与任务验证留痕」名词开头非动词开头 | F-012/F-293/F-298/F-299/F-303/F-308 |
| F-314 | 契约 verify 数组缺 sdurws_ird_modeling_contract_test 构建行（全新树按清单字面执行 ctest 首跑将 Not Run——验收者已亲补验证） | F-300/F-304/F-310 第四例 |
| F-315 | 在库 gates-ird-gates-branch.log 双重编码乱码（UTF-8→GBK 误读再编码），门禁结论经直跑独立成立——载体编码问题 | F-207/F-231/F-309 |

## 验收环境说明

- 验收者全新上下文，仅消费契约/设计文档/在库留痕/送验 diff 与自建复现产物；未读实施者任何过程叙述（ACC §2）。
- 复现现场（验收者本地临时目录，验收记录推送后清除）：worktree A `D:/10_Source_Repos/21_robot/acc-t07-A`（detached @ 7156ff13；集成 build/＋冒烟 build-smoke-acc/）；worktree C `D:/10_Source_Repos/21_robot/acc-t07-C`（detached @ 5e9719aa；门禁 base 对照＋变异备份）；evidence worktree B `D:/10_Source_Repos/21_robot/acc-t07-B`（分支 `acc/WP-13-T07/1`）。
- 工具链：VS 2022（MSBuild 17.12）x64、CMake 4.3、Qt 6.11.1（CMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64）、vcpkg 经典模式（主树 toolchain）、pwsh 7；环境前置两处（worktree 无 vcpkg 目录、gitignored ini 模板件缺失）按 F-007/F-200 在案口径从主树补齐后复现成功——非送验对象缺陷。
- 送验对象全程冻结：变异注入仅在验收者私有复现树内进行，结束后源码复原至与 7156ff13 逐字节一致（`git diff` 空）；未向 wp13-t07 推送任何提交，未改动任何实施产物。
