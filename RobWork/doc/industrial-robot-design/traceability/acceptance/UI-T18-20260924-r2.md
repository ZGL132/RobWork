# UI-T18 验收记录（attempt 2，2026-09-24）

| 字段 | 值 |
| --- | --- |
| 任务 | UI-T18（WP-10-T18）Dock 拓扑拆分＋状态行迁移宿主状态栏（O-43 裁决③承接） |
| 送验对象（冻结） | `f00fb104a7257911083e5eafff2a5186cec5db00`（分支 wp10-t18；`origin/wp10-t18` == headSha，无分支漂移） |
| diff 范围 | `feb09471b12d78fcb4e30f269afc03258f1b71ad..f00fb104a7257911083e5eafff2a5186cec5db00`（6 提交：0e54c27b 实施 → 3cb5e96e 合并基线 → d7e4a243/840375b3/f6d1a235/f00fb104 返工） |
| 前次验收 | attempt 1 fail（B-1 范围越界＋B-2 宿主 GUI 冒烟未执行；记录 `UI-T18-20260924.md` @ acc/UI-T18/1） |
| **verdict** | **pass**（4.1~4.11 全部通过；无阻断级问题；建议级 2 项转登 findings.json F-335/F-336，不阻塞合入） |
| 验收者环境 | 全新上下文；复现现场＝detached worktree `D:\wt-acc-uit18-a` @ f00fb104（集成模式全新 configure：`-G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON`＋vcpkg toolchain `D:/10_Source_Repos/21_robot/RobWork/vcpkg/scripts/buildsystems/vcpkg.cmake`＋`CMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`——与主仓库既有集成树同口径；缓存确认 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`）；基线门禁现场＝detached worktree `D:\wt-acc-uit18-c` @ feb09471；独立冒烟＝`D:\wt-smoke-uit18`（vcpkg toolchain＋Qt 前缀）。环境资产说明：`vcpkg/` 与 `RobWorkStudio.ini.template.static` 均为 git 未跟踪的本地构建资产，worktree 复现时按主仓库同值补齐（非送验内容）。记录与转登落 `acc/UI-T18/2`（本分支）。 |

---

## 1. 逐项证据（ACC §4，4.1~4.11）

### 4.1 改动范围 —— 通过

- `git diff --name-status feb09471..f00fb104`：68 个变更文件（M 8＋A 60），逐一对照契约 `allowedFiles` 三模式：
  - `RobWork/RobWorkStudio/src/rwslibs/industrialrobot/ui/**`：8 个 M（WorkbenchContent.hpp/.cpp/`_p.hpp`、WorkbenchShell.cpp/`_p.hpp`、UiPlugin.hpp/.cpp、gui_test/WorkbenchContentGuiTest.cpp）；
  - `RobWork/doc/industrial-robot-design/units/ui.md`：1 个 M（v1.14/v1.14-r2 登记）；
  - `RobWork/doc/industrial-robot-design/traceability/**`：59 个 A（builds/wp10-t18/ 留痕）。
- `forbiddenFiles`（REQUIREMENTS.md / ARCHITECTURE.md / development-task-breakdown.md）零触碰；attempt 1 阻断 B-1 的仓库根 `traceability/` 3 件越界 XML 已由 840375b3 删除（头提交处仓库根无 traceability/ 目录）。
- SA-02：diff 无 `patches/` 新文件，无工业robot/ui 以外任何框架源码改动。

### 4.2 可复现构建 —— 通过（附环境观察）

- **集成模式（验收者全新 configure＋全量构建）**：`RWS_BUILD_INDUSTRIALROBOT:BOOL=ON` 确认后全量 Release 构建——industrialrobot／RobWorkStudio 全部目标**零错误**；全树仅 RobWorkSim 两个框架插件目标失败（RWSimulatorPlugin 71 条＋bt_plugin 2 条，min/max 宏冲突家族）——与在册 F-301/F-323 已登记的全新树扩展观察完全同源，与本任务改动无关（`grep ': error' | grep -ci 'industrialrobot|rwslibs|RobWorkStudio'` = 0）。此前提下契约 verify[0] 六目标独立构建零错误（exit=0）。
- **独立冒烟模式（验收者全新目录 `D:\wt-smoke-uit18`）**：vcpkg toolchain＋Qt 前缀 configure 零错误；Debug 全树构建零错误（exit=0）；Release 全树构建零错误（exit=0）。
- 环境观察（不判不通过）：①fresh worktree 需手动补齐两个 git 未跟踪资产（vcpkg 目录、`RobWorkStudio.ini.template.static`——F-007 已登记模板供给口径）；②冒烟 configure 需 `-DCMAKE_PREFIX_PATH` 指向 Qt（AGENTS §4.1 冒烟命令模板只写了 toolchain 参数，照抄会因 Qt 缺失失败——F-007 同族，已在 findings 留痕范围）。

### 4.3 契约 verify 命令 —— 4/4 通过（第 4 条按证据裁决，见 §2）

1. `cmake --build build --config Release --target sdurws_ird_ui sdurws_ird_ui_plugin sdurws_ird_ui_app sdurws_ird_ui_test sdurws_ird_ui_gui_test sdurws_ird_ui_contract_test` → exit=0，六目标全部产出，零错误。
2. `ctest --test-dir build/RobWorkStudio/src/rwslibs/industrialrobot/ui -C Release -L "^ird$"` → **2/2 通过**。
3. `ctest … -L "^ird_gui$"` → **1/1 通过**。
4. 宿主 GUI 冒烟：AskUserQuestion 窗口问询未获所有者答复→验收者未亲驱真机；按送验头内 -r4 冒烟证据裁决为成立（详见 §2）。

### 4.4 acceptance 三条逐项核对 —— 逐条有证据（证据表见 §3 4.11③）

### 4.5 测试真实失败能力 —— 通过（2 断言实证会红）

对 `WorkbenchContentGuiTest.TwoPhaseAssemblyContract_UI_SPLIT` 的两条断言做破坏性验证（改动仅存在于验收现场，已还原并核对 `git status` 零残留，重建后 35/35 复绿）：

1. **PM-11 永久文本投影断言**（`EXPECT_FALSE(pm11Text.isEmpty())`）：注释 `refreshStatusBar()` 内 `m_statusTextObserver(text)` 调用→重建→`[FAILED] …TwoPhaseAssemblyContract_UI_SPLIT`（Actual: true）。测试确实会红。
2. **瞬态消息投影断言**（`EXPECT_TRUE(transientMessage.contains("未知命令"))`）：注释 `showStatusFeedback()` 内 `m_statusMessageObserver(message, timeoutMs)` 调用→重建→`[FAILED]`（Actual: false）。测试确实会红。

### 4.6 架构红线（AGENTS §5 八条） —— 逐条自查通过

1. **L2 计算内核零 Qt**：core/evidence/policy/runtime 源码与公共头 grep `#include <Q` 零命中；ui 为唯一 Widgets 允许目标。
2. **业务域互链（R-1）**：本任务改动仅 ui 单元；无业务域单元被触碰，无新增业务域互链。
3. **跨单元私有头（R-2）**：ui 单元引用他单元头全部为 `include/sdurws/ird/<unit>/` 公共头（core 35 处、diagnostics 65 处、project/execution/testkit 少量——均在公共 include 树）；`WorkbenchContent_p.hpp`/`WorkbenchShell_p.hpp` 私有头无跨单元消费者。
4. **SA-02 框架零修改**：见 4.1。
5. **R-4 名称前缀拼接**：diff 范围无名称拼接/剥离逻辑（R-4 静态扫描命中集与基线一致——见 4.4 第 3 条）。
6. **PA-1 权威唯一**：PM-11 文本唯一权威仍为 `formatProjectStatusText`（现取现投），ui 未新立状态语义；命令入口/运动学真值等权威面未触碰。
7. **PA-2 不可变历史**：无修订/写路径改动。
8. **不继承 old/**：无 old/ 引用。

### 4.7 注释规范（AGENTS §2） —— 通过

- **公共头全查**：`WorkbenchContent.hpp`（本任务契约面变更落点）——文件头设计依据增补 §10.1 v1.14 增量注⑦；双观测钩子 Doxygen 完整（`@param observer [in]`、UI 线程约束、生命周期≤本实例、超时单位 ms、0＝驻留语义）；文件头含背景说明与线程模型段。attempt 1 的 F-333（三文件头滞后）已由 f6d1a235 修复：UiPlugin.hpp @brief/设计依据、UiPlugin.cpp 设计依据、WorkbenchContent.hpp 设计依据均已随 UI-T18 增量；尺寸钳制注释与代码 `qBound(300,…,480)/qBound(190,…,340)` 对齐。
- **实现文件抽查 ≥3**：UiPlugin.cpp（多 Dock 拓扑/状态投影/旗标重施各语义段"为什么"注释完整，含 px/ms 单位、降级形态留痕、一次守卫）、WorkbenchContent.cpp（showStatusFeedback 唯一出线判空收口、build 序注释更新）、WorkbenchShell.cpp（等价迁移注释含"逐项等价"对照说明）。
- **测试文件**：gui_test 新断言带中文需求追溯（"PM-11 状态文本未经理观察者投影（UI-T18 契约面失效）"）。
- **高危信息表**：单位（px/ms）✓、线程约束（UI 线程/非线程安全）✓、所有权（窗口树托管/Qt 父子树托管）✓、错误语义（降级形态 reportLine＋Dev 日志如实留痕）✓。

### 4.8 文档同步＋findings 未关闭条目相关性 —— 通过（1 项缺口转登 F-335）

- **单元卡**：ui.md v1.14（落位登记：§10.1 增量注⑦⑧＋§13 UI-T18 行＋落位登记注①~⑥＋§16.7 v1.14 行）＋v1.14-r2（返工登记＋§16.7 v1.14-r2 行）；实现与登记注逐项对得上（双观测钩子签名/6 处瞬态收敛/多 Dock 拓扑结构/旗标重施语义——与代码逐字一致）。需求语义零变化（PM-11/PM-14/§4.1/§4.4 原文承接）。
- **findings 相关性逐条核对**（F-318~F-334 范围指令；实际在册 F-301~F-330 于 redesign-main，F-331~F-334 登记于 acc/UI-T18/1 证据分支 findings.json，F-318~F-320 为编号空缺）：
  - **F-331（blocking，open，acc/UI-T18/1）仓库根越界 XML**：已由 840375b3 删除——产物面闭环；
  - **F-332（suggestion）§16.7 v1.14 行口径（43→63/63、五→六目标）**：已由 f00fb104 修正（§16.7 v1.14 行现文⑥六目标/⑦63/63）——产物面闭环；
  - **F-333（suggestion）三文件头滞后＋钳制注释**：已由 f6d1a235 修复（4.7 已核）——产物面闭环；
  - **F-334（suggestion）p.hpp 死包含**：已由 f6d1a235 删除（现头仅存说明性注释）——产物面闭环；
  - **缺口（转登 F-335）**：上述 4 条在 acc/UI-T18/1 findings.json 中仍全部 `open`——返工消账未回写登记册状态翻转（任务分支的 findings.json 不含该 4 条，结构上无法翻转）。产物面已核实闭环，**状态翻转缺口由 F-335 跟踪**，收尾合并 findings.json 时按本记录核实的产物状态将 F-331~F-334 置 fixed；
  - 同单元在册 open 条目相关性：F-321/F-322/F-324/F-325（宿主冒烟取证面）——v1.14-r2 登记注已如实引用并声明口径（Dev 日志 sampled-tail、瞬态抽样 1/6、--rwsplugin 直载替代 ini 通道）；F-327/F-330（StorePortAdapter 译面/重开 UX）不在本任务 diff 范围、无交互；F-323/F-301（全新树 RobWorkSim 失败）本次复现与登记一致（4.2）；F-326 fixed 无复发。

### 4.9 commit 质量 —— 通过

范围 6 提交均 `[WP-10-T18]` 动词开头中文标题＋四段正文（What/Why/验证/影响面），附 Acc-Covered 映射；验证结论与在库留痕一致且明确区分"未执行"（实施段宿主冒烟如实登记"随验收段补录"）；无空话、无粉饰。历史提交 0e54c27b 正文的"ird_gates 命中 43 条"与后续实测 63/63 不一致——该失实已被 F-332 登记、d7e4a243/f00fb104 两级修正（注⑥/§16.7 行）并在 f00fb104 正文说明，属已闭环的历史口径缺陷，不再重复登记。

### 4.10 留痕完整 —— 通过

`traceability/builds/wp10-t18/`（规范路径）r4 件齐备且内部自洽：integration-build-r4.log（BUILD_EXIT=0）、verify1-targets-r4.log（六目标 22 处提及）、gtest-ui-{model,contract,gui}-r4.xml（tests=154/22/35，failures=0）＋console 日志＋ird-test-report-*-r4.json（decisive=true，total=154/failed=0）、ctest-ui-{ird,gui}-r4.log（2/2、1/1）、ird-gates-{head,base}-r4.log（各 119 行门禁发射）＋gate-hitset-{head,base,diff}-r4.txt（63/63）、smoke-build-r4.log（SMOKE_BUILD_EXIT=0）、validate-task-r4.log（PASS）、validate-docs-r4.log（PASS）、host-smoke-r4-README.md＋9 帧＋双轮日志＋驱动工具。时间线自洽（XML 20:14 → 冒烟 20:23~20:28 → 提交）。"phase-one-validation.log/resultNote" 为 phase-one 任务口径，本任务不适用；等价留痕（state.json 队列登记＋ui.md v1.14-r2＋本目录）齐备。

### 4.11 偷懒/缩水实现扫描 —— 通过

- ①diff 范围 `TODO|FIXME|stub|placeholder|未实现|not implemented` 增量行扫描**零命中**（无"触发式延后"豁免需列出）。
- ②空实现检测：双观测钩子在 `WorkbenchContentImpl` 为真实实现（存取观察者＋refreshStatusBar 现取现投/showStatusFeedback 判空收口）；插件侧 lambda 真实接线（QLabel 投影＋showMessage 转发）；无"仅返回默认值"的契约面函数。中央让位页"已构建不挂载"为契约显式形态（O-43 ③/O-38 ③），非缩水。
- ③**逐条证据表**见 §3。
- ④测试强度：断言校验行为与边界（投影值非空＋消息内容包含"未知命令"），非恒真；4.5 破坏性验证实证两断言会红。

---

## 2. 宿主 GUI 冒烟裁决（契约 verify 第 4 条）

- **执行状态如实登记**：验收者以 AskUserQuestion 向所有者请求桌面窗口（多 Dock 拓扑＋宿主状态栏真机序列约 3~5 分钟），**未获答复**→本次验收未亲驱真机。按编排口径"暂不方便则按所有者答复在记录中如实登记执行状态并按证据裁决"，以下为对送验头内 -r4 冒烟证据的对抗式核查结论。
- **证据核查（验收者逐帧目验＋日志比对）**：
  - 帧 01（host-smoke-r4-01-startup.png）：标题 `RobWorkStudio v26.9.24-wp10-t18`；主 Dock『IRD 工作台』（命令条＋项目导航纵排）＋右 Dock『IRD 属性与诊断』＋底 Dock『IRD 任务和状态』；菜单栏 File/Tools/View3D/视图/Plugins/Help；窗口底缘唯一状态栏含 PM-11"未打开项目"；任务栏时钟 20:23/2026-9-24——与 README §1 步 2 一致。
  - 帧 04b：宿主状态栏特写"已恢复默认布局"瞬态实拍（showStatusFeedback 通路的真机证据）。
  - 帧 08（20:27）：重载后右/底 Dock 保持隐藏、主 Dock 照常、中央三维显露——PM-14 旗标重施成立。
  - 帧 09（20:27）：『视图』菜单仅左栏勾选、右栏/底部任务和状态区未勾选——勾选态与模型位同步。
  - run1/run2 console：装载门控三行齐（诊断栈就绪／工作台装配完成（多 Dock…）／工作台多 Dock 已呈现）；run1/run2 dev-diagnostics：首行＋`[sampled-tail] 宿主已关闭工作单元`——与 F-322 采样口径声明一致。
  - 诚实缺口（README §3：F1 瞬态未确证、瞬态抽样 1/6、[geometry] 行未固化、布局记忆载体未单独取证）——均为如实登记的取证局限，不动摇"多 Dock 拓扑＋宿主状态栏唯一＋PM-11 投影＋瞬态通路＋PM-14 旗标重施"已实证部分的结论。
- **裁决**：verify 第 4 条按在库 -r4 冒烟证据判定**成立**（多 Dock 拓扑呈现＋宿主状态栏唯一＋PM-11 投影＋瞬态消息实拍＋PM-14 隐藏重载保持）。验收者未亲验事实与本节口径随本记录留痕。

## 3. acceptance 数组逐条证据表（4.11③）

| # | acceptance 条目（摘要） | 证据 | 结论 |
| --- | --- | --- | --- |
| 1 | 契约面变更落位：statusBarWidget() 移除→双观测钩子；6 处瞬态收敛 showStatusFeedback；内容层零状态栏 Widget；§10.1 v1.14 增量注⑦与实现逐字一致 | WorkbenchContent.hpp L216~236（钩子声明）/WorkbenchContent.cpp L1147~1153（现取现投）/L1247~1256（唯一出线）；内容层 grep QStatusBar/showMessage 代码零命中（仅注释）；showStatusFeedback 调用点 6 处（L893/947/1052/1057/1203/1207）＋定义 L1247；gui_test L229~246 双断言绿；§10.1 增量注⑦（ui.md L1084 起）与签名逐字对照 | 成立 |
| 2 | 多 Dock 拓扑落位：主 Dock＋属性右 Dock＋任务底 Dock；装载自证拍入宿主＋钳制；中央让位页不挂载；三区开关改绑 Dock 本体；布局记忆三区键零变化；harness 等价迁移；v1.11 等价迁移对照 | UiPlugin.cpp buildDockBody（主 Dock 纵排＋两 QDockWidget 创建＋setRegionVisibilityTarget 三绑）与 reassertEmbeddedPresentation（addDockWidget 入宿主＋重显＋旗标重施＋尺寸钳制 qBound(300,…,480)/qBound(190,…,340)）；WorkbenchShell.cpp L321~340（自有状态栏＋双钩子等价接线）；布局记忆键：diff 无 LayoutMemory 键变更；帧 01/06/07/08/09 真机核验；GUI 套件 35/35 回归 | 成立 |
| 3 | 门禁与留痕：ird_gates 归一化比对零新增；ctest ^ird$ 2/2＋^ird_gui$ 1/1；gtest XML＋日志留痕；宿主 GUI 冒烟如实登记执行状态；双模式构建零错误 | 验收者亲跑：ird_gates 引擎口径（ird_gates.cmake 头文档 `cmake -DIRD_ROOT=<…> -P`）base（feb09471）/head 各 119 行发射→归一化 63/63 **双向差集 0**，且与在库 gate-hitset-{head,base}-r4.txt **逐字一致**；ctest 2/2＋1/1；三套 gtest 154/154＋22/22＋35/35；留痕见 4.10；GUI 冒烟执行状态见 §2；双模式构建见 4.2 | 成立 |

## 4. 发现的问题清单

- **阻断级：无。**
- **建议级（已转登 findings.json，见同提交改动）：**
  - **F-335**（suggestion，open）：F-331~F-334 的消账未回写 findings.json 状态翻转（登记册条目在 acc/UI-T18/1，任务分支结构上不可翻转）——产物面已由本次验收核实闭环；处置：收尾合并 findings.json 时将 F-331~F-334 按 §4.8 核实结论置 fixed（登记册属 traceability/**，翻转本身不扩 allowedFiles）。
  - **F-336**（suggestion，open）：ird_gates 的 VS 构建目标捕获路径（`cmake --build --target ird_gates` 经 MSBuild 控制台）存在输出缩减与编码污染——本验收实测同一树构建目标捕获仅 43 行（缺 ui_app/ui_plugin 的 4 条 SUB 尾部命中，且中文经 GBK 双重编码有损），而引擎文档调用口径 `cmake -DIRD_ROOT=<…> -P cmake/ird_gates.cmake` 输出完整 119 行；送验 r4 门禁留痕为完整口径、比对结论不受影响，但后续取证若走 VS 捕获路径可能产生假差集。处置建议：门禁取证统一采用引擎文档口径或 gate-all.ps1 内固定该口径；与 F-309/F-315（门禁日志双重编码家族）同域。

## 5. 裁决

**verdict = pass**。4.1~4.11 全部通过，无阻断级问题；建议级 F-335/F-336 已转登，不阻塞合入。合入决策归所有者（本验收者不合入、不修改实施产物、未向任务分支推送任何提交）。
