# WP-13-T13 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | WP-13-T13 实现规范模型包导出/导入 roundtrip 与 WC/DWC XML 外供导出（IModelPackagePort，MDL-20） |
| 送验对象（冻结） | `6672a1995e1f203bf542c3f181d0f92b44c761ec`（40 位，diff 范围＝`2979e5cd56724f3ee2f1c87c8785a0ddfdee203a..6672a199…`，2 提交：0cb481d9 实现＋6672a199 留痕登记） |
| 裁决 | **pass**（4.1～4.11 全部通过；无阻断级问题；建议级 1 条已转登 findings.json：F-350；在案 F-342 的相关性核对见 4.8） |
| 记录分支 | `acc/WP-13-T13/1`（基于 origin/redesign-main @ 2979e5cd） |

## 0. 独立性声明与环境（ACC §2/§3）

- 验收者为全新上下文会话：只读任务契约（`tasks/foundation/WP-13-T13.json`）、设计文档（`units/modeling.md` v0.15 @ 送验 SHA、`units/io.md` §4.6/§7.1/§9.10 公共头对账、`REQUIREMENTS.md` 编号原文）、在库留痕；未消费实施者任何过程叙述；构建/测试/门禁全部亲手重跑（复现优先于采信），全程为"不通过"找证据。
- **分支漂移检查**：`git fetch` 后 `git rev-parse origin/wp13-t13` ＝ `6672a199…` ＝ 送验 headSha——**无漂移**；`origin/redesign-main` ＝ `2979e5cd…`（即 base）。
- **复现现场**：`git worktree add --detach <临时目录A> 6672a199…`（detached @ SHA）；记录回传现场：`git worktree add -b acc/WP-13-T13/1 <临时目录B> origin/redesign-main`；base 侧门禁对照用临时 detached worktree @ 2979e5cd（引擎直跑只读源树、自测夹具走缺省 TEMP——F-347 防呆口径），用毕即删。
- **集成模式**（A 内全新配置，源目录显式指向 worktree A）：`cmake -S <A>/RobWork -B <build-int2> -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DRWS_USE_STATIC_LINK_PLUGINS=ON -DCMAKE_TOOLCHAIN_FILE=<repo>/vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64` → exit 0（Configuring done 57.7s/Generating done）；缓存确认 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`。环境链缺口按在案口径处置：`RobWorkStudio/bin/` 两份 ini 模板为 gitignore 本机环境件（worktree 检出缺 bin/ 目录），自主树复制后配置通过（F-007 口径，属环境供给非送验对象改动）。
- **独立冒烟模式**（另一临时目录，`-S <A>/…/industrialrobot`＋vcpkg toolchain＋Qt CMAKE_PREFIX_PATH，AGENTS §4.1 修正口径）：配置 exit 0；三目标构建 exit 0、`sdurws_ird_modeling.lib`＋两测试 exe 落位。
- **验收者环境**：Windows 10.0.26200 x64；Visual Studio 17 2022（MSBuild 17.12.6）；Qt 6.11.1（msvc2022_64）；vcpkg 经典模式（主树 `vcpkg/installed/x64-windows`）；CMake 4.3。
- **验收过程环境事件（如实登记，均不触及送验对象，最终证据全部来自事件后的干净复现）**：
  1. 首次集成配置误以相对路径 `-S RobWork` 从主仓 cwd 解析到主检出源树（内容与冻结 SHA 对构建相关文件一致，但不合 ACC §3 现场口径）——该树（build-int）**弃置不作为证据**；以显式 worktree A 源路径重配（build-int2）重建，全部集成证据取自 build-int2。
  2. 变异测试注入（4.5）与 build-int2 首轮构建并发，Package.cpp 的 obj 在变异窗口内编译且 mtime 晚于还原时间，增量重建未重编 → build-int2 首轮 ctest 出现 `ImportRejectsForeignArtifact_WP13T13_ACC3` 假红（签名与 M2 变异一致）。处置：确认源树 pristine（`git status --porcelain`＝0 行）后 touch Package.cpp 强制重编并重建三目标 → **215/215＋14/14 复绿**。该假红非送验对象缺陷。
  3. 全新树测试 exe 在 Git Bash 会话下出现一次瞬态 DLL 加载失败（exit 127）与 ctest 无输出挂起；cmd 原生解析同径直跑正常、事后 bash 亦复现不出——判为验收会话瞬态环境因素（新构建 DLL 落盘/锁窗口），非产品缺陷；此后 bash/cmd 双口径运行一致。
- 变异测试在 A 内注入，验后 `git checkout --` 还原；还原后 `git status --porcelain` ＝ 0 行，重跑复绿（见 4.5）。

## 1. 逐项证据（ACC §4，4.1～4.11）

### 4.1 改动范围 —— 通过

`git diff --name-only 2979e5cd..6672a199` 共 **37 文件**：modeling 单元 12（CMakeLists.txt 修改＋DiagCodes.hpp/Errors.hpp/Package.hpp 三公共头修改或新增＋DiagCodes.cpp/Errors.cpp/Package.cpp/PackageWorkCell.cpp 四实现＋contract_test/CommandPipelineContractTest.cpp 修改＋test/DiagCodesTest.cpp 修改＋test/PackageTest.cpp/PackageWorkCellTest.cpp 新增）＋`traceability/builds/wp13-t13/` 留痕 24 件＋`units/modeling.md` 1 件。逐文件对照契约 `allowedFiles`：modeling `**` 通配、`units/modeling.md` 明列、`traceability/**` 通配——**全部在册**；`forbiddenFiles`（REQUIREMENTS.md/ARCHITECTURE.md）零触碰；diff 无 `patches/` 路径、无 industrialrobot 之外的任何框架文件（SA-02 无未登记框架改动）。

### 4.2 可复现构建 —— 通过

- 集成模式（worktree A 源、重配后）：`sdurws_ird_modeling` / `sdurws_ird_modeling_test` / `sdurws_ird_modeling_contract_test` 三目标 exit 0、错误计数 0（产物 `build-int2/RobWorkStudio/libs/Release/sdurws_ird_modeling.lib`＋`bin/Release/sdurws_ird_modeling_test.exe`、`sdurws_ird_modeling_contract_test.exe` 落位）。
- 独立冒烟模式：全新目录配置＋构建 exit 0、错误计数 0；`sdurws_ird_modeling_test` 亲跑 **169/169 PASSED**（16 suites，231 ms）＋`sdurws_ird_modeling_contract_test` 亲跑 **8/8 PASSED**（2 suites）——gated 的 Readiness/CommandHandlers/CompileBridge/PackageWorkCell TU 不编入（TARGET sdurw_kinematics gating，与在库 smoke-modeling-test.xml 169、smoke-modeling-contract-test.xml 8 一致）。

### 4.3 契约 verify 命令 —— 逐条独立执行完毕（7 条；环境事件后的干净复现口径）

| # | 命令 | 结果 |
| --- | --- | --- |
| 1 | `cmake --build … --target sdurws_ird_modeling` | exit 0，错误计数 0，lib 落位（build-int2，worktree A 源） |
| 2 | `cmake --build … --target sdurws_ird_modeling_test` | exit 0，错误计数 0，exe 落位 |
| 3 | `ctest --test-dir …/modeling -C Release -L "^ird$"` | **2/2 PASSED**（100% tests passed out of 2）；另亲跑两 exe：`sdurws_ird_modeling_test` **215/215 PASSED**（25 suites，含 gated MdlPackageWorkCell 1 条与 MdlPackage 6 条）＋`sdurws_ird_modeling_contract_test` **14/14 PASSED**（4 suites）——与在库 integration-modeling-test-report.json（215）、integration-modeling-contract-test-report.json（14，合并 229）逐件一致。**契约缺口（建议级→F-350）**：verify 数组缺 `sdurws_ird_modeling_contract_test` 构建行——本验收亲建该目标后方能全量执行 |
| 4 | `ctest --test-dir …/io -C Release -R sdurws_ird_io_test` | **1/1 PASSED**（100% out of 1，对端回归）。**契约缺口（并入 F-350）**：verify 数组缺 `sdurws_ird_io_test` 构建行——亲建后方能执行 |
| 5 | `cmake --build … --target ird_gates` | exit 1＝**存量命中（门禁按设计命中即 FATAL）**：MSBuild 捕获「依赖红线/补丁门禁存在 56 处命中——按 ARCH §3.2 SA-10 判构建失败；例外只经 DTB §4.5 登记」——与在库 ird-gates-comparison.txt 附注的构建树口径（56 处发射、退出码 1 属存量例外未清零既有状态）一致；按 F-336/F-347 建议口径另行引擎直跑取证（见下） |
| 6 | `pwsh -File …/validate-task.ps1 -TaskFile …/WP-13-T13.json` | **PASS** (1 tasks)，exit 0 |
| 7 | `pwsh -File …/validate-docs.ps1` | **PASS** (20 units, 12 trace entries, 189 task files)，exit 0 |

**ird_gates 引擎直跑 base..head 归一化比对（F-336/F-347 口径，验收者亲手）**：head 侧 `cmake -DIRD_ROOT=<A>/industrialrobot -P cmake/ird_gates.cmake`（exit 1，发射 **119 次**：SUB 57 / R3 41 / LIB 10 / T1 5 / R4 3 / T2 1 / R5 1 / R1 1——与 T12 期同分布）；base 侧临时 worktree @ 2979e5cd 同命令（exit 1，逐码同值）。以 `<IR>` 锚归一绝对路径后逐行多重集 diff＝**空**（归一化后各 121 行逐字节一致）＝**零新增命中**。与在库 ird-gates-base-hits.txt / ird-gates-branch-hits.txt（各 63 归一化条目、56×2＋7×1＝119 次发射、diff 空）及 ird-gates-branch-raw.txt（130 行/119 命中行/「命中 56 项」汇总）逐件口径一致。

### 4.4 acceptance 条目 —— 五条均有对应证据（全部为本次亲跑复现，具名用例）

| 契约 acceptance | 证据（亲手复现） |
| --- | --- |
| 1 Package.hpp/.cpp 落位＋包形态（ZIP 封装＋manifest 来源标识/schema/对象清单与 digest＋根/部件 canonical 字节＋Solidified 副本＋命名位姿） | `MdlPackage.ExportPackageShape_WP13T13_ACC1`（亲跑 OK）：包经 io `openZipChannel`（IO-T04 落位面）打开成功、逐条目 `compressionMethod=0`（STORED）＋`encrypted=false`；manifest 五要素齐全（formatId=ird-model-package／producer=ird-modeling／schemaVersion=1／entries 6 条〔根 1＋工具 1＋场景 1＋位姿集 1＋传动 1＋Solidified 资源 1〕／contentDigest=sha256- 前缀＋64 hex）；manifest 清单与归档条目集精确一致；命名位姿条目在册（MDL-20 保留面）；outcome 计数 entryCount=7（含 manifest）＋totalBytes=清单 size 和＋manifest 自身。接口落位：`Package.hpp:300-377` IModelPackagePort 抽象＋ModelPackagePort final 实现，签名 `exportPackage(closure, target, diags)`/`importPackage(source, diags)`（§9.4.9 行；ExportOutcome/ImportOutcome 落位为 Package 前缀命名——偏差已按 DTB §5.4 登记于单元卡 v0.15 ②，实现与登记一致） |
| 2 导出原子性 UT（io AtomicFile 临时区→校验→替换；失败→MDL-EXPORT-FAILED、项目状态不变且旧输出完好 V-29；会话级零修订） | `MdlPackage.ExportFailurePreservesPrevious_WP13T13_ACC2`（亲跑 OK）：①目标已存在＋NeverOverwrite→ok=false、error.code=ExportFailed、diags.front().code=MDL-EXPORT-FAILED、旧输出文件逐字节保留（V-29 文件层观测）；②父目录缺失→ExportFailed＋MDL-EXPORT-FAILED、目录不被代建、无半成品；③闭包缺根→RefMissing 值面且零诊断（诊断纪律）。实现链路（Package.cpp:900-1031 通读）：内存装配→io `ITempAreaManager` PackExport 会话暂存（io §7.5）→暂存位经 IAtomicFileWriter NeverOverwrite 原子就位→`IZipChannel` 重开自检（逐条目哈希＋manifest 重读摘要比对——不绿即拒绝替换）→目标位 IAtomicFileWriter prepare/write/commit；统一失败出口 cleanup 暂存＋目标零接触。零修订：`ExportDeterministicBytes_WP13T13_ACC1_ACC2` 断言导出前后闭包根字节不变＋同闭包两次导出同包字节（NFR-COR-02，createdAtUtc 调用方传入——服务不取时钟） |
| 3 导入回读 UT：仅识别本软件工件（manifest 来源标识＋schema 校验）；非本软件工件→MDL-IMPORT-PACKAGE-UNKNOWN 引导 MDL-18/R2；不与 MDL-18 通道混淆 | `MdlPackage.ImportRejectsForeignArtifact_WP13T13_ACC3`（亲跑 OK）：纯文本工件（非 ZIP）→ok=false、error.code=PackageUnknown、拒绝轨无草稿（report.design.joints/report.parts 空——fail-closed）、diags.front().code=MDL-IMPORT-PACKAGE-UNKNOWN 且 recommendedAction 含 "MDL-18"；同测试钉住 `mapWorkCellXml` 对同输入仍 NotImplemented＋无草稿＋零诊断（R1 边界不受影响——DTB 禁止项）。实现门（Package.cpp:1053-1168 通读）：finalPath 空/容器打不开/manifest 缺失或不可读/formatId 或 producer 不符→PackageUnknown＋引导诊断（fail-closed，五分支逐一）；schemaVersion 超前→SchemaVersionUnsupported 值面拒绝不产诊断（与 MDL-READINESS-SCHEMA-UNSUPPORTED 的对象 schema 语义不混用——登记于 v0.15 ⑥）；孤儿条目/清单失配/逐条目哈希不符→MalformedPayload（`TamperedPackageRejected_WP13T13_ACC3_ACC4` 亲跑 OK：包内第二本地头后数据区翻一字节→导入拒绝） |
| 4 roundtrip 逐项一致 UT（V-29/AT-28：五组逐项 diff=空——authority/physics/resource/collision/pose；导入报告承载逐项清单） | `MdlPackage.RoundtripItemwiseChecklist_WP13T13_ACC4`（亲跑 OK）：导出→重导入→`roundtripChecklist`（唯一实现——NFR-MNT-04）源侧与导入侧清单**整体相等（diff=空）**；五组覆盖逐一断言（authority/physics/resource/collision/pose 各 1）；根对象 operator== 相等（canonical 权威语义逐字段）；四部件按身份配对逐类值相等（含命名位姿逐条目）；资源两态逐项（resourceManifest 相等＋Solidified 副本 resourceId/objectId/bytes 逐字节回读——CON-03）；manifest 摘要回环（IZipChannel 重读重算＝导出报告值）。清单实现（Package.cpp:528-738 通读）：authority 组按双权威模式取编码权威字段（StandardDH 态 DH 四参数逐项、Explicit 态 axis/origin 权威＋dhDerived 不入清单——D-MDL-5）；physics 组连杆/工具 BodyData（kg/kg·m²/N·m·s/rad 单位尾注）；resource 组 digest/状态/固化 oid+cv（absPath 不入清单——NFR-SEC-01）；collision 组连杆几何引用＋场景画像提示（selfCollisionHints 不入——§4.3-B 登记边界）；pose 组位姿集逐条目 |
| 5 WC/DWC XML 外供导出 UT（数据源＝RuntimeSnapshot 只读视图；modeling 仅编排写出、不改造内容；零修订零失效） | `MdlPackageWorkCell.WorkCellDwcXmlExportFromSnapshotView_WP13T13_ACC5`（亲跑 OK，gated 集成——LastTest.log RUN→OK 在案）：S5 替身＋S6~S10 真实工厂编译快照→两面 XML 原子落盘（entryCount=2）；WC 面设备名/joint_1/限位 ±1.5 rad/WORLD 帧逐项透传（R-4 不消歧不改写）；DWC 面重力 −9.81/体名/质量 5 kg 透传（基线 SI 零换算）；同快照两次导出同字节（确定性）；目标已存在＋NeverOverwrite→ExportFailed＋MDL-EXPORT-FAILED＋旧输出 "PREVIOUS" 逐字节保留；无 DWC 能力而请求 DWC 面→ExportFailed。实现（PackageWorkCell.cpp 通读）：数据源仅 WorkCellConstView/DynamicWorkCellConstView 只读查询、逐项取数零增删改零单位换算、`atomicWrite` 经 io AtomicFile prepare/write/commit；XML 为 modeling 自有查看表示（ird-workcell-export/ird-dwc-export 元素）非 RobWork wc.xml——sdurw_loaders 未引入（DTB §4.6 表行，登记于 v0.15 ⑤） |

### 4.5 测试真实失败能力 —— 通过（两处变异均致红，注入后已还原）

在 detached worktree A 内注入（各自重建→亲跑→`git checkout --` 还原；还原后 `git status --porcelain`＝0 行、重跑复绿 **169/169**；冒烟树重建约 1 分钟）：

- **M1（门条件篡改）**：`Package.cpp` exportPackage ⑦ 目标原子替换段 `writer->prepare(target.targetFile, target.replace)` → `io::ReplacePolicy::OverwriteAtomic`（调用方替换策略被无视）→ **`MdlPackage.ExportFailurePreservesPrevious_WP13T13_ACC2` FAILED**（MdlPackage 组 1/6 红、5/6 绿）——ACC2 失败恢复与旧输出保留断言真实有效；
- **M2（诊断码篡改）**：`Package.cpp` pushPackageUnknownDiag（:428）`kMdlImportPackageUnknown` → `kMdlExportFailed` → **`MdlPackage.ImportRejectsForeignArtifact_WP13T13_ACC3` FAILED**（gtest 输出 "Which is: MDL-EXPORT-FAILED"≠"MDL-IMPORT-PACKAGE-UNKNOWN"）——导入门稳定诊断断言真实有效。

（两处变异均非恒真测试可放过：前者击穿"目标不变"值面+诊断面双断言，后者击穿稳定码交叉核对。）

### 4.6 架构红线 —— 通过（AGENTS §5 八条逐条）

1. **L2 零 Qt**：Package.hpp/Package.cpp/PackageWorkCell.cpp 及两测试 TU grep `#include <Q|<Qt` 零命中（PackageWorkCell.cpp 消费 rw/rwsim 非模板类——框架库非 Qt）；modeling 为计算内核，无界面目标。
2. **业务域互链 R-1**：新增 include 仅 `sdurws/ird/core|io|runtime` ＋ modeling 内部——无 modeling→requirements/kinematics/trajectory/dynamics/selection/optimization 出边；io/runtime 消费走 WP-13-T02 已登记六边（`ird_gates_whitelist.cmake:50-55`，modeling->io 明列）；门禁实测零新增命中佐证。
3. **跨单元私有头 R-2**：13 个跨单元 include（io 5＋core 3＋runtime 5）逐一 ls 验证全部落位对端 `include/sdurws/ird/<unit>/` 公共头；runtime 私有/测试头零 include（PackageWorkCellTest 夹具仅消费 runtime 公共面——文件头注自证）。
4. **SA-02 框架零修改**：diff 无 patches/、无框架文件（见 4.1）。
5. **R-4 名称前缀**：exportWorkCellXml 对设备/帧/体名逐字节透传（`WorkCellDwcXmlExport…` 断言 "IRB_PKG" 原样出现——"不消歧不改写"）；包导入不解析/拼接 RobWork 名称。
6. **PA-1 权威唯一**：导出/导入为会话级文件操作零修订（闭包字节零突变断言）；运动学真值不私设第二编译路径（WC/DWC 数据源＝runtime 已编译快照只读视图）；SHA-256 唯一经 core ContentDigester（SA-12；CRC-32 仅容器传输校验——Package.hpp 头注明确二者边界）。
7. **PA-2 不可变历史**：port 无状态纯函数服务（卡 §3.4 总约定 1——线程安全注在案），不触达修订/写路径。
8. **不继承 old/**：diff 无 old/ 引用；实现按单元卡 §6.8/§9.4.9 从零编写。

### 4.7 注释规范 —— 通过（公共头全查＋实现文件 4 个全读，超出"抽查 ≥3"要求）

- 公共头 `Package.hpp`（438 行）全查：文件头注含设计依据（modeling §6.8/§3.3/§9.4.9/§9.6/§3.4、io §7.1/§4.6/§9.10/§5.9.3/§10.5、MDL-20/AT-28/V-29/CON-01/PA-2/NFR-COR-01/02/SA-12、契约 acceptance 1~5、P-MDL-8 处置）＋三通道边界背景说明＋ZIP 容器自实现理由（为什么不用 libzip 写侧——DTB §4 通道）＋线程安全/确定性/单位制声明；逐类型/逐函数 Doxygen 中文契约（@param 方向、非 owning、fail-closed 语义、确定性来源、错误码面全列）。
- 实现文件 `Package.cpp`（1401 行）/`PackageWorkCell.cpp`（284 行）全读：文件头实现注逐段职责；每个函数中文 Doxygen 或段注；**语义段落级"为什么"**（暂存自检不用 AtomicTarget 暂存位的 commit 前独占句柄理由、资源按 oid 而非 resourceId 编址的路径安全理由、root 分支保留值语义、makeExportError 不跨 TU 暴露的"唯一实现"纪律边界等）。
- §2.5 高危信息逐项有落点：物理单位（rad/m/kg/kg·m²/N·m/N·s/m 尾注逐条；WC 内部 SI 说明）、坐标系（T-parent-frame 相对父帧契约、T_ab 语义方向）、角度制式（rad 显式——"不是度"层面在 fmtLimits/限位注）、所有权（closure 非 owning、调用方持有；entry.bytes 值语义）、线程约束（可重入/无共享可变状态声明）、错误语义（调用方数据错误 RefMissing 值面 vs 环境/写入失败 ExportFailed＋MDL-EXPORT-FAILED 诊断 vs PackageUnknown 引导面三分明确）、确定性来源（固定 DOS 时间戳 1980-01-01、条目字典序、std::to_chars 最短往返 locale 无关、createdAtUtc 调用方传入）。
- 测试文件两件：用例级 Doxygen 注明 ACC 编号与验证需求（IRD_TEST_INFO 携带 MDL-20/AT-28/V-29/V-20/MDL-18/MDL-22 追溯字段）；篡改位推导注释（本地头 30 字节＋变长名偏移）逐步讲解。
- 配套小改（DiagCodes.hpp/.cpp、Errors.hpp/.cpp、CMakeLists.txt、CommandPipelineContractTest.cpp、DiagCodesTest.cpp）diff 逐行核读：新码登记注释含 §9.5 行出处与字段口径；CMakeLists gated 增列有中文注释块；钉住断言过期化有推进先例引用。
- 在案 F-342 相关缺口：`DiagCodes.hpp` modelingCodeDescriptors @brief 计数（见 4.8）——非本任务新增缺陷，不计入本项判定。

### 4.8 文档同步 —— 通过（findings 未关闭条目逐条相关性核对见下）

- `units/modeling.md` 升 **v0.15**（@ 送验 SHA）：§3.3 表 Package.hpp 行 T13 落位注记（:266）、§14.6 v0.15 变更记录九项（①接口/类型落位；②**等价命名登记** PackageExport/ImportOutcome——DTB §5.4；③包格式与 ZIP 容器写侧自实现登记〔io 公共面无 ZIP 写出器、不新增未登记第三方依赖——所有者裁决通道注记〕＋"写已知、读权威"闭环；④导出原子性三段时序登记；⑤WC/DWC 外供编排面登记〔含"非 RobWork wc.xml 格式"边界与 sdurw_loaders 不引入〕；⑥诊断码两码注册＋域错误两值表尾追加＋包格式版本与对象 schema 语义不混用；⑦测试面逐用例登记；⑧CMakeLists 增列；⑨验证留痕口径）。**实现与任务卡零未登记偏差**——契约 acceptance/knownPitfalls 与实现逐项对账：包形态、原子时序、导入门、五组清单、WC/DWC 只读编排均一致；P-MDL-8（io Package/AtomicFile 面按 io.md v0.9 现状消费）成立：本任务消费的 IZipChannel/IAtomicFileWriter/ITempAreaManager/Json 均为 io 已落位公共面（io 公共头逐一核对签名一致），冻结后如漂移按 R-MDL-1 增量同步。
- **findings.json 未关闭条目相关性核对（4.8 v1.2 要求）**：open 条目逐条过筛——①**F-342**（DiagCodes.hpp @brief 计数失实）与本任务**直接相关**：本任务触碰 DiagCodes.hpp（追加 T13 两码常量、枚举止于 T09 的缺口已随之补齐）但未顺手修正 @brief 计数——该注现写"当前 20 项"而工厂实际返回 **24 项**（DiagCodes.cpp 文件头已同步 24，.hpp 滞后 4 项），F-342 维持 open、缺口扩大（22→24）；纯注释滞后零行为影响（DiagCodesTest 分期封闭断言 24 码分期正确），不顺手消账的理由未登记——登记为遗留事项，责任方 modeling 卡所有者，建议随下次 DiagCodes 触碰修正。②**F-336/F-347**（ird_gates 取证口径/自测夹具防呆）：本任务已按其建议口径执行（引擎直跑＋缺省 TEMP）——与 T12 期一致的遵循关系，非待消账项。③**F-338/F-344/F-345 家族**（契约 verify 缺目标构建行）：**在 T13 契约复发**（verify 缺 contract_test＋io_test 构建行）→ 转登 **F-350**。④F-343/F-346/F-348/F-349（散文计数/留痕 XML/文实相符家族）：本任务留痕**含 gtest XML 原件 4 件**（integration/smoke × modeling/contract）、散文计数经亲跑逐一核实为真（215/14/169/8/177/229/119）——无复发。⑤其余 open 条目（core/testkit/io/ui/policy/project/reporting 等域）与本任务无涉。
- 任务状态/追踪矩阵：契约 status=ready 与 PIPE 口径一致（翻转由合入收尾执行，非实施者职责）；modeling.md §13 需求/AT 追溯行（MDL-20→T13，:1396）在册。

### 4.9 commit 质量 —— 通过

- `0cb481d9`：标题 `[WP-13-T13] 实现规范模型包导出/导入 roundtrip 与 WC/DWC XML 外供导出`（动词开头，≤50 字）；正文四段齐备（What 按文件列要点/Why 对应契约与需求编号/How 验证数字/影响面）；验证段数字（集成 215/215＋14/14 合并 229/229、ctest 2/2＋io 1/1、冒烟 169/169＋8/8 合并 177、validate 双 PASS、门禁直方图 119 次零新增）**经亲跑逐一核实为真**；影响面（枚举表尾追加不重排、零新增链接边）与 diff 一致（ird_gates LIB 词表零新增行佐证）。
- `6672a199`：标题 `[WP-13-T13] 登记任务验证留痕（最终源码全新捕获，traceability/builds/wp13-t13/）`；四段齐备；留痕清单与 diff 实际文件逐件一致（**含 gtest XML 4 件**——F-346/F-348 家族本任务未复发）；ird_gates 退出码 1 的存量例外附注与实测一致。

### 4.10 留痕完整 —— 通过

- `traceability/builds/wp13-t13/` 24 件落位并逐件核读：集成三目标构建日志（error 计数 0）、integration-ctest-ird.log（2/2）＋integration-ctest-io-regression.log（1/1）、**gtest XML 4 件**（integration-modeling-test.xml tests=215 failures=0、integration-modeling-contract-test.xml tests=14、smoke-modeling-test.xml 169、smoke-modeling-contract-test.xml 8——逐 suite 计数亲数核对，MdlPackage 6＋MdlPackageWorkCell 1 在册）、testkit JSON 报告 4 件（逐案 outcome 全 passed：215/14/177/8）、门禁五件（base/branch 归一化直方图 diff 空＋branch 原始输出 130 行＋比对说明）、validate-task/validate-docs 双 PASS 日志——全部与验收者亲跑复现逐件一致。
- `phase-one-validation.log` 未更新：该文件自 CORE-T01 后无任务追加（T09/T10/T11/T12 同先例），在案运作口径＝留痕落 `traceability/builds/<task>/`＋单元卡 §15 变更记录承担 resultNote 职能——本任务 v0.15 ⑨已登记，视同满足"或 resultNote 已更新"。

### 4.11 偷懒/缩水实现扫描 —— 通过

- ①标记扫描：base..head 全量 diff 新增行 `TODO|FIXME|stub|placeholder|未实现|not implemented` 命中 **31 处**，逐条归类：**全部位于 traceability 留痕文件**（gtest 报告/日志中的历史用例名 `MapWorkCellXml_R1BoundaryNoStub_WP13T05`、`PlaceholderCylinder_SegmentGeometry_WP13T07`——语义与偷懒相反；及 mapWorkCellXml R1 NotImplemented 边界的在案契约用语）——实现代码零命中，无需契约豁免。
- ②空实现检测：公共接口 IModelPackagePort/ModelPackagePort/roundtripChecklist/exportWorkCellXml 全部有实质实现（Package.cpp 1401 行＋PackageWorkCell.cpp 284 行逐段通读：ZIP 容器逐字段装配、三道导入校验、五组清单、双面 XML 编排均为真实逻辑；无"仅返回默认值"的契约外空壳；diags 追加式输出纪律逐点履行）。
- ③逐条证据表：见 4.4——契约 acceptance 五条逐条有具名测试＋文件行号＋实测输出，无"已实现"声明裸信。
- ④测试强度：断言校验行为与边界（manifest 五要素逐项、条目集精确一致、旧输出逐字节保留、拒绝轨无草稿、五组覆盖逐一、Solidified 逐字节、篡改拒绝、DWC 能力缺失拒绝面）；与 4.5 联动两处变异实测变红（非恒真、非只编译通过）。

## 2. 问题清单（分级）

**阻断级：无。**

**建议级（已转登 `traceability/findings.json`，编号顺延自 F-349）：**

- **F-350**（F-300/F-304/F-310/F-314/F-338/F-344/F-345 家族复发）：`tasks/foundation/WP-13-T13.json` verify 数组缺 `sdurws_ird_modeling_contract_test` 构建行（ctest -L "^ird$" 条目含该目标）且缺 `sdurws_ird_io_test` 构建行（ctest -R sdurws_ird_io_test 的前置目标）——全新构建树按清单逐条执行至 ctest 条目时目标不存在/Not Run，须验收者补建目标方可全量执行。处置建议：后续契约把 contract_test 与对端 io_test 构建行补入 verify 数组（T08 契约曾自愈列全三目标，可在下次契约编译批量对齐）。责任方：契约编译会话。

**遗留事项（在案条目不重复登记，随本任务核对更新事实）：**

- **F-342 维持 open、缺口扩大**：DiagCodes.hpp @brief "当前 20 项" vs 工厂实际 24 项（本任务触碰该文件补齐枚举注释但未同步计数；DiagCodes.cpp 文件头已为 24）。建议 modeling 卡所有者随下次 DiagCodes 触碰修正，或在治理侧批量消账。

## 3. 裁决

**verdict ＝ pass**：4.1～4.11 全部通过、无阻断级问题；建议级 1 条（F-350）不阻塞合入（已转登跟踪）。送验对象＝SHA `6672a1995e1f203bf542c3f181d0f92b44c761ec`（非分支引用；分支漂移检测通过）。验收过程中的环境事件（§0 三项）均为验收侧现象、已如实披露并以干净复现取代，不构成送验对象缺陷。合入决策归所有者，验收者不合入。
