# PRJ-T10 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | PRJ-T10 命令服务与内置命令（CommandServiceImpl） |
| 单元 | project |
| 送验对象（冻结） | 37d05852530d6512b22091c9a1f24ad027908820（origin/wp04-t10 尖端＝该 SHA，实测无分支漂移） |
| diff 范围 | a02a8a9d073af7c78a93c6ee7531c75ad1032d6c..37d05852530d6512b22091c9a1f24ad027908820（state.base..headSha；base＝origin/redesign-main 尖端实测同值，4 提交 19 文件） |
| 验收会话 | 全新上下文（ACC §2 四条独立性逐条适用：未消费任何实施叙述，只读代码/契约/设计文档/留痕；构建测试门禁亲手重跑；为"不通过"找证据立场） |
| 复现现场 | `git worktree add --detach <临时目录A> 37d05852`（detached @ SHA）；独立冒烟目录另配（带 vcpkg toolchain）；全部验证在临时目录内完成，未触碰任务分支与送验对象 |
| **verdict** | **pass**（4.1～4.11 通过；零阻断级问题——acceptance 4 的"内置元数据命令族"半区为 P-PR-9 治理阻断、按 DTB §5.4 登记路径如实处置，不属偷懒缩水；建议级 5 项 F-152～F-156 随本记录转登 findings.json，不阻塞合入） |
| 日期 | 2026-09-16 |

---

## 1. 逐项证据（ACC §4，4.1～4.11）

### 4.1 改动范围 — 通过

- 分支漂移检查：`git rev-parse origin/wp04-t10`＝37d05852530d6512b22091c9a1f24ad027908820＝送验 SHA，零漂移。
- `git diff --name-only base..head`＝19 文件：project 单元 13（CMakeLists.txt、include/{CommandService,ProjectStore}.hpp、src/{CommandServiceImpl.hpp,CommandServiceImpl.cpp,DiagRecords.hpp,ProjectStoreImpl.hpp,ProjectStoreImpl.cpp}、test/{CommandContractTest,CommandServiceTest,QueryPortTest,StubHandlers.cpp,StubHandlers.hpp}）＋traceability/builds/wp04-t10/ 5（ird-test-report.json＋四份 gtest XML）＋units/project.md 1——全部落在契约 allowedFiles 三个通配内（`project/**`、`traceability/**`、`units/project.md`）。
- forbiddenFiles（REQUIREMENTS.md/ARCHITECTURE.md）零触碰（diff 文件清单实证）。
- SA-02：diff 中无 patches/ 文件、无 RobWork/RobWorkStudio 框架源码路径改动——零未登记框架改动（`git diff --stat base..head -- <框架路径>` 为空实证）。

### 4.2 可复现构建 — 通过（任务目标面两模式从零复现零错误）

- 集成模式重新配置（worktree A 内全新 build/）：`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON`＋vcpkg toolchain 绝对路径＋Qt 前缀 `D:/software/Qt/6.11.1/msvc2022_64`（主树 CMakeCache 实证参数同源）。首次配置按 AGENTS §4.1 字面命令失败两处（Boost 缺失、ini 模板缺失）——均为 **F-007/F-099/F-062 已登记的全新树环境链问题**（findings 既有 open 条目，非本任务缺陷），按其处置建议补齐后 configure exit 0；缓存 `grep RWS_BUILD_INDUSTRIALROBOT build/CMakeCache.txt`＝`RWS_BUILD_INDUSTRIALROBOT:BOOL=ON` 确认。
- 任务目标构建零错误：`cmake --build build --config Release --target sdurws_ird_project_test` exit 0、`: error` 计数＝0；`--target sdurws_ird_project_contract_test` exit 0 零 error（acc-build-integration.log／acc-build-integration-contract.log，验收者目录留痕）。
- 独立冒烟模式（另一临时目录，`-S industrialrobot -DCMAKE_TOOLCHAIN_FILE=<repo根>/vcpkg/scripts/buildsystems/vcpkg.cmake`——AGENTS §4.1/CORE-T01 G-2 口径）：configure＋Release 构建两测试目标 exit 0 零 error。
- 两模式两目标测试亲手执行：集成 206/206＋15/15 全绿；冒烟 206/206＋15/15 全绿（`[  PASSED  ]` 尾行实测）。

### 4.3 契约 verify 命令 — 通过

- 契约 verify 两条逐条亲手执行，全部 exit 0：
  1. `cmake --build build --config Release --target sdurws_ird_project_test`
  2. `cmake --build build --config Release --target sdurws_ird_project_contract_test`
- `validate-task.ps1 -TaskFile tasks/foundation/PRJ-T10.json`（pwsh -File）：`validate-task: PASS (1 tasks)`、exit 0（acc-validate-task.log）。

### 4.4 acceptance 条目逐条 — 通过（条目 4 的内置命令族半区＝P-PR-9 治理阻断如实登记，未标注通过、未私裁；其余全部有具名测试证据，双模式亲手重跑全绿）

| # | 契约 acceptance 原文要点 | 证据（CommandContractTest 5＋CommandServiceTest 23；双模式 206/206＋15/15 全绿，XML 双留痕） |
| --- | --- | --- |
| 1 | PRJ-TX-1 并发与过期基线：双线程并发 submit 全局串行（第二个等待）、各恰一修订（seq 单调无重复）；expectedRevision=旧 tip → Rejected(stale-revision) 且无修订落盘（PRJ-STALE-REVISION-REJECTED） | 契约目标两用例：`CommandContractTx1.TwoConcurrentSubmits_SerializedExactlyOneRevisionEach`（双线程并发——各恰一修订/seq 相差恰 1/parent 线性链/HEAD＝后提交者/磁盘 revisions 恰 +2）、`StaleBaseline_RejectedStaleRevisionNoRevision`（旧 tip → Rejected(stale-revision)＋PRJ-STALE-REVISION-REJECTED 双通道〔sink 用户级＋结果内诊断〕＋无修订落盘＋分支 tip 不变）。双模式全绿；探针 1 短路 S2 过期判定后该用例变红（4.5）——非恒真实证。 |
| 2 | PRJ-TX-3 双编译桩三路：WC 失败/DWC 失败/双成功；任一失败→无修订无发布＋.staging/tmp 清理；成功→恰一修订（MDL-06；D-09 注入形态零 runtime 编译依赖） | 契约目标三用例：`WcFail_NoRevisionNoPublishTmpCleaned`／`DwcFail_NoRevisionNoPublishTmpCleaned`（Failed(compile-failed)＋端口诊断透传＋无修订＋对象库零新增＋.staging/tmp 清理＋编译输入观测——plannedWrites/baseRevision/查询端口可用）、`BothOk_ExactlyOneRevision`（恰一修订＋对象库 +2）。§5.3.6 三字段冻结签名消费（P-PR-7）——StubCompilePort 按冻结签名实现，零 runtime 链接（LinkageContractTest 零改动维持）。双模式全绿；探针 2 绕过 `!compiled.ok` 后两失败腿变红（4.5）。 |
| 3 | 命令服务骨架：S1～S7 时序与全局串行槽（§6.1）；未知命令/非法输入/过期修订拒绝码（§6.3）；载荷序列化与版本策略（§6.4）；IModelCompilePort 冻结签名（§5.3.6/P-PR-7） | 单元面 23 用例：S1 拒绝矩阵 5 例（UnknownCommandType／PayloadVersionMismatch→invalid-payload／UnknownBranch〔实现口径①〕／ReadOnlyContext→not-writable＋PRJ-LOCK-HELD＋error=LockHeldByOther／ClosedContext→同＋PRJ-WRITE-AUTHORITY-LOST＋error=ContextClosed）；S3 三态 2 例；S4 确认四路 5 例（unresolved/rejected/lost×2/放行——§6.7）；S6/S7 六例（恰一修订 seq 单调＋事件 FIFO／对象身份双路径／逆命令持久化 round-trip／历史未知命令 unresolved 重开三态〔§6.3 末行＋T09 注入点真判定〕／R2 预留字段拒绝）；串行槽直接观测 1 例（SecondSubmitWaitsWhileFirstInPrepare——300ms 超时证明"第二个等待"）；注册表契约 2 例（字典序 NFR-COR-02／重复与空 token invalid_argument）。§6.4 版本策略＝受理集合 {currentPayloadVersion()}——冻结接口的可表达形态（CommandService.hpp 文件头"增量落位说明 3"按 DTB §5.4 登记），多历史版本受理随首个域处理器演进扩接口，不预建。 |
| 4 | 内置元数据处理器（project.create-branch 等）与测试处理器（断言判定注入）就位（HandlerRegistry）；P-PR-3 处置 | **部分兑现（如实登记，不标注通过）**：测试处理器半区全交付——StubHandlers 7 个域处理器桩（AppendObject 双分配路径/HardAssertFail/InvalidInput/Findings/DualCompile/CreateBranch/Gate——断言判定注入即 P-PR-3 处置的测试侧形态）＋HandlerRegistry 全机制（注册/查询/字典序/边界拒绝）＋QueryPort 判据注入兑现（kInitialCommandType bootstrap 豁免）。**内置元数据命令族实体不落位＝P-PR-9 治理阻断**：§4.4.4 冻结语法 `^[a-z0-9-]{3,64}`（不含点）与 §6.5 含点示例 `project.create-branch` 的二选一属所有者裁决面——该待裁决项于 **v0.6（PRJ-T04，2026-09-15）预先登记**且明示"PRJ-T10 落位内置元数据命令族时注册 token 被字段校验拒绝……不在代码中私自裁决"（units/project.md §15.3 P-PR-9 行）。实施者按登记处置执行、Codec 拒绝语义不私改，符合 DTB §5.4"卡间/卡与上游冲突→待裁决表登记→所有者裁决后消账"路径与 ACC §6 裁决条款——**不判缩水，遗留为所有者裁决事项**；机制面就绪（注册/提交/持久化/查询判据），裁决后随增量任务落位处理器实体。建支**语义机制面**已由无点 token 测试处理器验证（CreateBranchMechanics_NoTipUpdateAddedBranchVisible——无 tipUpdate＋addedBranches base/tip=源 tip，§4.5.1 走查步骤 1 形态）。 |
| 5 | P-PR-6 处置（PRJ-* 码经 StableCodeRegistry 消费、IDiagnosticsSink 注入不直链）；P-PR-1 处置（IDomainEventBus/DomainEvent 以 core.md v0.1 为基线）；事件发布失败不回滚（D-18） | P-PR-6：`src/DiagRecords.hpp` 共享工厂三码 PRJ-WRITE-AUTHORITY-LOST／PRJ-LOCK-HELD／PRJ-STALE-REVISION-REJECTED 逐字取 diagnostics.md §4.6 收编清单（该文件行 379 十项 PRJ 码实证）——经构造注入 IDiagnosticsSink 产出（ReadOnlyContext/ClosedContext/StaleBaseline 用例断言码值），零 diagnostics 链接（跨单元 include 扫描仅 core）。P-PR-1：测试与实现消费 `sdurws/ird/core/Events.hpp`（core v0.1 基线），零 core 修改。D-18：`CommandSubmitS7.EventPublishFailure_DoesNotRollbackRevision`（FailingEventBus 注入——发布失败修订仍提交＋开发诊断可见，经工厂装配上下文端到端）。 |

### 4.5 测试真实失败能力 — 通过（双探针注入即红、还原复绿）

- 探针 1（S2 过期基线判定）：`CommandServiceImpl.cpp` S2 的 `if (envelope.expectedRevision.has_value() && ...)` 前加 `false &&` 短路 → 重建 → `--gtest_filter=CommandContractTx1.*`：**StaleBaseline_RejectedStaleRevisionNoRevision FAILED**——测试对 §6.2 并发校验真实敏感。
- 探针 2（S5 编译失败路径）：`if (!compiled.ok)` 改为 `if (false)` → 重建 → `--gtest_filter=CommandContractTx3.*`：**WcFail 与 DwcFail 两用例 FAILED**——测试对 MDL-06 原子性真实敏感。
- 还原（按 F-068 教训对还原文件 `touch` 更新 mtime 强制重编译）后 `git diff` 与送验对象一致、全量契约套件 15/15 复绿 3 连跑。探针仅存在于验收者临时目录，未触碰送验对象与任务分支。

### 4.6 架构红线（AGENTS §5 八条逐条） — 通过（零新增门禁命中；门禁亲手实跑）

1. **零 Qt**：project 全部 include 清单去重扫描（include/＋src/）——无任何 Qt/QString/QObject 头；纯标准库＋core 公共头＋本单元私有头。
2. **业务域互链（R-1）**：全部 `#include <sdurws/ird/...>` 仅 `ird/core/*` 与 `ird/project/*`（测试同）——零业务域单元直链。
3. **跨单元私有头（R-2）**：include/ 公共头零 `#include "..."` 私有头引用；CommandServiceImpl.hpp/DiagRecords.hpp 均置 src/（R-2 纪律，注释明示）。
4. **SA-02**：diff 无框架文件、patches/ 零改动（4.1 已证）。
5. **R-4 名称拼接**：新增源码 `grep "RobWork"`（剔注释）零命中；门禁 IRD-GATE-R4 命中的 1 处 "RobWork" 字面量实证为 T08 既有 `kCreatedWithToolVersion = "RobWork-IndustrialRobot/0.1"` 工具版本串（base 行 105＝head 行 107，git show 对照），非本任务引入。
6. **PA-1 权威归属**：submit 为唯一写路径入口、修订与事务归 project（经宿主唯一写通道 executeCommit）、诊断文案数据面归 project／呈现归 diagnostics——未越权实现他单元语义（P-PR-3 处置：业务断言全在注入处理器，本类零业务数值——代码实证）。
7. **PA-2 不可变历史**：submit 恰产生一个新修订（TxEngine 七步协议，T07 已验；本任务 PRJ-TX-1/BothOk 用例钉住"恰好一个"）。
8. **不恢复 old/**：全部代码从零编写，无 old/ 引用。
- **ird_gates 亲手实跑**（集成构建树 `--target ird_gates`）：命中 9 项＝7×IRD-GATE-SUB＋1×IRD-GATE-T1＋1×IRD-GATE-R4，与报告声称及 wp04-t08/t09 基线逐项一致——**零新增命中**（门禁按 SA-10 设计命中即 exit 1，属基线登记面）。

### 4.7 注释规范（AGENTS §2） — 通过

- 公共头全查：`CommandService.hpp`（683 行）——文件头设计依据/背景/增量落位说明三项/线程模型齐备；每类型 Doxygen 中文描述＋线程安全＋错误语义；每方法 @param 方向/@return/@throws；§2.5 高危信息（线程约束、所有权非 owning、错误二分、确定性 NFR-COR-02）逐项在位。
- 实现文件抽查 4 个（≥3 达标）：`CommandServiceImpl.hpp`（实现口径①～⑥＋P-PR-9 阻断登记＋线程模型段）、`CommandServiceImpl.cpp`（S1～S7 每段"为什么"＋失败路径去向＋StoreError 映射面）、`DiagRecords.hpp`（码值收编来源＋纯函数线程安全）、`ProjectStoreImpl.cpp` 修改段（kInitialCommandType bootstrap 豁免理由＋collector 生命周期缺陷修复注释＋commands() 装配不变量）——单位/坐标系/错误语义/确定性来源标注齐备。
- 测试文件头 3 个（CommandServiceTest/CommandContractTest/StubHandlers）均带契约 acceptance 追溯与测试口径登记；CMakeLists 增列段中文注释块说明产物与 acceptance 对应。

### 4.8 文档同步与 findings 关联核对 — 通过（偏差全部按 DTB §5.4 登记；findings 既有 open 条目相关性逐项核对）

- 单元卡 v0.12 增量登记（提交 e9d6da3f）：落位清单、实现口径六项、P-PR-9 阻断如实登记、缺陷修复登记（T08 遗留 CollectingSink 悬空——diff 实证 shared_ptr 堆分配＋m_collectorLifetime 生命周期锚修复，声明序最先＝析构最后）、测试与门禁结论——与实际 diff 逐项核对一致；卡头版本行 v0.11→v0.12 已同步。本文契约语义零变更。
- 偏差登记路径合规：P-PR-9（v0.6 预登记→v0.12 处置登记）、增量落位说明 1～3（R2 预留字段/编译临时目录冻结面/版本受理集合）——均单元卡明文登记，无"代码先行、文档失实"。
- **findings.json 未关闭条目与本任务相关性（ACC §4.8 v1.2）**：
  - F-007/F-062/F-098/F-099/F-101（全新树集成配置环境链，open）：本验收亲手踩中两处（Boost、ini 模板）并按其处置建议补齐——**持续有效，责任方构建约定所有者，非本任务可消账**；
  - F-010 及其根因家族（.gitignore *.log 吞没留痕，open）：**本任务再次踩中**（留痕提交 11 份日志全数被吞、零入库）→转登 F-153；
  - F-012 标题家族（open）：本任务送验 4 提交中 2 例（e9d6da3f/37d05852）前缀后名词短语开头 →转登 F-154；
  - F-122/F-133/F-134/F-140/F-143/F-147 卡头日期家族（open）：units/project.md 日期行停留 2026-09-09（v0.12 触及卡头版本行而日期行第七跳未落地）→转登 F-155；
  - F-144/F-149 家族（实现类任务未实跑门禁自查）：**本任务不适用**——ird_gates 实跑且结论经独立复现为真；
  - F-019/F-051/F-072/F-115/F-145（门禁基线既有命中，open）：与基线一致零新增（4.6 实证）——维持既有登记，非本任务消账范围。
- 遗留待裁决：P-PR-9（units/project.md §15.3，project 详设所有者）——本验收维持其 open 状态，**合入后 PRJ-T10 不得视为"命令服务完全终结"，内置命令族仍需增量任务承接**。

### 4.9 commit 质量 — 通过（四段式齐备、结论与独立复现一致；两例标题家族问题转登）

- 4 提交（c6527070 实现／43925b3c 测试／e9d6da3f 单元卡／37d05852 留痕）：标题 `[WP-04-T10]`×2＋`[units]`/`[governance]`（AGENTS §6.2 文档前缀合规）；正文四段（What/Why/验证/影响面）齐备且附 Acc-Covered 逐条映射。
- 验证结论真实性核对（防粉饰）：206/206＋15/15（双模式亲手复现一致）、门禁 9 命中零新增（亲手实跑一致）、validate-task PASS（亲手实跑一致）、P-PR-9 部分交付如实申报（与代码事实一致）——**全部结论面无粉饰**；声称入库的 11 份日志未落库＝包装面失实（→F-152/F-153，家族既有定性为建议级）。
- 标题动词开头：c6527070『实现…』、43925b3c『新增…』合规；e9d6da3f『project.md v0.12：…』、37d05852『PRJ-T10 验证留痕：…』名词短语开头→F-154（F-012 家族，建议级）。

### 4.10 留痕完整 — 通过（XML×4＋ird-test-report.json 落位且与实跑一致；日志缺位如实登记）

- 四份 gtest XML：tests="206" failures="0"（双模式）／tests="15" failures="0"（双模式）——与本验收亲手执行结果一致。
- ird-test-report.json：partialDelivery（P-PR-9）/acceptanceCoverage（4=部分——如实）/构建记录/门禁状态/实现口径六项/缺陷修复登记，与 diff 及独立复现逐项一致。
- **构建/门禁/validate 日志 11 份（integration-build.log 等）声称在库而实际零入库**（.gitignore:87 `*.log` 全局规则吞没——git check-ignore 实证；既有先例 wp02-t02/wp02-t08 等同型日志均以 -f 强加入库）→转登 F-152/F-153。本验收以亲手复现补齐该证据面（ACC §2.4 复现优先于采信），结论不受影响。

### 4.11 偷懒/缩水实现扫描 — 通过

1. **标记扫描**：生产代码（include/＋src/ 新增与修改段）`TODO|FIXME|stub|placeholder|未实现|not implemented` 零命中；"stub" 命中全部为①测试桩类名（StubHandlers——契约 §12 点名产物 test/StubHandlers.\*，合法测试基建）②ProjectStore.hpp:35 注释引用"只能 stub，违反禁止空实现纪律"的设计决策文本——豁免依据成立。
2. **空实现检测**：公共接口零空函数体/仅返回默认值实现（setCompilePort 单行指针赋值＝注释文档化的装配通道；HandlerRegistry pimpl 全实现于 .cpp）。
3. **acceptance 逐条证据表**：见 4.4——条目 1/2/3/5 具名测试＋双模式实跑；条目 4 部分兑现（治理阻断按 DTB §5.4 登记，非无声缩水——机制面全交付且有建支语义机制用例）。
4. **测试强度**：断言校验行为与边界（恰一修订/seq 单调/磁盘 revisions 计数/稳定码双通道/字节透传/逆命令 round-trip/300ms 槽等待观测），非恒真非"只编译通过"——双探针实证敏感（4.5）。

---

## 2. 发现问题清单

**阻断级：无。**

**建议级（5 项，随本验收提交转登 traceability/findings.json，编号顺延 F-152～F-156）：**

| 编号 | 摘要 | 责任方 |
| --- | --- | --- |
| F-152 | 幻影清单家族再现且规模之最：留痕提交 37d05852 正文与 ird-test-report.json 声称 11 份日志，实际零入库 | 治理约定所有者／实施纪律 |
| F-153 | F-010 根因第八次踩中：.gitignore `*.log` 全局规则吞没 wp04-t10 全部留痕日志（git check-ignore 实证） | 构建约定所有者 |
| F-154 | F-012 标题家族再现（一次两例）：e9d6da3f／37d05852 前缀后名词短语开头 | 实施纪律 |
| F-155 | F-122→F-147 家族第七次再现：units/project.md 卡头日期行停留 2026-09-09（v0.12 版本半边已同步、日期半边未落地） | project 详设所有者 |
| F-156 | 既有用例 LockContractTest.CrashThenConcurrentTakeover_SingleWinner_D02 负载下偶发失败观察（探针还原后首轮 1 次失败，随后 3 连跑＋前后共 5 次全绿；非本任务新增用例） | 测试约定所有者 |

**遗留待所有者裁决（非新发现，维持既有登记）**：P-PR-9——内置元数据命令族 token 语法二选一（units/project.md §15.3）；裁决后需增量任务落位处理器实体与注册（机制面本任务已就绪）。

## 3. 验收者环境说明

- 复现现场：worktree A＝`D:/10_Source_Repos/21_robot/acc_prjt10_repro`（detached @ 37d05852）；冒烟＝`D:/10_Source_Repos/21_robot/acc_prjt10_smoke`；记录回传＝worktree B＝`D:/10_Source_Repos/21_robot/acc_prjt10_record`（分支 acc/PRJ-T10/1，基于 origin/redesign-main＝a02a8a9d）。验收后两临时目录删除（推送成功确认后）。
- 集成配置补参（F-007/F-099 已登记口径）：vcpkg toolchain 绝对路径＋Qt 前缀 `D:/software/Qt/6.11.1/msvc2022_64`＋自主树复制 RobWorkStudio.ini.template.static；配置 exit 0 后缓存确认 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`。
- 4.5 探针仅注入验收者临时目录源码副本并当场还原（还原后 `git diff` 与送验对象一致），送验 SHA 与任务分支零触碰。
- 本记录与 findings.json 转登为 worktree B 内单一提交，推送 `origin acc/PRJ-T10/1`；合入口径＝本 evidence 提交 SHA（PIPE §4.7 按 SHA 合并）。
