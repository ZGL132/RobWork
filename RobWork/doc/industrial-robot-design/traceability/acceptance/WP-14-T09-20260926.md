# WP-14-T09 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | WP-14-T09 requirements 契约测试套件与四套黄金数据集收口 |
| **verdict** | **pass**（4.1~4.11 全部通过；无阻断级问题；建议级 3 项 F-377/F-378/F-379 已转登 findings.json，不阻塞合入） |
| 送验对象（冻结） | aa4630078b53803b5aedbc3c2be61c7172c30b20（四提交：70d8c9d3 黄金数据集＋容差档案、d0285f5d 两契约套件、7138f7fa 单元卡 v0.9、aa463007 验证留痕） |
| base | a5b3b397f96109adb2fc377aefb0a9c1e5349fae（WP-14-T08 合入收尾＝state.base） |
| 验收日期 | 2026-09-26 |
| 复现现场 | detached worktree `D:/10_Source_Repos/21_robot/acc-t09-a` @ aa463007（集成 `D:/10_Source_Repos/21_robot/acc-t09-build-a`、冒烟 `D:/10_Source_Repos/21_robot/acc-t09-smoke`）；base 侧门禁比对 detached worktree `D:/10_Source_Repos/21_robot/acc-t09-base`；验收者证据目录 `D:/10_Source_Repos/21_robot/acc-t09-evidence/`（gtest XML 自留件＋lint 日志＋门禁归一化件） |
| 独立性 | 全新上下文，未消费实施叙述；构建/测试/门禁/变异/lint/治理脚本全部验收者亲手重跑；主检出（redesign-main @ 9b8de01b，WP-24 系并发会话占用）零触碰、零 git 写操作（仅只读复制 gitignored 模板 `RobWorkStudio.ini.template.static` 一件——T02~T08 验收同款前置）；实施者保留的 `wt-wp14-t09` 构建树未复用（验收者全自建，规避只读边界歧义） |

**分支漂移核查（ACC §3）**：`git ls-remote origin refs/heads/wp14-t09` ＝ `aa4630078b53803b5aedbc3c2be61c7172c30b20`，与送验 headSha 逐字节一致——零漂移。`refs/heads/acc/WP-14-T09/1` 远程不存在（attempt 1 首验，无分支名冲突）。

---

## 4.1 改动范围 —— 通过

`git diff --name-status a5b3b397..aa463007`：38 文件（13406 insertions），**逐文件均落在契约 allowedFiles glob 内**：

- `requirements/**`（3 件）：CMakeLists.txt（M，两测试目标源清单增列＋注释块）、test/GoldenDatasetsContractTest.cpp（A，2021 行）、contract_test/PeerFaceContractTest.cpp（A，757 行）；
- `testdata/golden/req-*/**`（19 件）：四套数据集各 1.0.0/ 四件（inputs/expected/generate/manifest）；
- `testdata/tolerance/req-golden/v1.0.0.json`（A，符合 `tolerance/req-*/**` glob）；
- `traceability/builds/wp14-t09/**`（14 件留痕）＋`units/requirements.md`（M，v0.8→v0.9）。

**forbiddenFiles 零触碰**：REQUIREMENTS.md／ARCHITECTURE.md 不在 diff 中。**SA-02**：diff 无任何框架源码文件，`patches/` 零新文件。范围内恰 4 个提交（`git log a5b3b397..aa463007` 实证）。

## 4.2 可复现构建 —— 通过（验收者全新构建树，双模式零错误）

- **环境前置**：worktree 检出缺 gitignored 模板 `RobWork/RobWorkStudio/bin/RobWorkStudio.ini.template.static`（configure_file 报错复现）——从主检出只读复制后配置成功（T02~T08 验收实录同款现象与处置）。
- **集成模式**：`cmake -S <worktreeA>/RobWork -B acc-t09-build-a -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/QT/6.11.1/msvc2022_64`（口径＝主构建树缓存与留痕 README 登记的配法）；`grep RWS_BUILD_INDUSTRIALROBOT CMakeCache.txt` → `:BOOL=ON`；configure 输出 `Current Git revision is aa463007`（构建树确指送验 SHA）。`cmake --build --config Release --target sdurws_ird_requirements_test sdurws_ird_requirements_contract_test sdurws_ird_testdata_lint` 零错误（exit 0），两测试 exe ＋ lint exe 产出。
- **独立冒烟模式**：`cmake -S <worktreeA>/RobWork/RobWorkStudio/src/rwslibs/industrialrobot -B acc-t09-smoke -DCMAKE_TOOLCHAIN_FILE=… -DCMAKE_PREFIX_PATH=…` 配置成功；构建两测试目标零错误（exit 0）。
- **测试执行（验收者亲手，两模式各自全量）**：集成树 `sdurws_ird_requirements_test` → **183 tests ran，182 PASSED，1 SKIPPED**（恰 `GuiRegistration.V22RegisteredNotExecuted_ACC1`——V-22 登记性跳过，与通过分开计数）；`sdurws_ird_requirements_contract_test` → **11 tests ran，11 PASSED**。冒烟树逐字节同口径复跑：183＝182＋1 跳过、11/11——两模式一致。
- **过程纠偏（如实登记）**：验收者首次以相对路径 `-S RobWork` 从主检出 cwd 起跑，构建树实际配置了主检出源码（非送验 SHA，测试仅 161 用例暴露差异）——该树已整目录删除，以 worktreeA 绝对路径重配并经 `Current Git revision is aa463007` 输出与用例数 183 双重确证。主检出 tracked 文件经 `git status` 复核零新增改动（configure 不写源码树）。

## 4.3 契约 verify 命令 —— 通过（4/4 逐条亲手执行）

| # | 契约 verify 原文 | 验收者执行 | 结果 |
| --- | --- | --- | --- |
| 1 | `cmake --build build --config Release --target sdurws_ird_requirements_test` | 于验收者集成树 acc-t09-build-a 同参执行（build 树路径不同属验收环境事实） | 成功零错误，exe 产出 |
| 2 | `cmake --build build --config Release --target sdurws_ird_requirements_contract_test` | 同上 | 成功零错误，exe 产出 |
| 3 | `ctest --test-dir build/RobWorkStudio/src/rwslibs/industrialrobot/requirements -C Release -L "^ird$"` | 同上同参执行 | **100% tests passed, 2/2**（requirements_test 0.12s＋contract_test 0.04s） |
| 4 | `pwsh -File RobWork/scripts/industrialrobot/validate-docs.ps1` | 于 worktreeA（送验 SHA 树）执行 | **PASS (20 units, 12 trace entries, 189 task files)** |

补做：`validate-task.ps1 -TaskFile tasks/foundation/WP-14-T09.json` → PASS (1 tasks)；`sdurws_ird_testdata_lint --root <testdata> --dict <testdata>/requirements-ids.json` → **扫描 13 个数据集版本，违规 0**（含本任务四套 req-*；`--dict` 显式传jar口径＝实施登记的缺省字典路径缺陷绕行，见 F-016）。

## 4.4 acceptance 条目逐项证据 —— 通过（6/6）

1. **§10.2 矩阵 requirements 侧可执行行全部落位执行＋留痕**——通过。T09 新套件 `GoldenDatasetsContractTest.cpp` 22 用例逐 V 具名：V-01（`V01EditGuardAndSingleApplyEnvelope_ACC1`——增点/改名/被引用点删除拒绝＋拒绝态字节不变＋恰一命令五槽载荷往返）、V-02（`V02DegenerateRefusedAndZeroPlanLegal_ACC1`——退化拒绝＋零样本计划合法可编码＋R6 `REQ-READY-PLAN-MISSING` Warning）、V-03（`V03OrientationResolutionAndIllegalLocalized_ACC1`——五规则解析留痕＋零向量目标 `REQ-READY-POSE-ILLEGAL` subject/localName/cause 三要素定位）、V-04（`V04DuplicateAndDanglingBlocked_ReqSideJointRegistered_ACC1`——重复名拒绝＋悬空 R4 Blocking；"漏验拦截"明注联合观测不写通过）、V-05（`V05RequiredCasesFrozenSchema_ACC1`——resolveRequiredCases 对照 expected 独立重算表逐字段＋mandatory≡(level==Must) 机械复核）、V-06（`V06MirrorGoldenReflectionAndProvenance_ACC1`——法向 Y 反射黄金分量＋Fixed 闭式 (−r,+p,−y)＋独立 ObjectId/溯源完整/顺序键与导入溯源不继承/源条目零回写）、V-08/V-09（`V08V09CsvFacesPartialSuccessAndUnits_ACC1`——RawTable 直构〔P-REQ-8 口径，卡 v0.9⑤ 已增量登记〕650 mm→0.65 m／90 deg→π/2 SI 双断言＋行级部分成功＋REQ-IMPORT-ROW-ERROR/DUPLICATE-ID/FRAME-UNKNOWN 定位）、V-10（`V10JsonRoundtripAndCopyExport_ACC1`）、V-11（`V11EmptySetLegalAndR5WarningProjection_ACC1`——空集编码往返＋R5 Warning＋ReadinessSummary 投影）、V-15/V-16/V-19（`V15V16V19SliceFacesJointRegistered_ACC1`——改工况 note 仅 ConditionSet 字节变＋同输入同解析＋分母对照黄金表＋req.* 四键词表；联合行明注登记）、V-17/V-18（`V17V18UnitAndIdentityFaces_ACC1`）、V-20/V-21（`V20V21PreviewZeroMutationJointRegistered_ACC1`——纯函数双调用同报告＋外部工具对象变化零响应）。**V-12/V-13**（契约列名的另两行）requirements 侧断言在前序任务测试落位且随本次全量 183 用例真实执行：V-12＝`ReqReqCommands.ReadinessBlockingRejectsPrepare_ACC6`（RejectedHardAssert＋REQ-READY-REF-MISSING 逐项定位，CommandHandlersTest.cpp L481/L510）＋ReadinessTest"无 EngineeringStatus 输出面"结构断言；V-13＝`PluginPanel.DraftSourceRoundtrip_ACC4`（V-13 模型半区）＋V01 用例 loadBaseline 恢复面。V-07/V-14 同理在前序文件（TemplateArrayTest V-07 逐断言；T05/T06 面）——收口状态成立，未把联合观测行整体写为通过。**V-22**：`V22RegisteredNotExecuted_ACC1` 以 GTEST_SKIP＋`setOutcome(EnvUnavailable)` 登记——ird-test-report-unit.json 该记录 `outcome: envUnavailable`（182 passed 与 1 envUnavailable 分开计数），未绿灯。
2. **四套黄金数据集交付＋lint 通过＋容差档案**——通过。四套目录齐全（req-transport-task/req-partial-pose/req-region-sampling/req-payload-events 各 1.0.0 四件）；验收者对 12 个申报文件逐个实算 `sha256sum`＋字节数，**与 manifest integrity 申报逐项一致**（12/12 OK）；manifest 契约面（ird-golden-manifest/1）逐字段核对：datasetId/版本目录一致、kind=contract-fixture、coveredRequirements/coveredAt、generator.committed=true、history 首版留痕、edgeCases 三布尔（zeroValue/nearZero/signCancellation）＋sampleRefs。边角样例实值置位验证：PE-full mass=0.0 provided（零值）、com.z=1e-18（近零）、x+y=±0.05 抵消对、invalid 保留原串。容差档案 `tolerance/req-golden/v1.0.0.json`：basis 锚 appendixD#12＋#4（测试 `ReqGoldenTolerance.ProfileAnchoredAndNotRelaxed_WP14T09_ACC2` 逐条断言七条目零宽容差且 tolerance==allowedMax——无放宽通道——＋七条消费 fieldPath 前置可解析）。lint 全量 13 版本 0 违规（验收者亲手复跑，见 4.3 补做行）。GoldenFixture 六步生命周期随 CI 双模式跑通（4.2 两树全绿）。
3. **T-1/T-2 测试侧红线用例通过**——通过。具名测试 `PeerRedLine.T1ProductFaceZeroTestkitHeaders_ACC3`（include/＋src/＋plugin/ 三面 #include 指令行扫描零 testkit 段，扫描面 >20 文件防失效）与 `PeerRedLine.T2TestTargetLinkFormAcyclicAndTestdataExclusion_ACC3`（CMakeLists 链接语句恰 {被测目标, sdurws_ird_testkit, GTest::gtest} 允许形态、testkit 构建脚本零 requirements 反向边、requirements 零 install 语句）均绿；验收者独立 grep 双新测试文件 include 面：零 Qt、零业务域互链单元、跨单元仅 core/evidence/io/project/diagnostics/testkit 公共头（`include/sdurws/ird/` 路径）。门禁 T1/T2 命中 base..head 零增量（见门禁专段）。
4. **contract_test 对端面套件**——通过。`PeerFaceContractTest.cpp` 6 用例：①`PeerProject.PayloadShapePreparePlanAndQueryFault_ACC4`——真实 prepare 管线 Planned＋写槽 token 词表逐槽断言＋FaultInterceptor（project/query/object 缝）runtime_error fail-fast 透传＋FaultLog 命中观测；②`PeerIo.MapCsvRawTableShapeAndAtomicCommitFault_ACC4`——rows 空而 dataRows>0 形状违约 invalid_argument＋IAtomicFileWriter commit 注入 ResAccessDenied → ok=false＋旧文件**字节级**完好＋命中日志；③`PeerEvidence.ReadinessSummaryProjectionShapeAndFault_ACC4`——两字段值类型与 evidence::ReadinessSummary 字面复核＋RecordingSink 消费＋evidence/sink/accept 第 2 次注入 throw＋命中观测；④`PeerDiagnostics.StableCodesRegisteredContractFixture_ACC4`——16 码逐码 find 命中＋ownerUnit/前缀一致＋恰 16 码无越权。对端形状漂移由测试面捕获、未构造产品边（fake 编译进测试目标——TK-T09 接缝原则，T-1 扫描互证）。
5. **确定性断言（NFR-COR-01/02/03）**——通过。`DeterminismFaces.SameInputSameBytesAndCanonicalSort_ACC5`：四数据集 8 个集合/根对象重复编码逐字节相等＋**任意输入序编码同字节（I-REQ-1 规范化写出复验）**＋显式规范化后同字节＋存储序严格升序机械核对（checkedSets==8 断言防缩水）；`DeterminismFaces.NonFiniteAndIllegalNotSilent_ACC5` 四段负样例：0.5 位模式→quiet NaN 字节注入 decode 拒绝（MalformedPayload）、NaN 容差构造拒绝、"furlong" 非法单位 → REQ-IMPORT-UNIT-ILLEGAL＋unitUsable=false＋零换算条目、缺失引用 AlignFrame → REQ-READY-REF-MISSING；另 `PartialPoseGoldenTest.NegativeSamplesFirstErrorCode_ACC5`（5 条负样例首违例码对照独立转写表）与 `PayloadEventsGoldenTest.NegativeCasesAndR4Blocking_ACC5`（重复名/Stations 空清单/悬空 R4/invalid 空原串 throw）。
6. **双模式构建＋ctest 全绿＋ird_gates＋矩阵刷新**——通过。见 4.2（双树零错误＋全绿）、4.3（ctest 2/2）、门禁专段（全码零增量）与 4.8（§13/§11/卡头 v0.9 同步核对）。

## 4.5 测试真实失败能力 —— 通过（2 处实质变异全真变红，还原后复绿）

变异窗口＝worktreeA 源树＋冒烟构建树增量重建；每处变异→具名测试真实变红→`git checkout --` 还原；两处还原后全量重建复跑 183＝182＋1 跳过＋11/11 全绿，`git status --porcelain` 零残留（送验对象未被改动）。

| # | 变异位置与内容 | 被验证测试 | 结果 |
| --- | --- | --- | --- |
| M1 | Services.cpp L309：D-REQ-2 规范化 `static_cast<std::uint32_t>(clamped) + 1u` → `+ 2u`（破坏 floor 规则黄金锚定） | RegionSamplingGoldenTest.V02NormalizationSynonymAndZeroSample_ACC1＋TransportGoldenTest.V15V16V19SliceFacesJointRegistered_ACC1 | **2 FAILED**（黄金计数与计划分母断言捕获——期望值非"照抄实现"） |
| M2 | Codec.cpp L1600：集合条目规范化写出 `sortEntriesByObjectId(entries)` → `std::reverse(entries…)`（破坏确定性排序写出） | DeterminismFaces.SameInputSameBytesAndCanonicalSort_ACC5 | **FAILED**（"乱序输入编码后与原序逐字节一致"断言捕获——字节级差异实证） |

## 4.6 架构红线 —— 通过（AGENTS §5 八条逐条）

零 Qt（双新测试文件与两测试目标零 Qt include/链接——ird_gates R3 零增量佐证）；无跨单元私有头（include 面全为 `sdurws/ird/<unit>/` 公共路径）；无业务域互链（requirements 链接边零改动，R1 零增量）；无名称前缀拼接（R4 零增量）；权威归属（测试仅消费各对端公共值类型做形状断言，project 写路径经真实 HandlerContext prepare 管线——无越权实现）；不可变历史（本任务为测试/数据资产新增，零修订写路径触碰）；SA-02（4.1 已证）；`old/` 零引用（diff 无 old/ 路径）。T-1/T-2 具名测试＋门禁 T1/T2/SUB 零增量为机器佐证。

## 4.7 注释规范 —— 通过（AGENTS §2 抽查 4 个新增/大改文件；1 处注释失准转登建议级）

- `GoldenDatasetsContractTest.cpp`（公共测试面，全查）：文件头 Doxygen 中文块（设计依据逐条到卡 §/testkit §/契约 acceptance 条目＋双实现互证口径＋值模型重建口径＋模式约束）；每用例中文说明"验证哪条需求/AT"（IRD_TEST_INFO 需求/AT 追溯字段＋注释块）；§2.5 高危信息齐备——单位（m/rad/kg/kg·m²/s 显式）、坐标系（refFrame 系/refFrame 恒 World＋"坐标变换归 runtime"边界）、角度制式（rad 非 deg 显式、π/2 黄金换算）、错误语义（构造边界值面 vs 就绪层诊断分层）、确定性来源（IEEE754 位级、caseId 规范文本字典序）。
- `PeerFaceContractTest.cpp`（全查）：文件头（TK-T09 接缝原则、四对端清单、T-1/T-2 依据）；故障点标识 `<unit>/<接口>/<动作>` 三段全注释；替身生命周期（"单线程用例内使用"）。
- `CMakeLists.txt`（M 段全查）：T09 增列中文注释块（acceptance 条目对应、V 行清单、V-22 登记）。
- `make_transport_task_golden.mjs`（抽查）：中文块注释七步生成逻辑＋"独立重算"语义＋LF 行尾登记。
- **例外（建议级 F-379）**：GoldenDatasetsContractTest.cpp L1846-1847 注释"乱序：编码门拒绝（编码器只接受规范化集合——不静默重排）"与紧随断言 `EXPECT_EQ(encodeOk(codec, shuffled), bytes)`（规范化**写出**且成功）矛盾——断言与卡 v0.9②/实现语义一致，注释为早期方案残留，违反 §2.5"注释随代码更新"。不影响行为与测试强度，转登不阻断。

## 4.8 文档同步＋findings 相关性 —— 通过

- **卡 §14.6 v0.9 行**与实现逐项对查为真：四套数据集目录/档案文件/两测试文件均在 diff 内且内容相符（lint 13 版本 0 违规、GoldenFixture 六步、V-22 envUnavailable、TK-T09 四对端、T-1/T-2 四断言面）；两项增量登记（⑤V-08 CSV 经 RawTable 直构——实测 V08 用例确为直构 RawTable 非 CSV 文件；lint `--dict` 显式传参——验收者裸跑缺省路径复现同款报错 `testkit/..\requirements-ids.json`）均属实且按 DTB §5.4 落卡。
- **卡 §13 矩阵刷新**：NFR-COR-01/02/03 行与附录 D 行"〔已落位——WP-14-T09：…〕"标记与 §11 T09 行落位标记在案，指向的交付物实测存在。
- **卡头版本行**（F-369/374 家族教训）：卡头 `文档版本 | v0.9（2026-09-26，WP-14-T09 落位登记——§14.6；…）` 与 §14.6 末行 v0.9 同步——家族主缺陷（最高版本失同步/少报）已修复。
- **findings.json 未关闭条目相关性核对**（v1.2 义务；open 共 307 条，逐条以 requirements/黄金/lint/门禁/标题家族关键词过滤后逐条判读）：
  - F-016（lint 缺省字典路径解析错位）——**与本任务直接相关**：本任务以 `--dict` 显式传参绕行并按卡 v0.9⑤ 登记成因；修正责任方仍为 testkit（TK-T03）所有者，维持 open 不消账（非本任务所有权）。
  - F-359（WP-14-T02 引入 +2 门禁 SUB：requirements_test/_contract_test 直链 testkit）——本任务 head 归一化命中中该两条在案且 base/head 零增量（存量）；T-1/T-2 具名测试断言的恰为该形态合法性，DTB §4.5 例外登记归治理面，维持 open。
  - F-363（canonical 魔数 "IRDREQO" 字面量无测试钉住）——**本任务可顺手消账而未消账，如实注明**：黄金 expected 为语义投影表非字节 dump，canonical 往返断言不锁定魔数（变异 kMagic 不影响对称往返）；本套件未补魔数钉子用例。维持 open，建议后续触及 Codec 的任务补钉（已在本记录留证）。
  - F-300 家族（契约 verify 数组缺行）——本契约 verify 数组含 4 条（两构建＋ctest＋validate-docs），无缺行，非本例。
  - F-361/364/366（提交标题家族）——**本任务再现新例**，转登 F-377（见 4.9）。

## 4.9 commit 质量 —— 通过（正文四段齐备且验证结论与留痕一致；标题格式偏差转登建议级 F-377）

四提交正文均为 What/Why/验证/影响面四段＋Acc-Covered 尾注；验证段计数（183＝182＋1 跳过、11 全通过、ctest 2/2、lint 13 版本 0 违规、门禁零增量）与验收者实测及机器留痕（gtest XML tests="183"/"11"、ird-test-report 182 passed＋1 envUnavailable／11 passed）逐项一致——无粉饰。**偏差**：四提交标题（70d8c9d3「新增 requirements 四套黄金数据集…」等）均缺 `[WP-14-T09]` 前缀，违反 AGENTS §6.3 标题格式且同波次 T07/T08 先例均有前缀——F-012/F-100/F-361/F-364/F-366 家族新例，转登 **F-377**（建议级：ACC §5 阻断清单不含 commit 格式；家族先例均 suggestion 转登后 pass 合入）。

## 4.10 留痕完整 —— 通过

三件留痕齐备且验收者实测一致：`gtest-requirements-test.xml`（tests="183" failures="0"、恰 1 处 status="skipped"＝V-22）、`gtest-requirements-contract-test.xml`（tests="11" failures="0"）原样留存（XML 内路径/时间戳未经修饰）；`ird-test-report-unit.json`（182 passed＋1 envUnavailable 分开计数）＋`ird-test-report-contract.json`（11 passed）；构建/运行/lint/ctest/validate 日志 14 件落 `traceability/builds/wp14-t09/`。`phase-one-validation.log` 为 phase-one 专项载体（WP-14 系 T03~T08 均不登记，非本波次适用）；verify 逐条执行登记由留痕 README 表承载。

## 4.11 偷懒/缩水实现扫描 —— 通过

①TODO/FIXME/stub/placeholder/未实现/not implemented 扫描（diff 新增行，unified=0）：命中 8 行全部为**留痕日志文件**中 ui 单元存量门禁输出的既有文件名 `View3DPlaceholder.cpp`（框架既有文件，WP-24 系范围；IRD-GATE-R3 存量命中在 base 同样在案——零增量），新增源码/数据资产/文档零命中；无"触发式延后"豁免需列出。②空实现检测：本任务零新增产品接口（纯测试＋数据资产），不适用；测试夹具的 fake 实现均为完整消费者自有接口实现（非空桩）。③逐条证据表＝4.4（6/6 均具名测试/命令输出/文件行号级证据，无"已实现"声明采信）。④测试强度：非恒真——4.5 两处实质变异实证真变红；断言校验行为与边界（字节级/位模式/字典序/黄金分量级对照）而非仅编译通过。

## 门禁专段（ird_gates base..head 归一化比对 —— 全码零增量）

口径＝编排者补丁第 2 条（引擎直跑，非 VS 目标；VS 目标 `ird_gates` 退出码 1 属 DTB §4.5 存量例外未清零既有状态）。验收者独立执行：

- base 侧：`git worktree add --detach acc-t09-base a5b3b397` → `cmake -DIRD_ROOT=<acc-t09-base>/…/industrialrobot -P cmake/ird_gates.cmake`（exit 1＝存量命中）；
- head 侧：同引擎对 acc-t09-a（exit 1 同口径）；
- 归一化：命中行路径前缀→`<IRD_ROOT>`→排序；
- **结果：base 135 行 vs head 135 行，diff 零输出（逐行全等）＝全码零增量**。按码分布：LIB 10/R1 7/R3 41/R4 3/R5 1/SUB 67/T1 5/T2 1（两侧一致；实施者 comparison.md 直方图 64 处命中为"唯一命中处数"口径，与附件行数口径差异转登 F-378，其 base/head 增量结论与验收者实测一致）。
- 附件：`acc-t09-evidence/gates/acc-gates-{base,head}-norm.txt`（验收者自留，随本记录摘录于下）。

<details><summary>验收者门禁归一化命中分布（head）</summary>

```
  10 IRD-GATE-LIB
   7 IRD-GATE-R1
  41 IRD-GATE-R3
   3 IRD-GATE-R4
   1 IRD-GATE-R5
  67 IRD-GATE-SUB
   5 IRD-GATE-T1
   1 IRD-GATE-T2
（base 侧同分布；135 行 diff 零输出）
```
</details>

## 建议级问题转登清单（findings.json，编号顺延自 F-376）

| 编号 | 一句话摘要 | 家族/依据 |
| --- | --- | --- |
| F-377 | WP-14-T09 四个送验提交标题均缺 `[WP-14-T09]` 前缀（F-012/F-100/F-361/F-364/F-366 家族新例） | AGENTS §6.3；4.9 |
| F-378 | 留痕 ird_gates_base_head_comparison.md 直方图计数（合计 64 处）与归一化附件行数（head 144 行/唯一 143）口径不一致且未注明口径（F-041/F-370/F-376 散文计数家族近邻；零增量结论本身经验收者独立直跑证实成立） | F-370/F-376 家族；门禁专段 |
| F-379 | GoldenDatasetsContractTest.cpp L1846-1847 注释「乱序：编码门拒绝」与断言（规范化写出且与原序同字节）矛盾——早期方案残留，违反 AGENTS §2.5 注释随代码更新 | AGENTS §2.5；4.7 |

## 验收者环境说明

- Windows 10（Git Bash＋pwsh 7）；MSVC VS2022 x64；Qt 6.11.1 msvc2022_64；vcpkg 经典模式（GTest CONFIG）。
- 残留进程检查：无 RobWorkStudio/msbuild/ctest 残留进程（tasklist 实查）。
- 推送口径：显式 refspec `git push origin HEAD:refs/heads/acc/WP-14-T09/1`＋`git ls-remote` 核实（编排者补丁第 6 条）。
- 本记录与 F-377/378/379 转登为同一提交（先例：F-375/376 转登提交形态）。
