# WP-13-T12 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | WP-13-T12 实现 RobotDesignReader 与 Description 构造（编译协作桥 CanonicalBridge） |
| 送验对象（冻结） | `541f554c1261085b8bac08c7563e98035d11bde0`（origin/wp13-t12 尖端；diff 范围＝`1752bb853dc96d9f485b3a143e34df45795e1446..541f554c…`，2 提交：d8f310f8 实现＋541f554c 文档留痕） |
| 裁决 | **pass**（4.1～4.11 全部通过；无阻断级问题；建议级 2 条已转登 findings.json：F-348、F-349） |
| 记录分支 | `acc/WP-13-T12/1`（基于 origin/redesign-main @ 1752bb85） |

## 0. 独立性声明与环境（ACC §2/§3）

- 验收者为全新上下文会话：只读任务契约（`tasks/foundation/WP-13-T12.json`）、设计文档（`units/modeling.md` v0.14 @ 送验 SHA、`units/runtime.md` §4.2/§5.2、`REQUIREMENTS.md` 需求编号原文）、在库留痕；未消费实施者任何过程叙述；构建/测试/门禁全部亲手重跑（复现优先于采信），全程为"不通过"找证据。
- **分支漂移检查**：`git rev-parse origin/wp13-t12` ＝ `541f554c1261…` ＝ 送验 headSha——**无漂移**；`origin/redesign-main` ＝ `1752bb85…`（即 base）。
- **复现现场**：`git worktree add --detach <临时目录A> 541f554c1261…`（detached @ SHA）；记录回传现场：`git worktree add -b acc/WP-13-T12/1 <临时目录B> origin/redesign-main`；base 侧门禁对照直接复用 B（检出时＝1752bb85，引擎直跑只读源树、自测夹具走缺省 TEMP〔F-347 防呆口径〕，跑后 `git status --porcelain` 零行）。
- **集成模式**（A 内全新配置）：`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DCMAKE_TOOLCHAIN_FILE=<repo>/vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DRWS_USE_STATIC_LINK_PLUGINS=ON` → exit 0、"Configuring done/Generating done"、CMake Error 计数 0；缓存确认 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`。环境链缺口按在案口径处置：①`RobWorkStudio/bin/` 两份 ini 模板为 gitignore 本机环境件，自主树复制进 worktree 后配置通过（F-007/F-062/F-098/F-099 口径，属环境供给非送验对象改动）；②集成**全量**构建 exit 1＝仅 F-323 登记的两框架插件目标（RWSimulatorPlugin 71 错＋bt_plugin.rwplugin 2 错——windows.h min/max 宏冲突同根因、同目标，基线同现），`sdurws_ird*` 全部目标零错误（日志 grep 计数 0），本任务 diff 不触达 RobWorkSim——按 T10/T11 先例逐目标构建契约涉及面。
- **独立冒烟模式**（另一临时目录，`-S …/industrialrobot`＋vcpkg toolchain＋Qt CMAKE_PREFIX_PATH，AGENTS §4.1 修正口径）：配置 exit 0"Generating done"；全量构建 exit 0、`error C|error LNK` 计数 0。
- 验收者环境：Windows 10.0.26200 x64；Visual Studio 17 2022（MSVC 19.42.34433）；Qt 6.11.1（msvc2022_64）；vcpkg 经典模式（主树 `vcpkg/installed/x64-windows`）；CMake 4.3。
- 变异测试在 A 内注入，验后 `git checkout --` 还原；还原后 `git status --porcelain` ＝ 0 行，重跑复绿（见 4.5）。

## 1. 逐项证据（ACC §4，4.1～4.11）

### 4.1 改动范围 —— 通过

`git diff --name-status 1752bb853d..541f554c` 共 26 文件：modeling 单元 7（CMakeLists.txt 修改＋CanonicalBridge.hpp/.cpp、BridgeFixtures.hpp、CanonicalBridgeTest.cpp、DescriptionBridgeContractTest.cpp、CompileBridgeContractTest.cpp 新增）＋`traceability/builds/wp13-t12/` 留痕 18 件＋`units/modeling.md` 1 件。逐文件对照契约 `allowedFiles`：modeling `**` 通配、`units/modeling.md` 明列、`traceability/**` 通配——**全部在册**；`forbiddenFiles`（REQUIREMENTS.md/ARCHITECTURE.md）零触碰；diff 无 `patches/` 路径（SA-02 无未登记框架改动）、无 industrialrobot 之外的任何框架文件。

### 4.2 可复现构建 —— 通过

- 集成模式（重配后逐目标）：`sdurws_ird_modeling` / `sdurws_ird_modeling_test` / `sdurws_ird_modeling_contract_test` 三目标 exit 0、编译/链接错误计数 0（产物 `build/RobWorkStudio/libs/Release/sdurws_ird_modeling.lib`＋`build/RobWorkStudio/bin/Release/sdurws_ird_modeling_test.exe`、`sdurws_ird_modeling_contract_test.exe` 落位）。全量构建仅 F-323 两框架目标失败（见 §0，与本任务无关、基线同现、INDUSTRIALROBOT 零波及）。
- 独立冒烟模式：全新目录配置＋全量构建 exit 0、错误计数 0；`sdurws_ird_modeling_test` 亲跑 **163/163 PASSED**＋`sdurws_ird_modeling_contract_test` 亲跑 **8/8 PASSED**（gated 的 CommandPipeline 4＋CompileBridge 2 不编入——与在库 smoke-modeling-test-report.json 163、smoke-modeling-contract-test-report.json 8 一致）。

### 4.3 契约 verify 命令 —— 逐条执行完毕（7 条，worktree A）

| # | 命令 | 结果 |
| --- | --- | --- |
| 1 | `cmake --build build --config Release --target sdurws_ird_modeling` | exit 0，错误计数 0，lib 落位 |
| 2 | `cmake --build build --config Release --target sdurws_ird_modeling_test` | exit 0，错误计数 0，exe 落位 |
| 3 | `cmake --build build --config Release --target sdurws_ird_modeling_contract_test` | exit 0，错误计数 0，exe 落位（F-338/F-344/F-345 家族缺陷——verify 缺 contract_test 构建行——**本契约未复发**：第 45 行已含该构建行） |
| 4 | `ctest --test-dir build/RobWorkStudio/src/rwslibs/industrialrobot/modeling -C Release -L "^ird$"` | **2/2 PASSED**；另亲跑两 exe：`sdurws_ird_modeling_test` **208/208 PASSED**（23 suites）＋`sdurws_ird_modeling_contract_test` **14/14 PASSED**（4 suites）——与在库 integration-modeling-test-report.json（208）、integration-modeling-contract-test-report.json（14）、合并 ird-test-report.json（222/222）逐件一致 |
| 5 | `cmake --build build --config Release --target ird_gates` | exit 1＝**存量命中（门禁按设计命中即 FATAL——基线侧同样 exit 1）**；MSBuild 捕获 43 行发射与 F-336 登记的缩减口径一致；按 F-336 建议引擎直跑取证：`cmake -DIRD_ROOT=<A>/industrialrobot -P cmake/ird_gates.cmake`（head，A）与同命令 @ B（base＝1752bb85）各得 **119 次发射**，归一化直方图 **SUB 57 / R3 41 / LIB 10 / T1 5 / R4 3 / T2 1 / R5 1 / R1 1 双侧逐码一致 → 多重集 diff 为空＝零新增命中**，与在库 ird-gates-comparison.txt 登记值逐件一致 |
| 6 | `pwsh -File …/validate-task.ps1 -TaskFile …/WP-13-T12.json` | **PASS** (1 tasks)，exit 0 |
| 7 | `pwsh -File …/validate-docs.ps1` | **PASS** (20 units, 12 trace entries, 189 task files)，exit 0 |

### 4.4 acceptance 条目 —— 五条均有对应证据（全部为本次亲跑复现，具名用例）

| 契约 acceptance | 证据（亲手复现） |
| --- | --- |
| 1 CanonicalBridge 落位＋§9.1 十类来源映射逐行 UT（DH 权威先 §7.4 展开；tools/scene 引用表解引用；scene worldPose 世界系固连不预乘；base 不算 R_world_base；全 SI 唯一经 core Units 入口） | ACC1 九用例＋Fixed 中性轴共 10 条：`ContractVersionAndRobotNamePassthrough`（版本同源初值 1＋名称透传）、`JointsExplicitMappingWithZeroOffsetFold`（零位折叠 origin·R(axis,q0) 手算逐元素 1e-15＋限位平移 −q0＋maxVel/maxAcc NotProvided）、`JointsDhAuthorityExpandsToExplicit`（T=Rot_z(θ+q0)·Trans_z(d)·Trans_x(a)·Rot_x(α) 手算对照 1e-12）、`LinksMappingBodyAndGeometry`（六分量→3×3 对称展开）、`ToolsMappingDefaultTcpFirstAndTcpCompose`（默认 TCP 居首＋mountInterface∘TCP 合成）、`SceneWorldPoseNotPreMultipliedByInstallRotation`、`BaseMappingCarriesEditorRepresentationOnly`（无矩阵承载）、`DrivetrainAndFrictionMapping`、`ResourceRefsBothStatesMapping`、`FixedJointAxisNeutralFill`；SI 门＝`tryConvert(v,si,si)` 同量纲恒等（CanonicalBridge.cpp:215-229） |
| 2 确定性（ARC-03）＋不重复计算内容身份 | `SameClosureSameDescriptionDeterministic`（全新闭包/编码两次装配深比较＋重复 read 无缓存〔tryObjectCalls 持续增长＝Recorded 每次重读声明面〕＋闭包字节变化→输出变化）、`NoContentIdentityRecompute`（声明摘要≠字节真实摘要仍按声明值透传——若自算 SHA-256 即失败；ContentVersion/builtFrom/contentIdentity 无承载面） |
| 3 reader 注入形态（P-RT-5/O-36）＋与 RT-T12 协作契约测试 | `ReaderSingleRootInputAndClosureBinding`（token 位不装配根对象仍成功＝单根字节输入；部件经闭包按 id 解析计数 1）、`ReaderFormatVersionGate`（版本 2→InputInvalid 含 SchemaVersionUnsupported）、`ClosureScopeRealStoredObjectsOnly`（关节/连杆子 id 不在闭包仍成功＋透传＋tryObjectCalls=0 负证——O-36 裁决钉住）、`ReaderNullClosureFailFast`（空指针构造→std::invalid_argument）；契约测试两模式：DescriptionBridge 3 条（§4.2 十类字段集对齐＋objectId 四子结构透传＋预设词表 static_assert 类型级钉住＋accessVersion=io::kAccessVersion；schemaVersion↔descriptionContractVersion 同源初值 1；失败轨值面 InputInvalid 含定位）＋CompileBridge 2 条（gated：reader 产出→runtime 公共 CanonicalModelBuilder S5 全不变量端到端＋CanonicalModel 字段逐项对齐；缺陷闭包 reader 恒值面拒绝、定位传导、S5 不接收） |
| 4 编译诊断定位（MDL-06）＋三层防线边界 | `RefMissingLocatesObject`（params object-id=悬空 OID＋两态零诊断记录）、`RefMissingTokenMismatch`（token 声称与字节不符定位）、`UnitIllegalInvalidStateField`（Invalid 态→UnitIllegal 定位 joints[0].bounds）、`DhExpandFailedLocalized`（缺 dhDerived／非旋转关节混入两反例）、`SchemaVersionUnsupportedDecodeGate`（major 字节 patch 99→拒绝）、`IncompleteModelRefusesWithoutRuntimeRepair`（可动关节轴缺失/场景无几何→RefMissing 不伪造＋diags 空＝runtime S3/S5 兜底不归） |
| 5 R-4 红线＋V-26/AT-18 输入质量面＋资源状态声明口径 | `NoNameGenerationOrDisambiguation`（"J_1.axis-2" 逐字节透传；链作用域重复→MalformedPayload 拒绝而非改名）、`LocalNameInputQualityFace`（空格→IllegalName 定位；robotLocalName 含 '/'/空→IllegalName；工具作用域重复→IllegalName 不消歧）、`ResourceRefsBothStatesMapping`（Recorded 声明键确定性＋Solidified 闭包复核＋路径不入 Description）＋ACC2 无缓存断言（Recorded 重读声明面）——与 runtime §9.1 口径一致 |

### 4.5 测试真实失败能力 —— 通过（两处变异均致红，注入后已还原）

在 detached worktree A 内注入（各自重建→亲跑→`git checkout --` 还原；还原后 `git status --porcelain`＝0 行、重跑复绿 **208/208＋14/14**）：

- **M1**：`CanonicalBridge.cpp:437` 零位折叠禁用（`rotMul(originModel.R(), axisAngleRotation(jd.axis, src.zeroOffset))` → `originModel.R()`）→ **2 FAILED**：`JointsExplicitMappingWithZeroOffsetFold_WP13T12_ACC1`＋`JointsDhAuthorityExpandsToExplicit_WP13T12_ACC1`（206/208）——零位折叠断言真实有效；
- **M2**：`CanonicalBridge.cpp:328` token 复核禁用（`if (found->objectTypeToken != expectToken)` → `if (false && …)`）→ **1 FAILED**：`RefMissingTokenMismatch_WP13T12_ACC4`（207/208）——token 不匹配定位断言真实有效。

### 4.6 架构红线 —— 通过（AGENTS §5 八条逐条）

1. **L2 零 Qt**：CanonicalBridge.hpp/.cpp 及两测试 TU grep `#include <Q|<Qt` 零命中；modeling 为计算内核，无界面目标。
2. **业务域互链 R-1**：新增 include 仅 `sdurws/ird/core|runtime|io` ＋ modeling 内部——runtime/io 为 modeling 六条登记边（`ird_gates_whitelist.cmake:50-55`，WP-13-T02 登记；门禁实测零新增命中佐证）；无 modeling→requirements/kinematics/trajectory/dynamics/selection/optimization 出边。
3. **跨单元私有头 R-2**：6 个跨单元 include 全部落位对端 `include/sdurws/ird/<unit>/` 公共头（Description/Errors/Units/DiagData/Identity/IoFwd 逐一 ls 验证）；产品编译器私有头 CompilerImpl.hpp 未被 include（S5 走公共 CanonicalModelBuilder——CompileBridgeContractTest.cpp:15-20 登记同款取舍）。
4. **SA-02 框架零修改**：diff 无 patches/、无框架文件（见 4.1）。
5. **R-4 名称前缀**：reader 不解析/不拼接/不消歧——`NoNameGenerationOrDisambiguation` 类型面钉住；robotLocalName 含 '/' 整体拒绝而非拆分。
6. **PA-1 权威唯一**：base 块只透传编辑表示不计算 R_world_base（M-11/P-RT-4——`BaseMappingCarriesEditorRepresentationOnly`）；内容身份不自算（`NoContentIdentityRecompute`）；名称映射不越权（R-4）；单位换算唯一经 core Units（SA-12 履行点）。
7. **PA-2 不可变历史**：builder/reader 为无状态纯函数，不触达修订/写路径。
8. **不继承 old/**：diff 无 old/ 引用；实现按单元卡 §9 从零编写。

### 4.7 注释规范 —— 通过（新增 6 文件全查，超出"实现文件抽查 ≥3"要求）

- 公共头 `CanonicalBridge.hpp` 全查：文件头（设计依据逐条列 Units/modeling.md §9.1/§9.2/§9.4.5、runtime §4.2/§5.2、MDL-14/MDL-06/ARC-03/ARC-04、O-36 裁决；"本单元不做的事"红线锚）＋三类型/三方法完整 Doxygen 中文契约（生命周期非 owning、线程安全并发只读、确定性来源、错误语义值面/异常面分工、@pre/@post/@param 方向单位）。
- 实现文件 `CanonicalBridge.cpp`：文件头五项实现层细化登记（DTB §5.4）；每个函数 Doxygen（单位 rad/m/kg·m² 显式、坐标系说明、错误码归类、纯函数/确定性声明）；语义段落注释含"为什么"（如零位折叠与 runtime S5 zeroOffset=0 的对端约定、Fixed 中性轴的结构承载理由、FNV-1a 双实例域分离防碰撞理由）。
- `BridgeFixtures.hpp`/`CanonicalBridgeTest.cpp`/两契约测试：用例级 Doxygen 均注明验证的需求/AT；`IRD_TEST_INFO` 携带追溯字段；冒烟 header-only 纪律（恒等变换逐元素构造）逐处注明缘由。
- §2.5 高危信息表逐项有落点：物理单位（rad/m/kg/kg·m²/N·m·s/rad 注明无 R1 量纲行走有限性门）、坐标系（世界系固连、连杆系质心）、角度制式（rad 显式）、所有权（closure 非 owning、字节非拥有）、线程约束（纯函数可重入/替身单线程）、错误语义（调用方违约 fail-fast vs 值面错误 vs InputInvalid 转译）、确定性（无环境/时钟/locale 读取、params 保序）。

### 4.8 文档同步 —— 通过（findings 逐条核对见下）

- `units/modeling.md` 升 v0.14（541f554c）：§3.3 表 CanonicalBridge.hpp 行 T12 落位（:263）、§11 T12 行、§13 需求/AT 追溯行（MDL-14/ARC-03/ARC-04/AT-18→T12）、§15 v0.14 变更记录八项（含 §9.1/§9.2 实现层细化登记＝DTB §5.4 偏差登记面：robotLocalName 来源、Fixed 中性轴、Recorded 声明键、tools 次序/TCP 选取、跨对象作用域重名拒绝）；P-MDL-8 处置＝runtime §4.2 字段集未变、blocked 未触发（对端 CompileBridgeContractTest 端到端钉住）；O-36 裁决落地。实现与任务卡零未登记偏差。
- 任务状态：契约 `status: ready`——翻转 done 属合入收尾（PIPE §4.7），验收不改写；pipeline/state.json 未纳入送验提交（编排者工作区文件，commit 正文已如实声明）。
- **findings.json 未关闭条目与本次送验面相关性逐条核对**（base 侧登记册 F-001~F-347；与 modeling/本任务相关者）：
  - F-338/F-344/F-345（open，契约 verify 缺 contract_test 构建行家族）：**本契约未复发**——verify 第 3 条即该构建行；
  - F-342（open，DiagCodes.hpp @brief 计数失实）：本任务 diff 不触达 DiagCodes（builder 两态零诊断、无新登记码），无法随本任务顺手消账，维持 open；
  - F-343（open，散文计数失准家族）：本任务提交内全部计数经亲跑核验**准确**（208/14/163/8/合并 222/171；MdlCanonicalBridge 恰 24 条）；但家族**新增一例**→转登 F-349（见 §2）；
  - F-346（open，留痕缺 gtest XML＋commit 表述失实）：**家族复发**——本任务 diff 同样无任何 .xml，且 541f554c 正文明确写"集成 gtest XML 两件＋冒烟 gtest XML 两件"→转登 F-348（见 §2）；
  - F-347（open，IRD_SELFTEST_DIR 无防呆）：操作面纪律——本次 base 直跑用缺省 TEMP，B 树跑后零改动，未触发；
  - F-336（open，gates VS 目标捕获缩减/编码损伤）：复现（目标捕获 43 行 vs 引擎直跑 119）；按建议采用引擎直跑口径；
  - F-323（open，RobWorkSim 框架插件全新 worktree 编译失败）：逐字复现（同两目标、同根因、INDUSTRIALROBOT 零波及）；
  - F-339/F-340/F-341（fixed）：本任务面未见复发（集成合并报告含 222 条用例级明细＝F-340 关闭面成立；无调试残块＝F-341 关闭面成立）；
  - F-321~F-330/F-335（ui 域 UI-T16/T17/T18 系列）：与本任务计算库送验面无交集；
  - F-337（fixed，blocking）：T09 已消账，本任务契约测试未复现同类问题。
- 本次验收新增发现转登：**F-348、F-349**（见 §2）。

### 4.9 commit 质量 —— 通过（附 1 条建议级：留痕清单表述失实→F-348）

- `d8f310f8`：标题 `[WP-13-T12] 实现 CanonicalBridge 编译协作桥（builder＋reader）＋UT＋契约测试`（动词开头）；正文四段齐备；验证段数字（208/208、14/14、163/163、8/8）**经亲跑逐一核实为真**；影响面（仅新增＋库源集增列、六条登记边无新增）与 diff 一致。
- `541f554c`：标题 `[WP-13-T12] 文档同步：modeling.md v0.14（T12 落位登记）＋验证留痕全集`；四段齐备；验证段（validate-task/validate-docs PASS）核实为真；门禁比对登记值与本次独立复现逐件一致。**但** What 段"集成 gtest XML 两件＋冒烟 gtest XML 两件"与 diff 不符（全量 diff 零 .xml 文件，实际为 testkit JSON 报告＋日志）——F-346 家族复发，按该在案先例定为建议级（验证结论本身真实且全部可复现，不构成对测试结果的粉饰），转登 F-348。

### 4.10 留痕完整 —— 通过（gtest XML 缺件并入 F-348）

- `traceability/builds/wp13-t12/` 18 件落位并逐件核读：集成/冒烟构建日志（error 计数 0，路径为实施者主树口径，与验收者在 worktree 的独立复现互为印证）、integration-ctest-ird.log（2/2）、两模式 testkit JSON 报告 4 件＋合并报告 2 件（**222/222、171/171 与亲跑一致**）、ird-gates 五件（直方图/原始输出/比对说明——与本次引擎直跑双侧直方图逐码一致）。
- gtest JUnit XML 原件缺位（AGENTS §4.2/DTB DoD 字面要求）→ 并入 F-348（建议级，F-346 同题先例）。
- `phase-one-validation.log` 未更新：该文件自 CORE-T01（2026-09-10）后无任何任务追加（T09/T10/T11 同先例），在案运作口径＝留痕落 `traceability/builds/<task>/`＋单元卡 §15 变更记录承担 resultNote 职能——本任务 v0.14 ⑧ 已登记，视同满足"或 resultNote 已更新"。

### 4.11 偷懒/缩水实现扫描 —— 通过

- ①标记扫描：6 个新增/修改实现与测试文件 `TODO|FIXME|stub|placeholder|未实现|not implemented` **零命中**；diff 全量 8 处命中全部位于留痕文件——7 处为 T05/T07 历史用例名（`…NoStub…`/`PlaceholderCylinder…`，语义与偷懒相反）＋1 处门禁日志行引用的 ui 存量命中文件名——均非实现代码，无需契约豁免。
- ②空实现检测：公共接口（ICanonicalModelInputBuilder/CanonicalModelInputBuilder/RobotDesignReader/ObjectClosureView）全部有实质实现（1301 行实现文件逐段通读）；无"仅返回默认值"的契约外空壳。
- ③逐条证据表：见 4.4——契约 acceptance 五条逐条有具名测试/命令输出/文件行号，无"已实现"声明裸信。
- ④测试强度：断言校验行为与边界（手算期望对照、容差纪律 1e-12/1e-15、错误定位 params 逐项断言、计数观测负证、static_assert 类型级钉住）；与 4.5 联动两处变异实测变红（非恒真、非只编译通过）。

## 2. 问题清单（分级）

**阻断级：无。**

**建议级（均已转登 `traceability/findings.json`，编号顺延自 F-347）：**

- **F-348**（F-346 家族复发）：WP-13-T12 留痕缺 gtest JUnit XML 原件，且文档同步提交 541f554c 正文"集成 gtest XML 两件＋冒烟 gtest XML 两件"表述与 diff 不符（零 .xml 文件；实际为 testkit JSON 报告＋构建/ctest 日志）。处置建议：后续任务把 `--gtest_output=xml:`（或 ctest 产出）原件随留痕落位，commit 正文留痕清单与实际文件一致；治理侧可考虑 verify-task 留痕清单核验 XML 在位。责任方：实施者＋治理。
- **F-349**（F-041/F-080/F-236/F-249/F-256/F-311/F-312/F-343 散文失准家族新增一例）：modeling.md v0.14 ②与 CanonicalBridge.cpp 文件头③称 Fixed 关节轴中性填充带"DerivedReadOnly＋methodTag \"fixed-axis-neutral\""标记，但该标记无任何承载面（runtime `JointDescription.axis` 为裸 Vector3D——Description.hpp:253；全库 grep 仅实现注释一处提及）；实际只有 +Z 值面（功能真实、有 UT）。处置建议：文档/注释按实现事实收窄，或待 Description 侧真实引入轴 provenance 承载后再声明。责任方：modeling 卡所有者。

## 3. 裁决

**verdict ＝ pass**：4.1～4.11 全部通过、无阻断级问题；两条建议级不阻塞合入（已转登跟踪）。送验对象＝SHA `541f554c1261085b8bac08c7563e98035d11bde0`（非分支引用）。合入决策归所有者，验收者不合入。
