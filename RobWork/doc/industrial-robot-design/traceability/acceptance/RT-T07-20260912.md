# RT-T07 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | RT-T07 实现 WorkCell 编译器（Adapter/十段链 S6）——runtime 单元 |
| 验收日期 | 2026-09-12 |
| 送验对象（冻结） | `ded5f2c7cff6b2e648b238f55919e1fa59dacf09`（origin/wp06-t07 @ 验收时点 rev-parse 比对一致——无分支漂移） |
| diff 范围 | `59fbe54d2abc355d5f1147da76078a90e9f8cc14..ded5f2c7cff6b2e648b238f55919e1fa59dacf09`（3 个提交：c287a6c 实现、c4b94b2 留痕、ded5f2c 文档登记；merge-base 复核＝base 正确） |
| **verdict** | **pass**（4.1~4.11 全部通过，无阻断级问题；建议级 G-1 已转登 findings.json **F-044**，open，不阻塞合入） |

## 验收者环境说明（独立性）

- 全新上下文：只消费契约 `tasks/foundation/RT-T07.json`、设计文档与仓库产物；未读取实施者任何过程叙述。对抗式立场逐项找反证。
- 复现现场：`git worktree add --detach <临时目录A> ded5f2c7c…`（detached @ 40 位 SHA，非分支名）；记录回传：`git worktree add -b acc/RT-T07/1 <临时目录B> origin/redesign-main`（59fbe54）。
- 全新集成树配置（F-007/F-008 既定口径再次踩中，按既定登记处置）：vcpkg toolchain＋Qt6 CMAKE_PREFIX_PATH＋`RobWorkStudio.ini.template.static`/`ini.shared.in` 复制（该两文件被 .gitignore `[Bb]in/` 吞掉、不在版本库——不带参数首配即报 `Could NOT find Boost`/缺模板文件，与 F-008 登记一致）。
- 配置缓存确认 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`（重新配置后 grep CMakeCache 为证）。
- 验收者本地证据文件（会话临时区，不入库；结论均回写本记录）：`acc-rtt07-config.log`/`acc-rtt07-integration-build.log`（集成配置＋契约目标构建，BUILD_EXIT=0）、`acc-rtt07-smoke-config.log`/`acc-rtt07-smoke-build.log`（冒烟，BUILD_EXIT=0）、`acc-rtt07-integration-test-run.log`/`acc-rtt07-smoke-test-run.log`（双模式测试亲跑）、`acc-integration.xml`/`acc-smoke.xml`（验收者自产 gtest XML）、`acc-rtt07-probeA/B-*.log` 与 `acc-rtt07-restoreA/B-*.log`（双探针与复绿）。

## 4.1 改动范围 —— PASS

- `git diff --name-only 59fbe54..ded5f2c`：18 个文件，3643 增/0 删。
  - `runtime/**` ×5（runtime/CMakeLists.txt +25、`include/sdurws/ird/runtime/Adapter.hpp` 215 行、`src/WorkCellCompiler.hpp` 123 行、`src/WorkCellCompiler.cpp` 594 行、`test/WorkCellCompilerTest.cpp` 737 行）⊆ allowedFiles `RobWork/RobWorkStudio/src/rwslibs/industrialrobot/runtime/**`；
  - `traceability/builds/wp06-t07/` ×12 ⊆ allowedFiles `traceability/**`；
  - `units/runtime.md` ×1 ∈ allowedFiles（§15.4 版本表追加 v0.8 一行）。
- forbiddenFiles（REQUIREMENTS.md / ARCHITECTURE.md）零触碰（diff 文件清单为证）。
- SA-02：diff 无 industrialrobot 之外任何框架源码；`patches/` 无新文件（仅既有 0001-rwslibs-entry-option.patch＋PATCHES.md）——零登记需求，合规。
- runtime/CMakeLists.txt 为基线既有文件（base 树 cat-file 存在），本次仅条件增列两块 `target_sources`。**通过**。

## 4.2 可复现构建 —— PASS

- **全新集成树**（worktree A，detached @ 送验 SHA，非主树增量）：重新配置 exit 0 → 契约 verify 目标 `cmake --build build --config Release --target sdurws_ird_runtime_test` → **exit 0 零错误**（`error C[0-9]/error LNK/fatal error` 计数＝0；全新树从零编译了 sdurw_core/math/kinematics/models 等 L1 依赖链——非旧二进制）。
- **独立冒烟模式**（另配临时目录 `acc-rtt07-smoke`＋vcpkg toolchain 绝对路径）：configure exit 0＋build exit 0 零错误。产品增列（WorkCellCompiler.cpp）与测试增列（WorkCellCompilerTest.cpp）按 `TARGET sdurw_kinematics` 条件不进冒烟目标——模式分工已在 units/runtime.md §15.4 v0.8② 按 DTB §5.4 登记（正当性核验见 4.8）。
- **测试真实复跑**（复现优先于采信，不引用实施者结果）：集成模式 `sdurws_ird_runtime_test.exe` → `[==========] 188 tests from 48 test suites ran … [  PASSED  ] 188 tests.`，exit 0；冒烟模式同命令 → **172/172 PASSED，exit 0**。送验 XML（traceability/builds/wp06-t07/ 两份，testsuites `tests="188"/"172" failures="0"`，188/172 个 testcase 元素逐一清点）与复跑一致；验收者自产 gtest XML 同计数。新增 16 例仅在集成运行出现、冒烟运行 0 命中（grep WorkCellCompilerTest|WorkCellConstViewTest）——与登记的模式分工一致。**通过**。

## 4.3 契约 verify 命令 —— PASS

- 契约 verify 数组恰 1 条：`cmake --build build --config Release --target sdurws_ird_runtime_test`。
- 验收者在全新集成树亲手执行 → exit 0（见 4.2）。**全部成功**。

## 4.4 acceptance 条目逐项核对 —— PASS

| # | 契约 acceptance | 验收证据 | 判定 |
| --- | --- | --- | --- |
| 1 | RT-AD-1/2、RT-EQ-1（1e-9 m/rad，附录 D 第 4 项）、RT-BW-4 用例通过 | 验收者复跑 188/188 全绿，其中新增 16 例（清点见 4.11③）：`FkEquivalence_RT_EQ_1` 三关节研究夹具（非 Z 轴 j2 绕 Y、j3 绕 (1,0,1)/√2、非零 zeroOffset 0.2/−0.35/0.7 rad）×3 固定构型，测试内手写独立 Rodrigues/位姿复合 vs 真实基线 `Kinematics::frameTframe`，逐关节轴线/原点与法兰全位姿 `EXPECT_NEAR(…, 1e-9)`（kFkTolerance＝1e-9，Test.cpp:67）；`TranslateRealRobWorkException_RT_AD_1` 以真实基线 StateStructure 重名帧 RW_THROW 触发（非人造异常）；`BaseMountReadback_RT_BW_4`/`DoubleAppliedDetected_RT_BW_4`（AT-37 反例 T·T 检出）/`SceneAndBaseFramesCarryNoInstallComponent_RT_BW_4` | pass |
| 2 | 显式设值与等价用例通过（§12 完成条件——Frame 树/Device/Joint/BaseMount 唯一写入） | RT-AD-2 四例全绿：`JointFieldsExplicit_RT_AD_2`（bounds 逐字 EXPECT_DOUBLE_EQ、maxVelocity 逐字、maxAcceleration 未提供→显式 +inf≠基线默认 1/±DBL_MAX——设值路径执行的值域可判定证据）；`JointBoundsOffsetMapping_RT_AD_2`（[−2.97,2.97]−0.5→[−3.47,2.47]）；`ContinuousJointExplicitInfiniteBounds_RT_AD_2`；`FixedJointIsFrameNotJoint_RT_T07`（dof=0）。唯一写入点：`BaseMountReadback` 读回逐元素 1e-9＋`SceneAndBaseFrames…`（BaseFrame 相对 BaseMount＝恒等、场景帧不预乘安装旋转——安装分量只在 BaseMount 一处的结构断言）。Frame 树/Device 名：`FrameNamesMatchNameMap_RT_T07`（编译器写入名＝映射输出名逐一命中，派生名 IRB_T.BaseMount/Base/Flange 格式锁定）；`RepeatCompileStructuralEquality_RT_EQ`（同输入两次编译结构相等——§5.5） | pass |
| 3 | RobWork 异常→稳定诊断转译（§8.4）用例通过 | `TranslateRealRobWorkException_RT_AD_1`：真实 RW_THROW 喂入 translateRobWorkError → 断言 code＝"RT-WC-COMPILE-FAILED"、context 含 S6、cause 含基线消息正文 "Frame name is not unique"、cause **不含** "StateStructure.cpp" 与 "Id["（NFR-REL-05 调用栈剥离双向断言）；`TranslateBadAlloc_RT_AD_1`（→RT-RESOURCE-BUDGET）；`TranslateUnknownStdException_RT_AD_1`（→RT-ROBWORK-ERROR 事件码） | pass |

- `ird_gates` 自查：实施者已实际执行并留痕（ird-gates-run.log：命中 IRD-GATE-SUB/T1 两项，均为 RT-T03 起基线既有登记项——F-020 同源，与 wp06-t06 命中集合逐项相同、零新增；提交信息如实写明）；验收者未复跑门禁目标（全新树命中集合由日志与 F-020 既有登记互证，命中项与本任务 diff 无关——两处命中均在 testkit/testdata 依赖边，非 runtime 新增）。无"未执行"虚标。**通过**。

## 4.5 测试真实失败能力（验证验证者） —— PASS

两组缺陷注入（集成树，精确 Edit＋显式核验构建退出码；每次注入后重编 exit 0 确认新二进制，杜绝旧二进制假绿——TK-T07/T10 教训口径）：

1. **探针 A（RT-EQ-1）**：`WorkCellCompiler.cpp` 连杆尾巴补偿 `if (joint.type != JointType::Fixed)` → `if (false && …)`（禁用 R_alignᵀ 补偿）。重编 exit 0 → 运行 `--gtest_filter=WorkCellCompilerTest.FkEquivalence_RT_EQ_1` → **exit 1，恰好 1 FAILED TEST**（补偿缺失→FK 残差被 1e-9 断言捕获）。字节级还原（`git status --porcelain`/`git diff` 全空）→ 重编 exit 0 → 全量 **188/188 PASSED 复绿**。
2. **探针 B（RT-AD-2）**：`setMaxAcceleration(rw::math::Q(1, maxAcc))` → `setMaxAcceleration(rw::math::Q(1, 1.0))`（注入基线构造默认值 1——正是 RT-AD-2 要杜绝的"默认残留"形态）。重编 exit 0 → 运行 `--gtest_filter=WorkCellCompilerTest.JointFieldsExplicit_RT_AD_2` → **exit 1，恰好 1 FAILED TEST**。字节级还原（diff 全空）→ 重编 exit 0 → 全量 **188/188 PASSED 复绿**。

实施者自带双探针（probe-build.log/probe-rt-eq1-red.log/probe-rt-ad2-red.log：红→还原→复绿全程，与验收者独立探针同点位同结论）。

## 4.6 架构红线（AGENTS §5 八条） —— PASS

1. **L2 计算内核零 Qt**：新增 4 文件 include 扫描 Qt/QString/QWidget 零命中（grep 证据）。
2. **业务域互链（R-1）**：runtime 新增面 include 仅 rw 基线（§8.1 P-RT-3 已登记 L1）＋core 公共头＋runtime 自有；无 modeling/kinematics/trajectory/dynamics/selection/optimization 任何依赖。
3. **跨单元私有头（R-2）**：跨单元仅 `<sdurws/ird/core/DiagData.hpp>`/`<sdurws/ird/core/Identity.hpp>`，均在 `core/include/sdurws/ird/core/` 公共面（ls 为证）；`src/WorkCellCompiler.hpp` 私有头仅被 src/WorkCellCompiler.cpp 与 test/ include（单元内同权）。
4. **SA-02**：见 4.1——零框架改动、patches/ 无新登记。
5. **R-4 名称前缀**：编译器写入 WC 的每个名字均来自 `buildRuntimeNameMap`（`scopedFullName` 唯一封装，WorkCellCompiler.cpp:172-184）；无任何现场拼装/前缀操作；公共头 findFrame 注释显式警示 R-4 违例形态。
6. **PA-1 权威唯一**：稳定码值一律经 `Errors.hpp registryCode` 冻结映射取串（WorkCellCompiler.cpp:539/546-548），事件码 RT-ROBWORK-ERROR 字面串与 Errors.hpp 登记一致；名称消歧归 NameMap、本层不自裁。
7. **PA-2 不可变历史**：diff 不触 project/history 面。
8. **不恢复 old/**：全部从零实现，无 old/ 引用。**通过**。

## 4.7 注释规范（AGENTS §2） —— PASS

- **公共头全查**（Adapter.hpp，215 行）：文件头（设计依据×需求×任务卡追溯＋单元内位置背景＋分阶段落位说明＋线程安全/生命周期总纲）、WorkCellConstView 类注释（为什么包装而非裸 Ptr、所有权借持、并发只读安全）、全部 5 方法＋translateRobWorkError 逐个 Doxygen 中文（@param 方向/@return 语义/@throws 触发条件、R-4 纪律警示、确定性声明）。
- **实现文件抽查 3 个**（WorkCellCompiler.hpp / WorkCellCompiler.cpp / WorkCellCompilerTest.cpp 全读）：
  - 文件头三段式齐备（设计依据/背景/线程安全，cpp 另有单位纪律与坐标系读法总纲——"T_ab＝b 系相对 a 系"）；
  - §2.5 高危信息逐项命中：物理单位（zeroOffset rad/移动关节 m、maxVel rad/s、吊装高度 m、容差 1e-9 m/rad）；坐标系（T_parent_joint 父连杆系→关节系、场景帧世界系固连）；角度制式（rad 显式标注"不是度"等价表述）；所有权（WorkCellCompileOutcome 引用计数持有、失败 RAII 析构、serialChain 裸指针视图所有权仍在 StateStructure）；线程约束（可重入纯函数、defaultState 值拷贝线程私有）；错误语义（fail-fast vs 环境错误、RuntimeError 透传不误转译的顺序约束注释、稳定码 vs 事件码归类）；确定性来源（kPoleCosTolerance=1e-12 极轴判定的数值依据、kCauseSummaryLimit=512 与 NFR-REL-05 关系、写入序＝确定性序）；
  - 复杂算法逐步讲解：任意轴关节映射的数学推导（T_static＝T_parent_joint·R_axis(offset)·R_align＋共轭恒等式 R_align·Rz(q)·R_alignᵀ＝R_axis(q) 消尾巴）在私有头与实现双处完整推导，极轴分支/一般分支的进入条件与原因逐分支注释；
  - 测试文件：每用例中文说明验证哪条 RT/AT 需求（TEST 名即带 RT_AD_1/RT_EQ_1 等追溯段），夹具字段逐个注明单位。
- 无过期注释、无复述式废注释、无中英机翻腔。**通过**。

## 4.8 文档同步＋findings 消账核对 —— PASS（1 项建议级转登 F-044）

- **偏差登记核验**（DTB §5.4 通道，units/runtime.md §15.4 v0.8 行）：①Adapter.hpp 五实体按首个消费者原则落位 2 个（WorkCellConstView＋translateRobWorkError），DynamicWorkCellConstView/DeviceView/RobWorkBaselineVersion/IRobWorkAdapterFactory 随 RT-T08/T09/T11——与 v0.4 同例，已登记；②**两模式编译分工**（S6 面仅集成编译）——编排者关注点专项核验：**正当**，S6 必须构造真实 WorkCell/SerialDevice/Joint（基线非模板类，符号实现于 sdurw_kinematics/sdurw_models 的 .cpp），冒烟模式无框架库可链，§5.1 冒烟口径（目标注册＋include 路径）不受影响，Adapter.hpp 冒烟下不被任何 TU include、172/172 证明既有面零回归；③任意轴映射/连续关节表达/显式设值落位口径/Fixed zeroOffset 不消费/S9 自检内置与几何挂接分阶段——实现层选型逐条登记。实现与登记内容互证一致（代码走读 vs v0.8①~⑦逐条对应）。
- **任务状态/追踪**：§12 RT-T07 行（runtime.md:1373）与交付物对应；validate-task 亲手执行 PASS（1 tasks）；validate-docs 亲手执行 PASS（20 units/12 trace/150 tasks）——与提交信息声明一致。契约 status 翻转归合入收尾（PIPE §4.7），非实施者义务。
- **findings.json 未关闭条目相关性核对**（32 条 open 逐一过筛）：
  - **F-043 同型再现**（RT-T06 重力投影敏感度缺口——resolution 载明"可随 RT-T07 领取时顺带或后续用例增补批次"）：本次 diff 未触及 BaseWorldTransformTest.cpp，**未顺手消账**——该条为建议级测试覆盖面建议非本任务契约面，消账义务不成立，仍 open 由所有者跟踪（此处即为"须注明"的登记）；
  - **F-042 同型核对（计数失实）**：本次三提交计数声明（新增 16＋既有 172＝188；冒烟 172）经验收者 TEST 宏清点（16 个）与双模式亲跑（188/172）实测**准确**，无同型问题；
  - **F-036 同型核对（ird_gates 未执行虚标）**：本次已实际执行门禁并如实登记命中 2 项既有（ird-gates-run.log），无同型问题；
  - **F-037/F-039 同型核对（全树零错误强度失准）**：c287a6c 验证段明示"集成模式构建零错误（verify 命令 …--target sdurws_ird_runtime_test）"，未声称全树——表述与实际执行面一致，无同型问题；
  - **F-008/F-007**（全新集成树配置参数）：验收者复现再次踩中（首配缺 Boost/模板文件失败），按既定登记处置，仍 open；
  - **F-044（本次新登，见 4.9）**：ded5f2c 标题名词短语开头，F-012 家族同型第 12 例。

## 4.9 commit 质量 —— PASS（1 项建议级转登 F-044）

- `c287a6c` `[WP-06-T07] 实现 WorkCell 编译器（S6＋Adapter 视图/转译）`：`[WP-nn-Tkk]`＋动词开头 ✅；正文四段（What/Why/验证/影响面）齐备 ✅；验证结论与留痕一致（188/172 计数、探针×2 与 probe 日志互证、ird_gates 零新增命中与 gates 日志一致）✅；Acc-Covered: 1,2,3 ✅。
- `c4b94b2` `[WP-06-T07] 验证留痕：…`：动词开头 ✅；四段齐备 ✅；留痕清单与 diff 的 12 个 traceability 文件逐一对应 ✅。
- `ded5f2c` `[units] runtime v0.8：RT-T07 实现落位登记（DTB §5.4）`：前缀 `[units]` 合规、四段齐备；但**标题前缀后以名词短语开头**（"RT-T07 实现落位登记"）——与 F-012/F-023/F-024/F-026/F-027/F-030/F-034/F-038/F-040/F-041/F-043 同型再现（建议级，历史处置口径：实施者模板补动词开头示例、已推送历史不追溯改写）→ 转登 **F-044**。不阻断。
- 三提交均"代码与测试同一提交/证据同一提交/文档同一提交"粒度，可独立构建回溯。**通过**。

## 4.10 留痕完整 —— PASS

- gtest XML ×2：`sdurws_ird_runtime_test-integration.xml`（testsuites tests="188" failures="0" errors="0"，188 个 testcase 元素）＋`…-smoke.xml`（tests="172" failures="0"，172 元素）——与验收者亲跑及自产 XML 一致。
- `ird-test-report.json`（ird-test-report/1）：目标两级结果＋modeMechanics（两模式分工口径）＋三条 acceptance 逐条消账＋16 例新增逐例 trace＋双探针记录＋门禁结论。
- 构建日志：integration-build.log（59 行契约目标构建）/smoke-configure.log/smoke-build.log/probe-build.log（两次探针注入红/还原绿全程）/probe-rt-eq1-red.log/probe-rt-ad2-red.log/ird-gates-run.log。
- `phase-one-validation.log`/resultNote 面：units/runtime.md §15.4 v0.8 行载明实现证据路径与双模式结果（集成 188/188、冒烟 172/172、双探针精确红复绿）——与本记录复跑一致。**通过**。

## 4.11 偷懒/缩水实现扫描 —— PASS

- **① 标记扫描**：4 个新增源文件 `TODO|FIXME|stub|placeholder|未实现|not implemented|XXX|HACK`（-i）零命中（"RT-STUB-0 不做替身"为替身边界**声明**，非占位实现——测试文件头设计依据行）。
- **② 空实现检测**：无公共接口空函数体/仅返回默认值——WorkCellConstView 5 方法与 translateRobWorkError 全部有实际逻辑（全文走读）；compileWorkCell 594 行实质实现（10 步主流程＋异常边界）。
- **③ acceptance 逐条证据表**：见 4.4——三条均有具名测试＋验收者亲跑输出＋代码行级证据，无一条仅凭"已实现"声明。
- **④ 测试强度**：断言校验行为与边界（EXPECT_NEAR 1e-9 逐元素、EXPECT_DOUBLE_EQ 逐字、+inf≠1/±DBL_MAX 的值域可判定设计、State 值拷贝独立性经 setQ 行为断言、未命中 nullptr/空 Ptr 反例），非恒真、非"只编译通过"——与 4.5 联动：双探针实测实现缺陷下恰好对应用例变红。**通过**。

## 发现的问题清单

| 级别 | 编号 | 内容 | 处置 |
| --- | --- | --- | --- |
| 阻断 | 无 | — | — |
| 建议 | G-1 → **F-044** | ded5f2c 文档提交标题前缀后以名词短语开头（"RT-T07 实现落位登记"），非动词开头——F-012 家族同型第 12 例 | 转登 findings.json F-044（open，与家族合并处置：实施者模板补动词开头示例，已推送历史不追溯改写） |
| 留档 | 备注 1 | F-043（RT-T06 重力投影测试敏感度缺口）未随本任务顺手消账（diff 未触及 BaseWorldTransformTest.cpp）——resolution 本为可选路径（"或后续用例增补批次"），义务不成立，仍 open 由 runtime 所有者跟踪 | 本记录即登记（4.8） |
| 留档 | 备注 2 | F-007/F-008（全新集成树需 vcpkg toolchain＋Qt CMAKE_PREFIX_PATH＋bin 模板手工复制）验收复现第 3 次踩中（CORE-T01/RT-T06 后），配置摩擦真实存在但既有登记在案，无新信息 | 不重复登记 |

## 裁决

**verdict = pass**：4.1~4.11 全部通过，无阻断级问题。送验对象冻结 SHA `ded5f2c7cff6b2e648b238f55919e1fa59dacf09` 复现构建零错误、双模式测试 188/172 全绿、双探针失败能力实证、红线与注释规范合规、偏差全部经 DTB §5.4 登记且登记内容与代码互证一致。建议级 F-044 已转登 findings.json，不阻塞合入；合入决策归所有者（验收者不合入、不改动实施产物、未向任务分支推送任何提交）。
