# RT-T12 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 ID | RT-T12（runtime 测试替身与契约套件——WP-06-T12，unit=runtime） |
| 契约 | tasks/foundation/RT-T12.json（status=ready，branch=wp06-t12，verify＝`cmake --build build --config Release --target sdurws_ird_runtime_test`；acceptance 4 条；knownPitfalls P-RT-1；interUnit=true） |
| 送验对象（冻结） | `ff4b943ca275fb8c9d010d17dca1d923dd5e43d6`（分支 wp06-t12 尖端；base=`ba61f3db313c939de93db52010a2ba9f39b63a23`＝origin/redesign-main 尖端＝RT-T11 合入收尾提交；`git merge-base --is-ancestor` 实证 base 即 diff 起点） |
| 分支漂移检测 | `git fetch origin` 后 `git rev-parse origin/wp06-t12`＝`ff4b943ca275fb8c9d010d17dca1d923dd5e43d6`（与送验 SHA 一致——ACC §3 冻结口径满足，无漂移） |
| **verdict** | **fail**（阻断级 1 项 B-1：黄金数据集字节完整性在规定复现环境下不可复现——干净 worktree 检出后集成 5/261、冒烟 3/176 变红；其余 4.1/4.2/4.3/4.5～4.11 通过，建议级 2 项新发现已转登 F-053/F-054，B-1 根因转登 F-052 供返工跟踪） |
| 独立性声明 | 验收者为全新上下文会话（PIPE §0.4 子代理派发模式），仅消费任务契约、设计文档、送验提交产物与留痕文件，未读取实施者任何过程叙述；构建、测试、探针、门禁全部由验收者亲手在独立 worktree 重跑，复现优先于采信（ACC §2 四条逐条满足，无需降级声明）。 |
| 验收方式 | 独立 worktree（detached @ 送验 SHA）亲手重配重跑双模式构建、契约 verify 命令、集成/冒烟测试、ird_gates 门禁与治理脚本；静态审查＝逐行通读全部新增/大改源码文件（RuntimeTestDoubles.hpp 539 行、ContractSuiteTest.cpp 1177 行、GoldenDataLintTest.cpp 158 行、test/README.md 82 行全文＋Codec.cpp/Snapshot.cpp diff＋CMakeLists.txt 增量）并对照 units/runtime.md §11/§12/§15.4 v0.13 与 tasks/foundation/RT-T12.json；动态探针＝2 处实现变异（身份编码浮点量化、反解平移符号）→精确还原→复绿。 |
| 验收环境 | Windows 10 x64 / Git Bash / VS2022（Visual Studio 17 2022 生成器 x64）/ Qt 6.11.1（CMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64）/ vcpkg 经典模式（仓库根，toolchain 绝对路径 D:/10_Source_Repos/21_robot/RobWork/vcpkg/scripts/buildsystems/vcpkg.cmake）/ pwsh 7（治理脚本）。复现现场＝`C:\Users\zgl18\AppData\Local\Temp\ird-acc-rt-t12-a`（detached worktree @ ff4b943c，集成树 build/ 与冒烟树 build-smoke/ 均全新配置，验收日志在其根下 integration-acc-build.log 等共 10 件，验收后随 worktree 删除——本记录为准一证据载体）；记录回传＝`C:\Users\zgl18\AppData\Local\Temp\ird-acc-rt-t12-b`（分支 acc/RT-T12/1，基于 origin/redesign-main）。 |
| 环境前置说明（复现者须知） | ①全新集成树配置需主仓库同款参数（toolchain＋Qt 前缀，F-007 既有），且 `RobWork/RobWorkStudio/bin/RobWorkStudio.ini.template.static` 被 .gitignore 吞掉、目标目录 `bin/` 整体不入 checkout，须先 `mkdir -p` 再从主仓库手工复制——F-007 再次实证（本次首次配置因此失败一次），非本任务引入。②从零集成树全量构建在 F-008 基线缺陷处失败（73 处 error：RWSimulatorPlugin.vcxproj 71＋bt_plugin.rwplugin.vcxproj 2，ird 目标零命中）——F-036/F-038 及 RT-T02/T03/T06/T11 验收判例同型；任务目标面从零构建（同契约 verify 命令）零错误。③手工运行测试 exe 需把 build/RobWork/bin/Release、build/RobWorkSim/bin/Release、build/RobWorkStudio/bin/Release（Windows 风格路径）加入 PATH，否则 exit 127。④**本任务新增第④条复现者须知（B-1 根因）**：仓库本地 `core.autocrlf=true`＋`RobWork/.gitattributes`（仅 ppm/pgm/stl/stlb 标 binary，未覆盖 json/mjs）⇒ 任何 fresh checkout（worktree/clone）会把 testdata 文本资产 LF→CRLF，黄金数据集 manifest 的 sizeBytes/SHA-256 完整性即失配——见 4.3/4.4。 |

---

## 4.1 改动范围 —— 通过

- base..head 共 5 个提交：`97455085`（修复 RT-RES-3 路径不作身份）、`2ed1c558`（数据集交付）、`324ad96c`（替身＋契约套件＋lint）、`d8bcd441`（RT-STUB-0 声明＋v0.13 登记＋findings）、`ff4b943c`（双模式留痕）；`git diff --name-status ba61f3d..ff4b943c` 共 27 个文件、+2794/−17：
  - `runtime/**`（allowedFiles 第 1 条）：CMakeLists.txt（M，+27）、src/Codec.cpp（M，RT-RES-3 修正）、src/Snapshot.cpp（M，同）、test/ContractSuiteTest.cpp（A，1177 行）、test/GoldenDataLintTest.cpp（A，158 行）、test/RuntimeTestDoubles.hpp（A，539 行）、test/README.md（A，82 行）——7/7；
  - `testdata/**`（allowedFiles 第 2 条）：golden/rt-fk-equation/1.0.0/ 4 件＋golden/rt-namemap-roundtrip/1.0.0/ 4 件＋tolerance/rt-runtime/v1.0.0.json——9/9；
  - `traceability/**`（allowedFiles 第 5 条）：builds/wp06-t12/ 留痕 11 件＋findings.json（M，F-049/F-050/F-051）——12/12；
  - `units/runtime.md`（allowedFiles 第 4 条，M，卡头 v0.13＋§15.4 v0.13 条目行）。
- forbiddenFiles（REQUIREMENTS.md / ARCHITECTURE.md）：diff 中零出现。
- SA-02：diff 无任何框架源码文件；`industrialrobot/patches/` 无新文件。
- 本任务零公共头改动（include/ 无 diff）——契约面零变化；产品实现仅 Codec.cpp/Snapshot.cpp 两处身份域修正（4.7 注释核查见下）。

## 4.2 可复现构建 —— 通过（集成按 F-008 闭合前"任务目标面从零"判例口径）

- **集成模式**（worktree A 全新配置）：`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=<vcpkg 绝对路径> -DCMAKE_PREFIX_PATH=<Qt6>` → `Configuring done (5.7s)`；缓存确认 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`（grep build/CMakeCache.txt 实证）。全树 `cmake --build build --config Release -j` → BUILD_EXIT=1，73 处 error **全部**归属 RWSimulatorPlugin.vcxproj（71）＋bt_plugin.rwplugin.vcxproj（2）（RobWorkSim 域，F-008 基线缺陷；grep industrialrobot 零命中；与 F-036/F-038 及 RT-T02/T03/T06/T11 验收判例逐字同型）——**任务目标面从零**：`cmake --build build --config Release --target sdurws_ird_runtime_test` → **exit 0**，错误计数 0（integration-acc-build.log、verify-target-build.log）。
- **冒烟模式**（独立目录 build-smoke，带 vcpkg toolchain 绝对路径）：配置 `Configuring done (3.8s)`、构建 exit 0、错误计数 0（smoke-acc-configure.log、smoke-acc-build.log；唯一 warning＝MSB8029 临时目录增量提示，环境性质）。
- 构建面结论：两模式零错误（集成按判例口径）；实施侧 integration-build.log 经核对产自主树存量增量树（路径 `D:\...\RobWork\build`、无 RWSimulatorPlugin 编译活动——F-008 掩盖效应），其提交信息未标注树性质→建议级 S-2（转登 F-054），表述本身无失实。

## 4.3 契约 verify 命令 —— 命令通过；其后测试执行**不可复现**（阻断 B-1 的观测面）

- 契约 verify 数组仅 1 条：`cmake --build build --config Release --target sdurws_ird_runtime_test`——验收者在 worktree A 逐字亲手执行（从零构建）**exit 0**。
- 其后亲手运行测试可执行：
  - 集成模式：**261 例＝255 PASSED＋5 FAILED＋1 SKIPPED**（SKIPPED＝ContractSuite.ChildProcessWorker_RT_ID_1，按设计仅子进程执行——父用例 CrossProcessIdentityStable 实际派生子进程并通过，占位跳过非缩水）；实施侧留痕为 260 过 0 败 1 跳过（builds/wp06-t12/integration-test-run.log）——**260−255＝5 个失败在我方规定复现环境下出现**（acc-integration-test-run.log、acc-integration-runtime-test.xml）。
  - 冒烟模式：**176 例＝173 PASSED＋3 FAILED**；实施侧留痕为 176/176（smoke-test-run.log）。
- 5 个集成失败与 3 个冒烟失败**同根因**（失败消息逐字聚类，仅两类）：
  - `dataset-invalid: integrity.inputs/model.json: size 不符（期望 1812，实际 1867）`
  - `dataset-invalid: integrity.inputs/robot.json: size 不符（期望 1170，实际 1207）`
- **根因链（验收者实测钉死）**：黄金数据集以 LF 提交（git blob 字节数＝manifest 申报：model.json blob=1812＝sizeBytes、robot.json blob=1170＝sizeBytes），manifest 另申报 SHA-256 与 generate/*.mjs、expected/*.json；仓库本地 `git config core.autocrlf`＝**true**（global 未设置），`RobWork/.gitattributes` 仅 4 行（\*.ppm/\*.pgm/\*.stl/\*.stlb binary）**未覆盖 json/mjs**⇒ fresh checkout（`git worktree add`＝ACC §3 规定复现路径）执行 LF→CRLF 转换：model.json 检出 1867 字节（＝1812＋55 行）、robot.json 检出 1207（＝1170＋37 行）→testkit GoldenDataset::load 完整性校验（testkit.md §4.5 size＋SHA-256）抛 TestKitError。实施侧全绿的原因＝其运行现场为主仓工作树（testdata 文件自创建后未重新检出、保持 LF），掩盖效应与 F-008/F-036 判例同构。**该失败与实施叙述无关、与运行方式无关——按 ACC §2.4 复现优先于采信，构成"测试不可复现"阻断。**

## 4.4 acceptance 条目逐项 —— 第 3 条 fail（B-1），其余通过

| 契约 acceptance | 证据 | 结果 |
| --- | --- | --- |
| ① §11 验证矩阵逐条通过并留痕（AT-16/AT-18/AT-37 载体） | test/README.md §3 给出 §11 全表→测试文件落位（RT-T02～T12 累积）；本套件 13 用例补组合面缺口：RT-ID-1 跨进程（CrossProcessIdentityStable_RT_ID_1——父进程编译＋子进程 gtest 过滤器再入，模型/快照/映射身份＋WC 帧名集 IRD_EXPECT_IDENTICAL）、RT-ID-2/3、RT-BW-1/3/4/5（四消费替身 AT-37 载体）、RT-CAP-1/2、RT-RES-3、RT-EQ-1/RT-NM-1 数据集载体——前 10 类在我方 255 通过集合内；**但 RT-EQ-1/RT-NM-1 的数据集载体（GoldenFkEquationDataset_RT_EQ_1／GoldenNameMapRoundtripDataset_RT_NM_1）在我方复现中变红**（B-1） | **部分不通过**（载体本身设计合格——见 4.5，变红根因是数据资产而非用例缺陷，但"逐条通过"在冻结对象上不成立） |
| ② RT-STUB-0 边界声明 | runtime/test/README.md §1 全文声明（替身只覆盖五个注入接口；RobWork/rwsim 本体与被测编译器/工厂不做替身；手写 FK 仅测试对照工具；未执行不得标注通过）＋RuntimeTestDoubles.hpp 文件头同款＋ContractSuiteTest.cpp 文件头复述——三处在案 | 通过 |
| ③ 数据集交付（rt-fk-equation analytic＋rt-namemap-roundtrip contract-fixture＋容差档案 rt）——**lint 通过并随 CI 跑通** | 资产齐备（两 golden 数据集 inputs/expected/generate/manifest 全链＋tolerance/rt-runtime/v1.0.0.json；档案 id 以 rt-runtime 承接——F-050 偏差已按 DTB §5.4 登记）；lint 用例随测试目标两模式编译（GoldenDataLintTest 4 例）——**但我方两模式复现中 lint 3 例全红**（B-1：数据集在规定检出形态下不满足自申报完整性）；"随 CI 跑通"在任何 fresh checkout 的 CI 上同样会红 | **不通过（阻断）** |
| ④ 未执行不得标注通过 | 全部 261＋176 例执行留痕在案（gtest XML×2＋run log×2＋ird-test-report.json 逐用例；*.log 经 git add -f 入库——F-010 既有问题被正确处置）；无未执行标注通过的粉饰——实施侧数据真实（其环境下确实全绿），失败属于可复现性缺陷而非造假 | 通过（留痕纪律）＋B-1 另行阻断 |

## 4.5 测试真实失败能力 —— 通过（2 处实现变异，均即红→还原复绿）

| 探针 | 注入点（worktree A 临时修改，已还原） | 结果 |
| --- | --- | --- |
| 探针 M1 | Codec.cpp `writeSourcedDouble` 的 Provided 值编码 `w.f64(*v.tryValue())` 改为量化 `w.f64(std::round(*v.tryValue()*1e6)/1e6)`（破坏"无浮点近似等价"位模式敏感性） | 重建后 `TinyFloatDeltaSplitsIdentityAndCache_RT_ID_2` **FAILED**（1e-15 差不再改变身份→EXPECT_FALSE(身份相等) 失败）——证明 RT-ID-2 断言钉住真实位模式编码 |
| 探针 M2 | BaseWorldTransform.cpp `invertWorldBase` 逆平移 `* (-1.0)` 改 `* (1.0)`（−Rᵀt→＋Rᵀt） | 重建后 `NumericExampleThroughSnapshotView_RT_BW_1` **FAILED**（反解平移期望 (0,0,+2) m 实得 (0,0,−2)）——证明 §6.5 数值例断言非恒真 |
| 还原 | 两处修改 `git checkout --` 还原（status 除验收日志外干净） | 重建后两用例**复绿**（2/2 PASSED）——红由变异唯一导致 |
| 旁证 | F-049 resolution 建议的"pathHint 注入即红"由 RT-RES-3 用例本体承载：`SourcePathHintIsNotIdentity_RT_RES_3` 在我方 255 通过集合内（该用例检出的 F-049 缺陷已随本任务修复） | 通过 |

## 4.6 架构红线（AGENTS §5 八条） —— 通过

1. **L2 零 Qt**：diff 全部新增行 grep Qt 头/QString/Q_OBJECT 零命中；runtime 产品 include 面未扩大。
2. **业务域互链禁止（R-1）**：新增文件跨单元 include 仅 `<sdurws/ird/core/*>` 与 `<sdurws/ird/testkit/*>` 公共头；测试目标增链 sdurws_ird_testkit＝testkit.md §10.2 交接表 runtime 行消费面（测试侧依赖，R-1 不适用），产品目标 sdurws_ird_runtime 链接面不变（仍 core＋L1 基线库，CMakeLists 文件末红线守卫在位）。
3. **跨单元私有头禁止（R-2）**：新增文件唯一非系统私有 include 为 `CompilerImpl.hpp`/`RuntimeTestDoubles.hpp`——均本单元内（测试与 src 同权先例，RT-T11 同款登记）。
4. **框架零源码修改（SA-02）**：diff 零框架文件、patches/ 零新增。
5. **名称前缀禁令（R-4）**：diff 无任何名称拼接/剥离；套件仅经 resolveObjectId/resolveRuntimeName 整串解析消费。
6. **权威唯一（PA-1）**：诊断断言全部经 registryCode 稳定码单点比对（hasDiagnostic）；数据集/档案只读消费，无第二套校验语义（lint 走 testkit 装载单点）。
7. **不可变历史（PA-2）**：无修订/历史写路径触碰。
8. **不继承 old/**：无 old/ 引用。

**ird_gates 实跑**（worktree A 构建树 `cmake --build build --config Release --target ird_gates`）→ exit 1，命中恰 3 项：IRD-GATE-SUB×2（runtime_test→testkit【＝F-051 新登记】＋testdata_lint→testkit【＝F-019 既有】）＋IRD-GATE-T1×1（testdata_lint→testkit【＝F-019 既有】）；门禁自测 9 项全部按预期检出（acc-ird-gates-run.log）——与送验留痕 ird-gates-run.log 逐项一致，**零未登记新增违规**。

## 4.7 注释规范 —— 通过

- 新增 4 文件全文通读：三份 C++ 文件均有合规文件头（@file/@brief＋设计依据编号＋背景说明）；RuntimeTestDoubles.hpp 每类/每函数 Doxygen 中文注释齐备，§2.5 高危信息全覆盖（线程约束、所有权"不拥有——调用方持有"、确定性来源"固定种子 SHA-256 派生"、单位 rad/rad/s/kg/m 逐字段标注、MSVC vector 校验误报的实现细节备注）；ContractSuiteTest.cpp 每用例段前有矩阵依据块注释、单位与坐标系逐处标注（"单位 rad""世界系坐标，单位 m""SI 真值——§4.4"）；GoldenDataLintTest.cpp 用例注释含需求追溯（NFR-COR-01/附录 D C4）。
- 修改文件（Codec.cpp/Snapshot.cpp）hunk 注释完整解释"为什么"（身份域跳过 pathHint 的规则出处＋v0.13 登记指针）；CMakeLists 增量段解释两模式分工与 testkit 链接治理依据。
- 公共头：本任务零公共头改动（无可查面）。
- 小瑕疵（建议级 S-1，转登 F-053）：ContractSuiteTest.cpp L123 附近 `loadRtProfile` 注释与 GoldenDataLintTest.cpp L103 注释仍写 "testdata/tolerance/**rt**/v1.0.0.json"，实际路径为 tolerance/**rt-runtime**/（F-050 改名的注释残留，零行为影响）。

## 4.8 文档同步 —— 通过（含 findings 相关性逐条核对）

- **单元卡同步**：units/runtime.md v0.13 条目完整登记本任务八面（§11 设施落位/契约套件用例清单/RT-RES-3 产品修正 F-049/数据集交付＋rt-runtime 偏差 F-050/RT-STUB-0 在案/RT-CAP-1 观测点口径/testkit 链接 F-051/执行证据）——实现与任务卡的偏差全部按 DTB §5.4 登记在案，无"代码先行、文档失实"。
- **findings 登记**：F-049（major，pathHint 入身份域——本任务修复并留 resolution）、F-050（minor，档案 id 句法）、F-051（major，ird_gates 引擎与白名单矛盾——处置归 WP-01）。
- **findings.json 未关闭条目相关性核对（43 项 open 逐类过筛）**：
  - **F-007**（全新树配置前置）：本次验收再次实证（ini 模板复制＋toolchain/Qt 前缀）——维持 open，环境链问题，与本任务 diff 无因果。
  - **F-008**（全新树全量构建失败）：本次 73 error 同簇复现——维持 open，非本任务引入。
  - **F-010**（.gitignore 吞 *.log）：实施侧 ff4b943c 提交正文自述以 git add -f 处置——维持 open（治本仍缺）。
  - **F-012/F-023/F-024/F-026/F-027/F-030/F-034/F-037/F-039/F-040/F-042/F-044/F-045/F-047/F-048**（提交标题名词短语族）：本任务 5 个提交标题全部动词开头（修复/交付/实现/登记/登记）——**零复发**。
  - **F-028/F-035**（"门禁未建成"失实理由族）：本任务实跑 ird_gates 并逐项登记命中——零复发。
  - **F-015/F-016/F-017/F-018**（TK-T03 装载器偏差族）：F-018① 双前缀错误消息在我方复现输出中再次可见（"dataset-invalid: dataset-invalid:"）；F-017（generate 段完整性不校验）与数据集 manifest 已申报 generate 条目的现状并存——均维持 open，非本任务可顺手消账（testkit 属他单元）。
  - **F-019**（ird_gates 既有红）：本任务新增第 3 处命中已拆分登记为 F-051——F-019 口径不变、维持 open。
  - **F-032**（planned 契约锚点不检）：RT-T12 契约 status=ready，validate-task 三查 PASS——本任务未触发该缺口。
  - **F-036/F-038**（构建树性质标注族）：本任务提交未标注 integration-build.log 产自主树增量树——表述无失实但标注纪律未完全落地→并入建议级 S-2（F-054）。
  - **F-043**（重力投影转置方向敏感度缺口）：**RT-T12 未顺手消账**——新增 RT-BW-1/RT-BW-5 的重力断言仍基于对称旋转 R_x(π)（R·g≡Rᵀ·g），转置方向依旧不被钉住；本验收在 runtime.md v0.13 与本记录中留痕，维持 open，建议后续以非对称旋转（如 R_y(π/2)）补一构型。
  - **F-046**（Snapshot.cpp 风格）：本任务 diff 未触及对应区段，无变化。
  - 其余 open 项（F-004/005/006/009/020/029/031/033）与本任务无关联面。
- **追踪矩阵/契约状态**：契约 status 保持 ready（翻转归合入收尾，PIPE §4.7 同先例）；requirements-ids.json 字典（192 ID）为 testkit 既有资产，lint 字典校验用例消费之（我方复现中该用例因 B-1 前置失败，非字典问题）。

## 4.9 commit 质量 —— 通过

- 5 个提交标题均 `[WP-06-T12]`＋动词开头（修复/交付/实现/登记/登记）；正文四段（What/Why/验证/影响面）齐备；每提交带 `Acc-Covered` 行把提交映射到 acceptance 条目（超出基线要求的良好实践）。
- 验证段与留痕逐项一致：集成 261/260 过 1 跳过、冒烟 176/176、lint exit=0、validate-task PASS、ird_gates exit=1（3 命中已登记）——全部与其提交的日志吻合；ird_gates 红如实写"exit=1 命中 3 处已登记、不带病前进"（诚实口径）。
- 无粉饰发现；F-048 族零复发（见 4.8）。
- 建议级 S-2（转登 F-054）：ff4b943c 的"两模式构建日志 exit=0"未标注集成日志的构建树性质（主树存量增量树，F-008 掩盖效应）——F-036 已判的标注纪律（"增量树/全新树显式区分"）未落地；表述字面真实，不构成粉饰。

## 4.10 留痕完整 —— 通过

- builds/wp06-t12/ 共 11 件：integration-build.log / smoke-build.log / integration-test-run.log / smoke-test-run.log / sdurws_ird_runtime_test-integration.xml / sdurws_ird_runtime_test-smoke.xml / ird-test-report.json（ird-test-report/1 格式，逐用例登记＋目标面 notes）/ testdata-lint-run.log / validate-task-run.log / ird-gates-run.log——与 4.9 声明逐项对得上；*.log 均已实际入库（F-010 处置核实）。
- 验收者侧独立留痕：worktree A 下 acc-integration-test-run.log / acc-integration-runtime-test.xml / integration-acc-build.log / verify-target-build.log / smoke-acc-configure.log / smoke-acc-build.log / mutation-build.log / restore-build.log / acc-ird-gates-run.log（随复现 worktree 删除，本记录为其准一载体）。
- runtime.md v0.13 即本任务"实现落位＋证据指针"的文档留痕落点；phase-one-validation.log 口径沿用各任务 builds/ 目录登记（与本仓 WP-06 既有实践一致）。

## 4.11 偷懒/缩水实现扫描 —— 通过（但 4.4③ 的数据资产缺陷独立成立）

1. **缩水词扫描**：diff 新增行 `TODO|FIXME|placeholder|未实现|not implemented` 零命中；`stub` 全部命中为 **RT-STUB-0**（契约登记 ID 与替身边界声明本身——契约显式语义，豁免有据）。
2. **空实现检测**：无公共接口空函数体；ContractSuite.ChildProcessWorker 的 GTEST_SKIP 为 §11 RT-ID-1 设计的子进程 Worker 入口（父用例派生时实际执行——CrossProcessIdentityStable 通过即其行为证据），非占位缩水。
3. **acceptance 逐条证据表**：见 4.4——每条均具名用例/命令输出/文件行号；第 3 条证据充分且**不通过**（证据恰恰证明其在规定复现环境会红）。
4. **测试强度**：断言校验行为与边界（身份位模式/缓存 verdict+reasons/四消费方互等/能力位+诊断计数+subject 定位/往返逐字节）；4.5 双变异实证非恒真。

---

## 问题清单

### 阻断级（返工输入）

| # | 问题 | 证据位置 | 返工方向建议（最终方案由实施者与设计所有者裁决） |
| --- | --- | --- | --- |
| B-1 | 黄金数据集与容差资产以字节级完整性（sizeBytes＋SHA-256）交付文本文件，但未对 git 行尾归一化做任何防护：仓库本地 `core.autocrlf=true`＋`RobWork/.gitattributes`（仅 ppm/pgm/stl/stlb binary）⇒ fresh checkout（ACC §3 规定复现路径 `git worktree add`，同构于 fresh clone/CI）把 testdata json/mjs LF→CRLF，manifest 申报值失配，两数据集装载即抛 dataset-invalid | 失败输出：integration-test-run 5 失败与 smoke 3 失败消息聚类（仅 "期望 1812 实际 1867"/"期望 1170 实际 1207" 两型）；blob 实测：model.json=1812、robot.json=1170（＝manifest 申报）；检出实测：1867/1207（＝＋行数 55/37）；.gitattributes 全文 4 行；`git config core.autocrlf`=true | 首选：在 allowedFiles 面内（`testdata/**`）新增目录级 `.gitattributes`（如 `**/*.json text eol=lf`＋`**/*.mjs text eol=lf`，或整目录 `-text`）钉死行尾；或改由 CRLF 无关的完整性语义（需动 testkit——他单元，须走交接）。修复后必须在**全新 worktree 检出形态**自证（不可只在主仓工作树自测）；修复属数据资产形态修正，rt-fk-equation/rt-namemap-roundtrip 的 manifest integrity 值若因重生成而变化须同步并重新留痕 |

### 建议级（已按 ACC §5 强制转登 findings.json）

| # | 问题 | findings |
| --- | --- | --- |
| S-1 | F-050 改名（rt→rt-runtime）的注释残留：ContractSuiteTest.cpp `loadRtProfile` 上方注释与 GoldenDataLintTest.cpp `ToleranceProfileRtLoadsAndResolvesAllSuitePaths` 注释仍写 `testdata/tolerance/rt/v1.0.0.json`（零行为影响） | F-053 |
| S-2 | ff4b943c 留痕提交未标注 integration-build.log 的构建树性质（主树存量增量树——F-008 掩盖效应）；F-036 判例的"增量树/全新树显式区分"纪律未落地（表述字面真实，无粉饰） | F-054 |
| B-1 根因本体 | 见阻断表（为返工跟踪与后续 CI/fresh-clone 场景防护而登记，severity=major） | F-052 |

---

## 裁决

**verdict＝fail**：4.4 acceptance 第 3 条（lint 通过）与 4.3 测试复现在 ACC §3 规定复现环境下不成立（B-1）；4.1/4.2/4.3 命令面/4.5/4.6/4.7/4.8/4.9/4.10/4.11 均通过。本验收针对送验对象 `ff4b943ca275fb8c9d010d17dca1d923dd5e43d6` 本身（按 SHA 冻结口径）；实施者按本记录修复（B-1＋顺手 S-1/S-2）后重新发起**全量**验收（attempt 2），不增量验收。合入与否不适用（fail 不进入所有者合入环节）。

**说明（非裁决项）**：B-1 不否定本任务的设计与用例质量——契约套件、替身设施、RT-STUB-0 声明、RT-RES-3 检出修复与文档登记均为本链路迄今最完整的交付面之一；fail 的唯一根因是数据资产的检出形态脆弱性，且恰被本任务的 lint 机制本身**如实检出**（机制工作正常，数据需要钉住行尾）。
