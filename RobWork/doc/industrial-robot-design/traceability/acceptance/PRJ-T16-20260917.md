# PRJ-T16 验收记录（acceptance record）

| 字段 | 值 |
| --- | --- |
| 任务 ID | PRJ-T16（文档与门禁同步；unit=project；masterWp=WP-C/WP-04-T16；纯文档任务——outputs=traceability-update、interUnit=false） |
| 送验对象（冻结） | 3fe0ee71ace3ab0c82675ddb6771004ba740c34e（分支 wp04-t16；diff 范围＝94a2072ecee79a746f1b00e3a2ab40606caf0617..3fe0ee71，state.base..headSha，含 af6417c4＋3fe0ee71 两个提交） |
| 验收尝试 | attempt 1（分支漂移检测：git fetch 后 origin/wp04-t16 == 3fe0ee71 全 40 位一致，零漂移；acc/PRJ-T16/1 分支名此前不存在） |
| 验收环境 | 全新 detached worktree（`git worktree add --detach` @ 3fe0ee71）从零配置复现（集成 build 树与冒烟 build 树均为本验收新建独立目录）；记录回传 worktree（acc/PRJ-T16/1 基于 origin/redesign-main@94a2072e）；pwsh 7＋MSVC 2022 x64＋vcpkg 经典模式（主仓库 vcpkg 目录——git 未追踪，worktree 天然不含）；验收全程只消费产物与文档，未读取实施者过程叙述 |
| verdict | **pass**（4.1～4.11 全部通过；无阻断级问题；建议级 3 条转登 findings.json F-175～F-177） |
| 验收日期 | 2026-09-17 |

## 4.1 改动范围 —— 通过

- `git diff --name-status 94a2072e..3fe0ee71` 实证 **4 个变更文件**：①`RobWork/RobWorkStudio/src/rwslibs/industrialrobot/project/include/sdurws/ird/project/README.md`（M，+2）②`RobWork/doc/industrial-robot-design/traceability/builds/wp04-t16/exec-record.json`（A，38 行）③`RobWork/doc/industrial-robot-design/traceability/wp04-t16-adjudication-requests.md`（A，104 行）④`RobWork/doc/industrial-robot-design/units/project.md`（M，+33/−11）——逐文件比对契约 allowedFiles 三条通配（`project/**`、`units/project.md`、`traceability/**`）**全部在册**。
- **逐提交核验**：af6417c4＝①②④、3fe0ee71＝③——两提交各自也零越界。
- forbiddenFiles（REQUIREMENTS.md／ARCHITECTURE.md）零触碰（diff 无该两文件）。
- 无未登记框架源码改动：diff 零 RobWork/RobWork/、RobWorkSim/、RWS 其他源码路径；patches/ 零新文件（SA-02）；**diff 零产品源码（.cpp/.hpp）与 CMake 文件**——纯文档＋留痕＋README 登记，与契约 outputs=traceability-update 一致。

## 4.2 可复现构建 —— 通过（全新树从零，任务目标面＋冒烟全量；纯文档任务 DTB §5.2 DoD 第 1 条免除口径已实证）

- **免除条款核实**（对抗式）：development-task-breakdown.md §5.2 DoD 第 1 条原文"**构建**：双模式配置构建零错误（纯文档任务免除）"（DTB:697）——exec-record.json 声明的免除依据属实。
- 集成模式：worktree A 全新从零配置 **exit 0**（Configuring done 28.4s＋Generating done），缓存确认 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`。
  - 环境备注（F-007/F-098/F-099/F-062 既有登记，非实施缺陷）：①worktree 不含 vcpkg——toolchain 指主仓库 `vcpkg/scripts/buildsystems/vcpkg.cmake`；②Qt prefix 补 `CMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`＋`CMAKE_CXX_FLAGS=-DNOMINMAX -DWIN32_LEAN_AND_MEAN -DWIN32`；③`RobWorkStudio.ini.template.static` 被 [Bb]in/ gitignore 吞（worktree 实测缺文件）——自主树复制后配置零错误。
  - 项目单元两测试目标构建：`sdurws_ird_project_test` **exit 0、": error" 计数 0**；`sdurws_ird_project_contract_test` **exit 0、error 计数 0**（依赖链 sdurw_math→core→testkit→project 全部全新编译）。
- 独立冒烟模式：独立目录从零配置（带 vcpkg toolchain——AGENTS §4.1 口径）＋全量构建 **exit 0、": error" 计数 0**。
- 全框架全量集成构建未单独重跑——F-008 基线框架缺陷判例（PRJ-T13/T14/T15 验收三次独立实证），本任务 diff 零框架触碰，不归本任务、不构成阻断。

## 4.3 契约 verify 命令 —— 通过

契约 verify 数组仅一条，验收者亲手在 worktree A 执行：

1. `pwsh -File RobWork/scripts/industrialrobot/validate-docs.ps1` → **PASS (20 units, 12 trace entries, 150 task files)，exit 0**——与 exec-record.json 声明的计数逐字一致，且执行时点在送验全部文档修改在盘之后（worktree A 即送验快照）。

附加（超出契约 verify 的自查面）：`pwsh -File RobWork/scripts/industrialrobot/validate-task.ps1 -TaskFile …PRJ-T16.json` → **PASS (1 tasks)，exit 0**。

## 4.4 acceptance 条目 —— 通过（三条逐项证据）

| # | 契约条目 | 证据（验收者亲手复现＋磁盘实测） |
| --- | --- | --- |
| 1 | units/project.md 与实现零偏差登记（偏差按 DTB §5.4 登记、不静默——§15.4 变更记录逐条可查）；公共头 README 核对（README 指向与 §12 任务拆分一致） | **§15.4 v0.18 行在位**（2026-09-17），卡头文档版本行 v0.17→v0.18 已同步。**零偏差磁盘实测**（验收者亲历 `ls`）：`include/sdurws/ird/project/` 恰 8 个阶段 A 公共头＋README（ArchivePort/CommandService/DraftService/PersistenceFormat/ProjectStore/QueryPort/StoreTypes/UndoRedo），ResourceRefs.hpp/PackageService.hpp/Upgrade.hpp 确实不存在＝§3.1 表内标注的阶段 B 落位项（设计内节奏非偏差）；StoreTypes.hpp 中 StorePath 仅 3 处注释提及、无类型定义＝v0.10"零消费者暂不落位"登记相符；`src/` 增量文件（Project.cpp、win32/IFileOps.hpp、win32/ILockOps.hpp、ConfirmationFlow.{cpp,hpp}、DiagRecords.hpp、QueryPortImpl.{cpp,hpp}）与新增 §3.4 磁盘现状注记**逐项一致**。**README 核对**：README.md 2026-09-17 登记段指向 units/project.md §12；§12 PRJ-T16 行（project.md:1269）产物列实测为"本文、`project/include/.../README.md`"、用例列含"README 核对；§11/§15.3 状态更新；门禁建议（含 P-PR-5/P-PR-6 裁决申请）提交"——README 声明的指向一致性成立 |
| 2 | §11 验证矩阵实现状态标注更新＋§15.3 待裁决项状态更新（P-PR-1～8 逐条最新状态：已消账/未决/裁决中——不留过期陈述） | **§11 标题过期陈述已更正**（"设计；未实现未运行"→"用例体已随 PRJ-T15 按本表落地"）＋表后新增 PRJ-TX-1～14 逐组实现状态登记表。**状态表数字亲手复现**：验收者在 worktree A 集成树实跑两测试可执行文件——单元 **255 中 254 PASSED＋1 SKIPPED**（跳过用例恰为 `Tx11CopyConsistency.StageBPrerequisiteNotReady_SkipRegistered`——TX-11 阻塞登记，GTEST_SKIP 在 Tx11Tx13SuiteTest.cpp:285 实证，不计通过）、契约 **28/28 PASSED**——与登记表"单元 254/255＋1 显式跳过、契约 28/28"及在库留痕 traceability/builds/wp04-t15/ird-test-report.json（"254 passed＋1 skipped＋0 failed"/"28/28"）**逐项一致**。**§15.3 P-PR-1～8 逐条对端事实核验**（全部亲查）：P-PR-1＝core.md 卡头实测 v0.11 Draft-Structured ✓；P-PR-2＝ARCHITECTURE 卡头实测 v0.12 Draft（待评审）✓；P-PR-3/8＝O-08（DTB:609）/O-19（DTB:620）登记维持 ✓；P-PR-4 已消账＝execution.md §9.5（:867"与 project 的归档协作（project §10.1 对端；P-PR-4 冻结）"）＋其 P-EX-5 行（:1236"本文 §9.5 已冻结……请 project 侧确认消账"）——本文确认零增量需求、对端消账由其卡承接，两侧一致 ✓；P-PR-7 已消账＝ui.md（:979 交叉核对通过、结论登记 §16.3 D-4；:1551"本文交叉核对通过（§9.2、D-4）"）＋runtime.md §10.2 承接答复"兼容"（:1219，快照句柄/取消令牌两处差异登记）及其 P-RT-8 行（:1510，等 project 侧确认——本文侧确认由本任务登记，不私改对端）✓。九行均附〔PRJ-T16 状态同步 2026-09-17〕时间戳标注，无过期陈述残留；P-PR-9 不在 acceptance 点名范围、行未被触碰（裁决申请附表"如实附记"口径一致） |
| 3 | P-PR-5/P-PR-6 裁决申请提交（O-09 处置联动：project→io 边补登或注入形态确认——裁决权在架构所有者，本任务只提交申请与依据，不私裁补边；WP-04-T17/T18 届时按 DOC-T10 契约 O-09 处置条款回填）；P-PR-6 申请联动 diagnostics.md（码值部分已按 §15.4 v0.2 消账，sink 形态待裁决——不私改对端） | **traceability/wp04-t16-adjudication-requests.md 在库**（104 行，文档头明示"只提交申请与依据，不构成裁决"）。**申请依据逐条实测为真**：①ARCH §3.5 project 行（ARCHITECTURE.md:219）实测"`project → core, diagnostics`"——确无 project→io 边；②io.md §8 依赖表（io.md:216）实测"io ←（注入）project……**未登记编译边**；P-IO-1（关联 P-PR-5）——本文按注入式设计，接口不因裁决结果变化"——承接答复逐字相符；③DTB §4.2 O-09（DTB:610）实测"登记（阻塞 WP-04-T18 编译形态，不阻塞注入式先行设计）＋架构所有者（补登边或确认注入形态）"——O-09 处置条款（§5 回填路径）与登记口径一致；④P-DIAG-5（diagnostics.md:1291）实测"IDiagnosticSink＋DiagnosticsSinkImpl 兼容形态已交付……report/reportDev 与 project §5.0/execution §3.3 同形签名逐字一致；消账路径＝project/execution 详设修订联动，本文不私改对端"✓，且 §9.7 IDiagnosticSink（diagnostics.md:1050/1064）在案；⑤签名同形亲验：project 侧 StoreTypes.hpp:212/222 `report(const core::DiagnosticRecord&)`/`reportDev(const std::string&, const std::string&)` ✓；⑥同案引用 execution.md §15.2 R-6（:1224"P-EX-8，P-PR-6 同模式"逐字相符）＋IExecutionDiagnosticsSink（execution.md:254）✓；⑦码值消账声明成立（diagnostics.md §4.6 内置码表 ：373 收编 PRJ-* 全量，:390 逐码展开登记）。**不私裁实证**：diff 无 ARCHITECTURE/io/diagnostics/execution 任何文件（4.1）；裁决者署名＝申请一架构所有者、申请二 diagnostics 详设所有者＋架构所有者 ✓ |

**ird_gates（门禁同步任务的核心自查面，验收者实跑复现）**：worktree A 集成树 `cmake --build … --target ird_gates` → **rc=1（命中即非零＝SA-10 设计）**，真实命中**恰 11 项**：IRD-GATE-SUB×9（diagnostics_contract/evidence/evidence_contract/policy/policy_contract/project/project_contract/runtime 八个测试目标直链 sdurws_ird_testkit＋sdurws_ird_testdata_lint 表外边）＋IRD-GATE-T1×1（testdata_lint）＋IRD-GATE-R4×1（project/src/ProjectStoreImpl.cpp RobWork 字面量——wp04-t08 既有，**本任务 diff 未触碰该文件**）。与 exec-record.json 声明的命中构成**逐项一致**、与 wp04-t15 验收基线 11 项一致（**零新增**）。引擎自测 9 项（pass_clean/pass_r4comment＋fail_r1/t1/r5/sub/r3/r4/t2 按预期检出）全部符合；SA-02 工作区核对零命中（worktree 干净）。exec-record 对门禁日志编码坑的注记（多命中共享同一物理行、需按 `[ird_gates] ` 分隔重建）经本验收实测属实。

## 4.5 测试真实失败能力 —— 通过（校验器三探针即红、还原复绿；本任务零测试代码，探针对象＝门禁与契约校验器）

本任务 diff 零测试代码（纯文档），4.5 的"改测试断言"形态不适用，按纯文档任务口径改用**校验器失效注入**（验证验证者）：

- 探针 1：worktree A 将 tasks/foundation/PRJ-T16.json 的 schemaVersion 行注入 `,,' 语法破坏 → validate-docs 报"从 JSON 转换失败…Invalid property identifier"＋"duplicate task ids"，**干净退出码 1**（有牙齿）。
- 探针 2：向 acceptance-protocol.md 追加过期措辞"记录落在 \`acc/<taskId>\` 分支" → validate-docs 报"ACC still contains stale acc/<taskId> evidence-branch wording"（锚点门禁有效）。
- 探针 3：契约副本将 status 行变异为重复键 → validate-task 抛"invalid task status: PRJ-T16"（契约校验器有效）。
- 逐次 `git checkout --` 还原后 `git status --porcelain` **零输出**（复现现场与送验 SHA 逐字节一致）；另以两测试可执行文件实跑（254＋1 skip／28 全绿）证实送验文档引用的行为数字真实。

## 4.6 架构红线 —— 通过

- **L2 零 Qt**：diff 零代码文件，新增内容零 `#include`（Qt 头计数 0）。
- **R-1 业务域互链禁止／R-2 跨单元私有头**：零 CMake/代码变更，零新增链接边与 include；ird_gates 实跑 SUB/T1 命中集合与基线逐项一致、零新增（4.4 实证）。
- **R-4 名称前缀拼接/剥离**：唯一 R4 命中＝既有 ProjectStoreImpl.cpp 字面量（wp04-t08 起），本任务 diff 未触碰；新增文档零拼接/剥离语义。
- **SA-02 框架零修改**：4.1 实证（diff 零框架路径、patches/ 零新文件）＋门禁 SA-02 核对零命中。
- **PA-1 权威唯一**：文档修改面＝project 单元卡（本单元权威）＋project 公共头 README（本单元产物）＋traceability/**（治理留痕面，契约 allowedFiles 明文）；裁决申请＝契约 acceptance 3 明文的流程动作（interUnit=false 判定成立），未改动任何对端单元卡/架构/需求语义。
- **PA-2 不可变历史**：§15.4 变更记录只增不改（v0.17 行原样保留，仅新增 v0.18 行）——diff 实证。
- **不继承 old/**：无。

## 4.7 注释规范 —— 不适用（零代码新增），README 登记段质量核查通过

- 本任务**零新增/大改代码文件**（无 .hpp/.cpp/.cmake 变更——4.1 实证），AGENTS §2 对代码注释的强制要求无承载面。
- 产品树唯一变更文件 README.md 为文档：新增 2026-09-17 登记段为中文、带日期与任务编号（PRJ-T16≙WP-04-T16）、逐头列名与落位登记指针（§15.4 v0.4～v0.16）、阶段 B 未落位项的"设计内节奏而非偏差"论证、"本文件仍不参与编译、不自行宣布实现通过"的边界声明——信息量与可追溯性合规。

## 4.8 文档同步 —— 通过（含 findings 未关闭条目相关性核对）

- **实现与任务卡零偏差**：acceptance 三条逐项对账（见 4.4）全部落位；零偏差声明经磁盘实测复核成立（8 公共头/src 增量/阶段 B 未落位项/StorePath 均与 §3.1/§3.4/§15.4 登记一致）。**零设计变更**：§3.1 一处事实性过期陈述更正（README 指向 §9→§12 的 2026-09-10 已修正事实同步）＋§11 标题/状态表＋§3.4 注记＋§15.3 状态标注，无设计条目语义修改（diff 逐行核验）。
- **单元卡同步**：卡头版本行 v0.18 已同步；§15.4 v0.18 行声明的核对结论与验收者实测逐项一致。任务状态/队列由编排者收尾更新（PRJ-T15 先例同口径）。
- **findings.json 未关闭条目相关性核对（153 条 open，逐条扫描本任务域）**：
  - ①**F-170 家族（卡头日期行）**：units/project.md 卡头『日期』行实测仍停留 **2026-09-09**（版本半边已同步 v0.18，日期半边未动）——F-122/F-133/…/F-168/F-170 家族**第十三次再现**，转登 **F-175**；
  - ②**F-012/F-119 家族（提交标题动词开头）**：af6417c4 标题"project 单元卡与实现零偏差同步及验证状态登记"前缀后名词开头——家族再现（3fe0ee71"提交…"合规），转登 **F-176**；
  - ③**F-172/F-010（幻影留痕/.gitignore *.log）**：本任务**预防性如实处置**——exec-record.json 明示 .log 原件因 .gitignore:87 `*.log`（验收者实测该行在册）不入库、evidenceFile 标注"工作区，git 忽略"，**零幻影声明**（F-172 教训被吸收，无新登记）；
  - ④**F-173/F-115（门禁 SUB 例外行实例增补归 WP-01-T03）**：仍 open；本任务门禁实跑零新增命中，11 项＝基线，无新联动面；
  - ⑤**F-174（ProcessCrashContractTest 加固）**：本任务零代码变更未触及该文件，resolution"随下次触及顺手加固"维持原状；
  - ⑥其余 open 条目（构建环境坑 F-007/F-062/F-068/F-075 等）与本任务文档改动面无相关性，其中 F-007 系列本次验收按既有建议规避成功。

## 4.9 commit 质量 —— 通过（附建议级）

- **af6417c4**「[WP-04-T16] project 单元卡与实现零偏差同步及验证状态登记」——正文四段（What/Why/验证/影响面）齐备＋Acc-Covered: 1,2；逐文件改动清单与 diff 一致；验证结论（validate-docs PASS 计数、validate-task PASS、ird_gates 11 项＝基线零新增、DTB §5.2 免除口径）经本验收逐项亲手复现**全部为真**；**但标题前缀后为名词短语开头（"project 单元卡…"）非动词开头**——F-012 家族再现，转登 F-176。
- **3fe0ee71**「[WP-04-T16] 提交 P-PR-5/P-PR-6 裁决申请（O-09 处置联动，不私裁）」——**动词开头合规**；正文四段齐备＋Acc-Covered: 3；"未私改对端：diff 无 ARCHITECTURE/io/diagnostics/execution 文件"实测为真；正文验证段"validate-docs PASS（150 task files 扫描含本文件）"表述不精确——validate-docs 扫描面为 tasks/**/*.json 等，**不含 traceability/*.md 新文件**（裁决申请文档不在其扫描面；exec-record.json 中同一事实的表述"执行时点在……裁决申请文档之后"才是准确口径）——无结论性失实（PASS 本身真实可复现），转登 **F-177**（表述精度，F-172 家族的弱化变体）。

## 4.10 留痕完整 —— 通过

- **在库**：traceability/builds/wp04-t16/exec-record.json（ird-exec-record/1）——三项 validations（validate-docs PASS／validate-task PASS／ird_gates rc=1 命中 11 项＝基线）经本验收**全部独立复现一致**；acceptanceCoverage 三键与契约 acceptance 三条一一对应。
- **未入库（已声明，非幻影）**：validate-docs.log/validate-task.log/ird-gates.log 留工作区——.gitignore:87 `*.log` 全局规则实测在册，exec-record 明示该口径并引用 F-172 处置建议（留痕后缀约定归 findings 流程，不私改 .gitignore——处置正确）。本任务零测试执行面（无 gtest XML/ird-test-report 义务）；测试行为数字的引用面（§11 表/README）由验收者实跑复现兜底（4.4/4.5）。
- phase-one-validation.log 未更新——PRJ-T12～T15 先例同口径（阶段一专用 log；本任务 resultNote 载体＝exec-record.json，已更新且完整）。

## 4.11 偷懒/缩水实现扫描 —— 通过

- **①标记扫描**：diff 新增行 `TODO|FIXME|XXX|HACK|stub|placeholder|not implemented|未实现|待实现|占位` 扫描——命中 2 处均为 units/project.md §15.4 v0.18 行**引述被更正的旧标题原文**（"标题『设计；未实现未运行』过期陈述同步更正"——记载更正动作本身，非未完成标记），零有效命中。**契约显式豁免面逐条列出**：ResourceRefs.hpp/PackageService.hpp/Upgrade.hpp 三头与 StorePath 类型未落位＝§3.1 表内标注的阶段 B 落位项/零消费者不预建（单元卡 v0.10 登记＋README 登记段双重声明"设计内节奏而非偏差"）——契约 outputs=traceability-update、零代码义务，无隐式缩水。
- **②空实现检测**：零代码变更，不适用。
- **③逐条证据表**：见 4.4 三条逐项表——每条均有具名文件/行号＋验收者亲手复现结果，任一条不缺。
- **④强度**：无新增测试；门禁与契约校验器的检出强度经 4.5 三探针实证即红；送验文档引用的测试行为数字（254＋1 skip／28）经验收者实跑为真——非"只编译通过"、非恒真声明。

## 问题清单（建议级，全部转登 findings.json，无阻断级）

| # | 级别 | 摘要 | findings 编号 |
| --- | --- | --- | --- |
| G-1 | 建议 | units/project.md 卡头『日期』行停留 2026-09-09（§15.4 v0.18 行＝2026-09-17；版本半边 v0.18 已同步）——F-122/F-133/…/F-168/F-170 家族第十三次再现 | F-175 |
| G-2 | 建议 | af6417c4 标题『project 单元卡与实现零偏差同步及验证状态登记』前缀后名词开头非动词开头——F-012/F-119 家族再现（3fe0ee71 合规） | F-176 |
| G-3 | 建议 | 3fe0ee71 正文验证段"validate-docs PASS（150 task files 扫描含本文件）"表述不精确：validate-docs 扫描面（tasks/**/*.json＋traceability JSON＋PIPE/ACC 锚点）不含 traceability/*.md 新文件——PASS 结论真实可复现、无结论性失实，但理由表述与扫描面不符（准确口径见 exec-record.json"执行时点在……裁决申请文档之后"） | F-177 |

## 验收者环境与可复现性说明

- 复现现场：`git worktree add --detach <_acc_tmp/PRJ-T16-A> 3fe0ee71ace3ab0c82675ddb6771004ba740c34e`；集成配置（含 F-007 三补点：vcpkg toolchain／Qt prefix＋NOMINMAX 系／ini 模板复制）→ 集成构建两测试目标 → 冒烟独立目录从零配置＋全量构建 → ird_gates 实跑 → validate-docs/validate-task → 测试可执行文件实跑（254＋1 skip／28）→ 三探针注入/还原 → `git status --porcelain` 零输出收尾——全部验收者亲手执行。
- 验收者运行留痕留存于验收临时目录（cfg_int.log／build_int_test.log／build_int_ctest.log／cfg_smoke.log／build_smoke.log／gates_run.log）；库内证据面以送验 exec-record.json 与 wp04-t15 在库留痕为准（其实测数据与验收者复现逐项一致）。
- 本记录与 findings.json（F-175～F-177 追加）经 acc/PRJ-T16/1 分支 commit＋push 回传；验收者未向任务分支 wp04-t16 推送任何提交、未修改送验产物、未执行合入。
