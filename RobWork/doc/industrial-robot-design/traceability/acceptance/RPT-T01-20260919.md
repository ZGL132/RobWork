# RPT-T01 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | RPT-T01（构建落位：reporting 占位转真实库；≙WP-12-T02，masterWp WP-H） |
| 送验对象（冻结） | 636f065ef43180cfa691bb9b802b3b1f8cf405fe（分支 wp12-t01；diff 范围＝state.base..headSha＝3c77c716e74f5e13fd6c8abc546c060ae5752860..636f065ef43180cfa691bb9b802b3b1f8cf405fe，range 内含 508b3e33／e80e05f9／636f065e 三个提交） |
| 验收尝试 | attempt 1（分支漂移检测：`git rev-parse origin/wp12-t01` ＝ 636f065e… 全 40 位逐字一致，零漂移；`acc/RPT-T01/1` 分支名此前不存在——ls-remote 实测 0 条） |
| 验收环境 | 复现 worktree A＝`git worktree add --detach D:/10_Source_Repos/21_robot/acc_worktrees/rpt-t01-repro` @ 636f065e（detached @ SHA，全新从零：集成 build 树与冒烟 build 树均本验收新建独立目录，零缓存复用）；基线门禁 worktree C＝detached @ 3c77c716（ird_gates 基线复跑用）；记录回传 worktree B＝`acc/RPT-T01/1` 基于 origin/redesign-main@3c77c716；冒烟目录＝D:/10_Source_Repos/21_robot/acc_worktrees/rpt-t01-smoke（源＝worktree A 的 industrialrobot 目录）。pwsh 7＋MSVC 2022 x64（VS 17.12，cl 19.42）＋vcpkg 经典模式（toolchain 绝对路径指向主仓库 vcpkg——worktree 天然不含，vcpkg 为 .gitignore 排除项）＋Qt 6.11.1 msvc2022_64。集成配置参数＝主树缓存实证口径（UI-T02 验收同款，F-098/F-099 既有登记非实施缺陷）：`-G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=<主仓>vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64 -DCMAKE_CXX_FLAGS="-DNOMINMAX -DWIN32_LEAN_AND_MEAN -DWIN32"`，并自主树复制 `RobWorkStudio.ini.template.static`＋`RobWorkStudio.ini.shared.in` 两件 git 未追踪本地辅助件（bin/ 整目录被 .gitignore 吞——UI-T02 验收同款亲历）。验收全程只消费产物、契约、设计文档与在库留痕，未读取实施者过程叙述；结论全部出自验收者亲手重跑 |
| verdict | **pass**（4.1～4.11 全部通过；无阻断级问题；建议级 2 条转登 findings.json F-230/F-231） |
| 验收日期 | 2026-09-19 |

## 4.1 改动范围 —— 通过

- `git diff --stat 3c77c716..636f065e` 实证 **25 个变更文件**（3 M＋22 A，2085 insertions）：①`industrialrobot/CMakeLists.txt`（M，父注册）②～⑥`industrialrobot/reporting/**` 五件（CMakeLists.txt 新、src/Reporting.cpp、test/BuildRedLineTest.cpp、contract_test/LinkageContractTest.cpp、include/sdurws/ird/reporting/README.md）⑦`units/reporting.md`（M，v0.4）⑧～㉕`traceability/builds/wp12-t01/` 18 件留痕。
- **allowedFiles 逐文件比对**：①命中契约 allowedFiles 第 2 条（父 CMakeLists）；②～⑥命中第 1 条 `reporting/**`；⑦命中第 3 条 `units/reporting.md`；⑧～㉕命中第 4 条 `traceability/**`——**25/25 全部在册，零越界**。
- forbiddenFiles（REQUIREMENTS.md／ARCHITECTURE.md）**零触碰**（diff 无该两文件；`git diff --name-only <range> -- '*REQUIREMENTS*' '*ARCHITECTURE*'` 空输出亲跑）。
- **SA-02**：diff 范围内零框架源码文件；`git diff <range> -- '*patches*'` 空输出——patches/ 无新文件即无登记义务，实证无未登记框架改动。

## 4.2 可复现构建 —— 通过（两模式全新树从零，零错误）

- **集成模式**（worktree A 内新建 `build/`，重配后缓存确认 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`——亲 grep `build/CMakeCache.txt` 输出原行）：`cmake --build build --config Release` **全量构建 exit 0、": error"/": fatal error" 计数 0**（RobWork 全库→RobWorkSim→RobWorkStudio→industrialrobot 全单元全新编译，含 20 单元全部测试目标；产物含 `build/RobWorkStudio/bin/Release/sdurws_ird_reporting{,_test,_contract_test}.exe|.lib`）。首配曾因 bin/ 缺两件 ini 模板失败（F-099 环境链，复制后从零重配成功——非送验对象缺陷，见环境段）。
- **独立冒烟模式**（独立目录，源＝worktree A 的 industrialrobot，`-DCMAKE_TOOLCHAIN_FILE=<主仓>vcpkg`＋Qt prefix）：配置＋全量构建 **exit 0、错误计数 0**；`reporting/Release/` 下 `sdurws_ird_reporting.lib`＋两测试 exe 就位。首配未带 Qt prefix 时在 `testkit/CMakeLists.txt:239` `find_package(Qt6)` 失败——testkit_qt 为 UI-T02 已落位的既有配置面依赖（本任务 diff 零触碰 testkit），补 Qt prefix（UI-T02 验收同款参数）后通过，属环境参数而非送验缺陷；送验留痕 `smoke-configure.log` 同样带 Qt6 前缀（WrapAtomic 探测行实证）。
- **C++17 混链（P-ENV-1）双模式实证**：生成树亲查 `sdurws_ird_reporting.vcxproj` 含 `<LanguageStandard>stdcpp17</LanguageStandard>`；与框架 C++11 基线目标同链同构建零错误——混链复证成立。

## 4.3 契约 verify 命令 —— 通过（两条逐条亲手执行）

在 worktree A 根目录按契约字面逐条独立执行，**两条全部 exit 0**：

1. `cmake --build build --config Release --target sdurws_ird_reporting_test` → exit 0
2. `cmake --build build --config Release --target sdurws_ird_reporting_contract_test` → exit 0

附加执行（验收者亲跑）：集成模式直接运行两测试可执行文件——`sdurws_ird_reporting_test.exe` **5/5 PASSED**（exit 0）、`sdurws_ird_reporting_contract_test.exe` **4/4 PASSED**（exit 0）；冒烟模式同口径复跑（rpt-t01-smoke）**5/5＋4/4 PASSED**（exit 0）。两模式四轮全绿。

## 4.4 acceptance 条目 —— 通过（五条逐项证据）

| # | 契约条目 | 证据（验收者亲手复现＋磁盘实测） |
| --- | --- | --- |
| 1 | 双模式构建零错误；sdurws_ird_reporting 由 INTERFACE 占位升级 STATIC（C++17、PUBLIC 链 core＋evidence＋diagnostics＋project——reporting.md §3.4），目标名与别名 RWS::ird::reporting 不变；新建 reporting/CMakeLists.txt 并在父 CMakeLists 注册；src/ 空起步不在骨架预建占位实现 | 双模式零错误见 4.2（两树全新从零亲跑）。`reporting/CMakeLists.txt:75`＝`add_library(sdurws_ird_reporting STATIC src/Reporting.cpp)`（STATIC＋锚点翻译单元）；`:82`＝`target_compile_features(PUBLIC cxx_std_17)`＋`:83-86`＝CXX_STANDARD 17/REQUIRED ON/EXTENSIONS OFF（不用 C++20）；`:93-95`＝/utf-8 PUBLIC；`:123-127`＝PUBLIC 链 `sdurws_ird_core`＋`sdurws_ird_evidence`＋`sdurws_ird_diagnostics`＋`sdurws_ird_project` 四行字面书写（统一真名，集成模式 RWS::ird::* 即其 ALIAS）；`:131-133`＝`add_library(RWS::ird::reporting ALIAS sdurws_ird_reporting)`（目标名/别名不变——DTB §5.1 升级口径）。父 `CMakeLists.txt` diff 实证＝`IRD_MODULES` 移出 reporting（:41 起仅剩 9 单元）＋`:166`＝`add_subdirectory(reporting)` 挂载于 ui（:146）/project（:100）/diagnostics（:89）之后（次序约束成立）。`src/Reporting.cpp` 亲读＝仅 73 行注释块、零声明零定义（"空起步"先例形态，CORE-T01/POL-T01/DIAG-T02/PRJ-T01/EX-T01 同款） |
| 2 | 红线扫描零命中（RP-GATE-1 全项）：编译依赖边仅四条 ARCH §3.5 登记边；零 Qt 含 Core；无私有头出 include；零 io/runtime/execution 编译边（注入形态——公共头零对端类型）；零 Eigen/rw::math 直含；产品目标不链 testkit | **ird_gates 命中集合独立复现**：head（worktree A）与 base（worktree C@3c77c716）两侧分别 `cmake -DIRD_ROOT=<各worktree>/industrialrobot -DIRD_ENABLE_GIT=OFF -P cmake/ird_gates.cmake`（脚本模式亲跑，两侧均 exit 1＝命中即 FATAL 预期形态），提取 IRD-GATE-* 行归一化 IRDRoot 后**去重命中集各恰 27 项、逐行 diff 零差异——GATES-HIT-SET-IDENTICAL**（本任务零新增命中；27 处全为 F-227/wp10-t02-gate-registrations.md 既有归属，与送验 gates-hit-set-diff.txt 结论一致）。白名单 `ird_gates_whitelist.cmake:38-41`＝"reporting->core/evidence/diagnostics/project" 四行预登记亲读在位。**include 面独立 grep**：reporting 五文件 `#include <Q`/`"Q`/`rw/`/`Eigen`/`sdurws/ird/<他单元>` 指令行**零命中**（grep 命中全为扫描器用例自身注释与字符串字面量）。**链接面**：配置期守卫（`reporting/CMakeLists.txt:264-285`，LINK/INTERFACE 两面读取，^sdurws_ird_ 通配拦 testkit/io/runtime/execution/业务域＋零 Qt＋产品库零 gtest）两模式配置通过即全绿。**运行期具名自证**：BuildRedLineTest 5 用例＋LinkageContractTest 4 用例亲跑全过（UnitEdgesFourRegistered 构建图白名单、NoTestkitEdge T-1、RegisteredEdgesPresent 四边存在性、NoIoRuntimeExecutionCompileEdge 双向扫描、NoQtInclude、NoEigenOrRwMathDirectInclude、NoCrossUnitInclude、PublicHeaderPathLayout） |
| 3 | 测试目标按 reporting.md §3.4/DTB §5.5 注册并可构建（gtest 经 vcpkg find_package(GTest CONFIG REQUIRED)，失败即停）；实现任务未落地前允许最小构建冒烟用例（PRJ-T01 同款口径） | `reporting/CMakeLists.txt:67`＝`find_package(GTest CONFIG REQUIRED)`（REQUIRED＝失败即停）；`:146-147`＝`add_executable(sdurws_ird_reporting_test test/BuildRedLineTest.cpp)`；`:195-196`＝`add_executable(sdurws_ird_reporting_contract_test contract_test/LinkageContractTest.cpp)`；`:167-169`/`:211-213`＝链 sdurws_ird_reporting＋GTest::gtest_main（main 自持——DTB §5.5 落位期最小形态）；`:174-175`/`:216-217`＝IRD_REPORTING_UNIT_ROOT 源码树注入；`:234-246`＝ird_add_gtest 宏（add_test＋`<target>_report` XML 报告目标，EXCLUDE_FROM_ALL）＋`:248-249` 两调用。**契约 verify 两命令亲跑 exit 0**（4.3）；9 条首批用例全为最小构建冒烟用例（红线扫描/构建图边界——契约 acceptance 3 明文允许，未预建业务占位实现；ScriptedSectionProvider/ScriptedResultSource 替身与 RP-* 用例体按卡 §11 归 RPT-T11） |
| 4 | reporting README 复核留痕：现文指向复核并在留痕中登记核对结论，保留"不参与编译/不表示已实现"声明 | `README.md:4` 亲读＝磁盘实指 `units/reporting.md §11`（任务拆分）——**实施者对卡 v0.1~v0.3"已指向 §9，实测一致"与磁盘不符的复核结论属实**（PRJ-T01 家族同型偏差）；`README.md:8`＝2026-09-19 复核行在位（指向核对＋落位事实＋零悬空指向＋RPT-T13 前置事实登记）；"不参与编译/不表示已实现"声明保留（:3-6、:8 末段亲读）。单元卡两处更正亲读在位：`units/reporting.md` 卡头"构建落位"行＋§3.1 组成表 README 行（v0.4，diff 实证），§14.4 v0.4 变更记录行登记 |
| 5 | 陷阱处置：P-ENV-1 显式 C++17 不用 C++20、双模式混链验证留痕；P-RPT-1 零 io 编译边（IReportIoFactory 注入）；P-RPT-9 四条边仅链接目标不消费上游公共头 | P-ENV-1：`reporting/CMakeLists.txt:82-86`（PUBLIC cxx_std_17＋CXX_STANDARD 17，零 C++20 字样）＋生成树 vcxproj stdcpp17 亲查＋4.2 双模式混链亲跑。P-RPT-1：PUBLIC 链接块恰四行（:123-127，零 sdurws_ird_io）；`NoIoRuntimeExecutionCompileEdge_DT_BUILD_PRPT1_PRPT2` 用例亲跑通过（include 面＋构建图面双向）；io/runtime/execution 头零 include 亲 grep；注入形态在 :114-119 注释与父 CMakeLists 注册注释双登记（签名零改动预留直连口径在位）。P-RPT-9：产品源码（Reporting.cpp）与测试源码（两测试 .cpp）**零 sdurws/ird/<上游单元> include**（亲 grep——仅注释提及），`NoCrossUnitInclude` 白名单扫描用例为该处置常驻自证（亲跑通过），"仅链接零消费"口径在 :56-59/LinkageContractTest.cpp:23-30 注释登记 |

## 4.5 测试真实失败能力 —— 通过（两处断言注入即红、还原复绿）

在 worktree A（本验收沙箱，实施分支零触碰、不提交不推送）注入两处应失败输入，重建后亲跑：

1. **零 Qt include 扫描断言**：`reporting/src/Reporting.cpp` 尾部注入 `#if 0` 块内 `#include <QObject>`（预处理剔除、可编译——专测运行期行扫描独立于编译器的敏感性）→ 重建 exit 0（编译面如设计不受影响）、`sdurws_ird_reporting_test` 运行期 **FAILED：`ReportingBuild.NoQtInclude_DT_BUILD_NFR_MNT_01`（1 failed，exit 1）**——扫描器对指令行真实敏感，非"只编译通过"。
2. **构建图白名单断言**：`reporting/CMakeLists.txt` 守卫块前注入 `if(FALSE) message(STATUS "injected reference: sdurws_ird_testkit") endif()`（语法有效、不执行——专测文本级目标引用扫描）→ 重配置成功（配置期守卫读链接属性、不受文本注入影响——两道防线职责分离的旁证）、`sdurws_ird_reporting_contract_test` 运行期 **FAILED×2：`ReportingLinkage.NoTestkitEdge_DT_BUILD_T1`＋`ReportingLinkage.UnitEdgesFourRegistered_DT_BUILD_R1_R2`（2 failed，exit 1）**。

`git checkout --` 还原两处后 `git status --porcelain` 源码树零残留（仅验收者自建日志文件）、重建 exit 0、两测试复绿 exit 0。**测试非恒真**：断言对输入真实敏感；断言内容校验行为与边界（白名单集合比对、指令行门控、存在性双向钉扎），非空转。

## 4.6 架构红线 —— 通过（AGENTS §5 八条逐条）

1. **L2 零 Qt**：reporting 为 L3 平台服务且采用更严 D-01（零 Qt 含 Core）——include 面亲 grep 零 Qt 指令行；链接面零 Qt 目标（配置期守卫 :277-280＋NoQtInclude 用例＋门禁命中集 27 项中零 reporting 新增亲证）；父 CMakeLists 注册注释明文"零 Qt（D-01 零 Qt 含 Core——比 L3 层规则上限更严）"。
2. **R-1 业务域互链禁止**：编译依赖边恰四条登记边（:123-127）；构建图白名单用例＋配置期 ^sdurws_ird_ 通配守卫＋门禁命中集零新增三面实证。
3. **R-2 跨单元私有头**：include/ 仅 README.md 保留位（亲 ls），无私有头出 include（PublicHeaderPathLayout 用例在位且随 RPT-T02+ 落头自动生效）。
4. **SA-02 框架零修改**：4.1 实证——diff 零框架文件、patches/ 零触碰。
5. **R-4 名称前缀拼接/剥离**：新代码零 RobWork 名称字符串拼接/剥离逻辑（五文件亲读；门禁 R4 命中集与 base 一致零新增——命中 2 处均为 project 单元既有归属）。
6. **PA-1 权威唯一**：本任务仅构建落位面——目标注册/链接边/测试注册，未实现 project/runtime/policy/core 等他单元语义；io/runtime/execution 能力按注入形态预留（P-RPT-1/P-RPT-2 口径与单元卡 §3.3 原文一致）。
7. **PA-2 不可变历史**：零修订写路径触碰（diff 无 project/history 相关文件）。
8. **old/ 不恢复**：零 old/ 引用（五文件亲读；全部按 reporting.md v0.4 与任务卡从零落位）。

## 4.7 注释规范 —— 通过（公共头全查＋实现文件 4 份全查，超出 ≥3 抽查线）

- **公共头**：本任务无 .hpp（include/ 仅 README.md 保留位，契约头随 RPT-T02+）——README.md 为说明文档，"不参与编译/不表示已实现"声明保留亲读确认。
- **实现/脚本/测试文件 4 份全读**（`reporting/CMakeLists.txt` 285 行、`src/Reporting.cpp` 73 行、`test/BuildRedLineTest.cpp` 244 行、`contract_test/LinkageContractTest.cpp` 262 行，父 CMakeLists 改动块同读）：①每文件均有文件头注释块（@file/@brief＋设计依据逐条标注 reporting.md §x.x/DTB §5.x/任务契约 acceptance n/需求 NFR-MNT-0x＋背景说明——"为什么"而非复述"做什么"，如 Reporting.cpp 头注释完整解释锚点翻译单元存在的 CMake 技术原因与 NFR-MNT-04 业务原因、isIncludeDirective 注释解释指令行门控与 PRJ-T01 误报先例）；②Doxygen 中文描述齐备，函数注释含职责与失败语义（readFile"读失败显性失败，不留假阳性通道"、collectTargetRefs 剥注释原因、白名单分支的畸形行兜底语义）；③§2.5 高危信息：线程约束（Reporting.cpp :66-69 无可执行代码无线程议题＋后续 §9.9 归属标注安排）、错误语义（配置期守卫 FATAL＝构建失败 SA-10）、确定性来源（collectCppFiles 排序——NFR-COR-02 精神）、所有权/生命周期（静态路径注入语义）；④魔法数字/模式来源全部标注（"#include <Q"两形态全盖理由、^sdurws_ird_ 通配拦截范围、unitEnd 兜底分支）；⑤CMake 关键步骤中文注释块（目标注册/依赖红线/门禁逻辑——AGENTS §2.7 要求）。
- 零过期注释、零中英机翻腔、零无信息占位（TODO 仅为"TODO(WP-xx)"要求面——本任务零 TODO，见 4.11①）。

## 4.8 文档同步 —— 通过

- **单元卡增量修订**：`units/reporting.md` v0.3→v0.4（diff 实证：卡头构建落位行更新为落位后事实、§3.1 README 行"指向 §9"误记按磁盘实测更正为 §11、§14.4 v0.4 行登记）——文档-代码同步按 DTB §5.4 执行，契约语义零变更（§3.4"骨架 INTERFACE → RPT-T01 升级 STATIC"兑现）。
- **README 复核结论**：acceptance 4 前置事实登记在位（4.4 条目 4）。
- **任务状态/追踪矩阵**：任务状态翻转属合入收尾（PIPE §4.7⑤）动作，验收段不要求；`traceability/phase-one-task-index.json:916-922` RPT-T01 行与契约一致亲读；DTB :332 WP-12-T02 行、:741 20/20 显式任务清单、reporting.md §11 RPT-T01 行（:1336）三处亲读一致。
- **findings.json 未关闭条目相关性核对（v1.2）**：全册 229 条（197 open）亲查——grep `reporting|RPT-` 仅 2 处提及：F-066（open，evidence 诊断码建议值越枚举清单，其文提及 reporting 侧 RPT-CURRENTNESS-UNEVALUABLE 为呈现面消费码）与本任务的关联归 RPT-T02+ 消费任务与 diagnostics 注册任务处置，RPT-T01 构建落位无可消账面；F-120（批次范围缺陷）已 fixed。**无历史发现静默积压、无本任务可顺手消账项**。
- **治理脚本复跑**（worktree A 亲跑）：`validate-task.ps1 -TaskFile tasks/foundation/RPT-T01.json` → **PASS (1 tasks)**；`validate-docs.ps1` → **PASS (20 units, 12 trace entries, 150 task files)**——与在库 `builds/wp12-t01/validate-task.log`/`validate-docs.log` 逐字一致。

## 4.9 commit 质量 —— 通过

三个提交逐一亲读（`git log --format=%B <range>`）：

| commit | 标题 | 四段 |
| --- | --- | --- |
| 508b3e33 | [RPT-T01] 实现构建落位：reporting 占位转真实库并注册两测试目标 | What（五文件逐件）/Why（契约＋需求 NFR-MNT-01/02＋P-ENV-1/P-RPT-1/P-RPT-9）/验证（集成 exit 0＋verify 两命令＋5/5＋4/4＋冒烟＋门禁 GATES-HIT-SET-IDENTICAL＋validate 双 PASS）/影响面（目标名别名不变、锚点 TU 豁免依据明示、无 TODO/FIXME）齐备 |
| e80e05f9 | [RPT-T01] 同步单元卡 v0.4 与 README 落位复核行 | What/Why（acceptance 4＋DTB §5.4）/验证（validate-docs PASS）/影响面（纯文档、零语义变更）齐备 |
| 636f065e | [RPT-T01] 登记验证留痕：双模式构建、门禁命中集 diff 与测试报告证据 | What（18 件证据逐类）/Why（AGENTS §4.2 DoD）/验证（留痕与执行一一对应）/影响面（纯留痕）齐备 |

- 标题均动词开头（实现/同步/登记）；正文四段齐备。
- 前缀 `[RPT-T01]`（契约任务号）而非模板 `[WP-nn-Tkk]`：任务契约明文 RPT-T01 ≙ WP-12-T02，redesign-main 已有 [IO-T07]×2/[DIAG-T06]/[DIAG-T07] 合入先例——**不构成粉饰或格式违规**，但两套编号并存属口径含糊，转登建议级 F-230（见问题清单）。
- **验证结论与留痕一致性**：提交所述各结论（构建 exit 0、5/5＋4/4、门禁命中集一致、validate PASS）经本验收独立复现**全部为真**；未发现"未执行的标注通过"或粉饰。

## 4.10 留痕完整 —— 通过

- **在库留痕**（送验提交 636f065e 内 `traceability/builds/wp12-t01/` 18 件，逐件在位亲验）：集成构建日志（integration-build.log，21619 B）＋冒烟 configure/build 日志；两测试目标 gtest XML 四件（integration/smoke × unit/contract_test——**亲读 tests="5" failures="0" ×2、tests="4" failures="0" ×2**）＋四份直接运行日志；门禁证据四件（target 口径 27 命中＋branch/base 两轮脚本口径日志＋hit-set-diff——GATES-HIT-SET-IDENTICAL）；validate-task/validate-docs 日志两件；ird-test-report.json（ird-test-report/1 schema——目标级结论、用例级 trace 字段、acceptance 逐条证据指针、P-ENV-1/P-RPT-1/P-RPT-9 处置留痕与 README 复核结论登记，亲读结构与内容完备）。
- **验收者自跑留痕**（本记录证据，临时目录随验收 worktree 说明于环境段）：集成/冒烟两模式四轮测试运行、两 verify 命令、门禁 head/base 两轮脚本日志、注入/还原构建与运行、validate 双复跑——关键结论已逐条摘录于本记录各节。
- `phase-one-validation.log`/resultNote：本任务留痕载体＝builds/wp12-t01/ 目录（EX-T10/UI-T02 等近期任务同款先例，未更新 phase-one-validation.log 不构成缺口——该文件属阶段一验收汇总面，非逐任务义务）。

## 4.11 偷懒/缩水实现扫描 —— 通过

1. **标记扫描**：`git diff 3c77c716..636f065e` 全文 `TODO|FIXME|stub|placeholder|not implemented|未实现`（-i）命中 4 处，**产品源码面（industrialrobot/**）零命中**（第二条窄域 grep 亲跑空输出）；4 处全为 `traceability/builds/wp12-t01/smoke-build.log` 中 MSBuild 编译输出转录的**他单元既有真实文件名**（ConfirmableProjectStubTest.cpp／CatalogHostTraceStubTest.cpp／StubHandlers.cpp——project 单元契约测试既有文件，与本任务零关系）。无未豁免命中。
2. **空实现检测**：公共接口函数本任务为零（无公共 .hpp）——"空实现且契约未声明"零命中。`src/Reporting.cpp` 空（仅注释）翻译单元＝**契约显式声明形态**（acceptance 1 原文"src/ 空起步，不在骨架预建占位实现"＋CMake 零源码 STATIC 技术不可行的既定先例形态，CORE-T01/POL-T01/DIAG-T02/PRJ-T01/EX-T01 五先例同款，Reporting.cpp 文件头与父 CMakeLists 注册注释双登记）——**豁免成立，非偷懒**。
3. **acceptance 逐条证据表**：见 4.4 表格——五条每条给出具名测试/亲跑命令输出/文件行号，无一条仅凭"已实现"声明。
4. **测试强度**：9 条断言全部校验行为与边界（白名单集合逐元素比对、#include 指令行门控扫描、四条登记边存在性钉扎、公共头路径布局前缀断言、锚点 TU/保留位存在性），非恒真、非"只编译通过"；与 4.5 联动——两处注入实证会红、还原复绿。

## 问题清单

- **阻断级**：无。
- **建议级（均已按 ACC §5 强制转登 findings.json，编号顺延自 F-229）**：
  - **F-230**（G-1，suggestion）：提交标题前缀双轨口径——本任务三提交用 `[RPT-T01]`（契约任务号），AGENTS §6.2/§6.3 模板字面为 `[WP-nn-Tkk]`（≙WP-12-T02），redesign-main 已有 [IO-T07]×2/[DIAG-T06/T07] 合入先例；同一任务两套编号（契约 RPT-T01 vs DTB WP-12-T02）并存且两式前缀均曾通过验收。建议 AGENTS.md §6.2/§6.3 或 DTB §5.1 固化"契约任务号前缀与 WP 号前缀等价可用"或统一其一（附映射口径），消除验收者逐案裁量。owner＝AGENTS/DTB 文档所有者；不阻塞合入。
  - **F-231**（G-2，suggestion）：留痕日志编码不统一——`builds/wp12-t01/gates-ird-gates-target.log` 为 GBK/ANSI 控制台捕获（UTF-8 工具直读为乱码，iconv 解码后内容完整可核），同目录其余 17 件为 UTF-8；后续读者 grep 证据时需额外转码。建议留痕捕获统一 UTF-8（pwsh 重定向显式 `-Encoding utf8` 或 `chcp 65001` 后捕获）。owner＝实施纪律（DTB §5 留痕约定维护方）；不阻塞合入（本验收已以 iconv＋脚本模式独立复现完成核验，27 命中集与 base 一致为实）。

## 验收者环境说明（复现要点）

- 全程按 SHA 不按分支：worktree A detached @ 636f065e；origin/wp12-t01 漂移检测通过；验收期间未向 wp12-t01 推送任何提交（送验对象冻结遵守）。
- 集成模式首配失败与冒烟模式首配失败均为环境参数（ini 模板补件/Qt prefix 缺省），补齐后从零双绿——非送验对象缺陷，与 UI-T02 验收环境段同款口径。
- 门禁命中集复现口径：脚本模式（cmake -P）与送验的构建目标模式命中数一致（27），脚本模式每命中打印两遍（扫描诊断＋末尾汇总），去重后 27——与送验 gates-ird-gates-target.log 的"命中 27 项"折算一致，GATES-HIT-SET-IDENTICAL 由去重集逐行 diff 实证。
- ird_gates 27 处命中为本仓库既知基线形态（F-227/wp10-t02-gate-registrations.md 既有归属：SUB×17＋LIB×4＋R3×3＋R4×1＋T1×2），非本任务引入；"红线扫描零命中"的验收判据按契约语境＝零**新增**命中（RP-GATE-1 对本任务交付面各项零命中），与 EX-T10/UI-T02 同口径。
