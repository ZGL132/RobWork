# RT-T08 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | RT-T08 实现 DynamicWorkCell 编译器（能力门控）（契约 tasks/foundation/RT-T08.json） |
| 单元 | runtime（WP-06-T08） |
| 送验对象（冻结） | a9f95c309521df7d40da2d6584de01fe5a21dcdf（分支 wp06-t08；分支漂移检测：origin/wp06-t08＝本地 wp06-t08＝该 SHA，零漂移） |
| diff 范围 | 430c30ad144a9f8c10f70832755e17f44056adc4..a9f95c309521df7d40da2d6584de01fe5a21dcdf（3 个提交：b4aa2378 实现 / cfcc3965 留痕 / a9f95c30 文档同步） |
| 验收协议 | acceptance-protocol.md v1.5（ACC），全新上下文对抗式核查，构建与测试全部由验收者亲手重跑 |
| **verdict** | **pass**（4.1～4.11 全部通过；零阻断级问题；建议级 1 项转登 findings F-045） |
| 验收日期 | 2026-09-12 |

## 验收者环境说明

- 复现现场：`git worktree add --detach <临时目录A> a9f95c309521df7d40da2d6584de01fe5a21dcdf`（detached @ SHA），集成模式在该 worktree **全新配置全新构建**（非主树增量）。
- 全新集成树配置参数（沿 F-007/F-009 已登记口径）：`-DCMAKE_TOOLCHAIN_FILE=vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`，并按 F-007 处置建议从主树复制被 .gitignore 吞掉的 `RobWorkStudio/bin/RobWorkStudio.ini.template.static`（环境修复，非实施产物改动）。首配不带 toolchain 时以 `Could NOT find Boost` 失败、带后配置零错误——F-007 所述环境缺陷的本次实测复证。
- 集成测试运行需将 Qt6/vcpkg bin 目录加入 PATH（DLL 加载）；冒烟测试直接运行各单元 test exe。
- 记录回传：本文件落 `acc/RT-T08/1` 分支（基于 origin/redesign-main＝430c30ad）随 findings.json F-045 登记同提交推送。

## 4.1～4.11 逐项证据

### 4.1 改动范围 —— 通过

- `git diff --name-status 430c30ad..a9f95c30`：共 19 文件——runtime 单元 7 个（CMakeLists.txt、Adapter.hpp、src/DynamicWorkCellCompiler.hpp/.cpp〔新增〕、src/WorkCellCompiler.hpp/.cpp、test/DynamicWorkCellCompilerTest.cpp〔新增〕）、traceability/builds/wp06-t08/ 留痕 11 个、units/runtime.md 1 个。全部落在契约 allowedFiles（`runtime/**`、`traceability/**`、`units/runtime.md`）内。
- forbiddenFiles（REQUIREMENTS.md / ARCHITECTURE.md）零触碰（不在 diff 清单）。
- SA-02：patches/ 无新文件（diff 内零 patches 路径），框架源码零改动；rwsim 仅经公共头/库消费。

### 4.2 可复现构建 —— 通过（验收者全新树亲手重跑）

- 集成模式：worktree A 全新配置（`RWS_BUILD_INDUSTRIALROBOT:BOOL=ON` 缓存确认），`cmake --build build --config Release --target sdurws_ird_runtime_test` 退出码 0，错误计数（`: error|error C\d+|error LNK|error MSB`）＝0。
- 独立冒烟模式：独立临时目录＋vcpkg toolchain（F-002 口径）配置零错误，`cmake --build . --config Release` 退出码 0、错误计数 0。
- 结论日志（验收者本地）：integration-configure.log / integration-build.log / smoke-configure.log / smoke-build.log（临时目录，结论以本记录与送验留痕对应；送验侧同名留痕 traceability/builds/wp06-t08/*.log 与验收者实测一致）。

### 4.3 契约 verify 命令 —— 通过

- 契约 verify＝`cmake --build build --config Release --target sdurws_ird_runtime_test`：亲手执行，退出码 0（见 4.2）。
- 全量测试（验收者自跑，非采信送验 XML）：
  - 集成：`sdurws_ird_runtime_test.exe` → `[==========] 199 tests from 50 test suites ran`、`[  PASSED  ] 199 tests.`、退出码 0；自产 XML `tests="199" failures="0" disabled="0" errors="0"`。
  - 冒烟：5 单元 test exe 全绿（core 61 / evidence 4 / policy 4 / **runtime 172** / testkit 97）；runtime 172/172 与送验声明一致（新增面按登记不进冒烟目标——见 4.8 模式分工核验）。
- 治理脚本：`validate-task.ps1 -TaskFile …/RT-T08.json` → PASS；`validate-docs.ps1` → PASS（20 units, 12 trace, 150 task files）。
- `ird_gates` 独立复跑（`cmake --build build --target ird_gates`）：命中恰 2 项＝IRD-GATE-SUB/T1（sdurws_ird_testdata_lint→testkit），与送验 ird-gates-run.log 及 findings F-019 登记的既有基线逐项相同，**runtime 单元零新增命中**。

### 4.4 acceptance 条目逐项 —— 通过（3/3 有证据）

1. **RT-CPX-1/2、RT-AD-3 门控/原子性/销毁顺序用例通过** —— 通过。验收者自跑 199 例含：RT-CPX-2＝`AllLinksNoPhysicsSkipsDwc_RT_CPX_2`（降级非失败＋缺失清单链序＋恰一条 RT-CAPABILITY-MISSING 警告＋WC 帧数不变）、`PartialMissingPhysicsSkipsWithPreciseList_RT_CPX_2`（清单精确指向 link_1）；RT-CPX-1＝`DwcConstructionFailureIsAtomic_RT_CPX_1`（预置同名帧触发真实基线"Frame name is not unique" RW_THROW→DwcCompileFailed＋detail 含 stage=S7＋WC 零残留）、`NullWorkCellRejected_RT_T08`（空句柄 fail-fast）；RT-AD-3＝`DestructionOrderDwcBeforeWorkCell_RT_AD_3`（weak_ptr 控制块观测：WC 不早于 DWC 释放、持 DWC 期间 WC 可查无 UAF、最终全释放无泄漏）。RT-AD-3 的 ASAN→控制块计数证据形态适配已按 DTB §5.4 登记 units/runtime.md §15.4 v0.9⑤，正当（MSVC 工具链无 ASAN 的等价观测，断言面覆盖顺序/泄漏/UAF 三轴）。
2. **能力缺失降级路径（§5.6 正交——RuntimeCapability 声明）用例通过** —— 通过。`ProvidedIllegalIsNotCapabilityMissing_S5_6`（m=−1 provided 非法→S5 InputInvalid 构造拒绝，与 NotProvided 降级严格分域）＋上述 RT-CPX-2 两例＋`ToolAndFrictionMissingStillCompiles_RT_T08`（工具物性/摩擦缺项不阻止构造、Body 数=3、承载材质不注册）。
3. **DWC/摩擦 API 表达力前置实测（R-2）** —— 通过。实测结论三条（DWC 构造器齐全／RigidBody 强制 MovableFrame 基帧／摩擦仅 material-pair 载体）登记于 units/runtime.md §15.4 v0.9②；表达力缺项按 §8.8 默认"无补丁方案"以 MaterialDataMap Custom 数据承载（typeName/参数名冻结），零 rwsim 源码修改、不触发补丁流程；端到端读回证据＝`FrictionCarriedViaMaterialDataMap_R2`（fv/fc/bias 逐字读回，验收者自跑通过）。knownPitfalls P-RT-3：仅消费 §8.1 已登记 L1 库，ird_gates 复跑零表外命中（见 4.3）。

### 4.5 测试真实失败能力 —— 通过（验收者独立双探针＋送验探针留痕核验）

- **探针 1**：精确注释 `dwc->setGravity(model.world().gravityWorld)`（DynamicWorkCellCompiler.cpp:356）→ 重建退出码 0（显式核验，防旧二进制假绿）→ `CompiledPathBodiesAndExplicitValues_RT_T08` **变红**（"重力应取 canonical 默认 −9.81（非基线 −9.82）"等断言失败，运行退出码 1）。
- **探针 2**：精确注释材质注册＋`addFrictionData` 两行（同文件 §3g 段）→ 重建退出码 0 → `FrictionCarriedViaMaterialDataMap_R2` **变红**（运行退出码 1）。
- 精确还原后 `git status --short`（实现树）零输出（无探针残留）、重建退出码 0、全量重跑 `[  PASSED  ] 199 tests.` 退出码 0——复绿。
- 送验侧探针留痕（probe-rt-cpx2-red.log）核验：禁用能力门控→恰 RT-CPX-2 两例变红、其余 9 例通过——失败面精确，与声明一致。

### 4.6 架构红线（AGENTS §5 八条）—— 通过

1. L2 零 Qt：`grep -rn "include.*Qt" runtime/{src,include,test}` 零命中；CMake 配置期 Qt 链接守卫在位。
2. R-1 业务域互链禁止：`sdurws_ird_runtime` 链接清单仅 `sdurws_ird_core`＋L1 基线（sdurw_kinematics/models/sdurwsim，P-RT-3 已登记）；CMakeLists 配置期红线守卫（其他 `sdurws_ird_*` 即 FATAL_ERROR）在位且验收者配置构建通过。
3. R-2 跨单元私有头禁止：新增私有头 src/DynamicWorkCellCompiler.hpp 仅被 src/DynamicWorkCellCompiler.cpp 与单元内 test include；对外新增实体 DynamicWorkCellConstView 落公共头 Adapter.hpp。
4. SA-02：零框架源码改动（4.1）；rwsim 摩擦缺项按 §8.8"无补丁方案"承载而非改源码。
5. R-4 名称前缀：S7 全部对象名（Body 承载帧/连杆帧/基座帧/Tcp/材质 id/设备名）经 `buildRuntimeNameMap`/`scopedFullName`/`resolveObjectId` 取自映射，零拼装；工具体复用 Tcp 条目名避免规则外拼名（§15.4 v0.9④ 选型）。
6. PA-1 权威唯一：能力位以 S5 派生的 `model.capabilities().hasDynamicWorkCell` 为唯一权威，S7 重检仅作防御性互核，不一致→fail-fast 而非自行改判（DynamicWorkCellCompiler.cpp:170-174）。
7. PA-2 不可变历史：编译输入全部只读注入（const CanonicalModel&），零项目写路径。
8. 不继承 old/：全新实现，无 old/ 痕迹。

### 4.7 注释规范（AGENTS §2）—— 通过

- 公共头全查：Adapter.hpp 新增 DynamicWorkCellConstView 类＋三方法 Doxygen 中文注释完整（背景/所有权/生命周期耦合/线程安全/@param/@return/@throws 与稳定 token 前缀）；WorkCellCompiler.hpp 新增 scopedFullName 声明注释含背景（§7.2/PA-1）、@param/@return/@throws。
- 实现文件全查（新增 2＋大改 2，超抽查下限）：DynamicWorkCellCompiler.hpp/.cpp 文件头（设计依据含需求号/任务号、R-2 实测结论、线程安全、单位纪律 kg·m²/N·m·s/rad、坐标系读法、异常边界顺序约束）、函数头（compileDynamicWorkCell 五步契约逐条）、函数体内逐步注释（每个语义段"做什么＋为什么"：门控互核两方向、降级零副作用、ownedPtr 与裸 Ptr 构造的悬垂差异、MovableFrame 单参构造语义、getDataID 抛错实测注记等）。
- §2.5 高危信息表逐项：物理单位（kg/m/kg·m²/N·m·s/rad/N·m/m·s²，makeBodyInfo 与文件头显式标注）；坐标系（体系下质心、世界系重力、T_ab 读法）；角度制式（rad 随 SI 单位纪律声明）；所有权（StateStructure 持有帧、ownedPtr 引用计数接管、getBodyFrame 裸指针只读回溯、快照唯一规范持有者）；线程约束（可重入无共享可变状态、发布后只读、构造期单线程可见）；错误语义（调用方违约 fail-fast vs 环境失败转译稳定码，异常边界注释明确"转译不吞"）；确定性（同输入同产物、材质 id 取映射全名、链序清单）。
- 测试文件：文件头用例↔需求/验收标准追溯＋每 TEST 注释验证目标与 AT/RT 编号（RT-CPX-1/2、RT-AD-3、§5.6、R-2）。
- 未发现中英混杂机翻腔/复述代码/过期注释。

### 4.8 文档同步＋findings 相关性 —— 通过

- 实现与任务卡零语义偏差；实现层选型/分阶段事项（RigidBody MovableFrame 承载帧、摩擦 material-pair 承载、工具体锚 Tcp 帧、销毁顺序证据形态、scopedFullName 提升、两模式编译分工续 v0.8②）已按 DTB §5.4 登记 units/runtime.md §15.4 v0.9（随 a9f95c30 提交），登记正当性核验：模式分工的技术依据成立——rwsim DynamicWorkCell/RigidBody/RigidDevice 为非模板外联符号，冒烟模式无框架库可链，故产品/测试增列按 `TARGET sdurw_kinematics` 条件；冒烟口径（目标注册＋include 路径）不受影响且 172/172 实证既有面零回归。
- 契约状态翻转属合入段（PIPE §4.7），不在验收者权限；validate-task PASS 确认契约结构合规。
- **findings.json 未关闭条目相关性核对**（open 项逐一过筛）：
  - F-004/F-005/F-006/F-012/F-013（同型）/F-015~F-018/F-020/F-023~F-026/F-028~F-033/F-034~F-042/F-044：分属构建约定/其他单元卡/既有提交风格与留痕面，与 RT-T08 交付面无交集或已按家族处置口径登记，本任务不消账。
  - F-007/F-008/F-009（全新树环境/插件构建/worktree 门禁）：本次验收实测复证其存在（Boost 配置失败、ini 模板缺失），属环境面既有登记，不阻塞本任务（任务目标两模式从零复现零错误——与 F-008 行"ired 范围内任务不受影响"一致）。
  - F-019（ird_gates 既有红＝IRD-GATE-SUB/T1 两项）：本次独立复跑命中集合与登记逐项相同，零新增——继续挂起，不消账。
  - F-043（RT-T06 重力投影方向测试敏感度缺口，owner＝runtime 实现所有者"随 RT-T07 或用例增补批次顺手修复"）：RT-T07/T08 契约均未承接（§6 基座—世界域非 S7 交付面），**继续挂起**；本任务新增的 §8.2 DWC 重力显式设值断言（−9.81 vs −9.82 判别）为另一facet 且探针 1 实证其敏感，但 gravityToBase 投影方向用例增补仍待后续。
  - F-012 家族（提交标题名词短语开头）：RT-T08 送验提交中 cfcc3965（"验证留痕：…"）与 a9f95c30（"文档同步：…"）同型再现（b4aa2378"实现…"为动词开头合规）——家族第 13/14 例，按既有判例为建议级不构成阻断，**转登 F-045**（见下）。
- 计数准确性（F-041 同型预检）：提交/报告声明"集成 199＝既有 188＋新增 11、冒烟 172"——实测 11 个 TEST 定义、集成 XML tests=199、基线 wp06-t07 XML 188、冒烟 172，**计数准确无出入**。

### 4.9 commit 质量 —— 通过（1 项建议级转登 F-045）

- 三提交均含四段正文（改动内容/改动原因/验证/影响面），任务编号前缀 `[WP-06-T08]`。
- 验证结论与留痕一致性：逐句对照验收者实测——"集成 199/199（XML tests=199＝存量 188＋新增 11）" ✔、"冒烟 172/172" ✔、"ird_gates 命中 2 项均为既有登记项（IRD-GATE-SUB/T1）" ✔（F-019 同源）、"validate-task PASS" ✔、"探针禁用能力门控→恰 RT-CPX-2 两例变红→精确还原复绿" ✔（probe-rt-cpx2-red.log 实证 9 通过＋2 失败且失败面恰为 RT-CPX-2）。**未发现粉饰**。
- 建议级：cfcc3965/a9f95c30 两标题以名词短语开头（"验证留痕：…"/"文档同步：…"），不合 ACC §4.9"动词开头"——F-012 家族同型再现（第 13/14 例），按家族判例纯风格瑕疵不阻断，转登 findings F-045，随家族统一处置（不追溯改写已推送历史）。

### 4.10 留痕完整 —— 通过

- traceability/builds/wp06-t08/ 十一件留痕入库（.log 经 git add -f 绕过 .gitignore——F-010 口径，`git diff --name-status` 实证 11 个 Add）。
- gtest XML 双份：integration `tests="199" failures="0"`、smoke `tests="172" failures="0"`，与 ird-test-report.json 声明一致；ird-test-report.json（ird-test-report/1）三条 acceptance 逐条消账＋11 例逐例 trace＋probe 段＋gates 段齐备。
- 构建日志（integration/smoke configure＋build＋test-run）、ird-gates-run.log、probe-build.log、probe-rt-cpx2-red.log 齐备；probe-build.log 末段含探针运行内容（送验侧无独立 probe-test-run.log——ird-test-report.json probe.method 已如实描述"探针运行内容在 probe-build.log 末段"形态，且本记录 4.5 已由验收者独立复现探针，不构成缺口）。

### 4.11 偷懒/缩水实现扫描 —— 通过

- ① 标记扫描：diff 范围内 `TODO|FIXME|HACK|stub|placeholder|未实现|not implemented|XXX` 大小写不敏感扫描，命中 2 处均为 `RT-STUB-0`（测试替身边界声明的**需求矩阵条目名**，"RobWork/rwsim 本体不做替身"——units/runtime.md §11:1355 定义的设计用例编号，非偷懒标记），豁免依据成立；零其余命中。
- ② 空实现检测：新增公共接口（DynamicWorkCellConstView 三方法）与 compileDynamicWorkCell 均为真实实现（541 行实现体走读：门控互核/降级分派/Body 装配/显式设值/摩擦承载/三重关联校验/异常边界），无空体、无"仅返回默认值"。
- ③ 逐条证据表：见 4.4——契约 acceptance 三条均有具名测试＋验收者亲手执行输出。
- ④ 测试强度：断言校验行为与边界（逐元素 mass/com/inertia、重力 −9.81/−9.82 数值判别、映射名逐字节命中、真实基线 RW_THROW 注入路径、weak_ptr 过期时序、N 体↔N 关节集合相等），非恒真；4.5 双探针实证断言会红。

## 问题清单

### 阻断级

无。

### 建议级（已转登）

| 编号 | 内容 | 处置 |
| --- | --- | --- |
| F-045 | F-012 家族同型再现（第 13/14 例）：RT-T08 送验提交 cfcc3965（"[WP-06-T08] 验证留痕：…"）与 a9f95c30（"[WP-06-T08] 文档同步：…"）标题以名词短语开头，不合 ACC §4.9 动词开头；b4aa2378（"实现 …"）合规。两提交正文四段齐备、验证结论与验收者实测逐项一致（集成 199/199、冒烟 172/172、门禁命中＝F-019 既有 2 项、validate-task/docs PASS、双探针即红还原复绿），纯风格瑕疵不构成阻断 | 随 F-012 家族统一处置（实施者模板补动词开头示例）；已推送历史不追溯改写（AGENTS §6.3） |

### 挂起项核对（不消账、不阻塞）

- F-043（gravityToBase 方向用例敏感度）：S7 域外，待 runtime 后续用例增补批次；本任务新增重力显式设值断言已实证敏感（探针 1），属另一 facet。
- F-019（ird_gates 既有 2 项命中）：本次复跑集合与登记相同，零新增。

## 裁决

**verdict＝pass**：4.1～4.11 全部通过，无阻断级问题；建议级 F-045 转登 findings.json（随本 evidence 分支同提交），不阻塞合入。验收者未合入、未修改实施产物、未向任务分支推送任何提交。
