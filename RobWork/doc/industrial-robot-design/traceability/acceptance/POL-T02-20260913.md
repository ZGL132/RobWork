# POL-T02 验收记录（对抗式验收）

| 字段 | 值 |
| --- | --- |
| 任务 ID | POL-T02（≙WP-07-T02，policy 错误与策略类型——PolicyErrorCode 全表＋PolicySet 四态承载/发布门） |
| 契约 | tasks/foundation/POL-T02.json（status: ready，随收尾翻转 done；acceptance 4 条＋O-10/O-11/O-18 登记类处置条款） |
| 验收对象 | 分支 `wp07-t02`，head＝`0c09beb4d682c494bb15bdc48e22bef71cbba78c`（40 位冻结对象）；漂移检测：origin/wp07-t02 == 0c09beb4（零漂移）；范围＝`12199ef04e28422399ba41572d23bc10280a1297..0c09beb4`（＝state.base 即 origin/redesign-main 尖端..headSha，单提交：代码＋测试＋文档同步＋留痕同一提交） |
| 尝试序号 | attempt=1（evidence 分支 `acc/POL-T02/1`） |
| **verdict** | **pass**（4.1~4.11 全过；无阻断级问题；建议级 1 项转登 findings F-075——ACC §4.5 探针头文件注入的增量编译陷阱，F-068 同族第二形态；另 F-065 留痕乱码在门禁日志再现，属既有 open 项延续，不新增编号） |
| 验收日期 | 2026-09-13 |
| 独立性声明 | 全新上下文，只消费契约/单元卡/代码/留痕产物；未读实施叙述。补偿性对抗动作＝worktree A（detached @ 0c09beb4）亲手全新配置双模式构建＋全量测试（30/30×2）、两个独立缺陷探针（不同文件不同断言，还原后复绿＋git status 零残留核验）、ird_gates base/分支命中清单逐字比对、单元卡 §5.2/§9.6/§4.4/§4.5/§4.6 权威原文与实现逐点核对 |

---

## 逐项证据（4.1~4.11）

| # | 检查 | 结论与证据 |
| --- | --- | --- |
| 4.1 | 改动范围 | `git diff --name-status 12199ef0..0c09beb4`：17 文件＝5 个代码/构建文件（policy/CMakeLists.txt＋policy/include/sdurws/ird/policy/{Errors,PolicySet}.hpp＋policy/src/Errors.cpp＋policy/test/PolicySetTest.cpp）全部位于 `policy/**`（allowedFiles）＋11 个留痕文件（traceability/builds/wp07-t02/，DoD §4.2"留痕随实现提交"口径）＋units/policy.md（allowedFiles 显式列出，+1 行＝§15.4 v0.3 变更记录）；forbiddenFiles（REQUIREMENTS.md/ARCHITECTURE.md）零触碰；diff 无框架源码文件、patches/ 无新文件——SA-02 零框架源码改动 |
| 4.2 | 可复现构建 | worktree A（detached @ 0c09beb4，全新 build 树）：集成配置两步命中 F-007 已登记环境缺口（①缺 vcpkg toolchain/Qt 前缀→Boost 找不到；②RobWorkStudio/bin/RobWorkStudio.ini.template.static 为 git 未跟踪文件 worktree 天然缺失）→ 自主树复制该未跟踪模板＋补 toolchain/Qt 参数 → 重配成功且缓存确认 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`（配置命令含 -D 显式 BOOL=ON）；`cmake --build build --config Release --target sdurws_ird_policy_test` exit 0（构建日志中仅 F-071 既有 C4099×2、F-004 既有 MOC 空转、Temp 目录 MSB8029 环境告警——均非本 diff 引入）；测试 30/30 PASSED（验收者亲手运行）。冒烟：独立临时目录 ird-acc-POLT02-smoke＋vcpkg toolchain 配置＋构建 exit 0；测试 30/30 PASSED（均验收者亲手执行） |
| 4.3 | verify 命令 | 契约 verify（单条）＝`cmake --build build --config Release --target sdurws_ird_policy_test`——在 worktree A 集成树亲手执行 exit 0（与 4.2 同一命令独立复跑核验）；附带运行测试可执行文件全绿 |
| 4.4 | acceptance 逐条 | ①"字段约束与不可变性用例通过"：验收者亲手双模式各 30/30（XML tests=30 failures=0）；不可变性为类型层静态断言（PolicySetTest.cpp L388-394 拷贝/整体赋值可、默认构造删、成员私有；L751-757 发布对象拷贝可/赋值删/默认构造删）＋域窗边界用例（D-08 边界：SafetyClearance=0、NearLimitRatio=1.0、ConditionNumber=1.0 合法；0/负/越窗/NaN/±Inf 逐项拒绝——L228-413）；②"O-10/P-POL-2 保守口径"：ConservativeDefaultsNoInventedValues_P_POL_2_O10（L424-443 默认 nullopt＝显式不适用、唯一冻结默认 4π rad＝DefaultAppendixD）＋FieldDefaultsPerCard（L516-526 safetyClearance 无 C++ 默认）＋EnabledWithoutClearanceRejected（L532-544 enabled 而 safetyClearance 缺省→ThresholdRequiredMissing 拒绝发布）——不发明数值核实（PolicySet.hpp 无任何模板数值常量，仅 kDefaultFiniteRotationTravelLimit=4π 一处）；③"O-11 不预填惯量比"：NoInertiaRatioField_O11_P_POL_3（L483-492 SFINAE static_assert 探测 inertiaRatio 成员不存在＋追加通道锚 kPolicySchemaVersionCurrent==1）；④"O-18 不并入耦合阈值"：NoCouplingThresholdField_O18_P_RT_7（L498-509 三候选命名 SFINAE 全部钉住不存在）；ird_gates（WP-01-T01 已建成，如实执行）：验收者比对送验 gates-current-wp07t02.log 与 gates-base-12199ef0.log——分支树与 base 快照 worktree 复跑命中**逐字相同**的 5 项（IRD-GATE-SUB×4＋T1×1，全部指向 sdurws_ird_evidence_test/evidence_contract_test/runtime_test/testdata_lint，均不在本任务 allowedFiles 内＝base 既有、非本任务引入），引擎自测 8 项按预期（引擎正常性自证）——对**本次变更**零命中，gate-all 16/17 唯一失败项即该既有命中，实施者未越界私改其他单元（F-019/F-072 既有治理项维持 open）；validate-task PASS（ird-test-report.json notes④ 登记） |
| 4.5 | 测试真实失败能力 | 探针一（Errors.cpp 码面表 mutation："policy/rule-duplicate"→"policy/rule-dup-mutated"）→ TokenTableFullEnumeration_NFR_COR_02 与 ErrorMessagePrefixConvention_AGENTS_ErrorSemantics 如期变红。探针二（PolicySet.hpp L259 NearLimitRatio 域窗 `siValue <= 0.0`→`siValue < 0.0`）→ 首次重建**未红**（增量依赖陷阱：touch 头文件未触发 PolicySetTest.cpp 重编——F-068 同族第二形态，转登 F-075）→ touch 测试源强制重编 → NearLimitRatioBoundaries_D08_POL_PARSE_2 如期变红（0.0 被接受后测试内 FAIL() 触发）。两探针共 3 用例红，证明码面全表断言与域窗边界断言均真实联动实现行为、非恒真；还原备份＋touch → 重建 0 error → 30/30 复绿；`git status` 零残留核验（送验对象未被污染） |
| 4.6 | 架构红线 | ①零 Qt：grep 实证 policy 全部源/头/测试 include 面无 Qt（构建日志 MOC 行为 F-004 既有空转，实证无 Qt 依赖）；②R-1 无业务域互链：policy 库链接面仅 sdurws_ird_core＋RobWork 基线（CMakeLists.txt L50-60，R-5 例外＝proximity 直链为 policy.md §3.2/DTB §4.5 登记的唯一许可，配置期红线守卫 L118-135 断言）；③R-2 跨单元私有头：仅 include 4 个 core 公共头（DiagData/Digest/Evaluation/Identity——均在 core/include/sdurws/ird/core/，ContentIdentity 定义于 Digest.hpp L86，include 均必要）；④R-4 无名称前缀拼接（本类型层不涉名称解析）；⑤PA-1 权威唯一：发布门不重复规则集语义（重复/冲突/循环/对象存在性归解析管线④⑤ POL-T04——PolicySet.hpp L459-461/L737-738 注释与实现一致，码值注册归 diagnostics——Errors.hpp L26 注释）；⑥PA-2：EngineeringPolicySet 全字段 const＋赋值编译期删除（L919-943 私有构造；"修改策略"＝重新解析新身份）；⑦SA-02 零框架改动（见 4.1）；⑧无 old/ 恢复（全新实现）。AGENTS §5 八条逐条过 |
| 4.7 | 注释规范 | 公共头 2 个全查＋实现 Errors.cpp 全查＋测试 PolicySetTest.cpp 全查（实现文件仅 1 个，超额覆盖）：两公共头文件头均含设计依据（章节/需求/契约编号）＋背景说明＋线程安全＋确定性声明；每类/每函数 Doxygen 中文（@param 方向与单位/@return/@throws 逐项）；§2.5 高危信息齐备——物理单位（m/rad/无量纲逐字段，L117"SI rad（不是度）"、L252"0 m 合法"、L295"单位由域固定"）、角度制式（rad 显式 4 处）、所有权（token"静态存储期，调用方无需释放"L119、值语义无共享所有权 L729）、线程约束（逐类"线程安全"声明）、错误语义（PolicyError 异常轨 vs PolicyParseResult.diagnostics 主轨双轨共用码面、D-15 不跨进程、调用方错误 fail-fast——Errors.hpp L28-34）、确定性（switch 全枚举无 default 靠 C4062 告警暴露、%.17g round-trip、"C" locale——L54-55/Errors.cpp L11-12）；魔法数字有来源（4π＝附录 D 第 11 项唯一冻结默认 L113-120；%.17g＝round-trip 精确 L326）；实现细节偏差登记可追溯（PolicyThresholdDomain 域枚举为"实现件非字段数据"的 why 说明 L170-184）。缺项未发现 |
| 4.8 | 文档同步 | units/policy.md §15.4 v0.3 变更记录（+1 行）登记实现细节 ①~⑥，验收者逐点与实现比对一致：①码表 14 值＝§5.2 表 13 码（表序）＋表尾 PolicyObjectInvalid——与 Errors.hpp 枚举序/token() 表序/registryCode() 逐字一致（§9.6 建议码清单 13 项 vs registryCode 全表核对通过；PolicyObjectInvalid 建议码 POLICY-POLICY-OBJECT-INVALID 为"补登建议值，P-PR-6 同模式"登记在案，是否收编归 diagnostics 所有者——未私裁）；②PolicyThresholdDomain 域枚举与 PolicySet.hpp L185-190 一致（私有元数据＋domain() 访问器、不入 §5.3 编码——m_domain 私有且注释"非编码数据"）；③封装式不可变（私有构造＋make()＋全字段 const）与 L212-333/L749-943 一致；④三常量先行落位（归属不变——PolicyInput.hpp 于 POL-T03 落地后复用）与 L88-100 注释一致；⑤发布门语义边界（§4.5 只产 Valid＋§5.2 行 5＋§5.1④ 空域＋域绑定；规则集语义归 POL-T04）与 make() 校验序①~⑦一致；⑥O-11/O-18 SFINAE 钉住＋锚＝代 1——与测试一致。语义零偏差声明属实；§12 POL-T02 行产物清单（Errors.hpp/.cpp、PolicySet.hpp 含 PolicyThreshold/四态承载）与交付一致；契约 status 翻转归收尾（PIPE §4.7⑥）不算偏差。findings.json 未关闭 60 条逐条核对：与本任务执行直接相关项——F-007（全新树配置缺口，本次两步复现并按其口径处置）、F-008（全新树 RobWorkSim 构建失败——本验收目标构建未触及 RobWorkSim 未受阻）、F-009/F-062（gate-all/集成树 worktree 环境前置同族）、F-004（MOC 空转，构建日志再现）、F-071（core/Units.cpp C4099，构建日志再现）、F-019/F-072（ird_gates 既有 5 命中，本次 base 对照再证实）、F-065（留痕门禁段中文双重编码乱码——本次 gate-all.log/gates-current-wp07t02.log 门禁段再现，UTF-8 份 gates-base/正常）、F-068（探针增量编译陷阱——本次头文件注入形态再现，同族转登 F-075）；上述均属治理/构建约定/其他单元辖域，无本任务可顺手消账项，亦无静默积压（ird-test-report.json gateAll 节已如实登记既有命中处置） |
| 4.9 | commit 质量 | 单提交 0c09beb4：标题"[WP-07-T02] 实现 policy 错误与策略类型（PolicySet 四态承载）"——前缀＋动词"实现"开头✓（无 F-012 同型问题）；正文四段齐备（What 逐文件/Why 对应契约＋需求＋O 条款/验证 六项含探针/影响面 兼容性＋对 POL-T03/T04 的消费约束）；验证结论与留痕逐条一致：30/30 与验收者实测一致、"26 用例"指 PolicySetTest.cpp 文件内新增用例（26＋POL-T01 红线扫描 4＝30，ird-test-report.json 同口径，无失实）、gate-all 16/17 与留痕一致、失败能力探针（实施者侧 >1.0→>2.0 mutation）与验收者独立探针（不同 mutation）相互印证；ird_gates 结论"对本次变更零命中"有 base/分支对照证据支持——未发现粉饰 |
| 4.10 | 留痕完整 | 送验留痕 11 份齐：集成/冒烟构建与配置日志×6＋双模式测试运行日志×2＋gtest XML×2（integration/smoke 均 tests=30 failures=0，与验收者亲手复跑一致）＋ird-test-report.json（schema ird-test-report/1，notExecuted:[]、contractVerifyCommand、gateAll 节含逐项命中清单与 base 对照方法、notes 四点如实——O 条款/偏差登记/门禁处置）；ird-test-report.json 即 resultNote 载体已更新；phase-one-validation.log 未更新——沿 TK-T10/TK-T11 先例（foundation 任务以 traceability/builds/<task>/ 证据包替代，该日志止于 da407b6）；留痕文件未被 .gitignore 吞（check-ignore exit 1，F-010 对本任务不适用；UTF-8 份正常，仅 F-065 既有乱码问题在两份门禁日志段内延续） |
| 4.11 | 偷懒/缩水实现扫描 | ①TODO/FIXME/stub/placeholder/未实现/not implemented 扫描 diff 范围零命中（grep 退出码 1）；②无空实现/仅返回默认值——token()/registryCode() 编译期全枚举 switch 实表（Errors.cpp L25-44/L51-68）、PolicyThreshold::make 四域窗校验实逻辑、发布门校验序①~⑦实逻辑（PolicySet.hpp L836-902）、detail 双谓词实体（L952-990）；契约无"触发式延后"标注故无豁免诉求；③acceptance 逐条证据表见 4.4（四条均有具名测试＋行号，另两条登记类条款 O-11/O-18 有 SFINAE 编译期钉住——"字段不存在"以 static_assert 强度承载）；④测试强度：断言校验行为与边界（域窗三边界值、错误码逐项、四态 nullopt 透传、同值异域不等、拷贝相等/异身份不等）＋4.5 双探针实证会红——非恒真、非"只编译通过" |

## 问题清单

- 阻断级：无。
- 建议级（已随本记录转登 traceability/findings.json）：
  - **F-075**：ACC §4.5 探针头文件注入陷阱（F-068 同族第二形态）——对 header-only 契约头（如 PolicySet.hpp）注入 mutation 后即使 touch 该头，MSBuild 增量亦未重编消费 TU（PolicySetTest.cpp），断言未即时变红；须再 touch 消费 TU（测试源）方触发。建议 ACC/PIPE 验收者模板补充操作纪律："探针注入头文件后，须 touch 全部受影响消费 TU 再重建"，避免后续验收者误判"测试恒真"。
- 既有 open 项延续（不新增编号，本任务留痕再现）：F-065（gate-all.log/gates-current-wp07t02.log 门禁段中文双重编码乱码）；F-007/F-008/F-009/F-062（worktree/全新树环境前置，本次按已登记口径处置）；F-004/F-071/F-019/F-072（构建日志与门禁命中再现，均非本任务引入）。

## 验收者环境说明

- 复现现场：`git worktree add --detach C:/Users/zgl18/AppData/Local/Temp/ird-acc-POLT02-wtA 0c09beb4`（detached @ 冻结 SHA，非分支名）；集成树全新配置于 wtA/build；冒烟树独立于 Temp/ird-acc-POLT02-smoke。
- 全部命令（配置/构建/测试/verify/双探针）由验收者本会话亲手执行；探针注入与还原均经 `git status` 零残留＋30/30 复绿双重核验（防旧二进制假绿）。
- 环境缺口处置（F-007 既有，两步）：①配置命令补 `-DCMAKE_TOOLCHAIN_FILE=vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=Qt/msvc2022_64`（自主树 CMakeCache 读取）；②复制主树 git 未跟踪的 RobWorkStudio.ini.template.static 至 wtA——未改动任何被跟踪产物。
- 送验对象冻结复核：验收全程 origin/wp07-t02 == 0c09beb4；验收者未向任务分支推送任何提交；pipeline/state.json 与 lock 未读写。
- 本记录与 findings.json（F-075 转登）落 evidence 分支 `acc/POL-T02/1` 并推送，commit SHA 见验收请求回执三元组。
