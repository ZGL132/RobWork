# POL-T11 验收记录（attempt 2）

| 字段 | 值 |
| --- | --- |
| 任务 | POL-T11 实现 policy 替身与契约套件（unit=policy，masterWp=WP-07-T11；分支 wp07-t11） |
| 送验对象（冻结） | `ba0cb7d5c4b0cbba340eec962e253eb804d748aa`（40 位；`origin/wp07-t11` 经 rev-parse 核对＝该 SHA——无分支漂移，ACC §3） |
| diff 范围 | `12097195531acfc5665af1e2c9f638977c86d3ce..ba0cb7d5c4b0cbba340eec962e253eb804d748aa`（state.base＝origin/redesign-main 尖端；4 个实现/登记/留痕提交：a595026f→5a2fd64a→1499a5a6→ba0cb7d5） |
| 验收协议 | acceptance-protocol.md v1.5（ACC），§4 全部 11 项逐项出证据；全新上下文，未消费实施者任何过程叙述（§2 四条独立性逐条满足：只读代码/契约/设计文档/在库留痕；一切构建、测试、门禁、探针由验收者亲手执行） |
| 裁决 | **pass**（4.1~4.11 全部通过；无阻断级问题；建议级 4 组随本记录分支转登 `traceability/findings.json` F-090/F-091/F-092/F-093，不阻塞合入） |
| 验收环境 | detached worktree `D:\10_Source_Repos\21_robot\acc-worktrees\pol-t11-a` @ ba0cb7d5（`git worktree add --detach`）。集成模式全新配置：`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=<仓库根>/vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`（F-062/F-007 登记的 worktree 复现前置——toolchain/Qt 前缀＋两件 RobWorkStudio.ini 本地资源从主树复制至 worktree `RobWork/RobWorkStudio/bin/`），配置 exit 0（64.4 s）、缓存 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`（grep 实证）；冒烟另配 `D:\tmp-acc-pol-t11\build-smoke`（带 vcpkg toolchain，AGENTS §4.1）。验收者日志目录 `D:\tmp-acc-pol-t11\`（configure-integration.log／verify-target-build.log／contract-target-build.log／integration-fullbuild.log＋.exit／smoke-configure.log／smoke-build.log／smoke-*.log 七目标／acc-integration-run.log／acc-contract-run.log／acc-gates-run.log／acc-lint-run.log／acc-validate-task.log／acc-ctest-{integration,smoke}.log／probe1-*／probe2-*） |
| 记录分支 | `acc/POL-T11/2`（attempt=2，验收请求下发序号；同日无既有 POL-T11-20260914.md——attempt 1 未产出记录，文件名不加 -r2） |

---

## 4.1 改动范围 — PASS

`git diff --name-status 12097195..ba0cb7d5` 共 25 个变更文件，逐一对照契约 `allowedFiles`（`policy/**`、`industrialrobot/CMakeLists.txt`、`units/policy.md`、`traceability/**`、`testdata/**`）：

| 变更组 | 文件数 | 归属 |
| --- | --- | --- |
| `policy/CMakeLists.txt`（M：注册 `sdurws_ird_policy_contract_test` 目标） | 1 | `policy/**` ✓ |
| `policy/test/*.cpp` 五个契约套件文件＋`policy/test/README.md`（A） | 6 | `policy/**` ✓ |
| `policy/test/sdurws/ird/policy/testdouble/PolicyTestDoubles.hpp`（A，header-only 替身） | 1 | `policy/**` ✓ |
| `testdata/golden/pol-policy-codec-fixture/1.0.0/`（manifest/inputs×2/expected/generate）＋`testdata/golden/pol-collision-analytic/1.0.0/`（同构四件） | 8 | `testdata/**` ✓（allowedFiles 含 testdata 为本任务数据集交付面——契约 note「allowedFiles 补 testdata/**（数据集交付面）」） |
| `testdata/tolerance/pol-codec/v1.0.0.json`＋`testdata/tolerance/pol-analytic/v1.0.0.json` | 2 | `testdata/**` ✓ |
| `traceability/builds/wp07-t11/`（4 份 gtest XML＋ird-test-report.json） | 5 | `traceability/**` ✓ |
| `units/policy.md`（M：§11 头段实现状态更新＋§15.4 v0.12 行，+2/−1） | 1 | allowedFiles 显式列出 ✓ |
| `industrialrobot/CMakeLists.txt` | 0（允许而未触碰，不构成越界） | — |

- `forbiddenFiles`（REQUIREMENTS.md / ARCHITECTURE.md）：diff 中零触碰 ✓。
- 框架源码（RobWork/、RobWorkSim/、RobWorkStudio 其余）：diff 范围零文件；`industrialrobot/patches/` diff 为空（无新登记，SA-02 无涉）✓。
- **产品 src/ 零改动**（`policy/src/` 不在 diff）——本任务交付面为测试设施＋契约套件＋数据集，与 §12 卡产物行（`policy/test/*`、`testdata/golden/pol-*`）一致 ✓。

## 4.2 可复现构建 — PASS

- **集成模式（worktree A 全新树）**：重新配置 exit 0（64.4 s），`grep RWS_BUILD_INDUSTRIALROBOT build/CMakeCache.txt` → `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON` 实证（日志 `configure-integration.log`）。
  - 契约 verify 目标 `cmake --build build --config Release --target sdurws_ird_policy_test` **从零编译 exit 0**、日志零 `: error` 行，产物 `build/RobWorkStudio/bin/Release/sdurws_ird_policy_test.exe` 落位（日志 `verify-target-build.log`）；`--target sdurws_ird_policy_contract_test` 随后从零构建 exit 0、零 error（`contract-target-build.log`）。
  - 全树 `cmake --build build --config Release` exit 1＝**73 处 error 全部位于框架侧插件**（RWSimulatorPlugin.vcxproj 71＋rwplugin 2；出错头聚集 VelocityScrew6D.hpp／Math.hpp——与 findings **F-008** 登记的框架基线 min/max 宏污染簇同分布；`grep ": error" | grep -ci "ird\|industrialrobot"`＝**0**，ird 面零错误）。按 F-036/F-054/F-055 家族既定处置口径以任务目标面从零构建补强（日志 `integration-fullbuild.log`）。
- **冒烟模式**：`cmake -S .../industrialrobot -B D:/tmp-acc-pol-t11/build-smoke -DCMAKE_TOOLCHAIN_FILE=<vcpkg>` 配置 exit 0＋全量 `cmake --build ... --config Release` exit 0、零 `: error`，9 个 exe 落位（含新 `sdurws_ird_policy_contract_test.exe`）（日志 `smoke-configure.log`／`smoke-build.log`）。
- **测试（验收者亲手运行）**：
  - 集成 `sdurws_ird_policy_test`：**200/200 PASSED**（`[==========] 200 tests from 38 test suites ran`）；`sdurws_ird_policy_contract_test`：**23/23 PASSED**（23 tests from 5 suites——PolicyContractDoubles 7＋PolicyProofMaterialContract 4＋PolCollisionAnalytic 4＋PolPolicyCodecFixture 5＋PolicyTestDoubleBoundary 3）。探针还原后复跑仍 200/200＋23/23。
  - 冒烟全量七目标直跑：core 61＋evidence 205＋evidence_contract 14＋**policy 131**＋**policy_contract 8**＋runtime 176＋testkit 97＝**692/692 零失败、全部 exit 0**。
  - ctest（从 `policy/` 子目录——build 根无 CTestTestfile.cmake 系 enable_testing 子目录调用的既有布局，非本任务引入）：双模式 policy 叶 **2/2 全 PASS**（`acc-ctest-integration.log`／`acc-ctest-smoke.log`）——与送验「ctest 双模式 policy 叶 2/2」一致。
- **门禁 `ird_gates`**（验收者亲手跑 `cmake --build build --config Release --target ird_gates`）：exit 1＝**7 处命中**——SUB×5（evidence_test／evidence_contract_test／policy_test／**policy_contract_test**／runtime_test → testkit）＋SUB×1（testdata_lint→testkit 表外边）＋T1×1（testdata_lint 产品目标链接 testkit），即 **POL-T10 基线 6＋本任务新增 1（policy_contract_test 直链 testkit）**；IRD-GATE-R4 零触发；引擎自测 fail_r1/t1/r5/sub/r3/r4/t2 按预期检出、pass_clean/pass_r4comment 干净通过（日志 `acc-gates-run.log`）——与送验「7 命中＝base 6＋新增 1、如实登记不私改门禁」一致；「无 policy_contract_test→evidence 边命中」亦实证（7 项命中清单无该边）。
- **数据集 lint**（`sdurws_ird_testdata_lint --root <testdata> --dict <requirements-ids.json>`——F-016 登记的显式传参口径）：**5 个数据集版本 0 违规**，含本任务 `pol-collision-analytic/1.0.0`＋`pol-policy-codec-fixture/1.0.0`（日志 `acc-lint-run.log`）。

## 4.3 契约 verify 命令 — PASS

契约 `verify` 数组仅 1 条：`cmake --build build --config Release --target sdurws_ird_policy_test`——验收者在 worktree A 全新树亲手执行，exit 0（见 4.2）。无其他条目漏执行。

## 4.4 acceptance 条目逐项核对 — PASS（3/3 有证据）

| # | 契约条目 | 证据（具名测试/命令输出） |
| --- | --- | --- |
| 1 | §11 验证矩阵逐条通过并留痕（AT-01/AT-19/AT-37 观测点载体） | **矩阵→文件映射全表**：`policy/test/README.md` §3（15 行映射，POL-ID~POL-TD-1 逐行落位到目标＋文件，含交付任务号）。**AT-01 载体**：`At01TravelLimitConfirmableReleaseChain`（PolicyContractDoublesTest.cpp:801，IRD_TEST_INFO("MDL-06",{"AT-01"})）——§10.4 确认放行全链（6π/4π/rad 比较型三要素→ConfirmableFinding 四元组→未确认阻止→确认放行摘要含 policyContentId→6π 多圈反例新身份复检放行）。**AT-19 载体**：`At19ThreeEntriesIdenticalIdPairsVerdictsAndReasonCodes`（:594）＋`ThreeEntriesShareSingleEvaluatorInstanceAndEquivalentOutputs`（:527，POL-SHARE-1 实例地址全等＋checkSetEquivalent）＋`AllCasesMatchHandDerivedExpectations`（CollisionAnalyticDatasetTest.cpp:368）。**AT-37 载体**：`At37InvertedMountConsumesCompiledStateWithoutSecondRotation`（:462，IRD_TEST_INFO("MDL-22",{"AT-37"})）＋`PolicySnapshotConsumedThroughPolicyObject`（:333）。**既有矩阵行**（POL-ID/POL-PARSE/POL-SCOPE/POL-EVAL/POL-JNT/POL-COMPAT/POL-TD-1 前半）＝既有 6 文件回归（集成 200/200、冒烟 131/131 全绿，与 POL-T03~T10 逐任务交付留痕一致）。全库执行面：集成 223/223＋冒烟 139/139（=报告 regression 节数值，实测逐位一致）；留痕 4 份 XML 在库（wp07-t11/）。**ird_gates 如实登记 7 命中非「零命中」**（未粉饰） |
| 2 | POL-TD-1 边界声明（替身不冒充碰撞算法验证） | **文书面**：`policy/test/README.md` §1 声明全文（替身四组逐一列举＋「不构成任何碰撞算法正确性证明」核心命题＋真实正确性两条独立通道指认——POL-T07 解析算例＋pol-collision-analytic 数据集）＋§2 替身清单表＋线程/确定性约束。**机检面**：`PolicyTestDoubleBoundaryTest` 3 用例双模式全绿——产品源码六替身符号零命中扫描（ProductSourcesContainNoDoubleSymbols）＋替身头声明在案＋README 声明在案；**本验收探针 1 实证机检真实即红**（见 4.5）。**行为面**：`ScriptedEvaluatorProducesAllFourScriptTracks`（真实会话对脚本输入的处置断言）。**替身头文件头**内嵌边界声明全文＋「设计取舍」说明（脚本注入点在后端查询面而非第二套评估逻辑——ARC-05 不做第二算法） |
| 3 | 数据集交付：testdata/golden/pol-*（contract-fixture 类＋analytic-case 类，按 testkit manifest schema 登记）——lint 通过并随 CI 跑通 | **lint**：验收者亲手跑 testdata_lint 显式 `--root/--dict`（F-016 口径）→ 5 数据集版本 0 违规，两新数据集在列。**schema 登记**：两 manifest 均为 `ird-golden-manifest/1`（pol-collision-analytic＝analytic-case＋referenceSource.hand-built＋independentOfProductionImpl=true＋producer.solverConfig 策略快照〔§4.3.2——4π 经 policy 对象 origin=DefaultAppendixD 消费断言，不旁路私设〕；pol-policy-codec-fixture＝contract-fixture＋双变体 inputs＋expected/identity.json 由 `make_pol_policy_fixture.mjs` 独立重实现产出）。**消费测试**：`PolPolicyCodecFixture` 5 用例＋`PolCollisionAnalytic` 4 用例全绿（GoldenDataset 全链装载/SHA-256 完整性重算/编码字节双实现互证/身份独立摘要对照/变体无关性/六工况手算对照/倒挂判别）。**随 CI 跑通**：ctest 双模式 policy 叶 2/2（验收者复跑）＋codec-fixture 消费用例为双模式编译（冒烟 8 用例内含） |

无任何条目虚标通过；ird_gates 结论如实登记为「7 命中＝base 6＋新增 1」（非零命中粉饰为通过），并登记「处置转 WP-01 所有者、不私改门禁」（F-072 同族第四数据点，与既有归口一致，不重复立案）。

## 4.5 测试真实失败能力 — PASS（双探针，F-075/F-068 纪律：mutation 落产品 .cpp、git checkout 还原（新 mtime）后重建复绿）

1. **探针 1（POL-TD-1 机检）**：向产品源码 `policy/src/Policy.cpp` 追加含替身符号 `testdouble` 的注释行 → 重建 contract_test 后 `PolicyTestDoubleBoundary.ProductSourcesContainNoDoubleSymbols` **FAILED**（同套件 DoubleHeaderCarriesBoundaryDeclaration/TestReadmeDeclaresBoundaryFullText 仍绿——探针精确落点，扫描确实逐文件逐符号在查）。`git checkout` 还原＋重建复跑 → 3/3 复绿。
2. **探针 2（双实现互证）**：对产品编码器 `policy/src/PolicyInput.cpp` buildFrame 注入布局漂移（`w.u32(schemaVersion)` → `w.u32(schemaVersion + 1)`）→ 重建后 `PolPolicyCodecFixture` **3 用例 FAILED**：`CanonicalEncodingMatchesIndependentExpectation`（产品编码 vs JS 独立参考逐字节对照捕获）＋`ContentIdentityMatchesIndependentDigest`（投影/摘要链捕获）＋`ShuffledAndDisplayUnitVariantKeepsIdentity`——恰为消费编码布局的三个用例（manifest 装载/完整性 2 用例不受影响，探针范围精确）。`git checkout` 还原＋重建 → contract 23/23＋policy 200/200 复绿。

两次探针后 `git status --porcelain` 均零残留（探针仅存在于临时 worktree 未提交工作区，送验冻结对象 ba0cb7d5 在 git 对象库中全程未被修改）。断言非恒真、非「只编译通过」。

## 4.6 架构红线（AGENTS §5 八条） — PASS

1. **L2 零 Qt**：本任务 diff 范围 7 个代码文件 grep Qt include 零命中（唯一 `#include <Q` 字样命中在既有 BuildRedLineTest.cpp:87——红线扫描器自身的匹配串，非本任务文件、非真实 Qt 依赖）；产品库链接面零改动，CMake 配置期断言（policy 仅链 core＋RobWork 基线，Qt/GTest 链接即 FATAL）在两模式配置中通过 ✓。
2. **R-1 业务域互链**：新文件 sdurws/ird include 面仅 `ird/core/*`、`ird/policy/*`（本单元）、`ird/testkit/*`（测试设施，仅测试目标链接——产品库零 testkit 依赖）＋集成专属 TU 的 rw 头；`PolicyProofMaterialContractTest` 对 evidence **公共头头级消费**（`../evidence/include` 仅集成 gating 内加入 include 根；零链接边——ird_gates 7 项命中无 policy_contract_test→evidence 边，实测）✓。
3. **R-2 跨单元私有头**：跨单元 include 仅 evidence 公共头（`include/sdurws/ird/evidence/`）＝R-2 允许形态；替身头落位本单元测试面 `policy/test/sdurws/ird/policy/testdouble/`，未跨单元暴露 ✓。
4. **SA-02 框架零修改**：4.1 已证（patches/ 无新文件）✓。
5. **R-4 名称前缀拼接/剥离**：diff 范围文件双引号对携带 "RobWork" 子串的字面量 grep 零命中；ird_gates IRD-GATE-R4 实测零触发 ✓。
6. **权威唯一（PA-1）**：本任务零产品源码改动，无语义新增面；替身不冒充策略权威（StubPolicyProvider 不做记忆化——§9.1 记忆化验证归 PolicyPortTest 的真实 Provider，替身头明文登记）；「policy 禁止二次旋转」MDL-22 消费口径经装置与数据集两侧对齐（基座帧安装变换单一注入）✓。
7. **不可变历史（PA-2）**：本任务无修订写路径涉足；数据集 expected/manifest 以 integrity SHA-256 冻结（IntegrityDigestsMatchCommittedFiles 全绿）✓。
8. **不恢复 old/**：diff 范围文件无 old/ 引用（grep 命中均为 "Threshold/" 内字节子串 "hold/"，与 POL-T10 验收同款误报）✓。

## 4.7 注释规范（AGENTS §2） — PASS

- **新头全查**（PolicyTestDoubles.hpp，537 行，全读）：文件头 Doxygen 齐备——设计依据（§11/§9.1/§9.3/§3.3/§9.7/契约 acceptance～3＋runtime/evidence 先例）、POL-TD-1 边界声明摘录、「为什么在真实会话上脚本化」的设计取舍说明（注入点选在后端查询面、状态机走产品代码的理由）、与 POL-T07 TU 内同型类的关系划界、线程约束（单线程设施）与确定性（构造序回放、无时钟无随机源）。每类/每脚本成员中文注释逐一带语义（FIFO 耗尽语义、故障注入语义、取消阈值与样本边界的对应关系、m 单位、所有权与生命周期——脚本后端 shared_ptr 保活口径、`@param/@return/@throws` 方向与失败行为）✓。
- **测试/支撑文件抽查（超 ≥3 要求，实读 5 个）**：五个测试文件＋CMakeLists.txt 全部有文件头（设计依据＋模式约束声明——双模式 vs 集成专属 gating 同因说明）；每个 TEST 上方有引用块注明验证条款（§11 行号/POL-\*/需求 ID/AT 观测点）；断言处行内中文说明（EXPECT_\* << 消息）；IRD_TEST_INFO 需求/AT 追溯字段逐用例在位；CMakeLists.txt 关键决策全部中文注释（gating 判定依据、链接面 T-1 合规说明、门禁新增命中的如实登记与归口、evidence include 根的 R-2/零链接边论证）✓。
- **§2.5 高危信息**：单位（距离脚本 SI m、4π/6π rad 字面量带单位注释）、坐标系（倒挂安装 R_world_base 编译产物单一权威、环境盒世界系固连）、确定性来源（FIFO 脚本按构造序、规范序稳定排序）、所有权（shared_ptr 持有口径逐一注明）、线程约束（替身单线程声明）、错误语义（PortAssemblyIncomplete 同码面镜像、脚本抛出轨不重复真实校验逻辑）逐项在位 ✓。
- 两处微观瑕疵见问题清单 G-4（guard 名残段＋一处冗余赋值——不缺必填项，不影响本项判定）。

## 4.8 文档同步＋findings 核对 — PASS

- 实现与任务卡**零语义偏差**；实现增量（替身四组、POL-TD-1 两面、contract_test 目标注册、pol-\* 数据集与容差档案、§11 矩阵收尾逐行、evidence 头级消费依赖面声明、门禁 7 命中登记）已按 DTB §5.4 以 **units/policy.md §15.4 v0.12（2026-09-14）＋§11 头段实现状态更新** 登记（diff 实证 +2/−1 行）——「代码先行、文档失实」不存在。
- 卡行对照：§12 POL-T11 行（policy.md:1124，产物 `policy/test/*`＋`testdata/golden/pol-*`、验证「§11 矩阵逐条」、完成条件「全部用例通过并留痕；替身边界声明在案」）与交付一致；DTB §2 WP-07-T11 行（L270）与 DTB §4.5 R-5 登记行（L660，`_test/_contract_test 经 testkit/policy 替身消费`——本任务 testkit 链接的登记出处）在案；§12 状态翻转与 §15.3 未决项更新按卡面归口 **POL-T12**（policy.md:1125＋DTB L271，依赖 POL-T01~T11）——非本任务失同步。
- `validate-task.ps1 -TaskFile POL-T11.json` 验收者亲手复跑（worktree A）：**PASS (1 tasks)** exit 0；`phase-one-validation.log:37` 含 `POL-T11.json: validate-task: PASS (1 tasks)` 在库条目 ✓。
- **findings.json 未关闭条目相关性核对**（v1.2 要求，基于 12097195 版本 89 项/75 open）：
  - **F-075/F-068（探针纪律家族）**：本验收双探针均落产品 .cpp、git checkout 还原（新 mtime）＋重建复绿＋零残留核验，按其处置执行；
  - F-076/F-079/F-082/F-084/F-086（F-012 标题家族）：**本任务复踩 2 例**（1499a5a6 [units] 名词短语＋ba0cb7d5「验证留痕与…」并列名词开头——见 G-3→F-092）；
  - F-025/F-041/F-073/F-089（计数失实家族）：**本任务复踩**——套级用例数四处失实、总数全对（见 G-2→F-091）；
  - F-010/F-077/F-080/F-081/F-083/F-085/F-087/F-088（留痕 .log 被 .gitignore 吞掉家族）：**本任务复踩且为「正文逐件列举」形态**（见 G-1→F-090）；
  - F-019/F-051/F-060/F-072（ird_gates testkit 边家族）：本验收实测 7 命中与四者登记及 POL-T10 基线逐码吻合，新增 policy_contract_test→testkit 为同根源第 4 处数据点——实施者已如实登记并转 WP-01 归口，维持原归口不重复立案；
  - F-016（lint 缺省字典路径）：本验收按其登记口径显式 `--dict` 传参通过——lint 端到端实测有效，维持原归口（工具缺省路径修复仍在 TK-T04/T05 辖区）；
  - F-065（留痕日志 GBK 双重编码）：本任务 gates 留痕同现象且提交正文已如实登记（F-065 同源）——维持原归口；
  - F-004/F-007/F-008/F-009/F-062（构建环境/框架基线家族）：本验收环境前置按 F-062/F-007 口径执行、全树构建实测止于 F-008（73 error 同分布）——均维持原归口；
  - F-078（policy.md 转义损毁第四实例）：本任务 diff 仅涉 §11 头段/§15.4，未触碰损毁两节（损毁文本原样在库，维持 DOC 治理归口 POL-T12）。
  - **无历史发现被静默积压；本任务可顺手消账项：无。**

## 4.9 commit 质量 — PASS

范围 4 提交逐份核查：

- 标题：`a595026f [WP-07-T11] 交付 pol-* 黄金数据集…`／`5a2fd64a [WP-07-T11] 实现 policy 替身与契约套件…` 动词开头合规；`1499a5a6 [units] policy v0.12：POL-T11 实现落位登记…`（前缀后名词短语开头，F-076/F-079 同型）与 `ba0cb7d5 [WP-07-T11] 验证留痕与编译放行登记…`（并列名词短语开头，F-084/F-086 同字面形态）——**2 例不合规，建议级**（G-3→F-092）。
- 正文四段（改动内容／改动原因／验证／影响面与兼容性）齐备且粒度充分；`Acc-Covered` 逐提交如实（数据集提交显式注明「lint 与 CI 消费见第 2/4 提交证据」——前向引用已披露）。
- 验证结论与留痕核对：集成 223/223（200＋23）、冒烟 139/139（131＋8）、ird_gates 7 命中＝base 6＋新增 1（含根因分析与转裁决处置）、lint 5/0、ctest 2/2 双模式、validate-task PASS、契约 verify 目标 exit 0——**全部经验收者亲手复现准确，无粉饰**；双探针实证断言有效。
- 散文计数失实四处（不属粉饰：总数全对、XML 权威面正确、逐用例均真实存在且通过，方向为套内归并口径错位）见 G-2，建议级转登（F-073/F-089 同判级先例）。

## 4.10 留痕完整 — PASS（附建议级 G-1）

- 在库证据本体：`traceability/builds/wp07-t11/` 双模式×双目标 gtest XML 4 份（integration 200/0＋23/0、smoke 131/0＋8/0——**顶格实测，与验收者复跑逐位一致**；库内 integration XML 的 PolPolicyCodecFixture tests="5" 亦与源码 5 TEST 一致）＋ird-test-report.json（targets/testSuites 逐用例 traces/datasets/regression/gates/verifyCommand/P-POL-4 处置 notes 全节）。
- resultNote 口径：ird-test-report.json 即本任务执行级结果登记（ACC 4.10「phase-one-validation.log 或 resultNote 已更新」满足）；validate-task 面由库内 `phase-one-validation.log:37` 在库条目覆盖（验收者复跑 PASS 一致）。
- **G-1（建议级）**：ba0cb7d5 正文声称 `traceability/builds/wp07-t11/` 下登记**全套留痕**并逐件列举 11 份 .log（smoke-configure/smoke-build/verify-target-build/integration-test-run/smoke-test-run/ctest-integration/ctest-smoke/gate-all/lint-run/gates-current-wp07t11/validate-task），`git ls-tree ba0cb7d5` 实证该目录**仅 5 件入库**（4 XML＋report），11 份 .log 全部未入库（.gitignore `*.log` 规则，F-010 根因**第七次踩中**——F-081 第二次、F-083 第三次、F-085 第四次、F-087 第五次、F-088 第六次之后）；ird-test-report.json 的 datasets.ci（引用 ctest-*.log/gate-all.log）与 gates.logs（引用 gates-current-wp07t11.log）、validateTask（引用 validate-task.log）、datasets.lint（引用 lint-run.log）同型悬空引用。判级沿 F-081/F-083/F-085/F-087/F-088 先例：证据本体（4 XML＋report）完整且全部数值主张经验收者亲手复现准确，建议级不阻断→转登 **F-090**（并按 F-088 resolution 建议与 F-087/F-088 合并同一根因处置单，不再逐任务重复立案）。
- 说明：gate-all.ps1 未由验收者复跑——F-004/F-009 登记其非主树无法全量运行；其 ird_gates 门禁面已由验收者在 worktree A 亲手执行覆盖（4.2/4.4），其 ctest/测试发现段结论（双模式各 7 注册全 PASS、policy 叶 2/2）与验收者实测一致。

## 4.11 偷懒/缩水实现扫描 — PASS

- ①**关键词扫描**：diff 范围 7 个代码文件（5 测试 .cpp＋替身 .hpp＋CMakeLists.txt）`TODO|FIXME|placeholder|未实现|not implemented` 零命中；`stub` 命中 30 处全部为 **StubPolicyProvider／stub 局部变量／test.stub-policy-provider 描述符**——即契约标题与 §11 明文交付的替身本体（「policy 替身与契约套件」），属契约显式交付面豁免，逐处可指认，无未登记豁免滥用。
- ②**空实现检测**：产品接口函数体零改动（产品 src 不在 diff）；替身方法体为「脚本应答/注入槽」的显式设计（README §2 逐类登记脚本面与用途），非未实现占位；CMake 无空目标。
- ③**acceptance 逐条证据表**：见 4.4 表格——三条均有具名测试/命令输出/文件行号，任一条不缺。
- ④**测试强度**：23 新用例断言校验行为与边界（字节级编码对照、SHA-256 重算对照、身份等值与变体无关性、四轨状态机、稳定排序、取消边界、闭区间放行、倒挂判别、六工况手算对照、源码级字段锚、符号扫描机检），非恒真、非「只编译通过」；与 4.5 联动双探针实证改实现测试即红。

---

## 问题清单（建议级，无阻断级）

| # | 级别 | 问题 | 处置 |
| --- | --- | --- | --- |
| G-1 | 建议 | F-010 根因第七次踩中：ba0cb7d5 正文逐件列举 11 份 .log 留痕全部未入库（git ls-tree 实证仅 5 件）；ird-test-report.json 四处字段悬空引用未入库 .log。证据本体（4 XML＋report）完整、数值主张全部复现准确 | 转登 **F-090**；按 F-088 resolution 建议与 F-087/F-088 合并同一根因处置单（.gitignore 负向豁免＋补提交／XML＋报告承载口径显式标注／留痕对账自查强制化，三选一归 WP-01/所有者裁决） |
| G-2 | 建议 | F-025/F-041/F-073/F-089 计数失实家族第五例：套级用例数四处失实——5a2fd64a 正文与 ird-test-report.json（targets[2].notes＋testSuites 两个 suite 标签）称 PolicyCodecFixtureContractTest「4 用例」（实测 5）、PolicyContractDoublesTest「5 用例」（实测 7）、smoke note「双模式文件 7 用例」（实测 8）、a595026f 正文称 PolPolicyCodecFixture「4/4」（实测 5）；policy.md v0.12 行同型（4＋5）。总数（23/8/200/131/223/139）全对，库内 XML 与 report 逐用例枚举无错——套内归并口径错位非虚报 | 转登 **F-091**；随 policy.md 下次增量修订（POL-T12）修正 v0.12 行套级分布；ird-test-report.json/提交正文已入库历史不追溯改写（留痕不可变）；模板补救与 F-089 同源（「套件数/套内分布以 gtest 输出 tests from N test suites 为准」自查项） |
| G-3 | 建议 | F-012 标题家族再例：1499a5a6（[units] 前缀后名词短语开头，F-076/F-079 同型）＋ba0cb7d5（「验证留痕与编译放行登记」并列名词开头，F-084/F-086 同字面形态）；另 2 提交动词开头合规 | 转登 **F-092**；并入 F-012 家族统一模板补救（覆盖 [units]/留痕各前缀形态，留痕类可用「登记…留痕」句式）；已推送历史不追溯改写 |
| G-4 | 建议（微观） | 两处代码卫生瑕疵（不缺 §2 必填注释项）：①PolicyTestDoubles.hpp:56 include guard 名 `SDURWS_IRD_POLICY_TESTDOUBLE_POLICYSERVICETESTDOUBLES_HPP` 含 "POLICYSERVICE" 残段（与文件名 PolicyTestDoubles 不一致——疑似早期命名残留）；②PolicyTestDoubles.hpp:419-421 createSession 对 `backend->throwInCollision` 先赋 `throwInCollision` 再下一行覆盖为 `throwInCollision || throwInDistance`——首行赋值为死存储（两行语义注释本身正确） | 转登 **F-093**；随 policy 单元下次触及该头的任务顺手修正（guard 重命名无 API 影响——header-only 测试面；死存储删除零行为变化），或随 POL-T12 文档同步任务一并处理；不阻塞合入 |

## 裁决

**pass**——4.1~4.11 全部通过，无阻断级问题；建议级 G-1~G-4 已全部转登 `traceability/findings.json`（F-090/F-091/F-092/F-093，随本记录分支提交）。送验对象 `ba0cb7d5c4b0cbba340eec962e253eb804d748aa` 冻结有效（origin/wp07-t11 无漂移）；本验收未合入、未修改实施产物、未向任务分支推送任何提交。

验收者：POL-T11 验收会话（attempt 2，全新上下文）
日期：2026-09-14
