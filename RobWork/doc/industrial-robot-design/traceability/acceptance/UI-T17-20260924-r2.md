# UI-T17 验收记录（attempt 2，2026-09-24）

| 字段 | 值 |
| --- | --- |
| 任务 | UI-T17 会话入口处理器补全＋宿主菜单融合（O-43 裁决承接；契约 tasks/foundation/UI-T17.json） |
| 送验对象（冻结） | `3a9c96ebd917ce6218e9ee1adf2a03c305d9d02a`（分支 wp10-t17；验收全程按 SHA——分支漂移检测：本地 wp10-t17 与 origin/wp10-t17 均与之逐位一致，通过） |
| diff 范围 | `ac5d107665d8d080b6f88a176ab7f5060acefbe6..3a9c96eb`（5 提交：c2c1e0a4 实现／656b7f30 merge base／83aa31a6 实施收尾 r2／e917734a 返工 r3〔F-326 注释修复＋F-327 延期＋r3 全量重验〕／3a9c96eb 冒烟取证 r4〔所有者人工执行〕；71 变更文件＝ui 产品面 7＋units/ui.md 1＋traceability 63） |
| 复现现场 | 全新 detached worktree @3a9c96eb（`git worktree add --detach`，仓库外目录）；集成模式全新 configure（显式 `-DRWS_BUILD_INDUSTRIALROBOT=ON`，grep 确认 `:BOOL=ON`）；独立冒烟模式另配全新树（vcpkg toolchain＋Qt 前缀） |
| 验证脚本 | validate-task PASS（验收者亲跑两次，worktree 内，留痕 acc-attempt2/acc2-validate-task.log）；ird_gates 验收者独立复跑 head↔base 同引擎归一化比对 |
| attempt 1 输入 | 不可变记录 `traceability/acceptance/UI-T17-20260924.md` @ acc/UI-T17/1（evidence c216d6c8）——verdict=fail，阻断唯一项 B-1；返工输入六条 |
| **verdict** | **pass（4.1～4.11 全过；阻断级问题零项；B-1 按 §2 专章裁定为证据充分——机制级在库实证＋所有者情节级确证；建议级新发现 F-328/F-329/F-330 随本记录转登 findings.json，不阻塞合入）** |

---

## 0. 环境说明（复现前提，影响后续验收者）

- **F-323 两个子项 100% 复现**：①configure 前须把主工作树 `RobWork/RobWorkStudio/bin/RobWorkStudio.ini.template.static` 手工补置到 worktree 同路径（attempt 1 记录所记"RobWork/RobWorkStudio.ini.template.static"路径表述欠准——实际在 `bin/` 子目录，本验收以 find 定位）；②RobWorkSim NOMINMAX 宏冲突须以 `CL="-DNOMINMAX"` 环境变量全量构建（Git Bash 下 `/D` 形式会被 MSYS 路径转换破坏）。
- **集成模式依赖口径（对 attempt 1 记录的补充）**：主工作树 `build/` 的 CMakeCache 实测带 `CMAKE_TOOLCHAIN_FILE=<repo>/vcpkg/scripts/buildsystems/vcpkg.cmake` 与 `CMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`——全新树集成 configure 若不带 toolchain 则 Boost 找不到（vcpkg 经典模式提供）。本验收按此口径重配成功（`RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`）。
- **集成构建两遍实录（如实登记）**：首遍全量构建在 `sdurws_ird_policy_test` **构建后步骤**（vcpkg z-applocal＋DLL 拷贝）出现 1 次失败——根因行 `open_for_read("...bin/Release\sdurw_proximity.dll"): permission denied`（并行构建下运行时 DLL 被并发拷贝步骤占用的瞬时竞争；policy 单元**不在本任务 diff 范围**，且失败步骤为构建后拷贝而非编译/链接）。增量二遍零错误、exit=0。判定＝环境性抖动，非送验对象缺陷；两遍日志均入库（acc2-integration-build.log / acc2-integration-build2.log）。
- 冒烟模式：全新树 configure 零 CMake Error＋全量构建零 error、exit=0（acc2-smoke-configure.log / acc2-smoke-build.log）。
- 本验收**未启动任何交互式宿主冒烟**——所有者已裁决不重走（"不用"，帧 host-smoke-r4-04 为时点帧）；B-1 按 §2 专章以在库证据＋独立非交互验证裁定。

---

## 1. 逐项核查（acceptance-protocol §4，4.1～4.11）

### 4.1 改动范围 —— **通过**

- `git diff --name-status ac5d107..3a9c96eb`：71 个变更文件逐一对照契约 `allowedFiles`——ui 产品面 7（app/PortAdapters.{cpp,hpp}、gui_test/WorkbenchContentGuiTest.cpp、include/.../WorkbenchContent.hpp、plugin/UiPlugin.{cpp,hpp}、src/WorkbenchContent.cpp）＋units/ui.md 1＋traceability 63，全部在 allowedFiles 内。
- `forbiddenFiles`（REQUIREMENTS.md / ARCHITECTURE.md）零触碰（不在 diff）；development-task-breakdown.md 在 allowedFiles 内且本次无变更（无需变更——DTB §2.11 行 base 已入）。
- SA-02：diff 无 `industrialrobot/patches/` 变更＝无框架补丁登记需求；无任何框架源码路径（RobWork/RWS 框架层、RobWorkSim）变更。ird_gates 独立复跑两侧命中集一致（见 4.6）亦为旁证。
- 相对 attempt 1 送验 SHA（83aa31a6）的增量面核查：e917734a 仅两 `.hpp` 文件头注释（逐行核对 diff，全部为 `*` 注释行，零代码变更）；3a9c96eb 仅 units/ui.md（8 行）与 traceability 新增。

### 4.2 可复现构建 —— **通过**

- 集成模式（全新树重新配置后）：`CL="-DNOMINMAX" cmake --build build --config Release --parallel 8`——二遍 **0 error、exit=0**（首遍瞬时竞争失败见 §0，与送验对象无关）；sdurws_ird_ui / _plugin / _app / 三测试目标 / RobWorkStudio.exe 全部产出。
- 独立冒烟模式：configure 零错误＋全量构建零错误、exit=0（与实施段 smoke-build-r3.log 结论一致；toolchain＋Qt 前缀口径同）。

### 4.3 契约 verify 命令 —— **verify 1～3 通过（验收者独立执行）；verify 4 见 §2 裁定**

| # | 命令 | 结果 | 证据（acc-attempt2/） |
| --- | --- | --- | --- |
| 1 | `cmake --build build --config Release --target sdurws_ird_ui sdurws_ird_ui_plugin sdurws_ird_ui_app` | **成功** exit=0；`sdurws_ird_ui_plugin.dll`（2,723,840 B）与 `RobWorkStudio.exe`（2,082,816 B）产出齐备 | acc2-verify1-targets.log |
| 2 | `ctest --test-dir .../ui -C Release -L "^ird$"` | **成功** 2/2（ui_test＋contract_test） | acc2-ctest-ird.log |
| 3 | `ctest --test-dir .../ui -C Release -L "^ird_gui$"` | **成功** 1/1（gui_test） | acc2-ctest-gui.log |
| 4 | 宿主 GUI 冒烟（菜单可见＋关闭确认流/Draining 收口/保存链路） | **裁定通过**——本验收未重走交互冒烟（所有者裁决"不用"），按 §2 专章对在库证据独立裁定 | §2 专章＋在库 host-smoke-r4-* 十三件 |

- 三测试 exe 直跑（验收者独立 PATH 注入）：model **154/154**、contract **22/22**、gui **35/35**，failures=0（acc2-gtest-{model,contract,gui}.xml＋控制台日志）——与实施段 -r3 登记（GUI 34/35 中 1 失败＝剪贴板环境干扰）对照：**同一二进制在本验收执行时 35/35 全绿**，独立佐证该失败确系环境干扰而非产品缺陷。
- ird_gates 命中集归一化比对（验收者独立复跑，同引擎 `-P ird_gates.cmake`，head 与 base 各自 detached worktree；归一化＝剥根＋排序去重）：**head 56 ↔ base 56，双向差集 0**（acc2-gates-headset.txt / acc2-gates-baseset.txt / diff 为空）。构成 LIB×5／R3×20／R4×1／SUB×28／T1×2。与实施段 r2〔94〕/r3〔55〕/attempt 1〔151〕计数差异均为归一化粒度差异，差集结论一致。

### 4.4 契约 acceptance 三条逐项 —— **全部通过（acceptance 1 宿主端到端半区按 §2 裁定）**

**acceptance 1（覆写面扩展＋插件侧真实编排＋双模式构建）——通过**：

| 证据项 | 证据（验收者独立复核） |
| --- | --- |
| WorkbenchContentDeps 增 saveProjectHandler/closeProjectHandler | WorkbenchContent.hpp 两 `std::optional<ICommandRegistry::CommandHandler>` 字段（未注入＝阶段 A 占位）；登记循环 src/WorkbenchContent.cpp L926~935 两 else-if 分支（描述符/谓词/登记序零变化）——本验收 4.5 突变直接命中该面 |
| 插件侧编排接线 | plugin/UiPlugin.cpp L265~271 三 handler 注入 orchestrateNewProject/orchestrateSaveProject/orchestrateCloseProject；L219 `sessionDeps.saveAllDraftsManual→saveDraftsNow`——本验收逐行读过 |
| 关闭编排（beginClose→统一确认对话框→resolveCloseDialog→Draining） | UiSessionController.cpp 关闭流后段 L364~390（CloseConfirmed→requestClose→Draining→`draining entered` dev 行→零在途同步完成路径）＋S2 切换段 L319~362——代码审查逐行通过；**在宿主执行的事实证据见 §2.1（L2/L4 两 dev 行）** |
| saveAllDraftsManual＋DraftController 装配 | UiPlugin.cpp 装配链＋DraftController.cpp saveAll（L324~379：脏模块收集→文档组装→落盘→脏代次守卫；**非脏模块 continue 跳过**——0 模块保存设计上不产生磁盘写入，见 §2.1⑦） |
| GUI 测试覆写路由与项目门控用例 | WorkbenchContentGuiTest.cpp L389 `SaveCloseEntryOverrideRoutesShellEntries_UI_SPLIT`（门控断言 L415~418＋路由断言 L431~436）——在本验收 35/35 XML 内，且经 4.5 双突变证明非恒真 |
| 双模式构建零错误 | 4.2 |

**acceptance 2（宿主菜单融合落位）——通过**：菜单位置半区本验收在入库帧上刷新核验——`host-smoke-r4-02b-file-menu-crop.png`（原生分辨率裁剪）：File 菜单内『工业机器人项目』子菜单（带 ▶）**精确位于 Reload 与 Preferences 之间**，Preferences 分隔符锚生效，且该帧摄于 e917734a 二进制（帧 01 标题 `RobWorkStudio v26.9.24-wp10-t17`）；其余各半区（Tools 追加命令面板／自建『视图』菜单插入 Plugins 之前／Plugins 仅基类开关／使能态全经快照／菜单只路由命令板／saveAs 等维持阶段 A 占位／harness 回归零变化）沿用 attempt 1 §4.4 代码级逐行结论——83aa31a6 之后产品面仅注释变更，代码事实未变，结论直接继承。

**acceptance 3（门禁与留痕）——通过**：

- ird_gates 双向差集 0（4.3 表）；ctest 双标签 2/2＋1/1；gtest XML＋ird-test-report.json＋构建日志在库齐备（4.10）。
- 登记的诚实边界与不变项逐项可查（units/ui.md §13 v1.12 立项注／v1.13 落位注／v1.13-r2/r3/r4 增补注，本验收逐条与代码/留痕对照成立——含草稿恢复编排未实现、DraftQueryPortStub 恒空、分支锚缺省不可达、SA-01/O-38③/SA-02 不变项）。
- 宿主 GUI 冒烟执行状态登记链完整：实施段"未执行"如实登记→所有者人工执行（v1.13-r4）→诚实缺口（过程视觉帧未捕获）如实登记→裁定权移交本验收（§2）。

### 4.5 测试真实失败能力 —— **通过（三突变两红一复绿，验收者自有 worktree 内）**

1. 突变 M1：WorkbenchContent.cpp L926 登记 id `"draft.save"` 改为 `"draft.save.NEVER-MATCH-ACC2"` → 重构 gui_test → `SaveCloseEntryOverrideRoutesShellEntries_UI_SPLIT` **FAILED**，断言消息精确命中（"draft.save 未路由到注入的保存编排（UI-T17 覆写面失效）"）（acc2-mut1-run.log）。
2. 突变 M2：还原 M1 后同法破坏 `"workbench.closeProject"` 匹配 → 同用例 **FAILED**（"workbench.closeProject 未路由到注入的关闭编排"）（acc2-mut2-run.log）。
3. 还原后复跑**全量 GUI 套件 35/35 PASSED**（acc2-restore-run.log）；`git diff` 确认送验对象零残留。
4. 测试设计注记（与 F-327 呼应）：该用例以注入 flag 断言**路由机制**——编排内部（orchestrateCloseProject/pollDrain）与 StorePortAdapter 翻译面不在此测试射程，须宿主端到端或未来直测承载；与本验收 §2 的机制/情节分层裁定一致。

### 4.6 架构红线（AGENTS §5 八条）—— **通过**

1. L2 零 Qt：不适用面（ui 为 R-3 唯一例外单元）；diff 新增 include 逐一核对（4.11 表）——Qt 头仅出现在 ui 单元文件。
2. 业务域互链禁止：改动只在 ui 单元；ui→project 仅经 C-3/C-5 冻结端口＋适配器（O-31 既定装配层特权；gates SUB 命中集 head↔base 逐条一致＝无新增白名单边）。
3. 跨单元私有头禁止：新增跨单元 include 仅 `sdurws/ird/project/DraftService.hpp`、`PersistenceFormat.hpp`（project 单元 include/ 公共面）。
4. 框架零源码修改：4.1（无 patches 新文件、无框架路径改动）；唯一框架头 include 为 `rws/RobWorkStudio.hpp`（宿主公开注入面）。
5. 名称前缀拼接/剥离：无——gates R4 命中 head↔base 各 1 处且为 project 单元既有文件（非本任务引入）。
6. 权威唯一：菜单只路由命令板、使能态只消费快照（attempt 1 逐行核实；其后仅注释变更）；保存/关闭语义归 DraftController/UiSessionController，插件只编排。
7. 不可变历史：本任务不触碰修订面（saveAll 注释明示"保存/应用分离红线：零修订"）。
8. 未恢复 old/ 实现：全部按 ui.md §5/§7/§8/§11.5 从零实现。

### 4.7 注释规范 —— **通过（F-326 修复核验成立）**

- **F-326 修复核验**：e917734a 对 UiPlugin.hpp/PortAdapters.hpp 文件头的更新逐行读过——UiPlugin.hpp @brief 改为"UI-T16 立项，UI-T17 增量扩展"并补保存/关闭编排＋草稿链装配＋宿主菜单融合（设计依据引 v1.12/v1.13 登记注）；PortAdapters.hpp 改为"两宿主共用（harness＝HarnessMain；插件宿主＝UiPlugin::initialize/assembleDraftChain）＋落盘线程纪律自 UI-T17 起由 SerialTaskExecutor 真实兑现"。diff 统计（2 文件 +30/−13）全部为注释行，**零行为变更**（本验收二遍全量构建＋35/35 GUI 亦为旁证）。AGENTS §2.5"注释随代码更新"消账成立。
- 类/函数级抽查（本验收独立读）：UiSessionController::resolveCloseDialog（分支语义、§5.4 原文引用、INV-SES-2 结构性保证说明齐备）、DraftController::saveAll（脏代次守卫的"为什么"、失败轨语义）、StoreLock::release（RAII 释放＋锁外 join 的死锁理由）、createNew 契约注释（执行序/异常/线程安全/复杂度俱全）——§2.2/§2.3/§2.5 要件（线程约束、所有权、错误语义、确定性来源）在抽查面内齐备。

### 4.8 文档同步＋findings 相关性 —— **通过**

- units/ui.md：文档版本行 v1.13-r4＋v1.13-r3 增补登记注（§13 L1610/L1612）＋§16.7 两行（L1810/L1811）与代码/留痕逐项对照成立（含 GUI 34/35 的三重环境证据、F-326/F-327 处置、冒烟执行登记与诚实缺口）。其中 v1.13-r4 的一处**时序表述错误**（dev 日志第 2 行归属与时刻）由本验收 §2.1 查明，登记为 F-328 建议级勘误项——不动摇其"诚实缺口如实登记＋裁定权移交"的框架。
- DTB：无变更需求（§2.11 行 base 已入，与本实现一致）；任务状态翻转归收尾环节（编排者辖权）。
- validate-task PASS（亲跑，留痕在库）。
- **F-318~F-327 逐条相关性复核**（attempt 1 §4.8 表为基准，状态更新如下）：

| 条目 | 现状态 | 本验收复核结论 |
| --- | --- | --- |
| F-318/F-319 | open @ acc/UI-T16/1 | 与 attempt 1 结论同（UI-T16 文档表述/失实载体问题，实质已由 ui.md v1.11 兑现；非本验收辖权）。建议随 findings 清理判定。 |
| F-320 | open @ acc/UI-T16/1 | 装载通道前置——r4 冒烟实际以 `--rwsplugin` 通道执行成功（帧 01），前置清单有效性再次获证；`--ini-file` 通道仍未复核。**保持 open**。 |
| F-321 | open @ redesign-main | 切换路径状态投影——**本验收获得该路径首次在宿主执行的留痕**（L4 `switch rejected`，§2.1⑤）：S2 机制按"候选验证成功才切"保守语义工作；其 UX 语义面补充观察登记 F-330，与 F-321 定案同批考量。**保持 open**。 |
| F-322 | open @ redesign-main | dev 日志采样尾段——r4 再实证（存活期仅首行＋退出后 [sampled-tail] 两行且**文件序与时间序倒挂**，§2.1②）。**保持 open**。 |
| F-323 | open @ redesign-main | 全新工作树可复现性——本验收 100% 复现两子项（§0），另补充集成树 toolchain 口径。**保持 open**。 |
| F-324 | open @ acc/UI-T17/1 | ini 通道失效——r4 未使用该通道，无新证据。**保持 open**。 |
| F-325 | open @ acc/UI-T17/1 | 取证管线脆弱——r4 再实证两次（子菜单合成点击未命中帧入库；dev 日志采样失序）。**保持 open**。 |
| F-326 | open @ acc/UI-T17/1 | **已修复**（e917734a 纯注释对齐，本验收 4.7 逐行核验）——本分支 findings.json 置 status=fixed。 |
| F-327 | open @ acc/UI-T17/1 | 延期裁决已按 DTB §5.4 登记（v1.13-r3 ②：直测需测试目标增链 project，抵触本任务"零 CMake/链接面变更"登记；随完整草稿链路装配任务补齐）——延期理由成立，**保持 open**（责任方与批次明确）。 |

### 4.9 commit 质量 —— **通过（5 提交全查）**

- c2c1e0a4／656b7f30／83aa31a6：attempt 1 已逐个四段核查（通过），本验收抽核无新发现问题。
- **e917734a**（本验收全文读）：`[WP-10-T17] 返工（attempt 2·实施段）：F-326 文件头注释对齐＋F-327 延期裁决＋r3 全量重验留痕`——四段齐备；验证段的每一结论（构建/GUI 34/35 三重证据/gates 55↔55/validate-task/冒烟"仍未执行如实登记"）均与在库 -r3 留痕逐件对得上；无粉饰（"仍未执行"如实写明）。
- **3a9c96eb**（本验收全文读）：`[WP-10-T17] 宿主 GUI 冒烟取证入库（所有者人工执行）＋ui.md v1.13-r4 登记注`——四段齐备；`.log 留痕经 git add -f 强制入库（B-1 同族教训）`与在库事实一致（4.10 核验）；"视觉帧未捕获…按 ACC 4.4 纪律如实登记缺口不标注通过"表述与证据状态一致。
- 提交信息与代码/留痕的一致性、诚实度：**通过**。

### 4.10 留痕完整 —— **通过**

- `git ls-tree -r 3a9c96eb` 下 `traceability/builds/wp10-t17/` 共 **62 件**逐一核对在库：gtest XML 三代（-r2/-r3 各三件＋控制台）＋ird-test-report 三代＋集成/冒烟构建日志三代＋r4-prebuild＋ctest 双标签两代＋ird-gates 两代＋gate-hitset/diff 三代＋validate-task 两代＋剪贴板探针＋冒烟脚本三代＋HANDOVER＋host-smoke-r4 十三件（README/4 PNG/dev 日志/锁/落盘汇编/清单/两工具脚本/prebuild）。**所有 .log/.xml 真实入库（无 .gitignore 吞件）**。
- r4 证据与宿主机活动现场交叉核对（§2.1⑧）：入库 dev 日志与活动文件逐字节一致；入库锁/落盘汇编与现场一致。
- 验收段自身留痕随本 evidence 分支入库：`traceability/builds/wp10-t17/acc-attempt2/` 25 件（configure/构建两遍/冒烟两段/verify1/ctest 双/gtest 三 XML＋三控制台/gates 两侧日志＋两归一化集/两突变 build+run/复绿 run/validate-task）。

### 4.11 偷懒/缩水实现扫描 —— **通过**

1. TODO|FIXME|XXX|placeholder|not implemented|未实现 扫描（产品面 diff）：**零命中**；"占位"×7 全部为契约显式的 §7.1/§11.4 阶段 A 占位形态表述（与 attempt 1 结论一致，豁免成立——project.saveAs/draft.apply/undo/redo 维持占位为契约 note 明文）。
2. stub 命中：SessionTaskPortStub/DraftQueryPortStub 为既有冻结类型引用（UI-T11/T12），契约 note 显式登记为诚实边界，豁免成立。
3. 空实现检测：attempt 1 已查全量（唯一"空"体＝forceAbandonAll 显式留痕空兜底）；83aa31a6 后产品面零代码变更，结论继承。
4. 逐条证据表：见 4.4（acceptance 三条逐项，含 §2 对宿主端到端半区的证据裁定）。
5. 测试强度：见 4.5（双突变双红＋复绿；断言校验路由行为与门控边界）。

---

## 2. B-1 独立裁定（专章——显式成文）

> 裁定问题：按 ACC §4.3/§4.4 纪律，在库证据是否足以把"契约 verify 第 4 条后半＋acceptance 1 的关闭/保存宿主端到端"判为通过？（所有者已裁决不重走交互冒烟；两个方向均被允许，本验收独立裁定如下。）

### 2.1 在库证据时序查证（本验收独立解码＋现场交叉验证）

**① dev 日志毫秒时间戳解码与时钟校准**。四行解码（本地 UTC+8）：

| 行 | 内容（缩写） | ms 时间戳 | 解码 | 独立交叉验证 |
| --- | --- | --- | --- | --- |
| L1 | 插件诊断栈就绪 | 1790229891548 | 14:04:51.548 | **dev-diagnostics.log 文件出生时间 14:04:51.5487——毫秒级吻合** |
| L2 | `draining entered: in-flight references=0` | 1790231499336 | **14:31:39.336** | （无直接文件时刻；见③时钟可信） |
| L4 | `switch rejected (candidate open failed): path=…projtarget-r4 code=lock-held-by-other` | 1790232049992 | 14:40:49.992 | 与 README"~14:42 再开尝试"相符 |
| L3 | `[sampled-tail] 宿主已关闭工作单元` | 1790232290511 | 14:44:50.511 | 文件 mtime 14:44:50.565＝采样写出完成时刻，吻合 |

时钟结论：ms 时间戳为墙钟毫秒精度（L1 与文件出生时间互证），**可信**。注意 L3（14:44:50）与 L4（14:40:49）在文件中**次序倒挂**且带 [sampled-tail] 标注——留痕管线次序不可靠（F-322/F-325 同族再实证）。

**② r4 项目库创建时刻**。projtarget-r4 三件出生时间戳：project.json 14:32:35.387／HEAD 14:32:35.390／lock 14:32:35.397；project.json `createdAtUtc=06:32:35.376Z`（=14:32:35.376 本地）。四证据一致：**库创建于 14:32:35**。

**③ 关键发现（登记 F-328）：L2 早于库创建 56 秒，不可能属于冒烟项目r4 的关闭**。L2＝14:31:39.336 < 14:32:35.38。且 `ProjectStore::createNew` 契约（project/ProjectStore.hpp L383~422）要求目标"**不存在或空目录**"（违反＝fail-fast）——若 projtarget-r4 在 14:32:35 前已有库，创建即失败。故 **L2 的 Draining 必然属于本会话中更早的另一个项目会话**（其库已不在盘上：本验收按 mtime 窗口 14:25~15:00 全搜 Temp 与仓库树，仅 projtarget-r4 一把锁存在）。README §1 与 v1.13-r4 ③把 L2 归属"~14:33-14:34 关闭编排进入 Draining"的时序重建**与自身引用的证据矛盾**（登记时刻与解码时刻不符）——登记表述错误，非产物造假（四件证据均为现场忠实拷贝，本验收逐字节核对）。

**④ L2 的真实证明力＝机制级在宿主执行的事实**。全代码库 `draining entered` 唯一发射点为 UiSessionController.cpp L382——位于 **S1 关闭流后段**：到达该行的代码路径为 统一确认对话框呈现→用户**确认**→草稿/任务两轴处置执行→`store.requestClose()`→状态 Draining→dev 行→（in-flight=0 时）L386~388 同步完成关闭。故 L2 证明：**在 r4 会话（同一宿主进程、同一送验二进制）中，S1 关闭确认流→requestClose→Draining→零在途同步收口这条完整路径于 14:31:39 真实执行过一次**（对一个更早打开的项目）。

**⑤ L4 的证明力＝统一确认对话框确认执行的第二件机制级留痕**。`switch rejected` 唯一发射点为 UiSessionController.cpp L334——位于 `resolveCloseDialog` 的 **S2 切换流后段**，到达该行同样前置**对话框确认与处置执行**（L305~315），随后候选验证失败（本项目锁被本会话持有＝自锁）→A 会话保持不变（保守拒绝）。故 L4 证明：14:40:49 统一关闭确认对话框又一次被呈现、确认并执行处置，S2"候选验证成功才切"语义在宿主真实工作。

**⑥ 锁生命周期**。`StoreLock::release()`（win32/StoreLock.cpp L701~722）只关句柄**不删除锁文件**——锁文件存在不能证明持续持有。但锁 `hb=06:44:44.430Z`＝宿主退出时刻（与 L3 采样时刻、优雅退出序一致），证明**退出时刻 projtarget-r4 上存在活锁**（原锁持续持有，或关闭后重开再持有——两读均与 L4 自锁拒绝相容，在库证据不可区分，亦无需区分）。

**⑦ 保存草稿链路的证据可得性**。DraftController::saveAll（L324~379）对非脏模块 `continue` 跳过——**0 模块保存设计上不产生任何磁盘写入**；保存失败会发 `saveAll failed` dev 行（L372）——日志中无此行。故 r4 场景（新建未编辑→保存草稿）下：磁盘无痕是**设计的必然**而非"未执行"的证据；落盘翻译面（C-5 写半区）在该场景中**空洞化**（与 F-327 登记的"该面仅能经宿主端到端验证"叠加，但端到端本身在本场景也无从产生磁盘证据）。

**⑧ 宿主机活动现场核对（本验收独立执行，只读）**：Temp/wp10-t17-smoke-r3/ 现场仍在——入库 dev 日志与现场文件**逐字节一致**（无隐匿行）；host.pid=10956 与锁 pid 一致；shots/ 存在**未入库帧 11 枚**：14:36 的 r4-06-pass1-endstate 实为所有者桌面其他应用前台（宿主窗口不在画面，**不构成关闭流证据**，且含所有者个人屏幕内容——不入库为妥）；14:06~14:12 各帧显示宿主"未打开项目"（创建前），其中 r4-03/r4-05 帧刷新呈现融合菜单栏 File/Tools/View3D/视图/Plugins＋五区工作台；**captureloop 两轮的 loop-*.png 帧在现场不存在**（目录 mtime 14:35/14:55 提示批量清理发生过）——"两轮连拍错过操作窗口"的登记无可复核的在盘留痕（F-329）。冒烟二进制来源链闭合：integration-build-r4-prebuild.log（14:03，目标＝主树）＋主树 sdurws_ird_ui_plugin.dll mtime=14:03＋帧 01 启动 14:05。

### 2.2 裁定推理（机制级／情节级两层）

B-1 要求宿主实证的三行为，拆成两层检验：

| 层 | 问题 | 证据 |
| --- | --- | --- |
| **机制层**（该二进制的这些代码路径在真实宿主中是否工作） | 统一关闭确认对话框→确认→处置；S1 requestClose→Draining→零在途同步收口；S2 候选验证保守拒绝 | **L2＋L4 两行毫秒精度的 dev 日志**（各自唯一发射点的代码位置决定其证明力，见 2.1④⑤）＋锁心跳（活锁至退出）＋真实库落盘（project.json/HEAD/内容寻址对象/修订）＋35/35 GUI（含路由突变双红） |
| **情节层**（所有者是否把上述操作施行于冒烟项目r4） | 新建→保存→关闭取消/确认→重开尝试 | **所有者确证原话**（"我已经手动验证了，都正确可以通过验收"）＋库创建/打开的磁盘事实＋L4（14:40 对 projtarget-r4 的打开尝试被自锁拒绝＝该项目当时在本会话中处于打开态）＋无任何相反证据（无 saveAll failed 行、无错误页帧、无异常锁状态） |

**方向权衡**：

- 判 **fail** 的唯一立足点是"情节层缺视觉帧＋L2 被登记错归属"。但：①机制层证据在本轮**新增且变强**（本验收的解码把 L2/L4 从"登记叙述"升格为"代码位置可考的执行事实"——尽管同时纠正了 L2 的归属）；②情节层的缺口根源是取证管线缺陷（F-322/F-325 在案）而非行为未发生，ACC 4.4 禁止的是把**未执行**标注为通过，而此处行为已由协议最高语义权威（所有者，ACC §6）当面执行并确证；③所有留痕缺口均已**如实登记**（v1.13-r4 ④），无任何粉饰——恰恰符合 4.4 纪律的诚实要求；④所有者已裁决不重走（补证方式的所有者裁决已作出）——判 fail 的唯一出路是要求所有者推翻自身裁决再走一遍，ACC §6 规定该裁决权本就在所有者，验收者无权倒置。
- 判 **pass** 的代价是接受"情节层以所有者确证为据"。ACC §2.2 禁止消费的是**实施者**叙述；所有者对交互式 GUI 行为的当面确证是本协议证据等级结构中此类行为可获得的最高等级证据，且其"不用补拍"裁决使更高等级证据在程序上不可获得。残余风险（r4 关闭情节若真有缺陷而所有者观察失误）被以下事实压低：同一二进制、同一会话中同族代码路径已有两行留痕证明工作正常；自动面 154＋22＋35 全绿；attempt 1 逐行代码审查零缺陷；无任何一行留痕与成功叙事情节矛盾。

### 2.3 裁定

**B-1 判为通过**：机制层有毫秒精度、代码位置可考的在库执行实证（L2/L4＋锁生命周期＋磁盘落盘）；情节层由协议最高语义权威的所有者当面执行并确证，且其已裁决取证方式到此为止；全部缺口如实登记无粉饰；无任何相反证据。按 ACC §5 判据，这不构成阻断级问题；残余不确定性以本节文字与本记录留痕固化，供后续任务（F-321 定案/完整草稿链路装配）沿用更可靠取证管线（截图判据＋优雅退出＋日志全量固化——F-325 处置方向）时一并加固。

### 2.4 附带裁定：`switch rejected (candidate open failed) code=lock-held-by-other` 的产品语义属性（4.4/4.11 指定项）

**独立判断：非缺陷（正确的保守拒绝），附一项 UX 语义改进空间（登记 F-330，建议级）。**

理由：S2 切换流的设计契约是"候选验证成功才切"（PM-03）——候选验证在 A 仍持锁时打开 B，防止双写与半切换态。当 B≡A（用户重开已打开的同一项目）时，候选打开必然撞上**自己持有**的锁而失败，按语义保守拒绝、错误页呈现、A 会话原样保留——数据安全侧零风险，行为与冻结设计一致（UiSessionController.cpp L319~337 注释即此语义）。产品语义上的不理想之处仅在 UX：同项目重开本可短路为无操作/聚焦既有会话，而非错误页；『最近项目』菜单使该路径在日常使用中可达，故值得改进。此属语义面完善而非缺陷修复，归 F-321（切换路径状态投影，open）定案时同批考量。

---

## 3. 问题清单

### 阻断级

- **无**。

### 建议级（已随本 evidence 分支转登 traceability/findings.json，编号自 F-328 起）

- **F-328** r4 冒烟登记的时间线归属错误：README §1 与 ui.md v1.13-r4 ③把 dev 日志第 2 行 `draining entered` 归属于冒烟项目r4 的关闭（"~14:33-14:34"），而该行毫秒时间戳解码为 **14:31:39.336**——早于 projtarget-r4 库创建（14:32:35，出生时间戳四证一致）56 秒，且 createNew"空目录"契约排除该库早先存在的可能；L2 实为同会话更早另一个项目（库已不在盘）的关闭流留痕。属登记表述/解码错误（四件证据本身忠实），不影响 §2.3 的裁定（裁定不依赖该归属，且机制级证明力因本查证而增强）。处置：随下批 ui/文档触碰窗口在 ui.md 与 README 增补勘误注（引用本记录 §2.1）。
- **F-329** r4 取证现场证据卫生：①captureloop 两轮约 1800 帧 loop-*.png 在现场目录不存在（目录 mtime 提示批量清理），"两轮连拍错过操作窗口"的登记无可复核在盘留痕；②shots/ 存在未入库帧（含 r4-06-pass1-endstate——所有者个人屏幕内容帧）。处置归所有者裁量：或补登记"已清理"事实，或对可入库帧脱敏归档；个人内容帧建议确认删除。
- **F-330** 同会话重开已打开项目的 UX 语义（§2.4）：S2 自锁保守拒绝正确但体验生硬（错误页 vs 短路/聚焦）。随 F-321 切换路径定案同批考量，不单独排期。

---

## 4. 裁决

- **verdict = pass**。4.1／4.2／4.3（verify 1~3）／4.5／4.6／4.7／4.8／4.9／4.10／4.11 通过；4.3 verify 4 与 4.4 acceptance 1 宿主端到端半区按 §2 专章裁定通过（机制级在库实证＋所有者情节级确证＋缺口如实登记）。
- 无阻断级问题；建议级 F-328/F-329/F-330 已转登本分支 findings.json（F-324~F-327 并集收编，F-326 置 fixed），不阻塞合入。
- 本验收未触碰 wp10-t17／redesign-main／main 与编排者辖权文件（traceability/pipeline/state.json）；未向送验分支推送任何提交；未执行任何交互式宿主冒烟（遵守所有者"不用"裁决）。
- 合入决策按 ACC §6 归所有者。

## 5. 验收者环境

Windows 11 x64，Git Bash＋pwsh 7，CMake 4.3.1，VS 2022 x64，Qt 6.11.1（D:/software/Qt/6.11.1/msvc2022_64），vcpkg 经典模式（仓库根，集成树亦经其 toolchain 解析 Boost 等）。复现现场＝detached worktree @3a9c96eb（仓库外）；集成构建树与冒烟构建树均全新配置；base 侧 ird_gates 在另一 detached worktree @ac5d107 复跑。宿主机活动留痕现场（Temp/wp10-t17-smoke-r3/）以只读方式核对，未修改任何现场文件。
