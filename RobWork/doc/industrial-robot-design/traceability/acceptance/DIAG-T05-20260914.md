# DIAG-T05 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 ID | DIAG-T05（≙WP-09-T03/T04 设施面，diagnostics 单元：ConfirmableFinding 服务——FindingId/Binding/状态机） |
| 契约 | `doc/industrial-robot-design/tasks/foundation/DIAG-T05.json`（status ready；branch wp09-t05；requirements MDL-06/ERR-01/CON-06；knownPitfalls P-DIAG-1/P-DIAG-4/P-DIAG-6/CR-02；interUnit true） |
| 送验对象（冻结） | `e98e693d187dbd613cfa2d2730fb9a24b6692940`（验收全程复核：`origin/wp09-t05`＝该 SHA，**无分支漂移**） |
| diff 范围 | `c155aa62cb9dadae2e03becf072d123724770ecd`（base，redesign-main 尖端）..`e98e693d`，共 2 提交（af6b00b3 实现＋e98e693d 单元卡/留痕）、7 文件（+2840/−8） |
| verdict | **pass**（4.1~4.11 全部通过，无阻断级问题；新增建议级登记 3 项＝F-103/F-104/F-105，沿 F-081/F-102 家族既判先例不阻塞合入） |
| 验收日期 | 2026-09-14 |
| 验收者 | 独立验收会话（全新上下文；未消费实施叙述；对抗式立场——复现优先于采信，下述构建/测试/门禁/探针均为验收者亲手执行） |

---

## 一、逐项证据（ACC §4.1~4.11）

### 4.1 改动范围 —— 通过

- diff 文件清单（`git diff --name-only c155aa62..e98e693d`，7 个）逐个比对契约 `allowedFiles`：
  - `RobWork/RobWorkStudio/src/rwslibs/industrialrobot/diagnostics/**`（5 个）：CMakeLists.txt（M，增列 Confirmable.cpp 与两测试文件）、include/sdurws/ird/diagnostics/Confirmable.hpp（A，公共头）、src/Confirmable.cpp（A）、test/ConfirmableTest.cpp（A）、test/ConfirmableProjectStubTest.cpp（A）；
  - `RobWork/doc/industrial-robot-design/units/diagnostics.md`（M，allowedFiles 第 2 项）；
  - `RobWork/doc/industrial-robot-design/traceability/builds/wp09-t05/ird-test-report.json`（A，allowedFiles 第 3 项）。
- `forbiddenFiles`（REQUIREMENTS.md / ARCHITECTURE.md）：diff 范围内零命中。
- SA-02（框架零源码修改）：diff 范围外文件计数 0（`git diff --name-only <range>` 过滤 industrialrobot/doc 前缀后空输出）；`industrialrobot/patches/` 零新文件零改动。
- **范围越界：零。**

### 4.2 可复现构建 —— 通过（验收者亲跑；环境走弯如实说明）

- **复现现场**：`git worktree add --detach <临时目录A> e98e693d187dbd613cfa2d2730fb9a24b6692940`（detached @ 送验 SHA）。
- **集成模式（干净构建树，重新配置）**：按主树缓存实际口径配置（`-DRWS_BUILD_INDUSTRIALROBOT=ON -DRWS_USE_STATIC_LINK_PLUGINS=ON`＋vcpkg toolchain＋Qt `CMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`），配置 exit 0 零 CMake Error，缓存确认 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`。
  - **三次失败配置的环境走弯（如实登记，均为既有环境链条目的复现实证，非本任务缺陷）**：①未带 vcpkg toolchain → Boost 未找到配置失败；②带 toolchain 缺 Qt 前缀 → Qt6/Qt5 未找到失败（F-007/F-098/F-062 在册）；③带 Qt 后 `RobWorkStudio.ini.template.static` 缺失失败于 RobWorkStudio/src/CMakeLists.txt:58（主树未跟踪本地辅助件——F-007/F-062/F-099 在册），自主树补拷（先 `mkdir bin`——worktree 无该本地目录）后配置通过。②③两轮失败均在**删除/覆盖 build 树后重配**，未落 F-101 所述缓存污染面（初配失败残留已随 `rm -rf build` 清除）。
  - **最终结果**：`cmake --build build --config Release --target sdurws_ird_diagnostics_test sdurws_ird_diagnostics_contract_test` **exit 0、零 error C／fatal error**（grep 计数 0）；`sdurws_ird_diagnostics`（STATIC 增列 1 源文件）、两测试可执行文件全部编译链接成功（产物 `build/RobWorkStudio/bin/Release/`）。口径说明：本次构建两目标及其依赖链（core＋框架库），未做全树目标构建——诊断单元改动面完全覆盖，无全树证据缺口（送验留痕亦为主树口径，见 4.9 注记）。
- **独立冒烟模式（另配临时目录，带 toolchain——F-002/CORE-T01 G-2 口径）**：`cmake -S RobWork/RobWorkStudio/src/rwslibs/industrialrobot -B <临时目录> -DCMAKE_TOOLCHAIN_FILE=.../vcpkg.cmake` 配置 exit 0、`cmake --build ... --config Release` **exit 0 零 error**（grep 计数 0）；两测试可执行文件产出。
- **测试执行（验收者亲跑，双模式，两目标）**：
  - 集成：`sdurws_ird_diagnostics_test.exe` → **54/54 PASSED**（exit 0）；`sdurws_ird_diagnostics_contract_test.exe` → **9/9 PASSED**（exit 0）。
  - 冒烟：冒烟树两可执行文件 → **54/54 PASSED**＋**9/9 PASSED**（exit 0）。
  - 与送验留痕 `ird-test-report.json` 的 regression 字段（integration 63/63、smoke 63/63＝54＋9）一致。

### 4.3 契约 verify 命令 —— 通过

契约 `verify` 数组仅一条，验收者在复现 worktree 根目录逐字亲跑：

```
cmake --build build --config Release --target sdurws_ird_diagnostics_test   → exit 0
```

产物 `build/RobWorkStudio/bin/Release/sdurws_ird_diagnostics_test.exe` 可直接运行（54/54，见 4.2）。

### 4.4 acceptance 条目逐项证据 —— 通过（4 条全部有证据；ird_gates 如实登记基线命中而非虚标通过）

| # | acceptance 条目 | 证据 |
| --- | --- | --- |
| 1 | DT-CFM-1~10 用例通过（含 project 桩联动）：服务端状态机全转移（Pending→Confirmed/Invalidated/Expired，core 投影三态）、绑定四元组双复核（确认提交时＋编译前——project §6.7）、失效路径（revision-changed/policy-changed/write-lost/interaction-lost/user-rejected）、确认留痕形状与 project §6.7 一致（DT-CFM-10） | 亲跑双模式 54/54＋9/9 全绿。**状态机全转移**：`DtCfm1_CreateThenConfirmTransitionsToConfirmed`（ConfirmableTest.cpp:229——Pending 初态/binding 逐成员冻结/token 分配/expiresAtUtc 默认 nullopt→Confirmed＋confirmation 写入＋投影 Confirmed＋token 一次性）；`DtCfm2_RejectionInvalidatesWithCoreRejectedProjection`（:403——user-rejected→Invalidated＋投影 Rejected＋终态不可逆）；`DtCfm3_ExpirationOnlyWhenExplicitlyArmed`（:439——默认恒 Pending〔推进时钟一年〕＋显式 armExpiration→Expired＋投影 Pending＋过期后确认 InvalidState）。**双复核（提交时半区）**：`DtCfm4_RevisionChangeYieldsBindingMismatchInvalidation`（:481——tip 前进→BindingMismatch＋Invalidated(revision-changed)＋token 作废后重放 InvalidState）；`DtCfm4_CommandDigestMismatchYieldsBindingMismatch`（:516——命令槽 nullopt→binding-mismatch）；`DtCfm5_PolicyChangeYieldsBindingMismatchInvalidation`（:535——policy-changed＋非策略来源对照半区策略变化不触发）。**双复核（编译前半区）**：`DtCfm7_PreCompileRebindRejectsStaleConfirmation`（ConfirmableProjectStubTest.cpp:573——S4 确认成功后、编译前 hook 注入 tip 变化→Rejected(confirmations-unresolved)＋无修订＋编排侧 invalidate(revision-changed)）。**失效路径**：`DtCfm6_InvalidateTransitionsAndTerminalGuards`（ConfirmableTest.cpp:573——interaction-lost/write-lost/canceled 三路＋终态再失效 InvalidState 异常＋未知 id/空 reason Usage）。**投影三态**：`DtCfm1`（Pending/Confirmed）＋`DtCfm2`（Rejected）＋`DtPdiag6_NonRejectInvalidationAndExpiryProjectToCorePending`（:692）。**留痕形状**：`DtCfm10_ConfirmationRecordShapeEntersCommandSummary`（ConfirmableProjectStubTest.cpp:537——Committed 后 confirmations[] 恰一条＝{binding 四元组, credential}，findingDigest 经 findingRecordDigest 复算比对、credential principal/时间逐字段回读） |
| 2 | SA-15 分工（DT-CFM-6~8）：worker 不产生可确认诊断（装配面静态断言）；UI 关闭回调失效即 Invalidated、无永久等待；确认≠自动成功（DT-CFM-9 编译失败仍不提交）；未确认不能提交命令（DT-CFM-7——project 桩 confirmations-unresolved） | **worker 禁令**：产品侧三 `static_assert`（Confirmable.hpp:559-564——IDiagnosticSink/IDevLogSink 与 IConfirmableFindingService 互不可转换、ConfirmableFinding 不得窄化为 DiagnosticRecord）＋`DtCfm8_WorkerAssemblyExposesNoConfirmableSurface`（ConfirmableProjectStubTest.cpp:484——测试侧复钉同款判据＋运行期通道帧均为普通诊断文本＋服务端 pendingFor 恒空）。**UI 关闭无永久等待**：`DtCfm6_UiCloseInvalidatesFindingWithoutWaiting`（:451——isAlive=false 前置拦截、回调零调用、Aborted(interaction-lost)、finding 即时 Invalidated(interaction-lost)、token 复用 InvalidState——桩流程同步返回无阻塞）。**确认≠自动成功**：`DtCfm9_ConfirmedButCompileFailureCommitsNothing`（:514——compileOk=false→Failed/无修订/confirmations 不入命令摘要/finding 保持 Confirmed）。**未确认不能提交**：`DtCfm7_NonInteractiveSubmitRejectedUnresolved`（:424——interaction==nullptr→Rejected(confirmations-unresolved)＋无修订＋finding 仍 Pending→会话关闭 canceled 失效） |
| 3 | P-DIAG-4 处置：FindingId 规范文本 fnd-<32hex> 由本单元自持解析，句法与 core Id128 严格一致；core 收编 fnd- tag 前不私改 core | `DtPdiag4_FindingIdCanonicalSyntaxMatchesCoreId128Rules`（ConfirmableTest.cpp:656——toCanonical 形态 fnd-＋32 小写 hex＝36 字符、parse(format(x))==x 往返、全零保留值 isValid=false、严格性探针〔tag 不符/大写 hex/长度违约/缺 tag/空串全拒〕、fromCanonical 违约抛 DiagnosticsError(Usage)）；实现自持（Confirmable.cpp:237-259 tryParseFindingId——与 core detail 零 include，grep 实证公共头/实现零 `ird/core/` detail 引用）；**core 零改动＝4.1 范围实证**（diff 零 core 文件；core 收编建议登记待裁决随单元卡 v0.6 承载） |
| 4 | P-DIAG-6/CR-02 处置：expiresAtUtc 默认 nullopt（无超时自动确认——project §5.3.4；启用超时＝需求变更，不私裁）；findingDigest/commandDigest 摘要只经 core ContentDigester，不私设第二哈希路径 | **P-DIAG-6**：`DtCfm3`（:439——默认 nullopt 恒 Pending＝无超时自动确认；显式设置唯一起点＝实现类原语 armExpiration〔Confirmable.cpp:671，不在 §9.3 虚接口〕）＋`DtPdiag6`（:692——失效/过期投影 Pending 不进 core 枚举）＋`DtCfm1` expiresAtUtc 默认 nullopt 断言（ConfirmableTest.cpp:264）。**CR-02**：`DtCr02_FindingDigestDeterministicAndSensitive`（:712——同记录两次摘要逐字节相等＋实际值/单位/context/subject 四字段敏感性）；实现唯一摘要路径（Confirmable.cpp:182-213 findingRecordDigest 只经 core::ContentDigester，magic "IRDDREC1" 域隔离，canonical 编码表登记于文件头；文件内 grep 零第二哈希实现——匿名空间 L221 注释明示不引入 FNV 路径） |

**ird_gates（如实登记）**：验收者亲跑 `cmake --build build --config Release --target ird_gates`（复现 worktree 集成树）→ exit 1，**命中 7 项与 wp09-t04 合入基线逐项一致、零新增**：IRD-GATE-SUB×5（evidence_test/evidence_contract_test/policy_test/policy_contract_test/runtime_test → sdurws_ird_testkit 直链，F-072/F-051 族既有）＋IRD-GATE-SUB 表外边×1（sdurws_ird_testdata_lint → testkit，F-019）＋IRD-GATE-T1×1（sdurws_ird_testdata_lint 产品目标链 testkit，F-019）；（输出中另见的 IRD-GATE-R1/R3/R4/R5/T2 各 1 次为引擎自测"按预期检出"行，非命中）。**diagnostics 相关命中＝0**（输出全文检索 diagnostics 计数 0）。exit 1＝基线既有命中的预期行为，处置权 WP-01 所有者——与送验 ird-test-report.json gates 段登记一致（如实登记基线命中，未私改门禁、未虚标通过）。

### 4.5 测试真实失败能力（验证验证者）—— 通过（两探针亲手构造、真实变红、还原复绿零残留）

验收者在复现 worktree 的实现文件临时注入缺陷、重建重跑、逐字还原（还原后 `git status` 零残留、grep `PROBE-TEMP` 计数 0，54/54＋9/9 复绿）：

- **探针 1（tip 比对短路）**：`src/Confirmable.cpp` verifyBinding 第④步改为 `if (false && !(m_environment->currentTipRevision(...) == record.binding.baseRevisionId))`——`DtCfm4_RevisionChangeYieldsBindingMismatchInvalidation` **真实变红**（1 FAILED，与送验留痕 realFailureEvidence 探针 A 预测的同用例）。
- **探针 2（惰性过期短路）**：`src/Confirmable.cpp` expireIfDue 函数体首注入 `if (record.state == FindingState::Pending) { return; }  // PROBE-TEMP`——`DtCfm3_ExpirationOnlyWhenExplicitlyArmed` **真实变红**（1 FAILED，与送验留痕 realFailureEvidence 探针 B 预测的同用例）。
- 两探针均证明断言非恒真、测试校验真实行为；与送验留痕 `realFailureEvidence` 字段的双探针设计相互印证（探针期间中间构建未计入任何结论）。

### 4.6 架构红线（AGENTS §5 八条逐条）—— 通过

1. **L2 计算内核零 Qt**：新增 4 文件 include 全集扫描零 Qt（`grep "#include <Q"` 计数 0）；ird_gates R-3 零 diagnostics 命中；CMakeLists 配置期 Qt 红线守卫（L228-231）在位——三道防线一致。
2. **业务域互链禁止（R-1）**：产品库链接边未变（PUBLIC 仅 `sdurws_ird_core`，Confirmable.cpp 增列不改链接）；CMakeLists 依赖守卫（L219-237）+ird_gates 零 diagnostics 命中。
3. **跨单元私有头禁止（R-2）**：公共头 Confirmable.hpp 落 include/sdurws/ird/diagnostics/；include 集合＝std＋`ird/core/{DiagData,Digest,Identity}`＋本单元 {Catalog,DiagCodes,Errors}（全为对端公共头）；core detail 层零引用（grep 实证）；本任务未新增私有头（实现细节均匿名校验空间/静态自由函数于 cpp 内）。
4. **框架零源码修改（SA-02）**：见 4.1——diff 零框架文件、patches/ 零改动。
5. **不做名称前缀拼接/剥离（R-4）**：本任务无名称解析语义（FindingId 体系与 RobWork 名称面无关，名称权威未触碰）；ird_gates R-4 零 diagnostics 命中。
6. **权威唯一（PA-1）**：确认设施＝diagnostics 单元职责（单元卡 §5.1 分工表逐字承接）；放行编排/提交/留痕持久化归 project（桩只验证契约不充当对端实现——P-DIAG-8 处置，桩注释明示）；码值权威＝StableCodeRegistry（create 前置只读查询 find，不注册不废弃）；文案键值归 ui/文案资源（服务只持派生键）；ui 判定权未越入（服务端无启发式判定，§14.4 v0.6 ②登记）。
7. **不可变历史（PA-2）**：FindingRecord 值语义（tryFind/pendingFor 值返回快照——调用方改拷贝不影响状态机）；状态字段仅经服务方法按状态机转移（§5.2 表头）；终态不可逆（Invalidated/Expired 再失效→InvalidState 异常，DtCfm6 实证）；确认凭据失效按未确认处置不覆盖确认事实（invalidate 对 Confirmed 仅登记复核结论，core 投影落回 Pending 不伪造放行）。
8. **不继承 old/**：实现按单元卡 §5.2/§5.3/§5.4/§9.3 从零编写（old/ 不在磁盘，零引用）。

### 4.7 注释规范（AGENTS §2）—— 通过（附一处残留注释微瑕——转登 F-105）

- **公共头全查（1/1，Confirmable.hpp 568 行）**：文件头设计依据逐条（§5/§9.3/需求 MDL-06④/ERR-01/CON-06＋SA-15＋契约 knownPitfalls 四项处置锚点）；SA-15 分工背景专段；线程约束专段（服务互斥/值类型并发只读）；生命周期专段（进程级服务＋会话级记录）；FindingId/FindingState/finding_reason 逐值出处注释、FindingBinding/FindingRecord 15 字段逐字段语义、ConfirmOutcome 四值原文、IConfirmationEnvironment 三查询方向与临界区约束传导、IConfirmableFindingService 五方法逐字承载＋错误语义（结果枚举优先）、ConfirmableService 构造注入四参数契约（非拥有/生命周期/可空 devLog 接线登记）＋行为要点逐条＋私有辅助锁内约束；SA-15 装配面断言专段论证。
- **实现/测试文件抽查（3 个全读，≥3 达标）**：`Confirmable.cpp`（783 行：文件头 canonical 编码表逐字段登记〔magic/u32le/位模式小端归一/枚举值基线钉住/确定性论证〕；create 前置校验①~④逐步业务依据；复核序①~④与 reason 归类锚点；const_cast 合法性论证；string_view 拼接技术注记）；`ConfirmableTest.cpp`（778 行：文件头 acceptance 1~4 映射表＋用例名后缀＝矩阵行编号与 trace 呼应的说明；每 TEST 注释锚定矩阵行/需求/陷阱；断言处中文说明）；`ConfirmableProjectStubTest.cpp`（600 行：文件头 P-DIAG-8 桩边界专段＋编译前复核实现口径登记；桩类逐个注明复刻的 project 章节号）。
- **§2.5 高危信息**：单位——比较值经 `UnitToken::find("mm"/"deg")` 显式 token，无裸数值；坐标系/角度制式不适用（本任务无几何量/无裸角度字面量）；所有权——ConfirmableService 四个指针成员逐个注明"非拥有"，构造注入引用"须覆盖服务生命周期，本类不接管"；线程约束——m_mutex 短临界区（复核探针在临界区内、"实现必须短小、无阻塞"传导）、thread_local 引擎无锁并发、tryFind noexcept 与 bad_alloc 的 terminate 权衡说明；错误语义——Usage＝调用方契约违约 fail-fast vs ConfirmOutcome＝状态类失败返回（头文件专段"返回值与异常双轨：结果枚举优先"）；确定性——std::map 字典序遍历（NFR-COR-02）、编码表"全部编码无环境依赖"、枚举值稳定性由 P-DIAG-1 基线钉住。无中英混杂机翻腔、无过期注释、无裸 TODO。
- **微瑕（建议级 F-105）**：Confirmable.cpp L221-223 匿名命名空间内存在无对应函数体的三行 Doxygen 注释（"/// FNV-1a 128 位哈希（……此函数未用则不引入……）"）——放弃引入 FNV 路径后的自澄清残留，内容自洽、零行为影响，属 F-046/F-063/F-093 微观代码卫生家族。

### 4.8 文档同步 —— 通过

- 单元卡 `units/diagnostics.md` v0.5→v0.6：卡头"构建落位"字段实测刷新（Confirmable.hpp/cpp/两测试文件/13＋6 用例落位清单）＋§14.4 v0.6 变更记录——**八项实现口径按 DTB §5.4 登记**（①绑定复核环境探针 IConfirmationEnvironment〔§9.3 未定义"当前值"来源——实现类构造约定不扩接口签名，seal() 先例〕；②复核定性归类〔复核序＝四元组序，首个不符成员定性 reason〕；③invalidate 状态域〔Pending/Confirmed→Invalidated——Confirmed 失效＝编译前复核失配登记面，与 §5.4 状态图"回到 Pending"字面的取舍已登记〕；④Expired 态提交返回 InvalidState〔结果枚举无该值——口径登记〕；⑤reasonToken 透传不白名单校验；⑥confirmTextKey 派生与 optionKeys 等默认空；⑦callbackToken 一次性；⑧DT-CFM-8 装配面断言）＋接线登记（Dev 日志随 DIAG-T07、记录清理随 T09）＋四个 knownPitfalls 处置锚点。实现与任务卡零未登记偏差——**§9.3 五方法签名与 Confirmable.hpp 逐字比对一致**（验收者亲核 create/submitConfirmation/submitRejection/invalidate/tryFind/pendingFor 六签名＋ConfirmOutcome 四值）。
- 契约流水线状态：`state.json` 的 DIAG-T05 队列项（branch wp09-t05）在案，翻转归收尾（PIPE §4.7⑤——非实施者职责）。
- **findings.json 未关闭条目相关性核对（open 条目逐类排查）**：与本任务直接相关的既有 open 条目——**F-102/F-081/F-083/F-085/F-087/F-088/F-090 家族**（声称入库实际未入库：本任务为第 8 例且首次全部缺失，新登记 **F-103**）；**F-010**（.gitignore *.log 吞留痕——本次 7 份 .log 被 gitignore 吞且未 `add -f`，根因第 9 次复踩，并入 F-103）；**F-012 标题家族**（本任务第二提交名词短语开头——同型再现，新登记 **F-104**）；**F-007/F-009/F-062/F-098/F-099/F-101**（全新树构建环境链：本次复现再次实证 toolchain/Qt 前缀/ini 模板三条，已在 4.2 如实登记，处置权构建约定所有者，无可随手消账项）；**F-019/F-051/F-072**（ird_gates 基线红：本次 7 命中同分布再印证，零 diagnostics 命中，处置权 WP-01 所有者）；**F-046/F-063/F-093**（微观代码卫生家族：本次 FNV 注释残留再现，新登记 **F-105**）；**F-065**（留痕日志中文乱码：本次 ird_gates 输出复现同现象——环境编码面，与本任务产物无关）。其余 open 条目属 testkit/evidence/runtime/policy/io 等其他单元，与本任务无相关性、无静默积压。

### 4.9 commit 质量 —— 通过（附标题风格再现与留痕清单出入——转登 F-104/F-103，不构成数值粉饰）

- `af6b00b3` `[WP-09-T05] 实现 diagnostics 可确认诊断服务（FindingId/绑定四元组/状态机/失效条件）`：标题动词开头、≤50 字；正文四段齐备（What 按文件逐项/Why 逐条 acceptance 与陷阱映射/验证含构建·测试·门禁·探针真实结论/影响面"仅新增文件＋CMake 增列、目标名/别名不变、依赖边仍仅 core"）；`Acc-Covered: 1, 2, 3, 4` 尾注。
- `e98e693d` `[WP-09-T05] 单元卡 v0.6 落位登记与验证留痕入库（双模式 54/54＋9/9）`：正文四段齐备、如实说明纯文档与留痕性质；**标题以名词"单元卡"开头非动词**——F-012/F-100 家族同型再现（转登 F-104，建议级）。
- **出入（建议级，转登 F-103）**：e98e693d 正文声称入库"ird-test-report.json…＋4 份 gtest XML（两目标×两模式）＋8 份日志"，ird-test-report.json 的 logs/gtestXml 字段列出同一批 13 份文件——**git ls-tree @e98e693d 实证 builds/wp09-t05/ 仅 ird-test-report.json 1 件，13 份声称文件零入库**。其中 7 份 .log 在实施工作树本地存在但命中 `.gitignore:87 *.log`（check-ignore 实证——git status 不显示，未按 wp09-t03/t04 先例 `add -f`；F-010 根因第 9 次复踩）；4 份 XML 中 2 份在实施者主构建树 `gtest_reports/`（2026-09-14 13:19 生成）、2 份冒烟版无踪，均未落 wp09-t05/。方向为"声称在库而实际未入库"（F-081→F-102 家族第 8 例且首次全部缺失）；**数值主张（双模式 54/54＋9/9、既有 41＋3 零回归、门禁 7 命中基线逐码一致零 diagnostics、validate-task PASS、双探针失败自证）经验收者亲手复现全部准确，测试结果无粉饰**。
- 注记：送验 integration-build.log 未入库故其构建树口径无从核对（F-102 曾核为主树增量口径）——验收者已以独立从零配置构建补强（exit 0 零 error，见 4.2），口径缺口不掩盖。

### 4.10 留痕完整 —— 字面部分未达成（建议级——F-103；沿 F-081/F-083/F-085/F-087/F-088/F-090/F-102 家族七例既判先例不阻断）

- **在库**（git ls-tree @e98e693d，wp09-t05/ 共 1 件）：`ird-test-report.json`（schemaVersion ird-test-report/1，含 per-suite 用例 trace、regression 双模式 63/63、gates 基线对比与 deltaAnalysis、realFailureEvidence 双探针设计、日志索引——结构化 resultNote 载体本身完整准确）。
- **未入库而报告字段/提交正文引用在案（13 份）**：`logs` 字段所引 integration-build.log／verify-target-build.log／integration-test-run.log／smoke-configure.log／smoke-build.log／smoke-test-run.log／validate-task.log／gates-current-wp09t05.log 等 8 份日志＋`gtestXml` 字段所列 4 份 XML＋（提交正文并列声称的同一批）——全部缺失。7 份 .log 在实施工作树被 `.gitignore:87 *.log` 吞（未 add -f，F-010 根因第 9 次复踩）；4 份 XML 未落留痕目录。**证据本体不缺**：双模式 54/54＋9/9、ird_gates 7 命中、validate-task PASS 由验收者从零亲手复现一致（本记录 4.2/4.4）。
- `phase-one-validation.log`：自 WP-05 起即为历史载体（DIAG-T03/T04 验收记录同结论），本任务留痕按现行惯例落 `builds/wp09-t05/`，ird-test-report.json 即结构化 resultNote 载体——符合"phase-one-validation.log **或** resultNote 已更新"的替代口径。
- validate-task：验收者亲跑 `validate-task.ps1 -TaskFile <契约>` → **PASS (1 tasks)**，exit 0。

### 4.11 偷懒/缩水实现扫描 —— 通过

- ①**标记扫描**：diff 新增行 `TODO|FIXME|placeholder|未实现|not implemented` 计数 **0**；`stub` 字样全部为 `ConfirmableProjectStubTest.cpp` 文件名/桩类名/桩语义注释（P-DIAG-8 处置的 project §6.7 桩联动——契约 knownPitfalls 与单元卡 §10 明文载明的验证形态，非实现缩水标记），无需豁免条目；实现源文件 Confirmable.cpp 零命中。
- ②**空实现检测**：公共接口全部真实实现——create 为前置校验链（C-1 形态/带凭据拒绝/未注册码/废弃码/非可确认码/上下文四身份/空命令类型/空作用对象/非法 ObjectId）＋记录组装＋四元组冻结＋token 分配＋互斥插入（Confirmable.cpp:369-496）；submitConfirmation 为存在性→惰性过期→状态→token→绑定复核→Confirmed 写入六步（:498-547）；submitRejection/invalidate 含状态域与终态守卫（:549-631）；tryFind/pendingFor 为加锁查询＋惰性转移＋确定性过滤（:633-669）；armExpiration/expireIfDue/verifyBinding/finishTransition 均为真实逻辑。无空函数体/仅返回默认值的契约函数。
- ③**逐条证据表**：见 4.4——acceptance 4 条全部具名测试＋文件行号级证据，无一条仅凭"已实现"声明。
- ④**测试强度**：断言校验行为与边界（错误码面逐值断言且不相符即失败、binding 四元组逐成员冻结断言、reason 归类三路逐 member 断言〔revision-changed/policy-changed/binding-mismatch〕、token 一次性双向〔成功后作废＋失效后作废〕、终态不可逆异常、投影三态全覆盖、Dev 日志行逐字段〔channel/findingId/目标态/reason token〕、摘要确定性＋四字段敏感性、pendingFor 过滤＋确定性序＋规模断言）；无恒真断言；4.5 双探针实证非恒真。

---

## 二、裁决

**verdict ＝ pass**

- 4.1~4.11 全部通过（4.10 的留痕入库缺口与 4.9 的标题风格为建议级，沿 F-081→F-102 家族七例既判先例不阻断），无阻断级问题：范围零越界、构建/测试/门禁/探针全部亲手复现成功、红线零命中、注释无缺项（一处残留注释转登）、4.11 零命中、无数值粉饰。
- 新增建议级登记 3 项：**F-103**（送验提交与 ird-test-report.json 声称入库的 13 份留痕〔8 日志＋4 XML＋正文并列表述〕全部未入库——F-081/F-102 家族第 8 例且首次全部缺失；7 份 .log 被 .gitignore 吞未 add -f＝F-010 根因第 9 次复踩；证据本体不缺，验收者双模式亲跑一致）；**F-104**（e98e693d 标题名词短语开头——F-012 家族再现）；**F-105**（Confirmable.cpp L221-223 无函数体的 FNV-1a 残留注释——F-046/F-063/F-093 微观卫生家族）。
- 环境类既有条目 F-007/F-062/F-098/F-099/F-101/F-008/F-065 本次复现再次实证（toolchain/Qt 前缀/ini 模板补齐后零错误配置构建；ird_gates 基线 7 命中同分布；日志乱码同现象），已在册、不重复登记。
- 本验收通过的是 **SHA e98e693d187dbd613cfa2d2730fb9a24b6692940**，非分支引用；验收者未修改任何实施产物、未向 wp09-t05 推送任何提交。

## 三、问题清单

| 级别 | 描述 | 处置 |
| --- | --- | --- |
| 阻断 | 无 | — |
| 建议 | **F-103**：送验两提交声称 13 份留痕入库（e98e693d 正文"4 份 gtest XML＋8 份日志"、ird-test-report.json logs/gtestXml 字段），实际 builds/wp09-t05/ 仅 ird-test-report.json 1 件——F-081/F-102 家族第 8 例且首次全部缺失；7 份 .log 被 .gitignore:87 吞且未 add -f（F-010 根因第 9 次复踩）；4 份 XML 未落留痕目录（2 份滞留主构建树 gtest_reports/、2 份冒烟版无踪）。数值主张全部复现准确、无粉饰 | 已转登 findings.json（F-103，open）；处置建议见该条目；不阻塞合入 |
| 建议 | **F-104**：e98e693d 标题"[WP-09-T05] 单元卡 v0.6 落位登记与验证留痕入库…"以名词"单元卡"开头非动词——F-012 家族再现（af6b00b3 动词开头合规） | 已转登 findings.json（F-104，open）；不阻塞合入 |
| 建议 | **F-105**：Confirmable.cpp L221-223 匿名命名空间内"FNV-1a 128 位哈希（…此函数未用则不引入…）"三行 Doxygen 注释无对应函数体——放弃引入 FNV 路径的自澄清残留；内容自洽零行为影响 | 已转登 findings.json（F-105，open）；随下次触碰该文件的提交顺手删除；不阻塞合入 |

## 四、验收者环境说明

- 复现 worktree（worktree A）：`D:/acc-diag-t05-a`（detached @ e98e693d）；集成树＝主树缓存口径从零配置（`RWS_BUILD_INDUSTRIALROBOT=ON`、`RWS_USE_STATIC_LINK_PLUGINS=ON`、vcpkg toolchain、Qt `CMAKE_PREFIX_PATH`；`RobWorkStudio.ini.template.static` 自主树补拷——F-007/F-062/F-099 既有缺陷的环境修复，不触及送验产物）；未设 `CMAKE_CXX_FLAGS`（未做全树构建，未触发 F-008/F-098 所述 RWSimulatorPlugin 宏污染面）。冒烟树：`D:/acc-diag-t05-smoke`（独立目录＋toolchain，显式 Release）。
- 工具链：VS 2022（MSVC 19.42，生成器 Visual Studio 17 2022，-A x64）、CMake 4.3、vcpkg 经典模式（gtest 1.18.0 @ installed/x64-windows）、pwsh（validate-task.ps1）。
- 探针注入/还原均在 worktree A 内：两轮注入（verifyBinding tip 比对短路、expireIfDue Pending 短路）各自重建重跑取证后备份还原＋touch 强制重编（规避 F-068 mtime 陷阱），最终 `git status` 零内容残留、grep `PROBE-TEMP` 计数 0、54/54＋9/9 复绿。
- 验收记录与 F-103/F-104/F-105 登记落 `acc/DIAG-T05/1` 分支（基于 origin/redesign-main @ c155aa62）并推送；验收者日志/XML 随临时 worktree 清理，本记录内数值均为亲测。
