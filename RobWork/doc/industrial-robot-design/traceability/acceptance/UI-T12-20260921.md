# UI-T12 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | UI-T12（WP-10-T12）DraftController（未保存标记/草稿投影/会话状态机界面侧） |
| 送验对象（冻结） | `417cfa5ab3200a28d5d6fdcb19e77c25fd1a7f23`（分支 `wp10-t12`；base `e071a0a7317fc6c2be0b56ee8dbf78b3896ba858`＝state.base） |
| 冻结核对 | 验收期间 `wp10-t12`＝`origin/wp10-t12`＝送验 SHA，零分支漂移；验收全程按 SHA（detached worktree），未向任务分支推送任何提交 |
| 验收依据 | acceptance-protocol.md v1.5（ACC §2 独立性／§3 执行环境／§4 清单 4.1~4.11）；全新上下文，只消费产物与证据，构建测试全部亲手重跑 |
| **裁决** | **pass**（4.1~4.11 全部通过，零阻断级问题；3 条建议级问题已按 F-281/F-282/F-283 转登 findings.json，不阻塞合入） |
| 验收日期 | 2026-09-21 |

## 一、逐项证据（ACC §4，4.1~4.11）

### 4.1 改动范围 —— 通过

- `git diff --name-status e071a0a7..417cfa5a` 共 26 文件：ui 单元 9（`ui/CMakeLists.txt`、`contract_test/DraftContractTest.cpp`、`include/sdurws/ird/ui/IDraftController.hpp`、`UiPorts.hpp`、`UiProjections.hpp`、`UiSessionController.hpp`、`src/DraftController.cpp`、`src/UiSessionController.cpp`、`test/DraftControllerModelTest.cpp`）＋traceability 留痕 16＋`units/ui.md` 1——全部落在契约 `allowedFiles`（`ui/**`、`units/ui.md`、`traceability/**`）内。
- `forbiddenFiles`（REQUIREMENTS.md／ARCHITECTURE.md）零触碰；diff 内框架源码（RobWork/RWS）与 `patches/` 零文件——SA-02 无登记需求。
- 范围内提交 2 个：`3c51a1c5`（[WP-10-T12] 实现＋测试＋CMake＋留痕同一提交）、`417cfa5a`（[units] ui.md v1.4 落位登记）。

### 4.2 可复现构建 —— 通过（双模式从零亲手复现）

- **集成模式**：worktree A（`D:/10_Source_Repos/21_robot/RobWork/acc-ui-t12-wt-a`，`git worktree add --detach @ 417cfa5a`）。全新配置 `cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=<主树>/vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`——CONFIGURE_EXIT=0（Configuring done，零 CMake Error）；缓存 grep 实证 **`RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`**。worktree 缺主树未跟踪本地辅助件 `RobWorkStudio.ini.shared.in`／`RobWorkStudio.ini.template.static`（F-007/F-062/F-099 在册缺陷——UI-T11 验收同先例），自主树补拷至 worktree `RobWork/RobWorkStudio/bin/`（环境修复，不触及送验产物）。
  - `cmake --build build --config Release --target sdurws_ird_ui_test` → **UI_TEST_BUILD_EXIT=0**；`--target sdurws_ird_ui_contract_test` → **CONTRACT_BUILD_EXIT=0**；另建 `sdurws_ird_ui_gui_test` → EXIT=0。三日志 `error C[0-9]／fatal error／error LNK` grep 计数均 **0**。
- **独立冒烟模式**：`acc-ui-t12-smoke`（源取 worktree A 同 SHA 的 `industrialrobot/` 目录，带 vcpkg toolchain 绝对路径＋Qt 前缀）——配置 **SMOKE_CONFIGURE_EXIT=0**、全量构建 **SMOKE_BUILD_EXIT=0**（error 计数 0）。
- 注：vcpkg 目录为 git 未跟踪的主树本地产物（git 树内无 `vcpkg/`），worktree 复现须引用主树 toolchain 绝对路径——环境事实，与送验留痕口径一致（README-evidence.md 同样记录"toolchain 绝对路径"）。

### 4.3 契约 verify 命令 —— 通过（逐条独立执行）

1. `cmake --build build --config Release --target sdurws_ird_ui_test`：exit 0（集成树亲跑）。
2. `cmake --build build --config Release --target sdurws_ird_ui_contract_test`：exit 0（集成树亲跑）。
- 测试 exe 亲跑（QT_QPA_PLATFORM=offscreen）：集成树 `sdurws_ird_ui_test.exe` → **119/119 PASSED**（exit 0，约 60 ms）；`sdurws_ird_ui_contract_test.exe` → **12/12 PASSED**（exit 0）；冒烟树两 exe → **119/119＋12/12 PASSED**——与送验留痕（ird-test-report.json 119/0、contract 12/0）同口径一致。
- 本地验收运行 XML：worktree A `reports/sdurws_ird_ui_test-acc.xml`（119/0）＋`sdurws_ird_ui_contract_test-acc.xml`（12/0）＋同 `-acc-smoke` 两件（119/0、12/0）。
- GUI 回归确认面（非契约 verify 行，如实附注）：验收者 offscreen 环境复跑 `sdurws_ird_ui_gui_test.exe` → 23/28，5 个失败全部为 WorkbenchShell（三件布局几何——offscreen 插件不支持 propagateSizeHints）、CommandPalette、PolicySummaryCard 既有面环境断言，**零 UI-T12 交付面用例**（草稿控制器无 GUI 用例——本任务交付为模型层）；送验环境中失败的那 1 件剪贴板用例（BatchPasteImpactDetail_UI_T08_ACC1）在验收者环境通过。两环境失败集互异且均不在本任务改动面，与送验"环境失败如实登记、testkit §7.5 不记通过"的定性自洽，不构成粉饰。

### 4.4 acceptance 条目 —— 通过（3/3，逐条具名证据）

| 契约 acceptance 条目 | 具名证据（均在验收者亲跑 119＋12 内 PASSED；行号＝测试文件实测行） |
| --- | --- |
| ①UI-DRF-1~4：保存/应用分离（save 只落 drafts/ 零修订、未保存标记两源合并、跨模块汇总、定时保存〔UI 线程零磁盘 IO/落盘线程串行/60 s 默认〕、.bak 恢复与横幅——§8.3-5 桩模块验证控制器协议） | 分离红线：`ManualSaveLandsDraftWithZeroRevision_PM04`（DraftControllerModelTest.cpp:412——savedCount=1、origin=Manual、载荷保真、**修订计数恒零**〔harness 零写入者＋控制器无命令通道〕、成功清脏、执行器队列排空）；标记合并：`UnsavedMarkerMergesSessionAndDisk_PM11`:440（仅会话脏→true、保存后磁盘 present→true、全部消费→false）；汇总：`CrossModuleSummaryMergesRowsAndSession`:460（挂接序＋孤儿追加＋stale/基线直读）；定时：`AutosaveDefaultIntervalAndTickGuards`:499（**默认 60 s 冻结断言** 506 行＋双触发防御 516/523 行＋非正周期拒绝 528 行）、`AutosaveThreadDisciplineAndFailureDiagnostic`:538（替身端口入口 `EXPECT_TRUE(ManualDiskExecutor::inDiskThread())` 191/212 行——**UI 线程零磁盘 IO 的执行面自证**；失败保留脏＋UI-DRAFT-AUTOSAVE-FAILED 目录 1 条＋Dev 通道）、`AutosaveInFlightDefersNewDirtyToNextCycle`:570（在途零分派＋代次守卫保留脏＋新周期新快照 v1→v2）；同步结局与只读拒绝：`ManualSaveSyncAndReadOnlyRejections`:603；恢复：`RestoreOnOpenRecoversBackupAndBanner`:640（.bak 回退进域源＋孤儿汇总＋横幅三动作固定序＋[放弃]可用位）、`CorruptDraftBannerAndDiscardDiscipline`:688（损坏清单＋显式 discard 写轨＋只读拒绝）——契约另明确 PM-04-S1 用户可调归 WP-04-T20 不提前实现，代码仅编程面接受正周期（526 行），口径一致 |
| ②StaleRevisionRejected 呈现（RV-10/§8.5：tip vs 草稿 baseRevision 对照、草稿保留 apply-retained、可继续编辑、不自动重试提交；两栈互不越界 §8.7） | `StaleRevisionRetainsDraftAndPresentsConflict`:730（拒绝后 anyUnappliedChanges 仍 true＝草稿保留；staleConflictData 逐字段——tip/base/摘要/涉事对象/三动作键 756~765 行；**零 save 零修订** 768~769 行＝不自动重试的行为面＋结构面双证）；`ReEditOnCurrentTipRebuildsBaseline`:776（无冲突调用拒绝 793 行；重建后冲突态清空＋脏重置）；两栈：`LocalUndoStackBoundToDraftNotRevisions`:812（检查点/撤销/重做回放零命令零修订 834~836 行；**应用成功清栈→undo 返回 false** 843 行＝"局部撤销不能撤销已应用修订"执行面；Aborted 草稿原样保留栈不动 849~854 行）；契约面：`StaleDetailPassesThroughProjectedInterfaces`（DraftContractTest.cpp:313——O-31 虚派发自证＋逐字段透传） |
| ③O-31 处置（已裁决随整链放行：C-3/C-4/C-5 对端类型经 ui 自有草稿/命令/查询端口＋值投影承载，产品面零对端链接/include；RV-10/§8.7 语义零变化） | 结构自证：新公共头 include 全集＝std＋core/Identity、diagnostics/Catalog・Factory、ui 自有四头（验收者逐文件 grep 实证，零 project/evidence/execution/policy/runtime）；常驻守卫 `NoCrossUnitInclude_O31_UI_BUILD` 验收者亲跑 PASSED（119 内）；`NoRobWorkPrefixLiteral_R4_UI_BUILD` 亲跑 PASSED；新增产品文件零 `RobWork` 字面量（grep 实证）；门禁命中集验收者独立复跑与 base **逐码零变化**（见 4.6）＝零表外边零例外面扩大；两栈边界语义由 ①② 具名用例钉住零变化 |
- 未执行项：无——三条 acceptance 全部有具名测试与命令输出；`ird_gates` 非 acceptance 行，已随 4.6 亲跑（非"未执行"项）。

### 4.5 测试真实失败能力 —— 通过（两处变异构造，均如期变红，已还原复绿）

1. **MUT-1**（`src/DraftController.cpp` saveAll origin 映射改为恒 `DraftOrigin::Autosave`）：重建后 `--gtest_filter=DraftControllerModelTest.ManualSaveLandsDraftWithZeroRevision_PM04` → **FAILED**（DraftControllerModelTest.cpp:427 origin 冻结断言）——保存 origin 语义真实被测。
2. **MUT-2**（`kDefaultAutosaveInterval{60}` → `{61}`）：重建后 `AutosaveDefaultIntervalAndTickGuards` → **FAILED**（:506 默认 60 s 断言，连带 :521/:524 双触发时序）——PM-04-S1 默认周期真实钉住。
3. 还原与复绿：两处变异逐一还原（`git status --porcelain` 对 industrialrobot/ **零残留**，仅验收者自建 `reports/` 未跟踪目录）→ 重建 exit 0 → 双 exe 复跑 **119/119＋12/12 全绿**。

### 4.6 架构红线（AGENTS §5 八条）—— 通过

1. **L2 零 Qt**：新增/改动产品文件（DraftController.cpp、IDraftController.hpp、UiPorts/UiProjections/UiSessionController 增量）include 全集为 std＋core/diagnostics/ui 公共头，零 `Q` 头（逐文件 grep 计数 0）；ird_gates R-3 命中验收者独立复跑 33 项与 base 逐码一致——新增文件**零新增命中**。
2. **R-1 业务域互链**：无 modeling/requirements/kinematics/trajectory/dynamics/selection/optimization 依赖；协作全部经 ui 自有端口（C-5 写半区 IUiDraftStorePort 本任务首消费冻结）＋值投影；SUB 命中 39 项与 base 一致零新增。
3. **R-2 跨单元私有头**：include 只指向 `include/sdurws/ird/<unit>/` 公共头（core/Identity、diagnostics/Catalog、diagnostics/Factory——均公共面）；实现类 DraftController 不进公共头（createDraftController 唯一构造面）。
4. **SA-02 框架零修改**：diff 零框架文件、patches/ 零新增（见 4.1）。
5. **R-4 名称前缀**：新增/触碰产品文件 `RobWork` 字面量 grep 计数 0；`NoRobWorkPrefixLiteral_R4_UI_BUILD` 亲跑通过；名称解析语义未在本单元实现。
6. **PA-1 权威唯一**：修订与写路径未在本单元实现——save 仅经写半区端口落 drafts/（对端 DraftService 权威）；控制器结构上不持有命令网关（应用回执只消费不提交）；单元/角色/策略/诊断文案语义零越权（诊断码 UI-DRAFT-AUTOSAVE-FAILED 经工厂注册通道产码）。
7. **PA-2 不可变历史**：本任务零修订数据结构触碰；局部撤销栈为会话内存栈（应用成功清空、不回写历史）。
8. **不恢复 old/**：diff 与新增文件 `old/` 引用 grep 计数 0。

### 4.7 注释规范 —— 通过

- **公共头全查**：
  - `IDraftController.hpp`（新，806 行）：文件头（设计依据 §8 全节/§10.5/PM-04/PM-08/PM-15/RV-10/PM-11/PM-18/PM-12＋契约 acceptance 1~3＋O-31 处置＋背景说明"控制器为何不提交命令"＋线程模型＋生命周期/所有权）；逐类型/逐方法 Doxygen 中文（@brief/@param [in] 含取值语义/@return 含失败行为/@throws 含触发条件）；§2.5 高危信息齐备——单位（interval 秒显式、ISO-8601 UTC）、坐标系语义锚（canonical 文本对照值零二次加工）、所有权（unique_ptr 应用壳持有/域源弱引用＋显式 detach/端口 shared_ptr 按值捕获覆盖在途存活期）、线程约束（公共方法 UI 线程、落盘任务串行执行器、Marshal 回投）、错误语义二分（调用方违约 fail-fast 异常／环境错误返回值轨＋稳定码）、确定性来源（steadyClock 单调钟/同批次共用 savedAtUtc/稳定排序纪律）。
  - `UiPorts.hpp` 增量（IUiDraftStorePort）：语义冻结三行（save 零修订/tryLoad .bak 回退/discard 幂等）＋线程约束段＋逐方法 Doxygen。
  - `UiProjections.hpp` 增量（草稿投影族＋StaleRevisionDetailProjection＋DraftRowProjection 三增列）：字段级语义锚 project.md §5.4/§4.4.5/§6.2，presence 纪律与"既有字段零变化"明示。
  - `UiSessionController.hpp` 增量（reportSessionDirty）：生产者接线语义＋写点纪律说明＋@param 方向。
- **实现文件抽查（≥3 达成，实际 4 个全读）**：`DraftController.cpp`（文件头 review 对照表＋逐段"为什么"注释——脏代次守卫、双触发防御、异常经 promise 通道重抛禁吞错、魔法数全有出处〔60 s＝PM-04-S1、64＝§4.1 词法、48＝diagnostics 通道词法〕）；`UiSessionController.cpp` 增量段；`DraftControllerModelTest.cpp`/`DraftContractTest.cpp`（用例名＋IRD_TEST_INFO 需求/AT 追溯字段＋替身设计说明）。
- 修饰级小疵（不影响规范符合性）：DraftControllerModelTest.cpp:862~904 一处测试注释块被同文件相邻类定义分隔（文本连续可读、语义完整）；已并入 F-281 同批修饰面留档，不单独立项。

### 4.8 文档同步 —— 通过（含 findings 相关性核对）

- `units/ui.md` v1.4 增量修订（提交 417cfa5a）：版本行（:8）＋§3.3 落位行（:220）＋**§13 UI-T12 落位登记注（:1576，六面逐条）**＋**§16.7 v1.4 行（:1766）**＋§10.5 实现冻结登记注——实现与任务卡零偏差；restoreOnOpen 对端形态按 O-31 改会话绑定注入已按"首消费冻结"机制增量登记（口径与契约 acceptance 3 一致）；需求语义零变化（PM-04/PM-08/PM-11/PM-15/RV-10/PM-18 按 §8/§12.3 原文落位）。
- 任务契约零改动（状态流转归治理合入收尾——UI-T11 同口径）；追踪矩阵无新增需求行（本任务承接既有 PM-04 族，无语义变更）。
- `traceability/findings.json` 未关闭条目相关性核对（244 open 全量筛查）：与本任务直接相关 3 族——**F-278**（UiPorts.hpp 尾换行：本任务增量触碰该文件，尾字节实测 0x50 未恢复——复踩）、**F-280**（UiSessionController.hpp 尾换行：本任务增量触碰，尾字节实测 0x50——复踩）、**F-279 家族**（.gitignore *.log 根因：本任务 README-evidence.md 索引再次引用 .console.log／ird_gates-*.log／validate-task.log／batchpaste-rerun*.console.log 等未入库留痕，实际入库 16 件——第 7 例）。三条均为建议级、可顺手消账，已按 F-281/F-282/F-283 顺延转登（见本记录第三节）。可随本任务顺手消账项：无（三处触碰文件尾换行均未恢复，反向构成复踩登记；无已修复条目需置 fixed）。

### 4.9 commit 质量 —— 通过

- `3c51a1c5`：标题 `[WP-10-T12] 实现 DraftController：…`（动词开头、≤50 字）；正文四段齐备（What 按文件列要点／Why 对应 acceptance 1~3＋O-31／How 含双模式构建、119＋12＋27/28、门禁零变化、validate-task PASS／影响面新增为主＋DraftRowProjection 增列带默认值零既有消费者改动）。
- `417cfa5a`：`[units]` 前缀文档修订，符合 AGENTS §6.2 前缀表。
- 交叉核验无粉饰：提交声称的构建/测试/门禁/validate-task 结论全部经验收者独立复现属实（119＋12 双模式、命中集逐码零变化、PASS(1 tasks) 实测 PASS）；GUI 27/28 的 1 项环境失败如实登记（见 4.3 复现注）。唯一出入＝README/正文索引的 .log 未入库（F-010 根因既有家族问题，XML＋JSON 留痕完整、结论可复现，不构成粉饰——F-283 登记在案）。

### 4.10 留痕完整 —— 通过

- `traceability/builds/wp10-t12/` 入库 16 件：README-evidence.md 1＋gates-hit-set 3（integration/smoke/diff）＋ird-test-report.json 6（ui/contract/gui × 集成/smoke）＋gtest XML 6（同名）。
- 实测内容与声明一致：ird-test-report ui=119/0、contract=12/0、**gui=28 用例 1 failed（BatchPasteImpactDetail_UI_T08_ACC1，环境失败如实登记不记通过）**；gates-hit-set.txt＝LIB×8/R-1×1/R-3×33/R-4×3/R-5×1/SUB×39/T-1×5/T-2×1，gates-hit-set-diff.txt 明载 base（e071a0a 同源 wp10-t11）逐码零变化。
- `phase-one-validation.log`／resultNote：本任务不涉阶段一验证链（无 phase-one-validation 更新义务）；流水线状态经 state.json（attempts.accept=1）登记，符合 PIPE 口径。

### 4.11 偷懒/缩水实现扫描 —— 通过

1. **标记扫描**：diff 范围产品面（ui/src＋ui/include）`TODO|FIXME|stub|placeholder|未实现|not implemented` 计数 **0**；test/contract_test 命中 18 处全部为 `Stub*/Memory*` 测试替身命名（§3.1"ui 测试以可控替身承载"契约口径——非未实现标记，豁免依据即替身本体）。契约显式"触发式延后"清单：PM-04-S1 用户可调 60–600 s 归 WP-04-T20（代码注释与测试 526 行双处明示不提前实现）、externalRefs 投影随阶段 B 首消费冻结（UiProjections.hpp 注释明示）——均契约在案，非缩水。
2. **空实现检测**：IDraftController 全部 21 个虚方法在 DraftController.cpp 有实质实现（公共接口无空体/仅返回默认值者——逐方法核对；`corruptRecovered()` 为声明的纯派生位）。
3. **逐条证据表**：见 4.4——三条 acceptance 逐条具名测试＋行号，无"已实现"声明依赖。
4. **测试强度**：断言校验行为与边界（origin/载荷/时刻值冻结、双触发时序、代次守卫、只读拒绝异常、幂等 discard、横幅动作键固定序、冲突数据逐字段）；非恒真——4.5 两处变异实测即红。

## 二、问题清单

**阻断级：无。**

**建议级（已转登 findings.json，不阻塞合入）：**

| 编号 | 摘要 | 责任方 | 状态 |
| --- | --- | --- | --- |
| F-281 | F-278 复踩：`UiPorts.hpp` 本任务增量修改后文件尾换行仍未恢复（尾字节实测 0x50 'P'） | ui 实施者（下次触碰时恢复） | open |
| F-282 | F-280 同族复踩：`UiSessionController.hpp` 本任务增量修改（reportSessionDirty）后尾换行仍未恢复（尾字节实测 0x50）——建议两文件与 F-278 一并一行修复消账 | ui 实施者（同上） | open |
| F-283 | F-010/F-279 家族第 7 例：README-evidence.md 索引引用多类 .log（.console.log／ird_gates-integration.log／ird_gates-smoke.log／validate-task.log／batchpaste-rerun*.console.log），实际入库 16 件零 .log（.gitignore 全局 *.log 根因未除）；验证结论本身经本验收独立复现全部属实，不构成粉饰 | 治理（F-010 根因）＋实施者（索引与入库清单对齐） | open |

## 三、验收者环境说明

- 复现 worktree A：`D:/10_Source_Repos/21_robot/RobWork/acc-ui-t12-wt-a`（detached @ 417cfa5a）；冒烟树：`D:/10_Source_Repos/21_robot/RobWork/acc-ui-t12-smoke`（源＝worktree A 同 SHA 的 industrialrobot/ 目录）；记录 worktree B：`acc-ui-t12-wt-b`（分支 `acc/UI-T12/1`，基于 `origin/redesign-main` @ e071a0a7）。
- 工具链：VS 2022（Visual Studio 17 2022，-A x64）、vcpkg 经典模式（gtest @ installed/x64-windows，toolchain 引主树未跟踪 vcpkg 绝对路径）、Qt 6.11.1（测试运行 QT_QPA_PLATFORM=offscreen；GUI 复跑另需 QT_PLUGIN_PATH 指向 Qt plugins）、pwsh 7（validate-task.ps1 → PASS 实测）。
- 变异注入均在 worktree A 内：两处变异逐次重建取证，逐字还原＋重建，最终 industrialrobot/ 目录 `git status --porcelain` 零修改、119＋12 复绿。
- 分支漂移：验收期间 `wp10-t12`＝`origin/wp10-t12`＝送验 SHA（本地引用核对）；evidence 记录落 `acc/UI-T12/1` 并推送 `origin`（推送结果见下）。
- **evidence 推送状态**：本记录＋F-281/F-282/F-283 登记已 commit 于本地分支 `acc/UI-T12/1` 并推送 `origin`（若网络不可达，worktree B 与分支保留未删除，由编排者按 UI-T11 先例补推）。
