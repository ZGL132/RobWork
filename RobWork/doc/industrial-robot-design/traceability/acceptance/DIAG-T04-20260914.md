# DIAG-T04 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 ID | DIAG-T04（≙WP-09-T04 部分，diagnostics 单元：诊断目录与工厂 Catalog/Factory/ErrorCodeTranslator） |
| 契约 | `doc/industrial-robot-design/tasks/foundation/DIAG-T04.json`（status ready；branch wp09-t04；requirements ERR-01/UX-03/PM-08/PM-15/TASK-02；knownPitfalls P-DIAG-1/P-DIAG-5/CR-08） |
| 送验对象（冻结） | `6dd6e665168d03fbe3f487e9c9fac3dc5839035e`（验收全程复核：`origin/wp09-t04`＝本地 `wp09-t04`＝该 SHA，**无分支漂移**） |
| diff 范围 | `340e75968099245547c13cd2df096c1fcf4d052f`（state.base＝redesign-main 尖端）..`6dd6e666`，共 2 提交（af5b7ee7 实现＋6dd6e665 留痕）、19 文件（+3406/−96） |
| verdict | **pass**（4.1~4.11 全部通过，无阻断级问题；新增建议级登记 1 项＝F-102，不阻塞合入） |
| 验收日期 | 2026-09-14 |
| 验收者 | 独立验收会话（全新上下文；未消费实施叙述；对抗式立场——复现优先于采信，下述构建/测试/门禁/探针均为验收者亲手执行） |

---

## 一、逐项证据（ACC §4.1~4.11）

### 4.1 改动范围 —— 通过

- diff 文件清单（`git diff --name-status 340e7596..6dd6e666`，19 个）逐个比对契约 `allowedFiles`：
  - `RobWork/RobWorkStudio/src/rwslibs/industrialrobot/diagnostics/**`（10 个）：CMakeLists.txt（M）、include/.../Catalog.hpp（A）、include/.../Factory.hpp（A）、src/Catalog.cpp（A）、src/DiagCodes.cpp（M）、src/EntryDetail.hpp（A，私有头）、src/Factory.cpp（A）、src/ParamSchema.cpp（A）、src/ParamSchema.hpp（A，私有头）、test/CatalogFactoryTest.cpp（A）；
  - `RobWork/doc/industrial-robot-design/units/diagnostics.md`（M，1 个，allowedFiles 第 2 项）；
  - `RobWork/doc/industrial-robot-design/traceability/builds/wp09-t04/**`（A，8 个留痕文件，allowedFiles 第 3 项）。
- `forbiddenFiles`（REQUIREMENTS.md / ARCHITECTURE.md）：diff 范围内零命中。
- SA-02（框架零源码修改）：diff 范围内无框架源码改动；`git diff --stat <range> -- '**/patches/'` 空输出（patches/ 零新文件、PATCHES.md 零改动）。
- **范围越界：零。**

### 4.2 可复现构建 —— 通过（验收者亲跑；环境差异如实说明）

- **复现现场**：`git worktree add --detach <临时目录A> 6dd6e665168d03fbe3f487e9c9fac3dc5839035e`（detached @ 送验 SHA）。
- **集成模式（干净构建树，重新配置）**：按主树缓存实际口径从零配置（`-DRWS_BUILD_INDUSTRIALROBOT=ON -DRWS_USE_STATIC_LINK_PLUGINS=ON -DBUILD_SHARED_LIBS=OFF`＋vcpkg toolchain＋Qt `CMAKE_PREFIX_PATH`＋`CMAKE_CXX_FLAGS="-DNOMINMAX -DWIN32_LEAN_AND_MEAN -DWIN32"`——后者来自主树 `build/CMakeCache.txt` 实值），配置 exit 0，缓存确认 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`。
  - **首次尝试的环境走弯（如实登记，非本任务缺陷）**：①初配因 `RobWork/RobWorkStudio/bin/RobWorkStudio.ini.template.static` 缺失失败于 RobWorkStudio/src/CMakeLists.txt:58——该文件为主树**未跟踪**本地辅助件（F-007/F-062/F-099 既有登记），自主树补拷后配置通过；②未带 `CMAKE_CXX_FLAGS` 的全量构建以 73 处 error 失败，**全部**位于框架目标 RWSimulatorPlugin.vcxproj（71）＋rwplugin.vcxproj（2）——Math.hpp min/max 宏污染簇，与 **F-008/F-098** 登记的基线缺陷同分布，ird 面零命中；③按 F-101 教训删除 build 树、带主树完整参数从零重配重建。
  - **最终结果：全树 `cmake --build build --config Release` 从零构建 exit 0、零 error C／fatal error**（强于送验留痕的主树增量口径——送验 integration-build.log 路径为主树 `D:\...\RobWork\build\`，见 4.9 注记）。`sdurws_ird_diagnostics`（STATIC 增列 3 源文件）、`sdurws_ird_diagnostics_test`、`sdurws_ird_diagnostics_contract_test` 全部编译链接成功。
- **独立冒烟模式（另配临时目录，带 toolchain——F-002/CORE-T01 G-2 口径）**：`cmake -S RobWork/RobWorkStudio/src/rwslibs/industrialrobot -B <临时目录> -DCMAKE_TOOLCHAIN_FILE=.../vcpkg.cmake` 配置 exit 0、`cmake --build ... --config Release` **exit 0 零 error**（显式 Release——规避 F-055 配置口径缺口；送验 smoke-build.log 亦 17 处 Release/0 处 Debug）。
- **测试执行（验收者亲跑，双模式，两目标）**：
  - 集成：`sdurws_ird_diagnostics_test.exe` → **41/41 PASSED**（exit 0）；`sdurws_ird_diagnostics_contract_test.exe` → **3/3 PASSED**（exit 0）。
  - 冒烟：冒烟树两可执行文件 → **41/41 PASSED**＋**3/3 PASSED**（exit 0）。
  - 与送验留痕 `ird-test-report.json` 的 regression 字段（integration 44/44、smoke 44/44＝41＋3）一致。

### 4.3 契约 verify 命令 —— 通过

契约 `verify` 数组仅一条，验收者在复现 worktree 根目录逐字亲跑：

```
cmake --build build --config Release --target sdurws_ird_diagnostics_test   → exit 0
```

产物 `build/RobWorkStudio/bin/Release/sdurws_ird_diagnostics_test.exe` 成功生成并直接运行（41/41，见 4.2）。

### 4.4 acceptance 条目逐项证据 —— 通过（4 条全部有证据；ird_gates 如实登记基线命中而非虚标通过）

| # | acceptance 条目 | 证据 |
| --- | --- | --- |
| 1 | DT-DIAG-1/2、DT-DUP-1/2、DT-CAT-1/2 用例通过：字段完整性（比较型三要素/不适用显式标记）、subject 边界（用户级码缺 subject 拒绝）、去重（不同作用对象绝不合并——DT-DUP-2）、分类区分（取消=Info 且正常取消非错误——UX-03/TASK-02） | 亲跑两模式 41/41 全绿。`DtDiag1_EntryFieldsCompleteFromRegistry`（CatalogFactoryTest.cpp:177，分类/严重自码表、entryId 单调、dedupKey/orderKey 已赋、注入时钟、contractVersions 版本标注）；`DtDiag1_ComparisonRequiredAndNotApplicableMarker`（:219，requiresComparison 缺三要素→ComparisonMissing；期望侧 `SourcedValue::notApplicable()` 显式标记＝state 与取值通道双断言——"不伪造数值"）；`DtDiag2_SubjectBoundary`（:279，用户级缺 subject/全零 subject→SubjectMissing，Dev 码可空）；`DtDup1_SameKeyOccurrencesCounted`（:423，同键×3 折叠计数）；`DtDup2_DifferentSubjectsNeverMerged`（:453，同码两 subject 两条独立＋subject 各自保留）＋`DtDup2_DifferentTaskScopeNotMerged`（:476，跨任务不合并）；`DtCat1_InputInvalidVsExecutionFailed`（:503）；`DtCat2_TerminalKindsDistinct`（:527，取消=Canceled/Info/actionKind none——正常取消非错误、失败=Error、中断=Info/rerun-interrupted、超时=Error，四终态 (category,severity) 两两互异集合断言） |
| 2 | ErrorCodeTranslator 骨架与 PRJ/RT/POLICY/EVI/EX 映射就位；CR-08 处置（禁字符串匹配、不跨边界抛对方异常类型——§8.1） | `Factory.hpp:336` ErrorCodeTranslator＝DiagnosticsFactory 别名视图（§9.2 创建与转译同接口——实现口径登记单元卡 v0.5 ①⑥，§8.8"映射数据归域"）；`DtCr08_RegisteredTypeTranslatesToStableCode`（:602，已登记类型→RT-ROBWORK-ERROR＋causedBy 链接＋subject 继承）；`DtCr08_UnregisteredTypeFallsBackNoThrow`（:631，未登记类型→DIAG-REGISTRY-UNKNOWN-CODE 兜底不抛、来源类型名保留）；`DtCr08_RegistrationGuardsAndStageAAudit`（:677，`registeredTranslations()` 审计面含 std::exception→RT-ROBWORK-ERROR＝§8.3 RT 族具体映射；重复登记 DuplicateCode/未注册目标码 CodeUnknown/seal 后 Usage）；`DtCr08_RootlessUserLevelTranslationRejected`（:709）。"就位"口径＝设施＋注册协议＋RT 具体映射＋审计面，PRJ/EX 逐值映射归域装配、POLICY/EVI 直接产码无异常转译行——已按 DTB §5.4 登记单元卡 §14.4 v0.5 ⑥（§8.2/§8.5 的对端错误枚举类型尚未存在，type_index 注册面无法逐值表达，NFR-MNT-04 不预建无消费者映射行）；禁字符串匹配＝`type_index` 键机制承载（Factory.cpp:234-249 查找序全静态类型判据）；不跨边界抛＝translate 就地转译不重抛（translateDispatch 无 rethrow 路径，代码走读证实） |
| 3 | P-DIAG-5 处置：IDiagnosticSink（§9.7）为统一 sink 标准接口，本任务保证 report/reportDev 形态兼容；最终统一待其详设修订（P-PR-6/P-EX-8），不私改对端契约 | `Catalog.hpp:398-414` IDiagnosticSink 四方法＝§9.7 原文签名；`DiagnosticsSinkImpl`（:521-579）report＝factory.create＋catalog.append（sourceUnit 取注入宿主标识——§9.7 尾注）、reportDev＝IDevLogSink::logDev 路由且 Dev 不入目录；`DtPdiag5_ReportReportDevShapeCompatible`（CatalogFactoryTest.cpp:741，report 入目录且上下文锚定宿主标识、reportDev 走日志不产生目录条目、IDiagnosticSink 面直通）；对端头零 include（跨单元 include 扫描仅 ird/core＋本单元——R-1/R-2，不私改对端实证） |
| 4 | P-DIAG-1 处置：DiagnosticEntry 信封内嵌 core::DiagnosticRecord——以 core.md v0.1 为基线，core 冻结 diff 后增量同步 | `DtPdiag1_EntryEmbedsCoreRecordBaseline`（:834）`static_assert` 三连钉住 `DiagnosticEntry::record`＝`core::DiagnosticRecord`、`DiagContext::task`＝`optional<core::TaskIdentity>`、`DiagContext::snapshotId`＝`optional<core::ContentIdentity>`（编译期契约基线证据）；实现侧 Catalog.hpp:257 record 字段原样内嵌不扩充（create 组装处 Factory.cpp:162 注释锚定 P-DIAG-1） |

**ird_gates（如实登记）**：验收者亲跑 `cmake --build build --config Release --target ird_gates`（复现 worktree 集成树）→ exit 1，**命中 7 项与 wp09-t03 合入基线逐项一致、零新增**：IRD-GATE-SUB×5（evidence_test/evidence_contract_test/policy_test/policy_contract_test/runtime_test → sdurws_ird_testkit 直链，F-072/F-051 族既有）＋IRD-GATE-SUB 表外边×1（sdurws_ird_testdata_lint → testkit，F-019）＋IRD-GATE-T1×1（sdurws_ird_testdata_lint 产品目标链 testkit，F-019）；（输出中另见的 IRD-GATE-R1/R3/R4/R5/T2 各 1 次为引擎自测"按预期检出"行，非命中）。**diagnostics 相关命中＝0**（输出全文检索 diagnostics 计数 0）。exit 1＝基线既有命中的预期行为，处置权 WP-01 所有者。与送验 `gates-current-wp09t04.log` 一致。

### 4.5 测试真实失败能力（验证验证者）—— 通过（两探针亲手构造、真实变红、还原复绿零残留）

验收者在复现 worktree 的实现文件临时注入缺陷、重建重跑、逐字还原（还原后 `git status` 零残留、grep `PROBE|false &&` 零命中，41/41＋3/3 复绿）：

- **探针 1（subject 边界短路）**：`src/Factory.cpp:118` 检查⑤改为 `if (false && userLevel && ...)`——`DtDiag2_SubjectBoundary` 与 `DtCr08_RootlessUserLevelTranslationRejected` **真实变红**（2 FAILED，恰为送验留痕 realFailureEvidence 预测的同两用例）。
- **探针 2（去重折叠失效）**：`src/Catalog.cpp:195` 去重查找键注入哨兵（`probeKey.scopeId += "PROBE"`——查找永不命中，折叠分支死路；首注 `if (false && auto it=...)` 为非法 C++ 编译失败，与送验留痕记载的同一教训一致）——`DtDup1_SameKeyOccurrencesCounted` **真实变红**（occurrences=3 断言失败，1 FAILED）。
- 两探针均证明断言非恒真、测试校验真实行为；与送验留痕 `realFailureEvidence` 字段的双探针设计相互印证。

### 4.6 架构红线（AGENTS §5 八条逐条）—— 通过

1. **L2 计算内核零 Qt**：单元 include 全集扫描＝std 头＋gtest（仅测试目标）＋`sdurws/ird/core/{DiagData,Identity,Digest,Provenance,Units}.hpp`＋本单元头，Qt 字样仅存在于 BuildRedLineTest.cpp 的红线扫描器自身（其职能即拦截 `#include <Q`）；既有 `NoQtInclude_DT_BUILD` 用例两模式亲跑通过。
2. **业务域互链禁止（R-1）**：产品库仅 PUBLIC 链 `sdurws_ird_core`（CMakeLists 增列 3 源文件未改链接边）；ird_gates R-1 零 diagnostics 命中。
3. **跨单元私有头禁止（R-2）**：新增私有头 `src/EntryDetail.hpp`、`src/ParamSchema.hpp` 位于 src/ 不入公共 include；test/ 零引用该两私有头（grep 实证）；公共头均落 `include/sdurws/ird/diagnostics/`；`PublicHeaderPathLayout_DT_BUILD`/`NoCrossUnitInclude` 用例通过。
4. **框架零源码修改（SA-02）**：见 4.1——diff 零框架文件、patches/ 零改动。
5. **不做名称前缀拼接/剥离（R-4）**：本任务无名称解析语义（catalog/factory 不触碰 RobWork 名称面）；ird_gates R-4 零 diagnostics 命中。
6. **权威唯一（PA-1）**：码值/分类/严重权威＝StableCodeRegistry 码表（create 从描述符解析，调用方无覆盖面——`DtDiag1_EntryFieldsCompleteFromRegistry`）；文案键派生单点（EntryDetail::codeLower）与注册期键约定一致（P-DIAG-9）；未越权实现 project/runtime/policy 等他单元语义（DedupKey 派生、translate 兜底均为单元卡登记的本单元职责）。
7. **不可变历史（PA-2）**：DiagnosticEntry 无 setter（类型层面杜绝）；目录追加式，去重命中只递增计数不改写首条目（Catalog.cpp:193-227）；"修改"＝新条目＋supersedes 链接且校验指向已存在条目；废弃码构造拒绝（`DtDiag2_CategoryFromRegistryAndCodeGuards` CodeDeprecated 分支）。
8. **不继承 old/**：实现按单元卡 §4.2/§6.4/§8.1/§9.2/§9.7 从零编写（old/ 不在磁盘，无引用）。

### 4.7 注释规范（AGENTS §2）—— 通过

- **公共头全查（2/2）**：`Catalog.hpp`（583 行）——文件头设计依据逐条（§4.1/§4.2/§6.4/§9.7/§4.4＋需求锚点）、P-DIAG-1/P-DIAG-5 处置锚点、subject 边界口径专节；每个类型/函数 Doxygen 中文注释齐备（含生命周期、线程安全：DiagCatalog append 并发安全/回调调用方线程同步派发/回调内禁 append 的死锁面提示、句柄析构须早于目录的契约）；`Factory.hpp`（362 行）——文件头五族映射就位口径逐条、create 七步检查序、translateDispatch 查找序①~④、TranslationInput 确定性说明（typeid 拼写跨编译器不承诺）、线程安全（entryId 原子、注册表运行期只读）。
- **实现/测试文件抽查（6 个，≥3 达标）**：`Catalog.cpp`（427 行：文件头实现口径登记＋pimpl 订阅 RAII 退订注释＋append 校验①~⑤逐步业务依据＋锁外派发理由）；`Factory.cpp`（309 行：检查序①~⑦逐段需求锚点、kMaxTranslateMessageBytes=512 的 §7.6 出处、兜底路径"恒不抛"论证）；`ParamSchema.cpp`（139 行：文法冻结说明、`std::isupper` 补查理由）；私有头 `ParamSchema.hpp`/`EntryDetail.hpp`（文件头含 R-2 不出公共 include 的登记理由、deriveDedupKey 的 DT-DUP-2 键面保证说明）；`CatalogFactoryTest.cpp`（849 行：文件头 acceptance 1~4 映射表、每 TEST 注释锚定矩阵行/需求、断言处中文说明）。
- **§2.5 高危信息**：单位——比较型三要素经 `UnitToken::find("rad")` 显式 token（测试 :248-249），无裸数值角度/长度；坐标系不适用（本任务无几何量）；线程约束逐类注明（见上）；所有权——DiagnosticsSinkImpl 三个非拥有引用成员逐个注明"调用方持有，本类不接管"；错误语义——DiagnosticsError 码面（调用方违约 fail-fast vs 兜底条目不抛）在 Factory.hpp 头注释专段；确定性——orderKey 三分量语义、paramSchema 纯函数、typeName 会话态不持久化均有说明。无中英混杂机翻腔、无过期注释、无裸 TODO。

### 4.8 文档同步 —— 通过

- 单元卡 `units/diagnostics.md` v0.3→v0.5（本范围 v0.4→v0.5）：卡头"构建落位"字段实测刷新（Catalog.hpp/Factory.hpp/三源文件/19 用例落位清单）＋§14.4 v0.5 变更记录——**六项实现口径按 DTB §5.4 登记**（①工厂与转译器一体＝ErrorCodeTranslator 别名视图；②subject 边界严格口径——并明示 §9.2 调用示例/§8.2/§8.5 表注 subject=∅ 记法与权威链不符、待映射落地任务修正且需所有者确认；③去重键派生 task→Task/object→Object/Case/Batch 保留；④translate 查找序与 512 字节截断；⑤§8.10 阶段 A 机械执行面＝execution 域码⇒task 必填；⑥阶段 A 转译清单就位口径）＋接线登记（threadTag/workerId/redactedContextSnapshot/容量清理/订阅异步化随 T07/T08/T09）。实现与任务卡零未登记偏差。
- 契约流水线状态：`state.json` 的 DIAG-T04 队列项（branch wp09-t04）随收尾翻转（PIPE §4.7⑤——非实施者职责，合入段处理）。
- **findings.json 未关闭条目相关性核对（open 条目 60+ 项逐类排查）**：与本任务直接相关的既有 open 条目——**F-007/F-009/F-062/F-098/F-099/F-101**（全新树构建环境链：本次复现再次实证 ini.template.static 缺失与 CXX_FLAGS 缺失两条，已在 4.2 如实登记，处置权构建约定所有者，无可随手消账项）；**F-008**（RWSimulatorPlugin 基线缺陷，本次实测 73 error 同分布再印证）；**F-010 家族**（.gitignore *.log 吞留痕——本任务 7 份 .log 按 wp09-t03 先例 `add -f` 入库，但 verify-target-build.log 被吞、**4 份 gtest XML 未入库**——新发现登记为 **F-102**）；**F-100**（DIAG-T02 提交标题风格——本任务两提交均动词开头，无再现）。其余 open 条目属 testkit/evidence/runtime/policy 等其他单元，与本任务无相关性、无静默积压。

### 4.9 commit 质量 —— 通过（附一处留痕清单出入——转登 F-102，不构成粉饰）

- `af5b7ee7` `[WP-09-T04] 实现 diagnostics 诊断目录与工厂（Catalog/Factory）`：标题动词开头、≤50 字；正文四段齐备（What 按文件逐项/Why 逐条 acceptance 映射/验证含构建·测试·门禁·探针真实结论/影响面"目标/别名/依赖边集合不变"）；`Acc-Covered: 1,2,3,4` 尾注。
- `6dd6e665` `[WP-09-T04] 补齐验证留痕日志（.gitignore *.log 例外入库——wp09-t03 同先例）`：四段齐备，如实说明纯留痕补录性质。
- **出入（建议级）**：af5b7ee7 验证段称"四份 gtest XML 与 ird-test-report.json 留痕 traceability/builds/wp09-t04/"、6dd6e665 称 report 的 logs 字段"与本批文件一一对应"——实测 4 份 XML 与 verify-target-build.log 均未入库（见 4.10/F-102）。方向为"声称在库而实际未入库"的清单虚报（F-077/F-081 家族）；数值主张（41/41＋3/3、门禁 7 命中基线零新增）经验收者亲手复现**全部准确**，测试结果无粉饰。
- 注记：送验 integration-build.log 产自主树存量增量树（日志内路径 `D:\...\RobWork\build\` 实证）——提交表述为"集成模式全量构建 exit 0"（对该树真实，未声称"全新树/最强口径"，较 F-036/F-038 所涉表述收敛）；验收者已以从零全树构建补强至最强口径（exit 0 零 error，见 4.2）。

### 4.10 留痕完整 —— 字面部分未达成（建议级——F-102；沿 F-081/F-083/F-085/F-087/F-088/F-090 家族先例不阻断）

- **在库**（git ls-tree @6dd6e666，wp09-t04/ 共 8 件）：`ird-test-report.json`（schemaVersion ird-test-report/1，含 per-suite 用例 trace、regression 双模式、gates 基线对比与 deltaAnalysis、realFailureEvidence、日志索引）＋7 份执行日志（integration-build/integration-test-run/smoke-configure/smoke-build/smoke-test-run/gates-current-wp09t04/validate-task——`add -f` 例外入库）。
- **未入库而报告字段引用在案**：`gtestXml` 字段所列 4 份 XML（sdurws_ird_diagnostics_{test,contract_test}-{integration,smoke}.xml——**非** gitignore 命中，check-ignore exit=1，单纯漏 `git add`；实施工作树本地实际存在该 4 件未跟踪文件）＋`logs.verifyTargetBuild` 引用的 verify-target-build.log（命中 .gitignore:87 `*.log`，F-010 根因第 8 次复踩）。证据本体不缺：双模式 41/41＋3/3 由验收者从零亲手复现一致（本记录 4.2），数值主张全部成立。
- `phase-one-validation.log`：自 WP-05 起即为历史载体（DIAG-T03 验收记录 §4.10 同结论），本任务留痕按现行惯例落 `builds/wp09-t04/`，ird-test-report.json 即结构化 resultNote 载体——符合"phase-one-validation.log **或** resultNote 已更新"的替代口径。
- validate-task：验收者亲跑 `validate-task.ps1 -TaskFile <契约>` → **PASS (1 tasks)**，exit 0。

### 4.11 偷懒/缩水实现扫描 —— 通过

- ①**标记扫描**：diff 全文 `TODO|FIXME|stub|placeholder|未实现|not implemented` 命中行全部为**测试名**（`DtDiag2_ParamSchemaPlaceholderConsistency`——占位一致性被测语义）与其在留痕日志/XML 中的运行记录，非实现缩水标记；实现源文件零命中；无需豁免条目。
- ②**空实现检测**：公共接口全部真实实现——create 为七步校验链＋条目组装（Factory.cpp:70-179）；registerTranslation/translateDispatch 为注册表与四步查找序（:185-294）；DiagCatalog 为 pimpl 五表（条目/索引/去重/计数/订阅）＋互斥（Catalog.cpp:121-360）；DiagnosticsSinkImpl 各方法为真实委托或组装（非空体）；actionKindToken/scopeKindToken 全枚举 switch。无空函数体/仅返回默认值的契约函数。
- ③**逐条证据表**：见 4.4——acceptance 4 条全部具名测试＋文件行号级证据，无一条仅凭"已实现"声明。
- ④**测试强度**：断言校验行为与边界（错误码面逐值断言＋错误码不相符即失败、两两互异集合断言、occurrences=3 精确计数、entryId 单调 +1、注入时钟相等断言、512 字节截断＋"已截断"标注在场、rootless 拒绝、seal 后 Usage、订阅/退订计数）；无恒真断言；4.5 双探针实证非恒真。

---

## 二、裁决

**verdict ＝ pass**

- 4.1~4.11 全部通过（4.10 的留痕入库缺口为建议级，沿 F-081 家族六例既判先例不阻断），无阻断级问题：范围零越界、构建/测试/门禁/探针全部亲手复现成功、红线零命中、注释无缺项、4.11 零命中、无数值粉饰。
- 新增建议级登记 1 项：**F-102**（4 份 gtest XML＋verify-target-build.log 未随送验提交入库，报告与提交正文的留痕清单与实际入库出入——F-081 家族新变体，首次涉及 XML 本体；证据本体不缺，验收者双模式亲跑一致）。
- 环境类既有条目 F-007/F-062/F-098/F-099/F-101/F-008 本次复现再次实证（ini 模板补拷＋主树完整参数＋删缓存重配后全树从零构建 exit 0），已在册、不重复登记。
- 本验收通过的是 **SHA 6dd6e665168d03fbe3f487e9c9fac3dc5839035e**，非分支引用；验收者未修改任何实施产物、未向 wp09-t04 推送任何提交。

## 三、问题清单

| 级别 | 描述 | 处置 |
| --- | --- | --- |
| 阻断 | 无 | — |
| 建议 | **F-102**：送验两提交声称 4 份 gtest XML 已留痕入库（af5b7ee7 验证段、6dd6e665"一一对应"、ird-test-report.json gtestXml 字段），实际未入库（非 gitignore 命中、单纯漏 add）；verify-target-build.log 被 .gitignore:87 `*.log` 吞掉（F-010 根因第 8 次复踩）——报告悬空引用（F-081/F-083/F-085/F-087/F-088/F-090 家族第 7 例，首次涉及 XML 本体）。数值主张全部复现准确、无粉饰 | 已转登 findings.json（F-102，open）；处置建议见该条目；不阻塞合入 |

## 四、验收者环境说明

- 复现 worktree（worktree A）：`<临时目录>/acc-diag-t04/repro`（detached @ 6dd6e666）；集成树＝主树缓存完整口径从零配置（`RWS_BUILD_INDUSTRIALROBOT=ON`、`RWS_USE_STATIC_LINK_PLUGINS=ON`、`BUILD_SHARED_LIBS=OFF`、vcpkg toolchain、Qt CMAKE_PREFIX_PATH、`CMAKE_CXX_FLAGS=-DNOMINMAX -DWIN32_LEAN_AND_MEAN -DWIN32`）；`RobWorkStudio.ini.template.static` 自主树补拷（F-007/F-062/F-099 既有缺陷的环境修复，不触及送验产物）。冒烟树：`<临时目录>/acc-diag-t04/smoke`（独立目录＋toolchain，显式 Release）。
- 工具链：VS 2022（MSVC 19.42，生成器 Visual Studio 17 2022，-A x64）、CMake、vcpkg 经典模式（gtest 1.18.0 @ installed/x64-windows）、pwsh（validate-task.ps1）。
- 探针注入/还原均在 worktree A 内：两轮注入（Factory.cpp 检查⑤短路、Catalog.cpp 去重键哨兵）各自重建重跑取证后 `git checkout --` 逐字还原，最终 `git status` 零内容残留、41/41＋3/3 复绿。
- 验收记录与 F-102 登记落 `acc/DIAG-T04/1` 分支（基于 origin/redesign-main）并推送；验收者日志/XML 随临时 worktree 清理，本记录内数值均为亲测。
