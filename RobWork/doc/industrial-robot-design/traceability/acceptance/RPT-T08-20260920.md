# RPT-T08 验收记录（acceptance record）

| 字段 | 值 |
| --- | --- |
| 任务 ID | RPT-T08（实现措辞冻结渲染规则——限定语映射/两类声明前置断言；unit=reporting；masterWp=WP-H；interUnit=false；卡内行≙DTB WP-12-T06；实现任务——全量清单验收） |
| 送验对象（冻结） | be46cf75f4f5736d2ae840122b04d7e5cd5429b5（分支 wp12-t08；diff 范围＝a6b17c2d5535737ca8b829cc2feed68f9b38da91..be46cf75f4f5736d2ae840122b04d7e5cd5429b5＝state.base..state.headSha；origin/redesign-main 实测停于 a6b17c2d＝base，range 内含 0c3dd4ee（实现）／8f44f45f（单元卡 v0.11 同步）／be46cf75（验证留痕）三个提交） |
| 验收尝试 | attempt 1（分支漂移检测：`git fetch` 后 `origin/wp12-t08` == be46cf75f4f5736d2ae840122b04d7e5cd5429b5 全 40 位逐字一致，零漂移；`acc/RPT-T08/1` 分支名此前不存在；state.attempts.accept==1 与请求一致） |
| 验收环境 | 复现 worktree A＝`git worktree add --detach D:/10_Source_Repos/21_robot/rpt-t08-acc1-wt-a` @ be46cf75（集成 build 树为本验收全新配置）；冒烟 build 树＝独立目录 `D:/10_Source_Repos/21_robot/rpt-t08-acc1-smoke`（本验收新建，与 A 分离）；记录回传 worktree B＝`git worktree add -b acc/RPT-T08/1 D:/10_Source_Repos/21_robot/rpt-t08-acc1-wt-b` origin/redesign-main@a6b17c2d；MSVC 2022 x64＋vcpkg 经典模式（toolchain 绝对路径指向主仓库 vcpkg——worktree 不含 vcpkg）＋Qt 6.11.1（D:/software/Qt）。集成配置＝`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=<主仓>vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`，缓存确认 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`；初配失败一次（缺 `RobWorkStudio.ini.template.static`——bin/ 整目录被 .gitignore `[Bb]in/` 吞，F-200 既有登记环境链本验收再次亲历），自主树复制 bin/ 两件未追踪辅助件后配置成功——非送验对象缺陷；冒烟配置同因补 Qt 前缀（F-007 同源）。验收全程只消费产物、契约、设计文档与在库留痕，未读取实施者过程叙述；结论全部出自验收者亲手重跑 |
| verdict | **pass**（4.1～4.11 全部通过；无阻断级问题；建议级 1 条已转登 findings.json F-241） |
| 验收日期 | 2026-09-20 |

## 4.1 改动范围 —— 通过

- `git diff --stat a6b17c2d..be46cf75` 实证 **22 个变更文件**（3 M reporting 代码/头/测试＋18 A traceability/builds/wp12-t08/ 留痕＋1 M units/reporting.md）；`--name-status` 逐件核对：无新增文件越出三段路径、无删除、无重命名。
- **allowedFiles 逐文件比对（越界零命中）**：`git diff --name-only a6b17c2d..be46cf75 | grep -v -E '^RobWork/RobWorkStudio/src/rwslibs/industrialrobot/reporting/|^RobWork/doc/industrial-robot-design/units/reporting\.md$|^RobWork/doc/industrial-robot-design/traceability/'` **零输出**——3 件代码命中 `reporting/**`、18 件留痕命中 `traceability/**`、1 件命中 `units/reporting.md`，22/22 全部在册。
- forbiddenFiles（REQUIREMENTS.md／ARCHITECTURE.md）**零触碰**（diff 无该两文件）。
- 无未登记框架源码改动（SA-02）：diff 零框架路径；`patches/` 目录变化 **0 件**。
- 涉及文件与契约卡行原文（『涉及文件＝`Render.cpp` 内』）一致：实现全部落在 src/Render.cpp；Render.hpp 为纯注释同步（见 4.7）、RenderTest.cpp 为测试面——均在 `reporting/**` 通配内。

## 4.2 可复现构建 —— 通过（集成全新树重配＋独立冒烟树，verify 目标零错误）

- **集成模式**（worktree A 全新 `build/`，重新配置后缓存 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON` grep 实证）：`sdurws_ird_reporting_test` 与 `sdurws_ird_reporting_contract_test` 两目标 `cmake --build build --config Release --target …` **均 exit 0，`error C[0-9]/error LNK/fatal error` 计数 0**（含 RobWork→RobWorkStudio→industrialrobot 依赖链的增量全新编译；产物 `build/RobWorkStudio/bin/Release/sdurws_ird_reporting_test.exe`＋`sdurws_ird_reporting_contract_test.exe` 就位）。
- **独立冒烟模式**（独立目录 rpt-t08-acc1-smoke，源＝worktree A 的 industrialrobot，`-DCMAKE_TOOLCHAIN_FILE=<主仓>vcpkg`＋Qt prefix——AGENTS §4.1 必带口径）：配置 **exit 0**＋全量构建 **exit 0、`error C/error LNK/fatal error/CMake Error` 计数 0**；`reporting/Debug/` 下两测试 exe 就位。
- 说明：本验收未做"全树从零全量构建"口径——基线框架缺陷（F-008/F-235 既有登记：RobWorkSim RWSimulatorPlugin 全新树必败）使该口径当前不可达（本验收曾启动全量构建，行进至框架层后按先例停止、转 verify 目标级口径）；送验留痕与近三任务（RPT-T05/T06/T07）同为 verify 目标级零错误口径，本验收复现同口径成立；本任务 diff 零框架触碰（4.1 实证），框架既有缺陷不归本任务。

## 4.3 契约 verify 命令 —— 通过（两条逐条亲手执行，测试真实运行）

worktree A 按契约字面逐条独立执行，**两条全部 exit 0**：

1. `cmake --build build --config Release --target sdurws_ird_reporting_test` → exit 0（verify-target1.log）
2. `cmake --build build --config Release --target sdurws_ird_reporting_contract_test` → exit 0（verify-target2.log）

**测试真实运行（非只构建，双模式四跑全绿）**：

- 集成模式亲跑两 exe → `[PASSED] 120 tests`（26 suites）＋`[PASSED] 38 tests`（16 suites），failures=0 errors=0；
- 冒烟模式亲跑两 exe → 同为 **120＋38 全绿**。

**与送验留痕逐字一致**：在库四份 gtest XML 顶部统计（120/38 × integration/smoke，failures="0" errors="0"）与验收者亲跑一致；且对 integration 单元 XML 做逐用例名解析比对——验收者运行日志 120 条 `[ OK ]` 用例名集合与在库 XML 120 条 testcase 名集合 **逐名相等（TEST-NAME-SET-IDENTICAL），其中 RPT08 具名用例恰 12 条**。

附加治理脚本（worktree A 上下文亲手复跑）：`validate-task.ps1 -TaskFile tasks/foundation/RPT-T08.json` → **PASS (1 tasks)**；`validate-docs.ps1` → **PASS (20 units, 12 trace entries, 150 task files)**；`verify-task.ps1 -TaskFile …RPT-T08.json -DryRun` → **DRYRUN PASS**（dryrun 回显的两条命令与契约 verify 字面逐字一致——交叉印证）；`validate-state.ps1` → **PASS (phase=idle, queue=18, history=124, tasks indexed=150)**。均与在库 `builds/wp12-t08/validate-task.log`/`validate-docs.log` 一致。

**ird_gates 独立复跑（双树直跑）**：base 树（detached worktree @ a6b17c2d）与 branch 树（worktree A）各以 `cmake -DIRD_ROOT=<各树>/…/industrialrobot -DIRD_ENABLE_GIT=OFF -P cmake/ird_gates.cmake` 直跑——**两侧原始 IRD-GATE- 行各 61**，路径归一化后逐行 diff 为空（唯一差异行为验收者自加的退出码标注行），**GATES-HIT-SET-IDENTICAL 独立成立**；两树均 exit 1＝F-019 既有红基线（非本任务引入），**reporting 目标命中数＝0**（grep 实证）——与送验 `gates-ird-gates-base.log`/`gates-ird-gates-branch.log`/`gates-hit-set-diff.txt` 结论一致。

## 4.4 acceptance 条目 —— 通过（六条逐项证据）

| # | 契约条目 | 证据（验收者亲读源码＋亲手复跑） |
| --- | --- | --- |
| 1 | RP-STATE-1 失败/取消/中断不入正式结论 | **前置断言生效（DataInvalid，无"渲染了再说"路径）**：unit `WordingFreezeTest.NonCompletedOutcome_DeclarationFlags_Rejected_DataInvalid_RPT08_ACC1`（Canceled/Failed/Interrupted × formalPass/reviewRecord 六路反例→artifact 缺席＋DataInvalid＋detail 含"断言①"）亲跑通过；实现＝Render.cpp `validateDeclarationConsistency` 断言①（Render.cpp:687-692，三格式渲染装配前统一执行——HtmlRender:1918/JsonRender:2476/CsvRender:2577 三调用点亲读）。**正文扫描计数=0＋状态呈现**：unit `NonCompletedOutcome_StatusPresentationOnly_BodyScanZero_RPT08_ACC1`（三类 outcome×`countOutsideQualifierSpans(html,"正式通过")==0`＋"已取消/失败/已中断——不构成结论"措辞在场＋否定声明"不构成正式结论（资格不成立）"＋outcome 状态事实 token 入矩阵 qualifier）亲跑通过；扫描辅助函数剥离限定语 span 的口径注释在案（screening-only 冻结文案含"正式通过"子串属限定语通道——RenderTest.cpp:273-276） |
| 2 | RP-STATE-2/3 两类声明独立 | **DataInsufficient**：unit `DataInsufficient_MissingListFull_Qualifier_NoConclusion_RPT08_ACC2`（章节 DataInsufficient＋缺失项全量清单两条均在场不短路＋data-qualifier="data-insufficient"＋矩阵 qualifier 含 DataInsufficient＋正文扫描"正式通过"计数=0）亲跑通过。**EngineeringInfeasible∧reviewRecord==true**：unit `InfeasibleReviewRecord_WordingAndProof_NeverPassWording_RPT08_ACC2`（§8.2② 引号形"经验证的不可行结论（正式评审记录）"＋证明证据项与"因不可行而不适用"替代说明在场＋kin 块内"通过"字样零出现＋同报告 formalPass 声明并存不混淆——各随其结果不同章节）亲跑通过；与"正式通过"两声明并存不混淆由断言④反向例钉住：unit `SectionFlagBeyondCitedResults_Rejected_DataInvalid_RPT08_ACC2`（声明超出所引结果资格——formalPass 侧与 reviewRecord 侧各一反例→DataInvalid＋"断言④"）亲跑通过；实现＝Render.cpp 断言③（:702-708）与断言④（:714-745） |
| 3 | 限定语逐 token 结构性输出＋模板纪律 | **§6.4 词表逐 token 三格式**：unit `QualifierWordList_PerTokenStructuralOutput_AllFormats_RPT08_ACC3`——单元格级 8 token（estimated/data-insufficient/screening-only/historical-superseded/not-applicable/canceled/failed/interrupted）在 HTML（data-qualifier="…"）/JSON（"…" token）/CSV（token 字面）三格式逐 token 在场断言亲跑通过；触发数据源在矩阵面逐项钉住（GeometricEstimate→estimated、EvidenceItemStatus::Missing→data-insufficient、mode==Quick→screening-only、Superseded→historical-superseded、SourcedValue::notApplicable→not-applicable、outcome 三态）；**报告级 2 token**：external-validation-incomplete（HTML/JSON）＋downgraded-reference-value（HTML/JSON＋CSV 诊断附表码行——条目级关联键未入模型的 v0.9 收口安排，CSV 面为数据面——单元卡 §14.4 v0.11 登记）同用例钉住；`ExternalValidationIncomplete_RecordedState_MandatoryBoundary_RPT08_ACC3`（Recorded 在场/Solidified 零输出——不伪造）亲跑通过。**§8.2④ 模板不得移除限定语**：实现＝`assertWordingFreezeTemplateDiscipline`（Render.cpp:777-808——§6.4 词表全 10 token 呈现文案缺位或模板版本低于强制下限 v1＝TemplateVersion 拒绝）亲读；其正向呈现义务由上列具名用例承担，防御面性质经单元卡 §14.4 v0.11 登记（与 MatrixIndex 重复键拒绝同性质）——非空实现（见 4.11②） |
| 4 | screening-only 与 Quick 结果 | unit `QuickResult_FormalPassFlag_Rejected_ScreeningOnlyMandatory_RPT08_ACC4`：①反例——Quick 结果伪造 formalPass 资格→DataInvalid＋"断言②"（Render.cpp:693-701，formalPass⇒Verified∧Completed∧Feasible——Quick 永不满足）亲跑通过；②正例——合法 Quick 结果"筛选级——不得单独支撑正式通过结论"（§6.4 行 5 冻结文案全串）＋data-qualifier="screening-only" 强制在场亲跑通过；三格式面由 4# 的 QualifierWordList 用例覆盖（CSV/JSON 同钉） |
| 5 | RP-COV-1 覆盖呈现 | unit `FullCoverage_CompletenessConclusion_RPT08_ACC5`（全覆盖→"覆盖完备性：全部启用必验工况已覆盖"）＋`MissedCases_OverallDataInsufficient_PresentationAndGate_RPT08_ACC5`（漏验 1→"漏验 1 项启用必验工况…整体数据不足（缺项呈现）"呈现面＋断言⑤并存反例 DataInvalid——Render.cpp:750-760；漏验报告正文扫描"正式通过"计数=0）＋`EnvelopeMergeAnnotation_NotReplacingPerCaseEntries_RPT08_ACC5`（包络条目 caseScope 跨双工况→"包络合并（呈现方式）"恰一次＋data-case-merge="envelope" 机器锚；逐工况条目零标注——不替代）均亲跑通过。呈现侧判定＝`coverageIncomplete`（Render.cpp:624-628——五态计数自洽兜底），漏验逐工况名单未入模型以计数呈现、不伪造逐工况状态（v0.9 登记，honestDeclarations 在案） |
| 6 | P-EV-5 处置 | unit `DowngradedReferenceValue_MandatoryPresentation_RPT08_ACC6`：①携带降级诊断（diagRefs 码＝evidence `kDiagRegionCoverageDowngraded`——常量同源不私抄字面，Render.cpp:644-652 消费 Verdict.hpp:444 既有公共常量）→HTML data-qualifier="downgraded-reference-value"＋§6.4 冻结文案全串"降级中——不可作为正式覆盖率结论"（不弱化为普通标注）＋JSON coverageSummary.qualifier 数组镜像；②无降级事实→零输出（不全局伪标注）——均亲跑通过；JSON 嵌套 qualifier 增补已按 DTB §5.4 登记于单元卡 §14.4 v0.11（顶层 17 键冻结集不变——kJsonTopLevelKeys 亲读在位） |

## 4.5 测试真实失败能力 —— 通过（两处实现变异即红、还原复绿）

在 worktree A（本验收沙箱，任务分支零触碰）对 src/Render.cpp 注入两处应失败变异，增量重建后亲跑：

1. **变异 1：禁用断言①**（`ref.outcome != Completed && (…)` → `false && …`）→ unit `NonCompletedOutcome_DeclarationFlags_Rejected_DataInvalid_RPT08_ACC1` **变红**（期望 DataInvalid 的反例不再被拒）——RP-STATE-1 的前置断言红能力成立。
2. **变异 2：还原 1 后禁用 P-EV-5 降级限定语呈现**（`if (hasDowngradedCoverageReference(…))` → `if (false && …)`）→ unit `DowngradedReferenceValue_MandatoryPresentation_RPT08_ACC6` **变红**（HTML 限定语缺席）——"不得弱化不得省略"的强制呈现红能力成立。

两处变异 `git restore`＋`touch`（防 F-068 时间戳陷阱）还原后重建 exit 0、120＋38 **全绿复现**，`git status --porcelain` 空（沙箱干净）。测试非恒真、非"只编译通过"。

## 4.6 架构红线 —— 通过（AGENTS §5 八条逐条）

1. **L2 计算内核零 Qt**：本任务 diff 三文件的 include 面亲读＝std＋`sdurws/ird/{core,evidence,diagnostics,reporting}/…` 公共头＋单元内私有 RenderText.hpp——**零 Qt include**； ReportingBuild.NoQtInclude_DT_BUILD 等既有红线用例随 120 全绿复跑通过；ird_gates 独立复跑本任务零新增命中（4.3）。
2. **业务域互链禁止（R-1）**：reporting 不属互链禁令七单元；本任务唯一新 include＝evidence/Verdict.hpp（P-EV-5 降级码常量消费），落在 RPT-T01 既登记的 core＋evidence＋diagnostics＋project 链接边内，零新增跨单元边；gates SUB 检查 reporting 零命中。
3. **跨单元私有头禁止（R-2）**：Verdict.hpp 位于 `include/sdurws/ird/evidence/`（公共头路径实证）；`kDiagRegionCoverageDowngraded` 为 base 已在的公共常量（`git show a6b17c2d` 实证），非本任务新增私有面；RenderText.hpp 为单元内私有共享头（不出 include/——T07 先例）。
4. **框架零源码修改（SA-02）**：diff 零框架路径、patches/ 零新文件（4.1）。
5. **不做 RobWork 名称前缀拼接/剥离（R-4）**：ird_gates R4 检查双树命中集归一化 diff 为空（4.3）——本任务零新增；唯一 R-4 命中为 project 单元既有登记（F-145），与本任务无关。
6. **权威唯一（PA-1）**：资格权威归 evidence 纯检查——`validateDeclarationConsistency` 注释与实现均不重算资格、只核对已冻结数据内部一致性（Render.cpp:658-663 亲读）；`coverageIncomplete` 为呈现侧推导（"覆盖判定的权威在 evidence——不构成第二套资格判定"注释在案，Render.cpp:616-617）；名称解析/修订写路径零触碰。
7. **不可变历史（PA-2）**：渲染器全只读（render 后报告/矩阵不变，纯函数纪律）；零修订/写路径触碰。
8. **不继承/不恢复 old/**：diff 无 old/ 引用。

## 4.7 注释规范 —— 通过（公共头全查＋实现/测试文件抽查）

- **公共头 Render.hpp（diff 全查）**：本任务 23 行变更**逐行为纯注释**（removed/added 非注释行计数均＝0 脚本实证）——FieldCell qualifier 成员注释同步 RPT-T08 收口描述（§6.4 词表触发数据源、报告级承接位、§8.2④ 自检引用），"值语义；线程安全：纯值"保留；零接口变更与提交声明一致。
- **实现文件 src/Render.cpp（全读关键面）**：文件头（L1-50）设计依据逐条锚定 §8.2①/§6.4/§6.5/§4.3.3/KIN-12＋确定性（NFR-COR-02）＋线程安全段；新增函数全部 Doxygen 中文齐备——`coverageIncomplete`（@param [in]/@return 两态语义＋noexcept）、`hasDowngradedCoverageReference`（数据源链路：RegionCoverageEvidence.downgraded→EVI-REGION-COVERAGE-DOWNGRADED→diagRefs 投影）、`validateDeclarationConsistency`（五路断言逐条依据＋@throws DataInvalid）、`assertWordingFreezeTemplateDiscipline`（@throws TemplateVersion＋防御性不变量性质）；§2.5 高危信息：错误语义（DataInvalid 数据错误 vs TemplateVersion 版本不兼容 vs Usage 调用方违约三分）、确定性来源（词表枚举序＝表行序、静态 constexpr）、所有权（只读借用）、线程约束（渲染函数无共享可变状态）——必填项零缺失（物理单位/坐标系/角度制式：本单元为文本/枚举呈现面，无数值物理量计算，N/A 合理——与 T07 同口径）。
- **测试文件 test/RenderTest.cpp（全读关键面）**：文件头（L1-32）含设计依据（两契约 acceptance 引用）＋**替身边界声明**（FakeCsvWriter/FakeJsonWriter/MinimalIoFactory 最小确定性替身、真实 canonical 归 io/SA-12；脱敏用例消费真实 RedactionService）＋确定性夹具纪律；用例名带 `RPT08_ACCx` 追溯字段（AGENTS §2.7）；扫描辅助 `countOutsideQualifierSpans` 注释说明"为何剥离限定语 span"（子串歧义的机械口径处理）。
- Render.cpp 内既有措辞调整处均留"为什么"注释（如 :1517-1521 否定声明改词的 RP-STATE-1 扫描口径依据、:1788-1789 附录列名词面化依据）。

## 4.8 文档同步 —— 通过（偏差已按 DTB §5.4 登记；findings 未关闭条目已核对）

- **单元卡 v0.9→v0.11**（8f44f45f，单行追加、v0.10 原文不动）：§14.4 v0.11 行逐项登记四处偏差（DTB §5.4 口径）——①否定声明措辞调整（"未达正式通过"→"不构成正式结论"，RP-STATE-1 机械扫描口径、资格语义不变）；②JSON 镜像 coverageSummary/externalResourceSummary 条目增补 qualifier 嵌套数组成员（顶层 17 键冻结集不变）；③包络合并标注的条目 caseScope 结构触发；④追溯附录资格列名词面化。逐项与实现对照核实（4.4/4.6/4.7 亲读）——**零未登记偏差**。
- **任务状态/追踪矩阵**：validate-docs PASS（12 trace entries）＋validate-task PASS（锚点校验过）；契约 status/resultNote 翻转归收尾段（PIPE §4.7，与既有任务同口径；F-201 已登记该条文与实践脱节）。
- **findings.json 未关闭条目相关性核对（v1.2）**：现册 **open 208 条**（total 240），逐族核对与本任务的关系——
  - **F-012/F-232~F-240 家族**（辅助提交标题名词短语开头）：**本任务再现**（见 4.9/G-1）——已按强制转登要求登记 **F-241**（编号自 F-240 顺延）；
  - **F-041/F-073/F-236/F-238 计数失准族**：本任务散文计数（十八件留痕 `ls | wc -l`＝18、单元 120＝108＋12、契约 38、新增用例 12＝`grep RPT08` XML 解析计数、gates 双树 61＝61）经验收者逐项实测**全部准确**——零复踩；
  - **F-010/F-223 幻影清单族**（.gitignore `*.log` 吞留痕）：本任务 18/18 件全部在库（`git diff --name-status` 逐件 A 实证）——零复踩；
  - **F-230**（[RPT-Txx] vs [WP-12-Txx] 前缀双轨口径）：本任务沿用 `[RPT-T08]` 前缀（契约 branch=wp12-t08、卡行≙WP-12-T06），属已登记双轨口径的沿例，无新增发现；
  - **F-007/F-062/F-098/F-099/F-200 环境链**（toolchain/Qt prefix/ini 辅助件）：验收复现期间再次亲历 ini 模板缺失与冒烟 Qt 前缀两项（见环境段），属既有登记的再证实，非本任务交付面问题，零新增登记；
  - **F-235/F-008**（全新树全量构建必败）：本验收据此采用 verify 目标级零错误口径（与送验留痕及 RPT-T05/T06/T07 先例一致）——已在 4.2 说明；
  - **F-201**（ACC §4.10 resultNote 条文与流水线实践脱节）：本任务同样以收尾段翻转登记——既有登记继续有效；
  - 其余 open 条目（core/testkit/evidence/project/io/execution/policy/runtime/diagnostics/ui 各单元家族）与本任务交付面（reporting 渲染规则层）无交叉；**无可随本任务顺手消账项**。

## 4.9 commit 质量 —— 通过（四段式齐备、验证结论经独立复跑证实；标题风格 1 条建议级）

- **0c3dd4ee** `[RPT-T08] 实现措辞冻结渲染规则（限定语映射/两类声明前置断言）`——动词开头合规；四段齐备（What 按函数/文件分点、Why 逐条对应 acceptance 1~6 与设计章节、How 构建与测试与门禁真实结论、影响面含行为增量与兼容性说明）；Acc-Covered: 1,2,3,4,5,6。
- **8f44f45f** `[RPT-T08] reporting 单元卡 v0.11：RPT-T08 实现落位登记（文档同步）`——四段齐备；标题前缀后以名词短语开头（F-012 家族，见 G-1/F-241）。
- **be46cf75** `[RPT-T08] 验证留痕：双模式构建/测试执行/门禁/治理自查证据（wp12-t08）`——四段齐备；标题同族瑕疵（同 G-1）。
- **验证结论与留痕一致性（粉饰排查）**：提交正文声称的十八件留痕（ls 实证 18/18）、两目标×两模式 gtest XML（120＋38，XML 顶格＋亲跑双证）、ird_gates 双树命中集 diff 为空（61＝61 归一化逐行比对实证）、validate 双 PASS（亲跑同值）、"Render.hpp 仅注释同步"（脚本计数实证）、"零新增跨单元公共头消费"（include 面亲读）——**逐项相符，零粉饰、零夸大**。

## 4.10 留痕完整 —— 通过

- gtest XML×4＋ird-test-report.json（schema `ird-test-report/1`，含 acceptanceMapping 六条＋honestDeclarations 三条）＋构建日志（integration-build.log/smoke-configure.log/smoke-build.log）＋测试运行日志×4＋门禁双树日志＋gates-hit-set-diff.txt＋gates-all-note.txt＋validate 双日志——**18 件全部落位在库**（4.1/4.9 实证）。
- 在库四份 XML 顶部 tests/failures/errors 属性与验收者双模式亲跑结论逐字一致（120/38 × 2，零失败零错误）；integration 单元 XML 用例名集合与亲跑逐名相等（4.3）。
- gates-all-note.txt 如实登记 gate-all.ps1 冒烟步环境失败（脚本内部未传 Qt 前缀）与 DoD 门禁承担口径（ird_gates 双树直跑）——honestDeclarations 首条同口径，无掩饰。
- resultNote/status 更新随收尾段（PIPE §4.7）——F-201 已登记该条文与实践的脱节，此处如实注明。

## 4.11 偷懒/缩水实现扫描 —— 通过

- **①TODO/FIXME/stub/placeholder/未实现/not implemented**：diff 范围三份代码/测试文件全文扫描（grep 六词族）**零命中**（无豁免条款需列出）。
- **②空实现检测**：`validateDeclarationConsistency` 为完整实现（Render.cpp:681-761，五路断言全量逻辑、逐条定位信息）；`assertWordingFreezeTemplateDiscipline`（:777-808）迭代全 10 token 校验呈现文案缺位＋模板版本下限——当前词表完整时恒通过属**已登记的防御性不变量**（单元卡 §14.4 v0.11 明示"与 MatrixIndex 重复键拒绝同性质"），非空实现；`coverageIncomplete`/`hasDowngradedCoverageReference` 均为完整判定逻辑；测试 io 替身为契约明文允许的注入形态且边界声明在案（测试文件头 L20-27）。
- **③逐条证据表**：见 4.4——acceptance 六条每条均有具名测试＋亲跑结论＋实现行号，任一条无证据即 fail 的反例不存在。
- **④测试强度**：4.5 两处实现变异实测变红（断言① 1 例、P-EV-5 呈现 1 例）＋还原复绿；断言校验行为与边界（六路反例矩阵、块内子串定位、扫描计数=0、恰一次计数、零伪造负例、错误码＋detail 词）——非恒真。

## 发现的问题清单

| 级别 | 编号 | 内容 | 处置 |
| --- | --- | --- | --- |
| 建议 | G-1 | 两辅助提交 8f44f45f／be46cf75 标题前缀后以名词短语开头（"reporting 单元卡 v0.11：…"/"验证留痕：…"），不合 ACC §4.9/AGENTS §6.3"动词开头"字面——F-012/F-232/F-233/F-234/F-237/F-239/F-240 家族再现（家族第 12/13 例，RPT 系连续七任务复踩）。正文四段齐备、验证结论经验收者独立复跑全部证实（无粉饰），纯风格瑕疵不构成阻断 | **已转登 findings.json F-241**（与家族合并处置；不追溯改写已推送历史；不阻塞合入） |
| 环境注记 | —— | 复现期间初配失败（ini 模板缺失——bin/ 被 .gitignore 吞）与冒烟 Qt prefix 需求均为 F-200/F-007 家族既有登记环境链的再证实；全新树全量构建不可达为 F-008/F-235 既有登记——均非本任务交付面问题，零新增登记 | 已在 4.8 逐族核对留痕 |

## 验收者环境说明

- 复现与记录均在临时 worktree（rpt-t08-acc1-wt-a／rpt-t08-acc1-wt-b，另 rpt-t08-acc1-smoke 冒烟树）完成；验收者对任务分支 wp12-t08 **零提交、零推送、零文件修改**（4.5 变异在沙箱 worktree A 内注入并已还原，`git status --porcelain` 复核干净；变异常量 `kDiagRegionCoverageDowngraded` 等只读消费未改 evidence 侧任何文件）。
- 构建树：集成 `rpt-t08-acc1-wt-a/build`（全新配置，`RWS_BUILD_INDUSTRIALROBOT:BOOL=ON` 缓存确认）；冒烟 `rpt-t08-acc1-smoke`（独立目录＋vcpkg toolchain＋Qt prefix，Debug 配置）。测试执行：两模式各 2 目标亲跑（本轮共 4 次全绿运行＋2 次变异红＋1 次复绿运行＋2 次 gtest_filter 定向红）；ird_gates 双树直跑（基线 detached worktree 比对后已清理）。
- 记录落位：本文件随 F-241 转登一并提交于 `acc/RPT-T08/1` 分支并推送 `origin`（ACC §3 记录回传）。
