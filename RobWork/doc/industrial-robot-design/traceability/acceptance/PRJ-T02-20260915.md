# PRJ-T02 验收记录（attempt 2，2026-09-15）

| 字段 | 值 |
| --- | --- |
| 任务 | PRJ-T02（project 单元存储原语 AtomicFile/IFileOps；契约 tasks/foundation/PRJ-T02.json） |
| **verdict** | **pass**（4.1～4.11 全部通过；零阻断级问题；建议级 2 项已转登 findings.json F-125～F-126） |
| 送验对象（冻结） | `04315ecb47b5950b31bf657fc16d01bc342ece20`（40 位）；分支漂移检测：`origin/wp04-t02`＝本地任务分支尖端＝该 SHA，零漂移（验收全程按该 SHA，不按分支名） |
| diff 范围 | `56dbd5aace0ee23d2ee031168e70f56a299c124b..04315ecb`（基线..headSha；2 个提交：290296ff 实现＋04315ecb 留痕/文档） |
| attempt 说明 | 尝试序号 2 由编排者随验收请求下发（state.attempts.accept 递增）；attempt 1 未产生任何落盘记录（remote/local 均无 acc/PRJ-T02/* 分支、traceability/acceptance/ 无同日同名文件）——本文件为该任务首份验收记录，文件名按编排者下发路径未加 -r 序号 |
| 验收者环境 | 全新上下文；复现现场＝`git worktree add --detach` @04315ecb（下称"全新树"）；集成模式全新配置（首轮即带 vcpkg toolchain＋Qt 前缀＋CMAKE_CXX_FLAGS，F-062/F-098/F-099/F-101 口径；`RobWorkStudio.ini.template.static/.shared.in` 两件本地资源自主树复制——F-062 登记件）；冒烟模式另配独立临时目录；CMake 4.3.1＋MSBuild/VS2022 x64；pwsh 7.6.6 治理脚本 |
| evidence | 分支 `acc/PRJ-T02/2`（本文件＋findings.json F-125～F-126 同支承载；commit 三元组见文末） |

---

## 1. 逐项证据（ACC §4.1～4.11）

### 4.1 改动范围 —— PASS

- `git diff --name-status 56dbd5aa..04315ecb` 共 13 文件，逐一比对契约 `allowedFiles` 三模式：
  `project/**`×6（CMakeLists.txt、src/Project.cpp、src/win32/{AtomicFile.cpp,AtomicFile.hpp,IFileOps.hpp}、test/AtomicFileTest.cpp）✓；`traceability/**`×6（builds/wp04-t02/ 下 ird-test-report.json＋s72-win32-docs-crosscheck.md＋4 份 gtest XML）✓；`units/project.md`（显式列名）✓。
- `forbiddenFiles`（REQUIREMENTS.md / ARCHITECTURE.md）零触碰 ✓。
- `industrialrobot/patches/` 零新文件；框架源码（RobWork/RWS/RobWorkSim）零改动（diff 全清单核对，无任何清单外路径）✓——SA-02 无登记义务产生。

### 4.2 可复现构建 —— PASS（验收者全新树亲手复现，不采信实施日志）

- **集成模式**：全新 build 树配置 exit 0（`-- Configuring done (53.9s)`＋`-- Generating done`，CMake Error 计数＝0；缓存 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON` grep 确认）。
  - 契约 verify 目标 `sdurws_ird_project_test` 从零构建 exit 0、`error` 行计数＝0（产物 `sdurws_ird_project.lib`＋`sdurws_ird_project_test.exe`，gtest DLL 拷贝步骤执行）。
  - `sdurws_ird_project_contract_test` 构建 exit 0。
  - 两测试可执行文件运行：**18/18＋5/5＝23/23 全绿**（XML 落验收者证据目录）。
- **独立冒烟模式**：`-S industrialrobot -B <独立临时目录> -DCMAKE_TOOLCHAIN_FILE=vcpkg…`（AGENTS §4.1＋CORE-T01 G-2 口径）配置 exit 0；`--config Release` 全量构建 exit 0（日志中 6 处 "error" 字样均为 diagnostics 单元文件名 `Errors.cpp`/`ErrorsTest.cpp`，非编译错误）；产出两测试 exe；运行 **23/23 全绿**。
- 口径说明：全新树**全量**集成构建受 F-008 基线框架缺陷阻挡（既有登记，非本任务引入）；本次复现覆盖任务目标面（project→core 链＋两测试目标＋门禁＋治理脚本），与 PRJ-T01 以来既往验收口径一致。送验提交信息中"集成模式全量构建 exit 0"的树身份因构建日志未入库而无法核对（→F-125），但其可复核面（双模式构建/测试/门禁/脚本）均被验收者独立证实为真。

### 4.3 契约 verify 命令 —— PASS

- 契约 `verify` 数组仅 1 条，验收者在全新树**逐字原样执行**两轮：
  ① 直接执行 `cmake --build build --config Release --target sdurws_ird_project_test`（worktree 根、相对 `build` 路径）→ **exit 0**；
  ② `verify-task.ps1 -TaskFile <契约>` 实跑（内部执行同一命令）→ **verify-task: PASS PRJ-T02，exit 0**。
  （先 `-DryRun` 核对解析：RepoRoot 解析正确、命令行一致。）

### 4.4 acceptance 条目逐条 —— PASS（3/3 有对应证据；无虚标）

| # | 契约条目 | 验收者证据 |
| --- | --- | --- |
| 1 | publishNew/原子替换/write-through 语义单测通过：提交（替换）点之前旧文件字节不变、任一步失败不留半写目标（§7.2 不超诺口径） | AtomicFileTest 13 用例两模式 26/26 通过（送验 XML `tests="18" failures="0"`×2 与验收者复跑 18/18 一致）；核心断言用例逐条在位：`ReplaceFile_BeforeCommitPoint_OldBytesStable`（提交点前 HEAD 字节不变）、`PublishNew_TargetExists_FailsAndKeepsBothSides`（已存在拒绝＋两侧原状＋实测 ERROR_ALREADY_EXISTS）、`ReplaceFile_FailureKeepsOldTargetIntact`/`PublishNew_FailureCreatesNoTarget`/`WriteThrough_OpenFailure`（各步失败零半写）、`WriteChunk_FailureMidway`（残留恰为一块＝失败边界精确）；不超诺口径落实——全组零断电/零进程杀灭断言（F8 归 PRJ-T15，注释明示）。测试真实失败能力另见 4.5（两组探针即红） |
| 2 | §7.2 Windows 文件操作保证对照 Microsoft Learn 官方文档口径逐行复核并留痕 | 留痕在库：`traceability/builds/wp04-t02/s72-win32-docs-crosscheck.md`（行 1～6 逐行：官方英文原文摘录＋一致性结论＋两处如实注记＋行 6 官方佐证；行 4/5 为 PRJ-T03 载体先行复核）。验收者**独立抽查官方文档**：现场拉取 Microsoft Learn 三页（CreateFileW/MoveFileExW/ReplaceFileW），逐句核对记录所引原文——FILE_FLAG_WRITE_THROUGH 0x80000000 表项、Caching Behavior 两段层级细分（含"written to the system cache but is flushed to disk without delay"）、CREATE_ALWAYS"last-error code is set to ERROR_ALREADY_EXISTS (183)"与 HIDDEN/SYSTEM 失败句、dwShareMode=0 不可共享句、MOVEFILE_REPLACE_EXISTING/WRITE_THROUGH/COPY_ALLOWED 全部逐字一致；REPLACEFILE_WRITE_THROUGH"This value is not supported."与 ReplaceFile 属性保留清单逐字一致。记录对"卡面无官方逐字句"处（行 2 原子可见性、行 2 失败码）如实标注为设计口径陈述＋实测取用，无超诺 |
| 3 | IFileOps 生产窄接口接缝就位（D-10 形态）；接缝位于 src/win32/ 私有实现头、不出 include/（R-2） | `src/win32/IFileOps.hpp`（6 方法接口＋faultpoint 命名空间冻结 5 个故障点标识，testkit §6.4 命名法）＋`AtomicFile(IFileOps*)` 注入构造（AtomicFile.hpp）；消费面＝AtomicFileTest 内 `FaultFileOps` 装饰器 fake（5 个注入用例经接缝驱动失败路径），零 testkit 头；CMakeLists 增 `target_include_directories(sdurws_ird_project_test PRIVATE src/)`（私有头唯一消费通道，diff 行核对）；include/ 公共头根零新增文件（diff 核对）＋`ProjectBuild.PublicHeaderPathLayout_DT_BUILD_R2` 两模式通过；产品库唯一单元边仍为 core（CMake 配置期守卫复跑通过＋ird_gates 零 project 条目） |

`ird_gates` 执行说明（ACC §4.4"未执行的不得标注通过"）：门禁已建成，验收者**实际执行** `cmake --build build --config Release --target ird_gates` → exit 1＝**8 处命中**，逐条与送验报告登记的基线一致：SUB×6（diagnostics_contract_test/evidence_test/evidence_contract_test/policy_test/policy_contract_test/runtime_test→testkit）＋SUB 表外边×1（testdata_lint→testkit）＋T1×1（testdata_lint 产品目标链 testkit）；**零 sdurws_ird_project 条目、零新增**。日志中 R1/R3/R4/R5/T2 token 为引擎自测"按预期检出"行（与报告口径一致）；报告以"exit 1＝基线既有命中的预期行为"如实登记，无虚标。

治理脚本（验收者亲跑）：`validate-task.ps1` → **PASS (1 tasks)** exit 0；`validate-docs.ps1` → **PASS (20 units, 12 trace entries, 150 task files)** exit 0；`verify-task.ps1` 实跑 → **PASS PRJ-T02** exit 0（见 4.3）。

### 4.5 测试真实失败能力 —— PASS（2 组实现级探针注入即红；均还原复绿）

探针打在 `src/win32/AtomicFile.cpp` 实现体（行为级用例，须重编译——每轮 Edit 引入新 mtime 规避 F-068/F-075 增量陷阱；每轮 `git checkout --` 还原并确认 `git status --porcelain`＝0）：

| 探针 | 注入 | 结果 |
| --- | --- | --- |
| 1 | `writeThrough` 第 3 步持久性闸门移除（flush 不再调用，直接置 ok） | `Flush_FailureFailsDurabilityGate_NeverPublishes` **红**（FAILED 1）；还原复绿 |
| 2 | `Win32FileOps::publishNew` 错误携带 `MOVEFILE_REPLACE_EXISTING`（只增语义破坏） | `PublishNew_TargetExists_FailsAndKeepsBothSides` **红**（3 处断言失败：L451 ok 判定、L453 旧字节、L455 错误码）；还原复绿 |
| 还原后 | `git checkout --`＋重编＋全量重跑 | **18/18 复绿** ✓ |

两组探针证明闸门时序断言与只增语义断言均具备行为区分力（非恒真）；与送验报告 targets[1].notes 自述的"EXPECT 反转→用例失败→复原"留证方式互为印证。

### 4.6 架构红线（AGENTS §5 八条逐查）—— PASS

1. **L2 零 Qt**：project 全部源/测试/CMake 零 Qt include/链接（grep 实证唯一命中为 BuildRedLineTest.cpp L126 注释散文；`NoQtInclude_DT_BUILD_NFR_MNT_01` 两模式绿；CMake 配置期守卫在位）。
2. **R-1 业务域互链禁止**：构建图仅 project→core 一条登记边（ird_gates 8 命中零 project 项＋`UnitEdgeOnlyCore` 用例绿＋CMakeLists L95 亲读；新增源文件未引入任何新边）。
3. **R-2 跨单元私有头**：新增头均在 src/win32/、include/ 零新增（`PublicHeaderPathLayout` 两模式绿）；实现/测试零他单元头（grep `sdurws/ird/<其他单元>` 零命中——P-PR-1 延续：零 core 头消费）；私有头仅经测试目标 PRIVATE src/ 目录消费。
4. **SA-02 框架零源码修改**：4.1 已证（patches/ 零触碰、diff 全清单无框架路径）。
5. **R-4 名称前缀拼接/剥离**：diff 内零名称处理逻辑（三原语均为字节/路径层操作，无名称语义代码）。
6. **PA-1 权威唯一**：共享摘要判定留给 PRJ-T05（publishNew 一律失败不越权，AtomicFile.hpp L142-144 注释明示）；错误码分类留给 PRJ-T07 事务引擎（原语层只透传 Win32 原始码，IFileOps.hpp L88-90 注释明示）；诊断码零私造（faultpoint 标识为注入接缝 ID，非诊断码）。
7. **PA-2 不可变历史**：publishNew 只增不覆盖（探针 2 反证断言在位）；replaceFile 为唯一提交点、失败旧字节不变（`ReplaceFile_FailureKeepsOldTargetIntact` 绿）——与"修订只增不改"一致。
8. **不恢复 old/**：diff 零 old/ 引用；实现按 units/project.md §7.1/§7.2/§3.4 从零编写。

### 4.7 注释规范（AGENTS §2）—— PASS

新增/大改代码文件共 5 份，全部逐一核查（超过"实现文件抽查 ≥3"口径；本任务零新增公共头——"公共头契约全查"对象为空集，两份新增头均为 src/win32/ 私有实现头，仍按契约文档标准全查）：

- `src/win32/IFileOps.hpp`：文件头（设计依据 §3.4/§7.6/§6.4/D-10/R-2＋"为什么需要这层间接"背景＋线程约束＋错误语义分类）；每个方法 Doxygen 中文全注（@param 方向/单位/约束、@return 失败行为、故障点对应）；faultpoint 常量逐一注明 testkit §6.4 命名法与冻结纪律；closeHandle 不设故障点的"为什么"在位。
- `src/win32/AtomicFile.hpp`：文件头（两种保证分离的语义表达＋不超诺口径＋失败残留语义＋线程约束/所有权）；kWriteChunkSize 常量注明字节单位、"不构成持久性承诺"、与测试共用事实来源；三方法全注（调用方协议、返回语义、复杂度）。
- `src/win32/AtomicFile.cpp`：文件头设计依据；handleToResult/boolToResult 说明了"成功态 osError 恒 0"的残留歧义消除理由；writeThrough 四步每步注释语义与失败去向；HandleGuard 注明"flush 成功而关闭失败保守报失败"的取向；部分写按失败处理的口径注明；魔法数字（64KiB 分块、DWORD 上限）来源在位。
- `test/AtomicFileTest.cpp`：文件头（契约 acceptance 1/3 对应＋接缝消费说明）；每个 TEST 锚定注释（需求/卡章节/契约条目编号）；夹具注释（进程 ID 隔离、清理失败保留现场、EnvUnavailable 致命退出口径）；fake 装饰器形态注释（不转发＝动作未发生）。
- `project/CMakeLists.txt`（增量段）：增列源文件/PRIVATE include 目录的注释块均注明出处（§12 PRJ-T02 行、acceptance 3、R-2）。
- §2.5 高危信息表：物理单位（length/chunk 单位字节、kWriteChunkSize 单位字节、osError 注明"Win32 系统错误码（无物理单位）"）；坐标系/角度制式——不适用（纯字节/文件层）；所有权（IFileOps* 非 owning、HANDLE 所有权移交、RAII 守卫）；线程约束（IFileOps 无状态实现线程安全、AtomicFile 同路径并发由调用方串行化——StoreLock 归 PRJ-T03）；错误语义（环境错误＝值返回透传原始码；空接缝＝调用方契约违约走异常 fail-fast——AtomicFile.cpp L173-181）；确定性来源（分块常量公开＝测试与实现同一事实来源）。

### 4.8 文档同步 —— PASS

- `units/project.md` v0.3→v0.4（在 allowedFiles 内）：diff 核对仅卡头文档版本行＋§15.4 v0.4 行两处，**本文契约语义零变更**（逐行核对属实）；§15.4 v0.4 行如实登记落位内容与证据路径；`src/Project.cpp` 头注释"PRJ-T02 已落位"更新（注释随代码更新——AGENTS §2.5）。
- 偏差登记：实现与任务卡/单元卡零偏差（三 acceptance 逐条兑现见 4.4；§7.2 行 1/2/3/6 实现载体语义逐行对照一致见 4.4-2；无超诺）。
- 状态/追踪同步：单元卡 v0.4 登记即本任务的状态载体（契约状态翻转归合入收尾 PIPE §4.7，与 PRJ-T01 同款）；validate-task/validate-docs 亲跑 PASS。
- **findings.json 未关闭条目相关性核对（v1.2）**：
  - F-019/F-051/F-072/F-115（门禁基线红）：直接相关——本任务门禁 8 命中全部落在该谱系、零 project 项（验收者逐条核对 target 名），报告 deltaAnalysis 与登记册一致。
  - F-010（.gitignore 吞留痕日志）：**本任务再现**——实施侧生成、提交信息声称的 9 份日志无一入库（根因同族；见 F-125 与 4.10）。
  - F-122（project.md 卡头日期行未随版本同步）：**本任务未顺手消账**——v0.4 编辑触及同一卡头（版本行已同步）而日期行仍停留 2026-09-09；F-122 保持 open，处置归文档所有者，建议随下次触及更正（此处显式注明，不静默积压）。
  - F-123（CoreEdgePresent 敏感性缺口）：本任务触及 project/CMakeLists.txt（增列源文件/测试 include 目录）但未改动守卫文本——F-123 形态不变、保持 open。
  - F-124（"冒烟模式无别名"表述失准）：project/CMakeLists.txt L97-99 该注释块不在本任务 diff 触及行内——保持 open。
  - F-065（门禁日志 mojibake）：验收者门禁日志同现同型乱码——家族沿袭，非新引入。
  - F-004（空转 MOC）：构建日志仍见 `Automatic MOC for target sdurws_ird_project`——同族沿袭。
  - 其余 open 条目（标题家族 F-012 系、幻影清单家族 F-081 系等）：与本任务面无交集——本任务**新增**该两家族各 1 例（→F-126/F-125）。
- 随本任务可顺手消账项：无（F-122/F-123/F-124 处置权均登记在其他辖域，且涉及卡面文本更正/守卫语义/DTB 源头行，按 PA-1 不越权代改）。

### 4.9 commit 质量 —— PASS（附建议级 F-125/F-126）

- 提交一 290296ff：标题 `[WP-04-T02] 实现 project 存储原语：…`——前缀＋动词开头合规；正文四段齐备（What 按文件列要点/Why 对应 NFR-REL-01＋卡行＋卡章节/How 双模式构建测试＋门禁＋真实失败能力留证/影响面）＋`Acc-Covered: 1, 3` 对账行；验证结论与验收者独立复现逐项一致。
- 提交二 04315ecb：标题 `[WP-04-T02] §7.2 官方文档口径逐行复核留痕＋…`——前缀后以**节号＋名词短语**开头，非动词开头 → F-012 家族再现（同日前例 F-121 之后）→ 转登 **F-126**（纯风格，既往判例均不阻断）。正文四段齐备＋`Acc-Covered: 2`；验证段全部结论（双模式 18/18＋5/5、verify exit 0、validate-task/validate-docs PASS、ird_gates 8 命中零新增）经验收者独立复现**全部为真**——无结论性粉饰。
- 但 What 段声称新增"4 份 gtest XML＋**9 份构建/测试/门禁/校验日志**"——实际入库仅 6 件（XML×4＋报告＋复核记录），**日志 0 份入库**（`git show 04315ecb --stat` 实证 7 文件，含 project.md）→ 幻影清单家族再现 → 转登 **F-125**（F-010/F-081 家族；结论性内容为真、清单性内容失实，按家族判例 suggestion 级）。

### 4.10 留痕完整 —— PASS（附建议级 F-125）

- gtest XML×4：**全部在库**（testsuites 属性 18/18/5/5、failures=0——与验收者双模式复跑一致）。
- `ird-test-report.json`：**在库且内容准确**——抽查的全部可复核断言（23 用例两模式、13 新用例名册、门禁 8 命中基线逐码、validate-task PASS、acceptance 逐条证据指向）均与验收者独立复现一致。
- 构建日志：**声称 9 份、入库 0 份**（集成/冒烟配置·构建·测试运行·verify-target·门禁·validate-task/validate-docs 日志均不在送验提交中；报告 gates.validateTask 字段引用的 `validate-task.log` 亦为库外幻影引用）→ 计入 **F-125**。按 ACC §5 判据不属于阻断项（构建/测试可复现性已由验收者亲手建立，结论无粉饰），按 F-081/F-102/F-103/F-106/F-108/F-111/F-113/F-114 家族既有判例登记为建议级。
- `phase-one-validation.log`／resultNote：未更新——与 CORE-T01 之后惯例一致（PRJ-T01 同判例）；`ird-test-report.json` 的 acceptance 逐条对账表承担 resultNote 载体，按 ACC §4.10"或"分支判定成立。

### 4.11 偷懒/缩水实现扫描 —— PASS

- ① 标记扫描：diff 全部新增行扫描 `TODO|FIXME|XXX|HACK|stub|placeholder|not implemented|未实现` **零命中**（grep 实证；契约亦无"触发式延后"标注需要豁免）。
- ② 空实现检测：`IFileOps` 纯虚接口＝契约 acceptance 3 显式要求的注入接缝（D-10 形态，非缩水）；`Win32FileOps` 六方法均有真实 Win32 封装体；`AtomicFile::writeThrough` 编排逻辑完整（四步＋RAII 守卫），publishNew/replaceFile 直通为门面语义（注释明示"提交点语义单一性"）；`writeChunk` 零长度早退为注释明示的合法边界。零"仅返回默认值"式空实现。
- ③ acceptance 逐条证据表：见 4.4——三条逐条具名（测试名/XML 属性/文件路径/官方文档页），无一条无证据；`ird-test-report.json` acceptance 数组三条证据指向经抽查全部属实（仅日志类引用为幻影→F-125）。
- ④ 测试强度：断言校验行为与边界（字节逐一相等、截断、空载荷、残留恰为一块、错误码透传、调用序零发布点、旧字节不变），2 组实现级探针注入即红（4.5）——非恒真、非"只编译通过"；断言含负向（EXPECT_FALSE）与正向、含值断言与存在性断言组合，读失败显性失败（readAll ADD_FAILURE 防假阳性通道）。

---

## 2. 裁决

**verdict = pass**（4.1～4.11 全部通过，零阻断级问题；不阻塞合入）

建议级问题 2 项，已按 ACC §5 强制转登 `traceability/findings.json`（编号顺延自 F-124）：

| 编号 | 摘要 | 严重级 |
| --- | --- | --- |
| F-125 | 幻影清单家族再现：04315ecb 正文声称"9 份构建/测试/门禁/校验日志"实际入库 0 份（F-081 系第 15 例；ird-test-report.json gates.validateTask 引用 validate-task.log 同为库外幻影引用）；根因沿袭 F-010（.gitignore `*.log` 吞 traceability/**）。验证结论本身经验收者独立复现全部为真，按家族判例不阻断 | suggestion |
| F-126 | 提交标题家族再现：04315ecb 标题前缀后以"§7.2 官方文档口径…"（节号＋名词短语）开头，非动词开头（F-012 系，同日前例 F-121 之后的同日再例）；纯风格瑕疵，四段齐备、结论与留痕一致 | suggestion |

## 3. 验收者环境说明

- 复现现场：`git worktree add --detach D:/10_Source_Repos/21_robot/acc-prjt02/repro 04315ecb…`（detached @SHA，非分支名）；冒烟构建树＝`acc-prjt02/smoke-build`（独立目录）；记录回传：`git worktree add -b acc/PRJ-T02/2 D:/10_Source_Repos/21_robot/acc-prjt02/rec origin/redesign-main`。
- 集成配置参数（F-062/F-098/F-099/F-101 口径）：`cmake -S <worktree>/RobWork -B <worktree>/build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=D:/10_Source_Repos/21_robot/RobWork/vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64 -DCMAKE_CXX_FLAGS="-DNOMINMAX -DWIN32_LEAN_AND_MEAN -DWIN32" -DRWS_USE_STATIC_LINK_PLUGINS=ON`；`RobWorkStudio.ini.template.static/.shared.in` 自主树复制入 worktree（git 未跟踪本地资源——F-062 登记件）。
- 探针方法：实现级注入须重编译——Edit 修改源文件（新 mtime）→ 增量构建 → 滤跑目标用例 → `git checkout --` 还原 → 确认工作树干净 → 全量重跑复绿（规避 F-068/F-075 mtime 陷阱）。
- 官方文档核对：验收者现场拉取 Microsoft Learn 三页（CreateFileW/MoveFileExW/ReplaceFileW）逐句核对送验复核记录所引原文（2026-09-15）。
- 本记录证据日志（configure/build/test-run/gates/探针原始输出）留存于验收者临时证据目录（随 worktree 清理）；持久证据以本记录内联关键输出＋送验提交自带 6 件留痕（已逐一核对实际入库与内容真实性）为准。
- 独立性声明（ACC §2）：全新上下文；仅消费契约/设计文档/送验产物与留痕，未消费实施者叙述；构建与测试全部亲手重跑；对抗式立场下逐项取证（为"不通过"找证据：实测门禁、双探针、官方文档抽查、幻影清单比对）。

## 4. 记录三元组（回传给编排者）

```
branch: acc/PRJ-T02/2
path:   RobWork/doc/industrial-robot-design/traceability/acceptance/PRJ-T02-20260915.md
commit: （见本分支推送后记录提交的 40 位 SHA——由验收会话随最终报告输出）
```
