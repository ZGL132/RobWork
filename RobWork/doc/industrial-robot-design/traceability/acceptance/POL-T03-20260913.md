# POL-T03 验收记录（对抗式验收）

| 字段 | 值 |
| --- | --- |
| 任务 ID | POL-T03（≙WP-07-T03，policy 策略编码与内容身份——PolicyCodec 双形态编码＋CR-02 摘要单点） |
| 契约 | tasks/foundation/POL-T03.json（status: ready，随收尾翻转 done；requirements CON-05/CON-06；acceptance 3 条；knownPitfalls CR-02） |
| 验收对象 | 分支 `wp07-t03`，head＝`35127c62f41248ee34dd42cd07529a2d81890d97`（40 位冻结对象）；漂移检测：验收起止两次核验 origin/wp07-t03 == 35127c62（零漂移）；范围＝`ab4f3ba2e6a7259d97b686db16bba22f0762a6f5..35127c62`（＝origin/redesign-main 尖端..headSha，4 提交：实现＋留痕登记×2＋留痕补登＋单元卡 v0.4） |
| 尝试序号 | attempt=1（evidence 分支 `acc/POL-T03/1`） |
| **verdict** | **pass**（4.1~4.11 全过；无阻断级问题；无新增建议级问题——既有 open 项再现均属治理/构建约定/其他单元辖域，逐条注明延续，不新增编号） |
| 验收日期 | 2026-09-13 |
| 独立性声明 | 全新上下文，只消费契约/单元卡/代码/留痕产物；未读实施叙述。补偿性对抗动作＝worktree A（detached @ 35127c62）亲手全新配置双模式构建＋全量测试（53/53×2）、两个独立缺陷探针（均为验收者自行设计的 .cpp 变异，与实施侧探针位点不同：①规则列表排序禁用②token 码面改写，还原后复绿＋git status 零残留核验）、ird_gates 分支树亲跑（命中 5 项与送验日志逐字一致）、单元卡 §5.3/§4.1/§4.2/§12/§15.4 权威原文与实现逐点比对 |

---

## 逐项证据（4.1~4.11）

| # | 检查 | 结论与证据 |
| --- | --- | --- |
| 4.1 | 改动范围 | `git diff --name-status ab4f3ba..35127c62`：22 文件＝8 个代码/构建文件（policy/CMakeLists.txt＋policy/include/sdurws/ird/policy/{Errors,PolicyInput,PolicySet}.hpp＋policy/src/{Errors,PolicyInput}.cpp＋policy/test/{PolicyInputTest,PolicySetTest}.cpp）全部位于 `policy/**`（allowedFiles）＋11 个留痕文件（traceability/builds/wp07-t03/，DoD §4.2 留痕口径）＋units/policy.md（allowedFiles 显式列出，+1 行＝§15.4 v0.4 变更记录）；forbiddenFiles（REQUIREMENTS.md/ARCHITECTURE.md）零触碰（路径过滤 diff 为空）；框架源码（RobWork/RobWorkSim/RobWorkStudio 非 industrialrobot 部分）零触碰；patches/ 无新文件——SA-02 零框架源码改动。4 提交＝a0fb9bb4（实现）＋3faa8534（留痕）＋08025e8d（留痕补登）＋35127c62（单元卡 v0.4） |
| 4.2 | 可复现构建 | worktree A（`git worktree add --detach` @ 35127c62，全新 build 树）：按 F-007 已登记口径两步处置（①配置命令补 `-DCMAKE_TOOLCHAIN_FILE=…/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`——自主树 CMakeCache 读取；②复制主树 git 未跟踪的 RobWorkStudio.ini.template.static 至 wtA——未改动任何被跟踪产物）→ 配置 exit 0 且缓存确认 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`（配置命令含 -D 显式 BOOL=ON）；`cmake --build build --config Release --target sdurws_ird_policy_test` exit 0、`: error` 零行（日志中 C4099×14＝F-071 既有、MSB8029×12＝临时目录环境告警、Eigen C4996＝框架基线，均非本 diff 引入；PolicyInput 编译零告警）；测试 53/53 PASSED（验收者亲手运行）。冒烟：独立临时目录 ird-acc-POLT03-smoke＋vcpkg toolchain 配置＋构建 exit 0；测试 53/53 PASSED（均验收者亲手执行） |
| 4.3 | verify 命令 | 契约 verify（单条）＝`cmake --build build --config Release --target sdurws_ird_policy_test`——在 worktree A 集成树亲手独立复跑 exit 0（ird-acc-polt03-verify-rerun.log）；附带运行测试可执行文件 53/53 全绿 |
| 4.4 | acceptance 逐条 | ①"POL-ID-1~5：canonical 往返＋位模式＋排序无关性用例通过"：验收者亲手双模式各 53/53（XML tests=53 failures=0×2）；具名用例全落位——往返 RoundTripFull/MinimalFieldsPreservesAll_POL_ID_1（逐字段相等＋二次编码不动点）、确定性 DeterministicEncodingAndIdentity_NFR_COR_02、排序无关 RuleListOrderInvariance/UnorderedPairEndsInvariance/SetOrderAndDuplicateInvariance_POL_ID_2、显示单位不入身份 DisplayUnitNotInIdentity_POL_ID_3、阈值变化新身份 ThresholdChangeYieldsNewIdentity_POL_ID_4、位模式 CanonicalF64RoundTripPreservesExactBits（±0/极值/次正规位级 memcmp 往返）；②"近似相等禁入身份"：NearEqualThresholdsDistinctIdentities_POL_ID_5（1 ulp 与 1e-15 两级差异身份必不等——std::nextafter 构造）＋OneUlpThresholdDifferenceChangesBytesAndIdentity_Acceptance2（full 形态字节面＋身份面双差异）；③"CR-02 处置约束"：DigestViaCoreContentDigesterOnly_CR_02（投影字节经 core::ContentDigester 手工重算逐字节一致——若存在第二摘要路径即失配）＋ExcludedFieldsPinned_CR_02（policyObject/origin/兼容注记变化→full 编码变而投影与身份不变）＋ThresholdValueOriginNotInIdentity（来源标注不影响——§4.1 原文"显示单位/字段排序/来源标注不影响"）；实现层核实：全单元唯一摘要调用点在 contentIdentity（PolicyInput.cpp L893-894，栈上 ContentDigester 实例），无任何其他哈希实现/常量（grep 全单元零命中）。ird_gates（WP-01-T01 已建成，如实执行）：验收者亲跑 `cmake -DIRD_ROOT=<wtA>/…/industrialrobot -P cmake/ird_gates.cmake`——命中 5 项（IRD-GATE-SUB×4＋T1×1，全部指向 sdurws_ird_evidence_test/evidence_contract_test/runtime_test/testdata_lint，均不在本任务 allowedFiles 内＝base 既有、非本任务引入），与送验 ird_gates-current-wp07t03.log 逐字一致；引擎自测 8 项按预期；ird-test-report.json gateAll 节含 base ab4f3ba2 快照复跑对照方法；validate-task.ps1 PASS（验收者亲跑，exit 0） |
| 4.5 | 测试真实失败能力 | 探针一（验收者设计：PolicyInput.cpp normalizedPairRules 的 std::sort 禁用——POL-ID-2 排序机制变异）→ RuleListOrderInvariance_YieldsIdenticalBytesAndIdentity_POL_ID_2 如期变红（1 failed / 52 passed）。探针二（验收者设计：Errors.cpp token 表 "policy/encoding-invalid"→"policy/encoding-mutated"——码面全表锚定变异）→ PolicyErrors.TokenTableFullEnumeration_NFR_COR_02 如期变红（1 failed / 52 passed）。两探针均为 .cpp 变异、首次重建即红（F-068/F-075 增量陷阱本轮未触发——无头文件注入形态）；证明排序无关性断言与码面全表断言均真实联动实现行为、非恒真。探针还原＋重建 → 53/53 复绿；`git status --porcelain -- RobWork` 零残留核验（送验对象未被污染）。与实施侧 mutation 自证（contentIdentity 摘要输入截断→DigestViaCoreContentDigesterOnly_CR_02 变红，ird-test-report.json failureCapabilitySelfCheck 节）相互独立、位点不同 |
| 4.6 | 架构红线 | ①零 Qt：grep 实证 policy 全部源/头/测试 include 面无 Qt 头（唯一命中＝BuildRedLineTest.cpp 红线守卫自身扫描逻辑；构建日志 MOC 行为 F-004 既有空转，实证无 Qt 依赖）；②R-1 无业务域互链：policy 库链接面本任务零改动（CMakeLists diff 仅增两源文件，链接节未动；R-5 proximity 直链为 policy.md §3.2/DTB §4.5 登记的唯一许可）；③R-2 跨单元私有头：PolicyInput.hpp/.cpp 对外 include 仅 core 公共头 Digest.hpp（include/sdurws/ird/core/ 内）＋policy 自有头，零跨单元私有头；④R-4 无名称前缀拼接（本类型层不涉名称解析）；⑤PA-1 权威唯一：摘要算法唯一归 core ContentDigester（CR-02 本体）；registryCode 仅"建议"不注册（注释明示 PA-1 不越权）；规则集语义不在此层实现（归 POL-T04）；⑥PA-2：EngineeringPolicySet 不可变封装未触碰；RawPolicyInput 为纯值承载（身份只对发布态计算——契约注释与 §4.1"配置≠身份≠已验证"一致）；⑦SA-02 零框架改动（见 4.1）；⑧无 old/ 恢复（全新实现）。AGENTS §5 八条逐条过 |
| 4.7 | 注释规范 | 公共头 PolicyInput.hpp 全查（529 行）：文件头含设计依据（章节/需求/契约编号逐项）＋背景说明（管线位置/身份两层区分/CR-02 处置/浮点纪律/确定性来源）＋线程安全声明；每类/每函数 Doxygen 中文（@param 方向与单位/@return/@throws 逐项——encode 4 个 @throws 触发条件逐一写明）；§2.5 高危信息齐备——物理单位（SI 真值 m/rad/无量纲逐字段，L221"SI rad 语义"、L217"无量纲"）、坐标系（不涉位姿）、角度制式（rad 显式）、所有权（bytes 指针"调用方保证可解引用 8 字节；本函数不接管所有权"L518）、线程约束（逐类"线程安全：纯值"声明＋L64-65 全实体可重入）、错误语义（encode/contentIdentity 失败＝调用方契约违约 fail-fast vs decode 失败＝字节损坏 EncodingInvalid 双轨明示 L352-359）、确定性来源（规范序/大端/presence/无 locale 无 I/O 无时间随机源 L59-62）；魔法数字有来源（0x00/0x01 形态字节、4π＝附录 D 第 11 项、kHeaderSize=16 逐段构成式注释 L52）。实现 PolicyInput.cpp 全查（951 行）：文件头实现纪律 4 条＋错误语义＋线程/确定性声明；每函数/每语义段中文注释（fail-fast 校验序①~④逐条、编码规范序机制、绝不按 count 预分配的 why）。Errors.cpp 全查＋Errors.hpp/PolicySet.hpp diff 段全查（inline ODR 修复附完整 why 注释）。测试 PolicyInputTest.cpp 全查：文件头设计依据＋范围声明（解析管线级观测归 POL-T04 的边界写明）＋用例名带追溯编号＋断言处中文注明验证条款。缺项未发现 |
| 4.8 | 文档同步 | units/policy.md §15.4 v0.4 变更记录（+1 行）登记实现细节 ①~⑦，验收者逐点与实现/权威章节比对一致：①双形态编码与帧格式＝PolicyInput.cpp buildFrame/write*/read* 与 §5.3 表逐行对应（magic IRDPOL1/长度前缀/大端/presence 字节/位模式/NaN 入口拒绝/schemaVersion 参与身份）；②编码规范序＝normalizedSet/normalizedPairRules/orderedPairEnds/scopeTargetLess 实现与登记逐字对应；③"来源标注不入身份"有 §4.1 原文锚点（"显示单位/字段排序/来源标注不影响"——非私裁），实现＝语义投影只编 siValue，测试 ThresholdValueOriginNotInIdentity 钉住；④RawPolicyInput 保留原文（§5.2 行 3 原文锚点）＝RawThresholdInput{value,unitToken} 可 NaN 驻留＋encode 拒绝（SA-15 口径）；⑤EncodingInvalid 表尾追加（Errors.hpp 既定扩展通道，14→15 值，PolicySetTest 全表锚定同步 L151-198 实测 15 值）——§9.2"字节损坏在解码期报错"锚点；⑥ODR inline 修复＝PolicySet.hpp diff 仅补 inline＋注释（语义零变化，登记在案）；⑦CR-02 摘要单点＝实现与测试双证。语义零偏差声明属实；§12 POL-T03 行产物清单（PolicyInput.hpp/.cpp：RawPolicyInput/PolicyCodec/contentIdentity）与交付一致；契约 status 翻转归收尾（PIPE §4.7⑥）不算偏差。findings.json 未关闭 61 条逐条核对：与本任务执行直接相关项——F-007（全新树配置缺口，本次两步复现并按其口径处置）、F-008（全新树 RobWorkSim 构建失败——本验收目标构建未触及 RobWorkSim 未受阻）、F-009/F-062（gate-all/worktree 环境前置同族）、F-004（MOC 空转，构建日志再现）、F-071（core/Units.cpp C4099×14，构建日志再现）、F-019/F-072（ird_gates 既有 5 命中，本次亲跑再证实）、F-065（留痕日志中文双重编码乱码——本次送验 gate-all.log 门禁段再现）、F-010（*.log 被 .gitignore 吞——实施提交 08025e8d 正文如实登记补登始末并以 add -f 处置，属该既有 open 项的操作性再现）、F-068/F-075（探针增量编译陷阱——本轮两探针均 .cpp 变异未触发，无新形态）；上述均属治理/构建约定/其他单元辖域，无本任务可顺手消账项，亦无静默积压 |
| 4.9 | commit 质量 | 4 提交逐一核查：主提交 a0fb9bb4 标题"[WP-07-T03] 实现策略编码与内容身份（PolicyCodec 双形态编码＋CR-02 摘要单点）"——前缀＋动词"实现"开头✓；正文四段齐备（What 逐文件 9 项/Why 对应 acceptance 1~3＋§3.1/§5.3/§5.1 依据/验证 六项含 mutation 自证/影响面 兼容性＋对 POL-T04/T05 的消费约束）；验证结论与验收者实测一致（53/53 双模式✓、gate 5 项 base 既有✓、留痕路径✓）——无失实。3faa8534（留痕登记）与 08025e8d（留痕补登，正文如实说明 *.log 被全局忽略规则吞掉的疏因与 add -f 处置——如实陈述不粉饰）均四段齐备；35127c62 用 [units] 前缀（文档修订，AGENTS §6.2 既有前缀✓）。未发现粉饰 |
| 4.10 | 留痕完整 | 送验留痕 11 份齐且 git ls-files 确认全部入库（11/11，08025e8d 补登后零缺漏）：集成/冒烟构建与配置日志×5＋双模式测试运行日志×2＋gtest XML×2（integration/smoke 均 tests=53 failures=0，与验收者亲手复跑一致）＋ird-test-report.json＋gate-all.log；ird-test-report.json（schema ird-test-report/1）含逐用例 53 条 trace 登记（suite/设计锚点/需求追溯/modes）、failureCapabilitySelfCheck（mutation 探针自证）、contractVerifyCommand、gateAll 节（16/17 判定＋base 对照方法）、notExecuted:[]（无未执行项虚挂）、summary 53/53 双模式——即 resultNote 载体已更新；phase-one-validation.log 未更新——沿 TK-T10/TK-T11 先例（foundation 任务以 traceability/builds/<task>/ 证据包替代） |
| 4.11 | 偷懒/缩水实现扫描 | ①TODO/FIXME/stub/placeholder/未实现/not implemented 扫描 diff 范围（industrialrobot 面）零命中（grep 退出码 1）；②无空实现/仅返回默认值——encode/decode 双形态实逻辑（ByteWriter/ByteReader 逐字段实表、fail-fast 校验序、每步越界校验、精确耗尽终检）、contentIdentity 实摘要（唯一调用点）、canonicalF64/parseCanonicalF64 位模式实逻辑（memcpy＋大端重组＋双端非有限拒绝）、normalize 通道实排序（专用比较器严格弱序）；契约无"触发式延后"标注故无豁免诉求；③acceptance 逐条证据表见 4.4（三条均有具名测试＋断言逻辑＋实现行号，CR-02 另有实现层唯一调用点核实）；④测试强度：断言校验行为与边界（位级 memcmp 往返、±0/极值/次正规样本集、截断/尾部垃圾/长度字段篡改/未知枚举/非法 presence/信号 NaN 载荷位、布局锚断言而非字节扫描——注释写明误匹配风险考量）＋4.5 双探针实证会红——非恒真、非"只编译通过" |

## 问题清单

- 阻断级：无。
- 建议级：无新增（无新发现可转登——findings.json 不新增 F-xxx 编号）。
- 既有 open 项延续（不新增编号，本任务留痕/验收过程再现）：F-065（gate-all.log 门禁段中文双重编码乱码）；F-007/F-008/F-009/F-062（worktree/全新树环境前置，本次按已登记口径处置）；F-004/F-071（构建日志再现，均非本任务引入）；F-019/F-072（ird_gates 既有 5 命中，本次验收者亲跑再证实）；F-010（*.log 全局忽略吞留痕——实施侧 08025e8d 已按补登处置并如实登记，根因治理项维持 open）；F-068/F-075（探针增量编译陷阱——本轮未触发，维持 open 备验后续任务）。

## 验收者环境说明

- 复现现场：`git worktree add --detach C:/Users/zgl18/AppData/Local/Temp/ird-acc-POLT03-wtA 35127c62f41248ee34dd42cd07529a2d81890d97`（detached @ 冻结 SHA，非分支名）；集成树全新配置于 wtA/build；冒烟树独立于 Temp/ird-acc-POLT03-smoke。
- 全部命令（配置/构建/测试/verify/ird_gates/validate-task/双探针）由验收者本会话亲手执行；探针注入与还原均经 `git status` 零残留＋53/53 复绿双重核验（防旧二进制假绿）。
- 环境缺口处置（F-007 既有，两步）：①配置命令补 `-DCMAKE_TOOLCHAIN_FILE=vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`（自主树 CMakeCache 读取）；②复制主树 git 未跟踪的 RobWorkStudio.ini.template.static 至 wtA——未改动任何被跟踪产物。
- 送验对象冻结复核：验收起止两次核验 origin/wp07-t03 == 35127c62（零漂移）；验收者未向任务分支推送任何提交；实施工作树（主树 wp07-t03 检出）未触碰；pipeline/state.json 与 lock 未读写。
- 本记录落 evidence 分支 `acc/POL-T03/1` 并推送，commit SHA 见验收请求回执三元组（本任务无新增建议级发现，findings.json 无需转登——无随附 findings 变更）。
