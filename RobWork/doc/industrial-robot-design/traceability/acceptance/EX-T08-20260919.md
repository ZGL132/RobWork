# EX-T08 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 ID | EX-T08（检查点与缓存存储治理 Checkpoint/CacheCoordinator；契约 tasks/foundation/EX-T08.json，status=ready） |
| 送验对象（冻结） | `1cc1c796aa6851624cb20b0fa2fb684b98b45bc2`（分支 wp08-t08 尖端；**验收全程按该 SHA**） |
| diff 范围 | `1ac72f5b38d9e2b9cd1965d20c4386f6d92bffa8..1cc1c796aa6851624cb20b0fa2fb684b98b45bc2`（base＝state.base＝1ac72f5b＝origin/redesign-main 尖端；线性三提交：f9db4c3a 实现＋3cfcf13a 文档同步与留痕＋1cc1c796 补全留痕） |
| 分支漂移检测 | `git rev-parse origin/wp08-t08` → ＝`1cc1c796…`＝冻结 SHA（fetch 后实测）——**零漂移**（ACC §3） |
| 复现现场 | `git worktree add --detach acc_worktrees/ext08-A 1cc1c796…`（detached @ SHA）；独立 worktree 全新配置全新 build/，无实施构建树残留 |
| 记录回传 | 本记录落 `acc/EX-T08/1` 分支（`git worktree add -b acc/EX-T08/1 acc_worktrees/ext08-B origin/redesign-main`）并推送；attempt=1 |
| **verdict** | **pass**（4.1~4.11 全部通过；零阻断级问题；3 项建议级问题 G-1~G-3 已按 ACC §5 转登 findings.json F-218/F-219/F-220，不阻塞合入） |
| 验收日期 | 2026-09-19 |
| 验收者环境 | Windows 10 x64（10.0.26200）；VS 2022 Community（MSVC 19.42.34433）；CMake；pwsh 7（治理脚本）；**独立 worktree 复现**，与实施会话零共享上下文（ACC §2 四条独立性要求逐条满足：全新会话／未读任何实施叙述、仅消费产物与文档／对抗式立场逐项找反证／构建测试全部亲手重跑） |

---

## 环境前置（ACC §3 复现口径）

- 集成模式：worktree A 内全新 `cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON`，对齐主树缓存口径（F-007/F-098/F-099 已登记的环境链）：`-DCMAKE_TOOLCHAIN_FILE=<repo>/vcpkg/scripts/buildsystems/vcpkg.cmake`、`-DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`。配置 exit 0；新缓存 `grep RWS_BUILD_INDUSTRIALROBOT build/CMakeCache.txt` → `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON` 实证。
- 未跟踪辅助件（F-007/F-200/F-099 已登记）：自主工作树复制 `RobWork/RobWorkStudio/bin/{RobWorkStudio.ini.shared.in,RobWorkStudio.ini.template.static}` 两件至 worktree A 后配置通过（fresh 树硬依赖——验收者首配即踩中 F-007，与登记完全一致；先例 EX-T07 attempt 1 / IO-T06 attempt 2 同口径）。
- 基线宏定义（F-098/EX-T07 先例）：首次全量构建未带全局 `-DNOMINMAX -DWIN32_LEAN_AND_MEAN -DWIN32`，复现 F-008 已登记基线缺陷——73 处 error 全部位于 RWSimulatorPlugin.vcxproj（71）＋sdurws_plugin.rwplugin.vcxproj（2），**industrialrobot 域零错误**；补齐三宏定义重配后全量重建 exit 0（与本单元 EX-T07 验收同口径，基线缺陷非本任务引入）。
- 冒烟模式：另配全新目录 `acc_worktrees/ext08-A-smoke`，源＝worktree A 的 industrialrobot/，**带 vcpkg toolchain 绝对路径**（CORE-T01 验收 G-2 教训）。

## 4.1 改动范围 —— 通过

- diff 文件清单（`git diff --name-status 1ac72f5b..1cc1c796`）：24 个文件＝execution 单元 7 个（CMakeLists.txt 改、Checkpoint.hpp/cpp 新增、CacheCoordinator.hpp/cpp 新增、test/CheckpointCacheTest.cpp 新增、contract_test/CacheJudgeContractTest.cpp 新增）＋units/execution.md 1 个（§15.4 增 v0.10 一行）＋traceability/builds/wp08-t08/ 留痕 16 个。
- 逐个对照契约 `allowedFiles`（`execution/**`、`units/execution.md`、`traceability/**`）：**24/24 全部命中**（机械过滤非三前缀文件结果为空集），零越界。
- `forbiddenFiles`（REQUIREMENTS.md、ARCHITECTURE.md）：diff 中**零触碰**。
- SA-02：diff 无任何 `industrialrobot/` 之外框架源码；`patches/` 目录无新文件（0001-rwslibs-entry-option.patch 为 base 既有）——零未登记框架改动。

## 4.2 可复现构建 —— 通过（验收者亲手重跑）

- 集成模式全新配置后全量 Release 构建（含三宏基线口径）：`final-exit=0`，`error C[0-9]+|error LNK[0-9]+|error MSB[0-9]+|fatal error` 计数 **0**（日志 acc_worktrees/ext08-A-build-full2.log；此前无宏构建的 73 错全部定位为 F-008 基线 RWSimulatorPlugin/rwplugin 域，与 industrialrobot 无关——对抗式排查排除本任务嫌疑）。
- 冒烟模式：配置 exit 0（Configuring done/Generating done，零 error）＋构建 exit 0、错误计数 0（acc_worktrees/ext08-A-smoke-config.log、ext08-A-smoke-build.log）。
- 实施留痕声称（build-integrated-full.log/build-smoke-config.log/build-smoke-full.log 三件在库）与验收者复跑结论一致：在库日志错误计数均为 0。

## 4.3 契约 verify 命令 —— 通过（2/2）

- verify[1]：`cmake --build build --config Release --target sdurws_ird_execution_test` → worktree A 集成树亲手执行 **exit 0**（sdurws_ird_execution_test.exe 产出）。
- verify[2]：`cmake --build build --config Release --target sdurws_ird_execution_contract_test` → 亲手执行 **exit 0**（sdurws_ird_execution_contract_test.exe 产出）。
- 冒烟树两测试目标同样构建成功并运行（见 4.4）。

## 4.4 acceptance 条目逐项核对 —— 通过（5/5 有证据）

| 契约 acceptance | 验收证据（验收者亲手） |
| --- | --- |
| 1. EX-CKP-1~3 全绿：篡改一字节→EX-CHECKPOINT-CORRUPT 拒绝恢复且原文件保留；checkpointFormatVersion/contractVersion 失配→EX-CHECKPOINT-INCOMPATIBLE＋reasons（判定归 evidence）；恢复失败原检查点完整保留（磁盘断言） | 集成＋冒烟两模式 143/143 全绿（验收者实跑两遍）；`EX_CKP_1_TamperedByteRejectedAndFilePreserved`（test/CheckpointCacheTest.cpp L461）：篡改批次文件第 2 字节最低位→Corrupt＋`EX-CHECKPOINT-CORRUPT` 诊断＋**目录快照逐字节相等**（篡改后快照为比对基准，重复恢复走 Discarded 且快照仍相等）＋废弃项出候选；`EX_CKP_2_VersionMismatchIncompatibleWithReasons`（L526）：存 7 请求 8→Incompatible＋reasons==["contract-mismatch"]（evidence token 溯源）＋不删盘＋同版本重试恢复成功对照；`SliceMismatchIncompatible`（L555）"slice-mismatch" 同证；`EX_CKP_3_NotResumableRefusedAndPreserved`（L573）：resumable=false→NotResumable＋**磁盘断言完整保留**＋未标废弃（可诊断性）；契约测试 `EX_CKP_3_NotResumableRefusalWiring`（contract_test L378）真实目录逐文件字节断言 |
| 2. P-EX-9 处置：旧版本检查点不迁移（Incompatible 拒绝——宁重算不可错续） | 结构面：src/Checkpoint.cpp 全文读毕——restore/listCompatible 全部经 evidence judgeCheckpointCompatibility 判定，**不存在任何迁移/升级代码路径**；写出面＝`WriteStampsCurrentEnvelopeVersion`（L384）：请求携带版本 99 被当前版本 1 无条件覆盖（信封版本所有权＝execution——本实现写出的永远是当前版本，"旧版本"只能来自历史存储装载）；区间判定归 evidence kCheckpointFormatVersionMin/Max（EV-T09 套件已验，本任务消费） |
| 3. EX-CCH-1/2：Quick 命中不替代 Verified（mode-mismatch→Incompatible——判定来自 evidence）；失败/取消不入正式缓存（仅 finalize 产物入缓存）；命中≠当前 | `EX_CCH_1_ModeMismatchNeverShortPath`（L892）：Quick 条目×Verified 请求→Incompatible＋reasons[0]==ModeMismatch（真实 evidence judgeCacheHit）＋guidance 恒 DispatchNormal（不派发伪装）＋同模式 FullHit 短路径对照＋inputBaselineId 随条目可见＋命中无当前性字段；`EX_CCH_2_StoreGateOnlyFinalizedCompleted`（L818）：五面构造（Completed+Archived 入 / Completed+Archiving 不入 / Running 不入 / Canceled、Failed 不入 / 未登记 fail-fast）＋唯一正例命中对照；`PartialBatchesStayDiagnosticOnly`（L862）：部分批次仅 DiagnosticOnly＋guidance 恒 DispatchNormal＋分账断言（diagnosticEntries=1、resultEntries=0）；契约测试 `EX_CCH_1_LookupVerdictEqualsDirectEvidenceJudge`（L415）Quick↔Verified 双向（方向三独立切片排除干扰） |
| 4. 兼容判定逻辑零复制：经 evidence/runtime 注入 ICompileCacheJudge 消费——P-EX-3：runtime 零编译依赖 | 结构性证明：契约测试两用例（EX_CKP_2_RestoreVerdictEqualsDirectEvidenceJudge L337、EX_CCH_1 L415）以"协调器输出 ≡ evidence 直接调用输出逐字段相等"钉死无本地复判；实现面：lookup 唯一判定调用点＝evidence::judgeCacheHit（CacheCoordinator.cpp L193/L243），模型面唯一判定调用点＝注入 m_compileJudge->judge（L299）；链接面：CMakeLists target_link_libraries 三目标全查——sdurws_ird_execution→core/evidence/project、test/contract_test→execution+gtest、worker→core/evidence，**无任何 runtime 链接边**；include 面：新增 6 文件 include 扫描零 `sdurws/ird/runtime/`；ird_gates 零新增命中实证（见下） |
| 5. OPT-06 平台机制承载：缓存键绑定 sliceId/inputBaselineId；同键并发合并为等待同一运行（D-10） | `ResultCacheKey` 复合编址（sliceId＋模式＋契约版本＋Profile——CacheCoordinator.hpp L184-212，§8.2 编址原文）＋inputBaselineId 随 CacheEntrySummary 携带（EX_CCH_1 用例 L922-923 断言可见）；`D10_ConcurrentSameKeyMergedToInFlightRun`（L935）：同键首派发→后续查找 WaitForInFlightRun 指向首个运行→同键第二派发保留首个（inFlightRuns=1）→完成＋storeResult 解除后 FullHit 闭环→失败路径解除后回正常派发（等待者不悬挂）→异键隔离 |
- ird_gates 自查：验收者亲手 `cmake --build build --config Release --target ird_gates` → **exit 1**（既有登记例外红，门禁设计行为）＋最终汇总段命中 **17 项**，规范化 token 集合（LIB io/io_test/io_contract_test、SUB 10 测试目标、SUB testdata_lint 表外边、T1 testdata_lint、R4 project）与送验 t08 留痕、base(wp08-t07) 留痕同集——**零新增命中**（P-EX-3 的 runtime 零边实证）。

## 4.5 测试真实失败能力（验证验证者） —— 通过（2 个变异注入：1 红 1 存活，存活已定性）

| 变异 | 注入内容 | 结果 |
| --- | --- | --- |
| A | Checkpoint.cpp `verifyCheckpointFiles()` 头部注入 `return {};`（完整性校验恒通过） | `EX_CKP_1_TamperedByteRejectedAndFilePreserved` **红**（1 FAILED）——篡改检测面有检出力 |
| B | CacheCoordinator.cpp `storeResult` 双门槛摘除 `archivePhase != Archived` 半边（保留 Completed 判定） | `EX_CCH_2_StoreGateOnlyFinalizedCompleted` **仍绿——未检出**。定性：用例②（Completed＋Archiving 不登记面）与①正例共用同一 ResultCacheKey（Verified/kSlice/7/kProfile），门槛失效后②的非法登记落为**同键冲突保留既有**路径（resultEntries 维持 1、dev 通道两路皆真）——断言被冲突规则完全遮蔽。实现本体经代码审查确认正确（双门槛两半均在位：CacheCoordinator.cpp storeResult），属**测试敏感度缺口**而非恒真测试（与 F-216〔EX-T07 资源闸停派发执行面零检出〕F-213/F-146 先例同族）；**建议级 G-2/F-219 转登，不阻断** |
- 变异还原：Edit 精确还原两文件（还原后 `git diff` tracked 零改动）＋重建重跑 **143/143＋31/31 全绿恢复**——复现现场无污染。

## 4.6 架构红线（AGENTS §5 八条） —— 通过

1. **L2 零 Qt**：新增 6 文件 `#include <Q` 扫描零命中；链接目标无任何 Qt 依赖。
2. **业务域互链（R-1）**：CMake diff 依赖边零变更——sdurws_ird_execution 仍仅链接 core/evidence/project 三条登记边；ird_gates SUB/LIB 零新增命中实证。
3. **跨单元私有头（R-2）**：新增 include 全部为 include/sdurws/ird/{core,evidence,project,execution}/ 公共头（机械清单核查）；无 src/ 私有头跨单元引用。
4. **SA-02 框架零修改**：见 4.1。
5. **R-4 名称前缀**：新增 4 产品文件 `RobWork` 字面量扫描零命中；ird_gates R4 命中仍为 base 既有 project 1 处。
6. **权威唯一（PA-1）**：兼容判定权威归 evidence judgeCacheHit/judgeCheckpointCompatibility（直接消费）与 runtime judgeCompileCacheCompatibility（ICompileCacheJudge 注入——P-EX-3 处置件，§3.3 原文形态）；execution 只做存储/查找/淘汰/写入；manifest 发布归 project（begin→writeBatch→finalize，零直接磁盘写——restore 全部拒绝路径零磁盘副作用亦为代码审查证实）；不触碰修订/写路径与名称解析。
7. **不可变历史（PA-2）**：本任务不触修订/写路径——不涉。
8. **不继承 old/**：Checkpoint/CacheCoordinator 全部按 units/execution.md §8.1/§8.2/§10.6/§10.7 语义从零编写（复合摘要编码/候选集判定/分账淘汰均为新实现）；CheckpointId 复用 TaskTypes.hpp 已落位定义（其头注明文不重定义——零重定义）。

## 4.7 注释规范（AGENTS §2） —— 通过

- **公共头全查**（2/2）：Checkpoint.hpp（文件头设计依据＋磁盘职责声明＋P-EX-9 处置口径＋线程约束；CheckpointRecord 十六字段逐字段中文注释含单位〔sizeBytes 字节〕/不可变/所有权列；CheckpointFileRef/WriteRequest/WriteResult/ResumeQuery/RestoreRequest/RestoreResult/Candidate 逐结构 Doxygen；接口四方法 @brief/@param/@return/@throws 逐个——错误语义二分〔调用方违约 fail-fast vs 环境 ArchiveFailed 结构化〕逐处标注；computePayloadDigest 编码与确定性说明）/CacheCoordinator.hpp（文件头背景说明〔为什么判定不在本单元〕＋调度边界三态；ICompileCacheJudge/CompileCacheKeyView/CompileCacheVerdict 逐个含 P-EX-3 处置与 D-17 退役路径；ResultCacheKey 键绑定纪律、CacheLookup 消费纪律、EvictionPolicy 2 GiB 实现参数来源、CacheStats 快照语义；实现本体依赖注入/会话索引/确定性来源/锁结构说明）。
- **实现文件抽查**（≥3 达成，实查 4）：Checkpoint.cpp（文件头实现要点三条＋逐语义段落注释：契约校验分支逐条业务含义、重发布拒绝协议语义、摘要重算防伪造面、版本打戳所有权依据、abandon 责任结束、只读装载零写通道、判定单点调用与 reasons 零改写、markDiscarded 零磁盘副作用）/CacheCoordinator.cpp（文件头零复制可核查点声明＋D-10 合并语义、同键冲突保留既有、双门槛登记事实检查非兼容判定、fail-closed 一次性留痕、三级字典序平局裁决）/CheckpointCacheTest.cpp（文件头替身边界声明＋时钟纪律＋逐用例设计依据注释）/CacheJudgeContractTest.cpp（文件头结构性证明声明＋逐用例注释）。
- §2.5 高危信息表：物理单位（字节/2 GiB＝2×1024³ 显式换算）、坐标系（不涉——纯身份/摘要语义）、角度制式（不涉）、所有权（archivePort/diagnostics/registry 非所有权引用逐处标注）、线程约束（仅调度线程 vs 并发只读逐处标注＋互斥临界区"锁内零 I/O"纪律）、错误语义（fail-fast vs 结构化诊断二分＋EX-ARCHIVE-FAILED/EX-CHECKPOINT-CORRUPT/EX-CHECKPOINT-INCOMPATIBLE 稳定码归属）、确定性来源（relPath 字典序、注入时钟、主键字典序、三级平局裁决）——**必填项零缺失**。

## 4.8 文档同步 —— 通过

- 实现与任务卡偏差已按 DTB §5.4 登记：units/execution.md §15.4 v0.10 行（diff +1 行实证）逐件登记落位（含归置增量两处——CheckpointCandidate 配对结构补主键关联〔§10.6 返回类型原文的归置修正〕、CheckpointId 复用 TaskTypes §4.1；实现口径登记两处——缓存查找候选集结构〔精确键查表会吞 mode-mismatch〕、检查点写入口经注入 IResultArchivePort 消费〔project 侧 checkpoints/ 白名单写入口落地前真存储端到端联调不可行——如实登记不私改 project〕）；P-EX-9/P-EX-3/P-EX-1 处置留痕齐备。validate-task **PASS (1 tasks)** 为验收者亲手复跑。
- 任务状态：契约 status=ready（翻转 done 属收尾环节，验收期现状正确）；pipeline/state.json 未被本任务触碰（治理状态归所有者——提交 3cfcf13a 正文明示）。
- **findings.json 未关闭条目相关性核对（ACC §4.8 v1.2）**：head SHA 处 findings.json 共 217 条，逐条检索**无一 open 条目点名 EX-T08/wp08-t08/WP-08-T09**。相关族核对：①F-007（fresh 树配置环境链）——验收者复现现场即踩中，按登记口径复制未跟踪模板件解决，无新义务；②F-216/F-217（EX-T07 同单元测试面/警告面建议）——本 diff 对涉案文件（ResourceGovernanceTest.cpp 等）零触碰，无顺手消账义务；本任务 4.5 变异 B 新增同族 1 例（G-2→F-219 转登）；③F-012 标题家族——本任务再现 2 例（G-1→F-218 转登，见 4.9）；④F-008（基线 RWSimulatorPlugin 构建失败）——验收者无宏构建复现与登记一致，补宏后消失，非本任务义务。历史 open 条目无一项构成本任务阻断。

## 4.9 commit 质量 —— 通过（附建议级 G-1）

- f9db4c3a「[WP-08-T09] 实现 EX-T08 检查点与缓存存储治理（Checkpoint/CacheCoordinator 两件＋ICompileCacheJudge 注入落位）」：**"实现"动词开头合规**；正文四段（What/Why/验证/影响面）齐备；验证段结论（双模式构建零错误、143/143＋31/31、ird_gates 零新增、validate-task PASS）经验收者复跑全部证实。
- 3cfcf13a「[WP-08-T09] EX-T08 文档同步与验证留痕（单元卡 v0.10 登记＋双模式证据）」与 1cc1c796「[WP-08-T09] EX-T08 补全验证留痕（…）」：前缀后以任务号名词"EX-T08"开头非动词开头——**F-012 家族同型再现**（先例 F-215〔EX-T07 b2b384b5 同型昨日登记〕/F-206/F-208/F-210/F-211/F-214，均为 suggestion 不阻断）→ **G-1/F-218 转登**。两提交正文四段齐备；验证段数字与在库留痕一致。
- **无粉饰**：ird-test-report.json 的全部数字（143/143、31/31、21+3 新增、16 件证据清单）与在库 XML/文件逐一相符（XML 顶层 tests=143/31、failures=0 双模式四份实测）；gates 段 exitCode 1＋"非零＝既有已登记命中存在"如实标注（未粉饰为 0 命中）。

## 4.10 留痕完整 —— 通过

- 16/16 件留痕（构建日志×3、门禁 3 件、validate-task.log、两目标两模式 gtest XML×4＋run.log×4、ird-test-report.json）**逐件在库实证**（diff 文件清单即 cat-file 清单）——无幻影（F-081/F-102 族零复踩）。
- 在库 XML 计数：ird-execution-test-{integrated,smoke}.xml 均 tests=143/failures=0/disabled=0；contract-test 均 31/0/0——与报告一致。
- 验收者复跑实测同数（集成 143/143＋31/31、冒烟 143/143＋31/31、ird_gates 命中集零新增）。
- `phase-one-validation.log 或 resultNote 已更新`条文：按 EX-T07 验收同口径（F-201 已登记的"与流水线实践脱节"），以 traceability/builds/wp08-t08/ 全套留痕＋ird-test-report.json（acceptanceMapping 五条＋gates＋buildSummary）为等效载体——已齐备。

## 4.11 偷懒/缩水实现扫描 —— 通过（附建议级 G-2/G-3）

1. **标记扫描**：diff 新增行 `TODO|FIXME|stub|placeholder|未实现|not implemented` 正则零命中（"替身/fake"为 §11 用例行明文的替身边界声明用语——归档端口 fake 为设计明文形态，非缩水标记）。
2. **空实现检测**：CheckpointCoordinator 四方法、computePayloadDigest/verifyCheckpointFiles、ExecutionCacheCoordinator 四方法＋五维护面、evictLocked/clearInFlightLocked 全部为真实逻辑（逐行读毕——摘要编码/门槛判定/LRU 三账户淘汰/在途合并均为实质实现），无空体/恒等返回；变异 A 实证 verifyCheckpointFiles 行为真实可测。
3. **acceptance 逐条证据表**：见 4.4——五条均有具名测试（文件行号级）＋命令输出证据，零条无证据。
4. **测试强度**：断言校验行为与边界（目录逐字节快照、reasons token 精确相等、五面门槛构造、D-10 全闭环含失败路径与异键隔离、双向 mode-mismatch、三级字典序平局）；变异 A 红证明非恒真；变异 B 揭示的 Archived 半边遮蔽（同键冲突规则混淆）为敏感度缺口而非恒真——转登 G-2/F-219。
- 建议 G-3（对应 F-220）：gates 命中清单留痕格式跨任务漂移（t08 排序清单改规范化 token、hitsTotal=15 与原始 17 口径不一、跨任务清单字节级不可比）——验收者亲手重跑独立证实零新增命中，结论成立，仅留痕可比性受损，转登留档。

---

## 发现的问题清单

| # | 级别 | 内容 | 处置 |
| --- | --- | --- | --- |
| G-1 | 建议 | 3cfcf13a/1cc1c796 两提交标题以任务号名词"EX-T08"开头非动词开头——ACC §4.9/AGENTS §6.3 字面要求未满足；F-012 家族同型再现（先例 F-215＝EX-T07 同型标题昨日登记）。主实现提交 f9db4c3a 合规；正文四段齐备、验证结论逐项复跑一致，纯风格瑕疵 | 转登 **F-218**；对已推送历史不追溯改写（AGENTS §6.3）；与 F-012 家族合并处置 |
| G-2 | 建议 | 4.5 变异实验（B）实证：storeResult 双门槛的 archivePhase==Archived 半边在单元测试套件零检出力——摘除后 143/143 仍绿；EX_CCH_2 用例②与①正例共用同一 ResultCacheKey，非法登记被同键冲突保留既有规则掩蔽。实现本体正确（双门槛代码在位），属测试敏感度缺口（F-216/F-213/F-146 同族） | 转登 **F-219**；建议用例②改独立键（异 slice/契约）或增断言开发通道消息内容区分拒绝原因，随后续 diff 顺手补钉 |
| G-3 | 建议 | gates 命中清单留痕格式跨任务漂移：gates-t08-hits-sorted.txt 自 t07 原始行排序改为（门禁类×目标）规范化 token（17 原始命中→15 token），ird-test-report.json hitsTotal=15 与门禁原始输出 17 口径不一；两份在库清单格式不同致 gates-hit-set-diff.txt（空 diff）无法从提交物字节级复算。验收者亲手重跑（exit 1、17 项与 base 同集）独立证实零新增命中，结论成立 | 转登 **F-220**；后续固定一种清单格式并注明计数口径，跨任务比对用同一规范化函数 |

## 裁决

- **verdict＝pass**：4.1~4.11 全部通过，零阻断级问题；G-1~G-3 均建议级并已转登 findings.json（F-218/F-219/F-220 编号顺延自 F-217），不阻塞合入。
- 本记录经 ACC §3 记录回传流程落 `acc/EX-T08/1` 分支并推送 origin 后，验收完成；合入决策归所有者（验收者不合入、未修改任何实施产物——worktree A 两处变异已精确还原并复跑全绿、未向任务分支 wp08-t08 推送任何提交）。
