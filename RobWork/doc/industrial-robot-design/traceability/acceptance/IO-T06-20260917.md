# IO-T06 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | IO-T06 包导入导出 io 侧设施＋固化中转（Package/TempArea/AtomicFile）——≙WP-11-T05/T07 |
| 送验对象（冻结） | `16d7fee90b8810b027ff8019c407777dfe9f31f3`（分支 wp11-t06 尖端；验收全程按 SHA，分支漂移检测：`origin/wp11-t06` == 该 SHA，无漂移） |
| diff 范围 | `0c778f51c281892c5037ea31f8cbb2c3db8ace14..16d7fee90b8810b027ff8019c407777dfe9f31f3`（3 个实施提交：ae8a15a1 产品库段 / 82aa5376 测试段 / 16d7fee9 文档留痕段） |
| 验收者环境 | 全新上下文（ACC §2 独立性四条满足：未消费实施叙述；复现现场与记录回传分离） |
| 复现现场 | worktree A（detached @ 16d7fee9）：`D:/10_Source_Repos/21_robot/acc-tmp/repro-IO-T06`——集成模式全新配置＋构建＋测试＋门禁＋缺陷注入均在此进行；冒烟目录 `D:/10_Source_Repos/21_robot/acc-tmp/smoke-IO-T06`（带 vcpkg toolchain） |
| 记录回传 | 分支 `acc/IO-T06/1`（基于 `origin/redesign-main`），本文件＋findings.json 新增登记（F-198/F-199）随该分支提交推送；三元组见文末 |
| **verdict** | **fail**（4.1~4.8/4.11 通过；**4.9/4.10 不通过**——同一根因：验证留痕日志未随提交入库而提交信息声称已入库。实现本体、测试、文档同步、红线、注释全部实测通过且可复现；返工仅需补齐留痕落位，不动产品代码） |

---

## 4.1 改动范围 —— 通过

- `git diff --name-status 0c778f51..16d7fee9` 实测 **20 个变更文件**，逐一对照契约 `allowedFiles` 三个通配：
  - `io/**`：CMakeLists.txt、contract_test/PackageContractTest.cpp、include 三公共头（Package/TempArea/AtomicFile）＋ZipChannel.hpp 增补、src 四新文件（Package/TempArea/AtomicFile/IoPlatform.hpp）＋ZipChannel.cpp/ResourceIo.cpp 修改、test/PackageTest.cpp——共 13 个，全在通配内；
  - `units/io.md`：v0.9 增量修订；
  - `traceability/**`：builds/wp11-t06/ 5 份＋findings.json（F-197 登记）。
- `forbiddenFiles`（REQUIREMENTS.md/ARCHITECTURE.md）**零触碰**（diff 文件清单实测）。
- SA-02：diff 无任何 `industrialrobot/` 之外框架源码；`patches/` 无新文件（git ls-tree 实测）——无未登记框架改动。

## 4.2 可复现构建 —— 通过（两模式零错误，验收者从零亲手复现）

- **集成模式**（worktree A 全新 build 树重新配置）：`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DCMAKE_TOOLCHAIN_FILE=<仓库根>vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64 -DRWS_BUILD_INDUSTRIALROBOT=ON`——配置成功（29.4s 零错误），缓存实测 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`；`cmake --build build --config Release --target sdurws_ird_io_test` exit=0、`--target sdurws_ird_io_contract_test` exit=0（日志计 error 均为 0；验收侧日志 `acc-build-io-test.log`/`acc-build-io-contract-test.log`，随本记录不入库）。
- **独立冒烟模式**（另配临时目录＋vcpkg toolchain，AGENTS §4.1 口径）：配置＋`cmake --build --config Release` exit=0（日志中 6 处 "error" 字符串均为框架文件名 Errors.cpp/ErrorsTest.cpp，非编译错误）。
- **测试实跑**（验收者亲手，非采信留痕）：集成模式 `sdurws_ird_io_test` **116/116 PASSED**、`sdurws_ird_io_contract_test` **8/8 PASSED**（exit 0）；冒烟模式同 **116/116＋8/8**——与送验留痕 XML（integration/smoke 两套，`tests="116" failures="0"`、`tests="8" failures="0"`）及 ird-test-report.json 声称逐数值一致。
- 环境复刻备注（供后续验收参考，非本任务产物问题）：全新 worktree 集成配置需带 vcpkg toolchain＋Qt CMAKE_PREFIX_PATH（AGENTS §4.1 文档命令未列这两项）；`RobWork/RobWorkStudio/bin/RobWorkStudio.ini.template.static` 为 gitignore 的环境产物，worktree 天然缺失，验收者自主仓库工作树复制后配置通过。

## 4.3 契约 verify 命令 —— 通过

契约 `verify` 数组两条逐条亲手执行（worktree A 集成模式）：
1. `cmake --build build --config Release --target sdurws_ird_io_test` → **exit=0**；
2. `cmake --build build --config Release --target sdurws_ird_io_contract_test` → **exit=0**。
两目标产物均在 build 树落位并可直接运行（DLL 随 POST_BUILD 拷贝）。另：`validate-task.ps1 -TaskFile tasks/foundation/IO-T06.json` 验收者实跑 **PASS**。

## 4.4 acceptance 条目逐项证据 —— 通过（证据表；ird_gates 验收者亲手复现）

| # | 契约条目 | 证据（具名测试/命令输出/行号；均为验收者亲手复现） |
| --- | --- | --- |
| 1 | V12/V13/V14 全绿（ZipBomb 比例/超展开＋ledger 观测、DuplicateEntries 展开前零落盘、HashMismatch 条目级定位） | `IoPackTest.ZipBombRatioAbortsAndCleansStaging`（PackageTest.cpp:533——IO-SEC-BOMB-RATIO＋state=Failed＋临时区清理＋assertNoTarget＋ledger ArchiveRatio limit=100 断言）、`ZipBombOverExpandAbortsOnBudgetExpand`（:570——伪随机 STORED 排除比例判据干扰＋tighten 1MiB→IO-SEC-BUDGET-EXPAND）、`DuplicateEntriesRejectedBeforeExtraction`（:604——手工字节 zip 同名×2，IO-PACK-DUPLICATE-ENTRY，stagingRoot/payload 零落盘断言 :643）、`CaseDifferingEntryPairRejectedAsDuplicate`（:651）、`TamperedPayloadHashMismatchLocatedPerEntry`（:688——manifest/totalDigest 自洁仅篡 payload，params entry 精确定位 :722）。全部实跑通过（116/116 内） |
| 2 | V18~V23 全绿（ExportConcurrentWrite 重读/取消不留半成品/DiskFull 比较型/CleanupFailed 脱敏＋幂等重试/FailureNoTarget·ExportStagingOnly） | contract_test：`ExportConcurrentWriteRereadsStableVersion`（PackageContractTest.cpp:421——readCounts==2 断言＋导入全绿＋openZipChannel 独立取证 v2 内容）、`ExportConcurrentWritePersistentFailureLeavesNoTarget`（:469）、`DiskFullReportsRequiredAvailableAndCleansUp`（:502——fake 注入可用 10 字节，required/available 三要素断言 :524-529）、`CleanupFailedListsResiduesAndRetrySucceeds`（:543——residual 无 \\?\ 前缀断言 :586＋二次 cleanup→Cleaned）、`ExportCommitFailureLeavesNoTargetFile`（:602）、`ExportFailurePreservesPreviousCompletePack`（:625——先前完整包逐字节保留）；unit：`ImportCancelMidExtractCleansAndEmitsNoDiagnostic`（PackageTest.cpp:734——IO-CANCELLED＋state=Canceled＋diagnostics 空〔UX-03〕＋临时区清理＋零写入）。全部实跑通过 |
| 3 | V29/V31/V32 全绿（发布用 project fake） | `ManifestReferencingMissingArchiveEntryRejected`（PackageTest.cpp:778——幽灵条目定位）、`MissingRequiredMirrorEntryRejected`（:815——必备缺失定位）、`BackgroundImportKeepsUiThreadResponsive`（ContractTest:658——24MiB 后台导入＋UI 打点间隙<2s＋进度回调线程断言）、`CommittedVsStagedOnlySnapshotEntriesPacked`（:720——包条目枚举与快照清单全等）。fake ISnapshotFileSource 注入（PackageTest.cpp:135 FakeSnapshotSource）。全部实跑通过 |
| 4 | 失败不留目标目录总断言；io 侧 rename 发布与读 .staging 禁止 | 结构性断言 `assertNoTarget`（PackageTest.cpp:417）/`expectNoTarget`（ContractTest:405）分布于全部失败样例；类型层面无 rename/发布 API（Package.hpp 全文实读——IPackageImporter 仅 begin/verifyThrough/cleanup）；导入内容仅来自 ZIP 容器、导出仅读快照清单（Package.cpp:26-28/1417-1420 实读）。实跑通过＋源码核查 |
| 5 | 测试目标登记＋P-IO-1/O-09、P-IO-2、P-IO-7 处置 | `sdurws_ird_io_contract_test` 随 82aa5376 以真实用例登记（io/CMakeLists.txt:264-296），本验收实建实跑 8/8；P-IO-1/O-09＝注入式（Package.hpp 零 project/runtime 类型与头依赖——grep 实测 include 面），不私裁补边；P-IO-2＝IO-T05 已落位（ResourceIo.hpp:865 实读），本任务零新码（如实引用）；P-IO-7＝未预写 selection 字段字典（Package.cpp 文件头注 29-31 行＋实读无目录包 schema）；ird_gates 验收者亲手构建运行（--target ird_gates）：**命中 15 项实测复现＝基线 14＋新增 1（IRD-GATE-LIB: sdurws_ird_io_contract_test→libzip::zip）**，与送验声明及 F-197 登记一致。V19「各阶段」覆盖深度缺口转登 **F-199**（建议级，不阻塞——卡面语义与单样例实现的偏差如实登记） |

## 4.5 测试真实失败能力 —— 通过（注入 2 处实证会红；还原复绿）

- **注入①**：Package.cpp `step4ExtractAndHash` 哈希比对改恒假（`if (false && actual != row.sha256)`）→重编后 `IoPackTest.TamperedPayloadHashMismatchLocatedPerEntry` **FAILED**（1 FAILED）——V14 非恒真。
- **注入②（第一次尝试，未变红——如实记录）**：禁用 Package.cpp `cancelHit` 后 V19 仍绿——ZipChannel `readEntryBytes` 内部取消检查点构成第二层防线，单层注入不足以破坏特性（**纵深防御的正面证据**；取消机制本体并非只靠单一检查点）。
- **注入②′（改注入点）**：取消路径强制落诊断（`failure.code != Cancelled || true`）→V19 **FAILED** 且断言消息精确命中「取消路径产生了诊断（违反 UX-03）」——UX-03 断言非恒真。
- 注入后 `git checkout --` 还原，重编重跑 **116/116 全绿**（复现现场恢复送验原状；注入仅存在于验收临时 worktree，未触碰任何分支与送验对象）。

## 4.6 架构红线（AGENTS §5 八条） —— 通过

1. **L2 零 Qt**：io 新增/改动文件 grep `Qt` 零命中（仅注释「零 Qt」字样）；CMakeLists 配置期守卫（:317-320）＋io_test 运行期头红线扫描用例双防线。
2. **业务域互链禁止（R-1）**：sdurws_ird_io 链接面仅 core＋diagnostics（CMakeLists:140-142）；配置期断言（:308-316）＋ird_gates 实跑零 R-1 新增命中。
3. **跨单元私有头（R-2）**：include 面仅 `sdurws/ird/io/*` 公共头＋`sdurws/ird/core/Digest.hpp`（登记边）；私有头 IoPlatform.hpp 位于 src/（:15-16 头注自律），未出 include/。
4. **框架零修改（SA-02）**：见 4.1。
5. **不做名称前缀拼接（R-4）**：io 新增文件零 "RobWork" 字面量拼接（ird_gates R4 扫描仅 project 既有登记例外 1 处，非本任务引入）。
6. **权威唯一（PA-1）**：发布/rename 归 project——IPackageImporter 无发布 API（类型层面排除）；单位/诊断文案归 core/diagnostics——io 仅注册 IO-* 码表（既有 P-IO-6 面）；命令入口不涉。
7. **不可变历史（PA-2）**：本任务无修订写路径语义；io.md §15.5 变更记录为表尾追加（v0.9 行），v0.8 行仅转义规范化外观改动（见 4.8 观察项）。
8. **不恢复 old/**：无 old/ 引用。

## 4.7 注释规范（AGENTS §2） —— 通过（公共头全查＋实现文件抽查 4 个）

- **公共头全查**（Package.hpp 457 行/TempArea.hpp 214 行/AtomicFile.hpp 202 行）：文件头注含设计依据（io.md 章节逐条＋需求 ID＋任务卡）、业务背景（「为什么 io 没有发布」等关键决策的可读解释）、线程约束、错误语义、确定性来源；每个类/结构/函数中文 Doxygen（@brief/参数方向/单位/返回/异常面）。ZipChannel.hpp 增补两字段带表尾增补登记注（DTB §5.4 出处）。
- **实现文件抽查**（≥3 要求，实查 4 个）：Package.cpp（1767 行——九步/六步协议逐段注释、分支业务含义、魔法数字出处〔如 attributeHostSystem==3＝PKWARE UNIX 宿主码：980-981 行〕）、TempArea.cpp（崩溃残留回收「仅当含标记才清」逐分支）、AtomicFile.cpp（ReplaceFile/MoveFileEx 同卷原子语义、TOCTOU 回落分支）、IoPlatform.hpp（四分类映射逐 case＋display 脱敏口径）。§2.5 高危信息表：物理量单位（字节/秒标注）、错误码归类（调用方违约 FormatInternal vs 环境错误 RES-*）、线程约束、所有权（pimpl shared 语义）齐备。
- **测试文件**：每用例中文注明验证的 IO-V 编号/acceptance 条目（PackageTest.cpp 断言纪律头注 :20-22）。
- CMakeLists.txt 关键决策（目标登记史/红线守卫/宏定义）中文注释块齐备（实读全文）。

## 4.8 文档同步＋findings 相关性 —— 通过（io.md v0.9 同步完整；F-194 状态翻转建议随返工/合入收口）

- **实现与卡面偏差均已按 DTB §5.4 登记**（io.md §15.5 v0.9 行「⑥等价调整与增补登记」六项逐条列明：verifyThrough 尾随 cancel/progress、PackageExportOptions 四元数据字段、availableBytes、AtomicTarget.write、adopt/impl pimpl 通道、稳定版本重读恰一次）；§3.3 测试目标行、§1.2 构建落位行、§12 IO-T06 行、文末落款同步 v0.9。`validate-task` PASS（验收者实跑）。
- **任务状态/追踪矩阵**：契约 status 翻转归收尾段（PIPE §4.7），不在送验对象义务内；io.md §11.2 覆盖矩阵与测试映射一致。
- **findings.json 未关闭条目相关性核对**（v1.2 要求；redesign-main 侧 open 条目 F-164~F-196 逐族过筛）：
  - **F-194**（ResourceIo.cpp 缺 `<mutex>` 显式包含）：**修复已随送验对象落地**（ResourceIo.cpp:59-61 实读——显式 include＋F-194 消账注释），但 findings.json 中该条 status 仍 open。**本记录不代翻**（verdict=fail，翻转应随修复代码的合入收口——F-188 先例为 pass 记录落档时移植置 fixed；建议实施者返工提交或收尾合并时置 fixed，fixedBy=16d7fee9，防止消账悬空）。
  - F-191/F-197（ird_gates LIB 词表）：归门禁所有者，本任务已如实分列（15 命中实测复现一致），无可顺手消账项。
  - F-192/F-196（IO-FORMAT-JSON/XML-SYNTAX 向 diagnostics 内置表收编）：归 diagnostics 所有者，与本任务无涉（本任务未新增 IoErrorCode 值）。
  - 其余 open 条目（CORE/DIAG/EV/POL/PRJ 期登记）与本任务交付面无交集。
- **观察项（非问题，不登记）**：io.md §15.5 v0.8 行内 `\\\\?\\`→`\\?\` 转义规范化（word-diff 实测唯一改动）——历史行外观性修订，语义零变化，未在 v0.9 行单独声明；建议后续卡修订统一全文转义口径。

## 4.9 commit 质量 —— **不通过（阻断）**：commit 3/3 留痕清单为幻影清单

- 三提交标题均为 `[WP-11-T05/T07]`＋动词开头，正文 What/Why/验证/影响面四段齐备，`[WP-11-T05/T07]` 与契约 note 的 ≙ 映射一致；commit 1/3（ae8a15a1）、2/3（82aa5376）的内容描述与 diff 实测相符，验证结论（116/116＋8/8 双模式）与验收者复现逐数值一致——**无结论性粉饰**。
- **但 commit 3/3（16d7fee9）What 段声称**「traceability/builds/wp11-t06/ 留痕：双模式构建日志（integration-build/smoke-build/smoke-configure）、verify 命令日志（verify-target-io-test/verify-target-io-contract-test/verify-task）、validate-task 日志、ird_gates 运行日志…四份测试运行日志」——`git ls-tree -r 16d7fee9` 实测 `builds/wp11-t06/` **仅 5 个文件**（4 份 gtest XML＋ird-test-report.json），上述 10 个 .log 文件**全部不在库**（推测：.gitignore 全局 `*.log` 忽略，未像 IO-T03 那样 `git add -f` 强制入库）。commit 2/3 验证段「留痕（XML＋运行日志＋ird-test-report.json）随实施提交 3 登记」的「运行日志」同样落空。
- 按前例口径（IO-T03-20260917-r2 §4.9：「11 份文件与 git ls-tree 实测逐一对应（**零幻影清单**）」为过判据），声称入库而实际不在库的清单＝幻影清单，**验证结论与留痕不一致**，ACC §4.9「发现粉饰即不通过」——本项不通过。返工输入见文末 B-1。

## 4.10 留痕完整 —— **不通过（阻断，与 4.9 同根因）**

- **在库且可信**：4 份 gtest XML（integration/smoke × io_test/io_contract_test，顶层 tests/failures 计数与验收者双模式实跑逐数值一致）＋ird-test-report.json（schemaVersion `ird-test-report/1`；targets/gates/selfCheck/testSuites 段齐备，22 用例×IO-V×acceptance 追溯字段完整，probeCapability 段与实跑行为一致）。
- **缺失**：构建日志（integration-build/smoke-build/smoke-configure）、测试运行日志（integration-test-run/smoke-test-run）、verify 命令日志（verify-target-io-*、verify-task）、validate-task 日志、ird_gates 运行日志（gates-ird-gates-target.log）——共 10 份，**全部未落位**。AGENTS §4.2 验证留痕三元组「gtest XML＋ird-test-report.json＋**构建日志**」缺一；ACC §4.10 pass 标准含「构建日志落位」；前例 wp11-t01~t05 各任务均有 8~11 份日志在库（git ls-tree 实测比对），本任务为唯一缺失者。
- 「phase-one-validation.log 或 resultNote 已更新」：phase-one-validation.log 为阶段一（WP-01/03 时代）历史留痕，非本阶段登记位；流水线 state.json 无 resultNote 字段（grep 0 命中）——按 DIAG/PRJ 期验收先例，本项以 ird-test-report.json 留档承载，不另判失。

## 4.11 偷懒/缩水实现扫描 —— 通过

- **①标记扫描**：diff 全部新增行 grep `TODO|FIXME|XXX|HACK|stub|placeholder|未实现|not implemented|待实现|以后完善` **零命中**（无需豁免登记）。
- **②空实现检测**：新增公共接口（IPackageImporter/IPackageExporter/ITempAreaManager/IAtomicFileWriter 及工厂）全部为实质实现——九步/六步协议、互斥/残留回收/幂等清理、FlushFileBuffers/ReplaceFileW 原子链均为完整代码路径（Package.cpp 1767 行/TempArea.cpp 394 行/AtomicFile.cpp 326 行实读）；无「仅返回默认值」的契约外空函数（防御性 FormatInternal 返回为契约规定的 fail-fast 面，非缩水）。
- **③逐条证据表**：见 4.4——契约 acceptance 5 条均有具名测试/命令输出/文件行号，且全部经验收者亲手复现；ird_gates 亲手运行复现 15 命中。任一条无证据的情形：无。
- **④测试强度**：断言校验行为与边界（稳定码面＋params 定位要素＋结构性零写入断言＋ledger 快照观测＋脱敏负断言＋幂等正断言），非恒真、非「只编译通过」——4.5 注入实证会红（V14、V19 各一处有效注入；cancelHit 单层注入不变红的纵深现象如实记录于 4.5）。

---

## 问题清单

### 阻断级（返工输入）

- **B-1（4.9/4.10 共同根因）：验证留痕日志未入库而提交信息声称已入库。** 返工要求：
  1. 在 wp11-t06 分支**以新提交补齐**（16d7fee9 已冻结且已推送，禁止 amend/改写历史——AGENTS §6.3）：双模式构建日志（integration-build/smoke-configure/smoke-build）、测试运行日志（integration-test-run/smoke-test-run）、契约 verify 两命令日志、validate-task 日志、ird_gates 运行日志（gates-ird-gates-target.log，15 处命中分列）——**须为返工时真实重跑的现产日志**（环境与本验收一致：vcpkg toolchain＋Qt prefix；ird_gates 命中集须与本验收复现的 15 项一致），经 `git add -f` 强制入库（.gitignore 全局 `*.log`），并同步刷新 ird-test-report.json 中引用的日志名使其真实可查；
  2. 提交信息按 §6.3 四段式如实说明「补齐 16d7fee9 幻影清单所指留痕」；
  3. 完成后**重新发起全量验收**（ACC §5——本记录其余各项的复现结论可作下一轮对照基线，但不豁免重跑）。

### 建议级（已按 v1.2 强制转登 traceability/findings.json，编号避让任务分支已占用的 F-197）

- **F-198**：writeBytesToFile/AtomicTarget::write 的 DWORD 4 GiB 截断语义（当前调用面受 SingleFileBytes 硬顶 2 GiB 约束不可达——纵深防御缺口，建议 fail-fast 或分块写）。
- **F-199**：IO-V19「各阶段注入取消令牌」仅覆盖展开阶段一个样例，begin 入口等阶段取消路径无用例（建议 IO-T07 契约套件整备时补「begin 入口预取消」用例）。

### 观察项（不构成问题，留档）

- O-1：io.md v0.8 行转义规范化（`\\\\?\\`→`\\?\`）为外观性修订，语义零变化（4.8）。
- O-2：禁用 Package.cpp cancelHit 单层后 V19 仍绿——ZipChannel 内部检查点构成取消第二层防线，纵深设计正面证据（4.5）。

## 裁决说明

- **verdict＝fail**，唯一阻断根因为 B-1（留痕落位）；实现本体、测试强度、文档同步、架构红线、注释规范经对抗式核查全部通过且可复现——返工面窄，预期下一轮验收以 B-1 补齐为主要复核点。
- 本验收未合入任何分支、未修改实施产物、未向 wp11-t06 推送任何提交（注入仅发生于验收临时 worktree 并已还原）；复现现场保留至本记录推送确认后清理。
- 需所有者/后续关注：F-194 status 翻转时机（随修复代码合入收口）；AGENTS §4.1 集成配置命令未含 vcpkg toolchain/Qt prefix 两项（全新树配置即失败——环境口径与主 build 树实测配置不一致，供治理修订参考，非本任务产物问题）。

## 验收者环境说明

- 仓库：`D:/10_Source_Repos/21_robot/RobWork`（wp11-t06 工作树检出中，与本验收隔离）；工具链 MSVC 2022 17.12、CMake 4.3、Qt 6.11.1（D:/software/Qt/6.11.1/msvc2022_64）、vcpkg 经典模式（仓库根，gtest 1.18.0/libzip 1.11.4/expat 2.8.3 x64-windows）。
- 送验对象冻结核验：验收开始时 `git fetch origin` 后 `origin/wp11-t06 == 16d7fee9`（无漂移）；验收全程零向任务分支推送。
- 验收侧过程日志（不入库，仅供编排者核对）：`acc-tmp/repro-IO-T06/acc-configure.log`、`acc-build-io-test.log`、`acc-build-io-contract-test.log`、`acc-run-io-test.log`、`acc-run-io-contract.log`、`acc-smoke-configure.log`、`acc-smoke-build.log`、`acc-fault1-build.log`、`acc-fault2b-build.log`、`acc-gates-build.log`、`acc-restore-build.log`。

## 记录回传三元组

- branch：`acc/IO-T06/1`（本记录所在分支；收尾按 evidence 提交 SHA 合并——SHA 由验收回复输出给编排者，不回写本文件）
- path：`RobWork/doc/industrial-robot-design/traceability/acceptance/IO-T06-20260917.md`
- 同分支随附：`traceability/findings.json` 新增 F-198/F-199 两条建议级登记（编号避让 wp11-t06 已占用的 F-197）
