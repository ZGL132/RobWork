# WP-13-T04 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | WP-13-T04 实现物性估算唯一公式表与动力学参数层（unit=modeling） |
| 送验对象（冻结） | 分支 `wp13-t04` 尖端 `9e24b4104d1af423c1fb40ba0d1442bb73990469`（验收全程按该 SHA） |
| 分支漂移检查 | 验收启动时 `git fetch origin` 后 `origin/wp13-t04` = `9e24b410…` ＝ 送验 SHA，**零漂移**（ACC §3） |
| diff 范围 | `7f1dc8a60b48691e26528802cc2d2aa4e386ae61..9e24b4104d1af423c1fb40ba0d1442bb73990469`（2 个提交：7326ccb6 实现＋9e24b410 双模式留痕与门禁比对证据；base＝`origin/redesign-main` 尖端，即 wp13-t03 合入收尾提交） |
| **verdict** | **pass**（4.1～4.11 全部通过，无阻断级问题；建议级 3 项转登 findings F-299/F-300/F-301） |
| 验收执行方式 | 验收者全新上下文独立复现：detached worktree @ 送验 SHA 亲手重配重跑全部构建/测试/脚本/门禁；未采信实施者任何运行结果（ACC §2 四条） |
| 复现现场 | `D:/wt-a-t04`（detached @ 9e24b410，集成模式）；`D:/wt-smoke-t04`（独立冒烟）；`D:/wt-c-t04`（detached @ base 7f1dc8a6，门禁 base 对照专建） |
| evidence 分支 | `acc/WP-13-T04/1`（基于 `origin/redesign-main` = 7f1dc8a6；记录 commit 见本分支尖端，验收完成后由编排者登记三元组） |

---

## 4.1 改动范围 —— 通过

`git diff --name-status 7f1dc8a6..9e24b410` 共 26 文件，逐一对契约 `allowedFiles` 核对：

- `RobWork/RobWorkStudio/src/rwslibs/industrialrobot/modeling/**`：5 文件（CMakeLists.txt 改；include/sdurws/ird/modeling/PropertyEstimation.hpp 新增；src/PropertyEstimation.cpp 新增；src/InertiaMath.hpp 改；test/PropertyEstimationTest.cpp 新增）——全在 modeling/** 内。
- `RobWork/doc/industrial-robot-design/units/modeling.md`（改，升 v0.5）——在 allowedFiles。
- `RobWork/doc/industrial-robot-design/traceability/builds/wp13-t04/**`：20 文件（构建/测试/门禁/脚本留痕）——在 allowedFiles `traceability/**` 内。
- **forbiddenFiles 零触碰**：REQUIREMENTS.md、ARCHITECTURE.md 不在 diff。
- **SA-02**：diff 无 modeling 之外任何框架源码文件；`patches/` 无新文件（无未登记框架改动）。

## 4.2 可复现构建 —— 通过（两模式均验收者亲手从零重配重跑）

- **集成模式**（`D:/wt-a-t04`，源＝detached worktree @ 9e24b410）：
  - 配置：`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON` ＋ 主树 vcpkg toolchain ＋ Qt 前缀（与主树缓存同参）。结果 `Configuring done`；缓存确认 **`RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`**。
  - 环境前置（非本任务产物缺陷）：①worktree 内 `vcpkg/` 整目录被 .gitignore 吞掉（F-007 已登记）——验收者将 toolchain 指向主树 `vcpkg/scripts/buildsystems/vcpkg.cmake` 并复用其 `installed/`；②`RobWorkStudio/bin/RobWorkStudio.ini.template.static` 同被 `[Bb]in/` 规则吞掉（F-007 原文点名的文件）——从主树复制后配置通过（F-007/F-009 验收环境缺口再证实，处置建议不变）。
  - 任务相关目标构建：`sdurws_ird_modeling`、`sdurws_ird_modeling_test`、`sdurws_ird_modeling_contract_test` 三目标退出码均 **0**，MSVC `error C/LNK` 计数 **0**。
  - 全树构建注记：验收者额外跑了全树 `cmake --build build --config Release` 以核实集成树状态——失败仅集中于 **RobWorkSim 框架插件**（`RWSimulatorPlugin.vcxproj`：`rw/math/Math.hpp` C2059/C2334/C2143 簇＝F-008 原文登记缺陷；另新观测 `bt_plugin.rwplugin.vcxproj` 同根 min/max 宏污染簇〔Eigen Parallelizer.h C2589/C2059〕，转登 **F-301** 与 F-008 合并处置）。送验 diff 中零框架文件，与该失败无因果路径；F-008 处置口径明文"ired 范围内任务不受影响——任务目标两模式从零复现零错误"，本任务任务面满足。
- **独立冒烟模式**（`D:/wt-smoke-t04`，`-DCMAKE_TOOLCHAIN_FILE=<主树 vcpkg>/scripts/buildsystems/vcpkg.cmake`＋`VCPKG_INSTALLED_DIR` 指向主树 installed＋Qt 前缀——AGENTS §4.1/F-007 口径）：
  - 配置 done；全量构建退出码 **0**、错误计数 **0**。
  - 冒烟测试：`sdurws_ird_modeling_test.exe` → **68/68 PASSED**；`sdurws_ird_modeling_contract_test.exe` → **5/5 PASSED**（两 exe 退出码均 0）。

## 4.3 契约 verify 命令 —— 逐条亲手执行（6/6 执行，结论如下）

| # | 命令（集成树等价执行） | 结果 |
| --- | --- | --- |
| 1 | `cmake --build build --config Release --target sdurws_ird_modeling` | 退出码 0，零错误 |
| 2 | `cmake --build build --config Release --target sdurws_ird_modeling_test` | 退出码 0，零错误 |
| 3 | `ctest --test-dir build/RobWorkStudio/src/rwslibs/industrialrobot/modeling -C Release -L "^ird$"` | 首跑 **1/2**——`sdurws_ird_modeling_contract_test` 报 Not Run（全新树中该 exe 尚未构建：**契约 verify 清单不含其构建目标行**，验收者补跑 `--target sdurws_ird_modeling_contract_test`（退出码 0）后复跑 ctest → **2/2 passed**。清单缺口转登 F-300（建议级），非实现缺陷——实施侧留痕日志表明其完整构建了三目标 |
| 4 | `cmake --build build --config Release --target ird_gates` | 门禁引擎运行并按 SA-10 以退出码 1 终止：自报 **51 处命中＝全部存量基线**（送验留痕 `gates-hit-set-diff.txt` 结论与验收者独立复现一致，见 4.4 门禁段） |
| 5 | `pwsh -File RobWork/scripts/industrialrobot/validate-task.ps1 -TaskFile …/WP-13-T04.json` | `validate-task: PASS (1 tasks)`，退出码 0 |
| 6 | `pwsh -File RobWork/scripts/industrialrobot/validate-docs.ps1` | `validate-docs: PASS (20 units, 12 trace entries, 186 task files)`，退出码 0 |

## 4.4 acceptance 条目逐条证据 —— 5/5 有对应证据（行号以送验 SHA 内容为准）

**门禁基线核对（acceptance 与 DoD 的 `ird_gates` 面）**：验收者专建 base 对照（detached worktree @ 7f1dc8a6，独立配置后亲跑 `--target ird_gates`），head/base 两份新鲜日志逐码多重集**完全一致**：`LIB=8, R1=1, R3=37, R4=3, R5=1, SUB=53, T1=5, T2=1`；两树门禁均自报"命中 51 项"；head 日志中新文件 token 扫描 `PropertyEstimation|InertiaMath` 命中 **0 次**。**结论：base..branch 零新增命中**，与送验留痕 `gates-hit-set-diff.txt` 三口径（码计数多重集/总量/新文件 token）结论一致；modeling 面仅存量两条测试目标→testkit SUB＝F-292（major/open，引擎级、`cmake/ird_gates.cmake` 不在契约 allowedFiles，本任务不可消账——实施侧已在单元卡 v0.5 ④如实登记）。

| acceptance 条目 | 证据（验收者亲验，实现行号＋具名测试＋亲跑结果） |
| --- | --- |
| 1. PropertyEstimation.hpp/.cpp 落位；IPropertyEstimator 按 §9.4.6 签名（estimateLink/migrateByParallelAxis/formulaVersion）；唯一公式表三段元解析算例对照 UT——手算闭式逐项双精度比对；来源标记 GeometricEstimate＋methodTag | 接口签名与卡 §9.4.6 原文逐项一致（PropertyEstimation.hpp:285/307/315 ↔ modeling.md §9.4.6 L1084-1090；EstimateOutcome 复用 runtime::Expected :236；EstimateErrorCode 三值 :172-180）。公式表三行实现：实心圆柱 PropertyEstimation.cpp:191-203（m=ρπr²L；Ixx=Iyy=m(3r²+L²)/12；Izz=mr²/2）、空心 :204-221、长方体 :222-246。UT：`SolidCylinderClosedForm`/`HollowCylinderClosedForm`/`BoxClosedForm`（测试侧独立抄写闭式逐项双精度 EXPECT_NEAR，容差 1e-12 相对；Box 用例另钉 `Ixx≠Iyy` 差>1e-6）；`FormulaVersionAndProvenanceMarking`（formulaVersion≡kPropertyFormulaVersion≡"mdl-property-formula/1"；provenance.kind=GeometricEstimate、methodTag 有值、无 sourceObject）；`DensityResolutionProvidedOverridesTable`（已提供值不被表值覆盖）；`EmptySegmentsRejectedIllegalDimension`。68/68 亲跑全过。 |
| 2. 多段合成 UT（平行轴定理闭式）；结果张量定义在连杆合成质心且参考姿态＝连杆系（M-2）；全 SI/双精度/不舍入；输出自检反例拒收——失败出内部错误码而非非法张量 | 合成算法四步注释完整（PropertyEstimation.cpp:124-320；步③ Rᵢ·Iᵢ·Rᵢᵀ＋mᵢ((dᵢ·dᵢ)E−dᵢdᵢᵀ) :266-279）；自检单一实现 `synthesisSelfCheck`（InertiaMath.hpp:156-192：有限性→对称 1e-12→SPD 严格>0→三角不等式）。UT：`TwoSegmentParallelAxisSynthesis`（测试侧独立装配含 Rz(90°) 旋转的第二段，上三角 6 分量逐项手算对照；合成质心 C 手算三项对照；**确定性＝同输入重复调用逐位 EXPECT_EQ** :434-440）；`SingleSegmentRotatedTensorInLinkFrame`（Rx(90°) 钉子——Iyy/Izz 随姿态互换，M-2 参考姿态＝连杆系）；`SynthesisSelfCheckRejectsCounterexamples`（对称超差 1e-6 拒/边界 1e-12 过〔闭边界钉子〕/非正定拒/三角不等式拒；公共面 1e200 溢出→`SynthesisFailed` 且 `get()` 抛 logic_error——非法张量不可观测）。 |
| 3. 质心修改确认流三分支 UT（V-17）：(a) 平行轴迁移、(b) 强制覆盖完整张量——接受并留痕；(c) 未决议→CentroidEditUnresolved 调用方错误拒绝 | `applyCentroidEdit`（PropertyEstimation.cpp:350-426）：前置 mass/com/inertia 均 Provided 违约抛 invalid_argument（fail-fast 轨）:361-368；(a) 迁移经估算器实例调用（与估算共用同一平行轴算式——单一实现点）并**保留原 provenance**:405-414；(b) 整体覆盖＋UserProvided:415-422；(c) nullopt→值面 ModelingError{CentroidEditUnresolved} 拒绝:380-392，先算后提交保证拒绝路径零副作用。UT：`CentroidEditMigrateBranch`（六分量手算 0.05/0.11/0.104/−0.039/−0.04/−0.018 逐项对照＋com→UserProvided＋inertia 保留 GeometricEstimate＋与 migrateByParallelAxis 直调逐分量恒等）；`CentroidEditOverwriteBranch`（replacement 六分量精确落位＋UserProvided）；`CentroidEditUnresolvedRejected`（拒绝＋稳定 token "CentroidEditUnresolved"＋**body 逐字节不变**断言；处置 token "migrate-inertia"/"overwrite-inertia" 留痕机器判别串钉子）；`CentroidEditPreconditionsFailFast`（缺 inertia/缺 replacementTensor 均抛 invalid_argument）。编辑差值/命令摘要留痕载体归编辑器/命令层（卡 §5.3 规则 2 原文），本域交付判定实现＋token——口径与卡一致。 |
| 4. 材料密度默认表（钢 7850/铝 2700/铸铁 7200/钛 4430/工程塑料 1200 kg/m³）查询 UT；物性缺失 NotProvided 不触发断言、登记 Warning 并作 DataInsufficient 降级预告；可信等级判定不归 modeling | `defaultMaterialDensity`（PropertyEstimation.cpp:56-76，五键精确值＋线性扫描确定性序）。UT：`MaterialDensityTableDefaults`（五键逐项 EQ＋词表外/空串/大小写变体/前导空白一律未命中——"不猜测"）；`DensityMissingValueFaceAndNotProvidedNoAssertion`（①density 不可解析→值面 MaterialDensityMissing 正常返回、detail 携材料键、diags 空〔分批注册纪律——§9.5 无 T04 行码〕；②全 NotProvided 连杆 checkInvariants 零违例＋分量级 `checkInertiaAssertions` NotProvided 跳过分支补钉；③可信等级判定接口面不存在性声明——DYN-06 判定权归 dynamics/evidence）。Warning 登记面归 T08 就绪校验＝卡 §5.3 规则 3 明文口径，头注/实现注/测试注三处一致，v0.5 ④登记在案。 |
| 5. DrivetrainDesign schema 落位（§4.7，MDL-16）：ratioPerJoint（>0 且有限）/frictionPerJoint（fv/fc/bias 全 SourcedValue）/torqueLimitsPerJoint（rated/peak）/catalogBackfill 字段；UT 覆盖字段约束与来源标记 | schema 载体已于 WP-13-T03 落位 Parts.hpp（卡 §4.7 行原文"schema 本卡登记，回填命令归属阶段 C"——本任务交付 UT 面，与契约 acceptance 5 字面一致）。UT：`FieldConstraintsAndProvenanceMarking`（ratio 逐项 >0 且有限经 I-MDL-11：0/−101/Inf 三反例逐个触发违例；CatalogBackfill/ImportMapped/UserProvided 三来源类别标记查证；摩擦 fv/fc/bias 全 SourcedValue 状态与 provenance 逐字段断言；力矩限值 rated/peak 承载；catalogBackfill 四字段＋值相等覆盖全字段；schemaVersion≡kRobotDrivetrainSchemaVersion 单一权威）；`FrictionTorqueNotProvidedAccepted`（摩擦/力矩限值缺失不触发传动不变量——DYN-06/M-8 建模侧事实承载＋部分提供中间态接受）。 |

未执行项标注：无——本任务全部 acceptance 面均执行；`ird_gates` 按 4.3/4.4 如实记录为"存量基线红 51 项＋零新增命中"，不标注"零命中"。

## 4.5 测试真实失败能力 —— 通过（两处变异均被捕获，验收后已复原）

对 worktree A 的 `src/PropertyEstimation.cpp` 做临时变异（仅验收现场，未触碰任何分支；每次变异后重建 `sdurws_ird_modeling_test` 并运行）：

1. **变异①**：密度默认表钢值 7850→7851（PropertyEstimation.cpp:63）。结果 **4 用例变红**：`SolidCylinderClosedForm_WP13T04_ACC1`、`TwoSegmentParallelAxisSynthesis_WP13T04_ACC2`、`SingleSegmentRotatedTensorInLinkFrame_WP13T04_ACC2`、`MaterialDensityTableDefaults_WP13T04_ACC4`（64 pass/4 fail）——证明公式与密度表断言非恒真。
2. **变异②**：确认流分支 (c) 改为静默接受质心修改（模拟 DTB 禁止项"质心与惯量基准静默脱钩"）。结果**恰 `CentroidEditUnresolvedRejected_WP13T04_ACC3` 变红**（67 pass/1 fail）——证明拒绝分支断言钉住的是行为而非实现形态。

两处变异均经 `git checkout --` 还原，重建复跑 **68/68 PASSED**，`git status` 确认工作树与送验 SHA 一致。

## 4.6 架构红线 —— 八条逐条自查通过

1. **L2 零 Qt**：modeling 全单元（含新增文件）`grep -ri "Qt|Q[A-Z]|qobject|QString"` 零命中；仅 std＋rw/math＋ird 公共头。
2. **R-1 业务域互链**：新增/既有文件 include 中无 requirements/kinematics/trajectory/dynamics/selection/optimization 任一单元头；跨单元仅 core（DiagData/Provenance）与 runtime（Errors 的 Expected 模板）——均为 T02 已登记白名单边（六边登记，契约测试 `UnitEdgesSixRegisteredAndClosed` 双模式亲跑通过）。
3. **R-2 跨单元私有头**：PropertyEstimation.hpp/.cpp 对外只 include `include/sdurws/ird/...` 公共路径；`src/InertiaMath.hpp` 私有头仅被本单元包含（:17 注释自证＋契约测试 NoTestkitEdge/IncludeFace 用例钉住）。
4. **SA-02 框架零修改**：见 4.1——diff 无框架文件、patches/ 无新增。
5. **R-4 名称前缀拼接/剥离**：新增代码无任何 RobWork 名称串拼接/剥离逻辑（名称解析归 runtime，未越权）。
6. **PA-1 权威唯一**：估算/确认流为 modeling 本域语义（卡 §5.3 规则 5 边界原文逐条对齐——估算质量不当证据、可信等级不判定）；diags 产码留空不私造诊断码（PA-1 diagnostics 单元口）。
7. **PA-2 不可变历史**：本任务无修订/历史写路径（纯函数域层），不适用即无违例。
8. **不继承 old/**：无任何 `old/` 引用（grep 零命中）。

## 4.7 注释规范 —— 通过（公共头全查＋实现文件 3 个＋测试＋CMake）

- **公共头 `PropertyEstimation.hpp`（402 行全查）**：文件头 Doxygen 中文（设计依据逐条到卡 §/需求 ID/契约 acceptance；"估算不等于证据"业务背景交代）；每个类型/函数均有 @brief＋参数方向/单位/取值范围＋返回语义＋错误语义（fail-fast 轨 vs 值面轨逐处标明）＋线程安全/确定性声明；§2.5 高危信息全表命中：物理单位（m/kg/kg·m²/N·m/kg·m³ 逐字段）、坐标系（T_link_seg m/rad、M-2 基准两处显式）、rad/deg（角度均 rad 显式）、所有权（纯函数输入输出归调用方）、线程约束（纯函数并发只读安全）、错误语义（IllegalDimension/MaterialDensityMissing/SynthesisFailed 逐值归类＋CentroidEditUnresolved 值面 vs invalid_argument fail-fast 分轨）、确定性来源（固定运算次序/固定表序/无迭代）。
- **实现 `PropertyEstimation.cpp`（428 行）**：文件头确定性纪律声明；四步算法逐段"当前步数学/业务含义"注释（:128-320）；密度解析序三路优先级逐条注因；分支边界（variant 未接入图元显性失败 :240-246）；魔法数字 1×10⁻¹² 在 InertiaMath.hpp:122-125 说明来源（卡 acceptance 2 原文）。
- **实现 `InertiaMath.hpp`（增补 85 行）**：synthesisSelfCheck 函数注含"为何提取六分量前必须自检"的通道分析（不对称噪声被表示层结构性对称掩盖）＋物理背景（正交相似变换保 SPD）——超出规范要求的"为什么"级注释。
- **测试 `PropertyEstimationTest.cpp`（797 行）**：文件头 ACC1~5 与契约 acceptance 映射；每用例 IRD_TEST_INFO 携需求/AT 追溯（MDL-05/MDL-16/DYN-06）；手算期望值过程注释（如迁移分支六分量算式 :529-532）。
- **`CMakeLists.txt` 增量**：T04 增列两处中文注释块（源文件登记依据＋测试具名自证映射）。

未发现 AGENTS §2.6 反面示例（复述代码/无信息量占位/机翻腔/过期注释）；TODO 均无（见 4.11①）。

## 4.8 文档同步 —— 通过（零未登记偏差；findings 相关性核对完成）

- **单元卡同步**：units/modeling.md v0.4→v0.5（文档头版本行＋§14.6 v0.5 增量登记）：§3.3 表 T04 行落位、§15（§14.6）登记偏差与澄清四项——①长方体公式表列头"Ixx=Iyy"为轴对称两行合并列，实现 Iyy=m(a²+L²)/12（Ixx/Izz 按表值原样），UT 以 a≠b 用例钉住（该澄清与解析事实一致，验收者独立验算确认）；②空心圆柱 rOut>rIn>0 前置显式化（质量公式数学前提）；③diags 参数分批注册纪律（§9.5 无 T04 行码，失败走值面、Warning 登记面归 T08）；④空段元列表归 IllegalDimension 值面。全部偏差按 DTB §5.4 口径随卡登记，实现与任务卡零未登记偏差。
- **契约状态**：契约 JSON 在送验 SHA 中 status="ready"——按 PIPE §4.7，done 翻转与队列移出归合入收尾提交（wp13-t03 先例 7f1dc8a6 同型），实施者不代改，正确。
- **追踪矩阵**：trace-matrix.json:411 已含 WP-13-T04 规划行（MDL-05/16/DYN-06 映射），实现落位不要求实施者改矩阵（消费侧登记随治理收尾）——与 WP-13-T03 先例口径一致。
- **findings.json 未关闭条目相关性核对（241 open 逐扫）**：①与本任务功能面（物性估算/公式表/确认流/密度表/传动 schema）相关者：**零**——无在册发现指向本任务交付物；②与本任务单元相关且必须注记者：**F-292**（modeling 两测试目标→testkit SUB 门禁命中，major/open，引擎级，owner＝WP-01 构建所有者）——本任务门禁输出含此存量两条，实施侧已在卡 v0.5 ④如实登记"无法由本任务消账"，验收者独立复现一致，**维持 open 不消账**（正确，修引擎才可消）；③环境前置家族 **F-007/F-009**（新树 vcpkg/ini 模板缺口）——验收者在独立 worktree 再次踩中并按其处置建议绕过（复制模板＋指向主树 toolchain），两发现维持 open，处置建议继续有效。

## 4.9 commit 质量 —— 通过（四段齐备；1 项建议级转登 F-299）

- 提交 1 `7326ccb6`「[WP-13-T04] 实现物性估算唯一公式表与动力学参数层」：前缀后动词开头合规；What/Why/验证/影响面四段齐备，验证段计数（68/68＋5/5、双模式零错误、门禁 51 存量）与验收者独立复现**逐项吻合**，无粉饰。
- 提交 2 `9e24b410`「[WP-13-T04] 双模式验证留痕与门禁命中集比对证据」：四段齐备、内容如实（留痕文件 20 件验收者逐件核对存在且计数自洽）；但标题前缀后为**名词短语**「双模式验证留痕…」开头，非动词开头——**F-012/F-293/F-298 家族同型再现**（与 WP-13-T03 送验第二提交 ff6c77d9 同一形态），转登 **F-299**（建议级，随家族统一处置，不追溯改写已推送历史）。

## 4.10 留痕完整 —— 通过

- 送验 SHA 内 `traceability/builds/wp13-t04/` 共 20 件：集成构建日志、integration-ctest.log（2/2）、gtest XML 4 份（unit 集成/冒烟、contract 集成/冒烟）、ird-test-report.json 4 份（integration/smoke/contract-integration/contract-smoke，summary 均 `total:68/5, passed:68/5, failed:0, decisive:true`）、冒烟 configure/build/测试运行日志、ird_gates 留痕＋gates-hit-set-diff.txt、validate-task/validate-docs 日志——验收者逐件核对存在并与亲测口径一致（XML `tests="68" failures="0"`＋17 条 ACC 用例名亲验在列）。
- `ird-test-report.json` 本任务四份落位（AGENTS §4.2"或"分支满足）；`phase-one-validation.log` 为阶段级日志、任务级证据按既有先例落 `builds/wp<nn>-t<kk>/`（WP-13-T03 同口径），不要求任务级改写。

## 4.11 偷懒/缩水实现扫描 —— 通过

1. **标记扫描**：diff 范围 modeling 侧全部新增/修改行 `TODO|FIXME|stub|placeholder|未实现|not implemented`（-i）**零命中**；无"触发式延后"豁免需列出。
2. **空实现检测**：公共接口 3 方法＋2 自由函数（defaultMaterialDensity/applyCentroidEdit）＋自检/累加辅助全部为完整实现（estimateLink 四步 196 行、applyCentroidEdit 77 行、synthesisSelfCheck 37 行）；`estimateLink` 的 `(void)diags` 为卡面签名保留＋分批注册纪律注释明示（非空实现——语义在值面返回，4.4 条目 4 证据）。
3. **逐条证据表**：契约 acceptance 5 条全部有具名测试＋实现行号＋验收者亲跑输出（见 4.4 表），无一条仅凭"已实现"声明。
4. **测试强度**：断言校验行为与边界（闭式数值逐项双精度、逐位确定性、容差闭边界、溢出反例、零副作用字节断言、fail-fast 异常断言、来源标记四态）；与 4.5 联动——两处实现变异均使对应断言变红，非恒真、非"只编译通过"。

---

## 发现的问题清单

**阻断级：无。**

**建议级（已按 ACC §5 转登 `traceability/findings.json`，编号顺延自 F-298）：**

| 编号 | 摘要 | 责任方 | 状态 |
| --- | --- | --- | --- |
| F-299 | 送验第二提交 9e24b410 标题名词短语开头（F-012/F-293/F-298 家族同型再现）；正文四段齐备、内容与验收者复现逐项吻合、无粉饰 | 实施者侧提交信息规范（家族处置归治理） | open |
| F-300 | 契约 verify 清单缺 `sdurws_ird_modeling_contract_test` 构建目标行——全新树按清单逐条执行时 ctest 首跑该测试 Not Run（exe 未构建），补建后复绿；建议 DTB §5.1/契约模板口径补全（先构建单元全部测试目标再 ctest） | 治理/编排（verify 清单口径）；F-297 验收环境陷阱家族同族新根因 | open |
| F-301 | 全新集成树全量构建失败簇自 F-008（RWSimulatorPlugin）扩展观测到 `bt_plugin.rwplugin.vcxproj`（Eigen Parallelizer.h C2589/C2059，同 min/max 宏污染根，RobWorkSim bullet 插件）——送验 diff 零框架文件、无因果路径，任务目标两模式零错误不受影响；与 F-008 合并处置 | 构建所有者（框架侧 NOMINMAX/patch 登记，随 WP-01-T02 CI 消费） | open |

## 验收者环境说明

- OS Windows 11（10.0.26200）/ Git Bash；CMake 4.3（D:/software/CMake）；VS 2022 Community（17，MSVC x64）；Qt 6.11.1 msvc2022_64；vcpkg 经典模式（主树 `vcpkg/installed/x64-windows`，gtest 1.18.0）。
- 三个临时 worktree（A＝复现 @9e24b410、B＝记录 @acc/WP-13-T04/1、C＝门禁 base 对照 @7f1dc8a6）＋一个冒烟构建目录，验收记录推送成功后删除。
- 验收全程未向 `wp13-t04` 或任何实施分支推送/修改任何提交；变异测试仅在 worktree A 工作树内进行并已还原。
