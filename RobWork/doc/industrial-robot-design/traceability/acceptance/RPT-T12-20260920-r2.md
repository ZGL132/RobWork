# 验收记录 —— RPT-T12（实现模型摘要与任务投影接入：ModelSummary/ITaskStatusSource）· attempt 2

| 字段 | 值 |
| --- | --- |
| 任务 | RPT-T12（contract：tasks/foundation/RPT-T12.json，schema ird-task/1，status=ready） |
| 送验对象 | **552c66d1ddb39e468899705822b0424da5b0fa70**（40 位冻结；分支 wp12-t12；`git ls-remote origin wp12-t12` 与该 SHA 逐字一致——无分支漂移） |
| diff 范围 | 0636368285a360ed56df4d869239853319a488f8..552c66d1ddb39e468899705822b0424da5b0fa70（＝base..headSha；range 内 3 提交：36bbac31 实现＋92db3656 留痕补交＋552c66d1 G-1 返工补交 11 件 .log） |
| 验收尝试 | attempt 2（前次：attempt 1 verdict=**fail**，唯一阻断 G-1＝11 件 .log 留痕被 .gitignore 吞掉未入库；本次按 ACC §5 **全量重跑**，非增量验收） |
| 验收环境 | 复现 worktree A＝`git worktree add --detach` @ 552c66d1（集成 build 树全新配置：`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=<主仓>vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`——配置 exit 0，缓存 grep `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON` 实证）；冒烟 build 树＝独立目录 `acc-rpt-t12-r2/smoke-build`（vcpkg toolchain＋Qt 前缀——AGENTS §4.1 冒烟命令缺 Qt 前缀在 testkit `find_package(Qt6)` 处不可配，F-254 既有登记，验收者亲历同款）；门禁 base 对照 worktree C＝detached @ 06363682（比对后清理）；记录回传 worktree B＝`git worktree add -b acc/RPT-T12/2` @ origin/redesign-main。配置前自主复制两件未追踪 ini 辅助件（RobWorkStudio.ini.template.static/.shared.in——框架 configure_file 硬依赖，F-007 既有登记环境链，非送验对象缺陷）。验收全程只消费产物、契约、设计文档与在库留痕，未读取实施者过程叙述；全部构建/测试/门禁/治理脚本/变异试验由验收者亲手执行 |
| verdict | **pass**（4.1～4.11 全部通过、无阻断级问题；attempt 1 唯一阻断 G-1 经独立复现确认已修复——11 件 .log 全部在库且内容与验收者亲跑逐字一致；两条新发现均为建议级，已转登 findings.json F-256/F-257，不阻塞合入） |

## G-1 返工核验（attempt 1 阻断项闭环确认）—— 已修复

- `git show --name-status 552c66d1`：恰 11 个 `A`（新增）条目，全部为 attempt 1 所列缺失件（integration-build、smoke-configure、smoke-build、integration-test-run-unit/contract、smoke-test-run-unit/contract、gates-ird-gates-base/branch、validate-task、validate-docs）——`git add -f` 破除 `.gitignore:87`（`*.log`，送验对象内仍在，行 87 实证）吞没，未 amend 已推送历史（92db3656 原样在链），符合 G-1 处置列。
- 留痕目录清点：`traceability/builds/wp12-t12/` 现有 **22 件＝21 件留痕＋schema-freeze-note.md**——92db3656 正文"21 件"声称在冻结头状态下与在库清点一致（10 件随 92db3656＋11 件随 552c66d1）。
- 三处悬空引用修复核验：schema-freeze-note §5 所引（integration-build.log、smoke-build.log、*-integration.xml、*-smoke.xml、ird-test-report*.json）全部可回查；92db3656 正文清单 21 件全部在库。残留一处指引文字失准见 4.10/G-2（建议级）。
- 11 件 .log 内容与验收者亲跑**逐字一致**（见 4.10 明细）——归档失实（多报型）已消除，无带病留痕。

## 4.1 改动范围 —— 通过

- `git diff --name-only 06363682..552c66d1` 实证 **29 个变更文件**，逐个对照契约 allowedFiles：6 件 `reporting/**`（CMakeLists.txt M＋ModelSummary.hpp/TaskStatusSource.hpp 两契约头 A＋两单测 A＋一契约测试 A）＋22 件 `traceability/builds/wp12-t12/**` A＋1 件 `units/reporting.md` M——**全部落在 allowedFiles 三类模式内**。
- forbiddenFiles（REQUIREMENTS.md/ARCHITECTURE.md）零触碰（全列表实证）；框架源码（RobWork/RobWork、RobWorkSim、rwslibs 其余单元）零改动；`patches/` 范围内零变更（grep 计数 0——SA-02 无登记需求）。

## 4.2 可复现构建 —— 通过（集成全新树重配＋独立冒烟树，双双零错误）

| 步骤 | 结果 |
| --- | --- |
| 集成重配 | worktree A 全新配置 exit 0（补齐 vcpkg toolchain＋Qt 前缀＋两件未追踪 ini 辅助件后一次通过；均为 F-007/F-254 既有登记环境链）；`grep RWS_BUILD_INDUSTRIALROBOT build/CMakeCache.txt` → `:BOOL=ON` |
| 集成构建 verify 双目标 | `cmake --build build --config Release --target sdurws_ird_reporting_test` exit 0；`…_contract_test` exit 0；`: error` 计数 0；两 exe 产物落位 `build/RobWorkStudio/bin/Release/` |
| 冒烟配置＋构建 | 独立目录 `cmake -S …industrialrobot -B <tmp> -DCMAKE_TOOLCHAIN_FILE=<vcpkg> -DCMAKE_PREFIX_PATH=<Qt>` 配置 exit 0；`cmake --build --config Release`（**industrialrobot 全目标**）exit 0、`: error` 计数 0 |

## 4.3 契约 verify 命令 —— 逐条亲手执行；两条全部 exit 0

| 契约 verify 命令 | 结果 |
| --- | --- |
| `cmake --build build --config Release --target sdurws_ird_reporting_test` | exit 0，零错误 |
| `cmake --build build --config Release --target sdurws_ird_reporting_contract_test` | exit 0，零错误 |

（契约 verify 数组恰两条构建命令，无 ctest 条目——F-251/F-252 ctest 空转问题在本任务不适用。）

**测试执行（验收者四跑全绿，与在库留痕逐字一致）**：

| 模式 | 单测 | 契约测试 |
| --- | --- | --- |
| 集成（A 树亲跑 exe） | 208 tests / 208 PASSED / 0 failed | 61 tests / 61 PASSED / 0 failed |
| 冒烟（独立树亲跑 exe） | 208 / 208 / 0 | 61 / 61 / 0 |

在库证据比对：四份 gtest XML 顶格统计 `tests="208" failures="0" errors="0"` ×2＋`tests="61" failures="0" errors="0"` ×2；在库四份运行日志尾行（208/61 PASSED）与亲跑一致。15 个 RPT-T12 具名新增用例（单测 12＋契约 3 个 TEST 函数）逐一在 `[ RUN ]/[ OK ]` 列在案（ModelSummarySchema×3、ModelSummaryProjection×3、TaskStatusSource×6；契约 ModelSummaryTaskStatusHeaderDiscipline×1＋TaskStatusVocabulary×2）。

**治理脚本（worktree A 上下文亲手复跑）**：`validate-task.ps1 -TaskFile …RPT-T12.json` → **PASS (1 tasks)**；`validate-docs.ps1` → **PASS (20 units, 12 trace entries, 150 task files)**——与在库 validate-task.log/validate-docs.log 同值。

**ird_gates 双树独立复跑＋归一化比对**：`cmake -DIRD_ROOT=<各树绝对路径> -DIRD_ENABLE_GIT=OFF -P cmake/ird_gates.cmake` 直跑 base（worktree C @ 06363682）与 head（worktree A）——两树 IRD-GATE- 行各 **65**；归一化路径前缀后逐行 diff＝**零差异（GATES-HIT-SET-IDENTICAL 独立成立）**（唯一原始差异为 `[ird_gates] 扫描根` 声明行的树路径，非命中项）；命中全部为既有基线项（reporting 相关仅 2 条既有 IRD-GATE-SUB——双测试目标直链 testkit 的既有允许形态，base 树同在）。

## 4.4 acceptance 条目 —— 五条逐项证据（验收者亲读源码＋亲跑测试）

| # | acceptance 原文要点 | 证据 | 结论 |
| --- | --- | --- | --- |
| 1 | schema 冻结（§9.7 字段全量＋冻结留痕） | 验收者独立逐字段比对 units/reporting.md §9.7（L1215-1240）与 ModelSummary.hpp（L159-248）：revision/snapshotIdentity/modelIdentity/nameMapIdentity/policyContentIdentity（core 四型 static_assert 锁定——契约测试 L65-83）、jointCount/allRevolute、installPresetToken（呈现注释原文）、Capabilities 八布尔（名与序全同 L195-204）、resources{resourceId,state,digest}/configurations{configKindToken,contentIdentity}——一致；provenanceNote 为已登记落位偏差②（§14.4 v0.15）；§12.1 runtime §13.2 行"已冻结（L5 适配）"（L1363）在案；冻结留痕 traceability/builds/wp12-t12/schema-freeze-note.md 在库且逐字段核对表与验收者独立比对结论一致 | 通过 |
| 2 | 投影单测（同修订同投影/快照已释放注记/try 轨 nullopt） | ModelSummaryTest.cpp 六用例亲跑通过：SameRevisionSameProjection（operator== 全字段＋callCount=2 穿透）、ReleasedSnapshotArchivedMetadataWithNote（snapshotIdentity isValid＋三身份全零＋注记非空＋installPresetToken"Custom"如实保留）、UnknownRevisionReturnsNullopt；另 FullFieldRoundtrip（§9.7 字段行逐项断言）与 ResourceStateTokenFrozenVocabulary 承载 schema 面 | 通过 |
| 3 | ITaskStatusSource 最小接口（值形态对齐/公共头零 execution 类型/事件不入报告内容） | TaskStatusSource.hpp：恰一个 tryStatus(core::TaskIdentity) const（static_assert L92-94 锁定最小面）；state＝core::TaskState 直传（static_assert L97-98——零第二词表）；progress 四字段对齐（L127-139）；termination 仅终态（TerminationOnlyOnTerminalStates 用例）；文件头 L41-46 明确事件仅预览刷新提示、无订阅面；公共头 include 仅 std＋core（grep 实证，零 runtime/execution/io/Qt）；契约测试 PublicHeadersZeroOffTreeIncludes 对两头做 include＋命名空间限定名字符串扫描亲跑通过；TaskStatusSourceTest 六用例亲跑通过（含 InterruptedTaskProjectionForPresentation_TASK02_REL03——"interrupted"词面与 core 终态逐字一致，契约测试 TerminationTokensMatch 四终态词面比对） | 通过 |
| 4 | P-RPT-2 处置：注入式维持＋签名冻结＋L5 适配建议随任务交付 | 契约测试 TrySummaryFn static_assert（`std::optional<ModelSummary>(IModelSummaryProvider::*)(core::RevisionId) const`——签名漂移即编译失败）亲跑编译通过；两契约头内各载 L5 适配建议（IRuntimeModelView→IModelSummaryProvider / tryTask·progress→ITaskStatusSource 装配要点）；零 runtime 编译边（CMake diff 仅测试源增列，产品目标四条链接边零改动——§3.4 登记边） | 通过 |
| 5 | 禁项锁定（不触发编译/不读 WC-DWC/installPresetToken 零判定/DH 权威模式不经本接口） | 返回值为纯值 ModelSummary（类型层无 WC/DWC 通道）；接口面恰一个方法——InstallPresetTokenPresentationOnly 用例（Ground/Inverted/Wall/Custom 四词透传＋接口面 static_assert 自证）亲跑通过；头内非法调用三条注释逐字落位（ModelSummary.hpp L263-271——§9.7 维度表原文；DH/显式权威模式归 modeling 提供方 L269-271） | 通过 |

knownPitfalls 双条均有处置承载：P-RPT-2＝签名冻结 static_assert（裁决前不变）；P-RPT-9＝schema 类型恒等 static_assert＋基线声明（core.md/runtime.md v0.1 Draft，冻结 diff 后增量同步——头内与 schema-freeze-note 双处声明）。

## 4.5 测试真实失败能力 —— 通过（四处产品面变异：三处被捕获＋一处覆盖缺口与 F-255 登记一致）

在 worktree A 逐次变异→重建→亲跑→`git checkout` 还原→重建复绿（每轮后 `git status --porcelain` industrialrobot 面 0 文件）：

1. **M1（TaskStatusSource.hpp `toToken(Interrupted)` 词面改 "failed"）**：3 用例红——单测 TerminationTokenFrozenVocabulary_RPT_T12_ACC3、InterruptedTaskProjectionForPresentation_TASK02_REL03＋契约 TerminationTokensMatchCoreTerminalStateTokens_RPT12_ACC3。
2. **M2（ModelSummary.hpp `toToken(Solidified)` 词面改 "recorded"）**：1 用例红——ResourceStateTokenFrozenVocabulary_RPT_T12_ACC1。
3. **M3（TaskStatusProjection::operator== 改恒真）**：1 用例红——SameIdentitySameProjection_RPT_T12_ACC3（EXPECT_NE 负例真实生效）。
4. **M4（ModelSummary::operator== 改恒真）**：208＋61 全绿不可检——与 attempt 1 转登的 **F-255** 登记一致（该等价算子无负向断言承载；实现本身经亲读证实为逐字段比较，属测试覆盖面建议，不阻断）。返工提交未改代码，缺口持续存在属预期。
5. 全部变异还原后重建 exit 0、208＋61 复绿，沙箱洁净。词表冻结纪律与值语义断言均具备真实红能力，非恒真、非"只编译通过"。

## 4.6 架构红线 —— 通过（AGENTS §5 八条逐条）

1. **L2 零 Qt**：两契约头＋三新增测试文件 `#include <Qt|<Q` 扫描零真实命中（唯一字面命中为契约测试自身的零 Qt 断言字符串 L148）。
2. **R-1 业务域互链**：CMake diff 仅两测试目标增列源文件；产品目标四条链接边（core/evidence/diagnostics/project——§3.4 登记边）零改动；runtime/execution 零编译边（注入形态）。
3. **R-2 跨单元私有头**：两契约头 include 面仅 std＋`sdurws/ird/core/` 公共头（Digest/Identity/Evaluation）＋本单元头——零私有头、零他单元头。
4. **SA-02 框架零修改**：diff 无框架文件；patches/ 零变更。
5. **R-4 名称前缀拼接/剥离**：双树门禁命中集恒同（R4 唯一命中＝project 单元既有 ProjectStoreImpl.cpp 1 处，base 树同在）——新增代码零 RobWork 字面量新增。
6. **PA-1 权威唯一**：本任务只定义 reporting 侧投影契约与词表（对端事实经注入值拷贝）；未实现 runtime/execution 语义（替换身边界声明在案——Scripted 夹具不构成对端行为证明）。
7. **PA-2 不可变历史**：纯新增文件＋文档追加登记，零改写。
8. **不继承 old/**：零 old/ 引用。

## 4.7 注释规范 —— 通过（两公共头全文＋三测试文件全文，超出"实现文件抽查 ≥3"下限；本任务 header-only 零产品 .cpp）

- **文件头**：五文件均有设计依据（§9.7/§3.3/§9.9/§11/runtime §10.12/§13.2/需求 ID/acceptance 编号追溯）＋背景说明（"为什么是投影而非封装"/TaskIdentity 对 TaskId 的替换理由/替身边界声明）。
- **类/函数**：Doxygen 中文齐备——IModelSummaryProvider/ITaskStatusSource 契约注释含前置/后置三支/线程（并发只读安全）/副作用（零、不触发编译）/所有权（借用语义、不接管实现对象）；try 轨失败语义（nullopt 不抛）明示；toToken 穷举 switch 与兜底返回的不可达性说明在案。
- **§2.5 高危信息**：物理量单位（jointCount/jointCount=6 标注"计数，无量纲"、digest＝SHA-256 原始字节、percent 无量纲）；确定性来源（同修订同投影/同身份同投影＋resources 投影不重排声明）；线程约束（实现方并发只读承诺、纯值类型、测试单线程）；错误语义（try 轨 nullopt vs 调用方违约 fail-fast 边界）；所有权与生命周期（返回值归调用方、投影为快照值、适配器随存储上下文存活）；installPresetToken 禁入判定以**加粗禁项**标注（结构体字段注释＋接口注释双处）。未发现复述式废注释与过期注释。

## 4.8 文档同步 —— 通过（偏差已按 DTB §5.4 登记；findings 未关闭条目逐族核对）

- reporting.md v0.15：文档头版本行、§3.1 组成表（TaskStatusSource.hpp 增列行＋公共头计数 12→13，L168/L184）、§14.4 v0.15 落位登记（四项落位偏差：独立成头/provenanceNote 承载位/两投影词表/archivePhase 不投影——逐项有理由与先例引用，L1493）——实现与单元卡零未登记偏差，契约语义零变更。
- **findings.json 未关闭条目相关性核对**（v1.2 要求；送验对象内 219 条 open，逐族核对与本任务相关者）：
  - **.gitignore 吞 .log 族（F-010/F-077/F-081/F-083/F-085/F-087/F-088/F-090/F-223/F-244/F-248/F-250/F-253）**：本任务 attempt 1 直接再现且为 fail 主因；attempt 2 复验确认本实例已闭环（11 件 `git add -f` 补交在库、内容与亲跑一致）。**F-223 系统性处置仍 open**——`.gitignore:87` 全局 `*.log` 规则未除（送验对象内实证），后续任务仍有复踩土壤，不因本任务顺手修而消账（.gitignore 不在本契约 allowedFiles 内，处置归构建约定所有者）。
  - **提交标题名词短语族（F-012/F-232~F-242/F-247）**：零复踩——三提交标题『实现…』『补交…』『返工补交…』均动词开头。
  - **计数失准族（F-025/F-041/F-073/F-235/F-236/F-238/F-243）**：92db3656"21 件"在冻结头状态下已与在库清点一致（不再失实）；**本任务发现同族新实例一处**——schema-freeze-note.md §5 与 36bbac31 正文"单测 12＋契约 4"，实际新增契约 TEST 函数 3 个（疑将四组 static_assert 面误计为用例），转登 **F-256**。
  - **ctest 空转/措辞族（F-251/F-252）**：本契约 verify 无 ctest 条目，不适用；本任务 gates-all-note 无"ctest 通过"措辞。
  - **全树口径族（F-008/F-235）**：本任务送验留痕与提交正文均未做"集成全树零错误"声称（口径＝交付面双目标＋冒烟全目标亲证），无复踩。
  - **测试敏感度族（F-043/F-255）**：M4 复验确认 F-255 缺口持续成立（返工未改代码，预期内）；F-255 维持 open，归后续 reporting 测试增列处置。

## 4.9 commit 质量 —— 三提交四段式齐备、验证结论经独立复跑证实

- **36bbac31** `[RPT-T12] 实现模型摘要与任务投影接入（ModelSummary/ITaskStatusSource）`——动词开头；四段齐备（What 按文件分点/Why 对应 RPT-01-B、TASK-02/NFR-REL-03 与 §9.7/§3.3/§9.9/runtime §10.12/§13.2/interUnit 判定/验证段双模式 208＋61＋GATES-HIT-SET-IDENTICAL＋validate 双 PASS/影响面 header-only 零行为变更＋四项偏差登记）；验证段声称经本验收独立复现全部为真（正文"契约 4"计数口径瑕疵归 F-256，见 4.8）。
- **92db3656** `[RPT-T12] 补交验证留痕 21 件（双模式构建/测试/门禁/治理脚本日志）`——动词开头；四段齐备；attempt 1 时"21 件"声称与在库 10 件不符构成 G-1，**冻结头状态下经 552c66d1 补齐后声称与在库一致**，不再是失实声称。
- **552c66d1** `[RPT-T12] 返工补交 11 件 .log 验证留痕（G-1：git add -f 破除 .gitignore 吞没）`——动词开头；四段齐备；Why 段如实援引 attempt 1 验收记录与 F-253、不 amend 已推送历史合规；How 段逐件核验声明（validate PASS、208/61、65×65）与验收者亲验值逐字一致。
- **粉饰排查**：测试计数（208＋61×2 模式）、门禁结论（命中集恒同）、validate 双 PASS、schema 核对结论、G-1 返工件"内容零改动零重新生成"（在库 .log 时间戳/统计与亲跑形态同源）——逐项经亲手复跑/复验为真；无粉饰。

## 4.10 留痕完整 —— 通过（attempt 1 阻断 G-1 已闭环）

- 在库（冻结树实证）：gtest XML×4＋ird-test-report.json×4＋gates 双树日志＋gates-hit-set-diff.txt＋gates-all-note.txt＋schema-freeze-note.md＋11 件 .log＝**21 件留痕＋1 冻结凭据**，与 92db3656"21 件"清单一致。
- **11 件 .log 内容与验收者亲跑逐字比对**：integration-test-run-unit.log `[PASSED] 208 tests`／integration-test-run-contract.log `61 tests`／smoke-test-run-unit.log `208 tests`／smoke-test-run-contract.log `61 tests`／validate-task.log `PASS (1 tasks)`／validate-docs.log `PASS (20 units, 12 trace entries, 150 task files)`／双 gates 日志 IRD-GATE- 行各 65——全部与亲跑同值；integration-build.log 含双测试目标＋产品库链接成功行、smoke-build.log 含 reporting 三产物行、smoke-configure.log 含配置命令与完成行——落位完整。
- XML 顶格统计四份与亲跑同值；schema-freeze-note §5 所引证据件全部可回查（attempt 1 悬空引用已消除）。
- **残留一处建议级**：gates-all-note.txt 尾句"归一化 diff 复核见 validate-task.log 附录"——validate-task.log 仅 1 行 PASS 输出、无附录内容（悬空指引）；GATES-HIT-SET-IDENTICAL 实质证据由在库 gates-hit-set-diff.txt（差异仅路径前缀）＋本验收双树直跑归一化零差异独立成立，纯指引文字失准，转登 **F-257**。

## 4.11 偷懒/缩水实现扫描 —— 通过

- **①标记扫描**：diff 新增行 `TODO|FIXME|stub|placeholder|未实现|not implemented` 零命中（grep 实证）；无"触发式延后"豁免需列出。
- **②空实现检测**：本任务 header-only——两接口为纯虚契约（=0 为设计形态非空实现）、两 inline toToken 有真实穷举 switch 体、三组 operator== 有逐字段体、局部夹具 ScriptedModelSummaryProvider/ScriptedTaskStatusSource 为脚本表＋调用计数真实实现（非空壳）；CMake 仅增列测试源。
- **③逐条证据表**：见 4.4——acceptance 五条逐一有具名测试/命令输出/文件行号；未执行而标注通过的情形为零（门禁证据＝验收者双树直跑亲证；治理脚本亲跑；留痕亲读比对）。
- **④测试强度**：与 4.5 联动——三处变异即红（4 用例）＋还原复绿；断言校验行为与边界（逐字段往返/词面冻结/三支后置/负例 EXPECT_NE/调用计数穿透），非恒真。残留不对称一处（M4）＝F-255 既有登记，属覆盖面建议非缩水。

## 发现的问题清单

| 级别 | 编号 | 内容 | 处置 |
| --- | --- | --- | --- |
| 建议 | G-2（→F-257） | gates-all-note.txt 尾句"归一化 diff 复核见 validate-task.log 附录"为悬空指引——validate-task.log 仅含 1 行 PASS 输出、无附录；实质结论（GATES-HIT-SET-IDENTICAL）由在库 gates-hit-set-diff.txt＋本验收双树直跑独立成立，纯指引文字失准 | 后续 reporting 留痕修订时更正该句指向（gates-hit-set-diff.txt），或在该 note 内直接载归一化比对结论；不阻塞合入 |
| 建议 | G-3（→F-256） | 留痕计数口径失准：schema-freeze-note.md §5 与 36bbac31 正文"本任务新增单测 12＋契约 4"，实际新增契约 TEST 函数 3 个（疑将四组 static_assert 断言面误计为用例）；实质证据完整（61 总数含 3 个具名新用例、四跑全绿），方向多报 1、无粉饰（F-041/F-243 同族） | 后续留痕/提交正文以 TEST 函数清点为计数口径；不阻塞合入 |

（attempt 1 阻断 G-1 已闭环，不在本清单；G-2/G-3 编号为本次验收记录内的问题序号，findings 登记号见括注。）

## 裁决

**verdict ＝ pass**。4.1～4.11 全部通过、无阻断级问题：范围零越界、双模式构建与四跑测试经全新树独立复现全绿、契约 verify 两条逐条成功、门禁命中集归一化零差异、治理双 PASS、acceptance 五条逐项有证、变异三红一绿（缺口＝F-255 既有登记）、注释与红线全项合规、G-1 返工真实闭环（11 件 .log 在库且内容与亲跑逐字一致）。建议级两处（G-2→F-257、G-3→F-256）已转登 findings.json，不阻塞合入评估；F-223 系统性处置（.gitignore 对 traceability/ 的负例豁免）维持 open，归构建约定所有者，建议在后续任务链中优先落地以终止该家族复踩。

**验收者**：独立会话（acc/RPT-T12/2）；2026-09-20。
