# WP-13-T09 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 ID | WP-13-T09（实现 DH↔显式转换五状态判定与等价验证） |
| 送验对象（冻结） | `7e8d3c369389099ee11e410d48b11801b08d8a19`（分支 wp13-t09 尖端；漂移检查：`git rev-parse origin/wp13-t09`＝该 SHA，无漂移） |
| diff 范围 | `0bb40848d219d117df8f80a8c963d9044077960b..7e8d3c369389099ee11e410d48b11801b08d8a19`（2 提交：b0cf4479 实现＋7e8d3c36 文档同步/留痕） |
| 验收分支／记录 | `acc/WP-13-T09/1`，本文件 |
| verdict | **fail**（阻断级 B-1 一项；建议级 G-1～G-4 已转登 findings.json F-338～F-341） |
| 验收日期 | 2026-09-25 |

## 1. 裁决与阻断级问题

### B-1（阻断）：集成模式契约测试全新构建不可复现为全绿，且在库留痕与复现矛盾

- **复现**：冻结 SHA 的 detached worktree 内集成模式全量构建后，亲手运行 `sdurws_ird_modeling_contract_test.exe`：**8/9，1 FAILED**——
  `MdlCommandPipelineContract.T08StableCodesRegisteredWithoutPrebuilding`
  断言失败于 `contract_test/CommandPipelineContractTest.cpp:288`：
  `EXPECT_EQ(registry.find("MDL-DH-NOT-EXPRESSIBLE"), nullptr);  // T09`。
- **根因**：该断言是 T08 时代"未到任务行不预建"的钉住面（该文件本次 diff **未改动**）；而本任务按契约 acceptance、§9.5 T09 行与单元卡 §14.6 v0.10 ⑤ **合法登记**了 MDL-DH-NOT-EXPRESSIBLE 等三码（`DiagCodes.cpp` 工厂 20 项，`registerModelingCodes` 逐条转发注册，DiagCodesTest.cpp:125 的 20 码封闭断言亲跑通过）。实现改了注册行为、未同步更新该钉住断言——送验源码状态下该测试**必然红**（与构建环境无关，冒烟模式不编入该 TU 故未暴露）。
- **留痕矛盾**：在库留痕 `traceability/builds/wp13-t09/integration-modeling-contract-test.xml` 记 `tests="9" failures="0"`（2026-09-25T01:24:27）——与冻结源码的亲跑结果（8/9）矛盾，该 XML 无法由送验对象复现。同秒生成的 `integration-modeling-test.xml`（159/0）可复现，唯 contract_test 件不可。
- **判级依据**：ACC §5 fail 判据"构建/测试不可复现"；留痕与复现不一致同时触发"验收粉饰"审查面（本记录按事实登记，机制不做推测）。

**返工输入**（实施者按此修复后重新发起**全量**验收）：
1. 更新 `contract_test/CommandPipelineContractTest.cpp` 的"不预建"钉住：T09 行三码缺席断言改为在册断言（或在册断言 T09 行＋缺席仅余 T13/T18 行），与 DiagCodesTest 的分期封闭性口径对齐；
2. 重跑集成全量（modeling_test 159＋contract_test 9 全绿）并**重新收取**全部留痕件（含 contract_test XML 与集成侧 ird-test-report.json，见 G-3）；
3. 建议级 G-1～G-4 随手消账或转治理侧。

## 2. 逐项证据（ACC §4，4.1～4.11）

### 4.1 改动范围 —— 通过

- `git diff --name-only 0bb40848..7e8d3c36` 共 16 文件，逐一比对契约 `allowedFiles`：modeling 单元 10 文件（CMakeLists.txt、DhConvert.hpp/.cpp、CommandHandlers.hpp/.cpp、DiagCodes.hpp/.cpp、test/DhConvertTest.cpp、test/DhConvertEquivalenceTest.cpp、test/DiagCodesTest.cpp）⊆ `modeling/**`；traceability/builds/wp13-t09/ 5 件留痕 ⊆ `traceability/**`；units/modeling.md 在列。
- `forbiddenFiles`（REQUIREMENTS.md/ARCHITECTURE.md）零触碰；diff 中无框架源码文件、无 `patches/` 新文件（SA-02 通过）。

### 4.2 可复现构建 —— 部分（构建零错误；测试复现见 B-1）

- **集成模式**（detached worktree @7e8d3c36，重新配置；环境件缺失按 F-099 在案口径自主树复制 `RobWorkStudio.ini.template.static`/`.shared.in` 后配置通过）：
  - 缓存确认 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`；
  - `cmake --build build --config Release --target sdurws_ird_modeling --parallel 8` → exit 0，`: error|错误 C|error LNK` 计数 0；
  - `sdurws_ird_modeling_test` → exit 0 计数 0；`sdurws_ird_modeling_contract_test` → exit 0 计数 0；
  - `ird_gates` 目标 → exit 1＝存量 56 命中（门禁引擎按设计命中即失败）；**命中集多重集比对**（引擎直跑口径，见 4.3 第 4 条）：base（detached worktree @0bb40848）56＝head 56，归一化后**逐条完全一致＝零新增命中**——实施登记口径"ird_gates 命中集比对＝EMPTY"独立复现成立。
- **独立冒烟模式**（独立临时目录，`-S …/industrialrobot`＋vcpkg toolchain＋Qt CMAKE_PREFIX_PATH，AGENTS §4.1 修正口径）：配置＋全量构建 exit 0，错误计数 0。

### 4.3 契约 verify 命令 —— 6 条逐条亲手执行；第 3 条不通过（B-1）

| # | 命令 | 结果 |
| --- | --- | --- |
| 1 | `cmake --build build --config Release --target sdurws_ird_modeling` | exit 0，零错误 |
| 2 | `cmake --build build --config Release --target sdurws_ird_modeling_test` | exit 0，零错误 |
| 3 | `ctest --test-dir …/modeling -C Release -L "^ird$"` | 首跑 1/2（contract_test **Not Run**——契约 verify 数组缺该目标构建行，→G-1）；补建后重跑：modeling_test PASSED（亲跑 exe **159/159**，与在库 integration-modeling-test.xml 一致）＋ contract_test **FAILED 8/9（B-1）** |
| 4 | `cmake --build build --config Release --target ird_gates` | exit 1＝56 存量命中；以引擎文档口径 `cmake -DIRD_ROOT=<…> -P cmake/ird_gates.cmake` 双侧（head/base detached worktree）直跑，归一化多重集 diff 为空——**零新增命中**（登记结论成立；命中明细含 ui 单元 Qt 面、F-302 pugixml LIB 等全部为存量） |
| 5 | `pwsh -File …/validate-task.ps1 -TaskFile …/WP-13-T09.json` | **PASS** (1 tasks)，exit 0 |
| 6 | `pwsh -File …/validate-docs.ps1` | **PASS** (20 units, 12 trace entries, 189 task files)，exit 0 |

- 冒烟补充亲跑：`sdurws_ird_modeling_test` **128/128**、`sdurws_ird_modeling_contract_test` **5/5** PASSED——与在库 smoke 侧留痕（128/5）一致。

### 4.4 acceptance 条目 —— 五条均有对应通过证据（未见"未执行标通过"）

| 契约 acceptance | 证据（亲手复现） |
| --- | --- |
| 1 无损展开/零位对齐/基座混入拒绝 | `MdlDhConvert.DhToExplicit{AccumulatesCardFormula,ZeroAlignment,RejectsEmptyAndMixedBase}_WP13T09_ACC1` 3 用例通过（独立参考展开另行实现；origin=T_parent_joint、axis 归一化 ‖·‖≈1、ChainEmpty/DegenerateBase 两态、不产诊断） |
| 2 五状态样例/结构检查先行/逐关节逐项上界 | NotExpressible（prismatic/fixed/continuous 未确认三支，终判不进求解、subject 定位）＋Exact（六轴 UR 类逐关节逐项回收 ≤1e-9）＋ExactNonUnique（重合/平行轴）＋Approximate（E>0、比较型三要素）＋AnalysisFailed（1e200 溢出、无参数产出）——9 用例全过 |
| 3 退化族字典序/确定性 | freeCoordinates＝{0,4} 字典序、钉中性值 0（EXPECT_DOUBLE_EQ 0.0）；解集证人 ≥2；`result == repeat` 逐字段相等两次复现 |
| 4 verifyEquivalent/CompileProbe/roundtrip | `MdlDhEquivalence` 6＋`MdlDhCommand` 5 用例（集成模式，编入 modeling_test 159 内）全过：正路径 ≤1e-9 m/rad、偏差链拒绝 1e-6 注入、roundtrip 参数级＋FK 级双重一致、探针调用计数 2。测试替身以 runtime 公共 CanonicalModelBuilder 装配 S5（产品编译器类在 runtime/src/CompilerImpl.hpp 为单元私有头，R-2 禁跨单元 include，生产探针归 L5）——**该装配取舍已登记单元卡 §14.6 v0.10 ⑥**，按 DTB §5.4 判为已登记偏差，不算私设 |
| 5 权威切换独立领域命令 | prepareAuthoritySwitch 四道门（前置/C-6 先决断/判定/FK 对照）＋命令 prepare 协作面（正路径重编码、C-3 终判拒绝 RejectedHardAssert＋计划清空、C-6 混合编辑 RejectedInvalidInput、非切换载荷零影响、装配缺失 fail-fast）8 用例全过 |

注：acceptance 3 提到的 contract_test 9/9 证据链随 B-1 失效，但五条 acceptance 本身均在 modeling_test 的 159 条中有独立通过证据（contract_test 的失败面是 T08 时代钉住断言，非 T09 acceptance 的直接断言面）。

### 4.5 测试真实失败能力 —— 通过（两处变异均致红，注入后已还原）

在 detached worktree A 内对 `src/DhConvert.cpp` 注入（构建→亲跑→还原，`git status` 复核源码与送验 SHA 一致，重跑复绿 159/159）：

- M1：`verifyEquivalent` 的 FK 全称判定条件反转（`>` → `<`）→ `MdlDhEquivalence` 4 用例 FAILED；
- M2：`dhStepTransform` 平移向量 (a,0,d)→(d,0,a) → `MdlDhConvert` ACC1 两用例＋Exact/ExactNonUnique 三用例 FAILED。

断言校验行为与边界，非恒真。B-1 的契约钉住断言本身亦为"实现行为变化可致红"的旁证。

### 4.6 架构红线 —— 通过（八条逐条）

- L2 零 Qt：modeling 产品面（include/src）grep Qt 头零命中；门禁 R3 命中全部为 ui 单元存量（多重集比对零新增）。
- R-1 业务域互链：无 `ird/<requirements|kinematics|trajectory|dynamics|selection|optimization>` include；产品链接面＝六条登记边（core/diagnostics/project/runtime/policy/io，CMakeLists 守卫＋白名单同源）。
- R-2 跨单元私有头：新公共头仅在 `include/sdurws/ird/modeling/`；对 runtime 消费全部经公共头（Compiler/CanonicalModel/Description/Errors/Sources.hpp）；私有实现头（匿名命名空间）不跨单元。
- SA-02 框架零修改：diff 无框架文件、patches/ 零新文件。
- R-4 名称前缀：无拼接/剥离逻辑。
- PA-1 权威唯一：转换器为纯函数服务，等价验证经只读探针（不发布快照、不产生修订）；写路径仍归命令 prepare→project。
- PA-2 不可变历史：本任务无修订写面。
- 契约 `knownPitfalls` P-MDL-8：未触发（buildCanonicalModel 分段入口签名未变——单元卡 v0.10 ⑦ 登记，亲测编译链接通过佐证）。

### 4.7 注释规范 —— 通过（一处建议级 → G-2/F-339）

- 公共头全查：`DhConvert.hpp` 文件头（设计依据/背景/容差纪律/确定性/线程安全）＋全类型全方法 Doxygen 中文注释，§2.5 高危信息齐备（rad/m 单位、坐标系读法〔core.md §4.6〕、角度制式、所有权〔probe 非 owning〕、错误语义〔值面/判定面互斥、产码唯一书写点〕、确定性来源〔附录 D 阈值 vs 过程常数两类分立〕）；`CommandHandlers.hpp` T09 增量块与 HandlerServices 注入位注释完整。
- 实现抽查 ≥3：`DhConvert.cpp`（逐步算法注＋过程常数区）、`CommandHandlers.cpp`（③.5 权威切换门逐段）、`DiagCodes.cpp`（T09 三码逐字段登记口径）——均达标。
- CMakeLists 关键步骤中文注释块：达标。
- 发现（建议级）：`DiagCodes.hpp:181` @brief 仍写"当前 19 项"，工厂实际 20 项（`DiagCodes.cpp:15` 文件头注已正确写 20；DiagCodesTest 20 码封闭断言通过）——F-316 同族复发（T08 验收同文件同模式已登记过一次），→F-339。

### 4.8 文档同步＋findings 相关性 —— 同步通过；findings 核对结论如下

- 单元卡 modeling.md v0.10（diff 在库）：T09 行落位＋实现细化登记（两阶段确定性求解/解析种子、自由坐标语义注〔平行轴样例为末级 θ——实现细化非语义变更〕、输出面规范分支、零位折叠、链级 Description 与 T12 分工、命令面 C-6 投影、探针 S5 装配取舍、P-MDL-8 处置、验证留痕数字）——实现与任务卡零未登记偏差。
- 契约状态字段仍为 `ready`（翻转归收尾 PIPE §4.7⑤，非实施面缺陷）。
- **findings F-318～F-336 逐条核对**（当前登记册 open 271 条）：
  - F-318～F-320：本登记册无独立 id 条目（F-321 resolution 注明"acc/UI-T16/1 已占 F-318~F-320"）——与本次送验面（modeling 域）无关联，无消账面。
  - F-321～F-325、F-327～F-330：UI-T16/T17/T18 取证管线与文档勘误族——与 modeling/构建域无关联；F-331～F-334 在 acc/UI-T18/1 分支登记册中，收尾并集处置已由 F-330 条目承载。
  - F-326：已 fixed（登记册状态）。
  - F-335/F-336（构建域——门禁取证口径）：本次验收已按 F-336 建议采用**引擎直跑口径**（cmake -P）做命中集比对，规避 MSBuild 捕获路径的截断/编码风险——两案维持 open（所有权治理侧），本任务无可消账面。
  - **与本次送验面相关者**：F-302（modeling pugixml LIB 存量）本 head 侧门禁命中仍在（存量，无消账面）；F-300/F-304/F-310/F-314（契约 verify 缺目标构建行家族）在 **T09 契约复发**（verify 数组缺 `sdurws_ird_modeling_contract_test` 构建行）→G-1/F-338；F-316（DiagCodes.hpp 计数注释失实）**同族复发**→G-2/F-339；F-317（ird-test-report.json 不完整）**复发**→G-3/F-340；F-293/F-298/F-299/F-303/F-308/F-313（提交标题家族）本两提交动词开头未再现；F-007/F-062/F-098/F-099/F-101（全新树配置环境链）本次再次实证（ini 模板环境件缺失，按 F-099 口径复制后通过），仍 open、所有权治理侧。
  - 无 open 条目声称由本任务修复——无静默积压。

### 4.9 commit 质量 —— 形式通过；验证段结论与复现矛盾并入 B-1

- b0cf4479「实现 DH↔显式转换五状态判定与等价验证」/7e8d3c36「文档同步：modeling.md v0.10（T09 落位登记）＋验证留痕」——`[WP-13-T09]` 前缀＋动词开头，四段（What/Why/验证/影响面）齐备。
- 计数自洽：159＝139＋20（MdlDhConvert 9＋MdlDhEquivalence 6＋MdlDhCommand 5，与源码逐一对应）；冒烟 128＝119＋9（gated 子集）与 CMake gating 逻辑一致。
- 验证段"contract_test 9/9""ctest 2/2 通过"与验收者全新复现（8/9、Not Run→FAILED）不符——已实测为 B-1 的构成部分，不再单独降级。

### 4.10 留痕完整 —— 不通过（B-1 留痕矛盾＋G-3 集成侧 ird-test-report 不完整）

- 可复现件：integration-modeling-test.xml（159/0）＝亲跑一致；smoke-modeling-test.xml（128/0）、smoke-ird-test-report.json（128 条全量含 MdlDhConvert 9）＝亲跑一致。
- 不可复现件：**integration-modeling-contract-test.xml（9/0）vs 亲跑 8/9**（B-1）。
- ird-test-report.json（集成侧）summary=9 仅含契约测试（BuildGraph 5＋CommandPipelineContract 4），缺 modeling_test 159 条——F-317 同族复发 →G-3/F-340（smoke 件完整）。
- 构建日志与 validate 日志：本次未在库要求（在库留痕五件已逐一核对）；phase-one-validation.log/resultNote：本任务未涉及（同 T08 先例不单列）。

### 4.11 偷懒/缩水实现扫描 —— 通过（一处建议级 → G-4/F-341）

- ① 关键字扫描：diff 内 `TODO|FIXME|XXX|stub|placeholder|not implemented|未实现|待实现` 零实质命中（5 处"占位"均为注释语义——报告字段缺省值说明〔NFR-COR-03 不伪造数值〕，非 stub 实现）。
- ② 空实现检测：IDhExplicitConverter 三方法与 prepareAuthoritySwitch 均实质实现；`dhToExplicit` 的 diags 输出参数为契约预留位（头注说明"无登记码不产诊断"——语义明确非缩水）。
- ③ acceptance 逐条证据表：见 4.4（每条具名测试＋亲手复现结论）。
- ④ 测试强度：边界与行为断言（逐关节逐项、字典序钉值、逐字段确定性相等、变异致红 4.5）——无恒真。
- 建议级：`DhConvert.cpp:510-515` 残留 `#ifdef IRD_DH_DEBUG` 调试块（`std::fprintf` 且未包含 `<cstdio>`——宏一旦定义即编译失败；if 缩进错位），建议清除 →G-4/F-341。

## 3. 问题清单

### 阻断级（1 项）

| 编号 | 问题 | 处置 |
| --- | --- | --- |
| B-1（→F-337，severity=blocking） | 契约测试 T08 时代"T09 行不预建"钉住断言未随 T09 登记同步；冻结源码全新构建 contract_test 8/9 失败；在库 integration-modeling-contract-test.xml（9/0）不可复现 | 实施者按 §1 返工输入修复后**重新发起全量验收** |

### 建议级（4 项，均已转登 findings.json）

| 编号 | 摘要 | findings |
| --- | --- | --- |
| G-1 | T09 契约 verify 数组缺 `sdurws_ird_modeling_contract_test` 构建行——全新树按 verify 清单执行 ctest 时 contract_test Not Run（F-300/F-304/F-310/F-314 家族复发） | F-338 |
| G-2 | DiagCodes.hpp:181 @brief"当前 19 项"vs 工厂实际 20 项（cpp:15 已正确；F-316 同族复发） | F-339 |
| G-3 | traceability/builds/wp13-t09/ird-test-report.json 仅含契约测试 9 条，缺 modeling_test 159 条（F-317 家族复发；smoke 件完整） | F-340 |
| G-4 | DhConvert.cpp:510-515 残留 IRD_DH_DEBUG 调试块（fprintf 未含 cstdio、缩进错位） | F-341 |

## 4. 验收者环境说明

- Windows 10（10.0.26200 x64）；Visual Studio 17 2022（MSVC）；pwsh 7；Qt 6.11.1（msvc2022_64）；vcpkg 经典模式（主树 `vcpkg/installed/x64-windows`，gtest/pugixml 已装）。
- 复现全程按送验 SHA `7e8d3c36…a19` detached worktree 执行（`/d/10_Source_Repos/21_robot/acc-t09-A`）；集成配置＝AGENTS §4.1＋F-007/F-098/F-099 环境链修正口径（vcpkg toolchain＋Qt 前缀＋ini 模板环境件自主树复制）；构建以 `--parallel 8` 执行（与契约命令语义等价）。
- 冒烟模式另配独立临时目录（toolchain＋Qt 前缀）；门禁取证按 F-336 建议采用引擎直跑口径（`cmake -DIRD_ROOT=<…> -P ird_gates.cmake`），base 侧另建 detached worktree @`0bb40848` 直跑同脚本后做归一化多重集比对。
- 独立性声明（ACC §2）：验收者未读取实施者任何过程叙述；全部结论以送验 SHA 产物、在库留痕与亲手复现输出为准；未向 `wp13-t09` 分支推送或修改任何内容（变异注入在临时 worktree 内实施并已还原，`git status` 复核与送验 SHA 一致）。
