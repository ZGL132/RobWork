# PRJ-T09 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | PRJ-T09 查询端口（QueryPort：视图/闭包/缓存/隔离） |
| 单元 | project |
| 送验对象（冻结） | 93489c02687e80eb97ba8e8d3267f72f530aff0e（origin/wp04-t09 尖端＝该 SHA，实测无分支漂移） |
| diff 范围 | 9f35fa2f67bdc74ece8f772d41e9c4c6c4bb64af..93489c02687e80eb97ba8e8d3267f72f530aff0e（state.base..headSha；merge-base 实测＝base＝9f35fa2f，4 提交 18 文件） |
| 验收会话 | 全新上下文（ACC §2 四条独立性逐条适用：未消费任何实施叙述，只读代码/契约/设计文档/留痕；构建测试亲手重跑；为"不通过"找证据立场） |
| 复现现场 | `git worktree add --detach <临时目录A> 93489c02`（detached @ SHA）；独立冒烟目录另配（带 vcpkg toolchain）；全部验证在临时目录内完成 |
| **verdict** | **pass**（4.1～4.11 全部通过；零阻断级问题；建议级 5 项 F-147～F-151 随本记录转登 findings.json，不阻塞合入） |
| 日期 | 2026-09-16 |

---

## 1. 逐项证据（ACC §4，4.1～4.11）

### 4.1 改动范围 — 通过

- `git diff --name-status base..head`＝18 文件：project 单元 12（CMakeLists.txt、include/{ProjectStore,PersistenceFormat,QueryPort}.hpp、src/{Codec.hpp,Codec.cpp,ProjectStoreImpl.hpp,ProjectStoreImpl.cpp,QueryPortImpl.hpp,QueryPortImpl.cpp}、test/{BuildRedLineTest,QueryPortTest}.cpp）＋traceability/builds/wp04-t09/ 5（ird-test-report.json＋四份 gtest XML）＋units/project.md 1——全部落在契约 allowedFiles 三个通配内（`project/**`、`traceability/**`、`units/project.md`）。
- forbiddenFiles（REQUIREMENTS.md/ARCHITECTURE.md）零触碰（diff 文件清单实证）。
- SA-02：diff 中无 patches/ 文件（PATCHES.md 零改动、无新 patch），框架源码路径 diff 为空——零未登记框架改动。

### 4.2 可复现构建 — 通过（任务目标面从零零错误；全树中止＝F-008 基线既有，本任务未尝试全树）

- 集成模式重新配置（worktree A 内全新 build/）：`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON`＋vcpkg toolchain 绝对路径＋Qt 前缀 `D:/software/Qt/6.11.1/msvc2022_64`＋`-DCMAKE_CXX_FLAGS="-DNOMINMAX -DWIN32_LEAN_AND_MEAN -DWIN32"`（主树 CMakeCache 实证参数同源；两件未跟踪 ini 模板自主树复制——F-062/F-007/F-098/F-099 登记的 worktree 复现前置逐件补齐）；configure exit 0，缓存 `grep ^RWS_BUILD_INDUSTRIALROBOT build/CMakeCache.txt`＝`RWS_BUILD_INDUSTRIALROBOT:BOOL=ON` 确认（acc-configure-integration.log）。
- 任务目标面从零构建零错误：`--target sdurws_ird_project_test` EXIT=0、`: error` 计数＝0（acc-build-project-test.log）；`--target sdurws_ird_project_contract_test` EXIT=0（acc-build-contract-test.log）。
- 独立冒烟模式（另一临时目录，`-S industrialrobot -DCMAKE_TOOLCHAIN_FILE=<repo根>/vcpkg/scripts/buildsystems/vcpkg.cmake`——AGENTS §4.1/CORE-T01 G-2 口径）：configure＋Release 构建 EXIT=0 零 error（acc-smoke-configure.log／acc-smoke-build.log），两测试 exe 产出。

### 4.3 契约 verify 命令 — 通过

- 契约 verify 唯一条 `cmake --build build --config Release --target sdurws_ird_project_test`：验收者亲手执行 EXIT=0，产物 `sdurws_ird_project_test.exe` 生成。
- `verify-task.ps1 -TaskFile tasks/foundation/PRJ-T09.json`（内部复跑同一 verify 命令）：`verify-task: PASS PRJ-T09`（acc-verify-task.log）。

### 4.4 acceptance 条目逐条 — 通过（四条均有具名测试证据，验收者双模式亲手重跑全绿）

| # | 契约 acceptance 原文要点 | 证据（QueryPortTest 14 用例；集成＋冒烟双模式 183/183、contract 10/10 全绿，XML 双留痕） |
| --- | --- | --- |
| 1 | 并发只读单测通过（§4.7 全方法线程安全；返回视图为进程内值拷贝、修订视图一经返回不可变——PA-2/PA-3 承接） | `ConcurrentReadOnly_ThreadSafeWithConcurrentCommits`（4 线程×40 轮×全方法＋主线程并发提交 3 次——head seq 单调不回退/种子修订视图恒定/对象字节恒等/历史链严格递减；gtest 断言不进工作线程、失败经互斥消息表汇合主线程；收尾断言磁盘 HEAD seq=5＋branchHistory 5 条——查询未干扰写入）＋`ViewIsValueCopy_ImmutableAfterReturn`（取快照→再提交→旧视图逐字段全等〔`==` 全字段比较〕；修改副本不影响端口——值拷贝非共享引用）。线程安全证据口径（"结果一致性断言，数据竞争静态检测归 TSAN/人工评审"）已按 DTB §5.4 在测试文件头登记，不超诺——视为有证据。 |
| 2 | 闭包外不可见隔离；tryObject/tryRevision 不存在返回空 optional 不抛；context-closed 拒绝；只读打开实例同样提供查询（PM-07） | `ClosureOutsideRevision_InvisibleToQueryPort`（植入闭包外修订〔合法清单＋悬挂元数据引用〕→重开→tryRevision/metadataAt nullopt、revision StoreCorrupt、head 指向闭包内、branchHistory 不含闭包外修订——对照腿防"全不可用"假阳性）、`TryTrack_MissingReturnsNullopt_NoThrow`（不存在/全零/未设置统一 nullopt/空表＋健康对照腿）、`ContextClosed_AllMethodsRejected`（抛轨 11 方法逐个 ContextClosed；tryObject noexcept 面 nullopt＋开发诊断 devContains 断言）、`ReadOnlyInstance_QueriesFullyServed`（显式 ReadOnly 打开〔写者持锁存活〕→writable()==false 且 head/object/branchTips/currentMetadata/listDrafts/listRuns 全量可用——PM-07）。 |
| 3 | ②端口所有者语义用例过：只读快照/对象/修订/分支/草稿/结果清单视图齐备（ARCH §7.2）；ObjectCache 预算参数生效（§5.2） | `RevisionView_HeadAndRevision_FieldsAssembled`（视图逐字段：seq/parent/branch/objectRefs 含域对象＋ProjectMetadata/metadataRef 完整形态/summary/inverse/hasUnresolvedPayload；与磁盘 HEAD 锚定）、`MetadataAndBranchViews_AuthoritativeDiscipline`（currentMetadata 权威 vs metadataAt 历史快照区分——INV-M3；branchTips 自权威投影）、`BranchHistory_WalksParentChainWithLimit`（全链 4 条新→旧/seq 严格递减/parent 逐级相扣/maxCount 截断/maxCount=0 空表/未知分支 invalid_argument）、`ObjectRead_TryTrackAndStrongTrack`（字节全等＋try/强轨引用缺失二分＋全零 fail-fast）、`DraftListing_CurrentOnly_StaleAndOrder`（.new 残留不列/stale 逐项正确/moduleId 字典序）、`RunListing_OnlyFinalizedFiltered_runDir`（D-13 仅 manifest 在/按修订过滤含反例腿/runDir 规范编址＋工件真实可寻/全零 fail-fast）、`ObjectCacheBudget_EnforcedThroughQueryPort`（1024B 预算传导 cacheStats.budgetBytes==1024、byteCount≤预算、LRU 逐出后最新键在缓存——经查询端口读侧真实路径）。 |
| 4 | P-PR-1 处置：视图身份类型消费以 core.md v0.1 为基线，冻结 diff 后增量同步、不私改 core | `Ppr1_ViewIdentityTypes_AreCoreContractTypes`（编译期 static_assert 五断言：RevisionView.id＝core::RevisionId、BranchTip.id＝core::BranchId、RunInfo.runId＝core::RunId、ProjectMetadataView.ref＝ObjectRefPair、InverseRef＝InverseCommand 别名；运行期 rev-/brn-/obj- 规范文本＋tryFromCanonical 往返）；零 core 修改实证：diff 无 core/ 路径文件。 |

### 4.5 测试真实失败能力 — 通过（双探针注入即红、还原复绿）

- 探针 1（生命周期门卫）：`QueryPortImpl::requireOpen` 的 `m_state == Closed` 判定改为恒假（禁用 context-closed 拒绝）→重建→`--gtest_filter=QueryPortTest.ContextClosed_AllMethodsRejected`：**FAILED**（关闭后查询不再拒绝）——测试对门卫行为真实敏感。
- 探针 2（运行清单过滤）：`listRuns` 的 `manifest.taskIdentity.revision != revision` 反转为 `==`（过滤语义倒置）→重建→`RunListing_OnlyFinalizedFiltered_runDir`：**FAILED**（runs.size 断言红）。
- 两探针各自还原（`git checkout --`，新 mtime 避免 F-068 增量陷阱）后 `git status` 干净、全量重跑 183/183 复绿（acc-test-run-revert-rerun.log）。探针仅存在于验收者临时目录，未触碰送验对象与任务分支。

### 4.6 架构红线（AGENTS §5 八条逐条） — 通过（零新增门禁命中；F-145 既有命中未触碰）

1. L2 零 Qt：project 单元 grep 实际 Qt include＝0 命中（BuildRedLineTest 两处命中文本均为该测试自身的注释字符串）；BuildRedLineTest 白名单排除仅覆盖 `#include "QueryPort`（单元卡 §3.1 点名自有头），`<Q` 角形态判定不变——误报修复非红线放松，且已随单元卡 v0.11 登记。
2. R-1 业务域互链：产品库唯一链接边 `target_link_libraries(sdurws_ird_project PUBLIC sdurws_ird_core)`（CMakeLists.txt:155 实证）；跨单元 include 仅 `sdurws/ird/core/`（core 为基座单元）。
3. R-2 跨单元私有头：新实现头 QueryPortImpl.hpp 置于 src/；公共头仅 include 公共头（core/Identity、本单元 PersistenceFormat/StoreTypes）；测试消费同单元私有头沿 ProjectStoreTest/TxEngineTest 先例（同单元消费不属 R-2 禁令）。
4. SA-02：见 4.1（零框架改动、patches/ 零新增）。
5. R-4 名称前缀：ird_gates 实跑 IRD-GATE-R4 命中 1 处＝ProjectStoreImpl.cpp:105 `kCreatedWithToolVersion = "RobWork-IndustrialRobot/0.1"`——**F-145 已登记的 PRJ-T08 既有命中**，`git diff base..head` 实证本任务零触碰该常量（该行 diff 命中计数＝0）；本任务新增代码（QueryPortImpl/QueryPort）零 R4 命中。
6. PA-1 权威唯一：查询语义全部落在单元卡 §5.2/§4.5-4.7 范围内；诊断码产出纪律（查询期不私造稳定码、容错走开发诊断通道——CR-08）在实现注释与实现口径④中显式登记。
7. PA-2 不可变历史：查询端口全只读（无任何写路径）；视图组装只读已注册不可变数据；闭包外残留"不装载不删除"，植入用例的磁盘残留原样保留。
8. 不恢复 old/：diff 无 old/ 路径。

**附：ird_gates 实跑结论（F-035/F-144 处置建议口径——验收者代行）**
`cmake --build build --config Release --target ird_gates`：exit 1，**9 处命中**＝PRJ-T08 验收登记基线（IRD-GATE-SUB×7：diagnostics_contract_test/evidence_test/evidence_contract_test/policy_test/policy_contract_test/runtime_test→testkit＋testdata_lint→testkit 表外边，F-051/F-019 家族；IRD-GATE-T1×1：testdata_lint；IRD-GATE-R4×1：ProjectStoreImpl.cpp——F-145）**＋零新增**。与 PRJ-T08 验收基线（9 命中）逐项一致。门禁自测 9 项（pass_clean/pass_r4comment/fail_r1/fail_t1/fail_r5/fail_sub/fail_r3/fail_r4/fail_t2）全部按预期。日志含 GBK 乱码（F-065 既有登记）。

### 4.7 注释规范 — 通过

- 公共头全查：`QueryPort.hpp`（526 行）——文件头设计依据（§5.2/§4.5/§4.6/§4.7/§6.3/§9.8＋ARC-02/CON-01/PA-2/PA-3＋任务契约）、ARC-02 背景（为什么查询必须是独立端口）、增量落位三处口径、线程模型/生命周期/错误二分逐项；12 个纯虚方法每个均有完整契约注释（@param 方向与取值、@return、@throws 触发条件、线程安全行）。`ProjectStore.hpp` 增量 query() 访问器注释（生命周期/只读实例/拒绝态/noexcept 面）齐备。`PersistenceFormat.hpp` 增量 RunManifest/RunManifestItem 注释（§4.4.7 字段表锚定、为什么只有 parse 无 dump、manifestDigest 透传 CR-02）齐备。
- 实现文件抽查（≥3，实为全部新增/大改文件）：`QueryPortImpl.hpp`（204 行——文件头实现形态背景/一致性窗口与锁序〔高危线程约束〕/错误二分/P-PR-1/生命周期所有权逐项）、`QueryPortImpl.cpp`（655 行——文件头实现口径五项登记；每函数语义段注释＋算法步骤〔如 branchHistory 两步：锁内取 tip→锁外沿链〕＋分支边界〔maxCount=0、目录不存在、单条目损坏跳过、迭代中断整表拒〕＋确定性来源〔moduleId/runId 字典序＝NFR-COR-02〕）、`ProjectStoreImpl.{hpp,cpp}` 增量段（装配时序/析构序安全论证/queryDevSink 窄访问器注释）、`QueryPortTest.cpp`（1238 行——每用例锚定注释：需求/AT/任务卡口径＋四项测试口径登记）。§2.5 高危信息（锁序、所有权 non-owning/unique_ptr、错误码二分、NFR-COR-02 排序、noexcept 边界）逐项在位。
- 反面示例扫描：未发现复述代码/无信息量占位/中英机翻腔/过期注释。

### 4.8 文档同步 — 通过（家族未消账/再现转登 F-147/F-148/F-149/F-150；零未登记实现偏差）

- 实现与任务卡零未登记偏差：五项实现口径（tryObject noexcept 边界/hasUnresolvedPayload 判据注入/branchHistory invalid_argument/清单扫描容错/ProjectMetadataView 字段落位）与 BuildRedLineTest 白名单修复、RunManifest 随消费者落位——全部按 DTB §5.4 以单元卡 §15.4 v0.11 行＋三个源码文件头登记；验收者逐项与代码核对一致。
- 单元卡 v0.11 登记内容与代码逐项核对：测试分组（acceptance 1～4 对应用例名逐一相符）、14 用例计数相符（XML `name="QueryPortTest" tests="14"`）、183/183＋10/10 相符。
- findings.json 未关闭条目相关性核对（v1.2 义务，125 条 open 逐族排查）：**F-140/F-143（卡头日期行同步＋家族注明）本任务未执行→再现，转登 F-147**；**F-141（幻影清单——builds[]/正文只列库内文件）本任务再现→转登 F-148**；**F-144（实现类任务实跑 ird_gates）本任务未执行且理由措辞失实再现→转登 F-149**；**F-145（major，R4 例外登记缺位）**——验收者实跑证实基线 9 命中与本任务零新增，该条维持 open 待 DTB §4.5 所有者登记，非本任务可消账；**F-146（失权后写拒绝端到端用例）**——属 PRJ-T08 交付面（写门卫②道），本任务为查询端口（只读），不相关，维持 open 归 PRJ-T15；F-005/F-006/F-058/F-078（单元卡转义损毁族）不涉及 project.md 相关节；F-007/F-062/F-098/F-099/F-101（worktree 环境前置族）验收复现时再次命中，按既有登记处置不重复登记；F-008（RobWorkSim 基线缺陷）与本任务无关（未构建全树）；F-065（日志 GBK 乱码）再现于本任务 ird_gates 日志，既有登记在案。
- 契约 status 维持 ready：翻转 done 归合入收尾（PIPE §4.7⑤），非验收项。

### 4.9 commit 质量 — 通过（标题家族问题转登 F-151；无粉饰实证）

- 4 提交（b2fa1c5e 实现／60e10e78 测试＋误报修复／3c0b8bf6 单元卡 v0.11／93489c02 留痕）正文四段（What/Why/验证/影响面）齐备，Acc-Covered 行逐提交在位且与 acceptance 对应合理。
- 验证结论与留痕逐项比对真实：183/183＋10/10、XML tests/failures 属性、validate-task PASS、exit 0——验收者双模式独立复现**全部为真**；b2fa1c5e 对"Q 前缀误报"的定性（自有头触发启发式、非放松红线）经代码走读属实；93489c02 留痕数据与库内 XML 一致。唯一失实措辞＝门禁理由栏（F-028/F-144 家族，转登 F-149）——属"未执行项的理由表述"而非"已执行项的结论粉饰"。
- 标题：b2fa1c5e「实现查询端口（…）」、60e10e78「新增…并修复…」动词开头合规；3c0b8bf6「[units] project.md v0.11：…」（[units] 车道文件名词，F-037 系）、93489c02「[governance] PRJ-T09 验证留痕：…」（F-139 同字面模板第八例）→转登 F-151（家族建议级，既往判例均不阻断）。

### 4.10 留痕完整 — 通过（库外 .log 混列转登 F-148）

- 库内实证（git ls-tree @93489c02）：ird-test-report.json（schema ird-test-report/1 全字段）＋四份 gtest XML——XML 根属性 tests="183" failures="0"（integration/smoke 两份）、tests="10" failures="0"（contract 两份），与提交声称一致。
- 验收者复跑产物与送验 XML 交叉核对：顶格计数 183/0（集成亲手重跑）、183/0（冒烟亲手重跑）、10/0×2 逐字一致；QueryPortTest 套件两模式均恰 14 用例。
- ird-test-report.json builds[] 引用的 4 份 .log 与提交正文列举的约 7 份留痕**不在库内**（根 .gitignore:87 `*.log`＝F-010 根因），提交正文未内联明示、report 无 localOnlyNote→F-148（家族建议级第 16 例）。验收者以双模式重跑补强了这些日志结论的真实性（构建零错误/测试全绿均亲手复现）。
- phase-one-validation.log 未更新（该文件自 WP-03 时代后未再滚动）；resultNote 等价物＝builds/wp04-t09/ird-test-report.json，沿本单元 T01～T08 先例，视为满足 4.10 的"或 resultNote"半边。

### 4.11 偷懒/缩水实现扫描 — 通过

- ①TODO/FIXME/stub/placeholder/未实现/not implemented：diff 全部新增行 grep **零命中**。无"触发式延后"新增（v0.10 登记的"端口访问器只能 stub"规避形态已被本任务以真实实现兑现——query() 挂载真实 QueryPortImpl，非空壳）。
- ②空实现检测：IProjectQueryPort 12 个纯虚方法在 QueryPortImpl 全部有实质实现（head 重读磁盘＋闭包锚定、assembleView 三步组装、listRuns 目录扫描＋双重过滤、listDrafts 容错扫描等）；无返回默认值的空函数体；friend 窄访问器（queryDevSink/objectStore）为声明的观测面而非缩水。
- ③逐条证据表：见 4.4——四条 acceptance 全部有具名测试/命令输出/XML 属性，无"已实现"声明式证据。
- ④测试强度：断言校验行为与边界（稳定码逐字、字段级全等、对照腿防恒真〔ClosureOutside 的 head/branchHistory 对照、RunListing 的 r1 反例腿、TryTrack 的 r0 健康对照〕、fail-fast 与数据错误二分、LRU 可观测面）；4.5 双探针实证改实现即红。

---

## 2. 发现的问题清单

**阻断级：无。**

建议级（已逐条转登 traceability/findings.json，编号顺延自 F-146）：

| 编号 | 严重度 | 摘要 |
| --- | --- | --- |
| F-147 | suggestion | F-122/F-133/F-134/F-140/F-143 家族第六例：卡头『日期』行停留 2026-09-09 未随 v0.11 推进至 2026-09-16；F-143 建议三项（日期行/家族注明/v0.10 行时序位）均未执行未注明；部分改善＝v0.11 新行追加于表尾正确。『顺手更正』模式已连续六跳失效，建议所有者显式指定收口载体＋validate-docs 机检 |
| F-148 | suggestion | 幻影清单家族第 16 例（F-141 无缓解形态再现）：93489c02 正文声称入库约 7 件留痕，ls-tree 实证仅 5 件（.log 被 .gitignore:87 吞）；ird-test-report.json builds[] 引用库外 .log 无 localOnlyNote。缓解＝四份 XML 本体全部在库；结论性数据复现全真 |
| F-149 | suggestion | F-028/F-035/F-144 家族再现（实现类第二次）：ird_gates 未实跑且 gates 字段理由『WP-01-T01 落地前口径』失实；验收者代行实跑＝9 命中＝基线、零新增——若实跑即得干净自查结论，缺位无后果但措辞与事实不符 |
| F-150 | suggestion | 计数失准家族（F-025/F-041/F-073 同型，少报）：提交 b2fa1c5e 与 v0.11 行称 IProjectQueryPort『十一方法』，实测 12 个纯虚方法（§5.2 原文同 12）——三方实现一致，纯散文计数瑕疵 |
| F-151 | suggestion | 提交标题家族再现（F-012 系两例）：3c0b8bf6（[units] 车道文件名词）＋93489c02（『PRJ-Txx 验证留痕』逐字模板第八例、[governance] 车道首例）；实现/测试两提交动词开头合规。正文四段齐备、结论复现全真，既往判例不阻断 |

## 3. 验收者环境说明

- 复现现场：`git worktree add --detach D:/10_Source_Repos/21_robot/acc_prjt09_a 93489c02687e80eb97ba8e8d3267f72f530aff0e`；冒烟独立目录 `D:/10_Source_Repos/21_robot/acc_prjt09_smoke`；记录回传：`git worktree add -b acc/PRJ-T09/1 D:/10_Source_Repos/21_robot/acc_prjt09_b origin/redesign-main`（attempt=1，验收请求下发序号）。
- worktree 集成配置按 F-062/F-007/F-098/F-099 既有口径补齐四件前置（vcpkg toolchain 绝对路径、Qt 前缀、CMAKE_CXX_FLAGS 三宏、两件未跟踪 ini 模板自主树复制）——属已登记环境缺口，不影响送验对象评价。
- 任务目标面（sdurws_ird_project＋_test＋_contract_test 及依赖）全新树从零构建零错误、双模式测试 183/183＋10/10 全绿、ird_gates 9 命中＝PRJ-T08 基线零新增——DoD 第 1 条按 F-036/F-038 确立的"任务目标面全新树从零"口径判定满足。
- ird_gates 日志中文呈 GBK 乱码（F-065 既有登记），命中项经形态辨识逐条核读，不影响判定。
- 本记录与 F-147～F-151 转登构成 evidence 提交（同支 acc/PRJ-T09/1），推送后由编排者写入 state.acceptanceRecord；验收者未合入、未修改实施产物、未向任务分支推送任何提交（送验对象冻结于 93489c02；探针改动均在临时目录并已还原，任务分支与 worktree A 的 RobWork/ 子树 git status 干净）。
