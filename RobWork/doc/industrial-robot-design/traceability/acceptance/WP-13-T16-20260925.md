# WP-13-T16 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | WP-13-T16 建模契约测试套件与黄金模型数据集落位（modeling；masterWp WP-E） |
| 送验对象（冻结） | `08a4a00133a0870f2eaddc6ca4a77ef99bdca217`（origin/wp13-t16 实测同 SHA——分支无漂移） |
| 比对基线 | `a4c8c4166b49261aa71c2ada661a91bf5d8512e1`（＝origin/redesign-main 送验时尖端） |
| **verdict** | **pass**（4.1~4.11 全部通过，无阻断级问题；建议级 1 条已转登 F-357） |
| 验收者环境 | 全新上下文独立会话；仓库根 `D:\10_Source_Repos\21_robot\RobWork`；worktree A＝`_acc_wp13t16_A`（detached @ 08a4a001）；集成构建树＝`_acc_wp13t16_build`（全新配置）；冒烟构建树＝`_acc_wp13t16_smoke`（全新配置）；base 临时 worktree＝`_acc_wp13t16_base`（@ a4c8c416，比对后已删） |

## 0. 环境说明（非实施产物改动登记）

1. worktree 缺 gitignored 的 `RobWork/RobWorkStudio/bin/RobWorkStudio.ini.template.static`＋`RobWorkStudio.ini.shared.in`（`.gitignore` `[Bb]in/` 规则；T15 验收同款现象）——从主检出复制两文件入 worktree A 后 configure 通过（worktree 内源码零改动，变异窗口外 `git status` 始终干净）。
2. 构建前 tasklist 核查无 RobWorkStudio.exe 残留进程（本次未触发 DLL 占用）。
3. 验收者首次经 ctest 后台启动测试时因调用 shell 未预置框架 DLL 的 PATH 出现加载滞留（已 taskkill 并改用预置 PATH 后 2/2 通过）——与 F-007/F-009 登记的"全新树环境前置未载明"同域，属验收环境操作面，非送验对象缺陷；两测试 exe 直接运行（同 PATH 口径）与本验收全程结果不受影响。
4. ird_gates 引擎直跑未设 `IRD_SELFTEST_DIR`（走缺省 TEMP——F-347 防呆教训口径），未触碰源树。

## 4.1 改动范围 —— 通过

- diff 范围＝`git diff --name-status a4c8c416..08a4a001`：49 文件（26 新增测试/数据＋CMakeLists＋18 留痕＋findings.json＋units/modeling.md）。逐文件对照契约 `allowedFiles` 七条 glob：**全部命中**（取补集过滤 grep 退出码 1＝零文件越界）；`forbiddenFiles`（REQUIREMENTS.md/ARCHITECTURE.md）**零触碰**；diff 内无 `patches/` 文件（SA-02 无登记需求＝无框架源码改动，双证一致）。

## 4.2 可复现构建 —— 通过

| 模式 | 命令要点 | 结果 |
| --- | --- | --- |
| 集成重配 | `cmake -S <A>/RobWork -B _acc_wp13t16_build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=<仓库>/vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64` | Configuring done（45.3s），exit 0 |
| 缓存确认 | grep CMakeCache | `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON` |
| 集成构建 | `cmake --build … --config Release --target sdurws_ird_modeling_test sdurws_ird_modeling_contract_test sdurws_ird_modeling` | **exit 0，零 error**（编译期 error 计数 grep＝0） |
| 冒烟 | `cmake -S <A>/…/industrialrobot -B _acc_wp13t16_smoke -DCMAKE_TOOLCHAIN_FILE=<vcpkg toolchain> -DCMAKE_PREFIX_PATH=<Qt6>`（Release——避开 Debug/跨 CRT 混载） | configure＋build **均 exit 0 零 error** |

## 4.3 契约 verify 命令逐条执行 —— 通过（7/7 亲手执行）

| # | 契约 verify 原文（build→本验收全新树 `_acc_wp13t16_build` 等价映射） | 结果 |
| --- | --- | --- |
| 1 | `cmake --build … --config Release --target sdurws_ird_modeling_test` | exit 0 |
| 2 | `… --target sdurws_ird_modeling_contract_test` | exit 0 |
| 3 | `ctest --test-dir …/modeling -C Release -L "^ird$"` | **2/2 passed**（0.42s；预置框架 DLL PATH 口径见 §0.3） |
| 4 | `… --target sdurws_ird_modeling` | exit 0 |
| 5 | `… --target ird_gates` | **exit 1＝58 处存量命中既有状态**（DTB §4.5 存量例外未清零——与本送验自报一致；权威比对见下） |
| 6 | `pwsh -File RobWork/scripts/industrialrobot/validate-task.ps1 -TaskFile …/WP-13-T16.json` | **PASS（1 tasks）** |
| 7 | `pwsh -File RobWork/scripts/industrialrobot/validate-docs.ps1` | **PASS（20 units, 12 trace entries, 189 task files）** |

**ird_gates 引擎直跑归一化比对（DTB §4.5 口径，双亲手）**：head 侧（worktree A）与 base 侧（临时 worktree @a4c8c416）各执行 `cmake -DIRD_ROOT=<各自 industrialrobot 根> -P <各自>/cmake/ird_gates.cmake`——裁决段精确命中行 **58＝58**，前缀归一化＋sort 后 **diff 为空（多重集完全一致）＝零新增命中**。modeling 面存量 5 条（pugixml 链接/R1 插件自边×1/SUB testkit 直链×2/表外 modeling->ui）均为 T15 及更早已在册状态，与本任务 diff 无关（diff 仅对既有测试目标增列源文件，未新增链接边）。

## 4.4 acceptance 数组逐项证据 —— 通过

| # | acceptance 条目 | 证据（全部为验收者亲手执行/核验） |
| --- | --- | --- |
| 1 | 契约测试套件落位、四组矩阵行组织、用例名绑定需求/AT、留痕、GUI 行不冒充 | 新增 22 条具名用例实测注册且全绿：集成 `sdurws_ird_modeling_test` **265/265**（246＋19：MdlGoldenDatasets 7＋MdlGoldenTemplate 3＋MdlGoldenImport 5＋MdlGoldenDhEquivalence 4，用例名逐条嵌 `WP13T16_ACCx`＋V/AT 号）、`_contract_test` **17/17**（14＋3：MdlGoldenPeerContract）；冒烟 **215/215＋8/8**（gated 50 条不编入）。留痕三方一致：送验 gtest XML（265/0、17/0 failures）＝送验 ird-test-report（modeling 265、contract 17、merged）＝本验收全新树复跑（265/17＋冒烟 215/8）。GUI V-30 仅登记（modeling.md §10.1"本次不启动"）——送验留痕无任何 GUI 运行伪迹 |
| 2 | 四数据集＋容差档案落位并经 testkit 装载校验 | 四套 `testdata/golden/<id>/1.0.0/` 各含 manifest.json（`ird-golden-manifest/1` 字段表齐：kind/units/frames/referenceSource〔mdl-dh-equivalence＝analytic-case 且 independentOfProductionImpl=true〕/producer/edgeCases 三布尔全 true/integrity/generator committed/history）＋inputs/expected/generate 同置；`MdlGoldenDatasets` 7 例装载全链绿。manifest 位置＝版本目录内（与既有 ev-slice-fixture 等 6 数据集同布局——存量实现事实，已按 DTB §5.4 在 modeling.md §15 v0.18 ②登记，非本任务私设）。**验收者独立核验**：四套 manifest 全部 integrity 条目 SHA-256＋sizeBytes 与磁盘实测逐一匹配（17 文件，0 mismatch）；manifest 覆盖 inputs+expected+generate 无遗漏（F-017 所指装载器不验 generate 段为存量 open——本验收以手工全量核验补位，不构成 T16 违规）。容差档案 `tolerance/mdl-dh/v1.0.0.json`：`ird-tolerance-profile/1`，锚定附录 D 第 4/5/6/7 项，全部条目 `source=appendixD-fixed` 且 tolerance==allowedMax（零放宽） |
| 3 | V-11/V-06/V-02/V-01 黄金全链用例通过 | V-11：GoldenExpansionClosedForm（七样本逐关节逐项走档案 dh[*] 条目 1e-9）＋GoldenRoundtripParameters（dh-solve[*] 参数级＋字典序自由坐标）＋GoldenFkEquivalence（真实 runtime 编译链探针，fk.* 档案上界 1e-9）＋GoldenNotExpressibleFiveStates（NotExpressible×3/Approximate/AnalysisFailed，连同 Exact/ExactNonUnique 五状态全覆盖）全绿；V-06：GoldenUrdfReportCounts（八清单逐项计数＋草稿值面）＋GoldenUrdfDraftValues 全绿；V-02：SevenAxisRegisteredNotEnabled（enabled=false＋TemplateDisabled＋无静默替换）绿；V-01：GoldenDraftMatchesDatasetExpectations（T-MDL-1 表值逐格＋7 连杆 6 关节＋J6 行程恰 4π＋钢 7850 kg/m³）＋ProjectPeerGoldenTemplateApplyPlan（命令通道 prepare 计划面收口）绿 |
| 4 | C4 边角样例＋确定性 | zero-anchor/near-zero/sign-cancel 三样本在 manifest edgeCases.sampleRefs 且被 GoldenExpansionClosedForm 同回路消费（测试断言逐项）；档案比较经 core closeWithin（IRD_EXPECT_CLOSE/AT_MOST 承载）。确定性：**验收者亲手重放四个 generate 脚本（node），全部 expected 产物 byte-identical**；全量回归（T03~T14 面含于 265 条）双模式全绿 |
| 5 | 跨单元契约测试面 | io 对端：IoPeerImportPipelineMissingLeafContract（IO-RES-MISSING 透传＋缺失叶 missing/digestHex 空＋草稿零摘要 Recorded）绿；project 对端：ProjectPeerGoldenTemplateApplyPlan（Planned＋恰 1 robot-design 写＋双编译声明＋prepare 零编译调用＋写入字节解码回黄金值）绿；AT-37：At37BasePlacementParameterInputFace（preset/参数无矩阵＋默认地面入补全清单＋场景/工具引用空）绿；runtime 对端归 T12 CompileBridge/DescriptionBridge 在册（本 TU 注明不重复）；AT-31/V-09：GoldenXacroMacroMapping＋GoldenXacroCyclePassthrough（两模式件承载）绿。联合观测行仅断言 modeling 侧输入/输出面 |

## 4.5 测试真实失败能力（变异测试） —— 通过（3 处变异，2 处实质变红，1 处为覆盖率情报）

变异窗口在冒烟构建完成**之后**开窗（T14 时序教训）；worktree A 变异前 `git status` 干净，全程 `git restore` 还原。

| 变异 | 内容 | 实测结果 |
| --- | --- | --- |
| M1 | `modeling/src/Template.cpp` T-MDL-1 第 2 行 qmax `kPiHalf→kPi`（黄金模板值篡改） | `MdlGoldenTemplate.Tmdl1GoldenParameterLock_WP13T16_ACC3` **真实变红**（1 failed）——黄金参数锁定有齿 |
| M2（首次） | `DhConvert.cpp` canonicalForm 翻转支内 `a` 分量 `−a→−a+1e-6` | 4 条黄金 DH 用例**全绿**——翻转支未被黄金数据集命中（与 F-356 登记的"数据集侧调整避开该支"一致）——覆盖率情报，转登 **F-357** |
| M2b | 同函数改为 `a` 分量无条件 `+1e-6`（m，超出档案 1e-9 上界三个量级） | `GoldenRoundtripParameters_V11`＋`GoldenFkEquivalence_V11` **双双真实变红**（2 failed；展开/五状态用例不受该函数影响、保持绿——失败面精确） |

还原后全量复跑：**265/265＋17/17 全绿**，`git status` 干净（源树零残留）。

## 4.6 架构红线 —— 通过（八条逐条）

1. L2 零 Qt：五个新增 `.cpp` grep Qt 头**零命中**；CMake 仅对既有测试目标增列源文件，目标链接面不变。
2. R-1 业务域互链：无新增链接边（ird_gates base..head 多重集一致为硬证）。
3. R-2 跨单元私有头：新增文件仅 include 各单元 `include/sdurws/ird/<unit>/` 公共头＋同单元 `../test/CommandFixtures.hpp`（单元内相对头，合规）；CommandFixtures 自身亦公共头集（预存文件抽核）。
4. 框架零源码修改：4.1 已证（无 patches/ 登记＋diff 无框架文件）。
5. R-4 名称前缀拼接：diff 零触碰名称解析面。
6. PA-1 权威唯一：黄金比较容差唯一来源＝档案 mdl-dh（测试注释明示"不私设字面阈值"）；SPD 判定复用 I-MDL-5 单一实现（checkInvariants）、摘要复用 core ContentDigester；测试域局部 jointTypeFromToken 映射带"非产品词表第二实现点"注记。
7. PA-2 不可变历史：本任务纯测试/数据资产，无修订写路径触碰。
8. 不恢复 old/ 实现：diff 无 old/ 引用。

## 4.7 注释规范 —— 通过

新增 5 个 `.cpp` 全查：文件头 `@file/@brief`＋设计依据（契约 acceptance 号/卡章节/需求 ID）＋两模式可编译性说明齐备；每个类/函数中文 Doxygen（参数方向/单位 rad·m·kg·m²/坐标系〔T_parent_joint、连杆系、世界系〕/错误语义〔数据集非法级 vs 环境错误〕/线程约束〔getenv 只读、单线程用例〕/确定性来源）；每条 TEST 以 `IRD_TEST_INFO` 绑定需求 ID＋AT 编号（§2.7 测试代码要求）；数据文件 manifest/档案中文描述随既有格式。公共头无新增（本任务零产品接口变更）。

## 4.8 文档同步＋findings 相关性 —— 通过

- units/modeling.md 升 **v0.18**：版本头＋§11 T16 行"已落位"注记（含四套 vs DTB"三套"计数偏差的消账指向）＋§15 v0.18 六项登记（含 manifest 布局偏差、V-06 期望面三处随实现对账修正、V-11 离散等价支行为事实、F-355 处置）——偏差全部按 DTB §5.4 在案，零"代码先行、文档失实"。
- findings.json：实施侧新增 **F-355**（DhConvert/XacroExpand 的 ExpandOutcome 同名冲突——验收者实证：两公共头同命名空间各一 struct，L208/L274）与 **F-356**（canonicalForm 翻转恒等式不保位置），均 open 建议级、归属 T06/T09 所有者，处置（TU 不含 XacroExpand.hpp、数据集侧避让）留痕在案。
- **未关闭条目相关性核对（v1.2）**：F-344/345/350/351/352（verify 数组缺 contract_test 构建行家族）——T16 verify 含两构建行，**零复发**；F-346/348（缺 gtest XML 原件）——T16 XML 双件在案，零复发；F-342/343/349/353/354（计数失准家族）——送验计数经 XML/report/验收者实测三方一致核验，零复发；F-015/016/017/018（TK-T03 装载器存量）——T16 数据集不受 F-015 影响（四 manifest 均含 parameters）、lint 路径不经 F-016 路径、F-017 generate 段由本验收手工核验补位，均保持 open 不静默积压、可消账项已注明；F-336/F-347（门禁取证口径）——本验收按引擎直跑＋缺省 TEMP 夹具目录执行，未复发。

## 4.9 commit 质量 —— 通过

4 提交（15437a8d 数据集→b41315ce 测试套件→8476f7ed 对端契约→08a4a001 文档/留痕）：标题均 `[WP-13-T16] 动词开头`；正文四段（What/Why/验证/影响面）逐份齐备且带 `Acc-Covered` 映射；验证段宣称（265/17、215/8、58=58、双 PASS）与本验收实测逐一相符，无粉饰；b41315ce 如实披露采纳前任中断 WIP 测试并修复两处——透明合规。

## 4.10 留痕完整 —— 通过

`traceability/builds/wp13-t16/`：gtest XML 原件双件（265/0＋17/0）＋ird-test-report 三件（modeling/contract/merged，计数三方一致）＋集成/冒烟构建与运行日志四件＋ird_gates 比对附件五件＋build-logs.txt 索引。本验收另产生独立证据（全新树 XML/日志、门禁双侧 raw/归一化清单）随验收会话登记。`phase-one-validation.log` 为 WP-01 时代历史文件（非 T16 留痕通道）；当前阶段留痕口径＝builds/wp13-t16/，与 T13~T15 先例一致。

## 4.11 偷懒/缩水实现扫描 —— 通过

1. diff 内 `TODO|FIXME|stub|placeholder|未实现|not implemented` 扫描**零命中**（无契约豁免条款需求）。
2. 空实现检测：本任务零产品接口变更；新增断言均校验具体值/计数/边界，无恒真断言（抽核：计数断言带 `ASSERT_EQ`＋逐项内容对账；`EXPECT_TRUE(checkInvariants(...).empty())` 有 4.5 变异背书）。
3. acceptance 逐条证据表见 4.4——22 条具名用例＋四数据集＋容差档案逐项有具名测试/命令输出。
4. 测试强度：4.5 两处实质变异实测变红；失败面与被篡改语义精确对应（黄金参数锁定/roundtrip 参数级/FK 等价）。

## 问题清单

| 级别 | 编号 | 内容 | 处置 |
| --- | --- | --- | --- |
| 阻断 | 无 | — | — |
| 建议 | **F-357**（随本记录转登 findings.json） | mdl-dh-equivalence 黄金数据集未覆盖 canonicalForm 翻转支（负 d 支）：验收者对该支内 `a` 分量偏置变异（M2）实测零红（翻转支死区），V-11 对该支语义无黄金钉定——与 F-356（翻转恒等式不保位置，归 T09 所有者裁决）同域；建议 F-356 裁决后补一例负 d 黄金样本，使 V-11 对分支选取语义具备变异敏感度 | testkit/modeling 数据集所有者（F-356 裁决后补样） |

## 裁决

**verdict＝pass**：4.1~4.11 全部通过，无阻断级问题；建议级 F-357 已转登，不阻塞合入。验收对象＝SHA `08a4a00133a0870f2eaddc6ca4a77ef99bdca217`（非分支引用）。合入决策归所有者（PIPE §4.7）。

验收记录：traceability/acceptance/WP-13-T16-20260925.md（本文件）；evidence 分支 `acc/WP-13-T16/1`（attempt 1，2026-09-25）。
