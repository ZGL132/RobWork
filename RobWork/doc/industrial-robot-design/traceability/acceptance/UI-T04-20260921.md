# 验收记录 —— UI-T04（七态公共状态呈现：StatusWordProjection＋九态短标签映射）· attempt 1

| 字段 | 值 |
| --- | --- |
| 任务 | UI-T04（contract：tasks/foundation/UI-T04.json，schema ird-task/1，status=ready；interUnit=true——映射触发数据源消费 core 九态/工程状态词表＋经 ui 自有当前性端口 C-7 值投影；requirements UX-06/UX-10/PM-11；knownPitfalls O-31/P-UI-1/P-UI-2/P-EV-4 均按契约处置条核对） |
| 送验对象 | **0516f7fbb922e9e90c23f6dcfbcabda7625725d0**（40 位冻结；分支 wp10-t04；`git fetch` 后 `git rev-parse origin/wp10-t04`＝该 SHA 逐字一致——无分支漂移，ACC §3 冻结判据通过） |
| diff 范围 | b25c30c83b4f5f9dc497f3dd5d5ae7b7a9af33b9..0516f7fbb922e9e90c23f6dcfbcabda7625725d0（＝state.base..headSha；base 与 `origin/redesign-main` 逐字一致；range 内 2 提交：aaea7842 实现＋测试、0516f7fb 单元卡 v0.6＋验证留痕归档） |
| 验收尝试 | attempt 1（state.attempts.accept 序号 1；本验收为全新上下文，只消费产物、契约、设计文档与在库留痕，未读取实施者过程叙述；全部构建/测试/门禁/治理脚本/变异试验由验收者亲手执行） |
| 验收环境 | 复现 worktree A＝`git worktree add --detach build-smoke-wp10-t04-acc-wt-a` @ 0516f7fb（集成 build 树全新配置：`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=<主仓>vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64 -DCMAKE_CXX_FLAGS="-DNOMINMAX -DWIN32_LEAN_AND_MEAN -DWIN32"`——配置 exit 0，`grep RWS_BUILD_INDUSTRIALROBOT build/CMakeCache.txt` → `:BOOL=ON` 实证；首配前按 F-099 既有登记自主树复制未追踪 `RobWorkStudio.ini.template.static`＋`RobWorkStudio.ini.shared.in` 两件——bin/ 整目录被 `.gitignore` 吞没的框架 configure 硬依赖，F-098/F-099 环境链在本次验收再次亲历、非送验对象缺陷）；冒烟 build 树＝独立目录 acc-ui-t04-smoke（vcpkg toolchain＋Qt 前缀——AGENTS §4.1）；GUI 测试执行＝`QT_QPA_PLATFORM=windows`＋`QT_PLUGIN_PATH=<Qt>/plugins`（wp10-t03-gate-registrations §2 既有登记口径）；门禁对照＝在库 wp10-t03 基线留痕（traceability/builds/wp10-t03/gates-branch-scriptmode.log，其基线即本任务 base 树）；记录回传 worktree B＝`git worktree add -b acc/UI-T04/1` @ origin/redesign-main |
| verdict | **pass**（4.1～4.11 全部通过、无阻断级问题；一条新发现为建议级，已随本 evidence 分支转登 findings.json **F-263**，不阻塞合入） |

## 4.1 改动范围 —— 通过

- `git diff --name-status base..head` 实证 **22 个变更文件**，逐个对照契约 allowedFiles 三模式：`ui/**` 5 件（CMakeLists.txt M、include 两公共头 M、src/StatusProjection.cpp A、test/StatusWordModelTest.cpp A）＋`units/ui.md`（M，v0.5→v0.6 增量修订）＋`traceability/**` 16 件（builds/wp10-t04/ 16 件 A）——**全部在 allowedFiles 内，零越界**。
- forbiddenFiles（REQUIREMENTS.md/ARCHITECTURE.md）零触碰（22 文件列表实证，两文件均不在 diff 内；本验收为对照需求原文以 `git show <sha>:<path>` 只读提取，未改动）。
- 框架源码（RobWork/、RobWorkSim/、RobWorkStudio/ 其余源码）零改动；`patches/` 范围内零变更（SA-02 无登记需求——22 文件列表实证）。

## 4.2 可复现构建 —— 通过（集成全新树重配＋全量构建＋独立冒烟；`: error` 计数 0）

| 步骤 | 结果 |
| --- | --- |
| 集成重配 | worktree A 全新配置 exit 0（补齐 vcpkg toolchain＋Qt 前缀＋CXX flags＋两件未追踪 ini 辅助件后通过；前四者为 F-098/F-099 既有登记口径——AGENTS §4.1 字面命令在全新树不可配，验收者亲历同款；ini 缺件＝F-099 环境链，非送验对象缺陷）；缓存 grep `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON` 实证 |
| 集成全量构建 | `cmake --build build --config Release` **exit 0、`: error`/`: fatal error` 计数 0**（RobWork 全库→RobWorkSim→RobWorkStudio→industrialrobot 全单元全新编译；日志 acc-integrated-build.log；产物 `build/RobWorkStudio/bin/Release/sdurws_ird_ui{,_test,_contract_test,_gui_test}` 全部在位，sdurws_ird_ui_test 链接清单含 StatusWordModelTest.cpp） |
| 独立冒烟模式 | `cmake -S …industrialrobot -B acc-ui-t04-smoke -DCMAKE_TOOLCHAIN_FILE=<vcpkg> -DCMAKE_PREFIX_PATH=<Qt>` 配置 exit 0；`cmake --build --config Release` **exit 0、错误计数 0**；`ui/Release/` 下 `sdurws_ird_ui.lib`＋三测试 exe＋运行期 DLL（Qt6Core/Gui/Widgets、gtest.dll）全部就位（与在库 smoke-configure.log/smoke-build.log 产物面同值） |

## 4.3 契约 verify 命令 —— 逐条亲手执行；全部 exit 0

在 worktree A 根目录按契约 verify 数组字面逐条独立执行（本契约 verify 仅 1 条）：

| 命令 | 结果 |
| --- | --- |
| `cmake --build build --config Release --target sdurws_ird_ui_test` | exit 0 |

附加测试真实执行（验收者亲手，非采信送验留痕）：三测试可执行文件直接运行 **16/16＋3/3＋6/6 全部 PASSED**（gtest XML＋console 留痕于验收临时目录；gui_test 按 §12.2 以 windows 平台运行）；冒烟树 ui_test 另跑 **16/16 PASSED**；`ctest -C Release`（ui 构建子目录）**3/3 通过（ird×2＋ird_gui×1）**——与在库 ctest.log 及三份 gtest XML（tests=16/3/6、failures=0）计数逐值一致。治理脚本亲跑：`validate-task.ps1 -TaskFile <契约绝对路径>` → **PASS (1 tasks)**；`validate-docs.ps1`（worktree A 树内）→ **PASS (20 units, 12 trace entries, 150 task files)**——与在库 validate-task.log 及提交正文声称逐字一致。

## 4.4 acceptance 条目 —— 3/3 逐条证据（未执行项零存在）

| # | 契约 acceptance 要点 | 证据（验收者亲手） |
| --- | --- | --- |
| 1 | UI-STG-3：七态×权威词表映射表（§6.3 表，求值优先级 empty-project＞computing＞incomplete＞failed＞data-insufficient＞results-stale＞computable）逐行断言；不新增状态词（R-2）；NotEvaluable 呈现归 results-stale＋「无法判定」原因（P-UI-2） | `SevenStateMappingTableRows_UISTG3_UI_T04_ACC1` 七行逐行亲跑通过（行 1 空项目短路且零伴随数据／行 2 incomplete／行 3 非终态五值 Queued/Preparing/Running/Paused/Canceling 逐值 computing／行 4 computable 含全缺省形态／行 5 Superseded×三判定值／行 6 DataInsufficient／行 7 Failed＋NotApplicable 表 3 约束）；`SevenStatePriorityChain_UISTG3_UI_T04_ACC1` 六相邻对全过＋Error 诊断触发面＋dominantStatusWord 聚合三例＋空输入 nullopt；`FrozenVocabularyNoNewWords_UI_T04_ACC1` token 全集与 §6.3 冻结七词一一对应（集合相等＋逐行相等双断言，映射表亲读一致）；`NotEvaluablePresentedAsStale_UI_T04_ACC1` 两成因均归 results-stale＋成因位/文案键（state.currentness.unevaluable.*）就位＋两持久态下成因位必空 presence 纪律。实现面：statusWordPriority 显式映射（StatusProjection.cpp:166-181）与 §6.3 冻结序逐项一致；词表/优先级未改一字（P-UI-1——ui.md §6.3 diff 纯增量实证） |
| 2 | 显示纪律（§6.3/§6.8）：FormalPassEligibility 五条件唯一放行、不满足禁止渲染「正式通过」；计算中附进度阶段与取消、过期附原因（InvalidationReason 清单）、失败附定位；九态短标签键 state.<token>.label 就位（PM-03/PM-11） | `FormalPassDisplayGate_UI_T04_ACC2` 亲跑通过：五条件全满足唯一放行＋available=false 禁止＋五 unmet token（mode-not-verified/outcome-not-completed/coverage-incomplete/evidence-incomplete/status-not-feasible——evidence §7.2 五条件原文对读一致）逐 token 禁止＋computable 态下放行位仍只看 formalPass 字段（formalPassRenderable 唯一实现点 UiProjections.hpp:617-622，`available && eligible` 亲读）；`DisplayDisciplineCompanions_UI_T04_ACC2` 四伴随面亲跑通过（computing 附 progressStageKey＋cancelAvailable／stale 附两条 InvalidationReason 逐字段／failed 附 DiagProjectionItem code/subject/actionKind 定位与处置入口／data-insufficient 附 Missing/Invalid/Unverified 全量三条原序、Satisfied/NotApplicable 不列）；`NineStateLabelKeys_PM03_UI_T04_ACC2` 九态键全表亲跑通过（键＝state.<core token>.label 与 core.md §4.7 冻结 token 逐字对断＋§6.3 中文短标签九值逐字一致＋failed 双词表键同形不冲突；core::toToken 自身由 core EvaluationTest 锁定同一冻结词表——两侧冻结面一致） |
| 3 | O-31 处置（2026-09-19 裁决随整链放行）：映射触发数据源/显示纪律所涉 evidence 三类型按 ui 自有当前性端口＋值投影承载（L5 适配），产品面零对端链接/include（NoCrossUnitInclude_O31_UI_BUILD 守卫常驻）；P-UI-1 冻结稿实现不改词表；P-UI-2/P-EV-4 按 §6.3 建议口径 | 验收者独立三重复核：①源码面——`grep -rn "sdurws/ird/" src include` 于 ui 产品面对 project/evidence/execution/policy/runtime/modeling 等业务域 **零命中**（全部 ird include 面＝core Evaluation/Events/Identity＋diagnostics Catalog/DiagCodes/Redaction＋ui 自有头，均为公共头）；链接面——CMakeLists.txt:109-114 亲读，`sdurws_ird_ui` 仅链 core＋diagnostics＋Qt6 三件（R-3 例外），零对端链接；②机器面——`sdurws_ird_ui_contract_test` 3/3 亲跑通过（含 `NoCrossUnitInclude_O31_UI_BUILD`，其 collectProductFaceFiles 动态枚举 src/include——新增 StatusProjection.cpp 自动入扫）；③门禁面——ird_gates 亲手复跑（集成树 `--target ird_gates`），扫描器自报命中 32 项、逐码分布与 wp10-t03 基线（在库 gates-branch-scriptmode.log：命中 32、SUB39/LIB8/T1 5/R3 13/R4 3/R1 1/R5 1/T2 1）**逐码零变化**，新增两文件在命中集中零出现。值投影语义锚点抽查：§7.2 五条件/§8.1 两持久态＋NotEvaluable 计算形态与投影字段一一对应；P-UI-1/P-UI-2/P-EV-4 处置在 §6.3 冻结稿原文内实现（无私定口径） |

## 4.5 测试真实失败能力 —— 通过（两处断言注入应失败输入，双层真实变红；试验后已还原并复绿）

在验收沙箱 worktree A（git 管控下，试验后 `git status --porcelain` 对已跟踪文件零输出＝与送验对象逐字节一致）注入两处变异：

| # | 变异 | 重建后亲跑结果 |
| --- | --- | --- |
| 1 | `StatusProjection.cpp` evaluateStatusWord 中 incomplete 短路 `if (!facts.readiness.valid)` 改 `if (false && …)`（优先级 2 失效） | `sdurws_ird_ui_test` → **恰 2 用例 FAILED**：`SevenStateMappingTableRows_UISTG3_UI_T04_ACC1`（失败点 StatusWordModelTest.cpp:159——行 2 断言得 Computable 期望 Incomplete）＋`SevenStatePriorityChain_UISTG3_UI_T04_ACC1`（失败点 :266——incomplete＞failed 对得 Failed 期望 Incomplete）；其余 14 用例保持 PASSED（定位精确，映射表行 2 与优先级链两处断言真实承重） |
| 2 | `UiProjections.hpp:621` formalPassRenderable `return p.available && p.eligible` 改 `return p.available`（显示纪律放行门弱化——五条件失效） | `sdurws_ird_ui_test` → **恰 2 用例 FAILED**：`FormalPassDisplayGate_UI_T04_ACC2`（五 unmet token 逐条禁止断言红）＋`CurrentnessPortValueCarriers_O31_UI_T04_ACC3`（端口回传资格 eligible=false 仍放行断言红）——显示纪律断言非恒真 |

两处变异还原后重建复跑 **16/16 全绿**，沙箱与送验对象零差异。

## 4.6 架构红线（AGENTS §5 八条）—— 逐条通过

1. **L2 计算内核零 Qt**：本任务仅改 ui 单元（R-3 全产品唯一 Qt Widgets 例外单元，DTB §4.5 预登记）；core/evidence/policy/runtime 等零触碰（22 文件清单实证）；新增实现 `StatusProjection.cpp` 零 Qt 头（仅 UiProjections.hpp＋\<array\>/\<utility\>——亲读），ird_gates 复跑 R3 项与基线逐码零变化（13 处均为既有：ui 链接 3＋产品面 Qt 类头 3 文件＋自测夹具，零新增）。
2. **业务域互链禁止（R-1）**：ui 产品面对 modeling/requirements/kinematics/trajectory/dynamics/selection/optimization/workflow/project/evidence/execution/policy/runtime/io/reporting 的 include **零命中**（grep 实证）；跨单元协作仅 core/diagnostics 两条 ARCH §3.5 登记边＋Qt（链接行＝CMakeLists.txt:109-114 亲读）；evidence 三类型按 O-31 裁决经 ui 自有值投影＋IUiCurrentnessSource 端口承载（UiPorts.hpp 新增段亲读）。
3. **跨单元私有头禁止（R-2）**：本任务零新增私有头；ui 消费的 core/diagnostics 头均为其 `include/sdurws/ird/<unit>/` 公共头（Evaluation.hpp/Identity.hpp/Catalog.hpp——路径亲读）。
4. **框架零源码修改（SA-02）**：diff 内零框架文件、patches/ 零变更（4.1 实证）。
5. **不做名称前缀拼接/剥离（R-4）**：ui diff 新增行 `RobWork` 字面量 **零命中**（grep 5 处命中全为 diff `+++` 文件头路径行，非内容）＋契约测试 `NoRobWorkPrefixLiteral_R4_UI_BUILD` 亲跑通过＋ird_gates R4 项仅 project 既有 1 处（与基线同值）。
6. **权威唯一（PA-1）**：七态为**呈现投影而非判定**——evaluateStatusWord 纯函数只按 §6.3 冻结优先级选取呈现词，零业务判定（就绪归 evidence/请求方、当前性归 evidence、资格归 evidence、任务控制归 execution——各触发面注释逐条锚定）；ui 不复算五条件（formalPassRenderable 只读放行位）；诊断码值权威仍归 diagnostics（failureDiagnostics 为 DiagProjectionItem 原样承载）；词表唯一所有者＝ui（NFR-MNT-03/O-11——§6.3 冻结 token 即权威）。
7. **不可变历史（PA-2）**：本任务零修订数据触碰（22 文件清单——无 project/ 修订面文件）；求值全程无副作用、不回写权威对象（StatusProjection.cpp 文件头"不持有可变状态"＋纯函数实现亲读）。
8. **不恢复 old/**：diff 新增行 `old/` 引用零命中（grep 实证）。

## 4.7 注释规范（AGENTS §2）—— 通过（公共头 2 件新增节段全查＋新增实现/测试/CMake 全查；本任务新增实现文件仅 1 件故全查覆盖"抽查 ≥3"要求）

- **文件头**：新增 2 件（StatusProjection.cpp/StatusWordModelTest.cpp）均有中文 `@file/@brief`＋设计依据（ui.md §6.3/§6.8/§3.5/§12.3、需求 ID、契约 acceptance 编号逐条标注）＋背景说明（"为什么实现是纯函数＋静态表""为什么映射用例放模型层"）；两公共头新增节段均有节段级注释块（C-7 端口"为什么三个方法而不是一个聚合方法"、七态"为什么状态词是投影而不是判定"）。
- **类/函数 Doxygen**：公共头逐函数 `@brief/@param [in]/@return/@note/@throws` 语义完整（含失败行为与线程约定——IUiCurrentnessSource 三方法均注"UI 线程调用、纯查询不抛、实现应返回快照值"；statusWordToken 注明未知值属调用方违约）；StatusWord/NotEvaluableCause/StatusFacts/StatusWordProjection 等每结构体均有业务概念＋生命周期（值语义）＋所有权注释。
- **§2.5 高危信息表**：线程约束（UI 线程调用约定 §3.4 M-1 逐函数注明、"全部函数纯/只读可重入"文件头声明）；错误语义（status==nullopt＝NotEvaluable 计算形态非错误、非法枚举防御返回空串"便于日志显性暴露"、九态查找 nullptr 注明"不可达——九态全表覆盖"）；确定性来源（表序＝枚举序＝§6.3 行序三序一致＋static_assert 兜底、"全量不短路、保持清单原序 NFR-COR-02"）；所有权（值投影零裸指针；DiagProjectionItem 原样承载"ui 不改写诊断语义"）；物理单位/坐标系/角度制式——本任务为纯呈现单元无此类物理量（记录在此如实说明，非缺失）。
- **魔法数字**：kStatusWordCount=7 注"编译期自检下界；少一行即漏登记"；优先级返回值 0~6 逐项注 §6.3 原文序；99 兜底注"非法枚举值排最低优先——防御性兜底"。
- **反例扫描**：零"复述代码"式注释、零无信息量 TODO、零过期注释（实现与 ui.md v0.6 同批登记）。

## 4.8 文档同步 —— 通过（零偏差＋增量修订已登记＋findings 相关性逐条核对）

- **单元卡增量修订**：`units/ui.md` v0.5→v0.6（文档头版本/日期 2 行替换＋§6.3 表尾落位登记注＋§16.7 v0.6 变更行——diff 实证**纯增量 4 行、语义内容零改动**）；v0.6 行登记的承载形状（StatusWord/StatusFacts/evaluateStatusWord/伴随数据/formalPassRenderable/dominantStatusWord/IUiCurrentnessSource/taskStateLabelKey）与实现逐一亲读比对一致；实施口径登记（progressStageKey 文案键形态、任务轴控制面归 UI-T13）如实划界。实现与任务卡零未登记偏差。
- **追踪同步**：validate-task PASS＋validate-docs PASS 亲跑同值（4.3）；§12.3 UI-STG-3 行（前置"桩投影数据集（含 NotEvaluable）"、观测点"StatusWordProjection 输出"）与交付测试形态一致；ui.md §2 需求表/§13 任务行 WP-10-T04 映射在位。契约 status=ready 维持（合入收尾时翻转——PIPE §4.7 既有口径，非偏差）。
- **findings.json 未关闭条目相关性核对（227 条 open 逐类筛查，与本任务相关者如下，可消账项为零）**：
  - **F-227/F-259**（ird_gates 机器面滞后：R3 例外链接/头包含白名单未回填，owner=WP-01-T03）：本验收门禁复跑的 13 处 R3 命中即该登记描述的既有集合——维持 open 正确（白名单文件不在本任务 allowedFiles，本任务无回填义务）；新增文件零命中，无扩大。
  - **F-261**（提交标题名词短语家族第 11 例）：本任务 0516f7fb 标题同型再现（第 12 例，见 4.9）——家族性复发，随本验收转登 **F-263** 顺延跟踪。
  - **F-260**（UI-T03 两处行为面缺口：折叠/门面违约轨）：属 UI-T03 交付测试集，本任务未触碰 WorkbenchShell 面——与本任务 diff 零交集，维持 open 正确。
  - **F-010/F-098/F-099**（.gitignore 吞 .log／全新树配置口径缺口）：本验收再次亲历后两者（ini 两件＋toolchain/Qt/CXXFLAGS 参数）——与本任务零代码关联，维持 open 正确（本记录环境段再留实证）；前者与本任务无关（17 件留痕中 4 件 .log 经 `git add -f` 在库，清点无缺）。
  - 其余 open 条目（F-004~F-258 族、F-262 已 fixed）与本任务 22 文件面无关联（逐类比对零交集）。

## 4.9 commit 质量 —— 通过（2 提交四段式齐备；1 处标题名词短语＝既有家族新例，转登 F-263）

- **aaea7842** `[WP-10-T04] 实现七态公共状态呈现（StatusWordProjection＋九态短标签映射）`：动词开头合规；四段（What/Why/验证/影响面）齐备；正文声称（集成四目标零错误、冒烟零错误、16/3/6＋ctest 3/3＋冒烟 16/16、门禁与 wp10-t03 基线逐码零变化）与本验收亲跑逐值一致。
- **0516f7fb** `[WP-10-T04] ui 单元卡 v0.6 增量修订与验证留痕归档（文档同步＋证据）`：**前缀后以名词短语『ui 单元卡』开头——不合 ACC §4.9/AGENTS §6.3『动词开头』字面**，F-012/F-232~F-242/F-247/F-258/F-261 家族第 12 例（纯风格瑕疵；正文四段齐备、validate 双 PASS 与 17 件留痕清点经验收者独立复跑/清点证实）——转登 **F-263**（建议级，循 F-261 先例随家族统一跟踪，不阻塞）。

## 4.10 留痕完整 —— 通过（17 件在库逐件清点；§4.10 条文载体循 F-201 登记口径）

- `traceability/builds/wp10-t04/` **17 件全部在库**（`git ls-tree -r` 清点）：gtest XML×3（tests=16/3/6、failures=0——亲读）＋ird-test-report.json×3（summary decisive=true、passed=16/3/6、failed/skipped/notRun=0——亲读）＋console 日志×4（含冒烟模式 ui_test 16/16）＋集成构建日志×1＋冒烟 configure/build×2＋ctest.log（3/3 通过亲读）＋门禁二件（gates-branch-run.log＋gates-hit-set.txt——"命中 32 项、新增文件零命中"结论与本验收复跑一致）＋validate-task.log（PASS）——与两提交正文声称的全部执行面一一对应、无虚标。
- phase-one-validation.log 尾行停早期任务、state.json 无 resultNote 字段——循 **F-201** 既有登记口径，以 `traceability/builds/wp10-t04/` 17 件为 4.10 判定载体。
- 验收者自跑留痕（集成/冒烟配置与构建日志、三测试 gtest XML＋console、冒烟 ui_test、ctest、门禁复跑、变异试验三份运行日志）存于 worktree A 临时目录，关键结论已逐条摘录于本记录；临时目录随 worktree A 生命周期消亡，不影响本记录为证据载体。

## 4.11 偷懒/缩水实现扫描 —— 通过（扫描零实质命中；豁免逐条列据；逐条证据表见 4.4）

- **①标记扫描**：diff 新增行 `TODO|FIXME|XXX|HACK|stub|placeholder|未实现|not implemented` 扫描——TODO/FIXME/XXX/HACK/未实现/not implemented **零命中**；`stub`（大小写不敏感）命中 2 行均为测试替身类名 `StubCurrentnessSource`（StatusWordModelTest.cpp:574/595——**契约 acceptance 3 明文的"ui 测试以可控替身承载（§3.1）"设计本体**，替身实现了端口全部三个方法且被多态调用断言，非未完成实现；豁免依据＝契约原文＋ui.md §3.1，逐条列出如上）；`placeholder` 零命中。
- **②空实现检测**：全部公共函数有真实实现（statusWordToken/LabelKey/TransitionalLabel/priority/dominantStatusWord/taskStateLabelKey/TransitionalLabel/currentnessUnevaluableLabelKey/TransitionalLabel/unsatisfiedEvidenceItems/evaluateStatusWord 均有实体——StatusProjection.cpp 全文亲读；formalPassRenderable 内联实体）；`IUiCurrentnessSource` 为纯虚端口声明＝O-31 裁决的注入面（L5 适配，契约明文"端口实现由 L5 注入"——非空实现）；防御性不可达分支（非法枚举返回空串）有注释与业务理由，非"仅返回默认值"。
- **③逐条证据表**：契约 acceptance 3 条逐条证据见 4.4 表——每条均有具名测试或文件行号，零"已实现"空声明。
- **④测试强度**：断言校验行为与边界（七行映射×多取值／非终态五值逐值／优先级六相邻对／token 冻结集集合相等＋逐行相等双断言／NotEvaluable 两成因＋presence 纪律／五 unmet token 逐条／伴随数据逐字段含原序／九态键全表）；与 4.5 联动抽改实现两处均真实变红且失败点恰为承重断言（非恒真、非只编译通过）。

## 发现的问题清单（建议级 1 条，无阻断级）

| 编号 | 级别 | 摘要 | 处置 |
| --- | --- | --- | --- |
| F-263 | suggestion | F-012/F-232~F-242/F-247/F-258/F-261 家族第 12 例：提交 0516f7fb 标题前缀后以名词短语『ui 单元卡』开头，不合『动词开头』字面；正文四段齐备、验证结论经验收者独立复跑证实，纯风格瑕疵 | 已随本 evidence 分支转登 findings.json（随 F-247 家族统一跟踪，不单独返工） |

## 裁决

**verdict＝pass**：4.1～4.11 全部通过、无阻断级问题；F-263 为建议级、已按 ACC §5 强制转登本 evidence 分支 `traceability/findings.json`，不阻塞合入。验收通过对象＝**0516f7fbb922e9e90c23f6dcfbcabda7625725d0**（非分支引用）；后续收尾按 PIPE §4.7 由所有者执行。
