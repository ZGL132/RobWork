# EV-T09 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | EV-T09 实现缓存/检查点兼容判定（Compatibility）——契约 tasks/foundation/EV-T09.json |
| 送验对象（冻结） | f9eaec775e794862082861d1bc86c66d9584c5ea（分支 wp05-t09 尖端；验收全程按该 SHA） |
| diff 范围 | 50a69a269a56d3656b6699d339d51e92ed02a92c..f9eaec775e794862082861d1bc86c66d9584c5ea（3 提交：e85430e1 实现、99e4752f 留痕、f9eaec77 单元卡 v1.0） |
| verdict | **pass**（阻断级 0；建议级 2，已转登 findings.json F-069/F-070） |
| 日期 | 2026-09-13 |
| 记录分支 | acc/EV-T09/1（基于 origin/redesign-main @50a69a26） |

## 0. 验收者环境说明

- **独立性声明**：本验收为全新上下文会话（PIPE §0.4 子代理派发模式），未消费实施者任何过程叙述；只读任务契约、设计文档、送验 SHA 的产物与本机复现输出；对抗式立场执行（逐项为"不通过"找证据后落裁决）；构建与测试全部由验收者亲手重跑（ACC §2 四条逐条满足，无需降级声明）。
- 复现现场：`git worktree add --detach %TEMP%\ird-acc-ev-t09-a f9eaec77…`（**detached @ SHA**）；记录回传：`git worktree add -b acc/EV-T09/1 %TEMP%\ird-acc-ev-t09-b origin/redesign-main`。
- 集成配置按 F-062/F-007 先例补齐环境前置（AGENTS §4.1 字面命令在非主树不足）：`-DCMAKE_TOOLCHAIN_FILE=D:/10_Source_Repos/21_robot/RobWork/vcpkg/scripts/buildsystems/vcpkg.cmake`（绝对路径）＋`-DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`＋从主树复制两件 git 未跟踪 ini 模板（RobWorkStudio.ini.template.static / .shared.in）到 worktree 的 RobWork/RobWorkStudio/bin/。参数均取自主树 build/CMakeCache.txt 实际配置；配置后 grep 确认 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`。
- 冒烟：独立临时目录 `%TEMP%\ird-acc-ev-t09-smoke`，vcpkg toolchain 绝对路径，Release。
- ird_gates / validate-task 在主仓库只读运行（gate 类脚本在 worktree 因缺 vcpkg 环境失败——F-009 既有口径）；运行未改变主仓工作树 git 状态。
- 探针（4.5）执行遵循 F-068 教训：恢复后 touch 强制重编译，并核验 git 零差异＋PROBE 标记零残留双条件。

## 1. 逐项证据表（ACC §4，4.1～4.11）

### 4.1 改动范围 —— 通过

- `git diff --name-only 50a69a26..f9eaec77` 共 33 文件，逐一对契约 allowedFiles 匹配：
  - 4 件实现面：`industrialrobot/evidence/CMakeLists.txt`、`evidence/include/sdurws/ird/evidence/Compatibility.hpp`、`evidence/src/Compatibility.cpp`、`evidence/test/CompatibilityTest.cpp` ⊂ `industrialrobot/evidence/**` ✓
  - 28 件留痕：`doc/industrial-robot-design/traceability/builds/wp05-t09/**` ⊂ `traceability/**` ✓
  - 1 件单元卡：`doc/industrial-robot-design/units/evidence.md` ✓（allowedFiles 显式列出）
- forbiddenFiles（REQUIREMENTS.md／ARCHITECTURE.md）零触碰（diff 文件清单无其一）✓
- SA-02：diff 内无 `patches/` 路径（无未登记框架改动）；industrialrobot/ 之外的框架源码零变更 ✓

### 4.2 可复现构建 —— 通过（全量树 F-008 既有错误按判例口径排除）

- 集成模式（worktree A，重新配置，缓存 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON` 已 grep 确认）：
  - 全量 `cmake --build build --config Release -j` 退出码 1——73 处 error **全部**落于 RobWorkSim 框架头（grep 统计：VelocityScrew6D.h 63＋Math.h 8＋Parallelizer.h 2，RWSimulatorPlugin/BtPlugin 簇），与既有基线缺陷 F-008 判例口径（71＋2 处、与 industrialrobot 无涉、以任务目标面构建为准）一致；industrialrobot 面零错误。
  - 任务目标面构建 exit 0（见 4.3）；测试可执行产出并运行全绿（见 4.3/4.4）。
- 冒烟模式（全新独立树）：`cmake -S RobWork/RobWorkStudio/src/rwslibs/industrialrobot -B <tmp> -DCMAKE_TOOLCHAIN_FILE=<vcpkg 绝对路径>` 配置成功，`cmake --build <tmp> --config Release -j` **exit 0 全量零错误**（含 evidence/core/policy/runtime/testkit 全部目标）。
- 结论：两模式任务面可复现构建通过；验收日志留存于验收者临时区（命令与退出码如上，逐条可在同环境复跑）。

### 4.3 契约 verify 命令 —— 通过

- 契约 verify 数组仅一条：`cmake --build build --config Release --target sdurws_ird_evidence_test`——在 worktree A 集成树**亲手执行，exit 0** ✓。
- 附加（完整性）：`--target sdurws_ird_evidence_contract_test` 亦 exit 0。
- 集成树运行：`sdurws_ird_evidence_test.exe` → **173/173 PASSED**（exit 0）；`sdurws_ird_evidence_contract_test.exe` → **5/5 PASSED**（exit 0）。
- 冒烟树运行：evidence **173/173**＋contract **5/5**（exit 0）；全量回归 core **61**＋policy **4**＋runtime **176**＋testkit **97**——合计 **516/516**，与实施侧 ird-test-report.json summary 声明逐项一致 ✓。

### 4.4 acceptance 条目逐项 —— 通过（ird_gates 已执行，非"未执行"口径）

| 契约 acceptance 条目 | 证据（验收者亲手复现） |
| --- | --- |
| ①EV-CPA-1~3：Quick≠Verified/契约/检查点独立用例通过 | EV-CPA-1＝`QuickCacheVersusVerifiedRequest_EV_CPA_1`（Quick 缓存 vs Verified 请求→Incompatible、reasons 恰 [ModeMismatch]）＋`VerifiedCacheVersusQuickRequest_EV_CPA_1`（反方向同判——D-13 双向）＋`SameModeAlignedPairIsFullHit_EV_CPA_1`（正例半边，排除过宽实现）；EV-CPA-2＝`ContractVersionMismatch_EV_CPA_2` 缓存面＋检查点面两用例（同 sliceId 契约不同→Incompatible[contract-mismatch]，token 精确断言）；EV-CPA-3＝`CheckpointIndependentOfTaskOutcome_EV_CPA_3`（Failed 检查点 integrityVerified=true→Resumeable；CheckpointSummary 无 outcome 字段的类型级独立＋Quick/Verified 请求判定一致）＋`IntegrityNotVerifiedRejected_EV_CPA_3`（完整性反面）。以上 7 用例在两模式均绿（测试文件 L126~L237，ird-test-report.json testSuites 逐条 trace）。 |
| ②部分/失败结果不得作正式缓存命中（§8.2 纯判定反例）用例通过 | `CanceledResultIsDiagnosticOnly_EV_ENV_2`（身份面对齐 Canceled→DiagnosticOnly **非 FullHit**、reasons=[OutcomeNotCompleted]）＋`FailedAndInterruptedResultsAreDiagnosticOnly_EV_ENV_2`（Failed/Interrupted 同轨）＋`UnfinalizedManifestIsDiagnosticOnly_EV_ENV_2`（Completed 但清单未封账→DiagnosticOnly[ManifestIncomplete]）＋`BothResultFaceFailuresListed_EV_ENV_2`（双失配全量列出）＋`IdentityFaceMismatchNeverDiagnosticOnly_EV_ENV_2`（身份面失配不降档）＋Slice/Profile 失配两反例。两模式全绿。 |

- 与 §8.2 判定式逐字对照：FullHit 六条件／DiagnosticOnly 身份面对齐∧(outcome≠Completed ∨ 未 finalize)／Incompatible 其余一切／Resumeable 五条件——实现（Compatibility.cpp L43~L91、L105~L142）与设计原文一致，无偏差。
- ird_gates 已建成并真实执行（见 4.8/4.10）——非"未执行"口径。

### 4.5 测试真实失败能力（验证验证者）—— 通过

验收者独立注入两处短路（非实施侧探针点位组合的照抄，PROBE 标记 `PROBE-ACC45-*`）：

1. `Compatibility.cpp` L43：`if (summary.mode != …)` → `if (false && …)`（judgeCacheHit 模式检查短路）；
2. L128：`if (!summary.integrityVerified)` → `if (false && …)`（judgeCheckpointCompatibility 完整性检查短路）。

重构建后运行：**exit 1，6 用例 FAILED**——探针 1 命中 `QuickCacheVersusVerifiedRequest_EV_CPA_1`／`VerifiedCacheVersusQuickRequest_EV_CPA_1`／`AllMismatchReasonsCollectedInCheckOrder_EV_CPA_2`／`IdentityFaceMismatchNeverDiagnosticOnly_EV_ENV_2`；探针 2 命中 `IntegrityNotVerifiedRejected_EV_CPA_3`／`MultipleRejectionsCollectedInCheckOrder_EV_CPA_2`。测试对实现偏转敏感，非恒真 ✓。

恢复：`git checkout` 逐字还原＋touch 强制重编译（F-068 教训）→ **173/173 复绿**（exit 0）；`grep -c PROBE`＝**0**、`git status --porcelain`（industrialrobot/）＝**0 条**——零残留 ✓。

### 4.6 架构红线（AGENTS §5 八条）—— 通过

1. **L2 零 Qt**：Compatibility.hpp/.cpp/CompatibilityTest.cpp 全部 include 扫描零 Qt 头（grep `#include <Qt` 零命中）；仅 core 公共头＋本单元 Slice.hpp＋标准库＋gtest ✓
2. **业务域互链禁止（R-1）**：include 面无 modeling/requirements/kinematics/trajectory/dynamics/selection/optimization 任何头；链接面仅 sdurws_ird_core（CMakeLists 未变更链接结构；实施留痕 ird_gates 引擎自测 pass_clean亦证）✓
3. **跨单元私有头禁止（R-2）**：跨单元 include 仅 `<sdurws/ird/core/Digest.hpp>`、`<sdurws/ird/core/Evaluation.hpp>`——均为 core 公共头（include/sdurws/ird/core/ 下）✓
4. **框架零源码修改（SA-02）**：见 4.1，patches/ 无新文件 ✓
5. **不做名称前缀拼接/剥离（R-4）**：Compatibility 全文无名称解析逻辑（grep 无 RDX/RWS 名称拼接面）✓
6. **权威唯一（PA-1）**：判定语义冻结于 evidence（§8.2 授权），不写缓存/检查点存储（无 I/O、无持久状态——C-5 非抛出纯函数）；诊断码未私注册（注释明示 EVI-CACHE-INCOMPATIBLE 为建议值、码值权威归 StableCodeRegistry）✓
7. **不可变历史（PA-2）**：两判定函数均无副作用、不改任何入参（全 const 引用；确定性注释 NFR-COR-02）✓
8. **不继承 old/ 历史**：全新实现，无 old/ 引用 ✓

### 4.7 注释规范（AGENTS §2）—— 通过（1 处测试名单笔误降为建议级 F-070）

- 公共头 Compatibility.hpp（全查，399 行）：文件头注释含设计依据（§8.2/§12/§14/需求编号）、背景说明（为什么纯函数）、实现口径 C-1~C-5、线程安全/确定性声明 ✓；5 个结构体＋2 函数全部 Doxygen 中文注释，逐字段说明业务含义与值域 ✓；§2.5 高危信息：单位（版本号无量纲显式注明）、线程约束（可重入纯函数）、错误语义（非抛出/调用方转译 EVI-CACHE-INCOMPATIBLE）、确定性来源（检查序固定、字节精确等值）齐备 ✓
- 实现文件 Compatibility.cpp（本任务唯一 .cpp，全查）：文件头实现纪律四条；函数内每个语义段落/分支注释"为什么"（身份面/结果面分野、双向模式拒绝的 D-13 依据、键相等性由 sliceId 承载的 C-2 推理、保守拒绝的 NFR-COR-03 依据）✓
- 测试文件 CompatibilityTest.cpp：文件头列 acceptance↔测试名映射；每 TEST 注释含 §11 行号定位＋需求/AT 追溯（EV_CPA_1/2/3、EV_ENV_2 后缀即追溯字段）✓——唯文件头注释测试名清单与实际 TEST 名两处失配（建议级，转登 F-070，零行为影响）
- CMakeLists.txt 增列处有中文注释块（EV-T09 职责与红线说明）✓

### 4.8 文档同步与 findings 相关性 —— 通过

- **单元卡同步**：units/evidence.md 卡头 v0.9→v1.0（版本链与 §15.4 末行一致——机检可核）；§15.4 v1.0 行登记实现落位（DTB §5.4），含实现口径 C-1~C-5、22 新增用例、探针/门禁/回归摘要。§8.2 设计正文零改动＝实现按卡内冻结代码逐字交付、无语义偏差需仲裁 ✓
- **任务状态/追踪矩阵**：§14 矩阵 CON-04 行（§8.2＋§5.1→EV-CPA-1~3）、OPT-06 行（§8.2）既有落点与实现一致，无需变更；契约 status 翻转归 PIPE §4.7⑥ 收尾动作（不属于实施提交，EV-T08 同例）✓
- **findings.json 未关闭条目相关性核对**（54 条 open 逐条判断，与 wp05-t09 面直接相关者）：
  - **F-019／F-051（门禁基线红，相关）**：本任务 ird_gates 3 处命中＝IRD-GATE-T1（testdata_lint→testkit）＋IRD-GATE-SUB×2（runtime_test 直链 testkit；testdata_lint 表外依赖边）——验收者在主仓复跑 ird_gates 实测命中区块与实施留痕逐项一致，归属标注（F-019×2＋F-051×1）与 findings.json 权威描述一致，零新增。两 finding 维持 open（归 TK-T03/白名单治理），非本任务可消账。
  - **F-020／F-058（evidence.md §3.5 引用缺失／'AI 执行就绪补充'转义损毁，相关）**：v1.0 仅动卡头版本行与 §15.4 表，未修复（不在本任务允许语义内——属 DOC 治理），维持 open；非本任务验收阻断。
  - **F-056／F-066（EVI-* 建议码超 §13 枚举，相关——本任务无新增）**：本任务消费的 EVI-CACHE-INCOMPATIBLE 在 §13 清单七值之内，未新增枚举外建议码；无再现。
  - **F-057（evidence.md 卡头版本行滞后，相关——已于 v0.4 消账）**：v0.4 变更记录明示"卡头版本链补全——F-057 消账"，本任务 v1.0 卡头行与 §15.4 末行机检一致——**该 finding 实质已闭合而状态仍 open**，建议所有者复核后翻转为 fixed（本验收顺手核证，不代为改状态）。
  - **F-059／F-012 族（提交标题格式，同族）**：本任务变体见 4.9 与 F-069。
  - **F-060（report 门禁归属笼统归并，相关——已规避）**：本任务 ird-test-report.json irdGates 字段逐项标注 F-051×1＋F-019×2 并点名"按 findings.json 权威归属逐项标注——F-060 教训不笼统归并"，教训已吸收。
  - **F-061／F-063／F-064／F-067（他任务交付面微瑕，无关/弱相关）**：本任务未触及 SnapshotTest.cpp/Verdict.hpp/Envelope.hpp/Currentness.hpp，无再现、无涉。
  - **F-065（留痕日志双重编码乱码，系统性，相关）**：本任务 ird-gates.log 门禁段同症（『娴嬭瘯鐩爣』型）——归 F-065 既有系统性条目跟踪，不另开新号；命中清单经主仓复跑逐项比对无歧义，不影响证据效力。
  - **F-068（探针 mtime 陷阱，相关——已按教训规避）**：本验收恢复后 touch 强制重编译＋双条件核验（git 零差异＋PROBE 零残留），复绿真实。
  - **F-062／F-007（worktree 环境前置未成文，相关——本验收踩坑并按先例补齐）**：配置三次迭代（Boost 缺失→Qt 缺失→ini 模板缺失）后按 F-062 先例口径（toolchain＋Qt 前缀＋两件 ini 模板复制）通过——再次实证该环境缺口，维持 open 归流程文档维护者。
  - 其余（F-004~F-018、F-021~F-055 中非上述条目、F-048/F-050/F-052~F-055 等）：runtime/testkit/io/project/policy 各单元交付面或流程治理问题，与 EV-T09（evidence 兼容判定）无涉，逐条判断无关。

### 4.9 commit 质量 —— 有瑕疵通过（标题前缀缺失降为建议级 F-069）

- 三份提交（e85430e1 实现＋测试＋CMake 同提交／99e4752f 留痕／f9eaec77 单元卡）**正文四段齐备**（改动内容/改动原因/验证/影响面与兼容性）＋Acc-Covered 尾注；代码＋测试＋CMake 同提交，粒度符合 §6.3 ✓。
- 验证结论与留痕/验收者复现逐项比对**无粉饰**：173/173＋5/5、516/516、门禁 3 既有命中归属、探针红绿与残留零、validate-task PASS——全部亲手复现一致 ✓。
- **瑕疵**：三份标题均缺 `[WP-05-T09]` 前缀（AGENTS §6.3/ACC §4.9 字面格式），与直接前例 EV-T07/T08 不一致；但动词开头满足、任务编号（EV-T09）在标题内可见。比照 F-048 先例（RT-T11 标题名词开头→建议级 finding 不阻断），登记 F-069，不构成阻断。

### 4.10 留痕完整 —— 通过

- gtest XML：实施留痕 sdurws_ird_evidence_test-smoke/-integration.xml 各 173 testcase（grep 计数核验）＋contract 5×2＋其余单元 XML，齐备 ✓
- ird-test-report.json：schemaVersion ird-test-report/1，summary 178/178（evidence 173＋contract 5）、smoke 全量 516/516、failureCapabilitySelfCheck（探针方法＋F-068 规避）、irdGates 逐项归属、`notExecuted: []` ✓
- 构建日志：integration-build-evidence.log／smoke-build-full.log／smoke-configure.log／probe-red/restore-build.log 等全套在 traceability/builds/wp05-t09/ ✓（*.log 经 git add -f 入库——F-010 教训）
- phase-one-validation.log L23 已有 `EV-T09.json: validate-task: PASS` 行（契约编译期登记；验收者主仓复跑 validate-task 亦 PASS exit 0）✓
- 留痕真实性交叉核验：实施侧 ird-gates.log 命中区块与验收者主仓复跑输出逐项一致（3 处，编码乱码不影响比对——F-065）✓

### 4.11 偷懒/缩水实现扫描 —— 通过

- ①diff 范围 `TODO|FIXME|stub|placeholder|未实现|not implemented` 扫描（industrialrobot/ 面 +新增行）**零命中** ✓
- ②空实现检测：两判定函数均为完整判定逻辑（六检查＋五条件＋三档/两档裁决），无空体/仅返回默认值的公共接口；kCheckpointFormatVersionMin/Max 为设计口径 C-3 的显式常量（非占位）✓
- ③acceptance 逐条证据表：见 4.4——两条 acceptance 均有具名测试＋两模式运行输出＋report trace 字段，无一条缺证据 ✓
- ④测试强度：22 用例全部校验行为与边界（verdict＋reasons 精确序列断言、六项全失配检查序、格式区间 0/1/2 三值边界、双向模式拒绝、确定性幂等），非恒真非"只编译通过"；4.5 探针实证 6 用例对实现偏转敏感 ✓

## 2. 问题清单

### 阻断级

无。

### 建议级（已全部转登 traceability/findings.json，编号顺延自 F-068）

1. **F-069**：三份送验提交标题缺 `[WP-05-T09]` 前缀（动词开头满足、任务编号在标题内可见、正文四段与验证真实性全过）——AGENTS §6.3/ACC §4.9 标题字面格式偏差，F-048 先例同口径降为建议级；送验对象已冻结不追改。
2. **F-070**：CompatibilityTest.cpp 文件头注释（L24-26）测试名清单与实际 TEST 名单两处失配（`UnfinalizedManifestIsDiagnosticOnly_EV_CPA_2` 应为 `_EV_ENV_2`；`PartialOutcomeNeverFullHit 反例组` 无对应具名测试——语义由三用例共同承载）。零行为影响。

### 顺手核证（不改状态，提请所有者复核）

- **F-057**：evidence.md 卡头版本行滞后问题实质已于 v0.4 消账（v0.4 变更记录明示），且本任务 v1.0 卡头行与 §15.4 末行机检一致——建议所有者复核后将 F-057 翻转为 fixed。

## 3. 裁决

- **verdict：pass**——4.1~4.11 全部通过（4.9 以建议级瑕疵通过，不属 ACC §5 阻断列举），无阻断级问题；建议级 2 条已转登 findings.json（F-069/F-070），不阻塞合入。
- 送验对象＝f9eaec775e794862082861d1bc86c66d9584c5ea（冻结 SHA）；本验收结论仅针对该对象。
- 本记录不构成合入动作——合入由所有者按 PIPE §4.7 决策执行。
