# WP-01-T02 验收记录（对抗式验收）

| 字段 | 值 |
| --- | --- |
| 任务 ID | WP-01-T02（masterWp WP-B，CI 常驻与本地一键门禁） |
| 契约 | tasks/WP-01-T02.json（status: ready，随收尾翻转 done） |
| 验收对象 | 分支 `wp01-t02`，commit 范围 `69130fc..aa9c4f9`（head＝`aa9c4f9e6c5160eb3f38846300abb586e35bcef0`，与 origin 一致——漂移检测 PASS） |
| 尝试序号 | attempt=1（evidence 分支 `acc/WP-01-T02/1`） |
| **verdict** | **pass**（无阻断级；1 条建议级转登 F-009） |
| 验收日期 | 2026-09-10 |
| 独立性声明 | PIPE v1.7 §0.4 单会话降级（同会话顺序执行）；补偿＝无管道真退出码复跑＋逐字节 cmp 对齐校验＋失败能力探针，不以实施自述为据 |

---

## 逐项证据

### 4.1 改动范围 —— PASS

diff＝5 文件：`scripts/industrialrobot/gate-all.ps1`＋`ci/`×3＋DTB——均在 allowedFiles（scripts/industrialrobot/**、development-task-breakdown.md）；forbiddenFiles（REQUIREMENTS/ARCHITECTURE/rwslibs/**）零触碰；**流水线 state.json 未混入**（吸收 DOC-T03 验收 G-1 观察）。

### 4.2/4.3 可复现构建与 verify 命令 —— PASS（环境口径限定，见 F-009）

- verify[0] `validate-docs.ps1`：主树与 worktree A（detached @ aa9c4f9）均 PASS；
- verify[1] 一键脚本全量执行：**主树（脚本设计口径＝仓库根检出，DTB §5.1 三段自检）真退出码运行 `TRUE-EXIT=0`，9/9 全绿**（ird_gates＋集成 core/testkit＋冒烟同；本 tick 两轮全绿＋终验一轮）；管道场景退出码陷阱已识别——仅采信无管道运行；
- worktree A 内执行失败（0/5）＝环境前置缺失（worktree 无 vcpkg/Qt，F-007 同类）——非脚本逻辑缺陷，登记 **F-009**（建议级）。

### 4.4 契约 acceptance 逐项 —— PASS

| # | 条目 | 证据 |
| --- | --- | --- |
| 1 | 一键执行全部门禁与测试；CI 前强制门槛 | gate-all.ps1：ird_gates＋双模式＋全部 `_test`/`_contract_test`（CTestTestfile 递归发现，2 注册×2 模式）9/9 TRUE-EXIT=0；失败即 exit 1＋"禁止提交"输出 |
| 2 | 零 Python | 脚本仅 pwsh/cmake/ctest；无 Python 调用 |
| 3 | 框架文件不越权 | 零触碰（4.1）；CI 激活前置登记 **O-34**（DTB §4.2：SA-02 补丁登记＋入口脚本命名裁决＋契约扩权，归构建约定所有者） |
| 4 | 与 eb777e2 对齐或说明差异 | **cmp 逐字节一致**（gitlab 5704B＋workflow 26364B 双双 IDENTICAL）；差异 3 项（落位路径/入口脚本命名/分支保护）在 ci/README 差异表说明——"对齐"成立 |

### 4.5 失败能力 —— PASS

破坏探针（-BuildDir 指向不存在路径）：`PROBE-TRUE-EXIT=1`，FAIL 行＋汇总 4/7＋"禁止提交"——门禁的二元结论与失败路径真实可用。

### 4.6/4.7/4.11 —— PASS

红线：零框架/产品源码改动（SA-02 ✓）；中文注释齐备（脚本头设计依据/用法/退出码；函数级说明；CTestTestfile 发现机制的理由注释）；偷懒扫描：diff 内 TODO/FIXME/stub/placeholder 零命中；CI 模板为固化引用（非本任务新写逻辑，逐字节对齐声明承载）。

### 4.8/4.9/4.10 —— PASS

DTB v0.14＋O-34 登记在位；commit 四段式＋Acc-Covered 1,2,3,4；留痕＝本记录＋gate-all 运行结论（两次全绿＋真退出码）载入记录；governance-log 无需增行（O-34 为 DTB §4 新登记，下次治理同步）。

## 问题清单

### 阻断级

无。

### 建议级（转登 findings.json）

| # | 问题 | 转登 |
| --- | --- | --- |
| G-1 | gate-all 在非主树检出（worktree/全新克隆）因 vcpkg toolchain/Qt 前缀/ini 模板缺失而无法全量运行（F-007 同类环境链）；建议 Toolchain 参数化＋VCPKG_ROOT 回退；CI 侧供给由 eb777e2 供给链承载（模板已固化），本地全新树口径随 F-007 一并处置 | F-009 |
