# EV-T03 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | EV-T03 实现 AnalysisSnapshot（快照/校验/修订闭包）——契约 tasks/foundation/EV-T03.json |
| 送验对象（冻结 SHA） | f3b8acf20b52ed5b5494e2ba5f8f1accd8b44fe4（origin/wp05-t03 @ 验收时点逐字节一致——分支漂移检查通过，见 §4.1） |
| commit 范围 | 0beadf9c33e02d94edce4afd8a62e3e37a6bfbf1..f3b8acf20b52ed5b5494e2ba5f8f1accd8b44fe4（3 个提交：6f39d355 实现 / 4d3a2ae8 文档同步 / f3b8acf2 留痕） |
| 验收尝试序号 | 1 |
| **verdict** | **pass**（4.1～4.11 全部通过；无阻断级问题；建议级问题 2 条已按 ACC §5 转登 findings.json F-059/F-060） |
| 验收者环境 | 独立全新上下文会话（PIPE §0.4 派发模式，未消费任何实施叙述；ACC §2 四条独立性要求逐条满足）。复现现场＝detached worktree @SHA（%TEMP%\ird-acc-ev-t03-a）；冒烟＝独立临时目录 %TEMP%\ird-acc-ev-t03-smoke（vcpkg toolchain 绝对路径）；记录回传＝worktree acc/EV-T03/1（基于 origin/redesign-main 0beadf9c）。验收全程未向任务分支推送任何提交、未修改实施产物（探针编辑已精确还原并经 git diff 零差异核验） |

## 验收者独立性声明

本验收为全新上下文，只消费任务契约（tasks/foundation/EV-T03.json）、设计文档（units/evidence.md §4.1/§5.1/§5.2、REQUIREMENTS CON-01/06）、送验产物（diff 与 traceability/builds/wp05-t03/ 留痕）与仓库治理文件（AGENTS.md、acceptance-protocol.md、findings.json）。未读取实施者的过程叙述；全部构建与测试由验收者亲手在 ACC §3 规定复现现场（detached worktree @ f3b8acf2）重跑；立场为对抗式核查（为"不通过"找证据），每项检查给出通过证据或失败反证。

## 逐项证据表（ACC §4，4.1～4.11）

### 4.1 改动范围 —— 通过

- **分支漂移检查**：`git fetch origin wp05-t03 && git rev-parse origin/wp05-t03` ＝ f3b8acf20b52ed5b5494e2ba5f8f1accd8b44fe4 ＝ 送验 SHA（无漂移）。
- **diff 文件清单**（`git diff --name-only 0beadf9c..f3b8acf2`，共 19 个）逐个对照契约 allowedFiles：
  - `RobWork/RobWorkStudio/src/rwslibs/industrialrobot/evidence/`（CMakeLists.txt、include/sdurws/ird/evidence/Snapshot.hpp、src/Snapshot.cpp、test/SnapshotTest.cpp）×4 ＝ allowedFiles[0]（evidence/**）；
  - `RobWork/doc/industrial-robot-design/traceability/**`（builds/wp05-t03/ 13 份＋findings.json）×14 ＝ allowedFiles[3]；
  - `RobWork/doc/industrial-robot-design/units/evidence.md` ×1 ＝ allowedFiles[2]。
- **forbiddenFiles 零触碰**：REQUIREMENTS.md / ARCHITECTURE.md 不在 diff 中。
- **无未登记框架源码改动**：diff 中无 `RobWork/`（框架）与 `RobWorkSim/`、`RobWorkStudio/`（非 industrialrobot）文件；patches/ 无新增登记需求。

### 4.2 可复现构建 —— 通过（按 F-008 判例口径：任务目标面从零零错误）

- **集成模式**（worktree A，全新从零）：`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=<vcpkg 绝对路径> -DCMAKE_PREFIX_PATH=<Qt>` → exit 0；`grep RWS_BUILD_INDUSTRIALROBOT build/CMakeCache.txt` ＝ `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`。
  - 契约 verify 目标从零构建：`cmake --build build --config Release --target sdurws_ird_evidence_test -j` → **exit 0、零 error**（sdurw_core/math → sdurws_ird_core → sdurws_ird_evidence → sdurws_ird_evidence_test 全链产出，验收者日志 acc-integration-verify.log）。
  - 全量 Release 构建：exit 1，error C 计数 73＝RWSimulatorPlugin 71＋bt_plugin 2，**零处涉及 industrialrobot**——与 F-008/F-038 判例登记的基线缺陷完全一致，不影响本任务判定（acc-integration-fullbuild.log）。
  - 环境注记：全新 worktree 配置先后踩 F-007 已登记的两处前置缺失（Boost/Qt 前缀需 toolchain＋CMAKE_PREFIX_PATH；RobWorkStudio.ini.template.static 被 .gitignore 吞导致 RobWorkStudio/src/CMakeLists.txt:58 配置失败）——均按 F-007 resolution 以环境供给方式解决（传参＋从主树复制样板），非本任务引入，F-007 维持 open。
- **独立冒烟模式**（%TEMP%\ird-acc-ev-t03-smoke，industrialrobot 子目录直配）：`cmake -S RobWork/RobWorkStudio/src/rwslibs/industrialrobot -B <临时目录> -DCMAKE_TOOLCHAIN_FILE=<vcpkg 绝对路径>` → exit 0；`cmake --build <临时目录> --config Release -j` → **exit 0 零 error**（acc-smoke-build.log 中 "error" 字样均为 Errors.cpp/ErrorsTest.cpp 文件名误匹配）。
- 集成测试（验收者亲手运行）：`sdurws_ird_evidence_test.exe` → **43/43 PASSED，exit 0**；冒烟测试 → **43/43 PASSED，exit 0**。

### 4.3 契约 verify 命令 —— 通过

- 契约 verify 数组仅一条：`cmake --build build --config Release --target sdurws_ird_evidence_test`——验收者在 worktree A 全新构建树上逐字亲手执行，exit 0（见 4.2）。实施侧对应留痕 integration-verify.log 尾部为同一命令的产物（exe 产出行）。

### 4.4 acceptance 条目逐条证据 —— 通过（3/3，均有具名测试＋亲手复跑）

| # | 契约 acceptance | 证据（具名测试，验收者双模式复跑通过） |
| --- | --- | --- |
| 1 | EV-ID-1/3 用例通过 | EvidenceSnapshotIdentity.SameContentDeterministicId_EV_ID_1（同内容异插入序 snapshotId/编码逐字节相等）、CodecRoundTrip_EV_ID_1（往返＋revisionSeq 恢复 0＋身份/再编码双稳定）、DisplayAndSessionIndependence_EV_ID_3（显示名/会话/序号不入身份，含反向对照断言）——integration/smoke XML tests=43 failures=0 |
| 2 | 修订闭包拒绝（混入反例）用例通过 | EvidenceSnapshotBuilder.RevisionClosureRejectsForeignObject_EV_SNAP（(oid,cv) 不在锚定修订 → SnapshotIncomplete＋"不属于锚定修订"定位）、ClosureCheckedAgainstAnchoredRevision_EV_SNAP（锚定读取正反对照）；正例 ValidBuildFreezesCanonicalSnapshot_EV_SNAP 另断言事实来源被逐条消费（queries==2）；真实失败能力另由 4.5 探针独立证实 |
| 3 | RequiredCaseSet 冻结承载（caseId 唯一/enabled＋mandatory 标记；O-14 未决保守字面）用例通过 | EvidenceSnapshotCaseSet.MarkersCarriedVerbatim_O14_EV_CASESET（enabled=false∧mandatory=true 反直觉组合与空 label 原样承载不私裁——O-14/P-EV-9 字面执行）、DuplicateCaseIdRejected_EV_CASESET（重复 caseId 拒绝）、EmptyCaseSetAllowed_EV_CASESET（空集合法＋凭据确定值）、RequiredCaseSetIdSensitivity_EV_CASESET（mandatory/label/增删敏感＋插入序不敏感） |

- ird_gates 自查：**实际执行**（非"未执行"标注）——见 4.6/4.8；实施侧已在 ird-test-report.json 与提交信息登记真实结论（3 处既有命中、退出码 1、零新增），符合 F-035 判例确立的实现类任务纪律。命中归属的编号标注有一处失准（F-019 应为 F-019×2＋F-051×1），实质主张成立——登记 F-060（建议级），不阻断。

### 4.5 测试真实失败能力（验收验证者）—— 通过

验收者独立双探针（在 worktree A 内编辑 → 增量构建 → 运行 → 精确还原 → 重建复绿 → `git diff` 对送验 SHA 零差异核验）：

1. **探针 1：防混入校验短路**——src/Snapshot.cpp 修订闭包包含性检查前注入 `false &&`：EvidenceSnapshotBuilder 组 10 用例中 3 用例如期变红（RevisionClosureRejectsForeignObject、ClosureCheckedAgainstAnchoredRevision、ValidBuildFreezesCanonicalSnapshot 的 queries 断言），exit 1——防混入校验真实存在且被测试钉住。
2. **探针 2：④b 载荷摘要校验短路**——verifyMaterializedPayload 的摘要比对注入 `false &&`：EvidenceSnapshotCodec.MaterializedIntegrity_EV_SNAPC 如期变红（物化/接收两端校验的测试敏感），exit 1。
3. 还原后重建终验：43/43 PASSED 复绿；`git diff --quiet` 确认工作树与送验 SHA 零差异（无残差）。

结论：断言非恒真，测试对实现行为真实敏感（ACC §4.5"验证验证者"成立）。

### 4.6 架构红线（AGENTS §5 八条逐条）—— 通过

| # | 红线 | 证据 |
| --- | --- | --- |
| 1 | L2 计算内核零 Qt | grep evidence/ 全部 .hpp/.cpp 无 Qt include/链接（零命中）；EvidenceBuild.NoQtInclude_EV_BUILD_NFR_MNT_01 现扫描含新增 Snapshot.hpp/.cpp 通过；CMakeLists 零 Qt 守卫在位 |
| 2 | 业务域互链禁止（R-1） | target_link_libraries 仅 sdurws_ird_core（evidence.md §3.2 唯一单元边）；CMake 配置期断言对非 core 单元边 FATAL_ERROR；ird_gates R-1 零命中 |
| 3 | 跨单元私有头（R-2） | include 面实测：Snapshot.hpp → core/Digest、core/Identity、evidence/Dependency、evidence/Errors（全部公共头 include/sdurws/ird/<unit>/）；无跨单元私有头；NoCrossUnitInclude/PublicHeaderPathLayout 用例通过 |
| 4 | 框架零源码修改（SA-02） | 4.1 已证 diff 无框架文件；patches/ 无新增 |
| 5 | 名称前缀拼接/剥离（R-4） | grep evidence 面零命中（nameMapRef 只承载 ContentIdentity，无名称字符串处理）；ird_gates R-4 零命中（pass_r4comment 自测过） |
| 6 | 权威唯一（PA-1） | 快照校验/编码/凭据计算归 evidence（§4.1/§5.1/§5.2 本职）；名称→runtime（仅持身份引用）、策略→policy（仅持身份引用）、标记 schema→requirements（O-14 只承载不解释）——未越权实现他单元语义 |
| 7 | 不可变历史（PA-2） | AnalysisSnapshot 无 setter、builder 唯一合法生产者、冻结后只读（代码走读证实）；快照身份为内容派生非申报 |
| 8 | 不继承 old/ 历史 | 实现全新编写，diff 内无 old/ 引用 |

### 4.7 注释规范（AGENTS §2）—— 通过

- **公共头全查**（Snapshot.hpp，622 行）：文件头（设计依据＋需求追溯＋背景说明＋实现形态＋线程安全声明）齐备；每个 struct/class/函数均有 Doxygen 中文注释；§2.5 高危信息表逐项在位——单位（计数无量纲、字节数）、坐标系（不适用——纯值类型面）、所有权（IObjectBytesSource/IRevisionClosureSource"调用方持有，本函数不接管"）、线程约束（builder 非线程安全仅组装线程、codec 纯函数可重入、冻结后跨线程共享）、错误语义（SnapshotIncomplete＝调用方组装违约 fail-fast / SnapshotIntegrity＝环境损坏检测，码面归类明确）、确定性来源（冻结序规范化、NFR-COR-02、presence 字节 NFR-COR-03）。
- **实现文件抽查**（Snapshot.cpp 全文 977 行＋CMakeLists.txt＋SnapshotTest.cpp，满足 ≥3）：Snapshot.cpp 文件头含字节布局表/固定校验顺序/实现口径 I-1～I-7 登记；每个语义段落有"为什么"级注释（防混入是 CON-01 根本破坏、digest 一致性闸门、先编码后回填无循环依赖等）；魔法数字均有来源（0xFFFF 编码上限、kMax=32 消息截断、"IRDCASE1"魔数）。CMakeLists.txt 关键步骤（红线守卫/gtest 接入/目标升级）中文注释块齐备。SnapshotTest.cpp 用例名带 EV-ID/EV_SNAP 追溯字段＋断言处中文说明验证的需求/验收条目。
- 未发现 §2.6 反面示例（复述代码/无信息占位/中英混杂/过期注释）。

### 4.8 文档同步＋findings 核对 —— 通过

- **单元卡增量修订（DTB §5.4）**：units/evidence.md §15.4 追加 v0.4 行——实现落位登记＋实现口径 I-1～I-7 逐条登记（revisionSeq 不入身份/digest 一致性闸门/冻结序规范化/IRDCASE1 字节级定义/形态字节/双端④b/空工况集合法——均为"实现细节精确化，语义与设计一致"级偏差，登记合规）＋范围边界显式声明（validateForMode(Verified) 入口归 EV-T05，本任务未实现——避免缩水质疑的反向声明）；卡头版本 v0.2→v0.4＋日期推进（版本链补全）。
- **机检**：validate-docs PASS（20 units, 12 trace entries, 150 task files）；validate-task EV-T03.json PASS（designRefs 锚点 units/evidence.md#4.1/#12 均存在——锚点强制对 ready 契约真实生效）。
- **F-057 消账核实**：findings.json F-057 open→fixed（fixedAt 2026-09-12），消账证据实测成立——evidence.md 卡头"文档版本 v0.4／日期 2026-09-12"与 §15.4 末行一致（diff 行级核对）；后半（validate-docs 机检维度）如实保留为开放建议未随条目关闭——消账记录诚实。
- **findings 未关闭条目相关性逐条核对**：
  - F-004（AUTOMOC 空转）：构建日志"Automatic MOC for target sdurws_ird_evidence_test"证实仍在——构建约定面，与本任务无关，维持 open。
  - F-005/F-006/F-058（单元卡转义损毁家族）：worktree A 实测 evidence.md L1109/L1121 损毁仍在——EV-T03 diff 仅卡头＋§15.4（反证在档），owner 归 DOC 治理，维持 open。
  - F-007/F-009（全新树配置前置）：本验收实际踩中（4.2 环境注记），非本任务引入，维持 open。
  - F-008（基线全树构建缺陷）：验收者全量构建复现 73 error（71+2），与登记一致，维持 open。
  - F-010（.gitignore 吞 traceability/**.log）：本任务 13 份 .log 留痕经 `git add -f` 入库（提交信息明示），留痕完整，维持 open（负向豁免仍未做）。
  - F-019（testdata_lint T-1/SUB 两处门禁命中）：验收者亲手复跑确认存在，evidence 面零新增，维持 open。
  - F-020（evidence.md §3.5 缺位）：EV-T03 designRefs 不涉 §3.5，实现未引用该锚点；EV-T02 侧已按 P-PR-6 补登建议码（F-056 同源）——维持 open（owner＝evidence 详设所有者，随 §3.5 增设处置）。
  - F-015~F-018（testkit 杂项）、F-021（fixed）、F-022（fixed）、F-032/F-033（治理脚本/契约文本）、F-043/F-046/F-048（runtime）、F-055（冒烟配置声明）：与本任务无涉或无新增实例；F-055 纪律本次已遵守（报告显式声明冒烟 Release）。
  - F-034/F-035 家族（标题动词开头＋门禁标注）：F-035 纪律本次已遵守（门禁实际执行＋真实结论）；标题家族**新增 2 例**——转登 F-059。
  - F-036/F-038/F-054（构建树性质标注）：本次已遵守（提交信息/报告显式区分"仓库根增量树"与"全新独立树"）。
  - F-051（runtime_test→testkit 门禁第 3 处命中）：验收者复跑确认存在；实施报告将其误归 F-019 名下——**新增实例**——转登 F-060（建议级，实质主张成立）。
  - F-056（EvidenceErrorCode 补登建议码）：EV-T03 仅消费既有码（SnapshotIncomplete/SnapshotIntegrity），无新码，维持 open（注册时裁决）。
  - F-049/F-050/F-052/F-053/F-057：fixed，无需动作。

### 4.9 commit 质量 —— 通过（含 1 建议级，见 F-059）

- 6f39d355『实现 evidence 分析快照：…』——[WP-05-T03] 前缀＋动词开头，合规；四段齐备（What 按文件列要点/Why 对应契约 acceptance 1~3＋设计章节/How 含双模式构建＋43/43＋门禁＋探针的真实结论/影响面声明链接面不变＋偏差登记位置），Acc-Covered: 1,2,3 显式消账。
- f3b8acf2『验证留痕：…』、4d3a2ae8『文档同步：…』——标题名词短语开头，F-012 家族同型（判例 F-034/F-040 已判『验证留痕…』同形态）——**建议级**，转登 F-059；正文四段齐备。
- **验证结论与留痕一致性实测**（防粉饰）：43/43（XML tests=43 failures=0×2＋验收者亲手复跑同值）、构建退出码（冒烟 0/0、集成 verify 0）、新增用例 21（SnapshotTest.cpp TEST 宏清点＝21，43−基线 22 相符）、树性质标注（增量树/全新独立树如实区分——F-036 纪律执行）、门禁命中 3 处既有——**全部与验收者独立复现一致**，无粉饰。一处编号归属失准见 F-060。
- 文档同步提交 4d3a2ae8 自述"历史变更记录行逐字未动"——diff 核对属实（仅追加 v0.4 行＋改卡头两行）。

### 4.10 留痕完整 —— 通过

- gtest XML ×2（sdurws_ird_evidence_test-integration.xml / -smoke.xml，均为 tests=43 failures=0 errors=0）；ird-test-report.json（ird-test-report/1 schema：43 用例分组明细＋acceptance/需求追溯＋双模式结果＋探针记录＋构建树性质 notes＋irdGates 真实结论）；构建/配置/测试/门禁/探针/validate 日志共 11 份——全部落位 traceability/builds/wp05-t03/（13 份证据）。
- resultNote 载体（ird-test-report.json）已更新且完整；phase-one-validation.log 为 WP-03 时代聚合文件，后续任务按既定实践以 builds/<taskId>/ 目录为留痕载体（ACC §4.10"或"支满足）。
- 留痕中引用的命令、计数、文件清单经 ls/grep 实测一致（13 份文件逐一在库）。

### 4.11 偷懒/缩水实现扫描 —— 通过

1. **TODO/FIXME/stub/placeholder/未实现/not implemented 扫描**：diff 范围 evidence/ 全部源文件零命中（grep exit 1）。
2. **空实现检测**：无公共接口空函数体/仅返回默认值；IObjectBytesSource/IRevisionClosureSource 为设计规定的纯虚注入接口（§3.3 原文签名，适配器归 L5/请求方——非缩水，接口可实现性由 FakeObjectBytesSource/FakeRevisionClosureSource 测试替身钉住）。
3. **acceptance 逐条证据表**：见 4.4——每条均有具名测试＋XML 计数＋验收者复跑，任一条无证据即 fail 的要求满足。
4. **测试强度**：断言校验行为与边界（确定性双组装、往返排除字段、混入拒绝＋锚定对照、presence 双向、凭据敏感性四向、双形态同身份、摘要翻转拦截、五类结构垃圾拒绝），非恒真非"只编译通过"；4.5 双探针实证会红。

## 问题清单

### 阻断级

无。

### 建议级（已按 ACC §5 强制转登 findings.json）

- **F-059**：f3b8acf2/4d3a2ae8 两份提交标题名词短语开头——F-012 家族第 18/19 例（模板补救落地前口径持续漂移）；正文四段齐备、验证结论与留痕一致，纯风格瑕疵。
- **F-060**：ird-test-report.json irdGates 字段把 runtime_test→testkit 命中（实为 F-051）归入 F-019 名下——发现编号引用失准；实质主张（3 处既有、零新增、evidence 面零命中）经验收者亲手复跑成立，方向为多归并非漏报，无粉饰。

## 裁决

**verdict ＝ pass**：4.1～4.11 全部通过，无阻断级问题；两条建议级问题已转登 findings.json（F-059/F-060），不阻塞合入。送验对象 f3b8acf20b52ed5b5494e2ba5f8f1accd8b44fe4 通过验收（按 SHA 口径，与分支引用无关）。合入决策按 ACC §6 归所有者。
