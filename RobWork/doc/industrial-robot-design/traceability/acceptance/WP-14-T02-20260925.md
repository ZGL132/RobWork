# WP-14-T02 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | WP-14-T02（requirements 构建落位：占位转真实计算库并登记门禁白名单六边） |
| 送验对象（冻结） | `07d2f6d633768ae924085d38c6e429e2892790a6`（分支 wp14-t02 尖端，单提交） |
| diff 范围 | `e42de11423db492622bfd4759a3c0379eaad4e1a..07d2f6d633768ae924085d38c6e429e2892790a6`（base..head） |
| 契约 | `RobWork/doc/industrial-robot-design/tasks/foundation/WP-14-T02.json` |
| 验收日期 | 2026-09-25 |
| 验收环境 | 独立会话；主检出 redesign-main @ e42de114 未受污染；全部构建/测试/门禁在临时 worktree 与独立构建树中由验收者亲手执行 |
| **verdict** | **pass（无阻断级问题；建议级 5 条，已转登 traceability/findings.json F-358~F-362）** |

## 0. 验收者环境与复现现场说明

- 复现现场：`git worktree add --detach D:/acc_tmp/wp14t02/wtA 07d2f6d6…`（detached @ SHA，非分支名）。
- worktree 缺 gitignored 辅助件的既有现象（T15/T16 / F-200 家族）再次出现：`RobWork/RobWorkStudio/bin/RobWorkStudio.ini.template.static` 不随 git 检出，configure 报 `CMake Error: File …ini.template.static does not exist`——从主检出复制该文件后 configure 通过。属环境前置（F-200 在案），非送验对象缺陷。
- 集成模式全新配置参数（F-007/F-062/F-254 家族在案口径）：除契约字面参数外需 `-DCMAKE_TOOLCHAIN_FILE=<主仓库>/vcpkg/scripts/buildsystems/vcpkg.cmake`＋`-DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`（与主构建树缓存 CMakeCache.txt 一致；wtA 内无 vcpkg 目录——gitignored）。
- 构建前 tasklist 核查：无 RobWorkStudio.exe 残留进程锁 DLL（编排提示第 1 条，未发生、无需 taskkill）。
- 验收记录按 ACC §3 落于 worktree B 分支 `acc/WP-14-T02/1`（基于 origin/redesign-main @ e42de114），commit 后推送。

## 1. 逐项证据（ACC §4.1~4.11）

### 4.1 改动范围 —— 通过

- `git diff --name-only base..head` 共 37 文件，逐一套 allowedFiles 模式比对：
  - 20 × `RobWork/doc/industrial-robot-design/traceability/**`（builds/wp14-t02/ 留痕 19 件＋dependency-graph.json）
  - 14 × `…/industrialrobot/requirements/**`（CMakeLists＋三公共头＋三实现＋4 测试＋2 契约测试）
  - 1 × `…/industrialrobot/CMakeLists.txt`、1 × `…/cmake/ird_gates_whitelist.cmake`、1 × `units/requirements.md`——均显式列入 allowedFiles。
- **越界文件＝0**；forbiddenFiles（REQUIREMENTS.md／ARCHITECTURE.md）零触碰（大小写精确 grep 计 0）。
- SA-02：diff 无 `patches/` 文件、无框架源码（RobWork/RobWorkSim/RobWorkStudio 框架区）改动。

### 4.2 可复现构建 —— 通过（双模式，验收者亲手重配重跑）

- 集成模式（全新构建树 `D:/acc_tmp/wp14t02/build-int`，源＝wtA）：
  - `cmake -S wtA/RobWork -B build-int -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON`（补 vcpkg toolchain＋Qt prefix，见 §0）→ **exit 0**；缓存 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON` 已 grep 确认。
  - `cmake --build build-int --config Release --target sdurws_ird_requirements` → **exit 0**，产物 `sdurws_ird_requirements.lib`；日志仅有的 3 处 "error" 字样均为 `Errors.cpp` 文件名（逐行核对）。
  - 两测试目标构建 exit 0（`sdurws_ird_requirements_test.exe`／`…_contract_test.exe` 产出）。
- 独立冒烟模式（全新构建树 `D:/acc_tmp/wp14t02/build-smoke`，源＝wtA 的 industrialrobot 子目录）：
  - configure 带 `-DCMAKE_TOOLCHAIN_FILE=…vcpkg.cmake`；首次未带 Qt prefix 失败（Qt6 find_package），按送验证据 smoke-configure.log 同口径补 `-DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64` 后 **exit 0**。
  - 全量 `cmake --build build-smoke --config Release` → **exit 0**，零 `: error`；三 requirements 目标全产出。
  - 冒烟侧直跑两测试：**18/18＋5/5 全过**（与提交声称一致）。

### 4.3 契约 verify 命令 —— 通过（3/3 逐条亲手执行）

| verify 条目 | 实测 | 结果 |
| --- | --- | --- |
| `cmake -S RobWork -B build …-DRWS_BUILD_INDUSTRIALROBOT=ON` | 全新构建树 configure exit 0（参数补齐见 §0） | ✅ |
| `cmake --build build --config Release --target sdurws_ird_requirements` | exit 0 | ✅ |
| `pwsh -File RobWork/scripts/industrialrobot/validate-docs.ps1` | **PASS (20 units, 12 trace entries, 189 task files)**，exit 0（对 wtA 文档态执行） | ✅ |

- 附带：`validate-task.ps1 -TaskFile <契约>` → **PASS (1 tasks)**。
- 契约 verify 数组缺两测试目标构建行／ctest 行（家族问题，见 F-358）：验收者在全新构建树**自行补建**两测试目标并执行（属验收者侧正常动作，F-300 家族口径），结果 18/18＋5/5。

### 4.4 acceptance 条目逐条核对 —— 通过（5/5 均有对应证据）

| # | acceptance 要点 | 证据 |
| --- | --- | --- |
| 1 | INTERFACE→STATIC（名/别名不变、C++17 显式、零 Qt、五边 PUBLIC） | `requirements/CMakeLists.txt` L53（`add_library(sdurws_ird_requirements STATIC …)`）、L61/L62-65（cxx_std_17 PUBLIC＋CXX_STANDARD 17）、L102-107（五目标 PUBLIC 链接）、L111-113（别名仅集成模式）；目标名/别名未改（上级 CMakeLists diff 只做挂载迁移）。“链 sdurw_math”执行口径＝经 core PUBLIC 传染（io 同款；commit 正文已披露），见 F-362 |
| 1 | 双模式构建零错误＋留痕 | §4.2 实测；两份日志在库 `traceability/builds/wp14-t02/` |
| 2 | 二分结构扫描（ird_gates）零误报零漏报；白名单五边＋runtime 边条件；IRD_EXTRA_EDGE_REFS；dependency-graph ⊆ 方向 | 4.5 节门禁比对：base 58→head 60，新增恰 2 条 SUB（testkit 同型 sanctioned 家族，F-359 转登）；GRAPH/SA02 检查零命中、引擎自测 7 类植入违例全检出＋干净夹具无误报（gates-head.log 实测）。白名单三处（IRD_ALLOWED_UNIT_EDGES／IRD_EXTRA_EDGE_REFS_EDGES／NOTES）各 +5 行一致；dependency-graph.json 追加同 5 边。**runtime 边不登记属实**：三头＋三实现 include 面全扫＝仅 std＋`sdurws/ird/diagnostics/DiagCodes.hpp`（公共头），零 runtime 引用；提交信息已注明（契约要求） |
| 2 | ARCH §3.5 表外新边检查（转 blocked 条件） | 未触发：五边属 §3.5 表“各业务域单元（L4）→ L2/L3 公共接口”许可方向行（ARCHITECTURE.md §3.5，wtA 实测 L228）的实例化，非表外新边 |
| 3 | 三头落位（五 token／三词表／schema 常量／RequirementError 8 值／REQ- 码批次边界） | 五 token 与卡 §4.1 表逐字一致（req-set/req-point-set/req-region-set/req-condition-set/req-plan-set）；ProcessTag 11 值＝§4.3 括注原文序；TemplateKind 6 值＝§7.1；ArrayKind 4 值＝§7.2；五 schema 版本常量＝1（§4.2）；RequirementErrorCode 8 值＝§9.3 @pre（1）＋§9.4 @错误（4）＋§9.5 @错误（3）全量收编（测试 TokenTableCompleteAndDistinct 机械比对）；**批次边界正确**：DiagCodes 工厂恰 1 项（REQ-SCHEMA-UNSUPPORTED，§9.6 T02/T03 行），Errors 映射仅 SchemaVersionUnsupported→该码、其余 7 值 nullopt 不预建——测试 FactoryScopeIsStagedRows 以 `size==1` 钉住 |
| 4 | _test/_contract_test 随文件注册（ird_add_gtest 自持、LABELS ird）；守卫自持；_plugin 不落位无冒充 | `ird_add_gtest` 宏于本单元 CMake 内定义（L186-198）并两次调用；`ctest -C Release -L "^ird$"` 实测 **2/2 Passed**（标签生效）；配置期守卫（L262-286）R-1/R-3/D-06 FATAL_ERROR 随 configure 执行（本次配置通过＝零违例触发）；_plugin 无目标、无冒充实现（BuildGraphContractTest L173-174 以断言钉住“_plugin 不落位／_worker 恒不存在”） |
| 5 | 中文注释规范自查＋ird_gates 零命中 | §4.7 抽查通过；ird_gates head 直跑本单元无新增红线码命中（R-1/R-3/R-4/T-1 与 base 全等） |

### 4.5 测试真实失败能力（变异测试）—— 通过（3 处实质变异，全部真实变红，已还原）

在 wtA 源树变异→集成构建树增量重编 `sdurws_ird_requirements_test`→直跑确认具名用例变红→`git restore` 还原：

| 变异 | 位置 | 变红用例（实测） |
| --- | --- | --- |
| M1 词表 token 篡改：`processTagToken(Inspect)` 返回 `"InspectX"` | src/ObjectTypes.cpp L37 | `ReqObjTypes.ProcessTagVocabularyElevenValuesRoundTrip_WP14T02_ACC3` FAILED（17 passed/1 failed，exit 1） |
| M2 错误码映射篡改：`requirementDiagCode(SchemaVersionUnsupported)` 返回 `nullopt` | src/Errors.cpp L59 | `ReqErrors.DiagCodeMappingStagedRows_WP14T02_ACC3` FAILED（17/1，exit 1） |
| M3 schema 版本常量篡改：`kReqSetSchemaVersion` 1→2 | include/…/ObjectTypes.hpp L95 | `ReqObjTypes.SchemaVersionConstantsAreOne_WP14T02_ACC3` FAILED（17/1，exit 1） |

还原后 `git status --porcelain` 干净；重建＋两测试全量复跑 **18/18＋5/5 全绿**（T14 教训口径：先还原再全量）。三个变异互不相同（词表／映射／常量），证明测试非恒真、对契约面篡改敏感。

### 4.6 架构红线（AGENTS §5 八条）—— 通过

| 红线 | 证据 |
| --- | --- |
| 1. L2 计算内核零 Qt | 产品面（include/＋src/）include 全扫＝零 Qt；/utf-8 下编译零 Qt 符号；BuildRedLineTest.ProductFaceHasZeroQtIncludes 在跑；门禁 R3 命中 base=head=20（全为 ui 存量，零新增） |
| 2. 业务域互链禁止（R-1） | 产品库链接面仅五条登记边（CMake L102-107）；配置期守卫 L265-276 对任何其他 `sdurws_ird_*` 目标 FATAL；门禁 R1 命中 base=head=1（存量，零新增） |
| 3. 跨单元私有头（R-2） | 唯一跨单元 include＝`sdurws/ird/diagnostics/DiagCodes.hpp`（diagnostics `include/` 公共头） |
| 4. 框架零源码修改（SA-02） | 4.1 已核：无 patches 新文件、无框架区改动 |
| 5. 不做名称前缀拼接/剥离（R-4） | 产品面无 "RobWork" 字符串字面量（grep 零命中）；BuildRedLineTest.ProductFaceHasNoRobWorkStringLiterals 在跑；门禁 R4 base=head=1（存量，零新增） |
| 6. 权威唯一（PA-1） | 稳定码注册权威留 diagnostics：本单元仅产出 CodeDescriptor 数据＋注册转发（DiagCodes.cpp L74-76），不私定码值（kReqSchemaUnsupported 唯一书写点，Errors.cpp include DiagCodes.hpp 交叉引用）；诊断文案零私设（titleKey/detailKey 键面） |
| 7. 不可变历史（PA-2） | T02 无修订写路径（无 project 写面消费），不适用即未触碰 |
| 8. 不继承 old/ 历史实现 | diff 无 old/ 文件；实现按卡从零编写（三头形态与 modeling 同构但独立实现） |

### 4.7 注释规范（AGENTS §2）—— 通过

- 公共头 3/3 全查（ObjectTypes.hpp 273 行／Errors.hpp 211 行／DiagCodes.hpp 136 行）：文件头 `@file/@brief`＋设计依据（卡面章节＋需求 ID＋契约条目）＋背景说明齐备；每个类/枚举/函数中文 Doxygen（`@param [in]`／`@return`／异常与失败语义）；§2.5 高危信息逐项在场——线程安全（三头均有声明）、确定性来源（NFR-COR-02 逐函数声明）、所有权（RequirementError“调用方所有”、registerRequirementCodes 的 registry“调用方持有——本函数不接管”）、错误语义（Errors.hpp 文件头“两个层面的码”双层区分＋nullopt 调用方契约）、登记簿纪律（表尾追加）。
- 实现文件 3/3 抽查（ObjectTypes.cpp／Errors.cpp／DiagCodes.cpp）：文件头＋逐语义段中文注释（switch 全枚举无 default 的编译期防线、try 轨“不猜测”、描述符逐字段卡面溯源注释）。
- 测试文件抽查 2（ObjectTypesTest.cpp／DiagCodesTest.cpp）：文件头＋用例中文说明＋IRD_TEST_INFO 需求/AT 追溯字段（ARC-04/CON-01/REQ-07/NFR-DEP-04/ERR-01/NFR-MNT-03 等），断言处中文注释（词表外拒绝逐行语义）。
- 未发现 §2.6 反面示例（无复述式/占位/过期注释）。

### 4.8 文档同步 —— 通过

- 单元卡 `units/requirements.md` v0.2（2026-09-25）随提交落位：卡头“构建落位”行翻新（INTERFACE 占位→真实 STATIC＋五边＋两测试目标＋三头）、§14.6 v0.2 落位登记行（五边、runtime 边未登记原因、三头批次边界、project/io/evidence“仅链接不消费”P-RPT-9 先例）——与实现逐项比对**一致，无语义偏差**。
- `dependency-graph.json` 与白名单五边同步（+5 行，⊆ 方向成立，引擎 GRAPH 检查零命中）。
- 卡 §13 追踪矩阵 NFR-MNT-01/03/07→T02 行本就存在，无需改动；DETAILED-DESIGN 索引同步归治理侧（卡 §14.5 明示）。
- **findings.json 未关闭条目相关性核对（v1.2）**：head 侧登记册共 350 条（最大 F-357），open 288。与本任务直接相关的 open 家族：F-072/F-173/F-221/F-292（测试目标→testkit SUB 存量模式，本任务 +2 使其再扩员，见 F-359）、F-300/F-304/F-310/F-314/F-338/F-344/F-345/F-350/F-351/F-352（契约 verify 缺测试目标构建行，本任务契约同型，见 F-358）、F-007/F-062/F-098/F-099/F-101/F-200/F-254（全新树环境前置——本次验收实际再次踩中 ini 模板＋toolchain＋Qt prefix 三件，已在 §0 留痕）、F-051/F-202（门禁 SUB 语义与白名单头/卡面矛盾——本任务 +2 命中的定性受其影响，非本任务可消账）。无可随本任务顺手消账的条目；P-REQ-8（契约 knownPitfalls）处置成立——DiagCodesTest 以真实 StableCodeRegistry 全过，未遇签名漂移。

### 4.9 commit 质量 —— 通过（含 2 条建议级瑕疵，见 F-360/F-361）

- 四段正文齐备（What/Why/验证/影响面），验证段逐条与留痕及本验收实测比对：集成/冒烟构建零错误 ✅、18/18＋5/5 ✅、base 58/head 60/新增恰 2 SUB ✅（与验收者独立直跑完全一致）、validate-docs/validate-task PASS ✅、GRAPH/SA02/SELF 零命中 ✅、“存量 58 处”✅。
- 瑕疵 1（F-360，建议）：“与 base 侧既有 36 处同型”计数失准——实测 base 侧 testkit 直链 SUB 去重 23 处（全码含 testkit 字样 25 处；base SUB 全量 29 处），送验附件 ird_gates_base.log 同样为 23；无任何口径可得 36。增量结论（+2）不受影响，属 F-041 家族散文计数失准。
- 瑕疵 2（F-361，建议）：标题“[WP-14-T02] requirements 构建落位：…”前缀后为名词短语非动词开头（F-012 家族；与 F-100 在案的 DIAG-T02 b5c9504e 同字面形态）。

### 4.10 留痕完整 —— 通过

- `traceability/builds/wp14-t02/` 共 28 件随提交入库：双模式 configure/build 日志、gtest XML 两份（原样 XML，`tests="18" failures="0"`／`tests="5" failures="0"`，与验收者实测同数）、ird-test-report 两份（summary total 18/5、passed 18/5、decisive=true，与实测一致）、四份测试运行日志、ird_gates base/head 全量日志＋归一化命中清单＋直方图＋比对 md（其去重结论 base 58/head 60/+2 与本验收独立直跑一致）。
- 契约状态翻转（ready→done）与 phase-one-validation.log/resultNote 收尾归编排者（PIPE §4.7；F-201 家族口径），不构成实施侧缺留痕。

### 4.11 偷懒/缩水实现扫描 —— 通过

- ① 标记扫描：diff 全文 `TODO|FIXME|stub|placeholder|未实现|not implemented` 新增行命中 9 处，逐条核对**全部**为留痕日志中引用的他单元既有文件名/既有门禁输出（ui/View3DPlaceholder.cpp、project 的 ConfirmableProjectStubTest.cpp 等，base 时代已存在）；`industrialrobot/requirements/` 源码内命中＝**0**。无豁免诉求。
- ② 空实现检测：三实现文件 8 个函数体全部为实质实现（switch 全枚举表／注册转发／描述符构造），无空体、无恒默认返回的接口函数。
- ③ 逐条证据表：见 §1/4.4——acceptance 5 条均有具名测试/命令输出/文件行号，无一条仅凭“已实现”声明。
- ④ 测试强度：23 用例含机械全表比对＋行数哨兵（防清单裁剪假绿）、真实 StableCodeRegistry 行为（注册/查询/manifest/重复注册拒绝）、运行期源码红线扫描（零 Qt/五边 include 封闭/R-4 字面量）、构建图契约（五边存在性/封闭性/无业务域互链/plugin 不冒充）；对抗敏感性由 4.5 三处变异实证。

## 2. 问题清单

**阻断级：无。**

**建议级（5 条，已按 F-358~F-362 转登 traceability/findings.json，status=open）：**

| 编号 | 摘要 |
| --- | --- |
| F-358 | 契约 verify 数组缺测试目标构建行与 ctest 行（F-300 十例家族新例；note ③“本任务尚无测试文件”在编译时点成立但实现面已含两测试目标——verify 清单未随实现更新；验收者已补建补跑） |
| F-359 | ird_gates 基线 +2 命中（SUB×2：requirements 两测试目标直链 sdurws_ird_testkit）——F-072/F-173/F-221/F-292 同族新实例；testkit.md §2.4 T-1 允许形态 sanctioned，登记册回填归 WP-01-T03 治理面 |
| F-360 | 提交信息与比对附件“既有 36 处同型”散文计数失准（F-041 家族）——实测 base 侧同型命中去重 23 处，附件自身日志可证；+2 增量结论不受影响 |
| F-361 | 提交标题前缀后名词短语开头（F-012 家族，F-100 同字面形态再现） |
| F-362 | acceptance 1“链 sdurw_math——照抄 core/io 形态”执行口径（经 core PUBLIC 传染、io 同款无直连边）仅登记于 commit 正文，单元卡 v0.2 未明示——建议 T03 公共头开始消费 rw::math 前在卡面/DTB §5.4 明确 |

## 3. 裁决

**verdict＝pass**：4.1~4.11 全部通过，无阻断级问题；建议级 5 条转登 findings.json，不阻塞合入。送验对象＝SHA `07d2f6d633768ae924085d38c6e429e2892790a6`（非分支引用）。验收者不执行合入；合入决策归所有者。

## 4. 验收过程操作留痕（可追溯）

1. worktree A（detached @ 07d2f6d6）建立；复制缺失的 gitignored `RobWorkStudio.ini.template.static`。
2. 集成模式全新配置（补 vcpkg toolchain＋Qt prefix）exit 0，缓存 ON 确认；构建 `sdurws_ird_requirements`＋两测试目标 exit 0。
3. 冒烟模式全新配置（补 Qt prefix）exit 0；全量构建 exit 0；两测试直跑 18+5 全过。
4. 契约 verify 3 条逐条执行全过；validate-task 附带 PASS。
5. ird_gates 引擎直跑（cmake -P）：base 临时 worktree（e42de114，用毕即删）58 命中 vs head 60 命中，归一化逐行 diff＝仅 +2 SUB（testkit 同型）；自测通过；比对结论以直方图佐证（与本记录 §1/4.4 同口径）。
6. 变异 M1/M2/M3 → 具名用例三度变红 → git restore 还原 → 全量复跑全绿 → 工作树干净确认。
7. worktree B（acc/WP-14-T02/1 @ origin/redesign-main）内写本记录→commit→push（显式 refspec）→ls-remote 核实 SHA。
