# UI-T02 验收记录（acceptance record）

| 字段 | 值 |
| --- | --- |
| 任务 ID | UI-T02（构建落位：ui 占位转真实库，R-3 例外登记；unit=ui；masterWp=WP-I；interUnit=true；实现任务——全量清单验收） |
| 送验对象（冻结） | 48e614cf19b62bf1ad5859ec1eeea6c286dcf7fd（分支 wp10-t02；diff 范围＝4d827120c5a28b7cbcec9adf7aa089d66688fb54..48e614cf19b62bf1ad5859ec1eeea6c286dcf7fd，state.base..headSha；origin/redesign-main 实测停于 4d827120＝base，range 内含 24be3aba／eb3de24c／1428e352／48e614cf 四个提交） |
| 验收尝试 | attempt 1（分支漂移检测：`git fetch` 后 `origin/wp10-t02` == 48e614cf… 全 40 位逐字一致，零漂移；`acc/UI-T02/1` 分支名此前不存在） |
| 验收环境 | 复现 worktree A＝`git worktree add --detach D:/tmp-acc-ui-t02-a` @ 48e614cf，全新从零（集成 build 树与冒烟 build 树均为本验收新建独立目录，全部无缓存复用）；基线门禁 worktree C＝detached @ 4d827120（ird_gates 基线复跑用）；记录回传 worktree B＝`acc/UI-T02/1` 基于 origin/redesign-main@4d827120；pwsh 7＋MSVC 2022 x64（VS 17.12，cl 19.42）＋vcpkg 经典模式（toolchain 绝对路径指向主仓库 vcpkg——worktree 天然不含）；集成配置参数＝主树缓存实证口径（F-098/F-099 既有登记，非实施缺陷）：`-G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=<主仓>vcpkg/.../vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64 -DCMAKE_CXX_FLAGS="-DNOMINMAX -DWIN32_LEAN_AND_MEAN -DWIN32"`，并自主树复制 `RobWorkStudio.ini.template.static`＋`RobWorkStudio.ini.shared.in` 两件 git 未追踪本地辅助件（bin/ 整目录被 .gitignore `[Bb]in/` 吞——F-099 实证同款，本验收再次亲历）；验收全程只消费产物、契约、设计文档与在库留痕，未读取实施者过程叙述；结论全部出自验收者亲手重跑 |
| verdict | **pass**（4.1～4.11 全部通过；无阻断级问题；建议级 1 条转登 findings.json F-228） |
| 验收日期 | 2026-09-19 |

## 4.1 改动范围 —— 通过

- `git diff --name-status 4d827120..48e614cf` 实证 **38 个变更文件**（2 M 工业产品 CMake＋35 A＋1 M traceability）：①`industrialrobot/CMakeLists.txt`（M）②`industrialrobot/testkit/CMakeLists.txt`（M）③～⑩`industrialrobot/ui/**` 八件（CMakeLists＋src/Ui.cpp＋test/contract_test/gui_test 各自 .cpp 与 TestMainReport.cpp）⑪～㉟`traceability/builds/wp10-t02/` 25 件留痕 ㊱`traceability/findings.json`（M，F-227）㊲`traceability/wp10-t02-gate-registrations.md` ㊳㊴`units/ui.md`（M，v0.3）＋`units/testkit.md`（M，v1.1）。
- **allowedFiles 逐文件比对**：①②命中契约 allowedFiles 第 2/3 条（父 CMakeLists、testkit/CMakeLists.txt——UI-T02 note 明文补登）；③～⑩命中 `ui/**`；⑪～㊲命中 `traceability/**`；㊳㊴命中 `units/ui.md`/`units/testkit.md`——**38/38 全部在册，零越界**。
- forbiddenFiles（REQUIREMENTS.md／ARCHITECTURE.md）**零触碰**（diff 无该两文件）。
- 无未登记框架源码改动（SA-02）：diff 零 `RobWork/RobWork/`、RobWorkSim、RobWorkStudio 其他 src 路径；`patches/` 零新文件（`git diff --name-only | grep patches` 零输出）。
- **逐提交核验**（四提交各零越界）：24be3aba＝①～⑩（实现本体）、eb3de24c＝㊱部分＋㊱gates 证据＋㊱findings＋㊲（门禁登记）、1428e352＝㊳㊴（单元卡同步）、48e614cf＝builds/wp10-t02 余下留痕（验证留痕）。

## 4.2 可复现构建 —— 通过（两模式全新树从零，零错误）

- **集成模式**（worktree A 内新建 `build/`，重配后缓存确认 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`）：`cmake --build build --config Release` **全量构建 exit 0、": error"/": fatal error" 计数 0**（含 RobWork 全库→RobWorkSim→RobWorkStudio→industrialrobot 全单元→ui 三测试目标全新编译；产物含 `build/RobWorkStudio/bin/Release/sdurws_ird_ui{,_test,_contract_test,_gui_test}.exe|.lib`）。首配曾因 ini 模板缺件失败（F-099 环境链，补两件辅助件后从零重配成功——非送验对象缺陷，见环境段）。
- **独立冒烟模式**（独立目录 `D:/tmp-acc-ui-t02-acc-smoke`，源＝worktree A 的 industrialrobot，`-DCMAKE_TOOLCHAIN_FILE=<主仓>vcpkg`＋Qt prefix——AGENTS §4.1 口径）：配置＋全量构建 **exit 0、错误计数 0**；`ui/Release/` 下 `sdurws_ird_ui.lib`＋三测试 exe＋运行期 DLL（Qt6Core/Gui/Widgets、gtest.dll）全部就位（POST_BUILD 拷贝生效）。
- 本任务 diff 零框架触碰（4.1 实证），框架既有缺陷（F-008 系）不归本任务。

## 4.3 契约 verify 命令 —— 通过（三条逐条亲手执行）

在 worktree A 根目录按契约字面逐条独立执行，**三条全部 exit 0**：

1. `cmake --build build --config Release --target sdurws_ird_ui_test` → exit 0
2. `cmake --build build --config Release --target sdurws_ird_ui_contract_test` → exit 0
3. `cmake --build build --config Release --target sdurws_ird_ui_gui_test` → exit 0

附加治理脚本（worktree A 上下文亲手复跑，与送验留痕一致）：`validate-task.ps1 -TaskFile tasks/foundation/UI-T02.json` → **PASS (1 tasks)**；`validate-docs.ps1` → **PASS (20 units, 12 trace entries, 150 task files)**——与在库 `builds/wp10-t02/validate-task.log`/`validate-docs.log` 逐字一致。

## 4.4 acceptance 条目 —— 通过（五条逐项证据）

| # | 契约条目 | 证据（验收者亲手复现＋磁盘实测） |
| --- | --- | --- |
| 1 | 双模式构建零错误；sdurws_ird_ui INTERFACE 占位升级 STATIC（C++17、PUBLIC 链 core＋diagnostics＋Qt Core/Gui/Widgets——ui.md §3.1），目标名不变；新建 ird/ui/CMakeLists.txt 并在父 CMakeLists 注册 | 双模式零错误见 4.2（两树全新从零亲跑）。`ui/CMakeLists.txt:62`＝`add_library(sdurws_ird_ui STATIC src/Ui.cpp)`（STATIC＋锚点翻译单元）；`:68`/`:69-72`＝`target_compile_features(PUBLIC cxx_std_17)`＋`CXX_STANDARD 17 REQUIRED OFF_EXTENSIONS`（不用 C++20）；`:101-106`＝PUBLIC 链 `sdurws_ird_core`＋`sdurws_ird_diagnostics`＋`Qt6::Core/Gui/Widgets` 字面书写；`:110-112`＝集成模式别名 `RWS::ird::ui`（目标名/别名不变）；父 `CMakeLists.txt` diff 实证＝`IRD_MODULES` 移出 ui（占位循环退位，PlacementStaticRegistration_UI_BUILD 用例断言 ` ui` 独立词零命中）＋`add_subdirectory(ui)` 挂载于 execution 之后（core/diagnostics 在前——次序约束成立）；:114-154 配置期守卫（链接面白名单 FATAL＋Qt 三件套齐备断言）在配置真机生效（两模式配置通过＝守卫全绿）。**DTB §2.11 WP-10-T02 输出列「ird/ui/CMakeLists.txt（新）」兑现** |
| 2 | 红线门禁含 ui 例外登记文本（R-3 注明「仅 ui」）；ird_gates/静态扫描零命中：Widgets 范围、QShortcut 作用域、RobWork 名称前缀拼接/剥离（R-4） | **登记文本两处在位（亲读）**：`cmake/ird_gates_whitelist.cmake:84`＝"既有登记（DTB §4.5）：ui 单元界面目标（Widgets 唯一例外，WP-10-T02 生效）"；DTB §4.5:656＝R-3 行"预登记（WP-10-T02 生效）"；用例 `QtLinkageLiteralAndExceptionText_UI_BUILD` 断言两文本在位并通过（亲跑）。**三项静态扫描零命中（ui 产品面）**：运行期常驻自证用例三件亲跑全过——`NoRobWorkPrefixLiteral_R4_UI_BUILD`（注释剥离后 `"…RobWork…"` 字面量零命中）、`NoGlobalShortcutPrivatization_SA16_UI_BUILD`（QShortcut＋WindowShortcut/ApplicationShortcut 同现零命中）、`NoCrossUnitInclude_O31_UI_BUILD`（产品面 include 面仅 core/diagnostics/ui）；门禁第 4b 步（产品面 Qt 头扫描）对本任务 ui/src 零命中（head 门禁 27 处命中中 R3 全部为链接面、无 ui 头包含项——命中清单逐条亲读归因）。**ird_gates 命中集合独立复现**：基线（worktree C@4d827120，`cmake -DIRD_ROOT=… -DIRD_ENABLE_GIT=OFF -P cmake/ird_gates.cmake`）＝**命中 19 项**（SUB×13＋LIB×4＋T1×1＋R4×1，与送验 gates-hits-base-skel.txt 一致）；head 侧（worktree A `--target ird_gates`）＝**命中 27 项**，逐条骨架与送验 after 骨架一致——**新增恰 8 处（3×R3＝sdurws_ird_ui→Qt6::Core/Gui/Widgets；3×SUB＝ui 三测试目标→testkit；1×SUB＋1×T1＝sdurws_ird_ui_gui_test 被 ird_classify_target 判 other 形态致链路面误查），既有 19 处零新增零移除**。8 处命中全部为契约 acceptance 1/3 明文要求的合法形态（机器面 `IRD_R3_EXCEPTION_TARGETS` 空册＋`_gui_test` 词表缺失滞后于人工登记册——DTB §4.5 已预登记），已由实施侧 F-227（suggestion，owner=WP-01-T03）＋登记提交件 `traceability/wp10-t02-gate-registrations.md`（只提交清单、不代行 cmake/ 改动——两文件均不在 allowedFiles，EX-T10 F-221 先例同口径）闭环登记。**判定：acceptance 2 的「零命中」限定语（Widgets 范围/QShortcut/R-4）在 ui 交付面成立；门禁目标层 8 处命中非红线实质违反、非粉饰（送验侧主动归因登记而非隐匿）** |
| 3 | ui 测试目标与 ird_gui 标签就位（ui.md §3.1/§12.1）：三目标按 DTB §5.5 注册（GTest CONFIG REQUIRED）；实现任务未落地前允许最小构建冒烟用例，不在骨架预建占位实现 | `ui/CMakeLists.txt:38`＝`find_package(GTest CONFIG REQUIRED)`（失败即停）；`:191/237/277`＝三 `add_executable`（test＝TestMainReport＋BuildRedLineTest 6 用例；contract_test＝TestMainReport＋LinkageContractTest 3 用例；gui_test＝TestMainReport＋BuildPlacementTest 2 用例——11 条最小冒烟用例，契约明文允许，未预建业务占位实现）；`:171-183`＝ird_add_gtest 宏（add_test＋`<target>_report` XML 报告目标，EXCLUDE_FROM_ALL）；`:315-318`＝`set_tests_properties … LABELS "ird"`（test＋contract_test）／`LABELS "ird_gui"`（gui_test）。**ctest 标签过滤亲跑复现**：`ctest -C Release -L "^ird$"`＝2 tests 100% passed；`-L "^ird_gui$"`＝1 test passed（集成/冒烟两模式各跑一遍，与送验 ctest-ird.log/ctest-ird_gui.log 一致）。三测试 main 自持（自有 main＋`installTestRecordListener`——DTB §5.5 形态）：ui_test/contract_test 构造 QCoreApplication（模型测试豁免形态）、gui_test 零 Qt 应用对象构造（§12.2 第 8 条钉住，`GuiMainNoGuiStartup_UI_GUIBUILD` 用例亲跑通过） |
| 4 | sdurws_ird_testkit_qt 启用登记：触发已成立；testkit CMake 登记单列目标（Qt Core 起、只被 GUI/模型测试目标链接、禁入生产计算库依赖），同步 testkit.md §10.1 启用表；首消费者理由归 UI-T13 契约登记 | `testkit/CMakeLists.txt` diff 实证新增块：`find_package(Qt6 COMPONENTS Core REQUIRED)`＋`add_library(sdurws_ird_testkit_qt INTERFACE)`＋`INTERFACE Qt6::Core`（Qt Core 起、按需 Widgets 注记在位）＋公共头根传播＋**配置期守卫**（零 ird 单元链接边 FATAL——含本体 testkit；零 gtest）。**消费者形态**：唯一链接者＝`sdurws_ird_ui_gui_test`（ui/CMakeLists.txt:290-294；LinkageContractTest `ThreeTestTargetsAndLabels_UI_CTR` 断言 gui 链 testkit_qt——亲跑通过）；testkit 本体链接面未触碰（T-2 守卫原文保留——diff 仅追加块）。**文档同步**：testkit.md §10.1 启用表该行更新为"触发已成立（2026-09-19，UI-T02 建立 ui 三测试目标）——**已随 WP-10-T02 启用落位**"（划线保留原触发条件），§9 增 2026-09-19 增量修订注记，§12 v1.1 行登记落位形态（INTERFACE 载体＋设施随 UI-T13/T14、不预建占位）；首消费者理由归 UI-T13 契约＝ui.md §12.1 原文"理由已在 UI-T13 任务卡登记"在位（本任务不代登——卡行原文兑现）。INTERFACE 而非 STATIC 的理由（启用期无设施源码＋allowedFiles 不含 testkit/src/**）在 CMake 注释与 v1.1 行明文登记——非空实现偷懒（4.11②豁免依据） |
| 5 | O-31 处置：交付面仅含 ARCH §3.5 表内边（ui→core、ui→diagnostics）与 R-3 例外登记，零指向 project/evidence/execution/policy/runtime 的链接或 include；P-UI-5 按 ui.md CF-1 落位（注册表类型归 ui 自有头），ui→core 边照常建立 | **亲读 diff 全量链接声明**：`sdurws_ird_ui` 链接块恰＝core＋diagnostics＋Qt 三件套；三测试目标链接块＝sdurws_ird_ui＋testkit（＋gui 的 testkit_qt）＋GTest——**零 project/evidence/execution/policy/runtime 出现**（LinkageContractTest `ArchEdgesCoreDiagnosticsOnly_UI_CTR` 以 17 个越界目标名逐反证——亲跑通过）。**include 面**：产品面仅 src/Ui.cpp（零 include——文件头声明落位期刻意形态，亲读核实）＋include/ 仅 README 保留位；`NoCrossUnitInclude_O31_UI_BUILD` 运行期扫描亲跑通过。**4.5 注入反证**：向 Ui.cpp 注入 `#include <sdurws/ird/project/Project.hpp>` 即双红（MSVC 编译失败＋运行期扫描用例 FAILED"O-31 越界 include（ui→project 非表内边）"）——边界真实有效。白名单 `ui->core`/`ui->diagnostics` 两行既有在位（ird_gates_whitelist.cmake:42-43）。P-UI-5：本任务零接口零实现（注册表类型归 ui 自有头——CF-1 口径在 CMakeLists.txt:27-31 注释与 ui.md v0.3⑤ 登记），ui→core 边已建立（链接行＋门禁 SUB 白名单），不私改 ARCH 文本（4.1 实证零 ARCH 触碰） |

## 4.5 测试真实失败能力 —— 通过（两处断言注入即红、还原复绿）

在 worktree A（本验收沙箱，实施分支零触碰）注入两处应失败输入，重建后亲跑：

1. **O-31 include 边界断言**：`ui/src/Ui.cpp` 注入 `#include <sdurws/ird/project/Project.hpp>` → ①重建编译失败（该头不存在）；②`sdurws_ird_ui_test` 运行期 **FAILED：`UiBuild.NoCrossUnitInclude_O31_UI_BUILD`——"O-31 越界 include（ui→project 非表内边，acceptance 5 禁止）"**（运行期源码扫描与编译期独立成红，双通道敏感）。
2. **GUI 不启动纪律断言**：`ui/gui_test/TestMainReport.cpp` main 内注入 `const char* injected = "QApplication";` → `sdurws_ird_ui_gui_test` **FAILED：`UiGuiPlacement.GuiMainNoGuiStartup_UI_GUIBUILD`——"落位期 gui_test main 出现 QApplication（违反 ui.md §12.2 第 8 条）"**。

还原两处注入后 `git status` 干净（工作树回到 48e614cf 内容）、重建 exit 0、两用例复绿 exit 0。**测试非恒真**：断言对输入真实敏感，非"只编译通过"。

## 4.6 架构红线 —— 通过（AGENTS §5 八条逐条）

1. **L2 计算内核零 Qt**：diff 零触碰 core/evidence/policy/runtime 及业务域源码；ui 为唯一例外单元（R-3 登记兑现——4.4#2）；testkit_qt 为测试设施目标（INTERFACE、无源码、Qt6::Core，testkit.md §3.5 登记），且本体 testkit 零变更、守卫拦截传递（亲读 diff＋守卫文本）。
2. **业务域互链禁止（R-1）**：ui 链接面仅 core/diagnostics（配置期 FATAL 守卫＋LinkageContractTest 17 目标反证＋亲读三重实证）。
3. **跨单元私有头禁止（R-2）**：ui 产品面零 include（4.4#5）；测试面仅 include testkit 公共头（`<sdurws/ird/testkit/…>`——公共头路径）与 gtest/Qt。
4. **框架零源码修改（SA-02）**：diff 零框架路径、patches/ 零新文件（4.1）。
5. **不做名称前缀拼接/剥离（R-4）**：ui 产品面 RobWork 字面量零命中（门禁 R4 仅既有 project 一处＝基线既有归属；用例 NoRobWorkPrefixLiteral 亲跑通过）；门禁 19→27 集合中 R4 零新增。
6. **权威唯一（PA-1）**：本任务未代行他单元语义——门禁数据缺口走登记提交件（不私改 cmake/）；QShortcut 全局注册语义归 GlobalShortcutRegistry 仅以扫描用例自证、无实现私裁；O-31/P-UI-5 均维持登记未决、消账权留所有者（ui.md v0.3⑤"不构成消账"明文）。
7. **不可变历史（PA-2）**：四提交均为追加；`git log` 4 提交线性、无改写（origin/wp10-t02 == headSha 亦证无强推）。
8. **不继承 old/ 历史**：diff 零 old/ 引用；实现全部按契约与单元卡新写（构建落位形态与 CORE-T01/EX-T01 等在库先例同构、非拷贝历史实现）。

## 4.7 注释规范 —— 通过（新增 7 个源文件全量通读；公共头零——保留位在位）

- **公共头**：ui 单元本任务未产出公共头（`include/sdurws/ird/ui/` 仅 README.md 保留位——§3.3 头随 UI-T03+ 落地，`SourceTreeReachable_UI_BUILD` 用例断言保留位存在），"公共头全查"空集满足；保留位文件在位亲证。
- **实现文件**：`src/Ui.cpp` 全文通读——文件头（设计依据三源＋背景说明解释"为何存在空翻译单元"＋落位期形态约束两条＋线程模型段）；命名空间锚点段注释说明防优化语义。
- **测试文件 6 件全读**（TestMainReport×3＋BuildRedLineTest＋LinkageContractTest＋BuildPlacementTest）：每件文件头含设计依据（契约/单元卡条目逐条）、背景说明（为何 main 自持、为何不构造 QApplication 等"为什么"）、线程模型段；类/函数级 Doxygen 中文齐备（unitRoot/readFile/stripComments/extractLinkBlock/ird_add_gtest 均有 @brief 语义说明＋边界注记——如"读失败显性失败不留假阳性通道"、链接块截取的词边界防误吸说明）；§2.5 高危信息：所有权（静态监听器"/gtest listeners 不接管所有权——官方 recommended"）、线程约束（单线程入口/QCoreApplication 同线程要求）、错误语义（FATAL 守卫按红线编号分类）、确定性来源（collectCppFiles 排序＝NFR-COR-02、IRD_UI_UNIT_ROOT lexically_normal）——物理单位/坐标系/rad 在本任务交付面无物理量，不适用（注释规范按交付面适用项核查）。
- CMakeLists.txt 关键步骤（目标注册、依赖红线守卫、门禁交互）中文注释块齐备且与 AGENTS §2.7 要求一致（亲读全文 318 行）。

## 4.8 文档同步 —— 通过

- **实现与任务卡偏差**：零语义偏差。落位形态细节（testkit_qt＝INTERFACE 载体、ctest 标签正则锚定 `^ird$`/`^ird_gui$`、contract_test 链接面 project/execution 延后）均已在单元卡增量修订与源码注释中显式登记——其中 contract_test 链接面差为契约 acceptance 5（O-31 边界）明文要求（不私裁增边），ui.md §3.1 行为最终形态描述，见本记录"发现的问题"G-1（建议级，已转登 F-228）。
- **任务状态/追踪同步**：ui.md v0.3（§16.7 变更行：§3.1 落地/R-3 承载/testkit_qt 启用/落位形态/O-31-P-UI-5 处置五点）与 testkit.md v1.1（§10.1 启用表＋§9 注记＋§12 变更行）亲读核实；contract-compile-log.md UI-T02 行（ready、O-31/P-UI-5、放行依据）在位；validate-docs/validate-task 双 PASS（4.3）。
- **findings.json 未关闭条目相关性核对**（197 条 open 逐族过筛）：
  - **F-227**（本任务实施段自登，suggestion，owner=WP-01-T03）：本任务门禁 8 处命中归因与回填请求——与本任务直接相关，登记完备（基线/变更后 diff、逐条归因表、回填后预期），验收者独立复现证实其全部事实主张（19/27、命中集逐条一致）。**状态处置正确：open，随 WP-01-T03 收口，本任务无消账义务**。
  - **F-115/F-173/F-221**（同家族：既有测试目标→testkit SUB 机器面滞后，owner=WP-01-T03）：本任务新增 3 个同型实例（ui 三测试目标→testkit），已并入 F-227 的回填清单与登记提交件——同家族合并路径正确，无静默积压。
  - **F-098/F-099**（全新集成树配置口径文档缺口，owner=AGENTS.md 维护方）：本验收再次亲历（ini 模板缺件＋toolchain/Qt/CXXFLAGS 参数），与本任务零代码关联、AGENTS.md 不在 allowedFiles——不可随本任务消账，维持 open 正确（本记录环境段已再留实证）。
  - **F-012 标题家族（F-100/F-226 等）**：本任务四提交标题前缀后均为动词开头（实现/登记/同步/登记）——**零再现**。
  - 其余 open 条目（RT/PRJ/POL/EX 单元内部事项、CI 扩展、dataset 等）与本任务交付面（ui 构建落位）无关联，无需本任务处置。

## 4.9 commit 质量 —— 通过（四提交逐份核对）

| 提交 | 标题（前缀后动词开头） | 四段正文 | 验证结论与留痕一致性 |
| --- | --- | --- | --- |
| 24be3aba | `[WP-10-T02] 实现构建落位：…`（动词"实现"） | What/Why/验证/影响面＋Acc-Covered | 双模式构建/11 用例/门禁命中归因——本验收全部亲手复现为真 |
| eb3de24c | `[WP-10-T02] 登记门禁命中归因与 R-3 机器面回填提交件（F-227）`（动词"登记"） | 四段齐备 | "命中 27＝基线 19＋8、既有零新增零移除"独立复现一致 |
| 1428e352 | `[WP-10-T02] 同步单元卡：…`（动词"同步"） | 四段齐备 | validate-docs/validate-task PASS 亲跑一致 |
| 48e614cf | `[WP-10-T02] 登记验证留痕：…`（动词"登记"） | 四段齐备 | 留痕文件 25 件在库亲验，ird-test-report decisive=true 与 XML 无失败一致 |

无粉饰：门禁 8 处命中在提交信息中**主动如实披露**并登记（eb3de24c 正文"不粉饰命中"），与 ACC 对抗式核查口径相容。

## 4.10 留痕完整 —— 通过

- 在库留痕（送验提交 48e614cf 内 `traceability/builds/wp10-t02/` 25 件，逐件在位亲验）：集成构建日志（407 行）＋冒烟 configure/build 日志；三测试目标各自 ird-test-report.json（ird-test-report/1 schema，summary decisive=true：6＋3＋2 全 passed、零 failed/skipped/notRun——ui_test 报告头亲读）＋直接运行日志；gtest XML 三份（gui_test XML 亲读：tests=2 failures=0）；ctest 标签过滤日志两份；validate-task/validate-docs 日志；门禁证据六件（基线/变更后原始行＋骨架＋两轮全量日志＋归因 diff）。
- **验收者自跑留痕**（本记录证据，未入库——临时目录随本 worktree 生命周期说明于环境段）：集成/冒烟两模式三测试目标 gtest XML 六份＋运行日志、ird_gates 基线/变更后两轮原始日志、注入/还原构建与运行日志——关键结论已逐条摘录于本记录各节。
- `phase-one-validation.log`/resultNote：本任务留痕载体＝builds/wp10-t02/ 目录（EX-T10 等近期任务同款先例，未更新 phase-one-validation.log 不构成缺口——该文件属阶段一验收汇总面，非逐任务义务）。

## 4.11 偷懒/缩水实现扫描 —— 通过

1. **标记扫描**：`git diff 4d827120..48e614cf` 全文 `TODO|FIXME|stub|placeholder|未实现|not implemented`（含中文"未实现"）**零命中**（grep 计数 0）。契约显式"触发式延后"豁免逐条列出依据：①src/Ui.cpp 零语义锚点翻译单元＝NFR-MNT-04"不在骨架预建占位"＋CMake 零源码 STATIC 不可生成的既定先例形态（CORE-T01/PRJ-T01/EX-T01 同款，Ui.cpp 文件头与父 CMakeLists 注释双登记）；②testkit_qt 设施本体随首消费者 UI-T13/T14 落地＝testkit.md §10.1 启用表"设施头/实现随首消费者落地后升级 STATIC"原文＋v1.1 行；③gui_test 不启动 QApplication＝ui.md §12.2 第 8 条明文＋钉住用例。三处豁免均有契约/单元卡原文依据，非偷懒。
2. **空实现检测**：公共接口函数本任务为零（无公共头）——"空实现且契约未声明"零命中；上列三处空/最小形态均契约已声明（4.11①豁免依据）。
3. **acceptance 逐条证据表**：见 4.4 表格——五条每条给出具名测试/亲跑命令输出/文件行号，无一条仅凭"已实现"声明。
4. **测试强度**：11 用例中 6 条红线扫描用例含真实解析逻辑（include 指令行判别、注释剥离器、正则字面量扫描、链接块括号截取＋词边界处理）而非恒真断言；3 条构建图契约以 17 目标反证枚举；2 条形态钉住断言零命中期望。**4.5 变异注入两例即红**实证敏感度；读失败通道显性失败（ADD_FAILURE）防"读不到＝零命中"假阳性——设计对抗性到位。

## 发现的问题（分级清单）

- **阻断级：无。**
- **建议级 G-1（已转登 findings.json F-228）**：ui.md §3.1 `sdurws_ird_ui_contract_test` 行链接列"被测目标＋core/project/execution/diagnostics＋testkit＋GTest"与 UI-T02 落位形态（直链＝sdurws_ird_ui＋testkit＋GTest；core/diagnostics 经 sdurws_ird_ui PUBLIC 链接传染可达；project/execution 按 O-31 边界延后至形态裁决/UI-T14）存在"最终形态 vs 落位形态"措辞差。落位本身为契约 acceptance 5 明文要求（合法、已登记）；建议 O-31 裁决或 UI-T14 契约测试套件落地时同步该行措辞（区分"直链"与"传染可达"），避免后续验收者误判偏差。不阻塞合入。

## verdict

**pass** —— 4.1～4.11 全部通过，无阻断级问题；建议级 1 条（G-1→F-228）随本记录转登 findings.json，不阻塞合入。合入决策归所有者（本验收者不合入、不改实施产物、未向 wp10-t02 推送任何提交）。
