# EX-T01 验收记录（attempt 2）

| 字段 | 值 |
| --- | --- |
| 任务 | EX-T01（构建落位：execution 占位转真实库；≙WP-08-T02，分支 wp08-t01） |
| 契约 | tasks/foundation/EX-T01.json（status=ready；DOC-T12 放行版） |
| 送验对象（冻结） | **a83a5ce476709c07de3d1a61f4580787f2c6ab43**（40 位，全程按 SHA 验收） |
| diff 范围 | 2673b21b7091c7c246b3542a369db5f616a2d960..a83a5ce476709c07de3d1a61f4580787f2c6ab43（2 个提交：b0861ec2 落位＋a83a5ce4 补交留痕） |
| 验收尝试 | attempt 2（attempt 1＝acc/EX-T01/1，verdict fail——唯一阻断项：九件验证日志声称入库而 0 件在库；本 attempt 按 ACC §5 **全量重跑**，不做增量验收） |
| verdict | **pass**（4.1~4.11 全部通过，零阻断级；建议级 2 项——F-206 镜像转登＋F-207 新登） |
| 验收日期 | 2026-09-18 |
| 记录回传 | 分支 **acc/EX-T01/2**（基于 origin/redesign-main＝840688a9340b5dba6dff557359686abf2f88a63b），路径 traceability/acceptance/EX-T01-20260918-r2.md，记录提交 SHA 由验收者向编排者回传三元组 |

## 分支漂移检查 —— 通过

- `git ls-remote origin`：`refs/heads/wp08-t01` = a83a5ce476709c07de3d1a61f4580787f2c6ab43 ＝ 送验 SHA，**零漂移**（ACC §3）。

## 独立性与环境（ACC §2/§3）

- **全新上下文**：本会话只读代码、契约、单元卡、在库留痕与 attempt 1 验收记录（后者仅用于①F-007 环境前置复现方法——IO-T05/T06/T07 既有先例、②F-206 条目镜像保真），未消费实施者过程叙述；一切结论以下述亲手执行为准。
- **复现现场**：`git worktree add --detach D:/10_Source_Repos/21_robot/acc-ex-t01-r2/wtA a83a5ce4…`（detached @ SHA）。集成树 wtA/build 为本次从零新建：补齐 F-007/F-200 环境前置三件（vcpkg toolchain 绝对路径＋`CMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`＋自主仓补拷 `RobWorkStudio.ini.template.static`/`RobWorkStudio.ini.shared.in`——bin/ 被 .gitignore 排除，fresh 树缺失，验收会话自备、未入版本控制）后 configure exit 0，缓存 grep 实测 **`RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`**。冒烟树 `acc-ex-t01-r2/smoke` 独立缓存另建。操作失误披露：首配曾因未带 toolchain 失败于 Boost NOTFOUND（缓存残留致二次配置复现失败），删树重配后一次通过——纯验收侧环境操作，与送验对象无关。
- **记录回传现场**：`git worktree add -b acc/EX-T01/2 D:/10_Source_Repos/21_robot/acc-ex-t01-r2/wtB origin/redesign-main`。
- **复现优先于采信**：双模式配置/构建、四跑测试、契约 verify 命令、ird_gates 复跑、双探针失效注入、双验证脚本均为验收者亲手执行；在库留痕数值仅作交叉比对。

## 4.1 改动范围 —— 通过

`git diff --name-status 2673b21b..a83a5ce4` 实证 **21 个变更文件**（2 M＋19 A），逐个落在契约 allowedFiles 内：

| 文件 | allowedFiles 依据 |
| --- | --- |
| industrialrobot/CMakeLists.txt（M）、execution/CMakeLists.txt（A）、execution/contract_test/LinkageContractTest.cpp（A）、execution/include/sdurws/ird/execution/README.md（M）、execution/src/Execution.cpp（A）、execution/test/BuildRedLineTest.cpp（A） | `industrialrobot/**` 显式列入 |
| units/execution.md（M） | 单元卡显式列入 |
| traceability/builds/wp08-t01/ 下 14 件（A） | `traceability/**` 显式列入 |

- forbiddenFiles（REQUIREMENTS.md/ARCHITECTURE.md）零触碰——diff 文件清单零命中；
- 框架源码零改动：diff 中 `industrialrobot/` 与 `doc/industrial-robot-design/` 之外零文件（`NO-OUT-OF-SCOPE-FILES` 实测）；patches/ 零新文件（SA-02 无登记义务）。

## 4.2 可复现构建 —— 通过

- **集成模式**（wtA/build 全新配置）：configure exit 0、零 CMake Error（integration-configure.log，`Configuring done`＋`Generating done`）；缓存实测 `:BOOL=ON`。`cmake --build build --config Release --target sdurws_ird_execution_test` **exit 0**、error C/LNK/fatal 计数 **0**（integration-build-test.log）；`--target sdurws_ird_execution_contract_test` 同样 **exit 0 计数 0**（integration-build-contract.log）；产物 `build/RobWorkStudio/bin/Release/sdurws_ird_execution_test.exe`、`…_contract_test.exe`、`build/…/libs/Release/sdurws_ird_execution.lib` 齐备。
- **独立冒烟模式**（acc-ex-t01-r2/smoke 独立缓存）：`cmake -S industrialrobot -B <smoke> -DCMAKE_TOOLCHAIN_FILE=<主仓>/vcpkg/…/vcpkg.cmake` configure **exit 0**；`cmake --build --config Release` **exit 0**、错误计数 **0**（smoke-configure.log/smoke-build.log）；产物 `execution/Release/` 下 `sdurws_ird_execution.lib`＋两测试 exe 齐备。
- **测试亲手执行**（非引用留痕）：集成 7/7＋5/5 PASSED exit 0；冒烟 7/7＋5/5 PASSED exit 0；本人产出 4 份 gtest XML：`tests="7"/"5"/"7"/"5"、failures="0"、errors="0"`。
- **ird_gates 复跑**：`--target ird_gates` exit 1＝**17 命中**（my-gates.log「命中 17 项」），与 wp11-t07 基线同集（evidence/policy/project/runtime/diagnostics/io 各契约目标 testkit 链接、io libzip/expat、testdata_lint、project R4 既有辖区）；日志中 `ird_execution` 字样 **0 命中**——零新增、零 execution 项。
- **validate-task/validate-docs 亲手复跑**：`validate-task: PASS (1 tasks)` exit 0；`validate-docs: PASS (20 units, 12 trace entries, 150 task files)` exit 0。

## 4.3 契约 verify 命令 —— 通过

契约 `verify` 数组仅一条：`cmake --build build --config Release --target sdurws_ird_execution_test`——验收者在 wtA 根**逐字亲手执行**，exit 0（见 4.2 集成模式）。无其他条目。

## 4.4 acceptance 条目逐项证据 —— 通过

| # | 契约条目 | 验收者亲手证据 |
| --- | --- | --- |
| 1 | 双模式构建零错误；INTERFACE 占位升级 STATIC（C++17、PUBLIC 链 core/evidence/project——§3.4；diagnostics 边经注入不落链接——P-EX-8）；EX-BLD-1 构建项通过 | 两模式 configure/build exit 0 零 error（4.2）；execution/CMakeLists.txt 实读：`add_library(sdurws_ird_execution STATIC src/Execution.cpp)`＋`CXX_STANDARD 17/CXX_STANDARD_REQUIRED ON/cxx_std_17 PUBLIC`＋`target_link_libraries(… PUBLIC sdurws_ird_core sdurws_ird_evidence sdurws_ird_project)`；`ExecutionLinkage.RegisteredEdgesPresent_DT_BUILD`（三边存在性）与 `DiagnosticsEdgeInjectionNotLink_DT_BUILD_PEX8`（零 diagnostics 链接引用）两用例我方四跑全绿；EX-BLD-1 前两项＝NoQtInclude＋PublicHeaderPathLayout 用例绿 |
| 2 | 红线扫描零命中：仅 execution→core/evidence/project 三条登记边；零 Qt＋零业务域互链/无私有头出 include——P-EX-3 注入消费零编译依赖 | 运行期三面扫描 12 用例四跑全绿；配置期守卫零触发（两模式构建成功即证）；ird_gates 我方复跑 17 命中＝基线同集、**零 execution 项**；双探针证实扫描检出力（4.5）；源码直 grep 复证：src/＋include/ 树零 `#include` 行（产品文件零包含），测试文件仅 gtest/std |
| 3 | `_test`/`_contract_test` 目标按 DTB §5.5 注册并可构建（vcpkg find_package(GTest CONFIG REQUIRED)；落位期允许最小构建冒烟用例） | 两目标两模式 build-pass＋run-pass（4.2）；`find_package(GTest CONFIG REQUIRED)` 于 execution/CMakeLists.txt（失败即停）；ird_add_gtest 宏自持（add_test＋XML 报告目标 EXCLUDE_FROM_ALL——我方四份 XML 即该宏产物同机制）；契约 acceptance 3 明文豁免＋父 CMakeLists 注释既有约定——落位期用例＝红线扫描＋构建图契约，非占位业务实现 |
| 4 | O-24：四词表按 core 现状承接，O-24 保持登记未决不私裁消账；P-EX-1：零跨单元头消费 | `NoVocabularyRedefinition_O24` 用例绿（公共头面仅 README.md——execution 侧零词表重定义）；DTB §4 O-24 行（head 版 L625）仍为「登记」未决态、diff 未触碰 DTB——消账权未越界；P-EX-1：产品与测试源码零 `sdurws/ird/<他单元>` include（4.6 直 grep 复证——仅三条链接边零头消费）；O-24「core 现状承接」口径于 Execution.cpp/CMakeLists/ird-test-report.json 注释登记一致 |

## 4.5 测试真实失败能力 —— 通过（2 处失效注入，亲手执行）

| 探针 | 注入 | 结果 |
| --- | --- | --- |
| 1 | 向 `src/Execution.cpp` 追加 `#include <QObject>` | `ExecutionBuild.NoQtInclude_DT_BUILD_NFR_MNT_01` **FAILED**（其余 6 用例 OK——检出精确无扩散），exit 1 |
| 2 | 还原后追加 `#include "sdurws/ird/policy/Policy.hpp"` | `NoCrossUnitInclude_DT_BUILD_R1_R2` 与 `RuntimePolicyZeroCompileFace_DT_BUILD_PEX3` **双双 FAILED**（P-EX-3 具名自证用例非恒真），exit 1 |

还原（`git checkout -- src/Execution.cpp`）后复跑：7/7＋5/5 全绿；wtA `git status --porcelain` 零残留。注入仅存在于临时 worktree 源码树且已还原；任务分支与送验对象零触碰、零推送。

## 4.6 架构红线（AGENTS §5 八条逐条） —— 通过

1. **L2 计算内核零 Qt**：execution 源码零 Qt include（NoQtInclude 绿＋探针 1 证实检出力；直 grep 零命中）；构建面零 Qt 链接（配置期守卫 Qt 模式分支＋ird_gates 零 execution 命中）。
2. **业务域互链禁止（R-1）**：链接边仅 core/evidence/project 三条（CMakeLists 实读＋配置期守卫 `^sdurws_ird_` 通配拦截＋UnitEdgesRegisteredOnly 绿＋ird_gates 零 execution 项）。
3. **跨单元私有头禁止（R-2）**：产品源码零跨单元头消费（P-EX-1 处置——连公共头也零消费）；include/ 树仅 README.md（PublicHeaderPathLayout＋NoVocabularyRedefinition_O24 绿）。
4. **框架零源码修改（SA-02）**：diff 范围零框架文件、patches/ 零新文件（4.1）。
5. **不做名称前缀拼接/剥离（R-4）**：diff 新增代码行（剔除注释）对 "RobWork" 字面量零命中——唯一出现处为 CMakeLists 注释「框架原宏见 RobWork/RobWork/gtest/CMakeLists.txt」（出处引注，非名称运算）；ird_gates R4 码 17 命中集中 project 既有辖区、零 execution 项。
6. **权威唯一（PA-1）**：本任务仅登记构建目标与测试，未实现 project 修订/runtime 真值/policy 共享/diagnostics 文案/ui 命令入口等他单元语义；Execution.cpp 为零声明零定义锚点翻译单元。
7. **不可变历史（PA-2）**：零数据写路径，无涉。
8. **不继承 old/**：diff 全文 `old/` 引用零命中。

## 4.7 注释规范（AGENTS §2） —— 通过

本任务交付 5 个文本文件（0 个可编译公共头——契约头随 EX-T02+ 落地，README 为不参与编译保留位），**全部全文实读**（超过「实现文件抽查 ≥3」要求）：

- **文件头注释**：5/5 齐备——Execution.cpp（设计依据/背景说明/后续任务安排/陷阱处置/线程约束五段）、execution/CMakeLists.txt（单元卡 §3.2/§3.4/§11/§12＋DTB §5.1/§5.3/§5.5＋需求编号逐项标注）、两测试文件（EX-BLD-1 条目级锚定＋先例引注）、README（保留位声明复核行）。
- **类/函数注释**：测试辅助函数（unitRoot/collectCppFiles/readFile/isIncludeDirective/readCMakeLists/collectTargetRefs/hasIncludeOfUnit）均有中文 Doxygen 简述；12 个 TEST 各带块注释说明「验证哪条红线/哪个陷阱处置」。
- **§2.5 高危信息**：本任务无可执行语句（锚点 TU），物理单位/坐标系/角度制式不适用（n/a 已由「零代码」事实自证）；**所有权/线程约束**——Execution.cpp 头注明当前无线程安全性议题、后续线程模型归 §6.2/§10.8；**错误语义**——readFile/readCMakeLists 读失败显性 ADD_FAILURE（「不留读不到＝零命中的假阳性通道」，中文注明）；**确定性来源**——collectCppFiles 排序（「失败信息确定性，NFR-COR-02 精神」）、isIncludeDirective/collectTargetRefs 的注释剥离门控（防注释散文误报——文档提及≠编译期包含，两处均中文详注）。
- **解释"为什么"**：抽读各段均为决策依据型注释（如 /utf-8 PUBLIC 的 MSVC GBK 乱码动机、统一真名链接的两模式可解析取舍、ird_add_gtest 单元自持的 NFR-MNT-04 依据），无复述代码行。

## 4.8 文档同步 —— 通过

- **单元卡增量修订**：units/execution.md v0.2→v0.3 diff 实读——卡头构建落位行更新＋§15.4 变更记录行，与实现逐项一致（STATIC、PUBLIC 三链、`_test`/`_contract_test` 注册、src/ 空起步锚点 TU、P-EX-1/P-EX-3/P-EX-8/O-24 处置留痕），「契约语义零变更」声明与 diff 相符；README 保留位复核行（§9→§12 指向确认零偏差）与卡内表述一致。**实现与任务卡零偏差**，无需 DTB §5.4 偏差登记。
- **§3.4 别名形态 vs 实现真名**：卡文 `target_link_libraries(… PUBLIC RWS::ird::core …)` 字面为别名形态，实现用真名 `sdurws_ird_*`——集成模式别名即其 ALIAS（同一 CMake 目标），冒烟模式无别名（DTB §5.1），**真名是两模式唯一公共可解析形态**；取舍已由实现注释（「project 落位同款取舍」）与 ird-test-report.json 登记，PRJ-T01 同款先例，链图语义（三条 PUBLIC 边）逐字等价——判定为非偏差。
- **findings.json 未关闭条目相关性核对**（基于 origin/redesign-main＝840688a9 版全量过筛 open 条目）：
  - **F-007/F-200**（worktree 环境前置三件）：本次验收实测同型命中（无 toolchain 首配 Boost NOTFOUND、ini 模板两文件缺失），按 IO-T05/T06/T07 与 attempt 1 先例补齐处置——保持 open（构建约定所有者）。
  - **F-012 家族**（提交标题名词开头）：本送验范围 b0861ec2 同型（见 4.9）——attempt 1 已转登 F-206，因 acc/EX-T01/1 未合入、redesign-main 版 findings.json 尚无该条，本记录分支**镜像转登 F-206**（条目内容与 attempt 1 版逐字一致——同号同义，PIPE §4.7③ 并集解算按「同号同义取一」去重，无撞车风险）。
  - **F-019/F-191/F-197/F-202** 等门禁词表/基线类：ird_gates 我方复跑 17 命中与 wp11-t07 基线同集，零新增零消账——保持 open（门禁所有者）。
  - **F-031**（EX-T09 AT-34 编号）属 EX-T09 辖区，与本任务无关；其余 open 条目（testkit/io/evidence/project/policy 各辖区）与本任务零交集。
  - **无可随本任务顺手消账项**。

## 4.9 commit 质量 —— 通过（附 1 项建议级既有发现）

- **a83a5ce4**（head）：标题 `[WP-08-T02] 补交 execution 落位九件验证日志留痕…`——动词开头✓；正文四段（What/Why/验证/影响面）齐备✓；验证段如实声明「本提交仅新增 doc 下留痕文本文件，未触发重新构建」并逐项引用 attempt 1 复现数值——**实读核对无粉饰**：声称九件补交与 diff（9 A）一致；「14 件」勘误说明与在库实际（ls-tree 实测恰 14 件）一致；不改写已推送历史（b0861ec2 仍在其位）符合 AGENTS §6.3。
- **b0861ec2**：正文四段齐备、全部数值结论经本验收者亲手复跑证实（双模式构建、7/7＋5/5、ird_gates 17 命中零 execution、双脚本 PASS）；唯一瑕疵＝标题名词短语开头（「execution 单元构建落位：…」）——**F-012 家族同型，attempt 1 已转登 F-206，本分支镜像登记**；纯风格瑕疵不构成阻断。
- 提交粒度：代码与其测试、CMake、文档同步同在 b0861ec2 单提交✓；留痕补交独立成提交且正文载明勘误✓。

## 4.10 留痕完整 —— 通过（attempt 1 阻断项已修复）

- 在库清点（ls-tree @ a83a5ce4）：traceability/builds/wp08-t01/ 恰 **14 件**＝4 份 gtest XML＋ird-test-report.json＋9 件日志（integration-build/smoke-configure/smoke-build/test-run/verify-target-build/gates-ird-gates-target/validate-task/validate-docs/test-fault-probe）——与 a83a5ce4 提交信息勘误后的声明清单一一对应，`.gitignore *.log` 吞没问题已以补交方式修复。
- **数值交叉比对（我方复跑 vs 在库声明）**：测试 7/7＋5/5＝XML×4 与 report 一致✓；ird_gates 17 命中零 execution＝gates log 与我方复跑一致✓；validate-task/validate-docs PASS＝我方复跑一致✓；verify 命令 exit 0＝我方逐字执行一致✓；探针检出＝test-fault-probe.log 与我方双探针同型一致✓。
- **resultNote 语义**：ird-test-report.json（ird-test-report/1 schema，acceptance 1~4 逐条对账＋acceptanceMapping）即本任务结果注记；phase-one-validation.log 为 2026-09-10 静态文档（末次变更 DTB v0.7），现行留痕口径＝builds/<分支>/ 目录，判定满足「或」条件。
- 留痕日志编码提示：gates log 含 GBK 双重编码乱码（系统性既有特征，非本任务引入）——转登 **F-207**（建议级），不构成返工项。

## 4.11 偷懒/缩水实现扫描 —— 通过

- **① 关键字扫描**：diff 全文 `TODO|FIXME|stub|placeholder|未实现|not implemented` 4 命中，全部位于 smoke-build.log 内 msbuild 对 io/project **既有**测试文件名（ConfirmableProjectStubTest.cpp 等）的编目输出——非本任务交付源码，交付源码零命中，无豁免义务。
- **② 空实现检测**：公共接口函数体——落位期公共头面为零（仅 README，NoVocabularyRedefinition_O24 钉住），零「空函数返回默认值」形态；src/Execution.cpp 为仅注释锚点翻译单元＝契约 acceptance 3「不在骨架预建占位实现」＋单元卡 §12「src/（空起步）」的显式豁免形态（契约豁免依据逐条：CMake 零源码 STATIC 生成期报错实测、CORE-T01/POL-T01/DIAG-T02/PRJ-T01 同款先例、NFR-MNT-04 无消费者能力不预建）——**逐条依据已列**。
- **③ 逐条证据表**：acceptance 1~4 每条证据见 4.4 表——四条全部有具名测试/命令输出/文件行号，无「已实现」声明依赖。
- **④ 测试强度**：12 用例非恒真——双探针（4.5）实证 NoQtInclude 精确红、NoCrossUnitInclude＋RuntimePolicyZeroCompileFace 双红；扫描器断言行为与边界（include 指令行门控防注释误报、读失败显性失败、白名单逐一比对），非「只编译通过」型用例。

## 问题清单

| 级别 | 项 | 处置 |
| --- | --- | --- |
| 阻断 | 无 | — |
| 建议 | F-206（b0861ec2 标题名词开头，F-012 家族同型） | attempt 1 已转登；本分支镜像登记（同号同义保真），与家族合并处置，不追溯改写已推送历史 |
| 建议 | F-207（gates 留痕日志系统性 GBK 双重编码乱码——wp02~wp11 全基线既有环境特征，非本任务引入） | 本次新登：建议留痕口径统一捕获管道 UTF-8，向后生效；既有日志不追溯改写 |

## 裁决

**verdict = pass**。4.1~4.11 全部通过且零阻断级问题；attempt 1 唯一阻断项（留痕未落位）已由 a83a5ce4 以新提交补齐并经全量重验证实。建议级 2 项（F-206 镜像＋F-207 新登）已按 ACC §5 转登 traceability/findings.json，不阻塞合入。验收者不合入、不修改实施产物、不向任务分支推送任何提交——合入决策归所有者。

## 验收者环境说明

- 复现现场：`git worktree add --detach D:/10_Source_Repos/21_robot/acc-ex-t01-r2/wtA a83a5ce476709c07de3d1a61f4580787f2c6ab43`；集成树 wtA/build（VS 17 2022 -A x64＋vcpkg toolchain＋Qt prefix＋RWS_BUILD_INDUSTRIALROBOT=ON，缓存实测 :BOOL=ON）与冒烟树 acc-ex-t01-r2/smoke 均本次从零新建；记录回传现场 acc-ex-t01-r2/wtB（分支 acc/EX-T01/2 基于 origin/redesign-main＝840688a9）。
- 全部证据（双模式配置/构建、四跑 7/7＋5/5、契约 verify 命令逐字执行、ird_gates 复跑 17 命中逐码对账、双探针失效注入与还原复绿、21 变更文件逐一比对、5 交付文件注释全文实读、在库 14 件留痕数值交叉比对、两送验提交全文实读）为验收者在本现场亲手执行；探针注入仅存在于临时 worktree 源码树且已还原（`git status --porcelain` 零残留）。任务分支 wp08-t01 与送验对象零触碰、零推送。验收完成、推送确认后两个临时 worktree 随流程删除，本记录与三元组为唯一持久证据。
