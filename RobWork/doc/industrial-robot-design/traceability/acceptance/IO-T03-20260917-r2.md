# IO-T03 验收记录（acceptance record，attempt 2）

| 字段 | 值 |
| --- | --- |
| 任务 | IO-T03（≙WP-11-T04）：CSV 读写器（方言标识＋可逆编码，唯一转义/还原实现） |
| 送验对象（冻结） | cb2b793b6a6d16207591caae9bf7e7a711ef37d1（分支 wp11-t03 尖端；diff 范围＝bff27c07c881494a5642b74d0eb42fa34a302114..cb2b793b＝state.base..headSha，3 个提交：c4cfdf06 实现＋64bdbcb0 留痕＋cb2b793b attempt-1 阻断 B-1 的 io.md v0.5 落位同步返工） |
| 分支漂移检测 | `git rev-parse origin/wp11-t03` 实测＝cb2b793b（与送验 SHA 一致，零漂移）；base＝origin/redesign-main 尖端 bff27c07（`git merge-base` 复核一致） |
| 验收环境 | 全新 detached worktree（`git worktree add --detach` @ cb2b793b＝`D:/10_Source_Repos/21_robot/RobWork/.acc-io-t03-r2/a`）从零复现：集成 build 树（worktree A 内 `build/`）与独立冒烟 build 树（`build-smoke/`）均为本验收新建（非主树缓存复用）；记录回传 worktree（acc/IO-T03/2 基于 origin/redesign-main@bff27c07）。MSVC 2022 x64＋vcpkg 经典模式。**环境前置三件**（worktree 干净检出的既有环境缺口，非实施缺陷——F-007/F-062/F-099 已登记同型）：①集成配置需 `-DCMAKE_TOOLCHAIN_FILE=<主仓>/vcpkg/scripts/buildsystems/vcpkg.cmake`（AGENTS §4.1 集成命令字面不含，主树 CMakeCache 实测同源）；②需 `-DCMAKE_PREFIX_PATH=D:/software/QT/6.11.1/msvc2022_64`（主树缓存 Qt6_DIR 实测）；③`RobWorkStudio/bin/` 下 `RobWorkStudio.ini.template.static`/`RobWorkStudio.ini.shared.in` 被 `.gitignore` `[Bb]in/` 规则吞、干净检出缺失，自主仓补拷（未跟踪环境文件，不入任何提交）。补齐后配置零错误。验收全程只消费产物与留痕（契约/单元卡 io.md v0.5/在库留痕/attempt-1 验收记录/findings.json），未读取实施者过程叙述（ACC §2 四条逐项满足；全部构建、测试、门禁、脚本、失效注入均为本验收者亲手重跑，不采信 attempt-1 与实施留痕结论） |
| verdict | **pass**（4.1~4.11 全部通过、零阻断级问题；attempt-1 阻断项 B-1〔4.8 文档落位同步缺失〕经本次全量重验确认已由 cb2b793b 修复。建议级 1 项新发现转登 F-189；F-188〔attempt-1 evidence 分支登记〕所指问题已随送验提交更正，本记录落档时移植入册并置 fixed——见 4.8） |
| 验收日期 | 2026-09-17 |

## 4.1 改动范围 —— 通过

- `git diff --stat bff27c07..cb2b793b` 实证 **18 个变更文件**（5032 insertions/12 deletions）：
  - 产品代码 6 件（全在 `industrialrobot/io/**`）：`io/CMakeLists.txt`（M：产品库增列 src/Csv.cpp、测试目标增列 test/CsvTest.cpp）、新增 `Csv.hpp`（A，605 行公共头）、`Csv.cpp`（A，2168 行）、`CsvTest.cpp`（A，1259 行 20 用例）、`IoDiagnostics.hpp`（M：增 makeRowColError 逐行列定位助手）、`IoDiagnostics.cpp`（M：同上实现）；
  - traceability 11 件：`builds/wp11-t03/` 下 8 份 .log＋2 份 gtest XML＋`ird-test-report.json`（全部 A）；
  - 单元卡 1 件：`units/io.md`（M：v0.4→v0.5 落位同步——cb2b793b，见 4.8）。
- **逐文件比对契约 allowedFiles 三条通配**（`industrialrobot/io/**`、`units/io.md`、`traceability/**`）：**全部在册，零越界**。
- forbiddenFiles（REQUIREMENTS.md／ARCHITECTURE.md）**零触碰**（diff 无该两文件）。
- 无未登记框架源码改动：diff 零框架路径；`industrialrobot/patches/` 零新文件（SA-02 无登记需求）。

## 4.2 可复现构建 —— 通过（全新树从零双模式复现）

- **集成模式**：worktree A 从零配置 **exit 0**（缓存实测 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`——`build/CMakeCache.txt` grep 实证）；`cmake --build build --config Release --target sdurws_ird_io_test` **exit 0**，`error C[0-9]/LNK/MSB/fatal` 计数 **0**，`build/RobWorkStudio/bin/Release/sdurws_ird_io_test.exe` 生成（日志 `.acc-io-t03-r2/a-integrate-configure.log`＋`a-integrate-build.log`，本记录外留档于验收现场，关键结论已摘录于此）。
- **独立冒烟模式**：worktree A 内 `build-smoke/` 独立临时目录从零配置＋构建（带 vcpkg toolchain——AGENTS §4.1/CORE-T01 G-2 口径）**exit 0、error 计数 0**，`build-smoke/io/Release/sdurws_ird_io_test.exe` 生成（`a-smoke-configure.log`＋`a-smoke-build.log`）。
- **测试执行（验收者亲手，非采信留痕）**：集成 exe **exit 0、40/40 PASSED**（448 ms）；冒烟 exe **exit 0、40/40 PASSED**（251 ms）。本任务新增 IoCsvTest 组 20 用例全绿，IO-T01/T02 既有 20 用例（IoSec/SafePath/Budget/IoDiag）零回归——与在库两份 gtest XML（各顶层 `tests="40" failures="0"`、`<testcase` 计 40）交叉一致。

## 4.3 契约 verify 命令 —— 通过

- 契约 verify 数组仅 1 条：`cmake --build build --config Release --target sdurws_ird_io_test`——验收者亲手执行（4.2 集成构建即该命令）**exit 0**。
- 治理脚本（验收者亲手，worktree A 内，日志 `a-validate-task.log`/`a-verify-task.log`）：
  - `pwsh -NoProfile -File RobWork/scripts/industrialrobot/validate-task.ps1 -TaskFile RobWork/doc/industrial-robot-design/tasks/foundation/IO-T03.json` → **PASS (1 tasks)**，exit 0；
  - `pwsh -NoProfile -File RobWork/scripts/industrialrobot/verify-task.ps1 -TaskFile …` → **PASS IO-T03**，exit 0（日志尾部实证 io.lib/io_test.exe 链接完成——脚本内部实跑契约 verify 命令）。

## 4.4 acceptance 条目逐项证据 —— 通过（5/5，全部为验收者亲手复现证据）

| # | 契约 acceptance 条目 | 证据（具名测试＋位置＋模式＋结果） |
| --- | --- | --- |
| 1 | V01/V02 全绿：RoundtripBasic（引号/分隔符/换行/中文/空串——write→read 逐字段比对＋再 write 文件 SHA-256 二次比较）与 RoundtripPrefixed（=、+、-、@、' 自带前缀样例 roundtrip 逐字符一致、decode(encode(s))==s 全样例自反、RawTable 无转义形式残留） | `IoCsvTest.RoundtripBasicQuotesDelimsNewlineChineseEmpty`（CsvTest.cpp L288 起：双模式 PASSED；断言文本核实——逐字段比对＋SHA-256 二次比较）、`IoCsvTest.RoundtripPrefixedEscapeOnlyAtFileLayer`（L380~439 全文核实：13 样例含 `'quoted'`/`'abc`/`'=x`/空串/中文/组合；文件层 contains 正断言 `'=SUM(A1)`/`'-3.5`/`''quoted'`＋逐字符还原比对＋转义残留负断言＋全样例自反 L436~438）、`IoCsvTest.EscapeUnescapeReflexiveOverPrefixSampleSet`（L255 起：双模式 PASSED）——**且经 4.5 两枚失效注入证明有牙齿** |
| 2 | V03 全绿：UnmarkedNoRewrite——无标识外部 CSV 全字段原样读入（'=x 保持 '=x），不执行任何前缀剥离或字段改写 | `IoCsvTest.UnmarkedNoRewriteKeepsAllFieldsVerbatim`（L450~486 全文核实：8 字段逐字段原文断言＋`report.marked==false` 判定路径钉住）——双模式 PASSED |
| 3 | V04/V05 全绿：BomQuotEol（BOM 剥离/UTF-16 编码识别/CRLF-LF-CR 行尾容错/RFC4180 引号嵌套；非 UTF-8 无 BOM 稳定拒绝 IO-FORMAT-CSV-ENCODING）与 ColumnAnomalies（重复列名/缺列/多列/空行——默认策略拒绝且定位、显式策略按声明处置并记录；RowError 集合内容与顺序可断言） | `BomStrippingAndUtf16Decoding`（L498）、`EolToleranceAndQuotedNewlineNesting`（L642）、`NonUtf8WithoutBomStableReject`（L701）、`DuplicateHeaderColumnsRejectedWithLocation`（L725）、`ArityPoliciesDefaultRejectAndExplicitPadTrim`（L754）、`BlankRowPoliciesSkipCountAndReject`（L848）——**六用例双模式 PASSED** |
| 4 | 数据-only 断言：解析路径零公式/命令执行入口；方言语法不超出 §5.1 声明集；部分成功粒度按 §5.8 | `IoCsvTest.DataOnlyNoExecutionEntryInCsvChannelSources`（L1085：Csv.hpp/Csv.cpp 源级执行入口 token 扫描）、`DialectMarkerClosedKeySetRejections`（L929：损坏标识行/未知键/quote='/encoding≠utf-8/重复键全拒绝）、`RowErrorTruncationAndStopOnFirstError`（L891：maxRowErrors 截断＋正确行照常交付）——双模式 PASSED；验收者另经红线扫描独立证实解析路径零 system/exec 入口（4.6） |
| 5 | P-IO-5 处置：方言标识行 v1 语法按 §5.1 已给全文实现，需求/架构评审冻结确认前不私改语法 | `IoCsvTest.RenderDialectMarkerMatchesCard51Verbatim`（L220：渲染与 §5.1 示例逐字节相同断言）双模式 PASSED；验收者实读 `parseDialectMarker`（Csv.cpp L761~872：#rwcsv1 字节级精确判定/#rwcsv12 前缀重合判非 v1/封闭键集/未知键值拒绝不猜测）与 `renderDialectMarker`（L2132 起）——实现与卡面 §5.1 词法一致，零"代为裁决"行为，冻结语义保留评审 |

## 4.5 测试真实失败能力 —— 通过（两枚实现侧失效注入均即红；还原后与送验对象逐字节一致、40/40 恢复）

- **探针 1（写侧转义有牙齿）**：`Csv.cpp` `escapeCsvText` 前缀判断条件改 `if (false && !text.empty())`（ACC-PROBE-1）→ 重建 → `--gtest_filter=IoCsvTest.*` **3 FAILED**（EscapeUnescapeReflexiveOverPrefixSampleSet／RoundtripBasicQuotesDelimsNewlineChineseEmpty／RoundtripPrefixedEscapeOnlyAtFileLayer）——转义编码缺失被 acceptance 1 三用例共同守卫。
- **探针 2（读侧还原有牙齿）**：还原探针 1 后，`unescapeCsvText` 剥离条件改 `if (false && …)`（ACC-PROBE-2）→ 重建 → **2 FAILED**（EscapeUnescapeReflexiveOverPrefixSampleSet／RoundtripPrefixedEscapeOnlyAtFileLayer）——前缀剥离缺失被实数据断言捕获。
- 两探针以精确 Edit 还原：`git status --porcelain`＋`git diff HEAD --stat`（io/ 范围）**零输出**＝与送验对象逐字节一致；重建复跑 **40/40 PASSED**（exit 0）。无恒真测试面。

## 4.6 架构红线 —— 通过（AGENTS §5 八条逐条）

1. **L2 零 Qt**：io 全单元（src/include/test）grep 零 Qt include；"Qt" 字样唯一命中在 SafePathTest.cpp:479＝IO-T02 既有的头红线自扫描字符串列表（非 include）。
2. **R-1 业务域互链禁止**：`target_link_libraries(sdurws_ird_io PUBLIC sdurws_ird_core sdurws_ird_diagnostics)`（CMakeLists.txt L92——§3.3 两条登记边）；测试目标 PRIVATE `sdurws_ird_io`＋`GTest::gtest_main`（L138）；零业务域单元目标出现。
3. **R-2 跨单元私有头禁止**：io 范围跨单元 include 仅 `<sdurws/ird/diagnostics/DiagCodes.hpp>`（IoDiagnostics.hpp L49，IO-T02 既有）与 `<sdurws/ird/core/Digest.hpp>`（CsvTest.cpp L23，测试消费）——均为 `include/sdurws/ird/<unit>/` 公共头；本任务新增的 Csv.hpp/Csv.cpp 零跨单元 include（仅 std＋io 公共头）。
4. **R-4 名称前缀拼接/剥离**：本任务新增文件 grep "RobWork" 零逻辑命中（唯一字面量在 io/CMakeLists.txt L163 注释中引用框架宏出处，非拼接/剥离）；ird_gates R4 实跑唯一命中在 project/src/ProjectStoreImpl.cpp＝DTB §4.5 登记例外，与本任务无关。
5. **SA-02 框架零修改**：diff 零框架路径、patches/ 零新文件（见 4.1）。
6. **PA-1 权威唯一**：实现只做文件层格式/安全事实（方言/编码/行列/转义——实读 Csv.cpp 证实）；数值原文保留（CsvCell 注"无单位语义——单位归业务"）、io 不触达命令端口、RawTable 纯内存零落盘（Csv.hpp RawTable 注）——零越权语义。
7. **PA-2 不可变历史**：不适用（本任务零项目修订写路径——实现无 project/命令端口 include）。
8. **不继承 old/**：io 单元 grep 零 `old/` 引用。

**ird_gates 独立实跑（验收者亲手，worktree A 内 `cmake --build build --config Release --target ird_gates`）**：exit 1（SA-10"命中判构建失败"基线口径），命中 **11 处全为基线既有**（SUB×9：diagnostics_contract/evidence×2/policy×2/project×2/runtime 测试目标直链 testkit×8＋testdata_lint→testkit 表外边×1；T1×1：testdata_lint；R4×1：project 登记例外）——**零 io 命中、零新增**，与在库 `gates-ird-gates-target.log` 及 wp11-t02/DTB §4.5 登记口径同源。（日志为 GBK 编码产生乱码——F-186 governance 侧既有登记，非本任务问题。）

## 4.7 注释规范 —— 通过（公共头全查＋实现文件 3 件全查＋测试抽查，超抽查下限）

- **公共头全查**：`Csv.hpp`（605 行全文）——文件头含设计依据（§5.1~§5.8/§9.3/§9.4/§9.0/§3.1＋NFR-SEC-03/REQ-05/AT-02＋契约 acceptance 编号）与"为什么"背景（转义/还原为何唯一实现点 SA-12/M-15、数据-only 承诺、线程约束、确定性来源）；每个类型/枚举/函数均有 Doxygen 中文契约注释（前置/后置/错误语义/生命周期/线程/取消）。`IoDiagnostics.hpp` 增量（makeRowColError）——契约注释含固定键序/参数方向/脱敏口径/线程安全声明。
- **实现文件 3 件全查**：`Csv.cpp`（文件头五段实现结构总览＋实读头 260 行〔常量出处/量纲、UTF-8 DFA 选用理由〕、中段 parseDialectMarker L761~872〔逐分支业务语义＋卡面出处〕、尾段转义/还原/渲染 L2102~2139〔只剥一层＝可逆性正确形态〕）；`IoDiagnostics.cpp` 增量（三键固定序/不二次截断的单一职责说明）；`CsvTest.cpp`（fixture＋抽读 RoundtripPrefixed/UnmarkedNoRewrite/BomStripping 等用例——用例头注明对应 IO-V 编号与卡面章节，符合测试代码注释要求）。
- **§2.5 高危信息**：物理单位（kMarkerScanCapBytes/kFieldScanCapBytes/kSnippetMaxBytes 等逐个注明"单位字节"及来源）、所有权（CsvRowView 零拷贝仅回调内有效/RawTable 归调用方/buffer 调用方持有/staging 随会话迁移）、线程约束（会话单线程/probe 结果可共享/纯函数并发安全——头注集中声明）、错误语义（调用方契约违约＝防御性 IO-FORMAT-INTERNAL vs 环境错误＝资源四分类/稳定码，逐处注明归类）、确定性来源（canonical 固定键序/to_chars 无本地化/rowErrors 稳定序/NFR-COR-01/02 引用）——逐项齐备。坐标系/rad 不适用（io 无位姿量）。
- **发现 1 处建议级瑕疵（不阻塞）**：Csv.hpp `ICsvReader::read` 的 Doxygen 中 `@param progress` 出现两次（L496~497 与 L500~501，一处写"已消费物理行号"、一处写"已消费物理行数"，语义重复且措辞不一致）——转登 **F-189**。

## 4.8 文档同步 ＋ findings.json 未关闭条目相关性 —— 通过

- **attempt-1 阻断 B-1 修复核实**：attempt-1（acc/IO-T03/1）以"io.md 在 diff 范围零修订、≥4 处 §9.0 等价调整未按 DTB §5.4 登记进单元卡"判 fail。本次送验对象新增第三笔提交 cb2b793b（纯文档、零代码），实读其 io.md diff 逐项核对：
  1. 卡头版本行 v0.4→v0.5（版本链补记）✔；
  2. §1.2"io 构建骨架"行补记 CSV 读写器落位（Csv.hpp/.cpp＋makeRowColError＋sdurws_ird_io_test 增列 20 用例）＋留痕目录指引 ✔；
  3. §3.1 头内容表 Csv.hpp 行内容清单按实现刷新（CsvEol/CsvEncoding/CsvDialect/转义原语/CsvCell/CsvArityPolicy/CsvBlankPolicy/CsvReadOptions/CsvRowView/CsvRowError/CsvParseReport/RawTable/CsvOutputTarget/CsvWriteOptions/ICsvReader/ICsvWriter/工厂——与实读公共头 17 项逐一对应）✔；
  4. §15.5 追加 v0.5 变更记录行，登记四项等价调整（①§9.4 open 首参 IOutputTarget&&→CsvOutputTarget&&；②probe/read 尾随增补 budgetScope；③CsvReadOptions 增补 retainRows/stopOnFirstRowError；④无标识文件"单遍流式"→"嗅探遍＋交付遍两遍"）＋P-IO-5 处置口径 ✔——与实现实况（实读 Csv.hpp 签名/Csv.cpp 两遍形态）逐一相符；
  5. 文末落款过期表述更正（v0.5 与当前落位进度）✔。
  实现与单元卡 v0.5 **零未登记偏差**（"实现与任务卡零偏差，或偏差已按 DTB §5.4 登记"的通过分支成立）。设计正文（§2~§14）语义零变更，符合"落位同步不改设计语义"先例（IO-T01 v0.3/IO-T02 v0.4 同型）。
- 契约 status=ready 属 PIPE §4.7⑤ 收尾环节翻转动作（先例同口径），非实施侧义务；contract-compile-log 无本任务新行需求（未触发契约修订）。
- **findings.json 未关闭条目相关性核对（redesign-main 侧 163 条 open、24 fixed）**：
  - io/CSV 直接相关 open 共 8 条：F-007/F-062/F-099（worktree 环境前置未成文——本验收遭遇的同款问题，属 governance/环境侧既有登记，本验收按三件补齐处置，非实施缺陷）；F-027/F-179/F-185（提交标题风格家族——本任务三笔标题均 `[WP-11-T04]`＋动词开头，家族未再扩散）；F-029（IO-T07 契约 dependsOn 前置——后续任务事项）；F-180（io.md 卡头日期行家族——本任务 v0.5 修订日期行实测 2026-09-17 与实际一致，未再现）；
  - F-186（门禁留痕载体弱）：io 侧半已持续兑现（在库 gates 日志含命中明细，本验收实跑证实其结论），governance 侧（GBK 编码规范化）保持 open，非本任务返工项；
  - F-153/F-010（.gitignore 吞 *.log）：本任务 11 份留痕 `git add -f` 强制入库（实测在库）——教训已吸收；
  - 其余 core/project/policy/runtime/evidence/diagnostics/testkit 各家族 open 条目与本任务产物零交集（io 相关过滤＋全库关键词复核）。
  - **F-188（attempt-1 evidence 分支 acc/IO-T03/1 登记的建议级 S-1：io.md 文末落款过期表述）**：所指问题已由送验提交 cb2b793b 更正（见上第 5 点）；该条目本体随 attempt-1 evidence 分支尚未进 redesign-main（fail 记录不合并的既定口径）——**本记录落档时移植入册并置 fixed**（fixedBy=cb2b793b），防止消账悬空；IO-T02 attempt-2 记录落档先例（"findings 移植与新增登记"）同口径。

## 4.9 commit 质量 —— 通过（三提交四段齐备、结论零粉饰、与验收者复现交叉一致）

- **c4cfdf06**『实现 io 单元 CSV 读写器（方言标识＋可逆编码，唯一转义/还原实现）』：`[WP-11-T04]`＋动词开头；四段齐备（What 按文件列要点/Why 引 NFR-SEC-03·REQ-05·AT-02＋P-IO-5/验证含双模式 40/40＋门禁口径/影响面声明公共面零改动）；等价调整在正文逐一列明。
- **64bdbcb0**『采集 IO-T03 双模式验证留痕与测试报告（11 份证据入库）』：动词开头、四段齐备；`git add -f` 理由（.gitignore 全局 *.log）如实说明；11 份文件与 `git ls-tree` 实测逐一对应（零幻影清单）。
- **cb2b793b**『补齐 io.md v0.5 落位同步：登记四项等价调整并刷新卡面现状（attempt-1 验收 B-1）』：动词开头（补齐）、四段齐备；"零代码变更（diff 仅 units/io.md 一个文件）"与实测一致；Acc-Covered 段如实声明 attempt-1 阻断项与 S-1 更正。
- 三笔验证结论（40/40、门禁 11 基线零 io、双模式零 error）均与本验收独立复现一致，无粉饰。

## 4.10 留痕完整 —— 通过（冻结对象内证据链闭环）

- `builds/wp11-t03/` **11 个文件在库**（`git ls-tree`/体积实测）：8 份 .log（integration-build/smoke-configure/smoke-build/integration-test-run/smoke-test-run/gates-ird-gates-target/validate-task/verify-task）＋2 份 gtest XML＋`ird-test-report.json`（schemaVersion `ird-test-report/1`，targets/gates/selfCheck/testSuites 段齐全）。
- **内容与验收者实测交叉一致**：两份 XML 顶层 `tests="40" failures="0"`、`<testcase` 计 40——与双模式实跑 40/40 一致；gates 日志命中集与实跑 11 项基线一致；ird-test-report.json 的 build-pass/run-pass 结论与实测一致。
- 测试追溯：testSuites 段 20 用例×acceptance 1~5×§11.2 IO-V01~V05 映射齐备，与 4.4 证据表互证。

## 4.11 偷懒/缩水实现扫描 —— 通过（①②④零命中；③逐条证据表见 4.4）

- **①标记扫描**：diff 新增行 grep `TODO|FIXME|XXX|HACK|stub|placeholder|未实现|not implemented|待实现|以后完善` **零命中**（无"触发式延后"豁免需求）。
- **②空实现检测**：Csv.cpp 2168 行全部有实质实现（实读多段证实；探针 1/2 注入即红证明转义/还原路径行为真实——空实现不可能通过 roundtrip 全绿）；IoDiagnostics.cpp 增量函数体实读非空。
- **③逐条证据表**：见 4.4——五条 acceptance 每条均有具名测试＋行号＋双模式 PASSED＋断言文本核实，零"无证据条目"。
- **④测试强度**：断言校验行为与边界（逐字段/逐字符/逐字节比对、文件层 contains 正断言＋转义残留负断言、稳定序断言、错误码＋row/column/snippet 定位参数断言、marked 判定路径钉住、RAII 暂存残留负断言），非恒真、非"只编译通过"；与 4.5 联动（两枚注入即红）证实测试有牙齿。

## 发现问题清单

| 级别 | 编号 | 内容 | 处置 |
| --- | --- | --- | --- |
| 阻断 | 无 | —— | —— |
| 建议 | **F-189**（新登） | Csv.hpp `ICsvReader::read` Doxygen 中 `@param progress` 重复两条（L496~497/L500~501，"物理行号"与"物理行数"措辞不一）——纯注释瑕疵，契约语义（IoProgressCallback 回调口径）在两条中均有表达且无矛盾 | io 实施者随后续任务顺手去重（不追溯已推送提交）；governance 无动作 |
| 建议 | F-188（移植＋消账） | attempt-1 登记的 io.md 文末落款过期表述——已由 cb2b793b 更正 | 本记录落档时移植入册、置 fixed（fixedBy=cb2b793b） |

## 验收者环境说明

- 复现现场：`git worktree add --detach D:/10_Source_Repos/21_robot/RobWork/.acc-io-t03-r2/a cb2b793b6a6d16207591caae9bf7e7a711ef37d1`；集成树 `a/build`（配置：VS17 2022 -A x64＋vcpkg toolchain＋Qt prefix＋`RWS_BUILD_INDUSTRIALROBOT=ON`）与冒烟树 `a/build-smoke`（vcpkg toolchain）均为本次新建；记录回传现场 `acc/IO-T03/2` worktree（`.acc-io-t03-r2/b`）基于 origin/redesign-main@bff27c07。
- 全部证据（构建/测试/门禁/脚本/注入）为验收者在本 worktree 内亲手执行；验收完成后该临时现场随流程删除，本记录与三元组为唯一持久证据。
