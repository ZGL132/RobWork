# IO-T03 验收记录（acceptance record，attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | IO-T03（≙WP-11-T04）：CSV 读写器（方言标识＋可逆编码，唯一转义/还原实现） |
| 送验对象（冻结） | 64bdbcb00352141608620fb4c7c878fe0074bbc8（分支 wp11-t03；diff 范围＝bff27c07c881494a5642b74d0eb42fa34a302114..64bdbcb0＝state.base..headSha，2 个提交：c4cfdf06 实现＋64bdbcb0 留痕） |
| 分支漂移检测 | `git rev-parse origin/wp11-t03` 实测＝64bdbcb0（与送验 SHA 一致，零漂移）；base＝origin/redesign-main 尖端 bff27c07 |
| 验收环境 | 全新 detached worktree（`git worktree add --detach` @ 64bdbcb0＝`D:/10_Source_Repos/21_robot/_acc_io_t03_tmp/wt-a`）从零复现：集成 build 树（worktree A 内 `build/`）与独立冒烟 build 树（`_acc_io_t03_tmp/smoke`）均为本验收新建；记录回传 worktree（acc/IO-T03/1 基于 origin/redesign-main@bff27c07）。pwsh 7＋MSVC 2022 x64＋vcpkg 经典模式（toolchain/安装树指主仓绝对路径）。环境前置（非实施缺陷，worktree 环境缺口，与 IO-T02 attempt 1/2 同型）：`RobWorkStudio.ini.template.static`／`RobWorkStudio.ini.shared.in` 被 `.gitignore` `[Bb]in/` 规则吞——自主仓补拷后配置零错误。集成配置带 `-DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64 -DCMAKE_TOOLCHAIN_FILE=<主仓>/vcpkg/scripts/buildsystems/vcpkg.cmake`（与主构建树 CMakeCache 实测口径一致）。验收全程只消费产物与文档（契约/单元卡/在库留痕/先行验收记录/findings.json），未读取实施者过程叙述（ACC §2 四条逐项满足） |
| verdict | **fail**（阻断级 1 项：**4.8 文档落位同步缺失**——units/io.md 在 diff 范围内零修订，本任务 ≥4 处 §9.0 等价调整与 §3.1 头内容表滞后未按 DTB §5.4「单元卡增量修订＋变更记录行」登记，AGENTS §6.1「不允许代码先行、文档失实」；IO-T01 v0.3／IO-T02 v0.4 两轮先例均为任务内完成卡面落位同步。4.1～4.7、4.9～4.11 全部通过——范围/构建/测试/门禁/脚本/失效注入全部复现绿，实现本体质量良好且注释完备；建议级 1 条转登 F-188。返工面小：补一笔 io.md v0.5 落位同步提交后按协议重新全量验收） |
| 验收日期 | 2026-09-17 |

## 4.1 改动范围 —— 通过

- `git diff --name-status bff27c07..64bdbcb0` 实证 **17 个变更文件**：
  - 产品代码 6 件（全在 `industrialrobot/io/**` 内）：`io/CMakeLists.txt`（M，产品库增列 src/Csv.cpp＋测试目标增列 test/CsvTest.cpp）、新增 `Csv.hpp`（公共头，A）、`Csv.cpp`（A）、`CsvTest.cpp`（A，20 用例）、`IoDiagnostics.hpp/.cpp`（M，增 makeRowColError 定位助手——IO-T02 头注承诺"随首个消费者 CSV 通道 IO-T03 落位"的兑现）；
  - traceability 11 件：`builds/wp11-t03/` 下 8 份 .log＋2 份 gtest XML＋`ird-test-report.json`（全部 A）。
- **逐文件比对契约 allowedFiles 三条通配**（`industrialrobot/io/**`、`units/io.md`、`traceability/**`）：**全部在册，零越界**（units/io.md 本范围未被修改——该事实在 4.8 作裁决对象，不属越界）。
- forbiddenFiles（REQUIREMENTS.md／ARCHITECTURE.md）**零触碰**（diff 无该两文件）。
- 无未登记框架源码改动：diff 零框架路径；`industrialrobot/patches/` 零新文件（SA-02 无登记需求）。

## 4.2 可复现构建 —— 通过（全新树从零双模式复现）

- **集成模式**：worktree A 从零配置 **exit 0**（`-- Configuring done`／`-- Generating done`，缓存实测 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`）；`cmake --build build --config Release --target sdurws_ird_io_test` **exit 0、`: error` 计数 0**，`build/RobWorkStudio/bin/Release/sdurws_ird_io_test.exe` 生成。
- **独立冒烟模式**：独立临时目录从零配置＋构建（带主仓 vcpkg toolchain——AGENTS §4.1 口径）**exit 0、`: error` 计数 0**，`smoke/io/Release/sdurws_ird_io_test.exe` 生成。
- **测试执行（验收者亲手，非采信留痕）**：集成 exe **exit 0、40/40 PASSED**（520 ms）；冒烟 exe **exit 0、40/40 PASSED**（237 ms）。与在库两份 gtest XML（各 `tests="40" failures="0"`、`<testcase` 计 40、`<failure` 计 0）及两份 test-run.log 交叉一致。本任务新增 IoCsvTest 组 20 用例，IO-T01/T02 既有 20 用例零回归。

## 4.3 契约 verify 命令 —— 通过

- 契约 verify 数组仅 1 条：`cmake --build build --config Release --target sdurws_ird_io_test`——验收者亲手执行 **exit 0**（worktree A 全新树，见 4.2）。
- 治理脚本（验收者亲手，worktree A 内）：
  - `pwsh -File RobWork/scripts/industrialrobot/validate-task.ps1 -TaskFile RobWork/doc/industrial-robot-design/tasks/foundation/IO-T03.json` → **PASS (1 tasks)**，exit 0；
  - `pwsh -File RobWork/scripts/industrialrobot/verify-task.ps1 -TaskFile …` → **PASS IO-T03**，exit 0（脚本内部实跑契约 verify 命令，日志尾部见 sdurws_ird_io_test.exe 链接完成）。

## 4.4 acceptance 条目逐项证据 —— 通过（5/5，全部为验收者亲手复现证据）

| # | 契约 acceptance 条目 | 证据（具名测试＋模式＋结果） |
| --- | --- | --- |
| 1 | V01/V02 全绿：RoundtripBasic（引号/分隔符/换行/中文/空串——write→read 逐字段比对＋再 write 文件 SHA-256 二次比较）与 RoundtripPrefixed（=、+、-、@、' 自带前缀样例 roundtrip 逐字符一致、decode(encode(s))==s 全样例自反、RawTable 无转义形式残留） | `IoCsvTest.RoundtripBasicQuotesDelimsNewlineChineseEmpty`（数据层逐字符 15 格比对＋`sha256Hex(file1)==sha256Hex(file2)`＋直接字节比对，CsvTest.cpp L288~367）、`IoCsvTest.RoundtripPrefixedEscapeOnlyAtFileLayer`（13 样例含 `'quoted'`/`'abc`/`'=x`/空串；文件层 contains 断言 `'=SUM(A1)`/`''quoted'` 转义形态存在＋结构化层无 ' 残留负断言，L380~439）、`IoCsvTest.EscapeUnescapeReflexiveOverPrefixSampleSet`（20 样例自反＋"转义只增不改"断言，L255~276）——**集成/冒烟双模式 PASSED** |
| 2 | V03 全绿：UnmarkedNoRewrite——无标识外部 CSV 全字段原样读入（'=x 保持 '=x），不执行任何前缀剥离或字段改写 | `IoCsvTest.UnmarkedNoRewriteKeepsAllFieldsVerbatim`（8 字段原文逐一断言＋`report.marked==false` 钉住判定路径，L450~486）——双模式 PASSED |
| 3 | V04/V05 全绿：BomQuotEol（BOM 剥离/UTF-16 识别/CRLF-LF-CR 容错/RFC4180 引号嵌套；非 UTF-8 无 BOM 稳定拒绝 IO-FORMAT-CSV-ENCODING）与 ColumnAnomalies（重复列名/缺列/多列/空行——默认拒绝定位/显式策略记录；RowError 内容与顺序可断言） | `BomStrippingAndUtf16Decoding`（UTF-8 BOM 带标识/无标识两态＋UTF-16LE/BE 真实字节＋UTF-16 带标识按解码后判定，L498~634）、`EolToleranceAndQuotedNewlineNesting`（三行尾混布＋引号内 CRLF 逐字节保留＋"" 字面引号，L642~694）、`NonUtf8WithoutBomStableReject`（Latin-1 0xE9 真实非法序列 read＋probe 双入口同码拒绝，L701~714）、`DuplicateHeaderColumnsRejectedWithLocation`（id/ID 折叠重复＋row=2/column=2 定位，L725~748）、`ArityPoliciesDefaultRejectAndExplicitPadTrim`（缺列/多列默认拒绝含 expected/actual params＋行错误按行号序断言＋PadTrailing/TrimExcess 记数，L754~841）、`BlankRowPoliciesSkipCountAndReject`（Skip 计数/Reject 拒绝＋detail 注明策略出处，L848~885）——双模式 PASSED |
| 4 | 数据-only 断言：解析路径零公式/命令执行入口；方言语法不超出 §5.1 声明集；部分成功粒度按 §5.8 | `IoCsvTest.DataOnlyNoExecutionEntryInCsvChannelSources`（Csv.hpp/Csv.cpp 源级扫描 13 个执行入口 token 零命中，L1085~1106）、`DialectMarkerClosedKeySetRejections`（9 种损坏标识行＋未知键/quote='/encoding≠utf-8/重复键/空记号全 IO-FORMAT-CSV-DIALECT 拒绝；#rwcsv12 字节级精确判非 v1 零改写，L929~1003）、`RowErrorTruncationAndStopOnFirstError`（maxRowErrors 截断＋计数＋正确行照常产出，L891~917）——双模式 PASSED |
| 5 | P-IO-5 处置：方言标识行 v1 语法按 §5.1 已给全文实现，不私改语法 | `IoCsvTest.RenderDialectMarkerMatchesCard51Verbatim`（缺省方言渲染与 §5.1 示例**逐字节相同**断言＋tab/LF 词法变体，L220~247）；实现侧 parseDialectMarker/renderDialectMarker 与 §5.1 词法逐行对应（Csv.cpp L761~872/L2132~2152）；代码与测试均无"代为裁决冻结"行为（P-IO-5 冻结确认仍留评审，登记口径未变）——双模式 PASSED |

## 4.5 测试真实失败能力 —— 通过（两枚实现侧失效注入均即红；还原后与送验对象逐字节一致、40/40 恢复）

- **探针 1（前缀剥离有牙齿）**：`Csv.cpp` deliver() 转义还原步 `if (marked)` 改 `if (false && marked)`（ACC-PROBE-1 注记）→ 重建 → `--gtest_filter=IoCsvTest.*` **4 FAILED**（RoundtripBasic／RoundtripPrefixed／BomStrippingAndUtf16Decoding／DialectMarkerClosedKeySetRejections）——带标识文件的剥离语义被三个 acceptance 条目共同守卫。
- **探针 2（RFC4180 引号化有牙齿）**：`Csv.cpp` appendFieldText() needsQuote 恒 false（ACC-PROBE-2 注记）→ 重建 → **3 FAILED**（RoundtripBasic／RoundtripPrefixed／EolToleranceAndQuotedNewlineNesting）——含分隔符/引号/换行字段的 roundtrip 一致性被实数据断言捕获。
- 两探针 `git checkout --` 逐字节还原：`git status --porcelain` 空、`git diff HEAD` 空；复跑 **40/40 PASSED**。无恒真面。

## 4.6 架构红线 —— 通过（AGENTS §5 八条逐条）

1. **L2 零 Qt**：Csv.hpp/Csv.cpp/CsvTest.cpp/IoDiagnostics 增量 grep 零 Qt include（实测 include 全集仅 std＋io 公共头；测试另含 gtest 与 core 公共头）。
2. **R-1 业务域互链禁止**：`target_link_libraries(sdurws_ird_io PUBLIC sdurws_ird_core sdurws_ird_diagnostics)`——仅 §3.3 两条登记边；测试目标链 io＋GTest；零业务域单元目标出现。
3. **R-2 跨单元私有头禁止**：跨单元 include 仅 `<sdurws/ird/core/Digest.hpp>`（core 公共头 `include/sdurws/ird/core/` 下——§3.2 C-1 登记消费：ContentDigester 供文件层 SHA-256；D-05 唯一摘要算法口径）。
4. **R-4 名称前缀拼接/剥离**：io 范围 grep 零 `RobWork` 字面量（ird_gates R4 唯一命中在 project 单元＝DTB §4.5 登记例外，与本任务无关）。
5. **SA-02 框架零修改**：diff 零框架路径、patches/ 零新文件（见 4.1）。
6. **PA-1 权威唯一**：实现仅做文件层格式/安全事实（方言/编码/行列/转义），零业务语义判定（数值原文保留、单位/语义归业务的注释与测试双面钉住——WriterFormatsNumbersCanonicallyWithoutEscape＋§5.8 口径）；io 不触达命令端口、RawTable 纯内存零落盘。
7. **PA-2 不可变历史**：不适用（本任务零项目修订写路径——实现内无任何 project/命令端口 include）。
8. **不继承 old/**：零 `old/` 引用。

**ird_gates 独立实跑（验收者亲手，worktree A 内 `cmake --build build --config Release --target ird_gates`）**：exit 1（SA-10"命中判构建失败"基线口径），末段扫描根＝worktree A、命中 **11 处全为基线既有**（SUB×9：diagnostics_contract/evidence×2/policy×2/project×2/runtime 测试目标直链 testkit×8＋testdata_lint→testkit 表外边×1；T1×1：testdata_lint；R4×1：project 登记例外）——**零 io 命中、零新增**，与在库 gates-ird-gates-target.log 及 wp11-t02 登记口径同源。

## 4.7 注释规范 —— 通过（公共头 2 件全查＋实现 3 件全查＋测试 1 件全查，超抽查下限）

- **文件头**：全部新增/大改文件具备（@file/@brief＋设计依据含卡面章节/需求 ID/契约 acceptance 编号＋"为什么"背景——如 Csv.hpp 的"转义/还原为何只在 io 做且只有一份"（SA-12/M-15）、"数据-only 承诺"、Csv.cpp 的 DecodedStream/RowScanner/解析器五段实现结构总览）。
- **类/函数 Doxygen 中文**：公共接口逐方法契约注释（前置/后置/错误/生命周期/线程/取消——ICsvReader.probe/read、ICsvWriter.open/writeHeader/writeRow/finish 逐项齐全）；实现文件语义段落注释标注算法步骤与卡面出处（prologue 三步、DFA 推进逐分支、RowScanner 语法决策逐条注卡面出处或登记理由、writer open/finish 协议四段、kMarkerToken/kFieldScanCapBytes/kSniffMaxRows=101 等常量逐个注明出处与量纲）。
- **§2.5 高危信息**：物理单位（字节/码元/行号/列号/条数逐处标注——`kSnippetMaxBytes // 单位字节` 型注释齐备）；坐标系/角度制不适用（io 无位姿量）；所有权（CsvRowView 零拷贝视图仅回调内有效、RawTable 归调用方、guard 非拥有、staging 随会话迁移——逐处声明）；线程约束（会话型单线程、probe 结果可跨线程只读、纯函数并发安全——头注集中声明）；错误语义（调用方契约违约＝防御性 FormatInternal vs 环境错误＝资源四分类/稳定码，逐处注明归类）；确定性来源（canonical 键序、to_chars 无本地化、嗅探统计确定性、rowErrors 稳定序、NFR-COR-01/02 引用——逐处注明）。
- 无过期注释、无复述式废注释、无中英混杂机翻腔（全查未发现）；魔法数字（1 MiB/4 KiB 余量/120 字节/101 行/64 KiB 窗口/10^6 字段数）均有来源与理由注释。

## 4.8 文档同步 ＋ findings.json 未关闭条目相关性 —— **不通过（阻断级，fail 判据）**

- **阻断事实：units/io.md 在 diff 范围内零修订**（`git diff --name-status bff27c07..64bdbcb0` 无该文件；卡头仍 v0.4/IO-T02 落位同步，§15.5 变更记录止于 v0.4 行），而实现相对卡面存在**多处 DTB §5.4 明文列出的"实现细节偏差（签名微调/文件名变化，不改语义）"**，全部未按「单元卡增量修订＋变更记录行」登记：
  1. **§9.4 open 首参**：卡面 `IOutputTarget&&`（原子目标 §4.6 或内存缓冲）→ 实现 `CsvOutputTarget&&` 新公共承载类型（AtomicFile.hpp 随 IO-T06 落位后收编）；
  2. **§9.3 probe/read 签名增补**：尾随 `BudgetScopeId budgetScope = {}` 参数（承载 §9.3 前置"budget scope 已开"——调用方可传 tighten 后 scope，0＝自开内部子 scope）；
  3. **§9.3 CsvReadOptions 增补**：`retainRows`（承载卡面后置"RawTable 可配置保留行"）与 `stopOnFirstRowError`（承载 §5.6"StopOnFirstError 可选"）两选项；
  4. **无标识文件读取形态**：卡面 §9.3"单遍流式"→ 实现"嗅探遍＋交付遍两遍"（类注登记了两遍分离理由，属卡面未载明的读取形态决策）。
  以上均为 §9.0"签名均为实现建议……实现期允许等价调整，语义不变"覆盖的等价调整，且已在**代码头注与提交信息**逐一登记（登记质量良好）——但 DTB §5.4 的登记位置是**单元卡**（增量修订＋§15.5 变更记录行），AGENTS §6.1 为强制语气（"必须按 DTB §5.4 登记……不允许'代码先行、文档失实'"）。IO-T01（io.md v0.3 落位同步行）与 IO-T02（io.md v0.4 落位同步行——其中就含 §9.2 两处签名等价调整的 §15.5 登记）两轮先例均为**任务内完成卡面落位同步**后才送验。另 §3.1 Csv.hpp 行内容清单（6 项）与实际公共头（另有 CsvEol/CsvEncoding/CsvArityPolicy/CsvBlankPolicy/CsvReadOptions/CsvWriteOptions/CsvRowView/CsvRowError/CsvOutputTarget/escapeCsvText/unescapeCsvText/renderDialectMarker/makeCsvReader/makeCsvWriter）已滞后——与 F-184（EquivKeyMutex 未入 §3.1 行）同型。
  **裁决**：4.8 通过标准的两个分支（"零偏差"或"偏差已按 DTB §5.4 登记"）在本送验对象内均不满足——4.8 不通过。鉴于 ACC §5 pass 判据要求 4.1~4.11 全部通过，verdict＝**fail**。定级说明：F-184 判例曾将"一处命名等价未登记"定为建议级，但该案 v0.4 落位同步行**已在库**（登记主分支满足、残余两处瑕疵）；本案例是落位同步**整体缺失**（含 §1.2 构建骨架行未更新"io CSV 读写器已落位"），与两轮先例形成的验收时点基线不符，故按阻断级处理。**返工指引**：补一笔 io.md v0.5 落位同步提交（卡头版本/日期＋§1.2 行＋§3.1 Csv.hpp 行内容清单刷新＋§15.5 v0.5 行登记上述 1~4 等价调整），属纯文档增量、零代码变更；随后按协议重新全量验收。
- 其余文档同步项无缺口：契约 status=ready 待收尾翻转（收尾环节动作）；io.md §11.2/§12 行为本任务验收基准且实现与之相符（V01~V05 用例一一对应）；contract-compile-log 无本任务新行需求（未触发契约修订）。
- **findings.json 未关闭条目相关性核对（redesign-main 侧 163 条 open）**：
  - **F-186（门禁留痕载体弱）——与本任务直接相关，io 侧半已兑现**：在库 `gates-ird-gates-target.log` 实测 19 行、**含"命中 11 项"完整命中明细行与汇总行**（较 wp11-t02 的 3 行尾部片段改善），ird-test-report.json 的 gates 字段声称可由该日志核对——本验收经独立实跑证实其结论（11 处全基线、零 io）；**governance 侧半（gate 输出 GBK 编码规范化）仍未落地**（该日志读作 UTF-8 仍为乱码）——F-186 保持 open，责任方 governance，非本任务返工项；
  - **F-153/F-010（.gitignore `*.log` 吞 traceability 留痕）**：本任务 8 份 .log 已按 wp11-t02 先例 `git add -f` 强制入库（64bdbcb0 实测在库）——该家族教训已吸收，条目保持 open 待 .gitignore 规则根治；
  - **F-185/F-012/F-179（提交标题家族）**：本任务两提交标题（"实现…"/"采集…"）均动词开头，家族未再扩散；
  - **F-004（AUTOMOC 空转）/F-187（收尾落分支）/其余 core/project/policy/runtime/evidence/diagnostics 各家族 open 条目**：与本任务产物零交集；
  - 与 CSV/IO-T03 直接相关的 open 条目除上述外零命中（全库 grep csv/NFR-SEC-03/REQ-05/P-IO 复核）。

## 4.9 commit 质量 —— 通过（两提交四段齐备、结论零粉饰、与验收者复现交叉一致）

- **c4cfdf06**『实现 io 单元 CSV 读写器（方言标识＋可逆编码，唯一转义/还原实现）』：标题动词开头；正文四段齐备（改动按文件列要点／Why 引 NFR-SEC-03/REQ-05/AT-02＋P-IO-5 处置／验证含双模式零 error、40/40、门禁 io 零命中／影响面声明公共面零改动）；**等价调整在提交信息内逐一列明**（budgetScope/CsvOutputTarget/retainRows/stopOnFirstRowError——登记位置问题见 4.8，登记内容本身完整如实）；验证段与本验收复现逐项一致。
- **64bdbcb0**『采集 IO-T03 双模式验证留痕与测试报告（11 份证据入库）』：标题动词开头、四段齐备；"全部证据为本次实施段实跑采集、未执行的项不存在通过标注"如实；11 份文件与 `git ls-tree` 实测逐一对应（零幻影清单——F-165/F-183 家族未再现）；`git add -f` 形态与 wp09/wp11-t02 先例一致且正文说明了理由。
- 在库 integration-build.log 系实施侧已建树的增量全量日志（CMake 重跑＋受影响目标，476 行零 `: error`）——其"集成全量构建 exit 0 零 error"表述与该日志相符，且本验收已在全新树独立复现 io 目标零 error（4.2），无粉饰。

## 4.10 留痕完整 —— 通过（冻结对象内证据链闭环）

- `git ls-tree -r 64bdbcb0 -- builds/wp11-t03/` 实测 **11 个文件在库**：8 份 .log（integration-build／smoke-configure／smoke-build／integration-test-run／smoke-test-run／gates-ird-gates-target／validate-task／verify-task）＋2 份 gtest XML（integration／smoke，各 `tests="40" failures="0"`、`<testcase`=40、`<failure`=0）＋`ird-test-report.json`（ird-test-report/1：targets/gates/selfCheck/testSuites 四段齐全）。
- **按名引用闭环**：ird-test-report.json gates/selfCheck 字段引用的日志全部在库（IO-T02 attempt 1 的 B-1 同型问题未再现）。
- **内容交叉一致**：integration/smoke-test-run.log（40/40）、validate-task.log（PASS）、verify-task.log（契约命令＋PASS）、smoke-build.log（零 error）均与验收者亲手复现一致；gates 日志含命中明细（F-186 io 侧半兑现；GBK 编码问题归 F-186 governance 半）。
- 测试追溯：testSuites 段 20 用例×acceptance 1~5×§11.2 IO-V01~V05 逐条映射齐备，与本验收 4.4 证据表互证。

## 4.11 偷懒/缩水实现扫描 —— 通过（①②④零命中；③逐条证据表见 4.4）

1. **标记扫描**：diff 范围 6 个产品文件 `grep -rniE "TODO|FIXME|stub|placeholder|未实现|not implemented"` **零命中**（exit 1）；无契约标注的"触发式延后"豁免需求。
2. **空实现检测**：Csv.cpp 2168 行全文件通读——DecodedStream/RowScanner/parseDialectMarker/sniffDelimiter/CsvReader/CsvWriter 全部为完整实现（BOM 三态判定、Höhrmann DFA 严格 UTF-8 校验、UTF-16 代理对合成、RFC4180 状态机、封闭键集解析、预算四检查点、原子替换协议），无空函数体/仅返回默认值的公共接口；IoDiagnostics 增量为完整纯函数。
3. **逐条证据表**：见 4.4（5/5 条目具名测试＋双模式结果＋文件行号）。
4. **测试强度**：断言校验行为与边界（逐字符/逐字节比对、负断言、错误码＋params 三要素、行错误顺序、截断计数、双探针即红——4.5），非恒真、非"只编译通过"。

## 发现的问题清单

| # | 级别 | 描述 | 位置 | 处置 |
| --- | --- | --- | --- | --- |
| B-1 | **阻断** | 4.8：units/io.md 落位同步缺失——本任务 ≥4 处 §9.0 等价调整（open 首参 CsvOutputTarget 承载／probe·read 尾随 budgetScope／retainRows＋stopOnFirstRowError 选项／无标识文件嗅探遍＋交付遍两遍形态）与 §3.1 Csv.hpp 行内容清单、§1.2 构建骨架行均未按 DTB §5.4「单元卡增量修订＋变更记录行」登记（AGENTS §6.1 强制项；IO-T01 v0.3／IO-T02 v0.4 先例均为任务内完成） | units/io.md 全文件（diff 零触碰）；对照 Csv.hpp 头注与 c4cfdf06 提交信息 | 返工：补 io.md v0.5 落位同步提交（纯文档、零代码）→新 headSha→attempt 2 全量重验 |
| S-1 | 建议 | io.md 文末落款行仍写"详细设计 v0.3……对应任务 WP-11-T01，构建落位 WP-11-T02 已随 IO-T01 完成"，与卡头 v0.4 及实际进度（IO-T03 送验）不一致——F-122 文档滞后家族残留（先于本任务 diff 存在，非本任务引入） | units/io.md 文末行 | 转登 **F-188**；随 B-1 的 v0.5 修订顺手更正 |

## 验收者环境说明（复现要据）

- 复现命令序列（worktree A 内，除注明外）：①`git worktree add --detach D:/10_Source_Repos/21_robot/_acc_io_t03_tmp/wt-a 64bdbcb0`；②补拷 `RobWorkStudio.ini.template.static`／`.ini.shared.in`（.gitignore `[Bb]in/` 环境缺口）；③`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64 -DCMAKE_TOOLCHAIN_FILE=D:/10_Source_Repos/21_robot/RobWork/vcpkg/scripts/buildsystems/vcpkg.cmake`（exit 0，缓存 `:BOOL=ON` 实测）；④`cmake --build build --config Release --target sdurws_ird_io_test`（exit 0 零 error）＋两模式测试 exe 直跑（各 40/40）；⑤冒烟：`cmake -S RobWork/RobWorkStudio/src/rwslibs/industrialrobot -B ../smoke -DCMAKE_TOOLCHAIN_FILE=<主仓>/vcpkg/...`＋`--build --config Release`（exit 0 零 error）；⑥validate-task／verify-task（双 PASS）；⑦`--target ird_gates` 实跑（exit 1＝SA-10 基线口径，扫描根 wt-a，命中 11 处全基线、零 io）；⑧两枚失效注入（Csv.cpp 剥离步 false 短路／appendFieldText needsQuote 恒 false）即红（4+3 FAILED）＋逐字节还原（`git status`/`git diff HEAD` 空）＋40/40 恢复。
- 验收者自身过程日志留存于 `_acc_io_t03_tmp/`（临时目录，按协议不随记录入库）；关键输出已逐字摘引于本记录各节。
- 独立性：全新上下文；只读代码、契约、设计文档、在库留痕与先行验收记录（IO-T02 r2 记录仅作环境口径与判例校准消费——其结论均未采信，全部本人复现）；未读取实施者任何过程说明。
- 本记录经 §3 记录回传流程落于 `acc/IO-T03/1` 分支并推送；三元组随验收输出回传编排者。
