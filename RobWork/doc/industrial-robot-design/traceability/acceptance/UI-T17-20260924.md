# UI-T17 验收记录（attempt 1，2026-09-24）

| 字段 | 值 |
| --- | --- |
| 任务 | UI-T17 会话入口处理器补全＋宿主菜单融合（O-43 裁决承接；契约 tasks/foundation/UI-T17.json） |
| 送验对象（冻结） | `83aa31a6300f41547e75be99759934b0feba2df4`（分支 wp10-t17；验收全程按 SHA，origin/wp10-t17 与之逐位一致——分支漂移检测通过） |
| diff 范围 | `ac5d107665d8d080b6f88a176ab7f5060acefbe6..83aa31a6`（35 文件：ui 产品面 7＋units/ui.md 1＋traceability 27；含 merge 提交 656b7f30 对齐派发基线） |
| 复现现场 | 全新 detached worktree（`git worktree add --detach` @83aa31a6）；集成模式重新配置（`RWS_BUILD_INDUSTRIALROBOT:BOOL=ON` 已 grep 确认）；独立冒烟模式另配临时目录（带 vcpkg toolchain＋Qt 前缀） |
| 验证脚本 | validate-task PASS（验收者亲跑，worktree 内）；validate-docs PASS（20 units / 12 trace / 188 task files） |
| **verdict** | **fail（阻断 1 项：B-1——契约 verify 第 4 条的关闭确认流/保存链路半区无宿主实证；其余 4.1～4.11 全过，实现面零缺陷检出）** |

---

## 0. 环境说明（复现前提，影响后续验收者）

- **F-323 已登记项在全新工作树 100% 复现**：①configure 因 `RobWorkStudio.ini.template.static` 被 .gitignore 吞掉而失败（以主工作树同名文件手工补置后 configure 通过）；②RobWorkSim 两目标 NOMINMAX 宏冲突——验收者以 `CL="-DNOMINMAX"` 环境变量完成全量构建（注意 Git Bash 下 `CL=/DNOMINMAX` 会被 MSYS 路径转换破坏为 `D:/software/Git/DNOMINMAX`，必须用 `-D` 形式）。
- 集成构建：configure 7.5s＋生成 9.7s，全量 Release 构建零 error（验收者日志 `builds/wp10-t17/acc-attempt1/` 未入库全量日志，结论行见下文 4.2）。
- 冒烟构建：configure 零 CMake Error，全量构建零 error。

## 1. 逐项核查（acceptance-protocol §4，4.1～4.11）

### 4.1 改动范围 —— **通过**

- `git diff --name-status ac5d107..83aa31a`：35 个变更文件逐一对照契约 `allowedFiles`——7 个 `ui/**` 产品文件＋1 个 `units/ui.md`＋27 个 `traceability/**` 留痕文件，全部在 allowedFiles 内；无范围外文件。
- `forbiddenFiles`（REQUIREMENTS.md / ARCHITECTURE.md）零触碰（不在 diff 列表）。
- SA-02：diff 无 `industrialrobot/patches/` 变更＝无框架补丁登记需求；无任何框架源码路径（RobWork/RobWorkStudio 框架层、RobWorkSim）变更。
- ird_gates SA-02 工作区核对（验收者独立跑引擎，head 与 base 两次）无新增命中（见 4.6）。

### 4.2 可复现构建 —— **通过**

- 集成模式（重新配置后）：`cmake --build build --config Release --parallel 8`（CL=-DNOMINMAX）——`error C/LNK/MSB` 计数 **0**（含 sdurws_ird_ui / _plugin / _app / 三测试目标 / RobWorkStudio.exe 全部产出）。
- 独立冒烟模式：`cmake -S <industrialrobot> -B <tmp> -DCMAKE_TOOLCHAIN_FILE=<repo>/vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=<Qt>` 配置零错误＋全量构建零 error（与实施段 smoke-build-r2.log 结论一致；Qt 前缀补充项与 ui.md v1.13-r2 登记口径一致）。

### 4.3 契约 verify 命令 —— **部分通过（B-1 所在）**

| # | 命令 | 结果 | 证据 |
| --- | --- | --- | --- |
| 1 | `cmake --build build --config Release --target sdurws_ird_ui sdurws_ird_ui_plugin sdurws_ird_ui_app` | **成功**（EXIT=0，0 error；三目标＋RobWorkStudio.exe 产出齐备，插件 DLL 位于 `build/RobWorkStudio/libs/Release/sdurws_ird_ui_plugin.dll`） | 验收者 verify1-targets.log |
| 2 | `ctest --test-dir .../ui -C Release -L "^ird$"` | **成功** 2/2（sdurws_ird_ui_test＋sdurws_ird_ui_contract_test） | acc-ctest-ird.log |
| 3 | `ctest --test-dir .../ui -C Release -L "^ird_gui$"` | **成功** 1/1（sdurws_ird_ui_gui_test） | acc-ctest-gui.log |
| 4 | 宿主 GUI 冒烟（加载后菜单可见项目命令＋关闭确认流＋Draining 收口＋保存链路） | **部分执行**：加载＋菜单可见半区**宿主实证通过**（见 4.4 acceptance 2）；**关闭确认流/Draining 收口/保存草稿落盘链路未获宿主实证**（B-1） | acc-attempt1/shots、shots-r2 |

verify 第 4 条的验收段执行经过（全部留痕 `builds/wp10-t17/acc-attempt1/`）：

1. **装载通道实测**（所有者当场授权交互执行后进行）：实施段移交手册推荐的 `--ini-file` 自动装载通道**实测不可用**——QSettings 报 FormatError（"RobWorkStudio.ini file not loaded" 对话框），插件不装载；去空节变体同样 FormatError（**F-324**，与手册 r2 观察段"插件未自动装载"互证、与同手册"实测可用"表述矛盾）。改用框架 `--rwsplugin <dll>` 直载通道（F-320 登记的首通道）：**实测装载成功**——插件 DLL 装载、IRD 工作台 Dock 嵌入呈现、宿主菜单栏 File/Tools/View3D/视图/Plugins 全部出现。
2. **菜单位置取证通过**（详见 4.4 acceptance 2 逐帧证据）。
3. **新建项目入口取证通过**：主页"新建项目"按钮单击后弹出插件自有对话框『新建项目——选择目标目录』（shots-r2/r2-02）。
4. **关闭确认流/保存链路未走通**：SendKeys 盲驱动对『原生文件夹对话框→输入法中文键入→QInputDialog』链条不可靠（原生对话框自动补全/导航吞键、回车语义为进入文件夹而非确认选择，6 次启动、5 版脚本迭代仍无法稳定走完；交互窗口远超所有者授权时长后终止）。**未把未执行项标注为通过**（ACC 4.4）。

### 4.4 契约 acceptance 三条逐项 —— **acceptance 1 部分（B-1）／2、3 通过（3 的冒烟半区同 B-1）**

**acceptance 1（覆写面扩展＋插件侧真实编排＋双模式构建）——代码/测试面全部实证；宿主端到端半区缺失（B-1）**：

| 证据项 | 证据 |
| --- | --- |
| WorkbenchContentDeps 增 saveProjectHandler/closeProjectHandler | 公共头 diff：`ui/include/sdurws/ird/ui/WorkbenchContent.hpp` L138~146 两 `std::optional<ICommandRegistry::CommandHandler>` 字段（中文注释含"未注入＝阶段 A 占位"）；登记循环 `src/WorkbenchContent.cpp` L925~935 两 else-if 分支（描述符/谓词/登记序零变化——分支仅在 has_value() 时覆写） |
| harness 形态不变 | `app/HarnessMain.cpp` 零改动（不在 diff）；grep 证实 harness 未注入两新 handler（占位路径不变） |
| 关闭编排（beginClose→对话框→resolveCloseDialog→Draining） | `plugin/UiPlugin.cpp` orchestrateCloseProject / presentCloseDialog（草稿/任务两轴 QDialog：无草稿行呈现"无未应用修改"、无任务呈现"无后台任务"、可写才可选保存、缺省决议保守化）/ startDrainWatch+pollDrainOnce（QTimer 200ms；ForceConfirmDue→resolveForceCloseDialog；GivenUp/ClosedNow 收口；logic_error 留痕不吞错）——代码审查逐行通过；**宿主端到端未取证（B-1）** |
| saveAllDraftsManual 接线＋DraftController 装配 | UiPlugin.cpp L217~228（sessionDeps.saveAllDraftsManual→saveDraftsNow）、assembleDraftChain（SerialTaskExecutor＋postToDiskThread/postToUiThread Marshal/onSessionDirtyChanged→reportSessionDirty）、openViaSessionController 内 bindSession（BundleCapturingStoreFactory 捕获→dynamic_pointer_cast 取 C-5 双面端口）与 presentContext 清空时 unbindSession |
| forceAbandonAll 显式留痕空兜底 | UiPlugin.cpp L223~230（Dev 留痕"零任务可放弃"——与登记的诚实边界一致，非静默空实现） |
| GUI 测试增覆写路由与项目门控用例 | `gui_test/WorkbenchContentGuiTest.cpp` L389 `SaveCloseEntryOverrideRoutesShellEntries_UI_SPLIT`：门控断言（无项目禁用/可写项目可用）＋两路由断言；在验收者独立复跑 35/35 XML 内（acc-gtest-gui.xml tests=35 failures=0） |
| 双模式构建零错误 | 见 4.2 |

**acceptance 2（宿主菜单融合落位）——通过（宿主帧证据）**：

| 证据项 | 证据（均为验收者亲录帧，acc-attempt1/shots/） |
| --- | --- |
| File 内按位插入『工业机器人项目』子菜单 | `host-smoke-02-file-menu.png`：宿主 File 菜单展开，子菜单精确位于 Reload 与 Preferences 之间（Preferences 分隔符锚生效；规避 updateLastFiles 重排） |
| 子菜单项集 | 代码 L440~251（新建/打开/保存草稿/项目另存为/关闭项目＋最近项目 aboutToShow 重建、失效项保留禁用）；子菜单展开帧因盲导航漂移未截获（B-1 同族取证缺陷，非产品缺陷） |
| Tools 追加命令面板 | `host-smoke-03-project-submenu.png`：Tools 菜单＝框架项（Print Colliding Frames/Workcell Editor）＋尾部『命令面板』 |
| 自建『视图』菜单（三区开关＋恢复默认布局，插入 Plugins 之前） | `host-smoke-05-view-menu.png`：左栏/右栏/底部任务和状态区（勾选态）＋恢复默认布局；菜单栏序 File/Tools/View3D/**视图**/Plugins（视图紧邻 Plugins 之前）全帧可见 |
| Plugins 仅留基类显示开关 | 代码 L419~194（setupMenu 只调基类＋registerHostMenus）；Plugins 菜单展开帧因导航漂移未截获（同上取证缺陷） |
| 使能态全经命令可用性快照（插件零判定） | 代码 L403 setCommandStateObserver→refreshHostMenuActions L575~589（逐 action 用 m_content->commandAvailability 求值；区域勾选只 setChecked 不回环）；addCommandAction L521~536 全部动作入 m_hostMenuCommandIds |
| 菜单只路由命令板 | addCommandAction 触发统一 `m_content->submitCommand(commandId)`（无任何直接业务调用） |
| project.saveAs/draft.apply/undo/redo 维持阶段 A 占位 | WorkbenchContent.cpp 占位分支未改（仅覆写分支新增）；saveAs 在子菜单中仅为占位路由（代码 L244） |
| harness 行为回归零变化 | 35/35 GUI 测试全过（含 harness 同源装配面回归）＋HarnessMain 零改动 |

**acceptance 3（门禁与留痕）——通过（冒烟半区同 B-1 如实登记）**：

- ird_gates 命中集归一化比对零新增：**验收者独立复跑**（head 与 base 各自 detached worktree 同引擎 `cmake -DIRD_ROOT=... -P ird_gates.cmake`，同一归一化规则 GBK 解码＋行内多命中拆分＋根剥离 <ROOT>＋排序去重）：head 151 ↔ base 151，双向差集 **0**；与实施段登记（94↔94，LIB×10/R3×22/R4×2/SUB×56/T1×4）口径一致（归一化粒度不同不影响差集结论）。留痕 gates-head/base-acc.log＋acc-hitset-head.txt。
- ctest 双标签：见 4.3 表（2/2、1/1，验收者亲跑）。
- gtest XML＋ird-test-report.json＋构建日志：在库件逐一核对——gtest-ui-model.xml tests=154 failures=0、contract 22/0、gui 35/0；ird-test-report-{model,contract,gui}.json summary 154/154、22/22、35/35 decisive=true；与验收者独立直跑三 exe 的 XML（acc-gtest-*.xml 154/22/35 failures=0）逐位一致；构建/ctest/validate-task 日志在库齐备（integration-build-r2.log、smoke-configure-r2/smoke-build-r2、ctest-ui-ird/gui.log、validate-task-r2.log）。
- 登记的诚实边界（草稿恢复编排/定时保存心跳/草稿清单 DraftQueryPortStub 恒空/分支锚缺省不可达）与不变项（SA-01/O-38③/SA-02）：ui.md §13 v1.12 立项登记注③⑤＋v1.13 落位登记注＋v1.13-r2 增补注逐项可查（与代码事实一致——分支锚 `core::BranchId{}` 缺省、DraftQueryPortStub 恒空等在代码中如实呈现）。
- 宿主 GUI 冒烟执行状态：实施段"未执行"如实登记＋所有者裁决随验收段补录（ui.md v1.13-r2④）；验收段补录**部分完成**（菜单位置半区通过、关闭/保存半区未执行——B-1）。

### 4.5 测试真实失败能力 —— **通过**

对新增 GUI 测试做两轮"抽改实现→应红→还原→复绿"（验收者自有 worktree 内，未触碰送验对象与任何分支）：

1. 突变 M1：`WorkbenchContent.cpp` 登记 id `"draft.save"` 改为永不匹配的 `"draft.save.never-matches"` → `SaveCloseEntryOverrideRoutesShellEntries_UI_SPLIT` **FAILED**（断言消息"draft.save 未路由到注入的保存编排（UI-T17 覆写面失效）"）。
2. 突变 M2：还原后同理破坏 `"workbench.closeProject"` 匹配 → 同用例 **FAILED**（"workbench.closeProject 未路由到注入的关闭编排"）。
3. 还原后复跑：**PASSED**（1/1）——基线恢复，测试非恒真。

### 4.6 架构红线（AGENTS §5 八条）—— **通过**

1. L2 零 Qt：不适用面（ui 为 R-3 唯一例外单元）；改动文件 include 面逐一核对（见 4.1 清单）：PortAdapters.cpp 新增 include 仅 std 头＋project 公共头，无 Qt（SerialTaskExecutor 的"零 Qt"注释与实现一致）。
2. 业务域互链禁止：改动只在 ui 单元内；ui→project 经 C-3/C-5 冻结端口＋适配器（O-31 既定装配层特权，gates SUB 白名单既有边）。
3. 跨单元私有头禁止：全部 `#include <sdurws/ird/<unit>/...>` 公共头（ProjectStore/DraftService/PersistenceFormat/QueryPort/StoreTypes/Events/Catalog/Factory/Logging 均为对方单元 include/ 公共面）。
4. 框架零源码修改：见 4.1（无 patches 新文件、无框架路径改动）；宿主菜单融合仅经公开 `getRobWorkStudio()->menuBar()` 读写。
5. 名称前缀拼接/剥离：无（gates R4 命中集与 base 一致，唯一命中在 project 单元既有文件，非本任务引入）。
6. 权威唯一：菜单只路由命令板（§4.2），使能态只消费快照（§7.6），无越权实现（保存/关闭语义归 DraftController/UiSessionController，插件只编排）。
7. 不可变历史：本任务不触碰修订面。
8. 未恢复 old/ 实现：全部按 ui.md §5/§7/§8/§11.5 从零实现。

### 4.7 注释规范 —— **通过（附两处文件头滞后，建议级 F-326）**

- 公共头（WorkbenchContent.hpp）新增字段：中文 Doxygen、触发语义、占位回退语义齐备。
- 新增类逐一具备 §2.2/§2.3 要件：BundleCapturingStoreFactory（职责/生命周期/失败语义）、SerialTaskExecutor（背景为什么在装配层/线程约束/stop 有界排空/所有权）、StorePortAdapter 双面增量（分支锚诚实边界、值翻译不改义、异常轨折叠规则）。
- §2.5 高危信息抽查：线程约束（SerialTaskExecutor post 任意线程/stop UI 线程/任务工作线程）；所有权（m_lastStore shared 观察、StorePortAdapter 独占 store）；错误语义（StoreError→返回值轨＋稳定 token、调用方违约原样上抛、logic_error 状态违约就地提示＋Dev 留痕）；确定性/单位（QTimer 200 ms 注明 §9.4 同源；枚举按名直映防布局错位）。
- 缺陷（建议级，F-326）：`UiPlugin.hpp` 文件头 @brief 仍为 UI-T16 任务口径（背景说明"会话入口编排（新建/打开项目…）"未含 UI-T17 的保存/关闭编排与菜单融合）；`PortAdapters.hpp` 文件头仍写"所有权在 HarnessMain 装配序列""harness 未装配 DraftController"——UI-T17 后插件宿主亦装配（UiPlugin::assembleDraftChain），头注滞后于代码。类/函数级注释本身完备，零行为影响。

### 4.8 文档同步＋findings 相关性 —— **通过（同步面）/ 逐条核对结论如下**

- 实现与任务卡零偏差：ui.md §13 v1.13 落位登记注①~⑥＋v1.13-r2 增补注①~④与代码/留痕逐项对得上（本验收逐条复核成立）；§16.7 v1.13/v1.13-r2 行已增；DTB §2.11 WP-10-T17 行（base 已入）与本实现一致；任务状态翻转归收尾环节。
- validate-task PASS（验收者亲跑）。
- **F-318~F-323 逐条相关性核对**（编排者指定）：

| 条目 | 状态/位置 | 与本任务送验面的关联与闭环状态 |
| --- | --- | --- |
| F-318 | open @ acc/UI-T16/1（attempt1，未随 r2 合入主干 findings） | UI-T16 文档表述强于实证——实质已由已合入的 ui.md v1.11 返工登记兑现（证据全量重录＋降级更正）；本任务登记注遵守同一诚实纪律。建议随 findings 清理判定关闭（非本验收辖权）。 |
| F-319 | open（blocking）@ acc/UI-T16/1 | 失实复跑载体——v1.11④ 已删除失实件＋登记 B-2 全新捕获纪律；本任务 r2 留痕全部带新鲜运行签名（gtest XML timestamp 2026-09-24T09:16 与提交时序一致），B-2 纪律被本任务遵守。闭环条件已满足，建议判定关闭。 |
| F-320 | open @ acc/UI-T16/1 | 宿主装载环境前置——v1.11③ 清单＋本任务交接手册实测增补（PATH 需 `build/RobWork/bin/Release`）。验收者实测：PATH 补齐后 `--rwsplugin` 通道装载成功（前置清单有效性获证）；但 `--ini-file` 通道在本机仍不可用（新增 F-324）。**保持 open**（通道可复现性未完全证实）。 |
| F-321 | open @ redesign-main | 切换路径状态投影——本任务新增 openRecentProject/beginSwitch（File『最近项目』→S2 切换流）与该发现同属"带活动会话的切换路径"语义域；本任务实现为该路径首次提供机制（统一确认对话框＋候选验证），但未运行取证（B-1 同因），产品语义裁决仍归所有者。**保持 open**，且其最终定案需要与 B-1 补录同批取证。 |
| F-322 | open @ redesign-main | Dev 日志采样尾段口径——验收者冒烟实测再次命中同族现象：dev-diagnostics.log 进程存活期仅首行落盘，退出后出现 `[sampled-tail]` 标注行（本任务 dev-diagnostics-lastrun.log 在案）。**保持 open**（留痕管线统一处置辖域），本次实测为其补充证据。 |
| F-323 | open @ redesign-main | 全新工作树可复现性——验收者 100% 复现其两个子项（ini.template.static 缺失需手工补置；RobWorkSim NOMINMAX 需 CL 注入），处置口径按其 resolution 执行。**保持 open**（治理辖域）。 |

### 4.9 commit 质量 —— **通过**

- c2c1e0a4（实现）：`[WP-10-T17] 会话入口处理器补全＋宿主菜单融合（O-43 裁决承接）`——四段齐备（What 按 6 模块列点/Why 对应 O-43＋PM-03/04/10＋UX-09/验证含未执行项如实登记/影响面含占位归属与 SA-01/O-38③/SA-02 不变项）。
- 83aa31a6（收尾）：四段齐备；验证段与在库留痕逐件对得上（验收者逐件核对 gtest XML 计数/ctest 日志/门禁三件/冒烟日志）；"宿主 GUI 冒烟移交"如实标注，无粉饰。
- merge 提交 656b7f30 信息如实说明入队基线对齐动机与零冲突核对。

### 4.10 留痕完整 —— **通过（验收段自身留痕随本记录回传）**

- 在库：gtest XML 三件＋ird-test-report.json 三件＋控制台日志＋集成/冒烟构建日志＋ctest 双日志＋ird-gates 三件＋gate-hitset 三件＋validate-task-r2.log＋HOST-SMOKE-HANDOVER.md＋host-smoke-automation.ps1＋host-smoke-autoload.ini.txt（逐一核对存在且计数一致）。
- phase-one-validation.log/unit-status：收尾环节按流程更新（编排者辖权），本验收不越权（UI-T16-r2 先例同口径）。
- 验收段新增留痕随本 evidence 分支入库：`traceability/builds/wp10-t17/acc-attempt1/`（冒烟帧 shots/shots-r2、双通道装载探针帧、验收者 ctest/gates 日志、独立 gtest XML、冒烟脚本两版、dev-diagnostics-lastrun.log、桌面占用探针 desktop-probe.png）。

### 4.11 偷懒/缩水实现扫描 —— **通过（证据表见 4.4；豁免登记见下）**

1. TODO|FIXME|XXX|placeholder|not implemented|未实现 扫描（产品面 diff，+1067 行）：TODO/FIXME/XXX **零命中**；"占位"命中 12 处全部为契约显式的 §7.1/§11.4 阶段 A 占位形态表述（描述占位语义本身，非新增空实现）——契约 note 显式登记 project.saveAs/draft.apply/undo/redo 维持占位（真实接线归 workflow/project 命令网关装配任务），豁免成立。
2. stub 命中 2 处：`SessionTaskPortStub`/`DraftQueryPortStub`——既有冻结类型（UI-T11/T12）的引用，契约 note 显式登记为诚实边界（"草稿清单真实适配（DraftQueryPortStub 恒空）""分支锚端口"），豁免成立。
3. 空实现检测：新增公共函数均有实体（save/tryLoad/discard 值翻译、SerialTaskExecutor 全方法、BundleCapturingStoreFactory、编排族）；唯一"空"体＝forceAbandonAll 留痕空兜底——显式留痕＋登记注③诚实边界，非静默。**发现一处覆盖缺口（建议级 F-327）**：StorePortAdapter 的 C-5 写半区翻译面（save/tryLoad/discard 值翻译与错误折叠）与 SerialTaskExecutor 无直接单元测试（契约仅要求 GUI 覆写用例——非违约；该翻译面当前仅能经宿主端到端验证，恰与 B-1 叠加）。
4. 逐条证据表：见 4.4（acceptance 三条逐项）——acceptance 1 的"关闭编排/保存链路宿主端到端"一条无宿主证据（B-1），其余全部有具名测试/命令输出/文件行号/帧证据。
5. 测试强度：见 4.5（两突变双红——断言校验路由行为与门控边界，非恒真）。

---

## 2. 问题清单

### 阻断级（导致 verdict=fail）

- **B-1 宿主 GUI 冒烟的关闭确认流/Draining 收口/保存草稿落盘链路三个行为无宿主实证**（契约 verify 第 4 条后半＋DTB §2.11 行验收标准句 2/3）。这不是产品缺陷检出（代码审查与单元/契约/GUI 测试面全部通过），而是**验证面缺口**：ACC 4.3"逐条独立执行，全部成功"未满足、4.4"未执行的不得标注通过"。实施段如实登记未执行＋所有者裁决随验收段补录；验收段补录执行了 6 次启动、双装载通道、5 版自动化脚本迭代后仍未能稳定走完（取证管线缺陷归因于 F-324/F-325），未完成的半区按纪律不标注通过。
- **返工输入（如何快速闭环）**：①装载用 `--rwsplugin <dll>`＋PATH 前置（HANDOVER 清单＋`build/RobWork/bin/Release`）——已实测可用；②放弃 `--ini-file` 通道（F-324）；③文件夹选择对话框不要键入路径——预先建目录、键入短名后**鼠标点击『选择文件夹』**（键盘 Enter 是进入文件夹不是确认；原生对话框对长路径/正斜杠/自动补全不可靠）；④中文显示名 SendWait 键入实测可行（r1 帧 09 在案）；⑤dev 日志断言门控不可靠（F-325）——改以**逐帧截图＋状态行**为判据；⑥桌面空闲窗口执行（本验收所有交互帧与脚本在 `acc-attempt1/` 可复用）。补录完成后重新发起**全量验收**。

### 建议级（已按编号顺延转登 traceability/findings.json）

- **F-324** 宿主自动装载 ini 通道失效＋交接手册表述自相矛盾（详见 findings 条目）。
- **F-325** 宿主冒烟自动化取证管线脆弱性（SendKeys 盲驱动/原生对话框吞键/dev 日志存活期仅首行落盘＋[sampled-tail] 采样标注）——F-321/F-322 同族。
- **F-326** UiPlugin.hpp/PortAdapters.hpp 文件头注释滞后于 UI-T17 增量（AGENTS §2.5"注释随代码更新"轻微违反，类/函数级注释完备，零行为影响）。
- **F-327** StorePortAdapter C-5 写半区翻译面＋SerialTaskExecutor 无直接单元测试（建议随草稿链路完整装配任务补齐）。

## 3. 裁决

- **verdict = fail**（阻断 B-1；4.1/4.2/4.5/4.6/4.7/4.9/4.10/4.11 通过，4.4 除 B-1 半区外通过，4.8 通过）。
- 返工范围唯一且明确：完成宿主冒烟关闭/保存半区取证（B-1 返工输入六条）→ 重新发起全量验收。实现代码面本验收零整改要求。
- 验收者未合入、未修改实施产物、未向 wp10-t17 推送任何提交；本记录经 acc/UI-T17/1 分支推送回传。

## 4. 验收者环境

Windows 11 x64（1920×1200），Git Bash＋pwsh 7.6.6，CMake 4.3.1，VS 2022 x64，Qt 6.11.1（D:/software/Qt/msvc2022_64），vcpkg 经典模式（仓库根）。复现现场为 detached worktree（非实施工作树）；集成构建树与冒烟构建树均为全新配置。宿主交互冒烟经所有者（AskUserQuestion 应答 2026-09-24"立即执行"）授权在前台桌面执行。
