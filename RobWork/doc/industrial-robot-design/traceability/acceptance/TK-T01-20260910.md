# TK-T01 验收记录（对抗式验收）

| 字段 | 值 |
| --- | --- |
| 任务 ID | TK-T01（≙WP-02-T01，masterWp WP-B） |
| 契约 | tasks/foundation/TK-T01.json（status: ready，验收 pass 后由所有者翻转） |
| 验收对象 | 分支 `wp02-t01`，commit 范围 `959fb9b..eb31fc9`（head＝`eb31fc9de7c6bd16dad1c4d856b7c994ca2493fa`，与 origin/wp02-t01 一致——分支漂移检测 PASS） |
| 尝试序号 | attempt=1（evidence 分支 `acc/TK-T01/1`） |
| **verdict** | **pass**（4.1～4.11 全部通过，无阻断级问题；3 条建议级问题已转登 findings.json F-006/F-007/F-008） |
| 验收日期 | 2026-09-10 |
| 独立性声明 | **降级模式（PIPE v1.7 §0.4）**：所有者指令"禁止使用子智能体"（2026-09-10），本验收与实施同会话顺序执行——独立性由"模型级全新上下文"降级为同会话角色切换。补偿措施：复现全部亲手重跑（不复用实施日志结论）、对抗式清单不缩减、以 headSha 冻结产物为唯一裁决输入；发现的问题如实转登，不采信实施阶段自述。 |
| 验收者环境 | Windows 10.0.26200 x64、Git Bash、VS2022＋CMake 4.3、pwsh、vcpkg 经典模式（gtest:x64-windows@1.18.0）；独立 worktree A（detached @ eb31fc9）复现，对照 worktree C（detached @ base 959fb9b） |

---

## 逐项证据（acceptance-protocol v1.5 §4，4.1～4.11）

### 4.1 改动范围 —— PASS

`git diff --name-only 959fb9b..eb31fc9` 共 11 文件：

- **allowedFiles 内（7）**：`industrialrobot/CMakeLists.txt`、`testkit/CMakeLists.txt`、`testkit/include/sdurws/ird/testkit/{README.md,TestPaths.hpp}`、`testkit/src/TestPaths.cpp`、`testkit/test/{TestPathsTest.cpp,BuildRedLineTest.cpp}`；
- **文档同步与留痕（4）**：`units/testkit.md`（偏差登记，4.8 强制项）＋`traceability/builds/wp02-t01/` 3 份（留痕，4.10 强制项）——沿用 CORE-T01 先例（core.md §10.2＋builds/wp03-t01 同样随任务分支提交并经验收）；
- forbiddenFiles（REQUIREMENTS.md/ARCHITECTURE.md）零触碰；`patches/` 无新文件（SA-02：无未登记框架改动，diff 无任何框架源码文件）。

### 4.2 可复现构建（验收者亲手重跑） —— PASS（范围限定说明见 F-008）

**集成模式（worktree A 全新配置，非实施构建树）**：
```
$ cmake -S <accA>/RobWork -B <accA>/build -G "Visual Studio 17 2022" -A x64 \
    -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=<vcpkg>/... -DCMAKE_PREFIX_PATH=<Qt6.11>
  → configure-exit=0，缓存 RWS_BUILD_INDUSTRIALROBOT:BOOL=ON 确认
$ cmake --build <accA>/build --config Release --target sdurws_ird_testkit_test
  → 任务范围目标从零构建零错误（sdurws_ird_testkit.lib＋sdurws_ird_testkit_test.exe）
$ <accA>/build/.../sdurws_ird_testkit_test.exe → 10/10 PASSED
```
**冒烟模式（worktree A 全新临时目录）**：configure-exit=0（standalone smoke mode 确认）＋build-exit=0；测试 10/10 PASSED。
**全量构建**：实施侧主构建树（build/，历史框架产物在位）全量 Release exit=0（日志已随任务提交）；全新树全量构建在 **RobWorkSim RWSimulatorPlugin** 失败——**对照实验**：base 959fb9b 的 worktree C 单建同目标复现同簇失败（71 处 error C，`Math.hpp` min/max 宏污染 C4003 先兆）→ 预存基线缺陷，与本任务 diff（仅 industrialrobot＋文档，与 RobWorkSim 零共享翻译单元）无关，转登 F-008；不构成本任务 4.2 失败。

### 4.3 契约 verify 命令 —— PASS

```
$ cd <accA> && pwsh -File RobWork/scripts/industrialrobot/verify-task.ps1 \
    -TaskFile RobWork/doc/industrial-robot-design/tasks/foundation/TK-T01.json
  → verify-task: PASS TK-T01（verify[0] cmake --build build --config Release
    --target sdurws_ird_testkit_test 于 worktree 根执行成功）
```

### 4.4 契约 acceptance 逐项 —— PASS

| # | 条目 | 证据 |
| --- | --- | --- |
| 1 | 两模式构建零错误；数据根解析（env/默认/缺失三态）用例通过 | 双模式 10/10（integration/smoke XML 各 10 用例全绿）；TestPaths 组 7 用例：EnvVarOverridesDefault（env 态原样返回）、DefaultFallbackWhenRootExists＋DefaultFallback_Adaptive（默认回落，双分支强断言）、EnvUnsetAndDefaultMissing_Throws（缺失态→TestKitError(env-unavailable)，纯函数＋公共 API 双层）、EmptyEnvTreatedAsUnset、DatasetAndToleranceDirLayout、ErrorCarriesKindTokenAndDetail |
| 2 | CR-07：gtest 安装＋版本登记于 DTB §4.4 定稿行 | 安装实证 `vcpkg/installed/vcpkg/info/gtest_1.18.0_x64-windows.list`；§4.4 定稿行原文在位（"首次安装执行：2026-09-10 随 WP-03-T01（≙CORE-T01）安装 gtest:x64-windows@1.18.0……两模式接入成功"）——首装先期完成，本任务 testkit 侧以同机制接入并核验登记，ird-test-report.json framework 字段载明 |
| 3 | find_package 失败→blocked，不引入第二框架 | 未触发（两模式配置均成功）；机制在位：testkit/CMakeLists.txt:32 `find_package(GTest CONFIG REQUIRED)`（失败即配置终止）；无 RW::gtest/源码 vendor/第二渠道引入（diff 与链接清单核验） |

ird_gates：**未执行——门禁随 WP-01-T01 落地，尚未建成**（如实标注，非本任务缺陷）。

### 4.5 测试真实失败能力（验证验证者） —— PASS

验收者于 worktree A 亲手执行（非引用实施侧自检）：
- 探针①：`testkit/src/QtProbe.cpp`（`#include <QWidget>`）→ `KitIncludesOnlyCoreAndSelf_T2_NFR_MNT_01_R3` 变红；
- 探针②：`core/include/sdurws/ird/core/Probe.hpp`（`#include <sdurws/ird/testkit/TestPaths.hpp>`）→ `ProductSourcesZeroTestkitInclude_T1` 变红；
- 探针删除后复跑：10/10 复绿。
另核验实施侧自检记录（ird-test-report.json）：TestPaths 变异（忽略 env）2 用例变红后还原复绿——共 4 断言具备真实失败能力，超出 ≥2 要求。

### 4.6 架构红线（AGENTS.md §5 八条） —— PASS

①L2 零 Qt：testkit 本体零 Qt 头（用例＋配置期守卫＋源码阅读三重）；②业务域互链（R-1）：testkit 仅链 core（T-2 唯一许可边，O-21 落位即建立该边——契约 knownPitfalls 预期行为）；③跨单元私有头（R-2）：公共头仅在 include/sdurws/ird/testkit/，无跨单元私有 include；④前缀拼接（R-4）：无名称处理逻辑；⑤权威归属（PA-1）：数据根解析归 testkit（§4.6 唯一入口），未越权；⑥不可变历史（PA-2）：N/A；⑦old/ 恢复：无；⑧框架零源码修改（SA-02）：diff 无框架文件，patches/ 零新增。CR-06 陷阱遵守：零 core 容差头消费（TestPaths 仅 std 头）。

### 4.7 注释规范（AGENTS.md §2） —— PASS

公共头全查（TestPaths.hpp）：文件头（@file/@brief/设计依据/背景/偏差说明）、TestKitError 类与五枚举逐个 Doxygen、每个函数 @brief/@param[in]/@return/@throws 齐备；高危信息：错误语义（助记码映射）、线程约束（env 串行说明）、确定性（纯函数声明）均在位。实现抽查 ≥3：TestPaths.cpp（语义段落注释、#error fail-fast 理由、switch 而非数组映射的防错位理由）、testkit/CMakeLists.txt（每个决策块中文注释＋设计依据引用）、两个测试文件（用例级"追踪"字段＋fixture 注释）。魔法值无未解释项。

### 4.8 文档同步 —— PASS（附发现）

三处实现细节偏差已按 DTB §5.4 登记（units/testkit.md 变更记录 v0.3：TestKitError 前置、detail 接缝、testdata 实体随 TK-T03）；契约 status=ready 待所有者合并时翻转。findings 核对：F-001/002/003 已 fixed 无关；**F-004** 相关性确认（testkit 目标同样出现空转 MOC 步骤——实证，留构建约定所有者）；**F-005** 同型转义损毁在 testkit.md 亦存在（"AI 执行就绪补充"节 `$(testkit.ToUpper())` 未展开、`alidate-docs.ps1` 缺 v、"标记为 locked"应为 blocked）→ 转登 **F-006**；另登记 **F-007/F-008**（见问题清单）。

### 4.9 commit 质量 —— PASS

两提交均 `[WP-02-T01] 动词开头`标题＋四段正文（What/Why/验证/影响面）；验证结论与留痕一致（失败能力自检如实记录、ird_gates 如实标注未执行）；Acc-Covered 标注在位（07c9cab: 1,3；eb31fc9: 2）。

### 4.10 留痕完整 —— PASS（附说明）

`traceability/builds/wp02-t01/`：gtest XML ×2（integration/smoke）＋ird-test-report.json＋构建日志 ×3。`phase-one-validation.log` 未随任务分支更新（head 已冻结无法追加）——resultNote 等价物＝ird-test-report.json（含全量结论与 NOT RUN 清单）；建议收尾治理提交时补登 TK-T01 摘要行（对齐 CORE-T01 配套留痕先例）。

### 4.11 偷懒/缩水实现扫描 —— PASS

①diff 范围 `TODO|FIXME|stub|placeholder|未实现|not implemented` 扫描（验收者亲手）：**零命中**；②空实现检测：公共接口函数全部有真实逻辑（toToken 全枚举 switch＋不可达兜底、resolveDataRoot 两级判定、目录拼装）；③逐条证据表：见 4.4（三条 acceptance 全部有证据）；④测试强度：断言校验行为与边界（env 原样返回逐字符比对、缺失态锚定前置断言、SourceTreeReachable 防空集恒真、双分支自适应均强断言）＋4.5 失败能力实证。

---

## 问题清单

### 阻断级

无。

### 建议级（不阻塞合入；已逐条转登 findings.json）

| # | 问题 | 转登 |
| --- | --- | --- |
| G-1 | testkit.md"AI 执行就绪补充/单元任务退出条件"两节转义损毁（F-005 同型：`$(testkit.ToUpper())` 未展开、`\v`/`\b` 被吞致 `alidate-docs.ps1`/`locked`）——详设所有者随下次增量修订重写 | F-006 |
| G-2 | 全新集成树配置所需参数未载明于 AGENTS §4.1（vcpkg toolchain＋Qt CMAKE_PREFIX_PATH）；且框架必需的 `RobWorkStudio/bin/RobWorkStudio.ini.template.static` 被 `.gitignore [Bb]in/` 规则吞掉，全新克隆/worktree 配置必卡（本验收以环境补齐方式复制该文件后配置成功） | F-007 |
| G-3 | 基线框架缺陷：RobWorkSim RWSimulatorPlugin 全新树从零构建失败（Math.hpp min/max 宏污染簇，C4003 先兆→C2059/C2039 连锁）——base 959fb9b 对照复现同样失败，主树为存量增量产物掩盖；将阻塞全新克隆/CI 全量构建（WP-01-T02 前置裁决，框架侧修复须按 SA-02 先登记 patch） | F-008 |

## 环境与复现说明

- worktree A（detached @ eb31fc9）＝验收复现现场；worktree C（detached @ base 959fb9b）＝预存缺陷对照现场；两者验收后即删。
- F-007 的环境补齐（ini template 复制）不改变送验对象内容（该文件不入 git，仅补配置输入）。
- 本记录经 acc/TK-T01/1 分支回传（ACC §3 v1.4）：commit 于该分支，推送后输出三元组。
