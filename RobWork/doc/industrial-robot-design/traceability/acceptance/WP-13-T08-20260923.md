# WP-13-T08 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | WP-13-T08 实现建模命令处理器族与物理合法性断言分域（unit=modeling，interUnit=true；knownPitfalls=O-35/P-MDL-8；需求 MDL-06/MDL-12） |
| 送验对象（冻结） | 分支 `wp13-t08` 尖端 `83d2b63fb0122353bde06e602b8075180f239f4e`（验收全程按该 SHA） |
| 分支漂移检查 | 验收启动时 `git fetch origin` 后 `origin/wp13-t08` = `83d2b63f…` ＝ 送验 SHA ＝ 本地检出 `wp13-t08` 尖端，**零漂移**（ACC §3） |
| diff 范围 | `fddf851b9316e5a1a855f725ef0e08756795eb0f..83d2b63fb0122353bde06e602b8075180f239f4e`（3 个提交：adc0475f 九码登记＋工作集增量；9a36f397 就绪校验器与五命令处理器实现＋测试；83d2b63f 单元卡 v0.9 同步与留痕；base＝验收请求指定，即 wp13-t07 合入收尾提交、`origin/redesign-main` 尖端） |
| **verdict** | **pass**（4.1～4.11 全部通过，无阻断级问题；建议级 2 项转登 findings F-316/F-317） |
| 验收执行方式 | 验收者全新上下文独立复现：detached worktree @ 送验 SHA 亲手重配重跑全部构建/测试/脚本/门禁与两处变异注入（4.5）；未采信实施者任何运行结果（ACC §2 四条） |
| 复现现场 | `D:/acc_tmp_t08/A`（detached @ 83d2b63f，集成模式 build/ ＋ 冒烟模式 /d/acc_tmp_t08/smoke）；记录回传于 `D:/acc_tmp_t08/B`（分支 `acc/WP-13-T08/1`） |
| evidence 分支 | `acc/WP-13-T08/1`（基于 `origin/redesign-main` = fddf851b；记录 commit＝本分支提交 40 位 SHA，由编排者登记三元组） |

---

## 4.1 改动范围 —— 通过

`git diff --name-only fddf851b..83d2b63f` 共 30 文件，逐一对契约 `allowedFiles` 核对：

- `RobWork/RobWorkStudio/src/rwslibs/industrialrobot/modeling/**`：12 文件（CMakeLists.txt 改；include/…/{CommandHandlers,DiagCodes,Readiness,Template}.hpp——前两新增、后两改；src/{CommandHandlers,DiagCodes,Readiness}.cpp——前两新增、DiagCodes 改；test/CommandFixtures.hpp、test/CommandHandlersTest.cpp、test/ReadinessTest.cpp 新增，test/DiagCodesTest.cpp 改；contract_test/CommandPipelineContractTest.cpp 新增）——全在 modeling/** 内。
- `RobWork/doc/industrial-robot-design/units/modeling.md`（改，升 v0.9）——在 allowedFiles。
- `RobWork/doc/industrial-robot-design/traceability/**`：17 文件（builds/wp13-t08/ 留痕：集成/冒烟构建与配置日志＋integration-ctest＋gtest XML 四件＋ird-test-report 双模式两件＋门禁 base/branch 归一命中集与 diff＋validate-task/validate-docs 日志）——在 allowedFiles `traceability/**` 内。
- **forbiddenFiles 零触碰**：REQUIREMENTS.md、ARCHITECTURE.md 不在 diff；任务契约 WP-13-T08.json 未被触碰（status 翻转归治理收尾）。
- **SA-02**：diff 无 modeling 之外任何框架源码文件；`industrialrobot/patches/` 在范围内零改动（`git diff <range> -- …/patches/` 为空，在库仍为 0001-rwslibs-entry-option.patch＋PATCHES.md 两件）。

## 4.2 可复现构建 —— 通过（两模式均验收者亲手从零重配重跑）

- **集成模式**（acc_tmp_t08/A，源＝detached worktree @ 83d2b63f）：
  - 配置：`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON` ＋ 主树 vcpkg toolchain ＋ Qt6 前缀（与主树缓存同参）。结果 `Generating done`、`CMake Error` 计数 0；缓存确认 **`RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`**。
  - 环境前置（非本任务产物缺陷——F-007/F-009/F-099/F-101 环境链家族，本次再次实证）：worktree 内无 `vcpkg/`（toolchain 指向主树复用已装包）；`RobWork/RobWorkStudio/bin/RobWorkStudio.ini.template.static`＋`RobWorkStudio.ini.shared.in` 被 `.gitignore` 的 `[Bb]in/` 规则吞掉不入库，首次配置报 `CMake Error: File … does not exist`——从主树复制这两个 gitignored 环境件后配置通过（与 F-099 登记口径一致）。
  - 任务相关目标构建（验收者亲手）：`sdurws_ird_modeling`、`sdurws_ird_modeling_test`、`sdurws_ird_modeling_contract_test` 三目标退出码均 **0**，`: error|错误 C|error LNK` 计数 **0**。
  - 验收者亲跑两 exe：`sdurws_ird_modeling_test` → **139/139 PASSED**（17 suite；含 T08 新增 20 条：MdlReadiness 5＋MdlReadinessAssertionDomains 4＋MdlReadinessTravelLimit 2＋MdlCommandHandlers 4＋MdlCommandPrepare 5）；`sdurws_ird_modeling_contract_test` → **9/9 PASSED**（MdlBuildGraph 5＋MdlCommandPipelineContract 4）。与在库留痕 gtest XML（integration 侧 `tests="139"`/`tests="9"`，failures=0）一致。
- **独立冒烟模式**（/d/acc_tmp_t08/smoke，`-S RobWork/RobWorkStudio/src/rwslibs/industrialrobot` ＋ `-DCMAKE_TOOLCHAIN_FILE=<主树 vcpkg>/scripts/buildsystems/vcpkg.cmake` ＋ Qt `CMAKE_PREFIX_PATH`——AGENTS §4.1 修正口径）：配置＋全量构建退出码 **0**、错误计数 0。冒烟亲跑：`sdurws_ird_modeling_test` → **119/119 PASSED**（T08 两 TU 按 `TARGET sdurw_kinematics` gating 不编入——CMakeLists.txt:252-263 注释与行为一致）；`sdurws_ird_modeling_contract_test` → **5/5 PASSED**（BuildGraph 子集）。与在库 gtest XML smoke 侧（119/5）一致。

## 4.3 契约 verify 命令 —— 逐条亲手执行（7/7 执行完毕；门禁按"零新增命中"口径判定）

| # | 命令 | 结果 |
| --- | --- | --- |
| 1 | `cmake --build build --config Release --target sdurws_ird_modeling` | 退出码 0，零错误 |
| 2 | `cmake --build build --config Release --target sdurws_ird_modeling_test` | 退出码 0，零错误 |
| 3 | `cmake --build build --config Release --target sdurws_ird_modeling_contract_test` | 退出码 0，零错误 |
| 4 | `ctest --test-dir build/RobWorkStudio/src/rwslibs/industrialrobot/modeling -C Release -L "^ird$"` | **2/2 passed**（unit＋contract_test） |
| 5 | `cmake --build build --config Release --target ird_gates` | 门禁引擎运行后按存量命中以退出码 1 终止——命中集为**存量基线红**（39 条唯一存量命中）。验收者以门禁码＋目标名 ASCII 签名集归一比对（绕开留痕乱码行拼接干扰——F-065/F-309 载体问题）：**本次亲跑命中签名集与送验 gates-branch-hits-norm.txt 完全一致（41=41 签名组、39 唯一命中）**；base..branch 零新增由范围一致性交叉证实——本范围对 CMake 图的唯一改动是向既有目标追加源文件（无新增 target/链接边），源面新增文件全部在 modeling（验收者直扫零 Qt include、零 RobWork 名称字面量），modeling 面命中恰 3 条均为在案存量：IRD-GATE-LIB `sdurws_ird_modeling→pugixml::pugixml`（F-302，T05）＋IRD-GATE-SUB 两条 modeling 测试目标直链 testkit（F-292，T02）。送验 gates-hit-set-diff.txt「EMPTY（39=39）」与独立证据链吻合。注：T08 契约 verify 数组已列全三目标构建行——F-300/F-304/F-310/F-314 家族问题在本契约**未再现** |
| 6 | `pwsh -File RobWork/scripts/industrialrobot/validate-task.ps1 -TaskFile …/WP-13-T08.json` | `validate-task: PASS (1 tasks)`，退出码 0 |
| 7 | `pwsh -File RobWork/scripts/industrialrobot/validate-docs.ps1` | `validate-docs: PASS (20 units, 12 trace entries, 186 task files)`，退出码 0（与送验 validate-docs.log 逐字一致） |

## 4.4 acceptance 条目逐条证据 —— 5/5 有对应证据（行号以送验 SHA 内容为准，验收者亲读亲跑）

| acceptance 条目 | 证据 |
| --- | --- |
| 1. Readiness/CommandHandlers 落位（卡 §3.3 表 T08 行）；IModelReadinessChecker 按卡 §9.4.4 签名（check 纯函数、L0→L11 分层、三组稳定排序——层号→对象 id 字典序）；分层表逐层 UT（短路优先级；L11 策略不可解析报 Blocking 不静默跳过行程校验）；就绪校验与 prepare 共用同一 AssertionSuite（D-MDL-11/NFR-MNT-04） | 落位：`Readiness.hpp`（328 行）＋`CommandHandlers.hpp`（720 行）在卡 §3.3 表 T08 行对应路径；CheckContext 三字段逐字落位（Readiness.hpp:224-232）。check 纯函数：`MdlReadiness.LegalWorkingSetAllLayersPassAndPureFunction`（ReadinessTest.cpp:79-112，同输入两次 check 逐字段相等 ：96）。分层与短路：`ShortCircuitPriorityStopsAtFirstBlockingLayer`（:120-139，L0 阻断→L1..L11 明细＝"未执行"）；L11 策略不可解析：`L11PolicyUnresolvableBlocksWithoutSilentTravelSkip`（:147-177，blocking note 含"策略不可解析""行程校验未执行"双断言）。稳定排序：`ResultGroupsStableOrderByLayerThenSubject`（:185-217，装配序 B→A 输出按对象 id 字典序）。共用 AssertionSuite：ReadinessChecker 与 handler 基类均持同一 `AssertionSuite`（Readiness.hpp:67 include＋CommandHandlers.cpp:608 构造序），判定数学唯一落套件与 InertiaMath/policy 评估器——验收者确认无第二套判定式（Readiness.cpp 文件头"零第二判定式"经逐行核对属实） |
| 2. 断言分域反例 UT（V-15）：①m≤0（MASS-NONPOSITIVE）②惯量非对称/非 SPD（INERTIA-NOT-SPD）③qmin≥qmax（LIMIT-INTERVAL）——就地阻断＋精确定位（subject＋localName＋比较型三要素）；④物性全缺失＋已确认有限范围 continuous→可应用（缺失走预告、continuous 豁免） | ①`MassNonpositiveBlocksPrecisely`（ReadinessTest.cpp:229-260）：code/subject=linkOid/localName="link1"/comparison actual=-5.0 kg、expected=0 kg 逐项断言＋L5 层归属。②`InertiaNotSpdBlocks`（:266-291，diag(-1,2,2)→λmin=-1，kg·m² 单位断言）。③`LimitIntervalBlocks`（:297-318，(2π,π)→actual=2π/expected=π/rad）。④`MissingPhysicsWithConfirmedContinuousApplicable`（:325-345，ReadyWithNotes＋blockers 空＋warnings 非空（PHYSICS-MISSING）＋confirmables 空）。prepare 侧反例④前半：`HardAssertBlocksWithPreciseDiagnostic`（CommandHandlersTest.cpp:435-474，RANGE-NOT-FINITE 就地阻断＋localName="Jspin" 定位） |
| 3. 行程上限确认放行 UT（V-16）：±1080°→IJointLimitEvaluator.evaluate（④端口 resolvePolicy——阈值唯一来源 policy JointThresholds 默认 4π，本地无第二常量）→MDL-06-TRAVEL-LIMIT ConfirmableFinding（实际行程/阈值/单位 rad 三要素）→确认放行/拒绝；policy 配置阈值后放行 | `MultiTurnJointProducesConfirmableFinding`（ReadinessTest.cpp:357-390，±6π→confirmables 恰 1、actual=12π/expected=4π/rad、state=Pending、cause 含 policyContentId）。边界与配置放行：`BoundaryTravelAndConfiguredThresholdPass`（:397-422，T=L=4π 不超限＋16π 阈值下 12π 场景放行）。prepare 侧④端口消费观测：`TravelExceededProducesConfirmableViaPolicyPort`（CommandHandlersTest.cpp:398-428，resolveCalls==1＋摘要含"行程上限待确认"）。阈值唯一来源：CommandHandlers.cpp 无任何 4π/阈值字面量（验收者 grep 实证，唯一 kFourPi 命中在 T07 既有 Template.cpp:362 模板默认限位、非本任务 diff、非行程阈值） |
| 4. 五命令处理器族注册与 prepare 管线 UT（卡 §9.3）：无点 token 五个；requiresDualCompile 逐命令表（apply-named-poses=false）；prepare：decode 失败→RejectedInvalidInput、基线防御性复核、新对象经 ctx.objectId() 分配、inverse 快照式逆载荷（D-MDL-9）、中文命令摘要 | token 表与双编译表：`CommandTokenTableDotFreeAndDualCompileTable`（CommandHandlersTest.cpp:100-130，五 token 逐字＋无点＋长度界）。注册：`RegistryRegistrationAndDuplicateRejection`（:170-189，registry.size==5＋重复注册 throw invalid_argument）。decode 失败：`MalformedPayloadRejectedInvalidInput`（:346-364）。防御性复核：`BaselineDefensiveRecheckFailsFast`（:370-392）。取号：`FirstApplyPlansWithAllocatedIdsAndSummary`（:201-241，objectId isValid＋中文摘要"应用机器人设计"＋首次应用 inverse nullopt＋compile.callCount==0）。inverse 快照：`ReplaceApplyProducesSnapshotInversePayload`（:248-289，Restore 载荷＝前一 (oid, canonical 字节) 原样）。named-poses：`NamedPosesSkipsDualCompileAndBackfillsRef`（:295-340，requiresDualCompile=false＋poseSetRef 回填） |
| 5. 双编译失败零修订与未就绪不编译 UT（契约测试）：V-19 Blocking→RejectedHardAssert 且编译端口零调用；V-20 故障注入编译失败→Failed(compile-failed)、HEAD 不变、修订计数不变、RT-*＋MDL-* 一并入目录；V-22 stale；T08 行九码注册不预建 | V-19：`V19NotReadyDoesNotCompileAndPlanEmptied`（CommandPipelineContractTest.cpp:93-129，RejectedHardAssert＋callCount==0＋计划清空）。V-20：`V20CompileFailureInjectsAndDiagnosticsCoexist`（:146-205，计划面 requiresDualCompile=true＋CompileRequest 装配＋故障注入 ok=false＋RT-COMPILE-FAILED 与 MDL-* 同面共存）——"HEAD 不变/修订计数不变"的全管线面归 project S5 已验契约（PRJ-TX-3，验收者在 project/test/CommandContractTest.cpp:4,13,23 证实该契约在库），modeling 侧以 HandlerContext 接缝验证并**已按 DTB §5.4 登记卡 §14.6 v0.9 ⑤a**——偏差登记在案，非静默缩水（R-2 禁跨单元私有头使全管线注入面不可得，理由成立）。V-22：`V22StaleBaselineFailsFastDefensively`（:217-245，fail-fast＋拒绝后处理器可复用；apply-retained 保留归 project 已验契约——同段登记）。九码注册：`T08StableCodesRegisteredWithoutPrebuilding`（:257-300，九码可查＋TRAVEL-LIMIT confirmable=true/requiresComparison=true＋硬断言码 confirmable=false＋T09/T13/T18 行缺席不预建＋重复注册拒绝） |

## 4.5 测试真实失败能力 —— 通过（验收者两处变异注入，均如期变红；变异在临时 worktree A 内实施并已还原复绿，未触任何分支）

1. **变异 A（断言有效性）**：CommandHandlers.cpp:176 `if (!(eig[0] > 0.0))` → `if (false && …)`（惯量 SPD 断言失效）→ 重建后 `MdlReadinessAssertionDomains.*` **1 FAILED**（`InertiaNotSpdBlocks`）——该测试非恒真。
2. **变异 B（短路优先级）**：Readiness.cpp:338 `if (hasBlocking) { shortCircuited = true; }` → `if (false)`（短路失效）→ `MdlReadiness.ShortCircuitPriorityStopsAtFirstBlockingLayer` **1 FAILED**——分层短路语义被测试钉住。
3. `git checkout --` 还原两文件后重建：**139/139＋9/9 复绿**（还原走 git checkout，mtime 刷新，F-068/F-075 mtime 陷阱未触发）。

## 4.6 架构红线（AGENTS §5 八条）—— 逐条自查通过

1. **L2 计算内核零 Qt**：modeling src/include 全树 Qt include/类型扫描**零命中**（Q_OBJECT/QWidget/QString/Qt 头均无；唯一"Qt"字样在 BuildRedLineTest.cpp:93 注释内说明判据本身）。
2. **业务域互链禁止（R-1）**：modeling 内无 kinematics/trajectory/dynamics/selection/optimization/requirements 任何 include（grep 零命中）；policy/project/core/runtime 消费均属卡面登记的端口/实现面（IJointLimitEvaluator ④端口、project ICommandHandler 实现——卡 §9.3 原文）。
3. **跨单元私有头禁止（R-2）**：本任务 4 文件全部跨单元 include 均为 `include/sdurws/ird/{core,policy,project}/…` 公共头（验收者逐条列举核对）；唯一私有 include 是本单元内 `src/InertiaMath.hpp`（特征值单一实现——合规）。
4. **框架零源码修改（SA-02）**：见 4.1（patches/ 无新文件、框架源码零触碰）。
5. **不做名称前缀拼接/剥离（R-4）**：新增文件零 "RobWork" 字面量操作；行程评估的名称解析经注入的 IPolicyNameContext（CommandHandlers.hpp:159-168 R-4 注与实现一致）。
6. **权威唯一（PA-1）**：新对象身份唯一经 `ctx.objectId()` 取号（CommandHandlers.cpp:1083；处理器零其他写路径）；行程阈值唯一来自 policy JointThresholds（ARC-05——本地无常量，4.4 条目 3 已证）；④端口诊断透传不产码（CommandHandlers.cpp:896-901"注册码借道数据流——非本单元产码"）。
7. **不可变历史（PA-2）**：处理器只产出 CommandPlan；inverse＝快照逆载荷（新修订恢复历史字节，不改写）；双编译失败路径零修订（V-19/V-20 契约钉住）。
8. **不继承 old/ 实现**：diff 无 old/ 路径引用；实现按卡面从零编写（与旧 robotmodelbuilder 无符号/结构关联）。

## 4.7 注释规范 —— 通过（公共头全查＋实现文件抽查 4 个，AGENTS §2 执行到位）

- **公共头全查**：Readiness.hpp/CommandHandlers.hpp——文件头含设计依据（卡章节＋需求 ID＋契约 acceptance 引用）与背景说明；每个类/结构体/函数均有中文 Doxygen（职责/参数方向与单位/返回/异常/线程/确定性/所有权），§2.5 高危信息全齐（rad/kg·m² 单位、非 owning 标注"调用方保证存活期覆盖"、线程约束"仅命令执行线程"、"纯函数；确定性（NFR-COR-02）"）；DiagCodes.hpp 九码常量逐码注明卡面行、任务列、SA-15/ARC-05 依据；Template.hpp v0.9 增量字段注明表尾追加与向后兼容。
- **实现文件抽查（4 个 ≥3）**：Readiness.cpp（逐层注释含"这一步的业务含义"与短路语义）、CommandHandlers.cpp（prepare 五步管线注释与 §9.3 管线图逐步对应；魔法数字 %.17g 注明"位级可往返"来源）、DiagCodes.cpp（逐码描述符逐字段注明卡面出处与词表投影）、CommandFixtures.hpp（替身设计依据与线程约定）。
- **反例扫描**：无复述式注释、无中英机翻腔、无过期注释（发现的一处注释失实为计数问题，见 4.9/F-316——属"注释与实现不同步"轻微形态，不在必填缺失项）。

## 4.8 文档同步 —— 通过（实现与任务卡零未登记偏差；findings 未关闭条目相关性核对完成）

- 卡 v0.9（83d2b63f）：§3.3 表 T08 行落位登记＋§9.5 表尾增登 `MDL-READINESS-PHYSICS-MISSING` 行＋§14.6 v0.9 全量变更记录（含六项偏差与范围登记 ⑤a～⑤f——V-20 全管线面归 project S5、L10 字段集完整半段呈现级、L6 探测归 io/runtime、MDL-ASSERT 拦截面口径、行程校验命令域门控、HandlerServices 落位形态）。验收者逐项核对偏差均已在卡登记且理由成立（DTB §5.4 口径），无"代码先行、文档失实"。
- 追踪矩阵：trace-matrix.json `MDL-06→WP-13-T08` 行在库（验收者读取确认）；任务契约 status 翻转归治理收尾（先例一致）。
- **findings.json 未关闭条目相关性核对（ACC v1.2）**：258 条 open 中与 modeling/本任务相关者逐一过筛——F-292（major，modeling 测试目标直链 testkit 存量）、F-302（pugixml LIB 存量）均系 T02/T05 在案存量、所有权在治理侧词表/白名单扩登记，本任务无可消账面；F-300/F-304/F-310/F-314 家族（契约 verify 缺目标构建行）在 **T08 契约已自愈**（verify 数组列全三目标——本家族未再现的正面证据）；F-293/F-298/F-299/F-303/F-308/F-313（提交标题名词短语家族）本任务三提交均动词开头，未再现；F-309/F-065（留痕乱码家族）本任务 gates 留痕仍为乱码载体（同族延续，验收者已绕开复现结论，随 F-207 家族统一处置）；F-007/F-062/F-098/F-099/F-101（全新树配置环境链）本次验收再次实证（ini 模板缺件），仍 open、所有权在文档/治理侧。无 open 条目声称由本任务修复——无静默积压。

## 4.9 commit 质量 —— 通过（三提交标题动词开头、正文四段齐备、验证结论与验收者独立复现吻合）

- adc0475f「登记断言分域九稳定诊断码并扩展建模工作集闭包部件视图」／9a36f397「实现模型就绪分层校验器与五命令处理器族及断言分域套件」／83d2b63f「同步单元卡 v0.9 落位登记并留存双模式验证证据」——均为 `[WP-13-T08]` 前缀＋动词开头（**F-012 家族未再现**）。
- 三提交正文均含改动内容/改动原因（对应卡章节与需求 ID）/验证/影响面四段；**计数全部与验收者复现一致**（139/139＋9/9 集成、119＋5 冒烟、新增 20 条用例按 suite 拆分 5+4+2+4+5＝20——F-025/F-041 计数失实家族未再现；留痕文件逐件列出且在库实存——F-077/F-080/F-102/F-103 家族未再现）。
- 一处轻微注释计数失实转登 **F-316**：DiagCodes.hpp `modelingCodeDescriptors` @brief 仍写"当前 16 项……T08 行八码"，实际工厂已含 17 项（MDL-READINESS-PHYSICS-MISSING 增登行的计数与列举未同步；同库 DiagCodes.cpp 文件头注与 DiagCodesTest 均为 17 码口径，测试以 17 码封闭断言通过——纯注释滞后，零行为影响）。AGENTS §2.5"注释随代码更新"之轻微违反，建议级。

## 4.10 留痕完整 —— 通过（gtest XML＋构建日志＋validate 日志齐备且与复现一致；ird-test-report.json 两件不完整，转登 F-317）

- gtest XML 四件：`tests="139"/"119"/"9"/"5"`、failures 全 0、时间戳 2026-09-23——与验收者亲跑结果逐位一致。
- 构建日志：integration-build.log（0 错误）、integration-ctest.log（2/2）、smoke-configure/smoke-build.log（exit=0、0 错误）、validate-task/validate-docs 双 PASS 日志——均在库且口径吻合。
- **ird-test-report.json 不完整（F-317）**：`ird-test-report-integration.json` summary 仅 **1/1**（testId 恰为 `MdlReadiness.ResultGroupsStableOrderByLayerThenSubject` 单条——过滤运行残留物，非全量 139 条）；`ird-test-report-smoke.json` 仅 **5/5**（恰为冒烟契约测试 BuildGraph 五条，缺 modeling_test 119 条报告）。gtest XML 完整可信故不构成粉饰或证据缺失，但 AGENTS §4.2 要求的 ird-test-report.json 留痕件与"全量运行"口径不符（对照 T07 先例：其 ird-test-report-integration.json summary 为 119/0 全量）。
- phase-one-validation.log 未更新：该文件自 WP-03-T01 后冻结（`git log` 末次 da40b761），本单元 T05/T06/T07 已验收先例同以 `traceability/builds/wp13-tXX/` 留痕集承载 4.10——口径一致，非本任务缺陷。

## 4.11 偷懒/缩水实现扫描 —— 通过

1. **标记扫描**：diff 范围新增行 `TODO|FIXME|XXX|HACK|not implemented|未实现|占位实现|暂不实现` 扫描**零命中**（"placeholder-cylinder"系卡 §5.2 设计概念 methodTag，非 stub；契约亦无"触发式延后"标注需豁免项）。
2. **空实现检测**：新增公共接口函数体抽查全部有实质实现（如 makeModelReadinessChecker→make_unique<ModelReadinessChecker>；五命令钩子 1119-1434 行逐命令槽形状校验/取号/引用挂载/编码；AssertionSuite 六断言方法均含完整判定与产码逻辑）；无"仅返回默认值"的公共函数。
3. **逐条证据表**：见 4.4——acceptance 五条全部给出具名测试/行号/命令输出，任一无证据即 fail 之要求满足。
4. **测试强度**：断言校验行为与边界（比较型三要素逐值、字典序相对序、短路逐层"未执行"注记、九码封闭性＋他行缺席、重复注册拒绝、评估器调用计数）；非恒真、非"只编译通过"——与 4.5 两处变异注入联动证实会红。

---

## 发现的问题清单

| 级别 | 编号/位置 | 描述 | 处置 |
| --- | --- | --- | --- |
| 阻断 | 无 | —— | —— |
| 建议 | F-316 | DiagCodes.hpp 工厂 @brief 计数失实："当前 16 项／T08 行八码"，实际 17 项九码（PHYSICS-MISSING 增登行漏计；DiagCodes.cpp 头注与 DiagCodesTest 均为 17 码口径） | 治理/实施侧随手勘误（随下批 modeling 触碰顺带修正）；不阻塞合入 |
| 建议 | F-317 | traceability/builds/wp13-t08/ird-test-report-integration.json 仅含 1 条用例（过滤运行残留）、ird-test-report-smoke.json 仅含契约测试 5 条（缺 modeling_test 全量报告）；gtest XML 四件完整可信 | 实施侧留痕管线修正：全量运行后从构建目录收取两 exe 的 ird-test-report.json 再入库（可随返工窗口或下批任务顺手补正）；不阻塞合入 |

## 验收者环境说明

- Windows 10（10.0.26200 x64）；VS 17 2022（MSVC 1942）；CMake 4.3；pwsh 7；Qt 6.11.1（msvc2022_64）；vcpkg 经典模式（主树 installed/x64-windows，gtest 1.18、pugixml 1.16）。
- 复现全程按送验 SHA（83d2b63f）detached worktree 执行；集成配置参数＝AGENTS §4.1 ＋ F-007/F-098/F-099 环境链修正口径（vcpkg toolchain＋Qt 前缀＋ini 模板环境件复制）；构建以 `--parallel 8` 执行（与契约命令语义等价的并行化参数）。
- 独立性声明：验收者未读取实施者任何过程叙述；全部结论以送验 SHA 的产物、在库留痕与亲手复现输出为准；未向 `wp13-t08` 分支推送或修改任何内容（变异注入在临时 worktree 内实施并已还原）。
