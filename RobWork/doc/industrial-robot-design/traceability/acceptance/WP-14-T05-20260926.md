# WP-14-T05 验收记录（attempt 1，2026-09-26）

| 字段 | 值 |
| --- | --- |
| 任务 | WP-14-T05（实现就绪校验（R0~R9）与预览/正式分离及需求命令族；单元 requirements） |
| 送验对象（冻结 SHA） | `70ddd39de47a9aa4248b09860bdac2d4aed141c2` |
| base | `5df21a81af1859b1b3bf9ee2233f6da50a80eede`（state.json 双确认） |
| diff 范围 | `5df21a81..70ddd39d`（3 提交：170d3d0d → b6a3e3ed → 70ddd39d） |
| 验收环境 | detached worktree @ 70ddd39d（复现现场）；集成全新构建树；独立冒烟构建树；base 侧临时 worktree @ 5df21a81（比对后即删）——全部由验收者亲手执行，未采信实施者运行结果 |
| **verdict** | **pass**（4.1~4.11 全部通过；无阻断级问题；建议级 1 项 F-368 已转登 findings.json，不阻塞合入） |

---

## 4.1 改动范围 —— 通过

- `git diff --name-only 5df21a81..70ddd39d`：22 个变更文件，全部落在契约 `allowedFiles` 内——
  - `requirements/**` 10 个（CMakeLists.txt；include 三头 Readiness/CommandHandlers/DiagCodes；src 三 TU Readiness/CommandHandlers/DiagCodes；test 三件 ReadinessTest/CommandHandlersTest/DiagCodesTest）；
  - `units/requirements.md` 1 个；
  - `traceability/builds/wp14-t05/` 11 个留痕文件。
- `forbiddenFiles`（REQUIREMENTS.md/ARCHITECTURE.md）：零触碰（不在 diff 列表）。
- SA-02：`git diff --stat 5df21a81..70ddd39d -- …/industrialrobot/patches/ RobWork/ RobWorkSim/ 框架其余路径` 为空——patches/ 两文件（0001-rwslibs-entry-option.patch、PATCHES.md）在 base 与 head 逐字节一致，零框架源码改动。

## 4.2 可复现构建 —— 通过（两模式零错误，验收者全新构建树亲手复现）

- **集成模式**：worktree 缺 gitignored 模板（F-062 既有登记的已知前置）——自主检出复制 `RobWorkStudio.ini.template.static`/`RobWorkStudio.ini.shared.in`/`bin/none/RobWorkStudio.ini.template` 后重新配置：
  `cmake -S <worktreeA>/RobWork -B <acc_t05_build> -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=<vcpkg>/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`
  → exit 0；`grep RWS_BUILD_INDUSTRIALROBOT CMakeCache.txt` → `:BOOL=ON`（验收树实测）。
  构建 `sdurws_ird_requirements`＋`sdurws_ird_requirements_test`＋`sdurws_ird_requirements_contract_test`（Release）→ **exit 0，error C/LNK/fatal 计数 0**（日志留存 acc_t05_build_int.log）。无 RobWorkStudio.exe 进程锁干扰，未动用 taskkill。
- **独立冒烟模式**：`cmake -S <worktreeA>/…/industrialrobot -B <acc_t05_smoke> -DCMAKE_TOOLCHAIN_FILE=<vcpkg>/vcpkg.cmake -DCMAKE_PREFIX_PATH=<Qt6>`（Release）→ configure exit 0；构建三目标 exit 0、零错误；两测试 exe 直接运行 108/108＋5/5 全绿。
- 本机无实施侧构建树复用——两条构建树均为本次验收从零配置。

## 4.3 契约 verify 命令 —— 通过（3/3 逐条亲手执行）

| # | 命令（契约原文） | 结果 |
| --- | --- | --- |
| 1 | `cmake --build build --config Release --target sdurws_ird_requirements` | 执行（验收树路径代入）→ exit 0，零错误 |
| 2 | `ctest --test-dir build/RobWorkStudio/src/rwslibs/industrialrobot/requirements -C Release -L "^ird$"` | 执行 → **2/2 passed（100%）**（sdurws_ird_requirements_test、sdurws_ird_requirements_contract_test） |
| 3 | `pwsh -File RobWork/scripts/industrialrobot/validate-docs.ps1` | 执行 → **PASS（20 units, 12 trace entries, 189 task files）**，exit 0 |

补充（验收者侧正常补建动作，F-368 家族口径）：契约 verify 数组未显式列出两测试目标构建行——验收者在两条构建树自行 `--target sdurws_ird_requirements_test sdurws_ird_requirements_contract_test` 补建后执行测试，全绿；已按建议级转登 F-368（见文末）。

## 4.4 acceptance 条目逐项证据 —— 通过（8/8；具名证据均为验收者实跑产出）

| # | 契约 acceptance | 证据（验收者实测） |
| --- | --- | --- |
| 1 | R0~R9 分层校验逐层用例＋短路优先＋三级断言＋六具名稳定码 | ReadinessTest.cpp 22 用例：R0（RootRefDanglingAndTokenMismatch/DuplicateSlotReference）、R1（RefFrameTargetMissing/OrientationRuleTargetTokenMismatch）、R2（RefFrameKindIllegalInSceneSlot）、R3（PoseValueViolations）、R4（StationBindingDangling）、R5（EmptyRequiredSetWarning——Warning 不阻断）、R6（DegenerateAndPlanMismatch Blocking＋ZeroPlanWarningAndNoRegionNotApplicable）、R7（SequenceViolations）、R8（ToolAndEnvironmentRefDangling）、R9（SchemaVersionUnsupported）、ShortCircuitStopsAfterFirstBlockingLayer（短路序断言：R3 Blocking＋R7 违例并存时最高发现层=R3）；具名码逐字断言于用例内（REF-MISSING/POSE-ILLEGAL/NO-REQUIRED-CASE/PLAN-DEGENERATE/PLAN-MISSING/SEQ-CYCLE 等）＋DiagCodesTest.T05ReadinessDescriptorsMatchSection96Rows（12 描述符全量逐字段）＋FactoryScopeIsStagedRows（封闭清单恰 12，5→12 随附同步）。集成/冒烟两模式 108/108 全绿实测 |
| 2 | ReadinessSummary 数据源（①级输入门禁） | SummaryListsAllInvalidMustItems_ACC2（两条非法 Must 全量列出＋升序）；SummaryProjectionMatchesEvidenceShape_ACC2（投影与 `evidence::ReadinessSummary{valid,invalidMustItems[]}` 聚合字面逐字段相等——对端值类型＝evidence/Verdict.hpp:137-142 实文两字段） |
| 3 | Must/Should 分级＋enabled 口径 | MustShouldGrading_ACC3：①启用 Must 非法→valid=false＋清单；②Should 条目非法→结构性 Blocking 但不进 Must 清单；③enabled=false 条目非法也不阻断不列清单（「任一启用的 Must 条目」口径）。V-12：就绪面 API 类型面无 EngineeringStatus 输出（Readiness.hpp 全文核） |
| 4 | 预览不产生正式证据与结果对象 | PreviewIsPureWithoutEvidenceObjects_ACC4：重复 check 逐字段相等＋工作集/上下文值快照不变＋类型面 static_assert 无 ContentIdentity/修订/证据对象字段 |
| 5 | O-39 处置（现场重算、不持久化） | O39ReentrantProjectionWithoutPersistence_ACC5（双实例交差调用同结果＋重入同结果＋投影两字段纯值静态断言）；`readinessSummary(check(ws,ctx))` 签名（Readiness.hpp:322）零 out 参数零静态状态 |
| 6 | 命令族（O-35 无点 token、prepare 管线、快照式逆命令、溯源断言、现场重估） | CommandHandlersTest 10 用例全绿：CommandTokensArePointFreeAndRegisterable（`apply-requirement-set`/`apply-requirement-import` 无点字面＋真实注册表装配）；PayloadRoundtripAndDamageRejection（magic IRDRCM1 破损/版本拒绝）；FirstApplicationPlanned（首次应用无前版＝inverse nullopt）；IncrementalReplaceProducesSnapshotInverse（增量替换→前版字节逆载荷往返）；ReadinessBlockingRejectsPrepare（RejectedHardAssert＋REQ-READY-INPUT-INCOMPLETE 汇总一条）；ReadinessWarningDoesNotBlock；ImportRequiresProvenanceCompleteness（I-REQ-8）；RestorePathWritesBytesVerbatim；DefensiveAndShapeRejections；CandidateReevaluatedAgainstBaselineDrift（基线漂移现场重估拒绝）。CommandHandlers.cpp:645-646 实文 `requiresDualCompile=false`/`confirmableFindings.clear()`（恒空——SA-15 不私设） |
| 7 | P-REQ-6 三字段边界声明 | PReq6ReportCarriesNoGateAction_ACC7：结构化绑定 `auto [layer, level, diag]` 编译期证明恰三字段（DomainReadinessItem 实文恰 layer/level/diag）；hasBlocking() 为事实查询非门控动作；产码全部在 StableCodeRegistry 注册断言 |
| 8 | 双模式构建零错误＋ctest 全绿＋留痕＋ird_gates | 见 4.2/4.3/4.10 与下方 ird_gates 专段——全部验收者实跑达成 |

**ird_gates base..head 归一化比对（编排者口径，验收者独立复算）**：引擎直跑 `cmake -DIRD_ROOT=<两侧各自源根> -P cmake/ird_gates.cmake`——head 侧 127 行、base 侧 127 行 IRD-GATE 记录（两侧 exit=1 均为 DTB §4.5 存量例外既有状态）；绝对路径归一化为占位后排序比对，**diff 为空（IDENTICAL_HIT_SETS）——零新增门禁命中**。requirements 两测试目标的 SUB 命中（直链 testkit）在 base 侧已存在（T02 期 F-359 登记既有），非本任务引入。与送验附件 `ird_gates_base_head_comparison.md` 宣称的 127=127 IDENTICAL 逐格吻合。

## 4.5 测试真实失败能力 —— 通过（3 处实质变异全部真变红，还原后复绿）

变异窗口＝worktreeA 源树＋冒烟构建树增量重建；每次变异→具名测试真实变红→`git checkout --` 还原→重建→全量 108/108 复绿；最终 worktreeA 源树 `git status` 干净（零残留）。

| 变异 | 注入点 | 变红测试（实测输出） |
| --- | --- | --- |
| M1 短路顺序失效 | Readiness.cpp R3 收尾 `if (r3Blocking)` → `if (false && r3Blocking)` | `ReqReadiness.ShortCircuitStopsAfterFirstBlockingLayer_ACC1` FAILED（“短路优先：最高发现层＝首个 Blocking 层（R3），后层不执行”断言不满足） |
| M2 快照逆命令槽恒空 | CommandHandlers.cpp buildInverseSlots 前版存在性判断追加 `|| true`（逆槽集恒空→inverse 丢失） | `ReqReqCommands.IncrementalReplaceProducesSnapshotInverse_ACC6` FAILED（同 filter 组内其余 9 例不受扰仍绿） |
| M3 R6 零计划 Warning 分支失效 | Readiness.cpp `if (!noRegions && noPlans)` → `if (false && …)` | `ReqReadiness.R6ZeroPlanWarningAndNoRegionNotApplicable_ACC1` FAILED |

三次还原后全量复跑：108/108＋5/5 全绿（冒烟树实测输出留档）。测试非恒真、有真实检出力。

## 4.6 架构红线（AGENTS §5 八条）—— 通过

1. **L2 零 Qt**：requirements include/src 全树 grep `Qt|Q_OBJECT` 唯一命中为 Errors.hpp 注释文字（“计算内核零 Qt”），零 Qt 头/链接；新增两 TU 同样零 Qt。
2. **业务域互链（R-1）**：全树 `#include <sdurws/ird/...>` 清点——仅 requirements 自有＋core/diagnostics/project/io/evidence 五条 §3.2 登记边（test 文件另含 testkit）；零 modeling/kinematics/trajectory/dynamics/selection/optimization include；浅校验只读 ObjectRef 的 oid/token 两字段（Readiness.cpp 实现要点③），不解码 modeling 字节。
3. **跨单元私有头（R-2）**：跨单元 include 全部为 `include/sdurws/ird/<unit>/` 公共头（evidence/Verdict.hpp、project/QueryPort.hpp、project/CommandService.hpp 实存于各单元 include 树——evidence 公共头清单实测核过）。
4. **框架零修改（SA-02）**：见 4.1——patches/ 与框架零改动。
5. **名称前缀（R-4）**：新增 TU 零 RobWork 名称拼接/剥离代码（grep 零命中；名称解析未越权）。
6. **权威唯一（PA-1）**：身份取号仅经 `ctx.objectId()`（CommandHandlers.cpp:723/759 注释与实现一致）；就绪判定语义在本单元、evidence 只消费投影（值类型直投）；单位/文案经 core/diagnostics 既有面。
7. **不可变历史（PA-2）**：inverse＝前版字节快照逆命令、撤销产生新修订；首次应用无前版＝nullopt（空历史语义）。
8. **不继承 old/ 实现**：从零编写（本单元前序任务同口径），diff 内无 old/ 引用。

## 4.7 注释规范（AGENTS §2）—— 通过

- **公共头全查**：`Readiness.hpp`（338 行）与 `CommandHandlers.hpp`（364 行）文件头中文 Doxygen 完整——设计依据（卡章节/需求号/契约 acceptance 逐条）＋“第一读者须知”背景段＋层-码映射逐层登记＋O-35/O-39 处置段＋线程安全/确定性声明；每个类型/函数/成员带 `@brief`/方向/单位语义/异常语义（`@throws std::invalid_argument` 调用方违约、`@throws std::logic_error` R9 防御面，错误二分清晰）；高危信息（线程约束“仅命令线程”、所有权“调用方持有”、确定性来源“集合规范序遍历”）逐项在位。
- **实现文件抽查 ≥3**：`Readiness.cpp`（1055 行，文件头“实现要点”四条＋逐层段落注释＋每个魔法上下文带语义）、`CommandHandlers.cpp`（902 行，prepare 五步管线逐步注释＋“实现决策登记”段）、`DiagCodes.cpp`（diff +168 行，逐码 §9.6 行号标注＋分批纪律注释）——均达标。
- **测试文件**：每用例中文说明对应 acceptance 编号（ACC1~ACC7）＋需求/AT 追溯（IRD_TEST_INFO 携带 REQ-06/EVI-01/NFR-MNT-04/V-20 等）。

## 4.8 文档同步＋findings 消账核对 —— 通过

- 单元卡 `units/requirements.md` v0.5（2026-09-26）行登记与实文逐项对查为真：§3.3 表 Readiness.hpp/CommandHandlers.hpp 两行（卡面实文核过）；§9.6 T05 六码标〔已落位〕＋表尾增登 `REQ-READY-PLAN-MISSING` 行（含 §8.1 R6“Warning（零计划）”分支依据与 modeling WP-13-T10 先例——增登合法性有据）；层-码映射决策（REF-MISSING 就近复用/SEQ-CYCLE 承载 R7 悬空前驱）在 §9.6 表行原文即有依据（“按‘引用悬空’语义就近承载同码，层标注区分”/“悬空前驱按层-码对齐以本码承载、cause 区分”），非实现私设；R8 Warning 分支随 T07 增补的边界声明（现值模型无承载字段、不私建状态语义——NFR-COR-03）如实；DiagCodesTest 分批封闭断言 5→12 随附同步与实文一致（kStagedSection96Codes 恰 12 项＋descriptors.size()==12 断言）；§12 evidence 行“ReadinessSummary 数据源〔已落位〕”与 workflow/ui 行“DomainReadinessItem 恰三字段”落位标记同步。卡头文档版本行 v0.5 与 §14.6 末行同步（F-133 家族无再现）。
- 追踪矩阵/状态：validate-docs PASS（12 trace entries）；任务状态翻转归治理收尾（PIPE 口径），本次不涉及。
- **findings.json 未关闭条目相关性核对**：在库 360 条、最大号 F-367、open 298 条。与本任务直接相关者：F-358/F-367（契约 verify 缺测试目标构建行家族——本任务契约同型缺口，见 F-368 转登）、F-359/F-360/F-361/F-364/F-366（T02~T04 期门禁命中登记/计数/标题家族——本任务三提交均 `[WP-14-T05]` 前缀＋动词开头、计数与实测逐项一致，无新家族实例）；F-005/F-006/F-058/F-078（他卡转义损毁）与本任务无涉；F-007/F-009/F-062（worktree 环境前置）本次复现属实但既有条目已覆盖，不重复登记。无静默积压。

## 4.9 commit 质量 —— 通过

- 170d3d0d `[WP-14-T05] 实现需求就绪校验器 R0~R9 分层校验与 ReadinessSummary 投影面`／b6a3e3ed `[WP-14-T05] 实现需求命令处理器族 apply-requirement-set/import 与就绪现场重估`／70ddd39d `[WP-14-T05] 同步单元卡落位登记与验证留痕（双模式构建/测试/门禁证据）`——三提交均 `[任务编号] 动词开头`（F-012 家族零再现），正文 What/Why/验证/影响面四段齐备＋Acc-Covered 对账行。
- 验证结论与实测逐项一致（防粉饰核对）：108/108＋5/5、ctest 2/2、validate 双 PASS、门禁 127=127 零新增、留痕 11 件清点无缺失无幻影（全部真实入库，`git diff --name-only` 实证）；提交内散文计数（22 用例/10 用例/5→12/127 行）与代码及附件实文逐项吻合，无 F-041/F-236/F-360 家族计数失实。

## 4.10 留痕完整 —— 通过

`traceability/builds/wp14-t05/` 11 件全部入库且计数与验收者独立复现一致：两 gtest XML（tests=108/failures=0、tests=5/failures=0——与本次实跑 XML 同值）、ird-test-report 两份（total=108/passed=108、total=5/passed=5）、冒烟构建日志（exit=0）、门禁 base/head 原始输出各 127 行＋归一化命中集各 127 行＋比对结论、README（verify 三命令逐条执行记录）。gtest XML 原样留存、无手工转抄痕迹（F-346/348 家族无再现）。

## 4.11 偷懒/缩水实现扫描 —— 通过

- ① diff 新增行 `TODO|FIXME|stub|placeholder|未实现|not implemented` 扫描**零命中**（grep exit 1）；无“触发式延后”豁免需求（R8 Warning 分支随 T07 增补已在卡面 §14.6⑦ 登记，非偷懒）。
- ② 空实现检测：新增公共接口（check/readinessSummary/prepare/decodeAndPlan/encode/tryDecode 等）实现体全部实质（Readiness.cpp 1055 行/CommandHandlers.cpp 902 行）；无仅返回默认值的桩。
- ③ 逐条证据表：见 4.4——acceptance 8 条逐条注明具名测试/命令输出/实文行号，零缺证。
- ④ 测试强度：断言校验行为与边界（短路序、全量清单不抽样、enabled 口径、值快照不变、往返逐字节、基线漂移拒绝）；4.5 三处变异实证测试会红，非恒真。

---

## 发现问题清单

**阻断级**：无。

**建议级（已转登 traceability/findings.json）**：

- **F-368**：F-300/F-304/F-310/F-314/F-338/F-344/F-345/F-350/F-351/F-352/F-358/F-367 家族新例——`tasks/foundation/WP-14-T05.json` verify 数组仅列 `sdurws_ird_requirements` 构建目标，缺 `sdurws_ird_requirements_test`/`sdurws_ird_requirements_contract_test` 两条显式构建行（ctest 行在位）；全新构建树按清单字面执行时第 2 条 ctest 将因测试 exe 未构建而 Not Run。验收者侧补建两目标后 108/108＋5/5 全绿（本次 4.2/4.3 实测）；建议任务契约模板为含测试目标的任务补显式构建行。

## 裁决

**verdict＝pass**。4.1~4.11 全部通过、无阻断级问题；建议级 1 项（F-368）已转登 findings.json，不阻塞合入。合入决策归所有者（本验收者不合入、不改实施产物、未向任务分支推送任何提交）。

## 验收者环境说明

- 复现现场：`git worktree add --detach` @ 70ddd39d（基线 5df21a81）；集成构建树与冒烟构建树均为本次新建；base 侧临时 worktree 比对后即删；记录经 acc/WP-14-T05/1 分支推送回传。
- 环境前置（F-062/F-007 既有登记事实）：worktree 检出后自主复制 RobWorkStudio.ini 三件模板；配置参数含 vcpkg toolchain＋Qt6 前缀（主树缓存同款）。
- 本机代理不稳定风险按编排者口径以显式 refspec 推送并 ls-remote 核验。
