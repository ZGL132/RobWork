# DIAG-T10 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | DIAG-T10（测试替身与契约套件：ScriptedDiagSource＋DT-\* 用例体） |
| 送验对象（冻结） | `76c128d7351200e00d5792271007e38d69113ff2`（分支 wp09-t10 尖端；全程按 SHA 验收） |
| diff 范围 | `13fc8fa9ac00f5c268c982beade70fc395dbe9fe..76c128d7`（2 提交：a71e42ee 实现＋76c128d7 留痕登记；merge-base＝origin/redesign-main@13fc8fa9） |
| 验收日期 | 2026-09-15 |
| 裁决 | **pass**（4.1～4.11 全通过、零阻断级问题；建议级 4 项已转登 findings.json F-114～F-117，不阻塞合入） |
| 验收者 | 独立验收会话（全新上下文，未消费实施叙述；构建/测试/门禁/脚本全部亲手重跑） |

---

## 一、裁决

**pass。** 送验 SHA `76c128d7` 的实现、测试、留痕、文档同步全部经验收者从零复现一致；建议级问题（F-114～F-117）均为治理/流程面瑕疵，不涉实现正确性，按 ACC §5 转登并随记录留档，不阻塞合入。

---

## 二、逐项证据（ACC §4，4.1～4.11）

### 4.1 改动范围 —— 通过

`git diff --name-status 13fc8fa9..76c128d7` 共 13 文件：

- `industrialrobot/diagnostics/**` 7 件：CMakeLists.txt（M）、test/{CollaborationQueryTest,DiagContractCheckTest,ScriptedDiagSourceTest}.cpp、test/ScriptedDiagSource.hpp、test/README.md（均 A）、test/LinkageContractTest.cpp（M）——契约 allowedFiles 第 1 条 ✓
- `traceability/**` 5 件：builds/wp09-t10/{integration,smoke}-gtest-diagnostics{,-contract-test}.xml＋ird-test-report.json——allowedFiles 第 3 条 ✓
- `units/diagnostics.md`（M）——allowedFiles 第 2 条 ✓

forbiddenFiles（REQUIREMENTS.md/ARCHITECTURE.md）零触碰；框架源码零改动、patches/ 无新文件（SA-02 ✓）；无未登记文件。

### 4.2 可复现构建 —— 通过（双模式从零亲手复现）

- **集成模式（干净构建树重新配置）**：worktree A（detached @ 76c128d7）。配置命令含 `-DRWS_BUILD_INDUSTRIALROBOT=ON -DRWS_USE_STATIC_LINK_PLUGINS=ON -DCMAKE_TOOLCHAIN_FILE=<主仓>/vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`——CONFIGURE_EXIT=0（Configuring done 83.3s，零 CMake Error）；缓存 grep 确认 **`RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`**。worktree 缺主树未跟踪本地辅助件 `RobWorkStudio.ini.shared.in`/`RobWorkStudio.ini.template.static`（F-007/F-062/F-099 在册缺陷），自主树补拷至 worktree 内 `RobWork/RobWorkStudio/bin/`（环境修复，不触及送验产物——DIAG-T05~T09 验收同先例）。
  - `cmake --build build --config Release --target sdurws_ird_diagnostics_test sdurws_ird_diagnostics_contract_test`：BUILD_EXIT=0，`error C/fatal error/error LNK` grep 计数 **0**；两测试 exe 落位 `build/RobWorkStudio/bin/Release/`。口径沿 DIAG-T05~T09 先例：构建两测试目标及其依赖链（diagnostics＋core＋testkit＋框架库），覆盖本任务全部改动面。
- **独立冒烟模式（另配临时目录 `acc-diag-t10-smoke`，带 vcpkg toolchain——F-002 口径）**：配置＋`cmake --build` SMOKE_EXIT=0，零 error（grep 计数 0）；两测试 exe 亲跑 115/115＋26/26 PASSED。
- **测试亲跑**：集成树 `sdurws_ird_diagnostics_test.exe` → **115/115 PASSED**（exit 0，467 ms）；`sdurws_ird_diagnostics_contract_test.exe` → **26/26 PASSED**（exit 0，24 ms）；冒烟树两 exe → **115/115＋26/26 PASSED**（exit 0）——与送验留痕"双模式 115/115＋26/26"一致。

### 4.3 契约 verify 命令 —— 通过

契约 `verify` 数组仅一条，验收者在 worktree A 根逐字亲跑：

```
cmake --build build --config Release --target sdurws_ird_diagnostics_test   → VERIFY_EXIT=0（零 error）
```

另亲跑 `verify-task.ps1 -TaskFile <契约>` → **verify-task: PASS DIAG-T10**；`validate-task.ps1` → **PASS (1 tasks)**，均 exit 0。

### 4.4 acceptance 条目逐项证据 —— 通过（3/3 有具名测试与亲测证据）

1. **§10 验证矩阵逐条执行通过并留痕**：送验 XML 顶格核对——integration/smoke 双模式 `sdurws_ird_diagnostics_test` tests=115 failures=0 errors=0（21 套件：DiagnosticsBuild 5＋DiagCodesRegistry 12＋DiagCodesVocabulary 2＋DiagCodesCoreBaseline 1＋DiagCodesTextKeys 1＋DiagnosticsErrors 1＋DiagCatalogFactory 11＋ErrorCodeTranslatorTest 5＋DiagnosticsSinkShape 2＋DiagnosticsEnvelopeBaseline 1＋DiagCatalogLifecycle 7＋DiagCatalogArchiveFailure 2＋ConfirmableTest 13〔DT-CFM-1~5〕＋DiagAggregation 11〔DT-AGG/CHAIN〕＋DiagLogging 12〔DT-LOG〕＋Redaction\* 17＋CrashReportWrite 4〔DT-SEC/LIFE-4〕＋DiagCatalogLifecycle 族〔DT-LIFE-1~3〕＋**DiagCollaboration 8〔DT-COLLAB-1＋DT-AT 七映射，本任务新增〕**）；`_contract_test` tests=26（Linkage 3＋ConfirmableProjectStub 6〔DT-CFM-6~10〕＋DiagHostTraceStub 5〔DT-LIFE-5〕＋**ScriptedDiagSourceSuite 3＋ScriptedDoubleBoundary 3＋DiagContractCheck 6〔本任务新增〕**）。矩阵→文件落位全表见 test/README.md §3（DT-REG/DIAG/DUP/CAT/AGG/CFM/CHAIN/COLLAB/LOG/SEC/LIFE 全组＋DT-AT 映射，DIAG-T02~T10 累积收口）。ird-test-report.json 20 新用例逐条 trace（映射 acceptance/需求/矩阵行）。验收者双模式亲跑计数一致——**未执行测试零虚标**。
2. **ScriptedDiagSource 替身就位＋替身边界声明在案＋产品目标不链 testkit**：替身落位 `test/ScriptedDiagSource.hpp`（统一序列步进回放：条目经真实工厂 create→目录 append、finding 经真实 ConfirmableService create、日志经真实 LoggingPipeline log——EV-REG-3"被测产品对象不做替身"）；边界声明三面在案＝test/README.md §1 全文（五条）＋替身头文件头声明＋ScriptedDoubleBoundary 机检 3 用例（亲跑通过）。产品目标链接面：CMakeLists.txt L96 `target_link_libraries(sdurws_ird_diagnostics PUBLIC sdurws_ird_core)` 唯一单元边；配置期守卫（L282~300，产品目标 LINK_LIBRARIES/INTERFACE 出现 core/diagnostics 之外 sdurws_ird_\* 即 FATAL_ERROR）＋LinkageContractTest.UnitEdgeOnlyCore_DT_BUILD_R1_R2 白名单断言＋替身符号机检三面钉住——亲跑全部通过。
3. **P-DIAG-8 处置**：契约测试以桩＋契约夹具验证——DiagContractCheckTest 6 用例消费 testkit `checkDiagnosticRecord`/`checkComparativeFields`（`_contract_test` 链 `sdurws_ird_testkit`＝testkit.md §2.4 T-1 允许形态 `{被测产品目标, sdurws_ird_testkit, gtest}`；testkit.md §10.2 L835 diagnostics 行"ContractCheck（DiagnosticRecord）"交接承接核实）；execution 侧零文件触碰（diff 实证）；双库集成冒烟按 P-EX-8 另行登记——governance-log.md L121 P-DIAG-8 状态仍 open（本任务如实不消账，与契约原文一致）。

**ird_gates（如实登记，亲跑）**：`cmake --build build --config Release --target ird_gates` → exit 1，**命中 8 项＝wp09-t09 基线 7 项＋新增 1 项**，与送验 ird-test-report.json gates 段逐条一致：IRD-GATE-SUB×6（**sdurws_ird_diagnostics_contract_test→sdurws_ird_testkit〔本任务新增，acceptance 3 明文消费面〕**＋evidence/evidence_contract/policy/policy_contract/runtime 五处既有）＋IRD-GATE-SUB 表外边×1＋IRD-GATE-T1×1（testdata_lint，F-019 既有）；引擎自测 pass_clean/pass_r4comment＋fail_r1/t1/r5/sub/r3/r4/t2 按预期检出。产品目标 sdurws_ird_diagnostics 零 gate 命中。exit 1＝基线红之预期行为（处置权 WP-01 所有者），非本任务引入的阻断。

### 4.5 测试真实失败能力（验证验证者）—— 通过（两探针真实变红、还原复绿零残留）

验收者在 worktree A 注入两处缺陷（均在本任务新增代码面）、重建重跑、逐字还原＋touch 强制重编（规避 F-068 mtime 陷阱）：

- **探针 1（替身回放计数失守）**：ScriptedDiagSource.hpp `replayAll()` 返回值改为恒 0 → **`ScriptedDiagSourceSuite.ExhaustedScriptIsBoundedNoOp` 真实变红**（EXPECT_EQ(replayAll(), 2u) 失败）——回放步数断言有真实观测面，非恒真。
- **探针 2（替身符号渗入产品源码面）**：向 `src/Diagnostics.cpp` 追加含 `ScriptedDiagSource` 的注释行 → **`ScriptedDoubleBoundary.ProductSourcesFreeOfDoubleSymbols` 真实变红**（产品源码面零替身符号递归扫描按预期捕获）——机检面真实在岗。
- 重建重跑：26 用例中恰该 2 例 FAILED、其余 24 PASSED（两探针与两红测一一对应、无交叉误伤）；`git checkout --` 还原＋touch＋重建 → **26/26 复绿**（exit 0）；`git status` 对 diagnostics 目录零修改、`PROBE-TEMP` grep 残留计数 **0**。

### 4.6 架构红线（AGENTS §5 八条逐条）—— 通过

1. **L2 计算内核零 Qt**：全部新增/修改文件 grep `#include <Q|QString|QObject` 计数 0；ird_gates R-3 零 diagnostics 命中。
2. **业务域互链禁止（R-1）**：产品单元边仍仅 diagnostics→core（CMakeLists L96＋LinkageContractTest 白名单亲跑通过）；`_contract_test` 链 testkit 为 testkit.md §2.4 明文 T-1 允许形态（测试侧单元，非产品互链）。
3. **跨单元私有头禁止（R-2）**：新增文件 include 全部为 `<sdurws/ird/core/...>`、`<sdurws/ird/diagnostics/...>`、`<sdurws/ird/testkit/ContractCheck.hpp>`（testkit include/ 公共头，ls 实证在位）——零私有头。
4. **框架零源码修改（SA-02）**：4.1 已证零框架改动。
5. **名称前缀拼接/剥离禁止（R-4）**：diff 无 RobWork 名称操作逻辑；ird_gates R-4 零新增命中。
6. **权威唯一（PA-1）**：本任务产物全部在 test/ 侧与测试目标段，未越权实现 project/execution/建模等他单元语义；CollaborationQueryTest/AT 映射用例以 DOC 注释逐处钉住"本文只提供观测点，不代验 AT"。
7. **不可变历史（PA-2）**：零修订/写路径触碰。
8. **不恢复 old/ 历史**：零 old/ 引用。

### 4.7 注释规范（AGENTS §2）—— 通过（新增文件全量核查）

5 个新增文件（ScriptedDiagSource.hpp/ScriptedDiagSourceTest.cpp/DiagContractCheckTest.cpp/CollaborationQueryTest.cpp/README.md）＋CMakeLists.txt 变更段＋LinkageContractTest.cpp 变更段全部通读：

- 文件头 @file/@brief/设计依据（需求 ID＋单元卡 §/契约 acceptance 逐条）齐备；类/函数 Doxygen 中文描述完整（含私有 ScriptedStep 结构）。
- §2.5 高危信息：所有权（四真实产品对象"调用方持有，本类不接管"L248~251）；线程约束（替身非线程安全"仅测试执行线程"L34~35，各测试文件单线程声明）；错误语义（"替身不捕获不吞错"L87~89）；确定性（ManualClock 固定 epoch、每用例独立码表）；物理单位（620mm/550mm 显式）；边界/分支注释（回放游标单调、脚本耗尽终态、EX 域码前置五元组）逐段在位。
- 无中英混杂机翻腔、无复述代码废注释、无占位 TODO。

### 4.8 文档同步＋findings.json 相关性 —— 通过

- **单元卡同步**：v0.10→v0.11；§1.2 卡头"构建落位"追加 DIAG-T10 落位实测（替身/契约消费/矩阵收口/链接面登记/两模式 115/115＋26/26）；§11 DIAG-T10 行完成条件与产物定义逐项落位；§14.4 v0.11 行实现口径三项登记（替身单一设施位/脚本负例不进替身/目标分工按 §10 头注）＋ird_gates 7→8 如实登记——实现与任务卡零偏差（单元卡任务行原文未动）。testkit.md §10.2 交接行承接核实。P-DIAG-8 governance-log 状态如实 open。
- **findings.json 未关闭条目相关性核对**（open 94 项逐一过筛）：
  - **F-007/F-009/F-062/F-098/F-099/F-101（全新树构建环境链）**：本次复现再次实证（ini 两件补拷后零错误配置构建；gate-all 未运行——F-009 环境受限），按 DIAG-T05~T09 验收口径一次配通，无可随手消账项；
  - **F-019/F-051/F-072/F-097（ird_gates 基线红）**：本次 8 命中＝基线 7 同分布＋新增 1（diagnostics_contract_test→testkit，本任务 acceptance 3 契约明文要求的消费面——产品目标零命中）；该新增边的 DTB §4.5 例外登记义务悬置（任务卡与单元卡均注明"登记册维护归 WP-01-T03、转登验收裁决"）——本验收转登 **F-115** 跟踪；
  - **F-113/F-111/F-087/F-090（悬空引用/幻影清单家族）**：本次再现且加重（送验报告 5 件 .log 悬空＋提交正文幻影清单）——转登 **F-114**；
  - **F-012/F-100/F-104/F-107/F-109/F-112（提交标题名词开头家族）**：76c128d7 标题再现——转登 **F-116**；
  - **F-010（.gitignore \*.log 吞没留痕根因）**：F-114 直接根因，家族第 13 例，流程级根治诉求再升级（F-114 内并注）；
  - **F-065（门禁输出乱码）**：本次 ird_gates 输出同现象（控制台编码面，与产物无关）；F-068/F-075（探针 mtime/头文件陷阱）本次主动以 touch 规避；
  - 其余 open 条目属 core/testkit/runtime/evidence/policy/io/doc 等其他单元辖域，与本任务无相关性、无静默积压。
- 单元卡"构建落位"字段 `test/ScriptedDiagSource.hpp/.cpp 对` 表述与实际 header-only（无 ScriptedDiagSource.cpp，ls 实证；§14.4 行与提交正文均只写 .hpp）不符——笔误级，转登 **F-117**。

### 4.9 commit 质量 —— 通过（数值主张全部复现准确；两处风格/清单瑕疵转登）

- **a71e42ee** `[WP-09-T10] 实现 diagnostics 测试替身与契约套件（ScriptedDiagSource＋DT-* 用例体收口）`：标题动词开头合规；正文四段齐备（What 按文件逐项/Why 逐条 acceptance/验证含双模式计数与门禁如实登记/影响面"产品目标零源码改动＋既有 107＋14 零回归"）；`Acc-Covered: 2, 3`。
- **76c128d7** `[WP-09-T10] 单元卡 v0.11 实现落位登记与验证留痕（traceability/builds/wp09-t10）`：正文四段齐备、`Acc-Covered: 1`；验证段结论（双模式 115/115＋26/26、validate-docs PASS）与送验 XML 亲测一致。**瑕疵（均转登，非粉饰）**：①标题名词"单元卡"开头非动词开头（F-012 家族再现→**F-116**）；②正文 What 段声称入库 integration-build.log/smoke-build.log/smoke-configure.log/测试运行日志×4/validate-task.log/verify-task.log/gates-ird-gates-target.log 等 ~12 件，`git ls-tree @76c128d7` 实证 builds/wp09-t10/ 仅 5 件（4 XML＋1 json，.log 全被 .gitignore 吞没——F-010 根因、F-111 家族第 13 例，较 DIAG-T09 的"正文干净"加重→**F-114**）。**数值面零粉饰**：双模式 115/115＋26/26、基线 7＋新增 1 门禁命中、validate-task/verify-task PASS 全部由验收者亲手复现准确。

### 4.10 留痕完整 —— resultNote 载体完整；.log 悬空引用为建议级（F-114，沿家族先例不阻断）

- **在库**（git ls-tree @76c128d7，builds/wp09-t10/ 5 件）：`ird-test-report.json`（schemaVersion ird-test-report/1，base=13fc8fa9 与送验范围一致，per-target 构建结论＋gates 基线对比＋per-suite 逐用例 trace 结构化 resultNote 载体完整准确）＋双模式 gtest XML 四份（115/115＋26/26 顶格、failures=0 errors=0，新增 14 用例〔Collaboration 8＋Scripted 6＋ContractCheck 6 中送验报告列名 20 含既有 Linkage 3〕全部在案）。
- **未入库而报告字段/提交正文引用**（共 5 件 .log：gates-ird-gates-target.log、integration-build.log、smoke-build.log、validate-task.log、verify-task.log）——F-087/F-090/F-113 家族再现（**F-114**）。**证据本体不缺**：上述四类证据（双模式构建/测试/门禁/脚本）全部由验收者从零亲手复现一致（本记录 4.2/4.3/4.4）。
- `phase-one-validation.log`：本任务留痕按现行惯例落 builds/wp09-t10/，ird-test-report.json 即结构化 resultNote 载体——符合"phase-one-validation.log **或** resultNote 已更新"的替代口径。

### 4.11 偷懒/缩水实现扫描 —— 通过

1. **标记扫描**：diff 范围 `TODO|FIXME|placeholder|未实现|not implemented|stub` 命中逐条核验，全部为合法语义非缩水——"PlaceholderDefaultRecordFailsWithFieldPaths"＝负例用例名（验证占位记录被拒）；"对未实现的真实业务场景…零证明力"＝替身边界声明原文；stub 命中＝在册测试桩（StubEnvironment/ConfirmableProjectStubTest/CatalogHostTraceStubTest——README §2 替身清单与 P-DIAG-8 处置明文载体的桩验证纪律）。零真 TODO/FIXME/未实现体。
2. **空实现检测**：新增公共接口（替身类 9 方法/3 登记面）全部有完整实现，无空体/仅返回默认值。
3. **acceptance 逐条证据表**：见 4.4——3 条全部有具名测试＋亲跑输出＋文件行号，零"仅声明已实现"。
4. **测试强度**：断言校验行为与边界（回放序/耗尽语义/绑定冻结字段回读/双面同判/量纲拒绝指明 unit/边界机检扫描计数 ≥15 防恒真）；与 4.5 联动——两探针实证改坏即红。

---

## 三、问题清单

| 级别 | 描述 | 处置 |
| --- | --- | --- |
| 阻断 | 无 | — |
| 建议 | **F-114**：76c128d7 的 ird-test-report.json gates/selfCheck 字段引用 5 件 .log（gates-ird-gates-target.log/integration-build.log/smoke-build.log/validate-task.log/verify-task.log）全部未入库（ls-tree 实证仅 5 件），且提交正文 What 段幻影列举 ~12 件留痕——F-113/F-111 家族第 13 例（较 DIAG-T09 加重：正文＋报告双幻影；根因 F-010）；数值主张全部亲测准确、无粉饰 | 已转登 findings.json（open）；修复方向同 F-113/F-111（add -f/.txt 后缀/gitignore 例外/报告只引用在库文件）；不阻塞合入 |
| 建议 | **F-115**：新增 IRD-GATE-SUB（sdurws_ird_diagnostics_contract_test→sdurws_ird_testkit，T-1 允许形态的契约谓词消费面）在 DTB §4.5 例外登记册/ird_gates 白名单的登记义务随任务流转悬置（本契约 allowedFiles 不含 DTB/白名单，任务卡与单元卡均"转登验收裁决"） | 已转登 findings.json（open，责任方 WP-01-T03 所有者）；合入后由门禁白名单任务收口；不阻塞合入 |
| 建议 | **F-116**：76c128d7 标题"单元卡 v0.11 实现落位登记与验证留痕…"名词开头非动词开头——F-012/F-112 家族再现（正文四段齐备、数值准确） | 已转登 findings.json（open）；纯风格纪律提示，不阻塞合入 |
| 建议 | **F-117**：单元卡 §1.2"构建落位"字段称"增列 test/ScriptedDiagSource.hpp/.cpp 对"，实际替身为 header-only 仅 .hpp（无 ScriptedDiagSource.cpp；§14.4 行与提交正文均只写 .hpp）——卡头字段笔误 | 已转登 findings.json（open）；随 diagnostics.md 下次增量修订订正；不阻塞合入 |

---

## 四、验收者环境说明

- 复现 worktree A：`D:/10_Source_Repos/21_robot/acc-diag-t10-wt-a`（`git worktree add --detach @ 76c128d7351200e00d5792271007e38d69113ff2`）；集成树从零配置（`RWS_BUILD_INDUSTRIALROBOT=ON`〔缓存 grep 确认〕、`RWS_USE_STATIC_LINK_PLUGINS=ON`、vcpkg toolchain、Qt `CMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`；`RobWorkStudio.ini.shared.in`＋`RobWorkStudio.ini.template.static` 自主树补拷——F-007/F-062/F-099 既有缺陷的环境修复，不触及送验产物）。冒烟树：`D:/10_Source_Repos/21_robot/acc-diag-t10-smoke`（独立目录＋vcpkg toolchain）。
- 工具链：VS 2022（MSVC 19.42，生成器 Visual Studio 17 2022，-A x64）、vcpkg 经典模式（gtest 1.18.0 @ installed/x64-windows）、pwsh（validate-task/verify-task.ps1）。
- 探针注入/还原均在 worktree A 内：两处注入（replayAll 恒 0；产品源码面注入替身符号）一次重建重跑取证，逐字还原＋touch 强制重编（规避 F-068），最终 diagnostics 目录 `git status` 零修改、PROBE-TEMP 残留 0、26/26 复绿。
- 分支漂移检查：验收首尾两次 `git ls-remote`——`origin/wp09-t10`＝76c128d7（**无漂移**）；origin 无 `acc/DIAG-T10/1` 分支（无 evidence 分支重名）。
- 验收记录与 F-114～F-117 登记落 `acc/DIAG-T10/1` 分支（基于 origin/redesign-main @ 13fc8fa9）并推送；验收者日志随临时 worktree 清理，本记录内数值均为亲测。
- 本验收通过的是 **SHA 76c128d7351200e00d5792271007e38d69113ff2**，非分支引用；验收者未修改任何实施产物、未向 wp09-t10 推送任何提交。
