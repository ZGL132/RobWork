# EX-T07 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 ID | EX-T07（ResourceController 基础节流——MemProbe/汇总/先节流后诊断；契约 tasks/foundation/EX-T07.json，status=ready） |
| 送验对象（冻结） | `b2b384b590bd9f4138a682570005103a8882fe60`（分支 wp08-t07 尖端；**验收全程按该 SHA**） |
| diff 范围 | `02c3025fe63a4277a5e1d0f661710e57e80192a4..b2b384b590bd9f4138a682570005103a8882fe60`（merge-base＝base＝02c3025f，线性两提交：c560d6b3 实现＋b2b384b5 文档同步与留痕） |
| 分支漂移检测 | `git rev-parse origin/wp08-t07 wp08-t07` → 两引用均＝`b2b384b5…`＝冻结 SHA——**零漂移**（ACC §3） |
| 复现现场 | `git worktree add --detach acc-t07-wa b2b384b5…`（detached @ SHA）；独立 worktree 全新配置全新 build/，无实施构建树残留 |
| 记录回传 | 本记录落 `acc/EX-T07/1` 分支（`git worktree add -b acc/EX-T07/1 acc-t07-wb origin/redesign-main`）并推送；attempt=1 |
| **verdict** | **pass**（4.1~4.11 全部通过；零阻断级问题；3 项建议级问题 G-1~G-3 已按 ACC §5 转登 findings.json F-215/F-216/F-217，不阻塞合入） |
| 验收日期 | 2026-09-19（验收执行跨 2026-09-18 深夜～09-19；记录文件名按当日 20260919） |
| 验收者环境 | Windows 10 x64（10.0.26200）；VS 2022 Community（MSVC 14.42.34433，VC 工具链）；CMake；Node.js（解析用）；pwsh 7（治理脚本）；**独立 worktree 复现**，与实施会话零共享上下文（ACC §2 四条独立性要求逐条满足：全新会话／未读任何实施叙述、仅消费产物与文档／对抗式立场逐项找反证／构建测试全部亲手重跑） |

---

## 环境前置（ACC §3 复现口径）

- 集成模式：worktree A 内全新 `cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DRWS_USE_STATIC_LINK_PLUGINS=ON`，并对齐主树缓存口径（F-007/F-098/F-099 已登记的环境链）：`-DCMAKE_TOOLCHAIN_FILE=<repo>/vcpkg/scripts/buildsystems/vcpkg.cmake`、`-DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`、`-DCMAKE_CXX_FLAGS="-DNOMINMAX -DWIN32_LEAN_AND_MEAN -DWIN32"`。配置 exit 0；新缓存 `grep RWS_BUILD_INDUSTRIALROBOT build/CMakeCache.txt` → `:BOOL=ON` 实证。
- 未跟踪辅助件（F-200/F-099 已登记）：自主工作树复制 `RobWork/RobWorkStudio/bin/{RobWorkStudio.ini.shared.in,RobWorkStudio.ini.template.static}` 两件至 worktree A 后配置通过（fresh 树硬依赖，先例 IO-T06 attempt 2 / POL-T12 attempt 4 同口径）。
- 冒烟模式：另配全新目录 `build-acc-ex-t07-smoke`，源＝worktree A 的 industrialrobot/，**带 vcpkg toolchain 绝对路径**（CORE-T01 验收 G-2 教训）。

## 4.1 改动范围 —— 通过

- diff 文件清单（`git diff --stat 02c3025f..b2b384b5`）：27 个文件＝execution 单元 9 个（CMakeLists.txt、Scheduler.hpp/cpp、WorkerSupervisor.hpp/cpp、win32/MemProbe.hpp/cpp 新增、test/ResourceGovernanceTest.cpp 新增、contract_test/WorkerProcessContractTest.cpp）＋units/execution.md 1 个＋traceability/builds/wp08-t07/ 留痕 17 个。
- 逐个对照契约 `allowedFiles`（`execution/**`、`units/execution.md`、`traceability/**`）：**27/27 全部命中**，零越界。
- `forbiddenFiles`（REQUIREMENTS.md、ARCHITECTURE.md）：diff 中**零触碰**（P-EX-2 的 ARCH §4.6 消费以引用锚定实现，未改 ARCH 本体——合规）。
- SA-02：diff 无任何 `industrialrobot/` 之外框架源码；`patches/` 目录无新文件——零未登记框架改动。
- gate 补充：`ird_gates` 亲手重跑（见 4.4/4.10），零新增命中——依赖边零变更（产品目标仍仅 core/evidence/project 三条登记边）。

## 4.2 可复现构建 —— 通过（验收者亲手重跑）

- 集成模式全新配置后全量 Release 构建：`BUILD_EXIT=0`，`error C[0-9]+|error LNK|error MSB|fatal error` 计数 **0**（日志 acc-t07-wa/acc-t07-integrated-fullbuild.log，18k+ 行全量构建含 RobWork/RWS/RWSim/插件/industrialrobot 全树）。
- 冒烟模式：配置 exit 0＋构建 exit 0、错误计数 0（acc-evi/smoke-config.log、smoke-build.log）。
- 警告面核查（对抗项）：execution 目标域 16 处警告逐一定位——2 处 C4244（wstring→string）实例化源为 WorkerSupervisor.cpp:291 与 worker/main.cpp:299，**均为 EX-T06 既有代码，非本任务引入**；C4005 宏重定义同既有测试同型模式（其中本任务新增 ResourceGovernanceTest.cpp 2 处，转登 G-3/F-217）。
- 实施留痕声称（build-integrated-full.log/build-smoke-*.log，16 件全部在库）与验收者复跑结论一致。

## 4.3 契约 verify 命令 —— 通过

- 契约 verify 数组仅一条：`cmake --build build --config Release --target sdurws_ird_execution_test` → worktree A 集成树亲手执行，构建成功 **exit 0**（并连带验证 `--target sdurws_ird_execution_contract_test` 亦 exit 0——ird-test-report.json builds.integration 声称两目标均含于集成构建，实证一致）。
- 冒烟树两测试目标同样构建成功并运行（见下）。

## 4.4 acceptance 条目逐项核对 —— 通过（3/3 有证据）

| 契约 acceptance | 验收证据（验收者亲手） |
| --- | --- |
| 1. EX-RES-2 全绿：先节流（停派发/降并行）→仍超限→资源不足诊断，已排队/运行任务不失败；观测点＝派发暂停＋诊断；三段治理 65%→停派发/降并行→70%（65% 为实现参数不冒充需求值） | 集成＋冒烟两模式 `ThrottleStopsDispatchThenExhaustDiagnostics_EX_RES_2` 全绿（122/122 内）；断言链：66% 节流段 t3 保持 Queued 且诊断 sink 为空（先节流后诊断）→71% 段 `EX-RESOURCE-INSUFFICIENT` 恰一次→全程事件流零 Failed/Interrupted→回落 50% 后取消 t1 腾出额度、t3 立即出队（节流段同等条件不出队/Normal 段出队的对照闭环）；`ThreeStageThresholdBoundaries_D05` 64.9/65.0/69.9/70.0% 边界全绿（≥ 语义）；NFR-PERF-04 上游值 70% 未改动（ResourceBudget::maxMemoryRatio 默认 0.70、断言 expected=0.70）；65% 实现参数登记于 §15.1 D-05（L1200 实证"65% 为实现参数（登记非需求阈值）"） |
| 2. 内存汇总覆盖主进程＋全部工作进程（§6.6 MemProbe）；资源不足诊断携带比较型三要素经 §3.3 注入 sink | 汇总三半区代码与测试双证：系统半区 `MemProbeRealApiTest.SystemAndProcessProbesReturnPlausibleReadings`（真实 GlobalMemoryStatusEx/GetProcessMemoryInfo 冒烟全绿）、worker 半区真值面＝真进程契约用例 `AggregateJobMemoryCoversRealWorkerAndReclaimShrinksIdlePool`（真 worker 作业内存峰值 >0 断言全绿）；比较型三要素＝`ExhaustedEdgeDiagnosticOncePerEpisodeWithComparativeFields` 断言 comparison.actual=0.71/expected=0.70/unit symbol="1"（Dimensionless 冻结 token）＋DerivedReadOnly/memprobe 来源＋subject 恒空，经 RecordingSink（IExecutionDiagnosticsSink 替身）捕获断言——§3.3 注入消费契约载体实证 |
| 3. 基础形态交付：规模化归 WP-23-T06 零提前实现；P-EX-2 处置；P-EX-1 处置 | 越界扫描：diff 无分批/流式/检查点峰值治理逻辑（仅有采样间隔低通与无迟滞带的即时重判——与登记一致）；P-EX-2/P-EX-1 处置登记于 units/execution.md §15.4 v0.9 行（L1255，验收者全文读毕）；maxWorkers 无编译期消费者如实陈述（代码证实仅 setResourceBudget 转发两字段） |
- ird_gates 自查（acceptance 相关诚实性）：验收者亲手 `cmake --build build --config Release --target ird_gates` → exit 1（既有登记例外红，与 base 行为一致）；命中集规范化解析（F-186/F-207 粘连乱码下按 IRD-GATE 类型＋目标 token 提取）＝**17 项，与送验 t07 留痕、base(wp08-t06) 留痕三方集合逐项比对完全一致——零新增命中**。（送验报告写作"18=18"系粘连物理行计数差异，语义结论"零新增"一致——见 F-186/F-207 家族观察，非粉饰。）

## 4.5 测试真实失败能力（验证验证者） —— 通过（3 个变异注入：2 红 1 绿，绿的已定性）

| 变异 | 注入内容 | 结果 |
| --- | --- | --- |
| M1 | Scheduler.cpp `decide()`：`ratio >= m_budget.maxMemoryRatio` → `>`（破坏 70% 边界 ≥ 语义） | `ThreeStageThresholdBoundaries_D05` **红**（1 FAILED）——边界语义有检出力 |
| M2 | Scheduler.cpp `tick()`：资源闸 `if (decision.pauseDispatch) return;` 的 return 置空（闸失效） | **全套件 122/122 仍绿——未检出**。定性：EX_RES_2 节流段 t3 的"保持 Queued"断言被并发额度闸（2/2 满）完全混淆——闸失效时 t3 同样因额度不出队；套件内无"额度未满＋节流段"组合用例。实现本体经代码审查确认正确（闸位于额度检查之前、短路 return），属**测试敏感度缺口**而非恒真测试（断言校验了真实行为，只是该行为存在等价遮蔽）；与 F-213（同单元进度节流终态断言不可区分两形态）、F-043/F-123/F-146 先例同族，**建议级 G-2/F-216 转登，不阻断** |
| M3 | WorkerSupervisor.cpp `reclaimIdleWorkers()` 头部注入 `return 0;`（回收空转——"降并行"半区失效） | 真进程契约用例 `AggregateJobMemoryCoversRealWorkerAndReclaimShrinksIdlePool` **红**（1 FAILED）——降并行半区有检出力 |
- 变异还原：`git checkout` 还原两文件＋touch 强制重编（规避 F-068/F-075 mtime 陷阱）；还原后 `git status` tracked 零改动，重跑 **122/122＋28/28 全绿恢复**——复现现场无污染。

## 4.6 架构红线（AGENTS §5 八条） —— 通过

1. **L2 零 Qt**：execution 全单元（include/src/win32/test/contract_test）`#include <Q|"Q|QObject|QString` 扫描唯一命中＝BuildRedLineTest.cpp:185-190——红线测试自身的扫描字面量，且该文件不在本任务 diff（0 行变更）。**零 Qt 成立**。
2. **业务域互链（R-1）**：CMake diff 依赖边零变更——sdurws_ird_execution 仍仅链接 core/evidence/project 三条登记边；新增 include 仅同单元（Ports.hpp/WorkerSupervisor.hpp/win32/MemProbe.hpp）＋既有登记边公共头。ird_gates SUB/LIB 零新增命中实证。
3. **跨单元私有头（R-2）**：win32/MemProbe.hpp 为 src/ 私有头，仅同单元实现与测试目标（经 `target_include_directories(... PRIVATE src)`）可见，不进 include/ 公共根；跨单元仅消费 include/sdurws/ird/{core,evidence,project}/ 公共头。
4. **SA-02 框架零修改**：见 4.1。
5. **R-4 名称前缀**：diff 无 RobWork 名称拼接/剥离；ird_gates R4 零新增命中。
6. **权威唯一（PA-1）**：诊断记录构造复用 core DiagnosticRecord/ComparativeFields/UnitToken/Provenance 契约（零重定义零私改——代码审查证实），经 §3.3 IExecutionDiagnosticsSink 注入外报；资源判定落 execution 调度域（tick 主锁内）、不越权 project 写路径与 runtime 名称解析。
7. **不可变历史（PA-2）**：本任务不触修订/写路径——不涉。
8. **不继承 old/**：实现全部按 units/execution.md §6.1/§6.6/ARCH §4.6 语义从零编写（MemProbe 三探针/ResourceController 三段判定/监督器聚合均为新实现）。

## 4.7 注释规范（AGENTS §2） —— 通过

- **公共头全查**（2/2）：Scheduler.hpp 增量（文件头双主题扩展＋MemoryReading/IMemorySampler/SupervisorMemorySampler/ResourceController 四类逐类 Doxygen 中文：业务概念/生命周期所有权〔引用注入非所有权、sink 可空合法装配〕/线程约束〔仅调度线程、§6.2 传染说明〕/错误语义〔nullopt 采样失败保持不猜值、sink 未装配只丢上报不丢决策〕/确定性〔纯函数＋边沿记忆、同读数同预算同上层级→同决策〕）/WorkerSupervisor.hpp 增量两法（aggregateJobMemoryBytes 聚合口径与"单条失败计 0"语义、reclaimIdleWorkers 按进程存亡分流语义与线程约束）。
- **实现文件抽查**（≥3 达成，实查 5）：MemProbe.hpp（不超诺声明逐 API 对照 Microsoft Learn 文档口径——dwLength 初始化约定/PSAPI_VERSION=2 K32 入口/P PeakJobMemoryUsed 单调语义）/MemProbe.cpp（逐调用文档约定注释）/Scheduler.cpp（ResourceController 段逐语义段落注释：间隔节流、失败边沿去抖、三段判定、边沿恰一次、比较型三要素的来源与 subject 不伪造纪律）/WorkerSupervisor.cpp 增量（支 1/支 2 分流理由：对死进程走 Shutdown 协议永远等不到第二次退出确认）/ResourceGovernanceTest.cpp（文件头替身边界声明＋时钟纪律＋逐用例设计依据）。
- §2.5 高危信息表：物理单位（字节/占比无量纲）、坐标系（不涉）、所有权（sampler 引用/supervisor 指针非所有权逐处标注）、线程约束（仅调度线程逐处标注）、错误语义（环境错误 nullopt/reportDev vs 调用方错误缺省）、确定性来源（注入时钟同源、纯函数判定）——**必填项零缺失**。单位制/角度制式不涉（无量纲占比语义并在注释显式说明）。

## 4.8 文档同步 —— 通过

- 实现与任务卡零偏差：units/execution.md §15.4 v0.9 行（L1255）对本任务落位逐件登记（含两处实现登记的归置选择——ResourceController 四件落位 Scheduler.hpp 的组成行先例依据、reclaimIdleWorkers 存亡分流；均为 DTB §5.4 口径的增量登记，非偏差 hiding）；validate-docs **PASS（20 units, 12 trace entries, 150 task files）**、validate-task **PASS (1 tasks)** 均为验收者亲手复跑，与送验提交验证段声称逐字一致。
- 任务状态：契约 status=ready（翻转 done 属收尾环节，验收期现状正确）；追踪矩阵 phase-one-task-index.json EX-T07 行为领取前静态索引（F-201 已登记的实践脱节按既定口径处理）。
- **findings.json 未关闭条目相关性核对（ACC §4.8 v1.2）**：head SHA 处 findings.json 共 214 条（open 184）。逐类核对：①与本任务直接相关的 F-212/F-213（EX-T05 登记的 execution 测试面建议，owner 明示"随 EX-T09 或后续冻结 diff 增量同步窗口顺手消账"）——本 diff 对两涉案文件（SchedulerEventBusTest.cpp/EventBusDeliveryContractTest.cpp）**0 行触碰**，未顺手消账亦无义务，维持 open 并按登记窗口流转，不构成偏差；②ird_gates 例外登记族（F-019/F-115/F-145/F-173/F-202）——本任务零新增命中，无新增登记义务；③幻影清单族（F-081/F-102/F-103/F-106/F-108/F-111/F-113/F-114/F-118/F-125/F-127/F-129/F-132/F-136/F-138/F-141/F-148/F-152/F-157/F-165/F-166/F-172）——本任务 16/16 留痕逐件 cat-file 实证在库，**无复踩**；④标题家族（F-012 系）——本任务再现 1 例（G-1，见 4.9）。历史 open 条目无一项构成本任务阻断。

## 4.9 commit 质量 —— 通过（附建议级 G-1）

- c560d6b3「[WP-08-T08] 实现 EX-T07 ResourceController 基础节流（MemProbe/汇总/三段节流/资源不足诊断）」：**"实现"动词开头合规**；正文四段（What/Why/验证/影响面）齐备；验证段结论（构建/测试/门禁）经验收者复跑全部证实。
- b2b384b5「[WP-08-T08] EX-T07 文档同步与验证留痕（单元卡 v0.9 登记＋双模式证据）」：前缀后以任务号名词"EX-T07"开头非动词开头——**F-012 家族同型再现**（先例 F-206/F-208/F-210/F-211/F-214，均为 suggestion 不阻断）→ **G-1/F-215 转登**。正文四段齐备；验证段 validate-docs PASS 字串与两模式 122/122＋28/28、"命中集零新增"结论经验收者复跑一致；"Acc-Covered"段如实声明无新增消账。
- **无粉饰**：ird-test-report.json 的全部数字（122/122、28/28、9+1 新增、16 件证据清单）与在库 XML/文件逐一相符；honestyNotes 的两处实现登记与 maxWorkers 如实陈述与代码事实一致。

## 4.10 留痕完整 —— 通过

- 16/16 件留痕（构建日志×3、门禁 4 件、validate-task.log、两目标两模式 gtest XML×4＋run.log×4、ird-test-report.json）**逐件 `git cat-file -e` 在库实证**——无 F-081/F-102 族幻影。
- 在库 XML 计数：ird-execution-test-{integrated,smoke}.xml 均 tests=122/failures=0/disabled=0；contract-test 均 28/0/0——与报告一致。
- 验收者复跑实测同数（集成 122/122＋28/28、冒烟 122/122＋28/28、ird_gates 零新增）。
- `phase-one-validation.log 或 resultNote 已更新`条文：按 F-201 已登记的"与流水线实践脱节"口径，以 traceability/builds/wp08-t07/ 全套留痕＋ird-test-report.json（acceptanceMapping 三条＋honestyNotes）为等效载体——已齐备。

## 4.11 偷懒/缩水实现扫描 —— 通过（附建议级 G-2/G-3）

1. **标记扫描**：diff 新增行 `TODO|FIXME|stub|placeholder|未实现|not implemented` 正则零命中；新增三文件（MemProbe.hpp/cpp/ResourceGovernanceTest.cpp）全文件扫描亦零命中（"采样替身/fake"为 §11 用例行明文的替身边界声明用语，非缩水标记，已豁免说明）。
2. **空实现检测**：ResourceController::evaluate/decide、SupervisorMemorySampler::sample、aggregateJobMemoryBytes、reclaimIdleWorkers、MemProbe 三探针全部为真实逻辑（逐行读毕），无空体/恒等返回；M3 变异实证 reclaimIdleWorkers 行为真实可测。
3. **acceptance 逐条证据表**：见 4.4——三条均有具名测试与文件行号级证据，零条无证据。
4. **测试强度**：断言校验行为与边界（≥ 语义四点边界、边沿恰一次 episode、比较字段逐字段、间隔 999/1000ms 边界、事件流全态扫描）；M1/M3 变异红证明非恒真；M2 揭示的闸执行面遮蔽（额度闸混淆）为敏感度缺口而非恒真——转登 G-2/F-216。

---

## 发现的问题清单

| # | 级别 | 内容 | 处置 |
| --- | --- | --- | --- |
| G-1 | 建议 | b2b384b5 提交标题前缀后以任务号名词"EX-T07"开头，非动词开头——ACC §4.9/AGENTS §6.3 字面要求未满足；F-012 家族同型再现（先例 F-206/F-208/F-210/F-211/F-214）。正文四段齐备、验证结论逐项复跑一致，纯风格瑕疵 | 转登 **F-215**；对已推送历史不追溯改写（AGENTS §6.3）；与 F-012 家族合并处置 |
| G-2 | 建议 | 4.5 变异实验（M2）实证："停派发"闸执行面在单元测试套件零检出力——tick 中 pauseDispatch 短路失效后 122/122 仍绿；EX_RES_2 节流段"派发暂停"观测点被并发额度闸（2/2 满）混淆，套件无"额度未满＋节流段"组合用例。实现本体正确（代码审查证实闸先于额度检查），属测试敏感度缺口（F-213/F-043/F-123/F-146 同族） | 转登 **F-216**；建议补"额度未满＋节流段下一拍不出队"用例（如额度 4、在途 1、推至 66% 后 tick 断言无新出队），随 EX-T09 或后续同步窗口执行 |
| G-3 | 建议 | 新增 ResourceGovernanceTest.cpp L77-78 `#define NOMINMAX/WIN32_LEAN_AND_MEAN` 与全局 CMAKE_CXX_FLAGS 冲突产生 2 处 C4005 宏重定义警告（同单元既有 6 测试文件同型模式；同任务 MemProbe.hpp 以 #ifndef 保护即零警告——范式现成） | 转登 **F-217**；零行为影响，后续测试文件统一 #ifndef 包裹即可 |
| 观察 | 留档 | 送验报告 gates 计数"18=18"与验收者规范化实测"17=17"的差异＝F-186/F-207 已登记的门禁日志控制台粘连/乱码导致物理行计数虚高；语义结论（命中集零新增）双方一致。不新立 finding，作为 F-186/F-207 家族补充形态在此留档 | 随 F-186/F-207 合并治理（门禁输出落盘改直写文件，绕开控制台编码） |

## 裁决

- **verdict＝pass**：4.1~4.11 全部通过，零阻断级问题；G-1~G-3 均建议级并已转登 findings.json（F-215/F-216/F-217 编号顺延自 F-214），不阻塞合入。
- 本记录经 ACC §3 记录回传流程落 `acc/EX-T07/1` 分支并推送 origin 后，验收完成；合入决策归所有者（验收者不合入、未修改任何实施产物、未向任务分支 wp08-t07 推送任何提交）。
