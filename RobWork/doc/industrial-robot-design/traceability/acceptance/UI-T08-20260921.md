# 验收记录 —— UI-T08（参数表与表单公共件·阶段 B 交付）· attempt 1

| 字段 | 值 |
| --- | --- |
| 任务 | UI-T08（contract：tasks/foundation/UI-T08.json，schema ird-task/1，status=ready——契约状态未被任务侧触碰，翻转归收尾〔PIPE §4.7 口径〕；interUnit=true——消费 core Units/Quantity 显示投影；requirements UX-04/05/07/KIN-12；designRefs ui.md §13/§6.6/§14；knownPitfalls O-31，已裁决 2026-09-19 随整链放行） |
| 送验对象 | **3bff786a9a02dc72644ac61fcc1b5da88b684269**（40 位冻结；分支 wp10-t08；`git rev-parse wp10-t08`＝该 SHA 逐字一致；`git rev-parse origin/wp10-t08`＝该 SHA 逐字一致——**分支漂移检测零漂移**，与 UI-T07 验收时远程引用缺失/网络不可达不同，本次远程比对实际完成） |
| diff 范围 | 961f0b5d503db47e893441927e8beb859d571c4c..3bff786a9a02dc72644ac61fcc1b5da88b684269（base 与 `origin/redesign-main` 尖端逐字一致＝merge-base；range 内 2 提交：a660f3c2 实现＋测试、3bff786a 单元卡 v1.0＋验证留痕归档；**15 个变更文件**） |
| 验收尝试 | attempt 1（state.attempts.accept 序号 1；本验收为全新上下文，只消费产物、契约、设计文档与在库留痕＋既往**验收**记录〔UI-T07 同日先例，仅取环境口径〕，未读取实施者过程叙述；全部构建/测试/门禁/治理脚本/变异试验由验收者亲手执行） |
| 验收环境 | 复现 worktree A＝`git worktree add --detach acc-ui-t08/wt-a` @ 3bff786a（6168 文件检出后 `git rev-parse HEAD`＝3bff786a 实证）；首配前按 F-098/F-099/F-200 既有登记自主树复制未追踪 `RobWorkStudio.ini.template.static`＋`RobWorkStudio.ini.shared.in` 两件（bin/ 被 .gitignore 吞没的框架 configure 硬依赖——环境链本次再次亲历，非送验对象缺陷）；集成 build 树全新配置：`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=<主仓>vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64 -DCMAKE_CXX_FLAGS="-DNOMINMAX -DWIN32_LEAN_AND_MEAN -DWIN32"`——**CONFIGURE_EXIT=0**，`grep RWS_BUILD_INDUSTRIALROBOT build/CMakeCache.txt` → `:BOOL=ON` 实证；独立冒烟 build 树＝`acc-ui-t08/smoke`（vcpkg toolchain＋CMAKE_PREFIX_PATH＋Qt6_DIR——AGENTS §4.1 口径）；GUI 测试执行＝`QT_QPA_PLATFORM=windows`＋`QT_PLUGIN_PATH=<Qt>/plugins`（ui.md §12.2 既有登记口径）；ird_gates 以**脚本模式**（`cmake -DIRD_ROOT=<industrialrobot 根> -P cmake/ird_gates.cmake`，exit 1＝设计内）亲跑并归一化比对（集成模式 MSBuild 面 GBK 双重编码乱码为 F-065/F-207 既有登记，本次未采信该形态输出）；验收者日志存 acc-ui-t08/logs/（集成配置/全量构建/verify 目标/三测试双模式 console/变异三轮构建与运行/门禁脚本/validate 双脚本共 23 件日志＋4 件命中集比对中间件，验收临时目录，关键结论逐条摘录于本记录）；记录回传 worktree B＝`git worktree add -b acc/UI-T08/1` @ origin/redesign-main |
| verdict | **pass**（4.1～4.8 与 4.11 通过；4.9/4.10 按 F-081/F-266/F-268 家族既有建议级先例判定不阻塞：两提交的**验证结论全部经验收者亲手复现准确**——双模式构建/契约 verify/六组测试 56＋3＋25 双模式全绿/门禁 41＝40＋恰增 1 逐码一致/validate 双脚本 PASS 无一虚报；四条新发现均为建议级，已随本 evidence 分支转登 findings.json **F-270/F-271/F-272/F-273**） |

## 4.1 改动范围 —— 通过

- `git diff --name-status 961f0b5d..3bff786a` 实证 **15 个变更文件**，逐个对照契约 allowedFiles 三模式：`ui/**` 6 件（CMakeLists.txt M、include/sdurws/ird/ui/FormEditCommon.hpp A 663 行、src/FormEditCommon.cpp A 684 行、src/ParamTablePanel.cpp A 575 行、test/FormEditModelTest.cpp A 643 行、gui_test/ParamTablePanelGuiTest.cpp A 449 行）＋`units/ui.md`（M，v0.9→v1.0 增量修订）＋`traceability/**` 8 件（builds/wp10-t08/ 7 件 A＋wp10-t08-gate-registrations.md A）——**全部在 allowedFiles 内，零越界**。
- forbiddenFiles（REQUIREMENTS.md/ARCHITECTURE.md）零触碰（15 文件列表实证，两文件均不在 diff 内；本验收对照需求原文 UX-04/05/07/KIN-12 以只读方式提取，未改动）；契约文件 tasks/foundation/UI-T08.json 不在 diff 内＝未被任务侧改动（status=ready 维持）。
- 框架源码（RobWork/、RobWorkSim/、RobWorkStudio/ 其余源码）零改动：`git diff --name-only base..head -- RobWork/RobWork RobWork/RobWorkSim RobWork/RobWorkStudio/src`（排除 industrialrobot/ui）**零输出**；`patches/` 范围内零变更（head 树 patches/ 仅既有两件，与 base 树逐文件一致——SA-02 无登记需求）。

## 4.2 可复现构建 —— 通过（集成全新树重配＋全量构建＋独立冒烟；`: error` 计数双零）

| 步骤 | 结果 |
| --- | --- |
| 集成重配 | worktree A 全新配置 **exit 0**（补齐 vcpkg toolchain＋Qt 前缀＋CXX flags＋两件未追踪 ini 辅助件后通过——前三者为 F-007/F-098/F-099/F-200 既有登记口径，AGENTS §4.1 字面命令在全新树不可配，验收者亲历同款）；缓存 grep `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON` 实证 |
| 集成全量构建 | `cmake --build build --config Release` **BUILD_EXIT=0、`: error` 计数 0、`: fatal error` 计数 0**（RobWork 全库→RobWorkSim→RobWorkStudio→industrialrobot 全单元全新编译）；产物 `build/RobWorkStudio/bin/Release/` 下 `sdurws_ird_ui_test.exe`（1217024 B）＋`sdurws_ird_ui_contract_test.exe`（303616 B）＋`sdurws_ird_ui_gui_test.exe`（950272 B）全部在位（另全部 ird 单元测试目标同批产出） |
| 独立冒烟模式 | `cmake -S …industrialrobot -B acc-ui-t08/smoke -G "Visual Studio 17 2022" -A x64 -DCMAKE_TOOLCHAIN_FILE=<vcpkg> -DCMAKE_PREFIX_PATH=<Qt> -DQt6_DIR=<Qt>/lib/cmake/Qt6` 配置 exit 0；`cmake --build --config Release` **exit 0、错误计数 0**；`smoke/ui/Release/` 下三测试可执行在位并亲跑（见 4.3——56/3/25 全绿） |

## 4.3 契约 verify 命令 —— 逐条亲手执行；全部成功

契约 verify 数组仅 1 条，在 worktree A 根目录按字面独立执行：

| 命令 | 结果 |
| --- | --- |
| `cmake --build build --config Release --target sdurws_ird_ui_test` | **exit 0**（VERIFY_EXIT=0、`: error` 计数 0） |

附加测试真实执行（验收者亲手，非采信送验留痕；日志存 acc-ui-t08/logs/）：

- 集成树三测试可执行直接运行（QT_QPA_PLATFORM=windows＋QT_PLUGIN_PATH）：**模型层 56/56、契约 3/3、GUI 25/25 全部 PASSED**（console 留痕；计数与在库三份 gtest XML〔tests=56/3/25、failures=0〕及三份 ird-test-report.json〔total=56/3/25、passed 同值、failed/skipped=0、decisive=true〕逐值一致；FormEditModel 十六用例＋ParamTablePanelGuiTest 十用例逐一在亲跑输出中在列且零失败——26 个 `_UI_T08` 后缀用例名与在库 XML 逐一对应）。
- 冒烟树三测试可执行另跑 **56/3/25 全部 PASSED**（"集成＋冒烟双模式同口径"的提交声称经验证属实）。
- 治理脚本亲跑（worktree A 树内）：`validate-task.ps1 -TaskFile <契约绝对路径>` → **PASS (1 tasks)**；`validate-docs.ps1` → **PASS (20 units, 12 trace entries, 150 task files)**——与提交正文声称逐字一致。

## 4.4 acceptance 条目 —— 2/2 逐条证据（未执行项零存在；ird_gates 已随 WP-01-T01 落地故为"已执行"）

| # | 契约 acceptance 要点 | 证据（验收者亲手） |
| --- | --- | --- |
| 1 | UX-04/05/07 公共编辑规则（阶段 B 交付面）：数值＋单位同显（单位显示消费 core Units/Quantity 显示投影——KIN-12，切换仅影响显示不改 SI 真值）、非法输入就地显示原因保留原值、表单级确认应用（UX-07：应用前确认交互，断言与放行归 project）、批量粘贴/筛选/错误定位、取消恢复；不用模态对话框做大量重复编辑（DTB WP-10-T08 禁止项） | ①**同显＋切换仅投影**：`UnitDisplaySwitchIsProjectionOnly_KIN12_UI_T08_ACC1` 亲跑通过——分列读点（"300"＋mm）与合显读点（"300 mm"）一致；mm→cm 切换显示随动（"30 cm"）；切回 m 读回 SI 真值 0.3（`currentValueSi` 精确断言）；零脏/零 pendingChanges/零出口触达（替身计数＝0，:148）——KIN-12"切换不改 SI 真值不产生修订"与 UX-07"仅改变会话显示不提示"模型层等价物；换算实现单点＝core::convert/tryConvert（FormEditCommon.cpp:199/:255——SA-12 唯一入口，本单元零第二换算实现点，验收者全量 grep 实证）；`FormatValueTextUnitSuffixAndDimensionless_UI_T08_ACC1`（"300 mm"/"0.3 m"/无量纲"42"不带"1"后缀/量纲违约 EXPECT_THROW）亲跑通过。②**非法输入就地显示原因保留原值**：`IllegalInputInPlaceReasonKeepsValue_UI_T08_ACC1` 亲跑通过——非数值原因回显原文＋"保留原值"字样、显示与 SI 真值一字不动（"300"/0.3）；空串/纯空白＝kFieldEmptyReason 非 0；nan 文本拒绝；整数约束 3.5 拒/3.0 与负整数受；有效输入清除本字段错误。`ParseSyntaxAndDisplayUnitSemantics_UI_T08_ACC1`（mm 制式解析 500→0.5 m SI、前导 '+'、科学计数法、"12abc" 整串消费拒绝）亲跑通过。保留原值为**结构保证**而非补丁：FormEditCommon.cpp:422-438 亲读——错误路径只写 errorReason、不触碰 baseline/staged 两层。GUI 面 `InPlaceErrorKeepsValue_UI_T08_ACC1` 亲跑通过（状态列就地原因＋值列回退"300"＋出口零触达＋修复后清空）。③**表单级确认应用（UX-07）**：`FormLevelConfirmApplyHandsOffEditSet_UX07_UI_T08_ACC1` 亲跑通过——pendingChanges 比较型明细（old→new 成对、SI 语义、注册序）、confirmApply 移交替身恰一次、载荷逐字段一致、应用后基线推进暂存清空、二次确认被拒（kApplyNoChangesText）；`ApplyBlockedUntilErrorsResolved_UI_T08_ACC1` 亲跑通过——存在未解决错误时不移交、拒绝原因携带首个错误定位（"存在非法输入…不是有效数值"）、修复后同一表单可应用且合法编辑不丢；"断言与放行归 project"以 kApplyHandedOffSuffix 契约文案单点承载（FormEditCommon.hpp:121-122）＋GUI `ApplyViaInlineConfirmRegion_UX07_UI_T08_ACC1` 断言反馈含"project 命令边界"字样（亲跑通过）——ui 不发明判定语义。GUI 面 `ConfirmRegionDeclineKeepsStaged_UX07_UI_T08_ACC1`（"再改改"可反悔零移交）亲跑通过。④**批量粘贴/筛选/错误定位**：`BatchPasteIndependentLinesWithImpact_UI_T08_ACC1`（四行混合：合法/未知键/非法值/同键覆盖——接受 2 拒 2、逐行原因、影响明细只含涉及键最终态 100→260、未涉及字段原值不动）与 `BatchPasteToleratesBlankLinesAndComma_UI_T08_ACC1`（空行跳过不计数、逗号分隔同价、两侧空白规整）亲跑通过；`FilterByKeyOrLabelKeepsOrder_UI_T08_ACC1`（键/中文标签大小写不敏感子串、空词全量、筛选不动编辑态）与 `FirstErrorIsRegistrationOrderStable_UI_T08_ACC1`（多错恒定位注册序首个——NFR-COR-02）亲跑通过；GUI 面 `BatchPasteImpactDetail_UI_T08_ACC1`（剪贴板真实写入→摘要＋逐行拒绝原因＋影响条目就地呈现零对话框）与 `FilterRowsByKeySubstring_UI_T08_ACC1` 亲跑通过。⑤**取消恢复**：`CancelRestoreDropsStagedKeepsDisplaySettings_UI_T08_ACC1`（暂存/错误整体丢弃、显示回基线、单位制式 cm 与筛选**不回滚**——显示设置与修改集正交）与 GUI `CancelRestore_UI_T08_ACC1`（值列回"300"、出口零触达、反馈＝契约文案）亲跑通过。⑥**非模态红线（DTB 禁止项）**：`NoModalDialogInFormCommon_UX05_UI_T08_ACC1` 亲跑通过——对 FormEditCommon.hpp＋ParamTablePanel.cpp 注释剥离后静态扫描 QDialog/exec(/setModal 零命中，且以 objectName 正向控制防"扫空文件"假绿灯；GUI `ApplyViaInlineConfirmRegion` 附加 `findChildren<QDialog*>().size()==0` 控件面双证（亲跑通过）——确认交互为面板内嵌非模态区域（ParamTablePanel.cpp:316-333 亲读）。⑦**UX-04 高级面板承载**：`AdvancedPanelDefaultFields_UX04_UI_T08_ACC1` 亲跑通过——三键冻结（solver-max-iterations/sampling-points/random-seed）、全计数类（无量纲＋仅整数）、基线一律未设不伪造数值；GUI `DevDiagnosticsToggleNeverEntersEditSet_UX04_UX07_UI_T08_ACC1` 亲跑通过——"开发诊断"开关只触发注入回调、零脏/零待应用/零移交、纯显示操作不触发确认交互（UX-07 后半句）。装配契约 fail-fast 面（`SpecFactoryAndModelRejectCallerErrors_UI_T08_ACC2`：量纲不匹配/无效 token/空键/空标签/空字段集/重复键/未知键全拒绝）与基线注入边界（`BaselineInjectionClearsStaleStagedState_UI_T08_ACC2`：非有限拒绝、新基线清旧暂存旧错误）亲跑通过。 |
| 2 | O-31 处置（已裁决 2026-09-19——随整链放行）：本任务自身交付物（表单公共件）不持有 C-3/4/5/7/8/10/11 对端类型（单位显示走已链接的 core Units/Quantity 显示投影；草稿接入经 ui 自有 IDraftController.attachModule 接口）——与原登记一致；阶段 B 交付面登记维持（卡 §13『阶段 B 交付，本文仅登记』——阶段 B 域消费者接入时按 ui.md v0.4 §10 引导注复核） | ①**零对端类型持有**：验收者独立 grep——本任务新增产品面（FormEditCommon.hpp/FormEditCommon.cpp/ParamTablePanel.cpp）`sdurws/ird/` include 仅 core（Units.hpp——ARCH §3.5 表内登记边）与本单元公共头两处，project/evidence/policy/runtime/execution 等 15 业务域**零命中**；全 ui 产品面（src＋include）命中仅 core（14）/diagnostics（9）/ui（24）三目录——与本任务前基线同构，本任务新增边＝零。②**草稿接入面＝ui 自有最小端口**：IFormEditOutlet 为 ui 命名空间自有纯虚接口（FormEditCommon.hpp:362-377 亲读——本头不 include 任何对端公共头）；域编辑器经 IDraftController::attachModule 接入为阶段 B 域消费者路径（注释逐处锚定），本交付物只把确认过的 ParamEditSet（ui 自有值类型）移交出口；`NoCrossUnitInclude_O31_UI_BUILD` 守卫用例随 56/56 亲跑通过（在库 XML 含该用例，BuildRedLineTest 常驻自证）。③**断言与放行归 project**：模型/面板零业务判定语义（confirmApply 前置仅为"无错＋有修改"两条呈现层事实——FormEditCommon.cpp:607-639 亲读）；反馈文案 kApplyHandedOffSuffix 明示边界（acc1 ③证据）。④**链接面零变化**：ui/CMakeLists.txt diff 亲读——仅产品源集增列 2 件＋两测试目标增列 1 件，`target_link_libraries(sdurws_ird_ui …)` 段零改动（零新链接边，O-31 产品面零对端边界不变）；ird_gates 亲跑 41 处命中中 R3 L2 链接三件套（Qt6::Core/Gui/Widgets）逐码不变＝基线既有。⑤**阶段 B 交付面登记维持**：units/ui.md §13 UI-T08 行原文"阶段 B 交付（本文仅登记 §14）"未被删改（diff 亲读——该行未触碰），落地登记以行后追加注承载（§13 UI-T08 落地登记注＋§16.7 v1.0 行），与 UI-T05 v0.7/UI-T07 v0.9 注格式同型；§14.1 承接行同步"公共件已交付、域消费者插件接入为余项"。 |

## 4.5 测试真实失败能力 —— 通过（三处实现变异注入应失败输入，均真实变红；试验后已还原并复绿、worktree 字节级还原）

在验收沙箱 worktree A（git 管控下，三轮试验后 `git status --porcelain -- …/industrialrobot/` 零输出＝与送验对象逐字节一致；`grep -n MUT` 零残留）注入三处变异：

| # | 变异 | 重建后亲跑结果 |
| --- | --- | --- |
| 1 | `FormEditCommon.cpp` confirmApply 错误阻断前置改 `if (false && hasErrors())`（"存在非法输入不得应用"轨被拆除） | `--gtest_filter=…ApplyBlockedUntilErrorsResolved_UI_T08_ACC1` → **FAILED**（MUT1_RUN=1；失败点即承重断言：存在就地错误时 ok 应为 false） |
| 2 | `FormEditCommon.cpp` setDisplayUnitForKind 批量切换循环体前加 `if (false)`（KIN-12 按量纲显示切换轨被拆除） | `--gtest_filter=…UnitDisplaySwitchIsProjectionOnly_KIN12_UI_T08_ACC1` → **FAILED**（MUT2_RUN=1；"30 cm" 显示随动断言即承重断言） |
| 3 | `ParamTablePanel.cpp` 未接出口禁用分支改 `if (false && outlet == nullptr)`（§11.4 不虚构可用性轨被拆除） | `--gtest_filter=ParamTablePanelGuiTest.NoOutletDisablesApply_UI_T08_ACC2` → **FAILED**（MUT3_RUN=1；apply 应禁用断言即承重断言——GUI 面） |

三处变异经 `git checkout --` 还原后重建（RESTORE_BUILD_EXIT=0）复跑：模型层 **56/56 PASSED**、GUI 层 **25/25 PASSED**。三组承重断言均为「错误阻断」「KIN-12 显示投影」「未接出口不虚构可用性」语义本身——非恒真、非"只编译通过"。

## 4.6 架构红线（AGENTS §5 八条）—— 逐条通过

1. **L2 计算内核零 Qt**：本任务仅改 ui 单元（R-3 全产品唯一 Qt Widgets 例外单元，DTB §4.5 预登记）；core/evidence/policy/runtime 等零触碰（15 文件清单实证）；新增模型层 FormEditCommon.cpp **零 Qt include**（亲读 include 面：标准库＋core/Units＋自有头）；ird_gates 脚本模式亲跑（exit 1＝设计内）归一化命中集与在库 gates-hit-set.txt **逐码一致（41/41 唯一命中 IDENTICAL，diff exit 0）**：恰增 1 处＝`ui/src/ParamTablePanel.cpp` 的 R3 头包含面命中（R-3 例外本体目标 sdurws_ird_ui 源集文件，与基线中 WorkbenchShell.cpp/CommandPalette.cpp/PolicySummaryCard.cpp 等命中同型同根源——非新例外形态，登记提交件 wp10-t08-gate-registrations.md 逐字对应）；FormEditCommon.hpp **零命中**（QWidget 仅前置声明——亲读实证）；测试面两件不在产品面扫描域零命中。
2. **业务域互链禁止（R-1）**：ui 产品面 include 扫描（src＋include 全量 grep）——`sdurws/ird/` 命中仅 core/diagnostics/ui 三目录，15 业务域**零命中**；本任务新增边＝零（新文件消费面只见 core Units＋ui 自有头）；链接面 CMakeLists.txt 亲读零对端链接、产品目标链接段零改动。
3. **跨单元私有头禁止（R-2）**：零跨单元 include（同上 grep——全部 `include/sdurws/ird/<unit>/` 公共路径）；新公共头 FormEditCommon.hpp 落位 include/sdurws/ird/ui/；无跨单元私有面暴露。
4. **框架零源码修改（SA-02）**：diff 内零框架文件、patches/ 与 base 逐文件一致（4.1 实证）。
5. **不做名称前缀拼接/剥离（R-4）**：ui diff 新增代码行 `RobWork` 字面量 **0 命中**（grep 计数实证）；ird_gates 复跑 R4 项仅 project 既有 1 处（ProjectStoreImpl.cpp——与基线同值，非本任务文件）。
6. **权威唯一（PA-1）**：单位换算权威归 core Units（formatFieldValueText/parseFieldValueText/displayNumberText 全部经 core::convert/tryConvert 单一入口——第二实现点全量 grep 零命中）；字段值语义权威归域消费者（spec 由装配期给定，ui 只执行公共编辑规则——QuantityFieldSpec 注释与实现一致）；确认应用零业务判定（移交 IFormEditOutlet、断言与放行归 project 命令边界——acc2 ③证据）；面板零语义纯渲染（规则唯一承载在 ParamEditModel——GUI 与模型测试对同一规则互证钉住）；"开发诊断"开关只回调 options 不进模型（GUI 测试钉住）。
7. **不可变历史（PA-2）**：本任务零修订数据触碰（15 文件清单——无 project/ 修订面文件）；编辑移交面只产 ParamEditSet 值快照不回写权威。
8. **不恢复 old/**：diff 新增行 `old/` 引用 **0 命中**（计数实证）；实现全部从零编写（同构先例 PolicySummaryCard 仅为分层模式参照，零代码复制）。

## 4.7 注释规范（AGENTS §2）—— 通过（新增公共头 FormEditCommon.hpp 全查＋实现 2 件全文（FormEditCommon.cpp 684 行、ParamTablePanel.cpp 575 行）＋两测试文件全文＋CMakeLists diff——覆盖"公共头全查＋实现抽查 ≥3"）

- **文件头**：新增 4 件＋大改 1 件均有中文 `@file/@brief`＋设计依据（需求 UX-04/05/07/KIN-12、ui.md §4.2/§4.5/§4.6/§6.6/§13/§14.1/§16.7 v1.0、任务契约 acceptance 1/2、O-31 裁决、同构先例 PolicySummaryCard 逐条对应）＋背景说明（"为什么表单公共件要分层成模型规则＋面板交互两半""为什么'保留原值'不靠额外保存""为什么整表重建而不是逐格更新""为什么 itemChanged 经单发定时器排队"等"为什么"层注释齐备且与技术事实一一对应）。
- **类/函数 Doxygen**：公共头逐结构体/逐函数 `@brief/@param [in]/@return/@throws/@note` 语义完整（QuantityBounds 闭区间语义、QuantityFieldSpec 七字段分工与装配契约、makeQuantityFieldSpec 五类拒绝条件、parseFieldValueText 六步判定序逐步列出、ParamEditModel 三层值语义词汇表、confirmApply 两前置＋移交后基线推进语义、batchPasteText 文法语义〔TAB/逗号、空行跳过、后行覆盖〕、IFormEditOutlet 生命周期与实现方约束、createParamTablePanel 逐控件 objectName 锚表）——review 者可据此判断实现是否符合设计，本验收 4.4 的逐条核对即以该契约头为基准完成。
- **§2.5 高危信息表**：物理单位（SI 真值/显示单位逐处显式——baselineSi/stagedSi 注明"SI 真值"、bounds 注明"SI 闭区间"、displayUnit 注明"KIN-12 投影制式"）；角度制式（行程上限经 core 单位表 rad/deg token 承载，无裸数值角度）；坐标系（本任务无坐标系面——如实无缺失）；所有权（outlet 裸指针"表单面板只存裸指针——调用方保证出口存活期覆盖面板存活期（面板不接管所有权）"、model"调用方持有——面板是它的视图"、Qt 父子树所有权逐处注明）；线程约束（"ParamEditModel 非线程安全——仅 UI 线程访问""纯函数任意线程可调用""applyEdits 仅 UI 线程调用""回调非线程安全——UI 线程内同步调用"逐处注明）；错误语义（调用方装配错误 fail-fast 抛 invalid_argument/out_of_range/logic_error 与用户输入错误走就地错误通道的**两轨划分**逐函数注明——含"这是装配/接线错误而非用户输入错误，fail-fast 不进就地错误通道"的归类理由）；确定性来源（from_chars/to_chars 与 locale 无关＋理由〔Qt 应用可能改设 locale〕、6 位有效数字呈现精度、注册序＝稳定呈现序 NFR-COR-02 逐处注明）。
- **魔法数字**：呈现精度 6 位有效数字（"呈现投影非工程比较值，6 位足够辨识"来源注释）、缓冲 40 字节（"double 全值域在 40 字节内恒可容纳"假设注释）、layout margins 4、初始选中显式旗标（"addItem 会把空下拉当前项自动置 0——不能以 currentIndex<0 作判据"陷阱注释）、字符串精确匹配而非 endsWith（"cm 也以 m 结尾，后缀匹配会误选"陷阱注释）——逐个注明依据。
- **反例扫描**：零复述式注释、零无编号 TODO（全 diff `TODO|FIXME|XXX|HACK` 零命中）、注释与代码一致——一处既有枚举计数漂移如实登记：CMakeLists AUTOMOC 复核注释行"十个源文件与私有头零 Q_OBJECT 声明"的源文件计数与 src/ 实际 11 个产品 TU 不符（base 行"八个源文件"时实际已有 9 TU——偏差先于本任务存在；本任务 +2 增列时按同口径同步改写，未校准绝对值；实质性主张"零 Q_OBJECT"经全文件 grep 证实为真，零行为影响）——转登 **F-273**；无过期注释残留（v0.9 行文在 §16.7 保留为历史版本行＝正确形态）。

## 4.8 文档同步 —— 通过（零偏差＋增量修订已登记＋findings 相关性逐条核对）

- **单元卡增量修订**：`units/ui.md` v0.9→v1.0（diff 实证六落点：文档头版本行、§3.3 公共头布局增行 FormEditCommon.hpp、§12.3 用例清单增行 UI-FRM-1、§13 UI-T08 行后"阶段 B 落地登记"注、§14.1 承接行更新、§16.7 变更记录增 v1.0 行——既有语义内容零改动，§13 UI-T08 卡行原文未触碰）；v1.0 行登记的承载形状（契约头内容逐件/面板逐控件/门禁 40→41＋ParamTablePanel.cpp 恰增/测试 56＋3＋25/域消费者承接）与实现逐一亲读比对一致。实现与任务卡零未登记偏差（acceptance 逐条对应见 4.4）。
- **追踪同步**：validate-task PASS＋validate-docs PASS 亲跑同值（4.3）；§12.3 UI-FRM-1 行（前置/操作/预期/观测点）与交付测试形态一致（模型十六＋GUI 十用例名与在库 XML 逐一对应）；契约 status=ready 维持（合入收尾翻转——PIPE §4.7 口径）。
- **findings.json 未关闭条目相关性核对（234 条 open 逐类筛查，与本任务相关者如下；顺手消账项为零）**：
  - **F-227/F-259**（ird_gates 机器面滞后：R3 例外链接/头包含白名单未回填，owner=WP-01-T03）：本任务新增 1 处 R3 命中即 F-259 同型（新实现 TU 的 Qt 头包含——R-3 例外本体），已按 wp10-t02~t07 先例以登记提交件 wp10-t08-gate-registrations.md 顺延登记（责任方 WP-01-T03，不重复开立请求条款）——维持 open 正确（白名单文件不在本任务 allowedFiles，本任务无回填义务）。
  - **F-261/F-263/F-264/F-267/F-269**（提交标题名词短语家族第 11~15 例）：本任务 3bff786a 标题同型再现（**第 16 例**，见 4.9）——家族性复发，随本验收转登 **F-271** 顺延跟踪。
  - **F-010/F-077/F-081/F-266/F-268**（.gitignore 吞 .log／留痕归档虚报家族）：本任务 **F-268 同型复踩（UI 系连续第 3 例）**——3bff786a 正文声称归档的 11 件 .log（gate-all、ird_gates 双形态、集成构建、冒烟配置/构建、三测试控制台、validate-task）被根 .gitignore:87 规则静默跳过未强制入库（实施主工作树 `git status --ignored --porcelain` 实证 11 件在盘未跟踪；在库仅 7 件〔3 XML＋3 report＋gates-hit-set.txt〕）；wp10-t08-gate-registrations.md 首节"原始输出：builds/wp10-t08/ird_gates.log 与 ird_gates-script.log"为在库悬空引用（F-113 同型）——随本验收转登 **F-270**（按 F-268/UI-T07 先例判定建议级：证据本体在库且全量复现）。
  - **F-220/F-236 家族**（命中清单留痕格式跨任务漂移／散文计数失准）：wp10-t08-gate-registrations.md ①称"本任务沿用 wp10-t07 留痕文件形态"且"行数 87→89 与门禁退出报告'命中 41 项'一致"——实际在库 gates-hit-set.txt 为 **82 行**（41×2 相位行；wp10-t07 文件 87 行含 `[ird_gates] ` 行首前缀＋尾部 7 行门禁自测段，本任务文件前缀剥离、自测段略去——"沿用形态"与"87→89"均与在库工件不符）；门禁实质结论（基线 40＋恰增 1＝41、新增＝ParamTablePanel.cpp R3）经验收者脚本模式亲跑逐码一致为真——随本验收转登 **F-272**（建议级：实质命中集零误差，方向为留痕散文与文件形态描述失准）。
  - **F-260**（UI-T03 行为面缺口：折叠/门面违约轨）：属 UI-T03 交付测试集；本任务零触碰折叠面，与本任务 diff 无交集，维持 open 正确。
  - **F-265**（WorkbenchShell_p.hpp 行尾半角括号笔误，owner=UI 后续任务顺手还原）：本任务未触碰该文件（15 文件清单实证）——维持 open 正确（建议随收尾提示下序 UI 任务消账）。
  - **F-201**（§4.10 条文载体与流水线实践脱节）：循其登记口径以 `traceability/builds/wp10-t08/` 在库 7 件为 4.10 判定载体（phase-one-validation.log 尾行停早期任务——亲读实证，见 4.10）。
  - **F-065/F-207/F-231**（gates 留痕 GBK 双重编码乱码/编码不统一）：本验收以脚本模式干净行序完成比对（集成模式 MSBuild 形态输出未采信）——既有登记持续适用，环境/工具链面非本任务新增缺陷。
  - **F-007/F-098/F-099/F-200/F-101**（全新树配置口径缺口家族）：本验收再次亲历（ini 两件＋toolchain/Qt/CXXFLAGS）——与本任务零代码关联，维持 open 正确（本记录环境段再留实证）。
  - 其余 open 条目与本任务 15 文件面无关联（逐类比对零交集）。

## 4.9 commit 质量 —— 按家族先例通过（2 提交四段式齐备、验证结论全数复现准确；1 处标题名词短语＝F-012 家族第 16 例转登 F-271；1 处归档清单与在库树不一致＝F-268 家族复踩转登 F-270）

- **a660f3c2** `[WP-10-T08] 实现参数表与表单公共件：公共编辑规则模型层与参数表面板`：动词开头（"实现"）合规；四段（What/Why/验证/影响面）齐备（含 Acc-Covered: 1, 2）；正文验证声称（集成全量零 error、verify 目标通过、冒烟零错误＋56/56、三目标 56＋3＋25、门禁恰增 1 处＋FormEditCommon.hpp 零命中、validate-task PASS）与本验收亲跑**逐值一致**；影响面段如实声明（仅新增文件＋CMakeLists 增列、链接面零变化、R-3 例外范围不变）。
- **3bff786a** `[WP-10-T08] ui 单元卡 v1.0 增量修订与验证留痕归档（文档同步＋证据）`：①**前缀后以名词短语『ui 单元卡』开头——不合 ACC §4.9/AGENTS §6.3『动词开头』字面**，F-012/F-232~F-242/F-247/F-258/F-261/F-263/F-264/F-267/F-269 家族**第 16 例**（纯风格瑕疵）——转登 **F-271**（建议级，随 F-247 家族统一跟踪）。②正文"改动内容"声称的留痕归档清单与在库树不一致——11 件 .log 被根 .gitignore:87 规则静默跳过未强制入册（F-010/F-077/F-081/F-266/F-268 家族复踩，**数值主张本身无一虚报**：gate-all 20/24 与 4 项既有环境面失败经在盘日志原件核读属实〔ird_gates 既有命中面/testkit_test 既有命中/GUI ctest 无 Qt 环境/冒烟步缺 toolchain 透传——均先于本任务存在〕；构建/测试/门禁/validate 计数全部经本验收亲手复现准确）——转登 **F-270**（建议级，按 F-268/UI-T07 先例"证据本体完整且全量复现"口径不阻塞；返工输入＝git add -f 回填 11 件或循 F-081 家族裁决闭环）。两提交正文四段齐备、如实区分已执行/未执行（state.json 流水线在途变更"不属于本任务未纳入提交"的声明与 diff 一致——state.json 不在 15 文件内）。

## 4.10 留痕完整 —— 按 F-201/F-081/F-268 先例通过（证据本体 7 件在库且全数复现；11 件日志在盘未入库＝F-270，回填责任已登记）

- **在库载体**：`traceability/builds/wp10-t08/` **7 件**（双模式同口径 gtest XML×3：tests=56/3/25、failures=0 亲读且用例名与源码一一对应〔26 个 `_UI_T08` 后缀用例全数在列〕；ird-test-report.json×3：decisive=true、passed=56/3/25、failed/skipped=0 亲读；gates-hit-set.txt：归一化命中集——与本验收脚本模式复跑**去重后 41/41 逐码一致（diff exit 0）**）＋`traceability/wp10-t08-gate-registrations.md`（执行方式/结论"恰增 1 处"登记行/其余文件零命中说明/gate-all 环境面 4 项既有失败逐项登记齐备；"40＋1＝41"结论与本验收复跑一致；其"行数 87→89/沿用形态"两处描述失准＝F-272）。在库 XML 计数与提交正文、本验收亲跑三方一致。
- **缺口（F-270 登记处置）**：提交正文声称归档的 11 件 .log（gate-all/integrated-build/ird_gates/ird_gates-script/三测试 console/smoke-configure/smoke-build/smoke-model-console/validate-task）实际**未入库**（在盘未跟踪实证）；按 F-268/UI-T07 先例以本验收亲跑证据（4.2/4.3/4.6 全套：集成/冒烟配置与构建日志、三测试双模式 console、门禁脚本模式复跑、validate 双脚本——存验收临时目录 acc-ui-t08/logs/ 并摘录于本记录）替代裁判依据；回填或根因修复循 F-270 resolution 裁决。
- phase-one-validation.log 尾行停早期任务（亲读实证）、state.json 无 resultNote 字段——循 **F-201** 既有登记口径，以 `traceability/builds/wp10-t08/`＋登记提交件为判定载体。
- 验收者自跑留痕（集成配置/全量构建/verify 目标/三测试双模式 console＋门禁脚本/变异三轮构建与运行/validate 双脚本输出，共 19 件）存于验收临时目录 acc-ui-t08/logs/，关键结论已逐条摘录于本记录；临时目录处置不随本记录效力变化。

## 4.11 偷懒/缩水实现扫描 —— 通过（扫描零实质命中；豁免逐条列据；逐条证据表见 4.4）

- **①标记扫描**：diff 新增行 `TODO|FIXME|XXX|HACK|stub|未实现|not implemented` **零命中**（grep exit 实证）；`placeholder`（大小写不敏感）命中仅 2 处均为非交付物本体：`setPlaceholderText`（Qt 筛选输入框 API——真实功能调用）与测试名 `UnsetValueShowsPlaceholderNotZero`（"未设"占位＝§6.6 契约显式呈现形态——与 0 值严格区分的语义内容，非空实现）；「未接编辑出口时应用禁用＋kNoOutletTooltip」「高级面板字段集基线未设」＝**契约显式形态**（§11.4 不虚构业务能力＋契约 acceptance 2"阶段 B 交付面登记维持"原文＋ui.md §14.1"域消费者插件接入为余项"登记；触发有真实禁用态＋tooltip 断言——非空实现、不虚构业务能力），豁免依据＝单元卡原文＋实现亲读，逐条列出如上。
- **②空实现检测**：全部公共接口函数有真实实现（makeQuantityFieldSpec 五类装配校验、formatFieldValueText 换算＋守卫＋异常、parseFieldValueText 六步判定全分支、ParamEditModel 十九个方法全分支〔含批量粘贴逐行判定与影响明细、confirmApply 两前置与基线推进〕、createParamTablePanel 控件树构建＋九组回调接线全实现——实现全文亲读）；零"仅返回默认值"的空壳公共接口；`ValueParseResult{}` 默认态＝"ok=false 就地错误"的载体初值而非空实现。
- **③逐条证据表**：契约 acceptance 2 条逐条证据见 4.4 表——每条均有具名测试、命令输出或文件行号，零"已实现"空声明。
- **④测试强度**：断言校验行为与边界（同显三读点交叉＋切换后 SI 真值精确回读＋零脏/零移交计数断言；就地错误五类谱系逐类文案断言＋"保留原值"双向断言〔显示不变＋SI 不变〕；解析语法边界〔'+'、科学计数法、整串消费、nan〕；确认应用移交载荷逐字段精确断言＋二次确认拒绝；错误阻断＋修复重放不丢修改；批量粘贴四行混合场景逐行断言＋影响明细最终态断言；取消恢复含"显示设置不回滚"负向断言；非模态红线静态扫描＋objectName 正向控制＋GUI 控件面双证；GUI 未接出口禁用＋tooltip 契约文案断言；开发诊断开关零脏/零移交/不触发确认三重断言）；**非恒真、非"只编译通过"**——与 4.5 联动抽改实现三处（错误阻断轨/KIN-12 切换轨/未接出口禁用轨）均真实变红且失败点恰为承重断言，其余用例不受扰。

## 发现的问题清单（建议级 4 条，无阻断级）

| 编号 | 级别 | 摘要 | 处置 |
| --- | --- | --- | --- |
| F-270 | suggestion | F-010/F-077/F-081/F-266/F-268 家族复踩（UI 系连续第 3 例）：提交 3bff786a 正文声称 traceability/builds/wp10-t08/ 归档 11 件 .log（gate-all、ird_gates 双形态、集成全量构建、冒烟配置/构建、三测试控制台、validate-task），实际全部被根 .gitignore:87 吞、未按 UI-T05 先例 git add -f（在库仅 7 件：3 XML＋3 report＋gates-hit-set.txt）；wp10-t08-gate-registrations.md 首节"原始输出：ird_gates.log/ird_gates-script.log"为在库悬空引用（F-113 同型）。数值主张全部经验收者亲手复现准确（构建零错误/56＋3＋25 双模式/门禁 41＝40＋1/validate PASS/gate-all 20/24 与 4 项既有环境面失败属实）——证据本体完整，方向为"声称在库而实际未入库" | 已随本 evidence 分支转登 findings.json（回填 11 件或按 F-081 家族裁决闭环；按家族先例不影响验收） |
| F-271 | suggestion | F-012/F-232~F-242/F-247/F-258/F-261/F-263/F-264/F-267/F-269 家族第 16 例：提交 3bff786a 标题前缀后以名词短语『ui 单元卡』开头，不合『动词开头』字面（同提交 a660f3c2 以动词『实现』开头合规）；正文四段齐备、验证结论经独立复跑证实，纯风格瑕疵 | 已随本 evidence 分支转登 findings.json（随 F-247 家族统一跟踪，不单独返工） |
| F-272 | suggestion | F-220/F-236 家族同型（命中清单留痕格式漂移＋散文计数失准）：wp10-t08-gate-registrations.md 称"本任务沿用 wp10-t07 留痕文件形态"且"行数 87→89 与门禁退出报告『命中 41 项』一致"——实际在库 gates-hit-set.txt 为 82 行（41×2 相位行；wp10-t07 文件 87 行含 `[ird_gates] ` 行首前缀＋尾部 7 行门禁自测段，本任务文件前缀剥离、自测段略去），两处描述均与在库工件不符；门禁实质结论（基线 40＋恰增 1＝41、新增命中＝ParamTablePanel.cpp R3 头包含面、其余文件零命中）经验收者脚本模式亲跑去重归一化后逐码一致为真——留痕散文/形态描述失准，无实质门禁误差 | 已随本 evidence 分支转登 findings.json（后继任务以实际文件形态为准作基线比对；与 F-220 一并跟踪格式归一） |
| F-273 | suggestion | 注释枚举计数漂移（先于本任务存在、本任务增量改写时沿袭）：ui/CMakeLists.txt AUTOMOC 复核注释行"十个源文件与私有头零 Q_OBJECT 声明"的源文件计数与 src/ 实际 11 个产品 TU 不符——base 行"八个源文件"时实际已有 9 TU（偏差先于本任务），本任务 +2 增列时按同口径同步改写未校准绝对值；实质性主张"零 Q_OBJECT"经全文件 grep 证实为真，零行为影响（AGENTS §2.5"注释随代码更新"口径的边缘项） | 已随本 evidence 分支转登 findings.json（ui 单元实施者后续随手校准，或随门禁/注释治理任务统一处理；不影响验收） |

## 裁决

**verdict＝pass**：4.1～4.8 与 4.11 全部通过；4.9/4.10 存在的缺口（标题名词短语第 16 例、11 件日志未入库＋2 处悬空引用、gate-registrations 行数/形态描述失准、CMake 注释枚举计数沿袭漂移）均为**既有家族性建议级问题**（F-266/F-268/F-220/F-236 先例明确："不影响验收——证据本体完整且全量复现"），两提交的全部验证结论经本验收亲手复现无一虚报，无阻断级问题。F-270/F-271/F-272/F-273 已按 ACC §5 强制转登本 evidence 分支 `traceability/findings.json`，不阻塞合入。验收通过对象＝**3bff786a9a02dc72644ac61fcc1b5da88b684269**（非分支引用）；后续收尾按 PIPE §4.7 由所有者执行（建议随收尾一并裁决 F-270 的日志回填路径、F-265 的顺手还原时点，并提示下序 UI 任务标题动词开头）。

验收者合规声明：本会话未合入任何分支、未修改实施产物（worktree A 变异试验后经 git 还原至与送验对象逐字节一致——`git status --porcelain` 对 industrialrobot/ 零输出）、未向任务分支 wp10-t08 推送任何提交；本记录唯一写入面＝evidence 分支 acc/UI-T08/1（验收记录＋findings.json 四条转登）。
