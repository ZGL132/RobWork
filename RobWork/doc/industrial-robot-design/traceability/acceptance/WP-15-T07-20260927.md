# WP-15-T07 实施验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | WP-15-T07（接入 policy 碰撞证据并落实 KIN-05 语义——④端口） |
| 契约 | `doc/industrial-robot-design/tasks/foundation/WP-15-T07.json`（status=ready，attempt 1 送验） |
| 送验对象（冻结） | `2a9ba53b63ce52500936ebb667977bcabb5ea093`（单提交；分支漂移检测：`git ls-remote origin refs/heads/wp15-t07` ＝ 2a9ba53b…，与送验 SHA 一致——无漂移） |
| diff 范围 | `82e56dfd6c73f6cc992b66b8594c239005a9a4bf..2a9ba53b63ce52500936ebb667977bcabb5ea093`（state.base..headSha；base 即送验提交的唯一父提交，实测 `git rev-parse 2a9ba53b^` 一致） |
| **verdict** | **pass**（4.1~4.11 全部通过；无阻断级问题；建议级 3 项转登 findings.json：F-398/F-399/F-400） |
| 验收环境 | Windows 10 x64（10.0.26200）；VS 2022 x64（17.12.6，生成器 Visual Studio 17 2022 -A x64）；Qt 6.11.1 msvc2022_64（CMAKE_PREFIX_PATH）；vcpkg 经典模式（仓库根 installed/x64-windows）；pwsh 7。复现现场＝`git worktree add --detach` @ 2a9ba53b（送验 SHA，非分支名）；worktree 缺 gitignored `RobWork/RobWorkStudio/bin/RobWorkStudio.ini.template.static`（及 `.shared.in`），已从主检出**只读复制**后 configure（T15/T16/T02~T06 实录同型现象）。构建前 tasklist 无 RobWorkStudio.exe/msbuild 残留进程；主检出零写操作（只读复制模板两件除外）；一切写操作在自建临时 worktree（wtA/wtBase）。 |
| 证据目录 | `traceability/builds/wp15-t07-acc/`（本 evidence 分支，13 件：集成/冒烟构建日志、gtest XML×4〔集成 69/0＋50/0、冒烟 63/0＋50/0〕、ird_gates 引擎直跑双侧日志＋归一化直方图×2、变异测试真红日志×3、变异后还原复绿记录见记录正文） |

---

## 4.1 改动范围 —— PASS

- `git diff --name-status 82e56dfd..2a9ba53b`：33 个变更文件（14 个 `kinematics/**` 源码/测试＋18 个 `traceability/builds/wp15-t07/**` 留痕＋1 个 `units/kinematics.md`），逐文件比对契约 `allowedFiles` 三模式（`RobWork/RobWorkStudio/src/rwslibs/industrialrobot/kinematics/**`、`RobWork/doc/industrial-robot-design/traceability/**`、`RobWork/doc/industrial-robot-design/units/kinematics.md`）——**全部命中，零越界**（grep 反证：范围外文件计数 0）。
- `forbiddenFiles`（REQUIREMENTS.md/ARCHITECTURE.md）：不在变更清单——零触碰。
- `patches/`：范围零变更——SA-02 无登记需求。
- 分支漂移：`git ls-remote origin refs/heads/wp15-t07`＝`2a9ba53b63ce52500936ebb667977bcabb5ea093`＝送验 SHA；`refs/heads/redesign-main`＝82e56dfd＝base（ACC §3 双漂移检测通过）。

## 4.2 可复现构建 —— PASS

- **集成模式**（全新构建树 `_accT07/build-int`，wtA 源）：`cmake -S RobWork -B build-int -G "Visual Studio 17 2022" -A x64 -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64 -DCMAKE_TOOLCHAIN_FILE=<仓库根>/vcpkg/scripts/buildsystems/vcpkg.cmake -DRWS_BUILD_INDUSTRIALROBOT=ON` → 配置成功；`grep RWS_BUILD_INDUSTRIALROBOT build-int/CMakeCache.txt` → `:BOOL=ON` 实证。`cmake --build build-int --config Release --target sdurws_ird_kinematics sdurws_ird_kinematics_test sdurws_ird_kinematics_contract_test` → **EXIT=0**，三目标 `.lib/.exe` 产物在案，日志 `wp15-t07-acc/int-build.log`（error 正则计数 0）。
- **独立冒烟模式**（`_accT07/build-smoke`）：`cmake -S RobWork/RobWorkStudio/src/rwslibs/industrialrobot -B build-smoke … -DCMAKE_TOOLCHAIN_FILE=<仓库根>/vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`（Qt6 前缀为配置期必需——testkit `sdurws_ird_testkit_qt` 消费面，T06 卡面同口径）→ `cmake --build build-smoke --config Release` 全量 → **EXIT=0**，error 计数 0，`sdurws_ird_kinematics{_test,_contract_test}` 冒烟产物在案，日志 `wp15-t07-acc/smoke-build.log`。
- **测试复现**（验收者亲手执行，非采信送验日志）：
  - 集成：`sdurws_ird_kinematics_test` **69/69**（XML `acc_kin_test.xml` tests=69 failures=0）＋`sdurws_ird_kinematics_contract_test` **50/50**（`acc_kin_contract.xml` tests=50 failures=0）；`ctest --test-dir build-int/RobWorkStudio/src/rwslibs/industrialrobot/kinematics -C Release -L "^ird$"` → **2/2 passed**。
  - 冒烟：`_test` **63/63**（`acc_smoke_kin_test.xml` tests=63）＋`_contract_test` **50/50**；`--gtest_list_tests` 实证冒烟二进制**零 KinCollisionPort 套件**、集成二进制恰 6 用例——CollisionPortTest 仅集成编译的登记口径逐字成立。
  - 计数对账：送验提交内 XML（tests=69/50 failures=0）与验收者实测一致；卡面 §14.6 v0.7 宣称"69/69＋50/50 集成、63/63＋50/50 冒烟"与实测一致；提交正文同。新增 14 用例（base 62＋43 → head 69＋50，差值＝6 CollisionPort＋1 覆盖设施臂＋4 Consistency＋2 单点＋1 批量，TEST 逐一具名清点一致）。

## 4.3 契约 verify 命令 —— PASS（2/3 亲测全过；1 条口径缺行已补建）

| # | 契约 verify 原文 | 结果 |
| --- | --- | --- |
| 1 | `cmake --build build --config Release --target sdurws_ird_kinematics` | **亲测通过**（独立再跑一次：产物刷新、零错误） |
| 2 | `ctest --test-dir build/RobWorkStudio/src/rwslibs/industrialrobot/kinematics -C Release -L "^ird$"` | **亲测通过**（2/2，ird 标签时间汇总在案） |
| 3 | `pwsh -File RobWork/scripts/industrialrobot/validate-docs.ps1` | **亲测通过**：`validate-docs: PASS (20 units, 12 trace entries, 189 task files)`，退出码 0 |

- 口径缺行（F-300/F-358/F-367/F-368/F-371/F-373/F-375/F-381/F-397 家族复发）：verify 数组无 `sdurws_ird_kinematics_test`/`sdurws_ird_kinematics_contract_test` 构建行——验收者已在全新构建树自行补建两测试目标并执行（上表 4.2），不构成阻断；转登 **F-399**。

## 4.4 契约 acceptance 逐条证据 —— PASS（5/5，逐条具名）

| # | acceptance 摘要 | 证据（验收者实测/行号） |
| --- | --- | --- |
| 1 | ④端口消费形态：CollisionScene 按快照装配（全 ObjectId 禁拼串）；构型级碰撞＝该解被过滤（KIN-COLLISION-FILTERED）；一构型碰撞另一构型有效→素材不受影响（V-08/C8） | 装配唯一执行点 `Collision.cpp` L126~204：链连杆/工具/场景对象三段投影一律 `ObjectId`（L166/177/187），相邻对＝链序＋工具安装对（L195~201）；⑥端口名称上下文纯转发零加工（L62~89——转发体无字符串操作，R-4）。测试：`KinCollisionPort.AssembleSceneFromSnapshotFacts_WP15T07_ACC1`（4 对象角色/几何事实/2 相邻对/CR-04 身份逐字段断言，通过）；`KinCollisionPort.RealSessionVerdictAndConsistency_WP15T07_ACC1_ACC3`（真实后端 q=0 相交→对象对含环境端 ObjectId；q=π 分离→Evaluated 无碰撞）；**V-08 双分支**：`KinIk.CollisionFilteredKeepsValidConfiguration_WP15T04_ACC2`（IkTest.cpp:697——q[1]>0 构型碰撞被过滤、另一构型留解集、PartialCollision、无 proofMaterial）经本任务三态轨改造后回归通过（含于 69）；`KinTaskPointIkEval.CollisionFilteredDiagEmittedPerRecord_WP15T07_ACC1`（恒碰撞→结局 3＋KIN-COLLISION-FILTERED 逐记录≥1＋UNAVAILABLE 恒否） |
| 2 | 缺检测器/策略未启用→DataInsufficient 绝不视为无碰撞（KIN-COLLISION-UNAVAILABLE）；设施异常→该样本/解标记＋诊断、不中断整批 | 接线三态：`KinCollisionPort.WiringPolicyDisabledAndDetectorArms_WP15T07_ACC5_ACC2`（PolicyDisabled 臂 session 恒空＋DetectorUnavailable 臂 detail 非空）；fail-safe：`KinCollisionPort.DisabledPolicySessionMapsToEvidenceMissing_WP15T07_ACC2`（禁用被误调用→EvidenceMissing 绝不当无碰撞）；设施臂：`KinCollisionPort.FacilityFailureMapsToFacilityFailed_WP15T07_ACC2`（注入 rw 异常→FacilityFailed＋inCollision=false 恒否＋statusDetail 非空）、`KinTaskPointIkEval.FacilityFailureYieldsEvidenceMissingDiag_WP15T07_ACC2`（解保留 payload 在场＋UNAVAILABLE 在产＋FILTERED 恒否——**不中断求解**）、`KinRegionCoverage.CollisionFacilityFailureSampleInsufficient_WP15T07_ACC2`（样本降级 DataInsufficient＋downgraded=1＋UNAVAILABLE；无要求对照不降级——标记面≠缺陷面）、`KinTaskPointsBatchEval.CollisionEvidenceRowsAndUnavailableDiag_WP15T07_ACC2_ACC4`（Missing 行 subject==缺失点＋UNAVAILABLE 恰 1，其余 2 项结局行齐备——**不中断整批**） |
| 3 | 三入口一致（AT-19/V-25）：无本地副本/无二次缓存（静态扫描＋运行断言）；无阈值/模式参数（R-POL-5）；禁直链 proximity（R-POL-2）；策略版本入身份（CON-06） | 静态扫描：`KinCollisionConsistency.StaticScanNoCollisionImplementationCopy_WP15T07_ACC3`（产品面 7 个 proximity token 零命中＋`policy/CollisionEvaluator.hpp` include 恰 2 处均限 Collision.hpp/.cpp＋createSession 单 TU）；运行断言：`KinCollisionPort.AdapterForwardsEveryQueryNoCache_WP15T07_ACC3`（计数后端 5 次适配器查询＝5 次底层 doInCollision）；R-POL-5：查询仅 `kind=SingleState＋configurations` 两字段赋值（Collision.cpp L230~232），结构上无阈值成员可传；CON-06：`KinCollisionConsistency.PolicyResolvedDependencyInAllDescriptors_WP15T07_ACC3`（四 descriptor policy.resolved＝Required＋Policy Kind；coverage 另有 collision-models Conditional）＋`RealSessionVerdictAndConsistency`（wired/adapter 两面 policyContentIdentity==resolved 身份＋重复接线会话身份等价）；验收者 grep 反证：include 面零 `collisionEnabled/enableCollision` 字样 |
| 4 | 证据明细三元组随 EvaluationOutput.evidence：{subjectPair(ObjectId×ObjectId), configurationRef, verdict}；作用域＝构型级；路径级不产 | `Evidence.hpp` L101~109 `kKinBatchCollisionRowId="kin.collision-verdict"`＋L614~630 `encodeCollisionVerdictRowCanonical` 布局登记（len+bytes＋16B×2＋verdict u8）；实现 `Evidence.cpp`（三元组逐条编码、条目序＝最佳解在前＋Collision 过滤记录全序在后、缺失态不出条目）；批量测试断言 Satisfied 行 digest 非全零＋同输入重复评估摘要逐位一致＋Missing/不出行三态清点；构型级作用域＝适配器 SingleState 单构型（Collision.cpp L230~232）＋`ik.region-coverage`/`kin.task-points-batch` 无任何路径级碰撞 API（结构性grep 零命中）；路径级归 trajectory（TRJ-03/04）与本单元无产出面一致 |
| 5 | 不另存碰撞布尔开关（DTB 禁止项）：启用状态只读自 policy；域值模型与配置 schema 无开关字段（结构性检查） | `KinCollisionConsistency.StructuralNoCollisionEnableSwitch_WP15T07_ACC5`（void_t 探测 IkRequest/TaskPointIkQuery/BatchQuery/RegionCoverageQuery × {collisionEnabled, enableCollision} 8 断言全过）；唯一开关读取点＝`makeCollisionSession` L313 `resolvedPolicy.collision.enabled`（读 policy 不读本单元状态）；配置 schema：AnalysisConfiguration 实体 T10 落位（卡 §4.4），现面可探测四型即请求/查询 schema 全集（include 面 grep 零开关字样佐证）；BatchDemands.collisionFreeRequired 系要求值非开关的语义澄清已随卡 §14.6 v0.7 登记 |

## 4.5 测试真实失败能力（变异测试）—— PASS（3 处实质变异，具名测试真红；已还原复绿）

全程在 wtA 源树实施，每处变异独立重建受影响目标后运行具名测试；日志留存 `wp15-t07-acc/acc_mut_m{1,2,3}_run.log`。

| # | 变异点（实质语义反转方向） | 预期红面 | 实测 |
| --- | --- | --- | --- |
| M1 | `Ik.cpp` 硬过滤③缺失臂（EvidenceMissing/FacilityFailed）→ 伪装"已评价无碰撞"（`evaluated=true, inCollision=false`，删缺失计数）——KIN-05 违约方向 | `KinTaskPointIkEval.FacilityFailureYieldsEvidenceMissingDiag_WP15T07_ACC2` | **FAILED（1 failed）**——UNAVAILABLE 诊断消失被断言捕获 |
| M2 | `Collision.cpp` `makeCollisionSession` 第 1 步 `if (!resolvedPolicy.collision.enabled)` → `if (false && …)`（策略启用直读篡改） | `KinCollisionPort.WiringPolicyDisabledAndDetectorArms_WP15T07_ACC5_ACC2` | **FAILED（1 failed）**——PolicyDisabled 三态翻转被断言捕获 |
| M3 | `Evidence.cpp` 批量碰撞行缺失臂 `Missing` → `Satisfied`（伪造已检方向） | `KinTaskPointsBatchEval.CollisionEvidenceRowsAndUnavailableDiag_WP15T07_ACC2_ACC4` | **FAILED（1 failed）**——行状态/subject 清点被断言捕获 |

- **还原与复绿**：三处逐一 `git checkout --` 还原，`git status --porcelain` 计数 0、`git rev-parse HEAD` 仍为 2a9ba53b（送验对象零污染）；还原后全量重建并复跑：**69/69＋50/50 全绿**（T14 教训口径——先还原再全量）。
- 结论：三具名测试非恒真，钉住的正是 KIN-05 三条关键语义（缺失不当作无碰撞/启用状态只读 policy/缺失不伪造 Satisfied 行）。

## 4.6 架构红线 —— PASS（AGENTS §5 八条逐条）

1. **L2 零 Qt**：kinematics include/src 全树 `#include <Q|Q_OBJECT|QWidget` 计数 0（grep 实证）；本批新增文件同。
2. **业务域互链 R-1**：`target_link_libraries(sdurws_ird_kinematics PUBLIC …)` 六边＝core/diagnostics/runtime/policy/evidence/execution（CMakeLists.txt:168~174，与 §3.2 登记册一致）；无 modeling/requirements/trajectory/dynamics/selection/optimization 边。
3. **跨单元私有头 R-2**：本批全部跨单元 include 均为 `include/sdurws/ird/<unit>/…` 公共头（policy/CollisionEvaluator.hpp、policy/CollisionQuery.hpp、policy/Contexts.hpp、runtime/CanonicalModel.hpp、runtime/Errors.hpp、core/Identity.hpp）。
4. **框架零源码修改 SA-02**：diff 无 patches/ 变更、无工业机器人区外源码变更（4.1 已证）。
5. **R-4 名称前缀**：⑥端口转发实现（Collision.cpp L62~89）只调 `m_source.tryObjectId/tryRuntimeName`，零拼拆；门禁比对 R-4 命中 3 处 base/head 相同（零新增）。
6. **权威唯一 PA-1**：碰撞实现唯一归 policy（本单元仅装配＋消费判定值）；诊断码只消费 DiagCodes.hpp 在册常量（`KinCollisionConsistency.CollisionDiagCodeConstantsRegistered_WP15T07_ACC2` 钉住两码文本）；名称解析归 runtime（转发）。
7. **不可变历史 PA-2**：本任务无修订/写路径触碰（纯计算单元语义）。
8. **不恢复 old/**：新增代码无任何 old/ 引用（grep 零命中）。

## 4.7 注释规范 —— PASS（AGENTS §2 抽查；公共头全查＋实现 4 件）

- **公共头全查**：`Collision.hpp`（357 行新增——文件头设计依据/背景/红线锚点/线程/确定性五段＋三类一函数的完整 Doxygen：@pre/@post/@param [in]/@return/@throws、错误分轨"调用方 fail-fast vs 环境证据缺失轨"、所有权"非 owning/共享别名保活"、单位"rad／m"逐处标注）；`Ik.hpp` 表尾追加（三值枚举逐值语义＋追加成员默认值纪律＋两标记分工与同码口径）；`Evaluators.hpp`/`Evidence.hpp` 追加行（产码面分工、行状态语义、编码布局定宽小端＋确定性声明）；`Sampling.hpp` 文件头 DataInsufficient 行随附同步。
- **实现文件抽查 4 件（≥3 达标）**：`Collision.cpp`（每语义段中文注释：装配四步、三态映射逐臂、接线四步序号）、`Ik.cpp`（三态轨分支语义＋"不中断其余候选"整批语义）、`Evaluators.cpp`（逐产码点登记 §9.6 行号＋计数口径"标记面≠缺陷面"）、`Evidence.cpp`（发射三态判据＋保留值占位语义）。
- **§2.5 高危信息**：单位（kClearQ rad、kBoxSize m、residual m/rad 逐处）、坐标系（方向"基座系 {B}"）、所有权（非 owning/宿主持有/共享别名）、线程（适配器"构造后只读、并发只读安全"）、错误语义（违约 fail-fast vs 环境素材轨逐点注明）、确定性（"同输入同输出"逐函数声明）——均在场。测试文件 fixture/断言处具名需求与 AT 追溯（IRD_TEST_INFO）齐备。

## 4.8 文档同步与 findings 相关性 —— PASS（含 3 项建议级转登）

- **单元卡同步**：`units/kinematics.md` 卡头版本行 v0.6→**v0.7**（2026-09-27）、§3.3 布局表 `Collision.hpp` 行新增（T07）、§11 WP-15-T07 行"已完成"标注、§14.6 v0.7 变更记录行（①~⑤逐项与实现面逐字核对一致——含两臂合并标记/两臂同码/设施异常复用同码 cause 区分/"不在范围"样本不入计数等全部语义扩张登记，编排者清单第 8 条逐项对上）。validate-docs PASS（20 units/12 trace/189 task files）。
- **任务状态**：契约 status 翻转与 DTB 行随治理收尾簿记（PIPE §4.7）执行——T02~T06 既有口径，非实施提交义务。
- **未关闭 findings 相关性核对**（重点四条逐条）：
  - **F-394**（F-387/F-390/F-393 散文纠正督办）：三处纠正已随 T06 卡 v0.6 兑现——head 卡面实证："＋1 项 sanctioned 命中（Eigen PRIVATE——O-40）"、"KinIk 18 新用例"、"KinTaskPointsBatchEval 14 新契约用例"均在场（本任务无需再兑现；F-380 状态翻转仍留治理收尾）。
  - **F-395**（IRDSSID1 登记布局 8B vs 实现 7B）：**head 原样未纠正**（Sampling.hpp:450 附近登记 `"IRDSSID1"（8 字节 ASCII magic）` vs Sampling.cpp:148~149 `magic+7`）——本任务触及 Sampling.hpp（仅文件头 DataInsufficient 段注释，未及该站点）但未执行 owner 指令"下次触及 Sampling.hpp 时统一登记与实现口径"→ 再次未兑现，转登 **F-400**（与 F-396 合并登记）。
  - **F-396**（generateDirections N=1 退化方向注释 +Z vs 公式 +X）：**head 原样未纠正**（Sampling.hpp:529 附近）→ 同上并入 F-400。
  - **F-397**（契约 verify 缺测试目标构建行家族）：本契约 WP-15-T07.json verify 数组同型 3 行——家族复发，转登 **F-399**。
  - 其余 open 条目（F-386/F-389/F-391 消费面等）：本任务未触及对应面，维持 open，无静默积压。

## 4.9 commit 质量 —— PASS

- 标题 `[WP-15-T07] 实现 kinematics 单元④端口碰撞接入与 KIN-05 语义`——任务编号前缀＋动词开头，≤50 字（F-392 家族形态已纠正）。
- 正文四段齐备（What 按文件列点/Why 对契约 acceptance 与 §8.1/knownPitfalls 消账/How 验证数据/影响面兼容性），`Acc-Covered: 1,2,3,4,5` 尾注。
- 验证段与验收者亲测**逐项一致**：集成构建零错误、69/69＋50/50、ctest 2/2、冒烟 63/63＋50/50、validate-docs 20/12/189——全部可复现。
- 一处口径标注不准（建议级，非粉饰）：验证段与卡面 v0.7 沿用附件 ird-gates 比对的"三套计数…去重条目 69"——该"去重"数实为引擎自计数；按附件自身声明的口径②（归一化后 sort 去重）实测为 **76**（见 4.11 门禁段），转登 **F-398**。

## 4.10 留痕完整 —— PASS

- 送验提交内 `traceability/builds/wp15-t07/` **18 件**实测在位：两模式构建/运行日志×6、gtest XML×2（原样格式，root `tests="69" failures="0"`／`tests="50" failures="0"`——非手工转抄）、ird-test-report 双份（summary 69/69/0＋50/50/0，decisive=true）、ird_gates 双侧日志＋base_hits.norm/head_hits.norm＋base-head.diff（空）＋比对附件 md。
- 验收者侧复现留痕 13 件落 `traceability/builds/wp15-t07-acc/`（本 evidence 分支）。
- DTB WP-15-T07 行在案；完成状态随收尾簿记（与 T02~T06 口径一致）。

## 4.11 偷懒/缩水实现扫描 —— PASS

1. **TODO/FIXME/stub/placeholder/未实现/not implemented 扫描**：diff 全量 grep 命中 9 行——逐条核验：①`+/// StubCollisionSession 同款形态`（TaskPointIkEvaluatorContractTest.cpp 新增替身注释，指称 T04 既有测试替身的形态同源，非产品缩水标记）；②其余 8 行均为 traceability/builds/wp15-t07/ 留痕日志内的门禁输出文本（IRD-GATE-R3 对 ui/View3DPlaceholder.cpp 的存量命中记录——证据文件内容，非源码）。产品源码零命中；无契约"触发式延后"豁免事项。
2. **空实现检测**：新增公共函数（assembleCollisionScene/PolicyCollisionSessionAdapter/makeCollisionSession/encodeCollisionVerdictRowCanonical/诊断产码面）函数体全部实质实现；4.5 三处变异真红反证实现与测试均非恒真/空壳。
3. **逐条证据表**：见 4.4——五条 acceptance 全部有具名测试/文件行号，任一未缺。
4. **测试强度**：断言校验行为与边界（对象对逐项、行状态清点、digest 逐位一致、降级标记、计数恰 N、恒否断言），非"只编译通过"；与 4.5 联动实证。

**ird_gates 门禁（引擎直跑 base..head 归一化比对）**：验收者在自建 wtA（head）/wtBase（82e56dfd）双侧直跑 `cmake -DIRD_ROOT=<源码根> -P cmake/ird_gates.cmake`，日志 `acc_gates_head.log`/`acc_gates_base.log`。三套计数并列：①行数＝145/145；②去重条目（归一化后 sort 去重）＝**76/76**；③引擎自计数＝"命中 69 项"/"命中 69 项"。归一化（剥盘符绝对路径＋剥 `[ird_gates] ` 前缀）后 `diff` **空**——零新增命中、零消除（与送验附件 base-head.diff 为空一致；因零新增，无 sanctioned 核对事项）。直方图逐码 base=head：LIB 12/R1 7/R3 41/R4 3/R5 1/SUB 75/T1 5/T2 1＝145。退出码双侧均 1——DTB §4.5 存量命中既有状态，非本任务新增。**口径偏差**：送验比对附件宣称"去重条目 69"，按其自身声明口径②实测 76（69 为引擎自计数；v0.4/v0.5/v0.6 卡面行同句式沿用）——转登 **F-398**；零增量结论不受影响（双侧文件逐字节一致）。

---

## 发现清单

**阻断级：无。**

**建议级（转登 findings.json，编号顺延自 F-397）**：

| 编号 | 一句话 | 责任方 |
| --- | --- | --- |
| F-398 | ird_gates 比对附件（及卡 v0.4~v0.7 同句式、提交验证段）宣称"去重条目 69"与自身声明口径②（归一化后 sort 去重）实测 76 不符——69 实为引擎自计数；零增量结论经验收者独立复跑证实不受影响 | kinematics 实施会话（下次产出比对附件时更正计数口径标注）；治理收尾会话（簿记时核对） |
| F-399 | tasks/foundation/WP-15-T07.json verify 数组仅 3 行，缺两测试目标构建行——F-300/F-358/F-367/F-368/F-371/F-373/F-375/F-381/F-397 家族复发；验收者已补建并执行（69/69＋50/50 集成、63/63＋50/50 冒烟） | 契约编译/治理会话（家族批量修订时补齐） |
| F-400 | F-395/F-396 的 owner 指令"下次触及 Sampling.hpp 时纠正"在本任务（触及该头文件文件头注释段）再次未兑现：:450 附近 8B 布局登记 vs Sampling.cpp:148~149 实写 7B、:529 附近 N=1 退化方向 +Z vs 公式 +X 两处原样 | kinematics 实施会话（下次触及 Sampling.hpp 时与 F-395/F-396 合并执行；evidence 卡冻结对齐前必须消账 F-395） |

## 裁决

**verdict = pass**：4.1~4.11 全部通过，无阻断级问题；建议级 3 项已转登 findings.json。验收对象＝SHA `2a9ba53b63ce52500936ebb667977bcabb5ea093`（非分支引用）。
