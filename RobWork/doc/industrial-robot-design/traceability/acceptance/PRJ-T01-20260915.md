# PRJ-T01 验收记录（attempt 1，2026-09-15）

| 字段 | 值 |
| --- | --- |
| 任务 | PRJ-T01（project 单元构建落位：INTERFACE 占位转真实库；契约 tasks/foundation/PRJ-T01.json） |
| **verdict** | **pass**（4.1～4.11 全部通过；零阻断级问题；建议级 4 项已转登 findings.json F-121～F-124） |
| 送验对象（冻结） | `5de89534c1c88eadce53cc6c8327e1fbd9b542e0`（40 位，单提交）；分支漂移检测：`origin/wp04-t01` ＝ 本地 `wp04-t01` ＝ 该 SHA，零漂移 |
| diff 范围 | `e69f70cf1739b90cbaa18b0048300826384116db..5de89534`（state.base..headSha；merge-base 核验＝base 本身） |
| 验收者环境 | 全新上下文；复现现场＝`git worktree add --detach` @5de89534（下称"全新树"）；集成模式全新配置（首轮即带 vcpkg toolchain＋Qt 前缀＋CMAKE_CXX_FLAGS，F-062/F-098/F-099/F-101 口径；`RobWorkStudio.ini.template.static/.shared.in` 两件本地资源自主树复制——F-062 登记件）；冒烟模式另配独立临时目录；MSBuild 17.12.6＋VS2022 x64；pwsh 7 治理脚本 |
| evidence | 分支 `acc/PRJ-T01/1`（本文件＋findings.json F-121～F-124 同支承载；commit 三元组见文末） |

---

## 1. 逐项证据（ACC §4.1～4.11）

### 4.1 改动范围 —— PASS

- `git diff --name-status base..head` 共 22 文件，逐一比对契约 `allowedFiles` 四模式：
  `industrialrobot/CMakeLists.txt`（显式列名）✓；`project/**`×5 ✓；`traceability/**`×15 ✓；`units/project.md`（显式列名）✓。
- `forbiddenFiles`（REQUIREMENTS.md / ARCHITECTURE.md）零触碰 ✓。
- `industrialrobot/patches/` 零新文件（`git diff --name-only base..head -- …/patches/` 输出 0 行）✓；框架源码（RobWork/RWS/RobWorkSim）零改动（diff 全清单核对）✓——SA-02 无登记义务产生。

### 4.2 可复现构建 —— PASS（验收者全新树亲手复现，不采信实施日志）

- **集成模式**：全新 build 树配置 exit 0（`-- Configuring done` 零 Error；缓存 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON` grep 确认）。
  - 契约 verify 目标 `sdurws_ird_project_test` 从零构建 exit 0、零 error 行（含框架链 sdurw_core/common/math＋sdurws_ird_core＋project）。
  - `sdurws_ird_project_contract_test` 构建 exit 0。
  - 两测试可执行文件运行：**5/5＋5/5＝10/10 全绿**（XML 落验收者证据目录）。
- **独立冒烟模式**：`-S industrialrobot -B <独立临时目录> -DCMAKE_TOOLCHAIN_FILE=vcpkg…`（AGENTS §4.1＋CORE-T01 G-2 口径）配置 exit 0；`--config Release` 全量构建 exit 0、零 error 行、产出 `sdurws_ird_project.lib`＋两测试 exe；运行 **10/10 全绿**。
- 树性质说明：全新树全量集成构建受 F-008 基线框架缺陷阻挡（既有登记，非本任务引入），本次复现覆盖任务目标面（project 链 core＋两测试目标＋门禁＋脚本），与既往验收口径一致；送验 `integration-build.log` 产自主树 `build/`（存量增量，日志路径与既有目标重链接可证），提交信息以"集成 build/ 树重配置（缓存确认 :BOOL=ON）"如实指认树身份、未宣称全新树/最强口径——按 F-036/F-054 纪律判定无强度失实。

### 4.3 契约 verify 命令 —— PASS

- 契约 `verify` 数组仅 1 条，验收者在全新树**逐字原样执行**（worktree 根、相对 `build` 路径）：
  `cmake --build build --config Release --target sdurws_ird_project_test` → **exit 0**（构建产物 `sdurws_ird_project_test.exe` 落位，gtest DLL 拷贝步骤执行）。

### 4.4 acceptance 条目逐条 —— PASS（5/5 有对应证据；无虚标）

| # | 契约条目 | 验收者证据 |
| --- | --- | --- |
| 1 | 双模式构建零错误；INTERFACE→STATIC（C++17、PUBLIC 链 core）；目标名/别名/include 根不变；新建 project/CMakeLists.txt 并在父 CMakeLists 注册；src/ 空起步 | 两模式配置/构建 exit 0（4.2）；CMakeLists 全文走读（`add_library(sdurws_ird_project STATIC src/Project.cpp)`、`target_compile_features PUBLIC cxx_std_17`＋`CXX_STANDARD 17` 双设、`target_link_libraries(… PUBLIC sdurws_ird_core)`）；父 CMakeLists `add_subdirectory(project)` 挂接 core/diagnostics 之后、移出占位循环（diff 行核对）；src/ 形态见 4.11② |
| 2 | 红线扫描零命中（PRJ-TX-14 全项）；仅 project→core 编译链接边；diagnostics 注入不落链接；零 io 编译期消费 | 运行期扫描用例 10/10（两模式）；`ird_gates` 验收者亲跑：**8 命中＝基线逐码一致、零 project 项**（SUB×6＝diagnostics_contract_test/evidence_test/evidence_contract_test/policy_test/policy_contract_test/runtime_test→testkit＋testdata_lint SUB＋testdata_lint T1；与 findings F-019/F-051/F-072/F-115 登记谱系一致）；白名单预登记核对：`ird_gates_whitelist.cmake` L30-31 含 `"project->core"`/`"project->diagnostics"`；LinkageContractTest 五用例（UnitEdgeOnlyCore/NoTestkitEdge/CoreEdgePresent/DiagnosticsEdgeInjectionNotLink/NoIoCompileTimeConsumption）全绿 |
| 3 | 两测试目标注册并可构建；gtest 经 vcpkg `find_package(GTest CONFIG REQUIRED)` 失败即停；最小构建冒烟用例 | 两目标两模式 build-pass＋run-pass（10/10）；CMakeLists 中 `find_package(GTest CONFIG REQUIRED)`＋`GTest::gtest_main`＋`ird_add_gtest` 宏（add_test＋`_report` XML 目标 EXCLUDE_FROM_ALL）；测试目标零 testkit 链接 |
| 4 | README 复核留痕（指向 §9→§12 修正核对）；保留"不参与编译/不表示已实现"声明 | `include/sdurws/ird/project/README.md` 追加 2026-09-15 复核行（diff 行核对）：指向 §12 确认零偏差＋声明保留；单元卡 v0.3 卡头构建落位行同步登记复核结论 |
| 5 | P-ENV-1／P-PR-1 陷阱处置 | P-ENV-1：C++17 双设＋验收者两模式构建亲证 C++17 目标与 C++11 框架库（sdurw_math 等）同树混链零错误；P-PR-1：产品与测试源码**零 `sdurws/ird/core` include 指令**（grep 实证唯一命中为 Project.cpp L48 注释散文；isIncludeDirective 门控语义正是为此设计），仅链接边不消费头 |

`ird_gates` 执行说明（ACC §4.4"未执行的不得标注通过"）：门禁已建成，验收者**实际执行** `cmake --build build --config Release --target ird_gates` → exit 1＝8 处基线既有命中（上述），**零新增、零 project 项**；送验报告以"F-072 族"家族归属并如实登记 exit 1（其中 diagnostics_contract_test 一项的精确编号为 F-115，家族归属兼容、无 F-060 型实质错归）。

### 4.5 测试真实失败能力 —— PASS（3 组探针注入即红；均还原复绿）

扫描类用例经 `IRD_PROJECT_UNIT_ROOT` 在**运行期**读源码树，改源码重跑 exe 即改结果，无需重编译——天然避开 F-068/F-075 增量构建陷阱；每轮探针后 `git checkout --` 还原并确认工作树干净（`git status --porcelain`＝0）。

| 探针 | 注入 | 结果 |
| --- | --- | --- |
| 1 | `Project.cpp` 追加 `#include <QCoreApplication>` | `NoQtInclude_DT_BUILD_NFR_MNT_01` **红**（exit 1）；还原复绿 5/5 |
| 2 | `project/CMakeLists.txt` 追加非注释行 `message(STATUS "probe sdurws_ird_testkit sdurws_ird_runtime")` | `UnitEdgeOnlyCore_DT_BUILD_R1_R2`＋`NoTestkitEdge_DT_BUILD_T1` **双红**（exit 1）；还原复绿 |
| 3b | CMakeLists 全文 `sdurws_ird_core`→`sdurws_ird_corex` | `UnitEdgeOnlyCore`＋`CoreEdgePresent` **双红**（exit 1）——断言本体存活非死代码；还原复绿 |
| 3（负发现） | 仅注释掉 `target_link_libraries(… PUBLIC sdurws_ird_core)` 链接行 | **仍绿**——配置期守卫文本（`NOT _lib STREQUAL "sdurws_ird_core"`）自身含同名 token 遮蔽链接行移除；→建议级 **F-123**（实现本身正确：链接行在位＋两模式链接成功亲证） |

### 4.6 架构红线（AGENTS §5 八条逐查）—— PASS

1. **L2 零 Qt**：project 全部源/CMake 零 Qt include/链接（测试用例持续钉住＋验收者 grep）；仅含 Q 前缀判别的扫描器。
2. **R-1 业务域互链禁止**：构建图仅 project→core 一条单元边（门禁零 project 命中＋UnitEdgeOnlyCore 用例＋CMake 文本亲读）。
3. **R-2 跨单元私有头**：include/ 仅 README.md（非编译头）；无私有头出 include（PublicHeaderPathLayout 用例）；测试/实现零他单元头（NoCrossUnitInclude＋io 双面扫描）。
4. **SA-02 框架零源码修改**：4.1 已证（patches/ 零触碰）。
5. **R-4 名称前缀拼接/剥离**：diff 内零名称处理逻辑（本任务无名称语义代码）。
6. **PA-1 权威唯一**：本任务纯构建落位，未实现他单元语义（命令/查询/评估器等零预建——Project.cpp 零声明零定义；诊断码未私造——零 PRJ-* 码注册代码）。
7. **PA-2 不可变历史**：不适用（无修订数据操作面）。
8. **不恢复 old/**：diff 零 old/ 引用。

### 4.7 注释规范（AGENTS §2）—— PASS

新增/大改代码文件共 4 份，全部逐一核查（超过"实现文件抽查 ≥3"口径）：

- `project/CMakeLists.txt`：文件头设计依据块（单元卡章节/DTB/需求编号逐项标注）＋每段落"为什么"级中文注释（真名取舍、/utf-8 缘由、TARGET_RUNTIME_DLLS 缘由、宏不跨单元共享的理由、守卫 NOTFOUND 语义）＋守卫 FATAL 消息含红线出处（§3.2/§11.2/ARCH §3.5）。
- `src/Project.cpp`：文件头 @file/@brief＋设计依据＋背景说明（为何锚点翻译单元而非零源码——CMake No SOURCES 报错实测）＋PRJ-T02~T14 落位安排＋P-PR-1 约束＋线程约束标注。
- `test/BuildRedLineTest.cpp` / `test/LinkageContractTest.cpp`：文件头设计依据＋每个 TEST 锚定注释（需求/红线/契约条目编号）＋分支边界注释（畸形行兜底、注释散文不误报的理由）。
- §2.5 高危信息表：物理单位/坐标系/角度制式——不适用（纯构建落位，零物理量）；所有权（README 保留位声明在位）；线程约束（Project.cpp 头显式标注）；错误语义（守卫 FATAL＝调用方契约违约 fail-fast，含出处）；确定性来源（扫描文件排序键——失败信息确定性，NFR-COR-02 注释在位）。
- 公共头：零新增（README.md 为保留位文档非编译头）——"公共头契约全查"对象为空集，如实记载。

### 4.8 文档同步 —— PASS

- `units/project.md` v0.2→v0.3（在 allowedFiles 内）：卡头构建落位行更新＋§15.4 v0.3 行登记；§3.3 括注"INTERFACE → PRJ-T01 升级 STATIC"兑现；正文契约语义零变更（diff 核对仅卡头行＋§15.4 行）。
- 偏差登记（DTB §5.4 口径）：src/ 锚点翻译单元形态（对"src/（空起步）"字面的技术化解释——零源码 STATIC 不可行）在单元卡 v0.3＋CMakeLists＋提交信息影响面三面登记；先例核验：CORE-T01 首版 `core/src/Core.cpp`（685c5f69）经 git 考古为 100% 注释行——"既定先例形态"主张属实。
- 状态/追踪同步：validate-task PASS（1 tasks）、validate-docs PASS（20 units, 12 trace entries, 150 task files）——验收者在 worktree 亲手复跑，数字与送验日志一致。
- **findings.json 未关闭条目相关性核对（v1.2）**：
  - F-019/F-051/F-072/F-115（门禁基线红）：直接相关——本任务门禁 8 命中全部落在该谱系、零 project 项，报告归属与登记册一致（未私改门禁，处置权 WP-01 所有者，符合 F-019 处置约束）。
  - F-065（留痕日志 mojibake 系统性）：gates 日志同现同型乱码——沿袭非新引入，家族已 open。
  - F-004（空转 MOC）：新目标构建日志仍见 `Automatic MOC for target sdurws_ird_project`——同族沿袭，消账归 F-004 辖域。
  - F-062/F-007/F-098/F-099/F-101（worktree/全新树环境前置链）：验收者按其口径补齐（toolchain/Qt 前缀/flags/两件 ini）后复现成功——反证该家族登记准确且仍必要。
  - F-057（卡头版本/日期同步家族）：发现 project.md 卡头**日期行**未随 v0.3 同步（停留 2026-09-09；版本行已同步）→ 转登 **F-122**。
  - 其余 open 条目（标题家族 F-012 系、幻影清单家族 F-081 系等）：与本任务面无交集；本任务**无**幻影清单（4.10）、标题家族再现 1 例（→F-121）。
- 随本任务可顺手消账项：无（各 open 条目处置权均在其他辖域，本任务 diff 未触及）。

### 4.9 commit 质量 —— PASS（附建议级 F-121）

- 标题 `[WP-04-T01]` 前缀＋编号合规；但前缀后为"**project 单元构建落位**：…"名词短语开头，非动词开头——F-012 家族再现、F-100（DIAG-T02 同为构建落位任务的同形态）之后再例 → 转登 **F-121**（纯风格，既往判例均不阻断）。
- 正文四段齐备（改动内容按文件列要点／改动原因对应需求与任务卡／验证结果／影响面与兼容性＋`Acc-Covered: 1,2,3,4,5` 对账行）。
- 验证结论与留痕/验收者独立复现逐项一致：双模式 10/10 ✓、verify exit 0 ✓、门禁 8 命中零 project 零新增 ✓、validate-task/validate-docs PASS ✓、留痕件数（15 件）与 `git diff --name-status` 实际入库一致 ✓。无粉饰。

### 4.10 留痕完整 —— PASS

- gtest XML×4＋`ird-test-report.json`＋构建日志（integration configure/build/test-run、smoke configure/build/test-run、verify-target-build、gates、validate-task、validate-docs）共 15 件**全部实际入库**（逐件 `git diff --name-status` 核对；提交信息"15 文件"与实际一致）——F-081/F-102/F-103/F-106/F-108/F-111/F-114 幻影清单家族本任务零命中。
- `phase-one-validation.log`／resultNote：未更新——与 CORE-T01 之后惯例一致（该 log 最后更新＝da407b61 CORE-T01；同族构建落位任务 DIAG-T02 亦未更新）；`ird-test-report.json` 的 acceptance 逐条对账表承担 resultNote 载体，按 ACC §4.10"或"分支判定成立。
- 口径观察（不立案）：本报告较 DIAG-T02 报告缺 `logs`/`realFailureEvidence` 索引键（F-106 曾将该缺项列为幻影发现的**加重项**而非独立违规）；本任务报告所有引用文件均实际在库、无幻影引用，判为报告口径差异不构成问题项。

### 4.11 偷懒/缩水实现扫描 —— PASS

- ① 标记扫描：diff 全文及 4 个新增文件独立扫描 `TODO|FIXME|stub|placeholder|未实现|not implemented` **零命中**（grep -c 实证）。
- ② 空实现检测：`src/Project.cpp` 仅注释、零声明零定义——系契约 acceptance 1 明文"src/ 空起步，不在骨架预建占位实现"的**既定先例形态**（CORE-T01/POL-T01/DIAG-T02 同款；CMake STATIC 零源码生成期报错的技术约束；单元卡 v0.3 已登记）——契约显式声明的豁免成立，逐条依据已列。
- ③ acceptance 逐条证据表：`ird-test-report.json` acceptance 数组 5 条逐条具名证据＋本记录 §1/4.4 验收者独立复核——无"无证据条目"。
- ④ 测试强度：断言校验行为与红线（include 指令行门控、目标引用白名单、配置期守卫），3 组探针注入即红（4.5）；`SourceTreeReachable` 存在性锚定防扫描失效；`CoreEdgePresent` 的"零表外边只证无多余、不证已建立"设计注释准确；敏感性缺口一处 → F-123。

---

## 2. 裁决

**verdict = pass**（4.1～4.11 全部通过，零阻断级问题；不阻塞合入）

建议级问题 4 项，已按 ACC §5 强制转登 `traceability/findings.json`（编号顺延自 F-120）：

| 编号 | 摘要 | 严重级 |
| --- | --- | --- |
| F-121 | 提交标题前缀后名词短语开头（F-012 家族再现；F-100 同形态"构建落位"第 2 例） | suggestion |
| F-122 | units/project.md 卡头日期行未随 v0.3/§15.4 同步（F-057 家族半同步残留） | suggestion |
| F-123 | CoreEdgePresent_DT_BUILD 敏感性缺口：守卫文本同名 token 遮蔽链接行移除（探针实证） | suggestion |
| F-124 | "冒烟模式无别名"表述失准（DTB §5.1:688 源头；core/diagnostics/project 注释沿袭第 3 例；验收者冒烟树以 `RWS::ird::core` 别名链接实证 exit 0） | suggestion |

## 3. 验收者环境说明

- 复现现场：`git worktree add --detach %LOCALAPPDATA%/Temp/acc-prjt01-a 5de89534…`（detached @SHA，非分支名）；记录回传：`git worktree add -b acc/PRJ-T01/1 %LOCALAPPDATA%/Temp/acc-prjt01-b origin/redesign-main`。
- 集成配置参数（F-062/F-098/F-099 口径）：`-G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=D:/10_Source_Repos/21_robot/RobWork/vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64 -DCMAKE_CXX_FLAGS="-DNOMINMAX -DWIN32_LEAN_AND_MEAN -DWIN32"`；`RobWorkStudio.ini.template.static/.shared.in` 自主树复制入 worktree（git 未跟踪本地资源——F-062 登记件）。
- 探针方法：运行期源码扫描用例改源码后直接重跑测试 exe（无重编译）；每轮还原并核对工作树干净。
- 本记录证据日志（构建/门禁/探针原始输出）留存于验收者临时证据目录；持久证据以本记录内联关键输出＋送验提交自带 15 件留痕（已逐一核对实际入库）为准。
- 独立性声明（ACC §2）：全新上下文；仅消费契约/设计文档/送验产物与留痕，未消费实施者叙述；构建与测试全部亲手重跑；对抗式立场下逐项取证。

## 4. 记录三元组（回传给编排者）

```
branch: acc/PRJ-T01/1
path:   RobWork/doc/industrial-robot-design/traceability/acceptance/PRJ-T01-20260915.md
commit: （见本分支推送后记录提交的 40 位 SHA——由验收会话随最终报告输出）
```
