# WP-14-T06 验收记录（attempt 1，2026-09-26）

| 字段 | 值 |
| --- | --- |
| 任务 | WP-14-T06（实现五种姿态规则解析与三维拾取/TCP 捕获域侧；单元 requirements） |
| 送验对象（冻结 SHA） | `392a6785c2344dafae1829b272b13554f15fedea`（origin/wp14-t06 == wp14-t06 == 该 SHA，零分支漂移） |
| base | `b1bdc8792be52a448a0cb7b6de4252b2a75f52a5`（state.json 双确认：base/headSha 字段逐字一致） |
| diff 范围 | `b1bdc879..392a6785`（2 提交：54bdceeb → 392a6785） |
| 验收环境 | detached worktree @ 392a6785（复现现场，D:/tmp/acc_t06_wtA）；集成全新构建树 D:/tmp/acc_t06_build；独立冒烟构建树 D:/tmp/acc_t06_smoke；base 侧临时 worktree @ b1bdc879（门禁比对后即删）——全部由验收者亲手执行，未采信实施者运行结果 |
| **verdict** | **pass**（4.1~4.11 全部通过；无阻断级问题；建议级 4 项 F-369~F-372 已转登 findings.json，不阻塞合入） |

---

## 4.1 改动范围 —— 通过

- `git diff --name-only b1bdc879..392a6785`：26 个变更文件，`grep -vE` 对照契约 `allowedFiles` 三前缀（`requirements/**`、`units/requirements.md`、`traceability/**`）**零越界**（无任何文件落在三前缀之外）——
  - `requirements/**` 12 个：CMakeLists.txt；新增四件 Capture.hpp/.cpp＋OrientationResolution.hpp/.cpp；修改四件 DiagCodes.hpp/.cpp＋Readiness.hpp/.cpp；测试三件新增 CaptureTest/OrientationResolutionTest＋修改 DiagCodesTest；
  - `units/requirements.md` 1 个；
  - `traceability/builds/wp14-t06/` 13 个留痕文件。
- `forbiddenFiles`（REQUIREMENTS.md/ARCHITECTURE.md）：`git diff --name-only <range> -- <两文件>` 空输出——零触碰。
- SA-02：`patches/` 零改动；`. ':!industrialrobot' ':!doc'` 全框架路径 diff 空；industrialrobot 内 requirements 之外零文件触碰。

## 4.2 可复现构建 —— 通过（两模式零错误，验收者全新构建树亲手复现）

- **集成模式**：worktree 缺 gitignored 模板（F-062/F-007 既有登记前置）——自主复制 `RobWorkStudio.ini.template.static`/`RobWorkStudio.ini.shared.in`/`bin/none/RobWorkStudio.ini.template` 后重新配置：
  `cmake -S <worktreeA>/RobWork -B D:/tmp/acc_t06_build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=<vcpkg>/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`
  → configure exit 0（80.4s）；`grep RWS_BUILD_INDUSTRIALROBOT CMakeCache.txt` → `:BOOL=ON`（验收树实测）。
  构建 `sdurws_ird_requirements`＋`sdurws_ird_requirements_test`＋`sdurws_ird_requirements_contract_test`（Release）→ **BUILD_EXIT=0，`error C*/LNK*/fatal error` 计数 0**（日志留存 D:/tmp/acc_t06_int_build.log）。本机无 RobWorkStudio.exe 进程锁干扰，未动用 taskkill。
- **独立冒烟模式**：`cmake -S <worktreeA>/…/industrialrobot -B D:/tmp/acc_t06_smoke -DCMAKE_TOOLCHAIN_FILE=<vcpkg>/vcpkg.cmake -DCMAKE_PREFIX_PATH=<Qt6>` → configure exit 0（与送验 build-smoke-t06-configure.log 同口径，同款 "standalone smoke mode" 横幅）；构建三目标 exit 0、零错误；两测试 exe 直接运行 **123/123＋5/5 全绿**。
- 两条构建树均为本次验收从零配置，未复用任何既有构建树。

## 4.3 契约 verify 命令 —— 通过（3/3 逐条亲手执行）

| # | 命令（契约原文） | 结果 |
| --- | --- | --- |
| 1 | `cmake --build build --config Release --target sdurws_ird_requirements` | 执行（验收树路径代入；按 F-368 家族在案口径随同补建两测试目标）→ exit 0，零错误 |
| 2 | `ctest --test-dir build/RobWorkStudio/src/rwslibs/industrialrobot/requirements -C Release -L "^ird$"` | 执行 → **2/2 passed（100%）**（sdurws_ird_requirements_test、sdurws_ird_requirements_contract_test） |
| 3 | `pwsh -File RobWork/scripts/industrialrobot/validate-docs.ps1` | 执行（worktreeA 根起跑）→ **PASS（20 units, 12 trace entries, 189 task files）**，exit 0 |

补充：README 宣称的 `validate-task.ps1 -TaskFile WP-14-T06.json` 亦由验收者复跑 → **PASS (1 tasks)**。契约 verify 数组缺两测试目标显式构建行的家族缺口转登 **F-371**（见文末）。

## 4.4 acceptance 条目逐项证据 —— 通过（5/5；具名证据均为验收者实跑产出）

| # | 契约 acceptance | 证据（验收者实测） |
| --- | --- | --- |
| 1 | 五规则解析成功＋resolution 留痕（V-03/AT-23） | `ReqOrientationResolution.FiveRulesResolveWithResolutionProvenance_WP14T06_ACC1`：Fixed（Z-Y-X 字面 rad 逐分量断言 0.1/0.2/0.3）/AlignFrame（targetObjectId==frameOid＋数值姿态 not_has_value——零求解隔离）/AlignGeometryNormal（目标＋feature＋invertNormal 留痕）/PointAtTarget（(3,0,4)→(0.6,0,0.8) 单位方向）/ToolRollFree（[−π,π]）五分支逐一断言；`FixedLiteralNotNormalized_WP14T06_ACC1`（φ 与 φ+2π 字面不等——NFR-COR-03）；`ResolutionIsDeterministic_WP14T06_ACC1`。实现＝resolveOrientationRule（OrientationResolution.cpp:90-183）固定短路序；两模式全绿实测 |
| 2 | 失败可定位＋浅校验边界＋非法参数拒绝 | `DanglingOrMismatchedTargetLocatableDiag_WP14T06_ACC2`（悬空＋token 失配两形态 REQ-READY-REF-MISSING，subject/localName==锚 id/名，context 三键 field/target/expected-token）；`IllegalParamsRejectedWithPoseIllegalDiag_WP14T06_ACC2`（零向量/rollRange 逆序/缺 feature/NaN 四反例＋REQ-READY-POSE-ILLEGAL＋field 键定位）；`ShallowBoundaryMetadataOnly_WP14T06_ACC2`（**结构性自证**：addRef 只填 ObjectRef 元数据四字段、全文件零 modeling 对象构造，成功解析与失配拒绝均成立）。全绿实测 |
| 3 | 几何特征拾取与 TCP 捕获域侧（L-R6/L-R7/REQ-10/AT-23） | `ConfirmedCaptureWritesFixedPointWithCapturedTag_WP14T06_ACC3`（Fixed＝捕获 rpy 字面＋六分量全约束＋source=UserProvided＋**methodTag=="captured-tcp"** 逐字断言＋草稿态 edits==1 零修订）；`StaleSessionEmitsWarningButWrites_WP14T06_ACC3`（session-revision="rev-0001" vs 草稿空基线→REQ-CAPTURE-STATE-STALE warning 恰 1 条、context 携两侧值对照、**accepted 照常写回**——保守知情语义）；`NotAppliedLeavesBaselineAndRevisionsUntouched_WP14T06_ACC3`（捕获→重载基线→工作集空＋dirty=false＋基线对象字节逐字节相等）；`ConfirmedFramePickUpdatesOrientationRule_ACC3`（AlignFrame 替换＋其余字段原样保留）＋`ConfirmedFeaturePickBuildsAlignGeometryNormal_WP14T06_ACC3`。全绿实测 |
| 4 | 三维编辑事务不绕过确认（REQ-08） | **接口面审查**：Capture.hpp 仅 `captureTcpAsFixedPoint`/`applyPickToOrientation` 两个写入口，均以 CaptureConfirmation 必填参数承接（无缺省放行形态）；服务仅持 IRequirementEditor，接口面零 project 写端口参数（未应用不失效的结构保证）；**编译期半边**：`WriteEntriesRequireConfirmationMemberByShape_WP14T06_ACC4` 双 static_assert（SFINAE 探测 confirmation 成员形状）；**运行期半边**：`UnconfirmedCaptureZeroDataChange_WP14T06_ACC4`（Rejected/Pending/凭据违约三形态全 rejectedUnconfirmed＋工作集/撤销栈/编辑计数零变化＋零诊断）＋`PickUnconfirmedOrDanglingZeroChangeWithDiag_ACC2_ACC4`。全绿实测 |
| 5 | 双模式构建零错误＋ctest 全绿＋留痕＋ird_gates | 见 4.2/4.3/4.10 与下方 ird_gates 专段——全部验收者实跑达成 |

**ird_gates base..head 归一化比对（编排者口径，验收者独立复算）**：引擎直跑 `cmake -DIRD_ROOT=<两侧各自源根> -P cmake/ird_gates.cmake`——head 侧 127 行、base 侧 127 行 IRD-GATE 记录（两侧 exit=1 均为 DTB §4.5 存量例外既有状态）；路径占位归一化后排序 diff **为空（IDENTICAL）——零新增门禁命中**。命中码直方图两侧逐码一致：SUB=63、R3=41、LIB=10、T1=5、R4=3、R1=3、T2=1、R5=1（合计 127）。与送验附件 `ird_gates_base_head_comparison.md` 的直方图与 IDENTICAL_HIT_SETS 结论逐格吻合（requirements 两测试目标的 SUB 直链 testkit 命中在 base 侧已存在——F-359 登记既有，非本任务引入）。

## 4.5 测试真实失败能力 —— 通过（3 处实质变异全部真变红，还原后复绿）

变异窗口＝worktreeA 源树＋冒烟构建树增量重建；每次变异→具名测试真实变红→`git checkout --` 还原→重建→最终全量 123/123＋5/5 复绿；还原先于全量复跑（T14 教训时序）；最终 worktreeA 源树 `git status --short RobWork/` 干净（零残留）。

| 变异 | 注入点 | 变红测试（实测输出） |
| --- | --- | --- |
| M1 解析留痕面失效 | OrientationResolution.cpp AlignFrame 分支注释掉 `out.targetObjectId = …` 留痕行 | `ReqOrientationResolution.FiveRulesResolveWithResolutionProvenance_WP14T06_ACC1` FAILED（filter 组内其余 5 例不受扰仍绿） |
| M2 确认门失效 | Capture.cpp L-R7 确认门 `if (confirmationGateFails(...))` → `if (false && …)` | `ReqCapture.UnconfirmedCaptureZeroDataChange_WP14T06_ACC4` FAILED（组内其余 7 例仍绿） |
| M3 STALE 对账判定反转 | Capture.cpp `sessionRevisionId != draftBaseline` → `==` | `ReqCapture.StaleSessionEmitsWarningButWrites_WP14T06_ACC3` FAILED（同 filter 另一例 ConfirmedCapture… 随之变红——错位/正常两形态同时失真，检出力充分） |

三次还原后全量复跑：123/123＋5/5 全绿（冒烟树实测输出留档）。测试非恒真、有真实检出力。

## 4.6 架构红线（AGENTS §5 八条）—— 通过

1. **L2 零 Qt**：T06 新增/修改六产品文件 grep `#include <Q|Q_OBJECT|QtCore|QtWidgets|QtGui` 零命中；新增 TU 零 Qt 链接（契约测试 ProductFaceHasZeroQtIncludes 全绿佐证）。
2. **业务域互链（R-1）**：新增产品文件跨单元 include 仅 `core/DiagData.hpp` 一条 §3.2 登记边；零 modeling/kinematics/trajectory/dynamics/selection/optimization include；浅校验只读 ObjectRef 的 objectId/objectTypeToken 两字段（closureRefViolation 实文），不解码 modeling 字节。
3. **跨单元私有头（R-2）**：core/DiagData.hpp 为 `include/sdurws/ird/core/` 公共头；余为 requirements 自有头。
4. **框架零修改（SA-02）**：见 4.1——patches/ 与框架零改动。
5. **名称前缀（R-4）**：T06 产品文件 grep RobWork 名称字面零命中；门禁 R4 命中 base/head 等量（3=3，既有）。
6. **权威唯一（PA-1）**：捕获写路径仅达编辑器草稿（IRequirementEditor——§4.6 编辑态所有者），零修订写入口；产码经 DiagCodes 单点常量（kReqCaptureStateStale）＋core DiagnosticRecord；单位/文案未私设第二真值。
7. **不可变历史（PA-2）**：服务接口面无任何 project 写端口/命令端口——捕获/拾取数据物理上不存在绕过草稿直达修订的通道；未应用零修订（NotApplied…ACC3 字节级断言）。
8. **不继承 old/ 实现**：T06 文件 grep `old/` 零命中；从零编写。

## 4.7 注释规范（AGENTS §2）—— 通过（1 项风格观察转登 F-372）

- **公共头全查**：`OrientationResolution.hpp`（172 行）与 `Capture.hpp`（332 行）文件头中文 Doxygen 完整——设计依据（卡章节/需求号/契约 acceptance 条目）＋"第一读者须知"背景段（解析两层语义/域侧三不变量）＋空基线口径决策段＋线程约束（仅 UI 线程）＋确定性声明；每个类型/函数/成员带 `@brief`/方向/单位（m/rad/无量纲）/坐标系语义（refFrame 系）/异常语义（confirmed 工厂空主体→std::invalid_argument fail-fast——调用方违约 vs 环境错误二分清晰）；resolution 字段有效性按 kind 分支表逐项登记。
- **实现文件抽查 ≥3**：`OrientationResolution.cpp`（185 行，文件头"实现要点"＋①②③逐步序注释＋IEEE754 确定运算说明）、`Capture.cpp`（410 行，流程序①~⑥逐步注释＋STALE 空基线决策标注＋产码唯一书写点）、`DiagCodes.cpp`/`Readiness.hpp`/`Readiness.cpp` 增量（逐码 §9.6 行号标注＋提升公共函数语义不变说明）——均达标。
- **观察项（建议级，转登 F-372）**：Capture.cpp L-R6 流内 ③/④ 序号导航注释含起草式自问自答句（"subject=目标任务点见④之后？不：……"，344-350 行）——内容真实准确（实际代码序 ②装配→④条目定位→③浅核对→⑤写入 已如实说明），但行文为草稿腔，建议后续触及该文件时改写为陈述式。
- **测试文件**：每用例中文说明对应 acceptance 编号（ACC1~ACC4）＋IRD_TEST_INFO 携带 REQ-08/09/10、AT-23、NFR-COR-01/03、ERR-01 追溯字段。

## 4.8 文档同步＋findings 消账核对 —— 通过（1 项家族再现转登 F-369）

- 单元卡 `units/requirements.md` §14.6 v0.6（2026-09-26）登记与实文逐项对查为真：§3.3 表 T06 两行（OrientationResolution/Capture）；§9.6 T06 行 `REQ-CAPTURE-STATE-STALE` 标〔已落位〕（行 780）；§12 ui 交接行落位标记同步（IRequirementCaptureService 两写入口＋CaptureConfirmation C-2 同构）；DiagCodes 工厂清单 12→13 表尾追加＋DiagCodesTest 分批封闭断言随附同步（kStagedSection96Codes 恰 13 项＋descriptors.size()==13 断言＋T06CaptureDescriptorMatchesSection96Row 逐字段）；**空基线口径实现决策有据且如实**（Editor.cpp:125 仅 clear、从不赋修订 id——draftStatus().baseRevisionId 恒空串；Capture.hpp 文件头/Capture.cpp④ 对应标注"保守知情：基线未标注即无法证明一致"）；**closureRefViolation 提升公共函数行为零变化属实**（diff 移出匿名命名空间，函数体与移除前逐字一致，仅可见性扩展）；**STALE 仅 L-R7 承载属实**（applyPickToOrientation 无对账步——§9.6 行语义钉在"TCP 捕获时"）；P-REQ-8 处置依据在卡面（§1.2 ui.md v1.5 基线消费＋ui/View3DContract.hpp 阶段 A 契约面实存——worktreeA ls 实证）。
- **偏差**：卡头「文档版本」行停留 **v0.5**（行 7）而 §14.6 末行已为 v0.6——F-123/F-133 家族（卡头版本行失同步，少报方向）在 requirements.md 再现，转登 **F-369**；登记内容本身完整准确。
- 追踪矩阵/状态：validate-docs PASS（12 trace entries）；任务状态翻转归治理收尾（PIPE 口径），本次不涉及。
- **findings.json 未关闭条目相关性核对**（以 worktreeA 检出版为基准，在库最大号 F-368）：与本任务直接相关者——F-367/F-368（契约 verify 缺测试目标构建行家族，本任务契约同型缺口→F-371 转登）；F-363（"IRDREQO" 魔数钉子测试，所有者指定"T04+ 顺手落"——本任务未触 CodecTest，未消账亦无新增积压责任，仍在案）；F-359（requirements 测试目标 SUB 直链既有命中——本次比对确认零变化）；F-361/F-364/F-366（标题格式家族——本任务两提交均 `[WP-14-T06]`＋动词开头，无再现）；F-041/F-360（散文计数家族——本次发现一例新实例，见 F-370）；F-007/F-009/F-062（worktree 环境前置——本次复现属实，既有条目已覆盖，不重复登记）。无静默积压。

## 4.9 commit 质量 —— 通过（1 项计数失准转登 F-370，无粉饰）

- 54bdceeb `[WP-14-T06] 实现五种姿态规则解析与三维拾取/TCP 捕获域侧`／392a6785 `[WP-14-T06] 附验证留痕（gtest XML/报告/双模式构建日志/门禁比对）`——两提交均 `[任务编号]` 前缀＋动词开头（F-361/F-364/F-366 家族零再现），正文 What/Why/验证/影响面四段齐备＋Acc-Covered 对账行。
- 防粉饰核对：123/123＋5/5、ctest 2/2、validate PASS、门禁 127=127 零新增、留痕"README＋12 件"清点（实 13 文件）——与验收者独立实测逐项一致；唯一失准＝提交正文与留痕 README 称 OrientationResolutionTest「5 用例」，实测/报告/送验 XML 均为 **6 用例**（5+8+1 与总数 123 的算术亦不自洽，108+6+8+1=123 才闭合）——F-360 家族新实例（少报方向、总数与全部核心结论准确），转登 **F-370**，不构成粉饰。

## 4.10 留痕完整 —— 通过

`traceability/builds/wp14-t06/` 13 件全部入库且计数与验收者独立复现一致：两 gtest XML（tests=123/failures=0、tests=5/failures=0——与本次实跑同值；ReqOrientationResolution tests="6"、ReqCapture tests="8"）、ird-test-report 两份（total=123/passed=123、total=5/passed=5，用例级明细含 6＋8 实数）、集成全量构建日志、冒烟 configure/构建日志（exit=0）、门禁 base/head 原始输出各 127 行＋归一化命中集各 127 行＋比对结论（直方图与验收者复算逐码一致）、README（verify 三命令逐条执行记录）。gtest XML 原样留存、无手工转抄痕迹（F-346/348 家族无再现）；日志为干净 UTF-8（F-2093 编码家族无再现）。任务状态翻转/resultNote 归治理收尾，与 T05 同口径。

## 4.11 偷懒/缩水实现扫描 —— 通过

- ① diff 新增行 `TODO|FIXME|stub|placeholder|未实现|not implemented` 扫描**零命中**（grep exit 1）；无"触发式延后"豁免需求（T07 派生族两码不预建系 DiagCodes.hpp 头注分批纪律既定口径，非偷懒）。
- ② 空实现检测：resolveOrientationRule（94 行实质）、captureTcpAsFixedPoint（确认门→参数面→引用浅核对→STALE→构造→写入六步全实质）、applyPickToOrientation（五步全实质）、confirmed/rejected 工厂与 wellFormed（实质不变量核对）——无仅返回默认值的桩。
- ③ 逐条证据表：见 4.4——acceptance 5 条逐条注明具名测试/命令输出/实文行号，零缺证。
- ④ 测试强度：断言校验行为与边界（逐字段产出断言、三态互斥、零数据变更全等断言、字节级基线比对、context 键面逐键、编译期形状钉）；4.5 三处变异实证测试会红，非恒真。

---

## 发现问题清单

**阻断级**：无。

**建议级（已转登 traceability/findings.json）**：

- **F-369**：F-123/F-133 家族在 units/requirements.md 再现——§14.6 已增 v0.6 行而卡头「文档版本」行停留 v0.5（少报方向；v0.6 登记内容完整准确；validate-docs 不校验该维度）。
- **F-370**：F-041/F-360 散文计数家族新例——提交 54bdceeb 正文与留痕 README 称 OrientationResolutionTest「5 用例」，实测/送验 XML/报告 JSON 均为 6 用例（总数 123 准确；108+6+8+1=123 才自洽）；少报方向、无结论性失实。
- **F-371**：F-300/…/F-368 家族新例——tasks/foundation/WP-14-T06.json verify 数组仅 3 行（库名构建＋ctest＋validate-docs），缺 sdurws_ird_requirements_test／sdurws_ird_requirements_contract_test 显式构建行；验收者已按家族在案口径自行补建（123/123＋5/5），建议契约模板统一补构建行（与 F-367/F-368 合并落地面）。
- **F-372**：Capture.cpp L-R6 流内序号导航注释（344-350 行）为起草式自问自答行文（内容真实准确），建议后续触及该文件时改写为陈述式，避免首次阅读者误读执行序。

## 裁决

**verdict＝pass**。4.1~4.11 全部通过、无阻断级问题；建议级 4 项（F-369/F-370/F-371/F-372）已转登 findings.json，不阻塞合入。合入决策归所有者（本验收者不合入、不改实施产物、未向任务分支 wp14-t06 推送任何提交；送验对象冻结于 392a6785）。

## 验收者环境说明

- 复现现场：`git worktree add --detach` @ 392a6785（base b1bdc879）；送验前核验 origin/wp14-t06 == wp14-t06 == 392a6785（零漂移）；集成/冒烟构建树均为本次新建；base 侧临时 worktree（门禁比对用，含同款 ini 模板补齐）比对后即删；记录经 acc/WP-14-T06/1 分支推送回传。
- 环境前置（F-062/F-007 既有登记事实）：worktree 检出后自主复制 RobWorkStudio.ini 三件模板；配置参数含 vcpkg toolchain＋Qt6 前缀（主树缓存同款 D:/software/Qt/6.11.1/msvc2022_64）。
- 本机无 RobWorkStudio.exe 进程锁干扰，全程未动用 taskkill；ird_gates 以引擎直跑（cmake -P）口径比对，VS 目标退出码 1 未采信。
- 本机代理不稳定风险按编排者口径以显式 refspec `git push origin HEAD:refs/heads/acc/WP-14-T06/1` 推送并 `git ls-remote` 核验 SHA 一致。
