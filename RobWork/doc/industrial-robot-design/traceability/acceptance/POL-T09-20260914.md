# POL-T09 验收记录（attempt 2）

| 字段 | 值 |
| --- | --- |
| 任务 | POL-T09 实现策略兼容判定（Compatibility）（unit=policy，masterWp=WP-07-T09） |
| 送验对象（冻结） | `04f3e762f749a7f5decc4789567f644222746776`（分支 wp07-t09；`origin/wp07-t09` 经 rev-parse 核对＝该 SHA——无分支漂移） |
| diff 范围 | `53f9f04c..04f3e762`（state.base 为 idle 快照 null，取 merge-base(04f3e762, origin/redesign-main)＝53f9f04c 复核——head 恰为 origin/redesign-main 尖端的直接子提交，单提交送验） |
| 验收协议 | acceptance-protocol.md v1.5（ACC），§4 全部 11 项逐项出证据 |
| 裁决 | **pass**（4.1~4.11 全部通过；无阻断级问题；建议级 1 条已转登 `traceability/findings.json` F-087，不阻塞合入） |
| 验收环境 | detached worktree `D:\10_Source_Repos\21_robot\acc-worktrees\pol-t09-a` @ 04f3e762（送验 SHA）；集成模式全新配置：`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=<仓库根>/vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`（F-062 登记的 worktree 复现前置——toolchain/Qt 前缀＋两件 ini 本地资源从主树复制），配置 exit 0、缓存 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`（grep 实证）；冒烟另配 `D:\tmp-acc-pol-t09\build-smoke`（带 toolchain）。全部构建/测试/门禁/探针由验收者亲手执行，未采信实施者运行结果（ACC §2） |
| 记录分支 | `acc/POL-T09/2`（attempt=2，验收请求下发序号） |

---

## 4.1 改动范围 — PASS

`git diff --name-status 53f9f04c..04f3e762` 共 8 个变更文件，逐一对照契约 `allowedFiles`：

| 文件 | 状态 | 归属 |
| --- | --- | --- |
| `RobWork/RobWorkStudio/src/rwslibs/industrialrobot/policy/CMakeLists.txt` | M | `policy/**` ✓ |
| `policy/include/sdurws/ird/policy/Compatibility.hpp` | A | `policy/**` ✓ |
| `policy/src/Compatibility.cpp` | A | `policy/**` ✓ |
| `policy/test/CompatibilityTest.cpp` | A | `policy/**` ✓ |
| `traceability/builds/wp07-t09/ird-test-report.json` | A | `traceability/**` ✓ |
| `traceability/builds/wp07-t09/sdurws_ird_policy_test-integration.xml` | A | `traceability/**` ✓ |
| `traceability/builds/wp07-t09/sdurws_ird_policy_test-smoke.xml` | A | `traceability/**` ✓ |
| `doc/industrial-robot-design/units/policy.md` | M | allowedFiles 显式列出 ✓（仅 §15.4 表尾 +2 行＝v0.10 变更记录） |

- `forbiddenFiles`（REQUIREMENTS.md / ARCHITECTURE.md）：diff 中零触碰 ✓。
- 框架源码（RobWork/、RobWorkSim/、RobWorkStudio 其余）：diff 范围零文件；`patches/` 目录 diff 为空（无新登记，SA-02 无涉）✓。

## 4.2 可复现构建 — PASS

- **集成模式（worktree A 全新树）**：配置 exit 0（43.9 s），`grep RWS_BUILD_INDUSTRIALROBOT build/CMakeCache.txt` → `:BOOL=ON` 实证。日志 `D:\tmp-acc-pol-t09\configure-integration.log`。
  - 全树 `cmake --build build --config Release` exit 1＝**73 处 error C 全部位于 RobWorkSim 侧插件**（RWSimulatorPlugin.vcxproj＋bt_plugin.rwplugin.vcxproj；出错头文件聚集 VelocityScrew6D.hpp×63／Math.hpp×8／Eigen Parallelizer.h×2——即 findings **F-008** 登记的框架基线 min/max 宏污染簇，基线 959fb9b 对照先例同型），**ird 面零错误**（73 行 error 文本中 `industrialrobot`/`ird` 计数均为 0）。按 F-036/F-054 家族既定处置口径以**任务目标面从零构建**补强（日志 `integration-full-build.log`）。
  - 契约 verify 目标 `cmake --build build --config Release --target sdurws_ird_policy_test` **从零编译零错误 exit 0**（产物 `build/RobWorkStudio/bin/Release/sdurws_ird_policy_test.exe`；日志 `verify-target-build.log`）。
- **冒烟模式**：`cmake -S .../industrialrobot -B D:\tmp-acc-pol-t09\build-smoke -DCMAKE_TOOLCHAIN_FILE=<vcpkg>` 配置＋`cmake --build ... --config Release` 均 exit 0（日志 `smoke-configure.log`／`smoke-build.log`）。
- **测试（验收者亲手运行）**：
  - 集成 `sdurws_ird_policy_test`：**181/181 PASSED**（`[==========] 181 tests from 34 test suites ran`；复跑 XML 顶格 `tests="181" failures="0"`，181 个 testcase 清点一致）——与送验主张 181/181（166 既有＋15 新增）一致。
  - 冒烟 `sdurws_ird_policy_test`：**112/112 PASSED**（XML 顶格 `tests="112" failures="0"`）——新增 TU 仅集成编译、冒烟测试面不变的主张成立。
  - 冒烟全量六目标：core 61＋evidence 205＋evidence_contract 14＋policy 112＋runtime 176＋testkit 97＝**665/665 零失败**——与送验"冒烟全量 665/665 零回归"主张逐目标一致。
- **门禁 `ird_gates`**（验收者亲手跑 `cmake --build build --config Release --target ird_gates`）：exit 1＝**5 处命中**——SUB×3（evidence_test／evidence_contract_test／runtime_test → testkit）＋SUB×1（testdata_lint→testkit 表外依赖边）＋T1×1（testdata_lint 产品目标链接 testkit），即 **F-019×2＋F-051×1＋F-072×2 既有基线**；零 Compatibility/policy 新增条目、IRD-GATE-R4 零触发；门禁自测（fail_r1/t1/r5/sub/r3/r4/t2 按预期检出、pass_clean/pass_r4comment 干净通过）全数符合——与送验"5 命中＝base 既有（SUB×4＋T1×1）、GATE_DELTA_ZERO、R4 零触发"主张一致（日志 `acc-gates-run.log`）。

## 4.3 契约 verify 命令 — PASS

契约 `verify` 数组仅 1 条：`cmake --build build --config Release --target sdurws_ird_policy_test`——验收者在 worktree A 全新树亲手执行，exit 0（见 4.2）。无其他条目漏执行。

## 4.4 acceptance 条目逐项核对 — PASS（2/2 有证据）

| # | 契约条目 | 证据（具名测试/输出） |
| --- | --- | --- |
| 1 | POL-COMPAT-1/2 版本/后端/锚失配全量 reason 用例通过 | 词表全表 `CompatibilityReasonTokens.ReasonTokenTableExactSeven`（七值 kebab token 逐一精确，含防御臂 policy-invalid）；版本臂 `SchemaFutureOnUpperBoundExceeded`／`SchemaObsoleteOnLowerBoundUnderrun`（比较型三要素 Provided(实际/期望)＋无量纲单位 "1" 逐字段断言）、`SchemaBoundariesClosedIntervalAccept`（[1,1] 端点与 [0,2] 覆盖双正例）；碰撞能力臂 `CollisionRequiredDisabledRejected`（双态正例＋无比较型三要素负断言）；后端臂 `BackendMismatchOnAnyFieldDifference`（id/version/tolerance 三字段分别变异各产一因、cause 逐字双侧原文）＋`BackendExactDescriptorOrAbsentAccepted`（精确命中/nullopt 缺省零误报）；锚臂 `AnchorMismatchOnAppendixDRevision`（v1.17×v1.16 失配＋精确命中正例）；模式臂 `ModeNotApplicableOutsideScope`（清单外失配＋清单内/空集双正例）；`PolCompat2.ContentIdentityChangeSuppliesSliceMaterial`（内容身份素材面＋不越权代 evidence 行使判定权）；全量收集 `AllMismatchedReasonsCollectedInEnumOrder`（五臂同失配按枚举序全量、每因一诊下标对齐）——集成 181/181 内全部执行通过 |
| 2 | O-20/P-POL-5 保守口径：后端版本串暂取 RobWork 构建版本（policy.md §15.3 P-POL-5）——NFR-DEP-05 基线冻结后由 WP-24-T01 锁定回填，本契约不私定最终取值 | `O20Ppol5.BackendVersionConservativeStance`：要求侧携带唯一装配线真值（`makeRobWorkCollisionEvaluator().backend()`）→ Compatible（判定内部推导与装配值逐字段相等＝同宏同字面量单点、无第二取值源的机械证明）；仅版本串变异 → backend-mismatch；`backendId=="rw.proximity.builtin-rw"` 冻结断言＋`backendVersion` 非空构建期口径。实现核对：`src/Compatibility.cpp:93` `detail::makeBuiltinBackendDescriptor(RW_VERSION)` 单点注入，无硬编码最终取值——"本契约不私定最终取值"条款如实执行（消账归版本基线所有者，报告 acceptanceCoverage.2 原样保留） |

无任何条目虚标通过；ird_gates 结论如实登记为"5 命中＝既有"（非零命中粉饰为通过）。

## 4.5 测试真实失败能力 — PASS（双探针）

对实现文件 `src/Compatibility.cpp` 注入两处 mutation（改后重建目标→跑用例→`git checkout` 还原→重建复跑）：

1. 探针 1：schema-future 臂比较 `policy.schemaVersion > requirements.maxSchemaVersion` 改 `>=` → **9 用例 FAILED**（`DefaultRequirementsAcceptPublishedPolicy`／`SchemaBoundariesClosedIntervalAccept`／`CollisionRequiredButDisabledRejected`／`BackendMismatchOnAnyFieldDifference`／`BackendExactDescriptorOrAbsentAccepted`／`AnchorMismatchOnAppendixDRevision`／`ModeNotApplicableOutsideScope`／`ContentIdentityChangeSuppliesSliceMaterial`／`O20Ppol5.BackendVersionConservativeStance`）——闭区间边界语义（策略代＝上限即兼容）被真实验证：边界翻红、越界用例（[0,0] 窗）仍绿，方向正确。
2. 探针 2：numeric-contract 臂 `!=` 改 `==` → **3 用例 FAILED**（`AnchorMismatchOnAppendixDRevision`／`ContentIdentityChangeSuppliesSliceMaterial`／`AllMismatchedReasonsCollectedInEnumOrder`）——恰为消费锚比较的三个用例，失配/命中两侧语义均被真实验证。

还原后 `git status --porcelain` 零残留，重建复跑 **181/181 复绿**。断言非恒真、非"只编译通过"（F-075 纪律：mutation 落在 .cpp，消费 TU 随重编，无头文件增量陷阱）。

## 4.6 架构红线（AGENTS §5 八条） — PASS

1. **L2 零 Qt**：三个新文件 grep `Qt|Q_OBJECT|QWidget` 零命中 ✓。
2. **R-1 业务域互链**：include 面仅 `ird/core/*`、`ird/policy/*`（本单元）＋std/gtest＋框架配置头 `RobWorkConfig.hpp`（policy→RobWork 基线为 §3 已登记 L1 编译链接边）；七个业务域命名空间 grep 零命中 ✓。
3. **R-2 跨单元私有头**：include 均为 `include/sdurws/ird/<unit>/` 公共路径 ✓。
4. **SA-02 框架零修改**：4.1 已证 ✓。
5. **R-4 名称拼接/剥离**：`src/Compatibility.cpp`/`CompatibilityTest.cpp` 双引号对携带 "RobWork" 子串的字面量 grep 零命中；版本宏 RW_VERSION 为纯版本串、尖括号包含不在扫描面；ird_gates R4 实测零触发 ✓。
6. **权威唯一（PA-1）**：判定单点在 `checkPolicyCompatibility` 自由函数，`IPolicyCompatibilityChecker` 契约注释明文"实现须委托自由函数（单一判定权威）"且有 `CheckerInterfaceDelegatesToFreeFunction` 用例钉住；不按内容身份拒复用（缓存判定权归 evidence——`ContentIdentityChange...` 用例断言不越权）；诊断码为 Compatibility.cpp 单点建议码字面量＋§15.4 v0.10 ⑥ 补登＋POL-T10 迁码表通道（码值权威归 diagnostics）✓。
7. **不可变历史（PA-2）**：本任务无修订写路径涉足，判定 TU 无任何删除/改写通道（CON-02 注释与实现一致）✓。
8. **不恢复 old/**：无 old/ 引用 ✓。
- 备注：src/test TU 集成模式专属 gating（框架配置头冒烟不可解析）与 RobWorkCollisionEvaluator.cpp/JointLimits.cpp 同因，CMakeLists 注释逐条登记（§15.4 v0.10 ⑤）；头内类型面零框架依赖，冒烟 112 用例不变实测成立——为单元卡原文口径的执行，不构成红线违反。

## 4.7 注释规范（AGENTS §2） — PASS

- **公共头全查**（Compatibility.hpp，323 行）：文件头（设计依据 §8.1/§8.2/§9.5＋"本判定在产品中的角色"背景＋实现口径 C-1~C-4 登记＋线程安全/确定性声明）、五实体逐一定义处中文 Doxygen 齐备（枚举七值各带语义与触发条件）；§2.5 高危信息逐项在位——物理单位（schema 代无量纲、单位 "1" 占位说明）、坐标系/角度制式（本判定无数值几何面，不适用处如实不标）、所有权（reasonToken 返回静态存储期"调用方无需释放"）、线程约束（每类型标注"线程安全：纯值/纯函数/无状态"）、错误语义（判定不抛＝查询非违约 vs 发布门 fail-fast 的分类、防御臂不可达性登记）、确定性来源（检查序＝枚举声明序、一经交付只许表尾追加、token 持久化不改名）✓。
- **实现文件**（Compatibility.cpp，372 行）：文件头（含 TU gating 说明与 R-4 门禁自律动机）；七臂逐步注释（各臂触发条件＋业务场景＋比较型口径）；魔法数解释（%.17g round-trip、"1" 无量纲 token 来源、诊断字段各要素归属）✓。
- **测试文件**（CompatibilityTest.cpp，640 行）：文件头含用例追溯命名约定与四项范围声明（集成专属/防御臂不可达/不链接 testkit/替身边界）；每个 TEST 注明验证的条款（§8.x/§11/acceptance 编号）；断言处行内中文注释（EXPECT_EQ 后 << 说明）✓。
- 本任务新增文件共 3 个，全部通读核查（超出"实现文件抽查 ≥3 个"的覆盖面）。

## 4.8 文档同步＋findings 核对 — PASS

- 实现与任务卡**零语义偏差**；实现增量（检查序＝枚举序全量不短路、比较型三要素数值/字符串两分口径、policy-invalid 防御臂类型层不可达、backend 核对对象＝唯一装配线同源、码面单点、POL-COMPAT-2 本侧面）已按 DTB §5.4 以 **units/policy.md §15.4 v0.10（2026-09-14）** 增量修订登记（七项细节，diff 实证 +2 行）——"代码先行、文档失实"不存在。
- 卡行对照：§12 POL-T09 行（产物 `Compatibility.hpp/.cpp`＝checkPolicyCompatibility、验证 POL-COMPAT-1/2、完成条件"版本/后端/锚失配全量 reason 用例通过"、依赖 POL-T04）与交付一致；§11 POL-COMPAT-1/2 用例行逐项有对应用例；§15.3 P-POL-5 开放项处置（暂取构建版本、不私定最终取值、WP-24-T01 回填）与契约 acceptance 2 原文一致。
- `validate-task.ps1 -TaskFile POL-T09.json` 验收者亲手复跑（worktree A）：**PASS (1 tasks)**；`phase-one-validation.log:35` 含 `POL-T09.json: validate-task: PASS (1 tasks)` 条目（DOC-T08 契约批次既有行），本次送验的执行级留痕以新增 `traceability/builds/wp07-t09/ird-test-report.json` 承载 ✓。
- **findings.json 未关闭条目相关性核对**（v1.2 要求，基于 origin/redesign-main@53f9f04c 版本共 86 项）：与 policy 链/本验收直接相关者——F-075（探针头文件注入陷阱：本验收双探针均落 .cpp 并重建消费目标，按其处置执行）；F-076/F-079/F-082/F-084/F-086（提交标题名词短语家族：本任务单提交标题"实现…"动词开头，家族未再踩）；F-077/F-080/F-081/F-083/F-085（留痕清单/入库出入家族：**本任务复踩该家族**——ird-test-report.json 引用 9 件未入库 .log，见问题清单 G-1→F-087）；F-078（policy.md 转义损毁第四例：本任务 diff 仅 +2 行于 §15.4，未触碰损毁节，维持 DOC 治理归口不顺手消账）；F-004/F-007/F-008/F-062（构建环境/框架基线家族：本验收环境前置即按 F-062 口径执行、全树构建实测仍止于 F-008——均维持原归口）；F-019/F-051/F-072（ird_gates 既有命中：本验收实测与三者登记逐一吻合）。**无历史发现被静默积压，亦无本任务可顺手消账项。**

## 4.9 commit 质量 — PASS

单提交 `04f3e762`：

- 标题 `[WP-07-T09] 实现 policy 单元策略兼容判定（Compatibility）并同步文档留痕`——`[WP-07-T09]` 前缀＋动词"实现"开头，合规（未踩 F-012 家族）。
- 正文四段（改动内容／改动原因／验证／影响面与兼容性）齐备；`Acc-Covered: 1,2` 与契约 acceptance 数组规模一致。
- 验证结论与留痕核对：集成 181/181（166＋15）、冒烟 112/112、冒烟全量 665/665（六目标分解逐项一致）、ird_gates 5 命中＝base 既有（SUB×4＋T1×1）、validate-task PASS、契约 verify 目标通过——**全部经验收者亲手复现准确，无粉饰**；探针自证（短路注入→变红→逐字恢复复绿）与验收者双探针同型。
- 影响面登记属实："仅新增文件＋policy/CMakeLists.txt 增列＋policy.md §15.4 追加行、零改动既有产品文件"与 4.1 diff 清单一致。

## 4.10 留痕完整 — PASS（附建议级 G-1）

- 在库证据本体：`traceability/builds/wp07-t09/` 双模式 gtest XML（integration `tests="181" failures="0"`／smoke `tests="112" failures="0"`，顶格实测）＋ird-test-report.json（两目标 notes／testSuites 15 用例逐条 trace／gates／builds／testRuns／validateTask／acceptanceCoverage 全节，数值主张全部经验收者亲手复现准确）。
- resultNote 口径：ird-test-report.json 即本任务执行级结果登记（ACC 4.10"phase-one-validation.log 或 resultNote 已更新"满足）；validate-task 面由库内 `phase-one-validation.log:35` 既有 PASS 条目覆盖（验收者复跑 PASS 一致）。
- **G-1（建议级）**：ird-test-report.json 的 `gates.log`／`builds.integration.logs`（integration-build.log、verify-target-build.log）／`builds.smoke.logs`（smoke-configure/build/test-run/test-run-all.log）／`testRuns.*.log`／`validateTask.log` 字段引用的 **9 件 .log 文件均未入库**（`git ls-tree 04f3e762` 实证 wp07-t09/ 仅 3 件；`git check-ignore -v` 实证命中根 `.gitignore:87` `*.log` 规则）——F-010 根因**第五次踩中**（F-081 第二次、F-083 第三次、F-085 第四次之后），"报告字段引用在库文件而实际未入库"方向（F-083/F-085 同型；本次提交正文未逐件列举 .log 文件名，虚报面较 F-085 为窄，但字段悬空引用形态相同）。判级沿 F-081/F-083/F-085 先例：证据本体（双模式 XML＋report）完整且全量复现，建议级不阻断→转登 **F-087**。

## 4.11 偷懒/缩水实现扫描 — PASS

1. **标记扫描**：三个新文件 `TODO|FIXME|stub|placeholder|未实现|not implemented`（忽略大小写）grep 零命中（exit 1 实证）。
2. **空实现检测**：公共接口均有完整实现——`reasonToken`（全枚举 switch 表）、`checkPolicyCompatibility`（七臂 372 行全逻辑、无空函数体/仅返回默认值的桩）；`IPolicyCompatibilityChecker` 为 §8.1 冻结的注入形态接口（纯虚＝设计本体，非缩水；测试文件头有"接口不是第二判定权威"的委托契约声明）。
3. **acceptance 逐条证据表**：见 4.4——两条均有具名用例＋验收者实测，任一不缺。
4. **测试强度**：断言校验行为与边界（闭区间端点兼容/越界即拒、三字段变异逐一失配、五臂按序全量收集＋每因一诊下标对齐、字符串臂 cause 逐字双侧原文、诊断逐字节相等确定性、接口委托落在非平凡 Incompatible 结果上）；4.5 双探针实证非恒真。

---

## 问题清单

### 阻断级

无。

### 建议级（已强制转登 traceability/findings.json）

- **G-1→F-087**：F-010 根因第五次踩中——ird-test-report.json 引用 9 件 .log 留痕（gate-all/integration-build/verify-target-build/integration-test-run/smoke-configure/smoke-build/smoke-test-run/smoke-test-run-all/validate-task）被 `.gitignore:87` `*.log` 规则吞掉未入库，字段悬空引用（F-083/F-085 同型；证据本体完整、数值主张全部复现准确，建议级不阻断）。已在记录分支一并登记 `traceability/findings.json` F-087 条目（含三选一处置建议：gitignore 豁免＋补提交／接受 XML＋报告承载口径／留痕清单对账自查强制化——五次复踩后建议所有者提级裁决根因修复）。

### 验收者环境说明

- 集成/冒烟构建、181/112/665 测试、ird_gates、validate-task、双探针 mutation 全部由验收者在 detached worktree @ 04f3e762 亲手执行；实施者运行结果仅作主张比对基准，未作证据采信（ACC §2.4）。
- F-062 环境前置（vcpkg toolchain＋Qt 前缀＋两件 RobWorkStudio.ini 本地资源复制）为 worktree 集成配置的必要输入，全程未省略。
- 探针 mutation 仅存在于临时 worktree 的未提交工作区，已 `git checkout` 还原并经 `git status --porcelain` 零残留核验；送验冻结对象（git 对象库中的 04f3e762）全程未被修改，任务分支未接收任何验收侧提交。
