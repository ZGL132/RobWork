# RT-T11 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 ID | RT-T11（实现编译器集成与取消——十段链/事务；WP-06-T11，unit=runtime） |
| 契约 | tasks/foundation/RT-T11.json（status=ready，branch=wp06-t11，verify＝`cmake --build build --config Release --target sdurws_ird_runtime_test`；acceptance 4 条；knownPitfalls P-RT-3；interUnit=true） |
| 送验对象（冻结） | `753f4460de7ee990a7dc6bf75a9bb6a9cfe8fd8f`（分支 wp06-t11 尖端；base=`dfd285803d3b73b95ad5a7e4d2afb0f19c94feba`＝origin/redesign-main 尖端＝RT-T10 合入收尾提交；`git merge-base` 实证两者即 diff 范围） |
| 分支漂移检测 | `git fetch origin redesign-main` 后 `git rev-parse origin/wp06-t11`＝`753f4460de7ee990a7dc6bf75a9bb6a9cfe8fd8f`（与送验 SHA 一致——ACC §3 冻结口径满足，无漂移） |
| **verdict** | **pass**（4.1～4.11 全部通过，无阻断级问题；建议级 2 项：新发现 1 条已转登 findings.json F-048，F-046 维持 open 并注明本次未顺手消账） |
| 独立性声明 | 验收者为全新上下文会话（PIPE §0.4 子代理派发模式），仅消费任务契约、设计文档、送验提交产物与留痕文件，未读取实施者任何过程叙述；构建、测试、探针、门禁全部由验收者亲手在独立 worktree 重跑，复现优先于采信（ACC §2 四条逐条满足，无需降级声明）。 |
| 验收方式 | 独立 worktree（detached @ 送验 SHA）亲手重配重跑双模式构建、契约 verify 命令、集成/冒烟测试、ird_gates 门禁与治理脚本；静态审查＝逐行通读全部新增/大改源码文件（CompilerImpl.hpp/.cpp、SnapshotAssembler.hpp、CompilerTest.cpp 全文＋三个公共头 diff）并对照 units/runtime.md §5.1～§5.7/§8.6 原文；动态探针＝2 处缺陷注入（禁用 S10 前资源复查、删除 S8 前取消轮询——与实施者探针同点异法/异点）→精确还原→全量复绿 244/244、git 工作树还原干净。 |
| 验收环境 | Windows 10 x64 / Git Bash / VS2022（Visual Studio 17 2022 生成器 x64）/ Qt 6.11.1（CMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64）/ vcpkg 经典模式（仓库根，toolchain 绝对路径 D:/10_Source_Repos/21_robot/RobWork/vcpkg/scripts/buildsystems/vcpkg.cmake）/ pwsh 7（治理脚本）。复现现场＝`D:\10_Source_Repos\21_robot\RobWork\tmp-acc-rt-t11-a`（detached worktree @ 753f4460，集成树 build/ 与冒烟树 tmp-smoke-build/ 均全新从零配置，验收日志在其 acc-logs/ 下，验收后随 worktree 删除——本记录为准一证据载体）；记录回传＝`D:\10_Source_Repos\21_robot\RobWork\tmp-acc-rt-t11-b`（分支 acc/RT-T11/1，基于 origin/redesign-main）。 |
| 环境前置说明（复现者须知） | ①全新集成树配置需主仓库同款参数（toolchain＋Qt 前缀，F-007 既有），且 `RobWork/RobWorkStudio/bin/RobWorkStudio.ini.template.static` 被 .gitignore 吞掉须从主仓库手工复制（目标目录须先 mkdir）——F-007 再次实证，非本任务引入、不阻塞；本次首次配置因此失败一次（configure.log:167/235），补齐后通过。②从零集成树全量构建在 F-008 基线缺陷处失败（73 处 error 全部 RWSimulatorPlugin.vcxproj／RobWorkSim 域 Math.hpp 宏污染簇，`grep industrialrobot` 零命中）——与 F-036/F-038 判例同型，非本任务引入；任务目标面从零构建（同契约 verify 命令）零错误。③手工运行测试 exe 需把 build/RobWork/bin/Release、build/RobWorkSim/bin/Release、build/RobWorkStudio/bin/Release 加入 PATH（rw/rwsim/gtest DLL），否则 exit 127——环境链备注。 |

---

## 4.1 改动范围 —— 通过

- base..head 共 3 个提交：`12bee611`（实现十段编译链产品集成）、`8bb71f8c`（登记验证留痕）、`753f4460`（runtime.md v0.12 登记）；`git diff --name-status dfd2858..753f4460` 共 22 个文件、+4702/−87：
  - `runtime/**`（allowedFiles 第 1 条）：CMakeLists.txt（M）、include/sdurws/ird/runtime/Compiler.hpp（M，+62）、Description.hpp（M，+29）、Sources.hpp（M，+12）、src/CompilerImpl.cpp（A，812 行）、src/CompilerImpl.hpp（A，227 行）、src/Snapshot.cpp（M，±165）、src/SnapshotAssembler.hpp（A，155 行）、test/CompilerTest.cpp（A，1103 行）——9/9；
  - `doc/industrial-robot-design/traceability/builds/wp06-t11/**`（allowedFiles 第 4 条）：留痕 12 件（A）；
  - `doc/industrial-robot-design/units/runtime.md`（allowedFiles 第 3 条，M，+20/−若干＝卡头版本行＋§3.1/§4.2/§10.0 代码块同步＋§15.4 v0.12 条目）。
- forbiddenFiles（REQUIREMENTS.md / ARCHITECTURE.md）：diff 中零出现。
- SA-02：diff 无任何框架源码文件；`industrialrobot/patches/` 无新文件。
- 三个公共头增量均为"只增不改"（CompileStage 枚举新设＋CompileRequest.project＋四子结构 objectId＋kRobotDesignObjectType 常量），逐一在 units/runtime.md §15.4 v0.12 与文件头注释双向登记（4.8 项核对）。

## 4.2 可复现构建 —— 通过

- **集成模式**（worktree A 全新配置＋全新 build/ 从零）：`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=<vcpkg> -DCMAKE_PREFIX_PATH=<Qt6>` → `Configuring done (6.5s)`；缓存确认 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`（grep build/CMakeCache.txt 实证）。全树 `cmake --build build --config Release -j` → BUILD_EXIT=1，73 处 error **全部**归属 RWSimulatorPlugin.vcxproj（RobWorkSim 域，F-008 基线缺陷；`grep -c industrialrobot` ＝ 0）——按 F-036/F-038 判例口径，DoD 第 1 条采用**任务目标面从零**：`cmake --build build --config Release --target sdurws_ird_runtime_test` → **exit 0**，错误计数 0（acc-logs/target-build.log；CompilerImpl.cpp 与 CompilerTest.cpp 均在该构建中编译）。
- **冒烟模式**（独立目录 tmp-smoke-build，带 vcpkg toolchain 绝对路径）：配置 `Configuring done`、构建 exit 0、错误计数 0（acc-logs/smoke-configure.log、smoke-build.log）；测试执行 **172/172 PASSED**（acc-logs/smoke-test-run.log，`172 tests from 46 test suites ran`，与送验留痕 smoke XML tests="172" failures="0" 一致）。
- 结论：两模式零错误（集成按 F-008 闭合前的"任务目标面从零"口径）；与提交声明一致。模式分工（CompilerImpl.cpp/CompilerTest.cpp 仅集成模式编译——消费 S6/S7 真实框架类）按 DTB §5.4 登记于 runtime.md §15.4 v0.12⑧（v0.8②/v0.9⑧/v0.10⑥ 同款先例）：核对 CMakeLists 确认两文件确在 `TARGET sdurw_kinematics` 条件内，CompileStage 枚举与 project 字段为纯值契约面两模式可用——**口径正当**。

## 4.3 契约 verify 命令 —— 通过

- 契约 verify 数组仅 1 条：`cmake --build build --config Release --target sdurws_ird_runtime_test`——验收者在 worktree A 逐字亲手执行（从零构建）**exit 0**（见 4.2）；构建产物运行 **244/244 PASSED**（`[==========] 244 tests from 53 test suites ran`＝既有 220＋新增 24，与送验留痕 integration XML tests="244" failures="0" 一致；acc-logs/integration-test-run.log）。
- 治理脚本（pwsh 7，worktree A 内）：`validate-task.ps1 -TaskFile RT-T11.json` → **PASS (1 tasks)**；`validate-docs.ps1` → **PASS (20 units, 12 trace entries, 150 task files)**。

## 4.4 acceptance 条目逐项 —— 通过（具名测试均在我执行的 244/244 中 PASSED）

| 契约 acceptance | 证据 | 结果 |
| --- | --- | --- |
| ① RT-CPX 全组＋RT-RES-1/2＋RT-CONT-1/2 事务状态机全转移用例通过 | RT-CPX-2/3/4＝CompilerTest.MassNotProvidedPublishesDegraded_RT_CPX_2 / MassProvidedNegativeFailsLocated_RT_CPX_3 / CancelAtStageBoundaryReturnsCancelledThenRetry_RT_CPX_4＋BadAllocTranslatedToResourceBudget_RT_CPX_4；RT-CPX-1（S7 层原子性，按 v0.12⑦ 登记的分工）＝DynamicWorkCellCompilerTest.DwcConstructionFailureIsAtomic_RT_CPX_1（RT-T08 交付面，仍在测试目标内）；RT-RES-1 三支＝ResourceMissingFailsLocated / ResourceDigestMismatchAtS4Fails / ResourceBudgetFails（均 _RT_RES_1）；RT-RES-2 双面＝ResourceSwappedMidCompileFailsBeforePublish / SolidifiedResourceSkipsRecheck；RT-CONT-1/2＝ObjectOutsideClosureRejected_RT_CONT_1 / CompileLeavesObjectStoreUnchanged_RT_CONT_2；事务状态机全转移＝TransactionRollbackIsIdempotentAndPublishIsTerminal（rollback 幂等＋发布后拒绝回滚）＋各段失败转移用例（ReaderFailure/JointObjectIdOutsideRefs/UnknownRevisionFailsFast/MissingProjectContext） | 通过 |
| ② 编译器无内部超时（D-11——取消只经外部请求） | 静态事实：grep CompilerImpl.cpp/.hpp 的 `chrono|clock|sleep|this_thread|gettimeofday` 零命中（验收者亲手扫描）；行为面＝NoTokenCompilesToPublish_NoInternalTimeout_D11_ACC2（无令牌不被中断）＋CancelPollsEveryStageBoundary_D11PollDensity（轮询密度恰 8 被断言钉住——且经本验收探针 2 实证该断言非恒真） | 通过 |
| ③ 十段编译逐步表（§5.2）S1~S10 装配完整且失败回滚（§5.3）用例通过 | 正向＝CompilesTenStageChainAndPublishes_RT_T11_ACC3（S1～S10 全链发布＋来源定位＋身份一致＋无 error 诊断）＋InvertedPresetResolvedIntoWorldTransform_S5Assembly＋FactoryWithProductCompilerPublishes_ViaSegmentEntrance＋RepeatCompileIsDeterministic_SameSnapshotIdentity；失败回滚＝4.4① 所列各段失败用例（全部断言 Failed＋snapshot==nullptr＋诊断含段号定位——MDL-06 不发布半成品） | 通过 |
| ④ 资源摘要复查（§8.6——资源变化→编译失败/缓存失效）用例通过 | ResourceSwappedMidCompileFailsBeforePublish_RT_RES_2（S4 读取 A→S10 前复读 B→Failed＋ResourceChanged＋provider 恰 2 次调用＝复查时点在发布前的行为证据）＋SolidifiedResourceSkipsRecheck_RT_RES_2（免复查面）＋RecordedResourceWarnsNotBlocked（§8.6 不代阻断）——且经本验收探针 1 实证：禁用 recheckConsumedResources 后恰好该组主用例变红（复查逻辑真实存在且被钉住） | 通过 |

ird_gates：验收者在 worktree A 构建树**实跑** `cmake --build build --config Release --target ird_gates` → exit 1，命中恰 2 项＝IRD-GATE-SUB＋IRD-GATE-T1（均 testdata_lint→testkit 边）＝**F-019 既有，零新增**；门禁自测 9 项（fail_r1/fail_t1/fail_r5/fail_sub/fail_r3/fail_r4/fail_t2/pass_clean/pass_r4comment）全部按预期检出（acc-logs/ird-gates-run.log）——与送验留痕 ird-gates-run.log 的"命中集与 wp06-t10 基线一致"声明一致。

## 4.5 测试真实失败能力 —— 通过（2 处独立探针，均即红→还原复绿）

| 探针 | 注入点（worktree A 临时修改，已还原） | 结果 |
| --- | --- | --- |
| 探针 1 | CompilerImpl.cpp compile() 内 `recheckConsumedResources(request, tx);` 改为 `if (false) recheck…`（禁用 S10 前资源复查） | 重建后 `--gtest_filter='CompilerTest.*RES_2*'`：**ResourceSwappedMidCompileFailsBeforePublish_RT_RES_2 FAILED**（替换窗口不再被拦截→Published），Solidified 免复查用例仍绿——精确命中，证明该用例钉住真实行为 |
| 探针 2 | CompilerImpl.cpp 删除 S8 前段边界取消轮询块（`if (cancelRequested…) return cancelledOutcome();` 注释化） | 重建后 D11PollDensity：`idleCancel.polls()` 实测 **7 ≠ 8 期望，FAILED**——证明轮询密度断言非恒真、真实钉住 D-11 边界检查密度 |
| 还原 | 两处修改逐一还原后 `git status --porcelain` 对 CompilerImpl.cpp **空输出**（与送验对象逐字节一致） | 重建复跑 **244/244 PASSED**（acc-logs/restore-build.log 同轮构建） |

## 4.6 架构红线（AGENTS §5 八条） —— 通过

1. **L2 零 Qt**：grep runtime 全部 include/Q_OBJECT——唯一命中为 BuildRedLineTest.cpp:84 的字符串字面量（红线自检测试本体），零真实 Qt 依赖。
2. **业务域互链禁止（R-1）**：runtime 对 modeling/requirements/kinematics/trajectory/dynamics/selection/optimization/ui 的 include 零命中；跨单元协作仅 core（公共头 37 处）＋注入接口（Sources.hpp 抽象——六类端口形态）。
3. **跨单元私有头禁止（R-2）**：对外仅 `#include <sdurws/ird/…>` 公共路径；`#include "../src/DescriptionValidator.hpp"` 为单元内测试消费本单元实现头（合法）；CompilerTest.cpp include "CompilerImpl.hpp" 同为单元内 src/ 私有头。
4. **框架零源码修改（SA-02）**：4.1 已核（diff 无框架文件、patches/ 无新文件）。
5. **名称前缀拼接/剥离禁止（R-4）**：CompilerImpl.cpp/SnapshotAssembler.hpp/CompilerTest.cpp grep `"RobWork` 拼接模式零命中；名称生成统一走 buildRuntimeNameMap（§7 单点）。
6. **权威唯一（PA-1）**：诊断码一律经 `registryCode()` 单点取值（不私裁码值——RT-RESOURCE-RECORDED/RT-NAME-DISAMBIGUATED 两个建议码均显式登记"待 diagnostics 收编"）；摘要一律经 core::ContentDigester（CR-02 单点，文件头注释自证）；S6 内置一致性检查不重复实现第二套判定（compile() S9 处注释显式声明）。
7. **不可变历史（PA-2）**：编译链全输入经只读注入接口（RT-CONT-2 类型层＋行为层双证据用例），零写路径。
8. **不继承 old/**：新增代码全部按 units/runtime.md §5 原文实现，逐段注释标注章节出处，无 old/ 引用。

## 4.7 注释规范（AGENTS §2） —— 通过

- **公共头全查**：Compiler.hpp（CompileStage 枚举全态注释＋project 字段"为什么必须加"论证）、Sources.hpp（kRobotDesignObjectType 常量单点注释）、Description.hpp（文件头"★ 分阶段落位登记（RT-T11）"＋四子结构 objectId 字段注释）——文件头/类/函数 Doxygen 中文描述齐备，均标注需求 ID 与单元卡章节。
- **新增实现文件全文通读**：CompilerImpl.hpp（CompileTransaction/CanonicalModelCompiler 类注释含生命周期、状态机全图、线程约束"非线程安全——仅编译线程访问"、错误语义三轨划分）、CompilerImpl.cpp（S0～S10 每段语义段落注释＋§5.2 逐行对应＋魔法数字来源，如"轮询密度恰 8 次""固定传 1——实现层登记"）、SnapshotAssembler.hpp（提升原因/职责/线程安全说明）、CompilerTest.cpp（每用例注明对应 acceptance/RT 编号与断言面）。
- **§2.5 高危信息抽查**：物理单位（gravityWorld "(0,0,−9.81) m/s²"、吊装高度注释"单位 m"、质量 kg 语境）；坐标系（T_world_base/T_parent_joint/T_flange_tcp/worldPose"世界系固连"）；角度/单位制（S3 警告注释"复核建模输入的单位制（SI 真值）"）；所有权（"借持语义（P-IO-2）""值拷贝返回——§3.3 所有权"）；线程约束（CompileTransaction 非线程安全、§5.5 可重入约定）；错误语义（违约轨 fail-fast vs 查询轨 vs 取消非错误，逐 catch 分支注释）；确定性来源（同输入同身份、无时钟/locale/环境依赖、zeroOffset=0 确定性下标）。**无缺项**。

## 4.8 文档同步 —— 通过（findings 消账核对见下）

- 实现与任务卡**零未登记偏差**：三处契约面增量（CompileRequest.project、Description 四子结构 objectId、CompileStage 枚举/kRobotDesignObjectType）与六处实现层选型（S2 恰一语义、格式版本固定 1、builtFrom 落点、deviceName 承载、zeroOffset=0、复读失败归 ResourceChanged）全部登记于 units/runtime.md §15.4 v0.12（八项），与代码逐项比对一致；§3.1/§4.2/§10.0 代码块同步更新。
- 契约状态与追踪矩阵：契约 status=ready（翻转归收尾，非验收者动作）；validate-task PASS；validate-docs PASS。
- **findings.json 未关闭条目与本任务相关性核对**（逐条）：
  - **F-019**（ird_gates 基线红 2 项，testdata_lint→testkit 边）：与本任务相关（门禁自查面）——实跑确认命中恰为该 2 项既有、**零新增**，处置维持 open（非本任务辖域）。
  - **F-046**（Snapshot.cpp assemble 缩进＋空 then 分支，owner 建议"RT-T11 触碰 Snapshot.cpp 时顺手规整"）：RT-T11 确实触碰 Snapshot.cpp（±165 行，SnapshotAssembler 提升）但**未顺手消账**——验收者实测两处原样保留（空 then＝现 L552-556 `if (std::equal…) { /* 命中期望 magic——继续 */ } else { return …; }`；assemble 定义现 L949 起、方法体 4 空格缩进与 return 续行对齐照旧）。维持 open 不阻塞，处置建议顺延至下次触碰该文件的会话（提升后的定义仍在 src/Snapshot.cpp，v0.12⑥ 未登记本次规整，无登记冲突）。
  - F-043（重力投影测试敏感度）：辖域＝gravityToBase/BaseWorldTransformTest（RT-T06/T07 面），本任务未触碰该面，无关。
  - 其余 open 条目（F-004/005/006/007/008/009/010/012 系/015~018/020/023~033/035~038/040~042/044/045/047）：均不在本任务改动面内（构建约定/治理脚本/其他单元），无静默积压；其中 F-008（全树构建阻断）由本验收再次实证并按 F-036/F-038 口径处置，F-007（ini 模板缺失）由本验收再次实证（见环境前置说明①）。
  - F-012 家族（提交标题动词开头）：**本任务文档提交 753f4460 再复现一例**（见 4.9 与问题清单 G-1，转登 F-048）。

## 4.9 commit 质量 —— 通过（1 条建议级：G-1 → F-048）

- `12bee611`「[WP-06-T11] **实现**十段编译链产品集成：事务/取消/资源复查」——动词开头 ✓；正文四段齐备；验证声明与留痕/复现逐项一致：集成 244/244＝既有 220＋新增 24（基线 RT-T10 XML tests="220"，TEST 清点 24，送验 XML classname="CompilerTest" 计数 24——三口径一致）、冒烟 172/172、门禁 2 项既有零新增、探针描述与 acc-logs 留痕同构。
- `8bb71f8c`「[WP-06-T11] **登记**验证留痕：…」——动词开头 ✓；正文四段齐备；12 件留痕与实际入库清单一致。
- `753f4460`「[units] runtime.md v0.12：RT-T11 实现落位登记（DTB §5.4）」——前缀后以名词短语（"runtime.md v0.12"）开头，非动词开头 → **G-1（建议级）＝F-012 家族第 17 例**，转登 findings.json F-048（详见问题清单）。
- 无粉饰：全部验证结论经验收者亲手复现为真；未发现虚标通过或隐瞒失败。

## 4.10 留痕完整 —— 通过

- `traceability/builds/wp06-t11/` 12 件齐备：integration-build.log／integration-test-run.log／sdurws_ird_runtime_test-integration.xml（tests="244" failures="0"）／smoke-configure.log／smoke-build.log／smoke-test-run.log／sdurws_ird_runtime_test-smoke.xml（tests="172" failures="0"）／ird-test-report.json（ird-test-report/1 结构化报告，taskId/逐用例追溯字段在位）／probe-build.log／probe-rtres2-red.log（失败能力探针证据）／ird-gates-run.log（2 项既有命中＋自测 9 项）／validate-task-run.log（PASS）。
- `phase-one-validation.log:49` 已登记 `RT-T11.json: validate-task: PASS (1 tasks)`。
- ird_gates 与测试均经本验收在独立树重跑复核（4.3/4.4），留痕与复现一致。

## 4.11 偷懒/缩水实现扫描 —— 通过

- ①**标记扫描**：diff 范围 runtime/ 内 `TODO|FIXME|XXX|HACK|placeholder|未实现|not implemented` 零命中；`stub` 命中均为 RT-STUB-0（§11 替身边界声明）合法口径（替身只覆盖注入接口——契约显式登记）；`WIP` 零命中。
- ②**空实现检测**：CompilerImpl.hpp 声明的 4 个接口方法（compile/buildCanonicalModel/contractVersion/implementationVersion）在 .cpp 全部有实质实现；CompileTransaction 的 advance/stage/rollback 为状态机语义本体（§5.3 原文承载），非占位；无"仅返回默认值"的契约函数。
- ③**acceptance 逐条证据表**：见 4.4——四条均有具名测试/命令输出对应，无一条仅凭声明。
- ④**测试强度**：断言校验行为与边界（终态 status、snapshot 空性、稳定码、段号定位文本、provider 调用计数恰 2/恰 1、轮询密度恰 8、快照身份逐字节相等、发布诊断无 error 级），非恒真、非只编译通过——并与 4.5 双探针联动实证会红。

---

## 问题清单

### 阻断级

无。

### 建议级

| # | 问题 | 处置 | 登记去向 |
| --- | --- | --- | --- |
| G-1 | 文档提交 753f4460 标题「[units] runtime.md v0.12：RT-T11 实现落位登记（DTB §5.4）」前缀后以名词短语开头，非动词开头——F-012/F-023/F-024/F-026/F-027/F-030/F-034/F-037/F-038/F-039/F-040/F-042/F-044/F-045/F-047 家族第 17 例；正文四段齐备、验证结论与留痕一致（本验收全新树亲手复现：目标面从零构建零错误、集成 244/244＝既有 220＋新增 24、冒烟 172/172、双探针即红还原复绿、ird_gates 命中＝F-019 既有 2 项零新增），纯风格瑕疵不构成阻断 | 与 F-012 家族合并处置（实施者模板补提交标题动词开头示例，句式如「[units] 登记 runtime v0.12 实现落位…」）；对已推送历史（753f4460 及本家族所涉提交）不追溯改写（AGENTS §6.3） | **F-048**（findings.json，本验收提交同批登记） |
| G-2 | F-046（Snapshot.cpp assemble 缩进＋空 then 分支）owner 曾建议"RT-T11 触碰 Snapshot.cpp 时顺手规整"，本任务实际触碰该文件（SnapshotAssembler 提升）但未顺手规整——两处瑕疵经实测原样保留（src/Snapshot.cpp L552-556、L949 起） | 不阻塞；F-046 维持 open，处置顺延至下次触碰 src/Snapshot.cpp 的会话（无需新编号，本记录即为消账提醒载体） | findings.json F-046 维持 open（不新增条目） |

## 裁决

**verdict＝pass**：4.1～4.11 全部通过、无阻断级问题；建议级 G-1 已转登 F-048、G-2 归 F-046 既有跟踪。本验收通过的是送验对象 `753f4460de7ee990a7dc6bf75a9bb6a9cfe8fd8f` 本身（按 SHA 冻结口径），合入决策归所有者（ACC §6）。
