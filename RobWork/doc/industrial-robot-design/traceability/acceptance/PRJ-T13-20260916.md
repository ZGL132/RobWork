# PRJ-T13 验收记录（attempt 1，2026-09-16）

| 字段 | 值 |
| --- | --- |
| 任务 | PRJ-T13 撤销/重做服务（UndoRedoServiceImpl）——tasks/foundation/PRJ-T13.json |
| 送验对象（冻结） | 03eb0fe0012b4e7a4318ca7bed812653b279f144（分支 wp04-t13；验收前复核 origin/wp04-t13 == 该 SHA，无分支漂移；验收全程未向任务分支推送任何提交） |
| diff 范围 | fcfa6feae7aae4eabe494f5d71644c17788dde3c（state.base＝origin/redesign-main）..03eb0fe0012b4e7a4318ca7bed812653b279f144，共 2 提交（c2144fe1 实现＋测试＋单元卡 v0.15，10 文件 → 03eb0fe0 验证留痕 5 文件，合计 15 文件 2359 insertions） |
| **verdict** | **pass**（4.1～4.11 全部通过、无阻断级问题；3 条建议级问题转登 findings.json F-163/F-164/F-165） |
| 验收环境 | 验收者全新上下文（未消费实施叙述，只消费产物与留痕）；复现现场＝detached worktree @SHA（D:\acc_tmp\PRJ-T13-A）；集成模式全新从零配置＋构建，独立冒烟另配临时目录（D:\acc_tmp\PRJ-T13-smoke，vcpkg toolchain 绝对路径）；MSVC 2022 x64 / Release；验收者亲跑日志落 D:\acc_tmp\（configure-integration.log／verify-target-build.log／mutation-m*.log／gates-build.log／smoke-*.log／validate-task.log 等） |

## 4.1 改动范围 —— 通过

- `git diff --name-only fcfa6fea..03eb0fe0`：15 文件，逐一对入契约 `allowedFiles`——`industrialrobot/project/**`（9 件：include/sdurws/ird/project/UndoRedo.hpp 新增、include/.../ProjectStore.hpp、src/UndoRedoServiceImpl.{hpp,cpp} 新增、src/ProjectStoreImpl.{hpp,cpp}、test/UndoRedoServiceTest.cpp 新增、test/StubHandlers.hpp、CMakeLists.txt）、`units/project.md`（2 处：卡头版本行＋§15.4 v0.15 行）、`traceability/builds/wp04-t13/**`（5 件：ird-test-report.json＋gtest XML×4）。
- `forbiddenFiles`（REQUIREMENTS.md/ARCHITECTURE.md）零触碰（diff name-only 无此二文件——反证在档）。
- SA-02：diff 范围无任何框架源码文件；`industrialrobot/patches/` 仅 base 既有二件（0001-rwslibs-entry-option.patch、PATCHES.md），本次 diff 零触碰——无未登记框架改动。

## 4.2 可复现构建 —— 通过（全新树从零，任务目标面）

- 集成模式：worktree A 全新从零配置（`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON`＋vcpkg toolchain＋`CMAKE_PREFIX_PATH=D:/software/QT/6.11.1/msvc2022_64`——主树缓存口径），**缓存确认 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`**，`Configuring done`＋`Generating done`（configure-integration.log）。
  - 环境备注（F-098/F-099 既有登记，非实施缺陷）：首次配置失败两次——①Qt 未找到（补 CMAKE_PREFIX_PATH）；②`RobWorkStudio.ini.template.static` 缺失（`[Bb]in/` gitignore 吞掉的本机辅助件，自主树复制）。第三次配置零错误。
- 契约 verify 目标 `sdurws_ird_project_test` 构建 **exit 0、error 计数 0**；`sdurws_ird_project_contract_test` 构建 **exit 0、error 计数 0**（verify-target-build.log／contract-target-build.log，各 `grep -c ': error'`＝0）。
- 独立冒烟模式：独立临时目录从零配置＋全量构建（`-DCMAKE_TOOLCHAIN_FILE=<repo>/vcpkg/scripts/buildsystems/vcpkg.cmake`——AGENTS §4.1 口径）**exit 0、error 计数 0**（smoke-configure.log、smoke-build.log）。
- 测试执行（验收者亲手）：集成 `sdurws_ird_project_test` **242/242 PASSED**、`sdurws_ird_project_contract_test` **15/15 PASSED**；冒烟同 **242/242＋15/15 PASSED**（acc-test-run-*.log／acc-contract-run-*.log＋acc-*.xml 四份）——与送验报告 ird-test-report.json 及四份 XML（tests=242/15、failures=0 ×4）逐项一致。
- 变异还原后核对：`git status`/`git diff` 对 `RobWork/` **零差异**（复现现场与送验 SHA 逐字节一致，实施产物无残留改动）；还原后重建＋全量复跑 242/242 复绿。

## 4.3 契约 verify 命令 —— 通过

- 契约 verify 仅一条：`cmake --build build --config Release --target sdurws_ird_project_test`——在 worktree A 集成树（全新从零）亲手执行 **exit 0、error 计数 0**，测试 exe 产出并运行成功（变异探针期间共重建 6 次均成功，见 4.5）。

## 4.4 acceptance 条目逐项证据 —— 通过（全部亲手复现；ird_gates 实跑）

| # | 契约 acceptance | 证据（验收者亲手复现） |
| --- | --- | --- |
| 1 | PRJ-TX-5（undo/redo 部分）用例通过：撤销/重做＝逆命令提交产生新修订、历史不改写（PM-18/PA-2；AT-29）；修订只增断言（revisionSeq 递增、既有修订清单不变） | `ApplyUndoRedoEachCommittedAsNewRevisionHistoryAppendOnly`（UndoRedoServiceTest.cpp:136-221——应用/撤销/重做各恰一新修订＋v2.seq>v1.seq、v3.seq>v2.seq＋revisions/ 目录逐次恰 +1＋branchHistory 清单原序只增＋既有修订视图（v1/v2）逐字段 `==` 不变＝历史不改写＋撤销修订 inverse＝原始 append 三元组＋AT-29 锚点全链）；`UndoThenRedoRestoresOriginalPayloadAndCommandFamily`（:226-263——撤销后 objectRefs 仅剩元数据、重做修订重新携带同负载对象且 tryObject 读回逐字节 "beta-1"＝重放原始载荷内容面）。集成＋冒烟两模式均绿。 |
| 2 | 边界语义：空历史稳定提示数据（不抛错）；跨分支提交后再回（栈＝会话内按分支、不持久化——D-11；重启后 canUndo 由 tip 推导）；逆命令由业务处理器产出、project 只提交（§6.5 注册协议） | `EmptyHistoryStablePromptDataAndPreconditionViolationFailsFast`（:270-298——空历史两轮幂等全 false＋空摘要＋无 blockedReason 不抛错＋未知分支稳定空状态＋canUndo/canRedo 为 false 时 undo/redo 双双 `EXPECT_THROW(std::invalid_argument)` fail-fast）；`CrossBranchCommitDoesNotTouchSiblingBranchStacks`（:305-358——§4.5.1 建支模型上 feature-b 提交不清 main 会话栈＋B 自身 canUndo 由 tip 推导/canRedo=false＋undoSummary 逐字等于 B tip 自身摘要（无跨分支泄漏）＋再回 main 重做成功且 seq 全局单调）；`ReopenProjectRedoLostUndoDerivedFromTip`（:410-455——close/reopen 后 canRedo 恒 false＋canUndo 由 tip inverse 推导照常可用＋重开 redo 前置违约＋L5 重注册边界）；`UnregisteredInverseTokenTypeRelayedAsUnknownCommandNoRevision`（:463-502——未注册逆 token 时 undo 透传 Rejected(UnknownCommand)＋零修订＋会话栈不入，处理器注册后同一请求成功＝§6.5"project 只提交"正反面）；`NewCommandClearsRedoStackWithOneShotStaleReason`（:365-403——新命令清空 redo＋一次性 blockedReason=StaleRevisionRejected＋旗标读即消费＋栈空后 redo 前置违约）。 |
| 3 | 与草稿局部撤销的边界用例过（§2.3 非目标：编辑级 undo 归 ui＋业务域——PM-18 与 REQ-11 分界） | `DraftEditingStaysOutsideCommandUndoService`（:511-554——草稿落盘零修订＋撤销服务状态零变化＋undo 产生新修订后草稿原样保留（payload/baseRevisionId 读回一致）＝REQ-11 分界的存储侧形态；编译面证据：UndoRedo.hpp 无任何草稿参数入口）。 |
| 4 | P-PR-1 处置：身份类型消费以 core.md v0.1 为基线，冻结 diff 后增量同步、不私改 core | `Ppr1_SessionTypesAreCoreContractIdentities`（:561-596——编译期 `static_assert` InverseRecord::undoRevision＝core::RevisionId＋运行期 toCanonical/tryFromCanonical 规范 API 往返并直接作 status/undo 消费键）；零 core 修改实证：本验收 diff 清单无 core/ 路径文件（4.1）；LinkageContractTest/BuildRedLineTest 既有红线自动覆盖新增文件（contract_test 15/15 两模式全绿）。 |
| — | （ird_gates 执行状态） | 已建成，本验收**实跑**：worktree A 构建 `ird_gates` 目标 exit 1（SA-10 设计行为——命中即非零），命中 **9 项＝7×IRD-GATE-SUB＋1×IRD-GATE-T1＋1×IRD-GATE-R4**，自测 9 项（pass_clean/pass_r4comment/fail_r1/fail_t1/fail_r5/fail_sub/fail_r3/fail_r4/fail_t2）全按预期检出（gates-build.log）。R4 唯一命中为 ProjectStoreImpl.cpp:109 `kCreatedWithToolVersion = "RobWork-IndustrialRobot/0.1"` 工具版本串字面量——`git show fcfa6fea` 核对 base:108 已存在、本任务 diff 零触碰（登记例外 DTB §4.5），与 wp04-t08～t12 基线逐项一致＝**零新增命中**。 |

## 4.5 测试真实失败能力 —— 通过（2 个有效探针即红＋1 个等价变异记录在案）

- 探针 M2（status() 的 canRedo 推导条件 `== *tip` 取反 `!= *tip`）：重建复跑 → **5 用例 FAILED**（ApplyUndoRedo…／CrossBranch…／NewCommandClears…／UnregisteredInverse…／Ppr1_…），[PASSED] 4／[FAILED] 5——断言真实观测行为（mutation-m2-run.log）。
- 探针 M3（buildUndoEnvelope 的 `expectedRevision = tipView.id` 篡改为默认 `RevisionId{}`）：重建复跑 → **多用例 FAILED**（undo 提交被 S2 stale-revision 拒绝，committed() 断言即红）（mutation-m3-run.log）。
- 探针 M1（移除 syncOnEntry 的 `redoStack.clear()`）：**9/9 仍绿**——经逐路径分析为（近）等价变异：栈顶一致性检查（status 的 canRedo 推导与 redo 前置均要求 `back().undoRevision == tip`）构成纵深防御，滞留栈底陈旧条目对公共 API 不可观测，非恒真测试缺陷；如实记录不掩饰。
- 全部探针还原后 `git diff` 零差异＋重建全量复跑 242/242 复绿（restore-rerun.log／final-rerun.log；还原经内容级编辑触发真实重编译，规避 F-068 mtime 陷阱——还原后首跑即终验）。

## 4.6 架构红线（AGENTS §5 八条） —— 通过

1. **L2 零 Qt**：新增/改动 4 个源文件 `grep -iE '#include <Qt|QtCore|QWidget'` 零命中；contract_test 的 BuildRedLineTest 源码扫描自动覆盖新增文件（15/15 含其绿）。
2. **业务域互链（R-1）**：`target_link_libraries(sdurws_ird_project PUBLIC sdurws_ird_core)` 唯一产品链接边（CMakeLists.txt:199，base 既有形态不变）；两测试目标仅链 project＋GTest。零 modeling/kinematics 等业务域边。
3. **跨单元私有头（R-2）**：新增 include 跨单元仅 `<sdurws/ird/core/Identity.hpp>`——core 公共头（core/include/sdurws/ird/core/ 实证在位）；UndoRedoServiceImpl.hpp 为 src/ 私有实现头，仅单元内消费（ProjectStoreImpl.cpp 与测试目标 PRIVATE include 路径）。
4. **SA-02**：见 4.1（零框架改动、patches 无新登记）。
5. **R-4 名称前缀**：ird_gates R4 扫描全单元产品面仅 1 处命中＝base 既有登记例外（ProjectStoreImpl.cpp:109，见 4.4）；本任务新增文件零命中。
6. **PA-1 权威唯一**：UndoRedoServiceImpl 零自有写通道——undo/redo 一律 `m_host.commands().submit()`（唯一写路径），提示数据读轨经 `m_host.query()`；用户文案不落地（undoSummary 为机器读摘要，人读文案归 ui）。
7. **PA-2 不可变历史**：undo/redo 均产生新修订（测试 4.4-1 钉住 seq/目录/清单/视图四维只增不改写）。
8. **不继承 old/ 实现**：全部实现从零编写（diff 无 old/ 路径；实现锚定 units/project.md §5.5/§6.9 现行文本——本验收逐行比对 §5.5 签名与 §6.9 详表一致）。

## 4.7 注释规范 —— 通过

- **公共头全查**：UndoRedo.hpp——文件头（设计依据/需求锚点/机制背景/线程模型/P-PR-1 处置齐备）＋UndoRedoStatus/InverseRecord/UndoRedoService 三类型逐字段 Doxygen 中文注释＋错误语义总表（调用方违约 fail-fast vs 环境错误四态透传二分）＋线程安全逐方法标注；ProjectStore.hpp 增量 undoRedo() 访问器注释完整（生命周期/只读语义/Draining 行为/草稿边界）。
- **实现文件抽查（超额，全部改动文件均查）**：UndoRedoServiceImpl.hpp——文件头（实现口径三项登记 DTB §5.4、锁序纪律、所有权生命周声明）＋全部私有步骤 @param 方向标注；UndoRedoServiceImpl.cpp——每个语义段落注释"为什么"（惰性同步窗口期说明、锁外提交锁序纪律、防并发错弹、一次性旗标消费面）；ProjectStoreImpl.{hpp,cpp} 增量段——装配序/析构序/宿主引用生存期论证齐备；UndoRedoServiceTest.cpp——文件头测试域语义约定＋逐用例锚点注释。
- §2.5 高危信息：物理单位（redoPayloadFormatVersion 标注"无单位——格式版本号"）、线程约束（m_mutex 锁序＋"提交段不持本锁"论证）、错误语义（调用方违约 fail-fast／环境错误映射表逐条）、确定性来源（观察点比对基准、拷贝时点口径）——抽查项全数在位。魔法数字无（未引入裸常量）。

## 4.8 文档同步 —— 通过（3 条建议级家族问题转登）

- units/project.md v0.15 落位登记与实现逐项一致（本验收逐行核对）：§5.5 签名（status noexcept／undo/redo 形态）与 UndoRedo.hpp 完全一致；§6.9 详表各行（可逆性声明制／撤销＝新修订／栈归属分支／新命令清空 redo／空历史／过期基线／跨分支／草稿边界）均有实现落点与具名用例；§12 PRJ-T13 行产物名一致。实现口径三项（惰性同步／redo 条目拷贝数据源／undo 字面语义推论）按 DTB §5.4 在文件头与单元卡 §15.4 双侧登记，属"文档未定义判据的最小收敛"而非语义偏差。
- 任务状态/追踪矩阵：state.json phase=awaiting_acceptance、currentTask=PRJ-T13、base/headSha 与送验一致（契约翻转归收尾段，非验收者职责）。
- **findings.json 未关闭条目相关性核对**（141 条 open）：无任何条目点名 PRJ-T13/undo/redo/PM-18——本任务无"顺手消账"项。家族性未关闭问题在本任务的再现如下（均建议级，转登 F-163/F-164/F-165）：①F-122/F-133/…/F-161 家族第十次——卡头日期行停留 2026-09-09（§15.4 v0.15 行＝2026-09-16，版本半边已同步）；②F-012/F-157/F-158/F-162 家族——03eb0fe0 提交标题 `[governance] PRJ-T13 验证留痕：…` 名词开头（实现提交 c2144fe1「实现撤销/重做服务…」动词开头合规）；③F-010/F-125 幻影清单家族——03eb0fe0 正文声称新增构建/测试/门禁/校验日志（"与 wp04-t03～t12 同构"），实际入库仅 5 件（见 4.10）。
- 环境缺口 F-098（Qt prefix）/F-099（ini 模板）本次验收再次踩中——均为已登记 suggestion、已有处置建议，不新登。

## 4.9 commit 质量 —— 通过

- c2144fe1 标题 `[WP-04-T13] 实现撤销/重做服务（UndoRedoServiceImpl）：逆命令提交产生新修订、历史不改写`——动词开头合规；正文四段（What/Why/验证/影响面）齐备，验证结论（两模式 242/242＋15/15、门禁 9 命中零新增、validate-task PASS）经本验收逐项亲手复现**全部为真**；Acc-Covered 对账行在位；实现期缺陷留证两项如实登记（无粉饰）。
- 03eb0fe0 标题 `[governance] PRJ-T13 验证留痕：…`——名词开头（建议级，转登 F-164）；正文四段齐备，其声称的测试/门禁/校验结论全部复现为真；但其"改动内容"所列 11 份日志未实际入库（建议级，转登 F-165，见 4.10）。

## 4.10 留痕完整 —— 通过（附保留：实施侧日志幻影引用，转登 F-165）

- gtest XML 四份入库（builds/wp04-t13/，tests=242/15、failures=0 ×4，UndoRedoServiceTest 组在位）＋ird-test-report.json（ird-test-report/1 契约：目标三行、构建九行、门禁与 validate-task 结论、acceptanceCoverage 四条逐项对账、implementationNotes 三项实现口径＋两项测试口径＋缺陷留证两项）——内容与本验收复现结果逐项一致。
- **保留项**：ird-test-report.json 的 builds[].log 与 gates 字段引用的 11 份 .log（integration-build.log、verify-target-build.log、smoke-build.log、ird-gates.log、validate-task.log 等）**不在库**——`.gitignore:87 *.log` 吞没（F-010 根因），提交正文亦声称"与 wp04-t03～t12 同构"而 T12 目录实有 16 件（T10/T11 同为 5 件形态）。验证结论性内容无失实（本验收全部独立复现为真），按 F-125 家族判例登记建议级不构成阻断；证据面由验收者亲跑日志（D:\acc_tmp\）＋入库 XML/report 补足。
- resultNote 面：state.json 待收尾段由编排者更新（本次验收三元组见文末）。

## 4.11 偷懒/缩水实现扫描 —— 通过

- ①diff 新增行 `TODO|FIXME|XXX|HACK|stub|placeholder|not implemented|未实现|待实现` 扫描：唯一命中 `namespace stub = sdurws::ird::project::stub;`（测试侧桩设施命名空间的既定别名——T10 起既有测试基建名，非缩水标记）；实现文件零命中。
- ②空实现检测：UndoRedoService 三方法均有实质实现（status 可用性推导/undo 逆命令组装提交/redo 会话栈重放）；`undoredoDevSink()`/`undoRedoService()` 为窄访问器（返回成员引用，两行语义完整，同单元 T12 先例形态）；AppendUndoHandler.prepare 实质产出逆声明——无空壳公共接口。
- ③逐条证据表：见 4.4——acceptance 四条均有具名测试＋行号，零"已实现"声明依赖。
- ④测试强度：断言校验行为与边界（seq 递增、目录计数、清单原序、视图逐字段相等、异常类型、错误码枚举、逐字节载荷），非恒真；4.5 双探针实证会红。

## 发现的问题清单

- **阻断级**：无。
- **建议级（已转登 findings.json，编号顺延自 F-162）**：
  - F-163：F-122/F-133/F-134/F-140/F-143/F-147/F-155/F-159/F-161 家族第十次再现——units/project.md 卡头『日期』行停留 2026-09-09，未随 §15.4 v0.15 行（2026-09-16）更新。
  - F-164：F-012/F-157/F-158/F-162 家族再现——03eb0fe0 `[governance]` 车道标题名词短语开头（『PRJ-Txx 验证留痕』逐字模板第五例）。
  - F-165：F-010/F-125 幻影清单家族再现——03eb0fe0 正文声称新增日志 11 份（"与 wp04-t03～t12 同构"）实际入库仅 5 件；ird-test-report.json 引用的日志均为库外幻影引用（根因 F-010 .gitignore *.log）。

## 验收者环境说明

- 复现现场：`git worktree add --detach D:\acc_tmp\PRJ-T13-A 03eb0fe0012b4e7a4318ca7bed812653b279f144`（验收终态 `git status`/`git diff` 对 RobWork/ 零差异还原）。记录回传：`git worktree add -b acc/PRJ-T13/1 D:\acc_tmp\PRJ-T13-B origin/redesign-main`。
- 工具链：MSVC 2022 x64（Visual Studio 17 2022 / MSBuild 17.12.6）、Qt 6.11.1（CMAKE_PREFIX_PATH）、vcpkg 经典模式（gtest 1.18.0 @ installed/x64-windows）。
- 全程未向 wp04-t13 推送任何提交；未修改任何实施产物（变异探针全部内容级还原并经 git diff 零差异核对）。

## 验收记录三元组

本记录随 evidence 分支 `acc/PRJ-T13/1` 提交并推送 origin（记录＋findings.json F-163～F-165 转登同一提交）；`{branch, path, commit}` 三元组由验收会话向编排者输出——commit＝该分支上本记录提交的 40 位完整 SHA（收尾按 SHA 合并的不可变对象）。
