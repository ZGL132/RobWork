# DOC-T08 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | DOC-T08 policy 主链整链编译放行（POL-T02~T12） |
| 送验对象（冻结 SHA） | `1343a91e1869dddee909a8d2eeb01b57aff78450`（分支 doc-t08 尖端） |
| diff 范围 | `19eb9fdeb817d03e3a0455bc4622c54118cc5ab0..1343a91e1869dddee909a8d2eeb01b57aff78450` |
| 分支漂移核查 | `git rev-parse origin/doc-t08` ＝ `1343a91e…`（与送验 SHA 一致，无漂移） |
| 复现现场 | detached worktree @ `1343a91e`（按 ACC §3，不用分支名），验收全程在该树执行 |
| 任务性质 | 纯治理文档任务（契约回填＋放行登记）：双模式 VS 构建不适用（4.1 证得 diff 内零产品源码/构建面文件），"构建"等价物＝治理脚本复跑（validate-task 逐份三查＋validate-docs），沿用 DOC-T04/06/07 纯治理批次口径 |
| **verdict** | **pass**（4.1~4.11 全部通过，无阻断级问题；建议级 1 条已转登 findings.json F-022） |
| 验收者环境 | 全新上下文会话（attempt 1）；pwsh 7 / Git Bash / Windows；仅消费产物与设计文档，未读取实施叙述；traceability/pipeline/state.json 与 pipeline-lock.ps1 按编排约束未读写 |

---

## 4.1～4.11 逐项证据

### 4.1 改动范围 — 通过

- `git diff --name-status 19eb9fd..1343a91` 共 15 个文件：11 份 `tasks/foundation/POL-T02..T12.json`（M）＋3 份 `traceability/builds/doc-t08/*.log`（A）＋`traceability/contract-compile-log.md`（M）——全部命中契约 `allowedFiles` 两个 glob（`tasks/foundation/**`、`traceability/**`）。
- `forbiddenFiles` 零触碰：REQUIREMENTS.md / ARCHITECTURE.md 不在变更清单；`git diff --stat 19eb9fd..1343a91 -- RobWork/RobWorkStudio` 输出为空（框架源码零改动；`patches/` 无新文件，SA-02 无需登记）。

### 4.2 可复现构建（治理脚本等价） — 通过

验收者于 detached worktree 亲手复跑（不采信实施者日志）：

- `validate-docs.ps1` → `validate-docs: PASS (20 units, 12 trace entries, 150 task files)`
- POL-T02～T12 逐份 `validate-task.ps1` → **11/11** `validate-task: PASS (1 tasks)`

送验留痕日志（`traceability/builds/doc-t08/validate-task-all.log`、`validate-docs.log`）与本人复跑输出逐字一致。

### 4.3 契约 verify 命令 — 通过

契约 `verify` 两条命令逐条原样独立执行，均成功：

1. `pwsh -File RobWork/scripts/industrialrobot/validate-docs.ps1` → PASS
2. `pwsh -File RobWork/scripts/industrialrobot/validate-task.ps1 -TaskFile RobWork/doc/industrial-robot-design/tasks/foundation/POL-T02.json` → PASS

### 4.4 acceptance 条目逐项 — 通过（含逐份原文证据，F-014 教训口径）

| 契约条目 | 证据 |
| --- | --- |
| ①十一份逐份回填（requirements 承接 policy.md §2 需求表／锚定 designRefs／真实 dependsOn／branch ≙ 映射／允许禁止文件／verify 与 acceptance） | 11 份契约原文逐一读取：requirements 与 DTB §2.8 WP-07-T02~T12 行及 policy.md §2.2 需求表逐行一致（T02=ARC-05/NFR-COR-03；T04=ARC-05/ERR-01；T05=ARC-05/UX-08/KIN-13；T06 补 MDL-04/15；T07=NFR-COR-05/KIN-02/05/TRJ-04/OPT-03；T08=MDL-06/MDL-12；T09=CON-04/05/NFR-DEP-05；T10=ERR-01/UX-03；T11=ARC-05/MDL-06/MDL-22/NFR-COR-05；T12=NFR-MNT-05；AT-xx 追溯编号迁入 acceptance 注记）；designRefs＝policy.md §12 任务行"输入"列原文（锚点真实性由 validate-task ②查 11/11 PASS 背书，且 4.5 探针证实该查会红——非恒真）；dependsOn 与 policy.md §12 依赖列一致（T02→T01、T03→T02、T04→T03、T05→T04、T06→T02/T05、T07→T06、T08→T04、T09→T04、T10→T02、T11→T05~T10＋TK-T01）；例外见 G-1（POL-T12 缺 T01，源头 DOC-T03，建议级不阻断） |
| ②十一份全部置 ready＋拓扑序登记 | 逐份 status/branch 原文输出（`grep -H '"status"\|"branch"' POL-T0x.json`）：11 份全部 `"status": "ready"`、branch `wp07-t02`～`wp07-t12`；compile-log 登记行序即拓扑序 POL-T02→T12（`contract-compile-log.md` L43–53）。"入队由 autoDiscovery 自动追加队尾"＝编排侧动作（state.json 验收者按约束禁读），不在送验对象内，不以之为本项证据 |
| ③登记类 O 项处置条款 | O-10→POL-T02 acceptance[1]／POL-T04 acceptance[1]／POL-T08 acceptance[2]（nullopt＝显式不适用，不发明数值）；O-11→POL-T02 acceptance[2]（归属 selection 卡 WP-19-T01 裁决）；O-18→POL-T02 acceptance[3]（runtime 卡建议并入，schema 次版本通道）；O-20→POL-T09 acceptance[1]（WP-24-T01 冻结基线后锁定）——四项均为"登记不私裁"保守口径，compile-log 对应行同步注记 |
| ④validate-docs＋逐份三查＋compile-log 一行一契约 | validate-docs PASS（4.2）；11/11 三查 PASS（4.2）；compile-log POL-T02~T12 十一行（L43–53）逐份注明放宽依据（所有者 2026-09-11 B 模式预授权整链放宽，沿用 DOC-T04 口径）与 O 项处置 |
| ird_gates 自查口径 | 本任务无产品代码与构建目标，送验提交 00b33db 正文如实声明"ird_gates 自查＝validate-task 逐份三查代替"——属如实声明适用口径，非把未执行项标注为通过，合规 |

### 4.5 测试真实失败能力（验证验证者） — 通过

治理脚本替代测试执行双向探针（在复现现场注入后还原）：

- **探针1（锚点失效）**：POL-T03 designRefs `units/policy.md#5.3`→`#5.99` → validate-task 报错变红：`designRef anchor not found in target: units/policy.md#5.99 (in POL-T03)`
- **探针2（依赖失效）**：POL-T03 dependsOn `POL-T02`→`POL-T99` → 脚本退出码 **1**：`dependsOn target not found: POL-T99 (referenced by POL-T03)`
- **还原复绿**：`git checkout --` 还原后复跑 POL-T03 → `validate-task: PASS (1 tasks)`；`git status --porcelain` 空（现场零残留）

证明三查②③为真实断言，非恒真。

### 4.6 架构红线 — 通过（等价核查）

diff 内零源码/构建文件（4.1），零 Qt／无私有头／无业务域互链物理不可能；契约文本层面：POL-T05 acceptance 明文"本任务零 runtime/project 编译依赖（R-1/R-2）"（注入边界 CR-04）；无 R-4 名称拼接/剥离语义引入；改动未越权威边界（仅触自身 allowedFiles，PA-1～PA-7 不适用面逐一排除）。

### 4.7 注释规范 — 通过（不适用）

变更文件清单中零 `.hpp`/`.cpp`/`.cmake` 文件（4.1 name-status 为证），无公共头与实现文件可查，AGENTS §2 注释规范无适用对象。

### 4.8 文档同步与 findings 相关性 — 通过（附建议级 G-1）

- 实现与任务卡零偏差：回填内容与 policy.md §12／DTB §2.8 WP-07 行逐字段一致（4.4①）；compile-log 十一行同步登记；无未登记偏差。
- **G-1（建议级）**：POL-T12 `dependsOn` 缺 POL-T01——policy.md §12 与 DTB WP-07-T12 行均登记依赖 T01~T11，契约仅列 T02~T11。**base 19eb9fd 已存在该状态**（diff 证实本任务未触碰该字段；编译源头 DOC-T03，其验收已 pass）；传递覆盖（T12→T02→T01）故无调度影响。已转登 **findings.json F-022**。
- **findings.json 未关闭条目相关性核对**（F-004~009、F-012、F-015~020 共 14 条 open）：与本任务相关的两条——**F-010**（.gitignore 吞 traceability 日志）：本任务留痕提交 1343a91 以 `git add -f` 规避并在正文注明（沿用 DOC-T06/07 先例），条目保持 open（.gitignore 不在本任务 allowedFiles，不可顺手消账）；**F-012**（提交标题名词开头风格判例分歧）：与本任务主提交同型（见 4.9），保持 open 随 F-012 统一处置。其余 12 条均为 core/testkit/runtime/evidence 辖域，与本任务无关，无静默积压。

### 4.9 commit 质量 — 通过（附注）

- 00b33db：标题 `[DOC-T08] policy 主链整链编译放行（POL-T02~T12 十一份契约回填）`，正文四段（What/Why/验证/影响面）齐备；验证结论（11/11 三查＋validate-docs PASS）与本人复跑一致，无粉饰。附注：标题以单元名"policy"开头非严格动词开头——与已验收先例 DOC-T07 `41b6e01` 同型，open 状态的 F-012 已在册跟踪该判例分歧，不重复转登。
- 1343a91：留痕补充提交（先例 DOC-T06 `3af4aae`／DOC-T07 `aa03084`），简式正文列明三份日志与 `git add -f` 原因（F-010 规避），`Acc-Covered` 尾注登记。

### 4.10 留痕完整 — 通过（等价口径）

- `traceability/builds/doc-t08/` 三份日志随 1343a91 入库（validate-task-all 11/11、validate-docs、validate-task-polt02），内容与验收者复跑输出一致（4.2）。
- compile-log 十一行放行登记（4.4④）。
- `phase-one-validation.log` 不更新：该文件最后变更停留在 DTB v0.7/DOC-T02 时代，DOC-T06/07 整链批次及验收均未触碰（git log 为证），纯治理批次沿用该先例。

### 4.11 偷懒/缩水扫描 — 通过

- ①diff 全文 `grep -iE "TODO|FIXME|stub|placeholder|未实现|not implemented"` → 零命中（exit 1）；无"触发式延后"豁免需要列出。
- ②空实现检测不适用（零代码接口）；且 11 份回填非模板批量替换——逐份各有专属变更：T06 早期批次补全（requirements/锚点/allowedFiles/branch/interUnit）、T07/T11 AT 编号迁入 acceptance、T02 三条 O 项处置、T11 补 testkit §10 锚与 testdata/** 交付面、T12 补 UT-BUILD 条目。
- ③逐条证据表：见 4.4（acceptance 四条均有具名证据），无一条缺证据。
- ④测试强度：与 4.5 联动——双探针证实三查会红，非"只编译通过"式验证。

---

## 问题清单

**阻断级：无。**

**建议级（已转登）：**

| 编号 | 内容 | 去向 |
| --- | --- | --- |
| G-1 | POL-T12 dependsOn 缺 POL-T01（unit card §12/DTB 行为 T01~T11；DOC-T03 编译源头、base 已存在、本任务未触碰；传递覆盖无调度影响） | findings.json **F-022**（open，随 POL-T12 实施领取前消账） |

（4.9 标题风格附注同型 F-012，已在册，不重复转登。）

## 裁决

**pass** —— 4.1～4.11 全部通过且无阻断级问题；建议级 G-1 已按 ACC §5 强制转登 findings.json（F-022），不阻塞合入。验收对象为冻结 SHA `1343a91e1869dddee909a8d2eeb01b57aff78450`，不随分支引用漂移。
