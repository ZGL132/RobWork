# 验收记录 —— RPT-T11（实现测试替身与契约套件：四具名替身＋RP-\* 用例体全量收口）

| 字段 | 值 |
| --- | --- |
| 任务 ID | RPT-T11（卡内行 ≙ WP-12-T09——DTB §2.13；提交前缀 [WP-12-T09]/[RPT-T11] 双轨为 F-230 登记口径，持续有效） |
| 送验对象（冻结） | e995689021af7a657e647fb728000a10ed03f64e（分支 wp12-t11；验收前 fetch 后 `git rev-parse` 实测 `origin/wp12-t11` == 该 SHA 全 40 位逐字一致——零分支漂移，ACC §3） |
| diff 范围 | 9fbdf3459372a2257ef1141f74caa3af83feed98..e995689021af7a657e647fb728000a10ed03f64e（＝state.base..state.headSha；range 内 5 提交：69d23968 两替身正本＋README、ad7e8d1e RP-\* 套件＋testkit 接入、08766bd2 局部夹具收敛、e412eea2 两 Fake 升格＋v0.14 登记＋留痕归档、e9956890 返工补交 11 件 .log） |
| 验收尝试 | attempt 2（attempt 1＝e412eea2，verdict=fail，阻断 G-1：11 件 .log 被 .gitignore 吞掉未入库——记录在 `acc/RPT-T11/1` 分支；本次全量重跑，非增量验收；文件名加 -r2 防覆盖） |
| 验收环境 | 复现 worktree A＝`git worktree add --detach .acc-rpt-t11-r2-repro` @ e9956890（集成 build 树为本验收全新配置：`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=<主仓>vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`，配置 exit 0、`CMake Error` 计数 0、缓存 grep `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON` 实证）；冒烟 build 树＝worktree A 内独立目录 `build-smoke`（本验收新建，vcpkg toolchain＋Qt 前缀显式参数——AGENTS §4.1）；门禁 base 对照 worktree C＝detached @ 9fbdf345（比对后清理）；记录回传 worktree B＝`git worktree add -b acc/RPT-T11/2 .acc-rpt-t11-r2-record` origin/redesign-main@9fbdf345。配置前自主树复制两件未追踪 ini 辅助件（RobWorkStudio.ini.template.static/.shared.in——F-007/F-200 既有登记环境链的预防性处置，非送验对象缺陷）。验收全程只消费产物、契约、设计文档与在库留痕，未读取实施者过程叙述；结论全部出自验收者亲手重跑 |
| verdict | **pass**（4.1～4.11 全部通过；无阻断级问题；建议级 1 条已转登 findings.json F-252——编号自 acc/RPT-T11/1 在册 F-251 顺延，待两记录分支合并后家族归位） |
| 验收日期 | 2026-09-20（attempt 2，-r2） |
| 记录回传 | 本文件随 F-252 转登一并提交于 `acc/RPT-T11/2` 分支并推送 `origin`（ACC §3）；三元组见文末 |

---

## 4.1 改动范围 —— 通过

- `git diff --stat 9fbdf345..e9956890` 实证 **35 个变更文件**：15 件 `reporting/**`（CMake 1＋contract\_test 4＋test 10；M 4 件＋A 11 件）＋19 件 `traceability/builds/wp12-t11/**`（attempt 1 的 8 件＋返工补交 11 件 .log）＋1 件 `units/reporting.md`；15372 insertions / 358 deletions。
- **allowedFiles 逐文件比对（越界零命中）**：`git diff --name-only 9fbdf345..e9956890 | grep -v -E '^RobWork/RobWorkStudio/src/rwslibs/industrialrobot/reporting/|^RobWork/doc/industrial-robot-design/units/reporting\.md$|^RobWork/doc/industrial-robot-design/traceability/'` **零输出**（exit 1＝零命中）——35/35 全部命中契约三白名单。
- forbiddenFiles（REQUIREMENTS.md／ARCHITECTURE.md）**零触碰**（diff 无该两文件）。
- 无未登记框架源码改动（SA-02）：diff 零框架路径（industrialrobot 之外零文件）；`patches/` 变化 0 件。

## 4.2 可复现构建 —— 通过（集成全新树重配＋独立冒烟树；交付面双目标零错误）

- **集成模式**（worktree A 全新 `build/`）：重新配置 exit 0、缓存 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`（grep 实证）；契约 verify 两目标 `--target sdurws_ird_reporting_test`／`…_contract_test` **均 exit 0、`: error`/`fatal error`/`error LNK` 计数 0**，两测试 exe 就位（`build/RobWorkStudio/bin/Release/`）。
  - 全树 `cmake --build build --config Release` 失败于 **RWSimulatorPlugin（71 处）＋rwplugin（2 处）**——Math.hpp min/max 宏污染簇（error C2059/C2334/C2143 等），**73 处命中全部位于 RobWorkSim 两插件、industrialrobot/RobWorkStudio 面零错误**：F-008 既有基线框架缺陷（TK-T01 登记、attempt 1 同型复现、RPT-T10 验收同口径），与本任务交付面无关（本任务 diff 零框架文件——4.1 实证）。
- **独立冒烟模式**（独立目录 `build-smoke`，toolchain＋Qt 显式参数）：配置 exit 0（`CMake Error` 计数 0）＋全量构建（industrialrobot 全目标）**exit 0、错误计数 0**；`build-smoke/reporting/Release/` 两测试 exe 就位并亲跑全绿（见 4.3）。

## 4.3 契约 verify 命令 —— 逐条亲手执行；第 1/2 条 exit 0，第 3 条字面空转（F-251 既有登记同型）但实质执行全绿

| verify 命令（契约原文） | 验收者执行结果 |
| --- | --- |
| `cmake --build build --config Release --target sdurws_ird_reporting_test` | exit 0，零错误（acc-verify1.log） |
| `cmake --build build --config Release --target sdurws_ird_reporting_contract_test` | exit 0，零错误（acc-verify2.log） |
| `ctest --test-dir build -C Release -R "^sdurws_ird_reporting" --output-on-failure` | **exit 0 但 "No tests were found!!!"——空转**（acc-verify3.log；build 根无 CTestTestfile.cmake——框架顶层未 enable_testing，worktree A 实证）——F-251（acc/RPT-T11/1 在册）登记的框架基线缺陷，attempt 2 独立复现，非送验对象引入（verify 命令字面为契约 note 援引 UI-T14 先例的同族口径） |

**实质执行（非只构建，双模式四跑全绿——验收者亲跑）**：

- 集成模式直接执行双 exe：`[==========] 196 tests from 31 test suites ran`＋`[PASSED] 196`；`58 tests from 21 test suites`＋`[PASSED] 58`——零失败（acc-run-unit-int.log / acc-run-contract-int.log）。
- 冒烟模式直接执行双 exe：同为 **196＋58 全绿**（acc-run-unit-smoke.log / acc-run-contract-smoke.log）。
- 叶子目录 ctest：`ctest --test-dir build/RobWorkStudio/src/rwslibs/industrialrobot/reporting -C Release -R "^sdurws_ird_reporting"` → **2/2 通过，100% tests passed**（acc-ctest-leaf.log）。

**与送验留痕比对**：在库四份 gtest XML 顶格统计（196/0 与 58/0 × integration/smoke，`failures="0"`）与亲跑一致；** ReportingContractSuite 32 个 RP\_\* testcase 名集合亲跑 XML 与在库 XML diff＝0（32/32 一致，含 file/line 锚）**；ird-test-report.json（unit 196/196 decisive:true、contract 58/58 decisive:true）与亲跑一致。

**治理脚本（worktree A 上下文亲手复跑）**：`validate-task.ps1 -TaskFile …RPT-T11.json` → **PASS (1 tasks)**；`validate-docs.ps1` → **PASS (20 units, 12 trace entries, 150 task files)**；`verify-task.ps1 -DryRun` → **DRYRUN PASS**（dryrun 回显三条命令与契约 verify 字面逐字一致）；`validate-state.ps1` → **PASS (phase=idle, queue=15, history=127, tasks indexed=150)**（queue-head：RPT-T11 claimable=true——状态翻转归收尾段，F-201 登记口径）。均与在库 validate-\*.log 结论同值。

**ird_gates 独立复跑＋双树归一化比对**：`cmake -DIRD_ROOT=<各树绝对路径>/industrialrobot -DIRD_ENABLE_GIT=OFF -P cmake/ird_gates.cmake` 直跑 base（9fbdf345 detached worktree）与 head 两树——**base 命中 27 项 / branch 命中 29 项（与在库 gates-ird-gates-\*.log 汇总行逐字同值）**；路径归一化后命中集 diff＝**恰 +2 项**（IRD-GATE-SUB：两测试目标直链 sdurws\_ird\_testkit——testkit.md §2.4 允许依赖形态的本任务登记落地，DTB §4.5 豁免登记族在 reporting 单元首次落地；与在库 gates-hit-set-diff.txt 结论一致）；**T-1（产品目标链 testkit）命中 0**。gate-all.ps1 冒烟步缺 Qt 前缀为既有环境项（gates-all-note.txt 如实登记，wp12-t07~t10 先例口径）。

## 4.4 acceptance 条目 —— 通过（六条逐项证据，验收者亲读源码＋亲手复跑）

| # | 契约条目 | 证据 |
| --- | --- | --- |
| 1 | §10.1 用例矩阵逐条执行通过并留痕（RP-MDL-1~3/SCOPE-1~4/STATE-1~4/CUR-1~3/COV-1/TRACE-1/CONS-1~4/CONC-1/CANC-1/IDEM-1/2/CONF-1/DISK-1/RO-1/SAN-1/REVW-1/2/BUN-1/GATE-1） | **32 组逐一在册且逐一真实执行**：`TEST_F(ReportingContractSuite, RP_…)` 源码清点恰 32（grep 实证），与契约 acceptance 1 枚举清单逐组对名；亲跑 196/58 全绿 ×双模式（4.3），RP\_\* 名集合与在库 XML diff＝0；ird-test-report.json **32 条 ReportingContractSuite 条目 requirementIds 与 atIds 全部非空**（如 RP\_MDL\_1→["RPT-01","NFR-COR-02"]/["AT-32"]、RP\_GATE\_1→["NFR-MNT-01","NFR-MNT-02","NFR-MNT-04"]）；三面留痕（gtest XML×4＋ird-test-report.json×2＋构建/运行/治理日志×11＋gates 双树日志＋hit-set-diff＋note＋validate 双日志＝19 件）在库齐全（4.10）。**未执行用例标注通过的情况：零**。阶段 A 范围收敛点（框架章节端到端随 RPT-T12/T14/T15、coverageSummary 聚合未冻结、RP-ROUND-1 阶段 C 不在矩阵）在套件文件头＋v0.14 行如实登记，不构成缩水（登记在案＋矩阵原文不含 RP-ROUND-1） |
| 2 | 四具名替身落地＋RPT-T03~T10 局部夹具收敛 | 四正本头注亲读（ScriptedResultSource.hpp L1-41／ScriptedSectionProvider.hpp L1-32／FakeArtifactSink.hpp L1-35／FakeArchiveWriter.hpp L1-46——具名正本登记＋§10 原文引用＋设计依据）；**收敛实证**：test/contract\_test 各 .cpp 内局部替身类定义 grep **零命中**（仅具名头内 class 定义在册——BuilderTest/SectionProviderTest/BundleTest/BuilderCrossUnitContractTest 四处局部夹具全部拆除改消费正本，与 08766bd2 正文与 README §2 收敛清单一一对应）；替身 envelope 一律经 evidence `ResultEnvelope::make` 构造（ScriptedResultSource.hpp 脚本值工厂 L66-120——合法组合-only；唯一 Preview 例外受控注记在案） |
| 3 | 替身边界声明在案 | `test/README.md` §3 全文六项约束（"不构成任何运动学/轨迹/动力学/选型结果的业务正确性证明"原文＋EV-REG-3 同源＋TestRecord/ReviewReport 无共享）；四具名头注逐头同源声明；`RP_STATE_4_DoubleBoundaryDeclarationOnFile` 用例机器核对（32 组亲跑全绿之一）；套件文件头（L26-30）与 ReportingScenario.hpp 文件头（L16-20）双重声明 |
| 4 | 跨单元依赖就绪声明（testkit/evidence/core 可用性事实） | testkit 公共头在位（include/sdurws/ird/testkit/{ContractCheck,Fixture}.hpp＋gtest/{AssertMacros,RecordListener}.hpp 亲见）；消费面经全新构建零错误＋测试运行通过实证可用（4.2/4.3）——『WP-02 可用』为可用性事实不入 dependsOn 的契约口径成立 |
| 5 | 消费纪律（T-1 允许形态＋追溯字段） | CMake 两测试目标链接面＝`sdurws_ird_reporting sdurws_ird_testkit GTest::gtest`（CMakeLists diff 逐字）；**产品目标 sdurws\_ird\_reporting 链接面零改动**（diff 中产品目标 link 语句未出现）；三面钉住：ird_gates T-1 零命中（4.3 双树复跑）＋LinkageContractTest::NoTestkitEdge 收窄重述（diff 亲读——testkit 引用必须落在测试目标链接语句行的逐行扫描断言；58 条亲跑全绿含此条）＋配置期守卫；IRD\_TEST_INFO 追溯 32/32 非空（第 1 条） |
| 6 | 陷阱处置 P-RPT-9/O-13 | **O-13**：ReportingContractSuiteTest.cpp:578 `static_assert(…EvidenceItemStatus::NotApplicable) == 4` 等五值断言在册＋RP\_STATE\_2/RP\_CONS 组呈现锁定用例（亲跑通过）；**P-RPT-9**：evidence/core v0.1 Draft 基线口径登记于 README §3 第 4 项＋v0.14 行（冻结 diff 后按影响面同步的同步义务落位在案） |

## 4.5 测试真实失败能力 —— 通过（两处产品面变异均被捕获，已还原复绿）

在 worktree A（验收沙箱，任务分支零触碰）对产品代码注入变异，增量重建后亲跑单元测试 exe：

1. **变异 M1（Render.cpp:1500 formalPass 资格门旁路）**：`if (section.eligibilityNote->formalPassAllowed)` → `if (true)` → **5 个失败**，含 **ReportingContractSuite.RP_STATE_3_InfeasibleReviewRecord**（套件用例红——"不可行评审记录场合永不输出正式通过措辞"断言真实生效）＋WordingFreezeTest 三例＋FormalPassWordingGate（acc-m1-run.log，196→191 passed）。
2. **变异 M2（Export.cpp:410 外部幂等字节判定取反）**：`digestOf(existingBytes) == wantDigest` → `!=` → **5 个失败**，含 **ReportingContractSuite.RP_IDEM_2_ExternalIdempotentNoOp**（"同字节导出＝无操作"断言红）与 **ReportingContractSuite.RP_CONF_1_ConflictDetectionAndResolution**（冲突判定联动面红）（acc-m2-run.log）。
3. `git checkout` 还原＋重建 exit 0 后 **196/196 复绿**（acc-restore-run.log）；`git status --porcelain` industrialrobot 面空（沙箱洁净）——套件对产品回归具备真实红能力，非恒真、非"只编译通过"。

## 4.6 架构红线 —— 通过（AGENTS §5 八条逐条）

1. **L2 计算内核零 Qt**：全部新增/修改文件 include 面亲读＋grep 扫描——`#include <Q|<Qt` 命中仅为红线扫描用例的字符串字面（BuildRedLineTest/套件内扫描代码）；reporting 产品目标未触碰；`NoQtInclude_DT_BUILD_NFR_MNT_01` 用例随 196 例亲跑通过。
2. **业务域互链禁止（R-1）**：产品目标四条登记边零改动；新增边仅测试目标→testkit（登记豁免族 +2 项，4.3 双树比对）；ird_gates SUB 扫描除该 +2 外零新增。
3. **跨单元私有头禁止（R-2）**：include 面仅他单元公共头（sdurws/ird/{core,evidence,diagnostics,project,reporting,testkit}/… 公共路径）；相对包含仅两族——`"../test/具名替身.hpp"`（contract\_test 消费单元内测试头）与 `"../src/ReportCodec.hpp"`（套件消费**本单元**私有头）——均为单元内相对包含，不跨单元。
4. **框架零源码修改（SA-02）**：diff 零框架路径、patches/ 零新文件（4.1）。
5. **不做 RobWork 名称前缀拼接/剥离（R-4）**：新增测试面零名称解析逻辑；ird_gates R4 双树命中集等同（4.3）——本任务零新增。
6. **权威唯一（PA-1）**：本任务为测试侧设施；测试报告（TestRecord/ird-test-report.json）不经 reporting 生成（TestMainReport.cpp 文件头消费纪律声明＋README §3 第 6 项——testkit §2.3 对照表冻结口径）；ReviewReport 渲染/导出器不用于测试序列化（用例面即证——ird-test-report 由 testkit RecordListener 产出）。
7. **不可变历史（PA-2）**：测试零项目写路径（TempDir 临时区自持）；归档/导出语义经注入缝与替身观测，不触碰项目历史。
8. **不继承/不恢复 old/**：diff 无 old/ 引用（name-status 全清单核对）。

## 4.7 注释规范 —— 通过（新增头文件全查 6 件，超出抽查下限；产品公共头零新增故无公共头契约面缺位）

- **全查六件**：①ScriptedResultSource.hpp——文件头（设计依据逐条锚 §10/§9.1/§11＋契约 acceptance 2/3/5＋替身边界声明三项构造纪律＋Preview 例外注记＋线程约束）；②ScriptedSectionProvider.hpp——同构齐全（§5.3 四态脚本化＋纯投影/值拷贝契约承载＋线程约束）；③FakeArtifactSink.hpp——D-13/D-14 语义边界＋三项故障注入口逐条＋"不伪造报告内容"；④FakeArchiveWriter.hpp——"不构成 ZIP 容器格式正确性证明"边界＋与真实 io 实现四点语义对齐＋三类故障注入＋RAII 放弃路径；⑤ReportingScenario.hpp——场景组装边界（io 最小镜像非第二实现——SA-12）＋单一 TU 消费声明；⑥两件 TestMainReport.cpp——自有 main 背景（为什么替换 gtest\_main）＋监听器所有权注记＋消费纪律＋线程模型。
- **ReportingContractSuiteTest.cpp**（104 KB 主套件）文件头六段：设计依据／阶段 A 范围如实登记（不私扩验收面）／替身边界声明／确定性上下文（固定种子 20260909、零随机）／线程约束；用例体逐组注释锚 §10.1 矩阵行（RP-SCOPE-1/2 抽读——观测点逐条对名＋语义说明）。
- **§2.5 高危信息**：线程约束逐头在案；错误语义（契约违约 fail-fast vs 环境失败注入面）在替身头与用例注释区分；确定性来源（固定种子/禁随机/同脚本同 contentIdentity）逐头声明；所有权（监听器静态存储期——gtest 官方形态注记）。单位/坐标系/rad：本任务纯文本/字节/身份呈现面，无几何量——N/A 合理。
- **CMakeLists.txt 增列段**中文注释块齐备（RPT-T11 增列说明——替身落位/套件/链接面变化/T-1 形态逐条，diff 亲读）；LinkageContractTest.cpp 修订处逐条中文说明（收窄重述的理由与扫描纪律）。

## 4.8 文档同步 —— 通过（偏差已按 DTB §5.4 登记；findings 未关闭条目相关性逐族核对）

- **单元卡 v0.13→v0.14**（e412eea2 内同步提交）：文档头版本行＋§14.4 v0.14 落地登记行（逐行亲读）；登记内容与实现对照核实——四具名正本落位/局部夹具收敛清单/RP-\* 32 组（阶段 A 收敛点如实登记）/testkit 接入 T-1 形态/README 正本/P-RPT-9/O-13 处置，与验收者亲读源码与亲跑结论一致；本文档契约语义零变更（纯登记行）。**零未登记偏差**（以 acceptance 六条反查实现，无超出登记的偏差）。
- **任务状态/追踪矩阵**：契约 status 维持 ready、随收尾段翻转（PIPE §4.7，F-201 登记口径；validate-state PASS 佐证队列状态一致）；validate-docs PASS 亲跑同值。
- **findings.json 未关闭条目相关性核对（ACC §4.8 v1.2；base＝origin/redesign-main 现册 245 条/open 217，另 acc/RPT-T11/1 在册 F-250/F-251 待随记录分支合并）**：
  - **F-010/F-223/F-244/F-248 .gitignore 吞留痕族**：attempt 1 阻断 G-1 即该根因第 3 次连续再现（已转登 F-250，blocking）；本次送验对象 e9956890 以 `git add -f` 补交 11 件并如实说明（不 amend 已推送历史合规）——**F-250 的本任务返工项已兑现**（19/19 件在库零证据缺失），系统性处置（.gitignore 负例/后缀惯例）仍 open 留构建约定所有者；
  - **F-251（ctest 空转，suggestion）**：本验收字面复跑同型再现（4.3）——实质执行成立、口径缺陷延续；其同源面的『ctest 通过』声称-引用失准两处为本验收新登记 **F-252**（见 4.9/G-1）；
  - **F-007/F-200 环境链**：本验收再次预防性复制 ini 双模板件——既有登记再证实，非交付面问题，零新增；
  - **F-008 全树口径族**：本任务送验留痕与提交正文均未做"集成全树零错误"声称（integration-build.log 口径＝交付面双目标），无复踩；本验收全树复现的 RobWorkSim 失败即 F-008 本体，与送验对象无关；
  - **F-019（门禁基线红）**：亲历复现（27→29 项，+2 为 DTB §4.5 登记豁免族本任务首次落地，gates-all-note.txt 如实登记处置说明）——既有登记继续有效；
  - **F-201（状态翻转条文与实践脱节）**：本任务同样以收尾段翻转——既有登记继续有效；
  - **F-230/F-012（提交前缀双轨/标题风格）**：本任务 4 提交 [WP-12-T09]＋1 提交 [RPT-T11]，均为动词开头，标题四段齐备——零新增复踩；
  - 其余 open 条目（core/testkit/evidence/project/io/execution/policy/runtime/diagnostics/ui 各单元家族）与本任务交付面（reporting 测试设施层）无交叉；**无可随本任务顺手消账项**（F-250/F-251 属 attempt 1 记录分支待合并件，本记录分支不再重复登记）。

## 4.9 commit 质量 —— 通过（四段式齐备、验证结论经独立复跑证实；『ctest 通过』措辞 2 处转登 F-252）

- **69d23968** `[WP-12-T09] 落位 reporting 四具名替身之二正本与替身边界声明文档`——动词开头；四段齐备；Acc-Covered 如实标注"部分——收敛随后续提交消账"（诚实分账）；验证段如实声明"仅新增测试头与文档，不参与编译的目标面零变化"。
- **ad7e8d1e** `[WP-12-T09] 落位 RP-* 契约套件（§10.1 矩阵 32 组）与 testkit 报告设施接入`——动词开头；四段齐备（What 按文件分点/Why 逐条对应 acceptance 1/4/5/影响面 T-1 三面自证）；验证段"ctest 收口通过"一处措辞与在库证据不对应（在库留痕为直接 exe 执行日志，字面 ctest 命令空转——见 4.3/F-251；实质执行 196＋58 全绿经本验收亲证为真，非粉饰）——**G-1 转登 F-252**。
- **08766bd2** `[WP-12-T09] 收敛 RPT-T03~T10 局部夹具为四具名替身并统一用例体引用`——动词开头；四段齐备；"既有用例语义零漂移"经亲跑对证（196 名集合与在库 XML 一致、attempt 1 亦证 testcase 集合 diff＝0）。
- **e412eea2** `[WP-12-T09] 升格两 Fake 头注为具名正本登记＋[DTB] reporting v0.14 落地登记与验证留痕归档`——动词开头；四段齐备；"19 份验证留痕"声称经返工后**首次与在库事实一致**（4.10）；"ird_gates 恰新增 2 项"经本验收双树独立复跑证实（27→29）。
- **e9956890** `[RPT-T11] 返工补交 11 件 .log 验证留痕（G-1：.gitignore 吞没致归档失实）`——动词开头；四段齐备且对 attempt 1 阻断项逐点回应（补交清单逐件列名、不 amend 合规声明、零代码变更声明、"未重跑构建/测试"如实声明并指明日志与 e412eea2 时代的对应关系）——返工提交质量合规。
- **粉饰排查**：提交正文声称的双模式测试计数（196＋58——XML 顶格＋亲跑四跑双证）、门禁 +2 项（双树归一化 diff 独立成立）、validate 三脚本 PASS（亲跑同值）、留痕均为真实执行原始输出——逐项相符；计数与归档历史由提交链自身如实呈现（attempt 1 失实→返工补交的闭环在案）。唯一残留＝上述 ctest 措辞两处（gates-all-note.txt 收尾句与 ad7e8d1e 正文），实质执行真实、标注失准——建议级，不构成粉饰。

## 4.10 留痕完整 —— 通过

- gtest XML×4＋ird-test-report.json×2（schema `ird-test-report/1`，32 条 RP 条目 requirementIds/atIds 全非空）＋构建日志（integration-build.log＋smoke-configure/build.log）＋测试运行日志×4＋门禁双树日志＋gates-hit-set-diff.txt＋gates-all-note.txt＋validate 双日志——**19 件全部在库**（冻结树 ls 实证；attempt 1 阻断 G-1 的 11 件 .log 已由 e9956890 补交归位）。
- 在库四份 XML 顶格属性与亲跑双模式结论一致（196/58 ×2 零失败）；RP\_\* 名集合亲跑 vs 在库 diff＝0；ird-test-report.json 总量（196/196＋58/58 decisive:true）与亲跑一致；gates 双树日志与亲跑逐字同值（27/29 项＋hit-set-diff 结论一致）；validate 双日志与亲跑同值。
- gates-all-note.txt 对 gate-all.ps1 环境失败（Qt 前缀）与 DoD 门禁承担口径（ird_gates 双树直跑＋归一化 diff）如实登记——无掩饰；其收尾句"双目标 ctest 通过"引用失准已归 G-1/F-252（实质执行留痕在库）。
- resultNote/status 更新随收尾段（PIPE §4.7）；`phase-one-validation.log` 未随本任务更新——F-201 已登记的条文与实践脱节，如实注明，不构成新的粉饰。

## 4.11 偷懒/缩水实现扫描 —— 通过

- **①TODO/FIXME/stub/placeholder/未实现/not implemented（六词族＋中文族）**：diff 范围 reporting 全部代码/测试文件新增行全文扫描**零命中**（无豁免条款需列出）。
- **②空实现检测**：本任务零产品源码变更（diff 仅 test/contract\_test/CMake/文档/留痕）；新增测试设施逐一亲读均为实质实现——ScriptedResultSource 脚本表＋ResultEnvelope::make 工厂＋调用计数观测面（非空壳）、ScriptedSectionProvider 四态脚本化＋观测面、FakeArtifactSink 内存态状态机（Staged→Finalized/幂等/冲突）＋调用日志、FakeArchiveWriter 真实临时文件生命周期＋SP-2 校验＋三类故障注入；TestMainReport 9 行 main 契约明文（testkit 监听器安装点——gtest\_main 无法承载，非偷懒）。
- **③逐条证据表**：见 4.4——acceptance 六条每条均有具名测试/命令输出/文件行号，任一条无证据即 fail 的反例不存在。
- **④测试强度**：4.5 两处产品面变异实测红（RP\_STATE\_3／RP\_IDEM\_2／RP\_CONF\_1 具名红）＋还原复绿；断言校验行为与边界（RP\_MDL\_3 同内容异路径同 sha256＋幂等摘要逐字节＋发布记录身份一致；RP\_SCOPE\_1 词表 8 项计数＋order 逐项＋结构完整三重——抽读亲证），IRD\_EXPECT\_IDENTICAL 追溯链谓词消费——非恒真、非"只编译通过"。

## 发现的问题清单

| 级别 | 编号 | 内容 | 处置 |
| --- | --- | --- | --- |
| 建议 | G-1 | 『ctest 通过』声称与在库留痕证据不对应（两处）：①ad7e8d1e 正文"ctest 收口通过"——字面 verify 第 3 条在两轮验收独立复跑中均空转（F-251），在库 19 件留痕无任何 ctest 执行日志；②gates-all-note.txt"双目标 ctest 通过（integration/smoke-test-run-\*.log）"——所引四件日志为直接 exe 执行的 gtest 输出。测试实质执行真实（双模式四跑 196＋58 全绿亲证＋叶子 ctest 2/2），纯声称-引用标注失准，非执行造假 | **已转登 findings.json F-252**（与 F-251 处置②合并——verify 口径按"发现并执行非零个测试"修订后此类声称方有落点；不阻塞合入） |

## 裁决

**verdict ＝ pass**：4.1～4.11 全部通过，无阻断级问题；建议级 1 条（G-1→F-252）不阻塞合入。attempt 1 阻断项 G-1（留痕归档失实）经 e9956890 返工补交后闭环，本验收为全量重跑（非增量）。

## 三元组（记录回传，ACC §3）

- branch：`acc/RPT-T11/2`
- path：`RobWork/doc/industrial-robot-design/traceability/acceptance/RPT-T11-20260920-r2.md`
- commit：（本记录在 evidence 分支上的提交 SHA——commit 自引用不可行，推送后由验收者回传编排者、随 state.acceptanceRecord 登记；收尾按该 40 位 SHA 合并）
