# DIAG-T08 验收记录（attempt 3）

| 字段 | 值 |
| --- | --- |
| 任务 ID | DIAG-T08（≙WP-09-T05，diagnostics 单元：脱敏与崩溃诊断文件——IRedactionService 路径四策略/凭据零记录/降级不放行＋CrashReportWriter 写出设施＋管线/目录/工厂三面接线） |
| 契约 | `doc/industrial-robot-design/tasks/foundation/DIAG-T08.json`（status ready；branch wp09-t08；requirements NFR-SEC-07/PM-17；designRefs units/diagnostics.md §7.6/§7.7/§9.5；knownPitfalls P-DIAG-1；interUnit true） |
| 送验对象（冻结） | `a11f1726d8d4dface4b33a2f3a145c3cbb2a1eee`（验收全程复核：`origin/wp09-t08`＝该 SHA，**无分支漂移**） |
| diff 范围 | `330914a64fc91b003381a628f2985cef66a09070`（base＝state.base＝origin/redesign-main 尖端，与 merge-base 一致）..`a11f1726`，共 2 提交（2f32278d 实现＋a11f1726 单元卡/留痕）、18 文件（+3096/−31） |
| verdict | **pass**（4.1~4.11 全部通过，无阻断级问题；新增建议级登记 2 项＝F-111/F-112，沿 F-081→F-108/F-012→F-109 家族既判先例不阻塞合入；F-110 处置建议本次落空——见 4.8 注记） |
| 验收日期 | 2026-09-15 |
| 验收者 | 独立验收会话（attempt 3，全新上下文；未消费实施叙述；对抗式立场——复现优先于采信，下述构建/测试/门禁/探针/脚本均为验收者亲手执行） |

---

## 一、逐项证据（ACC §4.1~4.11）

### 4.1 改动范围 —— 通过

- diff 文件清单（`git diff --name-status 330914a6..a11f1726`，18 个）逐个比对契约 `allowedFiles`：
  - `RobWork/RobWorkStudio/src/rwslibs/industrialrobot/diagnostics/**`（12 个）：CMakeLists.txt（M——仅注释更新＋`src/Redaction.cpp`、`src/CrashReport.cpp` 增列产品库＋`test/RedactionTest.cpp` 增列 `_test`；`target_link_libraries`/`find_package` 面 diff 零触碰——链接边不变）、include/…/Catalog.hpp（M——`attachRedactionService` 接线声明＋前向声明）、include/…/CrashReport.hpp（A，公共头）、include/…/Factory.hpp（M——接线声明＋析构声明＋成员）、include/…/Logging.hpp（M——`attachRedactionService`/`devTailSnapshot`/`kLogDevTailRingLines`）、include/…/Redaction.hpp（A，公共头）、src/Catalog.cpp（M——exportSafeSummary 双保险）、src/CrashReport.cpp（A）、src/Factory.cpp（M——redactedContextSnapshot＋translate 脱敏整备）、src/Logging.cpp（M——管线步骤②脱敏＋内部通道旁路＋环形缓冲）、src/Redaction.cpp（A）、test/RedactionTest.cpp（A）；
  - `RobWork/doc/industrial-robot-design/units/diagnostics.md`（M，allowedFiles 第 2 项——卡头 v0.9 落位＋§14.4 v0.9 变更记录）；
  - `RobWork/doc/industrial-robot-design/traceability/**`（5 个，allowedFiles 第 3 项）：builds/wp09-t08/ 下 integration-gtest-diagnostics-test.xml（A）、integration-gtest-diagnostics-contract-test.xml（A）、smoke-gtest-diagnostics-test.xml（A）、smoke-gtest-diagnostics-contract-test.xml（A）、ird-test-report.json（A）。
- `forbiddenFiles`（REQUIREMENTS.md / ARCHITECTURE.md）：diff 范围内零命中。
- SA-02（框架零源码修改）：diff 过滤 industrialrobot/doc 前缀后范围外文件计数 **0**（`git diff --name-only … | grep -cv …`＝0）；`industrialrobot/patches/` 零新文件零改动（diff 空输出）。
- **范围越界：零。**

### 4.2 可复现构建 —— 通过（验收者亲跑；双模式零错误）

- **复现现场**：`git worktree add --detach D:/10_Source_Repos/21_robot/.acc-worktrees/diag-t08-r3-repro a11f1726d8d4dface4b33a2f3a145c3cbb2a1eee`（detached @ 送验 SHA，不用分支名）。
- **集成模式（干净构建树，从零重新配置）**：`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DRWS_USE_STATIC_LINK_PLUGINS=ON -DCMAKE_TOOLCHAIN_FILE=<仓库根>/vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`——配置 exit 0 零 CMake Error（Configuring done 45.7s），缓存确认 **`RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`**。worktree 无未跟踪本地辅助件 `RobWorkStudio.ini.template.static`/`ini.shared.in`（F-007/F-062/F-099 在册既有缺陷），自主树补拷至 `RobWork/RobWorkStudio/bin/` 后配置通过（环境修复，不触及送验产物——DIAG-T05/T06/T07 验收同先例）。
  - `cmake --build build --config Release --target sdurws_ird_diagnostics_test sdurws_ird_diagnostics_contract_test`：两测试目标编译链接**零 error C／fatal error／error LNK（grep 计数 0）**，Redaction.cpp/CrashReport.cpp/RedactionTest.cpp 均有编译活动；产物 exe 落位 `build/RobWorkStudio/bin/Release/`。口径沿 DIAG-T05~T07 先例：构建两目标及其依赖链（core＋框架库），未做全树构建——本任务改动面（diagnostics 单元）完全覆盖。
  - **测试执行（验收者亲跑）**：`sdurws_ird_diagnostics_test.exe` → **98/98 PASSED**（exit 0，472 ms）；`sdurws_ird_diagnostics_contract_test.exe` → **9/9 PASSED**（exit 0）——既有 77＋9 零回归＋新增 21 全过，与送验 ird-test-report.json 登记一致。
- **独立冒烟模式（另配临时目录 `D:/10_Source_Repos/21_robot/.acc-worktrees/diag-t08-r3-smoke`，带 vcpkg toolchain——F-002/CORE-T01 G-2 口径）**：`cmake -S RobWork/RobWorkStudio/src/rwslibs/industrialrobot -B <冒烟目录> -DCMAKE_TOOLCHAIN_FILE=.../vcpkg.cmake` 配置 exit 0；`cmake --build ... --config Release` **exit 0 零 error（grep 计数 0）**；冒烟树两测试 exe 亲跑 → **98/98 PASSED＋9/9 PASSED**（exit 0），与送验留痕"双模式 98/98＋9/9"一致。

### 4.3 契约 verify 命令 —— 通过

契约 `verify` 数组仅一条，验收者在复现 worktree A 根目录逐字亲跑：

```
cmake --build build --config Release --target sdurws_ird_diagnostics_test   → exit 0
```

（增量链接产出 sdurws_ird_diagnostics_test.exe，exit 0；产物可直接运行 98/98，见 4.2。）另亲跑 `verify-task.ps1 -TaskFile <契约>` → **verify-task: PASS DIAG-T08**，exit 0（脚本内执行同条 verify 命令）。

### 4.4 acceptance 条目逐项证据 —— 通过（4 条全部有具名测试与行号证据；ird_gates 如实登记基线命中而非虚标通过）

| # | acceptance 条目 | 证据 |
| --- | --- | --- |
| 1 | DT-SEC-1~4 用例通过：凭据不记录（模式扫描零命中）、路径四策略（Keep/Hash/RootOnly/Strip 各自正确）、脱敏失败降级（[REDACTED:redaction-failed]＋DIAG-REDACTION-FAILED，不放行原文、不抛）、目录导出不泄敏感路径 | 亲跑双模式 98/98 全绿（送验 XML 四份同数据：tests=98/9 failures=0 两模式）。**DT-SEC-1**：`DtSec1_CredentialPatternsNeverRecorded`（RedactionTest.cpp:246——password/api_key/token/Authorization Bearer JWT/private_key 五形态混排，输出五段原文逐一 EXPECT_FALSE 零命中(:258-262)＋`[REDACTED:credential:1]` 替换计数在场(:264-265)＋stats 命中计数可观测(:267-268)）；`DtSec1_DeterministicAndIdempotent`（:271——同输入两次逐字节相等(:281)＋已脱敏文本再过脱敏逐字节稳定(:283-284)＝双保险链不变式）；管线半区 `DtSec1_PipelineScanZeroHitsBothTiers`（:535——attach 接线后两 Tier 文件凭据/敏感路径段零命中、替换计数与 RootOnly 形态在场(:559-564)）。**DT-SEC-2 四策略各一**：Keep（:292——redactPath 原样(:299)＋用户名不因 Keep 泄露(:302-303)）、Hash（:308——同路径同 token(:319)/异路径异 token(:320)/形态＝`[PATH-<16 位小写 hex>]` 逐段断言(:321-324)＋消息内嵌路径同样 Hash(:326-329)）、RootOnly（:332——默认策略断言(:337)＋盘符/退化根级文件/UNC/POSIX 四形态逐字(:340-351)）、Strip（:354——redactPath 与消息内嵌两面(:359-365)）。**DT-SEC-3**：`DtSec3_DegradationNoRawNoThrow`（:385——applyRules 虚缝注入抛出：EXPECT_NO_THROW(:395-396)、整条 `[REDACTED:redaction-failed]`(:397)、原文零泄露(:398)、safeSummary 同出口(:401)、DIAG-REDACTION-FAILED 开发诊断在场且消息不回显原文(:405-409)、失败计数与连续失败字面量稳定(:412-414)）；`DtSec3_SilentDegradeWithoutSink`（:417——sink 为空静默降级）。**DT-SEC-4**：`DtSec4_ExportNoSensitivePathsOrUsernames`（:636——目录条目 params 内嵌凭据/敏感路径/用户目录变量，exportSafeSummary 输出三者零命中(:646-648)＋RootOnly 呈现(:649)＋[USER] 在场(:650)＋subject 规范身份保真(:653-654)＋稳定码字段完整(:656)）；`DtSec4_UnattachedExportKeepsBaselineShape`（:659——未挂接＝T04 原样基线、挂接后输出脱敏且两输出不等(:666-671)＝接线非装饰） |
| 2 | DT-LIFE-4 用例通过：磁盘不足降级（Dev 诊断/stderr 摘要），不崩溃、不阻塞调用方 | `DtLife4_DiskFullDegradesWithoutThrowOrBlock`（RedactionTest.cpp:878——ILogFileOps 接缝注入恒返回 false：write EXPECT_NO_THROW 返回 nullopt(:887-889，如实报告不带病成功)；恰一条 `[crash-report-write-failed]`＋DIAG-LOG-WRITE-FAILED Dev 诊断(:893-896)；writer 可复用再次写出仍安全降级且每次恰一条诊断(:899-902，不风暴不递归)）；`DtLife4_OpsExceptionAlsoDegrades`（:905——注入轨二 appendLine 抛异常：同一降级出口无外溢(:914-917)）。两轨注入均为自持替身（FailingFileOps:165/ThrowingFileOps:175——testkit D-10 形态，不链 testkit）；进程存活＝函数如实返回（4.5 探针 2 反向实证该断言非恒真） |
| 3 | 崩溃诊断文件写出设施就位（脱敏后、用户目录——PM-17/AT-11）；OS 级崩溃捕获与应用壳装配归 WP-24-T02（dtb WP-09-T05 协作口径），本任务不越界实现 dump 机制 | `At11_WritesRedactedReportToDirectory`（RedactionTest.cpp:802——写出 EXPECT_NO_THROW 返回路径(:812-814)、文件名 `crash-<epochMs>-<pid>.log` 注入时钟断言(:816-818)、format=ird-crash-report/1 自描述(:822)、六段内容逐段在场(:832-847)、凭据与敏感路径全文零命中(:826-829)＋RootOnly 呈现(:830)、规范身份逐字内嵌(:838-842)、快照行再脱敏双保险(:845-847)、递归检查(:849-850)）；`ExceptionMessageTruncatedTo512Bytes`（:853——值段 ≤512＋[trunc]、凭据先于截断脱敏(:870-875)）；数据源面 `DevTailRingKeepsLast512Lines`（:567——600 行写入后快照恰 512 行、首行为第 89 行（环形覆盖）、写入序、maxLines 截取(:586-593)）。**不越界实证**：CrashReport.hpp/cpp 全文零 MiniDump/SetUnhandledExceptionFilter/signal/SEH/__try 实现（grep 唯一命中＝CrashReport.hpp:22 文件头注释的分工说明"OS 级崩溃捕获…归 WP-24-T02"）；位置＝调用方指定目录（测试传临时目录——D-10 口径，产品路径由 L5 装配给用户目录） |
| 4 | P-DIAG-1 处置：崩溃文件携带的诊断摘要内嵌 core 契约数据——core 冻结 diff 后增量同步 | **编译期**：static_assert×3（RedactionTest.cpp:78-88——`CrashReportInput::openProject` 必须内嵌 `std::optional<core::ProjectId>`、`activeTasks` 成员必须为 `core::TaskIdentity` 五元组、finding 身份＝FindingId；core.md v0.1 基线漂移即编译失败——"基线漂移被机器看见"）。**运行期**：At11 用例 `task[0]=`＋五元组 canonical 逐字、`finding[0]=`＋state token、`project=`＋ProjectId canonical 逐值断言（:838-842）。**哈希同源**：Hash 路径策略的 SHA-256 只经 core ContentDigester（Redaction.cpp:114-127 sha256Hex16——`core::ContentDigester`/`Digest256`，不私设第二哈希路径——CR-02 同源纪律）。**不私改 core**：diff 范围 core/** 零触碰（4.1） |

**ird_gates（如实登记）**：验收者亲跑 `cmake --build build --config Release --target ird_gates`（复现 worktree 集成树）→ exit 1，**命中 7 项与 wp09-t07 合入基线逐项同分布、零新增**：IRD-GATE-SUB×5（evidence/evidence_contract/policy/policy_contract/runtime 测试目标 → sdurws_ird_testkit 直链，F-072/F-051 族既有）＋IRD-GATE-SUB 表外边×1（sdurws_ird_testdata_lint → testkit，F-019）＋IRD-GATE-T1×1（sdurws_ird_testdata_lint 产品目标链 testkit，F-019）；引擎自测行（pass_clean/pass_r4comment＋fail_r1/t1/r5/sub/r3/r4/t2"按预期检出"）非命中。**diagnostics 相关命中＝0**（命中输出全文检索 diagnostics 计数 0）。exit 1＝基线既有命中的预期行为，处置权 WP-01 所有者——与送验 ird-test-report.json gates 段登记一致（如实登记基线命中，未私改门禁、未虚标通过）。

### 4.5 测试真实失败能力（验证验证者）—— 通过（两探针亲手构造、真实变红、还原复绿零残留）

验收者在复现 worktree 临时注入缺陷、重建重跑、逐字还原（`git checkout --`＋`touch` 强制重编规避 F-068 mtime 陷阱；还原后 `git status` 零内容残留、grep `PROBE-TEMP` 计数 0；最终 98/98 复绿）：

- **探针 1（RootOnly 路径策略击穿——DT-SEC-2 观测面）**：`Redaction.cpp` `applyPathPolicy` 的 `case PathPolicy::RootOnly: return rootOnlyForm(candidate);` 改为 `return candidate;`（RootOnly 退化为 Keep）→ `DtSec2_PathPolicyRootOnly` 与 `DtSec4_ExportNoSensitivePathsOrUsernames` **真实变红**（2 FAILED——"D:\data\…\arm.stl 形态"/"secret 段零命中"断言失败）：路径四策略语义与导出双保险均有机器观测面，非恒真。
- **探针 2（崩溃写失败降级击穿——DT-LIFE-4 观测面）**：`CrashReport.cpp` `write` 逐行落盘循环的 `if (!ok) { emitWriteFailure(…); return std::nullopt; }` 改为 `break;`（吞掉写失败、带病返回 file）→ `DtLife4_DiskFullDegradesWithoutThrowOrBlock` 与 `DtLife4_OpsExceptionAlsoDegrades` **真实变红**（2 FAILED——"写失败必须返回 nullopt"/"恰一条降级诊断"断言失败）：降级契约有机器观测面。
- 两探针证明断言校验真实行为边界、非恒真；探针期间中间构建未计入任何结论。

### 4.6 架构红线（AGENTS §5 八条逐条）—— 通过

1. **L2 计算内核零 Qt**：新增 5 文件（Redaction.hpp/Redaction.cpp/CrashReport.hpp/CrashReport.cpp/RedactionTest.cpp）`grep "#include <Q"` 计数 0/0/0/0/0；ird_gates 输出零 diagnostics 命中；diagnostics/CMakeLists.txt 配置期红线守卫（既有）未触碰（diff 仅源文件增列＋注释）。
2. **业务域互链禁止（R-1）**：产品库链接边未变（diff 零 `target_link_libraries` 改动，PUBLIC 依赖仍仅 `sdurws_ird_core`）；ird_gates SUB/R1 零 diagnostics 命中。
3. **跨单元私有头禁止（R-2）**：新文件 ird include 全集＝core 公共头 {Identity.hpp、Digest.hpp、DiagData.hpp（测试）}＋本单元公共头 {Catalog/Confirmable/CrashReport/DiagCodes/Factory/Logging/Redaction}（`include/sdurws/ird/` 公共层，grep 实证）；`detail` 引用计数 0；公共头对 IRedactionService 用前向声明＋pimpl/shared_ptr 保持最小依赖面（Catalog.hpp↔Redaction.hpp 无环——Catalog.hpp 仅前向声明）。
4. **框架零源码修改（SA-02）**：见 4.1——diff 零框架文件、patches/ 零改动。
5. **不做名称前缀拼接/剥离（R-4）**：本任务无名称解析语义（脱敏/崩溃文件与 RobWork 名称面无关）；grep 名称前缀类引用 0 命中；ird_gates R-4 零 diagnostics 命中。
6. **权威唯一（PA-1）**：脱敏与崩溃文件均为 §7.6/§7.7 明文登记的 diagnostics 职责面（非越权承接）；码值只用既登记码——`DIAG-REDACTION-FAILED`（DiagCodes.cpp:451 内置表在案，T03 收编）与 `DIAG-LOG-WRITE-FAILED` 复用（87 码冻结无 crash 专属码，消息以 [crash-report-write-failed] 前缀区分——§14.4 v0.9 口径 4 登记），无私造码值；工厂/目录/管线接线均为各自 owner 面内加法式扩展（attach 注入，不改写 core 契约语义）；无 project/runtime/policy 语义越入。
7. **不可变历史（PA-2）**：脱敏管线与崩溃文件均无任何修订/宿主写路径（脱敏为纯函数——已写行不回溯；崩溃文件只新写用户目录文件）；与修订历史无交集。
8. **不继承 old/**：实现按单元卡 §7.6/§7.7/§9.5 从零编写；grep old/ 零引用。

### 4.7 注释规范（AGENTS §2）—— 通过（公共头全查＋实现全读＋测试全读，抽查面全覆盖）

- **公共头全查（2/2）**：Redaction.hpp（327 行）——文件头设计依据逐条（§7.7/§7.3②/§9.5＋NFR-SEC-07＋AT-11＋契约 acceptance）；"为什么脱敏是纯函数＋绝不抛出"背景专段（三条铁律论证）；Tier 语义专段（Dev＝NFR-SEC-07 全量/User＝全量＋内部模式过滤的分工口径）；P-DIAG-1 处置锚点专段；线程安全专段（模式表只读＋策略写时拷贝快照读＋failureSink 引用生命周期）与确定性专段（纯文本扫描不查询进程环境、替换计数 n 为确定性组成部分）。PathPolicy 逐值语义（Hash 16 位截断避开呈现过滤的论证）、RedactionPolicy 逐字段默认值、IRedactionService 四方法 @param [in] 逐个＋降级语义、RedactionCounters/RedactionStats 逐字段、applyRules 模板方法"为什么留缝"专段（DT-SEC-3 确定性注入缝）——§2.5 高危信息（所有权"非拥有不接管"/线程/错误语义"失败降级 vs Usage fail-fast"/确定性）全在位。CrashReport.hpp（195 行）——文件头"为什么是写出设施而不是崩溃捕获器"专段（WP-24-T02 分工论证）；P-DIAG-1 专段；线程/生命周期/确定性专段；CrashReportInput 逐字段（自由文本由写出侧统一脱敏——"防线不依赖调用方自觉"）；CrashReportWriter 写出语义逐条（位置/文件名/内容六段/逐行接缝/失败降级/noexcept）＋构造四参数 @param 逐个注明方向与所有权。
- **实现文件全读（2/2）**：Redaction.cpp（657 行）——文件头实现口径八项登记（规则执行序/纯十六进制不按令牌/Hash 形态/RootOnly 退化形态/相对路径不处理/maxPreviewBytes 策略位/降级链不递归/正则方言约束）；模式表逐条注释（分组语义、词表来源、误伤防范论证）；replaceEach 迭代器区间推进注释、rootOnlyForm 根结构判定逐步注释、degrade thread_local 防护论证。CrashReport.cpp（264 行）——文件头实现口径五项（文件名/Tier 选择与脱敏面/换行消毒/失败码复用/目录创建 best-effort）；write 六段组装逐步注释（每段脱敏面与不过脱敏的理由——口径 2b 论证）；emitWriteFailure 降级出口逐条。
- **测试文件全读（1/1，RedactionTest.cpp 920 行）**：文件头 acceptance 1~4 映射表＋故障注入面专段（applyRules 虚缝/ILogFileOps 两轨——自持替身不链 testkit）＋线程约束专段；P-DIAG-1 static_assert 组注释；每 TEST 注释锚定矩阵行/需求/口径；断言处中文说明（"键名是结构不是秘密""降级路径绝不放行原文""接线非装饰"等）。21 用例与 ird-test-report.json per-suite trace 逐条对应。
- **修改文件新增段**：Logging.hpp/Catalog.hpp/Factory.hpp 的 attach/snapshot/kLogDevTailRingLines 新声明注释完备（行为/线程/生命周期/与原语义的关系）；Logging.cpp/Catalog.cpp/Factory.cpp 接线处逐步注释（内部通道旁路"为什么"论证——失败行回环；快照切换语义；专用互斥无嵌套论证）。
- **单位/坐标系/角度制式**：本任务无物理量计算面（字节数 maxBytes/512 B、毫秒 epochMs 已逐处标注；无位姿/角度语义），不适用——非缺项。
- 注记（非本任务新增问题）：F-110（Logging.cpp 注释接口名误拼"ILoggFileOps"）原样保留于 :1345（原 :1273 因本任务新增行号偏移）——DIAG-T07 已登记在册，见 4.8 消账注记；本任务新增代码零注释瑕疵。

### 4.8 文档同步 —— 通过

- 单元卡 `units/diagnostics.md` v0.8→v0.9：卡头"构建落位"字段实测刷新（Redaction.hpp/CrashReport.hpp＋实现＋接线三面＋21 用例/双模式 98/98＋9/9 落位清单）＋§14.4 v0.9 变更记录——**实现口径八项按 DTB §5.4 登记**（①规则执行序；②认证方案词表只收 bearer|basic；③纯十六进制长串不按令牌；④Hash 输出 [PATH-<16hex>]；⑤RootOnly 文本口径与卡内示例不一致按文本实现；⑥Tier 语义与消费面；⑦结构化身份字段不过脱敏；⑧崩溃写失败码复用 DIAG-LOG-WRITE-FAILED）＋**接线登记三面**（管线步骤②＋内部通道旁路＋devTailSnapshot；exportSafeSummary 双保险；工厂 redactedContextSnapshot＋translate 脱敏整备——均为 v0.5/v0.8 预登记的落地消账）＋陷阱处置（P-DIAG-1：static_assert×3＋运行期逐值＋SHA-256 只经 core ContentDigester）。**§9.5 接口与 Redaction.hpp 逐字比对一致**（PathPolicy 四值/RedactionPolicy 两字段默认值/IRedactionService 四方法签名——Redaction.hpp:101/118/157/167/180/189 与单元卡 §9.5 原文逐 token 一致）；§7.6 内容六段与 CrashReport.hpp/cpp 逐段对应；§7.7 规则表全量承载（凭据/用户名/环境变量/资源内容/命令载荷/降级/幂等/双保险各行有实现与用例）；§10 DT-SEC-1~4＋DT-LIFE-4 矩阵行与 21 用例逐行有承载；§11 DIAG-T08 行产物（Redaction.*/CrashReport.*）与验证方式（DT-SEC-1~4、DT-LIFE-4）兑现。
- 契约流水线状态：`state.json` currentTask＝DIAG-T08、phase awaiting_acceptance、attempts.accept＝3 在案，翻转归收尾（PIPE §4.7⑤——非实施者职责）。
- 治理脚本：验收者亲跑 `validate-task.ps1 -TaskFile <契约>` → **PASS (1 tasks)**，exit 0；`validate-docs.ps1` → **PASS (20 units, 12 trace entries, 150 task files)**，exit 0；`verify-task.ps1` → **PASS DIAG-T08**，exit 0。
- **findings.json 未关闭条目相关性核对（open 条目 91/110 逐家族排查）**：
  - **F-081/F-102/F-103/F-106/F-108 家族（声称在库而实际未入库）**：本任务 a11f1726 同型再现（声称的各 .log 全部未入库，见 4.9/4.10）——新登记 **F-111**；
  - **F-012/F-100/F-104/F-107/F-109 标题家族**：a11f1726 标题以名词"单元卡"开头同型再现——新登记 **F-112**；
  - **F-110（Logging.cpp:1273 拼写）**：处置建议为"随下次触碰 Logging.cpp 的提交顺手更正"——本任务 Logging.cpp 因接线面被大改（+147 行），**该建议落空**（:1345 拼写原样保留）；继续 open，随下次触碰更正，不重复登记（本验收在 4.7 注记留档）；
  - **F-007/F-062/F-098/F-099/F-101（全新树构建环境链）**：本次复现再次实证（ini 模板补拷后零错误配置构建）——按 DIAG-T05~T07 验收口径一次配通，处置权构建约定所有者，无可随手消账项；
  - **F-019/F-051/F-072/F-097（ird_gates 基线红）**：本次 7 命中同分布再印证，零 diagnostics 命中，处置权 WP-01 所有者；
  - **F-065（留痕日志中文乱码）**：本次 ird_gates 输出复现同现象（环境编码面，与本任务产物无关）；
  - **F-068/F-075（探针 mtime/头文件陷阱）**：本次验收主动以 touch 强制重编规避，未再现误判；
  - **F-009（gate-all 在 worktree 无法全量运行）**：本次再次受限（见 4.9 注记）；
  - 其余 open 条目属 evidence/runtime/policy/io/testkit/core 等其他单元，与本任务无相关性、无静默积压。

### 4.9 commit 质量 —— 通过（附标题风格再现与留痕清单出入——转登 F-111/F-112，不构成数值粉饰）

- `2f32278d` `[WP-09-T08] 实现 diagnostics 脱敏服务与崩溃诊断文件（Redaction/CrashReport）＋三面接线＋21 用例`：标题动词开头、≤50 字；正文四段齐备（What 按文件逐项/Why 逐条 acceptance 与需求映射/验证含双模式 98/98＋9/9·ird_gates 7 命中基线·validate/verify PASS 真实结论/影响面"加法式扩展、未挂接时行为与原语义逐字一致"）；`Acc-Covered: 1,2,3,4` 尾注。分支号 wp09-t08 与标题 [WP-09-T08] 沿契约 note 既定映射口径。
- `a11f1726` `[WP-09-T08] 单元卡 v0.9 实现落位登记与验证留痕（traceability/builds/wp09-t08）`：正文四段齐备、如实说明纯文档与留痕性质；**标题以名词"单元卡"开头非动词**——F-012/F-100/F-104/F-107/F-109 家族同型再现（转登 **F-112**，建议级）。
- **出入（建议级，转登 F-111）**：a11f1726 正文声称入库"双模式构建日志、gtest XML×4、测试运行日志、ird-test-report.json、validate-task.log、verify-task.log、verify-target-build.log、gates-current-wp09t08.log、gates-ird-gates-target.log"——git ls-tree @a11f1726 实证 builds/wp09-t08/ 仅 **5 件**（双模式 gtest XML 四份＋ird-test-report.json），正文声称的全部 .log 零入库（.gitignore:87 全局 `*.log` 吞——F-010 根因再现，未 add -f；ird-test-report.json gates 字段引用 gates-current-wp09t08.log＋gates-ird-gates-target.log 两处且两文件不在库）。**数值主张（双模式 98/98＋9/9、既有 77＋9 零回归、新增 21 用例分族计数、ird_gates 7 命中＝wp09-t07 基线逐码一致零 diagnostics、validate-task/verify-task PASS）经验收者全新 worktree 从零配置亲手复现全部准确，测试结果无粉饰。**
- 注记：正文与 ird-test-report.json 另声称"gate-all 其余 22 项全 PASS"——该项未独立复现（F-009 在册既有缺陷：gate-all 在非主树检出环境受限；且其依赖的全树双模式构建远超本任务改动面）。ird_gates 主体（本任务唯一门禁义务面）已亲手复现一致，该未复现项不掩盖核心口径缺口，如实登记。

### 4.10 留痕完整 —— 字面部分未达成（建议级——F-111；沿 F-081→F-108 家族既判先例不阻断）

- **在库**（git ls-tree @a11f1726，builds/wp09-t08/ 共 5 件）：`ird-test-report.json`（schemaVersion ird-test-report/1，base 与送验范围一致，含 per-target 构建结论、gates 基线对比、selfCheck 探针能力说明、per-suite 逐用例 trace〔21 新用例逐条映射 acceptance/需求/矩阵行，断言值与源码逐项核对一致〕——结构化 resultNote 载体本身完整准确）＋**双模式 gtest XML 四份**（integration/smoke × diagnostics-test 98 用例/diagnostics-contract-test 9 用例，tests=98|9 failures=0 errors=0，DtSec1~4/DtLife4/At11/DevTail/Snapshot/Translate 等新用例全部在案）。
- **未入库而提交正文声称在案（各 .log）**：integration/smoke 构建·配置日志、测试运行日志、validate-task.log、verify-task.log、verify-target-build.log、gates-current-wp09t08.log、gates-ird-gates-target.log——全部缺失（F-010 根因，见 4.9）。**证据本体不缺**：双模式 98/98＋9/9、ird_gates 7 命中、validate-task/validate-docs/verify-task PASS 由验收者从零亲手复现一致（本记录 4.2/4.3/4.4/4.8）。
- `phase-one-validation.log`：历史载体（DIAG-T03~T07 验收记录同结论），本任务留痕按现行惯例落 `builds/wp09-t08/`，ird-test-report.json 即结构化 resultNote 载体——符合"phase-one-validation.log **或** resultNote 已更新"的替代口径。

### 4.11 偷懒/缩水实现扫描 —— 通过

- ①**标记扫描**：新增/修改源码七文件（Redaction.hpp/Redaction.cpp/CrashReport.hpp/CrashReport.cpp/RedactionTest.cpp＋修改的 Logging.cpp/Catalog.cpp/Factory.cpp）`TODO|FIXME|stub|placeholder|未实现|not implemented` 计数全 0（grep -c 实证）；diff 范围内留痕 XML 命中仅为既有用例名（ConfirmableProjectStubTest——DIAG-T05 产物，验证用例数据行非实现缩水标记），无需豁免条目。
- ②**空实现检测**：公共接口全部真实实现——七条规则链（凭据键值/认证方案/环境变量/用户名/路径四策略/令牌形态/内部模式，Redaction.cpp:544-541）、降级链（degrade :255-272——计数＋常量诊断＋整条字面量＋thread_local 防护）、RootOnly 形态（rootOnlyForm :413-478——根结构判定/段拆分/退化形态）、safeSummary 截断（truncateUtf8 :131-140）、崩溃六段组装与逐行落盘（CrashReport.cpp:163-262）、devTailSnapshot 环形缓冲（Logging.cpp 新增段）、exportSafeSummary 双保险、工厂 redactedContextSnapshot＋translate 整备。无空函数体/仅返回默认值的契约函数；RedactionPolicy::maxPreviewBytes 为策略位非行为位已按 §14.4 v0.9 口径 6 登记（非未声明缩水）。
- ③**逐条证据表**：见 4.4——acceptance 4 条全部具名测试＋文件行号级证据，无一条仅凭"已实现"声明。
- ④**测试强度**：断言校验行为与边界（五形态凭据逐一 EXPECT_FALSE＋替换计数在场、Hash token 形态 23 字符逐段、RootOnly/Strip 四形态逐字、降级字面量全等＋失败计数精确 2/3、环形缓冲恰 512 行且首行为第 89 行、UTF-8 边界恰 99+7 字节、崩溃文件名逐字、六段内容逐段、写失败恰一条诊断且 writer 可复用、基线对照 EXPECT_NE）；无恒真断言；4.5 双探针实证非恒真。

---

## 二、裁决

**verdict ＝ pass**

- 4.1~4.11 全部通过（4.10 的留痕入库缺口与 4.9 的标题风格为建议级，沿 F-081→F-108/F-012→F-109 家族既判先例不阻断），无阻断级问题：范围零越界、双模式构建/测试/门禁/脚本全部亲手从零复现成功（集成 98/98＋9/9、冒烟 98/98＋9/9、ird_gates 7 命中基线同分布零 diagnostics、validate-task/validate-docs/verify-task PASS）、红线零命中、注释无缺项、4.11 零命中（新增源码面）、双探针实证测试真实失败能力、无数值粉饰。
- 新增建议级登记 2 项：**F-111**（a11f1726 正文声称入库的各 .log 留痕全部未入库——F-081/F-102/F-103/F-106/F-108 家族第 11 例；.gitignore:87 `*.log` 根因再现＝F-010；ird-test-report.json gates 字段引用两 log 不在库；改进面：双模式 gtest XML 四份已入库；数值主张全部复现准确、无粉饰）；**F-112**（a11f1726 标题名词"单元卡"开头——F-012/F-100/F-104/F-107/F-109 家族再现；同范围 2f32278d 动词开头合规）。
- F-110 消账注记：处置建议"随下次触碰 Logging.cpp 的提交顺手更正"本次落空（接线面大改未顺手更正拼写）——继续 open，随下次触碰更正。
- 环境类既有条目 F-007/F-062/F-098/F-099/F-101/F-019/F-051/F-072/F-097/F-065/F-068/F-075/F-009/F-010 本次复现再次实证（ini 模板补拷后零错误配置构建；ird_gates 基线 7 命中同分布；门禁输出乱码同现象；gate-all 环境受限），已在册、不重复登记。
- 本验收通过的是 **SHA a11f1726d8d4dface4b33a2f3a145c3cbb2a1eee**，非分支引用；验收者未修改任何实施产物（探针注入/还原均在临时 worktree 内、最终零残留）、未向 wp09-t08 推送任何提交。

## 三、问题清单

| 级别 | 描述 | 处置 |
| --- | --- | --- |
| 阻断 | 无 | — |
| 建议 | **F-111**：a11f1726 正文声称入库"双模式构建日志＋测试运行日志＋validate-task.log＋verify-task.log＋verify-target-build.log＋gates-current-wp09t08.log＋gates-ird-gates-target.log"等 .log 留痕，实际 builds/wp09-t08/ 仅 5 件（双模式 gtest XML 四份＋ird-test-report.json）——F-081/F-102/F-103/F-106/F-108 家族第 11 例；.gitignore:87 全局 `*.log` 吞且未 add -f（F-010 根因再现）；ird-test-report.json gates 字段引用两 log 不在库。数值主张全部复现准确、无粉饰；双模式 XML 四份已入库 | 已转登 findings.json（F-111，open）；修复方向同 F-103/F-106/F-108（gitignore 例外/强制 add -f/改 .txt 后缀）；不阻塞合入 |
| 建议 | **F-112**：a11f1726 标题"[WP-09-T08] 单元卡 v0.9 实现落位登记…"以名词"单元卡"开头非动词——F-012/F-100/F-104/F-107/F-109 家族再现（2f32278d 动词开头合规） | 已转登 findings.json（F-112，open）；建议模板固化标题口径（F-107/F-109 处置建议连续落空，固化诉求第三次提出）；不阻塞合入 |
| 注记 | **F-110 消账落空**：本任务 Logging.cpp 接线面大改（+147 行）但 :1345（原 :1273）"ILoggFileOps"拼写未顺手更正——已在册条目，不重复登记，继续 open | 随下次触碰 Logging.cpp 的提交顺手更正 |

## 四、验收者环境说明

- 复现 worktree（worktree A）：`D:/10_Source_Repos/21_robot/.acc-worktrees/diag-t08-r3-repro`（detached @ a11f1726）；集成树从零配置（`RWS_BUILD_INDUSTRIALROBOT=ON`〔缓存 grep 确认〕、`RWS_USE_STATIC_LINK_PLUGINS=ON`、vcpkg toolchain、Qt `CMAKE_PREFIX_PATH`；`RobWorkStudio.ini.template.static`＋`ini.shared.in` 自主树补拷至 worktree 内 `RobWork/RobWorkStudio/bin/`——F-007/F-062/F-099 既有缺陷的环境修复，不触及送验产物）。冒烟树：`D:/10_Source_Repos/21_robot/.acc-worktrees/diag-t08-r3-smoke`（独立目录＋vcpkg toolchain）。
- 工具链：VS 2022（MSVC，生成器 Visual Studio 17 2022，-A x64）、vcpkg 经典模式（gtest 1.18.0 @ installed/x64-windows）、pwsh（validate-task/validate-docs/verify-task.ps1）。
- 探针注入/还原均在 worktree A 内：两轮注入（applyPathPolicy RootOnly→Keep、CrashReport 写失败 break 吞错）各自重建重跑取证后 `git checkout --` 还原＋touch 强制重编（规避 F-068 mtime 陷阱），最终 `git status` 零内容残留、grep `PROBE-TEMP` 计数 0、98/98 复绿。
- 分支漂移检查：`origin/wp09-t08`＝a11f1726（本地缓存远程引用；验收期间 fetch 因网络 SSL 失败一次，推送 evidence 分支时重试）。
- 验收记录与 F-111/F-112 登记落 `acc/DIAG-T08/3` 分支（基于 origin/redesign-main @ 330914a6）并推送；验收者日志随临时 worktree 清理，本记录内数值均为亲测。
