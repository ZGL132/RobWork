# POL-T05 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | POL-T05——实现策略端口与上下文（④端口所有者，≙WP-07-T05） |
| 送验对象（冻结） | `1b54f7131f4808d56089a143e0d4370677a48591`（分支 wp07-t05 尖端；验收前漂移检测：`origin/wp07-t05` == 该 SHA，无漂移） |
| commit 范围 | `9223f6361e0043a5fe3fac8b44fedf706bf136e4..1b54f7131f4808d56089a143e0d4370677a48591`（3 提交：6eea8ba7 实现＋477491b7 留痕＋1b54f713 单元卡 v0.6） |
| 验收日期 / attempt | 2026-09-13 / 1 |
| **verdict** | **pass**（4.1~4.11 全部通过；无阻断级问题；建议级 2 项转登 findings.json F-079/F-080，不阻塞合入） |
| 独立性声明 | 全新上下文，只消费契约/单元卡/代码/留痕产物与既有验收登记；未读实施叙述。补偿性对抗动作＝worktree A（detached @ 冻结 SHA）亲手全新配置双模式构建＋全量测试（112/112×2）、两个独立缺陷探针（均为验收者自行设计的 .cpp 单行变异，送验报告无 mutation 自证段，位点完全独立：①decode 失败转发码面替换②内容版本剔除缓存键）、ird_gates 集成树亲跑（5 项既有命中与送验留痕一致）、单元卡 §3.3/§9.1/§9.7/§12/§15.4 v0.6 权威原文与实现逐点比对、留痕 11 件逐份清点 |

## 逐项证据（4.1~4.11）

| # | 检查项 | 证据 |
| --- | --- | --- |
| 4.1 | 改动范围 | `git diff --name-status 9223f636..1b54f713`：20 文件＝8 个代码/构建文件（policy/CMakeLists.txt＋include Contexts.hpp/PolicyPort.hpp/Errors.hpp＋src PolicyPort.cpp/Errors.cpp＋test PolicyPortTest.cpp/PolicySetTest.cpp）全部位于 `policy/**`（allowedFiles）＋11 个留痕文件（traceability/builds/wp07-t05/，DoD §4.2 留痕口径，属 `traceability/**`）＋units/policy.md（allowedFiles 显式列出，+5 行＝§9.6 表尾补登两码＋§15.4 v0.6 行）；forbiddenFiles（REQUIREMENTS.md/ARCHITECTURE.md）零触碰（路径过滤 diff 为空）；框架源码（RobWork/RobWorkSim/RobWorkStudio 非 industrialrobot 部分）零触碰；patches/ 无新文件——SA-02 零框架源码改动 |
| 4.2 | 可复现构建 | worktree A（`git worktree add --detach` @ 1b54f713，全新 build 树）：按 F-007/F-062 已登记口径两步处置（①配置命令补 `-DCMAKE_TOOLCHAIN_FILE=…/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`——自主树 CMakeCache 读取；②复制主树 git 未跟踪的 RobWorkStudio.ini.template.static 与 RobWorkStudio.ini.shared.in 至 wtA RobWorkStudio/bin/——`git status` 核验未改动任何被跟踪产物）→ 配置 exit 0 且缓存确认 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`（grep 实证）；`cmake --build build --config Release --target sdurws_ird_policy_test` exit 0、`: error`/`error C` 零行（日志唯一告警＝core/Units.cpp C4099 多实例——F-071 既有存量在全新树再现，本 diff 未触碰 core；policy 面零告警）；测试 112/112 PASSED（验收者亲手运行，`[==========] 112 tests from 21 test suites ran.`）。冒烟：独立临时目录 acc-tmp/pol-t05-smoke＋vcpkg toolchain 配置构建 exit 0 零 error；测试 112/112 PASSED（均验收者亲手执行） |
| 4.3 | verify 命令 | 契约 verify（单条）＝`cmake --build build --config Release --target sdurws_ird_policy_test`——在 worktree A 集成树亲手独立复跑 exit 0（即 4.2 集成构建，逐字命令）；附带运行测试可执行文件 112/112 全绿；`verify-task.ps1` 实跑 PASS、`-DryRun` PASS（RepoRoot 归约至 wtA、dryrun 命令行与契约逐字一致）；validate-task PASS、validate-docs PASS（20 units, 12 trace entries, 150 task files） |
| 4.4 | acceptance 逐条 | ①"注入接口与端口契约用例通过"：验收者亲手双模式各 112/112（XML tests=112 failures=0×2）；注入接口面＝套件 PolicyPortContexts（NameContextExpectedErrorYieldsNulloptIdempotent_CR_04：正向/反向双向查询、映射外 nullopt 不猜测、幂等、nameMapContentIdentity 转发——与 foundation-api-diff.md CR-04 适配表逐项一致；CallContextReflectsHostState：取消/存活标志投影）；端口契约面＝套件 PolicyPort 10 用例（发布成功=管线直调逐字段相等、存储缺失/版本未指定走 POLICY-OBJECT-MISSING 诊断轨不抛、全零编址违约 fail-fast PolicyObjectInvalid、字节截断转发 EncodingInvalid＋cause 携带 what() 全文、未来版本转发 POLICY-SCHEMA-VERSION-FUTURE、内容非法管线全量诊断不增不减转发、backend 描述符值语义逐字段相等、装配守卫 PortAssemblyIncomplete、4 线程并发只读安全）。②"缓存一致（POL-ID-1 联动）"：套件 PolicyPortCache 3 用例（同键重复解析逐字段相等＋内容身份相等；同对象双版本分键互不串值＋阈值 0.02/0.03 SI 真值与身份均分离——POL-ID-4 联动；失败结果亦记忆化）＋4.5 探针 2 证实"版本进键"机制被测试真实钉住。③"CR-04/P-POL-9 处置约束"：Contexts.hpp 全文核查＝仅三个纯虚最小接口、零适配器/工厂/默认实现；CR-04 映射写入头注释与 traceability/foundation-api-diff.md §CR-04 登记表（tryObjectId↔resolveObjectId、tryRuntimeName↔resolveRuntimeName、nameMapContentIdentity↔nameMapIdentity、Expected→nullopt；IPolicyBytesSource↔IProjectQueryPort::tryObject 一比一）逐字一致；P-POL-9 宿主＝ui（governance-log.md §1.5 P-POL-9 行 **closed**，2026-09-10 定稿 WP-10-T07）无漂移；零 runtime/project 编译依赖＝include 白名单机械扫描（本记录 4.6）＋BuildRedLineTest NoCrossUnitInclude 在 112 用例中保持通过＋ird_gates 对 policy 面零命中。ird_gates（WP-01-T01 已建成，如实执行）：验收者亲跑 `cmake --build build --config Release --target ird_gates`——exit 1、命中 5 项（IRD-GATE-SUB×4：evidence_test/evidence_contract_test/runtime_test→testkit＋testdata_lint→testkit 表外边；IRD-GATE-T1×1：testdata_lint 产品目标链接 testkit；归属＝F-019 家族），全部不在本任务 allowedFiles 内＝base 既有、policy 面零新增，与送验 ird_gates-current-wp07t05.log 逐字一致；送验 base 快照 log 同 5 项（diff 仅扫描根路径一行——GATE_DELTA_ZERO 证实）；引擎自测 9 项全部按预期；ird-test-report.json gateAll 节含 base 对照方法 |
| 4.5 | 测试真实失败能力 | 验收者自行设计两探针（送验报告无 mutation 自证，位点完全独立；各经注入→重建→运行→还原→重建→`git status` 零残留核验）：**探针 1**（PolicyPort.cpp 第五步 catch 分支 `registryCode(e.code())`→`kObjectMissingCode`——转发码面替换）：CorruptBytesForwardedAsDiagnosticNoThrow_PortContract 与 FutureSchemaBytesForwardSchemaDiagnosticNoThrow_PortContract 如期变红（InvalidPolicyContent 直通面保持绿——精确归因）；**探针 2**（第三步缓存键 `*request.expectedVersion`→`core::ContentVersion{}`——版本剔除出键）：CacheKeyIncludesContentVersionSeparatesEntries_POL_ID_1 等三用例如期变红（失败结果经空版本键污染共享条目——证实 POL-ID-1 分键断言真实敏感）；还原后 112/112 复绿 |
| 4.6 | 架构红线 | AGENTS §5 八条机械扫描＋走读：①零 Qt——新/改六文件 `#include.*(Qt\|Q[A-Z])` 零命中；②R-1 无业务域互链——include 面全量清单 ∈ {core/*, policy/*}（sort -u 实证），policy 与 modeling/kinematics 等业务域零 include；③R-2 无跨单元私有头——同上白名单扫描（{core, policy}，与 BuildRedLineTest 声明一致）；④SA-02 框架零改动——4.1 已证；⑤R-4 无名称前缀拼接——PolicyPort.cpp 全文无名称拼拆逻辑（名称上下文仅经 Contexts.hpp 接口声明转发语义，实现不消费）；⑥权威归属——诊断码仅产出建议码原文（PA-1 注释声明）、端口不实现 project/runtime 语义、sourceVersion 填充点为 POL-T04 v0.5⑧ 预留的 PA-1 落点兑现；⑦PA-2 不涉及（端口只读，无修订写路径）；⑧不涉及 old/ 恢复。链接面：policy→sdurws_ird_core＋框架基线库（R-5 例外既有登记）；测试→policy＋GTest；本任务未新增任何链接边 |
| 4.7 | 注释规范 | 公共头全查：Contexts.hpp（225 行）——文件头设计依据（§3.3/§9.7/§3.1/§12＋CR-04/CR-03＋P-POL-9＋ARC-05/UX-08/KIN-13）、"为什么是注入而不是链接"背景、CR-04 适配映射表、实现纪律、三接口逐方法 Doxygen（@param 方向/返回 nullopt 语义/线程约束"并发只读安全"/错误分类"环境事实不抛"）；PolicyPort.hpp（391 行）——类注释含装配契约、记忆化语义（POL-ID-1 机制）、错误矩阵六项、线程安全（mutable 互斥量/不可复制）、每个字段注释（借用契约/所有权/shared_ptr 不完整类型语义）；实现文件抽查 ≥3：PolicyPort.cpp（全读——七步流程每步"做什么/为什么/失败走向"、匿名命名空间单源纪律、成员遮蔽全限定说明）、Errors.hpp/.cpp 增量（表尾追加注释含与 PolicyObjectInvalid 的分工说明、16 值计数同步）、CMakeLists.txt（增量中文注释）、PolicyPortTest.cpp（全读——文件头替身边界声明 POL-TD-1、逐用例需求/AT 追溯）。§2.5 高危信息（单位/坐标系/线程/所有权/错误语义/确定性）在注释中齐备，无缺项 |
| 4.8 | 文档同步 | units/policy.md §15.4 v0.6 变更记录（+1 行）登记实现口径①~⑦，验收者逐点与实现/权威章节比对一致：①错误矩阵六项落点＝PolicyPort.cpp 步骤注释逐项对应；②记忆化语义（成功失败都入表/先入为准/无淘汰）＝实现与头注释一致；③装配口径差异（nameContext 不进构造参数）——PolicyPort.hpp 类注释与 v0.6 ③逐字对应，DTB §5.4 登记闭环（非"代码先行、文档失实"）；④错误码 15→16＋§9.6 补登 POLICY-OBJECT-MISSING/POLICY-PORT-ASSEMBLY-INCOMPLETE（diff 行 960-966 实证）；⑤CollisionBackendDescriptor 归属＝§3.1 组成表；⑥前置声明冻结④端口接口；⑦acceptance 3 处置兑现。§12 POL-T05 行（`Contexts.hpp`、`PolicyPort.hpp/.cpp`＋PolicyProvider 记忆化；验证列"注入接口与端口契约用例通过"＋POL-ID-1）与交付一致；契约 status 翻转归收尾（PIPE §4.7⑥）不算偏差。findings.json 未关闭条目（64 条 open）逐条核对相关性：F-019/F-072（ird_gates 既有 5 命中——本次亲跑再证实仍在，处置权在 cmake/＋DTB §4.5 登记册，不在本任务 allowedFiles，不可顺手消账）；F-004（MOC 空转——本次集成构建日志再现 Automatic MOC 行）；F-007/F-009/F-062（worktree/全新树环境前置——本次按已登记口径处置：toolchain/Qt 前缀＋两件 ini 模板复制）；F-008（RWSimulatorPlugin 全新树失败——本次构建目标面为 sdurws_ird_policy_test 未触及，不影响本任务复现）；F-071（core C4099——全新树再现多实例）；F-065（留痕日志中文双重编码——送验 smoke-build.log 中文段"正在生成代码"双重编码＋gate-all.log 门禁段混合编码再现，file 实证 Non-ISO extended-ASCII，证据要素 exit code/目标名/计数可读不受阻）；F-012 家族＋F-025/F-041 家族在送验提交有新例（转登 F-079/F-080）；其余 open 条目（F-005/006/010/012/015~018/020/023~035/036~048/051/055/056/058~060/063/064/066~070/073~078）归属 core/testkit/evidence/runtime/project/execution/ui 单元或治理脚本，与本任务 diff 面（policy 端口/注入接口）无交集，不可顺手消账；P-POL-9（closed）/CR-04（closed）无漂移 |
| 4.9 | commit 质量 | 3 提交逐一核查：6eea8ba7 标题"[WP-07-T05] 实现策略端口与上下文（④端口：Contexts.hpp＋PolicyPort 记忆化 Provider）"——前缀＋动词"实现"开头✓；正文四段齐备（What 逐文件/Why 对应契约 acceptance＋设计依据/验证 五项/影响面 含装配差异登记预告）；验证结论与验收者实测一致（双模式 112/112✓、ird_gates base 对比零增量✓——本次亲跑证实）。477491b7 标题"[WP-07-T05] 登记 POL-T05 验证留痕（…）"——动词"登记"开头✓；正文四段齐备；计数瑕疵转登 F-080（见问题清单）。1b54f713 标题"[units] policy.md v0.6：POL-T05 实现落位登记（DTB §5.4 增量修订）"——前缀后名词短语开头（F-012 家族同型，转登 F-079，纯风格）；正文四段齐备（What 登记口径①~⑦/Why DTB §5.4＋outputs traceability-update/验证 validate-docs PASS 如实/影响面 forbiddenFiles 边界声明）。未发现粉饰：全部数值主张（112/112、5 项命中既有、GATE_DELTA_ZERO）经验收者亲手复现证实；送验 integration-build.log 为主树存量增量树全量构建（F-036/F-037 先例口径），本验收以全新树目标面从零构建补强（4.2），提交正文"全量 Release 构建零 error（exit 0）"声明与日志相符、无"最强口径"式失准措辞 |
| 4.10 | 留痕完整 | 送验留痕 11 件 git diff --name-status 确认全部入库：冒烟配置/构建/测试日志×3＋集成构建/测试日志×2＋gtest XML×2（integration/smoke 均 tests="112" failures="0" errors="0"，与验收者亲手复跑一致）＋ird-test-report.json（schema ird-test-report/1）＋gate-all.log（16/17——唯一失败＝ird_gates 既有 5 命中）＋ird_gates base/current 对比 log×2；ird-test-report.json 含逐用例 15 条 trace 登记（suite/设计锚点/modes/trace）、既有 97 用例承接说明（含两张全表锚定用例同步 16 值说明）、contractVerifyCommand、gateAll 节（base 对照方法＋GATE_DELTA_ZERO）、notExecuted=[]——即 resultNote 载体已更新；smoke-build.log 为 Release 配置（22 处 Release 路径实证，F-055 口径合规）；phase-one-validation.log 未更新——沿 TK-T10/TK-T11/POL-T03/T04 先例（foundation 任务以 traceability/builds/<task>/ 证据包替代） |
| 4.11 | 偷懒/缩水扫描 | ①diff 范围 `TODO\|FIXME\|stub\|placeholder\|未实现\|not implemented` 扫描零命中（grep -ic 计数 0）；②空实现检测：Contexts.hpp 三接口纯虚＝header-only 接口契约的设计意图（§3.3/§9.7 权威，P-POL-9 明令零适配器）；PolicyProvider 四方法均有实质实现（错误矩阵六项/记忆化/装配守卫/值转发——4.5 探针证实行为真实）；CollisionBackendDescriptor/PolicyResolution 的 operator== 逐字段实现；③逐条证据表＝本记录 4.4 行（acceptance 1～3 每条注明具名测试/亲跑输出/文件行号）；④测试强度：断言校验行为与边界（码面/subject 绑定/cause 内容/逐字段相等/并发一致/版本分键/SI 真值 0.02≠0.03），非恒真、非"只编译通过"——4.5 双探针实证断言会红 |

## 问题清单

**阻断级：无。**

**建议级（已转登 traceability/findings.json）：**

- **F-079**：1b54f713 提交标题"[units] policy.md v0.6：POL-T05 实现落位登记…"前缀后以名词短语开头，非动词开头——F-012/F-023/F-024/F-026/F-027/F-030/F-034/F-037/F-038/F-039/F-040/F-042/F-044/F-045 家族同型再现（[units] 前缀形态，同 F-037/F-039/F-044）；正文四段齐备、验证结论与留痕一致（验收者逐字复现），纯风格瑕疵不构成阻断。
- **F-080**：477491b7 提交正文称"新增 10 件留痕"实际列出并入库 11 件（F-025/F-041/F-077 计数失实家族同型再现；与 F-077 不同——本例文件清单完整且全部真实入库，仅总数少报 1 件，方向保守、无粉饰）。数值主张本身（112/112、5 项既有命中）准确。

## 验收者环境说明

- 复现现场：`git worktree add --detach D:/acc-tmp/pol-t05-a 1b54f7131f4808d56089a143e0d4370677a48591`（detached @ 冻结 SHA，非分支名）；集成树全新配置于 wtA/build（缓存确认 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`）；冒烟树独立于 D:/acc-tmp/pol-t05-smoke（带 vcpkg toolchain 参数）。
- 记录回传：本记录与 findings.json F-079/F-080 转登写入 worktree B（`git worktree add -b acc/POL-T05/1 D:/acc-tmp/pol-t05-b origin/redesign-main`），commit 后推送 `origin acc/POL-T05/1`——三元组 {branch: acc/POL-T05/1, path: RobWork/doc/industrial-robot-design/traceability/acceptance/POL-T05-20260913.md, commit: 见推送后输出}。
- 环境缺口处置（F-007/F-062 既有，两步）：①配置命令补 `-DCMAKE_TOOLCHAIN_FILE=vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`（自主树 CMakeCache 读取）；②复制主树 git 未跟踪的 RobWorkStudio.ini.template.static 与 RobWorkStudio.ini.shared.in 至 wtA RobWorkStudio/bin/——未改动任何被跟踪产物。
- 全部命令（配置/构建/测试/verify/ird_gates/validate-task/validate-docs/verify-task DryRun+实跑/双探针注入还原）由验收者本会话亲手执行；探针注入与还原均经 `git status` 零残留＋112/112 复绿双重核验。
- 未合入、未修改实施产物、未向任务分支 wp07-t05 推送任何提交（送验对象冻结于 1b54f713）。
