# IO-T02 验收记录（acceptance record）

| 字段 | 值 |
| --- | --- |
| 任务 ID | IO-T02（SafePath＋BudgetGuard：路径穿越/预算防护；unit=io；masterWp=WP-E；≙DTB §2.12 WP-11-T03；requirements=NFR-SEC-01/NFR-SEC-02/PM-05；outputs=implementation＋unit-tests＋traceability-update） |
| 送验对象（冻结） | eaa6aa9975e0da756676e00177845104fdc1e2eb（分支 wp11-t02；diff 范围＝ce90896fa7c1fc43895148e3d302ca823d26f96f..eaa6aa99＝state.base..headSha，2 个提交：aafb66ac 实现＋eaa6aa99 文档/留痕） |
| 验收尝试 | attempt 1（分支漂移检测：`git fetch origin` 后 `git rev-parse origin/wp11-t02`＝eaa6aa9975e0da756676e00177845104fdc1e2eb 与送验 SHA 全 40 位逐位一致，**零漂移**；`git branch --list "acc/IO-T02/*"` 空——分支名此前不存在） |
| 验收环境 | 全新 detached worktree（`git worktree add --detach` @ eaa6aa99＝`D:/10_Source_Repos/21_robot/acc-tmp/io-t02-a`）从零复现：集成 build 树（worktree A 内 `build/`）与独立冒烟 build 树（`D:/10_Source_Repos/21_robot/acc-tmp/io-t02-smoke`）均为本验收新建；记录回传 worktree（acc/IO-T02/1 基于 origin/redesign-main@ce90896f）。pwsh 7＋MSVC 2022 x64＋vcpkg 经典模式（主仓库 `RobWork/vcpkg`——git 未追踪，worktree 天然不含）。环境前置按 F-007/F-098 既有登记处置（沿用 IO-T01 验收记录实测口径，非实施缺陷）：①`CMAKE_TOOLCHAIN_FILE` 指主仓 vcpkg；②`CMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`＋`CMAKE_CXX_FLAGS=-DNOMINMAX -DWIN32_LEAN_AND_MEAN -DWIN32`；③`RobWorkStudio.ini.template.static`＋`bin/none/` 被 gitignore 吞（worktree 实测缺文件）——自主树补拷后配置零错误。首配曾因缺上述前置报 `Could NOT find Boost`（预期环境现象，补前置后通过）。验收全程只消费产物与文档（契约/单元卡/留痕/先行验收记录/findings.json），未读取实施者过程叙述（ACC §2） |
| verdict | **fail**（阻断级 1 项：**4.10 留痕未随冻结对象落位**——提交正文与 `ird-test-report.json` 按名引用的 8 份日志类留痕被 `.gitignore:87` 全局 `*.log` 规则吞没，仅存在于实施侧工作树、不在送验对象内；CORE-T02 attempt-1 同型判例。4.1～4.9、4.11 全部通过——构建/测试/门禁/脚本五项复现全绿，实现本体质量良好；建议级 4 条转登 findings.json F-182～F-185。fail 仅涉留痕入库，返工面小：补证后按协议重新全量验收） |
| 验收日期 | 2026-09-17 |

## 4.1 改动范围 —— 通过

- `git diff --name-status ce90896f..eaa6aa99` 实证 **15 个变更文件**：
  - 产品代码 9 件（均在 `industrialrobot/io/` 下）：`io/CMakeLists.txt`（M）、`IoFwd.hpp`（M，增补 `IoResult<void>` 特化）、新增 `SafePath.hpp/.cpp`、`Budget.hpp/.cpp`、`IoDiagnostics.hpp/.cpp`、测试 3 件 `SafePathTest.cpp/BudgetTest.cpp/IoDiagnosticsTest.cpp`（A）；
  - 文档 1 件：`units/io.md`（M，+2/−1——§1.2 现状行＋§15.5 v0.4 行）；
  - traceability 4 件：`builds/wp11-t02/` 下 `integration-gtest-io-test.xml`、`smoke-gtest-io-test.xml`、`integration-gtest-io-test-report-target.xml`、`ird-test-report.json`（均 A）。
- **逐文件比对契约 allowedFiles 三条通配**（`industrialrobot/io/**`、`units/io.md`、`traceability/**`）：**全部在册，零越界**。
- forbiddenFiles（REQUIREMENTS.md／ARCHITECTURE.md）**零触碰**（diff 无该两文件）。
- 无未登记框架源码改动：diff 零框架路径；`patches/` 零新文件（SA-02）；ird_gates SA-02 面实跑零新增命中（见 4.6）。

## 4.2 可复现构建 —— 通过（全新树从零双模式）

- **集成模式**：worktree A 从零配置 **exit 0**（`-- Configuring done (39.5s)`，缓存实测 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`），`cmake --build build --config Release --target sdurws_ird_io_test` **exit 0**、`: error` 计数 0（日志唯一 "error" 字样＝diagnostics 源文件名 `Errors.cpp`，行 122——非编译错误），`sdurws_ird_io_test.exe` 生成并自拷 gtest DLL（CMake POST_BUILD 实测输出）。
- **独立冒烟模式**：独立临时目录从零配置（带主仓 vcpkg toolchain——AGENTS §4.1 口径）＋全量构建 **exit 0、`: error` 计数 0**，`io/Release/sdurws_ird_io_test.exe` 生成。
- 全框架全量集成构建未单独重跑——F-008 基线框架缺陷判例（PRJ-T13～T16 四次独立实证），本任务 diff 零框架触碰（4.1），不归本任务。

## 4.3 契约 verify 命令 —— 通过

- 契约 verify＝`cmake --build build --config Release --target sdurws_ird_io_test`：①验收者直接执行（4.2 集成构建即该命令）exit 0；②`verify-task.ps1 -TaskFile tasks/foundation/IO-T02.json` 实跑 **PASS**（脚本解析仓库根＝worktree A、命令逐字一致、构建零 error）；③`validate-task.ps1` **PASS (1 tasks)**。
- 测试执行（验收者亲手，非采信留痕）：集成 `sdurws_ird_io_test.exe` **exit 0、20/20 PASSED**（XML 实测 `tests="20" failures="0"`）；冒烟同 exe 形态 **20/20 PASSED**。与提交在库三份 XML（各 `tests="20" failures="0"`）交叉一致。

## 4.4 契约 acceptance 逐条证据 —— 通过（5/5 有对应证据；证据表见 4.11③）

1. **V09/V10/V11 全绿**：`IoSecTest` 6 用例全绿（双模式）——P-4 非法表 18 行＋2 边界（段长 256>255、总长 4097>4096）逐条拒绝＋正确码（ESCAPE/PACK-ENTRY/RESERVED/TOO-LONG）；P-5 非法表 10 行（含 `objects/../../evil.dll` 净逃逸、绝对/UNC 形态、保留名/尾点）＋SP-3 区外首段拒绝；P-5 合法行（消解后回落、大小写折叠、catalog 区）放行；`SameProjectAltPaths` 三拼写等价键相等＋EquivKeyMutex 互斥（heldCount=1）＋释放复用＋异项目不互斥；`SymlinkJunction` 文件/目录 symlink＋junction 三形态一律 IO-SEC-PATH-SYMLINK（真实 reparse point 断言，非替身）＋offender path 参数＋区内真实实体放行。
2. **V06 全绿**：`StreamBudgetRowsOverBudgetTriggersIoSecBudgetRowsWithBoundedMemory`——真实生成 10⁶ 行 CSV、行预算收紧 10⁵、恰在第 100001 笔检查点触发 IO-SEC-BUDGET-ROWS（三要素 actual=100001/limit=100000/unit=count）、峰值提交内存增量 < 文件大小/2（K32GetProcessMemoryInfo 实测采样）、账本 used 停在限额（失败笔不入账）。矩阵行名（IoCsv/StreamBudget）↔ gtest 套件映射经 `ird-test-report.json` testSuites 字段显式承载——CSV 读写器本体随 IO-T03，行检查点以最小流式路径承载（测试文件头范围声明，与 §11.2 IO-V06 行观测点一致）。
3. **预算三要素＋双保险＋账本可观测**：`BudgetTriadFileDepthTotal...`（FILE/DEPTH/TOTAL 分别触发，actual/limit/unit 三要素齐全、单位随维 bytes/levels）；`DeclaredVsActualDoubleBooking...`（预检笔＋展开检补差＝max(声明,实际)、失败笔状态不变）；`ledger()` 快照断言（维度按声明序、关闭后可取终值）；scope 树回收（子账回收至父、回收触发父超限则父子状态均不变）。
4. **例外不扩大化**：`ExceptionNotWidened...`——P-1 经 symlink 入口放行（equivKey＝实体直连同键）、P-7 允许尚不存在、不存在→IO-RES-NOT-FOUND（资源码非安全码）；反例钉住同一路径换 P-5 角色即拒。
5. **P-IO-4/P-IO-6/P-IO-1 处置**：P-IO-4＝`ProductDefaultValuesPinnedToCardSection451` 16 维 Draft 档位数值逐维钉住＋`PackImportChannelHardened...` 四维禁放宽/tighten 只收紧 fail-fast；P-IO-6＝`IoDiagCodeTable` 三用例（§9.12 全集 55 码机械比对、真实 StableCodeRegistry 注册含 seal/重复注册拒绝/内置 87 码合注册顺序无关 manifest）——码值逐字取 §9.12 建议值，**未私定**；diagnostics 内置表收编确认＝所有者环节（实现侧不扩编 diagnostics 单元文件，allowedFiles 红线约束下的正确形态），随本记录提请所有者裁决。P-IO-1＝SafePath/BudgetGuard 以 `std::filesystem::path` 接收解析基点、io 公共头零 project 类型（头红线测试含 `ird/project` 禁止形态扫描＋实现通读证实）。

## 4.5 测试真实失败能力 —— 通过（两枚实现侧失效注入均即红；还原后逐字节一致、20/20 恢复）

- **探针 1（饱和加法有牙齿）**：`Budget.cpp` satAdd 退化为普通 `a+b`（L136 注入标记）→ 重建 → `SaturatingAccumulatorNeverWraps` **FAILED**（回绕假象被捕获）。
- **探针 2（大小写折叠有牙齿）**：`SafePath.cpp` foldCase 条件改 `if (false)`（L184 注入标记）→ 重建 → `PathTraversalP5LegalRowsAccepted` **FAILED**（relKey 折叠断言捕获；注：`SameProjectAltPaths` 在此注入下仍绿——既有目录的 weakly_canonical 解析实盘大小写掩盖折叠缺失，折叠行为由 P-5 relKey 断言独立守卫，无恒真面）。
- 两探针后 `cp 备份还原`＋`git status`：io 源码树与送验对象逐字节一致（仅余验收者自身 gtest XML 两个未跟踪文件），重建后全量 **20/20 PASSED** 恢复。
- 测试强度（4.11④ 联动）：断言校验行为与边界（255/256、4096/4096/4097、恰满+1、UINT64_MAX 饱和投影、压缩 0 展开>0 除零式逃逸、重复注册 DuplicateCode、最小序号定码），无恒真、无"只编译通过"。

## 4.6 架构红线 —— 通过（AGENTS §5 八条逐条）

1. **L2 计算内核零 Qt**：新增 9 文件通读零 Qt include/符号（Win32 仅 `GetFileAttributesW`＋psapi 动态解析——io 属 L2/L1 边界的文件设施单元，非 Qt 面）；ird_gates R3 实跑零 io 命中；`IoHeadersFreeOfQtAndCrossUnitIncludes` 运行期扫描用例绿（扫描 7+ 文件）。
2. **业务域互链禁止（R-1）**：io 仅 PUBLIC 链 core＋diagnostics 两条 ARCH §3.5 登记边；CMake 配置期守卫保持；ird_gates SUB 实跑 11 命中全为基线既有（diagnostics/evidence/policy/project/runtime 的 testkit 直链×8＋testdata_lint×2＋project R4×1——DTB §4.5 登记例外），**命中集全为 diff 未触碰文件 ⇒ 零新增**结构性成立。
3. **跨单元私有头禁止（R-2）**：唯一跨单元 include＝`sdurws/ird/diagnostics/DiagCodes.hpp`（diagnostics **公共头**，io→diagnostics 登记边）；ird_gates R2 零 io 命中。
4. **框架零源码修改（SA-02）**：4.1 实证零框架路径＋patches/ 零新文件＋gates SA-02 面零新增。
5. **不做名称前缀拼接（R-4）**：ird_gates R4 实跑命中仅 project 基线 1 处，io 零命中。
6. **权威唯一（PA-1）**：IO-* 码值以 §9.12 建议值产出 CodeDescriptor、经 diagnostics 公共 API（`IDiagnosticRegistry::registerCode`）注册——码表权威仍在 diagnostics::StableCodeRegistry，io 未私设注册表、未改动 diagnostics 单元任何文件；诊断文案键按 `diag.<code-lower>.*` 约定生成（值归 ui，未越权）。
7. **不可变历史（PA-2）**：不适用（io 单元无修订写路径）。
8. **不继承 old/（R-5）**：全文件零 old/ 引用，实现从零编写（对照 §4.2/§4.3/§4.5 卡面语义逐节落位）。

## 4.7 注释规范 —— 通过（公共头 3 件全查＋实现 3 件全查＋测试 3 件全查，覆盖率超抽查下限）

- **文件头**：9 件全部具备（@file/@brief＋设计依据含卡面章节/需求 ID/契约 acceptance 编号＋背景说明"为什么"）。
- **类/函数 Doxygen 中文**：公共接口逐方法契约注释（前置/后置/错误/副作用/生命周期/线程/取消——ISafePathResolver、IBudgetGuard、EquivKeyMutex、BudgetSpec 逐项齐全）；实现文件语义段落注释标注算法步骤与卡面出处（如 normalizeCore ①~⑨ 步、词法消解净逃逸判据的 §4.2.2 表行对照）。
- **§2.5 高危信息**：物理单位（字节/UTF-16 码元/层级/计数/比例逐处标注）；坐标系/角度制不适用（io 无位姿量）；所有权（shared_ptr 共享语义、值拷贝归属、调用方持有逐处声明）；线程约束（resolver 无状态并发、EquivKeyMutex 内部锁、guard 单 scope 非并发＋ledger 并发只读，逐类声明）；错误语义（调用方契约违约 fail-fast〔tighten/relax 抛 invalid_argument〕vs 环境错误诊断码，错误码归类逐处注明）；确定性来源（A-Z 折叠不做本地化、饱和运算、十进制定点文本化不经 locale、清单序＝表行序，逐处注明）。
- 无过期注释、无复述式废注释、无中英混杂机翻腔（抽查未发现）。

## 4.8 文档同步 ＋ findings.json 相关性 —— 通过（偏差已登记；2 处文档瑕疵为建议级，转登 F-182/F-184）

- **实现与任务卡偏差均有登记（DTB §5.4）**：①`sdurws_ird_io_test` 提前随本任务以真实用例登记（契约 verify 指向该目标＋acceptance 要求 §11.2 行全绿——§12 IO-T01 禁止项语义＝无内容占位目标，io.md §15.5 v0.4 行登记辨析）；②§9.2 两处签名等价调整（openScope 增 parent 参数、增补 chargeArchive）——§9.0 L999 条款"签名均为实现建议……实现期允许等价调整，语义不变"在案，v0.4 行登记；③`IoResult<void>` 特化（§9.1/§9.2 动作型接口返回形态）登记。**设计正文（§2~§14）语义零变更**——实测 diff 仅 §1.2 现状行＋§15.5 v0.4 行，相符。
- **任务状态/追踪矩阵**：契约 status=ready 待收尾翻转（收尾环节动作，非实施侧）；io.md §11.2 矩阵行与 §12 行为本任务验收基准，无修改需求；contract-compile-log.md 无 IO-T02 新行需求（该册系契约编译留痕，本任务未触发契约修订）。
- **卡头同步瑕疵（建议级→F-182）**：io.md §15.5 已追加 v0.4 行，但卡头『文档版本』仍 `v0.3`、『日期』仍 `2026-09-10`（L7-8 实测）——F-122 家族第十五次再现，且本次**版本行也未随迁**（比 F-175 系"版本动日期不动"更退一步）。
- **§3.1 增记声称未兑现（建议级→F-184）**：v0.4 行声称 EquivKeyMutex「§3.1 头内容表按此增记」，实测 §3.1 `SafePath.hpp` 行（L198）仍为四类型、无 EquivKeyMutex（全卡 grep 该词仅命中 v0.4 行自身）——文档幻影表述；另 §3.1 `Budget.hpp` 行 `BudgetLedger` 与实现名 `BudgetLedgerSnapshot` 的命名等价未在 §15.5 登记（§9.0 条款覆盖语义，建议补记）。
- **findings.json 未关闭条目相关性核对（160 条 open）**：①**F-153/F-010（.gitignore `*.log` 吞 traceability/builds 留痕）——本任务直接受害者**：`builds/wp11-t02/` 8 份 .log 被吞（4.10 阻断详情见下）；②**F-180（io.md 卡头日期行滞留）——第十五次再现**，转登 F-182；③F-179（IO-T01 提交标题名词开头家族）——**本任务 eaa6aa99 再现**，转登 F-185；④F-165/F-166/F-172（幻影清单家族）——**本任务提交正文文件清单与 diff 不符再现**，转登 F-183；⑤F-181（收尾落分支）属流水线治理，与本任务产物无涉；⑥其余 open（core/project/policy/runtime/evidence/diagnostics 各单元家族）与本任务零交集。

## 4.9 commit 质量 —— 通过（结论零粉饰；1 处文件清单幻影为建议级，转登 F-183；1 处标题名词开头为建议级，转登 F-185）

- **aafb66ac**『实现 io 单元 SafePath＋BudgetGuard 与 IO-* 码表注册』：标题动词开头合规；正文四段（What/Why/验证/影响面）齐备；验证段逐项与验收者复现交叉一致（双模式零 error、20/20、门禁零新增——含"曾因 Psapi.lib 命中 1 处改 kernel32 动态解析后复扫通过"的过程如实陈述）、validate/verify PASS——**零粉饰零虚标**；Acc-Covered: 1,2,3,4,5 映射在案。
- **eaa6aa99**『io.md v0.4 同步 IO-T02 落位并登记实施留痕』：四段齐备；验证结论（双模式零 error/20/20/门禁 io 零命中/validate/verify PASS）全部复现为真。**但**①标题前缀后以文件名词短语开头（F-012/F-179 家族再现→F-185）；②"改动内容"声称新增 8 份日志类留痕，实测该提交仅 4 件（见 4.10）——文件清单幻影（F-165 系→F-183），与 4.10 阻断同根。

## 4.10 留痕完整 —— **不通过（阻断级，fail 判据）**

- 在库并核实的留痕：gtest XML 三份（integration/smoke/report-target，均 `tests="20" failures="0"`，与验收者复现一致）＋`ird-test-report.json`（ird-test-report/1，targets/gates/selfCheck/testSuites 四段齐全、用例↔acceptance↔§11.2 追溯完整）。
- **阻断事实**：`git ls-tree eaa6aa99 -- …/builds/wp11-t02/` 实测**仅 4 个文件**；而提交 eaa6aa99 正文声称新增「integration/smoke 两模式构建日志、两模式 gtest XML＋运行日志、ird_gates 门禁日志、validate-task/verify-task 日志」，`ird-test-report.json` gates/selfCheck 字段按名引用 `gates-ird-gates-target.log`、`integration-build.log`、`smoke-build.log`、`validate-task.log`、`verify-task.log`。实施侧主工作树 `git status --ignored --porcelain` 实测该 8 份 .log（gates-ird-gates-target/integration-build/integration-test-run/smoke-build/smoke-configure/smoke-test-run/validate-task/verify-task）**全部存在但被 `.gitignore:87` 全局 `*.log` 规则吞没、未跟踪、未进入送验冻结对象**——验收所通过的冻结对象不含这些证据，一旦合入或工作树清理即永久丢失；验收记录无法把它们作为在库证据引用。
- **判例**：CORE-T02 attempt-1 同型事实（"4.10 日志被 .gitignore 吞"）判 fail→返工补证→attempt-2 pass（state.json history 在案）；同单元前一任务 IO-T01 的 `builds/wp11-t01/` **9 份 .log 全部在库**（`git ls-tree` 实证）——同一留痕目录约定的直接先例，本任务为回归。
- phase-one-validation.log／resultNote：不适用（与 IO-T01 同口径——phase-one 时代字段；功能等价物＝io.md §15.5 v0.4 行＋ird-test-report.json，均已更新）。
- **返工输入**：在 wp11-t02 追加补证提交，将 8 份 .log 以 `git add -f` 强制入库（IO-T01 先例形态；如日志已过期则重新生成后 -f 入库），**不得改动既有两提交**（已推送历史不可改写）；补证后新 headSha 重新发起验收（attempt 2，全量重跑）。另建议同步治理侧消掉 `.gitignore:87` 对 `traceability/**` 的辖域（F-153 既有登记的处置项，归 governance 所有者）。

## 4.11 偷懒/缩水实现扫描 —— 通过（①②④零命中；③逐条证据表见 4.4）

- ① `git diff base..head | grep "^+" | grep -iE "TODO|FIXME|stub|placeholder|未实现|not implemented|待实现"` **零命中**。契约无"触发式延后"标注项；`IoDiagnostics.hpp` 文件头注明"逐行列定位助手随首个消费者（CSV 通道 IO-T03）落位，不预建无消费者能力"——系 §3.1 卡面既定的落位边界声明，非本任务缩水。
- ② 空实现检测：无——全部公共方法（resolver 两方法、guard 四方法、mutex 四方法、token/描述符/注册/三要素四函数）均有实质实现；`Io.cpp` 保持 IO-T01 锚点翻译单元（契约既定形态，非空实现）。
- ③ 逐条证据表：见 4.4（5/5 条均有具名测试＋文件行号＋双模式复现结果；任一条无证据即 fail——本任务无此情形）。
- ④ 测试强度：见 4.5（边界/饱和/除零式逃逸/注册冲突/最小序号定码等断言＋两枚失效注入实证会红）。

## 发现的问题清单

| # | 级别 | 问题 | 依据 | 处置 |
| --- | --- | --- | --- | --- |
| B-1 | **阻断** | 4.10：提交正文与 ird-test-report.json 按名引用的 8 份日志类留痕被 `.gitignore:87` 吞、未入送验冻结对象（幻影清单与留痕缺失同根；CORE-T02 attempt-1 同型判例） | 4.10 节实证 | 返工：`git add -f` 补证提交→新 headSha→attempt 2 全量重验 |
| S-1 | 建议 | io.md 卡头版本（v0.3）/日期（2026-09-10）未随 §15.5 v0.4 同步——F-122 家族第十五次再现且版本行也未随迁 | 4.8 节 | 转登 **F-182**；随返工提交顺手补正 |
| S-2 | 建议 | eaa6aa99 提交正文文件清单与 diff 不符（8 份日志幻影）——F-165 系再现；验证结论本身复现为真，非结论粉饰 | 4.9/4.10 节 | 转登 **F-183**；随 B-1 补证自然消解 |
| S-3 | 建议 | io.md v0.4 行「§3.1 头内容表按此增记」未兑现（EquivKeyMutex 未入 §3.1 行）；BudgetLedger→BudgetLedgerSnapshot 命名等价未登记 | 4.8 节 | 转登 **F-184**；随返工补正 |
| S-4 | 建议 | eaa6aa99 标题前缀后名词短语开头——F-012/F-179 家族再现 | 4.9 节 | 转登 **F-185**；已推送提交不改写（判例：不阻断） |
| O-1 | 所有者环节 | P-IO-6 后半："与 diagnostics 所有者收编确认"——实现侧已产出注册面＋真实注册表验证＋码值逐字 §9.12，收编（diagnostics 内置表 §4.6 扩编）归 diagnostics 所有者治理动作，随本验收提请 | 4.4⑤ | 登记 DTB §4 由所有者裁决；不阻塞本任务返工 |

## 验收者环境说明（复现要据）

- 复现命令序列（worktree A 内）：①`git worktree add --detach … eaa6aa99`；②补拷 ini 模板；③`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=<主仓>/vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64 -DCMAKE_CXX_FLAGS="-DNOMINMAX -DWIN32_LEAN_AND_MEAN -DWIN32"`（exit 0，缓存 `:BOOL=ON`）；④`cmake --build build --config Release --target sdurws_ird_io_test`（exit 0）；⑤`cmake -S RobWork/RobWorkStudio/src/rwslibs/industrialrobot -B <冒烟目录> -DCMAKE_TOOLCHAIN_FILE=…`＋`--build`（exit 0）；⑥双模式测试可执行文件直跑（20/20）；⑦`ird_gates.cmake` -P 实跑（11 基线命中、exit 1——SA-10 判构建失败的基线口径，零新增）；⑧validate-task/verify-task（PASS）；⑨两枚失效注入＋逐字节还原。
- 验收者自身过程日志留存于临时 worktree A（acc-*.log/xml），按协议不随记录入库；关键输出已逐字摘引于本记录各节。
- 独立性：全新上下文；只读代码、契约、设计文档、留痕与先行验收记录（IO-T01 环境口径复用属 §3 执行环境继承，非实施叙述消费）；未读取实施者任何过程说明。
