# WP-13-T03 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | WP-13-T03 实现 RobotDesign 领域对象与双权威参数化模型（unit=modeling） |
| 送验对象（冻结） | 分支 `wp13-t03` 尖端 `ff6c77d9326fb1b03ffee4e2269a86bf7da484c4`（验收全程按该 SHA） |
| 分支漂移检查 | `origin/wp13-t03` = `ff6c77d9…` ＝ 送验 SHA，**零漂移**（ACC §3） |
| diff 范围 | `440228b808d7b06a30e4cd894dc777a2ef84574d..ff6c77d9326fb1b03ffee4e2269a86bf7da484c4`（2 个提交：859d5955 实现＋ff6c77d9 文档同步与留痕） |
| **verdict** | **pass**（4.1～4.11 全部通过，无阻断级问题；建议级 1 项转登 findings F-298） |
| 验收执行方式 | 验收者全新上下文独立复现：detached worktree @ 送验 SHA 亲手重配重跑全部构建/测试/脚本；未采信实施者任何运行结果（ACC §2） |
| evidence 分支 | `acc/WP-13-T03/1`（基于 `origin/redesign-main` = 440228b8；记录 commit 见分支尖端） |

---

## 4.1 改动范围 —— 通过

`git diff --name-status 440228b8..ff6c77d9` 共 36 文件，逐一对契约 `allowedFiles` 核对：

- `RobWork/RobWorkStudio/src/rwslibs/industrialrobot/modeling/**`：14 文件（CMakeLists.txt 改；include/ 下 RobotDesign.hpp、Parts.hpp、Codec.hpp 新增，ObjectTypes.hpp、Errors.hpp 改；src/ 下 Codec.cpp、RobotDesign.cpp、Parts.cpp、InertiaMath.hpp 新增，Errors.cpp 改；test/ 下 RobotDesignTest.cpp、PartsTest.cpp、CodecTest.cpp 新增）——全在 modeling/** 内。
- `RobWork/doc/industrial-robot-design/units/modeling.md`（改）——在 allowedFiles。
- `RobWork/doc/industrial-robot-design/traceability/builds/wp13-t03/**`：20 文件（构建/测试留痕）——在 allowedFiles `traceability/**` 内。
- **forbiddenFiles 零触碰**：REQUIREMENTS.md、ARCHITECTURE.md 不在 diff。
- **SA-02**：diff 无 modeling 之外任何框架源码文件；`patches/` 无新文件（无未登记框架改动）。

## 4.2 可复现构建 —— 通过（两模式均验收者亲手重配重跑）

- **集成模式**（全新构建树 `D:/10_Source_Repos/21_robot/_acc_wp13t03_a_build`，源=detached worktree @ ff6c77d9）：
  - 配置：`cmake -S <A>/RobWork -B <build> -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON`（＋主树 vcpkg toolchain＋Qt 前缀，与主树缓存同参）。结果 `Configuring done`；缓存确认 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`。
  - 构建：`--config Release --target sdurws_ird_modeling` / `sdurws_ird_modeling_test` / `sdurws_ird_modeling_contract_test` 三目标退出码均 0，MSVC `error C/LNK` 计数 0。
  - 环境前置注记：全新 worktree 缺 git-ignored 的 `RobWorkStudio/bin/RobWorkStudio.ini.template.static`（配置脚本所需），验收者从主树复制两个 ini 模板后配置通过——**F-007/F-009 已登记的环境前置缺口**，非本任务产物缺陷。
- **独立冒烟模式**（`D:/10_Source_Repos/21_robot/_acc_wp13t03_a_smoke`，`-DCMAKE_TOOLCHAIN_FILE=<vcpkg>/scripts/buildsystems/vcpkg.cmake`——AGENTS §4.1 口径；＋Qt 前缀）：
  - 配置 done（7.1s）；全量构建退出码 0、错误计数 0。
  - 冒烟测试：`ctest --test-dir <smoke>/modeling -C Release -L "^ird$"` → **2/2 通过**；`sdurws_ird_modeling_test_report`/`..._contract_test_report` 报告目标产出 gtest XML：**unit tests=51 failures=0 errors=0；contract tests=5 failures=0 errors=0**；`ird-test-report.json` summary `total:51, passed:51, failed:0, decisive:true`。

## 4.3 契约 verify 命令 —— 逐条亲手执行（6/6 执行，结论如下）

| # | 命令（集成树等价执行） | 结果 |
| --- | --- | --- |
| 1 | `cmake --build … --config Release --target sdurws_ird_modeling` | 退出码 0，零错误 |
| 2 | `cmake --build … --config Release --target sdurws_ird_modeling_test` | 退出码 0，零错误 |
| 3 | `ctest --test-dir <build>/RobWorkStudio/src/rwslibs/industrialrobot/modeling -C Release -L "^ird$"` | **2/2 passed**（注：contract_test 目标须先构建，验收者首次运行报 Not Run，构建该目标后复跑全绿） |
| 4 | `cmake --build … --config Release --target ird_gates` | 门禁引擎运行并按 SA-10 以退出码 1 终止：**51 处命中，全部为存量基线**（F-019 家族在册）。基线判定证据见 4.4 门禁段：命中分布与送验 base 日志逐类一致，**本任务零新增命中**。 |
| 5 | `pwsh -File RobWork/scripts/industrialrobot/validate-task.ps1 -TaskFile …/WP-13-T03.json` | `validate-task: PASS (1 tasks)`，退出码 0 |
| 6 | `pwsh -File RobWork/scripts/industrialrobot/validate-docs.ps1` | `validate-docs: PASS (20 units, 12 trace entries, 186 task files)`，退出码 0 |

## 4.4 acceptance 条目逐条证据 —— 5/5 有对应证据

**门禁基线核对（acceptance 与 DoD 的 `ird_gates` 面）**：验收者新鲜构建树运行的命中分布 `LIB=8, R1=1, R3=37, R4=3, R5=1, SUB=53, T1=5, T2=1`（门禁自报 51 处；归一化 hit-set 58 条）与送验留痕 `traceability/builds/wp13-t03/gates-ird-gates-base.log` 与 `gates-ird-gates-branch.log` **逐类完全一致**；两份送验日志中 modeling 相关命中仅 `sdurws_ird_modeling_test→sdurws_ird_testkit`、`sdurws_ird_modeling_contract_test→sdurws_ird_testkit` 各 1 条（WP-13-T02 已自登记 F-292/F-051/F-072 同根因），`gates-hit-set-diff.txt` 为空文件（0 字节）。**结论：base..branch 零新增命中，未关闭门禁项全部为在册基线，不阻塞本任务**（F-019 major open 为门禁引擎级在册项）。

| acceptance 条目 | 证据（验收者亲验） |
| --- | --- |
| 1. RobotDesign/Parts/Codec 公共头与实现落位；五部件 schema；IRobotDesignCodec 确定性编解码；robot-design token 复用 runtime 字面 | 三公共头落位 `include/sdurws/ird/modeling/`（RobotDesign.hpp 687 行/Parts.hpp 401 行/Codec.hpp 197 行）；五对象 variant（Codec.hpp:121 ObjectVariant）；`ObjectTypes.hpp:48,66` robot-design token 以 `using runtime::kRobotDesignObjectType` 引入同一实体、不设第二常量；测试 `EncodeIsDeterministicPerObject` 断言 "IRDMDLO" 魔数＋token 子串；`SchemaVersionDefaultsToRegisteredConstant` 钉住 schema 常量单一权威。SourcedValue 全 SI（各字段注：kg/m/rad/kg·m²/N·m）、T_ab 位姿约定逐字段标注。 |
| 2. 对象分解按 O-36 已裁决口径（关节/连杆子 ObjectId 为模型内标识、内嵌根对象字节） | DTB §4 O-36 行（L634）裁决原文「objectRefs/CM-0 复核范围＝真实存储对象，子条目 ObjectId 属模型内标识」与实现一致：`RobotDesign.hpp:421-424,483-489` JointEntry/LinkEntry objectId 注明 O-36 口径；checkInvariants I-MDL-2 将关节∪连杆 ID 作为同模型内唯一性检查面（RobotDesign.cpp:341-372）；不逐连杆拆对象（D-MDL-1/D-MDL-2，P-MDL-2 按卡执行——单元卡 §14.3 L1452-1453 在册）。 |
| 3. 不变量 UT 逐条注册并通过（用例名绑定 I-MDL-1～12；StandardDH 态 axis/origin 编辑拒绝；Explicit 态 dhDerived 不入编码身份；非法组合拒绝） | 用例名逐条绑定（grep 亲验）：`IMdl1_StructureSizes`～`IMdl12_CouplingR1Locked`（RobotDesignTest.cpp:216-475，12 条齐全）＋ `DMdl5_ExplicitDhDerivedCacheNotEncoded`/`DMdl5_DhDerivedAxisOriginCacheNotEncoded`（CodecTest.cpp:391,402）；非法组合在 `IMdl4_LimitOrderAndIllegalCombinations`（continuous+bounds、prismatic+workingRange，RobotDesignTest.cpp:269）；C-1 AuthorityViolation 由 V14/MDL-09 用例覆盖。51/51 通过（双模式 XML 亲产）。 |
| 4. Codec canonical 编码确定性 UT（NFR-COR-02）；往返逐字段一致；未知主版本→SchemaVersionUnsupported | `EncodeIsDeterministicPerObject_WP13T03_ACC4`（五对象重复编码同字节）、`RoundtripPreservesAllFields_WP13T03_ACC4`、`DhModeRoundtripDerivedStateDefinition`、`EncodeRejectsUnsupportedVersion`、`DecodeRejectsUnknownMajorVersion`（CodecTest.cpp:339-527）全部通过；encode/decode 为完整实现（Codec.cpp 1383 行：Sink/Reader 小端原语、四段校验链、UTF-8 无 NUL 校验——验收者抽查 L148-260、L694-730、L783-851 非空实现）。 |
| 5. V-14 双权威冲突拒绝场景与 Axis 编辑权限随权威模式（MDL-09）用例通过并留痕 | `V14_DhAuthorityAxisEditRejectedBytesUnchanged_WP13T03_ACC5`（RobotDesignTest.cpp:502——拒绝＋AuthorityViolation＋"派生只读"提示＋编码字节不变机器判据）、`Mdl09_AuthorityFieldPermissionMatrix_WP13T03_ACC5`（:539——2×3 权限四象限）通过；留痕 gtest XML＋ird-test-report.json 双模式落位 `traceability/builds/wp13-t03/`（20 件，验收者逐件在送验提交中核对存在）。 |

未执行项标注：无——本任务所有 acceptance 面均执行；`ird_gates` 按 4.3/4.4 如实记录为"存量基线红＋零新增命中"，不标注"零命中"。

## 4.5 测试真实失败能力 —— 通过（两处变异均被捕获，已复原）

1. **探针 1**（authorityEditGuard 错误放行 StandardDH 态 axis 编辑——RobotDesign.cpp 注入一行）：`MdlRobotDesign.V14_DhAuthorityAxisEditRejectedBytesUnchanged` 与 `MdlRobotDesign.Mdl09_AuthorityFieldPermissionMatrix` **双双变红**（filtered run 0 passed / 2 failed，退出码 1）。
2. **探针 2**（Codec.cpp `Sink::u64` 小端改大端写入——破坏 f64/长度前缀字节序）：`MdlCodec.RoundtripPreservesAllFields_WP13T03_ACC4` **变红**（1 failed；`EncodeIsDeterministicPerObject` 仍过——确定性测试断言"同输入同输出"而非"往返正确"，口径自洽非恒真）。
3. 两探针 `git checkout --` 复原后重建，全量 51/51 复绿（退出码 0），worktree A 工作区零残留（`git status --porcelain` 空）。

## 4.6 架构红线 —— 八条逐条自查通过

1. **L2 零 Qt**：modeling src/include 全量 grep `#include <Q…>` 零命中（含 BuildRedLineTest 运行期复核，测试通过）。
2. **R-1 业务域互链**：modeling 产品代码零 include 其他业务域单元（requirements/kinematics/trajectory/dynamics/selection/optimization）。
3. **R-2 跨单元私有头**：跨单元 include 全为公共头路径 `sdurws/ird/{core,runtime,diagnostics}/…`（grep 逐行核）；单元私有头 `src/InertiaMath.hpp` 置于 src/ 不入 include/（R-2 自洽）。
4. **SA-02 框架零修改**：见 4.1（无 patches 登记＝无框架改动）。
5. **R-4 名称前缀**：modeling 面无 RobWork 名称拼接/剥离代码（值模型无名称解析逻辑；门禁 R4 命中零 modeling 文件）。
6. **PA-1 权威唯一**：诊断产码不经 modeling（`modelingDiagCode` 对 MalformedPayload 返回 nullopt、不预建映射；authorityEditGuard 返回值面错误不产诊断记录——RobotDesign.cpp:228-239 注释与实现一致）；token/词表复用 runtime 单源（ObjectTypes.hpp using）。
7. **PA-2 不可变历史**：值模型无写路径；文件头注明已应用态字节形态不可变。
8. **不继承 old/**：全量新增，无 old/ 引用（diff 无 old/ 路径）。

## 4.7 注释规范 —— 通过（公共头全查 3 件＋实现抽查 4 件）

- **公共头全查**：RobotDesign.hpp / Parts.hpp / Codec.hpp 三头文件头均含设计依据（需求 ID＋单元卡章节＋任务契约）、背景说明、所有权/生命周期、线程约束、确定性来源（AGENTS §2.2 全要素）；类/结构体/函数 Doxygen 中文注释完整，参数逐个 `[in]`＋单位＋语义。
- **§2.5 高危信息表**：物理单位逐字段带单位（kg·m²、rad、m、N·m、kg/m³）；坐标系逐处标注（T_parent_joint/T_flange_tool/T_ab、世界系固连、"core.md §4.6"）；角度制式显式 rad；容差来源标注（1×10⁻¹²＝§4.10/附录 D；1×10⁸＝P-RT-7 设计默认、P-MDL-7 单源登记，非自设魔数）；线程约束（值类型不可变共享安全、编辑态仅 UI 线程——RobotDesign.hpp:36-40）；错误语义（调用方错误值面 vs 环境错误诊断轨、Expected 契约违约抛 logic_error——Codec.hpp:107-115）；确定性（纯函数、无时钟/locale、浮点位模式非文本——Codec.hpp:32-36）。
- **实现抽查 ≥3**：RobotDesign.cpp（token 全枚举无 default 的编译器拦截纪律、I-MDL-1～10 逐段注释含业务出处）、Parts.cpp（R1 锁定/条件数重算分工注记）、InertiaMath.hpp（Smith 解析式特征值逐步数学推导＋"为什么不用迭代法"的确定性论证）、Codec.cpp（布局总表、D-MDL-5 排除落点、UTF-8 校验规则）——均达 §2.4 语义段落级注释标准。

## 4.8 文档同步 —— 通过（偏差已按 DTB §5.4 登记；findings 相关性核对完毕）

- `units/modeling.md` v0.3→v0.4：§14.6 增 v0.4 变更记录全量登记（公共头三件落位、schema 常量补位、MalformedPayload 表尾追加、七项偏差/分工 a)～g)——DhParameters 落位、JointPose 载荷类型同构替代〔rw Transform3D 外联符号与冒烟 header-only 纪律冲突〕、maxVel/maxAcc 不发明 schema、payloadAttributes 仅 ratedLoad、I-MDL-9 闭包半段归 T08、I-MDL-7 映射半段归 T05/T11、条件数重算复核归 T18/R2；D-MDL-5 编码面；验证结论）；§4.10 增补 I-MDL-12 显式行。实现与登记逐项对得上（验收者对照代码核实）。
- 任务状态/追踪矩阵：契约与 DTB 任务行状态流转归收尾（PIPE §4.7），不属送验 diff 义务。
- **findings.json 未关闭条目相关性核对（240 条 open）**：与本任务直接相关 3 条——①**F-292**（modeling 两测试目标→testkit SUB 命中，major/open）：本任务门禁命中集与 base 完全一致（见 4.4），未新增、也无法由本任务消账（引擎修正归 WP-01 所有者，cmake/ird_gates.cmake 不在 allowedFiles）；②**F-293**（T02 送验提交标题名词开头家族）：本任务主提交 859d5955 标题动词开头「实现…」合规；留痕提交 ff6c77d9 标题「modeling 单元卡 v0.4 同步与任务验证留痕」名词开头——同族新例，**转登 F-298**；③F-294（流水线簿记）与本任务产物无关。O-36/P-MDL-1/P-MDL-2 pitfalls：O-36 已裁决（DTB §4 L634）且实现按裁决执行；P-MDL-1/P-MDL-2 状态随单元卡 §14.3 留痕，无静默积压。

## 4.9 commit 质量 —— 通过（1 项建议级转登）

- **859d5955**「[WP-13-T03] 实现 modeling RobotDesign 领域对象、四部件值模型与 canonical 编解码」：动词开头✓；正文四段齐备（What 按文件列全 8 项/Why 引需求 MDL-01/02/09＋ARC-04/CON-01/05/NFR-COR-02/DEP-04/COR-03 与卡面章节＋pitfalls 处置/How 验证段/影响面）；验证段结论（双模式零错误、51/51＋契约通过、门禁零新增命中）与验收者独立复跑**逐项吻合，无粉饰**。
- **ff6c77d9**「[WP-13-T03] modeling 单元卡 v0.4 同步与任务验证留痕」：正文四段齐备、结论与实核一致；但标题前缀后为名词短语开头（非动词），同 F-012/F-293 家族——**建议级，转登 F-298，不阻塞**。

## 4.10 留痕完整 —— 通过

- gtest XML×4（unit/contract × 集成/冒烟）＋ird-test-report.json×4＋构建/配置日志＋ctest 过滤日志＋gates base/branch 日志＋hit-set diff＋validate-task/docs 日志共 20 件落位 `traceability/builds/wp13-t03/`（送验提交内，验收者逐件核对存在且内容自洽：ird-test-report-integration.json summary total:51 passed:51 decisive:true 与 XML 一致）。
- `phase-one-validation.log` 本任务未追加（该文件为阶段 A 历史聚合日志，阶段 B 起任务留痕落位即 `traceability/builds/<task>/`——WP-13-T02 同口径先例）；`state.json` 簿记归编排者收尾（提交信息已如实注明"未纳入本提交"）。

## 4.11 偷懒/缩水实现扫描 —— 通过

1. **标记扫描**：diff 范围内 `TODO|FIXME|stub|placeholder|未实现|not implemented` **零命中**（grep 亲验）。
2. **空实现检测**：公共接口函数体全为真实实现——IRobotDesignCodec encode/decode（1383 行布局/校验链）、authorityEditGuard、checkInvariants×3、token 表；无"仅返回默认值"的公共函数。
3. **逐条证据表**：见 4.4——acceptance 5/5 每条均有具名测试＋验收者亲产命令输出，无凭"已实现"声明采信。
4. **测试强度**：断言校验行为与边界（字节级比较、params 键值核对、非法组合逐项、四象限矩阵）；4.5 两处变异实证测试会红，非恒真。

---

## 发现的问题清单

### 阻断级

无。

### 建议级（已按 ACC §5 强制转登 traceability/findings.json）

| 编号 | 摘要 | 处置方向 |
| --- | --- | --- |
| F-298 | 送验留痕提交 ff6c77d9 标题「modeling 单元卡 v0.4 同步与任务验证留痕」前缀后名词短语开头，非动词开头（F-012/F-293 家族同型再现）；正文四段齐备、结论无粉饰、零行为影响 | 随 F-012 家族统一处置；后续提交标题前缀后动词开头；不追溯改写已推送提交 |

## 验收者环境说明

- 复现现场：detached worktree `D:/10_Source_Repos/21_robot/_acc_wp13t03_a` @ `ff6c77d9`（按 SHA 不按分支名，ACC §3）。
- 集成构建树：`_acc_wp13t03_a_build`（全新配置；VS2022 x64；`RWS_BUILD_INDUSTRIALROBOT:BOOL=ON` 缓存确认；toolchain=主树 vcpkg、Qt 前缀与主树缓存同参）。全新 worktree 需复制 git-ignored 的 RobWorkStudio ini 模板两件方可配置（F-007/F-009 在册环境前置，非产物缺陷）。
- 冒烟构建树：`_acc_wp13t03_a_smoke`（带 vcpkg toolchain＋Qt 前缀）。
- 记录回传：本记录写入 evidence 分支 `acc/WP-13-T03/1`（基于 origin/redesign-main=440228b8）并推送；本分支同时携带 findings.json 的 F-298 转登（ACG §5 强制闭环）。验收者未向任务分支 wp13-t03 推送任何提交、未修改实施产物（4.5 探针在临时 worktree 内注入并已完全复原）。
- 验收裁决：**pass**——可进入所有者合入环节（合入决策归所有者，本验收者不执行）。
