# RT-T02 验收记录（对抗式验收）

| 字段 | 值 |
| --- | --- |
| 任务 ID | RT-T02（≙WP-06-T02，runtime 错误与注入契约 Errors/Sources） |
| 契约 | tasks/foundation/RT-T02.json（status: ready；验收时点与 origin/redesign-main 侧一致，翻转 done 归合入收尾） |
| 验收对象 | 40 位冻结 SHA `f0a4c0ee0f27f1a303f23fac14628ab004c873f7`（分支漂移检测 PASS：`git ls-remote origin wp06-t02` ＝该 SHA）；范围 `12439b7897e9fb5c9b24ae31641315da405dca0c..f0a4c0ee0f27f1a303f23fac14628ab004c873f7`（3 提交：7790241／804224b／f0a4c0e） |
| 尝试序号 | attempt=1（evidence 分支 `acc/RT-T02/1`，基于 origin/redesign-main＝12439b7） |
| **verdict** | **pass**（4.1~4.11 全过；无阻断级问题；建议级 3 项 → F-034/F-035/F-036 随本 evidence 分支转登 findings.json） |
| 验收日期 | 2026-09-12 |
| 独立性声明 | 全新上下文（ACC §2 四条逐条适用）：只消费契约/设计文档/冻结产物，未消费实施叙述；构建与测试全部验收者亲手重跑（复现优先于采信）；对抗式立场贯穿（4.5 双探针注入、4.11 逐条反证扫描） |

---

## 逐项证据（4.1~4.11）

### 4.1 改动范围 —— PASS

- `git diff --name-status 12439b7..f0a4c0e` ＝ 19 文件：runtime 8（CMakeLists.txt M；Errors.hpp/Sources.hpp/Errors.cpp/ErrorsTest.cpp/ExpectedTest.cpp/SourcesTest.cpp A；BuildRedLineTest.cpp M）＋traceability/builds/wp06-t02/ 留痕 11。
- 契约 allowedFiles＝`runtime/**`＋`industrialrobot/CMakeLists.txt`＋`units/runtime.md`＋`traceability/**`：全部变更落在 `runtime/**` 与 `traceability/**` 内；`forbiddenFiles`（REQUIREMENTS.md/ARCHITECTURE.md）零触碰（不在 diff）；`patches/` 零触碰（SA-02 无登记需求）。

### 4.2 可复现构建 —— PASS（全新树从零复现）

复现现场＝`git worktree add --detach D:/10_Source_Repos/21_robot/acc-rt-t02-wt-a f0a4c0e`：

- 集成模式全新配置：`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DCMAKE_TOOLCHAIN_FILE=<vcpkg> -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64 -DRWS_BUILD_INDUSTRIALROBOT=ON` → **exit 0**（configure-integration.log，Configuring done 40.6s）；缓存确认 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`。F-007 前置（ini 模板缺失被 .gitignore 吞）由验收者按登记处置：从主树复制 `RobWorkStudio.ini.template.static`＋`RobWorkStudio.ini.shared.in` 至 worktree bin/。
- 契约 verify 目标从零构建：`cmake --build build --config Release --target sdurws_ird_runtime_test` → **exit 0、零错误**（error C[0-9]/LNK/fatal 计数＝0；build-verify-target.log）。
- 其余全部 ird 测试目标从零构建（core/evidence/policy/testkit）→ **exit 0、零错误**（build-other-ird-tests.log）。观测：core/Units.cpp 既有 C4099 警告（struct/class 声明序），属 CORE 已合入面、不在送验 diff，不计入本任务。
- 冒烟模式独立目录：`cmake -S <wt-a>/industrialrobot -B acc-rt-t02-smoke -DCMAKE_TOOLCHAIN_FILE=<vcpkg>` → **exit 0**；`cmake --build … --config Release` → **exit 0**（smoke-build.log 尾部 testkit_test.exe 链接完成）。
- 结论：任务目标面（sdurws_ird_runtime＋runtime_test＋全 ird 测试目标）双模式从零构建零错误。全树从零构建不可达属 F-008 基线既有缺陷（RobWorkSim 插件 min/max 宏污染，登记在案、非本任务面）。

### 4.3 契约 verify 命令 —— PASS

契约 verify 数组仅一条：`cmake --build build --config Release --target sdurws_ird_runtime_test`——在全新 worktree A 集成树亲手执行，**exit 0**（同 4.2 第二条）。

### 4.4 acceptance 条目逐项 —— PASS（3/3 均有具名测试＋双模式实测）

全量测试验收者亲手执行（非采信实施留痕）：

- 集成树（从零构建）：runtime **21/21**、core 63、evidence 4、policy 4、testkit 97＝**189 用例 0 失败**，各 exe exit 0（acc-rt-t02-xml/integration/）。
- 冒烟树：runtime **21/21**、core 61、evidence 4、policy 4、testkit 97＝**187 用例 0 失败**，各 exe exit 0（acc-rt-t02-xml/smoke/）。

| 契约 acceptance 条目 | 具名证据 | 验收者实测 |
| --- | --- | --- |
| ① 错误码全表用例通过（RT-ERR 全枚举——稳定诊断码/错误分类逐 token） | `RuntimeErrorsFullTable.AllCodesPerToken_RT_ERR_NFR_COR_03`（ErrorsTest.cpp:77）：15 值×(token/registryCode/category/isCancellation/isContractViolation) 五维手写期望表（抄录 §3.4 token 列＋§10.11 v0.3 冻结关系原文，防"实现即期望"）＋枚举序号 14 锚定（防插入/重排） | 双模式 PASSED；另以 §10.11 原文逐码比对 Errors.cpp 三张表逐字一致（12 个 1:1＋UnknownObject/ContextReleased 空＋RobWorkError 事件码路径） |
| ② Sources 注入契约（§3.3）语义用例通过——来源注入点与错误归属可定位 | SourcesTest.cpp 六用例（185/195/217/250/316/332）：接口 is_abstract 静态断言、字节命中/未命中（nullopt≠空 vector）、修订投影逐字段往返、`InjectionPointsMapToCodes_RT_SRC_ACCEPT2` 四注入点失败结果↔归属码逐对钉住、取消最终性、基类指针析构 | 双模式 PASSED；归属表与 Sources.hpp 文件头四行表及 §5.2 十段表一致 |
| ③ Expected 语义单元测试通过（§3.4——成功/失败两态不静默吞错） | ExpectedTest.cpp 八用例（48/56/64/79/86/100/113/140）：两态构造/取值、`GetOnErrorFailsFast_ACC3_NFR_COR_03`（错误态 get() 抛 logic_error——返回默认值即 NFR-COR-03 禁止的静默形态）、error-on-ok 对称、互斥、拷贝移动、move-only、查询镜像（不抛＋原名回显） | 双模式 PASSED |

ird_gates 口径：验收者亲手 `cmake --build build --config Release --target ird_gates`（全新集成树）→ exit 1，命中**恰为 IRD-GATE-T1＋IRD-GATE-SUB 两处**（sdurws_ird_testdata_lint→testkit 边），与 findings.json **F-019**（TK-T03 既有、基线 e743f61 已存在）逐字吻合，**runtime 面零命中**；门禁自测 9 项（pass_clean/pass_r4comment/fail_r1/t1/r5/sub/r3/r4/t2）行为全部符合预期。门禁本体建成且可运行——实施提交"ird_gates 未执行——门禁未建成"的理由失实（见建议级 F-035），方向为少报、无粉饰。

### 4.5 测试真实失败能力（验证验证者）—— PASS（双探针全红→精确还原→复绿）

在冻结 worktree A 上注入两处独立缺陷，每步显式核验构建退出码（防旧二进制假绿）：

- **探针 1（token 表变异）**：Errors.cpp NameConflict token 追加 `-MUTATED` 后缀 → 重建 exit 0 → 测试 **exit 1，恰 2 用例红**：`AllCodesPerToken_RT_ERR_NFR_COR_03`＋`MessagePrefixAndCodeAccessor_RT_ERR`（what() 前缀钉住连带暴露）→ Edit 逐字还原 → `git status --porcelain` 空（字节级一致）。
- **探针 2（Expected 前置违约路径）**：Errors.hpp `get()` 的 throw 注释掉、改返回 `static T` 默认值（恰为 NFR-COR-03 禁止的静默形态）→ 重建 exit 0（零编译错误，证明变异合法）→ 测试 **exit 1，恰 2 用例红**：`GetOnErrorFailsFast_ACC3_NFR_COR_03`＋`StatesAreMutuallyExclusive_ACC3` → 逐字还原 → git status 空。
- **复绿**：还原后重建 exit 0 → runtime 测试 **21/21 PASSED**（probe-restore-run.log）。

结论：被抽断言非恒真，测试对实现的变异有真实检出能力。实施侧 probe-build.log/probe-test-run.log（token 变异→AllCodesPerToken＋InjectionPointsMapToCodes 双红）与本验收探针互为独立印证。

### 4.6 架构红线（AGENTS §5 八条）—— PASS

1. **零 Qt**：新文件 include 面全量扫描（grep 证据在案）＝core 公共头（Identity/Digest）＋自有头＋gtest＋std，Qt 零命中；`NoQtInclude_RT_BUILD_NFR_MNT_01` 双模式 PASSED；CMake 配置期 Qt 链接守卫在位（runtime/CMakeLists.txt:129）。
2. **R-1 业务域互链禁止**：产品库链接面＝core＋L1 基线（kinematics/models/sdwsim，P-RT-3 清单），无任何业务域单元；配置期 fatal 守卫（:120-128）；跨单元协作经 Sources.hpp 注入接口（命令/查询端口的最小只读形态）。
3. **R-2 跨单元私有头禁止**：仅 include `sdurws/ird/core/{Identity,Digest}.hpp`（core 公共 include 根实证在位）；`NoCrossUnitInclude_RT_BUILD_R1_R2` 双模式 PASSED。
4. **SA-02 框架零修改**：diff 无框架文件；patches/ 零触碰。
5. **R-4 前缀禁令**：新增代码无任何 RobWork 名称前缀拼接/剥离逻辑（Errors/Sources 为错误码面与注入接口，名称语义不在本任务面）。
6. **PA-1 权威唯一**：码值权威归 diagnostics StableCodeRegistry（头注释显式声明"只产出 token/建议码，不注册码值"）；单位/文案不越权；Sources 接口零写路径（错误归属唯一归 runtime 编译链，替身不自定语义）。
7. **PA-2 不可变历史**：注入接口纯只读（无写方法——类型层证据）；枚举"顺序与数值一经交付不得改动"冻结纪律（Errors.hpp:52-54）＋测试序号锚定。
8. **不继承 old/**：old/ 引用扫描零命中。

### 4.7 注释规范（AGENTS §2）—— PASS

- **公共头全查**（Errors.hpp 309 行＋Sources.hpp 175 行逐行读过）：文件头四要素齐（设计依据含需求/任务卡追溯＋"为什么"背景段）；每类/每函数 Doxygen 中文完整（@param 方向/@return 语义/@throws 触发条件）；§2.5 高危信息齐——线程约束（每类显式声明并发只读安全/纯值）、所有权（字节值传递归调用方、适配器归 L5、虚析构）、错误语义（fail-fast vs 环境错误的分类总纲＋registryCode 三空值的冻结依据）、确定性来源（switch 全枚举无 default、静态存储期、无 locale）。
- **实现文件抽查**（≥3）：Errors.cpp（逐步注释＋表项逐字对齐 §3.4/§10.11＋防御性返回的边界说明）、runtime/CMakeLists.txt（目标注册/守卫/坑位注释块）、三份测试文件（每用例头注设计依据＋用例名带 RT-ERR/RT_SRC/ACC3/NFR_COR_03 追溯字段）。无复述式/机翻腔/过期注释。

### 4.8 文档同步与 findings 核对 —— PASS（零偏差，无登记义务）

- **实现↔任务卡零偏差**：§3.3 三接口＋两值类型逐字段与原文一致；§3.4 错误契约逐 token 与原文注释列一致；§10.11 v0.3 冻结关系（12 个 1:1＋2 个 fail-fast token＋2 个事件码）与 registryCode 逐项吻合；§12 RT-T02 行产物（Errors.hpp/.cpp、Sources.hpp＋单元测试）全部落地。Errors.hpp 额外承载的 RuntimeResolveError/RuntimeNameError 及 category()/isCancellation()/isContractViolation() 判别并非偏差——§3.1 模块清单明文登记二者归 Errors.hpp，判别函数为 §5.2/§5.6/§10.11 归属表的码面物化（收窄/扩大均无）。单元卡未改动＝零偏差的正确形态；契约 status 翻转归合入收尾（PIPE §4.7）。
- **findings.json 未关闭条目相关性核对**（open 项逐条）：F-004（AUTOMOC 空转——本验收全新树日志再现 mocs_compilation 空转，仍 open，构建约定所有者辖域，本任务不可消账）；F-007（全新树配置前置——本验收被迫手工补 ini 模板＋Qt 前缀，再次实证，仍 open）；F-008（全树从零构建基线阻断——本验收口径天然规避，未触发，仍 open）；F-010（.gitignore 吞日志——实施已按其教训 `git add -f` 入库 8 份 .log（f0a4c0e 正文自述，入库文件实证），系统性修复仍 open）；F-019（ird_gates 两处既有命中——本验收实测命中面与其登记逐字一致，runtime 零涉，仍 open）；F-028（"门禁未建成"过时措辞——本任务再现同型失实，转登 **F-035**）。其余 open 项（F-005/F-006/F-012/F-015~F-018/F-020/F-023~F-027/F-029~F-033）与本任务面无涉。顺手消账：无（无一条可在 runtime 错误/注入契约辖域内闭合）。

### 4.9 commit 质量 —— PASS（建议级 2 项，见 F-034/F-035/F-036）

- 三提交正文四段（What/Why/验证/影响面）齐备且带 Acc-Covered 覆盖声明；验证结论与留痕一致——验收者逐项复现：冒烟 21/21 ✓、集成 21/21 ✓、validate-task PASS ✓（亲手复跑输出 `validate-task: PASS (1 tasks)`）、探针双红→复绿 ✓（probe 日志在冻结对象内，尾部 2 FAILED 与 [PASSED] 21 tests 同在）。
- 建议级偏差（不粉饰、方向为口径失准/少报）：①三提交标题均以名词短语开头（"runtime 错误契约落地…"/"runtime 注入契约落地…"/"RT-T02 验证留痕…"）——F-012/F-023/F-024/F-026/F-027 同型再现 → **F-034**；②"ird_gates 未执行——门禁未建成"理由与 2026-09-12 时点事实不符（门禁建成且实测可运行）→ **F-035**；③"全树 Release 构建 exit 0——DoD 第 1 条最强口径"的强度表述失准（integration-fullbuild.log 产自主树存量增量树，RWSimulatorPlugin 等产物已存在＝F-008 掩盖效应；"最强口径"应为全新树从零，验收者已另行补强）→ **F-036**。

### 4.10 留痕完整 —— PASS

冻结对象内 `traceability/builds/wp06-t02/` 共 **11 文件**：构建/配置/测试日志 8（integration-build/integration-fullbuild/integration-test-run/smoke-configure/smoke-build/smoke-test-run/probe-build/probe-test-run）＋gtest XML 2（integration/smoke 各 tests=21 failures=0）＋`ird-test-report.json`（ird-test-report/1 格式，21 用例逐条含需求/卡章节追溯与双模式结果——抽查与验收者实测数字逐条一致）。日志入库未遭 .gitignore 吞没（F-010 教训已内化，`git ls-tree` 实证 11 文件在 f0a4c0e 树内）。验收者自有复现日志位于临时目录（不入库），关键数字已抄录本记录。

### 4.11 偷懒/缩水实现扫描 —— PASS

- ① diff 全文 `TODO|FIXME|stub|placeholder|未实现|not implemented` 扫描**零命中**（grep exit=1 实证）；无需豁免条款。
- ② 空实现检测：Errors.cpp 三张表＋RuntimeError 全实现；Expected 模板全实现（工厂/判别/带前置访问器）；Sources.hpp 三接口为纯虚（＝契约声明的注入接口形态，非缩水）；Runtime.cpp 为 RT-T01 落位任务的显式占位（先例豁免在案，非本任务引入）。
- ③ acceptance 逐条证据表见 4.4——三条全部具名测试＋双模式实测，无一条仅凭"已实现"声明。
- ④ 测试强度：断言校验行为与边界（15 值五维全表、枚举序号锚定、nullopt≠空 vector、nullopt≠false、取消只增不减、原名回显、move-only）；4.5 双探针证明变异检出能力——非恒真、非"只编译通过"。

---

## 问题清单

**阻断级：无。**

**建议级（3 项，已随本 evidence 分支转登 findings.json，编号顺延自 F-033）：**

| 编号 | 摘要 | 处置建议 | 责任方 |
| --- | --- | --- | --- |
| F-034 | RT-T02 三份送验提交标题以名词短语开头（"runtime 错误契约落地/runtime 注入契约落地/RT-T02 验证留痕"）——F-012/F-023/F-024/F-026/F-027 同型再现，纯风格瑕疵，正文四段齐备无粉饰 | 与 F-012 系合并处置：实施者模板补提交标题动词开头示例；已推送历史不追溯改写 | 实施纪律（DTB §5 会话流程所有者） |
| F-035 | 提交自查行"ird_gates 未执行——门禁未建成（如实标注）"理由失实：门禁已随 WP-01-T01 建成，验收者实测 ird_gates 目标可运行（exit 1＝F-019 两处既有命中，runtime 零命中）——F-028 同型再现且首现于实现类任务（实现类本应实际执行门禁） | 实现类任务一律实际执行 `ird_gates` 并登记真实结论（红＝逐条解码归属）；AGENTS §6.3"落地前"注记随下次修订删除（F-028 处置合并） | 实施纪律＋门禁所有者 |
| F-036 | "全树 Release 构建 exit 0 零错误——DoD 第 1 条最强口径"强度表述失准：integration-fullbuild.log 产自主树存量增量树（RWSimulatorPlugin 等产物已存在，F-008 掩盖效应），构建本身真实（exit 0、日志完整），但"最强口径"应为全新树从零构建（受 F-008 阻挡暂不可达）；本验收已在全新 worktree 完成任务目标面从零复现补强 | 提交信息中证据强度表述以实际构建树性质为准（增量树/全新树显式区分）；F-008 闭合前"DoD 第 1 条最强口径"统一以"任务目标面全新树从零零错误"口径替代 | 实施纪律（与 F-025 提交信息准确性面同辖域） |

---

## 验收者环境说明

- 复现现场：worktree A＝`D:/10_Source_Repos/21_robot/acc-rt-t02-wt-a`（detached @ f0a4c0e，探针在此注入/还原，终态 git status 空）；冒烟树＝`D:/10_Source_Repos/21_robot/acc-rt-t02-smoke`；测试 XML/日志＝`D:/10_Source_Repos/21_robot/acc-rt-t02-xml/{smoke,integration}/`。记录回传：worktree B＝`acc/RT-T02/1`（基于 origin/redesign-main＝12439b7）。
- 工具链：VS2022 17.12.6（Visual Studio 17 2022 / x64）、CMake＋vcpkg 经典模式（`D:/10_Source_Repos/21_robot/RobWork/vcpkg`）、Qt 6.11.1 msvc2022_64、GTest 1.18.0（vcpkg CONFIG 包）。
- 全新集成树配置前置按 F-007 处置：手工复制 ini 模板两件＋显式 toolchain/Qt 前缀参数。
- 探针残留检查：两次注入均以精确编辑还原并以 `git status --porcelain` 为空＋复绿 21/21 收口，worktree A 终态与冻结对象字节一致。
