# 验收记录 —— UI-T09（阶段状态投影与工程用语：StageNavigationModel/StageStatusModel/UiText）· attempt 1

| 字段 | 值 |
| --- | --- |
| 任务 | UI-T09（contract：tasks/foundation/UI-T09.json，schema ird-task/1，status=ready——契约状态未被任务侧触碰，翻转归收尾〔PIPE §4.7 口径〕；interUnit=true——StageStatusModel 为供 workflow 消费的跨单元汇聚投影；requirements UX-01/UX-02/UX-12；designRefs ui.md §13/§3.5/§6.4/§6.5/§6.6/§10.2；knownPitfalls O-31/P-UI-1/P-UI-6——O-31 已裁决 2026-09-19 随整链放行） |
| 送验对象 | **e4b30c7778ed2d000077b95ce12ceea5a09ffba4**（40 位冻结；分支 wp10-t09；`git rev-parse origin/wp10-t09`＝该 SHA 逐字一致——**分支漂移检测零漂移**，远程比对实际完成） |
| diff 范围 | 494491949234c3f7b87e58a1ffcee72015c379c6..e4b30c7778ed2d000077b95ce12ceea5a09ffba4（base 与 `origin/redesign-main` 尖端逐字一致＝merge-base；range 内 2 提交：ea3489e7 实现＋测试＋ui.md v1.1、e4b30c77 留痕入库；**23 个变更文件**） |
| 验收尝试 | attempt 1（state.attempts.accept 序号 1；本验收为全新上下文，只消费产物、契约、设计文档与在库留痕＋既往**验收**记录〔UI-T08 同日先例，仅取环境口径〕，未读取实施者过程叙述；全部构建/测试/门禁/治理脚本/变异试验由验收者亲手执行） |
| 验收环境 | 复现 worktree A＝`git worktree add --detach acc-ui-t09/A` @ e4b30c77（6187 文件检出，`git log --oneline -1`＝e4b30c77 实证）；按 F-007/F-098/F-099/F-200 既有登记自主树复制未追踪 `RobWorkStudio.ini.template.static`＋`RobWorkStudio.ini.shared.in` 两件（框架 configure 硬依赖——环境链亲历，非送验对象缺陷）；集成 build 树全新配置：`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=<主仓>vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64 -DCMAKE_CXX_FLAGS="-DNOMINMAX -DWIN32_LEAN_AND_MEAN -DWIN32"`——**CONFIGURE_EXIT=0**，缓存 grep `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON` 实证；独立冒烟 build 树＝`acc-ui-t09/smoke`（vcpkg toolchain＋CMAKE_PREFIX_PATH＋Qt6_DIR——AGENTS §4.1 口径）；GUI 测试执行＝`QT_QPA_PLATFORM=windows`＋`QT_PLUGIN_PATH=<Qt>/plugins`（ui.md §12.2 既有登记口径）；ird_gates 以**脚本模式**（`cmake -DIRD_ROOT=<industrialrobot 根> -P cmake/ird_gates.cmake`，exit 1＝设计内）亲跑并按在库同款归一化（去绝对前缀＋行尾归一）比对；变异三轮在冒烟树执行（重建快、与集成树同源码同测试）；验收者日志存 acc-ui-t09/（configure-integration.log、build-integration.log、configure-smoke.log、build-smoke.log、verify-target.log、gates-run.log、mut1~3/restore-build.log 等验收临时目录，关键结论逐条摘录于本记录）；记录回传 worktree B＝`git worktree add -b acc/UI-T09/1` @ origin/redesign-main |
| verdict | **pass**（4.1～4.8 与 4.11 通过；4.9/4.10 按 F-081/F-266/F-268/F-270 家族既有建议级先例判定不阻塞：两提交的**验证结论全部经验收者亲手复现准确**——双模式构建 exit=0 零 error／契约 verify exit=0／77＋3＋25 双模式全绿／门禁命中集 41=41 归一化逐码一致 HIT-SET-IDENTICAL／validate 双脚本 PASS 无一虚报；两条新发现均为建议级，已随本 evidence 分支转登 findings.json **F-274/F-275**） |

## 4.1 改动范围 —— 通过

- `git diff --name-status 49449194..e4b30c77` 实证 **23 个变更文件**，逐个对照契约 allowedFiles：`ui/**` 9 件（CMakeLists.txt M、include/sdurws/ird/ui/IStageNavigationModel.hpp A 367 行、UiText.hpp A 212 行、UiPorts.hpp M＋150、UiTypes.hpp M＋117、src/StageNavigationModel.cpp A 418 行、src/UiText.cpp A 334 行、src/StatusProjection.cpp M＋71、test/StageNavigationModelTest.cpp A 830 行）＋`units/ui.md`（M，v1.0→v1.1 增量修订）＋`traceability/builds/wp10-t09/**` 13 件（A）——**全部在 allowedFiles 内，零越界**。
- forbiddenFiles（REQUIREMENTS.md/ARCHITECTURE.md）零触碰（23 文件列表实证，两文件均不在 diff 内；本验收对照需求原文 UX-01/02/12 以只读方式提取，未改动）；契约文件 tasks/foundation/UI-T09.json 不在 diff 内＝未被任务侧改动（status=ready 维持）。
- 框架源码（RobWork/、RobWorkSim/、RobWorkStudio/ 其余源码）零改动：diff 文件清单中无 industrialrobot/ui 之外任何源码路径；`patches/` 范围内零变更（SA-02 无登记需求）。

## 4.2 可复现构建 —— 通过（集成全新树重配＋全量构建＋独立冒烟；`: error` 计数双零）

| 步骤 | 结果 |
| --- | --- |
| 集成重配 | worktree A 全新配置 **exit 0**（补齐 vcpkg toolchain＋Qt 前缀＋CXX flags＋两件未追踪 ini 辅助件后通过——F-007/F-098/F-099/F-200 既有登记口径）；缓存 grep `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON` 实证 |
| 集成全量构建 | `cmake --build build --config Release` **BUILD_EXIT=0、`: error` 计数 0、`: fatal error` 计数 0**（RobWork 全库→RobWorkSim→RobWorkStudio→industrialrobot 全单元全新编译）；产物 `build/RobWorkStudio/bin/Release/` 下 `sdurws_ird_ui_test.exe`（1398784 B）＋`sdurws_ird_ui_contract_test.exe`（303616 B）＋`sdurws_ird_ui_gui_test.exe`（950272 B）全部在位 |
| 独立冒烟模式 | `cmake -S …industrialrobot -B acc-ui-t09/smoke -G "Visual Studio 17 2022" -A x64 -DCMAKE_TOOLCHAIN_FILE=<vcpkg> -DCMAKE_PREFIX_PATH=<Qt> -DQt6_DIR=<Qt>/lib/cmake/Qt6` 配置 **exit 0**；`cmake --build --config Release` **exit 0、`: error` 计数 0**；`smoke/ui/Release/` 下三测试可执行在位并亲跑（见 4.3——77＋3＋25 全绿） |

## 4.3 契约 verify 命令 —— 逐条亲手执行；全部成功

契约 verify 数组仅 1 条，在 worktree A 根目录按字面独立执行：

| 命令 | 结果 |
| --- | --- |
| `cmake --build build --config Release --target sdurws_ird_ui_test` | **exit 0**（VERIFY_EXIT=0、`: error` 计数 0） |

附加测试真实执行（验收者亲手，非采信送验留痕）：

- 集成树三测试可执行直接运行（QT_QPA_PLATFORM=windows＋QT_PLUGIN_PATH）：**模型层 77/77、契约 3/3、GUI 25/25 全部 PASSED**（StageNavigationModelTest 二十一用例＋UiText 六用例均在亲跑输出中在列且零失败；计数与在库三份 gtest XML〔gui XML tests=25/failures=0〕及三份 ird-test-report.json〔total=77/25、passed 同值、failed/skipped=0、decisive=true〕逐值一致；56＋21＝77 与"本任务新增二十一用例"提交声称吻合）。
- 冒烟树三测试可执行另跑 **77＋3＋25 全部 PASSED**（"集成＋冒烟双模式同口径"的提交声称经验证属实）。
- 治理脚本亲跑（worktree A 树内）：`validate-task.ps1 -TaskFile <契约绝对路径>` → **PASS (1 tasks)**；`validate-docs.ps1` → **PASS (20 units, 12 trace entries, 150 task files)**。

## 4.4 acceptance 条目 —— 3/3 逐条证据（未执行项零存在；ird_gates 已随 WP-01-T01 落地故为"已执行"）

| # | 契约 acceptance 要点 | 证据（验收者亲手） |
| --- | --- | --- |
| 1 | UI-STG-1/2：七阶段就绪/锁定投影与级联失效提示数据用例通过（§6.4 六态——completed/in-progress/blocked/unavailable/not-started/view-only；门控数据源 WP-22-T03 未产出前用桩注入）；requestNavigate 允许/拒绝时序符合 §6.4 时序图（拒绝不改变 currentStage、就地提示原因＋缺项＋下一步建议） | ①**六态投影矩阵＋桩注入**：`StageViewsFixedOrderAndGateProjection_UISTG1_UI_T09_ACC1` 亲跑通过——七项固定序＝stageIdSequence（逐位断言）＋遍历注入门控四态（completed/blocked/unavailable/not-started 逐位核对）＋用户所在位 InProgress 会话合成；桩＝StubStageGate（test/StageNavigationModelTest.cpp:73-99——契约"未产出→桩"口径的可控替身）。②**级联失效提示数据**：`BlockedCarriesReasonMissingAndNextStep_UISTG1_UI_T09_ACC1` 亲跑通过——blockingReasonKeys 逐字透传（原因键＋缺项键同列）、nextStepKey 有值/无值两态（门控未提供→nullopt 不虚构，UX-01 建议权威在 workflow）。③**view-only 行**：`ReadOnlyProjectAllStagesViewOnly_UISTG1_UI_T09_ACC1`（writable=false 全阶段 ViewOnly、门控键不裁剪）亲跑通过。④**in-progress 合成**：`InProgressSynthesizedFromCurrentStage_UISTG1_UI_T09_ACC1`（用户所在位随会话态迁移）亲跑通过。⑤**允许支时序**：`NavigateAllowedUpdatesStageAndNotifies_UISTG2_UI_T09_ACC1` 亲跑通过——Allowed＋currentStage 更新＋观察者事件恰一次、事件序＝先更新后通知（回调内拉取已是目标阶段）。⑥**拒绝支时序（承重）**：`NavigateRejectedKeepsStageAndCarriesPrompt_UISTG2_UI_T09_ACC1` 亲跑通过——currentStage 不变、observer.calls==0（无事件）、reasonKeys 两条逐字透传（原因＋缺项）、unlockHintKey 透传（下一步建议）——"就地提示原因＋缺项＋下一步建议"三要素数据面齐备；4.5 变异 MUT1（拒绝路径错误切换会话态）使本用例**真实变红**（失败点即 ：453/:454 两条承重断言）。⑦时序边界三例亲跑通过：`ReadOnlyNavigateIsViewOnlyPath_UISTG2`（RejectedReadOnly 照常切换＋事件、gate.evaluateCalls==0 门控零咨询）、`SessionPreconditionFailsFast_UI_T09_ACC1`（Opening/Closed 违约抛 logic_error、先于门控咨询）、`NavigateToCurrentStageIdempotent_UI_T09_ACC1`（幂等：不评估不发事件）。 |
| 2 | StageStatusModel 汇聚投影（§6.5）按单侧冻结数据形状实现（StageReadinessSnapshot/DomainReadinessItem，epoch 标注）；投影只消费、不拥有门控规则（N-11 无第二套）；界面零哈希/Schema/插件名（UX-02）：一切文本经 UiText::resolve 文案键解析（§3.5/§6.6，数值带单位显示、对象定位经名称端口局部名） | ①**单侧冻结形状**：UiTypes.hpp:130-186 亲读——DomainReadinessItem 五字段（domainKey/verdict/inputComplete/missingItemKeys/hasActiveTask）与 StageReadinessSnapshot（stage/domains/epoch）逐字段对齐 ui.md §6.5 原文；`ReadySnapshotAggregatesInRegistrationOrder_UI_T09_ACC2` 亲跑通过——注册序稳定拼接、五字段逐字透传、epoch 标注、未注册域空清单不虚构。②**epoch 标注**：`EpochAnnotatedOnViewsAndSnapshot_UI_T09_ACC2` 亲跑通过——views 与快照携带纪元源当前值、纪元推进后新快照反映新值（§6.2 迟到丢弃的消费方依据）。③**投影只消费（N-11）**：`GateReceivesAggregatedSnapshot_UISTG2_UI_T09_ACC2` 亲跑通过——requestNavigate 传给门控的快照＝域源数据＋当前纪元（§6.4 时序数据流替身记录面实证）；`MissingGateDependencyFailsFast_N11_UI_T09_ACC2` 亲跑通过——缺门控装配 fail-fast（"无判定"不得伪装"全部放行"）；实现零门控逻辑：StageNavigationModel.cpp:201 门控判定零加工透传、readinessSnapshot（:238-256）只搬运不判定——亲读实证"无第二套状态机"。④**UX-02 零内部名**：`BuiltinTableZeroInternalIdentity_UISTG1_UI_T09_ACC2` 亲跑通过——内建表全表键解析值零 64 位十六进制/零 Schema 字样/零 sdurws/ird:: 词形；`ArgSubstitutionDiscipline_UI_T09_ACC2` 亲跑通过——摘要形态参数 fail-fast（4.5 变异 MUT2 使本用例**真实变红**）。⑤**一切文本经 UiText**：`TransitionalLabelsRouteThroughUiText_UI_T09_ACC2` 亲跑通过——七态/九态/「无法判定」三族过渡标签值源经 resolveText 同源解析、键不变值逐字不变（P-UI-1 词表未改一字）；StatusProjection.cpp 亲读——三个过渡函数全部改经 resolveText（值源单点化）。⑥**数值带单位**：`QuantityTextWithUnit_UI_T09_ACC2` 亲跑通过——formatQuantityText 走 core::displayValueIn 唯一换算入口（mm/m 双制式同一 SI 真值、量纲违约抛 core::CoreError 不吞不改）。对象定位经名称端口：C-11 IUiNameResolver::resolveObjectId 局部名语义在 UiPorts.hpp:60-92 冻结（UI-T03 既有），UiText.hpp 文件头注释明示"对象身份到呈现名的换算不经本头"——R-4 纪律登记与本任务零违背（新增代码 RobWork 字面量 0 命中）。 |
| 3 | O-31 处置（已裁决 2026-09-19——随整链放行）：自身接口面为 ui/core 类型＋workflow 桩门控维持（卡行"未产出→桩"口径不变，真实数据源接入时按 v0.4 §10 引导注经自有端口复核）；P-UI-6 处置维持：workflow 门控三方契约未定稿前按 §6.5 单侧冻结形状实现；P-UI-1 处置维持：StageView.sevenState 七态词表按 §6.3 冻结稿 | ①**O-31 注入面**：验收者独立 grep——本任务新增产品面（IStageNavigationModel.hpp/UiText.hpp/UiText.cpp/StageNavigationModel.cpp）`sdurws/ird/` include 仅 core（Events/Units——ARCH §3.5 表内登记边）与本单元头，workflow/project/evidence 等 15 业务域**零命中**；链接面零变化：ui/CMakeLists.txt diff 亲读——仅源集/测试文件集增列，`target_link_libraries(sdurws_ird_ui …)` 段零改动；门控/域就绪以 ui 自有端口 IUiStageGate/IUiDomainReadinessSource（UiPorts.hpp C-12）承载，测试以 StubStageGate/StubDomainSource 替身注入（"未产出→桩"契约明文）。②**P-UI-6 单侧冻结**：DomainReadinessItem/StageReadinessSnapshot 落位形状逐字段＝ui.md §6.5 原文（acc2 ①证据）；对端 workflow.md 不存在——diff 内零对端文件触碰（不私改对端）。③**P-UI-1 词表冻结**：`SevenStateTokenPerStageFromFrozenEvaluator_UISTG1_UI_T09_ACC2` 亲跑通过——sevenState 经 statusWordToken 唯一求值点输出 §6.3 冻结 token（"computing"/"computable" 字面断言）、无数据源 nullopt 不虚构；七态词表文件 StatusProjection.cpp 词表行未触碰（diff 亲读——token 列零改动）；UiTypes.hpp UiStateToken 注释明示"不另立词表——NFR-MNT-03"。 |

## 4.5 测试真实失败能力 —— 通过（三处实现变异注入应失败输入，均真实变红；试验后已还原并复绿、worktree 零残留）

在验收沙箱 worktree A（git 管控下，三轮试验后 `git status --porcelain -- …/industrialrobot/` 零输出＝与送验对象逐字节一致；`grep -rn MUT` 零残留）注入三处变异，均在冒烟树重建亲跑：

| # | 变异 | 重建后亲跑结果 |
| --- | --- | --- |
| 1 | `StageNavigationModel.cpp` requestNavigate 拒绝支（⑤b）前插入 `switchTo(stage)`（"拒绝不改变 currentStage、无事件"轨被拆除） | `--gtest_filter=…NavigateRejectedKeepsStageAndCarriesPrompt_UISTG2_UI_T09_ACC1` → **FAILED**（失败点＝:453 currentStage 不变＋:454 observer.calls==0 两条承重断言） |
| 2 | `UiText.cpp` ensureNoInternalIdentity 阈值 `run >= 64` 改 `run >= 65`（UX-02 摘要形态守卫被架空） | `--gtest_filter=UiText.ArgSubstitutionDiscipline_UI_T09_ACC2` → **FAILED**（64 位十六进制摘要参数应被拒的断言即承重断言） |
| 3 | `StageNavigationModel.cpp` composeStageView 的 in-progress 合成条件改 `false && stage == current_`（§6.4 会话合成轨被拆除） | `--gtest_filter="…StageViewsFixedOrderAndGateProjection_UISTG1_UI_T09_ACC1:…InProgressSynthesizedFromCurrentStage_UISTG1_UI_T09_ACC1"` → **FAILED ×2**（views[6].status==InProgress 断言即承重断言） |

三处变异经字节还原（cp 回原文件＋git status 零差异）后重建（RESTORE_BUILD_EXIT=0）复跑：模型层 **77/77 PASSED**。三组承重断言均为「拒绝时序」「UX-02 守卫」「六态合成」语义本身——非恒真、非"只编译通过"。

## 4.6 架构红线（AGENTS §5 八条）—— 逐条通过

1. **L2 计算内核零 Qt**：本任务仅改 ui 单元（R-3 全产品唯一 Qt Widgets 例外单元）；新增模型层 StageNavigationModel.cpp/UiText.cpp 与公共头 IStageNavigationModel.hpp/UiText.hpp **零 Qt include**（亲读 include 面：标准库＋core Events/Units＋自有头——grep 实证 `#include <Qt` 零命中）；ird_gates 脚本模式亲跑（exit 1＝设计内）归一化命中集与在库 gates-hit-set.txt **逐码一致（41/41 唯一命中 IDENTICAL，CRLF 归一后 diff exit 0）**：新文件零命中——UiText.cpp/StageNavigationModel.cpp 未出现在任何 R3 命中行（对比基线中 WorkbenchShell.cpp/CommandPalette.cpp 等 Qt 头包含命中同型既有项）。
2. **业务域互链禁止（R-1）**：本任务新增产品面 include 扫描——`sdurws/ird/` 命中仅 core（Events/Units）与 ui 自有头，15 业务域**零命中**；本任务新增边＝零（链接段零改动，acc3 ①证据）。
3. **跨单元私有头禁止（R-2）**：零跨单元 include（全部 `include/sdurws/ird/<unit>/` 公共路径）；两新公共头落位 include/sdurws/ird/ui/；无跨单元私有面暴露。
4. **框架零源码修改（SA-02）**：diff 内零框架文件、patches/ 零变更（4.1 实证）。
5. **不做名称前缀拼接/剥离（R-4）**：ui diff 新增代码行 `RobWork` 字面量 **0 命中**（grep 计数实证）；ird_gates 亲跑 R4 项仅 project 既有 1 处（ProjectStoreImpl.cpp——与基线同值，非本任务文件）。
6. **权威唯一（PA-1）**：门控判定权威归 workflow（IUiStageGate 端口注入，ui 零加工透传——N-11 无第二套）；运动学/名称语义零触碰（对象定位经 C-11 名称端口登记语义）；文案值权威单点＝UiText 内建表（三过渡函数值源收拢、StatusProjection 本地静态值表删除——第二值源消除）；单位换算权威＝core displayValueIn 唯一入口。
7. **不可变历史（PA-2）**：本任务零修订数据触碰（23 文件清单——无 project/ 修订面文件）；投影面只产只读快照值拷贝。
8. **不恢复 old/**：diff 新增行 `old/` 引用 **0 命中**（计数实证）；实现全部从零编写。

## 4.7 注释规范（AGENTS §2）—— 通过（新增公共头 IStageNavigationModel.hpp/UiText.hpp 全查＋UiPorts.hpp/UiTypes.hpp 增量段全查＋实现全文 2 件（StageNavigationModel.cpp 418 行、UiText.cpp 334 行）＋StatusProjection.cpp 改动段＋测试文件全文＋CMakeLists diff——覆盖"公共头全查＋实现抽查 ≥3"）

- **文件头**：全部新增/大改件均有中文 `@file/@brief`＋设计依据（ui.md §3.5/§6.4/§6.5/§6.6/§10.2/§16.7 v1.1、需求 UX-01/02/12/ERR-01/NFR-COR-02、任务契约 acceptance 1/2/3、O-31/P-UI-1/P-UI-6 处置逐条对应）＋背景说明（"为什么导航是投影＋会话态而不是状态机""为什么键→值要收口到一个函数""为什么无锁且无快照缓存""为什么内建表是过渡承载"等"为什么"层注释齐备且与技术事实一一对应）。
- **类/函数 Doxygen**：公共头逐结构体/逐函数 `@brief/@param [in]/@return/@throws/@note` 语义完整（StageView 六字段合成规则四步、NavigateResult 三态后置条件、requestNavigate 五步时序与两类 fail-fast 异常、readinessSnapshot 线程契约、resolveText 两形态替换纪律、formatQuantityText 三步与量纲违约口径、ensureNoInternalIdentity 的可机械化边界论证、StageNavigationModelDeps 七依赖逐项所有权语义）——review 者可据此判断实现是否符合设计，本验收 4.4 的逐条核对即以该契约头为基准完成。
- **§2.5 高危信息表**：物理单位（SI 真值/显示单位逐处显式——"SI 真值量值""显示单位 token""6 位有效数字"）；坐标系（本任务无坐标系面——如实无缺失）；角度制式（无裸角度数值——单位经 core 词表 token）；所有权（端口裸指针"注入方持有并保证存活期覆盖模型（模型不接管所有权——裸指针只读借用）"、观察者弱引用"观察者析构前必须退订或保证存活至句柄析构"、订阅句柄 RAII 语义逐处注明）；线程约束（"UI 线程方法串行假设（M-1）""readinessSnapshot 并发安全（不触碰共享可变状态）""端口调用一律 UI 线程""内建表编译期固定、全部函数可重入"逐处注明）；错误语义（调用方装配违约 fail-fast invalid_argument/logic_error 与门控拒绝正常业务值的**两轨划分**逐函数注明——含"伪装成 Rejected 会掩盖时序缺陷"的归类理由、core::CoreError 不吞不改）；确定性来源（to_chars locale 无关＋理由、6 位有效数字与 FormEditCommon 同口径、注册序＝稳定呈现序 NFR-COR-02、同键同文逐处注明）。
- **魔法数字**：64 位十六进制阈值（"SHA-256 摘要的呈现形态——CON-05 内容寻址身份在本产品的唯一哈希形态"来源注释）、26 键表规模与 keys.reserve(26)（五键族计数注释）、40 字节缓冲（"double 全值域在 40 字节内恒可容纳"假设注释）、deque 选型（"尾部追加不失效元素引用——句柄持有的 ObserverRecord 指针因此保持有效"结构性理由注释）——逐个注明依据。
- **反例扫描**：零复述式注释、零无编号 TODO（全 diff `TODO|FIXME|XXX|HACK` 零命中）、注释与代码一致。一处既有偏差如实登记（转登 **F-275**）：CMakeLists AUTOMOC 复核注释行"十二个源文件"与 src/ 实际 13 个产品 TU 不符（F-273 同一注释行的第二次沿袭——本任务 +2 增列时计数同步改写十→十二但绝对值仍未校准；实质性主张"零 Q_OBJECT"经验收者 grep 证实为真，零行为影响）。

## 4.8 文档同步 —— 通过（零偏差＋增量修订已登记＋findings 未关闭条目逐条核对）

- **单元卡增量修订**：`units/ui.md` v1.0→v1.1（六落点亲读实证：文档头版本行 v1.1、§3.3 公共头布局四行增注〔UiTypes 增量/UiPorts C-12/IStageNavigationModel.hpp/UiText.hpp〕、§3.5 UiText 落位注、§6.4 UI-T09 落位登记注〔六态合成口径/只读导航读法/端口注入三项〕、§6.5 UI-T09 落位登记注〔单侧冻结形状落位＋N-11 结构自证〕、§16.7 v1.1 行——既有语义内容零改动，§13 UI-T09 卡行原文未触碰）；v1.1 行登记的承载形状（两契约头逐件/C-12 端口/两实现 TU/值源迁移/二十一用例/77＋3＋25/HIT-SET-IDENTICAL）与实现及本验收亲测结果逐一比对一致。实现与任务卡零未登记偏差（acceptance 逐条对应见 4.4）。
- **findings.json 未关闭条目相关性核对**（238 条 open 逐类过筛）：①F-081/F-266/F-268/F-270（留痕 .log 家族，UI 系）——**本任务复踩**（e4b30c77 正文/README 所引构建与门禁 .log 未入库，验证结论本身经复现全部属实）→ 转登 **F-274**；②F-273（CMakeLists 注释计数漂移）——**同一注释行二次沿袭**（十/11→十二/13）→ 转登 **F-275**；③F-227/F-259（ird_gates 机器面滞后）——本任务零新增命中、沿"逐码比对零变化"既有口径留痕，无新增动作需求（责任方 WP-01-T03 不变）；④F-010（.gitignore *.log 根因）——仍 open，为 F-274 根因，处置建议随 F-274 登记指向该根因；⑤其余 open 条目与本任务范围（ui 模型层投影/文案）无涉。历史发现零静默积压。

## 4.9 commit 质量 —— 通过（两提交标题动词开头＋正文四段齐备；验证结论与验收者亲测逐项一致；一处文件清单失准按家族先例判建议级）

- ea3489e7「[WP-10-T09] 实现阶段状态投影与工程用语（…）」——动词"实现"开头；正文四段（What 按 9 文件列要点/Why 对 UX-12/01/02 与 ui.md 章节及 O-31/P-UI-1/P-UI-6/How 双模式构建＋77＋3＋25＋门禁 HIT-SET-IDENTICAL＋validate PASS/影响面两公共头两 TU＋值源迁移）齐备；**验证段全部结论经本验收亲手复现属实**（构建 exit=0、测试计数、门禁 41=41、validate PASS）。
- e4b30c77「[WP-10-T09] 入库执行留痕（…）」——动词"入库"开头（非 F-267/F-271 家族的名词短语形态）；正文四段齐备；**验证结论属实**，但 What 段所列"构建三件 .log／门禁 log 两件／validate-task.log／console.log"实际未入库（F-010 .gitignore 根因）——文件清单与入库内容不一致为 F-081/F-266/F-268/F-270 家族第 4 次复踩，**不构成粉饰**（结论可独立复现且在库 XML/JSON/gates 证据链完整），判建议级转登 F-274，不阻塞。
- 代码与测试同一提交（ea3489e7 内同含实现与 830 行测试）；无空话提交。

## 4.10 留痕完整性 —— 通过（按家族先例；在库件与我实跑逐值一致）

- 在库留痕 13 件亲验：三份 gtest XML（gui XML tests=25/failures=0 亲读；模型/契约 XML 同格式）＋五份 ird-test-report.json（total=77/25、passed 同值、failed/skipped=0、decisive=true，冒烟同名件同值）＋gates-hit-set.txt（41 条）＋gates-hit-set-diff.txt（HIT-SET-IDENTICAL 结论与验收者亲跑归一化 diff 一致）＋README-evidence.md——**与我双模式亲跑结果逐值一致**。
- 构建日志（integrated-build.log 等）未随库：F-081/F-266/F-268/F-270 家族先例（根因 F-010），本验收以亲手重跑补足复现证据，判建议级（随 F-274 转登），不阻塞。
- `phase-one-validation.log` 未更新——该文件自 WP-03-T01 后即停更（git log 实证），UI-T03~T08 各次验收同状态，无 resultNote 机制；按既有各次验收同口径不另溯责。

## 4.11 偷懒/缩水实现扫描 —— 通过

- ①**标记扫描**：全 diff 新增行 `TODO|FIXME|XXX|HACK|stub|placeholder|未实现|not implemented` 扫描——命中仅两类，均为**契约显式豁免**：`StubStageGate/StubDomainSource`（测试替身——契约 acceptance 1 明文"门控数据源 WP-22-T03 未产出前用桩注入"，且为受控替身非空实现：记录 evaluate 输入供断言）与 `parsePlaceholder/hasPlaceholder`（{0} 位置占位符替换引擎的领域词汇，非 stub 语义）；`TODO/FIXME/未实现/not implemented` 零命中。
- ②**空实现检测**：新增公共接口 20 个方法/函数逐一亲读——全部有实质实现（stageViews 七项合成/requestNavigate 五步时序/subscribe RAII 登记/readinessSnapshot 搬运/resolveText 查表＋替换/ensureNoInternalIdentity 滑窗/formatQuantityText 三步组装等），无"仅返回默认值"的空壳；两处防御性返回（stageToken 越界空串、StageGateView 默认 NotStarted）均有"安全空值"语义注释且被测试覆盖（越界防御属契约声明面）。
- ③**逐条证据表**：见 4.4——acceptance 三条每条均有具名测试（21 个 `_UI_T09` 后缀用例逐一在亲跑输出在列）＋代码行号/文件行号证据，任一条无证据即 fail 的条件未触发。
- ④**测试强度**：断言校验行为与边界（拒绝时序双断言 currentStage＋事件数、幂等断言 evaluateCalls==0、透传逐字断言、fail-fast 断言异常类型、epoch 推进断言、字段逐项断言）；非恒真、非"只编译通过"——4.5 三处变异全部真实变红实证承重性。

## 发现的问题清单（分级）

| 级别 | 编号 | 内容 | 处置 |
| --- | --- | --- | --- |
| 阻断 | 无 | — | — |
| 建议 | F-274 | 留痕 .log 家族第 4 次复踩（e4b30c77/README 所引构建/门禁/validate/console 日志未入库；根因 F-010；验证结论全部复现属实） | 已转登 findings.json；不阻塞合入；根因随 F-010 治理消账 |
| 建议 | F-275 | CMakeLists AUTOMOC 注释计数漂移第二次沿袭（"十二个源文件" vs 实际 13 TU；"零 Q_OBJECT"主张 grep 证实为真，零行为影响） | 已转登 findings.json；后续触碰该注释块时随手校准 |

## 验收者环境说明

- 全新上下文：未读取实施者任何过程叙述；仅消费契约、设计文档（REQUIREMENTS/acceptance-protocol/units/ui.md）、在库留痕与既往**验收**记录（UI-T08 记录仅取环境配置口径）。
- 复现优先于采信：集成配置/全量构建/契约 verify/双模式三组测试/ird_gates 脚本模式与命中集归一化比对/validate 双脚本/三处变异试验均亲手执行（日志存验收临时目录 acc-ui-t09/，关键结论已摘录本记录）；测试真实失败能力经三处变异注入实证。
- 送验对象冻结核验：验收全程按 e4b30c77（detached worktree），`origin/wp10-t09` 与该 SHA 逐字一致（零漂移）；验收者零提交至任务分支（本记录与 F-274/F-275 仅落 acc/UI-T09/1 evidence 分支）。
