# WP-13-T15 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | WP-13-T15 实现 modeling 插件界面面板与命令接入（unit=modeling；MDL-07） |
| 送验对象（冻结） | `cb40fb457ae4f7d4b60104dd91a96a6aaa937676`（分支 wp13-t15 尖端；验收全程按 SHA 不按分支名） |
| diff 范围 | `f3b87dfb3340831174dde01197e81db03ab78cfd..cb40fb457ae4f7d4b60104dd91a96a6aaa937676`（state.base..headSha，3 提交 27 文件） |
| 验收者 | 独立验收会话（全新上下文；只消费契约/产物/设计文档，不消费实施叙述；ACC §2 四条独立性要求逐条满足） |
| 裁决 | **verdict = pass**（4.1～4.11 全过、零阻断级问题；建议级 3 项已转登 findings.json：F-352/F-353/F-354） |
| 记录分支 | `acc/WP-13-T15/1`（基于 origin/redesign-main @ f3b87dfb；记录落盘后 commit＋push，commit SHA 见文末三元组） |

## 0. 验收执行环境（ACC §3）

- 复现现场：`git worktree add --detach D:/10_Source_Repos/21_robot/acc-t15-wtA cb40fb457ae4f7d4b60104dd91a96a6aaa937676`（detached @ 40 位 SHA）。
- 集成模式：**全新构建树** `D:/10_Source_Repos/21_robot/acc-t15-build` 重新配置
  `cmake -S <wtA>/RobWork -B acc-t15-build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64 -DCMAKE_TOOLCHAIN_FILE=<主检出>/vcpkg/scripts/buildsystems/vcpkg.cmake`；
  缓存确认 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`（grep CMakeCache.txt 实测）。
- 独立冒烟模式：另配 `D:/10_Source_Repos/21_robot/acc-t15-smoke`，带 vcpkg toolchain＋Qt6 前缀（配法与在库冒烟日志 build-smoke-t15 的 CMakeCache 一致）。
- 环境补做登记（非实施产物改动）：worktree 缺 gitignored 的 `RobWork/RobWorkStudio/bin/RobWorkStudio.ini.template.static`（`.gitignore` 第 79 行 `[Bb]in/` 规则使 tracked 树无此文件）导致首次 configure 报错退出码 1；从主检出复制 `RobWorkStudio.ini.template.static`＋`RobWorkStudio.ini.shared.in` 两文件入 worktree 后 configure 通过（exit 0）。worktree 内源码零改动（变异测试窗口外 `git status` 干净，见 §4.5）。
- ird_gates 口径：按 ACC/DTB §4.5 与门禁引擎自述，**引擎直跑**（`cmake -DIRD_ROOT=<IRD 根> -DIRD_SELFTEST_DIR=<临时> -P cmake/ird_gates.cmake`）base（f3b87dfb，临时 worktree `acc-t15-wt-base`）..head（wtA）归一化（IRD_ROOT 路径剥离）比对，逐码出具结论（§gates 节）。

## 4.1 改动范围 —— PASS

- `git diff --name-status f3b87dfb..cb40fb45`：27 文件全部落在契约 `allowedFiles` 内——
  `modeling/**`（CMakeLists.txt、contract_test/BuildGraphContractTest.cpp、plugin/ 12 文件、test/PluginPanelTest.cpp、test/TestMainReport.cpp）、
  `units/modeling.md`、`traceability/**`（builds/wp13-t15/ 12 文件）。无 allowedFiles 外文件。
- `forbiddenFiles`（REQUIREMENTS.md/ARCHITECTURE.md）零触碰；框架源码（industrialrobot 之外）零改动；`patches/` 无新文件（SA-02 无登记需求）。

## 4.2 可复现构建 —— PASS

- 集成模式（重新配置后）：
  `cmake --build acc-t15-build --config Release --target sdurws_ird_modeling_plugin sdurws_ird_modeling sdurws_ird_modeling_test sdurws_ird_modeling_contract_test` → **BUILD_EXIT=0，error C/LNK 计数 0**（含 plugin 目标 AUTOMOC——日志见 `Automatic MOC for target sdurws_ird_modeling_plugin` 与 `sdurws_ird_modeling_plugin.vcxproj -> ...Release\sdurws_ird_modeling_plugin.lib`）。
- 独立冒烟模式：全量构建 `cmake --build acc-t15-smoke --config Release` → **SMOKE_BUILD_EXIT=0，error 计数 0**（含 `Automatic MOC for target sdurws_ird_modeling_plugin`）。
- 结论留日志：验收者侧 `acc-t15-build-integrated.log`／`acc-t15-smoke-build.log`（验收会话工作文件，未入库；计数摘录见 §4.10）。

## 4.3 契约 verify 命令 —— PASS（7 条逐条独立执行）

| # | 命令 | 实测结果 |
| --- | --- | --- |
| 1 | `cmake --build build --config Release --target sdurws_ird_modeling_plugin` | 通过（增量无错，.lib 产出） |
| 2 | `... --target sdurws_ird_modeling` | 通过（.lib 产出） |
| 3 | `... --target sdurws_ird_modeling_test` | 通过（.exe 产出） |
| 4 | `ctest --test-dir build/RobWorkStudio/src/rwslibs/industrialrobot/modeling -C Release -L "^ird$"` | **2/2 通过**（sdurws_ird_modeling_test＋sdurws_ird_modeling_contract_test） |
| 5 | `... --target ird_gates` | 退出码 1＝**存量命中既有状态**（DTB §4.5 存量例外未清零）；逐码归一化比对见下节——base..head 恰增 2 处且均为契约 acceptance 1 明文 sanctioned 面，**零未登记新增命中** |
| 6 | `pwsh -File .../validate-task.ps1 -TaskFile .../WP-13-T15.json` | **PASS**（exit 0） |
| 7 | `pwsh -File .../validate-docs.ps1` | **PASS**（20 units, 12 trace entries, 189 task files） |
| 补 | 契约 verify 缺 `sdurws_ird_modeling_contract_test` 构建行（第 4 条 ctest 用例集含该目标）——F-350/F-351 家族在 T15 复发 | 验收者在全新构建树**自行补建该目标并执行**（14/14，见 §4.4）——执行未受阻；家族编号转登 F-352 |

### ird_gates 引擎直跑归一化比对（base f3b87dfb → head cb40fb45）

- base 直跑（临时 worktree，用毕即删）：命中行去重 **56 条**；head 直跑（wtA）：**58 条**；与送验在库 `traceability/builds/wp13-t15/ird_gates-base-normalized.txt`（56）／`ird_gates-head-normalized.txt`（58）**逐行集合一致**（验收者 ASCII 骨架逐行 diff 实证 IDENTICAL，编码差异仅控制台码页）。
- diff base..head：**恰增 2 处（各以引擎双相位输出呈现为 2 行×2）**：
  1. `IRD-GATE-R1: 业务域目标互链：sdurws_ird_modeling_plugin → sdurws_ird_modeling`——插件目标链接**本单元**计算库的自边；R-1 红线语义＝业务域单元**之间**互链禁止，同单元自边系卡 §3.2 二分结构"插件消费计算库"的 sanctioned 面（引擎按两端业务域粗分类与互链共用码面）。
  2. `IRD-GATE-SUB: 表外依赖边 modeling->ui（sdurws_ird_modeling_plugin → sdurws_ird_ui）`——acceptance 1 明文链接面（卡 §3.2 原文），与 DTB §4.5 既有登记行 ui->ui／ui->project（UI-T15/T16）同型；登记册回填归 WP-01-T03 治理面（本任务 allowedFiles 不含 whitelist 与 DTB，**未私改**——diff 实证两文件零触碰）。
- 其余全码面（R-2/R-3/R-4/R-5/T-1/T-2/GRAPH/LIB/SA02）零新增命中。**验收者独立判定：两处均经契约/治理面 sanction，无未登记新增。**

## 4.4 acceptance 数组逐项证据表 —— PASS（5 条全部有证据）

| # | acceptance 要点 | 证据（具名测试/命令输出/文件行号） |
| --- | --- | --- |
| 1 | 插件目标落位（Qt Widgets、AUTOMOC、仅插件链接 Qt；链接面＝modeling＋ui；零计算逻辑） | CMakeLists.txt：`add_library(sdurws_ird_modeling_plugin STATIC ...)`＋`AUTOMOC ON`（仅该目标）＋单行 `target_link_libraries(sdurws_ird_modeling_plugin PUBLIC sdurws_ird_modeling sdurws_ird_ui Qt6::Core Qt6::Gui Qt6::Widgets)`；计算库 `sdurws_ird_modeling` 链接面不含 Qt（include/＋src/ grep Qt 零命中，验收者实测）；`MdlBuildGraph.NoBusinessUnitOrExtraPlatformEdge`／`UnitEdgesSixRegisteredAndClosed` 行级钉住 ui 边仅落插件链接语句（contract_test 14/14 内）；ird_gates 恰增 2 处均 sanctioned（§4.3 节） |
| 2 | 五区信息架构 UT（树锚/属性投影/就绪条/预览类型化） | test/PluginPanelTest.cpp 7 条：`Tree_ObjectIdAnchorAndEngineeringLabels`（锚＝ObjectId 逐字节一致、分组无锚）、`Tree_HashLikeLabelRejectedByUX02Guard`（64 位十六进制名即抛）、`Properties_JointRelevantFieldsAndDHGrey`（六字段面＋StandardDH 下 axis/origin ReadOnlyGrey＋速度/加速度不属关节面）、`Properties_LinkProvenanceBadges`（未提供占位/估算徽标直投）、`Properties_ToolSceneDrivetrainProjection`（§4.4/§4.5/§4.7 面）、`ReadinessBar_LayersCountsAndJumpTargets`（12 层行＋三组计数＋subject 跳转/无主体不伪造）、`Preview_AppliedRevisionOnly_DMdl10`（空修订空页＋`buildPreviewPage` 只收 `AppliedRevisionView` 类型强制）——246/246 内全过 |
| 3 | 数据流抽测 UT（L-1/L-2/L-3/L-8；L-8/L-9 GUI 登记为 V-30 不启动） | `L1_SelectionSessionStateZeroRevision`（工作集值快照逐字节不变＋changes 空＝零修订）、`L1_ReverseLocateScrollAndHighlight`（LocateTarget 同锚双产出＋无效锚不伪造）、`L2_EditAcceptedIncrementalRefreshAndDirty`（"joints[0]"＋dirtyCount=1＋changes=1）、`L2_EditRejectedInlineErrorValuePreserved`（"limit-interval-invalid"＋工作集字节不变＋无模态路径）、`L3_ConfirmableThreeElementsViaBridgeMarshal`（真 ui::CommandInteractionBridge＋替身呈现器＋主线程泵送：三要素 42/40/N·m 在 UI 线程可观察＋凭据回传 principal=tester）、`L8_CentroidInlineChoiceModelData`（dry-run 预览＋三分支分流）；L-8/L-9 GUI 呈现＝V-30 登记不执行（卡 §10.2 V-30 行"本次不启动"＋测试文件头范围声明；无任何 GUI 执行留痕冒充——契约 note ④ 口径） |
| 4 | 域命令登记 UT（十条点分/卡表 readOnlyAllowed/SA-16/L-7/两套命名空间） | `Commands_TenDottedCommandIds`（十条逐行断言卡 §9.7.3 表序＋点分小写正则＋无重复；export/import-package 两条独立）、`Commands_ReadOnlyAllowedPerTable`（三真七假逐行）、`Commands_NoDefaultGlobalShortcut_SA16`（defaultShortcut 全空＋bindable=true）、`Commands_NamespaceSeparationFromProjectTokens`（无点词形＋词表零交叉＋kCmdApply* 冻结常量直用）、`Commands_ReadOnlyDisablesEditControls_L7`（Editable→ReadOnlyGrey 降级＋DH 灰显不复活）、`Registration_DescriptorShapeAndReadinessProjection`（pluginId/stage/能力三全＋就绪三态映射） |
| 5 | 刷新与真值纪律 UT（L-4 保序重演/零缓存/UI 线程守卫） | `Refresh_EventDrivenReplayInOrder_L4`（终值=0.9 保序实证＋刷新恰一次＋队列不自动消费）、`Refresh_ReplayFailureManualIntervention`（BlockedAtEdit 停在第 2 条＋队列原样保留 2 条＝不静默丢弃＋失败态仍刷新）、`Model_NoAuthoritativeDataCache`（同输入同输出＋变更后重投影立即反映）、`Thread_UiThreadOnlyConstraint`（跨线程 assertOnUiThread 即抛） |

## 4.5 测试真实失败能力（变异测试） —— PASS（2 处实质变异，均真实变红，全程还原）

| 变异 | 注入点 | 方式 | 结果 |
| --- | --- | --- | --- |
| A（门控三态篡改） | plugin/PanelCommandCatalog.cpp `applyReadOnlyGate` | `if (!writable)` → `if (writable)` | 增量重建后 `--gtest_filter=PluginPanel.Commands_ReadOnlyDisablesEditControls_L7_WP13T15_ACC4` → **FAILED 1 test**（具名用例捕获门控取反） |
| B（刷新重演"不静默丢弃"篡改） | plugin/PanelRefresh.hpp `PanelRefreshCoordinator::onRevisionEvent` 拒绝分支 | 在 `m_manualIntervention = true;` 前注入 `m_pending.clear();`（静默丢弃队列） | `--gtest_filter=PluginPanel.Refresh_*` → **FAILED 1 test：Refresh_ReplayFailureManualIntervention_WP13T15_ACC5**（`pending().size()==2` 断言捕获；保序用例仍绿——断言靶向精确） |
| 还原 | `git restore` 两文件；`git status --short` 干净（零残留）；重建后全量 smoke `sdurws_ird_modeling_test` → **200/200 PASSED**（恢复绿色） | | |

- 变异窗口时序（T14 教训）：变异在集成构建与冒烟构建均完成后进行；每次变异→只重建 smoke `sdurws_ird_modeling_test`→跑过滤用例→立即 `git restore`；还原后全量重跑确认 200/200。worktree A 源码与送验 SHA 逐字节一致（git status 干净）。

## 4.6 架构红线（AGENTS §5 八条） —— PASS

1. **L2 零 Qt**：modeling `include/`＋`src/` grep `#include <Q`／Qt 零命中（验收者实测）；Qt 仅 plugin/ 与测试目标（AUTOMOC 仅插件目标）——R-3 门禁 R3 码面零新增命中（§4.3 节）。
2. **业务域互链（R-1）**：插件链接面＝本单元计算库＋ui＋Qt（卡 §3.2 明文）；R-1 引擎恰增 1 处为同单元自边（sanctioned，见 §4.3 节）；无任何其他业务域单元链接（BuildGraphContractTest 业务域七单元断言全过）。
3. **跨单元私有头（R-2）**：plugin/ 12 文件全部 include 面验收者逐条核对——ui 侧 `ICommandRegistry.hpp`/`IWorkbenchShell.hpp`/`UiTypes.hpp`/`UiText.hpp`、project 侧 `CommandService.hpp`、core 侧 `Identity.hpp`/`Provenance.hpp` 均在 `ui|project|core/include/sdurws/ird/.../`（公共头，文件存在性逐个实测）；modeling 侧全为本单元公共头或同目录插件私有头（不入跨单元）。
4. **SA-02**：diff 无框架源码触碰；patches/ 无新文件；SA02 码面引擎直跑零新增命中。
5. **R-4 名称前缀**：plugin/ 与新测试 grep "RobWork" 仅注释引用一处（§9.7.4 卡文引用，非代码拼接）；R4 码面 base..head 零新增。
6. **权威唯一（PA-1）**：面板零业务判定（编辑裁决唯一经 applyJointFieldEdit/applyCentroidEdit——PanelEditFlow.cpp 编排序注释与代码一致；就绪判定零参与直投；身份分配/编码门不私设——buildDraftCommand 组装零判定；基线闭包重建归装配层 provider 注入）。
7. **不可变历史（PA-2）**：插件零修订写路径（PanelSelection/编辑流不触命令提交；预览唯一数据源＝已应用修订 AppliedRevisionView——D-MDL-10 结构强制）。
8. **不继承 old/**：diff 无 old/ 引用。

## 4.7 注释规范（AGENTS §2） —— PASS

- 新增公共头（plugin/ 7 个 .hpp——虽为单元私有面仍全查）：文件头设计依据/背景/线程约束/确定性注齐备；每个类/结构体/函数中文 Doxygen（@brief/@param 方向/@return/@throws）。
- §2.5 高危信息抽查（全部命中）：物理单位带单位（`kg·m²`、`m`、`rad`、`N*m`——PanelEditFlow.hpp:152、PanelModel.hpp:147 等）；坐标系（"连杆系""世界系固连"——PanelEditFlow.hpp:162、PanelModel.cpp:357）；所有权（"非 owning——调用方保证存活"——PanelSelection.hpp:80、PanelRefresh.hpp:123、ModelingPanelWidget.hpp:149 raw 指针＝Qt 父子所有权）；线程约束（"仅 UI 线程访问（§3.4）"逐头标注＋PanelUiThreadGuard 运行期执行面）；错误语义（fail-fast vs 值面分类——PanelUiThreadGuard 注、PanelModel.cpp 缺失态占位不伪造）；确定性来源（std::to_chars locale 无关、排序键＝localName 字典序 stable_sort——PanelModel.cpp:37/153）。
- 实现文件抽查 ≥3：实读 5 个（PanelModel.cpp/PanelEditFlow.cpp/ModelingUiModule.cpp/ModelingPanelWidget.cpp/PanelCommandCatalog.cpp）——语义段落注释解释"为什么"（如 ModelingPanelWidget.cpp:303"先回显权威值——拒绝时保持原值的强顺序保证"），魔法数字有来源（kAnchorColumn=1 隐藏锚列、spinGuard=20000 泵送上限）。无 §2.6 反面示例形态。

## 4.8 文档同步与 findings 消账 —— PASS（含 1 处已登记偏差核实）

- units/modeling.md 升 **v0.17**（diff 实证仅版本头＋§14.6 追加行）：登记内容与实现逐项对得上——插件目标/AUTOMOC/链接面/plugin 五件/门禁恰增 2 处/BuildGraphContractTest 随附同步/23 具名用例/属性字段划界/冒烟 boost 头注入/留痕数字（两组计数散文漂移见 F-353/F-354，实质主张全部核实为真）。
- **属性字段面偏差已按 DTB §5.4 登记**：卡 §9.7.1 行"关节→…速度/加速度"与实现（速度/加速度归传动 MDL-16——关节六字段含 origin/working-range）的划界偏差在 §14.6 v0.17 ⑥ 登记（引 §15 v0.4 ③c＋Template.hpp 同口径），并有 `Properties_JointRelevantFieldsAndDHGrey` 反向断言钉住——**登记在案，非未登记偏差**。
- P-MDL-8 接缝按契约 note ③：PanelSelection（ui SelectionModel 未落位→最小会话态承载）/PanelRegistrationRecord（§10.9 形状暂持，AboutDialog 先例同案）/ModelingUiModule（§11.2 三方法同形，落位后改继承）——各头注明 R-MDL-1 同步路径；allowedFiles 内合规处置。
- **findings.json 未关闭条目相关性核对**（282 open → 本任务相关）：①F-300/304/…/350/351 家族（契约 verify 缺构建行）在 T15 复发→**转登 F-352**；②F-342/343/349 散文计数家族在本次送验散文复发两例→**转登 F-353/F-354**；③F-278/280/282 文件尾换行族：本任务新增文件（plugin/ 12＋测试 2）尾字节实测均正常（无新增同族）；④P-MDL-8 相关在案条目（如 F-349 关联的 Description axis provenance 位）与本任务无新触碰面。无静默积压。
- 任务状态/追踪矩阵：契约状态翻转与 state.json 收尾归 PIPE §4.7⑤（所有者合入段）——送验 diff 内不含属正常（T12～T14 同口径）；卡 §11/§13 T15 行与追踪矩阵行（MDL-07→V-30→T15）为既有登记，本任务未收窄。

## 4.9 commit 质量 —— PASS（计数散文漂移转登 F-354，不构成粉饰）

- 3 提交均为 `[WP-13-T15] 动词开头` 标题＋四段正文（What/Why/验证/影响面）：
  `be8aa7cd`（实现＋23 具名用例）、`e73c6c7d`（文档同步 v0.17）、`cb40fb45`（留痕归档）。
- 验证结论与留痕一致性：集成 246/246＋14/14＋260/260、ctest 2/2、validate 双 PASS、门禁恰增 2 处——**全部经验收者亲手复跑证实**（§4.3/§4.4/§4.10）。两处散文计数漂移（冒烟 246/246 实为集成数、分组 6/5/6/4 实为 7/6/6/4）如实转登建议级（F-353/F-354）——在库日志与 XML 无任何虚假通过计数，两模式真实全绿，无"带病标注通过"。

## 4.10 留痕完整 —— PASS

- `traceability/builds/wp13-t15/` 12 文件在库：gtest XML×2（原样留存——modeling_test.xml tests=246/failures=0/PluginPanel 23 条；contract_test.xml tests=14/failures=0，验收者解析实证）、ird-test-report×3（合并 260/260＋两目标原始）、构建日志×2（集成四目标＋ctest 2/2；冒烟全量＋ctest 2/2）、ird_gates 比对 3 件（56/58/comparison.md——与验收者直跑集合一致，§4.3 节）。
- 验收者复跑计数：集成 exe 直跑 **246/246＋14/14**；冒烟 exe 直跑 **200/200＋8/8**（T08 集成专属 gating 46 条冒烟不编入——与 T14 的 223/177 差值同口径；在库 changelog 冒烟括注写 246/246 系散文失准→F-353）。
- `phase-one-validation.log`：WP-03-T01 时期专用聚合口径，不适用本任务（T08～T14 先例同口径）；state.json 无 resultNote 字段——如实登记，不虚构"已更新"。

## 4.11 偷懒/缩水实现扫描 —— PASS

- ①TODO/FIXME/XXX/HACK/stub/未实现/not implemented 扫描（diff 新增行，大小写不敏感）：实质命中**零**（唯一命中 `toDouble` 系 "toDo" 子串误报）；`placeholder`/`占位` 命中均为卡 §5.2 sanctioned 的 `modeling.generate-placeholder-geometry` 命令名与"缺失态占位不伪造数值"呈现语义——契约显式语义面，逐条列据豁免。
- ②空实现检测：公共接口函数体无空/恒默认值实现（PanelModel/PanelEditFlow/PanelCommandCatalog/PanelRefresh 全部实质实现；ModelingUiModule::onShellReady 为壳引用接线一行——有语义，非占位）。
- ③逐条证据表：acceptance 5 条全部有具名测试/命令输出佐证（§4.4 表）——无"已实现"声明无证据项。
- ④测试强度：断言校验行为与边界（字节级工作集不变、终值保序 0.9≠0.5、队列保留 2 条、三要素 42/40/N·m、跨线程即抛等）；4.5 变异联动证实非恒真（两处变异均真实变红）。

## 发现的问题清单（分级）

- **阻断级：无。**
- **建议级（已转登 traceability/findings.json，open）：**
  - **F-352**：契约 verify 数组缺 sdurws_ird_modeling_contract_test 构建行——F-300…/351 家族在 T15 复发；验收者补建未受阻（责任方：契约编译会话）。
  - **F-353**：modeling.md §14.6 v0.17 ⑧冒烟括注计数失准（246/246＋14/14 实为集成口径；冒烟实测 200/200＋8/8）——散文计数家族新增一例，零行为影响（责任方：modeling 卡所有者，下次增量修订顺手消账）。
  - **F-354**：be8aa7cd 正文＋v0.17 ⑤具名用例分组计数漂移（6/5/6/4＝21≠23；实测 7/6/6/4＝23）——总数与实质零影响（责任方：modeling 卡所有者）。

## 裁决

**verdict = pass**——4.1～4.11 全部通过、无阻断级问题；建议级 3 项已按 ACC §5 转登 findings.json（编号顺延自 F-351，取号以送验对象在库 findings.json 最大号 F-351 为基准）。本记录不构成合入动作：合入由所有者按 PIPE §4.7 决策。

## 记录三元组（PIPE 收尾按 commit SHA 合并）

```
{branch: acc/WP-13-T15/1, path: RobWork/doc/industrial-robot-design/traceability/acceptance/WP-13-T15-20260925.md, commit: <acc/WP-13-T15/1 上本记录的提交 SHA——由验收者随验收回复上报编排者>}
```

本验收者未修改任何实施产物、未向任务分支推送提交；验收记录与 findings.json（F-352/F-353/F-354）登记落于 `acc/WP-13-T15/1` 分支（本 commit）。

验收者环境：Windows 11 x64／VS2022 17.12.6 MSVC 1942／CMake＋Visual Studio 17 2022 生成器／Qt 6.11.1 msvc2022_64／vcpkg 经典模式（gtest 1.18.0）／pwsh 7。临时 worktree（acc-t15-wtA/wt-base/wtB）与构建树（acc-t15-build/acc-t15-smoke）在推送确认后清理。
