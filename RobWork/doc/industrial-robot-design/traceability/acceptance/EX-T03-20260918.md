# EX-T03 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | EX-T03（取消与终止协议——协作窗/强杀/DrainPolicy；≙WP-08-T04，分支 wp08-t03） |
| 契约 | tasks/foundation/EX-T03.json（status=ready；DOC-T12 编译放行版；requirements=NFR-PERF-02/UX-03） |
| 送验对象（冻结） | **b8cf368d2622b59c7852ff5138d15bb6c7073abc**（40 位，全程按 SHA 验收） |
| diff 范围 | b35d50f55c35143155479c039bb101da7a4eb763..b8cf368d2622b59c7852ff5138d15bb6c7073abc（3 个提交：4cc49435 实现＋3fdc7efa 留痕＋b8cf368d 单元卡同步；merge-base 实测＝base 自身＝state.base） |
| 验收尝试 | attempt 1（state.attempts.accept=1 与编排者下发一致——pipeline/state.json 实读） |
| verdict | **pass**（4.1~4.11 全部通过，零阻断级；建议级 1 项——b8cf368d 提交标题名词开头，转登 F-210） |
| 验收日期 | 2026-09-18 |
| 记录回传 | 分支 **acc/EX-T03/1**（基于 origin/redesign-main＝b35d50f55c35143155479c039bb101da7a4eb763），路径 traceability/acceptance/EX-T03-20260918.md，记录提交 SHA 由验收者向编排者回传三元组 |

## 分支漂移检查 —— 通过

- `git ls-remote origin`（fetch 后实测）：`refs/heads/wp08-t03` = b8cf368d2622b59c7852ff5138d15bb6c7073abc ＝ 送验 SHA，**零漂移**（ACC §3）；`refs/heads/acc/EX-T03/1` 远端不存在（记录分支名无冲突）。

## 独立性与环境（ACC §2/§3）

- **全新上下文**：本会话只读代码、契约、单元卡、在库留痕与既有验收记录（后者仅用于协议条目判定先例引用——EX-T02-20260918 的 F-008/F-200/F-201 处置口径），未消费实施者过程叙述；一切结论以下述亲手执行为准。
- **复现现场**：`git worktree add --detach D:/acc-tmp/ex-t03-repro b8cf368d…`（detached @ SHA，非分支名）。集成树 `ex-t03-repro/build` 从零新建 configure：`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON`＋`-DCMAKE_TOOLCHAIN_FILE=<主仓>/vcpkg/scripts/buildsystems/vcpkg.cmake`＋`-DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`（worktree 不含未入库的 vcpkg/ 与 Qt——按主仓 build 缓存实测值镜像，F-007/F-098/F-062 环境链）；缓存 grep 实测 **`RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`**、configure exit 0、CMake Error 计数 0。首配前按 **F-200 已登记环境前置**自主仓补拷两个未跟踪框架辅助件（`RobWorkStudio.ini.template.static`＋`RobWorkStudio.ini.shared.in`——bin/ 被 .gitignore 吞、文件不入库）至 worktree 同路径；该两文件属框架基础设施、在 industrialrobot 验收面之外，复制动作不触碰送验源（全程 `git status --short` 复证，探针还原后全树 0 行）。
- **冒烟现场**：`cmake -S <worktree>/RobWork/RobWorkStudio/src/rwslibs/industrialrobot -B D:/acc-tmp/ex-t03-smoke -DCMAKE_TOOLCHAIN_FILE=<主仓>/vcpkg/…/vcpkg.cmake`（独立缓存目录，带 toolchain——CORE-T01 G-2 教训），configure exit 0、CMake Error 计数 0。
- **记录回传现场**：`git worktree add -b acc/EX-T03/1 D:/acc-tmp/ex-t03-record origin/redesign-main`。
- **复现优先于采信**：双模式配置/构建、四跑测试（两模式×两目标）、契约 verify 命令、validate-task/validate-docs 复跑、ird_gates 复跑、双探针失效注入与还原均为验收者亲手执行；在库留痕数值仅作交叉比对。

## 4.1 改动范围 —— 通过

`git diff --name-status b35d50f5..b8cf368d` 实证 **24 个变更文件**（4 M＋20 A），逐个落在契约 allowedFiles 内：

| 文件 | allowedFiles 依据 |
| --- | --- |
| execution/CMakeLists.txt（M）、execution/include/sdurws/ird/execution/{Controller,Scheduler}.hpp（A）＋{StateMachine,TaskTypes}.hpp（M）、execution/src/{Controller,Scheduler}.cpp（A）＋StateMachine.cpp（M）、execution/test/CancelProtocolTest.cpp（A）——共 9 件 | `RobWork/RobWorkStudio/src/rwslibs/industrialrobot/execution/**` |
| units/execution.md（M，v0.4→v0.5） | 单元卡显式列入 |
| traceability/builds/wp08-t03/ 下 14 件（A：9 日志＋4 gtest XML＋ird-test-report.json） | `RobWork/doc/industrial-robot-design/traceability/**` |

- forbiddenFiles（REQUIREMENTS.md/ARCHITECTURE.md）零触碰——diff 文件清单零命中；
- 框架源码零改动：diff 中 industrialrobot/execution 与 doc/industrial-robot-design 之外**零文件**；patches/ 零新文件（SA-02 无登记义务）。

## 4.2 可复现构建 —— 通过（全新树任务目标面从零复现；全树口径受 F-008 基线缺陷阻挡，与本任务无关）

- **集成模式**（ex-t03-repro/build 全新配置）：configure exit 0、零 CMake Error，缓存 `:BOOL=ON` 实测。全树 `cmake --build build --config Release` 在框架 **73 处 error 处失败**——错误全部位于 RobWork/RobWorkSim 框架代码（**RWSimulatorPlugin.vcxproj 71 处＋rwplugin.vcxproj 2 处**；Eigen `Parallelizer.h` C2589/C2059 与 `rw::math/Math.hpp` C2059/C2334/C2143 等 min/max 宏污染簇），与 F-008 登记逐字同族（其登记 71 处 RWSimulatorPlugin 数字亦吻合）；**industrialrobot 面零错误**（73 处 error 中涉 `industrialrobot|ird_` 计数 **0**）。该失败即 F-008 基线缺陷的又一次全新树复现，非本任务引入——按 RT-T02/T03、EV-T02、EX-T02 验收先例，任务相关目标面从零复现：
  `cmake --build build --config Release --target sdurws_ird_execution_test sdurws_ird_execution_contract_test` **exit 0**、`: error` 计数 **0**；两测试 exe 于 `build/RobWorkStudio/bin/Release/` 产出。
- **测试亲手执行**（非引用留痕）：集成模式 `sdurws_ird_execution_test` **45/45 PASSED** exit 0、`sdurws_ird_execution_contract_test` **9/9 PASSED** exit 0。
- **独立冒烟模式**（ex-t03-smoke 独立缓存）：configure exit 0；`cmake --build /d/acc-tmp/ex-t03-smoke --config Release`（全量默认目标）**exit 0**、`: error|fatal error` 计数 **0**；冒烟测试亲手执行 **45/45＋9/9 PASSED** exit 0。
- **送验留痕交叉比对**：4 份在库 gtest XML 实测 `tests="45"/"45"/"9"/"9"、failures="0"`——与本人四跑逐一相同；在库 integration-build.log 为**主仓存量增量树**产物（89 处 `10_Source_Repos` 路径），提交信息表述为"集成模式全量 Release 构建零错误（exit 0）"，未宣称"从零/最强口径"，表述与留痕一致（F-036/F-038 口径复核）；本人全新树复现（任务目标面从零＋全树受 F-008 阻挡的事实记录）为其补强。

## 4.3 契约 verify 命令 —— 通过

契约 `verify` 数组仅一条：`cmake --build build --config Release --target sdurws_ird_execution_test`——验收者在 ex-t03-repro 根**逐字亲手执行**，exit 0、`: error` 计数 0（构建树该目标此前已从零建成，本次为增量零错误）。

## 4.4 acceptance 条目逐项证据 —— 通过（4/4；ird_gates 无豁免需求——已实际执行，见 4.8/4.10）

| # | 契约条目 | 验收者亲手证据 |
| --- | --- | --- |
| 1 | EX-SM-3/4 全绿：Running 取消 2 s 内入 Canceling＋停止派发批次、批次 10 s 收敛→Canceled（协议值 2 s/10 s 为上游需求值不改动；时序经 ManualClock 可注入时钟虚拟推进断言、不 sleep——testkit §6.5 同步纪律）；暂停中取消保检查点（EX-SM-4——检查点文件/manifest 仍在、磁盘断言，可续跑） | 两模式四跑全绿含 `RunningCancelEntersCancelingWithin2sAndSettlesWithin10s_EX_SM_3_NFR_PERF_02`（CancelProtocolTest.cpp:336——t0+1.9 s 一次 poll 内 `tryState==Canceling`＋`gate.stopped(id)`＋`worker.cancelRequests()==1`；窗内 3.9 s 不提前转移；9.9 s 收敛 Canceled；事件序 `Canceling→Canceled`；全程 `noDiagnostics` 空）＋`PausedCancelRetainsCheckpointFilesOnDisk_EX_SM_4_ARCH_11_2_6`（:453——TempDir 布置批次文件＋manifest，取消直达 Canceled 后两文件**逐字节一致**磁盘断言＋零诊断）；协议值钉子＝static_assert 两行（:315-318，`kCancelAcceptWindow==2000ms`/`kCancelCooperativeWindow==10000ms`），且本验收探针实证常量篡改会被编译期拦截（实施者留痕 test-fault-probe.log 探针①同型）。ManualClock（:60-72）＋bindClock（:322）全文无 sleep——grep `sleep` 零命中 |
| 2 | EX-WKR-4/5 通过：强杀编排就位（Job 进程树终止——KILL_ON_JOB_CLOSE；EX-FORCE-TERMINATED 显式区别于普通失败）；运行超时走同卡死路径并以诊断区分 timeout（§7.1）；取消协议无永久等待路径（10 s 收敛＋强杀兜底——自审「无永久等待」项） | `RequestForceTerminateKillsProcessTreeAndMarksExplicitly_EX_WKR_4`（:561——terminateCalls==1 恰一次＋cause=ForceTerminated＋handleClosed＋isAlive 翻转＋stopDispatch；Failed＋ForceTerminated＋EX-FORCE-TERMINATED＋`EXPECT_FALSE(hasDiagCode EX-WORKER-CRASHED)` 两码互斥；终态重复强杀幂等 no-op 不重复终止）＋`ForceKillDiagnosticsDifferFromOrdinaryFailure_EX_WKR_4`（:620——强杀甲 EX-FORCE-TERMINATED/ForceTerminated vs 普通失败乙 T9 Failed/无强杀码）＋`RunTimeoutTakesHungPathWithTimeoutDistinguishedDiag_EX_WKR_5`（:668——声明 5 s：4.9 s 不杀→5 s 强杀序列；EX-WORKER-HUNG 判定诊断 cause 含 "evaluationTimeout" 标记＋**因果序断言 hungIdx<forceIdx**；对照臂未声明时限不误杀）＋`CancelWindowTimeoutForceKillsNoPermanentWait_EX_SM_3_acceptance2`（:395——窗内 9.9 s 不强杀→窗满强杀→终态后推进 10 h 两次 poll 状态稳定、terminateCalls 仍 1）＋`DrainAbandonThresholdForceAbandonsNoPermanentWait_acceptance2_3`（:884——阈值 14.9 s 不兜底→15 s 自动 abandonAll→下一拍强杀→drained→恰一次）。KILL_ON_JOB_CLOSE 语义锚定于 IWorkerHandle::terminateForce 契约注（Controller.hpp:157-165），真进程兑现归 EX-T06/T09——与契约 acceptance 2 的"编排就位"口径一致 |
| 3 | DrainPolicy 排空语义实现就位（§7.5 关闭二选的执行侧：等待排空与协作取消两路径——行为用例 EX-ARC-4 归 §11 契约测试套件 EX-T09 承载） | DrainPolicy 枚举值域冻结（Scheduler.hpp:74-77，两值＝§10.1 原文）＋DrainCoordinator 实现本体（Scheduler.cpp 全读）：`ShutdownCancelQueuedAndWaitDrainsInflightUntouched_acceptance3`（:758——排队取消直达零诊断＋在途 `terminateCalls==0` 不被干预＋自然终态后 drained()==true）＋`ShutdownKeepQueuedTerminateRetainsQueued_acceptance3`（:805——排队保留且不阻塞 drained——P-EX-6）＋`RequestCancelAllCooperativeShutdownPath_acceptance3`（:843——批量入口受理 2＋排队直达＋在途走完整协作窗）＋`ManualAbandonAllForcedWhenAutoDisabled_acceptance3`（:937——阈值 0 禁用自动＋L5 手动入口）。EX-ARC-4 全场景行为用例按契约明文归 EX-T09——边界在测试文件头与 ird-test-report.json 一致声明，无夸大 |
| 4 | 正常用户取消零错误诊断（UX-03）；P-EX-2 处置（协议语义锚定 ARCH §4.4，评审 A9+ 变更按影响面增量同步）；P-EX-1 处置（消费契约 v0.1 Draft 未冻结，冻结 diff 后增量同步） | UX-03：`QueuedCancelDirectIdempotentZeroDiagnostics_EX_SM_1_UX03`（:520——直达 Canceled＋终态重复取消 accepted=false＋EX-INVALID-STATE 反馈仅应答携带不入任务诊断）＋EX-SM-3/4 用例内 `noDiagnostics` 全程空断言（严格"零诊断"强于"零错误级"）＋正常受理 feedback 空断言（:355/:536）；实现侧取消路径对 record().diagnostics 零写入（Controller.cpp 实读——仅强杀/超时判定写诊断）。P-EX-2：2 s/10 s 以 constexpr 公开常量冻结（Controller.hpp:249-250，注释明示"上游需求值不改动/无运行期修改点"）＋static_assert 钉值＋协议语义逐条对照 ARCH §4.4/卡 §7.1（Controller.hpp 文件头）；处置留痕登记于单元卡 §15.4 v0.5。P-EX-1：新增产品源 include 面直 grep 全集＝core/{DiagData,Errors,Evaluation,Events,Identity}.hpp＋evidence/{Evaluator,Snapshot}.hpp（全部他单元 include/ 公共根）＋execution 自有头——零新跨单元消费面，契约测试 9/9 全绿（NoRedefinition 扫描常驻）；处置留痕同登 §15.4 v0.5 |

## 4.5 测试真实失败能力 —— 通过（2 处失效注入，验收者独立设计、亲手执行，注入点位与实施者留痕探针不同；还原遵循 F-068 教训 touch 强制重编译）

| 探针 | 注入点（与实施者留痕探针不同点位） | 实测结果 |
| --- | --- | --- |
| ① 协作信号链断链 | Controller.cpp `enterCanceling` 内 `entry.worker->requestCooperativeCancel();` 注释禁用（sed 精确替换，:385） | **恰 2 用例红**：`RunningCancelEntersCancelingWithin2sAndSettlesWithin10s_EX_SM_3_NFR_PERF_02`（cancelRequests 期望 1 实得 0＋收敛不可达）＋`RequestCancelAllCooperativeShutdownPath_acceptance3`（在途任务永不收敛→Canceled 断言失败）；其余 43 绿——FakeWorker 的"未请求不收敛"协议次序钉子使断链必被检出 |
| ② 排空策略条件反转 | Scheduler.cpp `shutdown` 内 `if (policy == DrainPolicy::CancelQueuedAndWait)` 反转为 `KeepQueuedTerminate`（:80） | **恰 2 用例红**：`ShutdownCancelQueuedAndWaitDrainsInflightUntouched_acceptance3`（排队未取消，Canceled 断言失败）＋`ShutdownKeepQueuedTerminateRetainsQueued_acceptance3`（排队被误取消，Queued 断言失败）——两策略语义互为镜像，反转即双侧检出；其余 43 绿 |

两探针各自 `git checkout --` 还原＋touch 源文件＋重建后 **45/45 复绿**，`git status --short` 0 行（零残留）。测试非恒真、注入即红，真实失败能力成立。（实施者留痕探针为 kCancelCooperativeWindow 常量篡改被 static_assert 编译期拦截＋收敛窗判定损坏 2 项运行期红——本验收双探针为补充点位，互不重叠，见 test-fault-probe.log。）

## 4.6 架构红线 —— 通过（AGENTS §5 八条逐条）

1. **L2 零 Qt**：execution 树（include/src/test）`#include <Q|include "Q` 直 grep 仅命中 BuildRedLineTest.cpp:185-192 红线扫描用例自身的模式字符串（扫描器输入，非 Qt 依赖）；CMakeLists.txt:275-278 配置期 `^Qt` FATAL 守卫＋NoQtInclude 用例双保险，两模式构建成功＋用例绿实证。
2. **业务域互链（R-1）**：target_link_libraries 仅 `sdurws_ird_core/evidence/project` 三条 ARCH §3.5 登记边（CMakeLists.txt:125-128，本 diff 零变更）；配置期守卫（:264-283）对 `^sdurws_ird_` 通配白名单外链接 FATAL；ird_gates 复跑 41 处命中中**零 execution 项**（命中集合与送验留痕及 base 三方 diff 为空）。
3. **跨单元私有头（R-2）**：新增/改动源 include 面直 grep 全集＝core/{DiagData,Errors,Evaluation,Events,Identity}.hpp＋evidence/{Evaluator,Snapshot}.hpp——全部位于他单元 `include/` 公共根；`../` 相对路径 include 与 src/ 私有头跨单元引用零命中（新增公共头 Controller.hpp/Scheduler.hpp 均在 include/sdurws/ird/execution/ 下）。
4. **框架零源码修改（SA-02）**：4.1 已证 diff 零框架文件、patches/ 零新文件。
5. **不做名称前缀拼接（R-4）**：新增五件源文件 `RobWork` 字面量直 grep 零命中；ird_gates 复跑 R4 项仅 project/ProjectStoreImpl.cpp（既有登记例外）。
6. **权威唯一（PA-1）**：TaskController/DrainCoordinator 为 execution 自有域（卡 §3.1 组成行＋§7 原文）；诊断码 `EX-WORKER-HUNG`/`EX-FORCE-TERMINATED`/`EX-WORKER-CRASHED` 实证登记于 diagnostics/src/DiagCodes.cpp:399-403（StableCodeRegistry）——execution 侧仅以字符串消费、零码值私定（契约 note 口径）；九态词表/状态转移一律经 TaskStateMachine::request（单一事实源），编排层不直写状态字段；零 project 写路径/runtime 真值/policy 语义私建。
7. **不可变历史（PA-2）**：appendDiagnostic 为 push_back 追加语义（StateMachine.cpp 增量，只增不改——§4.2"追加"可变性原文）；termination/终态字段仅在 T13 转移内一次写入（EX-T02 既有状态机纪律）；本交付无修订数据面。
8. **不恢复 old/**：全新实现，零历史代码复制痕迹（实现逐条对照 units/execution.md §7.1/§7.4/§7.5/§10.2/§10.3 原文，注释标注出处；本人实读对照一致）。

## 4.7 注释规范 —— 通过（公共头 2 新增全查＋2 增量全查；实现文件全查；测试/CMake 全查）

- **公共头**：Controller.hpp（438 行）/Scheduler.hpp（193 行）两新增件全查——文件头均有 @file/@brief＋设计依据（需求 ID＋卡章节＋契约条目＋ARCH 锚点）＋背景说明（显式驱动模型边界、ITaskController 完整接口随 EX-T05 的归置声明、两编排同时钟源纪律）；类/结构体均有业务概念、生命周期（所有权移入/非所有权引用逐处标注）、线程安全说明（§2.5 所有权/线程约束逐项在位：IWorkerHandle 裸指针不接管所有权、命令通道任意线程/poll 仅调度线程、m_commandMutex 小粒度锁两交汇点、状态机本体单写者域不加锁）。CancelAck/StatusAck/ICancelSignal/IWorkerHandle/IDispatchGate/DrainPolicy 逐类型 Doxygen 中文完整。
- **既有头增量**：TaskTypes.hpp evaluationTimeout 字段（单位 ms、nullopt/0 语义、登记说明）与 StateMachine.hpp appendDiagnostic（线程约束、稳定码前置校验说明）——增量注释与周边同规格。
- **实现文件**：Controller.cpp（545 行）/Scheduler.cpp（187 行）全查（≥3 要求满足）——逐步分段讲解（poll 四段处理序、forceKillSequence 三步因果序、强杀恰一次防线理由）；§2.5 高危信息：错误语义（未知任务 fail-fast vs 终态 Ack 两分法逐分支注明）、确定性来源（steady_clock 单调理由：system_clock 会被 NTP 校时打乱、时长比较不比较绝对时刻）、线程约束（每函数域标注）、所有权（worker 非所有权、gate 非所有权）。StateMachine.cpp 增量 appendDiagnostic 实现注释（单一校验点、无锁理由）在位。
- **测试文件**：CancelProtocolTest.cpp（1037 行）全查——14 用例名携带需求/AT 追溯后缀（_EX_SM_3_NFR_PERF_02/_UX03/_EX_WKR_4/5/_acceptance3/_S102_D07），文件头边界声明（替身不构成业务证明——真进程归 EX-T06/T09、检查点写出协议归 EX-T08、EX-ARC-4 归 EX-T09）＋每用例注释说明验证的 acceptance 条目；fixture 与辅助函数按 §2.3 注释。
- **CMakeLists.txt**：增量注释块完整（EX-T03 增列说明、红线守卫逐条理由、/utf-8 纪律）。
- 本单元无可物理量/坐标系/rad 载荷（时序类任务），时长单位（ms/2 s/10 s/30 min/15 s）均显式标注，相关条目不缺项。

## 4.8 文档同步与 findings 核对 —— 通过

- **实现与任务卡**：units/execution.md v0.5 与实现逐项对照实读一致——§7.1 四步协议（2 s 生效插队/10 s 收敛/超时强杀 T13/UX-03 零诊断）、§7.4 强制终止列（不经协作点、句柄关闭、两码互斥）、§7.5 关闭二选（CancelQueuedAndWait 排队取消/KeepQueuedTerminate 保留、无永久等待超阈值 abandonAll 兜底）、§10.1 DrainPolicy 冻结值域、§11 EX-SM-1/3/4＋EX-WKR-4/5 观测点逐项有具名用例。**三处实现与卡的偏差全部按 DTB §5.4 登记于 §15.4 v0.5 行**：①TaskCapability 增 evaluationTimeout 字段（§11 EX-WKR-5 行"能力声明字段"明文承载位——登记为 §5.5 之外的执行期声明扩展偏差）；②协作窗起点语义冻结为"进入 Canceling 时刻"（ARCH §4.4 条 1→2 次序的显式化）；③两处头内归置登记（ITaskController 完整接口随 EX-T05；IWorkerHandle/IDispatchGate 接缝随 Controller.hpp）。drained() 判定＝无在途运行（归档完成面归 EX-T04）亦在 §15.4 v0.5 显式登记——不存在"代码先行、文档失实"。
- **任务状态/追踪**：契约 status=ready（翻转权在收尾，PIPE §4.7——非实施者可动）；ird-test-report.json 的 acceptanceCoverage 4 条逐条对账（本人逐条独立复核成立）；单元卡 §12 EX-T03 行（:1129）产物/验证方式与实际交付一致。
- **findings.json 未关闭条目相关性核对**（读时点 209 条）：
  - **F-007/F-062/F-098/F-099/F-101/F-200（全新树环境链）**：本次验收**直接踩中 F-200**（两个未跟踪 ini 模板缺失卡 configure），按其登记形态自主仓补拷解决；该链责任在构建环境治理，非本任务可顺手消账，维持 open。
  - **F-008（RobWorkSim 插件从零构建失败）**：本次全树从零构建**直接复现**（73 error 全框架代码——RWSimulatorPlugin 71＋rwplugin 2、零 industrialrobot 涉入）；维持 open，不影响本任务判定。
  - **F-019/F-051/F-072/F-115/F-173/F-202（ird_gates 基线命中群）**：本人复跑 41 处命中与 wp08-t03 送验留痕及 base（builds/wp08-t02）留痕三方 `sort|uniq -c` 后 **diff 为空**（8 LIB＋1 R1＋1 R3＋3 R4＋1 R5＋23 SUB＋3 T1＋1 T2），零新增、零 execution 项——基线红为既有登记事实，本任务无加重。
  - **F-010 族（.gitignore 吞留痕日志）**：本任务**未踩中**——builds/wp08-t03/ 14 件经 worktree 实存 `ls` 复证（9 日志＋4 XML＋report 全部真实在库），无"声称在库而未入库"。
  - **F-065/F-207（留痕日志 GBK 双重编码）**：本人全新树 gates log 同带该系统性特征（『命中』呈乱码形态）；已登记，不再重复转登；机器可读性（IRD-GATE-* ASCII 码）未受损，三方命中集合比对无歧义。
  - **F-201（§4.10 与流水线实践脱节）**：沿用其登记口径与 EX-T01 r2/EX-T02 先例——ird-test-report.json 即本任务 resultNote（见 4.10）。
  - **F-205/F-209（分支防线事故族）**：与本任务实施/送验对象无涉（编排侧流程缺陷），无新增实例。
  - **F-012 标题家族（open 的 F-012/023/024/026/027/030/034/037/039/040/042/044/045/047/048/059/076/079/082…＋F-208）**：本任务 b8cf368d 为该家族**新实例**（[units] 前缀＋"execution.md v0.5"名词短语开头）——按 ACC §5 强制转登 **F-210**（见"问题清单"）。

## 4.9 commit 质量 —— 通过（含 1 项建议级，转登 F-210）

- **4cc49435** `[WP-08-T04] 实现 execution 取消与终止协议（协作窗/强杀/DrainPolicy）`：动词开头合规；正文四段（What 按文件列点/Why 对 NFR-PERF-02、UX-03、TASK-01 与卡章节/How 七项验证结论/影响面）齐备；验证段主张（双模式构建、45/45＋9/9、validate-task、ird_gates 与 base diff 为空、故障探针双臂）**经本人逐项复现全部一致**，无粉饰。
- **3fdc7efa** `[WP-08-T04] 留痕入库：EX-T03 双模式构建/测试/门禁/故障探针证据＋ird-test-report.json`：动词短语开头合规；四段齐备；"纯留痕入库＋证据清单"与 `ls` 实测 14 件构成（9 日志＋4 XML＋report）一致；其"XML tests/failures 计数与 test-run 日志一致"主张经本人实测 XML（45/45＋9/9、failures=0）成立。
- **b8cf368d** `[units] execution.md v0.5：EX-T03 落位同步——…`：正文四段齐备、验证段 validate-docs PASS 与本人复跑同数字（20 units, 12 trace entries, 150 task files）；但**标题前缀后以名词短语（'execution.md v0.5'）开头**，非动词开头——F-012 家族（[units] 前缀形态同 F-037/F-039/F-044/F-076/F-079/F-208）再现，建议级不构成阻断，转登 F-210。
- 口径注：4cc49435/3fdc7efa 验证段"集成模式全量 Release 构建零错误（exit 0）"对应在库 integration-build.log（主仓存量增量树，89 处主仓路径、`: error` 0 行、INTEGRATION_FULL_EXIT=0）——表述与留痕一致且未宣称从零口径（F-036/F-038 复核口径）；本人全新树复现结论见 4.2（任务目标面从零零错误＋全树受 F-008 阻挡的事实记录），不构成粉饰。

## 4.10 留痕完整 —— 通过

- 在库清点（worktree @ b8cf368d 实存）：traceability/builds/wp08-t03/ 恰 **14 件**＝4 份 gtest XML＋ird-test-report.json＋9 件日志（integration-build/smoke-configure/smoke-build/test-run-integration-report-targets/verify-target-build/gates-ird-gates-target/validate-task/validate-docs/test-fault-probe）——与 3fdc7efa 提交清单一致，零缺件（F-010 族未踩中）。
- **数值交叉比对（本人复跑 vs 在库）**：测试 45/45＋9/9＝四份 XML（tests=45/45/9/9、failures=0）与 report 一致✓；ird_gates 命中集合三方 diff 为空＝gates log 与我方复跑同集✓；validate-task `PASS (1 tasks)`/validate-docs `PASS (20 units, 12 trace entries, 150 task files)`＝我方在冻结 worktree 复跑逐字一致✓；verify 命令 exit 0＝我方逐字执行一致✓；探针 static_assert 编译期拦截＋运行期 2 项红/还原 45 绿＝test-fault-probe.log（323 行）与本人双探针同结论（点位互补）一致✓。
- **resultNote 语义**：沿 F-201 登记口径与 EX-T01 r2/EX-T02 先例——ird-test-report.json（ird-test-report/1 schema，目标三件＋双模式 runs＋门禁/verify/探针节＋acceptanceCoverage 4 条对账）即本任务结果注记；phase-one-validation.log 为 2026-09-10 后停更的静态文档，判定满足「或」条件。

## 4.11 偷懒/缩水实现扫描 —— 通过（4 子项）

1. **标记扫描**：diff 新增行 `TODO|FIXME|stub|placeholder|未实现|not implemented` grep **零命中**（无契约标注的触发式延后项，故无豁免清单需求）。
2. **空实现检测**：公共接口逐函数实读——TaskController 全方法（attachTask 重复登记守卫/bindWorker 换绑防线/requestCancel 速览两分法/poll 四段编排/forceKillSequence 三步因果/releaseResources 三重守卫）/DrainCoordinator 全方法（shutdown 幂等、drained 快照判定、兜底恰一次防线）均为实质实现，无空体/恒默认值返回；ICancelSignal/IWorkerHandle/IDispatchGate 为有意接缝接口（纯虚＋契约注）——非空实现，属卡 §3.1/§10.3 设计面。
3. **逐条证据表**：acceptance 4 条逐条具名测试＋命令输出＋文件行号见 4.4 表——4/4 有证据，无"已实现"声明依赖。
4. **测试强度**：新增 14 用例断言校验行为与边界（1.9 s/9.9 s/窗满边界推进、恰一次防线计数、磁盘逐字节断言、诊断因果序下标断言、static_assert 编译期钉子、对照臂不误杀），非恒真非"只编译通过"；与 4.5 双探针联动证实会红。

## 问题清单

**阻断级：0 项。**

**建议级：1 项（已按 ACC §5 强制转登 findings.json）。**

- **F-210**（新登，编号顺延自 F-209）：b8cf368d 提交标题 `[units] execution.md v0.5：EX-T03 落位同步——…` 前缀后以名词短语开头，非动词开头（ACC §4.9/AGENTS §6.3 字面口径）——F-012 家族 [units] 前缀形态再现（先例 F-037/F-039/F-044/F-076/F-079/F-208）；正文四段齐备、验证结论与验收者亲手复现一致，纯风格瑕疵不构成阻断。处置建议同家族既有登记：实施者模板补动词开头示例（句式如『登记 execution.md v0.5 落位同步（…）』）；对已推送历史不追溯改写。

**既有发现踩中登记（不新登，已在册）**：F-200（worktree 配置缺未跟踪 ini 辅助件——本次复现并按登记形态绕过）、F-008（全树从零构建受框架插件宏污染阻挡——本次 73 error 复现、零 industrialrobot 涉入）、F-065/F-207（gates log 双重编码乱码——本次同型出现）。

## 裁决

**verdict = pass**。4.1~4.11 全部通过、零阻断级问题；建议级 F-210 已转登 findings.json，不阻塞合入。验收对象＝b8cf368d2622b59c7852ff5138d15bb6c7073abc（非分支引用）；合入决策归所有者（ACC §6）。
