# PRJ-T14 实施验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | PRJ-T14（归档端口 ArchiveServiceImpl：begin/batch/finalize/abandon） |
| 送验对象（冻结） | bdb01a2f8a2bd20df81d68458e988733f62de9fb（分支 wp04-t14 尖端；验收全程按该 SHA，不按分支名） |
| diff 范围 | 3a505119e520732792dd311a6695ede8202df7f1（state.base）..bdb01a2f，共 2 提交（1a0894c7 实现＋测试、bdb01a2f 验证留痕），18 文件 +4080/-2 |
| 分支漂移检查 | 验收开始时 origin/wp04-t14 == bdb01a2f（与冻结 SHA 一致，无漂移）；origin/redesign-main == 3a505119 == base |
| 验收环境 | 全新 detached worktree（git worktree add --detach <tmpA> bdb01a2f）从零配置复现；记录回传 worktree（acc/PRJ-T14/1 基于 origin/redesign-main@3a505119）；pwsh 7＋MSVC 2022 x64＋vcpkg 经典模式（主仓库 vcpkg 目录——git 未追踪的环境依赖，worktree 天然不含） |
| verdict | **pass**（4.1～4.11 全部通过；无阻断级问题；建议级 4 条转登 findings.json F-166～F-169） |
| 验收日期 | 2026-09-17 |

## 4.1 改动范围 —— 通过

- git diff --stat 3a505119..bdb01a2f 实证 18 文件：project 单元 12（CMakeLists.txt、ArchivePort.hpp、ProjectStore.hpp、ArchiveServiceImpl.{hpp,cpp}、Codec.{hpp,cpp}、DiagRecords.hpp、ProjectStoreImpl.{hpp,cpp}、ArchiveServiceTest.cpp、ArchiveContractTest.cpp）＋units/project.md 1＋traceability/builds/wp04-t14/ 5——逐文件比对契约 allowedFiles 三条通配（project/**、units/project.md、traceability/**）**全部在册**。
- forbiddenFiles（REQUIREMENTS.md／ARCHITECTURE.md）零触碰（diff 无该两文件）。
- 无未登记框架源码改动：diff 零 RobWork/RobWork/、RobWorkSim/、RWS 其他源码路径文件；patches/ 无新文件（SA-02）。
- ProjectStore.hpp 的 36 行修改＝archive() 访问器增量挂载＋IResultArchivePort 前向声明（契约 note 明示的跨单元依赖提供方落位），无对端签名侵入。

## 4.2 可复现构建 —— 通过（全新树从零，任务目标面；PRJ-T13 先例口径）

- 集成模式：worktree A 全新从零配置，缓存确认 RWS_BUILD_INDUSTRIALROBOT:BOOL=ON（configure 日志 Configuring done＋Generating done，exit 0）。
  - 环境备注（F-007/F-098/F-099 既有登记，非实施缺陷）：①worktree 不含 vcpkg（git 未追踪目录）——toolchain 改指主仓库 vcpkg/scripts/buildsystems/vcpkg.cmake；②Qt prefix 补 CMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64；③RobWorkStudio.ini.template.static 被 [Bb]in/ gitignore 吞掉——自主树复制（diff 逐字节一致）。第三次配置零错误。
- 契约 verify 目标构建：sdurws_ird_project_test **exit 0、": error" 计数 0**；sdurws_ird_project_contract_test **exit 0、error 计数 0**（仅既有 C4834 警告——与送验 ird-test-report「除既有 C4834」一致）。
- 全框架全量 Release 构建：**失败面唯一＝RobWorkSim RWSimulatorPlugin**（Math.hpp min/max 宏污染 C2059/C2334/C2143 连锁＋rw::rw::math 错乱）——与 **F-008 已登记基线框架缺陷逐字吻合**（F-008：base 959fb9b 对照同样失败），本任务 diff 零框架触碰，按既往 PRJ-T01～T13 验收判例不归本任务、不构成阻断。
- 独立冒烟模式：独立临时目录从零配置（vcpkg toolchain——AGENTS §4.1 口径）＋全量构建 **exit 0、error 计数 0**。
- 测试执行（验收者亲手，四轮）：集成 sdurws_ird_project_test **253/253 PASSED**、sdurws_ird_project_contract_test **18/18 PASSED**；冒烟同 **253/253＋18/18 PASSED**——与送验 ird-test-report.json 及库内四份 XML（tests=253/18、failures=0 ×4）逐项一致。

## 4.3 契约 verify 命令 —— 通过

逐条独立执行（worktree A 集成树）：
1. cmake --build build --config Release --target sdurws_ird_project_test → **exit 0**（error 计数 0）；
2. cmake --build build --config Release --target sdurws_ird_project_contract_test → **exit 0**（error 计数 0）。
验收者亲跑日志留存于验收临时目录（integration-build-acc.log／verify-target1.log／verify-target2.log；库内证据面以送验 XML＋report 为准）。

## 4.4 acceptance 条目 —— 通过（四条逐项证据，ird_gates 实跑非「未执行」）

| # | 契约条目 | 证据（具名用例＋复现结果） |
| --- | --- | --- |
| 1 | PRJ-TX-8（进程内模拟）：requestClose 后迟到完成事件到达——存活至 finalize、完成后才 Closed；跨项目事件不写入；绑定原修订不因 HEAD 拒绝 | PRJ_TX8_ContextSurvivesUntilFinalizeThenClosed（begin→requestClose 返回在途数≥1 且不关闭→batch/finalize→closed()==true＋回调恰一次＋ResultArchived 恰一条＋全序戳「事件先于关闭回调」）；PRJ_TX8_CrossProjectEventNotWritten（A 五元组入 B 端口 invalid_argument＋B results/ 磁盘无外来 run＋listRuns 空＋B 正常收尾）；Task03_ArchiveBoundToOriginalRevisionIgnoreHeadAdvance（HEAD 推进两次后绑定 r0 归档成功＋r0 查询面可见——TASK-03/PM-13/ARCH A1/A8）。三者分别在 contract_test（18/18）与 _test（253/253）两模式全绿 |
| 2 | F9：manifest 原子发布＝完整判据（D-13）；同 attempt 重投递幂等（D-14）；不一致＝PRJ-ARCHIVE-CONFLICT；begin/abandon 全路径终结 | F9_FinalizeManifestIsCompletenessJudge（finalize 前 manifest.json 不存在＋listRuns 不列；发布后磁盘 parse 全等＋dump 逐字节 round-trip＋digest 重算自洽＋重开可见）；D14_RedeliveryIdempotentAndConflictRejected（幂等不重写磁盘字节不变／分歧冲突拒绝＋用户诊断＋不覆盖＋会话未终结可 abandon）；D14_BatchLevelRedeliverySkipOrConflict（批次级两粒度）；F9_FinalizeVerifiesManifestAgainstDisk（sha256/size/缺失三类不符→StoreCorrupt 且不发布假完整）；Abandon_FullPathTerminationNoPermanentWait（失败不终结→abandon 排空归零→关闭＋回调恰一次＋残留保留未完成——A-4 无永久等待） |
| 3 | 端口与引用语义：ArchiveSessionRef 生命周期（闭包双通道归 project）；RunManifest 字段与 §4.4.7 一致（O-12） | SessionRef_LifecycleAndOwnership（引用持有通道＝requestClose 在途计数；finalize 后句柄 fail-fast；空句柄 fail-fast；abandon 幂等）；RelPath_WhitelistFailFast（穿越/绝对/盘符/反斜杠/保留名 manifest.json/.ird-part/设备名/控制字符/超长/空串全拒绝＋零磁盘副作用＋空批 no-op——NFR-SEC-01）；Begin_RejectionMatrix（跨项目 AT-10＋attempt=0＋runKind 空＋白名单外）；Begin_ReadOnlyContextRejected（LockHeldByOther＋PRJ-LOCK-HELD）；Draining_RejectsNewBeginButServesExistingSession；§4.4.7 一致性＝F9 用例内 parse 全等＋round-trip＋listRuns 逐字段透传；O-12＝finalize 磁盘核验把守发布路径（F9_FinalizeVerifiesManifestAgainstDisk） |
| 4 | P-PR-4 处置：调用线程单侧冻结、不私改对端契约；P-PR-1 处置：ResultArchived 经 core IDomainEventBus、零 core 修改 | Ppr4_ConcurrentSessionsSerializedByWriterMutex（双线程并发 begin→batch→finalize 全成功＋两 manifest 磁盘一致＋requestClose 同步完成；线程内不做 gtest 断言——汇合纪律）；P-PR-1＝PRJ_TX8 用例内 ResultArchived 事件 kind/载荷断言（core Events.hpp DomainEvent::make 基线）＋**零 core 修改实证：diff 无 core/ 路径文件**（4.1）；RunRegistry 对接细节未预设（实现注释与单元卡 §15.4 v0.16 同口径——待 WP-08-T01 冻结点二次对齐） |

ird_gates：验收者实跑（cmake --build build --config Release --target ird_gates）——**命中 9 项＝7×IRD-GATE-SUB＋1×IRD-GATE-T1＋1×IRD-GATE-R4**（ProjectStoreImpl.cpp 的 kCreatedWithToolVersion——T08 既有字面量，本任务 diff 未触碰该行），与 wp04-t08～t13 基线（F-145 后 9 命中）**逐项一致＝零新增**；exit 1 属 SA-10 设计（命中即非零）；门禁自测 9 项（pass_clean/pass_r4comment＋7 项 fail 按预期检出）全部符合。validate-task.ps1 -TaskFile …PRJ-T14.json → **PASS (1 tasks)，exit 0**（验收者亲跑）。

## 4.5 测试真实失败能力 —— 通过（双探针即红、还原复绿）

- 探针 1：ArchiveServiceImpl.cpp:920 D-14 幂等判定 == 反转为 != → D14_RedeliveryIdempotentAndConflictRejected **FAILED**（幂等重投递被误判冲突）。
- 探针 2：ArchiveServiceImpl.cpp:550 begin 白名单判定失活（&& requested.empty() 附加）→ Begin_RejectionMatrix **FAILED**（白名单外不再拒绝）。
- git checkout -- 还原后重建，同 filter **2/2 PASSED** 复绿；git status 对 RobWork/ 零差异（复现现场与送验 SHA 逐字节一致）。

## 4.6 架构红线 —— 通过

AGENTS §5 八条逐条（对 18 文件 diff 的 grep/走读实证）：
1. **零 Qt**：新增/改动文件零 Qt include（含测试）；
2. **业务域互链（R-1）**：零 modeling/requirements/kinematics/trajectory/dynamics/selection/optimization 单元头；跨单元仅消费 core 公共头（Identity/Events/DiagData——include/sdurws/ird/core/，契约 acceptance 4 明示的事件总线消费，非互链）；
3. **跨单元私有头（R-2）**：相对 include 全部在本单元（ArchiveServiceImpl.hpp/Codec.hpp/DiagRecords.hpp/ProjectStoreImpl.hpp/win32/AtomicFile.hpp 均 project/src/ 内）；公共头以类型擦除持有会话状态，零实现细节外泄；
4. **SA-02**：零框架源码改动、patches/ 零新文件；
5. **R-4**：门禁实跑 R4 命中 1 处＝T08 既有字面量（基线内），本任务零新增、零名称拼接/剥离逻辑；
6. **权威唯一（PA-1）**：results 写入口唯一归 project（O-12 磁盘核验把守发布路径）；当前性归 evidence 不越权（manifest 只记录不解释）；诊断经 DiagRecords 唯一装配点（PRJ-ARCHIVE-CONFLICT——diagnostics §4.6 收编清单，不私造码）；
7. **不可变历史（PA-2）**：归档绑定原修订 results/<run-id>/（A8），不因 HEAD 前进改变写入目标；
8. **不恢复 old/**：零 old/ 路径引用。

## 4.7 注释规范 —— 通过

- **公共头全查（ArchivePort.hpp，378 行）**：文件头五要素齐备（@file/@brief、设计依据 §5.6/§10.1/§10.2＋需求 TASK-03/PM-13/CON-04/CON-02＋契约 acceptance 1～4、背景说明、P-PR-4/P-PR-1 处置口径、线程模型）；每个 struct/enum/class/方法均有中文 Doxygen——begin 校验清单①～④＋零磁盘副作用边界、writeBatch 逐条目语义＋relPath 白名单规则＋失权防护、finalize 六步执行序、abandon 四值词表＋幂等；AGENTS §2.5 高危信息表逐项在位（所有权「非 owning——随上下文消亡」、线程「任意线程进入内部串行」、错误语义二分「环境/数据错误走返回值轨 vs 调用方契约违约 fail-fast」、生命周期「句柄共享持有会话状态但不延长归档责任」）。
- **实现文件抽查（≥3 要求，实查 6）**：ArchiveServiceImpl.{hpp,cpp}（文件头含实现口径①～⑦与锁序纪律；begin/finalize 逐步骤注释「第 N 步在做什么、失败走到哪」——如 D-14 比对先于磁盘核验的顺序语义论证）、Codec.{hpp,cpp}（dump(RunManifest) §4.4.7 表列序＋CR-02 透传）、DiagRecords.hpp（makeArchiveConflict 码值权威出处＋唯一装配点）、ProjectStoreImpl.cpp（装配序/析构序论证）、CMakeLists.txt（目标注册中文注释块——AGENTS §2.7）、两测试文件（用例锚定注释「§10.1/D-14/acceptance N」＋用例内分步中文说明）。
- 微瑕两处（零语义影响）转登 **F-169**：①ArchivePort.hpp writeBatch 注释「对锁句執行权威探测」繁体「執」字；②ArchiveServiceImpl.hpp 文件头实现口径④⑤两行缩进错位。

## 4.8 文档同步 —— 通过（含 findings 未关闭条目相关性核对）

- **实现与任务卡零偏差**：§5.6 四方法签名与 ArchivePort.hpp 逐字段一致；begin 校验清单①～④、§10.1 两段式/幂等冲突/失败即 abandon、§10.2 流程序（finalize→事件→引用释放→Closed——全序戳断言钉住）全部照语义实现。**七项实现口径已按 DTB §5.4 登记于单元卡 §15.4 v0.16 行与 ArchivePort.hpp/ArchiveServiceImpl.hpp 文件头**（如①begin 身份比对前置＝文档未定义判据的最小收敛——登记在案非私裁）。
- **单元卡同步**：§15.4 v0.16 行在位（版本行 v0.15→v0.16 已同步）；测试对账（新增 11＋3 用例逐条映射 acceptance）、门禁基线、缺陷留证三项齐备——与验收复现一致。追踪矩阵（trace-matrix.json）自 WP-00-T02 冻结后非任务级登记面（先例同口径）；任务状态/队列由编排者收尾更新。
- **findings.json 未关闭条目相关性核对（144 条 open）**：无条目点名 PRJ-T14/archive/IResultArchivePort 要求本任务消账。家族性未关闭问题在本任务的再现如下（均建议级、本次转登）：①**F-168**＝F-122/F-133/…/F-163 卡头日期行家族第十一次再现（卡头日期停留 2026-09-09，§15.4 v0.16 行＝2026-09-16；版本半边已同步——可顺手消账未做且未注明）；②**F-167**＝F-012 系提交标题家族再现（bdb01a2f 名词开头，[governance] 车道第六例）；③**F-166**＝F-010 幻影清单家族再现（见 4.10）；④F-145 门禁 9 命中基线——本任务零新增（已核对一致，无需消账动作）；⑤F-156 偶发用例（CrashThenConcurrentTakeover_SingleWinner_D02）——本次验收 4 轮 contract_test 全绿未再现；⑥F-007/F-098/F-099 环境坑——本次验收再次踩中（已登记有处置建议，不新登）。

## 4.9 commit 质量 —— 通过（附建议级）

- 1a0894c7 标题「[WP-04-T14] 实现归档端口（ArchiveServiceImpl）：manifest 原子发布＝完整判据、重投递幂等与冲突拒绝」——动词开头合规；正文四段（What/Why/验证/影响面）齐备，验证结论（两模式 253/253＋18/18、门禁 9 命中零新增、validate-task PASS）经本验收逐项亲手复现**全部为真**；Acc-Covered 对账行在位；实现期缺陷留证三项如实登记（含「resolveActiveSession 初版占位实现——后定稿」的自曝，终态非占位，见 4.11②）。
- bdb01a2f 标题「[governance] PRJ-T14 验证留痕：…」——任务名词开头非动词开头（建议级，转登 **F-167**）；正文四段齐备，其声称的测试/门禁/校验结论全部复现为真；但其「全部落盘」的日志声称与实际入库不符（见 4.10——转登 F-166，无结论性粉饰）。

## 4.10 留痕完整 —— 通过（附保留：日志幻影引用，转登 F-166）

- gtest XML 四份入库（builds/wp04-t14/，tests=253/18、failures=0 ×4，ArchiveServiceTest 11 用例组与 ArchiveContractTest 3 用例组在位）＋ird-test-report.json（ird-test-report/1 契约：任务/分支/base SHA、三目标逐条结果、构建与门禁台账、acceptanceCoverage 四条逐项对账、implementationNotes 实现口径①～⑦＋并发面＋文档同步）——内容与本验收复现结果逐项一致。
- **保留项**：ird-test-report.json 的 builds[].log 字段（integration-build.log、smoke-configure.log 等 10 处）与 gates 字段（ird-gates.log/validate-task.log）引用的 11 份 .log **不在库**——.gitignore:87 *.log 规则吞没（F-010 根因，check-ignore 实证）；提交正文亦声称「全部落盘 traceability/builds/wp04-t14/」且未内联明示该处置。验证结论性内容无失实（本验收全部独立复现为真），按 F-125/F-165 家族判例登记建议级不构成阻断；证据面由验收者亲跑日志（验收临时目录）＋入库 XML/report 补足。
- resultNote 面：state.json 待收尾段由编排者更新（本次验收三元组见文末）。

## 4.11 偷懒/缩水实现扫描 —— 通过

- ① 关键词扫描：diff 范围 11 个新增/大改文件对 TODO|FIXME|stub|placeholder|未实现|not implemented **零命中**（无契约标注的触发式延后项需豁免）；
- ② 空实现检测：IResultArchivePort 四方法全量真实实现（begin 校验清单逐项＋目录创建＋会话登记；writeBatch 暂存写＋publishNew＋摘要幂等；finalize 六步；abandon 责任终结＋票据释放）；resolveActiveSession/requireWriteAuthority/validateRelPath 等私有函数全实现——提交正文所述「初版占位实现」为历史缺陷留证，现状已定稿（代码走读证实）；
- ③ **逐条证据表**：契约 acceptance 四条均映射具名用例＋复现证据（见 4.4 表，无「已实现」声明而无证据的条目）；
- ④ 测试强度：断言校验行为与边界（磁盘事实存在性、listRuns 字段级、round-trip 逐字节、闭包回调计数、全序戳时序、拒绝矩阵零副作用），非恒真、非「只编译通过」——4.5 双探针实证会红。

## 问题清单

| 级别 | 编号 | 摘要 | 去向 |
| --- | --- | --- | --- |
| 阻断 | （无） | —— | —— |
| 建议 | F-166 | 幻影清单家族第 16 例：bdb01a2f 声称 11 份 .log 全量落盘，实际入库 5 件（.gitignore:87 吞没；无 localOnlyNote，正文未明示） | findings.json 已登记 |
| 建议 | F-167 | 提交标题家族再现：bdb01a2f 名词开头（[governance] 车道第六例；实现提交动词开头合规） | findings.json 已登记 |
| 建议 | F-168 | units/project.md 卡头日期行家族第十一次再现（停留 2026-09-09；版本半边 v0.16 已同步） | findings.json 已登记 |
| 建议 | F-169 | 注释排版微瑕两处（繁体「執」字；实现口径④⑤缩进错位）——零语义影响 | findings.json 已登记 |

## 三元组（编排者收尾用）

- branch：`acc/PRJ-T14/1`
- path：`RobWork/doc/industrial-robot-design/traceability/acceptance/PRJ-T14-20260917.md`
- commit：见验收回执（evidence 分支记录提交的 40 位 SHA；本记录提交同时包含 findings.json 的 F-166～F-169 转登）
