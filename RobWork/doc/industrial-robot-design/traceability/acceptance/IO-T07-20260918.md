# IO-T07 验收记录（attempt 2）

| 字段 | 值 |
| --- | --- |
| 任务 | IO-T07（≙WP-11-T07）契约测试套件整备与留痕（io 单元——阶段 A 收官任务） |
| 契约 | tasks/foundation/IO-T07.json（status=ready，dependsOn=IO-T01~T06，acceptance 四条，interUnit=true，knownPitfalls=P-IO-2/O-09） |
| 送验对象 | d381d9d3e430d910b2a8ed8a4bf41eadd5dbc180（40 位冻结；diff 范围 eab17eee77880b2121cca767053188cef986a67a..d381d9d3＝state.base..headSha，三笔提交 da13bb66/cee796ab/d381d9d3） |
| 分支漂移检测 | origin/wp11-t07 实测＝d381d9d3e430d910b2a8ed8a4bf41eadd5dbc180、origin/redesign-main 实测＝eab17eee77880b2121cca767053188cef986a67a（git rev-parse 核实）——双零漂移，验收全程按该 SHA（ACC §3 送验对象冻结） |
| 验收环境 | 全新 detached worktree（`git worktree add --detach` @ d381d9d3＝`D:/10_Source_Repos/21_robot/.acc-tmp/io-t07-a`）从零复现：集成 build 树（worktree A 内 `build/`，重新配置后缓存实测 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`）与独立冒烟 build 树（worktree A 内 `build-smoke-acc/`，独立缓存、带 vcpkg toolchain 绝对路径）均为本验收新建（非主树缓存复用）；记录回传 worktree（`D:/10_Source_Repos/21_robot/.acc-tmp/io-t07-b`，分支 acc/IO-T07/2 基于 origin/redesign-main＝eab17eee）。MSVC 2022 x64（VS 17 2022，MSBuild 17.12.6）＋vcpkg 经典模式。**环境前置三件**（worktree 干净检出的既有环境缺口，非实施缺陷——F-007/F-200 已登记同型，IO-T05/T06 验收记录同款处置）：①集成配置需 `-DCMAKE_TOOLCHAIN_FILE=<主仓>/vcpkg/scripts/buildsystems/vcpkg.cmake`（主树缓存实证）；②需 `-DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`（首配实测失败于 Qt5/Qt6 NOT found——主树缓存 `CMAKE_PREFIX_PATH` 同值）；③`RobWork/RobWorkStudio/bin/` 下 `RobWorkStudio.ini.template.static`/`RobWorkStudio.ini.shared.in` 未跟踪文件在 fresh 树缺失，自主仓补拷（F-200 实测复现：配置期 `CMake Error: File …RobWorkStudio.ini.template.static does not exist`——补拷后配置零错误；两文件未入任何提交）。独立性（ACC §2 四条逐条满足）：全新上下文只读代码/契约/单元卡/在库留痕，未读取实施者过程叙述；对抗式立场逐项找反证；全部构建、双模式测试实跑、ird_gates 对账、治理脚本与实现失效注入均为本验收者亲手执行（ACC §2.4 复现优先于采信——实施留痕数值仅作交叉比对，不作采信依据）。attempt 序号说明：本次为 pipeline attempts.accept=2；attempt 1 未在任何分支留下验收记录文件（`git log --all -- "*IO-T07-20260918*"` 零命中），本记录为该文件名首次落库，无需 -r2 后缀 |
| verdict | **pass**（4.1~4.11 全部通过、零阻断级问题；建议级 1 项新发现转登 findings.json——F-203，见问题清单；既有 open 条目相关性核对见 4.8） |
| 验收日期 | 2026-09-18 |

## 4.1 改动范围 —— 通过

- `git diff --name-only eab17eee..d381d9d3` 实证 **22 个变更文件**（`--stat`：10988 insertions/0 deletions），逐个比对契约 `allowedFiles`：
  - `RobWork/RobWorkStudio/src/rwslibs/industrialrobot/io/**`×15（CMakeLists.txt、src/AtomicFile.cpp、src/Package.cpp、test/ 9 文件、contract_test/ 2 文件）——在 `io/**` 内；
  - `RobWork/doc/industrial-robot-design/units/io.md`×1（cee796ab，§11.4 覆盖核对登记＋§15.5 v1.0）——allowedFiles 显式登记；
  - `RobWork/doc/industrial-robot-design/traceability/**`×22 中的其余 6 处路径族（builds/wp11-t07/ 21 件＋findings.json）——在 `traceability/**` 内。
- `forbiddenFiles`（REQUIREMENTS.md/ARCHITECTURE.md）零触碰（不在 diff 文件清单，grep 反证零命中）。
- SA-02：diff 文件清单无任何框架源码（RobWork/RobWorkStudio 本体）文件；`patches/` 零触碰——零未登记框架改动。

## 4.2 可复现构建 —— 通过（双模式零错误，验收者从零亲手复现）

- **集成模式（重新配置）**：worktree A 内全新 `build/`（首配因缺 Qt prefix 失败→清空重配；二配因 F-200 两模板缺失失败→补拷后续配），命令 `cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DCMAKE_TOOLCHAIN_FILE=<主仓>/vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64 -DRWS_BUILD_INDUSTRIALROBOT=ON`——exit 0，`grep RWS_BUILD_INDUSTRIALROBOT build/CMakeCache.txt` 输出 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`（ACC §3 要求的确认项），`Configuring done`＋`Generating done`。构建两条 verify 目标（见 4.3）均 exit 0，`: error` 计数＝**0**；产物 `build/RobWorkStudio/bin/Release/sdurws_ird_io_test.exe` 与 `sdurws_ird_io_contract_test.exe` 就位。
- **独立冒烟模式**：`cmake -S RobWork/RobWorkStudio/src/rwslibs/industrialrobot -B build-smoke-acc -DCMAKE_TOOLCHAIN_FILE=<主仓>/vcpkg/scripts/buildsystems/vcpkg.cmake`——`SMOKE_CONFIGURE_OK`；`cmake --build build-smoke-acc --config Release`（全量）——exit 0，`: error` 计数＝**0**；产物在 `build-smoke-acc/io/Release/` 两 exe 就位。
- **测试执行（双模式四跑，验收者亲手）**：集成 `sdurws_ird_io_test` `[==========] 120 tests from 13 test suites ran.` `[PASSED] 120 tests.`（exit 0）；集成 `sdurws_ird_io_contract_test` `[==========] 9 tests from 1 test suite ran.` `[PASSED] 9 tests.`（exit 0）；冒烟双 exe 同为 120/120＋9/9 全 passed（exit 0×2）。listener 报告（--ird_report）四份：summary 全部 `total=120/9, failed=0, skipped=0, notRun=0, decisive=true`。与送验留痕（ird-test-report-unit/contract-*.json 双模式四份）逐用例比对：**用例集合与状态零差异**。

## 4.3 契约 verify 命令 —— 通过

- 契约 `verify` 数组两条，逐条独立亲手执行（worktree A 根目录）：
  1. `cmake --build build --config Release --target sdurws_ird_io_test`——exit 0，`acc-build-verify1.log` 零 error，链接输出 `sdurws_ird_io_test.vcxproj -> …\bin\Release\sdurws_ird_io_test.exe`；
  2. `cmake --build build --config Release --target sdurws_ird_io_contract_test`——exit 0，`acc-build-verify2.log` 零 error，链接输出 `…sdurws_ird_io_contract_test.exe`。
- 配套治理脚本（验收者亲手）：`verify-task.ps1 -TaskFile <契约>`——二次驱动同一对命令，结尾 `verify-task: PASS IO-T07`；`validate-task.ps1 -TaskFile <契约>`——`validate-task: PASS (1 tasks)`；`validate-docs.ps1`——`validate-docs: PASS (20 units, 12 trace entries, 150 task files)`。

## 4.4 acceptance 条目逐项证据 —— 通过（四条全绿，验收者亲手复跑）

| # | 契约条目 | 证据（具名测试＋命令输出；双模式均过） |
| --- | --- | --- |
| 1 | **§11.2 V01~V32 全用例可执行且结果逐条登记（gtest XML＋ird-test-report.json；用例名携带需求/AT 追溯字段；AT-02/AT-20 迁注；组名族；不预设通过）** | 实跑 120＋9＝129/129 双模式一致 passed（4.2），与 io.md §11.4 覆盖登记表及任务级 ird-test-report.json（schema ird-test-report/1）声明数值一致：testSuites **28 项**（§11.4 清单 28 项逐项→V 行→gtest 用例全名→双模式结果）＋P-IO-2 专用项共 29 条目，零跳过零失败；summary `decisive=true`。用例级追溯（listener 报告实测校验，129 用例全集）：V 行映射 56 用例 `requirementIds` **零缺失**；`atIds` 按卡行「AT-02/AT-20 迁注：其支撑验证落在对应用例组的追溯字段」口径＝组/条目级登记（任务级报告逐条目 `ats` 字段：CSV 族 AT-02/AT-22、包/固化族 AT-20、V31＝AT-34），6 个支撑面用例（如 IoZipTest 容器层哈希、IoCsvTest 无标识还原）自身 atIds 为空但其归属条目级 AT 齐备——符合卡行迁注原文，非缺口。组名族 IoSec/IoCsv/IoJson/IoRes/IoPack 前缀系在案（IoZip/IoSolidify/IoTemp/IoAtomic/IoPackContract/IoDiag 系其分解面——io.md §11.4 登记同款）。 |
| 2 | **故障注入经 fake 适配层（FaultInterceptor/TempDir 每用例隔离/恶意 zip 构造器；不依赖真实磁盘满；产品目标零 testkit 链接）** | 实读＋实跑：V20 磁盘满＝`IoPackContractTest.DiskFullReportsRequiredAvailableAndCleansUp`（PackageIoFacilities 注入面 fake 可用字节与写失败，非真实磁盘满）；V21 清理失败＝`CleanupFailedListsResiduesAndRetrySucceeds`（注入删除失败→CleanupFailed 残留清单→重试成功）；V17 复制期变化＝FaultInterceptor 式检查点注入（IO-T05 先例用例，回归通过）；恶意 zip 构造器＝手工字节 zip（handcraftZip：重复条目/畸形）＋libzip 写侧构造（比例炸弹 200 KiB 全零 DEFLATE、超展开 3×512 KiB 伪随机 STORED——V12 两半边）；TempDir 每用例隔离＝各 fixture 的 m_root/m_dir 每用例临时根（实读 PackageContractTest/PackageTest fixture）。产品目标零 testkit：CMakeLists 实读 `sdurws_ird_io` 链接面＝PUBLIC core＋diagnostics／PRIVATE libzip＋expat，**testkit 仅出现在两测试目标**；ird_gates T-1 检查零新增命中（见 4.4 附行）。 |
| 3 | **覆盖核对登记（§11.4 清单 28 项逐项可查；跳过显式报告、伪造通过禁止；ird-test-report.json 追溯字段齐备）** | cee796ab 于 io.md §11.4 建立覆盖核对登记表（28 项→V 行→用例全名→双模式结果），状态行由「全部为设计，未执行」更新为 2026-09-18 实测执行登记——本次验收实跑逐项复核一致（V01~V32 对应用例全部在 129 用例集合中且 passed）。跳过＝零：源码 `grep -rn GTEST_SKIP`（io/ 全目录）零命中＋listener summary `skipped=0`。追溯字段齐备：任务级报告 28 条目逐条含 `reqs`（需求）/`ats`（AT）/`pre`（前置）/`op`（操作）/`expected`（预期）/`obs`（观测点）六面＋`cases[]` 双模式状态；acceptanceMapping acc1~acc4 逐条消账映射＋knownPitfalls 处置记录＋selfCheck 在案。 |
| 4 | **P-IO-2 五条裁决专用固化＋O-09 对端 fake（io 零 project/runtime 编译依赖）** | P-IO-2＝`IoSolidifyTest.AdapterResourceBytesLifecycleFiveRulingsPinned`（io.md §8.6 五条逐条断言面：第 1 条最低保证——跨后续调用旧缓冲逐字节完整；实际保证——至析构多轮视图校验；第 4 条 Recorded 两调用指针不同＋源更新后新调用新内容新摘要、Solidified 缓存同指针；第 2/5 条纪律/契约注释口径、第 3 条既有并发用例 AdapterConcurrentRecordedReadsAreSafe——4.5 缺陷注入实证该用例真实有效）。O-09＝V28 `IoSolidifyTest.ReassociateNewRevisionProjectCommandFakeSequence`（project 命令 fake 记录 materialize→reassociate→materialize 调用序列精确断言＋io 侧修订痕迹物负断言——PA-1）；V30 `IoDiagSanitization.UserTierLocatesWithoutFullPathOrRawSnippetDevKeepsSnippet`（FakeTwoTierSink 双级行槽＋真实 RedactionService——Tier-U 无完整路径/无片段、Tier-D 片段保留、safeSummary 无路径＋[trunc]）；V31 `IoPackContractTest.BackgroundImportKeepsUiThreadResponsive`（驱动线程 fake＝worker 线程承载 verifyThrough，UI 线程打点间隙 <2s＋进度回调线程 id ≠ UI 线程）。编译依赖面实读：io 公共头/实现零 project/runtime 类型与头依赖（4.6 第 2/3 条复验）。 |
| 附 | ird_gates | 验收者亲手复跑 `cmake --build build --config Release --target ird_gates`：命中 **17 处**＝wp11-t06 基线 15＋**2 处新增**（IRD-GATE-SUB：sdurws_ird_io_test→sdurws_ird_testkit、sdurws_ird_io_contract_test→sdurws_ird_testkit——本任务契约 acceptance 1 明文的报告设施消费边，T-1 允许形态＝仅测试目标链 testkit，产品目标零新增命中），与送验留痕 gates-ird-gates-target.log 逐边一致（仅根路径与 MSBuild hash 不同）；exit 1＝ARCH §3.2 SA-10 设计行为；引擎自测 pass_clean/pass_r4comment/fail_r1/fail_t1/fail_r5/fail_sub/fail_r3/fail_r4/fail_t2 全按预期。 |

## 4.5 测试真实失败能力 —— 通过（实现失效注入实证，还原后复绿）

1. **注入一（P-IO-2 第 4 条 Recorded 不缓存裁决破坏）**：ResourceIo.cpp `readRecorded` 尾部改为返回共享静态缓存（同资源后续调用复用首缓冲——违规缓存形态）→增量重编→`--gtest_filter=IoSolidifyTest.AdapterResourceBytesLifecycleFiveRulingsPinned` 实测 `[ FAILED ] 1 test`（exit 1）——本任务专用固化用例真实钉住 §8.6.4「Recorded 绝不缓存」，非恒真。
2. **注入二（V19/F-199 begin 入口取消失效）**：Package.cpp `begin` 入口 `cancelHit` 检查点改为恒假——单点注入实测**未变红**：begin 内建临时区 `tempAreas->create(…, cancel)` 透传同一令牌构成第二道防线，取消仍在任何副作用前被响应（与 IO-T06 验收对 V19 分层检查点的发现同型——行为契约由分层防线保障，用例断言的是契约行为而非实现行，属健康的非白盒脆弱设计，**记录为观察项、不判缺陷**）；扩展为契约级注入（入口检查点恒假＋create 传 nullptr 不感知取消）→实测 `[ FAILED ] IoPackTest.ImportCancelAtBeginPreCancelledTokenLeavesNoArtifacts`，失败信息精确为「预取消未被 begin 入口响应」——用例对「begin 忽略取消」的契约违例确实变红。
3. **还原复绿**：两处注入全部 `git checkout --` 还原后 `git status`（io/ 目录）零输出＝源码与送验 SHA 逐字节一致；增量重编后全量过滤重跑 `[ PASSED ] 2 tests`。注入仅存在于临时 worktree 构建产物且已还原，任务分支与送验对象零触碰、零推送。

## 4.6 架构红线（AGENTS §5 八条逐条） —— 通过

1. **L2 计算内核零 Qt**：io 单元全目录 `grep -E '#include <Qt|#include <Q|QtWidgets|QMainWindow'` 唯一命中为 SafePathTest.cpp 红线扫描用例的**字符串字面量**（扫描器词表本体，非代码）；CMake 链接面零 Qt。
2. **R-1 业务域互链禁止**：diff 范围新增 include 仅 `<gtest/gtest.h>` 与 testkit 测试设施头；产品目标链接面＝core＋diagnostics（io.md §3.2/§3.3 仅有的两条登记单元边）；ird_gates R-1 零新增命中。
3. **R-2 跨单元私有头禁止**：新增消费的他单元头仅 `sdurws/ird/testkit/gtest/RecordListener.hpp`、`…/AssertMacros.hpp`（均为 testkit 公共头 include/ 面）与 core/diagnostics 公共头——零私有头。
4. **SA-02 框架零修改**：见 4.1（patches/ 零新登记、diff 零框架文件）。
5. **R-4 名称前缀拼接/剥离禁止**：diff 范围零 "RobWork" 字面量拼接逻辑（ird_gates R-4 的 1 处既有命中在 project 单元 ProjectStoreImpl.cpp——DTB §4.5 已登记例外，与本任务无关、非新增）。
6. **PA-1 权威唯一**：V28 用例以负断言钉住 io 不产生修订痕迹物（revisions/.rev 递归扫描＝false）；重关联语义由 project 命令 fake 承载，io 仅为事实提供方——io 未顺手实现 project 语义。
7. **PA-2 不可变历史**：本任务零修订写路径触碰（纯测试整备＋留痕＋两处注释）。
8. **不恢复 old/**：diff 范围零 `old/` 引用。

## 4.7 注释规范（AGENTS §2） —— 通过

- **公共头全查**：本任务 diff **零公共头变更**（include/ 无文件）——无契约面新增，无需全查对象。
- **新增/大改文件实读（超出抽查 ≥3 要求）**：①`test/TestMainReport.cpp`（新增 44 行）与②`contract_test/ContractTestMain.cpp`（新增 34 行）——文件头注含设计依据（契约 acceptance 1/testkit §7.2/§7.3/D-06/PRJ-T15 先例）、「为什么替换 gtest_main」的背景说明、监听器所有权与线程模型（§2.5 所有权＋线程约束项在位）；③`test/ResourceSolidifyTest.cpp`（+278）——V28/P-IO-2 用例逐裁决中文注解（第 1 条最低/实际保证的断言形态论证、第 2/5 条为何为注释口径）；④`test/IoDiagnosticsTest.cpp`（+175）——FakeTwoTierSink 类注（fake 的是槽位组装而非脱敏规则本体——O-09 语义边界）；⑤`contract_test/PackageContractTest.cpp`（+407）——V22 聚合用例逐分支注释（构造依据、断言面、与单分支用例的冗余防线关系）；⑥`test/PackageTest.cpp`（+82）——F-199 消账用例逐断言注（UX-03 无诊断、.rwpack- 前缀零残留）。测试用例名与断言处均注明验证的需求/AT 编号（AGENTS §2.7）。
- **实现文件改动**：AtomicFile.cpp/Package.cpp 各 +5 行**纯注释**（F-198 处置②前置契约钉死：≤SingleFileBytes 2 GiB、DWORD 截断语义的不可达论证）——无代码变更，注释含需求追溯与错误语义归类。
- 未发现 AGENTS §2.6 反面示例（无复述性废注、无中英混杂机翻腔、无过期注释；TODO 扫描零命中故无空泛 TODO）。

## 4.8 文档同步＋findings.json 相关性 —— 通过（附 1 项建议级登记）

- **文档同步**：io.md v0.9→v1.0（cee796ab）——§11.4 覆盖核对登记表 32 行（V01~V32→用例全名→双模式结果）、§15.5 v1.0 变更行全记录（V22/V28/V30 补齐、testkit 报告设施接入、IRD_TEST_INFO 迁注、F-199/F-198 消账、129/129、ird_gates 17 处分解）、文档尾进度行（阶段 A 六任务全闭环）；设计正文（§2~§14）语义零变更——与逐字对照的 diff 一致。`validate-docs PASS`（20 units/12 trace/150 task files）。实现与任务卡零偏差：acceptance 四条一一对应（4.4）；testkit 消费链接边与 O-09 fake 对端均为契约/单元卡明文，无需 §5.4 偏差登记。
- **findings.json 未关闭条目相关性核对**（open 条目全量过筛，与本题相关者逐条）：F-191/F-197（ird_gates LIB 词表）——本次 17 处命中中含其 4 处既有命中，基线构成不变，保持 open（门禁所有者）；F-198（DWORD 截断纵深缺口）——**处置②已随本任务落位**（两处注释钉死，实读核验），F-199（V19 begin 预取消覆盖缺口）——**建议用例已随本任务落位**（ImportCancelAtBeginPreCancelledTokenLeavesNoArtifacts，与 resolution 建议逐字对应），但两条目 status 仍 open、resolution 未附消账记录（对照 F-194 先例应置 fixed＋fixedAt＋消账注）——纯登记簿记账滞后，非实现缺口，转登 **F-203**；F-200/F-007（worktree 环境前置）——本次验收实测同型命中、按先例处置（见验收环境栏），保持 open；F-202（本任务自登记的 2 处 SUB 命中）——实测复现一致，保持 open（门禁所有者）；F-194/F-195/F-196（IO-T05 验收登记）——F-194 已 fixed、F-196 与本任务无关（diagnostics 收编）、F-195 提交标题风格本任务未复踩（三笔标题 `[IO-T07]`×2＋`[units]`×1 均合规）。
- phase-one-validation.log 未更新：按 F-201 登记的实践口径，任务留痕以 traceability/builds/wp11-t07/（21 件）＋任务级 ird-test-report.json 承载——非缺口（ACC §4.10 条文自身脱节问题已在 F-201 在途）。

## 4.9 commit 质量 —— 通过

- 三笔提交逐份实读：da13bb66（测试套件实现面）、cee796ab（单元卡同步）、d381d9d3（留痕入库＋F-202 登记）——标题动词开头、正文四段（改动内容/改动原因/验证/影响面）齐备，需求 ID 与契约章节引注完整。
- 验证结论诚实性交叉核对：①"全部证据由本分支尖端代码真实执行产出（非复制）……durationMs 差异证实独立运行"——与本验收者双模式四跑逐用例零差异互证；②"ird_gates 17 处命中＝基线 15＋2"——与亲手复跑逐边一致；③"*.log 经 git add -f 强制入库"（F-010 家族）——与 diff 实际入库文件一致；④"产品库零行为变更（仅两处注释）"——与 diff 实读一致。**无粉饰**。
- 标题前缀：`[IO-T07]`×2（契约 taskId 直用，≙WP-11-T07 映射在契约 note 登记）＋`[units]`×1（单元卡修订专用前缀，AGENTS §6.2 合规）——未复踩 F-193/F-195 家族问题。

## 4.10 留痕完整 —— 通过

- `traceability/builds/wp11-t07/` **21 件**全部随 d381d9d3 入库：集成＋冒烟 configure/build 日志、双模式双目标 test-run 日志×4、gtest XML×4（实测 tests="120"/tests="9"、failures="0"——与实跑一致）、ird-test-report 用例级明细×4（unit/contract×integration/smoke）、任务级 ird-test-report.json（28＋1 条目六面追溯）、gates/validate-task/verify-task/verify-target 日志×6。
- 验收者独立复跑产物（双模式 gtest XML＋listener 报告×4＋配置/构建/测试/门禁/脚本日志）落 acceptance 临时目录（acc-evidence/），数值与在库留痕一致（129/129、error 0、门禁 17 处）。
- phase-one-validation.log/resultNote：按 F-201 口径判定（见 4.8），非缺口。

## 4.11 偷懒/缩水实现扫描 —— 通过

- **① 词表扫描**：`git diff eab17eee..d381d9d3 | grep -E "^\+.*(TODO|FIXME|stub|placeholder|未实现|not implemented)"` **零命中**（无"触发式延后"豁免需要列出）。
- **② 空实现检测**：本任务产品代码改动仅为两处注释块（AtomicFile.cpp/Package.cpp）——零函数体变更、零新增接口；新增 TU（双 main）均有完整执行体（InitGoogleTest→installTestRecordListener→RUN_ALL_TESTS）。无空壳。
- **③ 逐条证据表**：见 4.4 表——acceptance 四条逐条具名测试＋命令输出＋文件行号，任一条均有可复现证据，无"已实现"口头声明依赖。
- **④ 测试强度**：断言校验行为与边界（错误码逐值、比较型三要素、目录存在性负断言、调用序列精确断言、指针不等性、双级脱敏正反断言、UI 间隙上限），非恒真——4.5 两处失效注入实测转红为证（含单点注入不变红的分层防线观察——用例钉契约行为而非实现细节）。

## 发现的问题清单

| 级别 | 编号 | 内容 | 处置 |
| --- | --- | --- | --- |
| 阻断 | 无 | —— | —— |
| 建议 | F-203（新登 findings.json） | F-198/F-199 的实质消账动作已随 da13bb66 落位（处置②注释钉死＋begin 预取消用例，均经实读与实跑核验），但 findings.json 两条目 status 仍 open、resolution 未附消账记录——对照 F-194 先例（fixed＋fixedAt＋fixedBy＋消账注）缺登记簿翻转，纯记账滞后 | 转登 findings.json（open，责任方：io 单元实施者收尾提交顺手翻转，或 IO-T07 合入收尾时由治理动作统一翻转——引用本记录 §4.8 为依据）；不阻塞合入 |
| 观察（不新登） | —— | 4.5 注入二的单点形态（仅 begin 入口 cancelHit 恒假）未使用例变红——begin 内建临时区 create 透传取消令牌构成第二道防线，行为契约仍成立；用例断言契约行为而非实现行，属分层防御的健康形态。与 F-199 标题在案的 IO-T06 同型观察一致，无需跟踪项 | 留档于 4.5 |
| 建议（既有，不新登） | F-007/F-200 | worktree 环境前置三件（vcpkg toolchain×Qt prefix×bin 模板两文件）——本次验收同型命中（首配 Qt NOT found、二配 ini.template.static 缺失）、按先例补拷处置 | 保持 open（构建约定所有者） |
| 建议（既有，消账状态核对） | F-198/F-199 | 实质消账已落位、登记翻转滞后 | 见 F-203 |
| 建议（既有，不新登） | F-191/F-197/F-202 | ird_gates 词表/例外实例待扩（本次 17 处命中含其既有构成＋F-202 新增 2 处，逐边与登记一致） | 保持 open（门禁所有者） |

## 裁决

**verdict = pass**。4.1~4.11 全部通过、零阻断级问题；建议级 1 项（F-203）已按 ACC §5 转登 findings.json，不阻塞合入。合入决策按 ACC §6 归所有者。

## 验收者环境说明

- 复现现场：`git worktree add --detach D:/10_Source_Repos/21_robot/.acc-tmp/io-t07-a d381d9d3e430d910b2a8ed8a4bf41eadd5dbc180`；集成树 `io-t07-a/build`（配置：VS17 2022 -A x64＋vcpkg toolchain＋Qt prefix＋`RWS_BUILD_INDUSTRIALROBOT=ON`，缓存实测 BOOL=ON）与冒烟树 `io-t07-a/build-smoke-acc`（独立缓存＋vcpkg toolchain）均为本次从零新建；记录回传现场 `D:/10_Source_Repos/21_robot/.acc-tmp/io-t07-b`（分支 `acc/IO-T07/2`）基于 origin/redesign-main＝eab17eee77880b2121cca767053188cef986a67a。
- 全部证据（双模式配置/构建/四跑 129/129、契约 verify 两条与治理脚本三件、ird_gates 17 处逐边对账、两处失效注入与还原复绿、V 行追溯字段 129 用例程序化校验、红线/词表扫描、留痕与三笔提交实读）为验收者在本现场亲手执行；两处注入仅存在于临时 worktree 构建产物且已还原复绿（`git status` 零残留），任务分支与送验对象零触碰、零推送。验收完成后临时现场随流程删除，本记录与三元组为唯一持久证据。
