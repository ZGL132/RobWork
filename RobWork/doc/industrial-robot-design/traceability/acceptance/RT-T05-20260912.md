# RT-T05 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | RT-T05 实现 RuntimeNameMap（⑥端口，双射解析）——tasks/foundation/RT-T05.json |
| 送验对象 | **215578dec0d0021c8d5769679589451d8a714d0c**（40 位冻结 SHA；`origin/wp06-t05` == 该 SHA，分支漂移检测通过） |
| base..head | e3475226837a0e7382f7defaa2db9a75e48d5e73..215578dec0d0021c8d5769679589451d8a714d0c（3 提交：b6a2a95 实现、4b8c869 留痕、215578d 文档落位） |
| 验收者 | 全新上下文独立会话（ACC §2 四条独立性要求适用：只消费产物与留痕，未读取实施叙述；构建/测试/门禁/探针全部亲手重跑，对抗式立场） |
| 日期 | 2026-09-12 |
| **verdict** | **pass**（4.1~4.11 全部通过，无阻断级问题；建议级 G-1 已转登 findings.json **F-040**，open） |

---

## 复现现场与环境（ACC §3）

- worktree A＝`git worktree add --detach D:/10_Source_Repos/21_robot/acc-rt-t05-wtA 215578de…`（detached @ 冻结 SHA）；集成树全新配置于 A 内 `build/`；冒烟独立目录 `D:/10_Source_Repos/21_robot/acc-rt-t05-smoke`。
- OS Windows x64；MSVC VS2022 17.12（"Visual Studio 17 2022" 生成器，x64）；vcpkg 经典模式（仓库根，gtest 1.18.0 就位）；Qt 6.11.1（D:/software/Qt/6.11.1/msvc2022_64）。
- 环境前置处置（F-007/F-009 既有登记口径，RT-T04 验收同款）：首跑配置失败于 `RobWorkStudio.ini.template.static` 缺失（.gitignore `[Bb]in/` 吞掉的 untracked 框架前置，非送验产物）——从主树复制至 worktree A 同路径后重配通过；复制后 `git status --porcelain` 零行，跟踪树字节级一致。
- 验收者留痕（`D:/10_Source_Repos/21_robot/` 下持久文件，**不在**临时 worktree 内；关键数字摘录正文）：acc-rt-t05-configure.log／configure2.log（首次失败＋重配成功）、acc-rt-t05-integration-build.log、acc-rt-t05-smoke-configure.log、acc-rt-t05-smoke-build.log、acc-rt-t05-smoke-test-run.log、acc-rt-t05-smoke-test.xml、acc-rt-t05-integration-test-run.log、acc-rt-t05-integration-test.xml、acc-rt-t05-probe1-build.log／probe1-run.log、acc-rt-t05-probe2-build.log／probe2-run.log、acc-rt-t05-restore-build.log／restore-run.log、acc-rt-t05-gates-run.log。

## 逐项证据（4.1~4.11）

### 4.1 改动范围 —— PASS

`git diff --name-status e347522..215578d` 共 19 个变更文件，逐个对照契约 allowedFiles：

- `runtime/**` 6 个：CMakeLists.txt（M——target_sources 增列 NameMap.cpp＋测试增列 NameMapTest.cpp）、include/…/Codec.hpp（M——IRDNAME 三形态增量声明）、include/…/NameMap.hpp（A）、src/Codec.cpp（M——IRDNAME 实现增量）、src/NameMap.cpp（A）、test/NameMapTest.cpp（A）；
- `traceability/**` 12 个：builds/wp06-t05/ 下双模式 XML×2＋日志×9＋ird-test-report.json；
- `units/runtime.md` 1 个（M——v0.6 落位登记，卡头＋§15.4 一行）。

三组路径均在 allowedFiles（runtime/**、traceability/**、units/runtime.md）内；forbiddenFiles（REQUIREMENTS.md、ARCHITECTURE.md）零触碰（diff 零命中）；`patches/` 无新文件、PATCHES.md 零改动（SA-02 合规）。

### 4.2 可复现构建 —— PASS

- 集成全新配置：`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DCMAKE_TOOLCHAIN_FILE=<仓库根 vcpkg> -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64 -DRWS_BUILD_INDUSTRIALROBOT=ON` → 首跑 `Configuring incomplete`（ini 模板缺失，F-007 前置）→ 复制 untracked 前置文件后重跑 **exit 0**（Configuring done／Generating done）；缓存确认 **`RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`**。
- 契约目标从零构建（全新树，无任何存量产物）：`cmake --build build --config Release --target sdurws_ird_runtime_test` → **exit 0**；日志 grep `error C[0-9]|error LNK|fatal error` **零命中**；29 个目标全部从零编译（framework 依赖链 sdurw_*→sdurwsim→sdurws_ird_core→sdurws_ird_runtime→sdurws_ird_runtime_test.exe）。
- 冒烟独立目录：`cmake -S <A>/industrialrobot -B <smoke> -DCMAKE_TOOLCHAIN_FILE=<vcpkg>` → **exit 0**；`cmake --build <smoke> --config Release --target sdurws_ird_runtime_test` → **exit 0、零错误**（零框架链接口径——L1 清单为空由 BaselineLibsPinned 用例钉住）。
- 双模式测试：集成 **130/130 PASSED、exit 0**（XML `tests="130" failures="0"`）；冒烟 **130/130 PASSED、exit 0**（XML `tests="130" failures="0"`）。130＝既有 105（RT-T01~T04 回归）＋新增 25（NameMapTest.cpp `grep -c "TEST("` 实测 25）。
- 全树从零构建未纳入本次实测（F-008 基线缺陷已由 RT-T02/T03 验收两次实证阻挡全新树全量构建；本任务按 F-036/F-038 登记口径采用"任务目标面全新树从零零错误"，已达 runtime 实现任务硬验收面）。

### 4.3 契约 verify 命令 —— PASS

契约 verify 数组仅一条：`cmake --build build --config Release --target sdurws_ird_runtime_test`——验收者在全新 worktree 集成树亲手执行，**exit 0、零错误**（同 4.2 第二条，从零构建口径）。

### 4.4 acceptance 条目逐项 —— PASS（3/3 有证据）

| acceptance 条目 | 证据（验收者亲手复现） |
| --- | --- |
| 1. RT-NM-1~7 双向往返全量用例通过（AT-18 载体） | `NameMapRoundtripTest.ForwardChainObjectToNameToObject_FullCoverage_RT_NM_1`（正向链 10 对象逐 id 全量非抽样）＋`ReverseChainNameToObjectToName_RT_NM_1`（反向链 18 条目逐名＋身份条目计数＝对象数）＋`BijectionCounts_RT_NM_1`（无重复名＋范围划分不重不漏）＋`EncodeParseRoundtrip_RT_NM_1`（parse(encode(x))==x＋身份相等）＋RT-NM-2/3/4/5/7 各组——双模式 XML 全 PASSED |
| 2. 前缀操作仅限本模块（R-4 红线——唯一合法位置） | `NameMapR4SourceScan.PrefixJoinConfinedToNameMapModule_RT_R4`（runtime 产品源全扫描：joinScopeLocal 恰出现于 NameMap.hpp 声明＋NameMap.cpp 实现两文件，其余零命中）；验收者另做独立 grep 复核：runtime 单元除 NameMap 外仅 Codec.hpp 注释文本与 Codec.cpp L890 错误消息版本串（"major.minor"，非作用域名拼装）——非 R-4 违例 |
| 3. 全量生成双向校验（MDL-14：无遗漏/无重复前缀/无旧机械臂名） | 无遗漏＝`BuildsAllScopesFromRichFixture_RT_NM_1`（逐范围计数 Device1/Joint2/LinkFrame3/BaseMount1/BaseFrame1/Flange1/Tcp1/Geometry4/SceneObject1/Body3/Sensor0＝18 条目）；无重复前缀＝`DoublePrefixRejected_RT_NM_6`（"IRB_T.IRB_T.joint_1" 经 crossCheckRuntimeNames 抛 NameConflict 且回显原名）；无旧名＝`RenameRebuildNoOldResidue_RT_NM_5`（新映射旧名未命中＋旧快照旧名仍可反解）。WC 实物切面（真实 WorkCell 对象名收集）随 RT-T07/T11——ird-test-report.json acceptanceCoverage 与测试文件头均已显式声明该边界，非未声明缩水 |

无"未执行"项：ird_gates 已实际执行并登记真实结论（见 4.8——非 F-035 型失实标注，本任务提交如实写"命中 2 项均为既有登记项"）。

### 4.5 测试真实失败能力 —— PASS（双探针，注入即红、还原复绿）

- **探针 1**（消歧规则）：NameMap.cpp `kDisambiguationSuffixBegin` 2→3（单行精确编辑＋探针标记）→ 重建 **exit 0**（显式核验，防旧二进制假绿/假红）→ 全量运行 **exit 1、恰 3 例变红**：`DuplicateLocalNamesDisambiguatedByObjectIdOrder_RT_NM_2`（预期保留 `_2` 名）、`IllegalCharVariantsCollideAfterLegalization_RT_NM_2`、`ReservedWorldDisambiguated_RT_NM_2`——全部为消歧后缀语义的直接断言面 → 精确还原 → `git diff` **0 行**（字节级）。
- **探针 2**（编解码防御）：Codec.cpp parseNameMap 尾随字节校验条件 `r.offset() != bytes.size()` → `r.offset() > bytes.size()`（恒假＝校验失效）→ 重建 **exit 0** → 全量运行 **exit 1、恰 1 例变红**：`ParseRejectsMalformedInput_RT_NM_1`（尾随字节拒绝断言）→ 精确还原 → `git diff` **0 行**。
- 还原后重建复绿：**exit 0＋130/130 PASSED**（restore-run.log）。两探针均证明测试非恒真，且失败面与注入点语义精确对应。

### 4.6 架构红线 —— PASS（八条逐条）

1. **零 Qt**：runtime 单元 include/ src/ grep Qt 零命中（`RuntimeBuild.NoQtInclude_RT_BUILD_NFR_MNT_01` 回归绿；CMakeLists L196 配置期 Qt 目标守卫在位）。
2. **R-1 业务域互链**：新增文件对 modeling/requirements/kinematics/trajectory/dynamics/selection/optimization 头 include **零命中**（验收者 grep 实测）；唯一单元依赖＝core（CMake L190 配置期断言守卫在位）。
3. **R-2 跨单元私有头**：ird include 全部为 `sdurws/ird/core/{Digest,Identity}.hpp` 公共头＋runtime 自有头（NameMap.hpp 只 include 本单元公共头与 core 公共头）；无私有头跨单元。
4. **SA-02 框架零修改**：diff 零框架文件、patches/ 零新文件（4.1 已核）。
5. **R-4 名称前缀**：拼装单点＝NameMap.cpp `joinScopeLocal()`（L94）；`wellFormedFullName` 等处只做字节比对不构造名称；`PrefixJoinConfinedToNameMapModule_RT_R4` 源码扫描绿；全仓静态门禁归 WP-01-T01（单元卡与测试注释如实声明，无越权声明"已全仓扫描"）。
6. **PA-1 权威唯一**：名称解析权威落位 runtime（本任务即该权威的实现）；消歧/空名警告**不私裁诊断码**——RuntimeNameNotice 只承载数据三元组，码值权威留 diagnostics（NameMap.hpp L166-175 注释明示，units/runtime.md v0.6 ⑥ 登记）；摘要只经 core::ContentDigester（CR-02，computeNameMapContentIdentity 无第二套 SHA-256）。
7. **PA-2 不可变历史**：本任务无修订/写路径；映射本体构建后只读（全 const 访问器、私有成员仅 friend 构建路径可写——"构建后只读"由类型设计成立）。
8. **不恢复 old/**：无 old/ 引用，全部从零实现。

### 4.7 注释规范 —— PASS

- 公共头全查：**NameMap.hpp**（510 行——文件头设计依据/背景/映射条目构成/不变量/生命周期/线程安全全段；每 struct/class/枚举/函数中文 Doxygen；R-4 单点声明、Expected 错误语义、复杂度、确定性来源逐项在位；§2.5 高危信息——所有权（BoundRuntimeNameResolver 非拥有指针）、线程约束（构建后只读可并发共享）、错误语义（查询轨不抛 vs 构造期 fail-fast）、确定性（排序键/消歧分配序）——无缺项）。
- 实现抽查 ≥3：**NameMap.cpp**（文件头 R-4 红线落点声明；§7.2 每条规则常量带出处行注释；buildRuntimeNameMap 五步骤逐步注释含跨组安全性说明与防御性 NameConflict 通道；排序键与确定性来源显式）；**Codec.cpp 增量段**（IRDNAME 布局逐字段、五步校验链、条目数剩余字节预算防线的 magic number 37＝1+16+5×4 逐项解释）；**NameMapTest.cpp**（文件头替身边界声明＋未执行边界声明；每用例中文标注设计矩阵行/需求号/AT 载体；断言处逐条 why——实测满足测试代码注释要求）。**Codec.hpp 增量段**（三形态契约＋"编码不嵌摘要"差异说明）。§2.5 高危信息表逐类核对无缺项。

### 4.8 文档同步＋findings 核对 —— PASS

- units/runtime.md v0.6 八项登记逐项与实现比对一致：①双层条目口径（身份作用域＋派生共享 ObjectId）与 NameMap.cpp 收集器/身份索引实现一致 ✓；②消歧后缀格式/uint64 预算登记为设计默认待产品确认（与 DTB O-12 同源，代码注释同口径——不私裁）✓；③ruleVersion=1 ✓；④工具/场景几何 facet 统一 collision（pushGeometry 调用点实证）✓；⑤IRDNAME 不嵌摘要＋parseNameMap 字节预算防线（kMinEntryBytes 检查实证）✓；⑥RuntimeNameNotice 数据承载＋码值归 diagnostics ✓；⑦crossCheckRuntimeNames 纯函数交付（真实 WC 收集随 RT-T07/T11 显式声明）✓；⑧R-4 例外两文件清单 ✓。任务卡零未登记偏差；`validate-task.ps1 -TaskFile tasks/foundation/RT-T05.json` 验收者亲手复跑 **PASS**。
- 追踪面：phase-one-validation.log L43 RT-T05 行在位（validate-task PASS，DTB v0.7 批次登记）；ird-test-report.json 承载 resultNote 义务（双模式结果＋probe＋gates＋deviations）；单元卡 §12 RT-T05 行交付物与实际产物一致。任务状态翻转归流水线收尾（编排者辖域），非实施产物缺陷。
- **findings.json 未关闭条目相关性核对**（31 项 open）：F-007（全新树配置前置）——验收者本次再次踩中并按登记口径处置，与本任务产物无关；F-019（门禁基线红）——验收者在全新树实测命中同 2 项（IRD-GATE-SUB/T1，testkit 边），与本任务新增文件无关、零新增命中；F-008（全树从零构建阻挡）——本记录 4.2 如实采用目标面口径；F-012 家族（F-023/F-024/F-026/F-027/F-030/F-034/F-037/F-039）——本任务两份提交标题同型再现 → 转登 **F-040**（见 4.9）；其余 open 条目（F-004/005/006/009/010/012/015~018/020/025/028/029/031~033/035/036/038）辖域与 runtime NameMap 无交集。无静默积压，无可随本任务顺手消账项。

### 4.9 commit 质量 —— PASS（G-1 建议级）

- b6a2a95 `[WP-06-T05] 实现 RuntimeNameMap 双射名称解析（§7 名称映射/消歧/⑥端口）`——动词开头✓；四段齐备✓；验证段（双模式 130/130、探针注入即红复绿、门禁 2 项既有命中）与留痕及验收者复现**逐数字一致**✓。
- 4b8c869 `[WP-06-T05] 验证留痕：双模式测试报告＋失败能力探针＋门禁与契约校验证据`——正文四段齐备、12 份留痕清点属实✓，但标题"验证留痕"为名词短语开头（F-034 对 RT-T02『RT-T02 验证留痕…』的已判同型形态）。
- 215578d `[units] runtime v0.6：RT-T05 实现落位登记（反向链落位口径＋设计默认登记）`——正文四段齐备、八项登记内容属实✓，但前缀后以名词短语"runtime v0.6"开头（F-037/F-039 同前缀形态）。
- 两处标题瑕疵同属 F-012 家族，为该家族第 10 例（双形态各再复现一次）；纯风格瑕疵不构成阻断 → 合并转登 **F-040**。
- 无粉饰：实施侧声称的每个可复现数字（双模式 130/130＝105+25、探针恰 1 例红→复绿、ird_gates 命中 2 项既有、validate-task PASS、XML 计数 130）均被验收者独立复现证实；ird_gates 未虚标"未执行——门禁未建成"（F-035 教训已被本任务正确避开）。

### 4.10 留痕完整 —— PASS

- `traceability/builds/wp06-t05/` 12 件齐备：双模式 gtest XML（tests="130" failures="0"，testsuite 级逐套件计数与总计数一致）、integration-build/integration-test-run/smoke-configure/smoke-build/smoke-test-run/probe-build/probe-test-run/ird-gates-run/validate-task 日志、ird-test-report.json（ird-test-report/1 格式：25 新增用例逐条含设计矩阵行追溯＋105 回归登记、probe 段、gates 段、acceptanceCoverage 三条逐条对应、deviations 两项登记）。
- phase-one-validation.log L43 `RT-T05.json: validate-task: PASS`；resultNote 义务由 ird-test-report.json 承载。
- 验收者侧独立复现产物：双模式全新构建日志、双模式 XML、双探针 build/run 日志、门禁日志（扫描根＝worktree A industrialrobot，实证非主树增量）——路径见"复现现场"节。

### 4.11 偷懒/缩水实现扫描 —— PASS

- ①标记扫描：`git diff e347522..215578d` 全文 grep `TODO|FIXME|stub|placeholder|未实现|not implemented` **原始命中恰 1 行**——NameMapTest.cpp L143"替身边界声明（RT-STUB-0 精神）"，为引用"禁用替身"治理规则的注释文本（声明本测试组**不**用替身伪造），非未完成标记；无契约"触发式延后"豁免条款需要列出。
- ②空实现检测：无——buildRuntimeNameMap 五步规则全流程实质实现（合法化/保留字/消歧探测/稳定排序/身份）、resolveRuntimeName/resolveObjectId 双索引查询实质实现、crossCheckRuntimeNames 逐项核对实质实现、encodeNameMap/parseNameMap 全字段编解码＋五步防御实质实现；`IRuntimeNameResolver` 为设计声明的端口抽象（⑥端口形态），配套 BoundRuntimeNameResolver 参考实现——非空壳。
- ③逐条证据表：见 4.4——acceptance 3 条均有具名测试＋双模式 XML＋验收者复现，任一条不缺。
- ④测试强度：断言校验行为与边界（逐范围计数、字节级等值、错误码＋回显 detail 子串、警告三元组逐字段、编码幂等、身份互异）；恒真风险由 4.5 双探针（注入即红）实证排除；负路径（垃圾 magic/截断/尾随/坏版本/scope 越界/count 超预算/位翻转身份必变）逐类拒绝断言。

---

## 问题清单

| 级别 | 编号 | 描述 | 处置 |
| --- | --- | --- | --- |
| 阻断 | 无 | — | — |
| 建议 | G-1→**F-040** | 送验提交 4b8c869（`[WP-06-T05] 验证留痕：…`）与 215578d（`[units] runtime v0.6：…`）标题以名词短语开头，非动词开头——F-034 形态与 F-037/F-039 形态各再复现一次，F-012 家族第 10 例。正文四段齐备、验证结论与留痕一致（验收者逐数字复现），纯风格瑕疵。已转登 findings.json（本 evidence 分支同步提交） | F-012 家族合并处置（实施者模板补动词开头示例）；已推送历史不追溯改写（AGENTS §6.3） |

## 裁决

**verdict＝pass**：4.1~4.11 全部通过，无阻断级问题；建议级 G-1 已转登 findings.json（F-040，open），不阻塞合入。验收对象为冻结 SHA 215578dec0d0021c8d5769679589451d8a714d0c；合入决策归所有者（PIPE §4.7）。

## 附：验收者环境说明

- 复现现场：worktree A＝detached @ 215578d（配置/构建/双模式测试/门禁/双探针/复绿）；worktree B＝acc/RT-T05/1 @ origin/redesign-main（本记录）。
- 环境前置：ini 模板 untracked 文件按 F-007/F-009/RT-T04 先例自主树复制（跟踪树零改动，`git status --porcelain` 实证）。
- 探针纪律：每次注入后显式核验构建退出码（exit 0＝红来自测试失败而非编译失败），还原后 `git diff` 0 行核验字节级还原，再重建确认 130/130 复绿（TK-T07/T10 旧二进制教训口径）。
- 实施者未读取：本记录全部结论基于契约、设计文档、diff 与留痕产物及验收者亲手复现，未消费任何实施叙述。
