# PRJ-T04 验收记录（attempt 1，2026-09-16）

| 字段 | 值 |
| --- | --- |
| 任务 | PRJ-T04（project 单元格式契约与编码 PersistenceFormat/Codec；契约 tasks/foundation/PRJ-T04.json；需求 CON-01/NFR-DEP-04；knownPitfalls P-PR-1/CR-02） |
| **verdict** | **pass**（4.1～4.11 全部通过；零阻断级问题；建议级 2 项已转登 findings.json F-129～F-130） |
| 送验对象（冻结） | `a148a99e4d6bd5f3b06d0c6ad69aed9de349d1fa`（40 位）；分支漂移检测：`git rev-parse origin/wp04-t04`＝该 SHA（fetch 后实测），零漂移——验收全程按该 SHA 的 detached worktree 进行，不按分支名 |
| diff 范围 | `7dd1eaad0d00cd8a4b5ca1ea7e4cb61cc900edf9..a148a99e`（state.base..headSha；`origin/redesign-main` 尖端恰为该基线，即纯任务分支增量；3 个提交：ae7fb6aa 实现＋ae9596db 单元卡 v0.6＋a148a99e 留痕归档） |
| attempt 说明 | 尝试序号 1 由编排者随验收请求下发（state.attempts.accept＝1）；`git ls-remote --heads origin "acc/PRJ-T04/*"` 实测为空、origin/redesign-main 的 traceability/acceptance/ 无 PRJ-T04 同名文件——本文件为该任务首份验收记录 |
| 验收者环境 | 全新上下文；复现现场＝`git worktree add --detach D:/10_Source_Repos/21_robot/_acc_prjt04/wt-a a148a99e`；集成模式全新配置（vcpkg toolchain＋Qt CMAKE_PREFIX_PATH＋`-DCMAKE_CXX_FLAGS=-DNOMINMAX -DWIN32_LEAN_AND_MEAN -DWIN32`＋`RWS_USE_STATIC_LINK_PLUGINS=ON`，与主树缓存逐项一致；`RobWorkStudio.ini.template.static/.shared.in` 两件 git 未跟踪本地资源自主树复制——F-007/F-062/F-099 登记件，验收者本轮亲历复踩：裸配置先在 RobWorkSetup.cmake:96 find_package 失败、补 Qt 前缀后在 RobWorkStudio/src/CMakeLists.txt:58 失败，按登记口径补齐后 exit 0；首轮失败残留的污染缓存目录整目录删除后重配——F-101 口径）；冒烟模式另配独立临时目录 `_acc_prjt04/build-smoke-acc`；CMake 4.3＋MSVC VS2022 x64；pwsh 治理脚本 |
| evidence | 分支 `acc/PRJ-T04/1`（本文件＋findings.json F-129～F-130 同支承载；commit 三元组见文末） |

---

## 1. 逐项证据（ACC §4.1～4.11）

### 4.1 改动范围 —— PASS

- `git diff --name-status 7dd1eaad..a148a99e` 共 12 文件，逐一比对契约 `allowedFiles`：
  `project/**`×6（CMakeLists.txt、include/sdurws/ird/project/{PersistenceFormat.hpp〔新增〕,StoreTypes.hpp}、src/Codec.cpp〔新增〕、src/Codec.hpp〔新增，src/ 私有实现头——R-2〕、test/PersistenceFormatTest.cpp〔新增〕）✓；`traceability/**`×5（builds/wp04-t04/ 下 ird-test-report.json＋双模式 gtest XML 四份）✓；`units/project.md`（契约显式列名）✓。
- `forbiddenFiles`（REQUIREMENTS.md / ARCHITECTURE.md）零触碰 ✓。
- `industrialrobot/patches/` 零新文件；框架源码（RobWork/RWS/RobWorkSim）零改动（diff 全清单核对，无任何清单外路径）——SA-02 无登记义务产生 ✓。

### 4.2 可复现构建 —— PASS（验收者全新树亲手复现，不采信实施日志）

- **集成模式**：worktree A 内 `cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON`（补 F-007 环境链参数，见环境说明）→ **exit 0**（`-- Configuring done`＋`-- Generating done`，零 CMake Error）；缓存 grep 确认 **`RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`**。
  - 契约 verify 目标 `sdurws_ird_project_test` 从零构建 **exit 0**、`error C|error LNK|fatal error` 计数＝**0**；构建覆盖面＝依赖闭包从零编译：框架侧 sdurw_common/sdurw_core/sdurw_math＋sdurws_ird_core＋sdurws_ird_project＋测试目标，109 个翻译单元（`*.vcxproj ->` 产物行与 `.cpp` 编译行实测）。
  - `sdurws_ird_project_contract_test` 构建 **exit 0**、错误计数＝0。
  - 两测试可执行文件运行：**81/81＋10/10＝91/91 全绿**（gtest 输出实测，XML 落验收者证据目录）。
- **独立冒烟模式**：`-S industrialrobot -B _acc_prjt04/build-smoke-acc -DCMAKE_TOOLCHAIN_FILE=<仓库根 vcpkg 绝对路径>`（AGENTS §4.1＋CORE-T01 G-2 口径）配置 **exit 0**；Debug 全量构建 **exit 0**、错误计数＝0；另按 PRJ-T03 判例口径追加 `--config Release` 全量构建 **exit 0**、错误计数＝0；两配置下测试 exe 运行均 **81/81＋10/10 全绿**。
- 口径说明：全新树**全树**构建受 F-008 基线框架缺陷阻挡（既有登记，非本任务引入），本次复现覆盖任务依赖闭包面（框架 math/common/core＋project→core 链＋两测试目标＋门禁＋治理脚本），与 PRJ-T01/T02/T03 既往验收口径一致。

### 4.3 契约 verify 命令 —— PASS

- 契约 `verify` 数组仅 1 条，验收者在 detached worktree 根**逐字原样执行**：
  ① `cmake --build build --config Release --target sdurws_ird_project_test` → **exit 0**（增量确认＋首次从零构建各一次，均成功）。
- `verify-task.ps1 -TaskFile tasks/foundation/PRJ-T04.json` 实跑 → **verify-task: PASS PRJ-T04，exit 0**（内部执行同一命令集，双口径一致）；`validate-task.ps1` → **PASS (1 tasks)** exit 0；`validate-docs.ps1` → **PASS (20 units, 12 trace entries, 150 task files)** exit 0。

### 4.4 acceptance 条目逐条 —— PASS（4/4 有对应证据；无虚标；ird_gates 实际执行）

| # | 契约条目 | 验收者证据（全部亲手复跑，两模式一致） |
| --- | --- | --- |
| 1 | PersistenceFormat.hpp 磁盘格式类型 round-trip：parse(dump(x))==x 全类型（§4.4 字段级契约）＋§4.8 自有格式规则（ASCII/固定字段序/无浮点——全字符串身份与整数） | `PersistenceFormatRoundTrip` 9 用例两模式 18/18 通过：六类型＋§4.2 静态标识逐一 round-trip（`StaticIdentity_ValuePreserved_CON01`、`HeadRecord_ValueAndDeterministicBytes_CON01_NFRCOR02` 同值两次 dump 逐字节相同、`RevisionManifest_OptionalPresenceAndAbsence_CON01` 可选两态＋缺省键不出现、`MetadataRecord_ChineseEscapedRoundTrip_CON01_NFRCOR02` 中文经小写 `\u673a\u68b0` 转义还原、`CommandRecord_FullAndMinimal_CON01`、`DraftDocument_WithAndWithoutExternalRefs_CON01`＋origin 三值逐一、`HeadRecord_GoldenBytes_FieldOrderFixed_CON01_NFRCOR02` 黄金字节钉死字段序与紧凑形态、`AllTypes_DumpOutputPureAscii_CON01`、`MetadataRecord_AstralPlaneChar_SurrogatePairRoundTrip_CON01` U+1F600 代理对）；"无浮点"由 dump 侧无浮点路径＋parse 侧 `FloatAndExponentLiterals_Rejected` 双向钉住 |
| 2 | 非法输入拒绝（截断/越界/未知 schemaVersion——主版本破坏性拒绝＋升级指引数据〔NFR-DEP-04 数据侧〕；次版本追加可选字段兼容语义） | `PersistenceFormatReject` 20＋`PersistenceFormatVersioning` 5 用例两模式 50/50 通过：截断六类型＋收尾单字节、垃圾/空输入、非 ASCII 原始字节、尾随内容、非 canonical 空白、七字段逐一剔除、未知字段（顶层＋嵌套）、类型错位、浮点/指数、true/false/null、越界（2^64/2^32/2^31/负序号）、前导零、重复键、core 身份坏规范文本（tag 错/长度错/大写 hex/缺 cv- tag）、commandType 语法违例、origin 非冻结三值、objectRefs/branches 空数组、token 空值；版本侧：`FutureMajorVersion_Rejected_WithUpgradeGuidance_NFRDEP04`（SchemaFuture＋detail 三键 document=20000/supported=10000/upgrade= 逐键断言）、`LegacyMajorVersion_Rejected_FormatLegacy_NFRDEP04`（0.9/0/负值）、`FormatIdMismatch_Rejected_FormatLegacy`、`NextMinorVersion_UnknownOptionalFields_Tolerated_NFRDEP04`（1.1 文档未知可选字段顶层＋嵌套容忍＋降级读丢弃未知字段＋同文档 1.0 标签拒绝的对照）、`NextMinorVersion_NestedBranchFields_Tolerated`；升级器本体未实现＝契约明文归位阶段 B §8.11，非缩水 |
| 3 | externalRefs 记录结构单测（PM-01 存储侧：路径＋内容哈希——NFR-REL-04 检测数据源，§4.4.5；SourcedValue 来源标记透传不解释） | `ExternalRefRecords` 3 用例两模式 6/6 通过：`SixFieldsRoundTrip_LargeSizeBytes_NFRREL04`（六字段 round-trip＋sizeBytes＝uint64 上界 18446744073709551615 无损）、`MultipleRecords_KeepOrder`（登记序＝canonical 序）、`InvalidHashAndEmptyPath_Rejected`（非 64hex 哈希＋空路径拒绝）；SourcedValue 透传由 `DomainPayload_TransparentBytePreservation_CR02_D10` 承载（payload 内含 state/provenance 的 SourcedValue 样例序列化文本逐字节保真，project 不解释） |
| 4 | P-PR-1 处置（core 消费以 core.md v0.1 为基线不私改 core）＋CR-02 处置（对象负载摘要＝收到的字节经 core ContentDigester——D-10；自有格式编码器不私设第二哈希路径） | `ContentDigestBoundary` 5＋`CoreContractConsumption` 1 用例两模式 12/12 通过：`ContentVersionOf_MatchesCoreDigester_CR02_D10`（与 core 直算一致＋FIPS 180-2 向量 `cv-ba7816bf…15ad` 双重钉住）、`ContentVersionOf_DeterministicDistinctAndEmpty_CR02`（同/异字节＋空负载 FIPS 向量＋分块等价）、`DigestFields_PassedThroughNotRecomputed_CR02`（全 digest 类字段填"非真实"合法摘要值 round-trip 原样保留——编码器若私设重算路径此用例必红）；源码面亲查：`Codec.cpp` 全文除 `contentVersionOf()` 一处（L1354-1362，仅调 core::ContentDigester）外零哈希计算；core 侧零修改（4.1 diff 核对：core 目录零文件）；消费面显式 `using core::…` 引入（PersistenceFormat.hpp L57-67），Identity/Digest 均为 core v0.1 公共头原文消费 |

`ird_gates` 执行说明（ACC §4.4"未执行的不得标注通过"）：门禁已建成，验收者**实际执行** `cmake --build build --config Release --target ird_gates` → exit 1＝**8 处命中**：IRD-GATE-SUB×7（diagnostics_contract_test／evidence_test／evidence_contract_test／policy_test／policy_contract_test／runtime_test 各直链 testkit＋testdata_lint→testkit 表外边）＋IRD-GATE-T1×1（testdata_lint 产品目标链 testkit）——与 F-115（DIAG-T10 起）登记的 8 项基线逐项同分布、与送验报告声明一致，**零新增、零 sdurws_ird_project 条目**（门禁输出 grep "project" 计数＝0）；R1/R3/R4/R5/T2 token 为引擎自测"按预期检出"行。exit 1＝基线既有命中的预期行为（SA-10/DTB §4.5 登记义务随任务流转），非本任务问题。

### 4.5 测试真实失败能力 —— PASS（2 组实现级探针注入即红；均还原复绿）

探针打在 `src/Codec.cpp` 实现体（一次性 worktree 内；每轮 Edit 引入新 mtime、增量重编译、滤跑目标用例、`git checkout --` 还原并确认工作树干净、全量重跑复绿——F-068/F-075 口径）：

| 探针 | 注入 | 结果 |
| --- | --- | --- |
| 1 | `dump(HeadRecord)` 字段序变异：revisionId/branchId 输出位置互换（黄金字节/固定字段序破坏） | `HeadRecord_GoldenBytes_FieldOrderFixed_CON01_NFRCOR02` **红**（`[FAILED]`，黄金文本精确断言真实存活）；还原复绿 |
| 2 | `checkSchemaVersion` 主版本拒绝阈值放宽（`> kSchemaVersionMajorCurrent` → `> +5`，§8.11 破坏性拒绝旁路） | `FutureMajorVersion_Rejected_WithUpgradeGuidance_NFRDEP04` **红**（schemaVersion=20000 不再抛 SchemaFuture——"期望 SchemaFuture 未抛出"断言真实存活），同组其余 4 用例仍绿（边界精确）；还原复绿 |
| 还原后 | `git checkout --`＋重编＋全量重跑 | **81/81＋10/10 复绿** ✓（两目标，无探针残留） |

### 4.6 架构红线 —— PASS（AGENTS §5 八条逐条）

1. **L2 零 Qt**：新增/改动四源文件 Qt 头扫描零命中（grep 实测 exit 1）；CMake 配置期守卫在位；构建输出中的 `Automatic MOC` 为 F-004 已登记的全局空转现象（零 Qt 依赖实证）✓。
2. **R-1 业务域互链禁止**：产品库唯一单元边 project→core（CMakeLists.txt L113 `target_link_libraries(sdurws_ird_project PUBLIC sdurws_ird_core)`）；跨单元协作零新增边（ird_gates 输出零 project 条目佐证；契约测试 `ProjectLinkage` 组 5 用例零改动回归通过）✓。
3. **R-2 私有头不出单元**：本任务唯一新增实现头 `src/Codec.hpp` 位于 src/（不在 include/）；测试目标以相对路径 `#include "Codec.hpp"` 消费（同单元允许形态）；跨单元 include 仅 `<sdurws/ird/core/{Digest,Identity,DiagData}.hpp>`——core 公共头（include/sdurws/ird/core/ 下）✓。
4. **SA-02 框架零修改**：diff 无框架文件、patches/ 零新文件（见 4.1）✓。
5. **R-4 名称前缀拼接/剥离**：新增源码零 RobWork 名称操作逻辑（全文审读）✓。
6. **PA-1 权威唯一**：磁盘格式/编解码为 project 单元 §4.4/§4.8 设计内职责；身份/摘要类型一律消费 core（不复定义第二套）；发现 §4.4.4 与 §6.5 文档矛盾时按 DTB §5.4 登记待裁决（P-PR-9）而非代码私裁 ✓。
7. **PA-2 不可变历史**：本任务为纯格式契约/编解码，不触及修订写路径与历史改写面 ✓。
8. **不恢复 old/ 实现**：零 old/ 引用；自含 JSON 引擎按 §4.8"JsonLite（testkit）不用于产品格式"从零编写（对照文档逐节核对）✓。

### 4.7 注释规范 —— PASS（公共头全查＋实现文件抽查 ≥3）

- **公共头全查**：`PersistenceFormat.hpp`（636 行）——文件头（设计依据 §4.2/§4.4/§4.8/§8.11/§3.1＋需求 CON-01/NFR-DEP-04/PM-01＋契约 acceptance＋与 Codec 的分工背景＋六类型↔磁盘条目对应表＋线程安全/确定性声明）；每个 struct/enum 类级 Doxygen 中文背景说明（业务概念、权威归属、与其他结构的分工）；每个函数 @brief/@param 方向/@return/@throws 语义；§2.5 高危信息逐项在位：单位（sizeBytes＝字节、revisionSeq 无单位纯计数）、时间制式（ISO-8601 UTC）、角度/坐标（本头无——N/A）、所有权（纯值类型无共享）、线程（逐类型声明）、错误语义（数据侧 StoreError 与调用方 fail-fast 二分、format-legacy/schema-future/store-corrupt 分类）、确定性（字段序即 canonical 序、同值必同字节——NFR-COR-02）。
- **实现文件抽查**：`Codec.hpp`（文件头 CR-02 处置背景＋错误二分＋次版本兼容语义＋全函数契约级 Doxygen）、`Codec.cpp`（1391 行逐段注释：引擎规则四条、每步解析语义、溢出/前导零/代理对/深度上限每个边界的原因、kMaxDepth=64 的量化依据、crack 时刻的业务含义〔截断/拼接现场证据〕、确定性来源——小写 hex 字节面约定）、`StoreTypes.hpp` 增量段（StoreErrorCode 21 码封闭集声明＋detail 形态约定＋码表归 diagnostics 的权威划分）、`PersistenceFormatTest.cpp`（每用例锚定注释注明 acceptance/需求追溯＋P-PR-9 不含点样本的处置说明＋EXPECT_STORE_ERROR 宏契约）。无中英混杂机翻腔、无复述式废注释、无过期注释。

### 4.8 文档同步＋findings 消账核对 —— PASS

- 单元卡增量：`units/project.md` §15.4 v0.6 行（ae9596db）——产物清单（六类型/StoreError 21 码/Codec/contentVersionOf）、§4.8/§8.11 兑现结论、三项实现口径登记（schemaVersion 编码＝主版本×10000＋次版本／token 类字段落值口径＋路径类独立口径／无版本字段类型容器判定——设计未定义判据按 DTB §5.4 登记而非私裁）、CR-02 处置、测试结论（43 用例分布 9/20/5/3/5/1＋两模式 81/81＋10/10）与验收者独立复现**逐项一致**；§15.3 新增 P-PR-9 待裁决项（§4.4.4 语法与 §6.5 示例矛盾——实施期发现、正反两面均不私裁，正向样本不含点＋拒绝用例只钉无争议违例面）；本文契约语义零变更 ✓。追踪矩阵无本任务登记义务（PRJ-T01/T02/T03 同判例；契约 outputs 的 traceability-update 由 units/project.md v0.6＋builds/wp04-t04/ 承载）。
- **findings.json 未关闭条目相关性核对**（128 项累计、open 逐族排查）：
  - **F-007/F-062/F-099/F-101**（全新集成树环境链：配置参数未载明/ini 模板未入库/缓存污染）：本验收**亲历复踩**（裸配置两处失败→补 toolchain/Qt/flags；RobWorkStudio.ini 两件辅助件自主树复制；首轮污染缓存整目录删除）——保持 open，非本任务辖域。
  - **F-008**（全新树全树构建失败）：构成本次 4.2 目标面口径的原因之一——保持 open。
  - **F-010/F-125/F-127**（.gitignore 吞留痕日志/幻影清单家族）：**本任务再现**（a148a99e 声称 14 件证据含 10 份 .log，实际入库 5 件；ird-test-report.json 六处日志引用全部库外幻影——见 4.10；F-127 处置建议③"PRJ-T03 的 10 份日志随返工窗口补交"亦未执行）→ 转登 **F-129**。
  - **F-012 系标题家族**（F-126/F-128 同单元前例）：a148a99e 标题以任务名词开头＋ae9596db [units] 标题以文件名词短语开头（一次两例）→ 转登 **F-130**。
  - **F-115/F-019/F-051/F-072**（ird_gates 基线命中与登记义务悬置）：本任务门禁命中集与 8 项基线逐项同分布、零新增——登记义务仍归所有者，保持 open。
  - **F-123**（CoreEdgePresent 断言敏感度缺口；owner 栏点名"PRJ-T04 首个 core 头消费任务…一并处理"）：本任务已把 core 头消费扩至 PersistenceFormat.hpp（Identity/Digest），但 BuildRedLineTest 的行为级 PUBLIC 传染自证未随任务增补——F-123 处置文本"随 PRJ-T04 顺带**或**后续用例增补批次"留有后窗，**保持 open**；因该缺口实质风险已被 PRJ-T03 起的真实 core 头消费＋本任务两模式链接成功消解大半，验收者不追加新条目，仅在此留痕提示所有者：处置窗口已过一次，建议随下一触及任务收口。
  - **F-124**（"冒烟模式无别名"表述失准；project CMakeLists 注释列名"随下次触及顺手更正"）：本任务触及 project/CMakeLists.txt（源文件/测试增列段）但 L115-117 别名注释块未在触及 hunks 内、未更正——**保持 open**，同样留痕提示（该注释在冒烟模式下的实际行为已由 F-124 探针证伪，非本任务引入）。
  - **F-065**（留痕日志中文双重编码乱码）：本任务 .log 未入库（F-129），入库五件（JSON/XML）编码亲查正常——本轮无新触点。
  - 其余 open 条目（policy/runtime/evidence/diagnostics/testkit 各族）与本任务无交叠。

### 4.9 commit 质量 —— PASS（附建议级 F-129/F-130）

- 三提交逐一核对：
  - ae7fb6aa `[WP-04-T04] 实现 project 格式契约与编码（PersistenceFormat/Codec）`——动词开头 ✓；四段齐备（What 按文件列要点/Why 对应契约 acceptance 与单元卡章节/How 双模式构建＋测试＋门禁＋validate-task/影响面）；"实施期 4 处缺陷由用例真实失败驱动修复"声明与 ird-test-report.json 留痕互证，且经 4.5 探针证实相关用例确有致红能力——非虚构叙事。
  - ae9596db `[units] project.md v0.6：…`——单元卡前缀规范车道（AGENTS §6.2）✓；但前缀后以"project.md v0.6"名词短语开头非动词 → **F-012 系家族再现**（F-037/F-079/F-092 同型），并入 F-130。
  - a148a99e `[WP-04-T04] PRJ-T04 验证留痕归档（…）`——前缀后以任务名词开头非动词（F-128 同字面形态再现）→ 并入 F-130；正文"14 个证据文件"主张与实际入库 5 件不符 → **F-129**（幻影清单家族）。
- 验证段结论逐项比对验收者复现：两模式 81/81＋10/10 ✓、ird_gates 8 命中零 project ✓、validate-task PASS ✓、集成 RWS_BUILD_INDUSTRIALROBOT=ON ✓——**全部为真**；声称未入库的 10 份日志所承载的结论（构建 exit 0/测试计数/门禁命中）均被验收者以独立构建树重新建立，无结论性粉饰。

### 4.10 留痕完整 —— PASS（附建议级 F-129）

- 入库核验（`git ls-tree a148a99e -- builds/wp04-t04/`）：`ird-test-report.json`＋双模式 gtest XML 四份——**ird-test-report.json 在库且与实现/测试逐一对应**（schemaVersion/taskId/targets/gates/buildModes/testSuites/acceptanceCoverage 结构完整；acceptance 1～4 逐条 evidence 映射；两模式 81/81＋10/10 计数与入库 XML `tests="81"/"10" failures="0" errors="0"` 亲查一致），其 acceptanceCoverage 对账表承担 resultNote 载体，按 ACC §4.10"或"分支判定成立（PRJ-T01/T02/T03 同判例）；`phase-one-validation.log`/resultNote 未更新——同上判例不阻断。
- 幻影清单家族再现：提交正文声称的 integration-build.log/verify-target-build.log/smoke-configure.log/smoke-build.log/四份测试运行日志/gates-ird-gates-target.log/validate-task.log 等 **10 份 .log 实际入库 0 份**（`check-ignore -v` 实证命中根 `.gitignore:87 *.log`）；ird-test-report.json 的 gates 字段（validate-task.log/gates-ird-gates-target.log）与 buildModes 字段（四份构建日志）共六处库外幻影引用 → **F-129**（F-081/F-102/F-103/F-106/F-108/F-111/F-113/F-114/F-125/F-127 家族第 11 例）。按家族判例（证据本体不缺＋数值主张全部复现为真）登记建议级、不阻断；构建/测试可复现性已由验收者亲手建立（4.2/4.3）。

### 4.11 偷懒/缩水实现扫描 —— PASS

1. **标记扫描**：project 单元全目录 `TODO|FIXME|stub|placeholder|未实现|not implemented` 扫描**零命中**（grep 实测 exit 1）；无契约标注"触发式延后"项需要豁免（升级器本体归阶段 B §8.11 为契约明文归位，非本任务缩水）。
2. **空实现检测**：公共接口无空体/无仅返回默认值者——PersistenceFormat.hpp 全部为带完整字段契约的值类型＋operator== 逐字段比较；Codec 六 dump/六 parse 均为实质实现（1391 行自含 JSON 引擎：递归下降解析器/严格 canonical 词法/UTF-8 严格解码/转义对称/逐类型字段序映射，逐段审读）；toToken/draftOriginFromToken/contentVersionOf 均为真实实现。
3. **逐条证据表**：契约 acceptance 1～4 逐条证据见 4.4 表——每条均有具名测试＋验收者两模式实测输出＋（第 4 条）文件行号级源码核对，**无任何一条缺证据**。
4. **测试强度**：断言校验行为与边界而非恒真——黄金字节精确文本（非"语义等价即可"）、同值两次 dump 逐字节比较、可选字段缺省键不出现的负断言、uint64 上界/2^31/2^32/负值/前导零边界、同文档 1.0 拒绝 vs 1.1 容忍的对照反例、digest 字段"非真实摘要"透传反证、FIPS 180-2 标准向量、错误断言只认稳定 code 不锁 detail 文本（升级指引用例除外——三键逐键断言）；与 4.5 联动：两组实现级探针注入即红（黄金字节断言与 SchemaFuture 拒绝断言均实证存活）。

---

## 2. 裁决

**verdict = pass**（4.1～4.11 全部通过，零阻断级问题；不阻塞合入）

建议级问题 2 项，已按 ACC §5 强制转登 `traceability/findings.json`（编号顺延自 F-128）：

| 编号 | 摘要 | 严重级 |
| --- | --- | --- |
| F-129 | 幻影清单家族再现（F-081 系第 11 例，F-127 后同单元再例）：a148a99e 正文声称入库 14 件证据（含 10 份 .log），实际入库 5 件（ird-test-report.json＋gtest XML×4）；ird-test-report.json 的 gates/buildModes 字段六处日志引用全部库外幻影（.gitignore:87 `*.log` 吞没——check-ignore 实证）。F-127 处置建议③未执行。数值主张经验收者 detached worktree 从零复现全部为真，按家族判例不阻断 | suggestion |
| F-130 | 提交标题家族再现（F-012 系，一次两例）：a148a99e 标题"[WP-04-T04] PRJ-T04 验证留痕归档…"以任务名词开头（F-128 同字面形态）；ae9596db 标题"[units] project.md v0.6：…"以文件名词短语开头（F-037/F-079/F-092 同型）。纯风格瑕疵：两提交正文四段齐备、Acc-Covered 对账在位、结论与留痕一致 | suggestion |

## 3. 验收者环境说明

- 复现现场：`git worktree add --detach D:/10_Source_Repos/21_robot/_acc_prjt04/wt-a a148a99e…`（detached @SHA）；冒烟构建树＝`_acc_prjt04/build-smoke-acc`（独立目录、vcpkg toolchain 绝对路径，Debug/Release 两口径构建）；记录回传：`git worktree add -b acc/PRJ-T04/1 D:/10_Source_Repos/21_robot/_acc_prjt04/wt-b origin/redesign-main`。
- 集成配置参数（与主树缓存一致；F-007/F-062/F-098/F-099 口径）：`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=D:/10_Source_Repos/21_robot/RobWork/vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64 -DCMAKE_CXX_FLAGS="-DNOMINMAX -DWIN32_LEAN_AND_MEAN -DWIN32" -DRWS_USE_STATIC_LINK_PLUGINS=ON`；`RobWorkStudio.ini.template.static/.shared.in` 自主树复制入 worktree（git 未跟踪本地资源）——裸配置两处失败（RobWorkSetup.cmake:96 find_package／RobWorkStudio/src/CMakeLists.txt:58 configure_file）后按登记口径补齐，亲历证实 F-007 系描述仍然成立；首轮失败后污染缓存按 F-101 口径整目录删除重配。
- 探针方法：实现级注入须重编译——Edit 修改源文件（新 mtime）→ 增量构建 → 滤跑目标用例 → `git checkout --` 还原 → 确认工作树干净 → 全量重跑复绿（规避 F-068/F-075 mtime 陷阱）。
- 本记录证据日志（configure/build/test-run/gates/探针原始输出）留存于验收者临时证据目录（随 worktree 清理）；持久证据以本记录内联关键输出＋送验提交自带 5 件留痕（已逐一核对实际入库与内容真实性）为准。
- 独立性声明（ACC §2）：全新上下文；仅消费契约/设计文档/送验产物与留痕（含既往验收记录作为判例口径引用），未消费实施者叙述；构建与测试全部亲手重跑；对抗式立场下逐项取证（实测门禁、双探针改红、幻影清单比对、红线机械扫描、入库 XML 计数亲查）。
- 实施产物零改动声明：验收全程未向 `wp04-t04` 推送任何提交、未修改送验对象内容（4.5 探针仅落在一次性 detached worktree 内并已还原，还原后 `git status --porcelain` 工业机器人目录计数＝0）；本分支仅承载本记录与 findings.json 两条建议级登记。

## 4. 记录三元组（回传给编排者）

```
branch: acc/PRJ-T04/1
path:   RobWork/doc/industrial-robot-design/traceability/acceptance/PRJ-T04-20260916.md
commit: （见本分支推送后记录提交的 40 位 SHA——由验收会话随最终报告输出）
```
