# WP-13-T05 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | WP-13-T05 实现 URDF 导入字段映射与链型判定（unit=modeling，interUnit=true：io 对端消费） |
| 送验对象（冻结） | 分支 `wp13-t05` 尖端 `5e5f04f90df8340328722e4923bd5d63b8ef6dde`（验收全程按该 SHA） |
| 分支漂移检查 | 验收启动时 `git fetch origin` 后 `origin/wp13-t05` = `5e5f04f9…` ＝ 送验 SHA，**零漂移**（ACC §3） |
| diff 范围 | `dd97c920409f0e9d56be48ae789d633977adf993..5e5f04f90df8340328722e4923bd5d63b8ef6dde`（3 个提交：827e0e55 解析映射＋eb417a1e 链型判定＋5e5f04f 双模式留痕与文档同步；base＝`origin/redesign-main` 尖端，即 wp13-t04 合入收尾提交；已验证 `dd97c920` 为送验 SHA 祖先，且 industrialrobot 源树与 T04 acceptedHead `9e24b410` 逐字节一致——`git diff 9e24b410 dd97c920 -- …/industrialrobot` 为空） |
| **verdict** | **pass**（4.1～4.11 全部通过，无阻断级问题；建议级 5 项转登 findings F-303/F-304/F-305/F-306/F-307） |
| 验收执行方式 | 验收者全新上下文独立复现：detached worktree @ 送验 SHA 亲手重配重跑全部构建/测试/脚本/门禁与两处变异注入；未采信实施者任何运行结果（ACC §2 四条） |
| 复现现场 | `D:/10_Source_Repos/21_robot/acc_wp13t05_a`（detached @ 5e5f04f，集成模式＋冒烟模式 build-smoke＋base 对照）；`D:/10_Source_Repos/21_robot/acc_wp13t05_base`（detached @ dd97c920，门禁 base 对照专建） |
| evidence 分支 | `acc/WP-13-T05/1`（基于 `origin/redesign-main` = dd97c920；记录 commit＝本分支尖端 40 位 SHA，由编排者登记三元组） |

---

## 4.1 改动范围 —— 通过

`git diff --name-status dd97c920..5e5f04f` 共 32 文件，逐一对契约 `allowedFiles` 核对：

- `RobWork/RobWorkStudio/src/rwslibs/industrialrobot/modeling/**`：7 文件（CMakeLists.txt、include/…/DiagCodes.hpp 改；include/…/Import.hpp 新增；src/DiagCodes.cpp 改；src/Import.cpp 新增；test/DiagCodesTest.cpp 改；test/ImportTest.cpp 新增）——全在 modeling/** 内。
- `RobWork/doc/industrial-robot-design/units/modeling.md`（改，升 v0.6）——在 allowedFiles。
- `RobWork/doc/industrial-robot-design/traceability/**`：24 文件（builds/wp13-t05/ 留痕 22＋findings.json F-302 自登记）——在 allowedFiles `traceability/**` 内。
- **forbiddenFiles 零触碰**：REQUIREMENTS.md、ARCHITECTURE.md 不在 diff。
- **SA-02**：diff 无 modeling 之外任何框架源码文件；`patches/` 无新文件（无未登记框架改动）；vcpkg 环境安装（pugixml 1.16）属环境准备，allowedFiles 不含 vcpkg、diff 亦零 vcpkg 文件——与契约 note ③ 口径一致。

## 4.2 可复现构建 —— 通过（两模式均验收者亲手从零重配重跑）

- **集成模式**（acc_wp13t05_a，源＝detached worktree @ 5e5f04f）：
  - 配置：`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON` ＋ 主树 vcpkg toolchain ＋ Qt 前缀（与主树缓存同参）。结果 `Configuring done`；缓存确认 **`RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`**。
  - 环境前置（非本任务产物缺陷，F-007/F-009 已登记同族）：①worktree 内 `vcpkg/` 被 .gitignore 吞掉——toolchain 指向主树 vcpkg 并复用其 `installed/`；②`RobWorkStudio/bin/RobWorkStudio.ini.template.static` 被 `[Bb]in/` 规则吞掉（首次配置即报 `CMake Error: File … does not exist`）——从主树复制后配置通过。
  - 任务相关目标构建：`sdurws_ird_modeling`、`sdurws_ird_modeling_test`、`sdurws_ird_modeling_contract_test` 三目标退出码均 **0**，MSVC `error C/LNK` 计数 **0**（日志：复现现场 `logs/build-modeling.log`、`logs/build-modeling-test.log`、`logs/build-contract-test.log`——临时目录，结论以此记录为准）。
- **独立冒烟模式**（acc_wp13t05_a/build-smoke，`-DCMAKE_TOOLCHAIN_FILE=<主树 vcpkg>/scripts/buildsystems/vcpkg.cmake`＋Qt `CMAKE_PREFIX_PATH` 前缀——AGENTS §4.1 与送验留痕 smoke-configure.log 同口径）：
  - 配置 done；全量构建退出码 **0**、错误计数 **0**。
  - 冒烟测试（验收者亲跑两 exe）：`sdurws_ird_modeling_test` → **91/91 PASSED**；`sdurws_ird_modeling_contract_test` → **5/5 PASSED**。

## 4.3 契约 verify 命令 —— 逐条亲手执行（6/6 成功）

| # | 命令 | 结果 |
| --- | --- | --- |
| 1 | `cmake --build build --config Release --target sdurws_ird_modeling` | 退出码 0，零错误 |
| 2 | `cmake --build build --config Release --target sdurws_ird_modeling_test` | 退出码 0，零错误 |
| 3 | `ctest --test-dir build/RobWorkStudio/src/rwslibs/industrialrobot/modeling -C Release -L "^ird$"` | **2/2 passed**（验收者先补跑了第 2 条未含的 `--target sdurws_ird_modeling_contract_test`（退出码 0）——契约 verify 清单不含该构建目标行，全新树按清单字面执行时 ctest 首跑 contract_test 将报 Not Run；缺口与 F-300（T04 实例）同型，转登 **F-304**。非实现缺陷——实施留痕表明其完整构建三目标） |
| 4 | `cmake --build build --config Release --target ird_gates` | 门禁引擎运行：**引擎自测全过**（干净夹具 pass＋7 类植入违例按预期逐类检出），随后真实扫描按 SA-10 以退出码 1 终止——命中集为**存量基线红**（F-019/F-292 等在案基线）。验收者专建 base 对照（detached worktree @ dd97c920 直跑 `cmake -P` 同脚本）：base 51 类唯一命中 vs branch 50 类唯一命中，语义键（码＋目标名＋第三方库名，ASCII 免疫编码噪声——branch 日志经 MSBuild 管道转码，字面多重集比对不可行）比对结论：**modeling 面 delta 恰 +1＝`IRD-GATE-LIB: sdurws_ird_modeling 链接 pugixml::pugixml`**（即送验留痕 `gates-hit-set-diff.txt` 自登记的 F-302，O-40 已登记依赖的词表暴露，处置归治理侧）；modeling 面其余命中＝存量两条测试目标→testkit SUB（F-292）。与送验留痕结论一致 |
| 5 | `pwsh -File RobWork/scripts/industrialrobot/validate-task.ps1 -TaskFile …/WP-13-T05.json` | `validate-task: PASS (1 tasks)`，退出码 0 |
| 6 | `pwsh -File RobWork/scripts/industrialrobot/validate-docs.ps1` | `validate-docs: PASS (20 units, 12 trace entries, 186 task files)`，退出码 0 |

## 4.4 acceptance 条目逐条证据 —— 5/5 有对应证据（行号以送验 SHA 内容为准，验收者亲读亲跑）

| acceptance 条目 | 证据 |
| --- | --- |
| 1. Import.hpp/.cpp 落位（卡 §3.3 T05 行）；IModelImportMapper 按 §9.4.3 签名（mapUrdf/mapXacroExpanded/mapWorkCellXml——后者 R1 返回 NotImplemented、不留桩）；输入全部为 io 四方法产物、modeling 不自行读文件（SA-14）；按 DTB 规模列拆两提交 | `Import.hpp` 落位于 `include/sdurws/ird/modeling/`（§3.3 表 T05 行）；三方法签名 Import.hpp:620/639/659 与卡 §9.4.3 一致；`mapWorkCellXml`（Import.cpp:2275-2300）＝NotImplemented 稳定错误＋`workcell-channel-r2` 引导条目＋无草稿——非桩实现（无 TODO/占位，R2 通道归 WP-13-T17）；`ValidatedSource`（Import.hpp:103-107）成员全部为 io 公共值类型（ResourceSnapshot/ResourceDependencyTree），装配归调用方（P-MDL-8）；Import.cpp include 面无任何文件 I/O 头（fstream/cstdio/filesystem 零命中——SA-14 实证）。DTB 规模列两提交：827e0e55（解析映射）＋eb417a1e（链型判定）。具名测试：`MapWorkCellXml_R1BoundaryNoStub_WP13T05_ACC1`（无草稿＋NotImplemented＋引导条目）、`Mapper_ReentrantStateless_WP13T05_ACC1`（同实例/跨实例逐字段相等）、`MapXacroExpanded_ProvenanceRecorded_WP13T05_ACC1`（Recorded 留痕＋违约→SourceInconsistent）。 |
| 2. §6.3 字段映射表逐行 UT（inline fixture）四清单逐项可观察（MDL-03、AT-15）；默认补全必入清单不静默（NFR-COR-03——缺 inertial→NotProvided、无安装语义→basePlacement=ground 且 ImportMapped 入清单）；package:// →定位诊断引导改相对路径；文件缺失→IO-RES-MISSING＋Recorded 草稿可携带（§6.7） | `FullUrdf_FieldMappingRowsObservable_WP13T05_ACC2`（inline kFullUrdf 两关节链 fixture）：链结构 2 关节 3 连杆、j1 轴 (0,2,0)→归一化 (0,1,0)（非 Z 轴）、origin z=0.2、bounds ±3.14、j2 continuous NotApplicable、l1 mass/com/inertia Provided＋ImportMapped、材质 density NotProvided、basePlacement=Ground 且 defaults 清单 reason 含 "import-mapped"、gazebo/robot 级材质入 ignored、transmission/velocity/scale(2 1 1)/ros-uri 入 unsupported（ros-uri 断言 guidance 含"相对路径"＋`span.line>0` 定位可观察）、effort→Peak 候选、自碰撞候选且草稿连杆 selfCollisionHints 全空（P-MDL-3）、缺失 missing.dae→IO-RES-MISSING 诊断＋resources 行 state="missing"＋草稿 resourceManifest 以 Recorded＋externalRecord 携带（V-08 submittable 保持 true）。`MissingInertial_NotProvidedInDefaults_WP13T05_ACC2`（body 三字段 NotProvided＋defaults 登记）；`PhysicalIllegality_ReportErrorItems_WP13T05_ACC2`（m=0→mass-nonpositive、diag(10,1,1)→inertia-triangle、submittable=false）；`NameConflictAndIllegalValue_Preserved_WP13T05_ACC2`（净化冲突→NameConflict＋草稿仍产出＋mass "abc" Invalid 态原串保留）；`InertialRpy_RotatesInertiaToLinkFrame_WP13T05_ACC2`（Rz(90°)·diag(1,4,1)·Rᵀ→ixx'=4/iyy'=1，1e-12）。注：package:// 的"定位诊断"承载形态＝报告条目（span＋guidance）而非注册诊断码（§9.5 无对应行、实现未增登）——与 TEMPLATE-RANGE 的实现期增登处置不一致，语义解释未按 DTB §5.4 登记，转登 **F-306**（建议级，不阻断：定位与引导两要素实测可观察）。 |
| 3. 轴语义 UT（MDL-11）：任意有限非零轴可接受；缺 axis→局部 +X 入待确认清单；零轴/非有限轴→仅报告＋关节标记 Invalid＋含该类关节草稿不得提交（prepare 断言兜底口径） | `MissingAxis_DefaultPlusXPendingConfirm_WP13T05_ACC3`（+X Provided＋pendingConfirms "axis-default-plus-x"＋PENDING-CONFIRM 诊断＋submittable=true）；`ZeroAxis_ReportOnlyInvalidNotSubmittable_WP13T05_ACC3`（axis Invalid 态原串 "0 0 0" 保留＋jointStatuses "invalid"（reason 含轴原文）＋kMdlImportZeroAxis 诊断＋ZeroAxisReported＋submittable=false——实现 Import.cpp:1533 `normSq>0.0` 判零）；`NonFiniteAxis_ReportOnly_WP13T05_ACC3`（NaN 分量同口径）。实现侧轴映射仅作用于可动关节（Import.cpp:1526 `isMovableType` 分支）；"缺 axis 默认 +X"三重登记（默认补全清单＋待确认条目＋Warning 诊断）齐备。 |
| 4. 链型判定两维度 UT（MDL-12、AT-17）：维度一分支报告＋显式选链——未选链不排除可动分支（辅助分支转候选/忽略、不构成拒绝）；维度二 mimic/planar/floating→阻断＋UNSUPPORTED-JOINT、不转 FixedFrame、不得经选择绕过（V-10/M-6）；4/5 轴与含 prismatic→正式计算/报告阻断＋诊断，不静默降级（V12-01） | 维度一：`MultiBranch_NoSelectionRejected_WP13T05_ACC4`（无草稿＋MultiBranchNeedsSelection＋params branch-roots="la,lb"/branch-count/split-link＋report.branches 两条 pending-selection＋BRANCH-SELECTION info 诊断）；`MultiBranch_WithSelection_AuxCandidatesNotRejected_WP13T05_ACC4`（选 prismatic 分支 lb 放行＋类型保留＋lb=selected/la=aux-candidate＋la 不进草稿只入 ignored＋能力按所选链判定）；`DeepSplit_SelectionResidualBranches_WP13T05_ACC4`（选中分支内再分裂→新一轮候选 "lc,ld"，不静默任选）。维度二：`MimicJoint_TypePreservedBlocked_WP13T05_ACC4`（类型保留 Revolute＋UNSUPPORTED-JOINT＋MimicBlocked＋不可提交）；`PlanarJoint_UnsupportedNoDraft_WP13T05_ACC4`（无草稿＋UnsupportedJointType＋params joint-name/source-type）；4/5 轴与 prismatic：`FiveAxes_BeyondRange_DraftEditable_WP13T05_ACC4`（BeyondTemplateRange＋TEMPLATE-RANGE 诊断＋草稿可提交＋全 Revolute 类型保留）、`PrismaticInChain_BeyondRange_TypePreserved_WP13T05_ACC4`（6 关节五转一移→Beyond＋Prismatic 原样＋限位单位 m）、`SixRevoluteChain_FullTemplateRange_WP13T05_ACC4`/`SevenRevoluteChain_FullTemplateRange_WP13T05_ACC4`（FullTemplateRange 且无 TEMPLATE-RANGE 诊断）、`Continuous_WorkingRangePendingConfirm_WP13T05_ACC4`（bounds NotApplicable＋working-range-unconfirmed 待确认＋"确认值不回写权威 bounds"）。诊断码面：DiagCodes.hpp 五常量（唯一书写点）＋DiagCodes.cpp 五描述符（清单序＝§9.5 表行序；TEMPLATE-RANGE 为实现期增登行，卡 §14.6 v0.6 登记）。 |
| 5. pugixml 依赖引入（O-40，经 vcpkg 经典模式、modeling PRIVATE、版本登记）与确定性 UT（同字节同 options→同输出字节、诊断按源行序稳定排序 NFR-COR-02）；SelfCollisionHints 仅报告候选不写策略对象（P-MDL-3） | CMakeLists.txt:47 `find_package(pugixml CONFIG REQUIRED)`＋:133 单独 PRIVATE 链接语句（不入六边 PUBLIC 语句，公共头零 pugixml include——消费者零感知）；版本 1.16/x64-windows 登记于单元卡 §14.6 v0.6 与提交 827e0e55 正文；DOM 有界性归 io BudgetGuard 前置（Import.cpp:44-47 注释）。`Determinism_SameBytesSameOptionsSameOutput_WP13T05_ACC5`（ImportOutcome 逐字段相等＋Codec canonical 编码逐字节一致）；`Diags_SortedBySourceLineOrder_WP13T05_ACC5`（产出序与行序相反的 fixture→排序后按行序：第 2 行能力结论→第 5 行 IO-RES-MISSING→第 9 行缺 axis，断言 context 携 file:line:col）——实现为 DiagEntry（relFile,line,column,seq）稳定排序出口（Import.cpp:441-454，成功/失败出口共用 flushDiags）。数值路径 locale 无关（from_chars/to_chars，Import.cpp:114-141）。P-MDL-3：`FullUrdf…` 用例断言草稿连杆 selfCollisionHints 全空＋报告候选清单承载（Import.cpp:929-935 仅入 report.selfCollisionCandidates）。 |

未执行项标注：无——全部 acceptance 面均执行；`ird_gates` 按 4.3 如实记录为"存量基线红＋建模面恰 +1 已登记命中（F-302）"，不标注"零命中"。

## 4.5 测试真实失败能力 —— 通过（两处变异实证）

验收者在复现现场对 `src/Import.cpp` 注入两处变异后重建重跑 `sdurws_ird_modeling_test`：

1. **MUTATION-1**（Import.cpp:1533）：`if (!finite || !(normSq > 0.0))` → `if (!finite)`（零轴放行）→ **`ZeroAxis_ReportOnlyInvalidNotSubmittable_WP13T05_ACC3` 按预期变红**。
2. **MUTATION-2**（Import.cpp:2294）：mapWorkCellXml 的 `ImportErrorCode::NotImplemented` → `SourceInconsistent` → **`MapWorkCellXml_R1BoundaryNoStub_WP13T05_ACC1` 按预期变红**。

变异运行结果：89 passed / **2 failed**（恰为上述两用例）；`git checkout --` 复原后重建重跑 → **91/91 PASSED**。结论：断言校验行为而非编译通过，测试非恒真。

## 4.6 架构红线 —— 逐条自查通过

1. **L2 零 Qt**：Import.hpp/Import.cpp/DiagCodes.* 全文零 Qt include（含 `<Q…>` 扫描零命中）；CMakeLists 配置期守卫（:333 Qt 链接 FATAL）＋BuildRedLineTest 运行期扫描双防线，91 用例全过。
2. **R-1 业务域互链**：产品目标链接面仅六条登记边（core/diagnostics/project/runtime/policy/io，CMakeLists:121-127）＋pugixml PRIVATE；配置期守卫（:317-342）＋BuildGraphContractTest（5/5 过：六边封闭/无业务域互链/testkit 形态不受 pugixml PRIVATE 边影响）。
3. **R-2 跨单元私有头**：include 面仅他单元公共头（core/Provenance、io/IoDiagnostics、core/DiagData、core/Digest、io/ResourceIo）＋本单元私有头 `InertiaMath.hpp`（src/ 内相对包含，不入 include/——合规）。
4. **SA-02**：见 4.1（零框架文件、patches/ 无新文件）。
5. **R-4 名称前缀**：`sanitizeLocalName` 为 localName 字符集预处理，注释明文"最终消歧仍归 runtime（R-4）"（Import.cpp:257-279）；无 RobWork 前缀拼接/剥离。
6. **PA-1/PA-2 权威与不可变历史**：导入只产草稿（ImportOutcome.draft）＋报告，全程不产生修订、不落盘（头注与实现一致——真实 ObjectId 归命令 prepare 分配，临时句柄为确定性散列且注释声明"不入库"）；无任何写路径越权。
7. **SA-14 不自行读文件**：Import.cpp include 面零文件 I/O；输入全部为 ValidatedSource（io 产物组合）。
8. **old/ 历史实现**：全部实现按卡面从零编写，无 old/ 恢复痕迹。

## 4.7 注释规范 —— 通过（公共头全查＋实现文件抽查 3 个）

- **Import.hpp（公共头，全查）**：文件头 Doxygen（设计依据逐条追溯卡面/需求/任务契约＋P-MDL-8 落位说明）；每个 struct/enum/class 均有业务概念、生命周期/所有权、线程安全说明；每个函数 @brief/@param [in]/[out]/@return/异常语义完整；§2.5 高危信息齐备——物理量带单位（m/rad/kg/N·m）、坐标系说明（连杆系/T_parent_joint/core §4.6）、角度制式显式 rad、确定性来源声明（from_chars locale 无关、（文件，行，列，产出序）排序）、错误语义归类（值面 vs 调用方契约违约 fail-fast）。
- **Import.cpp（抽查①，全文 2303 行通读）**：文件头阅读地图＋匿名命名空间每个助手函数 Doxygen；mapUrdf 十步每步段前注释给出业务依据与卡面出处；魔法数字来源（FNV-1a 偏移基/素数、1e-12、枚举序对齐注释）。
- **DiagCodes.cpp（抽查②）**：逐描述符逐字段登记口径注释（分类/级别/paramSchema 键语义/confirmable/retryable 依据——§9.5 行原文对照）。
- **modeling/CMakeLists.txt（抽查③）**：目标注册、pugixml PRIVATE 边、红线守卫、ird_add_gtest 宏全部中文注释块说明。
- 测试文件（ImportTest.cpp）：文件头 ACC1~ACC5 映射＋每用例注释注明验证的 acceptance/需求/AT 编号＋IRD_TEST_INFO 追溯登记——符合 §2.7 测试代码口径。
- 小疵（不阻断）：Import.cpp 文件头"阅读地图"列步骤⑦"主链外元素登记"，正文对应块标签为"第八步前注"（第七步标签缺失）——注释与代码结构轻微漂移，转登 **F-307**。

## 4.8 文档同步 —— 通过

- 单元卡 modeling.md 升 **v0.6**（diff 实证）：版本行 v0.5→v0.6；§9.5 表增登 `MDL-IMPORT-TEMPLATE-RANGE` 行（导入/info，confirmable=false，任务列 T05，注明"实现期增登——§6.4 诊断语义的码面落位，原表缺行"）；§14.6 追加 v0.6 变更记录（Import.hpp/.cpp 落位、§6.3 逐行落位的十项偏差澄清〔velocity 无落点/图元/scale/多 visual/robot 级材质/惯量旋系/prismatic 缺限位待确认同名扩展/名称净化判据/1~3 轴归范围外/临时句柄二进制承载/缺失网格 Recorded〕、pugixml 1.16 版本登记、验证数据）——DTB §5.4 偏差登记口径执行到位。
- 任务状态/追踪矩阵：契约状态翻转与 pipeline state.json 簿记属流水线收尾动作（提交正文明示"state.json 未纳入本提交——工作区簿记属流水线"，与 T03/T04 先例一致）；trace-matrix.json 为治理侧生成器维护（`[governance]` 提交惯例，T04 同）——本任务无漏登记。
- **findings.json 未关闭条目相关性核对（245 条 open）**：与本任务相关者 6 条——**F-302**（本任务实施段自登记：pugixml 门禁词表暴露，open/治理侧，与本验收门禁复现一致）；**F-292**（modeling 两测试目标→testkit SUB 存量命中，open/引擎级治理侧，本任务不可消账、复现确认仍在）；F-293/F-298/F-300（治理侧家族/清单缺口，本任务不消账）；F-256（留痕计数家族，与本任务无涉）。无历史发现静默积压：本任务新增命中已自登记，存量命中维持 open 并在卡 v0.6 ⑤如实复述。
- 遗留观察（建议级）：§6.3 mesh 行"定位诊断"（package://）在实现中承载为报告条目而非注册诊断码、该解释未按 DTB §5.4 登记（同任务内 TEMPLATE-RANGE 原表缺行选择了增登、ros-uri 未增登——同任务两种处置）→ 转登 **F-306**。

## 4.9 commit 质量 —— 通过（1 项建议级）

三个提交（827e0e55 / eb417a1e / 5e5f04f）正文四段（What/Why/验证/影响面）齐备且逐条可对照 diff；验证结论与送验留痕一致、中间态如实标注（前两提交明写"ird_gates 与冒烟验证随文档同步提交后统一执行留痕（本提交未执行）"——无粉饰）；代码与测试同提交。**唯一违例**：第三提交 5e5f04f 标题「[WP-13-T05] 双模式验证留痕＋文档同步：…」前缀后为名词短语开头，非动词开头（ACC §4.9/AGENTS §6.3 字面）——F-012/F-293/F-298 家族同型再现，转登 **F-303**（建议级；不追溯改写已推送历史）。

## 4.10 留痕完整 —— 通过

- 送验提交内 `traceability/builds/wp13-t05/` 共 22 份留痕（验收者逐一抽查存在且内容与结论一致）：集成 integration-build.log（三目标零错误）/integration-ctest.log（2/2）/两测试目标 gtest XML＋ird-test-report.json×2 模式＋运行日志（91/91、5/5）；冒烟 smoke-configure.log（toolchain＋Qt 前缀）/smoke-build.log（零错误）/同套测试留痕；门禁 gates-ird-gates-branch.log＋gates-direct-{base,branch}.log＋gates-hit-set-diff.txt；治理 validate-task.log（PASS）/validate-docs.log（PASS）。验收者独立复现结果与上述全部吻合（见 4.2/4.3）。
- 载体口径：`phase-one-validation.log` 自 CORE-T01 后即非留痕载体，WP-13-T03/T04 起以 `traceability/builds/wp13-t0N/` 目录为验证留痕位（T04 验收记录同口径）——本任务遵循该先例，resultNote 面由契约状态收尾承接。

## 4.11 偷懒/缩水实现扫描 —— 通过

1. **字面扫描**：diff 范围 `TODO|FIXME|stub|placeholder|未实现|not implemented` 零实质命中——唯一命中为测试名 `MapWorkCellXml_R1BoundaryNoStub_WP13T05_ACC1`（断言"不留桩"的契约验证用例，豁免依据＝契约 acceptance 1 明文 R1 边界"不留桩实现"、R2 通道归 WP-13-T17）。
2. **空实现检测**：公共接口三方法均有完整实现（mapWorkCellXml 的 NotImplemented 为卡面规定行为而非桩——返回稳定错误＋通道引导＋无草稿，且经 4.5 MUTATION-2 实证测试钉住该行为）；无"仅返回默认值"的接口函数。
3. **逐条证据表**：见 4.4——acceptance 5 条全部具名测试/命令输出/文件行号对应，任一条不缺。
4. **测试强度**：23 条 MdlImport 用例断言行为与边界（值逐项对照/状态机/行序/字节级确定性/变异可红），非恒真、非"只编译通过"（4.5 实证）。

---

## 发现的问题清单

**阻断级：无。**

**建议级（5 项，已逐条转登 traceability/findings.json，编号自 F-302 顺延）：**

| # | 编号 | 摘要 |
| --- | --- | --- |
| 1 | F-303 | 5e5f04f 提交标题名词短语开头（F-012/F-293/F-298 家族同型）——正文四段齐备、无粉饰 |
| 2 | F-304 | 契约 verify 数组缺 `sdurws_ird_modeling_contract_test` 构建目标行（F-300 同型 T05 实例）——验收者实测补建后 ctest 方 2/2 |
| 3 | F-305 | Import.cpp 结构违例（悬空引用/多父/多根/环）的错误项复用 kind "name-conflict"，与 Import.hpp 词表定义（＝净化后 localName 重复）语义混淆——可观察但词表需澄清或扩充 |
| 4 | F-306 | §6.3"定位诊断"（package:// 行）实现承载为报告条目（span＋guidance）而非注册诊断码；同任务 TEMPLATE-RANGE 选择增登、ros-uri 未增登，口径不一致且未按 DTB §5.4 登记该解释 |
| 5 | F-307 | Import.cpp 文件头阅读地图步骤编号与正文段落标签漂移（⑦ 缺失，被"第八步前注"替代）——注释随代码更新纪律小项 |

## 验收者环境说明

- Windows 10 x64（26200）、VS2022 Community 17.12（MSVC 19.42）、CMake VS 生成器、pwsh 7；vcpkg 经典模式复用主树 `installed/x64-windows`（gtest 1.18/pugixml 1.16）；Qt 6.11.1 msvc2022_64。
- 复现配置参数与主树缓存一致（`CMAKE_PREFIX_PATH`/toolchain 绝对路径指向主树 vcpkg——F-007/F-009 登记的全新检出环境缺口，验收侧以环境准备补齐，非送验产物缺陷）。
- 门禁命中集比对因 branch 侧日志经 MSBuild 管道转码（UTF-8→GBK 混合）不可作字面多重集 diff，改用编码免疫的语义键（码＋目标名＋第三方库名 ASCII token）比对——结论不受影响：建模面 delta 恰 +1（pugixml，F-302）。
