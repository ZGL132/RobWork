# UI-T11 验收记录（attempt 2，同日 -r2 序号）

| 字段 | 值 |
| --- | --- |
| 任务 ID | UI-T11（UiSessionController＋关闭/切换对话框机制） |
| 送验对象（冻结 SHA） | `45baa315ab763cac0395768fd091c12e20fdfb6a`（分支 wp10-t11 尖端；验收按 SHA 不按分支名） |
| diff 范围 | `63e3007112c28d2b0dff48119fac3fa3a6232984..45baa315ab763cac0395768fd091c12e20fdfb6a`（state.base..head） |
| 裁决 | **pass**（4.1~4.11 全部通过；零阻断级；建议级 F-279/F-280 已随本记录转登 findings.json，F-277 消账注记） |
| 验收日期 | 2026-09-21 |

> attempt 1 未产生 pass/fail 记录（编排者侧流程性重派）；本记录为首次实质验收产出，按同日序号规则落 `-r2`。

---

## 一、逐项证据（ACC §4，4.1~4.11）

### 4.1 改动范围 —— 通过

- `git diff --name-status 63e3007..45baa315`：24 个变更文件（M4/A20），逐一对号——
  - `RobWork/RobWorkStudio/src/rwslibs/industrialrobot/ui/**`：CMakeLists.txt、contract_test/SessionContractTest.cpp、include/sdurws/ird/ui/{UiPorts,UiProjections,UiSessionController,UiTypes}.hpp、src/UiSessionController.cpp、test/SessionControllerModelTest.cpp（8 件）；
  - `units/ui.md`（1 件）；
  - `traceability/builds/wp10-t11/**`（15 件）——全部落在契约 allowedFiles 三个通配内。
- forbiddenFiles（REQUIREMENTS.md/ARCHITECTURE.md）零触碰（diff 文件清单实证）。
- SA-02：diff 无任何框架源码文件、无 `patches/` 新增——零登记需求。

### 4.2 可复现构建 —— 通过（双模式从零亲手复现）

- **集成模式**：worktree A（`D:/10_Source_Repos/21_robot/acc-ui-t11-wtA`，detached @ 45baa315）。全新配置 `cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=<主仓>/vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`——CONFIGURE_EXIT=0（Configuring done，零 CMake Error）；缓存 grep 确认 **`RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`**。worktree 缺主树未跟踪本地辅助件 `RobWorkStudio.ini.shared.in`/`RobWorkStudio.ini.template.static`（F-007/F-062/F-099 在册缺陷），自主树补拷至 worktree 内 `RobWork/RobWorkStudio/bin/`（环境修复，不触及送验产物——DIAG-T05~T10/EV-T11 验收同先例）。
  - `cmake --build build --config Release --target sdurws_ird_ui_test` → **UI_TEST_BUILD_EXIT=0**；`--target sdurws_ird_ui_contract_test` → **CONTRACT_BUILD_EXIT=0**；两日志 `error C/fatal error/error LNK` grep 计数 **0**；两 exe 落位 `build/RobWorkStudio/bin/Release/`。
- **独立冒烟模式**（`D:/10_Source_Repos/21_robot/acc-ui-t11-smoke`，源取自 worktree A 同 SHA，带 vcpkg toolchain＋Qt 前缀——F-002 口径）：配置 SMOKE_CONFIGURE_EXIT=0、全量构建 **SMOKE_BUILD_EXIT=0**（`error C/fatal/error LNK` 计数 0）；两 exe 亲跑 **106/106＋9/9 PASSED**。
- 告警核查：集成/冒烟构建日志的 `warning C` 计数 54/25 条全部归属既有文件（框架库与 project 等单元测试 TU），新增 3 TU（UiSessionController.cpp/SessionControllerModelTest.cpp/SessionContractTest.cpp）双模式 **零告警**——commit"零告警零错误"声明属实。

### 4.3 契约 verify 命令 —— 通过（逐条独立执行）

1. `cmake --build build --config Release --target sdurws_ird_ui_test`：exit 0（集成树亲跑）。
2. `cmake --build build --config Release --target sdurws_ird_ui_contract_test`：exit 0（集成树亲跑）。
- 测试 exe 亲跑：集成树 `sdurws_ird_ui_test.exe` → **106/106 PASSED**（exit 0，约 52 ms）；`sdurws_ird_ui_contract_test.exe` → **9/9 PASSED**（exit 0）；冒烟树两 exe → **106/106＋9/9 PASSED**——与送验留痕"106＋9（＋GUI 28）"同口径一致。
- 本地验收运行 XML：worktree A `reports/sdurws_ird_ui_test-acc.xml`（tests=106 failures=0）＋`reports/sdurws_ird_ui_contract_test-acc.xml`（tests=9 failures=0），6 条 SessionContract 用例行号（239/279/310/342/369/401）与源码逐一吻合。

### 4.4 acceptance 条目 —— 通过（3/3，逐条具名证据）

| 契约 acceptance 条目 | 具名证据（均在验收者亲跑 106＋9 内 PASSED，行号＝测试文件实测行） |
| --- | --- |
| ①UI-SES-2~7、UI-LCY-1（草稿三选＋任务二选统一确认机制——§5.4 S1/S2；只读打开提示与显示差异——UI-SES-2/3、PM-07/§5.3；等待覆盖在途归档——A7/SA-17/§5.7） | 装配：`AssembleDialogDataAreasAndAvailability`（SessionControllerModelTest.cpp:437）；五分支：`CloseCancelRestoresOriginalState`:476／`CloseSaveDispositionBranches`:503（保存失败 SaveFailed 中止）／`CooperativeCancelAddressesRequeriedRows`:552（重查集逐任务）／`WaitOptionCoversInFlightArchive`:667（在途引用 3 可见、不催促不取消、回调后 epoch=2＋退出就绪位）；只读提示：`LockHeldBannerCarriesPidAndActionKind`:585（**横幅含 PID=4242＋host**——UI-SES-2 观测点 598 行）＋`ReadOnlyCausesDistinguishActionKinds`:615（**retry-readonly vs contact-holder/inspect-resource 逐场景 actionKind**——UI-SES-3）＋`OpenFailureKeepsNoProjectWithErrorPageData`:645（§5.3 行 6 错误页）；切换/迟到写：`SwitchBindsBAndHoldsAUntilDrained`:701／`SwitchCandidateRejectedKeepsA`:742（UI-SES-4 机制半区＋"成功才切"）／`ContextInvalidNoticeWithoutRetry`:1016（UI-SES-7/UI-LCY-1——提示零动作位）；等待覆盖在途归档另有契约面 `SwitchKeepsOldStoreContextUntilArchiveDone`（SessionContractTest.cpp:310） |
| ②会话状态机（§5.2）逐状态不变量 INV-SES-1~4（writable 唯一只读判定源/无双 Open*/Draining 保活/关闭不永久阻塞——四级防线 2s·10s 协作取消、T_force 确认、T_force2 兜底） | `StateMatrixEpochAndInvSes1`:773（七态矩阵＋epoch 递增时机＋isReadOnlySession 唯一源）／`InvSes2AndCallOrderContract`:812（重入拒绝＋调用次序违约）／`InvSes3HoldReleasedByCallback`:849（shared 保活→回调/轮询后释放）／`FourLevelDefenseForceConfirm`:877（防线 3 T_force 确认链）／`TForce2GiveUpBounded`:934（防线 4 放弃＋UI-SESSION-CONTEXT-INVALID 双通道出线）／`ForceConfirmWithoutWiringFailsFast`:988（缺 abandonAll 接线 fail-fast、强杀零半执行）——INV-SES-4 的"绝不无限等待"由防线 3→4 串联阈值实现并实测 |
| ③O-31 处置（2026-09-19 裁决放行——ui 自有会话/查询/任务端口＋值投影承载，产品面零对端链接/include；INV-SES 语义零变化）＋P-UI-8 处置（T_force/T_force2 按 §5.6 默认 120 s/300 s 装配期可配，不私定终值） | O-31 结构自证：`O31SessionFacesAreUiOwnedInterfaces`（SessionContractTest.cpp:401——独立替身实现再次注入走通全程的虚派发证明）＋`NoCrossUnitInclude_O31_UI_BUILD`（BuildRedLineTest，验收者亲跑通过）＋门禁 R1/R3/SUB 命中集 base vs head 零变化（见 4.6）；P-UI-8：`PUi8DefaultsConservativeAndConfigurable`（SessionControllerModelTest.cpp:966——**默认 120 s/300 s 冻结断言（971~972 行）＋35 s 不触发**）＋`TForceThresholdsAssemblyConfigurable`（SessionContractTest.cpp:369——自定义 30 s 于 35 s 触发）双测试钉住 |
- 未执行项：无——三条 acceptance 全部有具名测试与命令输出；ird_gates 非本契约 acceptance 行（随 4.8/4.6 佐证，已亲跑）。

### 4.5 测试真实失败能力 —— 通过（两处变异构造，均如期变红，已还原复绿）

1. **MUT-1**（`src/UiSessionController.cpp` 装配函数：`saveDraftsAvailable = inputs.writable` → `= true`）：重建后 `--gtest_filter=SessionControllerModelTest.AssembleDialogDataAreasAndAvailability` → **FAILED**（`Expected: false / Actual: true`）——只读会话保存可用位断言真实有效。
2. **MUT-2**（`include/.../UiSessionController.hpp`：`forceWaitThreshold{120*1000}` → `{121*1000}`）：重建后 `PUi8DefaultsConservativeAndConfigurable` → **FAILED**（测试 971 行冻结断言）——P-UI-8 默认值钉住真实有效。
3. 还原与复绿：两处变异逐一还原（`git status` 对 industrialrobot/ **零残留**）＋touch 强制重编（F-068 教训）→ 重建 exit 0 → 双 exe 复跑 **106/106＋9/9 全绿**。

### 4.6 架构红线（AGENTS §5 八条）—— 通过

1. **L2 零 Qt**：新增/改动产品文件 include 全集为 std＋core/diagnostics/ui 公共头，零 `Q` 头（逐文件 grep 实证）；ird_gates 命中集**验收者独立复核**——同引擎同法亲跑 base worktree（detached @ 63e30071）与 head worktree（45baa315），同口径归一化 diff **EXIT=0 零差异**（引擎自计两侧均"命中 42 项"）；送验 `gates-hit-set-diff.txt` 为空文件（零变化自证一致）。
2. **R-1 业务域互链**：无 modeling/requirements/kinematics/trajectory/dynamics/selection/optimization 依赖；协作全部经 ui 自有六类端口（C-3/5/8 会话端口）＋core/diagnostics 登记边；门禁 R1/SUB 零新增。
3. **R-2 跨单元私有头**：include 只指向 `include/sdurws/ird/<unit>/` 公共头（core/Events、core/Identity、diagnostics/Catalog、diagnostics/DiagCodes、diagnostics/Factory——均公共面）。
4. **SA-02 框架零修改**：diff 零框架文件、patches/ 零新增（见 4.1）。
5. **R-4 名称前缀**：`NoRobWorkPrefixLiteral_R4_UI_BUILD` 验收者亲跑通过；diff 无 RobWork 前缀拼接/剥离代码；名称解析语义未在本单元实现（runtime 权威未侵占）。
6. **PA-1 权威唯一**：修订/写路径未在本单元实现——保存经 `saveAllDraftsManual` 接线点（→IDraftController，UI-T12 落位后接线，注释与测试 `CloseSaveDispositionBranches` 一致）；存储关闭经 C-3 端口（requestClose/closed/subscribeClose），未复制 project 关闭协议语义（端口注释"语义冻结（不改义）"＋契约测试互证）。
7. **PA-2 不可变历史**：本任务不涉及修订数据结构，无违背面。
8. **不恢复 old/**：diff 无 old/ 引用。

### 4.7 注释规范 —— 通过

- **公共头全查（4 个）**：
  - `UiSessionController.hpp`（新，634 行）：文件头（设计依据 §5 全节/§10.1/§3.3/§3.4/§6.2/§9.5/§11.5＋PM-03/PM-07＋契约 acceptance）、背景说明（装配图位置/对话框机制为何是数据＋决议）、P-UI-8 处置专段、线程约束段（§3.4 M-1）；类注释含生命周期/所有权（unique_ptr 独占＋shared 保活语义）、状态迁移表、四不变量实现位置、异常语义三分（构造期 invalid_argument／非法状态 logic_error／环境错误走返回值轨）；逐方法 @brief/@param[in]（含取值语义）/（@return 含失败行为）/@throws（含触发条件）；单位显式（`120 * 1000` 注明 120 s 保守值）；确定性来源（steadyClock 注入——测试不用 sleep 判据）。
  - `UiPorts.hpp` 增量（SessionPortBundle＋IUiStoreFactoryPort/IUiStoreCloseObserver/IUiProjectStorePort/IUiDraftQueryPort/IUiSessionTaskPort）：逐接口/逐方法 Doxygen 中文，含语义冻结出处（project.md §5.1）、出参形态理由（失败零绑定泄漏）、所有权（弱引用观察者/RAII 句柄析构即退订）、线程注（UI 线程调用）。
  - `UiProjections.hpp` 增量（会话投影族）：字段级注释说明 INV-SES-1 数据源、PM-07 actionKind 词表等。
  - `UiTypes.hpp` 增量（UiSessionState 七态/UiOpenMode/UiCloseIntent）：逐态语义锚＋落位第三头的防环理由（NFR-MNT-03）。
- **实现文件抽查（≥3）**：`src/UiSessionController.cpp`（文件头 review 对照表＋线程纪律段；装配/构造/INV-SES-1/打开/防线逐段"为什么"注释——如"时长阈值不用墙钟""行序保持端口快照原序 NFR-COR-02""过滤由端口侧完成不重定义"）；`test/SessionControllerModelTest.cpp` 与 `contract_test/SessionContractTest.cpp`（用例级中文说明＋IRD_TEST_INFO 需求/AT 追溯字段；替身与断言逐段中文）；`ui/CMakeLists.txt`（AUTOMOC 复核注 v1.3、测试 TU 分工注——中文块，关键决策说明）。
- §2.5 高危信息表逐项在位：物理单位（120 s/300 s/2 s/10 s 显式）、线程约束（UI 线程＋回调 Marshal 纪律）、所有权（shared 保活/unique RAII/弱引用观察者）、错误语义（调用方错误 fail-fast vs 环境错误返回值轨/诊断码 UI-SESSION-CONTEXT-INVALID）、确定性来源（steady_clock 注入、行序稳定 NFR-COR-02）。

### 4.8 文档同步＋findings 相关性 —— 通过

- **单元卡同步**：units/ui.md v1.3——文档头版本行、§3.3 三行增注（UiPorts/UiProjections/UiSessionController 落位，211/212/225 行）、§13 UI-T11 落地登记注（1572 行，①~⑧逐条）、§16.7 v1.3 行（1761 行）——与本 diff 交付物逐一对应，零偏差零未登记改动。
- 契约状态：送验分支保持 `ready` 正确——done 翻转属合入收尾（PIPE §4.7⑤，UI-T10 收尾提交 63e30071 同例），非实施者职责。
- **findings.json 未关闭条目相关性核对**（open 共 243 条，与本任务相关者逐条）：
  - **F-277（F-273/F-275 复踩——AUTOMOC 注释计数）**：本任务把该注释行改写为"十五个源文件"，实测 `add_library(sdurws_ird_ui STATIC)` 恰 15 个 .cpp——**已顺手消账**（建议所有者收尾时置 fixed）。
  - **F-278（UiPorts.hpp 尾换行丢失）**：本任务修改了 UiPorts.hpp 但尾换行仍未补（head 尾字节 0x50 'P'，与 base 同态）——未消账，维持 open。
  - **F-276 族（F-010/F-077/.../F-274——README 声称的 .log 留痕未入库）**：本任务 README-evidence.md 索引仍引用 integrated-build.log/smoke-configure.log/smoke-build.log/ird_gates-×3/validate-task.log/.console.log×6，而入库目录实际仅 xml×6＋json×6＋txt×2＋md×1，.log 零入库（gitignore `*.log` 根因未除）——**同族复踩（UI 系连续第 6 例），转登 F-279**。
  - **F-278 同族新例**：新增公共头 UiSessionController.hpp 文件尾同样无换行符（尾字节 0x50）——转登 F-280。
  - F-227/F-259（ui 产品面 Qt 门禁命中滞后/既有命中）：本任务零新增命中（4.6 独立比对证实），无新增事项。
  - F-261/F-263/F-264/F-267/F-269/F-271 家族（辅助提交标题名词短语开头）：本任务仅两笔提交且标题均动词开头（"实现…"/"入库…"），无新家族成员。
  - F-033（DOC-T13 契约②③字面冲突）：O-31 已于 2026-09-19 经所有者裁决放行（DTB §4 登记），本契约据此随批 ready 实施——与本任务实施无冲突，维持 open 待治理收口。
  - F-146（失权后写拒绝在存储上下文层缺端到端用例——project 侧）：本任务已承载 UI 侧呈现半区（makeContextInvalidNotice/UI-LCY-1），对端缺例归 project 单元任务范围，不适用本任务消账。
  - F-007/F-008/F-009（全新构建树环境类）：本次验收复现再次需要补拷 ini 辅助件＋toolchain/Qt 前缀参数——再次实证，维持 open。
- requirements-to-units.json：phase-one 12 条种子样本（base 同态，PM 系缺席属文件定位设计非本任务缺口）；本任务需求追溯由 ird-test-report.json requirementIds（PM-03×6/PM-07×4）＋单元卡承载。

### 4.9 commit 质量 —— 通过

- `21cba320`「[WP-10-T11] 实现 UiSessionController 与关闭/切换对话框机制（§5 状态机＋四级防线）」与 `45baa315`「[WP-10-T11] 入库执行留痕（构建/测试/门禁/治理证据）」——标题均为 `[WP-nn-Tkk] 动词开头` 中文格式。
- 正文四段（改动内容/改动原因/验证/影响面与兼容性）齐备，验证段与留痕一致：106＋9＋28 双模式计数与入库 XML 相符；"新增 TU 零告警零错误"经验收者双模式构建 grep 核实属实（54/25 条既有告警全部归属非本任务文件）；门禁"零变化"经验收者 base vs head 独立比对证实；GUI 执行缺平台插件直跑退出事实在 README 如实登记（非粉饰）。

### 4.10 留痕完整 —— 通过（附 F-279 建议级）

- gtest XML×6＋ird-test-report.json×6（集成＋冒烟双份）＋gates-hit-set.txt（49 条）＋gates-hit-set-diff.txt（空＝零变化）＋README-evidence.md 索引入库；ird-test-report.json 结构含 schemaVersion/summary/requirementIds/atIds 追溯字段（PM-03×6、PM-07×4 实测抽数）。
- phase-one-validation.log 为 WP-03-T01 时代工件；后续任务留痕口径已演进为 `traceability/builds/<task>/` 目录（UI-T10 等先例一致），本任务按现行口径执行且证据链完整。
- .log 文件未入库（gitignore 根因）——核心证据（XML/JSON）完整且验收者已独立复现同数，故降为建议级（F-279），不构成阻断。

### 4.11 偷懒/缩水实现扫描 —— 通过

- ①标记扫描：diff 新增行 `TODO|FIXME|stub|placeholder|未实现|not implemented`——产品源码（src/＋include/）**零命中**；测试文件命中仅为 `StubStorePort/StubDraftPort/StubTaskPort/StubStoreFactoryPort` 测试替身类名——**豁免依据**：契约 acceptance 3 明示"全部以可控替身承载，O-31 测试侧惯例"，且替身实现完整协议行为（计数/时序/回调），非未完成标记。
- ②空实现检测：无——实现 TU 754 行含完整状态机迁移、五分支决议解析、四级防线计时链；纯函数（assembleCloseDialogData/assembleReadOnlyBanner）为行为实现非占位返回。
- ③逐条证据表：见 4.4（三条 acceptance 逐一具名测试＋行号，无一条凭"已实现"声明采信）。
- ④测试强度：断言校验具体行为与边界（PID=4242、actionKind 序列逐位、epoch=2、inFlightReferences=3、120 s/300 s 冻结、logic_error 类型、takeExitPending 幂等、cancelCalls 空）；假时钟注入确定性驱动阈值（无 sleep 判据）；与 4.5 联动双变异实证会红。

---

## 二、发现的问题清单

- **阻断级**：无。
- **建议级（已随本记录转登 findings.json，编号顺延）**：
  - **F-279**：F-276 家族复踩（UI 系连续第 6 例）——README-evidence.md 索引引用的 9 类 .log 留痕（构建/门禁/validate-task/控制台）未入库，gitignore `*.log`（F-010 根因）持续吞没 traceability 留痕；责任方：实施纪律＋治理（建议白名单豁免 traceability/**/*.log 或登记提交件改法）；状态 open。
  - **F-280**：F-278 同族新例——新增公共头 `ui/include/sdurws/ird/ui/UiSessionController.hpp` 文件尾无换行符（尾字节 0x50）；同族既有 F-278（UiPorts.hpp）在本任务增量修改后仍未修复；责任方：实施者（下任务顺手补两处尾换行）；状态 open。
- **消账注记**：F-277（AUTOMOC 注释计数三袭）经本任务校准为准确值"十五个源文件"（实测 15 个 .cpp）——建议所有者收尾时置 fixed。

## 三、验收者环境说明

- 复现 worktree A：`D:/10_Source_Repos/21_robot/acc-ui-t11-wtA`（`git worktree add --detach @ 45baa315ab763cac0395768fd091c12e20fdfb6a`）；base 基线 worktree：`acc-ui-t11-wtBase`（detached @ 63e3007112c28d2b0dff48119fac3fa3a6232984，仅门禁比对用）；冒烟树：`D:/10_Source_Repos/21_robot/acc-ui-t11-smoke`（源取 worktree A 同 SHA）。
- 集成树从零配置（`RWS_BUILD_INDUSTRIALROBOT=ON`〔缓存 grep 确认 :BOOL=ON〕、vcpkg toolchain、Qt `CMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`；`RobWorkStudio.ini.shared.in`＋`RobWorkStudio.ini.template.static` 自主树补拷——F-007/F-062/F-099 既有缺陷的环境修复，不触及送验产物）。
- 工具链：VS 2022（生成器 Visual Studio 17 2022，-A x64，MSVC 19.42）、vcpkg 经典模式（gtest @ installed/x64-windows）、Qt 6.11.1（测试运行 QT_QPA_PLATFORM=offscreen）、pwsh 7（validate-task.ps1 → PASS 实测）。
- 探针/变异注入均在 worktree A 内：两处变异（装配可用位恒真；T_force 默认值 121 s）一次重建取证，逐字还原＋touch 强制重编，最终 industrialrobot/ 目录 `git status` 零修改、106＋9 复绿。
- 分支漂移：验收期间任何人均未向 wp10-t11 追加提交（冻结口径执行）；网络（代理 127.0.0.1:17890 与直连均不可达）导致 `git fetch`/`ls-remote` 失败，origin 侧漂移检测由收尾流程按 headSha＋evidence commit 双 SHA 拦截（PIPE §4.7 双漂移检测）。
- **evidence 推送状态**：验收记录与 F-279/F-280 登记已落本地分支 `acc/UI-T11/2`（基于本地 origin/redesign-main @ 63e3007112c28d2b0dff48119fac3fa3a6232984）并 commit；push 因网络不可达未成功（重试有限次未果），**worktree B 与分支保留未删除**，由编排者按 UI-T08 先例补推（ACC §3 记录回传的推送完成要求以补推达成，验收结论本身不受影响）。
- 验收者日志（构建/门禁/变异还原全过程输出）随临时 worktree 保留或清理，本记录内数值均为亲测，不引用实施者运行结果。
