# EV-T12 验收记录（attempt 2，2026-09-13）

| 字段 | 值 |
| --- | --- |
| 任务 | EV-T12 evidence 文档与门禁同步（≙WP-05-T12，单元 evidence） |
| 契约 | tasks/foundation/EV-T12.json（status ready，verify＝validate-docs.ps1，acceptance 3 条） |
| 送验对象（冻结） | `b898f0baf08c9c15416599d67c15c8b28a704c2a`（分支 `wp05-t12`） |
| diff 范围 | `1a8b9cb471b77d79e177d4cbe515b38755811a1a..b898f0ba`（base＝origin/redesign-main 尖端；4 提交：ed73db0c → 516dff91 → 8b6a1dec → b898f0ba） |
| 验收尝试 | attempt 2（编排者下发；state.attempts.accept 已递增至 2；远程与本地均无 acc/EV-T12 既有分支，redesign-main 无 EV-T12 同日记录——记录文件用基名，无 `-r2` 后缀必要） |
| **verdict** | **pass**（4.1～4.11 全部通过；阻断级问题零；建议级 1 项已转登 findings.json F-074，不阻塞合入） |
| 记录分支 | `acc/EV-T12/2`（基于 origin/redesign-main＝1a8b9cb） |

---

## 0. 独立性声明（ACC §2）

1. 本会话为全新上下文，不与实施者共享任何会话记忆；只读任务契约、设计文档、留痕与 git 产物。
2. 未读取实施者的过程说明/辩护文本；一切结论以下述亲手复现的命令输出与文件行号为据。
3. 对抗式立场执行：每项检查均先寻找反证（重点排查了送验 `ird_gates.log` 中的 5 处命中与 CI 缺口声明的真实性、报告计数的准确性、README 审计声明的可复核性）；逐项反证排查结果见下文。
4. 构建与测试全部由验收者亲手重跑（detached worktree @ 冻结 SHA），未采信实施者运行结果。

## 1. 验收者环境与复现现场（ACC §3）

- 复现现场：`git worktree add --detach <临时A> b898f0baf08c9c15416599d67c15c8b28a704c2a`（detached @ SHA，4911 文件检出，HEAD 实测＝b898f0ba）。
- 记录回传：`git worktree add -b acc/EV-T12/2 <临时B> origin/redesign-main`（本记录所在）；另建 `--detach` worktree @ `1a8b9cb`（base）作门禁基线对照。
- 集成模式重新配置：`cmake -S RobWork -B build-acc -G "Visual Studio 17 2022" -A x64 -DCMAKE_TOOLCHAIN_FILE=<主仓vcpkg绝对路径>/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DRWS_ENABLE_PYTHON=OFF`；配置后 `grep RWS_BUILD_INDUSTRIALROBOT build-acc/CMakeCache.txt` → `:BOOL=ON` 确认。
- 冒烟模式：独立临时目录，`cmake -S RobWork/RobWorkStudio/src/rwslibs/industrialrobot -B <冒烟目录> -DCMAKE_TOOLCHAIN_FILE=<vcpkg绝对路径>`（带 toolchain，AGENTS §4.1 / CORE-T01 G-2 口径）。
- 环境前置（F-007/F-062 已登记事项，本次再次复现确认）：全新 worktree 配置集成模式必须补三件主树本地状态——①vcpkg toolchain 绝对路径；②Qt CMAKE_PREFIX_PATH；③git 未跟踪的框架资源 `RobWork/RobWorkStudio/bin/RobWorkStudio.ini.template.static`（缺它则配置失败于 RobWorkStudio/src/CMakeLists.txt:58，本次实测命中）。验收者从主树原样复制该文件（内容为主树既有构建残留渲染产物，非实施产物），属环境就位而非修改送验对象。
- 工具链：MSVC 19.42（VS2022 17）、CMake＋VS 生成器、pwsh、gtest 1.18.0（vcpkg installed/x64-windows）。

## 2. 验收清单逐项证据（ACC §4，4.1～4.11）

### 4.1 改动范围 —— 通过

- `git diff --name-status 1a8b9cb..b898f0ba`：19 个变更文件（2 M＋17 A）＝
  ①`RobWork/RobWorkStudio/src/rwslibs/industrialrobot/evidence/include/sdurws/ird/evidence/README.md`（M）
  ②`RobWork/doc/industrial-robot-design/units/evidence.md`（M）
  ③其余 17 个新增全部位于 `RobWork/doc/industrial-robot-design/traceability/**`（builds/wp05-t12/ 16 件＋wp05-t12-gate-registrations.md）。
  契约 allowedFiles 三模式全覆盖，无越界文件。
- forbiddenFiles（REQUIREMENTS.md / ARCHITECTURE.md）零触碰（name-status 无此二文件）。
- SA-02：diff 零框架源码文件、`industrialrobot/patches/` 零新增（无登记必要）。

### 4.2 可复现构建 —— 通过（全树从零受已登记基线缺陷 F-008 拦截，任务目标面两模式从零零错误；细节与归因如下）

- **集成模式重新配置**：如上，exit 0（首次失败于 F-007/F-062 环境前置，补齐后通过——见 §1）。
- **集成模式全树从零构建**：`cmake --build build-acc --config Release` → exit 1，73 行 error **全部**位于 RobWorkSim 两个插件工程（RWSimulatorPlugin.vcxproj 71 行：VelocityScrew6D.hpp 63＋Math.hpp 8，C4003 min 宏→C2059/C2334 连锁；sdurwsim_bullet/bt_plugin.rwplugin.vcxproj 2 行：Eigen Parallelizer.h 同宏污染族）——即 findings.json **F-008**（基线框架缺陷，F-008 自证在 base 959fb9b 对照复现 71 处；F-038 验收在 RT-T03 时点复现 73 处）的精确再现。industrialrobot 与 RobWork 核心/Studio 库零 error（构建在该插件处中止，ird 目标尚未开始）。**该失败与 EV-T12 无因果**（本任务 diff 零 C/C++/CMake 文件），不构成对本任务的可复现性否定。
- **按 F-036/F-038/F-054 验收先例口径（任务目标面从零）**：`cmake --build build-acc --config Release --target sdurws_ird_evidence_test sdurws_ird_evidence_contract_test` → **exit 0，零 error**（从零产出 sdurws_ird_core.lib、sdurws_ird_evidence.lib、sdurws_ird_testkit.lib、sdurws_ird_evidence_test.exe、sdurws_ird_evidence_contract_test.exe）；随后运行两 exe → **205/205 PASSED＋14/14 PASSED（exit 0）**。
- **独立冒烟模式**：全新目录＋toolchain，configure/build **exit 0**；运行 `sdurws_ird_evidence_test.exe` → 205 PASSED（XML tests="205" failures="0"）、`sdurws_ird_evidence_contract_test.exe` → 14 PASSED（tests="14" failures="0"）。
- 送验留痕的口径诚实性：ird-test-report.json 明示"集成＝仓库根 build/ 存量增量树（F-054 树性质口径）；冒烟＝全新独立目录"——与 F-054/F-055 纪律一致，无增量树冒充从零构建的粉饰。
- 结论留痕：本记录 §2.4.2 即日志（验收者临时 worktree 随记录推送后销毁，关键输出行已抄录于此）；送验侧日志 traceability/builds/wp05-t12/（integration-build.log、smoke-*.log）与本验收同口径。

### 4.3 契约 verify 命令 —— 通过（逐条亲手执行）

- 契约 verify 数组仅 1 条：`pwsh -File RobWork/scripts/industrialrobot/validate-docs.ps1`（在 worktree A 仓库根执行）→ `validate-docs: PASS (20 units, 12 trace entries, 150 task files)`，exit 0。
- 超覆盖复跑（与送验一致）：`validate-task.ps1 -TaskFile RobWork/doc/industrial-robot-design/tasks/foundation/EV-T12.json` → `validate-task: PASS (1 tasks)`，exit 0。
- 与送验 validate-docs.log / validate-task.log 逐字同口径（同数字）。

### 4.4 acceptance 条目逐项证据 —— 通过（3/3，均有实证；无虚标通过）

| # | 契约 acceptance | 验收者证据 |
| --- | --- | --- |
| 1 | README 核对零偏差 | ①送验证据 traceability/builds/wp05-t12/README-audit.md（§1 指向核对/§2 目录实录/§3 十头逐头比对/§4 实跑口径/§5 EV-T11 交付面/§6 结论）；②验收者独立抽查全部吻合：include 目录实测 11 项＝10 公共头＋README（与 §2 实录一致）；`Errors.hpp` `enum class EvidenceErrorCode`（L72 起）实测 **13 值**（与 §3 第 1 行"现值 13"一致）；builds/wp05-t01～t12 目录全部存在（与 §1"留痕路径"行一致）；evidence.md §12（L971）/§15.4 锚点实测存在；README 新增文本声明（十模块、EvidenceErrorCode 9→10→13 表尾追加序列、当前口径 `sdurws_ird_evidence_test` 205＋`_contract_test` 14）与磁盘及本验收双模式实跑逐数一致 |
| 2 | §15.3 状态更新（含 P-RT-9 消账确认——project.md 完整性实测） | ①evidence.md §15.3 P-EV-1～P-EV-9 九行均增附〔EV-T12 状态同步 2026-09-13〕事实标注（diff L1070-1078 可验），open 项一律维持 open、裁决者列未动（不越权）；②**P-EV-6 就地消账**：问题列标注"已消账（2026-09-13，EV-T12 就地翻转；runtime.md P-RT-9 同源请求对账关闭）"，保留原登记原文；验收者独立实测 project.md：**1386 行、`__PART3__` 零命中、`## 1`～`## 15` 章节标题全在**——与消账依据列声明逐字一致；③runtime.md L1511 P-RT-9 确为同源请求（"请 evidence 详设所有者按磁盘现状更新其登记"），本次消账即对该请求的回应；④上游版本声明抽查属实：core.md 卡头 v0.11、ARCHITECTURE.md 卡头 v0.12、units/ui.md 与 units/reporting.md 存在、units/requirements.md 缺位 |
| 3 | UT-BUILD 建议并入 CI（登记给 WP-01 侧——§12 任务行原文） | traceability/wp05-t12-gate-registrations.md：§1 UT-BUILD 四例（BuildRedLineTest.cpp）与红线映射；§2 既有承载消账（gate-all.ps1＋ird_gates.cmake 双层）；§3 缺口登记——**验收者独立核实**：`industrial-robot-windows.github-workflow.yml` L310/L413、`industrial-robot-windows.gitlab-ci.yml` L122/L132 的 run-tests 正则实测仍钉 `^sdurws_ird_core_test$`（逐行 grep 命中，行号与登记件一致）；§4 登记请求指向 WP-01（scripts/ 与 ci/ 不在本任务 allowedFiles，只登记不代行——权限边界正确）；RT-T13 同型登记件交叉引用在案 |

- 无一标注"通过"而无证据；`ird_gates` 在送验 ird-test-report.json 中如实登记 `exitCodes.irdGates = 1`（命中 5 项既有），未粉饰为通过——符合 ACC §4.4"未执行的/未通过的不得标注通过"。

### 4.5 测试真实失败能力（验证验证者） —— 通过（双探针即红、还原复绿）

- 探针 A（validate-task 锚点断言）：worktree A 内将 EV-T12.json `designRefs` 注入假锚点 `units/evidence.md#999-bogus-anchor` → `validate-task.ps1` **exit 1**，报 `designRef anchor not found in target: units/evidence.md#999-bogus-anchor (in EV-T12)`；逐字还原（git checkout）后复跑 **exit 0**。
- 探针 B（validate-docs 任务文件 JSON 解析断言）：向 EV-T01.json 注入双逗号语法错误 → `validate-docs.ps1` **exit 1**（ConvertFrom-Json 失败→`duplicate task ids` Write-Error）；还原后复跑 **exit 0**（PASS 同数字）。
- 两探针均在验收者临时 worktree 内构造，还原后 `git status` 零残留，未触碰任何分支与实施产物。
- 附加：219 例既有测试两模式从零构建实跑全绿（§4.2），测试本体非恒真（其真实失败能力已由 EV-T10/EV-T11 验收探针与 UT-BUILD 自证先例覆盖，本任务零新增测试）。

### 4.6 架构红线（AGENTS §5 八条逐条） —— 通过

| 红线 | 结论与证据 |
| --- | --- |
| ① L2 计算内核零 Qt | diff 零源码/CMake 变更（4.1），无可引入面；产品面零 Qt 事实由 219 例含 `NoQtInclude_EV_BUILD_NFR_MNT_01`（UT-BUILD）双模式实跑钉住 |
| ② 业务域互链禁止（R-1） | diff 未触碰任何 CMakeLists/链接结构；链接边与 base 逐位相同 |
| ③ 跨单元私有头禁止（R-2） | 同上——零 include 变更；R-2 事实由 `NoCrossUnitInclude_EV_BUILD_R1_R2`、`PublicHeaderPathLayout_EV_BUILD_R2` 实跑钉住 |
| ④ 框架零源码修改（SA-02） | diff 无框架文件；patches/ 零新增（4.1） |
| ⑤ 不做名称前缀拼接/剥离（R-4） | diff 无名称解析逻辑变更（零代码） |
| ⑥ 权威唯一（PA-1） | P-EV-6 消账＝evidence 卡辖域内对 runtime P-RT-9 明文请求的回应（非代 runtime 裁决——runtime 卡行未动，见 F-074）；UT-BUILD 并入 CI 未代行 scripts/ci 改动（登记件 §4 明示接收方 WP-01）；§15.3 其余 open 项未越权翻转（九行裁决者列零变更） |
| ⑦ 不可变历史（PA-2） | diff 无修订/写路径数据改动（纯文档与留痕） |
| ⑧ 不继承 old/ | diff 无 old/ 相关内容 |
- **树级附加说明**：送验树上 `ird_gates` 为红（5 命中，exit 1）。验收者在 **base（1a8b9cb）worktree 对照复现＝同 5 命中、同 exit 1**（IRD-GATE-SUB×4＋IRD-GATE-T1×1，逐条与 head 相同）——命中为 **base 继承而非本任务引入**（本任务 diff 无 CMake/链接变更），归属与送验报告一致：F-019×2（testdata_lint 产品目标 T-1＋表外边）＋F-051×1（runtime_test→testkit）＋F-072×2（evidence _test/_contract_test→testkit），三者均 open、处置归 WP-01 构建所有者（cmake/ 门禁数据面不在任何实施任务 allowedFiles）。ird_gates 引擎自测 9 项（pass_clean/pass_r4comment/fail_r1/fail_t1/fail_r5/fail_sub/fail_r3/fail_r4/fail_t2）实测行为全部符合预期。

### 4.7 注释规范（AGENTS §2） —— 通过（无适用对象＋登记件表述合格）

- diff 扩展名清点：仅 `.md` / `.log` / `.xml` / `.json`，**零 .hpp/.cpp/CMake 文件**——不存在新增/大改的产品代码文件，AGENTS §2 文件头/Doxygen/§2.5 高危信息表无适用对象（4.7 的"公共头全查、实现文件抽查 ≥3 个"按零代码变更事实判定为空集通过，与 ird-test-report.json "taskNature：零代码变更"声明一致）。
- 抽查两份主要登记件（README-audit.md、wp05-t12-gate-registrations.md）全文：中文表述完整、结构清晰、无中英机翻混杂、事实指向均带行号/路径可复核——符合登记件可读性标准。

### 4.8 文档同步与 findings.json 核对 —— 通过

- **实现与任务卡零偏差**：evidence.md §12 EV-T12 行（L971）"README 指向核对；§15.3 待裁决项状态更新；UT-BUILD 建议并入 CI（WP-01 侧）"与 diff 逐项对应；完成条件"本文与实现零偏差登记；未决项有最新状态"满足（v1.3 变更行 ①～⑤ 与 diff 实际内容逐项一致；"双模式实跑 205＋14 全绿复证"经本验收亲手复现证实）。无需 DTB §5.4 偏差登记的偏差：零。
- **任务状态/追踪矩阵同步**：validate-docs PASS（含 20 单元/12 追踪条目/150 任务文件机检）；§14 矩阵 EV-T12 行在卡（L971）。
- **findings.json 未关闭条目相关性核对**（登记册共 73 条，末位 F-073）：
  - F-019/F-051/F-072（ird_gates 红基线 5 命中）——**直接相关**：构成送验门禁日志的全部命中；本任务 ird-test-report.json 的归属（F-072×2＋F-051×1＋F-019×2）与 findings.json 权威登记逐项一致——F-060 曾登记的"误归属"问题本次未再现；三者维持 open（WP-01 辖域），非本任务可顺手消账（cmake/ 不在 allowedFiles）。
  - F-007/F-062（worktree/全新树环境前置）——**相关**：本验收再次复现（toolchain/Qt/ini.template.static 三件），按登记绕行；维持 open。
  - F-058（evidence.md 'AI 执行就绪补充'转义损毁）——**相关且顺手消账未发生**：EV-T12 编辑了同一文件（units/evidence.md）但未修复该节 `$(evidence.ToUpper())`/`alidate-docs.ps1` 等损毁、亦未注明原因；本验收实测 L1119/L1131 损毁仍在。属可顺手消账而未消账的提醒（非任务卡偏差），维持 open，归 evidence 卡后续触及者；不构成阻断。
  - 其余未关闭条目（F-012 提交标题家族、F-015~F-018/F-041/F-043/F-061/F-063/F-064/F-065/F-066/F-067/F-068/F-069/F-070/F-071 等）与本任务无直接相关；F-065（留痕日志中文双重编码）本次送验 16 件留痕实测**零乱码**（grep 命中集为空），该系统性问题在本任务留痕中未再现。
  - 本验收新发现 1 条建议级（P-RT-9 对端承接滞后）已按 ACC §5 转登 **F-074**（见 §4）。

### 4.9 commit 质量 —— 通过

- 四提交标题均为 `[WP-05-T12] ＋动词开头`：`登记双模式构建测试实跑与门禁自查留痕`／`提交 evidence UT-BUILD 并入 CI 登记件（接收方 WP-01）`／`登记 evidence 单元卡 v1.3：§15.3 九项状态实测同步并消账 P-EV-6`／`同步 evidence 公共头 README 至 EV-T11 落位现状并完成指向核对`——格式合规（无 F-012 家族形态）。
- 四段正文齐备（What/Why/验证/影响面）且带 `Acc-Covered` 条目覆盖尾注；验证结论与留痕一致、如实标注失败面：irdGates exit=1 及 5 项命中归属、集成树"存量增量树"性质（F-054 纪律）均如实写明——**无粉饰**。
- 计数准确性抽查（F-041/F-073 纪律）："新增 16 件执行证据"＝builds/wp05-t12/ 实际 16 文件（ls 实测）——准确。

### 4.10 留痕完整 —— 通过

- gtest XML 四份：`sdurws_ird_evidence_test-{integration,smoke}.xml`（tests=205 failures=0）、`sdurws_ird_evidence_contract_test-{integration,smoke}.xml`（tests=14 failures=0）——验收者逐份核数。
- `ird-test-report.json`（ird-test-report/1）：汇总 integration/smoke 各 total=219 passed=219、exitCodes 七项全列（irdGates=1 如实）、irdGatesNote 命中归属、acceptanceMapping 三条、evidence 文件清单——与 XML/日志/门禁复现逐项一致。
- 构建日志：integration-build.log（存量增量树，F-054 口径标注）、smoke-configure.log/smoke-build.log（全新目录＋toolchain）；运行日志 integration-test-run.log/smoke-test-run.log 尾部 205 PASSED；validate-docs.log/validate-task.log 与验收者复跑同数字；README-audit.md、ird_gates.log 在档。
- `phase-one-validation.log 或 resultNote`：ird-test-report.json 的 `reworkNote` 字段即现行 resultNote 等价物；phase-one-validation.log 自 WP-03-T01（da407b61）后即为历史载体，现行惯例＝builds/<task>/＋报告 JSON（EV-T10/EV-T11 同口径），本任务已按惯例落位，无遗漏。

### 4.11 偷懒/缩水实现扫描 —— 通过

- ① diff 全文 `TODO|FIXME|stub|placeholder|未实现|not implemented` 扫描：**零命中**（git diff | grep -i，无豁免事项需要列出）。
- ② 空实现检测：diff 零代码文件，无公共接口函数体——不适用。
- ③ 逐条证据表：见 §2 的 4.4 表——三条 acceptance 均有具名文件＋验收者独立复核证据，无一条依赖"已实现"声明。
- ④ 测试强度：219 例既有用例两模式从零构建亲测全绿（非恒真、含边界与反例——EV-T10/EV-T11 验收已对测试强度做过探针级验证）；本任务机检面（validate-docs/validate-task）经 4.5 双探针证明断言真实会红。

## 3. 问题清单

### 阻断级

无。

### 建议级（已按 ACC §5 强制转登 findings.json）

1. **F-074**（本次新登记，编号顺延自 F-073）：runtime.md §15.3 P-RT-9 行未随 evidence 侧 P-EV-6 消账同步——EV-T12 已就地翻转 P-EV-6（project.md 完整性实测，验收者独立复核一致），对端 P-RT-9 行仍登记"仍 open……请 evidence 详设所有者更新的请求维持有效"，请求实际已被满足；units/runtime.md 不在 EV-T12 allowedFiles，实施侧无法代改。处置：runtime 详设所有者下次触及 units/runtime.md 时翻转 P-RT-9 行（引用 evidence.md §15.3 P-EV-6〔v1.3〕为消账证据），无独立任务必要。

### 提醒（已有登记，不重复登记）

- F-058（evidence.md 'AI 执行就绪补充'转义损毁）：本任务未顺手消账（见 4.8），维持 open，归 evidence 卡后续触及者。
- F-007/F-062（全新 worktree 环境前置）与 F-008（RobWorkSim 插件宏污染，全新集成树全量构建必失败）：本次验收再次复现确认仍 open；EV-T12 验收按已登记绕行口径执行（任务目标面从零＋存量增量树诚实标注）。
- F-019/F-051/F-072（ird_gates 红基线 5 命中）：base 继承、归属核实无误，维持 open，WP-01 构建所有者处置。

## 4. verdict

**pass** —— 4.1～4.11 全部通过，无阻断级问题；建议级 1 项（F-074）已转登 findings.json，不阻塞合入。

- 验收者未合入任何分支、未修改任何实施产物、未向 `wp05-t12` 推送任何提交（送验对象保持冻结于 b898f0ba）；本记录连同 F-074 登记仅落 `acc/EV-T12/2` 分支。
- 记录三元组：`{branch: acc/EV-T12/2, path: RobWork/doc/industrial-robot-design/traceability/acceptance/EV-T12-20260913.md, commit: <见 acc/EV-T12/2 分支尖端，推送后由编排者登记>}`。

## 5. 验收者环境说明

- 全新验收上下文（ACC §2）；复现现场为 detached worktree @ b898f0ba（临时 A）＋base 对照 worktree @ 1a8b9cb＋记录分支 worktree acc/EV-T12/2（临时 B，基于 origin/redesign-main＝1a8b9cb）。
- 环境前置按 F-007/F-062 绕行：vcpkg toolchain（主仓 vcpkg/，经典模式）＋Qt 6.11.1 msvc2022_64 前缀＋主树未跟踪框架资源 RobWorkStudio.ini.template.static 原样复制（非实施产物）。
- git 操作在仓库根执行；origin 已 fetch，分支漂移检测：origin/wp05-t12 尖端＝b898f0ba＝冻结 SHA（ACC §3，无漂移）。
- 验收者临时 worktree 内的全部探针注入均已逐字还原（git checkout 复绿验证）；记录推送成功后临时 worktree 才删除。
