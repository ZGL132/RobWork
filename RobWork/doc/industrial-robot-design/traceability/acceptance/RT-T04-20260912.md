# RT-T04 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | RT-T04 实现 CanonicalModel 与编码器（RT-Codec）——tasks/foundation/RT-T04.json |
| 送验对象 | **92b164fe6dc8c7b1e4d27addccf9025cf1cdeef6**（40 位冻结 SHA；分支 wp06-t04 尖端） |
| base..head | 08bc569bcb239d9d0a02b4350983fd53be25271b..92b164fe6dc8c7b1e4d27addccf9025cf1cdeef6（3 提交：97d8844 实现、c4b463d 留痕、92b164f 文档落位） |
| 验收者 | 全新上下文独立会话（ACC §2 四条独立性要求适用：只消费产物与留痕，未读取实施叙述；构建/测试/门禁/探针全部亲手重跑） |
| 日期 | 2026-09-12 |
| **verdict** | **pass**（4.1~4.11 全部通过，无阻断级问题；建议级 G-1 已转登 findings.json F-039） |

---

## 复现现场与环境（ACC §3）

- worktree A＝`git worktree add --detach <tmp>/acc_rt_t04_wtA 92b164fe…`（detached @ 冻结 SHA）；集成树全新配置于 A 内 `build/`；冒烟独立目录 `<tmp>/acc_rt_t04_smoke`。
- OS Windows x64；MSVC VS2022 17.12（"Visual Studio 17 2022" 生成器，x64）；vcpkg 经典模式（仓库根，gtest 1.18.0 就位）；Qt 6.11.1（D:/software/Qt/6.11.1/msvc2022_64）。
- 环境前置处置（F-007/F-009 既有登记口径）：从主树复制 untracked 文件 `RobWork/RobWorkStudio/bin/RobWorkStudio.ini.template.static` 至 worktree A 同路径（该文件不在任何提交内，非送验产物改动，跟踪树保持字节级一致——探针还原段同口径核验）。
- 验收者留痕（本机 `<tmp>/` 下，关键数字摘录于正文；**未**复制入 evidence 分支，正文物证为准）：acc_rt_t04_cfg.log、acc_rt_t04_verify_build.log、acc_rt_t04_acc-smoke-cfg/build/rebuild/run.log、acc_rt_t04_acc-integration/smoke.xml、acc_rt_t04_probe1/probe2-build/run.log、acc_rt_t04_restore-build.log、acc_rt_t04_acc-gates.log。

## 逐项证据（4.1~4.11）

### 4.1 改动范围 —— PASS

`git diff --stat 08bc569..92b164f` 共 21 个变更文件，逐个对照契约 allowedFiles：

- `runtime/**` 9 个：CMakeLists.txt（target_sources 增列两产品源＋测试两文件）、include/…/CanonicalModel.hpp、include/…/Codec.hpp、include/…/Errors.hpp（Expected 工厂实现）、src/CanonicalModel.cpp、src/Codec.cpp、test/CanonicalModelFixture.hpp、test/CanonicalModelTest.cpp、test/CodecTest.cpp；
- `traceability/**` 11 个：builds/wp06-t04/ 下双模式 XML×2＋日志×8＋ird-test-report.json；
- `units/runtime.md` 1 个（v0.5 落位登记）。

三组路径均在 allowedFiles（runtime/**、doc/…/traceability/**、units/runtime.md）内；forbiddenFiles（REQUIREMENTS.md、ARCHITECTURE.md）零触碰（diff 零命中）；`patches/` 无新文件（SA-02 合规，`git diff 08bc569..92b164f -- …/patches/` 为空）。

### 4.2 可复现构建 —— PASS

- 集成全新配置：`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=<vcpkg> -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64` → **exit 0**（Configuring done 28.3s；首跑失败＝ini 模板缺失，复制 untracked 前置文件后重配通过——F-007 登记在案）；缓存确认 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`。
- 契约目标从零构建：`cmake --build build --config Release --target sdurws_ird_runtime_test` → **exit 0、error C[0-9]/LNK[0-9]/fatal 零命中**（framework 依赖全量从零编译；产物 sdurws_ird_runtime.lib＋sdurws_ird_runtime_test.exe）。
- 冒烟独立目录：`cmake -S <A>/industrialrobot -B <smoke> -G "Visual Studio 17 2022" -A x64 -DCMAKE_TOOLCHAIN_FILE=<vcpkg>` → **exit 0**；`cmake --build <smoke> --config Release` → **exit 0、零错误**（探针还原后曾因旧二进制假红重跑一轮——见 4.5 教训注记，最终口径 acc-smoke-rebuild.log exit 0）。
- 双模式测试：集成 **105/105 PASSED、exit 0**（XML tests="105" failures="0" errors="0"）；冒烟 **105/105 PASSED、exit 0**（XML 同数字）。105＝既有 73（RT-T01/T02/T03 回归）＋新增 32（CanonicalModelTest 21＋CodecTest 11）。
- 全树从零构建未纳入本次实测（F-008 基线缺陷在 RT-T02/T03 验收已两次实证阻挡全新树全量构建；本任务目标面口径＝双模式从零零错误＋105/105，已达硬验收面）。

### 4.3 契约 verify 命令 —— PASS

契约 verify 数组仅一条：`cmake --build build --config Release --target sdurws_ird_runtime_test`——验收者在全新 worktree 集成树亲手执行，**exit 0、零错误**（同 4.2 第二条，从零构建口径）。

### 4.4 acceptance 条目逐项 —— PASS（4/4 有证据）

| acceptance 条目 | 证据（验收者亲手复现） |
| --- | --- |
| RT-ID-1/2/3 用例通过 | `RT_ID_1_RepeatBuildStability.SameFixtureYieldsByteIdenticalEncodingAndIdentity`（双构造等值＋编码逐字节＋重建身份相等）、`RT_ID_2_NoFloatToleranceEquivalence.TinyDeltaChangesIdentityWhileToleranceSaysEqual`（1e-15 微差与 ±0 位模式→身份不等，前提自检容差判等成立）、`RT_ID_3_DisplayFieldExclusion` 四用例——双模式 XML 全 PASSED |
| parse(encode(x))==x 往返用例通过 | `CodecRoundtripTest` 三用例（Minimal 精确往返／Rich 含排除域保真／SparseOptionals 缺席保真）＋`CodecNegativeTest` 两用例（malformed 五类拒绝、身份拼接/位翻转检测）全绿 |
| 身份域排除项（显示字段）用例通过 | RT_ID_3 四用例（revisionSeq/installPreset/diagnostics/严格子集）；实现核对：Codec.cpp L724（revisionSeq）、L738（installPreset）、L831-838（diagnostics/capabilities/contentIdentity）五项排除与 Codec.hpp CR-02 声明一一对应 |
| CanonicalModel 不持久化（D-1） | `CanonicalModelTransientTest` 三用例：NoPersistenceMemberSurface（static_assert 无 save/load/serialize/toFile/fromFile＋值语义）、CodecApiIsInMemoryOnly（签名级无文件参数）、SourceScanNoPersistenceIncludes（四文件源码扫描零 fstream/filesystem/iostream/project/io/modeling）全绿 |

无"未执行"项：ird_gates 已实际执行（见 4.8），非 F-035 型失实标注。

### 4.5 测试真实失败能力 —— PASS（双探针，注入即红、还原复绿）

- **探针 1**（身份域排除断言）：Codec.cpp L724 注入"身份域错误纳入 revisionSeq"（`if (!identityDomain)` 条件去除＋探针标记）→ 重建 exit 0（显式核验，防旧二进制）→ `--gtest_filter=RT_ID_3_DisplayFieldExclusion.*` → **RT_ID_3.RevisionSeqDoesNotAffectIdentity FAILED（exit 1，其余 3 用例仍绿）** → 精确还原 → `git status --porcelain` 零残留。
- **探针 2**（builder SPD 复核）：CanonicalModel.cpp requireValidInertia 注入正定判据恒过（`if (false && …)`＋探针标记）→ 重建 exit 0 → `RejectsIllegalProvidedValues` **FAILED（exit 1）** → 精确还原 → git status 零残留。
- 还原后双模式全量复绿：集成重建 exit 0＋**105/105**；冒烟重建 exit 0＋**105/105**。
- 教训注记（TK-T07/T10 同型，验收者亲历）：冒烟首轮运行出现 RevisionSeq 用例假红——冒烟构建落在探针 1 注入窗口内，消费了带缺陷旧二进制；显式核验构建退出码并重建后复绿。旧二进制假绿/假红风险在双探针流程中均以"注入后必重建＋还原后必重建"处置。

### 4.6 架构红线 —— PASS（八条逐条）

1. **零 Qt**：七个新文件 grep `#include <Q` 零命中（RuntimeBuild.NoQtInclude 回归用例亦绿）。
2. **R-1 业务域互链**：新文件对 modeling/requirements/kinematics/trajectory/dynamics/selection/optimization 头 include 零命中；唯一单元依赖＝core（CMake 既有边未变）。
3. **R-2 跨单元私有头**：ird include 全部为 `include/sdurws/ird/core/…`（Errors/Units/DiagData/Digest/Identity/Provenance 公共头）与 runtime 自有公共头——无私有头跨单元。
4. **SA-02**：diff 零框架文件、patches/ 零新文件。
5. **R-4 名称前缀**：CanonicalModel/Codec 无名称解析/拼接/剥离逻辑（deviceName 只承载值，生成归 §7/RT-T05）。
6. **PA-1 权威唯一**：内容摘要只调 core::ContentDigester（Codec.cpp computeContentIdentity，CR-02）；错误码经 Errors.hpp registryCode 单一来源（isHardErrorDiagCode 不复制字面量）；诊断文案引用既有码（RT-CAPABILITY-MISSING 等为 §10.11 冻结码）。
7. **PA-2 不可变历史**：本任务无修订/写路径（D-1 由测试三层钉住）。
8. **不恢复 old/**：无 old/ 引用。

### 4.7 注释规范 —— PASS

- 公共头全查：CanonicalModel.hpp（694 行——文件头设计依据/背景/不变量总览/线程安全/rw::math 说明；每 struct/class/函数中文 Doxygen；单位 rad/m/kg·m² 逐字段标注；坐标系（连杆系/质心系/工具系/世界系）显式；错误语义（调用方违约 fail-fast vs 查询轨 Expected）与确定性来源齐备）；Codec.hpp（194 行——CR-02 排除字段声明点五项逐条出处、三形态契约与失败语义、完整字节布局图）。
- 实现/测试抽查 ≥3：CanonicalModel.cpp（文件头错误语义定位＋魔法数字全带出处（1×10⁻¹² 两处容差均注设计原文）＋build() 十三步逐步注释）、Codec.cpp（编码布局逐字段＋parse 四步校验链＋CR-02 边界行注释）、CanonicalModelFixture.hpp（确定性种子来源声明＋单位标注）、Errors.hpp 增量段（标签构造 why 注释）。§2.5 高危信息表七类无缺项。

### 4.8 文档同步＋findings 核对 —— PASS

- runtime.md v0.5 六项登记逐项与实现比对一致：①层级索引未随本任务落地（CanonicalModel.hpp 仅有 m_objectIndex/m_resourceIndex 两索引，§3.1/§12 无独立任务行——首个消费者原则）✓；②isHardErrorDiagCode＝十段链硬失败码经 registryCode（CanonicalModel.cpp L321-334）✓；③Sylvester 顺序主子式正定复核（L298-305）✓；④parse 三重防御（builder 复核＋身份比对＋能力比对，Codec.cpp parse 与 L1097）✓；⑤hasDynamicWorkCell＝全连杆物性 Provided（deriveRuntimeCapability L389）✓；⑥Expected 工厂标签构造（Errors.hpp diff，公共签名不变）✓。任务卡零未登记偏差；`validate-task.ps1 -TaskFile tasks/foundation/RT-T04.json` 验收者亲手复跑 **PASS（exit 0）**。
- **findings.json 未关闭条目相关性核对**：F-019（testdata_lint→testkit 门禁边）直接影响本任务门禁基线——验收者实测命中同 2 项（IRD-GATE-SUB/T1），与本任务新增文件无关；F-008 影响全树从零构建口径（本记录 4.2 已如实采用目标面口径）；F-034~F-038（实施纪律/表述强度家族）与 4.9 核查相关（G-1 为其延续）；其余 open 条目（F-004/005/006/007/009/010/012/015~018/020/023~027/029~033）辖域与 runtime/CanonicalModel 无交集，无可随本任务顺手消账项，无静默积压。

### 4.9 commit 质量 —— PASS（G-1 建议级）

- 97d8844 `[WP-06-T04] 实现 CanonicalModel 与 RT-Codec 编码器（§4.3/§4.5）`——动词开头✓；四段齐备✓；验证段（105/105、门禁 2 项既有命中、过程缺陷修复叙述）与留痕及验收者复现一致✓。
- c4b463d `[WP-06-T04] 验证留痕：双模式测试报告＋失败能力探针证据`——动词开头✓；四段齐备✓；11 份留痕清点属实✓。
- 92b164f `[units] runtime v0.5：RT-T04 实现落位登记（层级索引分阶段＋三处实现层选型）`——四段齐备、内容属实，但前缀后以名词短语"runtime v0.5"开头，**F-037（RT-T03）同型第 9 次复现**（F-012 家族）；纯风格瑕疵不构成阻断→转登 **F-039**。
- 无粉饰：实施侧声称的每个可复现数字（105/105、探针 104/105→105/105、门禁命中 2 项既有）均被验收者独立复现证实。

### 4.10 留痕完整 —— PASS

- `traceability/builds/wp06-t04/` 11 件齐备：双模式 gtest XML（tests="105" failures="0"）、integration-build/integration-test-run/smoke-configure/smoke-build/smoke-test-run/probe-build/probe-test-run/ird-gates-run 日志、ird-test-report.json（ird-test-report/1 格式：105 用例逐条含需求/卡章节追溯、gates 摘要、summary 含 newCases=32/regressionCases=73 与 RT-ID 切面边界声明）。
- phase-one-validation.log RT-T04 行在位（L42，validate-task PASS）；resultNote 义务由 ird-test-report.json 承载（targets 双模式结果＋modeMechanics）。
- 验收者侧独立复现产物（本机临时目录）：双模式全新构建日志、双模式 XML、双探针 build/run 日志、门禁日志（扫描根＝worktree A，实证非主树增量）。

### 4.11 偷懒/缩水实现扫描 —— PASS

- ①标记扫描：TODO|FIXME|stub|placeholder|未实现|not implemented 在 7 个新增文件**零命中**（无契约豁免条款需要列出）。
- ②空实现检测：无——build() 十三步校验（值级不变量逐条拦截）、encodeImpl 全字段确定性编码、parse 四步防御链、deriveRuntimeCapability 逐位派生、operator== 全字段位模式比较，全部实质实现。
- ③逐条证据表：见 4.4——acceptance 4 条均有具名测试＋XML＋实现行号，任一条不缺。
- ④测试强度：builder 反例逐条断言稳定码＋detail 中文子串定位；负路径（截断/尾随/篡改/拼接身份）逐类拒绝；恒真风险由 4.5 双探针（注入即红）实证排除。

---

## 问题清单

| 级别 | 编号 | 描述 | 处置 |
| --- | --- | --- | --- |
| 阻断 | 无 | — | — |
| 建议 | G-1→**F-039** | 送验文档提交 92b164f 标题 `[units] runtime v0.5：…` 前缀后以名词短语开头，非动词开头——F-037（RT-T03，同前缀形态）同型第 9 次复现（F-012 家族：F-012/F-023/F-024/F-026/F-027/F-030/F-034/F-037→本例）。正文四段齐备、验证结论与留痕一致（验收者逐字复现），纯风格瑕疵。已转登 findings.json（本 evidence 分支同步提交） | F-012 家族合并处置（实施者模板补动词开头示例）；已推送历史不追溯改写（AGENTS §6.3） |

## 裁决

**verdict＝pass**：4.1~4.11 全部通过，无阻断级问题；建议级 G-1 已转登 findings.json（F-039，open），不阻塞合入。验收对象为冻结 SHA 92b164fe6dc8c7b1e4d27addccf9025cf1cdeef6；合入决策归所有者（PIPE §4.7）。

## 附：验收者环境说明

- 复现现场：worktree A＝detached @ 92b164f（配置/构建/测试/门禁/双探针）；worktree B＝acc/RT-T04/1 @ origin/redesign-main（本记录）。
- 环境前置：ini 模板 untracked 文件按 F-007/F-009 先例自主树复制（跟踪树零改动）。
- 冒烟二进制假红事件已按显式核验构建退出码口径处置并留痕（4.5 教训注记）；最终结论全部基于还原后重建的双模式全量复绿运行。
