# UI-T03 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | UI-T03 工作台壳与五区布局（WorkbenchShell/布局记忆/无项目首页/状态栏）——契约 `tasks/foundation/UI-T03.json` |
| 送验对象（冻结） | `dd9ff626392813f262f68d0d8dacc2cfb22bd4de`（分支 `wp10-t03` 尖端）；diff 范围＝`efed7256c618c583ede69493b2202ceb05ff0494..dd9ff626`（state.base..headSha，merge-base 复核＝origin/redesign-main 尖端 efed7256） |
| 分支漂移检测 | `git rev-parse origin/wp10-t03`＝dd9ff626…＝送验 SHA＝本地 wp10-t03——零漂移（ACC §3） |
| 验收尝试 | attempt＝1（state.attempts.accept=1；evidence 分支 `acc/UI-T03/1`） |
| **verdict** | **fail（1 项阻断级 B-1：4.10 留痕不完整——构建/运行日志被 `.gitignore` 吞、未随冻结对象入库）** |
| 验收环境 | 复现 worktree A＝`git worktree add --detach` @ dd9ff626（全新从零）；集成 build 树全新配置：`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=<主仓>vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64 -DCMAKE_CXX_FLAGS="-DNOMINMAX -DWIN32_LEAN_AND_MEAN -DWIN32"`（配置 exit 0，缓存 grep `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON` 实证；配置前自主复制两件 git 未追踪 ini 辅助件 `RobWorkStudio.ini.template.static/.shared.in`——`.gitignore:79 [Bb]in/` 吞没的环境件、框架 configure_file 硬依赖，F-007/F-009 既有登记环境链，非送验对象缺陷）；冒烟 build 树＝worktree A 内独立目录 `build-smoke`（vcpkg toolchain＋Qt 前缀——F-254 既有登记同款，本次亲历）；GUI 测试运行方环境＝`QT_QPA_PLATFORM=windows`＋`QT_QPA_PLATFORM_PLUGIN_PATH=<Qt>/plugins/platforms`（ui.md §12.2 纪律——环境由运行方供给）；记录回传 worktree B＝`git worktree add -b acc/UI-T03/1` @ origin/redesign-main；pwsh 7（validate-task/validate-docs 于 worktree A 上下文亲跑）；验收者全程只消费产物、契约、设计文档与在库留痕，未读取实施者过程叙述，结论全部出自亲手重跑 |

---

## 一、逐项证据（ACC §4，4.1～4.11）

### 4.1 改动范围 —— 通过

- 范围内变更 22 文件（`git diff --name-status efed7256..dd9ff626`）：11 件在 `industrialrobot/ui/**`（allowedFiles 第 1 项）、10 件在 `traceability/**`、1 件 `units/ui.md`——全部落在契约 allowedFiles 三模式内。
- forbiddenFiles（REQUIREMENTS.md/ARCHITECTURE.md）零触碰（不在变更清单）。
- 框架零源码修改（SA-02）：`git diff --name-only` 对 `RobWork/src`、`RobWork/libs`、`RobWork/RobWorkStudio/src/rwslibs`（剔除 industrialrobot/ui）＝空集；`industrialrobot/patches/` 在范围内零 diff（无新增登记＝无框架改动，自洽）。

### 4.2 可复现构建 —— 通过（两模式全新树从零，零错误）

- **集成模式**（worktree A 内新建 `build/`，见环境段重配参数）：`cmake --build build --config Release` **全量退出码 0、`: error/: fatal error` 计数 0**（RobWork 全库→RobWorkSim→RobWorkStudio→industrialrobot 全单元→ui 三测试目标全新编译；产物 `build/RobWorkStudio/bin/Release/sdurws_ird_ui{,_test,_contract_test,_gui_test}.exe/.lib` 全部生成）。验收者亲历两次首配失败（缺 Boost＝裸 AGENTS §4.1 字面命令在全新树的既有环境链 F-098/F-099；缺 ini 模板件）均为主仓环境链、补齐后从零成功——非送验对象缺陷。
- **独立冒烟模式**（worktree A 内 `build-smoke/`，源＝worktree A 的 industrialrobot，vcpkg toolchain＋Qt 前缀）：配置＋全量构建 **exit 0、错误计数 0**；`ui/Release/` 下 `sdurws_ird_ui.lib`＋三测试 exe＋运行期 DLL（Qt6Core/Gui/Widgets、gtest.dll）就位。

### 4.3 契约 verify 命令 —— 通过（逐条独立亲跑）

| 契约 verify | 结果 |
| --- | --- |
| `cmake --build build --config Release --target sdurws_ird_ui_test` | exit 0（sdurws_ird_ui.lib＋ui_test.exe 链接产出） |
| `cmake --build build --config Release --target sdurws_ird_ui_gui_test` | exit 0（gui_test.exe 链接产出） |

### 4.4 acceptance 条目逐项核对 —— 通过（全部有验收者亲跑证据）

| # | acceptance 要点 | 验收者亲跑证据 | 结论 |
| --- | --- | --- | --- |
| 1 | UI-WB-1/2/3、UI-SES-1 五区 Dock/跨会话布局记忆/view.resetLayout/损坏回退＋UI-LAYOUT-RESTORE-FAILED（Dev，不阻塞启动）/无项目首页三入口与项目作用域命令禁用（命令面板仍可达）——经 ctest 留痕 | 集成树直跑 gui_test **6/6 passed**（`FiveZoneLayout_PersistsAcrossSessions_UI_WB_1`、`ResetLayout_RestoresFactory_UI_WB_2`、`CorruptLayoutMemory_FallsBackWithDevDiagnostic_UI_WB_3`、`NoProjectHomepage_CommandGating_UI_SES_1`＋2 落位自证例逐个 OK）；ctest 标签亲跑：`ctest -C Release -L "^ird$"`＝**2/2 passed**、`-L "^ird_gui$"`＝**1/1 passed**（ui 测试注册目录下，与在库 ctest 声明一致）；UI-WB-1 断言跨会话可见性矩阵/浮动位形/几何（±8 px 容差）还原＋用户级 ini 存在＋`.rwdesign` 三重零触碰；UI-WB-3 断言 Dev 码经 devLog 出线＋IDiagnosticSink 零命中（Dev 不入目录）＋layout 组三键整段丢弃＋initialize true（不阻塞） | 通过 |
| 2 | 状态栏格式（PM-11）`<显示名>[*][（只读）]`——显示名取投影、*＝present∨会话脏、（只读）＝writable=false；布局状态与最近项目（PM-14）持久化用户级、绝不写入 .rwdesign | 模型层 `ShellModel.StatusBarFormat_PM11_Matrix_UI_T03_ACC2`（8/8 内）六行格式矩阵逐行断言（无项目/纯显示名/磁盘草稿*/会话脏*/*/次序/*（只读））；GUI 端到端（UI-SES-1 内）状态栏 QLabel＋windowTitle 同值三态翻转＋只读徽标；PM-14：`RecentProjects_CapDedupPersist_PM_10_PM_14`（上限 10 裁尾/去重置顶/规范化同键/失效保留 available=false/移除幂等/跨实例读回）＋UI-WB-1 的用户级 ini 存在与 .rwdesign 零写入断言 | 通过 |
| 3 | 最小可用布局（§4.4）三要素恒在＋低于最小尺寸侧栏折叠；WorkbenchShell 装配门面按 §10.1（initialize 恰好一次、mainWindow 唯一 Widget 出口、有界 shutdown） | 三要素恒在＝`regionVisible(Top/Central)` 恒 true 断言（UI-WB-1/2/3 多处）＋`setRegionVisible` 对 Top/Central 无操作（源码行号：`src/WorkbenchShell.cpp:522-525`，`userVisibilityFlag` 对 Top/Central 返回 nullptr）；尺寸折叠实现＝`src/WorkbenchShell.cpp:551-569`（1280×720 阈值，常量来源登记 `src/WorkbenchShell_p.hpp:77-80`）＋resize 钩子（:167）——**折叠行为无自动化用例**（§13 设计矩阵 UI-T03 行未含折叠例），见建议级 S-3/F-262；门面契约＝initialize 恰好一次/空 wiring 拒绝（`WorkbenchShell.cpp:80-89`）、mainWindow 唯一出口（:835-840，shutdown 后 nullptr :141-142）、有界 shutdown＝值序列化＋drainAndStop join（:136-137，`ShellSupport.cpp:221-238` 有界收口）——UI-WB-1/2/3 全程行使 initialize/shutdown 通路 | 通过（附建议 S-3） |
| 4 | O-31 处置：ShellWiring 持 ui 自有端口（IPolicySummarySource/IUiNameResolver）；零对 project/evidence/execution/policy/runtime 链接或 include（NoCrossUnitInclude_O31_UI_BUILD 守卫常驻）；P-UI-5 维持 CF-1 | 静态 grep：ui 产品面（src/＋include/）`sdurws/ird/` include 仅 core（Events/Identity）＋diagnostics（Catalog/DiagCodes/Redaction）＋ui 自有头（uniq 计数清单亲跑留档）；`UiLinkage.ArchEdgesCoreDiagnosticsOnly_UI_CTR` 等 contract_test **3/3 passed**（17 个越界目标逐一反证＋产品库零 testkit＋三目标标签登记）；`UiBuild.NoCrossUnitInclude_O31_UI_BUILD`（8/8 内，运行期目录扫描 include/＋src/——新源文件自动纳入扫描面亲证）＋`NoRobWorkPrefixLiteral_R4_UI_BUILD` 全绿；端口/投影形状随 ui.md v0.5 §10/§10.1 增量修订冻结登记（见 4.8）；ird_gates 机器面 3 处 R3 命中＝R-3 例外头包含半区机器面滞后，登记提交件 `traceability/wp10-t03-gate-registrations.md`＋findings F-259（F-227 同口径：人工登记册 DTB §4.5 R-3 行＋NFR-MNT-01 已登记例外本体，不构成违例），验收者复核 `gates-hit-set-diff.txt`：diff 恰为 3 行 R3（ShellSupport.cpp/WorkbenchShell_p.hpp/WorkbenchShell.cpp）、既有命中零新增零移除 | 通过 |

### 4.5 测试真实失败能力 —— 通过（三例注入即红；一例存活转建议级）

在 worktree A 逐次变异→亲跑→`git checkout --` 还原→复跑复绿（每轮后 `git status --porcelain` 确认还原洁净）：

| 变异 | 注入点 | 预期红用例 | 实测 |
| --- | --- | --- | --- |
| M1 | `UiProjections.hpp:204` PM-11 格式函数 `*` 追加置死（`if (false && ...)`） | ShellModel.StatusBarFormat_PM11_Matrix_UI_T03_ACC2 | **FAILED（1/8 红）**，还原后 8/8 复绿 |
| M2 | `ShellSupport.cpp:100` 命令板无项目分支置死（`if (false)`） | 预期 UI-SES-1 红 | **存活（6/6 仍绿）**——用例只断言 `enabled=false`＋reasonKey 非空，不断言 reasonKey 取值，且未覆盖 Project＋readOnlyAllowed=true（workbench.closeProject）的无项目态；该缺陷方向安全（变异后写命令仍禁用、仅禁用原因键变化），转建议级 S-2/F-260 |
| M2b | `ShellSupport.cpp:102` 无项目但 `enabled=true`（直接命中断言的等价注入） | UI-SES-1 门控断言 | **FAILED（1/6 红）**，还原后 6/6 复绿 |
| M3 | `ShellSupport.cpp:323` LayoutMemory::discard 置空（损坏段不丢弃） | UI-WB-3 整段丢弃断言 | **FAILED（1/6 红）**，还原后 6/6 复绿 |

结论：断言校验真实行为与边界（非恒真），测试会红能力由三例红实证；M2 存活暴露的盲区不影响"会红"判定、按 ACC §5 转登跟踪。

### 4.6 架构红线（AGENTS §5 八条）—— 通过

1. **L2 计算内核零 Qt**：范围内零 L2 文件触碰（4.1 清单）；Qt 头仅出现在 ui 单元（R-3 唯一例外单元，NFR-MNT-01）。
2. **业务域互链禁止（R-1）**：产品面 include 清单仅 core/diagnostics/ui 自有（uniq 计数亲跑）；链接面 contract_test 3/3 亲跑（17 越界目标反证全绿）。
3. **跨单元私有头禁止（R-2）**：全部 include 走 `sdurws/ird/<unit>` 公共路径；`WorkbenchShell_p.hpp` 留在 src/ 未外泄（测试亦未跨目标共享设施——各 TU 自持）。
4. **框架零源码修改（SA-02）**：4.1 已证（框架文件空集＋patches/ 零 diff）。
5. **不做名称前缀拼接/剥离（R-4）**：`grep -rn "RobWork" ui/src ui/include` 零命中；`NoRobWorkPrefixLiteral_R4_UI_BUILD` 用例全绿；名称解析经 IUiNameResolver 端口（注释明示"不得自行拼接/截断"）。
6. **权威唯一（PA-1）**：PM-11 格式唯一权威＝`formatProjectStatusText` 纯函数（ui.md §2.2 PM-11 主责 ui）；名称语义归 runtime 端口、策略只读面经 ui 自有端口；ui 未越权实现他单元语义（命令处理器显式归位 UI-T06/T11/T12/workflow——§14.1 交接面）。
7. **不可变历史（PA-2）**：本任务零修订/写路径触碰（纯界面壳），不适用即无违例。
8. **不恢复 old/**：`grep -rn "old/"` 产品面零命中；全部实现按 ui.md 从零编写。

ird_gates：验收者复核实施端基线隔离证据（`gates-hit-set-diff.txt`＝`3a4,6` 三行 R3 新增、`gates-hits-base-skel.txt` 29 行既有命中零移除）；3 处 R3 属 R-3 例外头包含半区的机器面滞后（人工登记册已登记例外本体），已按 wp10-t02 先例以登记提交件＋F-259 转登 WP-01-T03——不构成红线命中，维持建议级跟踪（F-227 同口径）。

### 4.7 注释规范（AGENTS §2）—— 通过

- **公共头全查（3/3）**：`IWorkbenchShell.hpp`（73 行文件头＋逐类逐函数中文 Doxygen：五区词表/命令作用域词表/ShellWiring 成员逐个语义与可空性/ShellTeardownReport 三字段/线程模型"initialize/shutdown 允许装配线程、其余 UI 线程"）、`UiPorts.hpp`（为什么最小端口不 include 对端的架构动机、resolveObjectId 的 optional 折叠理由、R-4 禁令）、`UiProjections.hpp`（逐字段语义锚点逐个标注对端权威、双脏位理由、PM-11 唯一权威纯函数逐段注释）——设计依据/需求号/背景动机齐备，§2.5 高危信息（线程约束、所有权"L5 独占持有/壳只持共享引用"、值语义快照、错误语义"返回值轨不抛"）逐项在位。
- **实现文件抽查（≥3 全查：WorkbenchShell.cpp 870 行/ShellSupport.cpp 433 行/WorkbenchShell_p.hpp 471 行，另 CMakeLists.txt 322 行与两测试文件抽读）**：文件头四段齐备；每语义段落中文注释说"为什么"（装配序①~⑤、折叠不覆盖用户意愿、Dev 码走日志不入目录、weakly_canonical 对不存在路径的词法规范化理由、写线程"值任务禁触 Widget"纪律等）；魔法数字全部在 `_p.hpp` 常量区登记来源（1280×720/240/280/160/10 均注明 §4.4/PM-10 原文行）；无复述式/机翻腔/过期注释。
- 测试文件：用例头注释逐条标注契约 acceptance 编号＋ui.md §12.3 用例行对应＋IRD_TEST_INFO 追溯字段。

### 4.8 文档同步 —— 通过

- 单元卡增量修订：`units/ui.md` v0.4→v0.5（§10 章首 O-31 注 v0.5 段＋§10.1 v0.5 增量冻结五条＋ShellWiring 代码块与实现逐字对齐＋§16.7 v0.5 变更行）——devLog 成员增量、门面 facets、端口/投影形状冻结、占位文本契约显式口径、零 Q_OBJECT 不开 AUTOMOC、门禁状态全部按 DTB §5.4 机制登记；验收者对照实现文件逐条核对无"代码先行、文档失实"。
- validate-task（worktree A 亲跑）＝**PASS (1 tasks)**；validate-docs（worktree A 亲跑）＝**PASS (20 units, 12 trace entries, 150 task files)**——与提交声明逐字一致。
- 任务状态/追踪矩阵：契约状态翻转与 history 登记归收尾（PIPE §4.7⑤）——不要求随送验对象携带（先例同）。
- **findings.json 未关闭条目相关性核对（open 225 条）**：
  - **F-259**（本任务实施端自登，3 处 R3 机器面滞后）：与本任务直接相关，验收者独立复核 diff 证据成立、处置链（WP-01-T03 回填）正确，维持 open；
  - **F-227**（wp10-t02 同型 R3 家族）：本任务 3 命中为其同口径延续，非静默积压（已随 F-259/登记提交件显式归因）；
  - **F-007/F-008/F-009/F-254**（全新树配置环境链族）：验收者本次全部亲历复现（ini 模板缺件/Boost 缺省/冒烟 Qt 前缀），与本任务零代码关联、处置权在框架/构建约定所有者，无可随手消账项；
  - **F-010**（`.gitignore *.log` 吞留痕——"后续任务留痕仍会踩中"的预言）：**本任务再次踩中且构成本次唯一阻断项 B-1**（见 4.10）——非静默积压（本次转登为阻断处置＋返工指令），F-010 本身维持 open（负向豁免规则归构建约定所有者）；
  - **F-201**（phase-one-validation.log/resultNote 条文载体脱节）：按既有登记口径以 `traceability/builds/wp10-t03/` 留痕目录为 4.10 实质载体；
  - 其余 open 条目属其他单元/横切面，与本任务无相关性。

### 4.9 commit 质量 —— 通过（附建议级 S-2）

- `d402cfef` `[WP-10-T03] 实现工作台壳与五区布局（…）`——动词开头合规；正文四段（What/Why/验证/影响面）齐备，Acc-Covered 尾注与仓库惯例一致；验证段声称（集成/冒烟零错误、8/8＋6/6＋3/3、ctest 3/3、门禁 29↔32 diff 3 处 R3）经验收者独立复跑**全部为真**。
- `dd9ff626` `[WP-10-T03] ui 单元卡 v0.5 增量修订与门禁登记提交件（…）`——正文四段齐备，验证段声称（validate 双 PASS 数值、findings 259 条解析、门禁基线隔离）经亲跑全部为真；**标题前缀后为名词短语『ui 单元卡』开头，不合 ACC §4.9/AGENTS §6.3『动词开头』字面**（F-012/F-232~F-242/F-247/F-258 家族延续，建议级转登 F-261）；另验证段"（日志见 traceability/builds/wp10-t03/）"与第二提交 What 段把日志列为该目录内容——与实际入库清单不符（见 B-1，同一症状家族，随 B-1 返工）。

### 4.10 留痕完整 —— **不通过（阻断级 B-1）**

- 在库留痕（冻结对象 dd9ff626 内 `traceability/builds/wp10-t03/` 9 件，逐件在位亲验）：gtest XML×3（`tests` 8/6/3、`failures="0" errors="0"`）＋ird-test-report.json×3（summary total 8/6/3、passed 全额、decisive=true）＋门禁骨架命中集×2＋diff×1——计数与提交声明一致。
- 实施端本地同目录实有 **23 件**（验收者于实施工作树旁证清点），其中 **14 份 .log 未入库**：ctest.log、gates-base-scriptmode.log、gates-branch-run.log、gates-branch-run.utf8.log、gates-branch-scriptmode.log、integrated-build-ui_test.log、integrated-build-ui_gui_test.log、sdurws_ird_ui_test.console.log、sdurws_ird_ui_contract_test.console.log、sdurws_ird_ui_gui_test.console.log、smoke-configure.log、smoke-build.log、validate-task.log、validate-docs.log——`git check-ignore -v` 实证被根 `.gitignore:87` 的 `*.log` 全局规则吞没（`git add` 静默跳过），冻结对象上 `git ls-tree` 仅 9 件。
- **判级依据**：AGENTS §4.2"测试必须真实执行并留痕（gtest XML＋ird-test-report.json＋**构建日志**）"的证据链在冻结对象上断裂；ACC §4.10"构建日志落位"不成立；**CORE-T02-20260910-r1 阻断级 B-1 同款同因**（该例返工＝`git add -f` 证据补交、r2 复验通过），且 wp10-t02（25 件含全部日志）等近期同单元先例均完整入库——本例为纪律回退而非惯例空白。已提交的门禁登记件正文引用 `ctest.log`/`smoke-configure.log`/`smoke-build.log` 为证据路径，冻结对象上不可达，加重留痕断裂。
- 注：本验收各结论均已由验收者亲手复现（4.2~4.5/4.8），B-1 不动摇"测试真实通过"的技术事实，动摇的是**证据链的仓库内可追溯性**——故为阻断级返工项而非粉饰指控（实施端留痕内容真实存在，仅入库缺失）。
- `phase-one-validation.log`/resultNote：按 F-201 既有登记以 builds 目录留痕为实质载体（4.8）。

### 4.11 偷懒/缩水实现扫描 —— 通过

1. **字面扫描**：diff 范围 `TODO|FIXME|stub|placeholder|未实现|not implemented`（新增行）——TODO/FIXME/stub/未实现/not implemented 零命中；`placeholder` 命中全部为 `kStagePlaceholder`/`stagePlaceholder` 标识符族＝契约 §4.1/§11.4 显式设计的阶段 A 占位说明（"本阶段将在后续版本提供"，ui.md v0.5 §16.7⑤ 登记为"契约显式设计，非未完成实现"）——豁免依据逐条列出如上，无表外豁免。
2. **空实现检测**：公共接口 11 方法全部有实质实现（无空体/仅返回默认值者；`setRegionVisible` 对 Top/Central 的无操作为 §4.4"三要素恒在"的契约语义并经注释与用例双向钉住）；未接线命令（project.new/open、draft.*、undo/redo、closeProject、commandPalette）触发时以状态栏占位说明反馈＝登记提交件与单元卡明文的阶段 A 形态（处理器归位 workflow/UI-T06/T11/T12），登记与门控与路由收口完整——非空壳。
3. **逐条证据表**：见 4.4——契约 acceptance 四条逐项注明具名测试/命令输出/文件行号，任一条无证据即 fail 的口径下无缺口。
4. **测试强度**：断言校验行为与边界（格式矩阵逐行、上限/去重/失效/置顶、三键整段丢弃、Dev 双通道出线）；非恒真、非"只编译通过"（4.5 三例注入即红实证）；M2 存活盲区转 S-2/F-260。

---

## 二、问题清单（分级）

### 阻断级（1 项——verdict=fail 的唯一依据）

| # | 问题 | 证据 | 处置指令（返工输入） |
| --- | --- | --- | --- |
| B-1 | **4.10 留痕不完整**：14 份构建/测试/门禁/校验日志在实施端本地存在但未随冻结对象 dd9ff626 入库——`.gitignore:87 *.log` 吞没，`git add` 静默跳过；提交信息声称日志在 `traceability/builds/wp10-t03/` 与实际入库 9 件不符；已提交登记件引用的 ctest.log/smoke-*.log 在冻结对象上不可达 | `git ls-tree -r dd9ff626 -- builds/wp10-t03/`＝9 件；`git check-ignore -v` 命中 `.gitignore:87`；本地 23 件清点（本记录 4.10 节）；先例＝CORE-T02-20260910-r1 阻断 B-1、wp09-t03/wp10-t02 `add -f` 入库先例 | 在 `wp10-t03` 追加**纯留痕补交提交**（零代码改动）：对上述 14 份 .log 逐份 `git add -f` 强制纳入（allowedFiles＝traceability/** 内，不越权改 .gitignore——例外规则归 F-010 所有者）；提交信息如实说明"补齐验证留痕日志（.gitignore *.log 例外入库——wp09-t03/wp10-t02 同先例）＋无新增执行"；推送后以新 headSha 重新发起验收（全量重跑，attempt 2，记录文件名 `-r2`）。已推送历史不得 amend（AGENTS §6.3），提交信息与实际入库的偏差随 B-1 补交一并留痕 |

### 建议级（3 项——全部转登 findings.json，不阻塞合入）

| # | 问题 | 转登编号 |
| --- | --- | --- |
| S-1 | 提交 dd9ff626 标题前缀后名词短语『ui 单元卡』开头（动词开头字面不合，家族第 N 例） | F-261 |
| S-2 | UI-SES-1 门控断言盲区（4.5 变异 M2 存活实证）：不校验 reasonKey 具体取值（no-project/readonly 不区分）、未覆盖 Project＋readOnlyAllowed=true 命令（workbench.closeProject）无项目态禁用 | F-260 |
| S-3 | §4.4"低于最小尺寸时侧栏折叠"有实现（WorkbenchShell.cpp:551-569）无自动化用例，契约 acceptance 3 该分句仅有实现行号证据（§13 设计矩阵 UI-T03 行亦未含折叠例） | F-262 |

---

## 三、裁决

**verdict＝fail**。8/11 项通过且证据充分（4.1/4.2/4.3/4.4/4.5/4.6/4.7/4.8/4.11），1 项附建议（4.9），1 项阻断（4.10 B-1）。按 ACC §5，阻断级问题存在即 fail；fail 记录即返工输入，实施者按 B-1 处置指令修复后重新发起全量验收。技术面（构建/测试/红线/文档同步）全部独立复现为真——本次 fail 不否定实现质量，仅阻断于证据链入库纪律。

验收者未合入、未修改实施产物、未向任务分支 `wp10-t03` 推送任何提交（送验对象冻结于 dd9ff626）。
