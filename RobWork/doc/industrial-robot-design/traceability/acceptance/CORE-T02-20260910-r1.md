# CORE-T02 验收记录（对抗式验收）——attempt 1：fail

| 字段 | 值 |
| --- | --- |
| 任务 ID | CORE-T02（≙WP-03-T02，身份与摘要 Identity/Digest/SHA-256） |
| 契约 | tasks/foundation/CORE-T02.json（status: ready） |
| 验收对象 | 分支 `wp03-t02`，head＝`29592f2b664e796ec5f8fbdc42f1075ebe1deba3`（与 origin 一致——漂移检测 PASS），范围 `aae0e4b..29592f2` |
| 尝试序号 | attempt=1（evidence 分支 `acc/CORE-T02/1`） |
| **verdict** | **fail（阻断级×1）**——4.10 留痕不完整：4 份构建/测试日志未进入冻结对象 |
| 验收日期 | 2026-09-10 |
| 独立性声明 | PIPE v1.7 §0.4 单会话降级（同会话顺序执行）；补偿＝worktree A（detached @ head）亲手复现＋探针，不以实施自述为据 |

---

## 复现结果（验收者亲手，worktree A）

- validate-task 三查 PASS；worktree 集成树全新配置（toolchain＋Qt＋ini 模板补齐）configure exit 0、缓存 :BOOL=ON；
- `sdurws_ird_core_test` 从零构建 exit 0；测试 **17/17 PASSED**（UT-ID-T 六用例＋UT-ID-D 七用例＋既有红线四用例）；
- 4.5 探针：错误消息前缀篡改（core/identity/parse→core/id/parse）后 IdentityStrictParse 如期变红，恢复后复绿——测试具备真实失败能力；
- 4.1 范围 12 文件全在 allowedFiles；4.11 偷懒扫描零命中；4.6/4.7 注释与红线核查通过（实现与 §4.1/§4.2/§5.1/§5.2 逐条一致；Errors.hpp 首消费者最小契约已登记 core.md v0.4）。

## 阻断级问题（fail 依据）

| # | 问题 | 证据 |
| --- | --- | --- |
| B-1 | **4.10 留痕不完整**：`git ls-tree 29592f2 -- builds/wp03-t02/` 仅含 ird-test-report.json＋XML×2；4 份日志（integration-build.log、integration-fullbuild.log、integration-test-run.log、smoke-configure.log、smoke-build.log）在本地存在但**未入库**——`git check-ignore` 实证被根 `.gitignore:87` 的 `*.log` 全局规则吞没（`git add builds/wp03-t02/` 静默跳过）。AGENTS §4.2"测试必须真实执行并留痕（…＋构建日志）"的证据链在冻结对象上断裂。根因＝findings 已登记的 F-009（.gitignore 吞 traceability/**），实施段未用 `-f` 强制纳入 | ls-tree 输出＋check-ignore 命中行 |

## 处置（返工输入）

- 修复方式（约束在 allowedFiles 内）：`git add -f` 强制纳入 5 份日志（.gitignore 例外规则的治理修复归 F-009 所有者，**本任务不得越权改 .gitignore**）；
- 其余全部合格项（范围/构建/测试/红线/注释/偷懒扫描）在本 attempt 已复核通过——返工仅补证据提交，无需改代码；
- 补交后重新全量验收（attempt=2，evidence 分支 acc/CORE-T02/2）。

## 环境与复现说明

- worktree A 复现后已清理探针（前缀篡改还原，工作树仅余 gitignore 未涉文件）。
- 另观察：findings.json 现存两条 F-009（编号冲突——所有者侧新登记 .gitignore 条目与 WP-01-T02 的 gate-all 条目重号），建议收尾治理提交将新条重编号 F-010（数据修复，不改语义）。
