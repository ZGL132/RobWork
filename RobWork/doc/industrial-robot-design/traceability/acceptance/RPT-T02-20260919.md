# RPT-T02 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | RPT-T02——实现错误与身份类型（Errors/Identity＋RPT-\* 码清单登记） |
| 裁决 | **pass**（4.1~4.11 全部通过；无阻断级问题；建议级问题 1 条已转登 findings.json F-232） |
| 送验对象（冻结） | `f77fc436eca1a1cad405931f75d1fee84e39b856`（分支 wp12-t02 尖端；验收开始前实测 `origin/wp12-t02` == 该 SHA，**无分支漂移**） |
| diff 范围 | `state.base` `612070b167d23a46cc5c9c5654cea10b57227915`..`f77fc436eca1a1cad405931f75d1fee84e39b856`（2 个提交：96777523 实现＋f77fc436 留痕） |
| 验收日期 | 2026-09-19 |
| 验收者 | 独立上下文会话（ACC §2 四条独立性要求适用：未消费实施叙述；全部构建/测试/门禁/治理脚本亲手复跑） |
| 复现现场 | `git worktree add --detach <临时目录A> f77fc436`（detached @SHA）；集成模式全新重新配置；冒烟另配独立临时目录 |
| 记录回传 | 分支 `acc/RPT-T02/1`（自 `origin/redesign-main` 612070b1 建立），本文件＋findings F-232 同分支提交并推送 |

---

## 4.1 改动范围 —— 通过

`git diff --name-status 612070b1..f77fc436` 共 25 个变更文件，逐一对照契约 `allowedFiles`：

| 变更 | 数量 | allowedFiles 归属 |
| --- | --- | --- |
| `RobWork/RobWorkStudio/src/rwslibs/industrialrobot/reporting/**`（CMakeLists.txt 修改；Errors.hpp/Identity.hpp、src/Errors.cpp/src/Identity.cpp、test/ErrorsIdentityTest.cpp、contract_test/IdentityCrossUnitContractTest.cpp 新增） | 7 | `reporting/**` ✔ |
| `RobWork/doc/industrial-robot-design/traceability/builds/wp12-t02/*`（17 件留痕） | 17 | `traceability/**` ✔ |
| `RobWork/doc/industrial-robot-design/units/reporting.md`（v0.4→v0.5） | 1 | 单元卡 ✔ |

- `forbiddenFiles`（REQUIREMENTS.md / ARCHITECTURE.md）：**零触碰**（diff 无此二文件）。
- 无未登记框架源码改动（SA-02）：diff 无 `RobWork/`（框架）、`RobWorkSim/`、`RobWorkStudio/` 其他源码；`patches/` 无新文件。
- 契约文件本身（tasks/foundation/RPT-T02.json）未被改动（状态翻转归合入收尾，正确）。

## 4.2 可复现构建 —— 通过（附环境注记）

**集成模式**（worktree A 全新构建树，`-S <A>/RobWork -B <A>/RobWork/build`，VS2022 x64）：

- 重新配置成功，缓存实测 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`（AGENTS §4.1 grep 口径）。
- 构建日志：验收者 `acc-verify-1.log` / `acc-verify-2.log`（临时目录，结论如下）——两目标均 exit 0、`: error`/`: fatal error` 计数 0；产物 `build/RobWorkStudio/bin/Release/sdurws_ird_reporting_test.exe` 与 `sdurws_ird_reporting_contract_test.exe` 实际生成。

**独立冒烟模式**（`<A>/acc-smoke-build`，`-S .../industrialrobot`，vcpkg toolchain＋Qt 前缀）：

- 配置成功（`acc-smoke-configure.log` 末行 Build files written）；全量 `cmake --build --config Release` exit 0、错误计数 0。
- 冒烟两测试可执行文件实测运行 14/14＋10/10 全过。

**环境注记（非本任务产物缺陷，不计入裁决）**：全新 worktree 首次配置两次失败——①`RobWorkStudio/bin/RobWorkStudio.ini.template.static` 被 `.gitignore [Bb]in/` 吞掉（配置卡 `RobWorkStudio/src/CMakeLists.txt:58`）；②冒烟/testkit 配置需 `CMAKE_PREFIX_PATH` 指向 Qt6。验收者自主树复制环境文件并补 Qt 前缀后成功；两者均为既有 **F-007**（open）登记内容，本次为又一实证。实施者留痕（smoke-configure.log）与 ird-test-report.json 的 smoke.configure 行均如实记载了 Qt 前缀参数，与复现口径一致。

## 4.3 契约 verify 命令 —— 通过

逐条独立执行（集成构建树）：

1. `cmake --build build --config Release --target sdurws_ird_reporting_test` → exit 0，零 error，exe 产出并运行 **14/14 PASSED**。
2. `cmake --build build --config Release --target sdurws_ird_reporting_contract_test` → exit 0，零 error，exe 产出并运行 **10/10 PASSED**。

## 4.4 acceptance 条目逐项 —— 通过（5/5＋ird_gates 如实执行）

| # | 契约 acceptance | 证据（验收者亲手） |
| --- | --- | --- |
| 1 | 身份往返/token/保留值/四概念类型边界 | 单测 5 条（`ReportIdRoundtripGenerateParse`／`ReportIdParseStrictRejections`〔12 反例族＋tag 隔离 obj-/cid- 互拒〕／`ReportIdReservedZeroValue`／`ReportLevelTokenSyntax`〔14 反例族〕／`ReportIdTypeBoundary_NoContentPayload`〔sizeof==16 静态断言〕）＋契约测试 3 条（`FourConceptTypeBoundary_TagIsolation`〔ReportId↔core::ContentIdentity 无互转/无隐式转换静态断言＋rpt-/cid- 运行期互拒〕／`IdempotencyKeyIsContentIdentity`／`IdentityIndependentOfPathsAndNames`）全部实测通过；Identity.hpp 与 reporting.md §4.1 身份纪律（ReportId 不承载内容/幂等判定用内容身份/身份与路径名称分离）逐条有类型与用例承载 |
| 2 | 16 token 稳定错误码＋detail 前缀＋try\* 声明 | `ErrorTokenTable_Full16MatchesDesign`（期望表为测试内独立字面，与 §3.5 清单 16 值逐字一致——验收者对照 Errors.hpp:71-119 枚举注释、Errors.cpp:63-78 switch 表、reporting.md §3.5 清单三方一致）；`ErrorWhatPrefixAndCodeAccess`（what()＝`"<token>[: <detail>]"`、前缀 `reporting/` 构造期强制拼装——Errors.cpp makeWhat 为唯一拼装点、无裸消息入口）；try\* 非抛出变体约定在头文件契约注释声明并具名落位（Errors.hpp 错误语义总纲节；Identity.hpp `tryFromCanonical`/`tryLevelFromToken`） |
| 3 | RPT-\* 码清单登记（按收编表） | diagcodes 8 常量＋`kStableCodes` 全量数组（Errors.hpp:196-218）与 reporting.md §3.5 清单及 diagnostics.md §4.6 收编表逐值逐序一致；字面量面用例 `RptStableCodeList_EightItemsMatchRegistry`＋`SuggestedDiagCodeMapping`（6/16 映射＋10 值 nullopt＋2 呈现类不入映射）；**注册表侧交叉自证** `RptCodesConsistentWithStableCodeRegistry`：`registerBuiltinCodes` 装配后逐码 find 断言 code/ownerUnit=="reporting"/severity 收编值/deprecated==false，且 `registeredCodes("reporting")` 全集恰 8 项——『引用登记非二次定义』成立（码值权威归 diagnostics，PA-1） |
| 4 | PublishedReportRecord/ReportLevel 落位 Identity.hpp | 六字段 `{reportId, contentIdentity, archiveState(Finalized), manifestDigest, artifactRelPaths, publishedAtUtc}` 与 reporting.md §7.3 原文（行 694）**逐字一致**；contentIdentity/manifestDigest 以 `core::ContentIdentity` 承载（SHA-256 唯一算法经 core ContentDigester——`DigestViaCoreDigesterByteEquality` 同载荷同身份/异载荷异身份/cid- 往返；reporting 侧零自建摘要实现）；比较用字节等值（`PublishedRecordCarriesCoreIdentity` 任一摘要字节漂移即不等）；ReportLevel B/C token level-b/level-c 与 §4.2 字段表一致 |
| 5 | 陷阱处置 P-RPT-3/P-RPT-8/P-RPT-9 | P-RPT-3：ReportId 为 reporting 自建 tag（Identity.cpp 仅 include 本单元头＋std——**零 core detail 消费**；diff 无 core 公共头改动）；P-RPT-8：登记按收编表（上述注册表交叉自证常驻）；P-RPT-9：core.md v0.1 Draft 基线声明在头注释（Identity.hpp/契约测试文件头），消费仅 core/Digest.hpp＋diagnostics/DiagCodes.hpp 两个登记边内公共头 |

**ird_gates**：实施者已实际执行（非豁免口径）——base（612070b1 detached worktree）与 branch 去重命中集各 27 项、归一化后逐行 diff 零差异（gates-hit-set-diff.txt，GATES-HIT-SET-IDENTICAL）。**验收者独立复跑** `cmake -DIRD_ROOT=<A>/industrialrobot -DIRD_ENABLE_GIT=OFF -P cmake/ird_gates.cmake`：27 项命中与留痕逐行一致（仅路径书写形式差异），命中全为既有归属（UI-T02 的 R3 例外、testdata_lint 的 T1/SUB〔F-019〕、io libzip 词表、project R4 字面量等——F-227 登记面），**零 reporting 命中、零新增**。

## 4.5 测试真实失败能力 —— 通过（两处缺陷注入均变红）

在 worktree A 冒烟树上亲手注入并重建复跑（注入后已全部回滚，`git status`/`git diff` 确认送验源码零残留，复跑复绿 14/14）：

1. **注入 A**：`src/Errors.cpp` token 表 `reporting/source-missing` → `reporting/source-missing-INJECTED` → `ErrorTokenTable_Full16MatchesDesign_RPT02_ACC2` 与 `ErrorWhatPrefixAndCodeAccess_RPT02_ACC2` **精确变红**（2 FAILED）。
2. **注入 B**：`src/Identity.cpp` tryParseReportId 增加大写 hex 宽容分支 → `ReportIdParseStrictRejections_RPT02_ACC1` **精确变红**（反例 `rpt-AAAA…` 被误接受，断言输出与预期反例逐字对应）。

结论：测试非恒真，断言钉住行为与边界（防"实现即期望"的独立期望表设计亦经核查）。

## 4.6 架构红线（AGENTS §5 八条）—— 通过

| 红线 | 证据 |
| --- | --- |
| L2 零 Qt | reporting 全部源文件 include 扫描零 Qt 头（验收者 grep 实测）；CMakeLists 配置期守卫对 Qt 链接 FATAL；测试 `NoQtInclude_DT_BUILD_NFR_MNT_01` 全绿 |
| R-1 业务域互链禁止 | 产品目标仅链 core/evidence/diagnostics/project 四条登记边（CMakeLists:130-134）；配置期守卫对表外 `sdurws_ird_*` 边 FATAL；`NoCrossUnitInclude`/`NoIoRuntimeExecutionCompileEdge` 用例全绿 |
| R-2 跨单元私有头 | 仅消费 `sdurws/ird/core/Digest.hpp` 与 `sdurws/ird/diagnostics/DiagCodes.hpp` 两个**公共头**（include/ 布局），无任何单元私有头；本单元无实现头出 include/ |
| SA-02 框架零修改 | diff 无框架文件；patches/ 无新增 |
| R-4 名称前缀拼接/剥离 | 新增源码零 RobWork 名称操作（ird_gates R4 命中仅在 project 既有处，非本任务引入） |
| PA-1 权威唯一 | RPT-\* 码值权威归 diagnostics StableCodeRegistry（报告侧仅引用登记——注册表交叉自证用例常驻）；本单元未实现 project/diagnostics/evidence 语义 |
| PA-2 不可变历史 | PublishedReportRecord 构造后无 setter、archiveState 单值枚举 Finalized（类型系统钉死）；ReportId 纯值 |
| 不恢复 old/ | 无 old/ 引用（该目录本就不存在） |

## 4.7 注释规范 —— 通过

- **公共头全查（2/2）**：Errors.hpp / Identity.hpp 均有文件头（设计依据含需求 ID＋任务卡章节＋acceptance 编号追溯、背景说明解释"为什么"、线程安全/确定性声明）；每个类型与函数有中文 Doxygen（@brief/@param 方向与单位/@return/@throws）；§2.5 高危信息齐备——单位与长度（16/32/36 字符、128/256 位）、UTC 时间点口径、所有权（调用方持有/值语义）、线程约束（thread_local 引擎并发安全、纯值只读安全）、错误语义（fail-fast vs 环境错误、DataInvalid 码面归类）、确定性来源（switch 全枚举无 default、无 locale、静态存储期字面量）。
- **实现文件（≥3 达标）**：src/Errors.cpp、src/Identity.cpp 逐函数注释＋算法分步（解析三步/字节装配对称性/生成零值重取）；test/ErrorsIdentityTest.cpp、contract_test/IdentityCrossUnitContractTest.cpp 每用例首行声明验证哪条 acceptance/需求（§2.7）；CMakeLists.txt 关键步骤中文注释块。
- 反面示例扫描：未发现复述式/过期/占位注释。

## 4.8 文档同步与 findings 核对 —— 通过

- 单元卡 reporting.md v0.4→v0.5：文档版本行、构建落位行（RPT-T02 落位事实＋证据指针 traceability/builds/wp12-t02/）、§14.4 v0.5 变更记录行、文末版本号同步——实现落位与登记一致，契约语义零变更声明与实测相符（实现与 §3.5/§4.1/§4.2/§7.3 原文逐字对齐，验收者逐处比对）。
- 契约状态维持 ready：合入收尾（PIPE §4.7⑤）翻转，符合 RPT-T01 先例，非失实。
- **findings.json 未关闭条目相关性核对**（全册 231 条、open 199 条）：与 reporting/RPT-T02 相关的未关闭条目仅 **F-066**（EV-T08 两枚 EVI-\* 码超出 evidence.md §13 枚举——evidence 侧登记问题，责任方 diagnostics 所有者；其中提及的 `RPT-CURRENTNESS-UNEVALUABLE` 恰为本任务登记码之一，本任务登记值与收编表一致、无冲突，**不构成本任务消账义务**）；**F-227**（ird_gates 既有命中归属）与本任务门禁结论直接相关——本任务 gates 日志如实归属 F-227 且验收者复现一致，无需处置。其余 open 条目（F-004~F-005、F-015~F-018、F-024~F-025 等）分属 core/testkit/io/TK-T03 等单元，与本任务无交集。无可随本任务顺手消账的条目。

## 4.9 commit 质量 —— 通过（1 条建议级，见 F-232）

- `96777523`：标题 `[RPT-T02] 实现 reporting 错误与身份类型：…`（动词开头✔）；正文四段齐备（What 按文件列要点/Why 对应 acceptance 1~5＋需求 ID/验证含双模式构建、测试 14/14＋10/10、ird_gates 27 项一致、validate 双 PASS/影响面）；**验证结论与验收者复现完全一致，无粉饰**。
- `f77fc436`：正文四段齐备，17 件留痕逐一列举与磁盘一致，"零代码改动"声明与 diff 相符。
- **建议级**：`f77fc436` 标题 `留痕与文档同步：…` 为名词短语开头，非动词开头（AGENTS §6.3/ACC §4.9 字面）——F-012/F-023/F-024 同族再现，转登 **F-232**（不阻断）。
- 提交前缀 `[RPT-T02]`（契约任务号式）沿用 RPT-T01 先例与 F-230 登记口径，判合规。

## 4.10 留痕完整 —— 通过

- gtest XML 四份（integration/smoke × test/contract）：`tests="14" failures="0"`／`tests="10" failures="0"`，时间戳 2026-09-19T16:54 与测试运行批次一致——验收者复现计数完全吻合（14/14＋10/10 两模式）。
- `ird-test-report.json`（ird-test-report/1）：三目标 build/run 结论、9 组 testSuites 逐用例 trace（每用例标注 acceptance/需求锚点）、builds.gates/governance 段、acceptanceCoverage 1~5 消账索引——作为任务级 resultNote 完整有效（phase-one-validation.log 自 RPT 批次起按单元卡/构建目录留痕实践不再逐任务追加，RPT-T01 同口径，非缺失）。
- 构建日志（integration-build.log、smoke-configure.log、smoke-build.log）、测试运行日志四份、门禁 base/branch 日志＋hit-set diff、validate-task/validate-docs 日志——全部落位 traceability/builds/wp12-t02/。
- 已知瑕疵（留档不阻断，F-231 同族）：留痕日志编码以 UTF-8 为主，本次验收核验未遇到乱码件。

## 4.11 偷懒/缩水扫描 —— 通过

1. diff 范围 `TODO|FIXME|stub|placeholder|未实现|not implemented` 扫描**零命中**（grep exit 1 实测；无契约豁免条目需引用）。
2. 空实现检测：全部公共函数有真实函数体（token/suggestedDiagCode 全枚举 switch、makeWhat 拼装、tryParse/format/generate/isValid/hash、try\* 双轨）——无空体、无仅返回默认值的占位接口。
3. acceptance 逐条证据表：见 §4.4 表格——5/5 条均有具名测试＋文件行号级证据，无一条仅凭"已实现"声明。
4. 测试强度：独立期望表防自证、24 反例族（身份 12＋级别 14＋码句法逐字符）、保留值/句法值面分离、编译期静态断言（类型边界 6 断言）＋运行期 tag 隔离、注册表全集恰 8 断言；经 §4.5 两处注入实证会红。

---

## 问题清单

| 级别 | 问题 | 处置 |
| --- | --- | --- |
| 阻断 | 无 | — |
| 建议 | G-1：提交 f77fc436 标题名词短语开头（F-012/F-023/F-024 同族再现） | 转登 findings.json **F-232**；不阻塞合入 |

## 验收者环境说明

- Windows 10.0.26200 x64；Git Bash＋pwsh；VS2022（MSVC 14.42）＋Qt 6.11.1＋vcpkg 经典模式（gtest 1.18.0 @ installed/x64-windows，与留痕声明一致）。
- 复现现场：detached worktree @ f77fc436（验收后删除）；记录分支 worktree 自 origin/redesign-main 612070b1。
- 首次配置两次失败（ini 模板被 gitignore 吞、Qt 前缀缺失）均按 F-007 既有登记口径补齐环境参数后成功——环境文件从主树复制，未改动任何送验产物；缺陷注入均在临时 worktree 内进行并已完全回滚（送验源码树 git 零残留）。
- 全部结论基于验收者亲手执行：集成重配＋双 verify 构建＋双模式测试运行（24/24）＋ird_gates 脚本复跑（27 命中逐行一致）＋validate-task/validate-docs 复跑（双 PASS）＋两处缺陷注入。
