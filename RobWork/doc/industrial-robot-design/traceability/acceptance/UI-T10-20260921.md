# 验收记录 —— UI-T10（帮助入口与关于对话框：AboutDialog 契约头/帮助菜单实条目/IUiAboutDataSource 端口）· attempt 1

| 字段 | 值 |
| --- | --- |
| 任务 | UI-T10（contract：tasks/foundation/UI-T10.json，schema ird-task/1，status=ready——契约状态未被任务侧触碰，翻转归收尾〔PIPE §4.7 口径〕；interUnit=false——纯 ui 自有组件与常量/装配报告值消费，零跨单元接口产出；requirements UX-14/NFR-DEP-05；designRefs units/ui.md §13/§11.1/§11.4；acceptance 前置 O-31 已裁决 2026-09-19 随整链放行） |
| 送验对象 | **afdfae98e14bc6b465d1de08fdf1fc69cbb96127**（40 位冻结；分支 wp10-t10；`git rev-parse origin/wp10-t10`＝该 SHA 逐字一致——**分支漂移检测零漂移**，远程比对实际完成） |
| diff 范围 | f6968386c6748fe6e7b17e88c18f6f22c00ffccd..afdfae98e14bc6b465d1de08fdf1fc69cbb96127（base＝state.base＝与 `origin/redesign-main` 尖端逐字一致＝merge-base；range 内 2 提交：61a6dbfd 实现＋测试＋ui.md v1.2、afdfae98 留痕入库；**27 个变更文件**） |
| 验收尝试 | attempt 1（state.attempts.accept 序号 1；本验收为全新上下文，只消费产物、契约、设计文档与在库留痕＋既往**验收**记录〔UI-T09 同日先例，仅取环境口径与家族判定先例〕，未读取实施者过程叙述；全部构建/测试/门禁/治理脚本/变异试验由验收者亲手执行） |
| 验收环境 | 复现 worktree A＝`git worktree add --detach acc-ui-t10/A` @ afdfae98（6208 文件检出，`git log --oneline -1`＝afdfae98 实证）；按 F-007/F-098/F-099/F-200 既有登记自主树复制未追踪 `RobWorkStudio.ini.template.static`＋`RobWorkStudio.ini.shared.in` 两件（框架 configure 硬依赖——环境链亲历，非送验对象缺陷）；集成 build 树全新配置：`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=<主仓>vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64 -DCMAKE_CXX_FLAGS="-DNOMINMAX -DWIN32_LEAN_AND_MEAN -DWIN32"`——**CONFIGURE_EXIT=0**，缓存 grep `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON` 实证；独立冒烟 build 树＝`acc-ui-t10/smoke`（vcpkg toolchain＋CMAKE_PREFIX_PATH＋Qt6_DIR——AGENTS §4.1 口径）；GUI 测试执行＝`QT_QPA_PLATFORM=windows`＋`QT_PLUGIN_PATH`/`QT_QPA_PLATFORM_PLUGIN_PATH` 指向 Qt 6.11.1 plugins（ui.md §12.2 既有登记口径）；ird_gates 以**脚本模式**（`cmake -DIRD_ROOT=<industrialrobot 根> -P cmake/ird_gates.cmake`，exit 1＝设计内）对送验树与 base detached worktree（acc-ui-t10/base @ f6968386）**双态亲跑**并同口径归一化（剥离绝对前缀＋去尾括注＋去重排序）比对；变异三轮在冒烟树执行（重建快、与集成树同源码同测试）；验收者日志存 acc-ui-t10/：**幸存件**＝configure-smoke.log、build-smoke.log、gates-run-integration.log、gates-run-base.log、head-set.txt/base-set.txt（门禁归一化命中集）、validate-task.log/validate-docs.log（validate 双件为记录定稿后在 afdfae98 新 detached 检出树重跑留档——VT/VD exit 0、PASS/PASS 与首次亲跑一致）、mut1~3-build/run.log、restore-build/run.log、run-int/（集成三测试 gtest XML＋console）、run-smoke/（冒烟同构）；集成侧 configure/build/verify-target 三件 console 日志在临时 worktree A 内产生、随 A 清理一并删除未幸存——其决定性数值结论（CONFIGURE_EXIT=0、BUILD_EXIT=0、`: error` 计数 0、VERIFY_EXIT=0）已逐字摘录本记录 §4.2/§4.3 对应表行，同源冒烟树全量构建（build-smoke.log 幸存）独立佐证双模式零 error；记录回传 worktree B＝`git worktree add -b acc/UI-T10/1` @ origin/redesign-main |
| verdict | **pass**（4.1～4.11 全部通过、无阻断级问题；三条新发现均为建议级——F-276/F-277/F-278 随本 evidence 分支转登 findings.json，按 F-266/F-268/F-270/F-274 家族既有先例不阻塞合入：两条留痕/注释偏差的验证结论全部经本验收亲手复现准确——双模式构建 exit=0 零 error／契约 verify exit=0／86＋3＋28 双模式全绿／门禁 42=41＋1 逐码一致／validate 双脚本 PASS） |

## 4.1 改动范围 —— 通过

- `git diff --name-status f6968386..afdfae98` 实证 **27 个变更文件**，逐个对照契约 allowedFiles：`ui/**` 10 件（CMakeLists.txt M＋20/-5、include/sdurws/ird/ui/AboutDialog.hpp A 405 行、IWorkbenchShell.hpp M＋11、UiPorts.hpp M＋62、src/AboutDialog.cpp A 331 行、src/UiText.cpp M＋48、src/WorkbenchShell.cpp M＋94、src/WorkbenchShell_p.hpp M＋14、test/AboutDialogModelTest.cpp A 398 行、gui_test/AboutDialogGuiTest.cpp A 462 行）＋`units/ui.md`（M，v1.1→v1.2 增量修订，13＋/5-）＋`traceability/**` 16 件（builds/wp10-t10/** 15 件 A＋wp10-t10-gate-registrations.md A）——**全部在 allowedFiles 内，零越界**。
- forbiddenFiles（REQUIREMENTS.md/ARCHITECTURE.md）零触碰（27 文件清单实证，两文件均不在 diff 内；本验收对照需求原文 UX-14/NFR-DEP-05〔REQUIREMENTS.md:408/:479〕以只读方式提取，未改动）；契约文件 tasks/foundation/UI-T10.json 不在 diff 内＝未被任务侧改动（status=ready 维持）。
- 框架源码（RobWork/、RobWorkSim/、RobWorkStudio/ 其余源码）零改动：diff 文件清单中无 industrialrobot/ui 与文档链之外任何源码路径；`patches/` 范围内零变更（SA-02 无登记需求）。

## 4.2 可复现构建 —— 通过（集成全新树重配＋全量构建＋独立冒烟；`: error` 计数双零）

| 步骤 | 结果 |
| --- | --- |
| 集成重配 | worktree A 全新配置 **exit 0**（补齐 vcpkg toolchain＋Qt 前缀＋CXX flags＋两件未追踪 ini 辅助件后通过——F-007/F-098/F-099/F-200 既有登记口径）；缓存 grep `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON` 实证 |
| 集成全量构建 | `cmake --build build --config Release --parallel` **BUILD_EXIT=0、`: error` 计数 0、`: fatal error` 计数 0**（RobWork 全库→RobWorkSim→RobWorkStudio→industrialrobot 全单元全新编译）；产物 `build/RobWorkStudio/bin/Release/` 下 `sdurws_ird_ui_test.exe`＋`sdurws_ird_ui_contract_test.exe`＋`sdurws_ird_ui_gui_test.exe` 全部在位；新增/大改 TU（AboutDialog.cpp/AboutDialogModelTest.cpp/AboutDialogGuiTest.cpp/UiText.cpp/WorkbenchShell.cpp）**零告警**（全量 25 处 warning grep 归属均为既有文件与框架头，与本次改动面零交集） |
| 独立冒烟模式 | `cmake -S …industrialrobot -B acc-ui-t10/smoke -G "Visual Studio 17 2022" -A x64 -DCMAKE_TOOLCHAIN_FILE=<vcpkg> -DCMAKE_PREFIX_PATH=<Qt> -DQt6_DIR=<Qt>/lib/cmake/Qt6` 配置 **exit 0**；`cmake --build --config Release --parallel` **SMOKE_BUILD_EXIT=0、`: error` 计数 0**；`smoke/ui/Release/` 下三测试可执行在位并亲跑（见 4.3——86＋3＋28 全绿） |

## 4.3 契约 verify 命令 —— 逐条亲手执行；全部成功

契约 verify 数组仅 1 条，在 worktree A 根目录按字面独立执行：

| 命令 | 结果 |
| --- | --- |
| `cmake --build build --config Release --target sdurws_ird_ui_test` | **exit 0**（VERIFY_EXIT=0、`: error` 计数 0） |

附加测试真实执行（验收者亲手，非采信送验留痕）：

- 集成树三测试可执行直接运行（QT_QPA_PLATFORM=windows＋QT_PLUGIN_PATH）：**模型层 86/86、契约 3/3、GUI 28/28 全部 PASSED**（AboutDialogModelTest 九用例＋AboutDialogGuiTest 三用例均在亲跑输出在列且零失败〔用例名清单见 4.4〕；计数与在库三份 gtest XML〔tests=86/3/28、failures=0〕及 ird-test-report.json〔total=86、passed=86、failed=0、skipped=0、decisive=true〕逐值一致；77＋9＝86、25＋3＝28 与"本任务新增九＋三用例"提交声称吻合）。
- 冒烟树三测试可执行另跑 **86＋3＋28 全部 PASSED**（"集成＋冒烟双模式同口径"的提交声称经验证属实）。
- 治理脚本亲跑（worktree A 树内）：`validate-task.ps1 -TaskFile <UI-T10.json 绝对路径>` → **PASS (1 tasks)**；`validate-docs.ps1` → **PASS (20 units, 12 trace entries, 150 task files)**。

## 4.4 acceptance 条目 —— 2/2 逐条证据（未执行项零存在；ird_gates 已随 WP-01-T01 落地故为"已执行"）

| # | 契约 acceptance 要点 | 证据（验收者亲手） |
| --- | --- | --- |
| 1 | UI-PLG-2：关于对话框插件清单＝静态白名单 ∩ AssemblyReport（逐插件：标题/版本/面板数/命令数/装配状态——§11.4），与静态白名单一致（白名单为编译期/装配期常量，§11.1 阶段 A 占位 token 先行登记）；产品/组件版本显示与冻结版本基线一致（NFR-DEP-05，版本数据由 L5 提供、ui 呈现）；帮助入口链接用户手册 | ①**白名单编译期常量**：`pluginUiWhitelist()` 以 `static const std::array<std::string,8>` 承载 §11.1 八 token 逐字词表（AboutDialog.cpp:61-70——modeling/requirements/kinematics/trajectory/dynamics/selection/optimization/workflow，顺序即装配序与呈现序）；`WhitelistFrozenEightTokens_11_1_UI_T10` 亲跑通过——测试侧独立登记词表逐位对照（内容/顺序/唯一性/两次调用稳定四断言，防"两处漂移"）。②**清单＝白名单 ∩ 报告、与白名单一致**：`aboutPluginRows` 唯一权威装配（AboutDialog.cpp:102-156——行只从白名单产生〔规则 1〕、报告对位充实 ok/面板数/命令数/失败码〔规则 2〕、白名单外条目天然不产生行〔规则 3 交集丢弃〕、版本列恒「不适用」占位〔规则 4——§10.9 报告形状无版本字段，不虚构〕）；`EmptyReportsYieldWhitelistPlaceholderRows_UI_PLG_2_UI_T10_ACC1`（报告空集八行占位、行序＝白名单序、计数 0）与 `IntersectionEnrichesAndDropsForeignReports_UI_PLG_2_UI_T10_ACC1`（一成一败一零能力命中行逐字段充实＋rogue 白名单外条目零污染＋失败证据不丢弃）亲跑通过；`DuplicateReportsTakeFirstDeterministically_UI_T10`（重复报告取首条确定性防御）亲跑通过。③**五列呈现**：`createAboutDialog` 以 §11.4 原文列序渲染只读表（AboutDialog.cpp:289-318——标题/版本/面板数/命令数/装配状态，列头常量单点 AboutDialog.hpp:361-365；全部文本为模型产出值，工厂零二次加工）。④**版本与注入基线一致（NFR-DEP-05）**：`aboutVersionRows` 注入值原样呈现、序＝注入序、空值「不适用」占位（AboutDialog.cpp:162-205）；`VersionRowsMirrorInjectedBaseline_NFR_DEP_05_UI_T10_ACC1`（产品行首＋组件行逐字一致＋未登记项占位）与 `UnavailableBaselineSinglePlaceholderRow_NFR_DEP_05_UI_T10`（available=false 单行「未装载」＋占位值零数字自证）亲跑通过；GUI 同源：`AboutDialogShowsBaselineAndListSameSource_UI_PLG_2_UI_T10_ACC1` 亲跑通过——菜单"关于"→统一提交路径→关于框打开，版本行/清单格与模型输出逐字同源＋端口消费计数==1/1（注入承载证据）。⑤**UX-02 零内部名**：`TitlesResolvedViaUiText_NoRawTokenLeak_UX02_UI_T10` 亲跑通过——八标题键＋三状态键全登记（UiText 内建表键族⑥⑦ UiText.cpp 增 11 键）、标题为中文解析值且 ≠ token、与 resolveText 同源。⑥**帮助入口链接用户手册**：`userManualPath()`（`<appDir>/../share/ird/user-manual/index.html` 部署布局，AboutDialog.cpp:211-224）＋`openUserManual()`（存在才启动 QDesktopServices::openUrl，缺失不启动、返回 false，AboutDialog.cpp:226-237）；`ManualPathFollowsShareDeploymentLayout_UX14_UI_T10_ACC1`（绝对路径＋固定布局）与 `HelpContentsMissingManualGivesVisibleFeedback_UX14_UI_T10`（help.contents 经统一提交路径触发→状态栏用户可见缺失反馈＋Dev 日志排障明文＋诊断目录零产出——不臆造稳定码）亲跑通过；壳集成两实条目（help.contents/help.about 菜单动作→submitShellCommand→registry.submit——WorkbenchShell.cpp:290-296/:632-646，SA-16 路径不变）。阶段 A 部署树无手册文件，§12.3 UI-HELP-1 行的预期即缺失反馈形态（打开成功支需受控部署树，测试文件头与 ui.md §12.3 已登记——与设计预期一致，非缩水）。 |
| 2 | O-31 处置（已裁决 2026-09-19——随整链放行）：自身交付物（帮助入口与关于对话框）不持有对端类型（版本数据 L5 注入、插件清单静态白名单 §11.1）；前置 UI-T03 已随裁决解除阻塞 | ①**验收者独立 include 扫描**：新增产品面 AboutDialog.hpp/AboutDialog.cpp 的 `sdurws/ird/` 命中仅 ui 自有头（UiTypes.hpp/UiText.hpp），project/evidence/execution/policy/runtime/modeling/kinematics 等 15 业务域**零命中**（grep 实证）；AboutDialog.hpp 零 Qt include——QDialog/QWidget 仅前置声明（:56-57），UiPorts.hpp/IWorkbenchShell.hpp 消费方不拖入 Widgets。②**链接面零变化**：ui/CMakeLists.txt diff 内 `target_link_libraries` 改动 **0 处**（grep 实证）——零新依赖边；ird_gates 亲跑 SUB/T1 族命中集与 base 逐码一致（42=41＋1，唯一新增为 R3 AboutDialog.cpp——见 4.6）。③**数据注入面为 ui 自有端口**：IUiAboutDataSource（UiPorts.hpp:384-437——assemblyReports/versionBaseline 两只读方法）＋ShellWiring.aboutSource 可空成员（允许为空＝无关于数据测试场景须显式声明）；测试以值对象替身（FakeAboutSource/AboutVersionBaseline 字面值）注入，零对端类型；为空时 `AboutDialogWithoutSourceShowsPlaceholders_UI_PLG_2_UI_T10` 亲跑通过——关于框照常打开、版本区「版本基线：未装载」单行、清单八行白名单占位零虚构。④**UI-T03 前置事实**：IWorkbenchShell/壳在本 diff 前已合入（base f6968386 含 UI-T03~T09 全部落位），前置成立。 |

## 4.5 测试真实失败能力 —— 通过（三处实现变异注入应失败输入，均真实变红；试验后已还原并复绿、worktree 零残留）

在验收沙箱 worktree A（git 管控下，三轮试验后 `git status --porcelain -- …/industrialrobot/` 零输出＝与送验对象逐字节一致；`grep -rn MUT` 零残留）注入三处变异，均在冒烟树重建亲跑（首轮 MUT2 因编辑未命中未重建，判无效后已按有效重建重做——下表三行均为构建 exit=0 的新二进制结果）：

| # | 变异 | 重建后亲跑结果 |
| --- | --- | --- |
| 1 | `AboutDialog.cpp` 白名单词表 `"trajectory"` 改 `"trajectoryX"`（§11.1 冻结词表被篡改） | `--gtest_filter=AboutModel.*` → **FAILED ×5**（WhitelistFrozenEightTokens 内容对照、EmptyReports 行序、Intersection、DuplicateReports、TitlesResolved 五用例连锁变红——词表冻结断言即承重断言） |
| 2 | `aboutPluginRows` 报告对位循环删除 `break`（重复报告由"取首条"变"取末条"——确定性防御被拆除） | `--gtest_filter=AboutModel.DuplicateReportsTakeFirstDeterministically_UI_T10` → **FAILED**（selection 重复报告应 Ok/1 实得末条值——首条确定性断言即承重断言） |
| 3 | `aboutVersionRows` 占位行 `kAboutBaselineNotLoadedText` 改 `"1.0.0"`（未装载占位伪造版本号——ERR-01 面被击穿） | `--gtest_filter=AboutModel.UnavailableBaselineSinglePlaceholderRow_NFR_DEP_05_UI_T10` → **FAILED**（占位值≠「未装载」＋"占位含数字"断言双触发） |

三处变异经字节还原（git checkout＋porcelain 零差异）后重建（RESTORE_BUILD_EXIT=0）复跑：模型层 **86/86 PASSED**。三组承重断言均为「词表冻结」「确定性防御」「占位不伪造」语义本身——非恒真、非"只编译通过"。

## 4.6 架构红线（AGENTS §5 八条）—— 逐条通过

1. **L2 计算内核零 Qt**：本任务仅改 ui 单元（R-3 全产品唯一 Qt Widgets 例外单元）；新增模型面 AboutDialog.hpp **零 Qt include**（亲读：仅标准库＋UiTypes.hpp——QDialog/QWidget 前置声明；grep `#include <Q` 零命中），AboutDialog.cpp 的 Qt include（QDialog/QLabel/QTableWidget/QDesktopServices 等 11 件）落在例外本体目标 sdurws_ird_ui 的源集文件——与基线 WorkbenchShell.cpp 等命中同型同根源；ird_gates 脚本模式亲跑（exit 1＝设计内）归一化命中集与 base 双态比对：**41→42 恰增 1 处**（`IRD-GATE-R3|ui/src/AboutDialog.cpp`），其余 41 处逐码零变化——与在库 gates-hit-set.txt（42 条）/gates-hit-set-diff.txt（恰增 1 处）/wp10-t10-gate-registrations.md 三件逐一比对一致（HIT-SET-IDENTICAL）；新公共头 AboutDialog.hpp **零命中**（前置声明策略生效，登记件声称证实）。
2. **业务域互链禁止（R-1）**：本任务新增产品面 include 扫描——`sdurws/ird/` 命中仅 ui 自有头；UiPorts.hpp 对 core/Identity.hpp 为既有登记边（C-11 端口入参，非本任务新增）；链接段零改动（4.4②证据）——本任务新增依赖边＝零。
3. **跨单元私有头禁止（R-2）**：零跨单元 include；新公共头落位 `include/sdurws/ird/ui/AboutDialog.hpp` 公共路径；测试面（diagnostics/testkit 公共头）为测试目标合法消费。
4. **框架零源码修改（SA-02）**：diff 内零框架文件、patches/ 零变更（4.1 实证）。
5. **不做名称前缀拼接/剥离（R-4）**：ui diff **代码内容行** `RobWork` 字面量 **0 命中**（grep 命中的 10 处均为 `+++ b/RobWork/...` diff 头行，非代码）；ird_gates 亲跑 R4 项仅 project 既有 1 处（ProjectStoreImpl.cpp——与 base 同值，非本任务文件）。
6. **权威唯一（PA-1）**：版本数据权威归 L5/部署侧冻结基线（AboutVersionBaseline 仅呈现值载体——available=false 占位零虚构、注入值不改写不补默认）；装配事实权威归装配器报告（ui 侧只做白名单∩报告投影，零装配逻辑）；文案值权威单点＝UiText 内建表（键族⑥⑦ 11 键入表，对话框零字面量文案——工厂"零字面量、零数据加工"由 GUI 同源测试钉住）；清单装配权威单点＝aboutPluginRows/aboutVersionRows（模型层与 GUI 同一函数输出——NFR-MNT-03）。
7. **不可变历史（PA-2）**：本任务零修订数据触碰（27 文件清单——无 project/ 修订面文件）；关于框为只读呈现面（打开时现取快照，不订阅不回写）。
8. **不恢复 old/**：diff 新增行 `old/` 引用 **0 命中**（计数实证）；实现全部从零编写。

## 4.7 注释规范（AGENTS §2）—— 通过（新增公共头 AboutDialog.hpp 全查＋UiPorts.hpp/IWorkbenchShell.hpp/WorkbenchShell_p.hpp 增量段全查＋实现全文/增量 3 件（AboutDialog.cpp 331 行全文、UiText.cpp 增量段、WorkbenchShell.cpp 增量段）＋两测试文件全文＋CMakeLists diff——覆盖"公共头全查＋实现抽查 ≥3"）

- **文件头**：全部新增/大改件均有中文 `@file/@brief`＋设计依据（ui.md §11.4/§11.1/§10.9/§3.5/§3.4 v1.2、需求 UX-14/NFR-DEP-05/UX-02/ERR-01、任务契约 acceptance 1/2、O-31 处置逐条对应）＋背景说明（"为什么清单是白名单∩报告而不是直接读装配器""为什么键→值要收口到一个装配函数""为什么对话框工厂不用 exec()""为什么占位行也是信息"等"为什么"层注释齐备且与技术事实一一对应）。
- **类/函数 Doxygen**：公共头逐结构体/逐函数 `@brief/@param [in]/@return/@throws/@note` 语义完整（pluginUiWhitelist 值拷贝隔离语义、PluginAssemblyReport 五字段逐项 §10.9 对齐、AboutVersionBaseline 投影≠重定义、aboutPluginRows 四条装配规则编号列出、aboutVersionRows 两形态后置条件与空组件名 fail-fast、openUserManual 二态返回语义、createAboutDialog 呈现纪律与生命周期归属）——review 者可据此判断实现是否符合设计，本验收 4.4 的逐条核对即以该契约头为基准完成。
- **§2.5 高危信息表**：物理单位/坐标系/角度制式（本任务无物理量面——如实无缺失；计数列为整数计数非物理量）；所有权（"裸指针且不 show——QDialog 生命周期交 Qt 父子树（parent 为 nullptr 时调用方自管）"、端口 shared_ptr 注入方持有语义、值拷贝隔离〔白名单函数返回值与内部表隔离防引用逸出〕逐处注明）；线程约束（"装配纯函数任意线程可调用；createAboutDialog/openUserManual 只允许 UI 线程（§3.4 M-1）""IUiAboutDataSource 调用一律 UI 线程、返回快照值"逐处注明）；错误语义（调用方装配违约 fail-fast invalid_argument〔空组件名、键表失同步〕与占位正常业务值〔「不适用」「未装载」〕的**两轨划分**逐函数注明、手册缺失不臆造稳定码走 Dev 通道的归类理由）；确定性来源（白名单序＝呈现序、重复报告取首条、组件序＝注入序不重排、1 起编号可读性）逐处注明。
- **魔法数字**：八 token 规模（§11.1 词表来源）、五列列序（§11.4 原文列序）、37 行表规模与 keys.reserve(37)（26＋8＋3 计数注释、与 registeredTextKeys 增量同步改写——本处计数已校准）、520×420 对话框初值（纯呈现布局，非物理量——不涉 §2.5）——逐个注明依据。
- **反例扫描**：零复述式注释、零无编号 TODO（全 diff `TODO|FIXME|XXX|HACK` 零命中）、注释与代码一致。一处既有偏差如实登记（转登 **F-277**）：CMakeLists AUTOMOC 复核注释行"十二个源文件"与 add_library 实际 14 个产品 TU 不符（F-273/F-275 同一注释行第三次沿袭——本任务在该注释块内增列 AboutDialog.cpp 说明与 v1.2 复核尾注但计数未校准；实质性主张"零 Q_OBJECT"经验收者 grep 证实为真〔新增 TU 零信号槽声明〕，AUTOMOC 关闭决策不受影响，零行为影响）。

## 4.8 文档同步 —— 通过（零偏差＋增量修订已登记＋findings 未关闭条目逐条核对）

- **单元卡增量修订**：`units/ui.md` v1.1→v1.2（八落点亲读实证：文档头版本行 v1.2、§2.2 UX-14 行承接位置 §11.5→§11.4 笔误修正〔§11.4 即节标题与 §15 矩阵既有指向——非语义变更，v1.2 行已登记〕、§3.3 AboutDialog.hpp 增行＋UiPorts.hpp 行增注〔IUiAboutDataSource 随 UI-T10 冻结〕、§3.5 插件键族⑥⑦ 冻结登记、§10.1 ShellWiring v1.2 增量冻结注＋代码块 aboutSource 成员行、§11.4 UI-T10 落地登记注〔装配规则/端口注入/手册落位口径〕、§12.3 UI-HELP-1 增行、§15 UX-14 行 UI-PLG-2、UI-HELP-1、§16.7 v1.2 行）；v1.2 行登记的承载形状（契约头逐件/C 端口/键族 11 键/九＋三用例/86＋3＋28/门禁 42=41＋1）与实现及本验收亲测结果逐一比对一致。实现与任务卡零未登记偏差（acceptance 逐条对应见 4.4）。
- **findings.json 未关闭条目相关性核对**（240 条 open 逐类过筛）：①F-010/F-077/F-081/F-266/F-268/F-270/F-274（留痕 .log 家族）——**本任务复踩**（afdfae98 正文与 README-evidence.md 所引构建/门禁/validate/console .log 未入库，`git show --name-only | grep -c '\.log$'`＝0、`git check-ignore -v` 命中 .gitignore:87 `*.log`；验证结论本身经复现全部属实）→ 转登 **F-276**；②F-273/F-275（CMakeLists AUTOMOC 注释计数漂移）——**同一注释行第三次沿袭**（"十二个源文件" vs 实际 14 TU）→ 转登 **F-277**；③F-227/F-259（ird_gates 机器面滞后）——本任务恰增 1 处命中（AboutDialog.cpp R3）已按 wp10-t02~t09 既有口径以登记提交件承接（责任方 WP-01-T03 不变），无新增动作需求；④F-004（全局 AUTOMOC 空转）——本任务未触碰 AUTOMOC 配置（维持关闭决策，集成构建 verify 日志可见的 MOC 步骤为既有全局传播，非本任务引入），无新增动作需求；⑤F-098/F-099/F-200（全新树环境链）——本验收亲历复用既有口径配置成功，佐证登记内容仍准确，无新增动作需求；⑥其余 open 条目与本任务范围（关于框呈现/帮助入口）无涉。历史发现零静默积压。
- 另登记一条微观测（转登 **F-278**）：UiPorts.hpp 文件尾换行符在本次增量中丢失（diff 实证 `\ No newline at end of file`——此前版本有尾换行），跨工具链 diff 噪声源，随下次触碰该文件恢复。

## 4.9 commit 质量 —— 通过（两提交标题动词开头＋正文四段齐备；验证结论与验收者亲测逐项一致；一处文件清单失准按家族先例判建议级）

- 61a6dbfd「[WP-10-T10] 实现帮助入口与关于对话框（白名单∩装配报告清单＋版本基线呈现）」——动词"实现"开头；正文四段（What 按 9 文件列要点/Why 对 UX-14/NFR-DEP-05 与 ui.md 章节及 O-31/How 双模式构建＋86＋3＋28＋门禁恰增 1 处＋validate PASS/影响面一实现 TU＋两公共头增量＋帮助菜单升级）齐备；**验证段全部结论经本验收亲手复现属实**（构建 exit=0、测试计数、门禁 42=41＋1、validate PASS）。
- afdfae98「[WP-10-T10] 入库执行留痕（构建/测试/门禁/治理证据）」——动词"入库"开头（非 F-267/F-271 家族的名词短语形态）；正文四段齐备；**验证结论属实**，但 What 段所列"ird_gates-integration/base/smoke 三份运行日志""validate-task.log（PASS）"等 .log 实际未入库（F-010 .gitignore 根因，`git show --name-only` 实证 0 件 .log）——文件清单与入库内容不一致为 F-081/F-266/F-268/F-270/F-274 家族第 5 次复踩，**不构成粉饰**（结论可独立复现且在库 XML/JSON/gates 证据链完整），判建议级转登 F-276，不阻塞。
- 代码与测试同一提交（61a6dbfd 内同含实现与 398＋462 行测试）；无空话提交。

## 4.10 留痕完整性 —— 通过（按家族先例；在库件与我实跑逐值一致）

- 在库留痕 16 件亲验：三份 gtest XML（sdurws_ird_ui_test.xml tests=86/failures=0、contract 3/0、gui 28/0 亲读；*-smoke 同值）＋五份 ird-test-report*.json（total=86/3/28、passed 同值、failed/skipped=0、decisive=true，冒烟同名件同值）＋gates-hit-set.txt（42 条）＋gates-hit-set-diff.txt（恰增 1 处结论与验收者双态亲跑归一化 diff 一致）＋wp10-t10-gate-registrations.md（净增 1 处登记与亲跑一致）＋README-evidence.md——**与我双模式亲跑结果逐值一致**。
- 构建日志（integrated-build.log/smoke-*.log/ird_gates-*.log/validate-task.log/console.log 等）未随库：F-081/F-266/F-268/F-270/F-274 家族先例（根因 F-010），本验收以亲手重跑补足复现证据，判建议级（随 F-276 转登），不阻塞。
- `phase-one-validation.log` 未更新——该文件自 WP-03-T01 后即停更（git log 末次 da407b61 2026-09-10 实证），UI-T03~T09 各次验收同状态，无 resultNote 机制；按既有各次验收同口径不另溯责。

## 4.11 偷懒/缩水实现扫描 —— 通过

- ①**标记扫描**：全 diff 新增行 `TODO|FIXME|XXX|HACK|stub|placeholder|未实现|not implemented` 扫描——命中仅一类：`placeholder` 为**契约要求行为本身的领域词汇**（aboutVersionRows 的「未装载」占位行局部变量名——UX-14/NFR-DEP-05"基线未装载时呈现占位、零虚构"的承载，非延后工作标记；占位行为有三条具名测试钉住）；`TODO/FIXME/未实现/not implemented` 零命中，无契约显式"触发式延后"项需豁免。
- ②**空实现检测**：新增公共接口逐函数亲读——全部有实质实现（pluginUiWhitelist 静态表拷贝/pluginTitleKey·pluginAssemblyStatusLabelKey 键族词法/aboutPluginRows 四规则装配/aboutVersionRows 两形态装配/userManualPath 部署布局解析/openUserManual 存在性判定＋系统打开器/createAboutDialog 版本区＋五列表构建/IUiAboutDataSource 两方法为纯端口契约〔实现由 L5 适配——端口本体按 O-31 注入面设计即如此，非空壳〕）；帮助菜单由 UI-T03 禁用占位升级为实条目（§13 行既定交付面）；无"仅返回默认值"的空壳。
- ③**逐条证据表**：见 4.4——acceptance 两条每条均有具名测试（九＋三用例逐一在亲跑输出在列）＋代码行号/文件行号证据，任一条无证据即 fail 的条件未触发。
- ④**测试强度**：断言校验行为与边界（词表逐位＋唯一性＋稳定性三断言、交集丢弃 rogue 行、重复报告首条确定性、失败证据不丢弃、占位零数字自证、GUI 同源逐字互证＋端口消费计数、缺失反馈非空＋语义点明＋Dev 留痕＋诊断目录零产出四观测点）；非恒真、非"只编译通过"——4.5 三处变异全部真实变红实证承重性。

## 发现的问题清单（分级）

| 级别 | 编号 | 内容 | 处置 |
| --- | --- | --- | --- |
| 阻断 | 无 | — | — |
| 建议 | F-276 | 留痕 .log 家族第 5 次复踩（afdfae98/README-evidence.md 所引构建/门禁/validate/console 日志未入库；根因 F-010；验证结论全部复现属实） | 已转登 findings.json；不阻塞合入；根因随 F-010 治理消账 |
| 建议 | F-277 | CMakeLists AUTOMOC 注释计数漂移第三次沿袭（"十二个源文件" vs 实际 14 TU；"零 Q_OBJECT"主张 grep 证实为真，零行为影响） | 已转登 findings.json；后续触碰该注释块时随手校准 |
| 建议 | F-278 | UiPorts.hpp 文件尾换行符在本次增量中丢失（跨工具链 diff 噪声源） | 已转登 findings.json；随下次触碰该文件恢复尾换行 |

## 验收者环境说明

- 全新上下文：未读取实施者任何过程叙述；仅消费契约、设计文档（REQUIREMENTS/acceptance-protocol/units/ui.md）、在库留痕与既往**验收**记录（UI-T09 记录仅取环境配置口径与家族判定先例）。
- 复现优先于采信：集成配置/全量构建/契约 verify/双模式三组测试/ird_gates 脚本模式双态（送验树＋base worktree）与归一化逐码比对/validate 双脚本/三处变异试验均亲手执行（幸存日志存验收临时目录 acc-ui-t10/，清单与幸存口径见"验收环境"行——集成侧三件 console 日志随临时 worktree 清理未幸存，其决定性结论已逐字摘录本记录并被同源冒烟构建日志佐证）；测试真实失败能力经三处变异注入实证（其中 MUT2 首轮因编辑未命中未重建判无效，已按有效重建重做并如实记于 4.5）。
- 送验对象冻结核验：验收全程按 afdfae98（detached worktree），`origin/wp10-t10` 与该 SHA 逐字一致（零漂移）；变异试验后 worktree A 已逐字节还原（porcelain 零差异）；验收者零提交至任务分支（本记录与 F-276/F-277/F-278 仅落 acc/UI-T10/1 evidence 分支）。
