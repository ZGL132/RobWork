# WP-15-T09 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | WP-15-T09（实现结果筛选/导出/批量复算域内设施——KIN-08/AT-04） |
| 单元 | kinematics |
| 送验对象（冻结） | 06364df81b1c792960da3df8638ba878ac73c7b8（`wp15-t09` 尖端，四提交 9d33e08b/22366cc0/de85bd4d/06364df8——分支漂移检测：`git branch --contains 06364df8` 仅 wp15-t09／origin/wp15-t09，零漂移） |
| diff 范围 | 0d11551404cc5e26e876362fbf7eadcaafbce31e..06364df81b1c792960da3df8638ba878ac73c7b8（base＝redesign-main 0d115514，即 origin/redesign-main 同值） |
| 验收日期 | 2026-09-27 |
| 验收者 | 独立上下文对抗式验收会话（全新会话，只消费契约/设计文档/产物，未消费实施叙述） |
| **verdict** | **pass**（4.1～4.11 全部通过，零阻断级问题；建议级 3 条转登 findings.json：F-403/F-404/F-405） |

## 环境说明（复现现场）

- worktree A：`D:/10_Source_Repos/21_robot/RobWork/tmp_acc_t09_wtA`（`git worktree add --detach @ 06364df81b1c792960da3df8638ba878ac73c7b8`——按 SHA 不按分支名）。worktree 缺 git 未跟踪模板 `RobWorkStudio.ini.template.static`（F-007/F-062/F-099 在册既有缺陷；`RWS_USE_STATIC_LINK_PLUGINS:BOOL=ON` 口径需要），自主检出只读补拷至 `RobWork/RobWorkStudio/bin/` 后配置通过（环境修复，不触及送验产物，T08 验收同先例）。
- 集成模式：从零重新配置于 `tmp_acc_t09_wtA/build`（`cmake -S <wtA>/RobWork -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=<主仓>/vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`——主构建树同款口径；缓存 grep 确认 **`RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`**）。不触碰主检出 `build/`。
- 冒烟模式：独立树 `D:/10_Source_Repos/21_robot/RobWork/tmp_acc_t09_smoke`（`cmake -S <wtA>/RobWork/RobWorkStudio/src/rwslibs/industrialrobot`＋vcpkg toolchain＋Qt6 前缀；`cmake --build ... --config Release` 全树 exit 0 零 error）。
- base 侧 worktree：`tmp_acc_t09_base`（detached @ 0d115514）——仅用于 ird_gates 引擎直跑比对（无需构建），已删。
- 验收者变异测试（4.5）仅在 worktree A 内实施并 `git checkout --` 还原，不涉及任何提交/推送；主检出零写操作。
- 验收者日志落盘（验收现场，随临时 worktree 用毕即弃，结论全部回写本记录）：`tmp_acc_t09_{cfg,int_build,int_testbuild,ctest,smoke_cfg,smoke_build,gates_head,gates_base,validatedocs}.log` 等。

## 4.1 改动范围 —— 通过

`git diff --name-status 0d115514..06364df8`：共 32 文件，逐一比对契约 `allowedFiles`：

- `RobWork/RobWorkStudio/src/rwslibs/industrialrobot/kinematics/**`×12（CMakeLists.txt、SolutionSet.hpp/.cpp M；Export.hpp/.cpp、Recompute.hpp/.cpp、SolutionSetViewTest.cpp、ExportTest.cpp、RecomputeTest.cpp A）✓
- `RobWork/doc/industrial-robot-design/units/kinematics.md` M ✓
- `RobWork/doc/industrial-robot-design/traceability/builds/wp15-t09/**`×19 A ✓（构建/测试/门禁全套留痕）

`forbiddenFiles`（REQUIREMENTS.md/ARCHITECTURE.md）零触碰 ✓；`patches/` 无新文件（SA-02：diff 无任何框架源码文件）✓。

## 4.2 可复现构建 —— 通过（两模式零错误，验收者亲手从零重配）

- 集成：`--target sdurws_ird_kinematics` exit 0、error 计数 0；补建 `sdurws_ird_kinematics_test`／`sdurws_ird_kinematics_contract_test` exit 0 零 error。
- 冒烟：全树构建 exit 0、`error C|error LNK|error MSB` 零命中；两测试 exe 产出行在案。
- 冒烟警告面（与 4.8/F-403 相关）：全树 22 处既有 C4834（`[[nodiscard]]` 弃返回值）——kinematics 单元仅 2 处（CommandsTest.cpp:590,5/611,5，T08 遗留、非本任务文件）、其余 20 处在 execution 单元测试（RunRegistryAdmissionTest/ArchiveFaultContractTest 等既有）；**零 error**，不影响零错误结论。

## 4.3 契约 verify 命令 —— 3/3 逐条亲手执行成功

| # | 命令 | 结果 |
| --- | --- | --- |
| 1 | `cmake --build build --config Release --target sdurws_ird_kinematics` | exit 0 零 error |
| 2 | `ctest --test-dir build/RobWorkStudio/src/rwslibs/industrialrobot/kinematics -C Release -L "^ird$"` | **100% tests passed, 0 failed out of 2**（ird 标签 2 tests） |
| 3 | `pwsh -File RobWork/scripts/industrialrobot/validate-docs.ps1`（worktree A 内） | **PASS (20 units, 12 trace entries, 189 task files)** |

额外（非 verify 数组）：`validate-task.ps1 -TaskFile <契约>` → PASS (1 tasks)。verify 数组缺 `sdurws_ird_kinematics_test`／`sdurws_ird_kinematics_contract_test` 两测试目标构建行属 F-397/F-399/F-401 家族复发 → 建议级转登 **F-404**；验收者已在全新构建树自行补建并执行（集成 99/53、冒烟 93/53 实测），不构成阻断。

## 4.4 acceptance 条目逐项证据 —— 3/3 通过

| # | 契约条目 | 证据（具名测试/输出/行号，全部验收者复跑） |
| --- | --- | --- |
| 1 | 筛选/最差排序唯一实现点＝IKinematicSolutionSet（§6.2，NFR-MNT-04）：filtered 谓词比较集中实现（可用解/含碰撞诊断解）、worstBy 三值、statistics；不可变视图、并发只读安全、已排序集合上幂等（V-04 排序面复用；§6.3 四键） | ①两规范谓词工厂集中落位 SolutionSet.hpp:157/174（声明）＋SolutionSet.cpp:136~153（唯一比较实现——`evaluated && !inCollision`／`inCollision` 各一处书写点）；②`UsablePredicateSelectsConfirmedCollisionFree_WP15T09_ACC1`：四解集仅选 init0/init3，证据缺失解（evaluated=false）不入可用集（KIN-05 保守面）＋筛选为稳定序子序列断言（SolutionSetViewTest.cpp:86~116）；③`CollisionDiagnosticPredicateSelectsMarked_WP15T09_ACC1`：选中标记解＋生产端形态（碰撞解已入 filteredRecords）恒空集（:122~146）；④`SortedIdempotentAndReusesSortSurface_WP15T09_ACC1`：视图构造排序≡sortSolutions 唯一实现点逐位一致＋已排序重复排序/已排序输入重建视图双重幂等（:153~197）；⑤`FilteredIdempotentWithNullPredicateContract_WP15T09_ACC1`：已筛选集合重复筛选幂等＋空谓词全保留（:203~232）；⑥`ConcurrentReadOnlyAccessAgrees_WP15T09_ACC1`：4 线程×200 轮 sorted/filtered/worstBy/statistics 混合并发只读与单线程基准全等＋并发后内容复验（:238~292）；⑦worstBy 三值 WorstMetric 词表（SolutionSet.hpp:122~130）＋T04 既有用例延续回归（全量 99 绿）。 |
| 2 | 导出值行（KIN-08/§7.4）：JSON/CSV 值行含 snapshotId/pointOid/regionOid，导出文件头声明来源，导出不产生修订（AT-04）；写出经 io 由插件侧触发——本任务只交付值行整理与 UT | ①`SolutionRowsCarryIdentityAndObjectRefs_WP15T09_ACC2`：rank/身份/对象引用逐行断言＋整理不重排（ExportTest.cpp:163~190）；②`FilteredRowsCarryReasonTokenAndCollisionPairs_WP15T09_ACC2`：原因 token 三值全表（residual-recheck/joint-limit/collision）＋碰撞对象对 canonical 竖线展平（R-4 零拼接，:192~220）；③`CoverageRowsTallyByRegionAndKind_WP15T09_ACC2`：(regionOid 首现序, position→pose) 分组六计数＋零样本口径不产行（:226~254）；④`CoverageRowsBijectionViolationFailsFast_WP15T09_ACC2`：数量失配/重复 sampleIndex 双臂 logic_error（:256~271）；⑤`HeaderSourceDisciplineFailsFast_WP15T09_ACC2`：归档缺 runId／会话带 runId 双臂 invalid_argument＋两合法形态对照（:277~299）；⑥`JsonEncodeDeclaresSourceAndIsDeterministic_WP15T09_ACC2`：format/source/runId/snapshotId/mode 声明面＋同包两次编码逐字节一致＋纯面（:305~339）；⑦`CsvEncodeHeaderSectionsAndEscaping_WP15T09_ACC2`：'#'注释头＋三段表＋最短往返词形（",0.1,0.2,"）＋RFC 4180 转义（"has,comma""quote"）（:345~380）；⑧`SessionMemoryHeaderOmitsRunIdLine_WP15T09_ACC2`：会话来源无 run-id 行/字段（:382~399）；⑨AT-04 结构保证：Export.hpp/.cpp 零项目/命令/归档依赖（include 面核查），写出器集成随 io 消费面——契约 acceptance 2 原文口径，无缩水（4.11①）。 |
| 3 | 批量复算＝按原 slice 重新提交 execution 新 run：不复用会话残留、当前性重新判定（§7.4；CON-02/05）——本单元保证重提交请求构造正确性 UT | ①`CarriesOriginalTaskDefinition_WP15T09_ACC3`：sliceId/mode/seed/referenceQ 逐字段取自原身份（RecomputeTest.cpp:73~90）；②`BindsCallerSuppliedCurrentSnapshot_WP15T09_ACC3`：当前快照直传不继承原快照锚＋工程未变对照＋subjectOid 显式/缺省双形态（:96~133）；③`DeterministicAndFreeOfResidue_WP15T09_ACC3`：同输入同值＋referenceQ/evaluationKey 换源对照（构造签名无会话形参的行为面证明，:139~171）；④`FailsFastOnEmptyKeyOrZeroSnapshot_WP15T09_ACC3`：空评估键/全零快照锚 invalid_argument（:177~193）；⑤类型面结构保证：KinRecomputeRequest 无 runId/attempt/结果载荷字段（Recompute.hpp:62~94——"新 run"结构性不可携带）。 |

## 4.5 测试真实失败能力（变异测试）—— 通过（2 处实质变异均真实变红，已还原）

- **M1 可用解谓词**：`src/SolutionSet.cpp` `usableSolutionPredicate` 返回式改 `return true;`（恒真变异）→ 重编译后 `UsablePredicateSelectsConfirmedCollisionFree_WP15T09_ACC1` **FAILED**（可用面证据完备性断言真实钉住 KIN-05 保守语义——非恒真）。
- **M2 CSV 来源声明头**：`src/Export.cpp` L536 `"# source: "` → `"# source:"`（声明头格式破坏）→ `CsvEncodeHeaderSectionsAndEscaping_WP15T09_ACC2` **FAILED**（声明面断言真实钉住 §7.4 来源声明义务）。
- `git checkout --` 还原后重建复跑两用例 **PASSED**（现场还原；送验对象仍为冻结 SHA 的树内容）。

## 4.6 架构红线 —— 通过（八条逐条）

1. **L2 零 Qt**：六个新/改代码文件 grep `Qt|QObject` 零命中（仅注释提及"零 Qt"设计语义）✓
2. **R-1 禁互链**：Export/Recompute/SolutionSet 增量零 modeling/project/trajectory/dynamics 等 业务域 include；跨单元协作仅值面（无端口新增，interUnit=false 与实现一致）✓
3. **R-2 私有头**：全部 include 走 `include/sdurws/ird/core/...`（Digest/Evaluation/Identity 公共头）与本单元头＋std ✓
4. **SA-02 框架零改**：diff 无框架文件、patches/ 无新文件 ✓
5. **R-4 零名称拼接**：碰撞对象对/身份字段一律 `ObjectId::toCanonical()` 直出（Export.cpp:263~268 等）✓
6. **权威唯一（PA-1）**：写出归 io、提交归 execution、当前性归 evidence、覆盖判定归 evidence 聚合——本单元正确不越权（行面只整理事实计数，Export.hpp:177~185 注）；谓词比较集中 NFR-MNT-04 ✓
7. **不可变历史（PA-2）**：导出/复算均只读纯值面，类型面无任何修订/写通道（AT-04 结构保证）✓
8. **不恢复 old/**：无 old/ 引用 ✓
ird_gates 引擎直跑 base..head 比对**零新增零消除**（三套计数亲跑复核见 4.11）。

## 4.7 注释规范 —— 通过

- **公共头 3 个全查**：SolutionSet.hpp（修改——文件头"T09 增列"段＋两谓词工厂 Doxygen：KIN-05 保守面依据/生产端不变式/线程安全逐点）、Export.hpp（新增——文件头五要素＋三值行结构逐字段单位标注：rad|m、m、rad、无量纲、计数＋runId 两分纪律＋确定性来源 to_chars 最短往返）、Recompute.hpp（新增——三要件构造面逐条＋D-KIN-4 显式携带＋@throws 调用方错误语义）。
- **实现抽查 ≥3**：Export.cpp（to_chars 不失败论证、RFC 8259/4180 转义依据、双射 fail-fast 三站点、分组确定性"首现序键表避免 unordered_map 迭代不确定序"）、Recompute.cpp（fail-fast 两臂逐条业务依据、字段携带三要件映射）、SolutionSet.cpp 增量（无捕获 lambda 可重入论证、谓词语义与 KIN-05 引用）——§2.5 高危信息表逐项在场（单位/错误语义/确定性来源/线程约束）。
- **测试文件**：三文件用例名 `_WP15T09_ACCn`＋`IRD_TEST_INFO` 需求/AT 追溯＋断言处中文说明"本用例验证哪条需求"——符合 §2.7。

## 4.8 文档同步与 findings 相关性 —— 通过

- 单元卡 v0.9（卡头版本行同步）：§3.3 布局表新增 Export.hpp/Recompute.hpp 两行（T09）；§11 任务表 T09 行置已完成（含落位文件清单）；§14.6 新增 v0.9 行（三 acceptance 语义登记＋99/99＋53/53 集成、93/93＋53/53 冒烟实证＋覆盖缺口登记 a/b/c 三条——CSV/JSON 写出器集成随 io 消费面、导出头无环境字段、覆盖行不承载聚合结论）。**实现与登记零偏差**：17 个 T09 具名用例（5 SolutionSetView＋8 Export＋4 Recompute）与验收者实测运行日志逐一相符；两模式测试计数与留痕 XML 一致。
- findings.json 未关闭条目相关性核对（332 open）：**F-387/F-390/F-393/F-394 家族卡面散文纠正已全部兑现**（head 卡面实证：v0.3 行改"＋1 项 sanctioned 命中（Eigen PRIVATE——O-40）"、v0.4 行改"KinIk 18 新用例"并附消账说明、v0.5 行改"14 新契约用例"并附消账说明——本任务未新增同族未兑现项）；F-395/F-396（Sampling.hpp 两站点）本任务 diff 未触及 Sampling.hpp，无触发义务，维持 open；F-386/F-389/F-391 消费面本任务未触及，维持 open；F-398/F-402（比对附件口径）本任务附件已按三套计数分列（F-398 口径遵守）但 F-402 精炼建议（注明 76 组成）未兑现 → 转登 **F-405**；F-397/F-399/F-401 家族在本契约 verify 数组复发 → 转登 **F-404**；另 C4834 措辞口径 → 转登 **F-403**。

## 4.9 commit 质量 —— 通过

- 四提交：`[WP-15-T09] 增列解集视图规范筛选谓词集中实现…`（9d33e08b）、`[WP-15-T09] 新增结果导出值行整理与 JSON/CSV 编码设施`（22366cc0）、`[WP-15-T09] 新增批量复算请求构造设施…`（de85bd4d）、`[units] kinematics 卡 v0.9：WP-15-T09 落位登记与验证留痕`（06364df8）——实现提交 `[WP-nn-Tkk]` 前缀＋动词开头；登记提交 `[units]` 前缀（AGENTS §6.2 治理惯例）。
- 正文四段（What/Why/验证/影响面）逐提交齐备＋`Acc-Covered` 映射；验证段数字（99/99＋53/53、93/93＋53/53、gates 三套计数 145/76/69、validate-docs PASS）与验收者独立复跑逐一相符——无粉饰。

## 4.10 留痕完整 —— 通过

evidence 目录 `traceability/builds/wp15-t09/` 19 件：集成/冒烟构建日志＋四运行日志＋gtest XML×4＋ird-test-report.json×4＋gates 附件×4＋integration-ctest.log。XML 计数（99/53/93/53，failures 全 0）与验收者独立复跑（集成 stdout 实测 99/53、冒烟 93/53）逐一相符；ird-test-report.json summary（total 99/passed 99/failed 0）与 XML 同源一致。`phase-one-validation.log` 未更新——T06～T08 同先例（验证留痕由 evidence 目录承载，符合 4.10"或 resultNote"替代面精神）。

## 4.11 偷懒/缩水扫描 —— 通过

- ①diff 范围（kinematics/**）`TODO|FIXME|stub|placeholder|未实现|not implemented|XXX|HACK` 新增行扫描零命中；契约显式"触发式延后"豁免面（CSV/JSON 写出器集成随 io 消费面验证）为契约 acceptance 2 原文口径＋卡 §14.6 v0.9 覆盖缺口 a 登记在案，援引合规。
- ②空实现检测：公共接口全部实质函数体（两谓词工厂、行构建三函数、包组装、JSON/CSV 编码两函数、复算构造——逐字段搬移与逐段编码，无仅返回默认值的占位）。
- ③逐条证据表：见 4.4（三条 acceptance 全部具名测试＋行号证据，零"已实现"声明采信）。
- ④测试强度：断言校验行为与边界（fail-fast 双臂、生产形态恒空、幂等逐位、并发一致性、RFC 转义字节面、身份逐字段）；**4.5 变异实证非恒真**（2 处变异各致 1 具名测试真实变红）。
- **gates 比对附件独立复核**（验收者双侧 worktree 引擎直跑，三套计数分列——F-398/F-402 纪律）：
  - ①直方图原始行数：base **145**＝head **145**（逐码 base=head：SUB 75、R3 41、LIB 12、R1 7、T1 5、R4 3、T2 1、R5 1）；
  - ②归一化 sort -u 去重条目（剔除绝对路径前缀，无锚定捕获口径）：base **76**＝head **76**，逐行 diff **空**——零新增零消除；
  - ③引擎自计数：base **69**＝head **69**；
  - ④F-402 锚定口径复核（`^IRD-GATE-` 行锚定捕获去重＝命中本体）：base **69**＝head **69**——三套计数各自内部一致、与送验附件声明一致；附件口径②未按 F-402 精炼建议注明"76＝69 命中＋7 自测 token"组成 → 建议级转登 **F-405**（零增量结论不受影响）。

## 发现问题清单

**阻断级：无。**

**建议级（已转登 findings.json，编号顺延自 F-402）：**

- **F-403**：单元卡 §14.6 v0.9 行「冒烟仅 CommandsTest.cpp 两处 C4834 既有警告——T08 遗留非本任务引入」的"仅"字限定与自身留痕 `run-smoke-build.log` 的 22 处 C4834 不符（kinematics 单元 2 处 CommandsTest.cpp 属实，另 20 处在 execution 单元既有测试——RunRegistryAdmissionTest.cpp/ArchiveFaultContractTest.cpp 等）；按全量冒烟构建口径应写「kinematics 单元 2 处＋execution 单元 20 处既有」。零 error 结论与"非本任务引入"归属判断均不受影响（验收者实测同值 22 处），属措辞精度建议。
- **F-404**：`tasks/foundation/WP-15-T09.json` verify 数组仅 3 行（产品库构建＋ctest＋validate-docs），缺 `sdurws_ird_kinematics_test`／`sdurws_ird_kinematics_contract_test` 两测试目标构建行——F-300/F-358/F-367/F-368/F-371/F-373/F-375/F-381/F-397/F-399/F-401 家族新例（本批 WP-15 任务契约逐单复发形态）；验收者已在全新构建树自行补建执行（集成 99/53、冒烟 93/53 实测），不构成阻断。建议契约治理按家族批量修订时补齐本契约两行。
- **F-405**：`builds/wp15-t09/ird-gates-base-head-compare.md` 口径②仍为「去重条目 76」未按 F-402 owner 指令（"下次产出比对附件时按本条精炼口径②"）注明组成（76＝69 命中本体＋7 条引擎自测检出裸码 token；验收者锚定捕获实测命中本体去重 69/69 与引擎自计同值）；三套计数分列本身已遵守（F-398 口径无重犯）、零增量结论不受影响，属 F-402 精炼建议的兑现延迟登记。

**非问题备忘**：验收复现环境口径（集成配置需 vcpkg toolchain＋Qt prefix＋static 链接模板补拷）为 F-007/F-062/F-099 在册既有缺陷与环境事实，历次验收同先例处理，不重复登记。

## 裁决

**verdict＝pass**。4.1～4.11 全部通过、无阻断级问题；建议级 F-403/F-404/F-405 随本记录转登 findings.json，不阻塞合入。验收者未合入、未修改实施产物（变异测试在临时 worktree 内实施并当场还原）、未向任务分支推送任何提交（送验对象冻结于 06364df81b1c792960da3df8638ba878ac73c7b8）。
