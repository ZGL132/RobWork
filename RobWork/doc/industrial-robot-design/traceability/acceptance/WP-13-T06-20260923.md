# WP-13-T06 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | WP-13-T06 实现 Xacro 受控展开语义（unit=modeling，interUnit=true：io 对端——护栏原语与已验证字节消费） |
| 送验对象（冻结） | 分支 `wp13-t06` 尖端 `803b435a549a3579834f724aec612c874fc5fd79`（验收全程按该 SHA） |
| 分支漂移检查 | 验收启动时 `git fetch origin` 后 `origin/wp13-t06` = `803b435a…` ＝ 送验 SHA ＝ 本地 `wp13-t06`，**零漂移**（ACC §3） |
| diff 范围 | `6707f447581b4fc7bd859134064801427560071c..803b435a549a3579834f724aec612c874fc5fd79`（2 个提交：3aa4f096 实现＋803b435a 双模式留痕与文档同步；base＝验收请求指定，即 wp13-t05 合入收尾提交、`origin/redesign-main` 尖端） |
| **verdict** | **pass**（4.1～4.11 全部通过，无阻断级问题；建议级 4 项转登 findings F-308/F-309/F-310/F-311） |
| 验收执行方式 | 验收者全新上下文独立复现：detached worktree @ 送验 SHA 亲手重配重跑全部构建/测试/脚本/门禁与两处变异注入；未采信实施者任何运行结果（ACC §2 四条） |
| 复现现场 | `C:/Users/zgl18/AppData/Local/Temp/acc-wp13-t06-repro`（detached @ 803b435a，集成模式 build＋冒烟模式 acc-wp13-t06-smoke）；`C:/Users/zgl18/AppData/Local/Temp/acc-wp13-t06-base`（detached @ 6707f447，门禁 base 对照专建） |
| evidence 分支 | `acc/WP-13-T06/1`（基于 `origin/redesign-main` = 6707f447；记录 commit＝本分支尖端 40 位 SHA，由编排者登记三元组） |

---

## 4.1 改动范围 —— 通过

`git diff --name-only 6707f447..803b435a` 共 29 文件，逐一对契约 `allowedFiles` 核对：

- `RobWork/RobWorkStudio/src/rwslibs/industrialrobot/modeling/**`：7 文件（CMakeLists.txt 改；include/…/DiagCodes.hpp 改；include/…/XacroExpand.hpp 新增；src/DiagCodes.cpp 改；src/XacroExpand.cpp 新增；test/DiagCodesTest.cpp 改；test/XacroExpandTest.cpp 新增）——全在 modeling/** 内。
- `RobWork/doc/industrial-robot-design/units/modeling.md`（改，升 v0.7）——在 allowedFiles。
- `RobWork/doc/industrial-robot-design/traceability/**`：21 文件（builds/wp13-t06/ 留痕 13 日志＋4 gtest XML＋4 ird-test-report JSON）——在 allowedFiles `traceability/**` 内。
- **forbiddenFiles 零触碰**：REQUIREMENTS.md、ARCHITECTURE.md 不在 diff；任务契约 WP-13-T06.json 亦未被触碰（status 翻转归治理收尾，实施侧未越权——F-289 反例未再现）。
- **SA-02**：diff 无 modeling 之外任何框架源码文件；`industrialrobot/patches/` 无新文件（base..head 对 patches 路径 diff 为空）；diff 零 vcpkg 文件。
- **P-MDL-4「io 公共面零改动」提交面证据**：diff 对 `*/io/` 零文件——与 acceptance 4 与提交正文声明一致（验收者 diff 实证）。

## 4.2 可复现构建 —— 通过（两模式均验收者亲手从零重配重跑）

- **集成模式**（acc-wp13-t06-repro，源＝detached worktree @ 803b435a）：
  - 配置：`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON` ＋ 主树 vcpkg toolchain ＋ Qt 前缀（与主树缓存同参）。结果 `Configuring done`；缓存确认 **`RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`**。
  - 环境前置（非本任务产物缺陷，F-007/F-009/F-200 已登记同族）：①worktree 内 `vcpkg/installed/` 不存在——toolchain 指向主树 vcpkg 复用其已装包；②`RobWorkStudio/bin/RobWorkStudio.ini.template.static` 被 `.gitignore` 的 `[Bb]in/` 规则吞掉且整个 bin 目录不入库（首次配置即报 `CMake Error: File … does not exist`）——从主树复制后配置通过。
  - 任务相关目标构建：`sdurws_ird_modeling`、`sdurws_ird_modeling_test`、`sdurws_ird_modeling_contract_test` 三目标退出码均 **0**，MSVC `error C/LNK` 计数 **0**（日志：复现现场临时文件，结论以此记录为准）。
  - 验收者亲跑两 exe：`sdurws_ird_modeling_test` → **104/104 PASSED**（含 `MdlXacroExpand.*` 13 条全过）；`sdurws_ird_modeling_contract_test` → **5/5 PASSED**。
- **独立冒烟模式**（acc-wp13-t06-smoke，`-S RobWork/RobWorkStudio/src/rwslibs/industrialrobot -DCMAKE_TOOLCHAIN_FILE=<主树 vcpkg>/scripts/buildsystems/vcpkg.cmake`＋Qt `CMAKE_PREFIX_PATH` 前缀——AGENTS §4.1 与送验留痕 smoke-configure.log 同口径）：
  - 配置 done（退出码 0）；全量构建退出码 **0**、错误计数 **0**（core/diagnostics/project/io/runtime/execution/evidence/policy/modeling/ui/report 全单元测试 exe 齐产出）。
  - 冒烟测试（验收者亲跑两 exe）：`sdurws_ird_modeling_test` → **104/104 PASSED**；`sdurws_ird_modeling_contract_test` → **5/5 PASSED**。

## 4.3 契约 verify 命令 —— 逐条亲手执行（6/6 成功；门禁按"零新增命中"口径判定）

| # | 命令 | 结果 |
| --- | --- | --- |
| 1 | `cmake --build build --config Release --target sdurws_ird_modeling` | 退出码 0，零错误 |
| 2 | `cmake --build build --config Release --target sdurws_ird_modeling_test` | 退出码 0，零错误 |
| 3 | `ctest --test-dir build/RobWorkStudio/src/rwslibs/industrialrobot/modeling -C Release -L "^ird$"` | **2/2 passed**（验收者先补跑了第 2 条未含的 `--target sdurws_ird_modeling_contract_test`（退出码 0）——契约 verify 清单不含该构建目标行，全新树按清单字面执行时 ctest 首跑 contract_test 将 Not Run；缺口与 F-300（T04）/F-304（T05）同型第三例，转登 **F-310**。非实现缺陷——验收留痕表明实施者完整构建三目标） |
| 4 | `cmake --build build --config Release --target ird_gates` | 门禁引擎运行后按 SA-10 以退出码 1 终止——命中集为**存量基线红**（集成树 MSBuild 面 52 处存量）。验收者专建 base 对照（detached worktree @ 6707f447）**两侧均以 `cmake -DIRD_ROOT=<绝对路径> -DIRD_SELFTEST_DIR=<临时> -P cmake/ird_gates.cmake` 直跑同脚本**（configure-free，免 MSBuild 转码噪声）：base 侧 111 行 `IRD-GATE-` vs branch 侧 111 行，路径归一化（`<源树>/industrialrobot`→`<ROOT>`）后 `sort+diff` **多重集 diff 为空＝零新增命中**——与送验留痕 gates-hit-set-diff.txt 的"111=111 归一后逐条一致"结论一致。modeling 面相关命中恰 3 条均为在案存量：IRD-GATE-LIB pugixml::pugixml（F-302，O-40 已登记依赖的词表暴露，处置归治理侧）＋IRD-GATE-SUB ×2 两条测试目标直链 testkit（F-292）。注：直跑命令字面 `IRD_ROOT=.` 会报 `file RELATIVE_PATH must be passed a full path`——须绝对路径（留痕方法说明与实测一致） |
| 5 | `pwsh -File RobWork/scripts/industrialrobot/validate-task.ps1 -TaskFile …/WP-13-T06.json` | `validate-task: PASS (1 tasks)`，退出码 0 |
| 6 | `pwsh -File RobWork/scripts/industrialrobot/validate-docs.ps1` | `validate-docs: PASS (20 units, 12 trace entries, 186 task files)`，退出码 0 |

## 4.4 acceptance 条目逐条证据 —— 5/5 有对应证据（行号以送验 SHA 内容为准，验收者亲读亲跑）

| acceptance 条目 | 证据 |
| --- | --- |
| 1. XacroExpand.hpp/.cpp 落位（卡 §3.3 表 T06 行）；IXacroExpandService 按卡 §9.4.9 签名（expand——宏/参数/属性替换语义引擎）；输入为 io 已验证字节＋依赖树（IResourceReader 产物），护栏原语由 io 机制承载（io.md §6.2），modeling 在护栏内解析——绝不执行任意代码 | `XacroExpand.hpp` 落位于 `include/sdurws/ird/modeling/`（§3.3 表 T06 行；卡 v0.7 §3.3 同步登记）。接口签名 XacroExpand.hpp:326-328 `ExpandOutcome expand(const ValidatedXacroSource&, const XacroSubstitutionMap&, std::vector<core::DiagnosticRecord>&) const` 与卡 §9.4.9 一致（测试经 `IXacroExpandService&` 多态引用调用——`BasicPropertyMacroSubstitution_WP13T06_ACC1` 注释明示且实测）。输入面 `ValidatedXacroSource`（XacroExpand.hpp:116-122）＝entryBytes＋entrySnapshot＋dependencyTree＋includeBytes 全部为 io 产物值类型（io/ResourceIo.hpp），装配归调用方、本单元零文件访问（XacroExpand.cpp include 面无 fstream/cstdio/filesystem——SA-14 实证；io.md §10.5 注释对应）。护栏原语归 io：递归深度/总字节/循环检测执行值在 modeling 侧复核透传（kXacroMaxExpansionDepth=16＝io.md §6.2 边界；XacroExpand.hpp:146-161 注明数值权威在 io 卡）。**绝不执行任意代码**：`NoArbitraryCode_ExpressionAndRosArgsRejected_WP13T06_ACC1`（${1+1} 表达式拒绝——cause 含被拒原文"1 + 1"；$(env HOME) ROS 替换参数拒绝——unsupported-construct）；实现为 `isPlainIdentifier` 白名单＋substituteText 显式分支（XacroExpand.cpp:263-277/497-511/529-545）。受控子集其余面：include 拼接（`IncludeSpliceRegistersMacroFromIncludedFile_WP13T06_ACC1`——被包含文件宏登记后可调用）、输入契约违约值面（`InputContractViolation_SourceInconsistentNoThrow_WP13T06_ACC1`——树缺入口→SourceInconsistent 不抛异常）。 |
| 2. 展开失败/依赖缺失可定位诊断 UT（MDL-19、AT-31）：未定义宏/参数→行列定位诊断（宏名＋源行列）；io 护栏码透传（IO-FORMAT-XML-CYCLE/IO-RES-MISSING——V-09）：无草稿、无修订、无对象写入；参数列表展示数据＋substitutions 清单随导入报告留痕 | 行列定位：`UndefinedMacro_LocatableDiag_WP13T06_ACC2`（MDL-IMPORT-XACRO-UNRESOLVED＋cause 含 "undefined-macro"/"symbol=nonexistent"＋context 断言 `xacro-expand@robot.xacro:3:4`——行 3 列 4）；`UndefinedParam_LocatableDiag_SubstitutionResolves_WP13T06_ACC2`（undefined-param＋`:3:4`；对照面——substitutions 提供 nope 后同一输入成功展开）。护栏透传：`IncludeCycle_PassThroughCycleCodeNoProduct_WP13T06_ACC2`（带环树→IO-FORMAT-XML-CYCLE 诊断＋cause 含环路径 "robot.xacro -> macros.xacro -> robot.xacro"＋无产物——V-09；实现 XacroExpand.cpp:926-968 防御性 DFS＋:670-685 拼接栈双保险）；`MissingInclude_PassThroughResMissingWithList_WP13T06_ACC2`（缺失叶→IO-RES-MISSING＋cause 含 "missing-count=1; missing-list=macros.xacro"——XacroExpand.cpp:1063-1096 缺失预扫描）；`MacroRecursionDepth_GuardBudgetInclude_WP13T06_ACC2`（自递归宏→IO-SEC-BUDGET-INCLUDE＋cause 含 "limit=16"——actual/limit 三要素）。无草稿/无修订/无对象写入：失败面一律 `expandedBytes=nullopt`（XacroExpand.cpp:1180-1182；上述用例逐一 ASSERT_FALSE）；服务无落盘（纯函数值面）。参数列表＋留痕：`ParameterListDisplayData_WP13T06_ACC2`（substitution 条目在前无定位＋property 条目携定义处行 3；代入优先级 robot6_j1）；substitutions 随导入报告留痕见 acceptance 3 第⑤断言（xacro-substitution/ sourcePath 计数=1）。 |
| 3. 展开产物进 URDF 同一映射与安全边界 UT（卡 §6.5）：expand 产物经 mapXacroExpanded 走 mapUrdf 同一校验（§6.3/§6.4 口径不变）；原始 .xacro 的 ResourceSnapshot digest 进草稿 externalRefs（来源记录——MDL-19 来源可追溯）；展开失败不产生草稿 | `ExpandedProduct_SameUrdfBoundaryAndProvenance_WP13T06_ACC3`：①六轴样例受控展开（step 0.1→0.2 代入）；②展开产物装配回 ValidatedSource 后 `mapper.mapUrdf` 与 `mapper.mapXacroExpanded` 均产出草稿；③**等价手写 URDF（kGen6Urdf）走 mapUrdf——草稿逐字段相等**（`*fromExpandedUrdf.draft == *fromPlainUrdf.draft`——同一映射最直接证据）；④mapXacroExpanded 草稿剥除 xacro-source 来源条目后与 mapUrdf 基线恒等（无其他字段漂移）；⑤来源留痕：xacro-source 条目 contentDigest == provenance.sourceDigest == digestOf(kGen6Xacro)（原始 .xacro 内容真实 SHA-256——fixture 用 core ContentDigester 非占位值）＋externalRecord.absPath=="Z:/fake/robot.xacro"；⑥substitutions 逐条随导入报告留痕（xacro-substitution/ 计数=1）。`ExpansionFailure_NoProductNoDraft_WP13T06_ACC3`（xacro:ghost→无产物——调用方无草稿可装配）。 |
| 4. 所有权口径落地（P-MDL-4「护栏 io／语义 modeling」）：io 交付护栏原语确认、modeling 交付展开语义引擎——实现按此切分且 io 公共面零改动；pugixml 复用 T05 已登记依赖（O-40，不二次引入渠道） | io 公共面零改动：diff 对 `*/io/` 零文件（4.1 验收者 diff 实证）；护栏执行面按同一 io 边界值复核透传（IO-SEC-BUDGET-INCLUDE/TOTAL、IO-FORMAT-XML-CYCLE、IO-RES-MISSING 全部经 `io::errorCodeToken` 取码——XacroExpand.cpp:45 注释"禁字符串拼码"，无字符串拼码命中）。modeling 侧交付语义引擎：本单元头/实现/13 用例。pugixml 复用：CMakeLists.txt 的 `find_package(pugixml CONFIG REQUIRED)` 登记语句零改动（diff 仅增源文件登记两行＋注释块）；XacroExpand.cpp:48-52 注释 O-40 复用 T05 已登记依赖（vcpkg 经典模式 1.16/x64-windows，modeling PRIVATE）；未引入第二渠道（diff 零 vcpkg/CMake 依赖语句变更）。MDL-IMPORT-XACRO-UNRESOLVED 实现期增登：DiagCodes.hpp 常量＋DiagCodes.cpp 描述符表尾追加（paramSchema `["item-kind","symbol"]`）＋DiagCodesTest 工厂封闭性同步 T02+T05+T06 七码（`FactoryScopeIsStagedT02Rows_WP13T02_ACC4` 实测过）——单元卡 §9.5 表行＋§14.6 v0.7 同步（4.8）。 |
| 5. 确定性 UT（NFR-COR-02）：同输入字节＋同 substitutions→同展开字节、诊断顺序稳定；可重入无共享可变状态（卡 §3.4 纯函数服务约定） | `DeterministicAndReentrant_WP13T06_ACC5`：两实例交错调用（A 成功→B 失败面→B 成功→A 再成功——失败态不残留）后同实例两次调用产物逐字节相等＋诊断逐条相等；跨实例产物/参数清单/provenance 三字段/诊断序逐项相等。实现侧确定性来源（验收者逐行核对）：单遍扫描替换文本不二次展开（防注入递归，XacroExpand.cpp:525/头注）；属性绑定按宏签名序非调用点序（:827）；诊断（relFile,line,column,seq）稳定排序出口（:146-159，成功/失败共用）；Engine 全部状态为栈上局部（:317-341）——无共享可变状态；不读环境变量/时钟/locale/文件系统（头注声明＋include 面实证）。 |

未执行项标注：无——全部 acceptance 面均执行；`ird_gates` 按 4.3 如实记录为"存量基线红＋验收者独立比对零新增命中"，不标注"零命中"。

## 4.5 测试真实失败能力 —— 通过（两处变异实证）

验收者在复现现场对 `src/XacroExpand.cpp` 注入两处单行变异后重建重跑 `sdurws_ird_modeling_test`（F-068/F-075/F-297 教训：sed 直接改写保证 mtime 更新、增量编译真实发生——构建输出确认重编译链接）：

1. **MUTATION-M1**（XacroExpand.cpp:497）：`if (!isPlainIdentifier(inner))` → `if (false)`（表达式拒绝分支旁路——${1+1} 走符号查找失败路径）→ **`MdlXacroExpand.NoArbitraryCode_ExpressionAndRosArgsRejected_WP13T06_ACC1` 按预期变红**（cause 期待 unsupported-expression、实际 undefined-param）。
2. **MUTATION-M2**（XacroExpand.cpp:967）：`return dfs(source.dependencyTree.rootRel);` → `return true;`（include 图环检测短路）→ **`MdlXacroExpand.IncludeCycle_PassThroughCycleCodeNoProduct_WP13T06_ACC2` 按预期变红**（ASSERT_FALSE(expandedBytes.has_value()) 失败——无环检测时入口无 include 元素、展开成功产出）。

变异运行结果：各次均为 **0 passed / 1 failed**（恰为目标用例，其余用例未受扰动）；`git checkout --` 复原后重建重跑 → **104/104＋5/5 PASSED**，工作树复原干净（`git status` 零残留）。结论：断言校验行为与失败语义而非编译通过，测试非恒真。

## 4.6 架构红线 —— 逐条自查通过

1. **L2 零 Qt**：XacroExpand.hpp/.cpp/DiagCodes.* include 面全清单核对（4.3 节记录）——零 Qt 头（验收者逐行读 include 段）；modeling 目标链接面无 Qt（CMakeLists diff 未触碰链接语句）。
2. **R-1 业务域互链**：modeling 产品源 include 面仅 core/io 公共头＋本单元＋pugixml PRIVATE；无 requirements/kinematics/trajectory/dynamics/selection/optimization 任何 include（include 清单逐行核对零命中）。
3. **R-2 跨单元私有头**：全部跨单元 include 位于 `include/sdurws/ird/<unit>/` 公共路径（core/DiagData、core/Digest〔仅测试〕、io/ResourceIo、io/IoDiagnostics——io 公共头清单核对 IoDiagnostics.hpp 在列）；无 src/ 私有头跨单元引用。
4. **SA-02 框架零修改**：见 4.1（零框架文件、patches/ 无新文件）。
5. **R-4 名称前缀**：XacroExpand 全文无 RobWork 名称拼接/剥离逻辑（名称解析归 runtime——本单元不涉名称面）。
6. **PA-1 权威唯一**：expand 为纯函数值面——不产生修订、不落盘、不写对象（失败面 expandedBytes=nullopt 即"无草稿面"）；运动学真值/名称/单位语义零越权（io 护栏值按 io 卡执行，DiagCodes 归本单元注册面）。
7. **PA-2 不可变历史**：无任何历史写路径。
8. **old/ 历史实现**：全部按卡面从零编写，无恢复痕迹。

## 4.7 注释规范 —— 通过（公共头全查＋实现文件抽查 4 个，超 ≥3 下限）

- **XacroExpand.hpp（公共头，全查）**：文件头 Doxygen（设计依据四源＋背景说明"展开引擎在导入管线中的位置"＋所有权口径＋线程安全/确定性声明）；每个类型（ValidatedXacroSource/XacroSubstitutionMap/XacroParameterItem/XacroExpandErrorCode/XacroExpandError/ExpandOutcome）与接口（IXacroExpandService::expand @pre/@post/子集五条/注释与 PI 边界）中文契约注释齐备；字段逐一注释。
- **§2.5 高危信息表**：①物理单位——护栏常量注明 unit=depth/unit=bytes（诊断 cause 三要素含 unit）；②坐标系——本单元不涉位姿，头注显式声明"全 SI（m/rad/kg/N·m）；角度制式 rad"；③角度制式——同上显式声明；④所有权/生命周期——"纯值类型，调用方所有""解析缓冲所有权""与引擎同栈保活——无悬空面"（SourceFile 注释）逐处标注；⑤线程约束——"无共享可变状态、可重入、多线程并发调用安全（卡 §3.4）"；⑥错误语义——XacroExpandErrorCode 值面/诊断面两层面分工＋SourceInconsistent＝调用方契约违约（值面不抛异常）逐枚举注释；⑦确定性来源——"单遍扫描/属性绑定按签名序/（相对键，行，列，产出序）排序/不读环境变量时钟 locale"逐条注明。
- **实现文件抽查**：XacroExpand.cpp（文件头"实现结构（阅读地图）"六步与正文 ①～⑥ 段前标签一一对应——T05 Import.cpp 的步骤标签漂移问题（F-307）在本文档未再现；魔法数字 16/2GiB 均注明来源 io.md §6.2/Budget.hpp；每个 if 分支注业务场景）；DiagCodes.cpp（增登块逐字段登记口径注释＋"表尾追加＝登记簿纪律"）；XacroExpandTest.cpp（文件头 ACC1/2/3/5 具名自证地图＋fixture 口径＋每用例注释验收来源）；DiagCodesTest.cpp（封闭性断言消息中文化）。
- **CMake 脚本**：T06 增列注释块两处（源登记＋测试登记，注明契约与 §3.3 行）。
- 反面示例扫描：无复述型/无信息量占位/中英机翻/过期注释命中。

## 4.8 文档同步 —— 通过（实现与任务卡零偏差，偏差与澄清已按 DTB §5.4 登记）

- **单元卡同步**：units/modeling.md 升 v0.7——①卡头版本行；②§3.3 表 T06 行落位登记（XacroExpand.hpp/.cpp 全套接口清单）；③§9.5 表尾增登 MDL-IMPORT-XACRO-UNRESOLVED 行（导入/error，confirmable=false，任务列 T06，行序插于 TEMPLATE-RANGE 与 PACKAGE-UNKNOWN 之间——与 DiagCodes.cpp 描述符清单序一致，验收者两侧比对一致）；④§14.6 v0.7 变更记录行（含⑤偏差与澄清五项：展开产物快照调用方装配／深度护栏码面取 IO-SEC-BUDGET-INCLUDE／行列按 offset_debug 元素名起点／宏默认值与 property name 不做 ${} 解析／验证留痕）。逐项与实现核对一致。
- **任务卡零偏差面**：MDL-IMPORT-PENDING-CONFIRM 的 T05/T06 共用消费者（note ④）在卡 §9.5 行"任务列 T05/T06"保持；knownPitfalls 三项（O-40/P-MDL-4/P-MDL-8）处置与契约一致。
- **追踪矩阵**：需求行归属未变（MDL-19），矩阵修订归治理收尾（先例同口径）；任务契约 status 仍 ready——翻转归 PIPE §4.7 治理动作，实施侧未越权（F-289 反例未再现）。
- **findings.json 未关闭条目相关性核对（250 条 open，逐族排查）**：
  - **F-292（modeling 测试目标 SUB×2）/ F-302（pugixml LIB 词表）**：本单元门禁存量——本次独立比对零新增，两存量原样在基线（4.3）。不可随本任务消账（处置归治理侧扩词表/例外登记），维持 open。
  - **F-200（全新 checkout 配置依赖未跟踪环境件）**：本次验收亲历再现（RobWorkStudio.ini.template.static 缺失阻断首次配置）——环境件缺陷非本任务引入，处置归治理侧（.gitignore/文件入库口径），维持 open；本记录 4.2 环境前置段留痕。
  - **F-300/F-304（契约 verify 缺测试目标构建行）**：T06 契约 verify 同型缺口第三例（缺 `--target sdurws_ird_modeling_contract_test` 行）→ 转登 **F-310**。
  - **F-207/F-231（gates 日志双重编码乱码）**：本任务留痕 gates-ird-gates-branch.log 再现（40 乱码行；direct 直跑日志干净）→ 转登 **F-309**。
  - **F-012 标题家族（F-293/F-298/F-299/F-303 同单元先例链）**：本任务留痕提交标题再现 → 转登 **F-308**。
  - **F-305/F-306/F-307（T05 验收的 Import.cpp 建议）**：Import.cpp 本任务零改动、XacroExpand 不复用其结构违例/mesh/步骤标签面——与本任务无关，维持 open。
  - 其余 open 条目（core/testkit/diagnostics/project/runtime/evidence/execution/policy/ui/report 各单元）与本任务 diff 无接触面。

## 4.9 commit 质量 —— 通过（四段式齐备、验证结论与验收者独立复现逐项吻合；两处建议级转登）

- **3aa4f096**（实现）：标题「[WP-13-T06] 实现 Xacro 受控展开语义引擎（IXacroExpandService）与展开诊断码」——动词开头合规；四段齐备；验证段三条（集成三目标零错误＋104/104＋5/5＋ctest 2/2；冒烟零错误＋104/104＋5/5；门禁 111=111 零新增）经验收者独立复现**逐项吻合**（4.2/4.3）。
- **803b435a**（留痕＋文档）：标题「[WP-13-T06] 双模式验证留痕＋文档同步：卡 v0.7（T06 落位登记＋§9.5 增登行）＋门禁比对证据」——前缀后以名词短语开头，非动词开头，不合 ACC §4.9/AGENTS §6.3 字面——**F-303 同字面再现**（T05 留痕提交标题逐字符同型），转登 **F-308**；正文四段齐备；验证段两节（validate-task/docs 双 PASS；留痕计数与实现提交一一对应）实测吻合；散文计数「十八件验证留痕」与实际归档 21 文件不符（F-041/F-080/F-236/F-249/F-256 散文计数失准家族再现，方向＝少报、无粉饰——21 件实测清点：13 日志＋4 XML＋4 JSON），转登 **F-311**。
- 无粉饰判定：两提交验证结论与验收者复现零出入；无"带病前进"表述。

## 4.10 留痕完整 —— 通过

- 在库 `traceability/builds/wp13-t06/` 21 件：gtest XML 四件（unit/contract × integration/smoke——integration 104/0f/0e＋5/0f/0e，smoke 同计数，验收者逐件读取测试/失败属性）＋ird-test-report 四件（integration/smoke 均 total=104 failed=0；contract 两件 5/0）＋构建/配置/测试运行日志十件＋validate-task/validate-docs 双 PASS 日志＋门禁比对三件（direct base/branch 各 111 行＋hit-set-diff 口径说明）。
- 计数与验收者独立复现一致（104/104＋5/5 × 双模式）；与提交正文声明一致。
- `phase-one-validation.log`：按 F-201 登记的口径，该条文与流水线实践脱节——本任务以 builds/wp13-t06 留痕＋单元卡 §14.6 v0.7 验证段承载 resultNote 等价面（T03/T04/T05 先例同口径），不构成缺口。

## 4.11 偷懒/缩水实现扫描 —— 通过

1. **标记扫描**：diff 新增行（`git diff 6707f447..803b435a -- …/modeling | grep "^+"`）对 `TODO|FIXME|stub|placeholder|未实现|not implemented` 扫描**零命中**（exit 1）；无"触发式延后"豁免需列出（mapWorkCellXml 类 R1 边界不在本任务面）。
2. **空实现检测**：公共接口函数 `IXacroExpandService::expand` 由 `XacroExpandService::expand` 完整实现（1188 行实现文件——六步全链）；`xacroExpandErrorCodeToken` switch 全枚举无 default＋逐值返回；XacroExpandError/ExpandOutcome 为值聚合无空函数体。无契约未声明的默认值空实现。
3. **逐条证据表**：见 4.4——acceptance 1～5 每条均有具名测试＋实现文件行号＋验收者亲跑输出，无一条仅凭"已实现"声明。
4. **测试强度**：13 条 MdlXacroExpand 断言行为与边界（行列定位精确到 `:3:4`、码面/cause 结构化键值、草稿逐字段恒等、诊断序逐条相等）；4.5 两处变异实证用例会红——非恒真、非"只编译通过"。

---

## 发现的问题清单

**阻断级：无。**

**建议级（4 项，已转登 findings.json，编号顺延自 F-307）：**

| # | 发现 | 登记 |
| --- | --- | --- |
| G-1 | 提交 803b435a 标题前缀后以名词短语「双模式验证留痕＋文档同步」开头，非动词开头（F-012/F-303 家族——与 T05 留痕提交标题逐字符同型再现；正文四段齐备无粉饰） | F-308 |
| G-2 | 留痕 gates-ird-gates-branch.log 含双重编码乱码 40 行（F-207/F-231 家族再现——集成树 MSBuild 管道输出编码；direct 直跑三件日志干净；结论可读性不受影响） | F-309 |
| G-3 | 契约 verify 数组缺 `--target sdurws_ird_modeling_contract_test` 构建行（F-300/F-304 家族第三例——全新树按清单字面执行时 ctest 首跑 contract_test 将 Not Run；验收者补跑后通过，非实现缺陷） | F-310 |
| G-4 | 提交 803b435a 正文散文计数「十八件验证留痕」与实际归档 21 文件不符（F-041/F-080/F-236/F-249/F-256 散文计数失准家族再现；方向＝少报，无粉饰） | F-311 |

## 验收环境说明

- 复现现场与主树共用 vcpkg `installed/`（worktree 内 vcpkg 无 installed——F-007/F-200 环境链）；`RobWorkStudio.ini.template.static` 从主树复制（`.gitignore [Bb]in/` 吞掉、git 不跟踪——F-200 再现实证，本记录 4.2 留痕）。
- 门禁直跑须绝对路径 `IRD_ROOT`（`.` 字面报 `file RELATIVE_PATH must be passed a full path`——ird_gates.cmake:82）；两侧均 configure-free 直跑，多重集 diff 免疫 MSBuild 转码噪声。
- 变异注入经 sed 原地改写＋目标重建（mtime 更新规避 F-068/F-075/F-297 增量构建陷阱），复原后工作树零残留。
- 验收时间：2026-09-23；验收者环境：Windows 11 x64，VS2022 17，Qt 6.11.1 msvc2022_64，vcpkg 经典模式（主树）。
