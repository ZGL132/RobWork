# WP-15-T02 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | WP-15-T02 落位 kinematics 计算库真实目标并登记门禁白名单六边 |
| **verdict** | **pass**（4.1~4.11 全部通过；无阻断级问题；建议级 3 项 F-380/F-381/F-382 已转登 findings.json，不阻塞合入） |
| 送验对象（冻结） | 8b88d0806453de24a255d3dc770a1dd9fc4e4cc1（单提交，base 之上恰 1 个提交） |
| base | bcf22c083a3349f0a1e519d5619af31fe4b4ad84（＝state.base＝origin/redesign-main） |
| 验收日期 | 2026-09-26 |
| 复现现场 | detached worktree `D:/10_Source_Repos/21_robot/acc-wp15-t02/A` @ 8b88d08（集成构建树 `D:/10_Source_Repos/21_robot/acc-wp15-t02/build-int`、冒烟构建树 `D:/10_Source_Repos/21_robot/acc-wp15-t02/build-smoke`）；base 侧门禁比对 detached worktree `D:/10_Source_Repos/21_robot/acc-wp15-t02/A-base` @ bcf22c08；验收者证据目录 `D:/10_Source_Repos/21_robot/acc-wp15-t02/evidence/`（关键机器留痕 23 件已随本记录提交至 `traceability/builds/wp15-t02-acc/`） |
| 独立性 | 全新上下文，未消费实施叙述；构建/测试/门禁/变异/治理脚本全部验收者亲手重跑；主检出（redesign-main @ bcf22c08，WP-24 系并发会话占用）零 git 写操作、零文件修改（仅只读复制 gitignored 模板目录 `RobWork/RobWorkStudio/bin/`〔含 RobWorkStudio.ini.shared.in＋RobWorkStudio.ini.template.static〕一件——T02~T16 验收同款前置） |

**分支漂移核查（ACC §3）**：`git ls-remote origin refs/heads/wp15-t02` ＝ `8b88d0806453de24a255d3dc770a1dd9fc4e4cc1`，与送验 headSha 逐字节一致——零漂移。`refs/heads/acc/WP-15-T02/1` 远程不存在（attempt 1 首验，无分支名冲突）。

---

## 4.1 改动范围 —— 通过

`git diff --name-status bcf22c08..8b88d08`：36 文件（4618 insertions，14 deletions），**逐文件均落在契约 allowedFiles glob 内**：

- `industrialrobot/CMakeLists.txt`（M，kinematics 移出 INTERFACE 占位循环＋add_subdirectory 挂载＋状态头注 7→6）；
- `industrialrobot/cmake/ird_gates_whitelist.cmake`（M，六边登记）；
- `industrialrobot/kinematics/**`（11 件 A：单元 CMakeLists.txt＋公共头 Errors.hpp/DiagCodes.hpp＋实现 src/Errors.cpp/src/DiagCodes.cpp＋test/ 4 件＋contract_test/ 2 件）；
- `traceability/builds/wp15-t02/**`（21 件留痕）＋`traceability/dependency-graph.json`（M，六边入图）＋`units/kinematics.md`（M，v0.1→v0.2）。

**forbiddenFiles 零触碰**：REQUIREMENTS.md／ARCHITECTURE.md 不在 diff 中。**SA-02**：diff 无任何框架源码文件，`patches/` 零新文件。范围内恰 1 个提交（`git log bcf22c08..8b88d08` 实证；其父即 base）。

## 4.2 可复现构建 —— 通过（验收者全新构建树，双模式零错误）

- **环境前置**：worktree 检出缺 gitignored 模板目录 `RobWork/RobWorkStudio/bin/`（内含 `RobWorkStudio.ini.shared.in`＋`RobWorkStudio.ini.template.static`）——从主检出只读复制后配置成功（T02~T16 验收实录同款现象与处置）。
- **集成模式**：`cmake -S <worktreeA>/RobWork -B build-int -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=D:/10_Source_Repos/21_robot/RobWork/vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`（配法＝主构建树缓存 CMakeCache.txt 实查同款：toolchain＋Qt 前缀均在案）；缓存 grep → `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`（configure 日志与 grep 输出随记录留痕）。`cmake --build --config Release --target sdurws_ird_kinematics sdurws_ird_kinematics_test sdurws_ird_kinematics_contract_test` 从零构建（全新树含框架链全量编译）**exit=0**，日志 grep `error C|: error|fatal error` 计 0，三目标 lib/exe 产出（`build-int/RobWorkStudio/libs/Release/sdurws_ird_kinematics.lib`＋`bin/Release/` 两测试 exe）。
- **独立冒烟模式**：`cmake -S <worktreeA>/RobWork/RobWorkStudio/src/rwslibs/industrialrobot -B build-smoke -DCMAKE_TOOLCHAIN_FILE=…vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64` 配置成功（exit 0）；构建同三目标 **exit=0** 零错误（`sdurws_ird_kinematics.lib`＋两测试 exe 产出）。
- **测试执行（验收者亲手，两模式各自全量直接运行）**：`sdurws_ird_kinematics_test` → 集成 **12 tests ran，12 PASSED**；冒烟同 **12/12**。`sdurws_ird_kinematics_contract_test` → 集成 **5/5**；冒烟 **5/5**。gtest XML 四份（tests="12"/"5"×两模式，failures/errors 全 0）与 ird-test-report.json 四份（summary total=12/5 passed 同值）落 `builds/wp15-t02-acc/`。

## 4.3 契约 verify 命令 —— 通过（3/3 逐条亲手执行＋2 项验收者侧补做）

| # | 契约 verify 原文 | 验收者执行 | 结果 |
| --- | --- | --- | --- |
| 1 | `cmake --build build --config Release --target sdurws_ird_kinematics` | 于验收者全新集成树 build-int 同参执行（build 树路径不同属验收环境事实；实际三目标一次性构建，产品库为其依赖自然在内） | 成功零错误（exit 0），lib 产出 |
| 2 | `cmake --build build --config Release --target ird_gates` | 同上同参执行 | **exit=1——68 处命中，属 DTB §4.5 存量例外未清零既有状态**（编排者补丁第 2 条口径；与验收者引擎直跑 head 侧自计数 68 一致；非本任务引入，见门禁专段：增量仅 +2 且为已登记 sanctioned 形态） |
| 3 | `pwsh -File RobWork/scripts/industrialrobot/validate-docs.ps1` | 于 worktreeA（送验 SHA 树）执行 | **PASS (20 units, 12 trace entries, 189 task files)** |

补做（验收者侧正常动作）：`validate-task.ps1 -TaskFile tasks/foundation/WP-15-T02.json` → **PASS (1 tasks)**；契约 verify 数组**缺两测试目标显式构建行与 ctest 行**（F-300/F-371/F-373/F-375 家族新例，转登 **F-381**）——验收者已在集成/冒烟两棵全新构建树自行补建两测试目标并执行（12/12＋5/5、`ctest -C Release -L "^ird$"` 于两树 kinematics 构建目录各 **100% tests passed, 2/2**，标签过滤生效）。

## 4.4 acceptance 条目逐项证据 —— 通过（5/5）

1. **双模式构建零错误并留痕（INTERFACE→STATIC、零 Qt、C++17；冒烟带 vcpkg toolchain）**——通过。STATIC 落位＝kinematics/CMakeLists.txt L56 `add_library(sdurws_ird_kinematics STATIC …)`（目标名/别名不变——L115-117 别名仅集成模式）；C++17 显式＝L63-67（`cxx_std_17` PUBLIC＋CXX_STANDARD 17/REQUIRED/EXTENSIONS OFF）；/utf-8＝L74-76；零 Qt＝链接面仅 L105-111 六个 `sdurws_ird_*` 目标（无任何 Qt 目标）＋`KinBuildRedLine.ProductFaceHasZeroQtIncludes_WP15T02_ACC1` 运行期扫描绿＋ird_gates R3 直方图 base 41＝head 41（零增量，kinematics 目标零 R3 命中）。双模式零错误与两测试全绿＝4.2 实测。
2. **二分结构扫描通过（计算库零 Widgets；`_plugin` 不创建随 WP-15-T12；`_test`/`_contract_test` 按 §3.2 注册自持 ird_add_gtest）**——通过。ird_gates 零**未登记**增量（见门禁专段：+2 SUB 为 testkit 报告设施存量允许形态，非 Widgets/结构命中）；`_plugin` 零落位＝单元 CMakeLists 全文无 plugin 目标＋`KinBuildGraph.UnitEdgesSixRegisteredAndClosed_WP15T02_ACC3` 显式断言 `refs.find("sdurws_ird_kinematics_plugin") == end`（同用例并断言 `_worker` 恒不存在——卡 §3.1"不新建"）；两测试目标注册＝L138-142/L214-216（add_executable）＋自持宏 `ird_add_gtest` L190-204（add_test＋XML 报告目标＋EXCLUDE_FROM_ALL）＋LABELS "ird" L248-249；ctest 2/2×两模式实测。
3. **ird_gates 白名单六边登记（六边属 ARCH §3.5 既有许可方向实例化；对落位后现状"零命中"；两文件改动与登记一致）**——通过。六边三面登记实证：①`ird_gates_whitelist.cmake` `IRD_ALLOWED_UNIT_EDGES` 增 `kinematics->core/diagnostics/runtime/policy/evidence/execution` 六行（diff 在案）；②同文件 `IRD_EXTRA_EDGE_REFS_EDGES`/`_NOTES` 成对增登（成对计数 18=18，机械核对）；③`dependency-graph.json` 追加六边（引擎 GRAPH 检查＝白名单↔图一致性面：head 侧 **GRAPH 0 命中**）。「零命中」字面在存量未清零现状下不可达，按 DTB §5.2 DoD 第 2 条「ird_gates 对**本次变更**零命中」口径执行——本任务变更的未登记新增命中为 **0**（+2 均 sanctioned，见门禁专段）；「表外边检出即失败」由引擎 SUB 检查＋`KinBuildGraph.NoBusinessUnitOrExtraPlatformEdge_WP15T02_ACC5` 用例承载。两文件改动一致性：引擎 `ird_gates.cmake` 本任务零改动（其第 0 步注释明文「数据/引擎分离，增补数据不改代码」——六边为纯数据面增补，无需引擎变更），数据面白名单改动与登记逐字一致。
4. **Errors.hpp/DiagCodes.hpp 公共头落位（§9.6 全表 15 码清单登记；注册义务随各消费任务展开）**——通过。Errors.hpp：`KinematicsErrorCode` 恰 4 值（IllegalQ/NoDevice/NoTcp/FrameUnresolved——枚举序＝卡 §9.2 IFkEvaluator @错误 行原文序，逐值注释给出产生接口与落位任务）＋`KinematicsError{code,params,detail}` 值类型（§9.1 非异常出口承载）＋`kinematicsDiagCode` 映射（**全表 nullopt＝零映射行**——契约 acceptance 4 括号「注册义务随各消费任务展开——装配期注册，不预建无消费者条目」明文豁免形态，§9.6 全表任务列最早 T03 实查为凭；头注/提交正文如实登记，非空实现偷懒，4.11② 复核）。DiagCodes.hpp：15 个码值常量（唯一书写点）＋`kinematicsCodeDescriptors()` 全表 15 行描述符——验收者与卡 §9.6 表**逐行 15 行机械核对一致**（码值/行序全同；级别列 6 error＋9 warning 与卡面逐行一致——注意头注散文声称「4 error＋11 warning」失实，转登 F-380，**数据与卡面一致**）；`registerKinematicsCodes(registry)` 真实注册＝`KinDiagCodes.RegistersIntoStableCodeRegistry_WP15T02_ACC4` 构造**真实 diagnostics::StableCodeRegistry** 注册后 find 15/15 命中＋ownerUnit 归属正确＋registeredCodes("kinematics") 反查 15 码＋manifest 含全表；重复注册拒绝＝`DuplicateRegistrationRejected_WP15T02_ACC4`（EXPECT_THROW DiagnosticsError）。测试自持字面清单（kSection96FullTable/kSection96IsError/kExpectedCategoryRows）与实现机械比对，任一侧漂移即失败。
5. **架构红线自查（R-1/R-2/SA-02——ird_gates 零命中为机器证据）**——通过。R-1＝R1 直方图 7→7 零增量＋链接面六边白名单封闭（`UnitEdges…Closed` 用例）；R-2＝产品面唯一跨单元 include 为 `<sdurws/ird/diagnostics/DiagCodes.hpp>`，验收者实查该头位于 diagnostics/include/ 公共面（`BuildRedLine.IncludeFaceRestrictedToSixRegisteredEdges` 的公共头可达性断言与 `IncludeFaceMatchesRegisteredEdges` 双实现互证绿）；SA-02＝4.1 零框架文件＋patches/ 零新增＋引擎 SA02 检查 0 命中。

## 4.5 测试真实失败能力 —— 通过（3 处实质变异全真变红，还原后复绿）

变异窗口＝worktreeA 源树＋冒烟构建树增量重建；每处变异→具名测试真实变红→`git checkout --` 还原；全部还原后全量重建复跑 12/12＋5/5 全绿，`git status --porcelain` 零残留（送验对象未被改动）。

| # | 变异位置与内容 | 被验证测试 | 结果 |
| --- | --- | --- | --- |
| M1 | DiagCodes.cpp 行 11 描述符：KIN-SOLVER-INTERNAL `DiagnosticSeverity::Error` → `Warning`（§9.6 级别列篡改） | KinDiagCodes.DescriptorFieldsMatchSection96Rows_WP15T02_ACC4 | **FAILED**（级别列逐位断言捕获） |
| M3（与 M1 同批） | DiagCodes.cpp 行 6 描述符：KIN-NEAR-LIMIT `requiresComparison` `true` → `false`（§5.5 比较型锚点篡改） | KinDiagCodes.CategoryAndRetryMappingMatchesAnchors_WP15T02_ACC4 | **FAILED**（恰两码 true 断言捕获；同批仅该用例与 M1 目标用例变红，其余 10 用例不误伤） |
| M2 | Errors.cpp token 转发表：`case NoTcp: return "NoTcp"` → `"NoTcpV2"`（§9.2 错误码 token 篡改） | KinErrors.ErrorCodeTokensMatchSection92Row_WP15T02_ACC4 | **FAILED**（全表机械比对捕获，恰 1 用例变红） |

## 4.6 架构红线 —— 通过（AGENTS §5 八条逐条）

零 Qt（链接面/头包含面双零——R3 零增量＋零 Qt 扫描用例绿）；无跨单元私有头（唯一跨单元 include 为 diagnostics 公共头，公共头可达性断言绿）；无业务域互链（链接边恰六条登记边，R1 零增量，业务域七单元＋reporting 表外边断言绿）；无名称前缀拼接（`ProductFaceHasNoRobWorkStringLiterals` 线性状态机剥注释扫描绿，R4 3→3 零增量）；权威归属正确（码值注册权威留 diagnostics——本单元仅产出描述符数据并经真实注册表注册，未改 diagnostics 任何文件；无 HEAD/名称解析语义实现）；不可变历史（本任务零修订写路径触碰）；框架零源码修改（4.1 已证）；`old/` 零引用（diff 无 old/ 路径，实现按卡从零编写）。

## 4.7 注释规范 —— 通过（AGENTS §2 抽查全部 8 个新增源文件＋2 个自有 main＋单元 CMakeLists；1 处注释失准转登建议级 F-380）

- 公共头全查（Errors.hpp/DiagCodes.hpp）：文件头 Doxygen 中文块（设计依据逐条到卡 §/diagnostics §/契约 acceptance 条目；「两个层面的码」业务背景、「映射数据的阶段纪律」豁免依据、P-KIN-7 处置锚点）；每个类/结构/函数中文 Doxygen（@brief/@param 方向与取值/@return 语义/@throws 触发条件）；§2.5 高危信息齐备——线程安全（「纯值/纯函数，并发只读安全」「注册期单线程约定」）、确定性来源（「switch 全枚举编译期固定表，同码同串跨进程逐字节一致——NFR-COR-02」「清单序＝§9.6 表行序」）、所有权（「registry 调用方持有——本函数不接管」）、错误语义（域错误面 vs 诊断面分层、fail-fast 轨道 vs 值返回轨道、nullopt 调用方契约）。
- 实现 TU 全查（Errors.cpp/DiagCodes.cpp）：逐码落值锚点注释（15 码每码给出分类词表锚点/级别出处/重试族依据/比较型点名列）；switch 无 default 保编译器漏项告警的注释说明；makeDescriptor 共字段口径单点陈述。
- 测试文件抽查（4 件全查）：每用例中文说明「验证哪条 acceptance/需求」（IRD_TEST_INFO 需求追溯字段＋注释块）；自持字面清单的「失同步防线」注释。
- 单元 CMakeLists.txt：关键步骤中文注释块（目标注册/链接边/守卫逻辑/宏定义逐段，符合 AGENTS §2.7）。
- **例外（建议级 F-380）**：DiagCodes.hpp L130、DiagCodesTest.cpp L67 与 L137 三处散文声称 §9.6 级别列为「4 error＋11 warning」，而实现数据、测试数据数组与卡面 §9.6 原表**均为 6 error＋9 warning**（error＝NO-DEVICE/NO-TCP/TARGET-ILLEGAL/SOLVER-INTERNAL/CONFIG-ILLEGAL/SAMPLE-IDENTITY-MISMATCH）；送验提交正文同句失实（「级别列 4 error/11 warning 原值」）。行为、数据、测试断言全部正确（断言按 6/9 真值锁定，变异 M1 变红为证），纯散文计数失实——F-379 同族新例（注释与机器事实矛盾），转登不阻断。

## 4.8 文档同步＋findings 相关性 —— 通过

- **卡 v0.2 修订与实现逐项对查为真**：卡头「文档版本」行 v0.1→v0.2（2026-09-26）与「日期」行同步（F-369/F-374 家族教训已吸收——版本括注随推进更新）；「构建落位」字段与 §1.2「代码落位」行改为落位后事实（STATIC＋两头＋两测试目标＋_plugin 随 T12）；§11 T02 行「KinTypes」字样对齐 §3.3 布局表任务列（T02 公共面＝Errors/DiagCodes 两头，KinTypes.hpp 归 T03/T04——卡内两表不一致以布局表为权威的对齐登记，§14.6 v0.2 行明载「无语义变更」，验收者对照 §3.3 表核实成立）；§14.6 v0.2 行所列交付物全部在 diff 内且内容相符。
- **DTB §2.16 WP-15-T02 行**（「计算库＋_plugin」）与本契约范围（_plugin 随 WP-15-T12）的差异由单元卡 §3.1/§11 与契约 acceptance 2 明文承载（卡＝单元内拆分权威），非未登记偏差。
- **findings.json 未关闭条目相关性核对**（v1.2 义务；open 共 310 条，以 kinematics/WP-15/testkit-SUB/verify 缺行/注释失实/提交标题等关键词过滤后逐条判读）：
  - **F-359**（WP-14-T02 引入 +2 门禁 SUB：requirements 两测试目标直链 testkit；登记册回填归 WP-01-T03 治理面）——**本任务再现同型新例**（kinematics 两测试目标 +2 SUB），sanctioned 裁决与 F-359 同口径（见门禁专段），转登 **F-382** 顺延登记；
  - **F-300/F-371/F-373/F-375 家族**（契约 verify 数组缺测试目标构建行/ctest 行）——本契约 verify 仅 3 行，家族新例成立，转登 **F-381**；验收者已按家族在案口径自行补建执行；
  - **F-379**（注释与机器事实矛盾家族）——新例 **F-380**（见 4.7）；
  - **F-378**（门禁比对直方图口径未自洽教训）——本记录与送验附件均已按「三套计数同注」执行（见门禁专段），无新例；
  - F-016/F-359 等其余 open 条目与本任务无所有权交集，维持 open 不消账。

## 4.9 commit 质量 —— 通过（标题格式合规；正文四段齐备且验证结论与留痕一致；1 处散文计数失实并入 F-380）

标题 `[WP-15-T02] 落位 kinematics 计算库真实目标并登记门禁白名单六边`——前缀＋动词开头合规。正文 What/Why/验证/影响面四段＋Acc-Covered: 1,2,3,4,5 齐备；验证段计数与验收者实测及机器留痕逐项一致（12/12＋5/5 两模式、直方图 SUB +4 行/去重 +2 条、引擎自计数 66→68、GRAPH/SA02/SELF 零命中、validate-task/validate-docs PASS）——无粉饰。**偏差**：正文「级别列 4 error/11 warning 原值」一句与机器事实（6/9）不符——散文计数家族（F-041/F-370/F-376/F-380 同段）失实方向为多报 error，测试断言与实现数据均正确，无掩盖性，并入 F-380 登记（提交已冻结不可改，记录留档为凭）。

## 4.10 留痕完整 —— 通过

送验留痕 21 件落 `traceability/builds/wp15-t02/`：gtest XML 两份（tests="12"/"5"，failures="0"，原样留存未修饰）＋ird-test-report.json/ird-test-report-contract.json 两份（summary 12 passed/5 passed）＋双模式构建/配置/测试运行/ctest 口径日志＋ird_gates 引擎直跑 base..head 比对附件（compare.md＋base/head 全量日志＋直方图＋归一化命中行清单）＋validate-task/validate-docs 留痕——验收者独立复现的 XML/报告/门禁行数（139/143、去重 73/75、引擎 66/68）与其逐项一致。验收者自留件 23 件随本记录落 `builds/wp15-t02-acc/`（四 XML＋四报告 JSON＋门禁 base/head 日志与归一化件/直方图＋ctest 日志＋validate 日志＋VS 目标日志）。`phase-one-validation.log` 为 phase-one 专项载体（末条为 CORE-T01 期登记；WP-14 系 T03~T09 同先例不逐任务登记），任务级留痕以 builds/ 目录承载。

## 4.11 偷懒/缩水实现扫描 —— 通过

①TODO/FIXME/stub/placeholder/未实现/not implemented 扫描（diff 新增行，unified=0，产品代码＋单元卡）：**六标记零命中**（送验留痕日志中无新命中；无「触发式延后」豁免需列出）。②空实现检测：`kinematicsDiagCode` 全表返回 nullopt 为**契约 acceptance 4 括号明文豁免**（「注册义务随各消费任务展开——装配期注册，不预建无消费者条目」；§9.6 任务列最早 T03 实查为凭）且头注/提交正文/测试三面如实登记——非未声明缩水；其余函数（token 转发表/15 码描述符工厂/注册转发）均为实表实现，无空函数体。③逐条证据表＝4.4（5/5 均具名测试/命令输出/文件行号级证据，无「已实现」声明采信）。④测试强度：非恒真——4.5 三处实质变异实证真变红；断言校验行为与边界（全表逐行机械比对/真实注册表行为/重复拒绝/红线扫描）而非仅编译通过。

## 门禁专段（ird_gates base..head 归一化比对 —— 增量仅 +2 且为已登记 sanctioned 形态）

口径＝编排者补丁第 2 条（引擎直跑 `cmake -DIRD_ROOT=<industrialrobot 根> -P cmake/ird_gates.cmake`，非 VS 目标；VS 目标 `ird_gates` exit 1 属 DTB §4.5 存量例外未清零既有状态）。验收者独立执行：base 侧 `git worktree add --detach A-base bcf22c08` → 引擎直跑（exit 1，66 处）；head 侧对 A（exit 1，68 处）。归一化＝命中行剥绝对路径前缀（→`<IRD_ROOT>`）＋剥 `[ird_gates] ` 前缀后逐行 diff。

**三套计数同注（F-378 教训：口径不一致须注明——本表三套口径的增量一致，均为 +2，差异仅在重复呈现行是否计数）**：

| 口径 | base（bcf22c08） | head（8b88d08） | 增量 |
| --- | --- | --- | --- |
| ①归一化命中行数（含 message＋汇总重复呈现行） | 139 行 | 143 行 | +4 行 |
| ②去重命中条目（①sort -u） | 73 条 | 75 条 | +2 条 |
| ③引擎自计数（日志末尾汇总原文） | 66 处 | 68 处 | +2 处 |

按码直方图（口径①）：LIB 10/R1 7/R3 41/R4 3/R5 1/SUB 71→75/T1 5/T2 1（其余七码零增量；附件 acc_base/acc_head_histogram.txt）。

**新增命中逐条核对（去重后恰 2 条，head − base）**：`IRD-GATE-SUB: 测试目标 sdurws_ird_kinematics_test 直链他单元产品目标 sdurws_ird_testkit（…）` 与 `…_contract_test 直链 …sdurws_ird_testkit（…）`。

**验收者独立裁决：sanctioned（已登记允许形态，非违规）**——依据：①units/testkit.md §2.4 T-1 允许形态「测试目标 → {同单元产品目标, sdurws_ird_testkit, gtest 系}」（引擎白名单文件头同文）；②DTB §4.5 例外登记册在案行「R-5（同上）各 `_test`/`_contract_test` 经 testkit/policy 替身消费｜§5.3；POL-T11｜**生效**｜2026-09-10」；③与 base 侧既有同型命中完全同模式（其余各单元 `_test`/`_contract_test`→testkit 的 SUB 命中在 base 同样在案——存量 71 条 SUB 中的主体）；④先例 F-359（WP-14-T02 同型 +2，同口径裁决）。产品目标零 testkit 由 `NoTestkitEdgeOnProductTarget_WP15T02_ACC3`＋配置期守卫双面钉住。登记册回填（把 kinematics 两实例并入该模式条目）归 WP-01-T03 治理面待办——转登 F-382 顺延跟踪。**GRAPH/SA02/SELF：head 侧零命中**（白名单↔dependency-graph 一致、框架工作区零登记外改动、引擎自测通过）。

## 建议级问题转登清单（findings.json，编号顺延自 F-379）

| 编号 | 一句话摘要 | 家族/依据 |
| --- | --- | --- |
| F-380 | DiagCodes.hpp L130＋DiagCodesTest.cpp L67/L137 三处注释与送验提交正文均声称 §9.6 级别列「4 error＋11 warning」，实现/测试数据/卡面原表实为 6 error＋9 warning——数据与断言正确、纯散文计数失实（F-379/F-041/F-370/F-376 家族新例） | AGENTS §2.5；4.7/4.9 |
| F-381 | tasks/foundation/WP-15-T02.json verify 数组仅 3 行，缺 sdurws_ird_kinematics_test/_contract_test 两测试目标构建行与 ctest 行（F-300/F-371/F-373/F-375 家族新例；验收者已在两棵全新构建树补建执行 12/12＋5/5＋ctest 2/2） | F-300 家族；4.3 |
| F-382 | WP-15-T02 引入同型 +2 门禁 SUB（kinematics_test/_contract_test 直链 sdurws_ird_testkit）——sanctioned 形态（testkit.md §2.4 T-1＋DTB §4.5「各 _test/_contract_test 经 testkit/policy 替身消费」生效登记），登记册回填归 WP-01-T03 治理面（F-359 同族新例） | F-359 同族；门禁专段 |

## 验收者环境说明

- Windows 10（Git Bash＋pwsh 7）；MSVC VS2022 x64（MSBuild 17.12）；Qt 6.11.1 msvc2022_64；vcpkg 经典模式（GTest CONFIG，x64-windows）。
- 残留进程检查：tasklist 实查无 RobWorkStudio/msbuild/ctest 残留进程；未杀任何他人会话进程；本验收全部构建发生在自建临时目录，无锁冲突。
- 推送口径：显式 refspec `git push origin HEAD:refs/heads/acc/WP-15-T02/1`＋`git ls-remote origin refs/heads/acc/WP-15-T02/1` 核实 SHA 一致（编排者补丁第 6 条）。
- 本记录与 F-380/381/382 转登、验收者证据件（builds/wp15-t02-acc/ 23 件）为同一提交。
- base 侧临时 worktree（A-base）比对完成后即删；A/B 两 worktree 在确认推送成功后删除（ACC §3）。
