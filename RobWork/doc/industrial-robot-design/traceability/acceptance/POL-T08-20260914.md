# POL-T08 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | POL-T08 实现关节限位与行程阈值评估（unit=policy，masterWp=WP-07-T08） |
| 送验对象（冻结） | `2edfdf01455c6508dd0e11c8de0c1acc1e5ea279`（分支 wp07-t08；`origin/wp07-t08` 尖端经核与该 SHA 一致——无分支漂移） |
| diff 范围 | `5bf781e1..2edfdf01`（state.base..headSha；merge-base 复核一致） |
| 验收协议 | acceptance-protocol.md v1.5（ACC），§4 全部 11 项逐项出证据 |
| 裁决 | **pass**（4.1~4.11 全部通过；无阻断级问题；建议级 2 条已转登 `traceability/findings.json` F-085/F-086，不阻塞合入） |
| 验收环境 | detached worktree `D:\10_Source_Repos\21_robot\acc-worktrees\pol-t08-a` @ 2edfdf01（送验 SHA）；集成模式全新配置（`RWS_BUILD_INDUSTRIALROBOT:BOOL=ON` 经 CMakeCache grep 确认）＋vcpkg toolchain＋Qt 前缀（F-062 登记的 worktree 复现前置——两件 ini 本地资源从主树复制）；冒烟另配 `build-smoke`（带 toolchain）。全部构建/测试/门禁/探针由验收者亲手执行，未采信实施者运行结果 |
| 记录分支 | `acc/POL-T08/1`（attempt=1，state.attempts.accept） |

---

## 4.1 改动范围 — PASS

`git diff --name-status 5bf781e1..2edfdf01` 共 8 个变更文件，逐一对照契约 `allowedFiles`：

| 文件 | 状态 | 归属 |
| --- | --- | --- |
| `RobWork/RobWorkStudio/src/rwslibs/industrialrobot/policy/CMakeLists.txt` | M | `policy/**` ✓ |
| `policy/include/sdurws/ird/policy/JointLimits.hpp` | A | `policy/**` ✓ |
| `policy/src/JointLimits.cpp` | A | `policy/**` ✓ |
| `policy/test/JointLimitsTest.cpp` | A | `policy/**` ✓ |
| `traceability/builds/wp07-t08/ird-test-report.json` | A | `traceability/**` ✓ |
| `traceability/builds/wp07-t08/sdurws_ird_policy_test-integration.xml` | A | `traceability/**` ✓ |
| `traceability/builds/wp07-t08/sdurws_ird_policy_test-smoke.xml` | A | `traceability/**` ✓ |
| `doc/industrial-robot-design/units/policy.md` | M | allowedFiles 显式列出 ✓ |

- `forbiddenFiles`（REQUIREMENTS.md / ARCHITECTURE.md）：diff 中零触碰 ✓。
- 框架源码（RobWork/、RobWorkSim/、RobWorkStudio 其余）：diff 范围零文件；`patches/` 目录 diff 为空（无新登记，SA-02 无涉）✓。

## 4.2 可复现构建 — PASS（环境说明见注）

- **集成模式（worktree A 全新树）**：`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON`（附 F-062 前置：`-DCMAKE_TOOLCHAIN_FILE=<vcpkg>`＋`-DCMAKE_PREFIX_PATH=<Qt6.11.1>`＋复制两件 ini 模板）配置成功，缓存 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`（grep 实证）。日志：`acc-worktrees/configure-integration.log`。
- 全树 Release 构建 exit 1＝**73 处 error 全部位于 `RWSimulatorPlugin`**（rw/math/Math.hpp、VelocityScrew6D.hpp、Parallelizer.h 的 min/max 宏污染簇）——即 findings **F-008** 登记的框架基线缺陷（基线对照同样失败），**ird 面 0 错误**（`grep -ci "industrialrobot|ird_"` = 0）。按 F-036/F-054 家族既定处置口径以**任务目标面从零构建**补强：
  - 契约 verify 目标 `cmake --build build --config Release --target sdurws_ird_policy_test` **从零编译零错误** exit 0（产物 `build/RobWorkStudio/bin/Release/sdurws_ird_policy_test.exe`）；日志 `acc-worktrees/verify-target-build.log`。
- **冒烟模式**：`cmake -S .../industrialrobot -B build-smoke -DCMAKE_TOOLCHAIN_FILE=<vcpkg>` 配置＋`cmake --build build-smoke --config Release` 构建 exit 0（日志 `acc-worktrees/smoke-configure.log`／`smoke-build.log`）。
- **测试（验收者亲手运行）**：集成 **166/166 PASSED**（`[==========] 166 tests from 29 test suites ran`；复跑 XML 顶格 `tests="166"`、`failures="0"`）；冒烟 **112/112 PASSED**（21 suites；存档 XML `tests="112" failures="0"`）——与送验 ird-test-report.json 数值主张逐项一致。送验存档 XML（builds/wp07-t08/ 双份）顶格计数同 166/112 零失败。
- 门禁 `ird_gates`：exit 1＝**5 处命中**，逐条解码为 evidence_test/evidence_contract_test/runtime_test→testkit（IRD-GATE-SUB ×3）＋testdata_lint→testkit（SUB＋T1）＝F-019×2＋F-051×1＋F-072×2 **既有基线，零新增、零 JointLimits 相关条目**——与送验"GATE_DELTA_ZERO"主张一致（日志 `acc-worktrees/ird-gates-run.log`）。

## 4.3 契约 verify 命令 — PASS

契约 `verify` 数组仅 1 条：`cmake --build build --config Release --target sdurws_ird_policy_test`——验收者在 worktree A 全新树亲手执行，exit 0（见 4.2）。无其他条目漏执行。

## 4.4 acceptance 条目逐项核对 — PASS（3/3 有证据）

| # | 契约条目 | 证据（具名测试/输出） |
| --- | --- | --- |
| 1 | POL-JNT-1/2：行程上限 4π 边界＋比较型三要素用例通过 | `TravelAtFourPiBoundaryIsNotExceeded_POL_JNT_1`（T＝L 无发现＋裕量解析算例 margin=2π/r=1.0）、`TravelAboveFourPiCarriesComparativeTriple_POL_JNT_1`（actual=6π/threshold=4π `EXPECT_DOUBLE_EQ` 逐位精确、Must、rad）、`ContinuousJointExempt…`、`ExplicitSixPi…`、`NearLimitBoundary…`、`LimitViolationClosedIntervalBoundary…`、`MarginValuesFollowSpecFormulaAndOrdering…`、`DeterministicOutput…`——16 用例集成模式逐条执行通过（166/166 内） |
| 2 | 检出非法为诊断级不阻断（D-13） | `IntervalInvalidIsDiagnosticLevelAndSkipsJoint_POL_JNT_2`（Completed＋finalized=true、跳过三证：无裕量/行程未评/合法关节全评）、`EngineeringRangeInvalidIsDiagnosticLevel_POL_JNT_2`（min≥max 与 NaN 端点两形态；Invalid 态保留 "nan" 原文） |
| 3 | O-10/P-POL-2 保守口径：nullopt→显式 NotApplicable（消账归需求所有者） | `UnsetThresholdsOutputExplicitNotApplicable_POL_JNT_2`（双标记＋cause 引 P-POL-2＋贴限构型零 NearLimit＋裕量仍 Provided＋六成员结构化绑定钉输出形）、`TravelCheckDisabledEmitsExplicitMarker`（开关关闭标记＋8π 对照）；契约注记"消账归需求所有者"在报告 acceptanceCoverage.3 原样保留（未被宣称消账）✓ |

无任何条目虚标通过；ird_gates 结论如实登记为"5 命中＝既有"（非零命中粉饰为通过）。

## 4.5 测试真实失败能力 — PASS（双探针）

对实现文件 `src/JointLimits.cpp` 注入两处 mutation（改后重建目标→跑用例→`git checkout` 还原→重建复跑）：

1. 探针 1：行程比较 `travel > travelLimitRad` 改 `>=` → `TravelAtFourPiBoundaryIsNotExceeded_POL_JNT_1` 与 `ExplicitSixPiThresholdPassesMultiTurnJoint_POL_JNT_1` **双双 FAILED**（T＝L 边界语义被真实验证）。
2. 探针 2：近限位 `ratio < nearRatio` 改 `<=` → `NearLimitBoundaryIsStrictLessThanWithShouldLevel_POL_JNT_2` **FAILED**（r＝阈值不警告的严格小于边界被真实验证）。

还原后 `git status --porcelain` 零残留，重建复跑 **166/166 复绿**。断言非恒真、非"只编译通过"（F-075 纪律：mutation 落在 .cpp，消费 TU 随重编，无头文件增量陷阱）。

## 4.6 架构红线（AGENTS §5 八条） — PASS

1. **L2 零 Qt**：三个新文件 grep `Qt|Q_OBJECT|QWidget` 零命中 ✓。
2. **R-1 业务域互链**：include 面仅 `ird/core/*`、`ird/policy/*`（本单元）＋std/gtest；grep 七个业务域命名空间零命中 ✓。
3. **R-2 跨单元私有头**：include 均为 `include/sdurws/ird/<unit>/` 公共路径 ✓。
4. **SA-02 框架零修改**：4.1 已证 ✓。
5. **R-4 名称拼接/剥离**：`src/JointLimits.cpp` "RobWork" 字符串计 0（零字面量 TU）；hpp 内 RobWork 仅存于注释；无名称拼接/剥离代码形态 ✓。
6. **权威唯一（PA-1）**：阈值唯一来自 evaluate 传入的 EngineeringPolicySet（API 无阈值参数——KIN-13/R-POL-5）；评估期建议码为 JointLimits.cpp 单点字面量＋§9.6 补登注记＋POL-T10 迁码表通道（与 CollisionQuery.cpp 同模式、单元卡 v0.9⑧ 登记），码值权威归 diagnostics ✓。
7. **不可变历史（PA-2）**：本任务无修订写路径涉足 ✓。
8. **不恢复 old/**：无 old/ 引用 ✓。
- 备注：JointLimits.hpp include CollisionQuery.hpp 复用 `CollisionEvaluationStatus` 词表使公共头间接携带 rw include 面——文件头已按"集成模式专属 gating"登记（CMakeLists 中 src/test TU 均在 `if(TARGET sdurw_kinematics)` 门内，冒烟无 TU include 新头，实测冒烟测试面 112 用例不变），为 §9.4 原文口径的执行，不构成红线违反。

## 4.7 注释规范（AGENTS §2） — PASS

- **公共头全查**（JointLimits.hpp，468 行）：文件头（设计依据＋背景"第一读者须知"＋实现纪律）、每个 struct/class/enum/函数中文 Doxygen 齐备；§2.5 高危信息逐项在位——物理单位（SI rad/无量纲逐字段标注）、角度制式（"rad（不是度）"）、坐标系语义（关节位置 SI rad 口径）、线程安全（每个类型"线程安全：纯值"/评估器并发只读）、错误语义（调用方 fail-fast vs 评估级 Failed＋诊断码分类）、确定性来源（规范序/稳定排序键/首个命中样本约定）✓。
- **实现文件**（JointLimits.cpp，596 行）：文件头＋七步执行序逐步注释＋每分支业务场景说明＋魔法数解释（%.17g、rad token 来源）✓。
- **测试文件**（JointLimitsTest.cpp，844 行）：文件头含用例追溯命名与范围声明；每个 TEST 注明验证的条款/AT 编号；断言处行内注释 ✓。

## 4.8 文档同步＋findings 核对 — PASS

- 实现与任务卡**零语义偏差**；实现增量（notApplicableChecks 载体 JointCheckNotApplicable、查询/裕量记录成员清单、首个命中样本约定、margin 排序键、诊断码单点）已按 DTB §5.4 以 **units/policy.md §15.4 v0.9（2026-09-14）** 增量修订登记（九项细节），§9.6 码表补登注记同步（diff 实证）——"代码先行、文档失实"不存在。
- 卡行对照：§12 POL-T08 行（产物 `JointLimits.hpp/.cpp`、验证 POL-JNT-1/2、完成条件"行程上限边界（4π）与比较型三要素用例通过"）与交付一致；§11 POL-JNT-1/2 用例行逐项有对应用例。
- `validate-task.ps1 -TaskFile POL-T08.json` 验收者亲手复跑：**PASS (1 tasks)**；`phase-one-validation.log:34` 含 `POL-T08.json: validate-task: PASS (1 tasks)` 条目 ✓。
- **findings.json 未关闭条目相关性核对**（v1.2 要求）：open 面中与 policy 链相关者——F-075（探针纪律，本验收 4.5 已按其处置执行）、F-076/F-079/F-082/F-084（提交标题家族）、F-077/F-080/F-081/F-083（留痕清单/gitignore 家族）、F-078（policy.md 转义损毁第四例——本任务 diff 未触碰损毁节，维持家族 DOC 治理归口，不顺手消账）、F-019/F-051/F-072（ird_gates 既有命中，与本任务门禁结论直接相关——本验收实测与三者登记逐一吻合）。**本任务复核现 F-081/F-083 根因第四次踩中＋F-084 家族同型再现**，转登 F-085/F-086（见问题清单）。

## 4.9 commit 质量 — PASS（附建议级 G-2）

三提交（5b7f272e→8011f16e→2edfdf01）逐一核查：

- 标题均带 `[WP-07-T08]` 前缀：『实现关节限位与行程阈值评估…』『构建 POL-JNT-1/2 关节限位评估用例组（16 用例）』动词开头合规；第三提交『**留痕与文档同步（双模式证据＋policy.md v0.9 实现登记）**』以并列名词短语开头，非动词开头——F-084 所引 POL-T07 提交 e5ad5960 的同字面形态再现（建议级 G-2→F-086）。
- 正文四段（What/Why/验证/影响面）三提交齐备；验证结论与留痕核对：166/166、112/112、gates 5 既有、validate-task PASS 全部与验收者亲手复现一致，**无粉饰**。
- 计数口径：提交 2edfdf01 称"验证留痕 11 件"并列 8 件 .log 文件名——与 4.10 的 G-1 同源（见下），一并归 F-085。

## 4.10 留痕完整 — PASS（附建议级 G-1）

- 在库证据本体：`traceability/builds/wp07-t08/` 双模式 gtest XML（166/0 与 112/0，顶格实测）＋ird-test-report.json（两目标/16 用例逐条 trace/gates/testRuns/acceptanceCoverage 全节）——**全部数值主张经验收者亲手复现准确**。
- `phase-one-validation.log` 已更新（L34 POL-T08 PASS 条目）✓。
- **G-1（建议级）**：`git ls-tree 2edfdf01` 实证 wp07-t08/ 仅 **3 件入库**；提交正文声称的 integration-build/verify-target-build/integration-test-run/smoke-configure/smoke-build/smoke-test-run/gate-all/validate-task 八件 `.log` 被根 `.gitignore:87` `*.log` 规则静默吞掉（`git check-ignore -v` 实证命中），ird-test-report.json 的 gates.log/builds.*/testRuns.*.log/validateTask.log 字段引用的文件均未入库——**F-010 根因第四次踩中（F-081 第二次、F-083 第三次之后）**，"声称在库而实际未入库"方向（F-083 同型）。判级沿 F-081/F-083 先例：证据本体完整且全量复现，建议级不阻断→F-085。

## 4.11 偷懒/缩水实现扫描 — PASS

1. **标记扫描**：三个新文件 `TODO|FIXME|stub|placeholder|未实现|not implemented` 零命中（grep exit 1 实证）。
2. **空实现检测**：公共接口（IJointLimitEvaluator::evaluate／makeJointLimitEvaluator）均有完整实现；evaluate 596 行七步执行序无空函数体、无仅返回默认值的桩。
3. **acceptance 逐条证据表**：见 4.4——三条均有具名用例＋验收者实测，任一不缺。
4. **测试强度**：断言校验行为与边界（EXPECT_DOUBLE_EQ 逐位解析算例、闭区间/严格小于/4π 边界、operator== 逐字段相等、六成员结构化绑定防成员漂移、非空性自检防"相等退化为双方全空"）；4.5 双探针实证非恒真。

---

## 问题清单

### 阻断级

无。

### 建议级（已强制转登 traceability/findings.json）

- **G-1 → F-085**：留痕 `.log` 被 `.gitignore` 吞（F-010 根因第四次踩中，F-081/F-083 同型）——8 件 .log 未入库而提交正文声称在库；ird-test-report.json 六处字段引用未入库文件。根因修复裁决（三选一）沿 F-081/F-083 既登记口径，四次踩中后建议与 F-010 一并提级处置。
- **G-2 → F-086**：提交 2edfdf01 标题『留痕与文档同步（…）』名词短语开头，非动词开头——F-012 家族（含 F-084 同字面先例）再现；正文四段齐备、无粉饰，纯风格瑕疵。

## 裁决依据（ACC §5）

4.1~4.11 全部通过且无阻断级问题 → **pass**。建议级 G-1/G-2 已按 v1.2 强制转登 findings（F-085/F-086，编号顺延自 redesign-main 侧 F-084，合入时由所有者确认与并行 evidence 分支无撞车），不阻塞合入。合入决策归所有者，本验收者不执行合入、未修改实施产物、未向 wp07-t08 推送任何提交（送验对象保持冻结 @ 2edfdf01）。

## 验收者环境说明

- 复现现场：`git worktree add --detach <A> 2edfdf01455c6508dd0e11c8de0c1acc1e5ea279`（ACC §3 v1.3）；集成配置需 F-062 登记的 worktree 前置（vcpkg toolchain＋Qt CMAKE_PREFIX_PATH＋两件 RobWorkStudio ini 本地资源复制）——该前置仍未成文，第四次在验收复现中实际发生（F-062 持续 open）。
- 验收产生的探针已全部还原（git 零残留）；worktree A 为临时现场，记录回传经本分支（acc/POL-T08/1）完成。
- 本记录与 findings.json F-085/F-086 登记同分支提交；commit SHA 见三元组输出。
