# IO-T04 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 ID | IO-T04（JSON 读写器＋ZIP 通道〔P-IO-3 冻结后〕，≙WP-11-T05；契约 tasks/foundation/IO-T04.json） |
| 送验对象（冻结） | 595737049abff486e823436a0690afe4553e099a（分支 wp11-t04 尖端；base＝c96138060650d7a9b360b21cdb361130b154a93c——P-IO-3 选型冻结裁决，即 state.base） |
| commit 范围 | c9613806..59573704（三笔实施提交：e8240fb8 JSON 读写器〔1/3〕、d820e5dd ZIP 通道〔2/3〕、59573704 文档同步与验证留痕〔3/3〕） |
| 分支漂移检测 | origin/wp11-t04 实测＝595737049abff486e823436a0690afe4553e099a（git rev-parse 核实）——零漂移，验收全程按该 SHA（ACC §3 送验对象冻结） |
| 验收环境 | 全新 detached worktree（`git worktree add --detach` @ 59573704＝`C:/Users/zgl18/AppData/Local/Temp/io-t04-wt-a`）从零复现：集成 build 树（worktree A 内 `build/`，重新配置后缓存实测 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`）与独立冒烟 build 树（`build-smoke/`，带 vcpkg toolchain）均为本验收新建（非主树缓存复用）；记录回传 worktree（acc/IO-T04/1 基于 origin/redesign-main@c9613806）。MSVC 2022 x64＋vcpkg 经典模式。**环境前置三件**（worktree 干净检出的既有环境缺口，非实施缺陷——F-007/F-062/F-099 已登记同型，IO-T03 验收记录同款处置）：①集成配置需 `-DCMAKE_TOOLCHAIN_FILE=<主仓>/vcpkg/scripts/buildsystems/vcpkg.cmake`（主树 CMakeCache 实测同源）；②需 `-DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`（主树缓存实测）；③`RobWorkStudio/bin/` 下 `RobWorkStudio.ini.template.static`/`RobWorkStudio.ini.shared.in` 被 `.gitignore` `[Bb]in/` 规则吞、干净检出缺失，自主仓补拷（未跟踪环境文件，不入任何提交）。补齐后配置零错误。独立性（ACC §2 四条逐项满足）：全新上下文只读代码/契约/单元卡/在库留痕，未读取实施者过程叙述；对抗式立场逐项找反证；全部构建、测试、门禁脚本核查、两枚失效注入均为本验收者亲手执行（ACC §2.4 复现优先于采信——实施留痕数值仅作交叉比对，不作采信依据） |
| verdict | **pass**（4.1~4.11 全部通过、零阻断级问题；建议级 1 项新发现转登 F-193——提交标题前缀风格，见 4.9；F-191/F-191 相关性核对见 4.8） |
| 验收日期 | 2026-09-17 |

## 4.1 改动范围 —— 通过

- `git diff --name-only c9613806..59573704` 实证 **24 个变更文件**（`--stat`：6270 insertions/31 deletions），逐个比对契约 `allowedFiles`：
  - `RobWork/RobWorkStudio/src/rwslibs/industrialrobot/io/` 下 10 个（CMakeLists.txt、include/sdurws/ird/io/{IoError,Json,ZipChannel}.hpp、src/{IoDiagnostics,Json,ZipChannel}.cpp、test/{IoDiagnosticsTest,JsonTest,ZipChannelTest}.cpp）——全部落在通配 `io/**` 内；
  - `RobWork/doc/industrial-robot-design/units/io.md`——契约显式允许；
  - `traceability/builds/wp11-t04/` 11 件留痕＋`traceability/findings.json`——落在通配 `traceability/**` 内。
- **forbiddenFiles 零触碰**：REQUIREMENTS.md 与 ARCHITECTURE.md 不在 diff 中（同命令实测零命中）。
- **无未登记的框架源码改动（SA-02）**：`git diff --name-only c9613806..59573704 | grep -v -E 'industrialrobot|industrial-robot-design'` 输出为空；`*patches*` 路径 diff 为空——patches/ 无新文件即无登记，红线自洽。

## 4.2 可复现构建 —— 通过（双模式零错误，验收者从零亲手复现）

- **集成模式（重新配置）**：worktree A 内全新 `build/`（清空重配），命令 `cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DCMAKE_TOOLCHAIN_FILE=…/vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64 -DRWS_BUILD_INDUSTRIALROBOT=ON`——exit 0（28.4s），`grep RWS_BUILD_INDUSTRIALROBOT build/CMakeCache.txt` 输出 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`（ACC §3 要求的确认项）。构建 `cmake --build build --config Release --target sdurws_ird_io_test`——exit 0；`grep -E 'error C[0-9]+|fatal error|LINK : fatal'` **零命中**（warning 仅框架源码 LinearAlgebra.cpp 的 Eigen C4996 弃用提示，非本任务产物）。产物 `build/RobWorkStudio/bin/Release/sdurws_ird_io_test.exe` 就位。
- **独立冒烟模式**：`cmake -S RobWork/RobWorkStudio/src/rwslibs/industrialrobot -B build-smoke -DCMAKE_TOOLCHAIN_FILE=…/vcpkg.cmake`——exit 0；`cmake --build build-smoke --config Release --target sdurws_ird_io_test`——exit 0 零 error。
- **测试执行（双模式）**：集成 `sdurws_ird_io_test.exe` `[==========] 75 tests from 7 test suites ran. [PASSED] 75 tests.`；冒烟同 75/75。与本任务 io.md §15.5 v0.7 及 ird-test-report.json 声明数值一致（75＝既有 40＋IoJsonTest 26＋IoZipTest 9）。

## 4.3 契约 verify 命令 —— 通过

- 契约 `verify` 数组仅一条：`cmake --build build --config Release --target sdurws_ird_io_test`——验收者在集成构建完成后**独立再次执行**，exit 0，增量输出 `sdurws_ird_io.vcxproj -> …\sdurws_ird_io.lib` 与 `sdurws_ird_io_test.vcxproj -> …\sdurws_ird_io_test.exe`（唯一提示为 MSB8029 中间目录位于系统 Temp 的环境提示——验收 worktree 选址所致，非实施缺陷）。
- 配套治理脚本口径核对：在库留痕 validate-task.log＝`validate-task: PASS (1 tasks)`、verify-task.log＝io 构建链零 error 实录（主树路径），与本验收者亲手复跑结论一致。

## 4.4 acceptance 条目逐项核对 —— 通过（5/5，逐条具名证据）

| # | 契约 acceptance 条目 | 验收者亲证证据 |
| --- | --- | --- |
| 1 | **V07 全绿：SchemaUnknownFuture**——未知字段拒（Preserve 名单外）、schemaVersion=999 未来版本只读拒绝＋升级指引数据、重复键/缺必填定位（错误码＋JSON 路径＋行列区间） | 实读测试断言（test/JsonTest.cpp L199~305）：`SchemaUnknownKeyRejectedWithLocation` 断言 code=FormatJsonUnknown＋path=`$.bogus`＋row=3＋column 非空；`PreserveSubtreePassedThrough` 断言 ext 子树 futureKey 透传可读；`SchemaVersion999FutureRejectWithUpgradeData` 断言 FUTURE＋params `file-version=999`＋`supported=1..1`＋detail 含 "project"（升级器归属注记）＋失败 value 默认构造（无部分 DOM 外泄）；`DuplicateKeyRejectedWithLocationAndNestingScoping` 断言 DUPKEY＋path=$/`$.o`＋第二次键 token 列=8 钉住＋`\u0061` 解码后同名判重；`RequiredMissingLocatedAndNoDefaultInjection` 断言 REQUIRED＋key=a＋path=$。双模式实测全过（75/75 含上述用例） |
| 2 | **V08 全绿：NaNInfLimits**——NaN/Inf/1e999 溢出拒、超长字符串/深嵌套触发 IO-SEC-BUDGET-JSON*（诊断含实际值/上限——比较型三要素） | 实读测试断言（JsonTest.cpp L402~521）：`NanInfLiteralsRejectedAsNumber` 断言三种具名记号→NUMBER＋写出侧 isfinite 断言（程序化非有限 Real 拒写）；`NumberOverflowRejectedUnderflowAccepted` 断言 1e999→NUMBER、1e-999→0 接受、1e308 合法；`DeepNestingTriggersDepthBudget` 断言 SEC-BUDGET-JSON-DEPTH＋actual=65/limit=64/unit=levels（先比后递归）；`OversizedStringTriggersStringBudget` 断言 SEC-BUDGET-JSON-STRING＋limit=16Mi/unit=chars；`DocBytesBudgetChargedThroughGuard` 断言调用方 tighten(16) 经 budgetScope 记账拒（actual=18/limit=16/unit=bytes）＋预算内同 scope 放行（记账通道非旁路比较）。双模式实测全过 |
| 3 | **canonical 输出字节一致；manifest/内容摘要全经 core ContentDigester（SHA-256 唯一——第二摘要算法禁止 SA-12/NFR-MNT-03）** | 实读测试断言（JsonTest.cpp L613~719）：`CanonicalSameSemanticsSameBytes` 断言同语义异写法（键序/空白/1 与 1.0/5e-1 与 0.5）canonical 字节全等＋摘要随字节全等；`CanonicalDeterministicAndDigestViaCoreDigester` 断言同 DOM 两次全等＋版面钉住（2 空格缩进有/LF 无 CR）＋digestCanonicalJson 恒等于对 canonical 字节手工经 core ContentDigester 复算（摘要算法唯一性的通道证据）；`CanonicalKeyOrderProfileDeclaredVsLexicographic` 断言键序二态。实现侧实读：Json.cpp canonical 序列化器 std::to_chars（L1548~1565）；ZipChannel.cpp verifyOne 哈希复算恒经 core ContentDigester（L552~554）；`grep -rn 'ContentDigester'` 双文件命中一致，io 产物无第二哈希实现（ZipChannel.cpp renderDigestHex 为 hex 渲染非哈希原语——L179 注释显式澄清）。双模式实测全过 |
| 4 | **ZIP 通道：解包逐字节还原＋manifest 条目哈希校验可用（V14 前置）；JsonLite 不用于产品格式** | 实读测试断言（ZipChannelTest.cpp L272~400）：`StoreAndDeflateEntriesByteExactRestore` 断言 STORED/DEFLATE/空条目逐字节还原（二进制 NUL/高位字节安全；libzip 写侧构造与被测读侧互为独立实现）＋条目枚举元数据＋不存在条目 REF-INCOMPLETE（ref）；`ManifestHashVerifyOkMismatchLocatedAndRefIncomplete` 断言全一致逐条目 Ok／篡改声明摘要→HASH-MISMATCH（entry/expected/actual、其余条目不牵连）／幽灵条目→REF-INCOMPLETE。**JsonLite 禁入产品**：`grep -rn -i 'jsonlite\|testkit' io/src/ io/include/` 仅命中 Json.hpp 文件头注释（说明"为什么不用 JsonLite"的背景），零 include 零类型引用。双模式实测全过 |
| 5 | **P-IO-3 处置（blocked 触发条件）**——选型冻结后方可实施，不私选第二渠道 | 冻结时序核实：base 提交 c9613806＝[governance] P-IO-3 选型冻结裁决（ZIP=libzip 1.11.4、XML=expat 2.8.3，governance-log §1.9），**在先**于三笔实施提交——未在未冻结状态开工。执行面核实：io/CMakeLists.txt `find_package(libzip/expat CONFIG REQUIRED)`（REQUIRED＝不可用即配置失败，失败即停）＋ PRIVATE 链接（libzip 类型不出公共面——ZipChannel.hpp 文件头注落实）；`grep` io 产物零其它第三方 JSON/ZIP 通道（无 nlohmann/rapidjson/miniz 等第二渠道）；门禁 3 处新 LIB 命中如实登记 F-191 归门禁所有者（未越权改词表——词表不在 allowedFiles）。登记链核实：io.md §15.5 v0.6/§3.3 v0.7、findings F-191 三处闭环 |

## 4.5 测试真实失败能力 —— 通过（两枚失效注入均验证测试变红，注入后已还原、复绿 75/75）

- **注入 1（ZIP 哈希比对失效）**：worktree A 内将 ZipChannel.cpp verifyOne 的 `if (actual != entry.sha256)` 改为 `if (false)`，重建后 `IoZipTest.ManifestHashVerifyOkMismatchLocatedAndRefIncomplete` **FAILED**——哈希校验断言真实（恒真测试排除）。随后 `git checkout --` 还原。
- **注入 2（JSON 版本门失效）**：首次将 `if (fv > hi)` 改 `if (false)`，测试仍绿——原因是实现存在"界内空洞→FUTURE"兜底分支（L1198），单一分支失效被兜底掩盖（该多层防御本身是质量加分项）；改为在 checkSchemaVersion 函数体入口注入 `return IoError{};`（版本门整体失效）后，`SchemaVersion999FutureRejectWithUpgradeData`、`SchemaVersionLegacyReject`、`VersionGateMissingTypeAndPrecedesSchema` 三用例 **FAILED**——版本判定断言真实。首次注入位置误置于函数签名与 `{` 之间导致编译失败、跑的是旧 exe——已识别并修正注入方式后重试（编译失败时旧 exe 的运行结果不作为证据）。
- **还原核验**：两处注入 `git checkout --` 还原后 `git status --short -- RobWork/` 干净、重建并全量重跑 75/75 全绿——worktree A 恢复与送验对象一致。
- 说明：注入仅发生于验收临时 worktree A 的构建产物，未触碰任务分支、未推送任何实施产物修改（送验对象 59573704 冻结不动）。

## 4.6 架构红线（AGENTS §5 八条逐条自查）—— 通过

1. **L2 计算内核零 Qt**：`grep -rn -i 'include.*Qt\|QWidget\|QObject' io/{src,include,test}/` 零真实引用（唯一命中为 SafePathTest.cpp 内**红线扫描测试自身的词表字符串**——那是零 Qt 门禁的执行体而非 Qt 引用）；Json.cpp/ZipChannel.cpp include 面均为标准库＋io/core/diagnostics 头。
2. **业务域互链禁止（R-1）**：io 产物 include 的 `<sdurws/ird/*>` 头全集＝core/Digest.hpp、diagnostics/DiagCodes.hpp＋io 自有头（grep 全集清单）——零 modeling/requirements/kinematics/trajectory/dynamics/selection/optimization 引用；跨单元协作面为 JsonProfileRegistry 注册协议（C-6——格式所有者装配期注册、io 执行校验）与 core ContentDigester 消费，均属端口/注入面。
3. **跨单元私有头禁止（R-2）**：所引 core/diagnostics 头均为其 `include/sdurws/ird/<unit>/` 公共头。
4. **框架零源码修改（SA-02）**：见 4.1——diff 排除 industrialrobot 与设计文档后为空，patches/ 无新文件。
5. **不做 RobWork 名称前缀拼接/剥离（R-4）**：`grep -rn 'RobWork' Json.cpp/ZipChannel.cpp/Json.hpp/ZipChannel.hpp` 零命中。
6. **权威唯一（PA-1）**：io 侧只做格式层（语法/安全/版本/结构＋容器层字节）——实现与头注释显式承诺"版本判定与升级器归 project、字段业务语义归格式所有者、io 不注入默认值、九步导入协议归 IO-T06、条目路径裁决归导入器"（Json.hpp/ZipChannel.hpp 文件头与边界承诺段）；未发现越权实现 project/runtime/policy 语义。
7. **不可变历史（PA-2）**：本任务产物无修订/历史写路径（IoErrorCode 表尾追加为只增不改——第 57→58 值，既有值数值与语义零变更，IoError.hpp 注释明示"持久化契约面只增不改"）。
8. **不继承 old/ 历史实现**：`grep -rn 'old/'` 零命中；实现为按 io.md §5.9/§7.1 从零编写（含与 CSV 通道的 DFA 表副本"暂不合流"治理决策注释——L112 表注，非 old/ 复用）。

## 4.7 注释规范（AGENTS §2）—— 通过

- **公共头全查**（Json.hpp 598 行＋ZipChannel.hpp 216 行＋IoError.hpp 增改段＋IoFwd.hpp 未改）：文件头四件齐（设计依据逐条引 io.md §/需求 ID/任务卡、背景说明"为什么自研而不用 JsonLite"／"为什么容器层不是导入器"、线程约束、确定性声明）；每个类型/函数 Doxygen 中文齐备——含参数方向 [in]/返回语义/失败行为/异常条件（如 JsonProfileRegistry::registerProfile 列明 invalid_argument/logic_error 触发条件）；**§2.5 高危信息逐项**：单位（kReadChunkBytes/kCancelWindowBytes "单位字节"、uncompressedSize "字节"、范围/数组界注单位语义）、所有权（registry "调用方不接管"、DOM 归调用方、buffer 存活契约、libzip source/FILE* 逐级回收链）、线程约束（reader 并发安全/writer 会话单线程/registry 装配期单线程注册后并发只读、ZipChannel 会话单线程多会话并行）、错误语义（调用方装配契约违约＝invalid_argument fail-fast vs 环境错误＝IO-RES-* 四分类 vs 防御性内部错误＝IO-FORMAT-INTERNAL，逐处归类）、确定性来源（canonical 固定键序＋to_chars 无本地化＋2 空格＋LF、报告清单序、NFR-COR-01/02 引用）；行列/字节列口径显式（UTF-8 字节列、闭区间、1 起）。坐标系/rad 不适用（io 无位姿/角度量——CSV 通道验收同口径）。
- **实现文件抽查 ≥3**：①Json.cpp（文件头实现结构图＋逐段语义注释——UTF-8 DFA 表注含出处与"两份副本暂不合流"治理决策、解析器关键步骤"先比后递归栈安全"、版本空洞按 FUTURE 的语义理由、canonical 排序策略）；②ZipChannel.cpp（libzip 集成形态段、打开链所有权逐级回收注、加密双源检测注、声明双记账/补差笔的攻击面理由、zip_fclose 错误文本先取的句柄生存期纪律注）；③io/CMakeLists.txt（任务落位史注释链、L1 依赖 REQUIRED/PRIVATE 的封装与链接两全理由、测试链 libzip 的 T-1 合规理由）；④测试两文件（每用例中文说明"验证哪条 acceptance/§/需求 ID"＋恶意样例构造手法注）——抽查均达标。
- 未发现 AGENTS §2.6 反面注释形态（复述代码/无信息量占位/中英机翻/过期注释）；新增 TODO 零（见 4.11）。

## 4.8 文档同步 ＋ findings.json 未关闭条目相关性 —— 通过

- **实现与任务卡偏差零遗留**：逐项实读 io.md v0.7 增量 diff 并与实现交叉比对——①§3.1 Json.hpp 行按实现刷新＋ZipChannel.hpp 新增行（§3.4 布局同名公共头，文件名等价增补按 DTB §5.4 在 §15.5 v0.7 登记——卡面 §3.4 原文为 ZipChannel.cpp 无 .hpp，实现补了同名公共头，已登记）；②§3.3 L1 链接登记落地（libzip::zip＋expat::expat PRIVATE——含 expat 消费方 ResourceIo.cpp 尚随 IO-T05 的先行登记说明）；③§9.12 JSON 族表尾追加 IO-FORMAT-JSON-SYNTAX（IoErrorCode 第 58 值——IoError.hpp 枚举尾追加＋ioCodeDescriptors reserve(56)＋IoDiagnosticsTest 双清单机械比对同步，三处实测一致）；④§9.5 两项等价调整（parse/parseBytes 尾随 budgetScope 参数、IOutputTarget&&→JsonOutputTarget&&）按 §9.0 条款登记——与实现签名逐一吻合；⑤§15.5 v0.7 变更记录行＋文末落款 v0.6→v0.7。任务卡 note 的 acceptance 五条拆分与实现/测试映射齐备（ird-test-report.json testSuites 四组逐用例 trace 与契约 acceptance 1~5 对应）。
- **任务状态/追踪矩阵**：契约 status=ready（翻转归收尾——PIPE §4.7，不属实施者动作）；io.md §14 矩阵 NFR-DEP-04/PM-06/REQ-12/SA-12 行由 v0.7 落位支撑（"设计完成，未执行"的验证状态列语义为系统级 AT——本任务具名用例为其执行侧证据，无矩阵失实）。
- **findings.json 未关闭条目相关性核对**（全库 open 167 条，io 相关过滤＋关键词复核）：
  - 本任务自登 **F-191**（ird_gates LIB 词表未含 libzip::zip/expat::expat——3 处命中归门禁所有者扩词表；本验收者在 gates 留痕中实测该 3 处存在、io 侧零 R-1/R-3/R-4/T-1 命中，登记属实）与 **F-192**（IO-FORMAT-JSON-SYNTAX 向 diagnostics 内置码表收编确认——P-IO-6 同款治理侧在途事项）：均 open、责任方明确、非本任务返工项；
  - io 直接相关历史 open：F-007/F-062/F-099（worktree 环境前置未成文——本验收遭遇的同款三件，按 IO-T03 先例处置，属 governance/环境侧，非实施缺陷）；F-027/F-179/F-185（提交标题风格家族——本任务见 4.9，家族未消）；F-029（IO-T07 契约 dependsOn——后续任务事项）；F-180（io.md 卡头日期行家族——本任务 v0.7 日期行实测 2026-09-17 与实际一致，未再现）；F-186/F-187（门禁留痕载体弱——本任务 gates 日志含 14 处命中明细与自测 fail 用例实录，io 侧持续兑现）；F-153/F-010（.gitignore 吞 *.log——本任务 11 份留痕 `git add -f` 强制入库，实测 11 件均在库：`git ls-tree 59573704 -- traceability/builds/wp11-t04/` 与 diff 清单一致——教训已吸收）；F-189（Csv.hpp @param 重复——IoCsv 通道文件，本任务零改动，保持 open）；F-190（编排合并误落——governance，与本任务产物零交集）；F-185/F-187 等其余经 title 复核与本任务产物无直接关联。
  - 结论：无历史发现静默积压；无可随本任务顺手消账条目（上述 open 条目责任方均非 io 实施侧）。

## 4.9 commit 质量 —— 通过（三提交四段齐备、结论与留痕一致、零粉饰；1 处建议级风格瑕疵转登 F-193）

- 三笔提交逐份实读（git log --format='%B'）：标题均为动词开头中文摘要；正文四段（改动内容按文件列点/改动原因对应 acceptance 与设计节/验证结果/影响面与兼容性）齐备；`Acc-Covered` 字段标注 acceptance 消账（1,2,3→第 1 笔；4,5→第 2 笔；第 3 笔标注"无新增——文档同步与留痕载体"），消账路径清晰。
- **验证结论与验收者复现交叉一致**：提交声明"集成 75/75＋冒烟 75/75、零 error、ird_gates 14 处命中＝基线 11＋F-191 的 3"——与本验收者从零复跑（4.2/4.3）与 gates 留痕实读逐数值吻合；中间提交（第 1/2 笔）如实声明"本提交状态仅冒烟可构建、全量验证随第 3 笔登记"（分步提交的中间态如实标注，无带病粉饰）。
- **建议级瑕疵（S-1，转登 F-193）**：第 3 笔提交标题前缀 `[docs]`——其内容主体为 units/io.md 单元卡增量修订（AGENTS §6.2 规定单元卡修订用 `[units]` 前缀；同单元先例 IO-T03 同类提交 cb2b793b 用 `[WP-11-T04]`）。属 F-012/F-027/F-180/F-185 提交标题风格家族同型再现，纯风格瑕疵，正文与验证面无任何失实，不构成阻断。

## 4.10 留痕完整 —— 通过

- **gtest XML**：integration-gtest-io-test.xml 与 smoke-gtest-io-test.xml 双份在库，实测各 75 `<testcase>`、8 个 suite `tests=` 属性合计一致、`failures="0"`×8、`<failure>` 元素零。
- **ird-test-report.json**：在库且四段结构齐备（targets 双目标 build/run 结果＋gates 14 处命中分解＋selfCheck（validateTask/probeCapability/pitfall）＋testSuites 四组 35 用例逐个 trace 与 modes 标注）——acceptance 1~5 逐条具名映射可追溯。
- **构建日志**：builds/wp11-t04/ 共 11 件（integration-configure/build/test-run、smoke-configure/build/test-run、双 XML、gates-ird-gates-target.log、validate-task.log、verify-task.log、ird-test-report.json）全部随送验提交入库（.gitignore 吞 .log 的历史坑按先例 `add -f` 规避——F-153/F-010 家族未复踩）。
- phase-one-validation.log 未更新：该文件为阶段一验收期聚合留痕（在库内容止于 WP-03-T01 时代）；现行阶段 A 任务留痕口径为 builds/<branch>/＋ird-test-report.json（IO-T02/T03 同款且均 pass）——ACC §4.10 "或 resultNote 已更新"以任务报告承载满足，非缺口。

## 4.11 偷懒/缩水实现扫描 —— 通过

- **① 词表扫描**：`grep -rn -E 'TODO|FIXME|stub|placeholder|未实现|not implemented'` 于 Json.cpp/ZipChannel.cpp/Json.hpp/ZipChannel.hpp/JsonTest.cpp/ZipChannelTest.cpp **零命中**（无"触发式延后"豁免需要列出）。
- **② 空实现检测**：Json.cpp 2172 行/ZipChannel.cpp 616 行全部为实质实现（250/84 处 IoResult/return 语句；公共接口三方法＋工厂全部有完整执行体——实读核验，无返回默认值的空壳）。
- **③ acceptance 逐条证据表**：见 4.4 表——五条全部有具名测试＋断言明细＋双模式通过实测，无一条仅凭"已实现"声明。
- **④ 测试强度**：断言校验行为与边界而非恒真——错误码＋params 三要素（actual/limit/unit 逐字面值断言）、行列定位钉到具体列号（第 1 行第 8 列）、canonical 字节全等（非前缀/长度弱断言）、libzip 写侧与被测读侧互为独立实现的逐字节比对、篡改摘要/大小谎报/幽灵条目三态、加密标志 bit0 与压缩方法 93 手工字节构造（恶意形态不依赖 libzip 写侧能力）、CRC 反码；且与 4.5 联动——两枚抽改注入实测变红（测试会红的直接证据）。测试对失败路径的覆盖（编码/语法/预算/取消/资源四分类）与设计 §5.9.1 全表逐行对应。

## 发现问题清单

| 级别 | 编号 | 内容 | 处置 |
| --- | --- | --- | --- |
| 阻断 | 无 | —— | —— |
| 建议 | F-193（新登 findings.json） | 第 3 笔提交 59573704 标题前缀 `[docs]` 与内容主体（io.md 单元卡修订）应适用的 `[units]`/`[WP-11-Tkk]` 前缀不符——F-012/F-027/F-179/F-180/F-185 提交标题风格家族同型再现；纯风格瑕疵，正文四段与验证结论无失实 | 转登 findings.json（F-193，open，责任方：实施流程纪律——随家族统一治理）；不阻塞合入 |
| 建议（既有，不新登） | F-007/F-062/F-099 | worktree 干净检出的环境前置三件（vcpkg toolchain/Qt prefix/bin 模板）未成文——本验收按 IO-T03 先例处置 | 保持 open（governance 侧既有登记） |

## 裁决

**verdict = pass**。4.1~4.11 全部通过、零阻断级问题；建议级 1 项（F-193）已按 ACC §5 转登 findings.json，不阻塞合入。合入决策按 ACC §6 归所有者。

## 验收者环境说明

- 复现现场：`git worktree add --detach C:/Users/zgl18/AppData/Local/Temp/io-t04-wt-a 595737049abff486e823436a0690afe4553e099a`；集成树 `a/build`（配置：VS17 2022 -A x64＋vcpkg toolchain＋Qt prefix＋`RWS_BUILD_INDUSTRIALROBOT=ON`，缓存实测 BOOL=ON）与冒烟树 `a/build-smoke`（vcpkg toolchain）均为本次从零新建；记录回传现场 `acc/IO-T04/1` worktree（`C:/Users/zgl18/AppData/Local/Temp/io-t04-wt-b`）基于 origin/redesign-main@c96138060650d7a9b360b21cdb361130b154a93c。
- 全部证据（双模式配置/构建/测试 75/75、契约 verify、两枚失效注入与还原复绿、红线/偷懒扫描、留痕实读）为验收者在本 worktree 内亲手执行；两枚注入仅存在于临时 worktree 构建产物且已还原复绿，任务分支与送验对象零触碰、零推送。验收完成后临时现场随流程删除，本记录与三元组为唯一持久证据。
