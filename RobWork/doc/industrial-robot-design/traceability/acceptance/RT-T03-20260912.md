# RT-T03 验收记录（对抗式验收）

| 字段 | 值 |
| --- | --- |
| 任务 ID | RT-T03（≙WP-06-T03，runtime Description 值类型与 S3 校验器——含耦合矩阵校验） |
| 契约 | tasks/foundation/RT-T03.json（status: ready；翻转 done 归合入收尾） |
| 验收对象 | 40 位冻结 SHA `d0fc6e5baad0c9ffb7b92ff07e8d8549c599c806`；分支漂移检测 PASS：`git ls-remote origin wp06-t03` ＝该 SHA（验收时点实测）；范围 `d4faae3002b504a58797fdd5c5cd33d337be9da5..d0fc6e5baad0c9ffb7b92ff07e8d8549c599c806`（3 提交：ff9e443／dd1a0dc／d0fc6e5） |
| 尝试序号 | attempt=1（evidence 分支 `acc/RT-T03/1`，基于 origin/redesign-main＝d4faae3） |
| **verdict** | **pass**（4.1~4.11 全过；无阻断级问题；建议级 2 项 → F-037/F-038 随本 evidence 分支转登 findings.json） |
| 验收日期 | 2026-09-12 |
| 独立性声明 | 全新上下文（ACC §2 四条逐条适用）：只消费契约/设计文档/冻结产物与留痕，未消费实施叙述；构建、测试、门禁、探针全部验收者亲手重跑（复现优先于采信）；对抗式立场贯穿（为 fail 找证据：4.2 全树从零构建、4.5 阈值探针、4.11 逐条反证扫描） |

---

## 逐项证据（4.1~4.11）

### 4.1 改动范围 —— PASS

- `git diff --name-status d4faae3..d0fc6e5` ＝ 21 文件：runtime 8（CMakeLists.txt M；Description.hpp/Resource.hpp/DescriptionValidator.cpp/DescriptionValidator.hpp/DescriptionTest.cpp/DescriptionValidatorTest.cpp A；BuildRedLineTest.cpp M）＋traceability/builds/wp06-t03/ 留痕 13＋units/runtime.md M。
- 契约 allowedFiles＝`runtime/**`＋`industrialrobot/CMakeLists.txt`＋`units/runtime.md`＋`traceability/**`：全部变更落在 `runtime/**`、`traceability/**`、`units/runtime.md` 内（顶层 industrialrobot/CMakeLists.txt 未触碰——允许但无需要）；`forbiddenFiles`（REQUIREMENTS.md/ARCHITECTURE.md）零触碰（不在 diff）；`patches/` 零新文件（SA-02 无登记需求）；框架源码零改动。

### 4.2 可复现构建 —— PASS（全新树从零复现；全树从零失败面＝F-008 基线既有，非本任务）

复现现场＝`git worktree add --detach <tmp>/A d0fc6e5`：

- 集成模式全新配置：`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=<vcpkg> -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64` → 首跑失败（`RobWorkStudio.ini.template.static` 不存在——该文件 untracked 且未入 git，F-007/F-009 登记在案的环境前置）；验收者从主树复制该 untracked 环境文件至 worktree bin/ 后重配 → **exit 0**（integration-configure.log：Configuring done 5.0s）；缓存确认 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`。
- 契约 verify 目标从零构建：`cmake --build build --config Release --target sdurws_ird_runtime_test` → **exit 0、零错误**（error C[0-9]/LNK/fatal 计数＝0；acc-verify-target-build.log）。
- 集成测试从零复跑：**73/73 PASSED、exit 0**（acc-integration-test-run.log；XML tests="73" failures="0"）。
- 冒烟模式独立目录：`cmake -S <wt-a>/industrialrobot -B <tmp>/smoke-build -G "Visual Studio 17 2022" -A x64 -DCMAKE_TOOLCHAIN_FILE=<vcpkg>` → **exit 0**；`cmake --build … --config Release` → **exit 0、零错误**；测试复跑 **73/73 PASSED、exit 0**（acc-smoke-test-run.log）。
- 全树从零构建观测（非契约面，忠实登记）：`cmake --build build --config Release` 从零全树在 RobWorkSim 框架插件失败——73 个错误全部位于 `RWSimulatorPlugin.vcxproj`/`bt_plugin.rwplugin.vcxproj`（rw/math/Math.hpp min/max 宏污染 C4003 → VelocityScrew6D.hpp 等语法错误雪崩），与 findings.json **F-008**（基线框架缺陷）逐项吻合；该 73 错中**零个**涉及 industrialrobot/runtime（送验 diff 未触碰任何出错文件，见 4.1）。实施侧 integration-fullbuild.log 产自主树存量增量树、确实零错误（对照 F-008 已知），非本任务引入问题。结论：任务目标面（sdurws_ird_runtime＋runtime_test）双模式从零构建零错误＋73/73 全绿——硬验收面达成。

### 4.3 契约 verify 命令 —— PASS

契约 verify 数组仅一条：`cmake --build build --config Release --target sdurws_ird_runtime_test`——在全新 worktree A 集成树亲手执行，**exit 0、零错误**（同 4.2 第二条）。

### 4.4 acceptance 条目逐项 —— PASS（3/3 均有具名测试＋双模式实测）

全量测试验收者亲手执行（非采信实施留痕）：集成树与冒烟树各 **73/73 PASSED**（19 suites，含 RT-T02 既有 21 例回归零破坏）。

| 契约 acceptance 条目 | 具名证据 | 验收者实测 |
| --- | --- | --- |
| ① RT-CPX-3/RT-CPL-1/RT-BW-6 校验器部分用例全部通过 | RT-CPL-1：CouplingValidation 11 例（WellConditionedCPasses/SingularCBlocked/NearSingularDetApproxZeroBlocked/ConditionNumber1e10Blocked 及形状/范围/数据量/非有限/申报值不可信）；RT-CPX-3：ProvidedIllegal 5 例（NegativeMassLocated_RT_CPX_3 精确定位 links[1].mass 等）；RT-BW-6：IllegalTransform 5 例（反射/非正交/NaN 平移/场景位姿/工具 tcpOffset） | 双模式 PASSED；覆盖面边界声明（文件头：只断言到校验器切面，全链断言归 RT-T04/T11/T12）与单元卡 §11 校验器部分口径一致，无缩水 |
| ② 结构校验（§4.2）硬反例逐条拒绝且消息含字段路径 | StructureHard 19 例 + expectBlocked 辅助（DescriptionValidatorTest.cpp:186——:196 显式断言 `detail` 必含字段路径，acceptance 2 字面承载）；实现侧 ValidationIssue.fieldPath/detail 契约（DescriptionValidator.hpp:78-87） | 双模式 PASSED；实现全量收集不短路（939 行实现逐块核对：块 0～块 8 检查序固定） |
| ③ 单位校验（§4.2——非有限/非法单位拒绝，NFR-COR-03 编译边界切面）反例通过 | UnitValidation 5 例：NonFiniteBoundsRejected_ACC3_NFR_COR_03（:770）、NonFiniteVelocityRejected_ACC3_NFR_COR_03（:785）、DegreeMagnitudeBoundWarnsButDoesNotBlock_ACC3（:795——deg 误作 rad 给警告不阻断，§4.4 抽样纪律）、MillimeterMagnitudePrismaticWarns_ACC3（:814）、FixedJointProvidedBoundsStillValidated（:836） | 双模式 PASSED；NaN 与 ±Inf 两族都测（kNaN/kInf 常量注入） |

ird_gates 口径：验收者亲手 `cmake --build build --config Release --target ird_gates`（全新集成树）→ exit 1，命中**恰为 IRD-GATE-SUB＋IRD-GATE-T1 两处**（sdurws_ird_testdata_lint→testkit 边），与 findings.json **F-019**（TK-T03 基线既有）逐字吻合，**runtime 面零命中**——本任务引入的 LIB 命中已如实施提交所述消除；门禁自测 9 项行为全部符合预期。实施提交如实报告该 2 处 base 既有并声明"超出 allowedFiles 不私裁"——处置正确。
辅助校验：validate-task.ps1（RT-T03.json）→ **PASS (1 tasks)**；validate-docs.ps1 → **PASS (20 units, 12 trace entries, 150 task files)**——均验收者亲手执行，与实施声明一致。

### 4.5 测试真实失败能力（验证验证者）—— PASS（探针注入→红→精确还原→复绿）

在冻结 worktree A 上注入一处阈值缺陷，每步显式核验构建退出码（防旧二进制假绿，TK-T07/T10 教训）：

- **探针（条件数阈值变异）**：DescriptionValidator.cpp:55 `kConditionNumberLimit` 1e8→1e12（Edit 精确注入）→ 重建 **exit 0**（变异合法可编译）→ 测试 **exit 1，恰 2 用例红**：`CouplingValidation.ConditionNumber1e10Blocked_RT_CPL_1`＋`CouplingValidation.DeclaredConditionNumberNotTrusted`（κ=1e10 反例在新阈值 1e12 下放行——恰为 P-RT-7 阻止边界的被钉住行为）。
- **精确还原**：Edit 逆向还原 → `git status --porcelain` 过滤 untracked 后零输出（跟踪文件字节级一致，DIFF_CLEAN 确认）。
- **复绿**：重建 **exit 0** → **73/73 PASSED、exit 0**（acc-restore-test-run.log）。

结论：被抽断言非恒真，测试对实现的变异有真实检出能力。实施侧 probe-build.log/probe-test-run.log（同型阈值变异→同 2 用例红）与本验收探针互为独立印证。

### 4.6 架构红线 —— PASS（八条逐条）

1. **L2 零 Qt**：runtime 4 个实现/头文件 include 面＝std＋rw/math＋`sdurws/ird/core/*`＋自身，**零 Qt**；CMakeLists.txt:176-179 配置期 Qt 链接断言在位。
2. **R-1 业务域互链禁止**：仅链接 core＋L1 基线（kinematics/models/sdursim——P-RT-3 白名单，CMakeLists.txt:62-99）；CMakeLists.txt:167-175 配置期单元互链 FATAL 断言在位，本次配置通过即实证。
3. **R-2 跨单元私有头禁止**：跨单元仅 include core 公共头（`include/sdurws/ird/core/{Digest,Identity,Provenance}.hpp`，均在 core 公共 include 根）；校验器私有头 src/DescriptionValidator.hpp 仅单元内消费（测试同权，CMakeLists.txt:128-132 注释留痕）。
4. **SA-02 框架零修改**：diff 无框架文件、patches/ 无新文件（4.1）。
5. **R-4 名称前缀拼接/剥离**：校验器对 localName 只做空名/`'/'` 字符校验（归属校验切面），无任何 RobWork 前缀拼接/剥离逻辑，名称解析未越权实现。
6. **PA-1 权威唯一**：单位换算未在 runtime 实现（Description.hpp 文件头明示归 core 换算入口＋reader 侧 SI 化）；preset→矩阵归 RT-T06；诊断码归 diagnostics（校验器只产 ValidationIssue，转译归 RT-T11）——无"顺手实现他人语义"。
7. **PA-2 不可变历史**：本任务无修订/写路径触碰（N/A）。
8. **不继承 old/ 历史**：Description/校验器全部按 units/runtime.md §4.2/§5.2 原文契约从零实现（字段逐一可溯源到单元卡行）。

### 4.7 注释规范 —— PASS（公共头全查 3/3；实现文件全查 2/2＋测试 2/2）

- **Description.hpp**（公共头，455 行全查）：文件头（设计依据/需求追溯/背景说明/单位纪律/分阶段落位登记/线程安全/rw header-only 约束）齐备；每个枚举/结构体/字段均有中文 Doxygen——单位逐字段显式（rad/m/kg/kg·m²/N·m/N·m·s/rad），坐标系显式（连杆系/质心系/工具系/法兰系/世界系），错误语义显式（非法→InputInvalid＋出处），确定性显式（枚举序入 RT-Codec 编码面一经交付不得改动）。
- **Resource.hpp**（公共头，86 行全查）：同上齐备（§8.6 身份纪律"路径不作身份"、分阶段落位登记、线程安全、CON-03/CON-05 追溯）。
- **DescriptionValidator.hpp/.cpp**（私有头＋实现，133＋939 行全查）：文件头含确定性来源专段；全部阈值常量带出处与单位（kOrthoTolerance 1e-12←§6.6；kConditionNumberLimit 1e8←P-RT-7；kAngleMagnitudeWarn 4π×10 rad←§4.4 原文等）；单侧 Jacobi 选型注释完整（为何不用两侧法——动态范围平方化论证，Demmel–Veselić 精度结论）；每个函数 Doxygen＋逐步算法注释（checkTransform 三步检查序及跳过理由；奇异/病态两档分界论证）。
- **测试文件**：文件头含覆盖面声明（防验收误读的边界声明）；用例名带 RT-CPX-3/RT-CPL-1/RT-BW-6/ACC3/NFR-COR-03 追溯锚＋中文说明验证点。
- 反面示例扫描：未发现复述代码/无信息量占位/中英机翻腔/过期注释。

### 4.8 文档同步 —— PASS

- 单元卡增量修订（DTB §5.4）：units/runtime.md v0.3→v0.4——①InstallationPresetToken 落位调整（§3.1 两行同步）；②Resource.hpp 值类型先行落位（首个消费者原则）；③单侧 Jacobi 选型登记；④冒烟 rw 模板头机制；§15.4 v0.4 变更记录完整（d0fc6e5）。两处分阶段偏差均在 allowedFiles 内、契约字段零变化、登记先于送验——"代码先行、文档失实"不成立。
- 追踪矩阵/任务状态：validate-task PASS（含 acceptance 锚点核验）；phase-one-validation.log:41 含 RT-T03 validate-task PASS 行。
- **findings.json 未关闭条目相关性核对（28 条 open 逐条过目）**：与本任务相关者——F-008（RobWorkSim 插件从零构建失败，基线缺陷，本次验收全树从零构建再次实证，非本任务引入）；F-007/F-009（全新树环境前置：toolchain/Qt 前缀/ini 模板，本验收按既有登记处置）；F-019（ird_gates 对 testkit 的 2 处 base 命中，本验收实测复现一致）；F-004（AUTOMOC 空转，实证零 Qt 依赖不受影响）。上述均属基线/环境/他单元辖域，**无一条要求本任务顺手消账**；本任务未新增未登记偏差。

### 4.9 commit 质量 —— PASS（建议级 2 项转登，无粉饰）

- ff9e443 `[WP-06-T03] 实现 Description 值类型与 S3 校验器（含耦合矩阵校验）`：动词开头；四段齐备；验证段声明（双模式 73/73、探针恰 2 红、ird_gates LIB 命中已消除＋SUB/T1 base 既有）与本验收独立复现**逐项吻合**；Acc-Covered: 1,2,3 标注在位。
- dd1a0dc `[WP-06-T03] 验证留痕：…`：动词开头；四段齐备；`git add -f` 处理 .gitignore 吞 *.log（F-010 既有口径）如实登记。
- d0fc6e5 `[units] runtime v0.4：…`：四段齐备（Acc-Covered 注明为文档同步提交）。建议级：前缀后以名词短语开头（"runtime v0.4：…"），ACC §4.9 口径同 F-012/F-034 家族 → **F-037**。
- dd1a0dc 验证段"全树集成构建日志（cmake --build build --config Release，exit 0——DoD 第 1 条最强口径）"：日志真实（主树增量全量、零错误行），但按 F-036 对 RT-T02 的同类裁定，增量树不宜表述为"最强口径"（全新树从零在 F-008 处不可达，本次验收已实证）→ **F-038**（F-036 同型复现）。该表述为少报环境限制、非粉饰验证结果（73/73、门禁、探针全部与留痕一致），不构成 ACC §5"验收粉饰"阻断。

### 4.10 留痕完整 —— PASS

- `traceability/builds/wp06-t03/` 13 件齐备：ird-test-report.json（ird-test-report/1 格式，73 用例双模式逐条含需求/卡章节追溯）、gtest XML×2（集成/冒烟各 tests=73 failures=0）、integration-build/integration-fullbuild/integration-test-run/smoke-configure/smoke-build/smoke-test-run/probe-build/probe-test-run/ird-gates-run 日志。
- 验收者侧独立留痕（本记录引用）：acc-verify-target-build.log、acc-integration-test-run.log（＋XML）、acc-smoke-test-run.log（＋XML）、acc-probe-build/test-run.log、acc-restore-build/test-run.log、acc-ird-gates-run.log、integration-configure.log——命令与退出码均写入本记录正文。
- phase-one-validation.log（RT-T03 行在位）；resultNote 义务由 ird-test-report.json 承载（targets 双模式结果＋modeMechanics 说明）。

### 4.11 偷懒/缩水实现扫描 —— PASS

- ①标记扫描：实现 diff 内 `TODO|FIXME|stub|placeholder|未实现|not implemented` **零命中**（grep 实证；契约无"触发式延后"标注需求）。
- ②空实现检测：无公共接口空函数体/仅返默认值——Description.hpp 类型为契约规定的值承载（聚合体）；校验器 939 行真实算法（Jacobi 两条数值路径＋八块校验链）；reader 为契约规定的抽象接口（实现归 modeling，§11 替身边界声明在位）。Resource.hpp 按文件头分阶段登记仅落位值类型——登记在案（runtime.md §15.4 v0.4②），非缩水。
- ③逐条证据表：见 4.4 表——三条 acceptance 均有具名测试＋双模式实测＋行号锚点，零"已实现"空声明。
- ④测试强度：断言校验行为与边界（比较型诊断的 actual/threshold 双向区间断言 :286-288/:317-319；申报值双向不可信 :384-397；枚举序号锚定 DescriptionTest.cpp:215）；与 4.5 联动——阈值变异实测变红，非恒真、非"只编译通过"。

---

## 问题清单

### 阻断级

无。

### 建议级（已随本 evidence 分支转登 findings.json）

| # | 内容 | 处置建议 | 登记编号 |
| --- | --- | --- | --- |
| G-1 | d0fc6e5 标题 `[units] runtime v0.4：RT-T03 实现落位登记…` 前缀后以名词短语开头（ACC §4.9 动词开头口径；F-012/F-023/F-024/F-026/F-027/F-030/F-034 同家族第 7 次复现） | 后续文档类提交标题在代号前缀后以动词短语开头；提交已推送不追溯改写 | **F-037** |
| G-2 | dd1a0dc 验证段"全树集成构建日志（…exit 0——DoD 第 1 条最强口径）"强度表述失准：日志产自主树存量增量树（日志内路径为 `D:\...\RobWork\build\`），全新树从零构建在 F-008（RobWorkSim 插件宏污染）处不可达（本验收实测 73 错误、零个涉及 industrialrobot）；F-036 对 RT-T02 的同型裁定第 2 次复现 | 增量树/全新树显式区分，禁止把增量树全量构建表述为"最强口径"；F-008 闭合前"全树"口径须带限定语 | **F-038** |

### 验收者环境说明

- OS Windows 11 x64（10.0.26200）；MSVC VS2022 17.12（Visual Studio 17 2022 生成器，x64）；CMake 3.x（MSBuild 17.12.6）；vcpkg 经典模式（仓库根，gtest/Eigen3 就位）；Qt 6.11.1（D:/software/Qt/6.11.1/msvc2022_64）。
- 复现现场：worktree A＝detached @ d0fc6e5（构建/测试/门禁/探针）；worktree B＝acc/RT-T03/1 @ origin/redesign-main（本记录）。
- 环境前置处置（沿用 F-007/F-009 既有登记）：从主树复制 untracked 文件 `RobWork/RobWorkStudio/bin/RobWorkStudio.ini.template.static` 至 worktree A 同路径（集成配置必需；该文件不在任何提交内，非送验产物改动，跟踪树保持字节级一致——4.5 还原段同一口径核验）。
- 网络偶发 SSL 握手失败按编排者口径重试处理（fetch 一次失败后 ls-remote/push 环节恢复正常重试）。

## 裁决

**verdict＝pass**：4.1~4.11 全部通过，无阻断级问题；建议级 G-1/G-2 已转登 findings.json（F-037/F-038，open），不阻塞合入。验收对象为冻结 SHA d0fc6e5baad0c9ffb7b92ff07e8d8549c599c806；合入决策归所有者（PIPE §4.7）。
