# TK-T05 验收记录（对抗式验收）

| 字段 | 值 |
| --- | --- |
| 任务 ID | TK-T05（≙WP-02-T05，数值断言与比较详情 Check） |
| 契约 | tasks/foundation/TK-T05.json（status: ready，随收尾翻转 done） |
| 验收对象 | 分支 `wp02-t05`，head＝`dc4296a8c85c3a366317598fee98862bb68a6adf`（与 origin 一致——漂移检测 PASS），范围自 redesign-main 分叉点起 |
| 尝试序号 | attempt=1（evidence 分支 `acc/TK-T05/1`） |
| **verdict** | **pass**（4.1~4.11 全过；无阻断、无新增建议级发现） |
| 验收日期 | 2026-09-11 |
| 独立性声明 | PIPE v1.7 §0.4 单会话降级；补偿＝worktree A（detached @ head）亲手复现（43/43）＋diff 恒零探针实证＋证据链前置自查（8 份证据文件 -f 入冻结对象——F-010 教训内化） |

---

## 逐项证据（4.1~4.11）

| # | 检查 | 结论与证据 |
| --- | --- | --- |
| 4.1 | 改动范围 | diff＝testkit {Check.hpp,Check.cpp,CheckTest.cpp}＋gtest/AssertMacros.hpp＋CMakeLists＋traceability——allowedFiles 内；forbiddenFiles 零触碰 |
| 4.2 | 可复现构建 | worktree A 全新配置 exit 0（ini 模板前置补齐）＋verify 目标构建 exit 0＋测试 43/43 PASSED（验收者亲手）；集成全量 exit 0 |
| 4.3 | verify 命令 | `cmake --build build --config Release --target sdurws_ird_testkit_test` exit 0 |
| 4.4 | acceptance 逐条 | ①委托 core::closeWithin（唯一公式——CR-06 合法消费）用例通过：单点/序列/正负抵消反例（+0.01/−0.01 逐元素超差）——两模式 XML；②CompareDetail 机器可读（fieldPath/hasElement/actual/expected/diff 逐字段断言）✓ |
| 4.5 | 失败能力 | diff 恒零探针（makeDetail 的 |a−b| 置 0）→委托用例如期变红，恢复后 43/43 复绿（报告记录；探针零残留 grep 实证） |
| 4.6 | 红线 | 零 Qt；D-06（Check.hpp 本体零 gtest——gtest 仅在 AssertMacros.hpp 宏层与测试 TU）；T-2（core＋std＋gtest 仅测试面）；SA-02 零框架改动 |
| 4.7 | 注释 | 公共头全查（三级断言不可混用/失败行为总表引用/宏展开要求 D-06）；实现关键步（长度不匹配列清单/缺失侧 NaN 占位语义） |
| 4.8 | 文档同步 | 接口零偏差（§5.3.1/§5.3.2 签名逐项对应——CR-06 合法消费）；findings 核对：F-010 前置自查；F-004（同源累计）；F-008/F-015~F-018 无涉 |
| 4.9 | commit 质量 | 两提交四段式＋Acc-Covered（1,2 / 3）；-f 补交说明 |
| 4.10 | 留痕完整 | 8 份证据文件齐（日志 6＋XML×2＋report——testdata/ 字典 CSV 等 by-design 不生成，详见 report notes） |
| 4.11 | 偷懒扫描 | 零命中；Check 为实质包转＋报告组织（非空实现）；测试含反例矩阵（非有限/长度/精确相等反例） |

## 问题清单

阻断级：无。建议级：无新增。
