# RT-T06 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | RT-T06 实现基座—世界变换规则（四预设/正反解）——runtime 单元 |
| 验收日期 | 2026-09-12 |
| 送验对象（冻结） | `ca789342c6a5c61d4d8d216bed162ea93ed4c0b2`（origin/wp06-t06 @ 验收时点 ls-remote 比对一致——无分支漂移） |
| diff 范围 | `5eff65d8104011a24d6c2052201acfc4b32d5d91..ca789342c6a5c61d4d8d216bed162ea93ed4c0b2`（3 个提交：39fae2e 实现、5034611 留痕、ca78934 文档登记） |
| **verdict** | **pass**（4.1~4.11 全部通过，无阻断级问题；建议级 G-1/G-2/G-3 已转登 findings.json **F-041/F-042/F-043**，open，不阻塞合入） |

## 验收者环境说明（独立性）

- 全新上下文：只消费契约 `tasks/foundation/RT-T06.json`、设计文档与仓库产物；未读取实施者任何过程叙述。
- 复现现场：`git worktree add --detach <临时目录A> ca789342…`（detached @ 40 位 SHA，非分支名）；记录回传：`git worktree add -b acc/RT-T06/1 <临时目录B> origin/redesign-main`（5eff65d）。
- 全新集成树配置（F-007 登记口径再次踩中，按既定登记处置）：vcpkg toolchain＋Qt6 CMAKE_PREFIX_PATH＋`RobWorkStudio.ini.template.static`/`ini.shared.in` 复制（该两文件被 .gitignore [Bb]in/ 吞掉、不在版本库——F-007 既有内容）。
- 验收者本地证据文件（会话临时区，不入库；结论均回写本记录）：`rw-acc-rtt06-a-integration-build.log`（全树）、`rw-acc-rtt06-acc-integration-run.log` / `rw-acc-rtt06-acc-smoke-run.log`（双模式测试）、`rw-acc-rtt06-acc-gates.log`（门禁）、`rw-acc-rtt06-probe1/2/2b-*.log` 与 `rw-acc-rtt06-restore-*.log`（探针与复绿）。

## 4.1 改动范围 —— PASS

- `git diff --name-only 5eff65d..ca78934`：15 个文件，4034 增/4 删。
  - `runtime/**` ×4（CMakeLists.txt、BaseWorldTransform.hpp 364 行、BaseWorldTransform.cpp 530 行、BaseWorldTransformTest.cpp 799 行）⊆ allowedFiles `RobWork/RobWorkStudio/src/rwslibs/industrialrobot/runtime/**`；
  - `traceability/builds/wp06-t06/` ×10 ⊆ allowedFiles `traceability/**`；
  - `units/runtime.md` ×1 ∈ allowedFiles。
- forbiddenFiles（REQUIREMENTS.md / ARCHITECTURE.md）零触碰（diff 文件清单为证）。
- SA-02：diff 无 industrialrobot 之外任何框架源码；`patches/` 无新文件——零登记需求，合规。
- 无未登记改动。**通过**。

## 4.2 可复现构建 —— PASS（按 F-008/F-036/F-038 既定"任务目标面全新树从零"口径）

- **全新集成树全树构建**（worktree A，重新配置后缓存确认 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`）：`cmake --build build --config Release` → EXIT=1，73 个 error 全部位于 `RWSimulatorPlugin.vcxproj`/`bt_plugin.rwplugin.vcxproj`（rw/math/Math.hpp min/max 宏污染簇——**F-008 既有基线缺陷**，与基线 959fb9b 对照复现同型；含 industrialrobot 的 error 计数＝0）。按 findings F-008/F-036/F-038 既定口径，DoD 第 1 条以任务目标面全新树从零为验收面。
- **任务目标面从零构建**：`cmake --build build --config Release --target sdurws_ird_runtime sdurws_ird_runtime_test` → **exit 0 零错误**（验收者亲手执行，日志 rw-acc-rtt06-acc-gates.log 同树）。
- **独立冒烟模式**（另配临时目录＋vcpkg toolchain 绝对路径）：configure＋build → **exit 0 零错误**（零框架链接；rw 仅模板头可达——本任务全部矩阵运算逐元素实现、零 rw 外联符号的纪律经受了两模式实际编译检验）。
- **测试真实复跑**（复现优先于采信，不引用实施者结果）：集成模式 `sdurws_ird_runtime_test.exe` → `[==========] 172 tests from 46 test suites ran … [  PASSED  ] 172 tests.`，exit 0；冒烟模式同命令 → **172/172 PASSED，exit 0**。送验 XML（traceability/builds/wp06-t06/ 两份，tests="172" failures="0"，172 个 testcase 元素逐一清点）与复跑一致。

## 4.3 契约 verify 命令 —— PASS

- 契约 verify 数组恰 1 条：`cmake --build build --config Release --target sdurws_ird_runtime_test`。
- 验收者在全新集成树（非主树增量）亲手执行 → exit 0（见 4.2）。**全部成功**。

## 4.4 acceptance 条目逐项核对 —— PASS

| # | 契约 acceptance | 验收证据 | 判定 |
| --- | --- | --- | --- |
| 1 | RT-BW-1/2/3/6 四预设＋反例用例通过（AT-37 载体） | 单元卡 §12 RT-T06 行自带"（规则部分）"限定＝本任务契约面。验收者复跑 172/172 全绿，其中新增 42 例（清点见 4.11③）：§6.5 手算数值例 4 断言（①p_world=(0.3,−0.2,1.5)②逆平移 t'=(0,0,+2)＋回代逐位③重力投影 (0,0,−9.81)→(0,0,+9.81)＋地面恒等④结合律两顺序）＋四预设精确矩阵（EXPECT_EQ 位相等）＋默认地面（RT-BW-3）＋反射/非正交/非有限/失配反例（RT-BW-6）。全链"编译＋view 读取"切面随 RT-T07/T11——单元卡行原文即此分阶，非实施缩水 | pass |
| 2 | 单一写入点（禁止下游二次旋转——§6.3 唯一存储/唯一写入） | 规则面三承载全部在代码与用例中实证：①产生侧唯一入口 resolveWorldBaseTransform()（预设→矩阵唯一经 installationPresetRotation()，正解与直接换算逐位一致用例 CustomResolvesViaRodrigues 钉住）；②检测侧 checkBaseMountConsistency() 实现 §6.6 S9 判定——DoubleApplicationDetected_RT_BW_4 用 AT-37 反例（候选＝T·T）实证"下游二次旋转"必被检出且 doubleAppliedPattern=true；WrongRotationNotMarkedDoubleApplied 钉住标记不误报。S6 唯一写入点实体（BaseMount 一次性置入）随 RT-T07——§15.4 v0.7④⑤已按 DTB §5.4 登记，非静默缩水 | pass |
| 3 | P-RT-4 冻结留痕（modeling 卡交叉核对后） | ①冻结矩阵唯一权威产出点 installationPresetRotation()（代码＋文件头留痕）＋两用例以 EXPECT_EQ 精确断言钉住 R_x(π)/R_y(π/2)；②units/runtime.md §15.3 P-RT-4 行〔RT-T06 冻结留痕 2026-09-12〕＋§15.4 v0.7 第②条登记。**"modeling 卡交叉核对"判据**：modeling.md 尚不存在（units/ 目录清单为证），交叉核对在物理上不可先行；DTB §4 **O-16** 原文即登记"modeling 卡（WP-13-T11 交叉核对冻结）"为 P-RT-4 轴向的最终冻结权威、状态"登记"；契约 note（DOC-T06 放行，2026-09-11 所有者 B 模式预授权）明示"O-15/O-16/O-18 登记类不阻塞编译、runtime.md §12 卡内锚点即交接核对"。故 runtime 侧冻结留痕＋锚点声明＝本任务契约面的正确执行，交叉核对余量由 O-16 持续跟踪（未静默积压） | pass |

- `ird_gates` 自查：实施者已实际执行并留痕（ird-gates-run.log：命中 IRD-GATE-SUB/T1 两项）；验收者全新树复跑同 2 项命中（见 4.6）——符合 F-035 处置要求，无"未执行"虚标。**通过**。

## 4.5 测试真实失败能力（验证验证者） —— PASS（附 1 项覆盖面建议）

三组缺陷注入（冒烟树，精确编辑＋显式核验构建退出码；注入后 `git -C <A> diff` 为空证明字节级还原）：

1. **探针 1**：`checkWorldBaseTransform` 反射拦截 `det < 0.0` → `det < -2.0`（禁用）。重编 exit 0 → 运行 exit 1，**恰好 `ValidateTest.ReflectionMatrixRejected_RT_BW_6` 一例变红**（1 FAILED TEST）。还原后 diff 空。
2. **探针 2a（覆盖面发现，见 G-3）**：`gravityToBase` 改为正向乘 `rotateVector`（R·g 而非 Rᵀ·g）→ `InverseAndPointTest.*` **6/6 仍全绿**——数学原因：测试所用旋转 R_x(π)=diag(1,−1,−1) 与 I 均为对称阵，R·g≡Rᵀ·g，方向混淆对现有输入不可检。
3. **探针 2b**：注入禁止清单 2 明示的"就地取反"反模式（返回 −g_W）→ **恰好 `InverseAndPointTest.NumericExampleGravityProjection_RT_BW_1` 变红**（1 FAILED TEST）。证明该用例组对真实禁用形态有失败能力，非恒真。

复绿：字节级还原（worktree A `git status`/`git diff` 全空）→ 重编 exit 0 → 全量 **172/172 PASSED**（rw-acc-rtt06-restore-run.log）。实施者自带探针（probe-build.log 内含 红→还原→复绿 全程，PROBE_TEST_EXIT=1/RESTORE_BUILD_EXIT=0）与验收者独立探针结论一致。

## 4.6 架构红线（AGENTS §5 八条） —— PASS

1. **L2 零 Qt**：runtime 全目录 `grep "include <Q|QtCore|QtWidgets|QtGui"` 命中 0（唯一字面命中为 RT-T01 BuildRedLineTest 的扫描器源码自身）；新增文件 include 仅 `<rw/math/*>`、`<optional>/<cmath>/<string>`、本单元公共头。
2. **R-1 业务域互链**：新增代码只依赖 rw/math＋本单元（Description/Errors）＋测试文件依赖 core 公共头（Compare/Provenance——runtime 卡既定依赖 core，非业务域互链）。modeling/kinematics/trajectory/dynamics/selection/optimization 零 include。
3. **R-2 跨单元私有头**：include 全部为 `include/sdurws/ird/<unit>/` 公共头路径；无私有头。
4. **SA-02**：见 4.1，零框架改动、patches/ 零新增。
5. **R-4 名称前缀**：新增代码无 RobWork 前缀拼接/剥离；ird_gates R-4 扫描零命中（见下）。
6. **PA-1 权威唯一**：诊断码值未私裁——BaseWorldInconsistent/InputInvalid 的码值装配显式归 RT-T11/diagnostics StableCodeRegistry（头注释与 §15.4 v0.7⑤ 双处登记）；单位规则未在本单元重定义；命令/查询端口语义未越界实现。
7. **PA-2 不可变历史**：不涉修订/撤销数据面，零触碰。
8. **old/ 不恢复**：无任何 old/ 引用。

**ird_gates 实跑**（验收者全新树 `cmake --build build --config Release --target ird_gates`）：exit 1，命中恰 2 项＝IRD-GATE-SUB＋IRD-GATE-T1（目标均为 `sdurws_ird_testdata_lint → sdurws_ird_testkit` 边）——与 F-019 既有登记逐字一致，runtime 面/本任务新增文件零命中；与送验 ird-gates-run.log 一致。门禁自测段 pass_clean/pass_r4comment 等行为正常。

## 4.7 注释规范（AGENTS §2） —— PASS

- **公共头全查**（BaseWorldTransform.hpp，365 行）：文件头五要素齐备（设计依据含需求 ID/任务卡/卡章节、背景说明、P-RT-4 冻结留痕专段、实现纪律、线程安全/确定性声明）；9 个函数＋1 结构逐个 Doxygen 中文注释，@param 方向/单位/取值范围、@return 语义、@throws 触发条件齐备。
- **§2.5 高危信息表**逐项核对：物理单位（m／rad／m/s²／无量纲逐一标注）、坐标系（T_world_base/T_base_world 读法、§4.6 core 读法引用）、角度制式（"单位 rad（不是度）"显式）、所有权（纯函数/值语义/快照不可变说明）、线程约束（每函数纯函数声明＋并发只读安全）、错误语义（InputInvalid＝调用方错误 fail-fast、Expected 错误轨、与异常轨分工）、确定性来源（常量逐元素写定、求和序固定、std::to_string 无 locale）。
- **实现文件**（BaseWorldTransform.cpp 全查＋测试文件＋CMakeLists，≥3 达标）：匿名命名空间助手逐个注释；魔法数字 1e-12/1e-9 均注明档案出处（§6.6/附录 D 第 4/6 项）与三处同源同步评审要求；Rodrigues/逐元素算法分段讲解；每个分支注明业务场景（如"NaN 使比较无意义，先拦截"）；全枚举 switch 的防御性兜底说明。测试用例名带 RT-BW/P-RT-4 追溯字段，断言处注明需求/验收出处。**无 2.6 反面示例形态**。

## 4.8 文档同步＋findings 核对 —— PASS（附 G-1 计数失实建议）

- **单元卡同步**：runtime.md v0.6→v0.7（§15.4 变更记录行齐备）；本任务全部实现层选型/分阶（Custom 换算落点、S5 全量接线随 RT-T11、S9 码值装配归 RT-T11、冒烟零外联纪律⑥）均登记，与代码一一对应——实现与任务卡零未登记偏差。
- **§6.5② "事实修正"独立复核（编排者指定判据）**：数学代入——R=R_x(π)=diag(1,−1,−1)、t=(0,0,2) ⇒ Rᵀ=R（对合）、Rᵀt=(0,0,−2) ⇒ **t′=−Rᵀt=(0,0,+2)**，卡文原 −2 确为笔误；反证——若 t′=−2，则 p_base′=Rᵀ·p_world+t′=(0.3,0.2,−1.5)+(0,0,−2)=(0.3,0.2,−3.5)，与同例"与输入逐位一致"闭环断言矛盾，+2 被该例自身唯一锁定。**修正属实**。**登记合规性**：DTB §5.4 表"卡间/卡与上游冲突→在对应卡待裁决/变更记录登记"与"实现细节偏差→单元卡增量修订＋变更记录行"——本次以卡文内〔v0.7 修正〕注记块＋§15.4 v0.7 第①条双留痕，验收断言行（闭环断言）未被改动、需求语义未收窄/扩大，**合规**；实现与用例（NumericExampleInverseRoundtrip_RT_BW_1）按修正值 +2 执行且两模式实测通过。
- **任务状态/追踪矩阵**：契约 status 翻转（ready→done）与队列收尾归所有者合入段（PIPE §4.7⑤⑥，RT-T05 收尾提交 5eff65d 同例），不属实施者职责——无需实施侧改动。
- **findings.json 未关闭条目相关性核对**（open：F-004/005/006/007/008/009/010/012/015~020/023~033/034~040 等）：**F-007**（全新树配置前置缺失）——验收者本次再次逐项踩中（toolchain/Qt 前缀/ini 模板复制），按登记口径处置，与本任务产物无关，维持 open；**F-019**（门禁基线红）——全新树复现同 2 项命中，零新增，维持 open；**F-035**（实现类任务须实跑门禁）——本任务已合规执行（gates 段留痕真实）；**F-036/F-038**（"最强口径"措辞）——本任务提交信息采用目标面口径（"集成模式构建零错误（--target sdurws_ird_runtime_test）"），措辞合规；**F-012 家族**（提交标题动词开头）——本任务两份提交同型再现 → 转登 **F-042**；其余 open 条目辖域与 runtime 基座—世界变换面无交集。无静默积压；无可随本任务顺手消账项（G-3 建议项随 RT-T07 消账路径见 F-043）。

## 4.9 commit 质量 —— PASS（附 G-1/G-2 建议级）

- 39fae2e `[WP-06-T06] 实现基座—世界变换规则…`：动词开头 ✓；四段齐备 ✓；验证结论与留痕一致（目标面口径，无 F-036 型失准）。
- 5034611 `[WP-06-T06] 验证留痕：…`、ca78934 `[units] runtime v0.7：…`：四段齐备、验证结论与留痕实质一致，**但标题名词短语开头**——F-012 家族第 11/12 例（G-2 → F-042，建议级，不追溯改写）。
- **G-1（计数失实）**：提交与 ird-test-report.json 多处称"新增 30 例／既有 142 例回归"；实测**新增 42 例**（新测试文件 TEST 定义逐条清点＝42；送验 XML 172 − 基线 wp06-t05 XML tests="130"＝42；报告自身逐例 trace 清单亦为 42 条）、**既有 130 例**。总量 172 与通过态准确、逐例清单完整——方向为构造性错拆（30+142 恰凑 172），无粉饰；F-025 同型，建议级 → F-041。

## 4.10 留痕完整 —— PASS

- `traceability/builds/wp06-t06/` 10 件：双模式 gtest XML（tests="172" failures="0"，双份一致；testcase 元素逐一点数=172）、integration-build/integration-test-run/smoke-configure/smoke-build/smoke-test-run/probe-build（内含探针红→还原→复绿全程与退出码）/ird-gates-run 日志、ird-test-report.json（ird-test-report/1 格式：三组 acceptanceCoverage 逐条消账＋probe/gates/deviations 段＋逐例 trace）。
- phase-one-validation.log L44 `RT-T06.json: validate-task: PASS (1 tasks)`（契约编译批次留痕）；resultNote 义务由 ird-test-report.json 承载——RT-T05 验收（PASS 先例）同口径。
- 瑕疵（并入 G-1）：报告 probe 段引用 "probe-test-run.log" 文件名，实际该内容位于 probe-build.log 末段（文件名笔误，证据本体在库且完整）。

## 4.11 偷懒/缩水实现扫描 —— PASS

- ①diff 新增行 `TODO|FIXME|stub|placeholder|未实现|not implemented` 扫描：唯一命中为测试文件头"RT-STUB-0 同款纪律"（testkit 登记的边界声明机制引用，非未实现标记）；契约无"触发式延后"豁免条目，无需逐条豁免。
- ②空实现检测：9 个公共函数全部有完整实现（逐元素算术本体），无返回默认值的占位体；防御性兜底分支有实现（抛 RuntimeError）。
- ③逐条证据表：见 4.4——三组 acceptance 逐条给出具名测试/命令输出/文件行号，无"已实现"空声明。（报告内 30 例计数失实已在 G-1 登记改正值为 42 例。）
- ④测试强度：断言校验行为与边界（精确位相等、容差正反两侧、错误码、detail 文案回显、确定性位比较）；4.5 双探针红＋复绿实证非恒真。

## 发现问题清单（全部建议级，无阻断）

| # | 级别 | 内容 | 转登 |
| --- | --- | --- | --- |
| G-1 | 建议 | 计数失实：提交信息与 ird-test-report.json 称"新增 30 例/既有 142 例"，实测 42 新增/130 回归（基线 XML 130＋新文件 42 例＝172；报告自身逐例清单 42 条）；另报告 probe 段引用不存在的 probe-test-run.log 文件名（内容实在 probe-build.log） | **F-041**（open） |
| G-2 | 建议 | 提交标题名词短语开头（5034611『验证留痕：…』、ca78934『[units] runtime v0.7：…』）——F-012 家族第 11/12 例，模板补救落地前口径持续漂移 | **F-042**（open，与家族合并处置） |
| G-3 | 建议 | 重力投影方向测试敏感度缺口：现有用例仅用对称旋转（R_x(π)、I），R·g 与 Rᵀ·g 数学不可区分——探针 2a 实证方向混淆（Wall/Custom 类非对称安装）不可检。建议随 RT-T07 或后续用例补一条非对称预设（如 Wall：g_base=R_y(π/2)ᵀ·(0,0,−9.81)=(+9.81,0,0)）投影断言 | **F-043**（open） |

## 裁决

**verdict＝pass**：4.1~4.11 全部通过，无阻断级问题；建议级 G-1/G-2/G-3 已逐条转登 findings.json（F-041/F-042/F-043，open），不阻塞合入。验收对象为冻结 SHA `ca789342c6a5c61d4d8d216bed162ea93ed4c0b2`（origin/wp06-t06 与该 SHA 一致，无分支漂移）；合入决策归所有者（PIPE §4.7），验收者不合入、不改动实施产物、不向任务分支推送。
