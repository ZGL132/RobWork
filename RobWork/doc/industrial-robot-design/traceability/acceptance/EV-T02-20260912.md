# EV-T02 验收记录（对抗式验收，attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 ID | EV-T02（≙WP-05-T02，evidence 错误与依赖类型 Errors/Dependency） |
| 契约 | tasks/foundation/EV-T02.json（status: ready；requirements: CON-05；designRefs: units/evidence.md §3.4/§4.2/§12；verify: `cmake --build build --config Release --target sdurws_ird_evidence_test`） |
| 验收对象 | 分支 `wp05-t02`，head＝`d1314a6dd44c23bad084e431c419a29a6bbaf92e`（40 位冻结对象）；漂移检测 PASS——主仓工作树 HEAD、`origin/wp05-t02`、该 SHA 三者实测一致（`git rev-parse`，2026-09-12）；范围 `fd05641f5f7cd5b6413553606a18ec67a4f88411..d1314a6dd44c23bad084e431c419a29a6bbaf92e`（base＝origin/redesign-main，3 个提交：9f5fc00b 错误契约→d134ef5b 依赖类型→d1314a6d 留痕与文档同步） |
| 尝试序号 | attempt=1（evidence 分支 `acc/EV-T02/1`，基于 origin/redesign-main＝fd05641f） |
| **verdict** | **pass**（4.1～4.11 全部通过、零阻断级问题；新增建议级发现 2 条——F-057 evidence.md 卡头版本行未随 §15.4 v0.3 同步、F-058 evidence.md「AI 执行就绪补充」F-005/F-006 同族转义损毁新实例，均已按 ACC §5 转登 findings.json，不阻塞合入） |
| 验收日期 | 2026-09-12 |
| 独立性声明 | 验收者为全新上下文会话（PIPE §0.4 子代理派发模式，无需降级声明），仅消费任务契约、设计文档、送验提交产物与留痕文件，未读取实施者任何过程叙述；构建、测试、探针、门禁、治理脚本全部由验收者亲手在独立 worktree 重跑，复现优先于采信（ACC §2 四条逐条满足）。对抗式立场：为"不通过"找证据——对 22 个测试用例逐用例通读核对断言强度、对实施侧留痕全部数值主张逐项复算、以两个独立注入探针验证测试真实失败能力，并实测发现卡头版本行失同步（F-057）与 F-005/F-006 同族损毁在 evidence.md 的新实例（F-058）。 |
| 验收环境 | Windows 10 x64（10.0.26200）/ Git Bash / VS2022（MSVC）/ Qt 6.11.1（CMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64）/ vcpkg 经典模式（toolchain 绝对路径 D:/10_Source_Repos/21_robot/RobWork/vcpkg/scripts/buildsystems/vcpkg.cmake）/ pwsh 7。复现现场＝`%TEMP%\ird-acc-ev-t02-a`（detached worktree @ d1314a6d，集成树全新配置）；冒烟树＝`%TEMP%\ird-acc-ev-t02-smoke`（独立从零目录）；记录回传＝`%TEMP%\ird-acc-ev-t02-b`（分支 acc/EV-T02/1）。验收日志在 worktree A 根与 %TEMP% 下，验收后随 worktree 删除——本记录为唯一证据载体。 |
| 环境前置说明（均为既有环境链问题、非本任务引入） | ①全新集成树配置需主仓同款参数：vcpkg toolchain＋Qt CMAKE_PREFIX_PATH（F-007 既有）；②`RobWork/RobWorkStudio/bin/RobWorkStudio.ini.template.static`／`ini.shared.in` 被 .gitignore 吞掉不入 checkout，从主仓手工复制（F-007 再次实证）；③按 F-008 判例口径以任务目标面从零构建（本验收目标＝`sdurws_ird_evidence_test`，其依赖闭包 sdurws_ird_core＋sdurw_math 从零编译）——未做全树构建，F-008 判例沿用 RT-T12/RT-T13 系列记录。 |

---

## 4.1 改动范围 —— 通过

- `git diff --name-status fd05641f..d1314a6d` 共 **24 个文件**：evidence 域 8 件（CMakeLists.txt 修改、`include/sdurws/ird/evidence/Errors.hpp`＋`Dependency.hpp` 新增、`src/Errors.cpp` 新增、`test/ErrorsTest.cpp`＋`DependencyTest.cpp` 新增、`include/sdurws/ird/evidence/README.md` 修改）＋units/evidence.md（M，仅追加 §15.4 v0.3 一行）＋traceability 15 件（builds/wp05-t02/ 下 14 件留痕＋findings.json F-056 登记）——全部落在契约 allowedFiles（`industrialrobot/evidence/**`、`industrialrobot/CMakeLists.txt`〔未实际触及，允许〕、`units/evidence.md`、`traceability/**`）。
- forbiddenFiles（REQUIREMENTS.md／ARCHITECTURE.md）零触碰（24 文件清单反证）。
- 无未登记框架源码改动：diff 内无 industrialrobot 之外任何文件；`patches/` 零新增（SA-02 不触发）。

## 4.2 可复现构建 —— 通过（集成全新树从零、按 F-008 判例口径任务目标面）

- **集成模式**（worktree A 全新配置）：`cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=<vcpkg 绝对路径> -DCMAKE_PREFIX_PATH=<Qt6>` → 配置 exit 0（Configuring done 74.8s）；缓存 grep 实证 **`RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`**。
- **任务目标面从零**：`cmake --build build --config Release --target sdurws_ird_evidence_test -j` → **exit 0、`: error ` 计数 0**（依赖闭包 sdurw_math→sdurws_ird_core→sdurws_ird_evidence→测试目标全部从零编译；61 处 warning 均为框架既有告警面，无新增 warning 类型）。
- **集成测试**（验收者亲手运行 exe）：**exit 0，22/22 PASSED**（`--gtest_brief=1`：22 tests from 7 test suites）。
- **冒烟模式**（独立从零目录，vcpkg toolchain 绝对路径，`--config Release -j`）：配置 exit 0、构建 **exit 0、`: error ` 计数 0**（唯一 warning＝MSB8029 临时目录增量提示，环境性质，与 RT-T12/RT-T13 记载一致）；测试 **exit 0，22/22 PASSED**。
- 实施侧留痕交叉核对：ird-test-report.json 显式声明两模式树性质与配置（集成＝仓库根增量树 Release、冒烟＝全新独立树 Release——F-054/F-055 纪律已落地）；其 XML `tests="22" failures="0" errors="0"`（集成＋冒烟各一份）与我方独立复跑逐数一致。

## 4.3 契约 verify 命令 —— 通过

- 契约 verify 数组仅 1 条：`cmake --build build --config Release --target sdurws_ird_evidence_test`——验收者在 worktree A 于纯净状态（探针注入前）**逐字执行 → exit 0**（构建日志 `%TEMP%\ird-acc-ev-t02-a\verify-command-exact.log`）。
- 加跑（超出契约要求）：`validate-task.ps1 -TaskFile tasks/foundation/EV-T02.json` → **PASS (1 tasks)，exit 0**；`validate-docs.ps1` → **PASS (20 units, 12 trace entries, 150 task files)，exit 0**——均与实施侧 validate-task.log/validate-docs.log 结论一致。
- `validate-state.ps1` 未执行——state.json 为编排者专属，本验收不涉流水线状态文件（派发说明第 2 条）。

## 4.4 acceptance 条目逐项 —— 全部通过

| 契约 acceptance | 验收者独立证据 | 结果 |
| --- | --- | --- |
| ① 声明闭包校验用例通过（依赖类型正反例） | 正例＝`EvidenceDependencyDeclarationClosure.ValidDeclarations_EV_DEP`（Required 无条件＋Conditional 引用已声明键/快照事实键＋七类 Kind 声明→零 issue）；反例＝`MissingClosureDeclaration_EV_DEP`（条件输入未声明且非事实键→ReferencedKeyNotInClosure 逐键定位＋下标，D-10 核心防线）、`RequirednessPairingViolations_EV_DEP`（Conditional 缺条件/Required 带条件）、`ConditionContentViolations_EV_DEP`（token 空/引用键空集/引用键语法/声明键语法四类）、`DuplicateDeclarationKey_EV_DEP`（重复键定位到下标 2）；异常轨＝`EvidenceDependencyRequire.ThrowOnClosureViolation_EV_DEP`（非法集抛 EvidenceError(DeclarationInvalid)、what() 带 evidence/ 前缀并聚合涉事键）——两模式实跑全绿（集成 22/22＋冒烟 22/22，验收者亲手执行） | 通过 |
| ② EvidenceErrorCode 全表 token 用例通过（§3.5——稳定 token 逐枚举） | `EvidenceErrorsFullTable.AllCodesPerToken_EV_ERR_NFR_COR_02`：手写期望表（9 值 token＋registryCode 逐值抄录设计原文，非实现反推——防恒真）＋`ASSERT_EQ(EvaluatorDuplicate, 8)` 枚举序号钉死防插入/重排；`Section13SuggestionsAllCovered_EV_ERR`：§13 清单 7 值各恰被 1 个枚举值承载（不重不漏）；`EvidenceErrorsThrow.MessagePrefixAndCodeAccessor_EV_ERR`：what()＝"<token>[: <detail>]"、code() 访问器、基类捕获——两模式实跑全绿。**口径说明**：acceptance 原文引用的"§3.5"章节在设计文本中不存在（F-020 既有登记，DOC-T07 已按 ready 锚点强制改锚 §3.4）——本条实质内容（稳定 token 逐枚举）由实现锚定 §3.1 组成表＋各抛错点原文承载，逐值与 units/evidence.md §13 行建议码清单及 §4.1.5④b/§9.4 抛错点原文核对一致，无语义缺口 | 通过 |
| ③ Dependency 条目语法与闭包校验器（§4.2）正反例用例通过 | 正例＝`EvidenceDependencyEntrySyntax.ValidEntries_EV_DEP`（七类 Kind 各一合法条目＋未适用条件条目 applied=false＋非空原因，字典序存放→零 issue；Kind 稳定名表 7 值钉住）；反例＝`PayloadKindMismatch_EV_DEP`（monostate＋Object 装 Policy 载荷）、`ObjectPayloadViolations_EV_DEP`（零 oid/零 cv/空 typeToken/条目键语法）、`OtherPayloadViolations_EV_DEP`（Configuration 空 canonicalBytes、Policy/NameMap 零身份〔CON-06〕、SampleSet 零身份〔KIN-04〕、UpstreamResult 键语法＋零 sliceId、Environment 空 valueToken——六类各一）、`AppliedReasonPairing_EV_DEP`（缺原因/空串原因/已应用带原因三种配对矛盾）、`EntrySetInvariants_EV_DEP`（空集/组内乱序/跨 kind 乱序/(kind,key) 重复）；键语法正反例＝`EvidenceDependencyKeySyntax.ValidKeys_EV_DEP`（§4.2.1 四个原文示例＋长度边界 3/64）/`InvalidKeys_EV_DEP`（空/2 字符/65 字符/首字符四类非法/后续字符四类非法含 NUL）——两模式实跑全绿 | 通过 |

## 4.5 测试真实失败能力 —— 通过（2 个负向探针，均即红→还原复绿）

| 探针 | 注入点（worktree A 临时修改，`git checkout --` 还原，终态 `git status --porcelain` 实测 0 行改动） | 结果 |
| --- | --- | --- |
| 探针 A（稳定 token 表漂移——acceptance ② 的守护面） | src/Errors.cpp L62 token 表返回值 `"evidence/snapshot-incomplete"` → `"evidence/snapshot-incomplete-PROBE"` | 重编译后 `AllCodesPerToken_EV_ERR_NFR_COR_02` **即红**（21 PASSED＋1 FAILED，exit 1）——全表用例对手写期望的逐字锚定真实有效；还原后复绿 |
| 探针 B（DependencyKey 语法边界——acceptance ③ 的守护面） | Dependency.hpp L78 长度上限 `key.size() > 64` → `> 65` | **首次重编译未红（MSVC 头依赖增量追踪迟滞——非测试弱点），touch DependencyTest.cpp 强制重编后 `InvalidKeys_EV_DEP` 即红**（65 字符反例应 false 实 true 被捕获，exit 1）；还原＋强制重编后 22/22 复绿——长度边界断言非恒真 |
| 补充 | 探针 B 还原后全量复跑 | 22/22 PASSED（exit 0），worktree A 终态零跟踪改动——零残留 |
| 对照 | 实施侧自证探针 probe-red-run.log | 同型（token 注入→同用例变红→exit 1 留痕）——其自证与我方独立探针互证成立 |

## 4.6 架构红线（AGENTS §5 八条） —— 通过

1. **L2 零 Qt**：全部新增文件（Errors.hpp/Errors.cpp/Dependency.hpp/两测试）include 面逐行核对＝仅 std＋gtest＋core 公共头，零 Qt；集成＋冒烟两棵全新树从零构建零错误佐证无 Qt 链接需求；`EvidenceBuild.NoQtInclude_EV_BUILD_NFR_MNT_01` 用例（现扫描含新增两公共头）在档且通过。
2. **业务域互链禁止（R-1）**：evidence 产品库链接面仍仅 `RWS::ird::core`（evidence/CMakeLists.txt 实测，diff 仅增列源文件未动链接）；Dependency.hpp 消费 core 值类型（ObjectId/ContentVersion/ContentIdentity/RunId）属 §3.2 唯一单元边授权面；不涉其他业务域单元。
3. **跨单元私有头禁止（R-2）**：仅 include `<sdurws/ird/core/Digest.hpp>`/`<sdurws/ird/core/Identity.hpp>`——实测两文件均在 `core/include/sdurws/ird/core/`（core 公共头目录）；无跨单元私有头；公共头均位于 `include/sdurws/ird/evidence/`（`PublicHeaderPathLayout` 用例在档）。
4. **框架零源码修改（SA-02）**：4.1 已证 diff 零框架文件、patches/ 零新增。
5. **不做名称前缀拼接/剥离（R-4）**：全部新代码无名称拼接/剥离逻辑（逐文件通读）；NameMap 载荷注释明示"evidence 永不解析名称内容——R-4"，仅承载内容身份。
6. **权威唯一（PA-1）**：token/建议码注释与实现明示"码值权威归 diagnostics StableCodeRegistry，本单元只产出 token/建议码面，不注册码值"；两处超出 §13 枚举的建议码按 P-PR-6 同模式作**建议值**补登并登记 F-056 转所有者裁决（不私裁）；声明闭包/条目校验器不越权实现注册表（EV-T10）或切片冻结（EV-T04）语义——抛错时机明确留白并登记。
7. **不可变历史（PA-2）**：units/evidence.md 仅追加 §15.4 v0.3 一行（diff 实测 1 insertion）；EvidenceErrorCode/DependencyIssueCode 枚举头内纪律注释"一经交付不得改动/插入，只能表尾追加并留痕"；无既有数据改写。
8. **不继承 old/ 实现**：全部代码从设计文档原文锚定编写（头文件逐节锚注 §3.1/§4.2.1/§4.2.2/§4.2.3/§13）；无 old/ 引用。

## 4.7 注释规范 —— 通过（公共头全查＋实现文件全查）

- **公共头全查**（本任务 2 个公共头）：`Errors.hpp`——文件头含设计依据（章节锚＋F-020 状态＋任务契约）、背景说明（为什么自有错误码不复用 core::CoreError）、错误语义总纲（fail-fast vs 环境错误归类）、线程安全（纯值并发只读安全）与确定性来源（编译期固定表）；9 个枚举值逐值中文注释含章节锚与业务场景；`token()`/`registryCode()`/`EvidenceError` 全部 Doxygen 中文（@brief/@param [in]/@return/@throws 语义、D-15 进程边界约束）。`Dependency.hpp`——文件头含失效链背景（为什么声明值得整个校验器）、header-only 形态说明、core 契约消费状态（P-EV-1 锚点）；全部类型/函数 Doxygen 中文（键语法语义、N-5 词表边界说明、presence 语义、复杂度、线程安全）。
- **实现文件**：`Errors.cpp`（本任务唯一 .cpp，全查）——文件头确定性来源说明（switch 全枚举无 default→C4061/C4062 告警暴露＋测试侧强制防线的双层理由）、makeWhat 前缀拼装逐步注释、两张表逐 case 命名出处注释。header-only 的 Dependency.hpp 校验器函数体内按规则 1~5 分步中文注释、每分支业务场景说明——等效实现文件抽查覆盖。
- **§2.5 高危信息表**：物理单位（本单元纯计数/身份/字符串，无物理量——不适用且头内明示"切片不承载物理量数值"语义）；坐标系/角度制（不适用）；所有权（token 返回"静态存储期，调用方无需释放"）；线程约束（逐头声明线程安全属性及其依据）；错误语义（fail-fast vs 环境错误归类＋D-15 进程边界＋token/建议码归类说明）；确定性来源（固定 switch 表、无 locale、issue 顺序＝输入序×检查序、(kind,key) 枚举声明序权威）。
- **测试文件**：两文件头注明验证的需求/acceptance 映射与组名出处说明（EV-ERR/EV-DEP 为实现侧组名、对照 §12 行，EV-BUILD 同例）；每用例中文说明验证点与设计锚点。
- 反面示例扫描：未发现复述代码、无信息量占位、中英混杂机翻腔或过期注释。

## 4.8 文档同步＋findings 未关闭条目相关性 —— 通过（含 2 条建议级新发现）

- **实现与任务卡零偏差声明核对**：三项 acceptance 与交付物对应关系成立（4.4 逐项）；契约产物列（`Errors.hpp/.cpp`＋`Dependency.hpp`）逐文件交付——Dependency.hpp 无 .cpp 与契约产物列一致（header-only 并在单元卡 v0.3 ①登记）。两处**实现侧收紧**（Conditional 必带条件/Required 必不带、引用键非空集、同集键唯一）与 Kind 稳定名表均已按 DTB §5.4 在 §15.4 v0.3 行显式登记——属"偏差已登记"合规路径，且逐条锚定设计原文（D-10/§4.2.3①/§5.2）非私改语义。
- **单元卡同步**：§15.4 v0.3（2026-09-12）实测在档，四项登记内容与实现逐项相符（9 值码面来源、Kind 名表、校验器分轨、测试组名）。**发现 F-057（建议级）**：卡头"文档版本"行仍为 v0.2（日期 2026-09-09），未随 v0.3 同步推进——runtime.md（v0.15）/core.md（v0.11）先例为卡头版本行承载版本链；提交信息"单元卡版本按仓库惯例 v0.2→v0.3"仅 §15.4 侧成立、卡头侧失同步（方向为文档内部不一致，validate-docs 不校验此维度故未红）。**发现 F-058（建议级）**：evidence.md「AI 执行就绪补充」节存在 F-005/F-006 同族转义损毁（`$(evidence.ToUpper())` 未展开、"locked" 应为 "blocked"〔\b 被吞〕、"alidate-docs.ps1/alidate-task.ps1" 缺首字母〔\v 被吞〕）——第三份单元卡新实例（F-005＝core.md、F-006＝testkit.md），既有损毁非本任务引入（v0.3 仅追加一行，diff 反证），随 F-005/F-006 家族统一处置。
- **追踪矩阵/任务状态**：契约 status 翻转 done 归收尾段（PIPE §4.7⑥），非实施者责任；validate-docs PASS（20 units, 12 trace entries, 150 task files）实证单元卡/任务索引/追踪矩阵机读一致性（EV-T02 designRefs §3.4/§4.2/§12 锚点均有效）；§14 追踪矩阵 CON-05 行设计落点（§4.2/§5.1/§5.3）与本实现交付面吻合。
- **findings.json 未关闭条目逐条相关性核对**（open 类全部过目）：
  - **F-020（§3.5 章节缺位）**——本任务直接相关：acceptance ②与 §12 行引用的 §3.5 不存在，实现以 §3.1 组成表＋抛错点原文承载并逐项锚定、未私设章节（合规处置）；F-056 处置建议②已将"增设 §3.5 时把 9 值全表一并析出"与 F-020 合并——**两发现已联动，无可顺手消账面**（章节增设归 evidence 详设所有者）。维持 open。
  - **F-056（两建议码超出 §13 枚举——本任务实施侧已登记）**——与本任务直接相关且登记合规（来源/处置/责任方齐备）；我方核对实现（Errors.cpp registryCode 表）与登记内容逐字一致。维持 open（转 evidence 详设所有者＋diagnostics 所有者）。
  - **F-019/F-051（ird_gates 红：testdata_lint 两边＋runtime_test→testkit 边）**——我方独立实跑 ird_gates 命中恰该 3 项（IRD-GATE-T1×1＋IRD-GATE-SUB×2）、evidence 面零命中、本任务零新增链接边——与实施侧登记一致；修复面在 cmake/（WP-01 辖域），非本任务可消费。维持 open。
  - **F-007/F-009（环境链）**——本轮复现再次实证（toolchain＋Qt 前缀＋ini 模板三件手工补）；修复面在 AGENTS §4.1/治理文档。维持 open。
  - **F-008（全树构建基线缺陷）**——本轮按判例以任务目标面从零构建规避；非本任务可消费。维持 open。
  - **F-005/F-006（core.md/testkit.md 转义损毁家族）**——本任务发现 evidence.md 同型损毁新实例，已登记 F-058 并挂靠该家族；F-005/F-006 本体修复面在对应单元卡（DOC 治理任务），非本任务 allowedFiles 可消费。维持 open。
  - **F-012 家族（提交标题名词短语：F-023/024/026/027/030/034/037/039/040/042/044/045/047/048）**——本任务 3 提交中 2 个动词开头合规（"实现…"/"实现…"）；第 3 个"留痕与文档同步：…"以名词短语开头，**家族同型再现 1 例**（与 F-034 已判"RT-T02 验证留痕…"、F-040 已判"[WP-06-T05] 验证留痕…"同形态）——并入家族既有 open 记录口径，不另立新编号（家族根因与补救建议已在案，模板补救落地前预计继续漂移）；纯风格瑕疵不构成阻断，正文四段齐备。
  - **F-025/F-041（计数失实家族）**——本任务计数主张（22/22 两模式、EV-ERR 3＋EV-DEP 15＋既有 4＝22、9 值＝§13 7＋补登 2、7 test suites）经我方逐项实测**全部吻合，零失实**。
  - **F-028/F-035（"ird_gates 未执行/门禁未建成"失实家族）**——本任务实跑 ird_gates 并如实登记 exit=1＋3 处既有命中（ird-gates.log 在档），无同型失实。
  - **F-010（.gitignore 吞留痕日志）**——本任务 14 件留痕已正确入库（commit 信息称 `add -f 强制纳管——F-010 教训`；git ls-tree 实测在档）；规则面问题本身维持 open。
  - **F-032（validate-task planned 锚点弱覆盖）/F-029/F-031/F-033/F-043/F-046/F-048**——他单元/横切项，与本任务无涉。维持 open。
  - **结论：无一条 open 发现可在本任务 allowedFiles 内顺手消账；本任务新增建议级发现 2 条（F-057/F-058），F-012 家族同型再现 1 例（并入既有家族口径）。**

## 4.9 commit 质量 —— 通过（2 合规＋1 家族同型，建议级）

- 3 提交标题均带 `[WP-05-T02]` 前缀：9f5fc00b"**实现** evidence 错误契约…"、d134ef5b"**实现** evidence 依赖类型…"动词开头合规；d1314a6d"留痕与文档同步：…"名词短语开头——F-012 家族同型（见 4.8，建议级不阻断）。
- 正文四段（改动内容/改动原因/验证/影响面与兼容性）逐提交齐备，均带 `Acc-Covered` 追溯尾注（2/1,3/无新增——与三条 acceptance 消账分布一致）。
- 验证段与留痕一致性抽查：9f5fc00b 称"本提交时点测试 7/7（EV-T01 四红线＋EV-ERR 三用例）"与该时点测试组成相符（Dependency 后合入故 7/7 合理）；d134ef5b 称"全量 22/22 两模式"与留痕 XML 一致；d1314a6d 称"ird_gates 3 处命中均为 F-019 既有基线缺陷、evidence 面零命中"经我方独立实跑逐项复得；"token 漂移探针注入错值→EV-ERR-1 变红→恢复复绿"经 probe-red-run.log＋我方独立探针 A 互证。无粉饰、无与留痕矛盾的表述。

## 4.10 留痕完整 —— 通过

- `traceability/builds/wp05-t02/` 实测 14 件齐备：integration-build.log／integration-cache-check.txt／integration-verify.log／integration-test-run.log＋sdurws_ird_evidence_test-integration.xml（`tests="22" failures="0" errors="0"`）／probe-red-run.log／ird-gates.log／smoke-configure.log／smoke-build.log／smoke-test-run.log＋sdurws_ird_evidence_test-smoke.xml（`tests="22" failures="0" errors="0"`）／validate-docs.log／validate-task.log／ird-test-report.json。
- ird-test-report.json（resultNote 载体）实测为有效 JSON：summary 22/22 两模式、逐用例需求/acceptance trace 清单 22 条、构建树性质与配置显式声明（F-054/F-055 纪律落地）、failureCapabilitySelfCheck 探针段、notExecuted=[]（如实——ird_gates 有独立登记字段且 exit 1 语义为既有命中）。
- phase-one-validation.log 未更新——该文件为 WP-03-T01 时代遗留聚合日志，现行口径以任务目录 ird-test-report.json 为聚合载体（RT-T13 等近期验收同判例），不构成缺口。
- 实施侧 XML／日志数值与我方独立复跑逐数一致（22/22 两模式、gates 3 命中、validate PASS）。

## 4.11 偷懒/缩水实现扫描 —— 通过

- ①diff 新增行 `TODO|FIXME|stub|placeholder|未实现|not implemented` 扫描**零命中**（`git diff base..head | grep "^+" | grep -iE ...` exit 1 实证）；无"触发式延后"豁免需列出。
- ②空实现检测：公共接口函数（token/registryCode/EvidenceError 构造与访问器、isValidDependencyKey/isWellFormedToken/dependencyKindToken/两个 validate*/requireValid*）逐一通读——全部为实质实现（switch 全表、逐字符白名单、逐规则校验循环、聚合抛错），无空体/仅返回默认值的占位；monostate 保留态为显式校验拒绝对象而非偷懒路径。
- ③acceptance 逐条证据表：见 4.4——三条 acceptance 每条均有具名测试/断言/命令输出级证据，不依赖"已实现"声明。
- ④测试强度：22 用例断言校验行为与边界（手写期望表防实现即期望、枚举序号钉死防重排、逐类正反例含空集/NUL/长度边界/presence 三态、下标定位断言）；非恒真经 4.5 双探针实证（注入即红）；与 EV-T01 四红线用例（零 Qt/无私有头/头布局/源码树）回归保持 4/4。

---

## 问题清单

| 级别 | 编号 | 描述 | 处置 |
| --- | --- | --- | --- |
| 阻断 | 无 | — | — |
| 建议 | **F-057**（已转登 findings.json） | evidence.md 卡头"文档版本"行未随 §15.4 v0.3 同步：卡头仍为 v0.2（日期 2026-09-09），提交信息"单元卡版本按仓库惯例 v0.2→v0.3"仅 §15.4 侧成立——runtime.md（v0.15）/core.md（v0.11）先例为卡头版本行承载版本链；validate-docs 不校验该维度故机检未红。方向为文档内部不一致（少报），v0.3 登记内容本身完整准确 | 随下次触及 evidence.md 的任务（EV-T03 或 §3.5 增设/F-020 处置）顺手把卡头版本行推进至最新并补版本链 |
| 建议 | **F-058**（已转登 findings.json） | evidence.md「AI 执行就绪补充」节 F-005/F-006 同族转义损毁新实例（第三份单元卡）：`$(evidence.ToUpper())` 未展开、"locked" 应为 "blocked"（\b 被吞）、"alidate-docs.ps1/alidate-task.ps1" 缺首字母（\v 被吞）——既有损毁非本任务引入（v0.3 diff 仅 1 行，反证在档） | 并入 F-005/F-006 家族统一处置（DOC 治理任务批量修正三份单元卡同节） |
| 建议（既有家族，不另立编号） | F-012 家族同型再现 1 例 | 送验第 3 提交 d1314a6d 标题"留痕与文档同步：…"名词短语开头（家族第 18+ 例，与 F-034/F-040 已判同形态）；正文四段齐备、验证结论与留痕一致（验收者逐项复得），纯风格瑕疵 | 家族根因补救建议已在案（实施者模板补动词开头示例），随模板补救落地统一消解 |
| 建议（既有，非新增） | F-007/F-009 再次实证 | 全新验收树需手工补 toolchain＋Qt 前缀＋ini 模板三件（再实证）；gate-all 类扫描脚本仍限主树运行（本轮改用 worktree A 构建树 `--target ird_gates` 实跑成功——F-009 的 worktree 缺 vcpkg 前提在该路径下可绕过，供后续验收参考） | 维持 open，归治理/AGENTS §4.1 辖域 |

## 验收者环境补充

- 集成树全新配置一次成功（toolchain＋Qt 前缀＋ini 模板三件前置就位后）；目标面从零构建含完整框架依赖闭包编译。
- ird_gates 经 worktree A 构建树 `--target ird_gates` 实跑（exit 1＝命中语义，非环境失败）；命中 3 项与实施侧日志逐项一致（console GBK 输出乱码不影响命中行判读——IRD-GATE-T1/SUB 标识符与目标名可辨）。
- 探针 B 首次未红的 MSVC 头依赖增量追踪迟滞现象已如实登记处置过程（touch 强制重编后即红）——该现象与送验对象无关，属构建工具链行为，且不影响"测试真实失败能力"结论（红已实证）。
- 验收后 worktree A 已还原零残留（`git status --porcelain` 0 行实测）；两个 worktree 在本记录推送成功后删除。
