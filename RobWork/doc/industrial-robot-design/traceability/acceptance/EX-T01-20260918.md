# EX-T01 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | EX-T01（构建落位：execution 占位转真实库；≙WP-08-T02；单元 execution；契约 tasks/foundation/EX-T01.json） |
| 送验对象（冻结） | b0861ec2671d74e004d65e8724256c966fe6a130（分支 wp08-t01 尖端；验收前后 `git rev-parse origin/wp08-t01 wp08-t01` 均＝该 SHA——分支零漂移，ACC §3） |
| diff 范围 | 2673b21b7091c7c246b3542a369db5f616a2d960..b0861ec2671d74e004d65e8724256c966fe6a130（单提交 b0861ec2，12 变更文件，1049 insertions/7 deletions） |
| verdict | **fail**（阻断级 1 项：验证日志留痕声称入库而实际 0 件在库——4.9 陈述失实＋4.10 构建日志未落位；其余 9 项检查全部通过，技术实现零缺陷，返工面仅为留痕补交） |
| 验收日期 | 2026-09-18 |
| 验收环境 | 全新 detached worktree（`git worktree add --detach` @ b0861ec＝`C:/Users/zgl18/AppData/Local/Temp/acc-ext01-a`）从零复现：集成 build 树（worktree A 内 `build/`，重新配置后缓存实测 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`）与独立冒烟 build 树（`%TEMP%/acc-ext01-smoke`，独立缓存、vcpkg toolchain 绝对路径）均为本验收新建（非主树缓存复用）；记录回传 worktree（`%TEMP%/acc-ext01-b`，分支 acc/EX-T01/1 基于 origin/redesign-main＝840688a9340b5dba6dff557359686abf2f88a63b）。MSVC 2022 x64（VS 17 2022）＋vcpkg 经典模式。**环境前置三件**（fresh worktree 既有环境缺口，非实施缺陷——F-007/F-200 已登记同型，IO-T05/T06/T07 验收同款处置）：①集成配置需 `-DCMAKE_TOOLCHAIN_FILE=<主仓>/vcpkg/scripts/buildsystems/vcpkg.cmake`；②需 `-DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`（首配实测失败于 Qt5/Qt6 NOT found——主树缓存 CMAKE_PREFIX_PATH 同值）；③`RobWork/RobWorkStudio/bin/` 下 `RobWorkStudio.ini.template.static`/`RobWorkStudio.ini.shared.in` 未跟踪文件在 fresh 树缺失（配置期 `CMake Error: File …RobWorkStudio.ini.template.static does not exist`），自主仓补拷后配置零错误——两文件被 `.gitignore [Bb]in/` 规则排除在版本控制外，补拷后 worktree A `git status --porcelain` 零残留。独立性（ACC §2 四条逐条满足）：全新上下文只读代码/契约/单元卡/在库留痕，未读取实施者过程叙述；对抗式立场逐项找反证；全部构建、四跑测试、ird_gates 复跑、两处失效注入探针均为本验收者亲手执行（ACC §2.4 复现优先于采信——实施留痕数值仅作交叉比对）。本日（2026-09-18）无既有同名验收记录（`git log --all -- "*EX-T01-20260918*"` 零命中），无需 -r2 后缀 |

## 4.1 改动范围 —— 通过

- `git diff --name-only 2673b21b..b0861ec26` 实证 **12 个变更文件**，逐个比对契约 `allowedFiles`：
  - `RobWork/RobWorkStudio/src/rwslibs/industrialrobot/CMakeLists.txt`×1——allowedFiles 显式列出；
  - `…/industrialrobot/execution/**`×5（CMakeLists.txt、src/Execution.cpp、test/BuildRedLineTest.cpp、contract_test/LinkageContractTest.cpp、include/sdurws/ird/execution/README.md）——在 `execution/**` 内；
  - `RobWork/doc/industrial-robot-design/units/execution.md`×1——allowedFiles 显式登记；
  - `RobWork/doc/industrial-robot-design/traceability/builds/wp08-t01/**`×5——在 `traceability/**` 内。
- `forbiddenFiles`（REQUIREMENTS.md/ARCHITECTURE.md）零触碰（不在 diff 文件清单）。
- SA-02：diff 文件清单无任何框架源码文件；`industrialrobot/patches/` 零触碰——零未登记框架改动。

## 4.2 可复现构建 —— 通过

- **集成模式**（worktree A 内 `build/`，全新配置）：补齐环境前置三件后 `cmake -S <A>/RobWork -B <A>/build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON -DCMAKE_TOOLCHAIN_FILE=… -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64` → `Configuring done (5.9s)`/`Generating done`，exit 0；缓存 grep 实测 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`。`cmake --build <A>/build --config Release --target sdurws_ird_execution_test` exit 0（error C/LNK/fatal 计数＝0，产物 `build/RobWorkStudio/bin/Release/sdurws_ird_execution_test.exe`）；`--target sdurws_ird_execution_contract_test` 同样 exit 0 零 error。
- **独立冒烟模式**（`%TEMP%/acc-ext01-smoke`，独立缓存）：`cmake -S <A>/RobWork/RobWorkStudio/src/rwslibs/industrialrobot -B <冒烟树> -DCMAKE_TOOLCHAIN_FILE=<主仓>/vcpkg/scripts/buildsystems/vcpkg.cmake`（绝对路径——CORE-T01 验收 G-2 教训）configure exit 0；`cmake --build --config Release` exit 0，产物 `execution/Release/` 下 `sdurws_ird_execution.lib`＋两测试 exe 齐全（唯一告警 MSB8029 系本验收将构建树置于 %TEMP% 所致，非实施引入）。
- 结论留痕：本验收日志存于验收临时现场（随流程销毁），数值已如实转录于本记录各节；两模式构建均零错误。

## 4.3 契约 verify 命令 —— 通过

- 契约 `verify` 数组仅一条：`cmake --build build --config Release --target sdurws_ird_execution_test`——本验收者在 worktree A 集成树逐字亲手执行，exit 0（见 4.2）。无其他条目。

## 4.4 acceptance 条目 —— 通过（ird_gates 已建成并实测复跑）

| # | 契约条目 | 验收者证据 |
| --- | --- | --- |
| 1 | 双模式构建零错误；INTERFACE→STATIC（C++17、PUBLIC 链 core/evidence/project；diagnostics 注入不落链接——P-EX-8）；EX-BLD-1 构建项 | 4.2 两模式 exit 0；`execution/CMakeLists.txt` L68-69（STATIC）、L76-80（C++17 双设）、L113-116（PUBLIC 三链）、L120-122（别名）；RegisteredEdgesPresent/DiagnosticsEdgeInjectionNotLink 用例绿 |
| 2 | 红线扫描零命中（仅三条登记边；零 Qt；零业务域互链/无私有头出 include；P-EX-3 注入零编译依赖） | 7/7＋5/5 扫描用例两模式全绿（4.4/4.10 XML）；配置期守卫 L254-273 两模式构建成功即未触发；ird_gates 本验收者复跑＝17 命中，与 wp11-t07 基线逐码同集、**零 execution 项**（自测 pass_clean~fail_t2 九项全过） |
| 3 | 两测试目标按 DTB §5.5 注册并可构建（vcpkg find_package(GTest CONFIG REQUIRED)） | 两模式产物 exe＋lib 实存；`find_package(GTest CONFIG REQUIRED)`＝CMakeLists L56；ird_add_gtest 宏 L224-236；落位期最小构建冒烟用例形态＝契约本条明文允许 |
| 4 | O-24 四词表按 core 现状承接不私裁消账；P-EX-1 零跨单元头消费 | NoVocabularyRedefinition_O24 用例绿（公共头面仅 README.md）；源码零 `sdurws/ird/<他单元>` include（RuntimePolicyZeroCompileFace＋NoCrossUnitInclude 绿＋本验收者全文实读）；O-24 未动 DTB §4（diff 无该文件） |
| ird_gates | （WP-01-T01 已落地——非"未执行"豁免情形） | 本验收者亲手复跑 exit 1＝17 命中（SUB×11/LIB×4/T-1×1/R-4×1），与提交声明及 wp11-t07 基线一致、零新增 |

## 4.5 测试真实失败能力 —— 通过

- **探针 1**：向 `src/Execution.cpp` 追加 `#include <QObject>` → `ExecutionBuild.NoQtInclude_DT_BUILD_NFR_MNT_01` **FAILED**（其余 6 用例 OK，检出精确无扩散）。
- **探针 2**：还原后追加 `#include "sdurws/ird/policy/Policy.hpp"` → `NoCrossUnitInclude_DT_BUILD_R1_R2` 与 `RuntimePolicyZeroCompileFace_DT_BUILD_PEX3` **双双 FAILED**（P-EX-3 具名自证用例非恒真）。
- 还原（`git checkout -- src/Execution.cpp`）后复跑：7/7＋5/5 全绿；worktree A `git status --porcelain` 零残留。注入仅存在于临时 worktree 源码树，已还原；任务分支与送验对象零触碰。

## 4.6 架构红线 —— 通过（AGENTS §5 八条逐条）

1. **L2 零 Qt**：execution 源码零 Qt include（NoQtInclude 绿＋探针证实检出力）；构建面无 Qt 链接（配置期守卫 L265-268＋ird_gates 零 execution 命中）。
2. **R-1 业务域互链**：链接边仅 core/evidence/project 三条（配置期守卫 L256-264＋UnitEdgesRegisteredOnly＋ird_gates 零 execution 项）。
3. **R-2 跨单元私有头**：`include/` 下仅 README.md（PublicHeaderPathLayout 绿＋目录实读）；无任何跨单元头 include。
4. **SA-02 框架零修改**：见 4.1（diff 无框架源码、patches/ 零触碰）。
5. **R-4 名称前缀拼接/剥离**：ird_gates R4 仅命中 project 既有辖区 1 处（ProjectStoreImpl.cpp，例外登记在案），execution 零命中；diff 无 RobWork 字面量拼接新增。
6. **PA-1 权威唯一**：本任务仅构建落位＋单元卡登记；src/ 锚点翻译单元零代码，未越权实现 runtime/project 等他单元语义。
7. **PA-2 不可变历史**：不涉修订写路径与历史数据。
8. **不恢复 old/**：diff 无 old/ 相关内容。

## 4.7 注释规范 —— 通过

- **公共头全查**：`include/sdurws/ird/execution/` 下仅 README.md（不参与编译的保留位说明）——无可注释代码面。
- **实现文件 4 个全查（≥3 要求满足）**：
  - `execution/CMakeLists.txt`（273 行）：每个命令段均有中文注释块，载明设计依据（units/execution.md §3.2/§3.4/§12、DTB §5.1/§5.5、需求 NFR-MNT-01/02、红线 R-1/R-2/T-1）与陷阱处置（P-EX-1/P-EX-3/P-EX-8/O-24）；守卫段逐模式解释匹配语义与 NOTFOUND 兜底。
  - `src/Execution.cpp`：68 行文件头 Doxygen（设计依据、"为什么是注释文件"的背景说明、EX-T02~T09 落位安排、四陷阱处置约束、线程约束段——AGENTS §2.5 线程约束项覆盖）。
  - `test/BuildRedLineTest.cpp`：每用例 Doxygen 中文说明锚定需求/条目编号（NFR_MNT_01/O24/R1_R2/PEX3 后缀自证）；`isIncludeDirective` 注释载明"注释散文 vs 编译期包含"的区分依据与 PRJ-T01 误报史；`collectCppFiles` 排序注明确定性来源（NFR-COR-02）。
  - `contract_test/LinkageContractTest.cpp`：同风格；与 project 落位白名单的差异、三面证据承载（文本扫描/配置期守卫/链接实证）均有说明。
- §2.5 高危信息：错误语义（守卫 FATAL_ERROR 消息含红线编号与登记出处）、线程约束（Execution.cpp 头）、确定性（排序键）均在位；本任务无物理量/位姿代码面，单位/坐标系/角度制式项不适用。

## 4.8 文档同步 —— 通过（含 findings.json 未关闭条目相关性核对）

- `units/execution.md` v0.2→v0.3（diff 实读）：卡头构建落位行更新＋§15.4 变更记录行——与实现逐项一致（STATIC、PUBLIC 三链、`_test`/`_contract_test` 注册、锚点翻译单元、P-EX-1/P-EX-3/P-EX-8/O-24 处置留痕），契约语义零变更——**实现与任务卡零偏差**。
- findings.json 未关闭条目相关性（基于 origin/redesign-main＝840688a 版本全量过筛 open 条目）：
  - **F-007/F-200**（worktree 环境前置三件）：本次验收实测同型命中（首配 Qt NOT found、二配 ini.template.static 缺失），按 IO-T05/T06/T07 先例补拷处置——保持 open（构建约定所有者）。
  - **F-030**（DOC-T12 送验提交标题名词开头，F-012 家族）：本送验提交 b0861ec2 标题**同型复现**（见 4.9）——转登 **F-206**（本次新登，编号顺延自 F-205）。
  - **F-031**（EX-T09 acceptance AT-34 编号问题）：属 EX-T09 范围，与本任务无关。
  - **F-019/F-191/F-197/F-202** 等门禁词表/基线类：本任务 ird_gates 复跑 17 命中与 IO-T07 基线同集，未新增未消账——保持 open（门禁所有者）。
  - 其余 open 条目（testkit/io/evidence/project 各辖区）与本任务无交集，无随本任务可顺手消账项。

## 4.9 commit 质量 —— **不通过（阻断级）**

- 单提交 b0861ec2，正文四段（What/Why/验证/影响面）结构齐备；提交声明的构建/测试/门禁**数值结论**（双模式零错误、7/7＋5/5、ird_gates 17 命中零新增、validate-task/doc PASS）经本验收者亲手复跑**全部证实为真**——结果无造假。
- **建议级**：标题「[WP-08-T02] execution 单元构建落位：INTERFACE 占位升级 STATIC 真实库」以名词短语开头，非动词开头（ACC §4.9/AGENTS §6.3 字面要求）——F-012/F-023/F-024/F-030 家族同型再现，转登 F-206。
- **阻断级失实**：正文声称「证据留痕 traceability/builds/wp08-t01/（**14 文件，日志按 wp04-t01 先例入库**）：双模式配置/构建/测试日志、verify-target-build.log、4 份 gtest XML、ird_gates 日志、validate-task/validate-docs 日志、测试真实失败能力探针日志、ird-test-report.json」。`git show b0861ec --name-only` 实证该目录**仅 5 件**（4 份 gtest XML＋ird-test-report.json），**日志类 0 件入库**。ird-test-report.json 全篇亦以九个日志文件名（integration-build.log、smoke-configure.log、smoke-build.log、test-run.log、verify-target-build.log、gates-ird-gates-target.log、validate-task.log、validate-docs.log、test-fault-probe.log）作为在库证据路径引用。声称入库与 diff 实际不符＝留痕陈述失实——ACC §4.9「验证结论与留痕一致（发现粉饰即不通过）」命中（性质：验证真实发生但留痕在库性陈述不实，非结论造假）。

## 4.10 留痕完整 —— **不通过（阻断级）**

- gtest XML×4＋ird-test-report.json 已落位：XML 实测 `tests="7"/"5"、failures="0"`，与本验收者四跑（7/7＋5/5 × 两模式全 PASSED）一致。
- **构建日志未落位**：ACC §4.10 通过标准明列「gtest XML＋ird-test-report.json＋**构建日志**落位」——九件日志 0 件在库（见 4.9）。机制根因＝`.gitignore:87` 全局 `*.log` 规则（F-010 已登记）；IO-T07 先例为实施者 `git add -f` 强制入库（21 件全入库），本提交未执行强制添加。
- phase-one-validation.log 未更新：按 F-201 口径可以任务级留痕承载，但本任务级 report 所引日志本身缺失，该豁免口径救不了本例。

## 4.11 偷懒/缩水实现扫描 —— 通过

- ①**标记扫描**：diff 增量行 `TODO|FIXME|stub|placeholder|未实现|not implemented` 零命中（grep exit 1）。
- ②**空实现检测**：src/Execution.cpp 仅注释零声明零定义——契约 acceptance 3 与 note 显式声明「不在骨架预建占位实现」＋「src/（空起步）」产物形态，且为 CORE-T01/POL-T01/DIAG-T02/PRJ-T01 四单元既定先例（CMake STATIC 至少一翻译单元纪律）——**契约显式豁免**，依据逐条列出如上，非本项命中。
- ③**逐条证据表**：见 4.4——四条 acceptance 均有具名用例/命令输出/文件行号级证据；acceptance 1/2 evidence 字段引用的缺失日志文件，其实质证据（构建零错误、扫描零命中、门禁基线）已由本验收者独立复现补足——同一事实已在 4.9/4.10 判阻断，此处不重复双计。
- ④**测试强度**：12 用例均具名、断言校验行为与边界（指令行门控防注释误报、白名单逐项比对、守卫两面读取防漏）；4.5 两处注入探针证实非恒真、非"只编译通过"。

## 问题清单

| 级别 | 编号/位置 | 问题 | 处置 |
| --- | --- | --- | --- |
| **阻断** | 4.9/4.10（提交 b0861ec2 正文＋ird-test-report.json） | 九件验证日志（双模式配置/构建/测试日志×8＋verify-target-build.log＋ird_gates 日志＋validate-task/validate-docs 日志＋探针日志）声称「按 wp04-t01 先例入库」，实际 0 件入库（.gitignore `*.log` 吞没，未 `git add -f`）；提交信息声称 14 文件与实际 5 件不符 | **fail 依据**。返工（窄面）：技术实现与测试**零改动**；以**新提交**（AGENTS §6.3 禁止改写已推送历史，不 amend/rebase）`git add -f` 补齐九件日志入库；ird-test-report.json 如有路径性出入一并修订；提交信息正文含勘误说明（14→实际清单）。补交后按 ACC §5 重新发起**全量**验收（attempt 2） |
| 建议 | 4.9（提交标题） | 「execution 单元构建落位：…」名词短语开头，非动词开头——F-012/F-023/F-024/F-030 家族同型再现 | 已转登 findings.json **F-206**；与家族合并处置（提交模板补动词开头示例），对已推送历史不追溯改写 |
| 建议（既有，不新登） | F-007/F-200 | worktree 环境前置三件（vcpkg toolchain×Qt prefix×bin 模板两文件）——本次验收同型命中、按先例补拷处置 | 保持 open（构建约定所有者） |

## 裁决

**verdict = fail**。阻断级 1 项（4.9 留痕陈述失实＋4.10 构建日志未落位，同一事实两面）；建议级 1 项新发现（F-206）已按 ACC §5 转登 findings.json。除留痕完整性外，4.1~4.8/4.11 全部通过——技术实现、构建、测试、红线、注释、文档同步零缺陷，返工面仅为留痕补交。合入决策不适用（fail 不合入）；本记录分支 acc/EX-T01/1 推送后由编排者登记为返工用 lastFailureRecord。

## 验收者环境说明

- 复现现场：`git worktree add --detach C:/Users/zgl18/AppData/Local/Temp/acc-ext01-a b0861ec2671d74e004d65e8724256c966fe6a130`；集成树 `acc-ext01-a/build`（配置：VS17 2022 -A x64＋vcpkg toolchain＋`CMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`＋`RWS_BUILD_INDUSTRIALROBOT=ON`，缓存实测 `:BOOL=ON`）与冒烟树 `%TEMP%/acc-ext01-smoke`（独立缓存＋vcpkg toolchain 绝对路径）均为本次从零新建；记录回传现场 `C:/Users/zgl18/AppData/Local/Temp/acc-ext01-b`（分支 `acc/EX-T01/1`）基于 origin/redesign-main＝840688a9340b5dba6dff557359686abf2f88a63b。
- 全部证据（双模式配置/构建、四跑 7/7＋5/5、契约 verify 命令、ird_gates 17 命中逐项对账、两处失效注入与还原复绿、12 变更文件 allowedFiles 逐一比对、四份源码文件注释实读、在库 XML/report 数值交叉核对、送验提交全文实读）为验收者在本现场亲手执行；两处注入仅存在于临时 worktree 源码树且已还原复绿（`git status --porcelain` 零残留），任务分支与送验对象零触碰、零推送。验收完成后临时现场随流程删除，本记录与三元组为唯一持久证据。
