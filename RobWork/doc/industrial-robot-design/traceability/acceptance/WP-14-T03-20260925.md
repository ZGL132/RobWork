# WP-14-T03 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | WP-14-T03（实现任务点/区域/工况领域对象与 canonical 编解码，含采样计划定义） |
| 送验对象（冻结） | `30f80a0bcb5c136a7f8d21cd5fb56e53a8828e52`（origin/wp14-t03 尖端，ls-remote 核实一致——无分支漂移） |
| diff 范围 | `6394918b87eb1acfa9939cd7a809c5a7cb12aa95..30f80a0bcb5c136a7f8d21cd5fb56e53a8828e52`（base..head，共 2 提交） |
| 契约 | `RobWork/doc/industrial-robot-design/tasks/foundation/WP-14-T03.json` |
| 验收日期 | 2026-09-25 |
| 验收环境 | 独立会话（全新上下文，只消费产物与文档，未消费实施叙述）；全部构建/测试/门禁/变异在临时 worktree 与独立构建树中由验收者亲手执行 |
| **verdict** | **pass（无阻断级问题；建议级 3 条，已转登 traceability/findings.json F-363~F-365）** |

## 0. 验收者环境与复现现场说明

- 复现现场：`git worktree add --detach D:/10_Source_Repos/21_robot/_acc_t03_a 30f80a0b…`（detached @ SHA，非分支名）；base 侧临时 worktree `_acc_t03_base` @ `6394918b…`（门禁比对用毕已删）。
- worktree 缺 gitignored 辅助件（F-200/F-007 家族在案现象）：`RobWork/RobWorkStudio/bin/RobWorkStudio.ini.template.static` 与 `RobWorkStudio.ini.shared.in` 不随 git 检出——验收者从主检出复制后 configure 通过。属环境前置，非送验对象缺陷。
- 集成模式全新配置参数（F-007 家族在案口径）：`-DCMAKE_TOOLCHAIN_FILE=<主仓库>/vcpkg/scripts/buildsystems/vcpkg.cmake`＋`-DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`（与主构建树缓存一致；worktree 内无 vcpkg 目录——gitignored）。首次裸配置（无 toolchain）在 FindBoost 失败——与主树缓存口径核对后补参数，属环境前置。
- 构建前 tasklist 核查：无 RobWorkStudio.exe 残留进程锁 DLL（编排提示第 1 条——本次未发生，无需 taskkill）。
- 验收记录按 ACC §3 落于 worktree B 分支 `acc/WP-14-T03/1`（基于 origin/redesign-main @ `6394918b…`），commit 后以显式 refspec 推送。

## 1. 逐项证据（ACC §4.1~4.11）

### 4.1 改动范围 —— 通过

- `git diff --name-status base..head` 共 38 文件，逐一套 allowedFiles 模式比对：
  - 19 × `RobWork/RobWorkStudio/src/rwslibs/industrialrobot/requirements/**`（CMakeLists 修改＋5 公共头新增＋5 实现 TU（含 Errors.cpp 增列）＋5 测试文件新增＋ErrorsTest 修改）
  - 18 × `RobWork/doc/industrial-robot-design/traceability/builds/wp14-t03/**`（留痕证据）
  - 1 × `RobWork/doc/industrial-robot-design/units/requirements.md`（§12 交接行＋§14.6 v0.3 登记）
- **越界文件＝0**；forbiddenFiles（REQUIREMENTS.md／ARCHITECTURE.md）零触碰。
- SA-02：diff 无 `patches/` 文件、无框架源码改动。

### 4.2 可复现构建 —— 通过（双模式，验收者亲手重配重跑）

- 集成模式（全新构建树 `_acc_t03_bld`，源＝worktreeA）：
  - `cmake -S <A>/RobWork -B _acc_t03_bld -G "Visual Studio 17 2022" -A x64 -DCMAKE_TOOLCHAIN_FILE=…vcpkg.cmake -DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64 -DRWS_BUILD_INDUSTRIALROBOT=ON` → **Configuring done / Generating done**；`grep RWS_BUILD_INDUSTRIALROBOT CMakeCache.txt` → `:BOOL=ON` 确认。
  - `cmake --build . --config Release --target sdurws_ird_requirements` → **exit 0**，产出 STATIC 库；requirements 源 **0 warning**。
  - 两测试目标构建 exit 0（`sdurws_ird_requirements_test.exe`／`…_contract_test.exe` 产出）。
- 独立冒烟模式（全新构建树 `_acc_t03_smoke`，源＝worktreeA 的 industrialrobot 子目录）：
  - configure 带 vcpkg toolchain＋Qt prefix → exit 0；全量 `cmake --build . --config Release` → **error 计数 0**；`requirements/Release/` 下 lib＋两测试 exe 齐备。
  - 唯一 warning（C4834）位于 `ui/gui_test/CommandPaletteGuiTest.cpp`——WP-10 存量、不在本 diff。

### 4.3 契约 verify 命令 —— 通过（3/3 亲手执行）

| # | 命令（按本验收构建树等价换算） | 结果 |
| --- | --- | --- |
| 1 | `cmake --build build --config Release --target sdurws_ird_requirements` | **exit 0**（零错误） |
| 2 | `ctest --test-dir …/requirements -C Release -L "^ird$"` | **2/2 passed**（`sdurws_ird_requirements_test` 0.25 s＋`…_contract_test` 0.19 s，`100% tests passed`） |
| 3 | `pwsh -File RobWork/scripts/industrialrobot/validate-docs.ps1` | **PASS（20 units, 12 trace entries, 189 task files）** |

- verify 数组无测试目标构建行/ctest 前的显式构建行（F-300 系家族在案现象）——验收者侧补建：两测试目标构建＋两 exe 直跑 **52/52 与 5/5 全绿**（gtest XML 留档于验收者证据目录）；另跑 `validate-task.ps1`（PASS, 1 tasks）与 `validate-state.ps1`（PASS, phase=idle, queue=19, history=161）。

### 4.4 acceptance 条目逐项证据 —— 通过（6/6）

| # | acceptance 条目 | 证据（验收者实测） |
| --- | --- | --- |
| 1 | I-REQ-1~10 逐条具名用例 | `ReqInvariants.IREQ1~IREQ10_*_WP14T03_ACC1` 十个具名测试全绿（52/52 内）。抽查断言强度：IREQ1 正例（isCanonicalOrder 真）＋反例（逆序假）；IREQ3 同名 DuplicateName 拒绝＋空名同码（无静默加后缀——NFR-COR-03）；IREQ8 类型面无路径字段；IREQ9 行为面委托 `ResolveRequiredCasesFrozenSchema`（四象限＋确定性投影，实测强断言）；IREQ10 类型面（generation 无源引用字段——行为面属 T07）。**4.5 变异 M1/M2′ 均真变红，证明非恒真** |
| 2 | canonical 编码登记 | `ReqCodec` 四测试全绿：RoundtripAllFiveObjectsFieldWise（五对象逐字段）、DeterministicBytesAndOrderInsensitive、UnsupportedMajorVersionRejectedWithGuidance（encode/decode 双侧拒绝＋detail 携"升级"指引＋params 三键对齐）、ProfileNeverEncodedVariantClosed（`static_assert(variant_size==5)` 封闭面＋派生档重算确定性）。encode 侧规范化**副本**排序写出（Codec.cpp:1597 `auto entries = o.entries; sortEntriesByObjectId(entries);`——不改写调用方输入）；decode 四步校验链（版本门→结构→规范序→不变量复核——Codec.cpp ①③④注释与 malformedInvariant 落位）。卡 §12 evidence 交接行已同步（req.* 四键点名＋canonical 编码/planContentIdentity 输入"已落位——WP-14-T03"标记——unit 卡 diff 实测） |
| 3 | 采样计划确定性 | `ReqSampling` 五测试全绿：GridBySpacingNormalizationDeterministic（D-REQ-2：`counts[i]=floor(size[i]/spacing[i])+1`——Services.cpp:309 实测）、PlanDigestIdentityForSynonymousPlans（同义计划同摘要）、ZeroSamplePlanLegalStorage（V-02 零样本合法）、BuilderInterfaceHasNoSampleExit（`static_assert` 钉死接口仅 buildPlan/digest 两出口——§5.2 边界）、BuildPlanErrorSemantics。**变异 M1（+1u→+2u）致前两者＋BuilderInterface 共 3 用例真变红** |
| 4 | 领域服务构造校验 | `ReqServices.CreatePoint/Region/ConditionErrorSemantics`（DuplicateName/IllegalTolerance/ZeroVectorTarget/AllDofFree/DegenerateRegion 逐项）＋ResolveRequiredCasesFrozenSchema（mandatory≡(level==Must) 逐条投影＋规范序确定性——§6.2 冻结 schema 唯一实现点）＋OrientationRuleIllegalParamsRejected（五规则非法参数：零向量/rollRange 逆序/缺 feature/非有限角）＋IREQ7 拓扑三测试（环/重复/悬空） |
| 5 | O-36 处置 | `ReqServices.O36_SubEntryObjectIdModelInternalAnchor`（临时句柄唯一＋五真实存储对象 token 恰为登记簿五值——objectRefs 复核范围口径）＋`ReqEditor.O36_AnchorStableAndSchemaRejection`（Update 同 id 不变——subjectObjectId 锚跨编辑稳定＋future-major 拒绝）。子 id 内嵌集合对象字节由 codec roundtrip（entries 序列化含 id）承载 |
| 6 | 双模式构建＋ctest＋留痕＋门禁 | 本表 4.2/4.3 实测；gtest XML（tests=52 failures=0／tests=5 failures=0）＋ird-test-report.json（52/52、5/5）＋构建/ctest/门禁日志 18 件随 `30f80a0b` 提交；ird_gates 零新增（见下条与 4.2） |

**ird_gates base..head 归一化比对（独立复算）**：验收者在 base/head 两侧临时 worktree 以 `cmake -DIRD_ROOT=… -P ird_gates.cmake` 直跑，各得 127 行命中；按绝对路径前缀归一化（剥离两侧临时目录名）后 `diff` 为空——**命中集逐行完全一致，零新增命中**。分码直方图两侧一致且与送验附件 `ird-gates-base-head-compare.md` 完全吻合：LIB 10、R1 3、R3 41、R4 3、R5 1、SUB 63、T1 5、T2 1＝127（DTB §4.5 存量例外未清零既有状态；requirements 产品面零命中，`…_contract_test → testkit` 直链为 base 侧已有存量）。

### 4.5 测试真实失败能力 —— 通过（变异测试 2 处实质变异，均真变红后恢复）

| 变异 | 位置与内容 | 结果（具名测试真实变红） |
| --- | --- | --- |
| M1 | `Services.cpp` normalizeSampling：`+ 1u` → `+ 2u`（D-REQ-2 规范化规则篡改） | 重编后 `ReqSampling.GridBySpacingNormalizationDeterministic`／`PlanDigestIdentityForSynonymousPlans`／`BuilderInterfaceHasNoSampleExit` **3 用例 FAILED** |
| M2′ | `Codec.cpp` checkCollection:1509：`if (prev > cur)` → `if (prev < cur)`（canonical 字典序校验方向翻转） | 重编后 `ReqCodec.DeterministicBytesAndOrderInsensitive`／`UnsupportedMajorVersionRejectedWithGuidance`／`ReqEditor.RemoveReferenceProtection` **3 用例 FAILED** |
| （附带）M2 | `Codec.cpp` kMagic `'O'`→`'X'`（魔数篡改） | **52 用例全绿——变异存活**（encode/decode 自洽所致）：转登建议级 **F-363**（魔数字面量无测试钉住） |

- 全程 `git checkout --` 恢复（M1/M2/M2′ 三处均还原，`git status` 干净）；恢复后重编并全量重跑：**52/52 PASSED**（T14 教训口径——先还原再跑全量）。

### 4.6 架构红线 —— 通过（AGENTS §5 八条逐条）

1. L2 零 Qt：产品面 `grep "#include <Q…"` 0 命中；门禁 R3 命中两侧一致（均为 ui 单元存量）＋运行期测试 `ProductFaceHasZeroQtIncludes` 在守。
2. 业务域互链（R-1）：产品面跨单元 include 仅 `core`（DiagData/Digest/Identity/Provenance）与 `diagnostics`（DiagCodes）——均在 §3.2 登记边内；无 modeling/kinematics 等业务域 include。
3. 跨单元私有头（R-2）：全部 include 为 `include/sdurws/ird/<unit>/` 公共面，无私有头。
4. SA-02：4.1 已核——无框架改动、patches/ 零新文件。
5. R-4 名称前缀：门禁 R4 命中两侧一致（仅 project 单元存量 1 文件）＋测试 `ProductFaceHasNoRobWorkStringLiterals` 在守。
6. PA-1 权威唯一：诊断码不私设（diags 参数恒不写入——`assertDiagsUntouched`＋`ServicesTest` `EXPECT_TRUE(diags.empty())` 断言在守；无新 REQ-* 码预建）；SHA-256 复用 core::ContentDigester（不实现第二套摘要）；必验解析单点 resolveRequiredCases。
7. PA-2 不可变历史：本任务不写修订（编辑器局部栈为内存快照，不动项目修订）；无覆盖旧数据路径。
8. 不继承 old/：实现为五对象值模型＋canonical 编解码形态，与旧 RequirementCompiler/Freezer 结构无对应；无历史源码拷贝痕迹。

### 4.7 注释规范 —— 通过

- 公共头全查（5/5）：`RequirementTypes/Codec/Editor/Services/Sampling.hpp` 均有中文文件头 Doxygen（@file/@brief＋设计依据带需求/卡节/契约 acceptance 追溯＋背景说明）；类与函数中文 Doxygen（RequirementTypes.hpp @brief 73 处、Services.hpp 19 处、Codec.hpp 9 处、Sampling.hpp 7 处、Editor.hpp 14 处）。
- 实现文件抽查 ≥3（实查 5）：Codec.cpp（/// 24＋// 111——错误族/魔数/校验链逐步讲解含失败语义）、Services.cpp（// 73——每校验段业务含义＋确定性来源）、Editor.cpp（// 43——撤销栈快照语义）、Sampling.cpp（文件头含线程安全/确定性声明）、RequirementTypes.cpp（// 86）。
- §2.5 高危信息：单位（m/rad/kg 等）与 SI 声明、错误语义归类（调用方错误值面返回 vs 诊断码纪律）、确定性来源（IEEE754 floor/SHA-256）、所有权（临时句柄 vs project 正式分配——O-36）、线程约束（编辑器仅 UI 线程）均有标注。

### 4.8 文档同步 —— 通过（含 findings 未关闭条目相关性核对）

- 单元卡 `units/requirements.md` 增量修订随 diff 落位：§14.6 **v0.3** 登记（五头＋实现落位、增量决策①MalformedPayload 表尾增列②ErrorsTest 封闭断言 8→9 随附同步③RequirementExpected 自持决策（不复用 runtime::Expected——§3.2 runtime 边"无编译依赖"依据，Codec.hpp 头注同源）④RequirementObjectClosureView 注入抽象⑤rw::math 头注入机制）＋§12 evidence 交接行同步。与实现逐项对照**零偏差**（九值错误码表/ErrorsTest 全表封闭断言/CMakeLists 冒烟分支均与登记一致）。
- `validate-docs`/`validate-task`/`validate-state` 三脚本 PASS（4.3 表）。
- **findings.json 未关闭条目相关性**（60 条 open，最高 F-362）：逐条标题扫描，无 P-EV-9/RequiredCaseSet/SamplingPlan/canonical/必验 相关未关闭条目；与本任务相关的仅 **F-362**（「core=直连/io=传染」两义性应在卡面明示）——本次提交以 CMakeLists.txt 注释（"集成模式下 sdurw_math 经 sdurws_ird_core 的 PUBLIC 边传递可达"）与 v0.3⑤ 部分覆盖，卡 §3.2 边表本身仍未明示 → 残余转登 **F-365**；其余 open 条目（F-004~F-075 环境前置/文档损毁/早期任务风格族）与本任务无关、不构成消账对象。

### 4.9 commit 质量 —— 通过（附建议级 1 条）

- 两提交（`3b6344b7` 实现＋`30f80a0b` 留痕）正文四段（What/Why/验证/影响面）齐备、逐条带需求/acceptance 追溯；验证段全部计数（52/52、5/5、2/2、127/127 零新增、"零错误零警告"〔integration-build.log 0 warning 实测〕、冒烟仅零错误宣称）经验收者独立复算**逐项一致，无粉饰**。
- 建议级：两提交标题均无 `[WP-14-T03]` 前缀（ACC §4.9/AGENTS §6.3 标题格式 `[任务编号] 动词开头`）——F-361 家族再现，转登 **F-364**（纯风格瑕疵，不阻塞）。

### 4.10 留痕完整 —— 通过

- `traceability/builds/wp14-t03/` 18 件随 `30f80a0b` 提交：集成构建日志、冒烟 configure/build 日志、两测试运行日志、**gtest XML 原样留存**（XML 头 `tests="52" failures="0"`／`tests="5" failures="0"`——与 ird-test-report.json summary 52/52、5/5 及运行日志三方一致，无手工转抄痕迹）、ctest 日志、ird_gates base/head 直跑日志＋norm 直方图＋比对附件。
- 状态翻转（契约置 done/history 追加/队列移出）按 PIPE §4.7 属合入收尾（治理侧）动作，head 处 state.json 仍为 queue 态——与 T02 先例（`6394918b` 收尾提交）一致，非缺陷。

### 4.11 偷懒/缩水实现扫描 —— 通过

- ① TODO/FIXME/stub/placeholder/未实现/not implemented：diff 新增行 grep **0 命中**。
- ② 空实现检测：无公共接口空函数体/恒默认返回。`precheckDerivation` 为最小真实实现（源 id 有效性核对，输入→确定性输出）且**如实登记非桩**（Services.cpp:201 注释明示"T03 落最小真实实现…这不是桩——T07 的扩展属其任务卡既定落位面"）；`undoLocal/redoLocal` 等均为实质实现（实测抽查）。
- ③ 逐条证据表：见 4.4——6 条 acceptance 全部有具名测试/命令输出/文件行号证据，无"已实现"空声明。
- ④ 测试强度：断言校验行为与边界（四象限投影/非法参数逐项/规范化确定性/签名面 static_assert 钉死）；**4.5 变异实证会红**；diags 恒不写入有测试断言（ServicesTest.cpp:69）。

## 2. 问题清单（分级）

**阻断级：无。**

**建议级（3 条，已逐条转登 traceability/findings.json，编号自 F-362 后顺延）：**

| 编号 | 内容 | 处置建议 |
| --- | --- | --- |
| F-363 | canonical 家族魔数 "IRDREQO" 字面量无测试钉住：变异 M2（kMagic `'O'`→`'X'`）后 52 用例全绿（encode/decode 自洽，同变异对存量字节跨版本对账才暴露） | CodecTest 增加编码产物首 7 字节与 "IRDREQO" 字面比对的钉子测试；责任方 requirements 实施侧，随后续任务顺手落 |
| F-364 | F-361 家族再现（一次一例）：本任务两送验提交（`3b6344b7`、`30f80a0b`）标题均缺 `[WP-14-T03]` 前缀（ACC §4.9／AGENTS §6.3）；正文四段齐备、验证结论与实测一致，纯风格瑕疵 | 后续提交标题遵循 `[任务编号] 动词开头`；不追溯改写已推送历史 |
| F-365 | F-362 残余：「集成模式 sdurw_math 经 sdurws_ird_core PUBLIC 边传染到达」口径本次仅登记于 requirements/CMakeLists.txt 注释与卡 §14.6 v0.3⑤（rw 头依赖/零新增链接边），卡 §3.2 边表仍无明示 | 治理/文档所有者在卡 §3.2 补一句明示（core=直连/io=传染区分），随之可关闭 F-362 与本条 |

## 3. 裁决

**verdict＝pass。** 4.1~4.11 全部通过、无阻断级问题；建议级 3 条不阻塞合入。送验对象冻结于 `30f80a0bcb5c136a7f8d21cd5fb56e53a8828e52`；验收全程未向任务分支推送任何提交、未修改实施产物。合入决策归所有者（ACC §6）。

## 4. 验收者证据目录（本地临时，不入库）

- `_acc_t03_ev/acc_gates_base.log`／`acc_gates_head.log`（引擎直跑原始输出，127/127 行）
- `_acc_t03_ev/acc_base.norm`／`acc_head.norm`（归一化命中集，diff 为空）
- `_acc_t03_ev/acc_sdurws_ird_requirements{,_contract}_test.xml`（52/52、5/5）
- 构建树 `_acc_t03_bld`（集成）／`_acc_t03_smoke`（冒烟）；变异测试在冒烟树执行并恢复
