# DIAG-T11 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 | DIAG-T11 文档与门禁同步（≙WP-09-T11，diagnostics 单元，纯文档任务零代码变更） |
| 契约 | tasks/foundation/DIAG-T11.json（branch wp09-t11，verify＝validate-docs.ps1） |
| 送验对象（冻结） | 8d345a43cc90beb7be3434d5d9545a5713868402（提交范围 d22b1295..8d345a43，共 2 提交：866ce899＋8d345a43） |
| 验收方式 | 全新上下文；detached worktree @SHA 复现现场；构建/测试/门禁/脚本全部验收者亲手重跑（ACC §2 复现优先于采信） |
| **verdict** | **PASS**（4.1~4.11 全过、无阻断级问题；建议级 3 项，已按 ACC §5 转登 findings.json：F-117 消账＋F-118/F-119 新登） |

## 一、逐项证据（ACC §4，4.1~4.11）

### 4.1 改动范围 —— PASS

- `git diff --stat d22b1295..8d345a43`：8 文件（+556/−13）——`units/diagnostics.md`、`diagnostics/include/sdurws/ird/diagnostics/README.md`、`traceability/builds/wp09-t11/` 6 件（README-audit.md＋gtest XML×4＋ird-test-report.json）。
- allowedFiles 逐件核对：units 卡 ✓；README.md 实际路径为 `industrialrobot/diagnostics/include/sdurws/ird/diagnostics/README.md`（`git ls-tree`/`find` 实证），在 `industrialrobot/diagnostics/**` 通配内 ✓（diff --stat 显示的省略前缀曾疑似 `industrialrobot/include/...`，实测排除）；traceability 6 件 ✓。
- forbiddenFiles（REQUIREMENTS.md/ARCHITECTURE.md）零触碰——diff 文件清单实证 ✓。
- `git diff d22b1295..8d345a43 -- .../patches/ --stat` 空输出——无 patch 登记，无框架源码改动（SA-02）✓。

### 4.2 可复现构建 —— PASS

- **集成模式**（worktree A `acc-wt-a-diag-t11`，detached @ 8d345a43）：从零配置 `CONFIG_EXIT=0`，缓存 grep 确认 `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON`；`cmake --build build --config Release --target sdurws_ird_diagnostics_test sdurws_ird_diagnostics_contract_test`（DIAG-T10 验收同口径：目标链覆盖本单元全部改动面）`BUILD_EXIT=0`，`error C|fatal error|error LNK` grep 计数 **0**，两测试 exe 落位 `build/RobWorkStudio/bin/Release/`。
  - 首次配置失败（`RobWorkStudio.ini.template.static does not exist`）＝worktree 缺主树被 .gitignore `[Bb]in/` 忽略的本地辅助件（F-007/F-062/F-099 在册缺陷）；自主树补拷 `RobWorkStudio.ini.shared.in`＋`RobWorkStudio.ini.template.static` 后重配通过——环境修复，不触及送验产物（DIAG-T05~T10 验收同先例）。
- **独立冒烟模式**（`acc-wt-diag-t11-smoke`，带 vcpkg toolchain——F-002 口径）：配置＋构建 exit 0，error grep 计数 0。
- 日志（验收者磁盘）：acc-diag-t11-integrated-config.log（失败首次）/‑config2.log /‑build2.log /‑smoke-config.log /‑smoke-build.log /‑probe-build.log /‑restore-build.log /‑gates.log。

### 4.3 契约 verify 命令 —— PASS

- `pwsh -File RobWork/scripts/industrialrobot/validate-docs.ps1`（worktree A 内亲手执行）：**PASS (20 units, 12 trace entries, 150 task files)**，exit 0。
- 增强（ACC §3 脚本清单）：validate-task.ps1 -TaskFile 契约 → **PASS (1 tasks)**；verify-task.ps1 → 内部复跑 validate-docs PASS＋**verify-task: PASS DIAG-T11**。

### 4.4 acceptance 条目逐项 —— PASS（三条全过，证据亲证）

1. **本文与实现零偏差登记**：§14.4 v0.12 行（diagnostics.md L1314）在位。验收者独立实测：include/ 10 文件（9 契约头＋README.md，`ls` 实录）↔ §3.1 模块表 10 头行逐行对应；src/ 11 .cpp＋2 私有头（EntryDetail.hpp/ParamSchema.hpp）＝登记累加 1+2+3+1+1+1+2 ✓；test/ 14 测试 .cpp＋仅头替身 ScriptedDiagSource.hpp＋README.md＝累加 2+1+1+2+1+1+1+2+3 ✓。双模式实跑 115/115＋26/26（四 exe 亲跑）与 §14.4 v0.11 基数逐套件一致 ✓；ird_gates 8 命中亲跑与 wp09-t10 基线逐码一致零新增 ✓。卡内措辞 slips（§1.2"hpp/.cpp 对"→仅头替身）按 DTB §5.4 更正＋v0.12④ 登记，不静默 ✓。
2. **README 指向核对零偏差**：README.md L5 任务拆分指向 `units/diagnostics.md §11`——维持无回退无第二指向 ✓；L13~22 实现状态段同步 DIAG-T10 落位现状（九契约头逐头锚点＋§14.4 版本链＋builds/wp09-t02~t11＋../test/README.md 指引）✓；L26 过期前瞻句"本文不表示库已实现或测试已通过"已消除，改为"以 §14.4 与留痕为准"的如实指向 ✓。审计证据 README-audit.md §1/§5 与验收者实测一致。
3. **§14.3 状态更新＋P-PR-6/P-EX-8 消账确认**：P-DIAG-1~9 九条均附〔DIAG-T11 状态同步 2026-09-15〕状态段（L1287~1295，问题/依据/影响/建议/裁决者五栏本体未动）✓。验收者独立实测对端：project.md 与 execution.md 均实测 **v0.2**、P-PR-6/P-EX-8 均仍登记 open（project L457"码值部分消账；sink 名称/归属统一仍按 P-PR-6 裁决"、execution L101/L268"不因卡存在自动消账"）——与 P-DIAG-5 消账确认段登记吻合 ✓；对端契约与上游文档 diff 零触碰（不私改对端）✓；P-DIAG-8 维持 open 不消账 ✓；九项裁决归属均未私裁 ✓。

### 4.5 测试真实失败能力 —— PASS（两断言探针真实变红）

- worktree A 注入①：`StableCodeRegistryTest.cpp:207` `EXPECT_EQ(first.entries.size(), 87u)`→`86u` → `sdurws_ird_diagnostics_test` 变红：**[FAILED] DiagCodesRegistry.DtReg1_BuiltinTableRegistersFullyAndManifestStable**（114 passed＋1 failed）。
- 注入②：`DiagContractCheckTest.cpp:153` 正例断言 `EXPECT_TRUE(result.passed)`→`EXPECT_FALSE` → `sdurws_ird_diagnostics_contract_test` 变红：**[FAILED] DiagContractCheck.FactoryUserRecordPassesCheckDiagnosticRecord**。
- 逐字还原＋touch 强制重编（F-068 规避）→ 复绿 115/115＋26/26；`git status` diagnostics 目录零残留（行尾归一后 WORKTREE_CLEAN_CONFIRM）。测试非恒真，失败能力证实。

### 4.6 架构红线（AGENTS §5 八条） —— PASS

- 机械证据：diff 零代码文件（`.hpp|.cpp|.cmake|.txt|CMakeLists` grep 零命中）；零 Qt/互链/私有头/名称拼接各条对本 diff 无作用面（纯文档）。
- 语义证据：REQUIREMENTS/ARCHITECTURE 零触碰（红线 4/8 面）；不私改对端契约（project/execution 零触碰，消账走"确认提交"登记路径——P-EX-8 处置纪律）✓；§14.3 裁决者栏未动（不私裁、权威归属 PA-1）✓；§14.4 只增不改（PA-2 不可变历史）——diff 实证 v0.11 行原文未改、仅追加 v0.12 ✓。

### 4.7 注释规范 —— PASS（无新增/大改源码文件，规范无作用面）

- diff 内唯一 src 树文件为 README.md（不参与编译的说明文档）；无 `.hpp/.cpp` 新增或修改——AGENTS §2 对产品代码的注释要求无作用对象。README 新增段落为中文完整句、含指向与状态语义，无占位文本。

### 4.8 文档同步＋findings.json 核对 —— PASS

- 实现与任务卡零偏差（4.4 三条证据）；偏差登记路径 DTB §5.4 使用合规（卡头 slips 一处已登记）。
- **findings.json 未关闭条目相关性核对**（98 open，最新 F-117）：
  - **F-117**（卡头 ScriptedDiagSource"hpp/.cpp 对"笔误）：**已随本任务顺手修复**（README-audit.md §4.1 发现＋§1.2 卡头更正＋§14.4 v0.12④ 登记）——本次随本记录消账 status=fixed（验收者在 evidence 分支登记，附本任务证据）。
  - **F-114/F-113/F-111 悬空引用家族**：本任务 ird-test-report.json 的 gates/selfCheck 字段仍引用 7 件未入库 .log（gates-ird-gates-target.log/validate-task.log/verify-task.log/validate-docs.log/integration-build.log/smoke-build.log/smoke-configure.log——`git ls-tree 8d345a43` 实证 builds/wp09-t11/ 仅 5 件）→ 新登 **F-118**（较 F-114 轻：提交正文本次如实声明"留磁盘不提交"口径，仅报告字段引用幻影）。
  - **F-112/F-116 提交标题名词开头家族**：本任务两提交标题分别以"README""单元卡"开头，均非动词开头 → 新登 **F-119**（沿先例建议级不阻断）。
  - **F-115**（ird_gates 白名单登记悬置）：本任务门禁基线 8 命中含该边（亲跑实证），但收口辖域归 WP-01-T03、本任务零新增面——注明相关性，不消账。
  - 其余 open 条目（F-110 Logging 注释拼写等）与本任务（零代码变更）无相关性。

### 4.9 commit 质量 —— PASS（附 1 项建议级转登 F-119）

- 两提交均 `[WP-09-T11]` 前缀＋四段正文（What/Why/验证/影响面）齐备＋`Acc-Covered` 逐条标注（866ce899→acceptance 2；8d345a43→acceptance 1/3）。
- 验证结论与留痕一致性：正文声称的双模式 115/115＋26/26、ird_gates 8 命中＝基线逐码一致、validate-task/verify-task PASS——验收者全部亲跑吻合，**无粉饰**；"日志留磁盘不提交（同 wp09-t10 口径）"为如实声明（较 F-114 正文幻影改进）。
- 标题"README 指向核对同步…""单元卡 v0.12 零偏差登记…"非动词开头 → 转登 F-119。

### 4.10 留痕完整 —— PASS

- 提交内落位：gtest XML×4（integration/smoke × test/contract_test，顶格实测 `tests="115" failures="0"`＋`tests="26"`）＋ird-test-report.json（regressionReRun 两模式 115/115＋26/26，与本验证亲跑一致）。
- 构建日志沿 wp09-t10 既有口径留磁盘不提交（commit 正文如实登记）；resultNote 载体＝builds/wp09-t11/ird-test-report.json（phase-one-validation.log 自 da407b61/WP-03-T01 后停更为既有口径）。

### 4.11 偷懒/缩水扫描 —— PASS

- ①diff 范围 `TODO|FIXME|stub|placeholder|未实现|not implemented` 扫描：TODO/FIXME/placeholder/未实现/not implemented **零命中**；`stub` 命中均为正式登记的测试术语（套件名 ConfirmableProjectStubTest/DiagHostTraceStub、文件名 StubTest.cpp、"替身"设计词汇——P-DIAG-8 桩联动处置与 §14.4 v0.11 的正式登记词），非未实现占位，无契约豁免需求。
- ②空实现：零代码变更（4.6 机械证据），无空实现面。
- ③acceptance 逐条证据表：见 4.4——三条均有具名文件行号/亲跑命令输出，无一条仅凭"已实现"声明。
- ④测试强度：与 4.5 联动，两探针证明断言校验行为与边界、非恒真。

## 二、发现的问题清单

| # | 级别 | 内容 | 处置 |
| --- | --- | --- | --- |
| 1 | 建议级 | F-117 消账：卡头笔误已随本任务修复（详见 4.8） | 本次登记 status=fixed |
| 2 | 建议级 | F-118 新登：悬空引用家族第 14 例——report 字段引用 7 件未入库 .log（commit 正文已如实声明留磁盘口径，较 F-114 轻） | 转登 findings.json |
| 3 | 建议级 | F-119 新登：提交标题名词开头家族再现（两提交） | 转登 findings.json |

无阻断级问题。

## 三、验收者环境说明

- 复现 worktree A：`D:/10_Source_Repos/21_robot/acc-wt-a-diag-t11`（`git worktree add --detach @ 8d345a43cc90beb7be3434d5d9545a5713868402`）；集成树从零配置（`RWS_BUILD_INDUSTRIALROBOT=ON`〔缓存 grep 确认〕、`RWS_USE_STATIC_LINK_PLUGINS=ON`、vcpkg toolchain `D:/10_Source_Repos/21_robot/RobWork/vcpkg/scripts/buildsystems/vcpkg.cmake`、Qt `CMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`、生成器 Visual Studio 17 2022 -A x64；`RobWorkStudio.ini.shared.in`＋`RobWorkStudio.ini.template.static` 自主树补拷——F-007/F-062/F-099 环境修复）。冒烟树：`D:/10_Source_Repos/21_robot/acc-wt-diag-t11-smoke`（独立目录＋vcpkg toolchain）。
- 工具链：VS 2022 MSVC、vcpkg 经典模式（gtest 1.18.0 @ installed/x64-windows）、pwsh 7（validate-docs/validate-task/verify-task.ps1）。
- 探针注入/还原均在 worktree A 内：两处断言注入一次重建重跑取证，逐字还原＋touch 重编复绿（规避 F-068），diagnostics 目录 git status 零修改（行尾归一后确认）。
- 分支漂移检查：验收首（fetch 后 `git rev-parse`）与末（`git ls-remote`）两次核对——`origin/wp09-t11`＝8d345a43（**无漂移**）；origin 无 `acc/DIAG-T11/1` 分支（无 evidence 分支重名）。
- 本记录与 findings.json（F-117 消账＋F-118/F-119 新登）落 evidence 分支 `acc/DIAG-T11/1`（基于 origin/redesign-main d22b1295）提交并推送。
