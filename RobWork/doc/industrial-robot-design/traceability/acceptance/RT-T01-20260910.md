# RT-T01 验收记录（对抗式验收）

| 字段 | 值 |
| --- | --- |
| 任务 ID | RT-T01（≙WP-06-T01，runtime 构建落位） |
| 契约 | tasks/foundation/RT-T01.json（status: ready，随收尾翻转 done） |
| 验收对象 | 分支 `wp06-t01`，head＝`fea293d2801f2ac6d4f39c728aecaa7aef22cf93`（与 origin 一致——漂移检测 PASS），范围 `219fa29..fea293d` |
| 尝试序号 | attempt=1（evidence 分支 `acc/RT-T01/1`） |
| **verdict** | **pass**（4.1~4.11 全过；无阻断、无新增建议级发现） |
| 验收日期 | 2026-09-10 |
| 独立性声明 | PIPE v1.7 §0.4 单会话降级；补偿＝worktree A（detached @ head）亲手复现＋证据链前置自查（6 份日志在冻结对象——F-010 教训内化） |

---

## 逐项证据（4.1~4.11）

| # | 检查 | 结论与证据 |
| --- | --- | --- |
| 4.1 | 改动范围 | diff＝10 文件：runtime/{CMakeLists,src/Runtime.cpp,test/BuildRedLineTest.cpp}＋industrialrobot/CMakeLists＋traceability 6——allowedFiles 内；forbiddenFiles 零触碰（REQUIREMENTS/ARCHITECTURE 未动；DTB §4.6 回登不在 allowedFiles——消账输入以 report notes＋本记录归档，归档后由所有者侧治理批回登） |
| 4.2 | 可复现构建 | worktree A 全新配置 exit 0（ini 模板前置补齐）＋verify 目标构建 exit 0＋测试 4/4 PASSED（验收者亲手）；集成全量 exit 0 |
| 4.3 | verify 命令 | `cmake --build build --config Release --target sdurws_ird_runtime_test` exit 0 |
| 4.4 | acceptance 逐条 | ①两模式构建零错误（4/4）；零 Qt/零单元边扫描（NoQt/NoCrossUnit 用例）；基线库链接清单留痕：集成模式 IRD_RUNTIME_BASELINE_LIBS="sdurw_kinematics, sdurw_models, sdurwsim"（vcxproj 实证）＋运行期断言钉住——与 DTB §4.6 runtime 行一致（sdurw_math 经 core 传递），P-RT-3 消账输入成立；②gtest 按 §5.5 接入（find_package REQUIRED＋GTest::gtest_main＋ird_add_gtest）——CR-07 机制 ✓ |
| 4.5 | 失败能力 | 清单漏登探针（IRL_BASELINE_LIBS 移除 sdurwsim＋重配重编）→BaselineLibsPinned 如期变红，恢复后 4/4 复绿（报告记录；探针零残留） |
| 4.6 | 红线 | 零 Qt（扫描＋配置期守卫双层）；唯一单元边＝core（配置期 fatal 守卫＋扫描）；R-2 头根纪律；SA-02 零框架改动 |
| 4.7 | 注释 | CMake 关键决策块全注释（含三处坑位留痕：顶层配置顺序/分号拆分/名称链接）；测试每用例设计依据；占位翻译单元说明 |
| 4.8 | 文档同步 | 零偏差（§3.4/§12 RT-T01 行逐项对应）；findings 核对：F-010 前置自查（6 份日志入库实证）；F-004 AUTOMOC 空转（本目标同样存在——留构建所有者，累计第三处）；F-008 无涉（verify 目标从零构建成功） |
| 4.9 | commit 质量 | 两提交四段式＋Acc-Covered（1,2 / 2）；留痕提交 -f 说明 |
| 4.10 | 留痕完整 | 9 文件齐（日志 6＋XML×2＋report）；ird-test-report notes 四条（P-RT-3 清单/顶层顺序/分号坑/DLL 拷贝） |
| 4.11 | 偷懒扫描 | 零命中；占位 Runtime.cpp＝任务形态显式豁免（T01 落位任务，真实接口随 RT-T02+——非契约豁免但为任务行明文产物形态）；测试断言行为与边界（清单三分句＋冒烟空串双分支强断言，防恒真） |

## 问题清单

阻断级：无。建议级：无新增。
