# EV-T08 验收记录（attempt 1）

| 字段 | 值 |
| --- | --- |
| 任务 ID | EV-T08（实现 ResultCurrentness 当前性纯投影；≙WP-05-T08） |
| 送验对象（冻结） | 46c0c682dfa7013c92068ebd8486555dc5780e64（分支 wp05-t08 尖端） |
| diff 范围 | 82c669e62a99feb0460ec02a314d6a97673b3e73..46c0c682dfa7013c92068ebd8486555dc5780e64（23 个文件，+4205/−3） |
| 验收日期 | 2026-09-13 |
| 验收协议 | acceptance-protocol.md v1.5（ACC §4 全部 11 项） |
| **verdict** | **pass**（4.1～4.11 全部通过，零阻断级问题；建议级问题 4 条已转登 findings.json F-065～F-068） |
| 独立性声明 | 本验收为全新上下文会话，按 PIPE §0.4 派发模式执行（无需降级声明）：不与实施者共享会话记忆；未消费任何实施叙述/过程辩护文本，仅消费任务契约、设计文档链、送验提交内的产物与留痕；对抗式立场（为"不通过"找证据）；全部构建/测试/门禁/脚本由验收者亲手在独立 worktree 重跑，复现优先于采信（实施者留痕仅作比对基准，不作结论依据） |

## 0. 验收者环境说明

- 复现现场：`git worktree add --detach` 于送验 SHA 46c0c682（**按 SHA 不按分支名**——主仓工作树当时恰亦检出 wp05-t08 @46c0c682 且含编排者 state.json 未提交修改，验收全程未触碰）；记录回传：`git worktree add -b acc/EV-T08/1` 基于 origin/redesign-main（82c669e6）。
- worktree 集成配置按 F-062 先例补齐环境前置（AGENTS §4.1 字面命令在非主树不足）：`-DCMAKE_TOOLCHAIN_FILE=D:/10_Source_Repos/21_robot/RobWork/vcpkg/scripts/buildsystems/vcpkg.cmake`（绝对路径）＋`-DCMAKE_PREFIX_PATH=D:/software/Qt/6.11.1/msvc2022_64`＋从主树复制两件 git 未跟踪 ini 模板（RobWorkStudio.ini.template.static / .shared.in）到 worktree 的 RobWork/RobWorkStudio/bin/。参数取自主树 build/CMakeCache.txt 实际配置。
- 冒烟：另配独立临时目录，带 vcpkg toolchain 绝对路径，Release。
- ird_gates / validate-task / verify-task 在主仓库只读运行（gate 类脚本在 worktree 因缺 vcpkg 环境失败——F-009 既有口径）。
- 实施侧留痕的 ird-gates.log 存在中文双重编码乱码（详见 F-065）——不影响命中清单比对（3 处命中逐项可辨识）。

## 1. 逐项证据表（ACC §4，4.1～4.11）

### 4.1 改动范围 —— 通过

- `git diff --stat 82c669e..46c0c682`：23 个文件，逐一比对契约 allowedFiles：
  - `industrialrobot/evidence/**`：CMakeLists.txt、include/sdurws/ird/evidence/Currentness.hpp、src/Currentness.cpp、test/CurrentnessTest.cpp（4 个）✓
  - `doc/industrial-robot-design/traceability/**`：traceability/builds/wp05-t08/ 留痕 18 个 ✓
  - `doc/industrial-robot-design/units/evidence.md`：1 个 ✓
  - 合计 23，无 allowedFiles 之外文件。
- forbiddenFiles（REQUIREMENTS.md、ARCHITECTURE.md）：diff 零触碰 ✓。
- SA-02：`git diff --name-only <range> -- RobWork/RobWork/ RobWork/RobWorkSim/ RobWork/RobWorkStudio/src/`（排除 industrialrobot）零输出＝无未登记框架改动；patches/ 无新文件 ✓。

### 4.2 可复现构建 —— 通过

- 集成模式（worktree A，**全新树重新配置**）：
  - `cmake -S RobWork -B build -G "Visual Studio 17 2022" -A x64 -DRWS_BUILD_INDUSTRIALROBOT=ON`（＋F-062 前置）→ 配置成功；`grep RWS_BUILD_INDUSTRIALROBOT build/CMakeCache.txt` → `RWS_BUILD_INDUSTRIALROBOT:BOOL=ON` ✓
  - `cmake --build build --config Release --target sdurws_ird_evidence_test sdurws_ird_evidence_contract_test -j` → **exit 0 零 error**（仅 MSB8029 警告＝worktree 位于 TEMP 所致，与实现无关；"Automatic MOC"空转＝F-004 既有现象）。按 F-008 判例口径以任务目标面构建为准（送验留痕同口径如实声明）。
- 独立冒烟模式（独立临时目录＋toolchain 绝对路径）：配置成功；`cmake --build <dir> --config Release` 全量（core/evidence/policy/runtime/testkit 全部目标）→ **exit 0**（验收者首跑 exit 码取自管道尾端不严谨，已重跑增量构建复核 `SMOKE_REBUILD_EXIT=0`）；产出的 evidence_test/contract_test 可执行体实际运行通过（下条）。
- 测试真实执行（验收者亲手，双模式）：集成 evidence_test **151/151 PASSED**、contract_test **5/5 PASSED**；冒烟 evidence_test **151/151 PASSED**、contract_test **5/5 PASSED**。与送验留痕 XML（integration/smoke 各 151/0、5/0）及 ird-test-report.json 汇总 156/156 逐项一致。

### 4.3 契约 verify 命令 —— 通过

- 契约 verify 数组仅 1 条：`cmake --build build --config Release --target sdurws_ird_evidence_test`——验收者在 worktree A 逐字执行 → **exit 0**（增量重链 sdurws_ird_evidence_test.exe 成功）。
- 另以 `--target sdurws_ird_evidence_contract_test` 核验契约测试目标 → exit 0（送验留痕同口径）。
- 脚本化复核：主仓库 `pwsh -File RobWork/scripts/industrialrobot/verify-task.ps1 -TaskFile <EV-T08.json>` → **verify-task: PASS EV-T08**（exit 0）；`validate-task.ps1` → **PASS (1 tasks)**。

### 4.4 acceptance 条目逐项 —— 通过（ird_gates 已执行，非"未执行"口径）

| 契约 acceptance 条目 | 证据（具名用例，验收者实跑通过） |
| --- | --- |
| ① EV-CUR-1~4 内容身份/不可解析/跨上下文/不写回用例通过 | 内容身份＝CurrentOnIrrelevantHeadAdvance_EV_CUR_1（无关 HEAD 前进仍 Current，目标快照锚 rev2≠rev1 钉"修订号不参与"）＋SupersededOnConsumedObjectChange_EV_CUR_1（cvA→cvC，detail 含两版规范文本）＋NewSessionSameContentStaysCurrent_EV_CUR_1；不可解析＝UnresolvedDependencyNeverDefaultsToCurrent_EV_CUR_2（status==nullopt＋诊断非空、绝不 Current）＋UnresolvedListsAllMissingKeys_EV_CUR_2＋TargetRefreezeFailureIsUnevaluable_EV_CUR_2＋FactSourceEntryMismatchFailsFast_EV_CUR_2；跨上下文＝CrossProjectQueryIsUnevaluable_EV_CUR_4＋CrossBranchQueryIsUnevaluable_EV_CUR_4＋EvaluationKeyMismatchIsCrossContext_I_7；不写回＝SupersededDoesNotTouchArchivedEnvelope_EV_CUR_3＋CurrentnessLivesOnlyInProjectionIndex_EV_CUR_3 |
| ② 当前性纯投影、不写回（断言无持久化副作用） | 上行 EV-CUR_3 两用例：判定前后归档包络全字段等值（operator==＋payload->digest＋engineeringStatus）、判定本身不写索引（find 未命中）、store 为拷贝（副本变异不回染）——实现侧全入参 const 引用＋无 I/O（Currentness.cpp 实现纪律段，验收者通读确认） |
| ③ AT-05 载体：依赖提示驱动的重算判定（§4.2.4——修改求解预算后的复评不冒充原运行） | BudgetChangeSupersedesWithBaselineUnchanged_AT_05：仅改预算 → Superseded＋{solve.budget, ConfigurationChanged}＋detail 含旧→新配置身份；同快照双切片钉双层身份——sliceId 不等（重算→新运行）∧ inputBaselineId 相等（同一次冻结输入研究） |
| ird_gates | 验收者在主仓库亲手执行 `cmake --build build --config Release --target ird_gates`：命中 **3 项**，与送验留痕逐项一致——IRD-GATE-SUB（sdurws_ird_runtime_test→sdurws_ird_testkit）＝F-051 既有、IRD-GATE-SUB（testdata_lint→testkit 表外边）＝F-019 既有、IRD-GATE-T1（testdata_lint 链接 testkit）＝F-019 既有。**零新增命中**；evidence 本任务链接面仅 core（CMakeLists L66 `target_link_libraries(sdurws_ird_evidence PUBLIC sdurws_ird_core)`）。ird-test-report.json 的 irdGates 字段按 F-060 教训逐项标注归属，无笼统归并 ✓ |

### 4.5 测试真实失败能力（验证验证者）—— 通过

验收者采用**与实施者探针不同的注入点**（对抗式独立构造）：向 src/Currentness.cpp 步骤 1 上下文匹配条件注入 `false &&` 短路（PROBE 标记注释）→ 重编译 exit 0 → 过滤运行：**CrossProjectQueryIsUnevaluable_EV_CUR_4 与 CrossBranchQueryIsUnevaluable_EV_CUR_4 如期变红**（2 FAILED——status.has_value() Actual true / Expected false 等 4 处断言检出）→ 逐字还原（git status 零差异）→ touch 强制重编译（时间戳陷阱见 F-068）→ **151/151＋5/5 复绿**、PROBE 标记 grep 计数 0、工作树零残留。测试非恒真、非"只编译通过"。实施者自证探针（unresolved 分支置 Current→EV-CUR_2 两用例红，probe-red-run.log exit 1、probe-residue-check.txt 计数 0）比对一致。

### 4.6 架构红线（AGENTS §5 八条）—— 通过

1. **L2 零 Qt**：三个新文件 grep Qt/QtCore/QtWidgets 零命中 ✓（集成构建出现"Automatic MOC"＝F-004 既有全局传播，非本任务引入）。
2. **R-1 业务域互链**：库链接面仅 `sdurws_ird_core`（evidence/CMakeLists.txt L66）；测试目标仅 evidence＋GTest；无 modeling/kinematics/trajectory/dynamics/selection/optimization 依赖 ✓。
3. **R-2 跨单元私有头**：include 面仅 `sdurws/ird/core/*`（公共头）与本单元公共头 ✓。
4. **SA-02 框架零修改**：见 4.1 ✓。
5. **R-4 名称前缀**：makeCurrentnessDiag 显式置 localName/runtimeName 为 nullopt 并注释"名称反解归消费方经 runtime⑥端口——R-4，投影层不拼接名称" ✓。
6. **PA-1 权威唯一**：诊断码为建议值（kDiagCurrentness* 注释"码值权威归 diagnostics StableCodeRegistry，PA-1"）；写缓存是调用方显式动作（本类不替调用方计算）；不实现 project/policy/runtime 语义（依赖重建经 ICurrentnessFactSource 注入）✓。
7. **PA-2 不可变历史**：纯投影零写回（EV-CUR_3 断言面＋实现纪律）✓。
8. **不继承 old/**：全新文件，无 old/ 引用 ✓。

### 4.7 注释规范（AGENTS §2）—— 通过

- 公共头 **Currentness.hpp 全查**（543 行）：文件头含设计依据（§8.1/§4.2.4/§10.1⑥/§10.4＋CON-02/05＋契约 acceptance 1~3）、背景说明（为什么纯投影）、P-EV-4 处置、实现口径 I-1～I-8 逐条、线程安全声明；每个类型/函数 Doxygen 中文注释齐备，参数逐个 [in] 标注方向/语义/默认值，返回值与异常（@throws 含触发条件与错误语义归类：调用方违约 fail-fast vs 环境不可判定）完整。
- 实现文件抽查（≥3，实为全查）：**Currentness.cpp**（五步算法逐步注释——每步设计出处、失败去向、分支业务含义；全枚举 switch 无 default 的告警暴露策略注释；防御分支注明"理论上不可达"与暴露理由）；**CurrentnessTest.cpp**（文件头替身边界声明——EV-REG-3 同源；每个用例注释标注验证的需求/AT 编号与观测点）；**evidence/CMakeLists.txt**（新增行中文注释块，说明注册语义）。
- §2.5 高危信息表：所有权（targetSnapshot/facts/resultSlice 借用指针"调用方持有，本单元不接管所有权"）✓；线程约束（CurrentnessIndex 互斥量保护＋事件线程×查询线程；computeCurrentness 可重入纯函数；事实源并发安全性归实现方）✓；错误语义（fail-fast 契约违约 vs 不可判定环境形态两轨，注释＋异常文档双载体）✓；确定性来源（reasons 冻结序比对、诊断声明序、detail 同差异同文本——NFR-COR-02）✓；物理单位（本模块仅计数/版本/身份——无量纲，注释显式注明"无量纲"）✓；坐标系/角度制式（不适用——无几何量）。

### 4.8 文档同步与 findings 相关性 —— 通过

- 单元卡 units/evidence.md v0.9 变更记录行已按 DTB §5.4 登记实现落位（类型面/五步算法/I-1～I-8/CurrentnessIndex/诊断建议码/18 用例/探针/门禁基线比对），与实现逐项对照一致；卡头版本链 v0.8→v0.9 同步（F-057 纪律）✓。实现口径 I-2（descriptor 注册表 EV-T10 落地前值传递注入）、I-4（resultSlice 可选降级）、I-5（InvalidationKind 两处"…"保守扩展）、I-6（scheme 可选字符串记录面）、I-7（评估键并入上下文匹配）、I-8（判据分层——条目级对比为主判据的 §5.1/步骤 4 括号注记推导）均为**已登记的实现口径**，语义与设计一致，无未登记偏差。
- 契约状态保持 ready——按 PIPE §4.7 状态翻转归合入收尾（所有者），非实施者义务 ✓。追踪矩阵（evidence.md §14）CON-02/CON-05 行的 EV-CUR-* 验证落点已由本任务用例兑现，无需改矩阵。
- **findings.json 未关闭条目相关性核对**（逐条，结论）：
  - **F-019／F-051（门禁边红，相关）**：本任务实测复现 3 处命中＝F-019×2＋F-051×1，与 wp05-t07 基线逐项一致、零新增；属 testkit/runtime 侧既有债，本任务不可消账（涉他单元目标链接形态），维持 open。
  - **F-020（evidence.md §3.5 失引引用，相关）**：v0.9 未触碰 §3.5 引用面（仅卡头＋§15.4），仍 open——属文档治理任务（DOC 链）责任，不阻塞本任务。
  - **F-032（validate-task planned 锚点免检，弱相关）**：EV-T08 契约为 ready，validate-task PASS 已走强制锚点路径；本任务无涉，维持 open。
  - **F-043（RT-T06 重力投影测试敏感度，无关）**：runtime 单元用例面，与本任务零交集。
  - **F-046（Snapshot.cpp 缩进风格，弱相关）**：evidence 单元文件但属 EV-T03 交付面，本任务 diff 未触及 Snapshot.cpp，未顺手消账（避免任务范围越界），维持 open 归 evidence 后续任务。
  - **F-048（RT-T11 标题名词开头，无关）**：他任务提交风格问题；本任务三份提交标题均为动词开头（见 4.9），无再现。
  - **F-055（冒烟留痕未声明构建配置，相关——已规避）**：本任务留痕显式声明 Release（smoke-build-full.log 路径含 Release、ird-test-report.json notes 声明配置与树性质），验收者复现亦为 Release——该教训已吸收，finding 本身仍 open（归 RT-T13 原任务）。
  - **F-056（EVI-* 建议码超出 §13 枚举，相关——同族延续）**：EV-T08 新增两枚 EVI-* 建议码（EVI-CURRENTNESS-CROSS-CONTEXT／EVI-CURRENTNESS-UNRESOLVED-DEPENDENCY），已在单元卡 v0.9⑤ 显式登记（非静默漂移），归 diagnostics 所有者注册时裁决——已转登 F-066 跟踪。
  - **F-058（evidence.md 转义损毁，相关）**：v0.9 未修复（不在本任务允许语义内——属 DOC 治理），仍 open；非本任务验收阻断。
  - **F-059（EV-T03 标题风格，无关）**；**F-060（门禁归属标注教训，相关——已遵守）**：本任务 ird-test-report.json 逐项标注 F-051×1＋F-019×2，无笼统归并，教训已吸收。
  - **F-061（RT-T09 联合断言注释失配，无关）**；**F-063（Verdict.hpp 风格微瑕，弱相关）**：他任务交付面，本任务未触及对应文件。
  - **F-064（Envelope.hpp 冗余 include，弱相关）**：EV-T07 交付面，本任务未触及 Envelope.hpp。
  - **F-004／F-007／F-008／F-009／F-062（构建/环境前置族，相关）**：验收者在 worktree 复现时全部按既有先例绕过（MOC 空转＝F-004；toolchain/Qt 前缀/ini 模板＝F-007/F-062；全树构建以任务目标面为准＝F-008 判例；gates 回主仓＝F-009）——均为流程/框架债，实施产物无涉，维持 open。
  - **F-005／F-006（core/testkit 卡转义损毁，无关）**；**F-010（.gitignore 吞 *.log，相关——已按教训处置）**：本任务 .log 留痕经 `git add -f` 强制入库（提交 8712d029 声明，diff 实证 5 份 .log 在库），教训已吸收。
  - **F-012／F-023／F-024／F-025／F-026／F-027／F-028／F-029／F-030／F-031／F-033／F-034／F-035／F-036／F-037／F-038／F-039／F-040／F-041／F-042／F-044／F-045／F-047／F-061（他任务提交风格/计数/表述族，无关）**：本任务对照核查无同型再现——三份提交标题动词开头、计数准确（18 新增＝151−133 实证）、门禁未谎称"未执行"、构建强度表述按判例口径如实限定（"任务目标面增量构建 exit 0——F-008 目标未参与"）、全树/增量树性质如实声明（F-054/F-055 教训条款在 commit 正文出现）。
  - **F-015／F-016／F-017／F-018（TK-T03 testkit，无关）**。
- 结论：无"可随本任务顺手消账而未消账"的遗漏（F-046/F-063/F-064 属他任务交付面，顺手修改会扩大本任务 diff 范围）；本任务新增建议级问题 4 条见 §2。

### 4.9 commit 质量 —— 通过

- 660bd497「[WP-05-T08] 实现 evidence 当前性纯投影：computeCurrentness 与 CurrentnessIndex」、8712d029「[WP-05-T08] 登记双模式构建与测试证据…」、46c0c682「[WP-05-T08] 登记单元卡 v0.9 当前性纯投影落位…」——三份标题均为 `[WP-nn-Tkk]`＋动词开头中文摘要 ✓。
- 三份正文四段齐备（改动内容/改动原因/验证/影响面与兼容性），并带 Acc-Covered 尾注；代码＋测试＋CMake 同提交（660bd497），证据与文档各一提交，粒度符合 §6.3 ✓。
- 验证结论与留痕/复现逐项比对**无粉饰**：151/151＋5/5、门禁 3 既有命中、validate-task PASS、探针红/绿与残留零——验收者全部亲手复现一致；构建树性质与配置（增量树/独立冒烟树/Release）显式声明（F-054/F-055 教训条款）✓。

### 4.10 留痕完整 —— 通过

- gtest XML 4 份（integration/smoke × test/contract，tests=151/failures=0、tests=5 实测读出）＋ird-test-report.json（156/156 汇总＋逐用例 modes/trace 追溯＋failureCapabilitySelfCheck＋notExecuted:[]）＋构建日志（integration-build-evidence.log INTEG_BUILD_EXIT=0、smoke-build-full.log SMOKE_BUILD_EXIT=0、integration-cache-check.txt =ON、探针三日志＋残留检查）全部随送验提交在库（F-010 教训 `git add -f`）✓。
- resultNote：phase-one-validation.log 末次更新停在 CORE-T01（2026-09-10）——近期任务既定实践改为单元卡 §15.4 变更记录行承载（ACC 4.10"或 resultNote 已更新"的"或"支），本任务 v0.9 行已更新，判定满足 ✓。

### 4.11 偷懒/缩水实现扫描 —— 通过

- ①diff 范围 `TODO|FIXME|stub|placeholder|未实现|not implemented` 扫描（industrialrobot 面）**零命中**；无"触发式延后"豁免需要列出 ✓。
- ②空实现检测：公共接口全部有实现体——computeCurrentness 五步算法完整、CurrentnessIndex 四方法完整、fromEnvelope 投影完整；无"仅返回默认值"的空壳 ✓。
- ③acceptance 逐条证据表：见 4.4 表（每条具名用例＋验收者实跑结论），无一条缺证据 ✓。
- ④测试强度：18 用例断言校验行为与边界（presence 纪律——Current 恒空 reasons/诊断仅不可判定非空；输出确定性——全量列出、声明序、detail 文本；拷贝语义；fail-fast 错误码逐一断言；双层身份 sliceId 不等∧inputBaseline 相等的排除面断言），非恒真——4.5 探针实证会红 ✓。

## 2. 问题清单

### 阻断级

无。

### 建议级（已全部转登 traceability/findings.json，编号顺延自 F-064）

| 编号 | 摘要 | 责任方 |
| --- | --- | --- |
| F-065 | traceability/builds/ 留痕日志中文双重编码乱码（ird-gates.log 自 wp05-t06 起系统性；证据效力不受损，人读可追溯性受损） | 治理脚本/流程文档维护者 |
| F-066 | EV-T08 两枚新 EVI-* 建议诊断码待 diagnostics 所有者注册裁决（F-056 同族延续；已在卡 v0.9⑤ 登记，非静默漂移） | diagnostics 所有者 |
| F-067 | CurrentnessResult::reasons 头文件注释"按 (kind,key) 字典序输出"与 diffEntries 两段拼接实现不完全一致（确定性不受影响；.cpp 注释准确） | evidence 实现所有者 |
| F-068 | ACC §4.5 探针恢复后的增量构建时间戳陷阱（mv 还原保旧 mtime→MSBuild 不重编译→探针二进制残留假红）；建议协议补"恢复后强制重编译"口径 | acceptance-protocol 维护者 |

## 3. 裁决

**verdict＝pass**：4.1～4.11 全部通过、无阻断级问题；4 条建议级问题已转登 findings.json 跟踪，不阻塞合入。按 ACC §6，合入决策归所有者；本验收者未合入、未修改实施产物、未向任务分支推送任何提交（送验对象冻结于 46c0c682）。
