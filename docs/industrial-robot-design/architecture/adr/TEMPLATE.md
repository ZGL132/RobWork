# ADR-NNN：<决策标题>

- 状态：`Proposed`
- 日期：YYYY-MM-DD
- 决策所有者：<WP/角色>
- 必需评审者：<消费者、架构、测试>
- 关联契约：`CTR-...`
- 需求：`REQ-ID`
- 受影响工作包：`WP-XX`

## 背景

说明必须做出决策的约束、现状、失败风险和不可延后的原因。

## 决策

给出唯一可执行的决策、适用范围、不变量和禁止行为。

## 备选方案

| 方案 | 结论 | 原因 |
| --- | --- | --- |
| <方案> | 采用/拒绝 | <取舍> |

## 影响

分别记录正向影响、成本、兼容性、风险和后续约束。

## 迁移与回滚

说明旧实现处置、迁移门禁、数据变化、失败回滚和 `Superseded` 条件。

## 验证证据

列出契约测试、黄金数据、静态扫描、阶段门禁、证据工件和评审记录。

## 接受条件

列出从 `Proposed` 转为 `Accepted` 必须全部满足的可验证条件。
