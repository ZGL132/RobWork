# WP-XX 工作包实施计划模板

**治理状态：** Planned；D0 基线：`IRD-D0-20260829`；所有者：<唯一责任人>；评审者：<独立评审人>。

**目标：** 一个可验证的交付目标。

**阶段/发布：** 阶段 X，R1/R2。

**需求与契约：** 需求 ID；引用 `architecture/<contract>.md`。

**拥有目录：** 唯一允许修改目录；明确禁止依赖和公共接口所有者。

**输入/输出：** 输入工件、输出工件、状态和诊断。

**前置门禁：** 依赖 WP 状态；引用契约状态；代码基线提交；环境与黄金数据可用性。

## 任务

每个任务必须有对应 `agent-tasks/WP-XX-TYY-*.md`，并包含失败测试、正常测试、验证命令、证据和停止条件。

## 验证

列出前置门禁、精确命令和环境前置。

## 迁移与删除

列出旧目标保留、迁移、删除和兼容规则。

## 独立评审

列出验证者、评审者和必须审阅的证据。

## 退出条件

列出需求、测试、证据和发布门禁。

仅当需求与契约已冻结、依赖已满足、子任务均具备可执行验证命令、证据责任人明确时，工作包才可从 Planned 转为 Ready。
