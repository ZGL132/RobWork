# 模块详细方案：<模块名>

方案版本：v0.1；对应需求基线：v0.8；负责 WP：WP-XX；阶段/发布：阶段 X / R1 或 R2；架构契约：`architecture/<file>.md`；任务卡：`agent-tasks/WP-XX-TYY-*.md`。

治理状态：Draft；文档所有者：<模块/WP 所有者>；评审者：<架构/测试负责人>；D0 基线：`IRD-D0-20260829`；最近评审：未评审。

## 1. 目标与非目标

目标是明确本模块可交付的行为和实现边界；非目标必须列出留给其他模块或后续阶段的内容。

## 2. 代码与构建

拥有目录、公共头、测试目录、CMake target、允许依赖和禁止依赖必须逐项列出。

## 3. 数据与接口

输入输出必须列出字段、类型、单位、可空性、来源、版本、状态和证据；跨线程只传不可变值。

## 4. 调用与状态

描述输入校验、规范化、核心计算、适配、结果接纳的顺序，以及失败、取消、恢复和迟到回调。

## 5. 算法与序列化

冻结坐标系、单位、公式、数值容差、确定性规则、JSON/XML 示例和未知字段策略。

## 6. 测试与证据

列出 Given/When/Then 失败、正常、边界、契约、数值、GUI、性能和安全测试，精确到命令和输出工件。

从 Draft 转为 Ready 前，必须确认引用的架构契约已 Accepted、依赖与目录边界无冲突、任务卡与验证命令可执行、证据路径可写入且评审者明确。

## 7. 迁移与扩展

说明旧实现迁移、删除时机、兼容窗口、回滚方法、新适配器和必须新增的 ADR/测试。
